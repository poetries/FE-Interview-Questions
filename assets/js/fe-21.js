(window.webpackJsonp=window.webpackJsonp||[]).push([[21],{1128:function(n){n.exports=JSON.parse('{"html":"<p>栈与队列相关的问题就比较微妙了，很多时候相关题目中压根不会出现“栈”、“队列”这样的关键字，但只要你深入到真题里去、对栈和队列的应用场景建立起正确的感知，那么很多线索都会在分析的过程中被你轻松地挖掘出来。</p>\\n<p>这里也和大家分享一位读者在试读过程中的学习感悟：</p>\\n<p>感觉算法题除了理解还要靠练习，就像高考数学题，要锻炼出解题常规思维。任重道远啊🙊</p>\\n<p>其实就是这么回事，这也正是我们开篇就跟大家指明“以题为纲”这条路的初衷。</p>\\n<p>好啦，开工了老哥们！</p>\\n<h2 id=\\"典型真题快速上手-有效括号-问题\\"><a class=\\"header-anchor\\" href=\\"#典型真题快速上手-有效括号-问题\\">#</a> 典型真题快速上手-“有效括号”问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>遇左括号就把“期待的右括号”入栈，遇右括号与栈顶比较。</li>\\n<li>右括号出现时栈为空、类型不匹配，都立即返回 <code>false</code>。</li>\\n<li>扫描结束还要检查栈为空，否则 <code>((</code> 会被误判。</li>\\n<li>每个字符至多进出栈一次：时间 <code>O(n)</code>，最坏空间 <code>O(n)</code>。</li>\\n</ul>\\n<p><strong>遇到左括号就把对应的右括号压栈，遇到右括号时只和栈顶比较。</strong> 括号嵌套要求最后打开的括号最先闭合，本质上正好符合栈的后进先出。若右括号出现时栈为空，或者弹出的字符与它不一致，就可以立即返回 <code>false</code>。扫描结束后还要确认栈为空，否则像 <code>((</code> 这种未闭合输入会被误判；整体时间和最坏空间都是 <code>O(n)</code>。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“括号嵌套要求最后打开的括号最先关闭，正好符合 LIFO。我把期望的闭括号压栈，右括号只需和栈顶比较。”</p>\\n</blockquote>\\n<blockquote>\\n<p>题目描述：给定一个只包括 \'(\'，\')\'，\'{\'，\'}\'，\'[\'，\']\' 的字符串，判断字符串是否有效。</p>\\n</blockquote>\\n<p>有效字符串需满足：</p>\\n<ul>\\n<li>左括号必须用相同类型的右括号闭合。</li>\\n<li>左括号必须以正确的顺序闭合。</li>\\n<li>注意空字符串可被认为是有效字符串。</li>\\n</ul>\\n<p>示例 1:</p>\\n<ul>\\n<li>输入: &quot;()&quot;</li>\\n<li>输出: true</li>\\n</ul>\\n<p>示例 2:</p>\\n<ul>\\n<li>输入: &quot;()[]{}&quot;</li>\\n<li>输出: true</li>\\n</ul>\\n<p>示例 3:</p>\\n<ul>\\n<li>输入: &quot;(]&quot;</li>\\n<li>输出: false</li>\\n</ul>\\n<p>示例 4:</p>\\n<ul>\\n<li>输入: &quot;([)]&quot;</li>\\n<li>输出: false</li>\\n</ul>\\n<p>示例 5:</p>\\n<ul>\\n<li>输入: &quot;{[]}&quot;</li>\\n<li>输出: true</li>\\n</ul>\\n<p><strong>思路分析</strong></p>\\n<p>括号问题在面试中出现频率非常高， 这类题目我们一般首选用栈来做。</p>\\n<p>为什么可以用栈做？大家想想，括号成立意味着什么？意味着对称性。</p>\\n<p>巧了，根据栈的后进先出原则，一组数据的入栈和出栈顺序刚好是对称的。比如说1、2、3、4、5、6按顺序入栈，其对应的出栈序列就是 6、5、4、3、2、1：</p>\\n<pre class=\\"language-text\\"><code>123456\\n654321\\n</code></pre>\\n<p>对称关系一目了然。</p>\\n<p>因此这里大家可以记下一个规律：题目中若涉及括号问题，则很有可能和栈相关。</p>\\n<p>回到题目中来，我们的思路就是在遍历字符串的过程中，往栈里 push 括号对应的配对字符。比如如果遍历到了 (，就往栈里 push )。</p>\\n<blockquote>\\n<p>假如字符串中所有的括号都成立，那么前期我们 push 进去的一定全都是左括号、后期 push 进去的一定全都是右括号。而且左括号的入栈顺序，和其对应的右括号的入栈顺序应该是相反的，比如这个例子：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>({[]})\\n</code></pre>\\n<blockquote>\\n<p>最后一个入栈的左方括号[，与之匹配的右方括号]正是接下来第一个入栈的右括号。</p>\\n</blockquote>\\n<p>因此，我们可以果断地认为在左括号全部入栈结束时，栈顶的那个左括号，就是第一个需要被配对的左括号。此时我们需要判断的是接下来入栈的第一个右括号是否和此时栈顶的左括号配对。如果配对成功，那么这一对括号就是有效的，否则直接 return false。</p>\\n<p>当判断出一对有效的括号之后，我们需要及时地丢掉它，去判断其它括号是否有效。这里这个“丢掉”的动作，就对应着两个括号一起出栈的过程。</p>\\n<p>每配对成功一对括号，我们都将这对括号出栈。这样一来，我们就可以确保栈顶的括号总是下一个需要被匹配的左括号。</p>\\n<p>如果说我们出栈到最后，栈不为空，那么意味着一部分没有被匹配上的括号被剩下来了，说明字符串中并非所有的括号都有效，判断 false；反之，则说明所有的括号都配对成功了，判断为 true。</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>// 用一个 map 来维护左括号和右括号的对应关系\\nconst leftToRight = {\\n  &quot;(&quot;: &quot;)&quot;,\\n  &quot;[&quot;: &quot;]&quot;,\\n  &quot;{&quot;: &quot;}&quot;\\n};\\n\\n/**\\n * @param {string} s\\n * @return {boolean}\\n */\\nconst isValid = function(s) {\\n  // 结合题意，空字符串无条件判断为 true\\n  if (!s) {\\n    return true;\\n  }\\n  // 初始化 stack 数组\\n  const stack = [];\\n  // 缓存字符串长度\\n  const len = s.length;\\n  // 遍历字符串\\n  for (let i = 0; i &lt; len; i++) {\\n    // 缓存单个字符\\n    const ch = s[i];\\n    // 判断是否是左括号，这里我为了实现加速，没有用数组的 includes 方法，直接手写判断逻辑\\n    if (ch === &quot;(&quot; || ch === &quot;{&quot; || ch === &quot;[&quot;) stack.push(leftToRight[ch]);\\n    // 若不是左括号，则必须是和栈顶的左括号相配对的右括号\\n    else {\\n      // 若栈不为空，且栈顶的左括号没有和当前字符匹配上，那么判为无效\\n      if (!stack.length || stack.pop() !== ch) {\\n        return false;\\n      }\\n    }\\n  }\\n  // 若所有的括号都能配对成功，那么最后栈应该是空的\\n  return !stack.length;\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>模板编辑页输入 <code>([)]</code>，有人只统计三类左右括号数量相等就判为合法，你会用哪个代码现象指出漏洞？</strong></p>\\n<p>数量相等只能证明括号没有缺失，无法证明闭合顺序正确，<code>([)]</code> 正是反例。扫描到 <code>)</code> 时，栈顶期望的是 <code>]</code>，应立即返回 <code>false</code>；校验必须同时覆盖类型匹配和后进先出的嵌套关系。</p>\\n</li>\\n<li>\\n<p><strong>编辑器需要在用户输入时标出首个错误位置，现有 <code>boolean</code> 返回值不够用，栈里应保存什么？</strong></p>\\n<p>栈中可保存期望的右括号以及对应左括号下标，遇到不匹配的右括号就返回当前下标。扫描结束仍有元素时，再报告未闭合左括号的位置；错误定位语义需明确，否则最早打开与最晚打开的位置可能产生冲突。</p>\\n</li>\\n<li>\\n<p><strong>校验接口原本只接收六种括号，现在产品把 <code>if (a[0]) {}</code> 整段代码传入，普通字符应该怎样处理？</strong></p>\\n<p>先固定输入契约：纯括号题中，任何其他字符都应视为非法；表达式场景则可跳过普通字符，只处理六种括号。两种语义不能隐式混用，否则同一输入会在不同调用方得到不一致结果，并掩盖上游数据错误。</p>\\n</li>\\n<li>\\n<p><strong>线上校验偶发把单个 <code>]</code> 判为有效，而 <code>{[]</code> 也能通过，你会检查哪两个分支？</strong></p>\\n<p>先检查遇到右括号时是否在空栈上仍执行或忽略了 <code>pop</code>，单个 <code>]</code> 应因没有期望项立即失败。再检查遍历结束是否返回 <code>stack.length === 0</code>，否则剩余左括号不会被发现；这两个边界分别覆盖多余闭合和缺少闭合。</p>\\n</li>\\n<li>\\n<p><strong>日志服务按数据块接收超长文本，不能等完整字符串到齐再校验，栈方案还能保留吗？</strong></p>\\n<p>可以逐块扫描，并在块之间保留尚未匹配的期望括号栈，匹配逻辑不依赖完整字符串。只有收到流结束信号后才能确认栈是否为空；若输入最坏全是左括号，未决状态仍会增长到 <code>O(n)</code>，无法靠分块消除。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"栈问题进阶-每日温度问题\\"><a class=\\"header-anchor\\" href=\\"#栈问题进阶-每日温度问题\\">#</a> 栈问题进阶-每日温度问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>栈里存“还没找到右侧更大值”的下标，不存温度值。</li>\\n<li>对应温度从栈底到栈顶单调不增；新温度更高时连续弹栈并结算距离。</li>\\n<li>相等温度不能弹出，因为题目要“严格更高”。</li>\\n<li>每个下标最多进出栈一次，时间 <code>O(n)</code>、空间 <code>O(n)</code>。</li>\\n</ul>\\n<p><strong>这道题用单调栈保存尚未找到右侧更高温度的下标，新温度更高时连续弹栈并计算等待天数。</strong> 栈中存下标而不是温度，是因为结果需要填写当前下标与目标下标的距离。对应温度从栈底到栈顶保持单调不增，相等温度不能弹出，因为题目要求的是严格升高。每个下标最多入栈、出栈各一次，所以时间复杂度为 <code>O(n)</code>，最坏空间复杂度为 <code>O(n)</code>。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“这是 next greater element。我维护未结算下标的单调递减栈，新元素一旦更大，就成为被弹下标右侧第一个更大值。”</p>\\n</blockquote>\\n<blockquote>\\n<p>题目描述: 根据每日气温列表，请重新生成一个列表，对应位置的输出是需要再等待多久温度才会升高超过该日的天数。如果之后都不会升高，请在该位置用 0 来代替。</p>\\n</blockquote>\\n<p>例如，给定一个列表 temperatures = [73, 74, 75, 71, 69, 72, 76, 73]，你的输出应该是 [1, 1, 4, 2, 1, 1, 0, 0]。</p>\\n<p>提示：气温 列表长度的范围是 [1, 30000]。每个气温的值的均为华氏度，都是在 [30, 100] 范围内的整数。</p>\\n<p><strong>思路分析</strong></p>\\n<p>看到这道题，大家不难想到暴力遍历法：直接两层遍历，第一层定位一个温度，第二层定位离这个温度最近的一次升温是哪天，然后求出两个温度对应索引的差值即可。</p>\\n<p>一个数组两层遍历，属于比较少见且高危的操作。事出反常必有妖，此时我们就需要反思：这道题是不是压根不该用暴力遍历来做？</p>\\n<p>答案是肯定的。因为在这个暴力遍历的过程中，我们其实做了很多“多余”的事情。</p>\\n<p>拿第三个索引位上这个 75 来说，我们在定位比 75 高的第一个温度的过程中，就路过了 71、69、72 这三个温度，其中，72 正是 71 对应的目标温度，可我们却像没看见它一样、啥也没干。只有等最外层遍历走到 71 时，我们才又重复了一遍刚刚走过的路、确认了 71 和 72 之间的关系——像这种不必要的重复，我们要想办法把它干掉。</p>\\n<blockquote>\\n<p>栈结构可以帮我们避免重复操作</p>\\n</blockquote>\\n<p>避免重复操作的秘诀就是及时地将不必要的数据出栈，避免它对我们后续的遍历产生干扰。</p>\\n<p>拿这道题来说，我们的思路就是：尝试去维持一个递减栈。</p>\\n<p>当遍历过的温度，维持的是一个单调递减的态势时，我们就对这些温度的索引下标执行入栈操作；只要出现了一个数字，它打破了这种单调递减的趋势，也就是说它比前一个温度值高，这时我们就对前后两个温度的索引下标求差，得出前一个温度距离第一次升温的目标差值。这么说可能有点抽象，我们用一张动图来理解一下这个过程</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122755.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>在这个过程中，我们仅对每一个温度执行最多一次入栈操作、一次出栈操作，整个数组只会被遍历一次，因此时间复杂度就是O(n)。相对于两次遍历带来的 O(n^2)的开销来看，栈结构真是帮了咱们大忙了。</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {number[]} T\\n * @return {number[]}\\n */\\n// 入参是温度数组\\nconst dailyTemperatures = function(T) {\\n    const len = T.length // 缓存数组的长度 \\n    const stack = [] // 初始化一个栈   \\n    const res = (new Array(len)).fill(0) //  初始化结果数组，注意数组定长，占位为0\\n    for(let i=0;i&lt;len;i++) {\\n      // 若栈不为0，且存在打破递减趋势的温度值\\n      while(stack.length &amp;&amp; T[i] &gt; T[stack[stack.length-1]]) {\\n        // 将栈顶温度值对应的索引出栈\\n        const top = stack.pop()  \\n        // 计算 当前栈顶温度值与第一个高于它的温度值 的索引差值\\n        res[top] = i - top \\n      }\\n      // 注意栈里存的不是温度值，而是索引值，这是为了后面方便计算\\n      stack.push(i)\\n    }\\n    // 返回结果数组\\n    return res \\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>气温页输入 <code>[73,74,75,71,69,72,76,73]</code>，双层循环结果正确但数据接近 <code>30000</code> 项时卡顿，单调栈消除了哪些重复扫描？</strong></p>\\n<p>单调栈保存尚未找到更高温度的下标，新温度升高时一次结算所有比它低的栈顶项。每个下标最多入栈和出栈一次，时间复杂度降为 <code>O(n)</code>；栈中必须保存下标，否则无法计算等待天数。</p>\\n</li>\\n<li>\\n<p><strong>代码评审有人只用一次 <code>if</code> 弹出栈顶，输入 <code>[73,72,71,74]</code> 时前两天结果错误，为什么必须用 <code>while</code>？</strong></p>\\n<p>温度 <code>74</code> 不只解决最近的 <code>71</code>，还同时是 <code>72</code> 和 <code>73</code> 右侧第一个更高温度。必须持续弹出所有低于当前温度的栈顶下标并分别计算距离；只弹一次会把仍可结算的旧元素错误地留在栈中。</p>\\n</li>\\n<li>\\n<p><strong>需求把“首次升温”改成右侧第一个温度“大于等于当天”，输入 <code>[70,70]</code> 的期望变为 <code>[1,0]</code>，弹栈条件怎么改？</strong></p>\\n<p>弹栈条件应从当前温度严格大于栈顶温度的 <code>&gt;</code> 改为 <code>&gt;=</code>，这样相等值也会完成结算。条件变化会改变栈内相等元素的保留方式，必须补充连续相等温度用例，避免仍沿用原题的严格升温语义。</p>\\n</li>\\n<li>\\n<p><strong>线上结果里最后几天出现 <code>undefined</code>，而题目要求之后不升温时返回 <code>0</code>，你会先检查结果数组和收尾逻辑吗？</strong></p>\\n<p>先确认结果数组是否按输入长度创建并用 <code>0</code> 填充，未被弹出的下标应自然保留为 <code>0</code>。无需在结束时继续弹栈计算距离，因为它们没有右侧更高温度；若只在出栈时写结果，未结算位置就可能保持 <code>undefined</code>。</p>\\n</li>\\n<li>\\n<p><strong>气象数据每天流式到达，页面希望尽早推送已确定答案，单调栈能输出哪些项，哪些项必须等待？</strong></p>\\n<p>每个新温度到达时，可立即输出所有被它弹出的下标及等待天数，因为它就是这些位置右侧首个更高值。仍留在栈中的日期尚不能确定答案，只能继续保留；流结束后才能把剩余项统一结算为 <code>0</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"栈的设计-最小栈-问题\\"><a class=\\"header-anchor\\" href=\\"#栈的设计-最小栈-问题\\">#</a> 栈的设计——“最小栈”问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>主栈保存全部值，辅助栈保存“截至当前位置的最小值”。</li>\\n<li><code>push(x)</code> 时辅助栈压入 <code>min(x, currentMin)</code>，两栈等长，重复最小值也不会丢。</li>\\n<li><code>pop</code> 两栈同步弹出，<code>getMin</code> 直接读辅助栈顶。</li>\\n<li>四个操作都是 <code>O(1)</code>，代价是 <code>O(n)</code> 额外空间。</li>\\n</ul>\\n<p><strong>我会用一个主栈保存全部元素，再用辅助栈记录每一层对应的最小值。</strong> 执行 <code>push(x)</code> 时，辅助栈压入 <code>x</code> 和当前最小值中更小的那个，因此两栈始终等长，重复的最小值也能正确保留。<code>pop()</code> 时同步弹出两个栈，<code>getMin()</code> 直接读取辅助栈顶，这样四个操作都是 <code>O(1)</code>，代价是额外使用 <code>O(n)</code> 空间。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“我用等长最小值栈保存每一层的历史最小值，弹栈会自动恢复上一层最小值。”</p>\\n</blockquote>\\n<blockquote>\\n<p>题目描述：设计一个支持 push ，pop ，top 操作，并能在常数时间内检索到最小元素的栈。</p>\\n</blockquote>\\n<blockquote>\\n<p>push(x) —— 将元素 x 推入栈中。</p>\\n</blockquote>\\n<ul>\\n<li><code>pop()</code>—— 删除栈顶的元素。</li>\\n<li><code>top()</code> —— 获取栈顶元素。</li>\\n<li><code>getMin()</code> —— 检索栈中的最小元素。</li>\\n</ul>\\n<p>示例:</p>\\n<ul>\\n<li>MinStack minStack = new MinStack();</li>\\n<li>minStack.push(-2);</li>\\n<li>minStack.push(0);</li>\\n<li>minStack.push(-3);</li>\\n<li>minStack.getMin(); --&gt; 返回 -3.</li>\\n<li>minStack.pop();</li>\\n<li>minStack.top(); --&gt; 返回 0.</li>\\n<li>minStack.getMin(); --&gt; 返回 -2.</li>\\n</ul>\\n<p><strong>思路分析</strong></p>\\n<p>这道题并不难，但是综合性很强，整个题做下来能够相对全面地考察到候选人对栈结构、栈操作的理解和掌握，是不少一面/少数二面面试官的心头好。</p>\\n<blockquote>\\n<p>其中前三个操作：push、pop 和 top，我们在数据结构快速上手环节已经给大家讲过了，这里不多赘述。需要展开讲的是 getMin 这个接口，这个接口有时候会直接单独拎出来作为一道题来考察，需要大家对它的实现思路有一个真正扎实的掌握。</p>\\n</blockquote>\\n<p>getMin 要做的事情，是从一个栈里找出其中最小的数字。我们仍然是抛砖引玉，先说一个大部分人都能想到的思路：</p>\\n<p>初始化一个最小值变量，它的初始值可以设一个非常大的数（比如 Infinity），然后开始遍历整个栈。在遍历的过程中，如果遇到了更小的值，就把最小值变量更新为这个更小的值。这样遍历结束后，我们就能拿到栈中的最小值了。</p>\\n<p>这个过程中，我们对栈进行了一次遍历，时间复杂度无疑是 O(n)。</p>\\n<p>按照这个思路，整个栈的设计我们可以这样写：</p>\\n<p>编码实现1</p>\\n<pre class=\\"language-js\\"><code>/**\\n * 初始化你的栈结构\\n */\\nconst MinStack = function() {\\n  this.stack = []\\n};\\n\\n/** \\n * @param {number} x\\n * @return {void}\\n */\\n// 栈的入栈操作，其实就是数组的 push 方法\\nMinStack.prototype.push = function(x) {\\n  this.stack.push(x)\\n};\\n\\n/**\\n * @return {void}\\n */\\n// 栈的入栈操作，其实就是数组的 pop 方法\\nMinStack.prototype.pop = function() {\\n  this.stack.pop()\\n};\\n\\n/**\\n * @return {number}\\n */\\n// 取栈顶元素，咱们教过的哈，这里我本能地给它一个边界条件判断（其实不给也能通过，但是多做不错哈）\\nMinStack.prototype.top = function() {\\n  if(!this.stack || !this.stack.length) {\\n      return \\n  }\\n  return this.stack[this.stack.length - 1]\\n};\\n\\n/**\\n * @return {number}\\n */\\n// 按照一次遍历的思路取最小值\\nMinStack.prototype.getMin = function() {\\n    let minValue = Infinity  \\n    const  { stack } = this\\n    for(let i=0; i&lt;stack.length;i++) {\\n        if(stack[i] &lt; minValue) {\\n            minValue = stack[i]\\n        }\\n    }\\n    return minValue\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>订单价格栈连续执行 <code>push(2)</code>、<code>push(1)</code>、<code>push(1)</code>，再执行一次 <code>pop()</code> 后，页面上的最低价变成了 <code>2</code>；你会检查哪段判断？</strong></p>\\n<p>应检查辅助最小栈是否只在 <code>x &lt; currentMin</code> 时入栈，以及弹出时是否错误移除了唯一的 <code>1</code>。重复最小值必须逐层保存，或为最小值维护计数；否则删除一个副本就会丢失仍在主栈中的最小值。</p>\\n</li>\\n<li>\\n<p><strong>监控面板每秒执行大量 <code>getMin()</code>，现有实现每次都遍历整个数组；在不能改变四个接口语义的前提下，你会怎样改？</strong></p>\\n<p>应增加与主栈等长的最小值栈，每次 <code>push(x)</code> 时压入 <code>min(x, previousMin)</code>，<code>pop()</code> 时同步弹出。这样四个操作都只访问栈顶，时间复杂度为 <code>O(1)</code>，代价是为每一层额外保存一个历史最小值。</p>\\n</li>\\n<li>\\n<p><strong>内存预算要求不能维护第二个数组，但仍要求 <code>push</code>、<code>pop</code>、<code>top</code>、<code>getMin</code> 都是常数时间，你会怎样调整栈项结构？</strong></p>\\n<p>可把每个栈项改为 <code>{ value, min }</code>，其中 <code>min</code> 记录压入该层后整个栈的最小值。它只使用一个容器，却仍为每层保存了历史最小值，因此空间复杂度并未从 <code>O(n)</code> 降为 <code>O(1)</code>。</p>\\n</li>\\n<li>\\n<p><strong>线上日志显示主栈还有 37 个元素，但最小值栈已经为空，随后 <code>getMin()</code> 返回 <code>undefined</code>；你会怎样定位状态从何时失配？</strong></p>\\n<p>应围绕每次 <code>push</code> 和 <code>pop</code> 记录两栈长度及栈顶，并验证操作结束后长度始终一致。常见原因是最小值栈采用非等长设计却无条件弹出，或某条异常分支只更新了一个栈；修复时要把两者更新封装为同一原子操作。</p>\\n</li>\\n<li>\\n<p><strong>组件负责人主张用遍历实现以节省辅助空间，接口负责人坚持 <code>getMin()</code> 必须稳定常数时间；你如何做选型？</strong></p>\\n<p>既然接口明确要求常数时间检索最小值，就不能采用每次遍历主栈的 <code>O(n)</code> 实现。应选择等长最小值栈或携带 <code>{ value, min }</code> 的栈项；两者时间复杂度相同，差异主要在状态分离方式和调试便利性。</p>\\n</li>\\n</ul>\\n</section>\\n<p>这样写，用例也能跑通，但是不够酷。如果你在面试时这样做了，面试官有99%的可能性会追问你这句：</p>\\n<p>“这道题有没有时间效率更高的做法？”</p>\\n<p>人家都这样问了，咱当然要说“有”。然后，面试官就会搬个小板凳，坐你旁边看你如何妙手回春，变 O(n) 为 O(1)。</p>\\n<p>时间效率的提升，从来都不是白嫖，它意味着我们要付出更多的空间占用作为代价。在这道题里，如果继续沿着栈的思路往下走，我们可以考虑再搞个栈（stack2）出来作为辅助，让这个栈去容纳当前的最小值。</p>\\n<p>如何确保 stack2 能够确切地给我们提供最小值？ 这里我们需要实现的是一个从栈底到栈顶呈递减趋势的栈（敲黑板！递减栈出现第二次了哈）：</p>\\n<p>取最小值：由于整个栈从栈底到栈顶递减，因此栈顶元素就是最小元素。</p>\\n<p>若有新元素入栈：判断是不是比栈顶元素还要小，否则不准进入 stack2。</p>\\n<p>若有元素出栈：判断是不是和栈顶元素相等，如果是的话，stack2 也要出栈。</p>\\n<p>按照这个思路，我们可以有以下编码：</p>\\n<p>编码实现2</p>\\n<pre class=\\"language-js\\"><code>const MinStack = function() {\\n    this.stack = [];\\n    // 定义辅助栈\\n    this.stack2 = [];\\n};\\n\\n/** \\n * @param {number} x\\n * @return {void}\\n */\\nMinStack.prototype.push = function(x) {\\n    this.stack.push(x);\\n    // 若入栈的值小于当前最小值，则推入辅助栈栈顶\\n    if(this.stack2.length == 0 || this.stack2[this.stack2.length-1] &gt;= x){\\n        this.stack2.push(x);\\n    }\\n};\\n\\n/**\\n * @return {void}\\n */\\nMinStack.prototype.pop = function() {\\n    // 若出栈的值和当前最小值相等，那么辅助栈也要对栈顶元素进行出栈，确保最小值的有效性\\n    if(this.stack.pop() == this.stack2[this.stack2.length-1]){\\n        this.stack2.pop();\\n    }\\n};\\n\\n/**\\n * @return {number}\\n */\\nMinStack.prototype.top = function() {\\n    return this.stack[this.stack.length-1];\\n};\\n\\n/**\\n * @return {number}\\n */\\nMinStack.prototype.getMin = function() {\\n    // 辅助栈的栈顶，存的就是目标中的最小值\\n    return this.stack2[this.stack2.length-1];\\n};\\n</code></pre>\\n"}')}}]);