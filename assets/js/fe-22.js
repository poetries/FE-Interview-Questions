(window.webpackJsonp=window.webpackJsonp||[]).push([[22],{1129:function(n){n.exports=JSON.parse('{"html":"<p>结束了针对栈结构的定点轰炸，我们现在开始要缓缓过渡到队列的世界了。</p>\\n<p>关于队列，在算法面试中大家需要掌握以下重点：</p>\\n<ul>\\n<li>栈向队列的转化</li>\\n<li>双端队列</li>\\n<li>优先队列</li>\\n</ul>\\n<blockquote>\\n<p>以上考点中，1 属于基础难度， 2 对一部分同学来说已经有点吃力，3 的区分度最高——优先队列属于高级数据结构，其本质是二叉堆结构，考虑到相关题目具有较强的综合性，我们把它放在小册二叉树和堆相关的专题来展开。在本节，我们集中火力向前两个命题点开炮。</p>\\n</blockquote>\\n<h2 id=\\"为什么一道题可以成为高频面试题\\"><a class=\\"header-anchor\\" href=\\"#为什么一道题可以成为高频面试题\\">#</a> 为什么一道题可以成为高频面试题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>高频题的价值在于同时考数据结构约束、状态不变量、复杂度分析和边界处理，而不是题面出现次数</li>\\n<li>“用栈实现队列”要求只通过栈顶操作恢复 FIFO，核心是两个栈分担输入与输出顺序</li>\\n<li>回答时先给操作契约，再解释为何顺序正确，最后给均摊复杂度，避免只背代码</li>\\n</ul>\\n<p><strong>一道题能成为高频面试题，通常是因为它既考经典知识，又能用较短时间检验候选人的综合基本功。</strong> “用栈实现队列”同时涉及栈和队列两种结构，知识点集中、实现量适中，也比较容易看出编码是否扎实。它的深度和复杂度不算高，但这对务实的前端算法面试未必是缺点，面试官通常更关注基础是否可靠，而不是刻意炫技。</p>\\n</section>\\n<p>如何用栈实现队列？这个问题在近几年的算法面试中热度非常高。</p>\\n<p>所谓“热度”从何而来？这里就引出了一个非常有趣的话题：（在前端算法面试中）什么样的题目是好题？</p>\\n<p>首先，不能剑走偏锋：好的面试题，它考察的大多是算法/数据结构中最经典、最关键的一部分内容，这样才能体现公平；其次，它的知识点要尽可能密集、题目本身要尽可能具备综合性，这样才能一箭双雕甚至一箭N雕，进而体现区分度、最大化面试过程的效率。</p>\\n<p>能够同时在这两个方面占尽优势的考题其实并不是很多，“用栈实现队列”这样的问题算是其中的佼佼者：一方面，它考察的确实是数据结构中的经典内容；另一方面，它又覆盖了两个大的知识点、足以检验出候选人编码基本功的扎实程度。唯一的 BUG 可能就是深度和复杂度不够，换句话说就是不够难。</p>\\n<p>这个特点，在普通算法面试中可能是 BUG，但在前端算法面试中，实在未必。大家要知道，你是前端，你的面试官也是前端，前端行业普遍的算法水平是啥样他心里还没个数吗...... 实际上大多数前端算法面试题的风格都是非常务实的，需要你炫技的实属特殊情况。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人认为“用栈实现队列”太简单，要求换成一道冷门高难题才能拉开差距；作为前端面试官，你会接受这个判断吗？</strong></p>\\n<p>不会仅凭难度换题，因为冷门技巧可能放大知识偶然性，反而削弱公平性。“用栈实现队列”同时覆盖栈、队列和基本编码能力，知识点经典且密集；它的短板是深度有限，需要靠追问补充分辨能力。</p>\\n</li>\\n<li>\\n<p><strong>一场前端技术面只剩 <code>15</code> 分钟，候选人还要完成编码和复杂度说明，你为什么可能保留“用栈实现队列”而不是考复杂动态规划？</strong></p>\\n<p>这类题能在有限时间内同时检查数据结构理解、顺序转换和代码完整性，适合压缩面试成本。复杂动态规划可能把时间消耗在建模上，无法稳定观察基本功；若岗位确实要求更强算法能力，则仍需增加更深的独立题目。</p>\\n</li>\\n<li>\\n<p><strong>招聘的是偏业务交付的前端工程师，却有人主张所有候选人都必须现场解决高难算法题，你会如何调整题目约束？</strong></p>\\n<p>我会优先选择经典、务实且综合度较高的题，再按岗位要求逐步增加复杂度。前端算法面试通常不以炫技为主要目标，但这不等于取消区分度，可以通过复杂度、边界条件和替代方案追问继续分层；岗位职责更偏算法时应另设标准。</p>\\n</li>\\n<li>\\n<p><strong>同一道“用栈实现队列”连续几轮都无法区分候选人，有人秒写模板，有人只会背代码，你会先排查题目还是评分方式？</strong></p>\\n<p>先排查评分维度和追问设计，而不是直接认定题目失效。应观察候选人能否解释两次逆序为何恢复 <code>FIFO</code>、何时转移元素以及如何保证操作正确；若所有考点仍集中在模板复现，题目深度不足，需要追加约束或更换后续题。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"如何用栈实现一个队列\\"><a class=\\"header-anchor\\" href=\\"#如何用栈实现一个队列\\">#</a> 如何用栈实现一个队列？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>inStack</code> 只负责入队，<code>outStack</code> 只负责出队。</li>\\n<li>只有 <code>outStack</code> 为空时才把 <code>inStack</code> 全部倒过去，不能每次出队都搬运。</li>\\n<li>每个元素最多被压入、转移、弹出各一次：单次最坏 <code>O(n)</code>，均摊 <code>O(1)</code>。</li>\\n<li><code>empty</code> 同时检查两个栈，<code>peek</code> 与 <code>pop</code> 应共用转移函数。</li>\\n</ul>\\n<p><strong>用两个栈就能实现队列：<code>stack1</code> 负责接收入队元素，<code>stack2</code> 负责提供出队元素。</strong> 因为元素从一个栈转移到另一个栈会再逆序一次，所以 <code>stack2</code> 的栈顶正好对应最早入队的元素。只有 <code>stack2</code> 为空时，才把 <code>stack1</code> 中的元素全部转过去；否则继续从 <code>stack2</code> 取值，避免破坏已有顺序。<code>pop</code> 和 <code>peek</code> 都遵守这套转移规则，<code>empty</code> 则要确认两个栈同时为空。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“两次 LIFO 会恢复 FIFO。我采用懒转移：输出栈非空时继续消费，只有它空了才批量反转输入栈。”</p>\\n</blockquote>\\n<p>题目描述：使用栈实现队列的下列操作：</p>\\n<ul>\\n<li>push(x) -- 将一个元素放入队列的尾部。</li>\\n<li>pop() -- 从队列首部移除元素。</li>\\n<li>peek() -- 返回队列首部的元素。</li>\\n<li>empty() -- 返回队列是否为空。</li>\\n</ul>\\n<p>示例:</p>\\n<ul>\\n<li>MyQueue queue = new MyQueue();</li>\\n<li>queue.push(1);</li>\\n<li>queue.push(2);</li>\\n<li>queue.peek(); // 返回 1</li>\\n<li>queue.pop(); // 返回 1</li>\\n<li>queue.empty(); // 返回 false</li>\\n</ul>\\n<p>说明:</p>\\n<ul>\\n<li>你只能使用标准的栈操作 -- 也就是只有 push to top, peek/pop from top, size, 和 is empty 操作是合法的。</li>\\n<li>你所使用的语言也许不支持栈。你可以使用 list 或者 deque（双端队列）来模拟一个栈，只要是标准的栈操作即可。</li>\\n<li>假设所有操作都是有效的 （例如，一个空的队列不会调用 pop 或者 peek 操作）。</li>\\n</ul>\\n<p><strong>思路分析</strong></p>\\n<p>做这道题大家首先要在心里清楚一个事情：栈和队列的区别在哪里？</p>\\n<p>仔细想想，栈，后进先出；队列，先进先出。也就是说两者的进出顺序其实是反过来的。用栈实现队列，说白了就是用栈实现先进先出的效果，再说直接点，就是想办法让栈底的元素首先被取出，也就是让出栈序列被逆序。</p>\\n<p>乍一看有点头大：栈结构决定了栈底元素只能被死死地压在最底下，如何使它首先被取出呢？</p>\\n<p>一个栈做不到的事情，我们用两个栈来做：</p>\\n<p>首先，准备两个栈：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122814.png\\" alt=\\"\\"></p>\\n<p>现在问题是，怎么把第一个栈底下的那个 1 给撬出来。仔细想想，阻碍我们接触到 1 的是啥？是不是它头上的 3 和 2？那么如何让 3 和 2 给 1 让路呢？实际上咱们完全可以把这三个元素按顺序从 stack1 中出栈、然后入栈到 stack 2 里去：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122822.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>此时 1 变得触手可及。不仅如此，下一次我们试图出队 2 的时候，可以继续直接对 stack2 执行出栈操作——因为转移 2 和 3 的时候已经做过一次逆序了，此时 stack2 的出栈序列刚好就对应队列的出队序列。</li>\\n<li>有同学会问，那如果 stack1 里入栈新元素怎么办？比如这样：</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210905122830.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>你会发现这个4按照顺序应该在 1、2、3 后出栈。当 4 需要被出栈时，stack2 一定已经空掉了。当 stack2 为空、而 stack1 不为空时，我们需要继续把 stack1 中的元素转移到 stack2 中去，然后再从 stack2 里取元素。也就是说，所有的出队操作都只能依赖 stack2 来完成——只要我们坚持这个原则，就可以确保 stack1 里的元素都能够按照正确的顺序（逆序）出栈。</p>\\n</blockquote>\\n<p>我们按照这个思路来写代码：</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * 初始化构造函数\\n */\\nconst MyQueue = function () {\\n  // 初始化两个栈\\n  this.stack1 = [];\\n  this.stack2 = [];\\n};\\n\\n/**\\n* Push element x to the back of queue.\\n* @param {number} x\\n* @return {void}\\n*/\\nMyQueue.prototype.push = function (x) {\\n  // 直接调度数组的 push 方法\\n  this.stack1.push(x);\\n};\\n\\n/**\\n* Removes the element from in front of queue and returns that element.\\n* @return {number}\\n*/\\nMyQueue.prototype.pop = function () {\\n  // 假如 stack2 为空，需要将 stack1 的元素转移进来\\n  if (this.stack2.length &lt;= 0) {\\n    // 当 stack1 不为空时，出栈\\n    while (this.stack1.length !== 0) {\\n      // 将 stack1 出栈的元素推入 stack2\\n      this.stack2.push(this.stack1.pop());\\n    }\\n  }\\n  // 为了达到逆序的目的，我们只从 stack2 里出栈元素\\n  return this.stack2.pop();\\n};\\n\\n/**\\n* Get the front element.\\n* @return {number}\\n* 这个方法和 pop 唯一的区别就是没有将定位到的值出栈\\n*/\\nMyQueue.prototype.peek = function () {\\n  if (this.stack2.length &lt;= 0) {\\n    // 当 stack1 不为空时，出栈\\n    while (this.stack1.length != 0) {\\n      // 将 stack1 出栈的元素推入 stack2\\n      this.stack2.push(this.stack1.pop());\\n    }\\n  }\\n  // 缓存 stack2 的长度\\n  const stack2Len = this.stack2.length;\\n  return stack2Len &amp;&amp; this.stack2[stack2Len - 1];\\n};\\n\\n/**\\n* Returns whether the queue is empty.\\n* @return {boolean}\\n*/\\nMyQueue.prototype.empty = function () {\\n  // 若 stack1 和 stack2 均为空，那么队列空\\n  return !this.stack1.length &amp;&amp; !this.stack2.length;\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审时有人在每次 <code>push(x)</code> 后立刻把 <code>stack1</code> 全部搬进 <code>stack2</code>，页面上的出队顺序暂时仍正确；这份实现的问题在哪里？</strong></p>\\n<p>顺序暂时正确不代表转移策略合理，频繁搬运会破坏懒转移带来的效率。新元素应先留在 <code>stack1</code>，只有读取队头且 <code>stack2</code> 为空时才整体转移；否则旧元素尚未消费完时还可能扰乱既有队头顺序。</p>\\n</li>\\n<li>\\n<p><strong>消息面板需要实现 <code>push</code>、<code>pop</code>、<code>peek</code>、<code>empty</code>，你会怎样把两个栈封装成可评审的队列实现？</strong></p>\\n<p>用 <code>stack1</code> 接收入队元素，用 <code>stack2</code> 统一承担 <code>pop</code> 和 <code>peek</code>。访问队头时若 <code>stack2</code> 为空，就逐个弹出 <code>stack1</code> 并压入 <code>stack2</code>，之后直接读取其栈顶；<code>empty</code> 必须同时检查两个栈，不能只看其中一个。</p>\\n</li>\\n<li>\\n<p><strong>面试官临时限制只能调用标准栈操作，候选人却用数组的 <code>shift()</code> 直接取队头，这份代码还能算“用栈实现队列”吗？</strong></p>\\n<p>不能，因为 <code>shift()</code> 直接提供了头部删除能力，绕过了题目要求验证的栈操作约束。数组可以作为栈的载体，但只能使用栈顶相关的 <code>push</code>、<code>pop</code>、读取栈顶和判空；若放宽约束，这道题的考察目标也随之消失。</p>\\n</li>\\n<li>\\n<p><strong>线上队列偶尔把新入队的 <code>4</code> 提前到旧元素 <code>2</code>、<code>3</code> 之前，当前状态是 <code>stack2</code> 尚未清空、<code>stack1</code> 已有新元素；你会从哪条不变量开始排查？</strong></p>\\n<p>先检查是否违反“所有出队都从 <code>stack2</code> 完成”以及“仅在 <code>stack2</code> 为空时转移”的不变量。只要 <code>stack2</code> 仍有元素，<code>stack1</code> 中的新元素就必须等待；若此时提前合并或直接从 <code>stack1</code> 弹出，就会让后入队元素越过旧元素。</p>\\n</li>\\n<li>\\n<p><strong>接口约定所有 <code>pop</code> 和 <code>peek</code> 都有效，但团队希望组件也能防御空队列调用；你会保留题设假设还是增加错误处理？</strong></p>\\n<p>算法题中可以明确依赖“调用始终有效”的前提，把注意力放在顺序转换上。工程封装则应约定空队列行为，例如返回 <code>undefined</code> 或抛出明确错误，并让 <code>pop</code> 与 <code>peek</code> 保持一致；额外分支会增加接口语义，需要调用方共同遵守。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"认识双端队列\\"><a class=\\"header-anchor\\" href=\\"#认识双端队列\\">#</a> 认识双端队列</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>deque 支持两端 <code>push/pop</code>，是滑动窗口、0-1 BFS 等问题的基础结构。</li>\\n<li>JavaScript 数组的 <code>shift/unshift</code> 通常要移动元素，并不是真正的 <code>O(1)</code> 队首操作。</li>\\n<li>算法题可用数组加 <code>head</code> 下标模拟；生产场景可用环形缓冲区或成熟 deque。</li>\\n<li>窗口题通常存下标，因为既要判断过期位置，也要读取对应值。</li>\\n</ul>\\n<p><strong>双端队列就是一种可以在队首和队尾分别插入、删除元素的队列。</strong> 在 <code>JavaScript</code> 里，可以用数组直观模拟：尾部使用 <code>push</code>、<code>pop</code>，头部使用 <code>unshift</code>、<code>shift</code>。它比普通队列多了两端操作的自由度，因此很适合处理需要动态维护一段元素的场景，比如常见的滑动窗口问题。这里先抓住“两端都能进出”这个核心特征，具体作用结合题目会更容易理解。</p>\\n</section>\\n<blockquote>\\n<p>双端队列衍生出的滑动窗口问题，是一个经久不衰的命题热点。关于双端队列，各种各样的解释五花八门，这里大家不要纠结，就记住一句话：</p>\\n</blockquote>\\n<p>双端队列就是允许在队列的两端进行插入和删除的队列。</p>\\n<p>体现在编码上，最常见的载体是既允许使用 pop、push 同时又允许使用 shift、unshift 的数组：</p>\\n<pre class=\\"language-js\\"><code>const queue = [1,2,3,4] // 定义一个双端队列   \\nqueue.push(1) // 双端队列尾部入队 \\nqueue.pop() // 双端队列尾部出队   \\nqueue.shift() // 双端队列头部出队 \\nqueue.unshift(1) // 双端队列头部入队\\n</code></pre>\\n<p>现在相信你对双端队列已经形成了一个感性的认知，咱们紧接着就开始做题，在题里去认知这种结构的特征和效用。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在白板上把双端队列写成只能 <code>push</code> 和 <code>shift</code> 的普通队列，随后声称它也能支持滑动窗口；你会指出哪个能力缺失？</strong></p>\\n<p>缺失的是队尾删除和队头插入能力，因此它并未完整表达双端队列。滑动窗口最大值需要从队尾淘汰失去竞争力的候选，并从队头移除过期元素；若底层结构不能高效操作两端，算法形式虽然相似，代价却可能不同。</p>\\n</li>\\n<li>\\n<p><strong>浏览器页面要维护一个两端都可能增删的任务列表，用数组实现时，你会如何约束调用接口，避免业务代码混用方向？</strong></p>\\n<p>应把数组封装成语义明确的双端队列接口，例如队首与队尾各自的插入、删除方法，而不是让业务层散落调用。数组可用 <code>push</code>、<code>pop</code>、<code>shift</code>、<code>unshift</code> 表达这些动作；具体性能不能仅凭接口名称假定为真正的常数时间。</p>\\n</li>\\n<li>\\n<p><strong>面试官把约束改成“只能使用标准队列操作，不能从队尾删除”，原来的单调双端队列方案还能原样解决窗口最大值吗？</strong></p>\\n<p>不能原样使用，因为维护递减候选序列依赖从队尾连续淘汰元素。只保留普通队列能力时，可以退回逐窗口扫描等方案，但会失去该优化的核心；若仍要求线性处理，就需要提供等价的可双端操作结构。</p>\\n</li>\\n<li>\\n<p><strong>线上窗口统计结果正确，但长列表滚动时明显卡顿，代码用数组 <code>shift()</code> 频繁删除队首；你会怎样区分算法问题和容器实现问题？</strong></p>\\n<p>先确认候选元素是否按规则只入队、出队有限次数，再检查 <code>shift()</code> 对底层数组布局产生的额外成本。单调队列的算法分析建立在双端操作足够高效的前提上，普通数组只是便于表达；数据规模较大时应考虑带头指针的数组或专用双端队列。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"滑动窗口问题\\"><a class=\\"header-anchor\\" href=\\"#滑动窗口问题\\">#</a> 滑动窗口问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>deque 存下标，并保持对应值从队首到队尾单调递减，队首就是当前最大值。</li>\\n<li>新下标入队前，弹掉队尾所有 <code>&lt;= nums[i]</code> 的下标：它们更旧且不更大，已被支配。</li>\\n<li>每轮移除队首过期下标 <code>&lt;= i - k</code>，窗口形成后记录队首值。</li>\\n<li>每个下标最多进出队一次：时间 <code>O(n)</code>、空间 <code>O(k)</code>。</li>\\n</ul>\\n<p><strong>我会用单调递减的双端队列保存下标，队首对应的元素就是当前窗口最大值。</strong> 新元素入队前，持续弹出队尾所有更小的元素，因为它们更早离开窗口，也不可能再成为最大值。随后移除下标 <code>&lt;= i - k</code> 的过期队首，等窗口达到 <code>k</code> 个元素后记录答案。每个下标最多入队、出队一次，所以时间复杂度是 <code>O(n)</code>，空间复杂度是 <code>O(k)</code>。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“暴力法重复扫描窗口。我用单调递减 deque 保存仍可能成为最大值的下标，队首负责答案，队尾负责淘汰候选。”</p>\\n</blockquote>\\n<blockquote>\\n<p>题目描述：给定一个数组 nums 和滑动窗口的大小 k，请找出所有滑动窗口里的最大值。</p>\\n</blockquote>\\n<p>示例:</p>\\n<blockquote>\\n<p>输入: nums = [1,3,-1,-3,5,3,6,7], 和 k = 3 输出: [3,3,5,5,6,7]</p>\\n</blockquote>\\n<p>解释: 滑动窗口的位置</p>\\n<hr>\\n<pre class=\\"language-text\\"><code>[1 3 -1] -3 5 3 6 7\\n\\n1 [3 -1 -3] 5 3 6 7\\n1 3 [-1 -3 5] 3 6 7\\n1 3 -1 [-3 5 3] 6 7\\n1 3 -1 -3 [5 3 6] 7\\n1 3 -1 -3 5 [3 6 7]\\n</code></pre>\\n<p>最大值分别对应：</p>\\n<pre class=\\"language-text\\"><code>3 3 5 5 6 7\\n</code></pre>\\n<p>提示：你可以假设 k 总是有效的，在输入数组不为空的情况下，1 ≤ k ≤ 输入数组的大小。</p>\\n<p><strong>思路分析：双指针+遍历法</strong></p>\\n<blockquote>\\n<p>这道题如果只是为了做对，那么思路其实不难想，我们直接模拟题中描述的这个过程就行。</p>\\n</blockquote>\\n<p>按照题意，它要求我们在遍历数组的过程当中，约束一个窗口——窗口的本质其实就是一个范围，像这样：</p>\\n<pre class=\\"language-text\\"><code>[1  3  -1] -3  5  3  6  7 \\n</code></pre>\\n<p>范围就被圈定在了前三个元素。</p>\\n<p>我们前面学过，约束范围，可以用双指针。因此我这里定义一个 left 左指针、定义一个 right 右指针，分别指向窗口的两端即可：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122848.png\\" alt=\\"\\"></p>\\n<p>接下来我们可以把这个窗口里的数字取出来，直接遍历一遍、求出最大值，然后把最大值存进结果数组。这样第一个窗口的最大值就有了。</p>\\n<p>接着按照题意，窗口每次前进一步（左右指针每次一起往前走一步），此时的范围变成了这样：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122855.png\\" alt=\\"\\"></p>\\n<p>我们要做的仍然是取出当前范围的所有元素、遍历一遍求出最大值，然后将最大值存进结果数组。</p>\\n<p>反复执行上面这个过程，直到数组完全被滑动窗口遍历完毕，我们也就得到了问题的答案。</p>\\n<p>基于这个淳朴的思路，我们来写一波代码：</p>\\n<p>编码实现：双指针+遍历法</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {number[]} nums\\n * @param {number} k\\n * @return {number[]}\\n */\\nconst maxSlidingWindow = function (nums, k) {\\n  // 缓存数组的长度\\n  const len = nums.length;\\n  // 定义结果数组\\n  const res = [];\\n  // 初始化左指针\\n  let left = 0;\\n  // 初始化右指针\\n  let right = k - 1;\\n  // 当数组没有被遍历完时，执行循环体内的逻辑\\n  while (right &lt; len) {\\n    // 计算当前窗口内的最大值\\n    const max = calMax(nums, left, right);\\n    // 将最大值推入结果数组\\n    res.push(max);\\n    // 左指针前进一步\\n    left++;\\n    // 右指针前进一步\\n    right++;\\n  }\\n  // 返回结果数组\\n  return res;\\n};\\n\\n// 这个函数用来计算最大值\\nfunction calMax(arr, left, right) {\\n  // 处理数组为空的边界情况\\n  if (!arr || !arr.length) {\\n    return;\\n  }\\n  // 初始化 maxNum 的值为窗口内第一个元素\\n  let maxNum = arr[left];\\n  // 遍历窗口内所有元素，更新 maxNum 的值\\n  for (let i = left; i &lt;= right; i++) {\\n    if (arr[i] &gt; maxNum) {\\n      maxNum = arr[i];\\n    }\\n  }\\n  // 返回最大值\\n  return maxNum;\\n}\\n</code></pre>\\n<p><strong>解法复盘</strong></p>\\n<p>上面这个解法，你在面试的时候写上去，完全没有问题，也不用担心超时。</p>\\n<p>有的同学可能会觉得 calMax 这个函数多余了，认为可以直接用 Math.max 这个 JS 原生方法。其实就算是Math.max，也不可避免地需要对你传入的多个数字做最小值查找，calMax 和Math.max做的工作可以说是一样的辛苦。我这里手动实现一个 calMax， 大家会对查找过程造成的时间开销有更直观的感知。</p>\\n<p>现在我们来思考一下，上面这一波操作下来，时间复杂度是多少？\\n这波操作里其实涉及了两层循环，外层循环是 while，它和滑动窗口前进的次数有关。滑动窗口前进了多少次，while 就执行了多少次。</p>\\n<p>假设数组的规模是 n，那么从起始位置开始，滑动窗口每次走一步，一共可以走 n - k 次。注意别忘了初始位置也算作一步的，因此一共走了 n - k + 1次。然后每个窗口内部我们又会固定执行 k 次遍历。注意 k 可不是个常数，它和 n 一样是个变量。因此这个时间复杂度简化后用大 O 表示法可以记为 O(kn)。</p>\\n<p>O(kn) 虽然不差，但对这道题来说，还不是最好。因此在面试过程中，如果你采用了上面这套解法做出了这个题，面试官有 99% 的可能性会追问你：这个题可以优化吗？如何优化？（或者直接问你，你能在线性时间复杂度内解决此题吗？）</p>\\n<p>答案当然是能，然后面试官就会搬个小板凳坐你旁边，看看你怎么妙手回春，变 O(kn) 为 O(n)。</p>\\n<p>接下来你需要表演的，正是面试官期待已久的双端队列解法啊！</p>\\n<p><strong>思路分析：双端队列法</strong></p>\\n<p>要想变 O(kn) 为 O(n)，我们就要想怎么做才能丢掉这个 k。</p>\\n<p>k 之所以会产生，是因为我们现在只能通过遍历来更新最大值。那么更新最大值，有没有更高效的方法呢？</p>\\n<p>大家仔细想想，当滑动窗口往后前进一步的时候，比如我从初始位置前进到第二个位置：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122906.png\\" alt=\\"\\"></p>\\n<p>（图中红色的范围是初始位置时，滑动窗口覆盖到的元素）</p>\\n<blockquote>\\n<p>此时滑动窗口内的元素少了一个 1，增加了一个 -3——减少的数不是当前最大值，增加的数也没有超越当前最大值，因此最大值仍然是 3。此时我们不禁要想：如果我们能在窗口发生移动时，只根据发生变化的元素对最大值进行更新，那复杂度是不是就低很多了？</p>\\n</blockquote>\\n<p>双端队列可以完美地帮助我们达到这个目的。</p>\\n<p>使用双端队列法，核心的思路是维护一个有效的递减队列。</p>\\n<p>在遍历数组的前期，我们尝试将遍历到的每一个元素都推入队列内部（下图是第一个元素入队的示意图）：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122914.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>每尝试推入一个元素前，都把这个元素与队列尾部的元素作对比。根据对比结果的不同，采取不同的措施：</p>\\n</blockquote>\\n<ul>\\n<li>如果试图推入的元素（当前元素）大于队尾元素，则意味着队列的递减趋势被打破了。此时我们需要将队列尾部的元素依次出队（注意由于是双端队列，所以队尾出队是没有问题的）、直到队尾元素大于等于当前元素为止，此时再将当前元素入队。</li>\\n<li>如果试图推入的元素小于队列尾部的元素，那么就不需要额外的操作，直接把当前元素入队即可。</li>\\n</ul>\\n<p>我用动画来表达一下这个过程：</p>\\n<p><img src=\\"https://p1-jj.byteimg.com/tos-cn-i-t2oaga2asx/gold-user-assets/2020/4/19/171918a1ae330dd3~tplv-t2oaga2asx-watermark.awebp\\" alt=\\"\\"></p>\\n<p>维持递减队列的目的，就在于确保队头元素始终是当前窗口的最大值。</p>\\n<blockquote>\\n<p>当遍历到的元素个数达到了 k 个时，意味着滑动窗口的第一个最大值已经产生了，我们把它 push 进结果数组里：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905122939.png\\" alt=\\"\\"></p>\\n<p>然后继续前进，我们发现数组索引 0 处的元素（1）已经被踢出滑动窗口了（图中红色方块对应的是当前滑动窗口覆盖到的元素们）：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122950.png\\" alt=\\"\\"></p>\\n<p>为了确保队列的有效性，需要及时地去队列检查下 1 这个元素在不在队列里（在的话要及时地踢出去，因为队列本身只维护当前滑动窗口内的元素）。</p>\\n<blockquote>\\n<p>这里大家思考一下，我在查找 1 的时候，需不需要遍历整个队列？答案是不需要，因为 1 是最靠前的一个元素，如果它在，那么它一定是队头元素。这里我们只需要检查队头元素是不是 1 就行了。 此时我们检查队头，发现是 3：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905122957.png\\" alt=\\"\\"></p>\\n<p>没错，1早就因为不符合递减趋势被从队头干掉了。此时我们可以断定，当前双端队列里的元素都是滑动窗口已经覆盖的有效元素——没毛病，继续往下走就行了。</p>\\n<blockquote>\\n<p>接下来，每往前遍历一个元素，都需要重复以上的几个步骤。这里我总结一下每一步都做了什么：</p>\\n</blockquote>\\n<ul>\\n<li>检查队尾元素，看是不是都满足大于等于当前元素的条件。如果是的话，直接将当前元素入队。否则，将队尾元素逐个出队、直到队尾元素大于等于当前元素为止。</li>\\n<li>将当前元素入队</li>\\n<li>检查队头元素，看队头元素是否已经被排除在滑动窗口的范围之外了。如果是，则将队头元素出队。</li>\\n<li>判断滑动窗口的状态：看当前遍历过的元素个数是否小于 k。如果元素个数小于k，这意味着第一个滑动窗口内的元素都还没遍历完、第一个最大值还没出现，此时我们还不能动结果数组，只能继续更新队列；如果元素个数大于等于k，这意味着滑动窗口的最大值已经出现了，此时每遍历到一个新元素（也就是滑动窗口每往前走一步）都要及时地往结果数组里添加当前滑动窗口对应的最大值（最大值就是此时此刻双端队列的队头元素）。</li>\\n</ul>\\n<blockquote>\\n<p>这四个步骤分别有以下的目的：</p>\\n</blockquote>\\n<ul>\\n<li>维持队列的递减性：确保队头元素是当前滑动窗口的最大值。这样我们每次取最大值时，直接取队头元素即可。</li>\\n<li>这一步没啥好说的，就是在维持队列递减性的基础上、更新队列的内容。</li>\\n<li>维持队列的有效性：确保队列里所有的元素都在滑动窗口圈定的范围以内。</li>\\n<li>排除掉滑动窗口还没有初始化完成、第一个最大值还没有出现的特殊情况。</li>\\n</ul>\\n<p>结合以上的分析，我们来写代码：</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {number[]} nums\\n * @param {number} k\\n * @return {number[]}\\n */\\nconst maxSlidingWindow = function (nums, k) {\\n  // 缓存数组的长度\\n  const len = nums.length;\\n  // 初始化结果数组\\n  const res = [];\\n  // 初始化双端队列\\n  const deque = [];\\n  // 开始遍历数组\\n  for (let i = 0; i &lt; len; i++) {\\n    // 当队尾元素小于当前元素时\\n    while (deque.length &amp;&amp; nums[deque[deque.length - 1]] &lt; nums[i]) {\\n      // 将队尾元素（索引）不断出队，直至队尾元素大于等于当前元素\\n      deque.pop();\\n    }\\n    // 入队当前元素索引（注意是索引）\\n    deque.push(i);\\n    // 当队头元素的索引已经被排除在滑动窗口之外时\\n    while (deque.length &amp;&amp; deque[0] &lt;= i - k) {\\n      // 将队头元素索引出队\\n      deque.shift();\\n    }\\n    // 判断滑动窗口的状态，只有在被遍历的元素个数大于 k 的时候，才更新结果数组\\n    if (i &gt;= k - 1) {\\n      res.push(nums[deque[0]]);\\n    }\\n  }\\n  // 返回结果数组\\n  return res;\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人把窗口里的“值”存进递减队列，输入含多个相同最大值时页面偶尔提前删除最大值；为什么只存值会埋下故障？</strong></p>\\n<p>只存值无法判断某个候选是否已经越过窗口左边界，也难以区分多个相同值的先后位置。队列应保存数组下标，通过 <code>nums[index]</code> 比较大小，并用下标判断过期；这样队首既代表最大值，也带有明确的有效期。</p>\\n</li>\\n<li>\\n<p><strong>实时图表要对长度为 <code>k</code> 的窗口持续输出最大值，你会怎样组织每次收到 <code>nums[i]</code> 后的更新顺序？</strong></p>\\n<p>先从队尾弹出所有值小于当前值的下标，再压入 <code>i</code>，随后从队首移除满足 <code>index &lt;= i-k</code> 的过期项。当 <code>i &gt;= k-1</code> 时，<code>nums[deque[0]]</code> 就是当前窗口答案；实现时必须统一边界条件，避免多留一轮过期元素。</p>\\n</li>\\n<li>\\n<p><strong>产品把需求从窗口最大值改成窗口最小值，但仍要求整体线性处理，你会改动哪些比较方向？</strong></p>\\n<p>把候选队列改为单调递增：新元素进入前，从队尾淘汰所有比它更大的候选，队首便保持当前最小值。过期判断和按下标存储的方式不变；相等元素是否淘汰可按策略选择，但必须保证剩余下标的有效期正确。</p>\\n</li>\\n<li>\\n<p><strong>输入 <code>[1,3,-1,-3,5,3,6,7]</code>、<code>k=3</code> 时结果少了第一个 <code>3</code>，其余值大致正确；你会优先检查哪两个条件？</strong></p>\\n<p>优先检查结果写入条件是否误写成 <code>i &gt;= k</code>，正确起点应是 <code>i &gt;= k-1</code>。再检查队首过期条件是否与当前窗口 <code>[i-k+1,i]</code> 对齐，应移除 <code>index &lt;= i-k</code>；这两个边界最容易造成少首项或窗口错位。</p>\\n</li>\\n<li>\\n<p><strong>评审会上有人坚持每个窗口调用一次 <code>Math.max</code>，因为代码更短；在数组规模和 <code>k</code> 都可能增长时，你如何做方案取舍？</strong></p>\\n<p>逐窗口求最大值会重复扫描，每个窗口处理 <code>k</code> 个元素，总体为 <code>O(kn)</code>，代码虽短但扩展性受限。单调双端队列让每个下标只经历有限次入队和出队，整体可达 <code>O(n)</code>；代价是状态维护更复杂，边界和过期逻辑更容易写错。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);