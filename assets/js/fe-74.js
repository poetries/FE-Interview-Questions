(window.webpackJsonp=window.webpackJsonp||[]).push([[74],{1180:function(e){e.exports=JSON.parse('{"html":"<p><strong>event loop它的执行顺序：</strong></p>\\n<ul>\\n<li>一开始整个脚本作为一个宏任务执行</li>\\n<li>执行过程中同步代码直接执行，宏任务进入宏任务队列，微任务进入微任务队列</li>\\n<li>当前宏任务执行完出队，检查微任务列表，有则依次执行，直到全部执行完</li>\\n<li>执行浏览器UI线程的渲染工作</li>\\n<li>检查是否有Web Worker任务，有则执行</li>\\n<li>执行完本轮的宏任务，回到2，依此循环，直到宏任务和微任务队列都为空</li>\\n</ul>\\n<p><strong>微任务包括</strong>：<code>MutationObserver</code>、<code>Promise.then()</code>或<code>reject()</code>、<code>Promise</code>为基础开发的其它技术，比如<code>fetch API</code>、<code>V8</code>的垃圾回收过程、<code>Node</code>独有的<code>process.nextTick</code>。</p>\\n<p><strong>宏任务包括</strong>：<code>script、script</code> 、<code>setTimeout</code>、<code>setInterval</code> 、<code>setImmediate</code> 、<code>I/O</code> 、<code>UI rendering</code></p>\\n<h2 id=\\"_1-promise的几道基础题\\"><a class=\\"header-anchor\\" href=\\"#_1-promise的几道基础题\\">#</a> 1 Promise的几道基础题</h2>\\n<h3 id=\\"_1-1-题目一\\"><a class=\\"header-anchor\\" href=\\"#_1-1-题目一\\">#</a> 1.1 题目一</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>new Promise</code> 的 executor 会在构造时同步执行，所以先输出 <code>promise1</code></li>\\n<li>没有调用 <code>resolve</code> 或 <code>reject</code>，Promise 会一直保持 <code>pending</code></li>\\n<li>构造器之后的普通 <code>console.log</code> 仍按同步顺序执行</li>\\n</ul>\\n<p><strong>这段代码会先输出 <code>promise1</code>，再输出 <code>1</code> 和一个状态为 <code>pending</code> 的 <code>Promise</code>。</strong> 原因是 <code>new Promise</code> 的 <code>executor</code> 在构造实例时会同步执行，并不会等到微任务阶段。<code>executor</code> 内没有调用 <code>resolve</code> 或 <code>reject</code>，所以实例始终保持 <code>pending</code>。构造完成后，外层的 <code>console.log</code> 继续按同步代码的顺序执行。</p>\\n</section>\\n<pre class=\\"language-text\\"><code>const promise1 = new Promise((resolve, reject) =&gt; {\\n  console.log(&#39;promise1&#39;)\\n})\\nconsole.log(&#39;1&#39;, promise1);\\n</code></pre>\\n<ul>\\n<li>从上至下，先遇到new Promise，执行该构造函数中的代码promise1</li>\\n<li>然后执行同步代码1，此时promise1没有被resolve或者reject，因此状态还是pending</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&#39;promise1&#39;\\n&#39;1&#39; Promise{&lt;pending&gt;}\\n</code></pre>\\n<h3 id=\\"_1-2-题目二\\"><a class=\\"header-anchor\\" href=\\"#_1-2-题目二\\">#</a> 1.2 题目二</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>从上至下，先遇到new Promise，执行其中的同步代码1</li>\\n<li>再遇到resolve(\'success\')， 将promise的状态改为了resolved并且将值保存下来</li>\\n<li>跳出promise，往下执行，碰到promise.then这个微任务，将其加入微任务队列</li>\\n<li>本轮宏任务全部执行完毕，检查微任务队列，发现promise.then这个微任务且状态为resolved，执行它</li>\\n</ul>\\n<p><strong>最终输出顺序是 <code>1 2 4 3</code>。</strong> 创建 <code>Promise</code> 时，<code>executor</code> 会立即同步执行，所以先打印 <code>1</code>；调用 <code>resolve(\'success\')</code> 后状态变为 <code>resolved</code>，但后面的同步代码仍会继续执行并打印 <code>2</code>。随后 <code>then</code> 回调进入微任务队列，主流程先打印 <code>4</code>。等本轮同步代码执行完，事件循环才处理这个微任务并打印 <code>3</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const promise = new Promise((resolve, reject) =&gt; {\\n  console.log(1);\\n  resolve(&#39;success&#39;)\\n  console.log(2);\\n});\\npromise.then(() =&gt; {\\n  console.log(3);\\n});\\nconsole.log(4);\\n</code></pre>\\n<ul>\\n<li>从上至下，先遇到new Promise，执行其中的同步代码1</li>\\n<li>再遇到resolve(\'success\')， 将promise的状态改为了resolved并且将值保存下来</li>\\n<li>继续执行同步代码2</li>\\n<li>跳出promise，往下执行，碰到promise.then这个微任务，将其加入微任务队列</li>\\n<li>执行同步代码4</li>\\n<li>本轮宏任务全部执行完毕，检查微任务队列，发现promise.then这个微任务且状态为resolved，执行它。</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>1 2 4 3\\n</code></pre>\\n<h3 id=\\"_1-3-题目三\\"><a class=\\"header-anchor\\" href=\\"#_1-3-题目三\\">#</a> 1.3 题目三</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>executor 中的两个日志同步输出，不会因为包在 Promise 中而延后</li>\\n<li>没有调用 <code>resolve</code> 或 <code>reject</code>，状态始终是 <code>pending</code></li>\\n<li><code>then</code> 回调只有 Promise 状态确定后才会进入微任务队列，因此本题不会输出 <code>3</code></li>\\n</ul>\\n<p><strong>这段代码只会依次输出 <code>1 2 4</code>，不会输出 <code>3</code>。</strong> <code>Promise</code> 的 <code>executor</code> 是同步执行的，因此其中两个日志会立即打印，不会因为写在 <code>Promise</code> 里而延后。由于代码始终没有调用 <code>resolve</code> 或 <code>reject</code>，这个 <code>Promise</code> 会一直保持 <code>pending</code>。本质上，状态没有确定，<code>then</code> 中的回调就不会进入微任务队列。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const promise = new Promise((resolve, reject) =&gt; {\\n  console.log(1);\\n  console.log(2);\\n});\\npromise.then(() =&gt; {\\n  console.log(3);\\n});\\nconsole.log(4);\\n</code></pre>\\n<ul>\\n<li>和题目二相似，只不过在<code>promise</code>中并没有<code>resolve</code>或者<code>reject</code></li>\\n<li>因此<code>promise.then</code>并不会执行，它只有在被改变了状态之后才会执行。</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>1 2 4\\n</code></pre>\\n<h3 id=\\"_1-4-题目四\\"><a class=\\"header-anchor\\" href=\\"#_1-4-题目四\\">#</a> 1.4 题目四</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>从上至下，先遇到<code>new Promise</code>，执行该构造函数中的代码<code>promise1</code></li>\\n<li>碰到<code>resolve</code>函数, 将<code>promise1</code>的状态改变为<code>resolved</code>, 并将结果保存下来</li>\\n<li>碰到<code>promise1.then</code>这个微任务，将它放入微任务队列</li>\\n<li><code>promise2</code>是一个新的状态为<code>pending的</code>Promise`</li>\\n<li>执行同步代码1， 同时打印出<code>promise1</code>的状态是<code>resolved</code></li>\\n</ul>\\n<p><strong>输出顺序是 <code>promise1</code>、两个 <code>Promise</code> 的当前状态，最后才是 <code>resolve1</code>。</strong> 创建 <code>promise1</code> 时 <code>executor</code> 同步执行，先打印 <code>promise1</code>，随后 <code>resolve(\'resolve1\')</code> 将它变为 <code>resolved</code>。调用 <code>promise1.then</code> 会返回新的 <code>promise2</code>，此时回调尚未执行，所以同步打印时 <code>promise1</code> 已完成，而 <code>promise2</code> 仍是 <code>pending</code>。本轮同步代码结束后，微任务执行并打印 <code>resolve1</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const promise1 = new Promise((resolve, reject) =&gt; {\\n  console.log(&#39;promise1&#39;)\\n  resolve(&#39;resolve1&#39;)\\n})\\nconst promise2 = promise1.then(res =&gt; {\\n  console.log(res)\\n})\\nconsole.log(&#39;1&#39;, promise1);\\nconsole.log(&#39;2&#39;, promise2);\\n</code></pre>\\n<ul>\\n<li>从上至下，先遇到<code>new Promise</code>，执行该构造函数中的代码<code>promise1</code></li>\\n<li>碰到<code>resolve</code>函数, 将<code>promise1</code>的状态改变为<code>resolved</code>, 并将结果保存下来</li>\\n<li>碰到<code>promise1.then</code>这个微任务，将它放入微任务队列</li>\\n<li><code>promise2</code>是一个新的状态为<code>pending的</code>Promise`</li>\\n<li>执行同步代码1， 同时打印出<code>promise1</code>的状态是<code>resolved</code></li>\\n<li>执行同步代码2，同时打印出<code>promise2</code>的状态是<code>pending</code></li>\\n<li>宏任务执行完毕，查找微任务队列，发现<code>promise1.then</code>这个微任务且状态为resolved，执行它。</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&#39;promise1&#39;\\n&#39;1&#39; Promise{&lt;resolved&gt;: &#39;resolve1&#39;}\\n&#39;2&#39; Promise{&lt;pending&gt;}\\n&#39;resolve1&#39;\\n</code></pre>\\n<h3 id=\\"_1-5-题目五\\"><a class=\\"header-anchor\\" href=\\"#_1-5-题目五\\">#</a> 1.5 题目五</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>调用 <code>fn()</code> 时立即创建 Promise 并同步执行 executor，所以先输出 <code>1</code></li>\\n<li><code>resolve</code> 只改变状态并安排后续微任务，不会立刻执行 <code>then</code></li>\\n<li>同步代码 <code>start</code> 输出后，微任务才输出 <code>success</code></li>\\n</ul>\\n<p><strong>最终会依次输出 <code>1</code>、<code>start</code>、<code>success</code>。</strong> 调用 <code>fn()</code> 时会立即创建并返回一个 <code>Promise</code>，它的 <code>executor</code> 同步执行，因此最先打印 <code>1</code>。<code>resolve(\'success\')</code> 会确定 <code>Promise</code> 状态并让后续 <code>then</code> 回调等待微任务阶段执行，不会当场打印结果。所以主流程先输出 <code>start</code>，同步代码结束后才由微任务输出 <code>success</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const fn = () =&gt; (new Promise((resolve, reject) =&gt; {\\n  console.log(1);\\n  resolve(&#39;success&#39;)\\n}))\\nfn().then(res =&gt; {\\n  console.log(res)\\n})\\nconsole.log(&#39;start&#39;)\\n</code></pre>\\n<p>请仔细看看哦，fn函数它是直接返回了一个new Promise的，而且fn函数的调用是在start之前，所以它里面的内容应该会先执行。</p>\\n<pre class=\\"language-text\\"><code>1\\n&#39;start&#39;\\n&#39;success&#39;\\n</code></pre>\\n<h3 id=\\"_1-6-题目六\\"><a class=\\"header-anchor\\" href=\\"#_1-6-题目六\\">#</a> 1.6 题目六</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先执行同步的 <code>start</code>，再调用 <code>fn()</code> 创建 Promise</li>\\n<li>executor 仍然同步输出 <code>1</code>，调用位置决定它与其他同步日志的先后</li>\\n<li><code>then</code> 回调进入微任务队列，最后输出 <code>success</code></li>\\n</ul>\\n<p><strong>输出顺序是 <code>start</code>、<code>1</code>、<code>success</code>。</strong> <code>fn</code> 只是定义时不会执行，先运行的同步代码会打印 <code>start</code>，调用 <code>fn()</code> 后才创建 <code>Promise</code>，其 <code>executor</code> 会立即同步执行并打印 <code>1</code>。<code>resolve</code> 之后，<code>then</code> 回调进入微任务队列，所以要等当前同步代码执行完才输出 <code>success</code>。这里要注意，看到 <code>new Promise()</code> 不能直接判断执行时机，还要看它是否被包在尚未调用的函数里。</p>\\n</section>\\n<p>如果把fn的调用放到start之后呢？</p>\\n<pre class=\\"language-js\\"><code>const fn = () =&gt;\\n  new Promise((resolve, reject) =&gt; {\\n    console.log(1);\\n    resolve(&quot;success&quot;);\\n  });\\nconsole.log(&quot;start&quot;);\\nfn().then(res =&gt; {\\n  console.log(res);\\n});\\n</code></pre>\\n<p>现在start就在1之前打印出来了，因为fn函数是之后执行的。</p>\\n<blockquote>\\n<p>注意⚠️：之前我们很容易就以为看到<code>new Promise()</code>就执行它的第一个参数函数了，其实这是不对的，就像这两道题中，我们得注意它是不是被包裹在函数当中，如果是的话，只有在函数调用的时候才会执行。</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>&quot;start&quot;\\n1\\n&quot;success&quot;\\n</code></pre>\\n<h2 id=\\"_2-promise结合settimeout\\"><a class=\\"header-anchor\\" href=\\"#_2-promise结合settimeout\\">#</a> 2. Promise结合setTimeout</h2>\\n<h3 id=\\"_2-1-题目一\\"><a class=\\"header-anchor\\" href=\\"#_2-1-题目一\\">#</a> 2.1 题目一</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>刚开始整个脚本作为一个宏任务来执行，对于同步代码直接压入执行栈进行执行，因此先打印出start和end</li>\\n<li>setTimout作为一个宏任务被放入宏任务队列(下一个)</li>\\n<li>Promise.then作为一个微任务被放入微任务队列</li>\\n<li>本次宏任务执行完，检查微任务，发现Promise.then，执行它</li>\\n<li>接下来进入下一个宏任务，发现setTimeout，执行</li>\\n</ul>\\n<p><strong>最终输出顺序是 <code>start</code>、<code>end</code>、<code>resolve</code>、<code>time</code>。</strong> 整段脚本先作为一个宏任务执行，因此两个同步的 <code>console.log</code> 会先完成；<code>Promise.then</code> 被放入微任务队列，<code>setTimeout</code> 的回调则等待后续宏任务。当前宏任务结束后会先清空微任务，所以 <code>resolve</code> 早于 <code>time</code>。简单来说，即使定时器写在前面，它也不会抢在本轮微任务之前执行。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>console.log(&#39;start&#39;)\\nsetTimeout(() =&gt; {\\n  console.log(&#39;time&#39;)\\n})\\nPromise.resolve().then(() =&gt; {\\n  console.log(&#39;resolve&#39;)\\n})\\nconsole.log(&#39;end&#39;)\\n</code></pre>\\n<ul>\\n<li>刚开始整个脚本作为一个宏任务来执行，对于同步代码直接压入执行栈进行执行，因此先打印出start和end。</li>\\n<li>setTimout作为一个宏任务被放入宏任务队列(下一个)</li>\\n<li>Promise.then作为一个微任务被放入微任务队列</li>\\n<li>本次宏任务执行完，检查微任务，发现Promise.then，执行它</li>\\n<li>接下来进入下一个宏任务，发现setTimeout，执行。</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&#39;start&#39;\\n&#39;end&#39;\\n&#39;resolve&#39;\\n&#39;time&#39;\\n</code></pre>\\n<h3 id=\\"_2-2-题目二\\"><a class=\\"header-anchor\\" href=\\"#_2-2-题目二\\">#</a> 2.2 题目二</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>从上至下，先遇到new Promise，执行该构造函数中的代码1</li>\\n<li>然后碰到了定时器，将这个定时器中的函数放到下一个宏任务的延迟队列中等待执行</li>\\n<li>跳出promise函数，遇到promise.then，但其状态还是为pending，这里理解为先不执行</li>\\n<li>一轮循环过后，进入第二次宏任务，发现延迟队列中有setTimeout定时器，执行它</li>\\n<li>首先执行timerStart，然后遇到了resolve，将promise的状态改为resolved且保存结果并将之前的promise.then推入微任务队列</li>\\n</ul>\\n<p><strong>输出顺序是 <code>1</code>、<code>2</code>、<code>4</code>、<code>timerStart</code>、<code>timerEnd</code>、<code>success</code>。</strong> 创建 <code>Promise</code> 时 <code>executor</code> 同步执行，所以先打印 <code>1</code>、注册定时器，再打印 <code>2</code>；此时 <code>Promise</code> 仍是 <code>pending</code>，<code>then</code> 回调暂不执行，随后同步输出 <code>4</code>。定时器作为下一轮宏任务运行，打印 <code>timerStart</code> 后调用 <code>resolve</code>，把对应的 <code>then</code> 回调加入微任务队列，但当前回调仍会继续打印 <code>timerEnd</code>。等这个宏任务结束，微任务才执行并输出 <code>success</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const promise = new Promise((resolve, reject) =&gt; {\\n  console.log(1);\\n  setTimeout(() =&gt; {\\n    console.log(&quot;timerStart&quot;);\\n    resolve(&quot;success&quot;);\\n    console.log(&quot;timerEnd&quot;);\\n  }, 0);\\n  console.log(2);\\n});\\npromise.then((res) =&gt; {\\n  console.log(res);\\n});\\nconsole.log(4);\\n</code></pre>\\n<blockquote>\\n<p>和题目1.2很像，不过在resolve的外层加了一层setTimeout定时器。</p>\\n</blockquote>\\n<ul>\\n<li>从上至下，先遇到new Promise，执行该构造函数中的代码1</li>\\n<li>然后碰到了定时器，将这个定时器中的函数放到下一个宏任务的延迟队列中等待执行\\n执行同步代码2</li>\\n<li>跳出promise函数，遇到promise.then，但其状态还是为pending，这里理解为先不执行\\n执行同步代码4</li>\\n<li>一轮循环过后，进入第二次宏任务，发现延迟队列中有setTimeout定时器，执行它</li>\\n<li>首先执行timerStart，然后遇到了resolve，将promise的状态改为resolved且保存结果并将之前的promise.then推入微任务队列</li>\\n<li>继续执行同步代码timerEnd</li>\\n<li>宏任务全部执行完毕，查找微任务队列，发现promise.then这个微任务，执行它</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>1\\n2\\n4\\n&quot;timerStart&quot;\\n&quot;timerEnd&quot;\\n&quot;success&quot;\\n</code></pre>\\n<h3 id=\\"_2-3-题目三\\"><a class=\\"header-anchor\\" href=\\"#_2-3-题目三\\">#</a> 2.3 题目三</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>一个是为定时器timer3，一个是为Promise.then</li>\\n<li>但是如果是定时器timer3的话，它会在timer2后执行，而Promise.then却是在timer2之前执行</li>\\n<li>你可以这样理解，Promise.then是微任务，它会被加入到本轮中的微任务列表，而定时器timer3是宏任务，它会被加入到下一轮的宏任务中</li>\\n<li>这两个例子，看着好像只是把第一个定时器中的内容换了一下而已</li>\\n<li>理解完这两个案例，可以来看看下面一道比较难的题目了</li>\\n</ul>\\n<p><strong>两个例子的差别在于，新增的 <code>setTimeout</code> 要等后续宏任务，而 <code>Promise.then</code> 会在当前宏任务结束后立即作为微任务执行。</strong> 第一个例子中，<code>timer1</code> 执行时才注册 <code>timer3</code>，此时 <code>timer2</code> 已经在队列里，因此顺序是 <code>start</code>、<code>timer1</code>、<code>timer2</code>、<code>timer3</code>。第二个例子中，<code>timer1</code> 创建的 <code>then</code> 回调进入本轮微任务队列，会在运行下一个宏任务前执行，所以顺序变成 <code>start</code>、<code>timer1</code>、<code>promise</code>、<code>timer2</code>。本质上不能只看代码长得像不像，还要看新任务属于微任务还是后续宏任务。</p>\\n</section>\\n<p>题目三分了两个题目，因为看着都差不多，不过执行的结果却不一样，大家不妨先猜猜下面两个题目分别执行什么：</p>\\n<ol>\\n<li></li>\\n</ol>\\n<pre class=\\"language-js\\"><code>setTimeout(() =&gt; {\\n  console.log(&#39;timer1&#39;);\\n  setTimeout(() =&gt; {\\n    console.log(&#39;timer3&#39;)\\n  }, 0)\\n}, 0)\\nsetTimeout(() =&gt; {\\n  console.log(&#39;timer2&#39;)\\n}, 0)\\nconsole.log(&#39;start&#39;)\\n</code></pre>\\n<pre class=\\"language-text\\"><code>&#39;start&#39;\\n&#39;timer1&#39;\\n&#39;timer2&#39;\\n&#39;timer3&#39;\\n</code></pre>\\n<ol start=\\"2\\">\\n<li></li>\\n</ol>\\n<pre class=\\"language-js\\"><code>setTimeout(() =&gt; {\\n  console.log(&#39;timer1&#39;);\\n  Promise.resolve().then(() =&gt; {\\n    console.log(&#39;promise&#39;)\\n  })\\n}, 0)\\nsetTimeout(() =&gt; {\\n  console.log(&#39;timer2&#39;)\\n}, 0)\\nconsole.log(&#39;start&#39;)\\n</code></pre>\\n<pre class=\\"language-text\\"><code>&#39;start&#39;\\n&#39;timer1&#39;\\n&#39;promise&#39;\\n&#39;timer2&#39;\\n</code></pre>\\n<ul>\\n<li>这两个例子，看着好像只是把第一个定时器中的内容换了一下而已。</li>\\n<li>一个是为定时器timer3，一个是为Promise.then</li>\\n<li>但是如果是定时器timer3的话，它会在timer2后执行，而Promise.then却是在timer2之前执行。</li>\\n<li>你可以这样理解，Promise.then是微任务，它会被加入到本轮中的微任务列表，而定时器timer3是宏任务，它会被加入到下一轮的宏任务中。</li>\\n<li>理解完这两个案例，可以来看看下面一道比较难的题目了</li>\\n</ul>\\n<h3 id=\\"_2-4-题目三\\"><a class=\\"header-anchor\\" href=\\"#_2-4-题目三\\">#</a> 2.4 题目三</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>这道题稍微的难一些，在promise中执行定时器，又在定时器中执行promise</li>\\n<li>并且要注意的是，这里的Promise是直接resolve的，而之前的new Promise不一样</li>\\n<li>刚开始整个脚本作为第一次宏任务来执行，我们将它标记为宏1，从上至下执行</li>\\n<li>遇到Promise.resolve().then这个微任务，将then中的内容加入第一次的微任务队列标记为微1</li>\\n<li>遇到定时器timer1，将它加入下一次宏任务的延迟列表，标记为宏2，等待执行(先不管里面是什么内容)</li>\\n</ul>\\n<p><strong>最终输出顺序是 <code>start</code>、<code>promise1</code>、<code>timer1</code>、<code>promise2</code>、<code>timer2</code>。</strong> 整段脚本先作为宏任务执行，同步打印 <code>start</code>，同时把第一个 <code>then</code> 放进微任务队列，把 <code>timer1</code> 放进后续宏任务队列。当前宏任务结束后执行 <code>then</code>，打印 <code>promise1</code>，此时才注册 <code>timer2</code>，所以它排在 <code>timer1</code> 后面。执行 <code>timer1</code> 时又产生 <code>promise2</code> 微任务，它会在进入 <code>timer2</code> 之前执行。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>Promise.resolve().then(() =&gt; {\\n  console.log(&#39;promise1&#39;);\\n  const timer2 = setTimeout(() =&gt; {\\n    console.log(&#39;timer2&#39;)\\n  }, 0)\\n});\\nconst timer1 = setTimeout(() =&gt; {\\n  console.log(&#39;timer1&#39;)\\n  Promise.resolve().then(() =&gt; {\\n    console.log(&#39;promise2&#39;)\\n  })\\n}, 0)\\nconsole.log(&#39;start&#39;);\\n</code></pre>\\n<ul>\\n<li>这道题稍微的难一些，在promise中执行定时器，又在定时器中执行promise；</li>\\n<li>并且要注意的是，这里的Promise是直接resolve的，而之前的new Promise不一样。</li>\\n</ul>\\n<p><strong>因此过程分析为</strong></p>\\n<ul>\\n<li>刚开始整个脚本作为第一次宏任务来执行，我们将它标记为宏1，从上至下执行</li>\\n<li>遇到Promise.resolve().then这个微任务，将then中的内容加入第一次的微任务队列标记为微1</li>\\n<li>遇到定时器timer1，将它加入下一次宏任务的延迟列表，标记为宏2，等待执行(先不管里面是什么内容)</li>\\n<li>执行宏1中的同步代码start</li>\\n<li>第一次宏任务(宏1)执行完毕，检查第一次的微任务队列(微1)，发现有一个promise.then这个微任务需要执行</li>\\n<li>执行打印出微1中同步代码promise1，然后发现定时器timer2，将它加入宏2的后面，标记为宏3</li>\\n<li>第一次微任务队列(微1)执行完毕，执行第二次宏任务(宏2)，首先执行同步代码timer1</li>\\n<li>然后遇到了promise2这个微任务，将它加入此次循环的微任务队列，标记为微2</li>\\n<li>宏2中没有同步代码可执行了，查找本次循环的微任务队列(微2)，发现了promise2，执行它</li>\\n<li>第二轮执行完毕，执行宏3，打印出timer2</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&#39;start&#39;\\n&#39;promise1&#39;\\n&#39;timer1&#39;\\n&#39;promise2&#39;\\n&#39;timer2&#39;\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/03/2.png\\" alt=\\"\\"></p>\\n<h3 id=\\"_2-5-题目四\\"><a class=\\"header-anchor\\" href=\\"#_2-5-题目四\\">#</a> 2.5 题目四</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>从上至下，先执行第一个new Promise中的函数，碰到setTimeout将它加入下一个宏任务列表</li>\\n<li>跳出new Promise，碰到promise1.then这个微任务，但其状态还是为pending，这里理解为先不执行</li>\\n<li>promise2是一个新的状态为pending的Promise</li>\\n<li>执行同步代码console.log(\'promise1\')，且打印出的promise1的状态为pending</li>\\n<li>执行同步代码console.log(\'promise2\')，且打印出的promise2的状态为pending</li>\\n</ul>\\n<p><strong>前两次打印时两个 <code>Promise</code> 都是 <code>pending</code>，两秒后 <code>promise1</code> 已成功，<code>promise2</code> 则因回调抛错而失败。</strong> <code>new Promise</code> 的执行器会同步运行，但第一个定时器尚未触发，所以 <code>promise1.then</code> 暂时不能执行，链式调用返回的 <code>promise2</code> 也只能等待。约一秒后 <code>resolve(\'success\')</code> 让 <code>promise1</code> 状态改变，并把对应的 <code>then</code> 回调加入微任务队列；回调抛出的错误会使 <code>promise2</code> 变成 <code>rejected</code>。由于这个失败没有被处理，控制台还会显示未捕获的 <code>Promise</code> 错误。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const promise1 = new Promise((resolve, reject) =&gt; {\\n  setTimeout(() =&gt; {\\n    resolve(&#39;success&#39;)\\n  }, 1000)\\n})\\nconst promise2 = promise1.then(() =&gt; {\\n  throw new Error(&#39;error!!!&#39;)\\n})\\nconsole.log(&#39;promise1&#39;, promise1)\\nconsole.log(&#39;promise2&#39;, promise2)\\nsetTimeout(() =&gt; {\\n  console.log(&#39;promise1&#39;, promise1)\\n  console.log(&#39;promise2&#39;, promise2)\\n}, 2000)\\n</code></pre>\\n<ul>\\n<li>从上至下，先执行第一个new Promise中的函数，碰到setTimeout将它加入下一个宏任务列表</li>\\n<li>跳出new Promise，碰到promise1.then这个微任务，但其状态还是为pending，这里理解为先不执行</li>\\n<li>promise2是一个新的状态为pending的Promise</li>\\n<li>执行同步代码console.log(\'promise1\')，且打印出的promise1的状态为pending</li>\\n<li>执行同步代码console.log(\'promise2\')，且打印出的promise2的状态为pending</li>\\n<li>碰到第二个定时器，将其放入下一个宏任务列表</li>\\n<li>第一轮宏任务执行结束，并且没有微任务需要执行，因此执行第二轮宏任务</li>\\n<li>先执行第一个定时器里的内容，将promise1的状态改为resolved且保存结果并将之前的promise1.then推入微任务队列</li>\\n<li>该定时器中没有其它的同步代码可执行，因此执行本轮的微任务队列，也就是promise1.then，它抛出了一个错误，且将promise2的状态设置为了rejected</li>\\n<li>第一个定时器执行完毕，开始执行第二个定时器中的内容</li>\\n<li>打印出\'promise1\'，且此时promise1的状态为resolved</li>\\n<li>打印出\'promise2\'，且此时promise2的状态为rejected</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>&#39;promise1&#39; Promise{&lt;pending&gt;}\\n&#39;promise2&#39; Promise{&lt;pending&gt;}\\ntest5.html:102 Uncaught (in promise) Error: error!!! at test.html:102\\n&#39;promise1&#39; Promise{&lt;resolved&gt;: &quot;success&quot;}\\n&#39;promise2&#39; Promise{&lt;rejected&gt;: Error: error!!!}\\n</code></pre>\\n<h3 id=\\"_2-6-题目五\\"><a class=\\"header-anchor\\" href=\\"#_2-6-题目五\\">#</a> 2.6 题目五</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>Promise executor 先同步输出内容，此时两个 Promise 都是 <code>pending</code></li>\\n<li>第一个定时器 <code>resolve</code> 后安排 <code>then</code> 微任务，回调抛错使链上的新 Promise 变为 <code>rejected</code></li>\\n<li>第二个定时器执行时可以观察到第一个 Promise 已 fulfilled、第二个已 rejected</li>\\n</ul>\\n<p><strong>输出依次表现为执行器中的内容、两个 <code>pending</code> 状态、<code>timer1</code>、未处理的错误，最后是 <code>timer2</code> 和两个已经确定的状态。</strong> <code>Promise</code> 执行器是同步运行的，所以先打印 <code>promise1里的内容</code>，而两个定时器尚未触发时，<code>promise1</code> 和 <code>promise2</code> 都处于 <code>pending</code>。第一个定时器调用 <code>resolve(\'success\')</code> 后仍会继续同步打印 <code>timer1</code>，随后才执行 <code>then</code> 微任务；回调抛错会让 <code>promise2</code> 变成 <code>rejected</code>。第二个定时器执行时，便能看到 <code>promise1</code> 已成功、<code>promise2</code> 已失败。</p>\\n</section>\\n<p>如果你上面这道题搞懂了之后，我们就可以来做做这道了，你应该能很快就给出答案：</p>\\n<pre class=\\"language-js\\"><code>const promise1 = new Promise((resolve, reject) =&gt; {\\n  setTimeout(() =&gt; {\\n    resolve(&quot;success&quot;);\\n    console.log(&quot;timer1&quot;);\\n  }, 1000);\\n  console.log(&quot;promise1里的内容&quot;);\\n});\\nconst promise2 = promise1.then(() =&gt; {\\n  throw new Error(&quot;error!!!&quot;);\\n});\\nconsole.log(&quot;promise1&quot;, promise1);\\nconsole.log(&quot;promise2&quot;, promise2);\\nsetTimeout(() =&gt; {\\n  console.log(&quot;timer2&quot;);\\n  console.log(&quot;promise1&quot;, promise1);\\n  console.log(&quot;promise2&quot;, promise2);\\n}, 2000);\\n</code></pre>\\n<pre class=\\"language-js\\"><code>&#39;promise1里的内容&#39;\\n&#39;promise1&#39; Promise{&lt;pending&gt;}\\n&#39;promise2&#39; Promise{&lt;pending&gt;}\\n&#39;timer1&#39;\\ntest5.html:102 Uncaught (in promise) Error: error!!! at test.html:102\\n&#39;timer2&#39;\\n&#39;promise1&#39; Promise{&lt;resolved&gt;: &quot;success&quot;}\\n&#39;promise2&#39; Promise{&lt;rejected&gt;: Error: error!!!}\\n</code></pre>\\n<h2 id=\\"_3-promise中的then、catch、finally\\"><a class=\\"header-anchor\\" href=\\"#_3-promise中的then、catch、finally\\">#</a> 3. Promise中的then、catch、finally</h2>\\n<ul>\\n<li>Promise的状态一经改变就不能再改变</li>\\n<li>.then和.catch都会返回一个新的Promise</li>\\n<li>catch不管被连接到哪里，都能捕获上层的错误</li>\\n<li>在Promise中，返回任意一个非 promise 的值都会被包裹成 promise 对象，例如return 2会被包装为return Promise.resolve(2)</li>\\n<li>Promise 的 .then 或者 .catch 可以被调用多次, 当如果Promise内部的状态一经改变，并且有了一个值，那么后续每次调用.then或者.catch的时候都会直接拿到该值</li>\\n<li>.then 或者 .catch 中 return 一个 error 对象并不会抛出错误，所以不会被后续的 .catch 捕获</li>\\n<li>.then 或 .catch 返回的值不能是 promise 本身，否则会造成死循环</li>\\n<li>.then 或者 .catch 的参数期望是函数，传入非函数则会发生值穿透</li>\\n<li>.then方法是能接收两个参数的，第一个是处理成功的函数，第二个是处理失败的函数，再某些时候你可以认为catch是.then第二个参数的简便写法</li>\\n<li>.finally方法也是返回一个Promise，他在Promise结束的时候，无论结果为resolved还是rejected，都会执行里面的回调函数</li>\\n</ul>\\n<h3 id=\\"_3-1-题目一\\"><a class=\\"header-anchor\\" href=\\"#_3-1-题目一\\">#</a> 3.1 题目一</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>Promise 状态只能从 <code>pending</code> 改变一次，第一次 <code>resolve</code> 生效</li>\\n<li>后续 <code>reject</code> 和第二次 <code>resolve</code> 都会被忽略</li>\\n<li>因此执行成功分支并输出第一次传入的 <code>success1</code></li>\\n</ul>\\n<p><strong>最终只会进入 <code>then</code>，并输出第一次传给 <code>resolve</code> 的 <code>success1</code>。</strong> <code>Promise</code> 的状态只能从 <code>pending</code> 改变一次，第一次调用 <code>resolve(\'success1\')</code> 后，结果就已经确定。后续的 <code>reject(\'error\')</code> 和 <code>resolve(\'success2\')</code> 都会被忽略，因此不会进入 <code>catch</code>，也不会覆盖第一次的成功值。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const promise = new Promise((resolve, reject) =&gt; {\\n  resolve(&quot;success1&quot;);\\n  reject(&quot;error&quot;);\\n  resolve(&quot;success2&quot;);\\n});\\npromise\\n.then(res =&gt; {\\n    console.log(&quot;then: &quot;, res);\\n  }).catch(err =&gt; {\\n    console.log(&quot;catch: &quot;, err);\\n  })\\n</code></pre>\\n<pre class=\\"language-text\\"><code>&quot;then: success1&quot;\\n</code></pre>\\n<blockquote>\\n<p>构造函数中的 resolve 或 reject 只有第一次执行有效，多次调用没有任何作用 。验证了第一个结论，Promise的状态一经改变就不能再改变</p>\\n</blockquote>\\n<h3 id=\\"_3-2-题目二\\"><a class=\\"header-anchor\\" href=\\"#_3-2-题目二\\">#</a> 3.2 题目二</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>第一次 <code>reject</code> 决定原 Promise 状态，后续 <code>resolve</code> 无效</li>\\n<li>错误会沿没有拒绝处理器的 <code>then</code> 向后传播，直到被 <code>catch</code> 捕获</li>\\n<li><code>catch</code> 正常返回后链恢复 fulfilled，后续 <code>then</code> 收到 <code>undefined</code></li>\\n</ul>\\n<p><strong>第一次调用 <code>reject(&quot;error&quot;)</code> 已经把原 <code>Promise</code> 定为 <code>rejected</code>，后面的 <code>resolve(&quot;success2&quot;)</code> 不会再生效。</strong> 前两个 <code>then</code> 都没有提供拒绝处理函数，所以错误会沿着链继续向后传递，最终由 <code>catch</code> 捕获并输出 <code>error</code>。<code>catch</code> 执行完没有显式返回值，相当于正常返回 <code>undefined</code>，链的状态也随之恢复为 <code>fulfilled</code>。于是最后一个 <code>then</code> 仍会执行，并拿到 <code>undefined</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const promise = new Promise((resolve, reject) =&gt; {\\n  reject(&quot;error&quot;);\\n  resolve(&quot;success2&quot;);\\n});\\npromise\\n.then(res =&gt; {\\n    console.log(&quot;then: &quot;, res);\\n  }).then(res =&gt; {\\n    console.log(&quot;then: &quot;, res);\\n  }).catch(err =&gt; {\\n    console.log(&quot;catch: &quot;, err);\\n  }).then(res =&gt; {\\n    console.log(&quot;then: &quot;, res);\\n  })\\n\\n</code></pre>\\n<pre class=\\"language-text\\"><code>&quot;catch: &quot; &quot;error&quot;\\n&quot;then3: &quot; undefined\\n</code></pre>\\n<blockquote>\\n<p>验证了第三个结论，catch不管被连接到哪里，都能捕获上层的错误。</p>\\n</blockquote>\\n<h3 id=\\"_3-3-题目三\\"><a class=\\"header-anchor\\" href=\\"#_3-3-题目三\\">#</a> 3.3 题目三</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>每次 <code>then</code> 或 <code>catch</code> 都返回一个新的 Promise，链式调用不是 <code>return this</code></li>\\n<li>成功值 <code>1</code> 进入第一个 <code>then</code>，其返回值 <code>2</code> 会被包装为 fulfilled Promise</li>\\n<li>中间没有错误所以跳过 <code>catch</code>，最后一个 <code>then</code> 输出 <code>2</code></li>\\n</ul>\\n<p><strong>这段代码会依次输出 <code>1</code> 和 <code>2</code>，因为每次调用 <code>then</code> 或 <code>catch</code> 都会生成一个新的 <code>Promise</code>。</strong> <code>Promise.resolve(1)</code> 先把 <code>1</code> 交给第一个 <code>then</code>，该回调打印后返回普通值 <code>2</code>。这个返回值会被当成 <code>fulfilled</code> 结果继续向后传递，本质上等同于 <code>resolve(2)</code>。中间没有发生错误，因此 <code>catch</code> 会被跳过，最后的 <code>then</code> 接收到 <code>2</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>Promise.resolve(1)\\n  .then(res =&gt; {\\n    console.log(res);\\n    return 2;\\n  })\\n  .catch(err =&gt; {\\n    return 3;\\n  })\\n  .then(res =&gt; {\\n    console.log(res);\\n  });\\n</code></pre>\\n<pre class=\\"language-text\\"><code>1\\n2\\n</code></pre>\\n<ul>\\n<li>Promise可以链式调用，不过promise 每次调用 .then 或者 .catch 都会返回一个新的 promise，从而实现了链式调用, 它并不像一般我们任务的链式调用一样return this</li>\\n<li>上面的输出结果之所以依次打印出1和2，那是因为resolve(1)之后走的是第一个then方法，并没有走catch里，所以第二个then中的res得到的实际上是第一个then的返回值</li>\\n<li>且return 2会被包装成resolve(2)</li>\\n</ul>\\n<h3 id=\\"_3-4-题目四\\"><a class=\\"header-anchor\\" href=\\"#_3-4-题目四\\">#</a> 3.4 题目四</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Promise.reject(1)</code> 跳过成功回调并进入 <code>catch</code></li>\\n<li><code>catch</code> 输出拒绝原因 <code>1</code>，随后返回普通值 <code>3</code></li>\\n<li>普通返回值会让链恢复 fulfilled，后续 <code>then</code> 输出 <code>3</code></li>\\n</ul>\\n<p><strong>把开头改成 <code>Promise.reject(1)</code> 后，代码会依次输出 <code>1</code> 和 <code>3</code>。</strong> 初始状态是 <code>rejected</code>，所以成功分支的第一个 <code>then</code> 不会执行，拒绝原因 <code>1</code> 会直接传给后面的 <code>catch</code>。<code>catch</code> 打印 <code>1</code> 后返回普通值 <code>3</code>，这个正常返回会让后续链变为 <code>fulfilled</code>。于是最后一个 <code>then</code> 能够继续执行，并接收到 <code>catch</code> 返回的 <code>3</code>。</p>\\n</section>\\n<p>如果把3.3中的Promise.resolve(1)改为Promise.reject(1)又会怎么样呢？</p>\\n<pre class=\\"language-js\\"><code>Promise.reject(1)\\n  .then(res =&gt; {\\n    console.log(res);\\n    return 2;\\n  })\\n  .catch(err =&gt; {\\n    console.log(err);\\n    return 3\\n  })\\n  .then(res =&gt; {\\n    console.log(res);\\n  });\\n</code></pre>\\n<pre class=\\"language-text\\"><code>1\\n3\\n</code></pre>\\n<blockquote>\\n<p>结果打印的当然是 1 和 3啦，因为reject(1)此时走的就是catch，且第二个then中的res得到的就是catch中的返回值</p>\\n</blockquote>\\n<h3 id=\\"_3-5-题目五\\"><a class=\\"header-anchor\\" href=\\"#_3-5-题目五\\">#</a> 3.5 题目五</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>一个 Promise 可以注册多个 <code>then</code>，状态确定后所有对应回调都会执行</li>\\n<li>Promise executor 和定时器只运行一次，不会因为注册两个 <code>then</code> 重复执行</li>\\n<li>两个回调接收相同成功值，时间差只来自实际调度误差</li>\\n</ul>\\n<p><strong>同一个 <code>Promise</code> 可以注册多个 <code>then</code>，状态确定后，这些成功回调都会拿到相同的结果并执行。</strong> 这里构造函数和内部定时器只运行一次，所以只会打印一次 <code>timer</code>，不会因为写了两个 <code>then</code> 就重复发起任务。定时器完成后调用 <code>resolve(&quot;success&quot;)</code>，两个回调都会收到 <code>success</code>。日志中的 <code>1001</code>、<code>1002</code> 只是实际调度造成的时间差；执行足够接近时，也可能显示相同耗时。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const promise = new Promise((resolve, reject) =&gt; {\\n  setTimeout(() =&gt; {\\n    console.log(&#39;timer&#39;)\\n    resolve(&#39;success&#39;)\\n  }, 1000)\\n})\\nconst start = Date.now();\\npromise.then(res =&gt; {\\n  console.log(res, Date.now() - start)\\n})\\npromise.then(res =&gt; {\\n  console.log(res, Date.now() - start)\\n})\\n</code></pre>\\n<pre class=\\"language-text\\"><code>&#39;timer&#39;\\nsuccess 1001\\nsuccess 1002\\n</code></pre>\\n<p>当然，如果你足够快的话，也可能两个都是1001。\\nPromise 的 .then 或者 .catch 可以被调用多次，但这里 Promise 构造函数只执行一次。或者说 promise 内部状态一经改变，并且有了一个值，那么后续每次调用 .then 或者 .catch 都会直接拿到该值</p>\\n<h3 id=\\"_3-6-题目六\\"><a class=\\"header-anchor\\" href=\\"#_3-6-题目六\\">#</a> 3.6 题目六</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>return new Error()</code> 只是返回普通对象，不等于抛出错误</li>\\n<li>普通返回值会被包装成 fulfilled Promise，因此后续走 <code>then</code> 而不是 <code>catch</code></li>\\n<li>要进入拒绝链必须 <code>throw</code> 或返回 <code>Promise.reject</code></li>\\n</ul>\\n<p><strong><code>return new Error()</code> 只是在返回一个普通值，并不会让 <code>Promise</code> 进入拒绝状态。</strong> <code>then</code> 会把这个值按成功结果继续传递，本质上等价于返回 <code>Promise.resolve(new Error(\'error!!!\'))</code>，所以下一个 <code>then</code> 能拿到这个错误对象。想让后续进入拒绝链，需要使用 <code>throw new Error(\'error!!!\')</code>，或者返回 <code>Promise.reject(new Error(\'error!!!\'))</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>Promise.resolve().then(() =&gt; {\\n  return new Error(&#39;error!!!&#39;)\\n}).then(res =&gt; {\\n  console.log(&quot;then: &quot;, res)\\n}).catch(err =&gt; {\\n  console.log(&quot;catch: &quot;, err)\\n})\\n</code></pre>\\n<ul>\\n<li>你可能想到的是进入.catch然后被捕获了错误。</li>\\n<li>结果并不是这样的，它走的是.then里面：</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&quot;then: &quot; &quot;Error: error!!!&quot;\\n</code></pre>\\n<blockquote>\\n<p>这也验证了第4点和第6点，返回任意一个非 promise 的值都会被包裹成 promise 对象，因此这里的return new Error(\'error!!!\')也被包裹成了return Promise.resolve(new Error(\'error!!!\'))</p>\\n</blockquote>\\n<p>当然如果你抛出一个错误的话，可以用下面👇两的任意一种：</p>\\n<pre class=\\"language-js\\"><code>return Promise.reject(new Error(&#39;error!!!&#39;));\\n// or\\nthrow new Error(&#39;error!!!&#39;)\\n</code></pre>\\n<h3 id=\\"_3-7-题目七\\"><a class=\\"header-anchor\\" href=\\"#_3-7-题目七\\">#</a> 3.7 题目七</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>then</code> 回调不能返回由该次 <code>then</code> 创建的同一个 Promise</li>\\n<li>这种自引用无法完成解析，会形成 chaining cycle</li>\\n<li>规范要求拒绝该 Promise，并抛出 <code>TypeError</code></li>\\n</ul>\\n<p><strong><code>then</code> 的回调不能返回这次 <code>then</code> 创建出来的同一个 <code>Promise</code>，否则会产生链式循环。</strong> 这个例子里，<code>promise</code> 想完成就要等待回调结果，但回调又返回了 <code>promise</code> 自己，因此解析过程无法继续。遇到这种自引用，<code>Promise</code> 会转为拒绝状态，并产生 <code>TypeError</code>，常见提示是 <code>Chaining cycle detected for promise</code>。实际写链式调用时，应返回另一个值或另一个 <code>Promise</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const promise = Promise.resolve().then(() =&gt; {\\n  return promise;\\n})\\npromise.catch(console.err)\\n</code></pre>\\n<blockquote>\\n<p>.then 或 .catch 返回的值不能是 promise 本身，否则会造成死循环。</p>\\n</blockquote>\\n<p>因此结果会报错：</p>\\n<pre class=\\"language-js\\"><code>Uncaught (in promise) TypeError: Chaining cycle detected for promise #&lt;Promise&gt;\\n</code></pre>\\n<h3 id=\\"_3-8-题目八\\"><a class=\\"header-anchor\\" href=\\"#_3-8-题目八\\">#</a> 3.8 题目八</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>then</code> 的参数必须是函数，数字或 Promise 对象作为参数会被忽略</li>\\n<li>缺少有效处理器时发生值穿透，fulfilled 值沿链向后传递</li>\\n<li>最后的 <code>console.log</code> 因此仍然收到最初的 <code>1</code></li>\\n</ul>\\n<p><strong>这段代码最终输出 <code>1</code>，因为 <code>then</code> 接收的处理器必须是函数，传入其他类型会被忽略。</strong> 第一个 <code>then</code> 收到数字 <code>2</code>，第二个收到 <code>Promise.resolve(3)</code> 对象，它们都不是可执行的回调，因此发生值穿透。最初由 <code>Promise.resolve(1)</code> 产生的成功值会沿着链继续传递，最后交给 <code>console.log</code>。如果希望把结果改成 <code>3</code>，需要传入函数并在函数中返回对应值或 <code>Promise</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>Promise.resolve(1)\\n  .then(2)\\n  .then(Promise.resolve(3))\\n  .then(console.log)\\n</code></pre>\\n<ul>\\n<li>其实你只要记住原则8：.then 或者 .catch 的参数期望是函数，传入非函数则会发生值穿透。</li>\\n<li>第一个then和第二个then中传入的都不是函数，一个是数字类型，一个是对象类型，因此发生了穿透，将resolve(1) 的值直接传到最后一个then里。</li>\\n</ul>\\n<p>所以输出结果为：</p>\\n<pre class=\\"language-text\\"><code>1\\n</code></pre>\\n<h3 id=\\"_3-9-题目九\\"><a class=\\"header-anchor\\" href=\\"#_3-9-题目九\\">#</a> 3.9 题目九</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>下面来介绍一下.then函数中的两个参数</li>\\n<li>第一个参数是用来处理Promise成功的函数，第二个则是处理失败的函数</li>\\n<li>也就是说Promise.resolve(\'1\')的值会进入成功的函数，Promise.reject(\'2\')的值会进入失败的函数</li>\\n<li>但是有一个问题，如果是这个案例呢？</li>\\n</ul>\\n<p><strong><code>then</code> 的第一个参数处理成功结果，第二个参数处理当前 <code>Promise</code> 已有的拒绝结果。</strong> 因此 <code>Promise.reject(\'err!!!\')</code> 会进入第二个回调；如果没有提供这个回调，拒绝会继续向后传递，再由 <code>catch</code> 处理。边界在于，第一个成功回调内部新抛出的错误不会被同一次 <code>then</code> 的第二个参数捕获，因为两个参数是并列处理器。这个新错误会让 <code>then</code> 返回的 <code>Promise</code> 被拒绝，所以示例中最终执行的是后面的 <code>fail2</code>。</p>\\n</section>\\n<p>下面来介绍一下.then函数中的两个参数。</p>\\n<p>第一个参数是用来处理Promise成功的函数，第二个则是处理失败的函数。\\n也就是说Promise.resolve(\'1\')的值会进入成功的函数，Promise.reject(\'2\')的值会进入失败的函数。</p>\\n<p>让我们来看看这个例子</p>\\n<pre class=\\"language-js\\"><code>Promise.reject(&#39;err!!!&#39;)\\n  .then((res) =&gt; {\\n    console.log(&#39;success&#39;, res)\\n  }, (err) =&gt; {\\n    console.log(&#39;error&#39;, err)\\n  }).catch(err =&gt; {\\n    console.log(&#39;catch&#39;, err)\\n  })\\n</code></pre>\\n<p>这里的执行结果是：</p>\\n<pre class=\\"language-js\\"><code>&#39;error&#39; &#39;error!!!&#39;\\n</code></pre>\\n<blockquote>\\n<p>它进入的是then()中的第二个参数里面，而如果把第二个参数去掉，就进入了catch()中：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>Promise.reject(&#39;err!!!&#39;)\\n  .then((res) =&gt; {\\n    console.log(&#39;success&#39;, res)\\n  }).catch(err =&gt; {\\n    console.log(&#39;catch&#39;, err)\\n  })\\n</code></pre>\\n<p>执行结果：</p>\\n<pre class=\\"language-text\\"><code>&#39;catch&#39; &#39;error!!!&#39;\\n</code></pre>\\n<p>但是有一个问题，如果是这个案例呢？</p>\\n<pre class=\\"language-js\\"><code>Promise.resolve()\\n  .then(function success (res) {\\n    throw new Error(&#39;error!!!&#39;)\\n  }, function fail1 (err) {\\n    console.log(&#39;fail1&#39;, err)\\n  }).catch(function fail2 (err) {\\n    console.log(&#39;fail2&#39;, err)\\n  })\\n</code></pre>\\n<blockquote>\\n<p>由于Promise调用的是resolve()，因此.then()执行的应该是success()函数，可是success()函数抛出的是一个错误，它会被后面的catch()给捕获到，而不是被fail1函数捕获。</p>\\n</blockquote>\\n<p>因此执行结果为：</p>\\n<pre class=\\"language-text\\"><code>fail2 Error: error!!!\\n\\t\\t\\tat success\\n</code></pre>\\n<h3 id=\\"_3-10-题目十\\"><a class=\\"header-anchor\\" href=\\"#_3-10-题目十\\">#</a> 3.10 题目十</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>finally</code> 在 fulfilled 和 rejected 两条路径上都会执行</li>\\n<li><code>finally</code> 回调默认收不到前一个成功值或失败原因</li>\\n<li>除非 <code>finally</code> 自己抛错或返回 rejected Promise，否则原状态和值会继续向后传递</li>\\n</ul>\\n<p><strong><code>finally()</code> 无论前面的 <code>Promise</code> 成功还是失败，都会执行。</strong> 它的回调默认拿不到之前的成功值或失败原因，适合放清理资源、关闭加载状态这类不关心结果的操作。通常执行完后，原来的状态和值会继续向后传递；但如果回调主动抛错或返回 <code>rejected</code> <code>Promise</code>，后续链路就会改为处理这个新错误。</p>\\n</section>\\n<p>接着来看看.finally()，这个功能一般不太用在面试中，不过如果碰到了你也应该知道该如何处理。</p>\\n<pre class=\\"language-js\\"><code>function promise1 () {\\n  let p = new Promise((resolve) =&gt; {\\n    console.log(&#39;promise1&#39;);\\n    resolve(&#39;1&#39;)\\n  })\\n  return p;\\n}\\nfunction promise2 () {\\n  return new Promise((resolve, reject) =&gt; {\\n    reject(&#39;error&#39;)\\n  })\\n}\\npromise1()\\n  .then(res =&gt; console.log(res))\\n  .catch(err =&gt; console.log(err))\\n  .finally(() =&gt; console.log(&#39;finally1&#39;))\\n\\npromise2()\\n  .then(res =&gt; console.log(res))\\n  .catch(err =&gt; console.log(err))\\n  .finally(() =&gt; console.log(&#39;finally2&#39;))\\n\\n</code></pre>\\n<p>结果：</p>\\n<pre class=\\"language-js\\"><code>&#39;promise1&#39;\\n&#39;1&#39;\\n&#39;error&#39;\\n&#39;finally1&#39;\\n&#39;finally2&#39;\\n</code></pre>\\n<h2 id=\\"_4-promise中的all和race\\"><a class=\\"header-anchor\\" href=\\"#_4-promise中的all和race\\">#</a> 4. Promise中的all和race</h2>\\n<ul>\\n<li>在做下面👇的题目之前，让我们先来了解一下Promise.all()和Promise.race()的用法。</li>\\n<li>通俗来说，.all()的作用是接收一组异步任务，然后并行执行异步任务，并且在所有异步操作执行完后才执行回调。</li>\\n<li><code>.race()</code>的作用也是接收一组异步任务，然后并行执行异步任务，只保留取第一个执行完成的异步操作的结果，其他的方法仍在执行，不过执行结果会被抛弃。</li>\\n</ul>\\n<p>来看看题目一。</p>\\n<h3 id=\\"_4-1-题目一\\"><a class=\\"header-anchor\\" href=\\"#_4-1-题目一\\">#</a> 4.1 题目一</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>如果我用<code>.all()</code>来执行它会怎样呢？</li>\\n<li>先来想想此段代码在浏览器中会如何执行？</li>\\n<li>.all()后面的.then()里的回调函数接收的就是所有异步操作的结果</li>\\n<li>而且这个结果中数组的顺序和Promise.all()接收到的数组顺序一致！！！</li>\\n<li>所以你现在能理解这句话的意思了吗：有了all，你就可以并行执行多个异步操作，并且在一个回调中处理所有的返回数据</li>\\n</ul>\\n<p><strong><code>Promise.all()</code> 会同时等待这组异步任务完成，并把全部成功结果交给后面的 <code>then()</code>。</strong> 示例中的三个任务都会在约一秒后完成，所以控制台会输出 <code>1</code>、<code>2</code>、<code>3</code>，随后得到数组 <code>[1, 2, 3]</code>。结果数组严格按照传入任务的顺序排列，不取决于谁先完成。我一般会用它处理资源预加载，等图片和静态文件全部就绪后再初始化页面。</p>\\n</section>\\n<p>我们知道如果直接在脚本文件中定义一个Promise，它构造函数的第一个参数是会立即执行的，就像这样：</p>\\n<pre class=\\"language-text\\"><code>const p1 = new Promise(r =&gt; console.log(&#39;立即打印&#39;))\\n</code></pre>\\n<p>控制台中会立即打印出 “立即打印”。</p>\\n<p>因此为了控制它什么时候执行，我们可以用一个函数包裹着它，在需要它执行的时候，调用这个函数就可以了：</p>\\n<pre class=\\"language-js\\"><code>function runP1 () {\\n\\tconst p1 = new Promise(r =&gt; console.log(&#39;立即打印&#39;))\\n\\treturn p1\\n}\\n\\nrunP1() // 调用此函数时才执行\\n</code></pre>\\n<p>OK 👌， 让我们回归正题。\\n现在来构建这么一个函数：</p>\\n<pre class=\\"language-js\\"><code>function runAsync (x) {\\n\\tconst p = new Promise(r =&gt; setTimeout(() =&gt; r(x, console.log(x)), 1000))\\n\\treturn p\\n}\\n</code></pre>\\n<ul>\\n<li>该函数传入一个值x，然后间隔一秒后打印出这个x。</li>\\n<li>如果我用<code>.all()</code>来执行它会怎样呢？</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>function runAsync (x) {\\n\\tconst p = new Promise(r =&gt; setTimeout(() =&gt; r(x, console.log(x)), 1000))\\n\\treturn p\\n}\\nPromise.all([runAsync(1), runAsync(2), runAsync(3)])\\n  .then(res =&gt; console.log(res))\\n\\n</code></pre>\\n<ul>\\n<li>先来想想此段代码在浏览器中会如何执行？</li>\\n<li>没错，当你打开页面的时候，在间隔一秒后，控制台会同时打印出1, 2, 3，还有一个数组[1, 2, 3]。</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>1\\n2\\n3\\n[1, 2, 3]\\n</code></pre>\\n<blockquote>\\n<p>所以你现在能理解这句话的意思了吗：有了all，你就可以并行执行多个异步操作，并且在一个回调中处理所有的返回数据</p>\\n</blockquote>\\n<ul>\\n<li>.all()后面的.then()里的回调函数接收的就是所有异步操作的结果。</li>\\n<li>而且这个结果中数组的顺序和Promise.all()接收到的数组顺序一致！！！</li>\\n</ul>\\n<blockquote>\\n<p>有一个场景是很适合用这个的，一些游戏类的素材比较多的应用，打开网页时，预先加载需要用到的各种资源如图片、flash以及各种静态文件。所有的都加载完后，我们再进行页面的初始化。</p>\\n</blockquote>\\n<h3 id=\\"_4-2-题目二\\"><a class=\\"header-anchor\\" href=\\"#_4-2-题目二\\">#</a> 4.2 题目二</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>我新增了一个runReject函数，它用来在1000 * x秒后reject一个错误</li>\\n<li>同时.catch()函数能够捕获到.all()里最先的那个异常，并且只执行一次</li>\\n<li>想想这道题会怎样执行呢 🤔️？</li>\\n<li>没错，就像我之前说的，.catch是会捕获最先的那个异常，在这道题目中最先的异常就是runReject(2)的结果</li>\\n<li>另外，如果一组异步操作中有一个异常都不会进入.then()的第一个回调函数参数中</li>\\n</ul>\\n<p><strong>这段代码中，<code>Promise.all()</code> 会因 <code>runReject(2)</code> 最先失败而进入错误处理，得到 <code>Error: 2</code>。</strong> 只要其中一个任务失败，整体就不会进入 <code>then()</code> 的成功回调，而是由 <code>catch()</code> 或 <code>then()</code> 的第二个参数接收最先出现的异常。需要注意，其他任务并不会因此停止，所以四秒后 <code>runReject(4)</code> 仍会继续执行并打印 <code>4</code>，只是它的结果不再改变 <code>Promise.all()</code> 的状态。</p>\\n</section>\\n<p>我新增了一个runReject函数，它用来在1000 * x秒后reject一个错误。</p>\\n<p>同时.catch()函数能够捕获到.all()里最先的那个异常，并且只执行一次。</p>\\n<p>想想这道题会怎样执行呢 🤔️？</p>\\n<pre class=\\"language-js\\"><code>function runAsync (x) {\\n  const p = new Promise(r =&gt; setTimeout(() =&gt; r(x, console.log(x)), 1000))\\n  return p\\n}\\nfunction runReject (x) {\\n  const p = new Promise((res, rej) =&gt; setTimeout(() =&gt; rej(`Error: ${x}`, console.log(x)), 1000 * x))\\n  return p\\n}\\nPromise.all([runAsync(1), runReject(4), runAsync(3), runReject(2)])\\n  .then(res =&gt; console.log(res))\\n  .catch(err =&gt; console.log(err))\\n</code></pre>\\n<pre class=\\"language-js\\"><code>1\\n3\\n// 2s后输出\\n2\\nError: 2\\n// 4s后输出\\n4\\n</code></pre>\\n<p>没错，就像我之前说的，.catch是会捕获最先的那个异常，在这道题目中最先的异常就是runReject(2)的结果。</p>\\n<p>另外，如果一组异步操作中有一个异常都不会进入.then()的第一个回调函数参数中。</p>\\n<p>注意，为什么不说是不进入.then()中呢 🤔️？</p>\\n<p>哈哈，大家别忘了.then()方法的第二个参数也是可以捕获错误的：</p>\\n<pre class=\\"language-js\\"><code>Promise.all([runAsync(1), runReject(4), runAsync(3), runReject(2)])\\n  .then(res =&gt; console.log(res), \\n  err =&gt; console.log(err))\\n</code></pre>\\n<h3 id=\\"_4-3-题目三\\"><a class=\\"header-anchor\\" href=\\"#_4-3-题目三\\">#</a> 4.3 题目三</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>所以使用.race()方法，它只会获取最先执行完成的那个结果，其它的异步任务虽然也会继续进行下去，不过race已经不管那些任务的结果了</li>\\n<li>使用场景还是很多的，比如我们可以用race给某个异步请求设置超时时间，并且在超时后执行相应的操作</li>\\n<li>这个race有什么用呢？</li>\\n</ul>\\n<p><strong><code>Promise.race()</code> 只采用最先完成的那个任务的结果，无论它最终是成功还是失败。</strong> 示例里 <code>runAsync(1)</code> 最先完成，因此后续 <code>then()</code> 收到 <code>1</code>；另外两个任务仍会继续执行并打印 <code>2</code>、<code>3</code>，只是它们的结果已不会影响这次竞争。这个特性常用来给异步请求增加超时控制，让请求任务和超时任务竞争，并根据最先返回的结果继续处理。</p>\\n</section>\\n<p>所以使用.race()方法，它只会获取最先执行完成的那个结果，其它的异步任务虽然也会继续进行下去，不过race已经不管那些任务的结果了。</p>\\n<pre class=\\"language-js\\"><code>function runAsync (x) {\\n  const p = new Promise(r =&gt; setTimeout(() =&gt; r(x, console.log(x)), 1000))\\n  return p\\n}\\nPromise.race([runAsync(1), runAsync(2), runAsync(3)])\\n  .then(res =&gt; console.log(&#39;result: &#39;, res))\\n  .catch(err =&gt; console.log(err))\\n\\n</code></pre>\\n<pre class=\\"language-text\\"><code>1\\n&#39;result: &#39; 1\\n2\\n3\\n</code></pre>\\n<blockquote>\\n<p>这个race有什么用呢？使用场景还是很多的，比如我们可以用race给某个异步请求设置超时时间，并且在超时后执行相应的操作</p>\\n</blockquote>\\n<h3 id=\\"_4-4-题目四\\"><a class=\\"header-anchor\\" href=\\"#_4-4-题目四\\">#</a> 4.4 题目四</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>Promise.all()的作用是接收一组异步任务，然后并行执行异步任务，并且在所有异步操作执行完后才执行回调</li>\\n<li>.race()的作用也是接收一组异步任务，然后并行执行异步任务，只保留取第一个执行完成的异步操作的结果，其他的方法仍在执行，不过执行结果会被抛弃</li>\\n<li>Promise.all().then()结果中数组的顺序和Promise.all()接收到的数组顺序一致</li>\\n<li>好的，让我们来总结一下.then()和.race()吧，😄</li>\\n</ul>\\n<p><strong><code>Promise.all()</code>等待全部任务完成并按传入顺序返回结果，而<code>Promise.race()</code>只采用最先完成的任务结果。</strong> 两者接收任务后都会并行执行，区别在于结果如何收敛。即使<code>race()</code>已经得到结果，其余任务仍会继续执行，只是后续结果会被丢弃。最先完成的任务如果是拒绝状态，例如<code>runReject(0)</code>，最终就会进入<code>catch()</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>function runAsync(x) {\\n  const p = new Promise(r =&gt;\\n    setTimeout(() =&gt; r(x, console.log(x)), 1000)\\n  );\\n  return p;\\n}\\nfunction runReject(x) {\\n  const p = new Promise((res, rej) =&gt;\\n    setTimeout(() =&gt; rej(`Error: ${x}`, console.log(x)), 1000 * x)\\n  );\\n  return p;\\n}\\nPromise.race([runReject(0), runAsync(1), runAsync(2), runAsync(3)])\\n  .then(res =&gt; console.log(&quot;result: &quot;, res))\\n  .catch(err =&gt; console.log(err));\\n\\n</code></pre>\\n<blockquote>\\n<p>遇到错误的话，也是一样的，在这道题中，<code>runReject(0)</code>最先执行完，所以进入了<code>catch()</code>中：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>0\\n&#39;Error: 0&#39;\\n1\\n2\\n3\\n</code></pre>\\n<p><strong>总结</strong></p>\\n<blockquote>\\n<p>好的，让我们来总结一下.then()和.race()吧，😄</p>\\n</blockquote>\\n<ul>\\n<li>Promise.all()的作用是接收一组异步任务，然后并行执行异步任务，并且在所有异步操作执行完后才执行回调。</li>\\n<li>.race()的作用也是接收一组异步任务，然后并行执行异步任务，只保留取第一个执行完成的异步操作的结果，其他的方法仍在执行，不过执行结果会被抛弃。</li>\\n<li>Promise.all().then()结果中数组的顺序和Promise.all()接收到的数组顺序一致</li>\\n</ul>\\n<h2 id=\\"_5-async-await的几道题\\"><a class=\\"header-anchor\\" href=\\"#_5-async-await的几道题\\">#</a> 5. async/await的几道题</h2>\\n<blockquote>\\n<p>既然谈到了Promise，那就肯定得再说说async/await，在很多时候async和Promise的解法差不多，又有些不一样。不信你来看看题目一。</p>\\n</blockquote>\\n<h3 id=\\"_5-1-题目一\\"><a class=\\"header-anchor\\" href=\\"#_5-1-题目一\\">#</a> 5.1 题目一</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>执行函数中的同步代码async1 start，之后碰到了await，它会阻塞async1后面代码的执行，因此会先去执行async2中的同步代码async2，然后跳出async1</li>\\n<li>跳出async1函数后，执行同步代码start</li>\\n<li>在一轮宏任务全部执行完之后，再来执行刚刚await后面的内容async1 end</li>\\n<li>（在这里，你可以理解为await后面的内容就相当于放到了Promise.then的里面）</li>\\n<li>来看看区别，如果我们把await async2()换成一个new Promise呢？</li>\\n</ul>\\n<p><strong>输出顺序是<code>async1 start</code>、<code>async2</code>、<code>start</code>、<code>async1 end</code>。</strong> <code>async1()</code>先同步执行，遇到<code>await async2()</code>时会立即运行<code>async2()</code>中的同步代码，但暂停执行<code>await</code>后面的内容。当前宏任务里的同步代码结束后，<code>async1 end</code>才作为后续内容执行。对比来看，直接创建<code>new Promise()</code>不会阻塞后面的同步代码。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>async function async1() {\\n  console.log(&quot;async1 start&quot;);\\n  await async2();\\n  console.log(&quot;async1 end&quot;);\\n}\\nasync function async2() {\\n  console.log(&quot;async2&quot;);\\n}\\nasync1();\\nconsole.log(&#39;start&#39;)\\n</code></pre>\\n<p>这道基础题输出的是啥？</p>\\n<p>答案：</p>\\n<pre class=\\"language-text\\"><code>&#39;async1 start&#39;\\n&#39;async2&#39;\\n&#39;start&#39;\\n&#39;async1 end&#39;\\n</code></pre>\\n<ul>\\n<li>首先一进来是创建了两个函数的，我们先不看函数的创建位置，而是看它的调用位置\\n发现async1函数被调用了，然后去看看调用的内容</li>\\n<li>执行函数中的同步代码async1 start，之后碰到了await，它会阻塞async1后面代码的执行，因此会先去执行async2中的同步代码async2，然后跳出async1</li>\\n<li>跳出async1函数后，执行同步代码start</li>\\n<li>在一轮宏任务全部执行完之后，再来执行刚刚await后面的内容async1 end。</li>\\n</ul>\\n<p>（在这里，你可以理解为await后面的内容就相当于放到了Promise.then的里面）</p>\\n<p>来看看区别，如果我们把await async2()换成一个new Promise呢？</p>\\n<pre class=\\"language-js\\"><code>async function async1() {\\n  console.log(&quot;async1 start&quot;);\\n  new Promise(resolve =&gt; {\\n    console.log(&#39;promise&#39;)\\n  })\\n  console.log(&quot;async1 end&quot;);\\n}\\nasync1();\\nconsole.log(&quot;start&quot;)\\n</code></pre>\\n<p>此时的执行结果为：</p>\\n<pre class=\\"language-text\\"><code>&#39;async start&#39;\\n&#39;promise&#39;\\n&#39;async1 end&#39;\\n&#39;start&#39;\\n</code></pre>\\n<blockquote>\\n<p>可以看到new Promise()并不会阻塞后面的同步代码async1 end的执行。</p>\\n</blockquote>\\n<h3 id=\\"_5-2-题目二\\"><a class=\\"header-anchor\\" href=\\"#_5-2-题目二\\">#</a> 5.2 题目二</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>没错，定时器始终还是最后执行的，它被放到下一条宏任务的延迟队列中</li>\\n<li>现在将async结合定时器看看</li>\\n<li>给题目一中的 async2函数中加上一个定时器</li>\\n</ul>\\n<p><strong>输出顺序是<code>async1 start</code>、<code>async2</code>、<code>start</code>、<code>async1 end</code>、<code>timer</code>。</strong> <code>async2()</code>中的普通日志会同步执行，而<code>await</code>之后的<code>async1 end</code>要等当前同步代码执行完。<code>setTimeout(..., 0)</code>会被放入下一条宏任务的延迟队列，所以仍然排在最后。简单来说，延迟时间为<code>0</code>并不代表定时器会立刻执行。</p>\\n</section>\\n<ul>\\n<li>现在将async结合定时器看看。</li>\\n<li>给题目一中的 async2函数中加上一个定时器</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>async function async1() {\\n  console.log(&quot;async1 start&quot;);\\n  await async2();\\n  console.log(&quot;async1 end&quot;);\\n}\\nasync function async2() {\\n  setTimeout(() =&gt; {\\n    console.log(&#39;timer&#39;)\\n  }, 0)\\n  console.log(&quot;async2&quot;);\\n}\\nasync1();\\nconsole.log(&quot;start&quot;)\\n</code></pre>\\n<p>没错，定时器始终还是最后执行的，它被放到下一条宏任务的延迟队列中。</p>\\n<p>答案：</p>\\n<pre class=\\"language-text\\"><code>&#39;async1 start&#39;\\n&#39;async2&#39;\\n&#39;start&#39;\\n&#39;async1 end&#39;\\n&#39;timer&#39;\\n</code></pre>\\n<h3 id=\\"_5-3-题目三\\"><a class=\\"header-anchor\\" href=\\"#_5-3-题目三\\">#</a> 5.3 题目三</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>async</code> 函数先同步执行到第一个 <code>await</code>，<code>await</code> 之后进入微任务</li>\\n<li>定时器按注册顺序进入后续宏任务队列，本题先注册 <code>timer2</code>，再注册 <code>timer3</code> 和 <code>timer1</code></li>\\n<li>必须先清空当前宏任务产生的微任务，之后才依次执行定时器</li>\\n</ul>\\n<p><strong>最终顺序是<code>async1 start</code>、<code>async2</code>、<code>start</code>、<code>async1 end</code>、<code>timer2</code>、<code>timer3</code>、<code>timer1</code>。</strong> <code>async1()</code>先同步执行到<code>await</code>，它后面的代码进入微任务，因此会在当前宏任务结束后、定时器执行前输出<code>async1 end</code>。三个延迟均为<code>0</code>的定时器按注册顺序处理：先注册<code>timer2</code>，再注册<code>timer3</code>，而<code>timer1</code>要等<code>await</code>恢复后才会注册。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>async function async1() {\\n  console.log(&quot;async1 start&quot;);\\n  await async2();\\n  console.log(&quot;async1 end&quot;);\\n  setTimeout(() =&gt; {\\n    console.log(&#39;timer1&#39;)\\n  }, 0)\\n}\\nasync function async2() {\\n  setTimeout(() =&gt; {\\n    console.log(&#39;timer2&#39;)\\n  }, 0)\\n  console.log(&quot;async2&quot;);\\n}\\nasync1();\\nsetTimeout(() =&gt; {\\n  console.log(&#39;timer3&#39;)\\n}, 0)\\nconsole.log(&quot;start&quot;)\\n</code></pre>\\n<p>其实如果你能做到这里了，说明你前面的那些知识点也都掌握了，我就不需要太过详细的步骤分析了。</p>\\n<p>直接公布答案吧：</p>\\n<pre class=\\"language-js\\"><code>&#39;async1 start&#39;\\n&#39;async2&#39;\\n&#39;start&#39;\\n&#39;async1 end&#39;\\n&#39;timer2&#39;\\n&#39;timer3&#39;\\n&#39;timer1&#39;\\n</code></pre>\\n<blockquote>\\n<p>定时器谁先执行，你只需要关注谁先被调用的以及延迟时间是多少，这道题中延迟时间都是0，所以只要关注谁先被调用的。</p>\\n</blockquote>\\n<h3 id=\\"_5-4-题目四\\"><a class=\\"header-anchor\\" href=\\"#_5-4-题目四\\">#</a> 5.4 题目四</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>await</code> 后面不是 Promise 时会等价为 <code>Promise.resolve(value)</code></li>\\n<li><code>async</code> 函数的返回值始终会被包装成 Promise</li>\\n<li>因此普通值 <code>123</code> 最终通过 <code>then</code> 异步取得</li>\\n</ul>\\n<p><strong>当 <code>await</code> 后面是普通值时，可以把它理解为先经过 <code>Promise.resolve(value)</code>，再得到对应结果。</strong> 这里的 <code>123</code> 不会导致报错，<code>async</code> 函数会继续执行并返回这个值。由于 <code>async</code> 函数的返回结果始终会包装成 <code>Promise</code>，所以调用方需要通过 <code>then</code> 获取 <code>123</code>。实际使用时，即使 <code>return await 123</code>，最终拿到的结果也仍然是普通值 <code>123</code>。</p>\\n</section>\\n<ul>\\n<li>正常情况下，async中的await命令是一个Promise对象，返回该对象的结果。</li>\\n<li>但如果不是Promise对象的话，就会直接返回对应的值，相当于Promise.resolve()</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>async function fn () {\\n  // return await 1234\\n  // 等同于\\n  return 123\\n}\\nfn().then(res =&gt; console.log(res))\\n</code></pre>\\n<p>结果：</p>\\n<pre class=\\"language-text\\"><code>123\\n</code></pre>\\n<h3 id=\\"_5-5-题目五\\"><a class=\\"header-anchor\\" href=\\"#_5-5-题目五\\">#</a> 5.5 题目五</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>await</code> 等待的 Promise 没有调用 <code>resolve</code> 或 <code>reject</code>，会永久保持 <code>pending</code></li>\\n<li><code>await</code> 之前的同步日志正常执行，之后的函数体不会继续</li>\\n<li>外层 <code>async1().then</code> 也不会触发，因为 async 函数返回的 Promise 尚未完成</li>\\n</ul>\\n<p><strong><code>await</code> 等待的 <code>Promise</code> 如果始终没有调用 <code>resolve</code> 或 <code>reject</code>，它就会一直停在 <code>pending</code> 状态。</strong> 因此 <code>async1 start</code> 和 <code>promise1</code> 会同步输出，但程序执行到 <code>await</code> 后无法继续恢复。外层代码不受阻塞，仍会输出 <code>script end</code>；不过 <code>async1 success</code> 不会出现。相应地，<code>async1()</code> 返回的 <code>Promise</code> 也没有完成，所以后面的 <code>then</code> 不会执行。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>async function async1 () {\\n  console.log(&#39;async1 start&#39;);\\n  await new Promise(resolve =&gt; {\\n    console.log(&#39;promise1&#39;)\\n  })\\n  console.log(&#39;async1 success&#39;);\\n  return &#39;async1 end&#39;\\n}\\nconsole.log(&#39;srcipt start&#39;)\\nasync1().then(res =&gt; console.log(res))\\nconsole.log(&#39;srcipt end&#39;)\\n</code></pre>\\n<blockquote>\\n<p>在async1中await后面的Promise是没有返回值的，也就是它的状态始终是pending状态，因此相当于一直在await，await，await却始终没有响应...</p>\\n</blockquote>\\n<p>所以在await之后的内容是不会执行的，也包括async1后面的 .then。</p>\\n<pre class=\\"language-js\\"><code>&#39;script start&#39;\\n&#39;async1 start&#39;\\n&#39;promise1&#39;\\n&#39;script end&#39;\\n</code></pre>\\n<h3 id=\\"_5-6-题目六\\"><a class=\\"header-anchor\\" href=\\"#_5-6-题目六\\">#</a> 5.6 题目六</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>executor 同步输出 <code>promise1</code>，调用 <code>resolve</code> 后安排其 <code>then</code> 微任务</li>\\n<li><code>await</code> 等待的是链式 <code>then</code> 返回的新 Promise</li>\\n<li>链完成后恢复 async 函数，依次输出成功日志和最终返回值</li>\\n</ul>\\n<p><strong>给内部 <code>Promise</code> 调用 <code>resolve</code> 后，<code>await</code> 后面的代码就能在这条异步链完成后继续执行。</strong> <code>Promise</code> 的执行函数会同步打印 <code>promise1</code>，而 <code>resolve</code> 触发的 <code>then</code> 会稍后输出 <code>promise1 resolve</code>。这里 <code>await</code> 实际等待的是链式 <code>then</code> 返回的新 <code>Promise</code>，所以要等该回调完成，才会打印 <code>async1 success</code>。最后 <code>async1</code> 返回 <code>async1 end</code>，外层 <code>then</code> 再拿到并输出它。</p>\\n</section>\\n<p>让我们给5.5中的Promise加上resolve：</p>\\n<pre class=\\"language-js\\"><code>async function async1 () {\\n  console.log(&#39;async1 start&#39;);\\n  await new Promise(resolve =&gt; {\\n    console.log(&#39;promise1&#39;)\\n    resolve(&#39;promise1 resolve&#39;)\\n  }).then(res =&gt; console.log(res))\\n  console.log(&#39;async1 success&#39;);\\n  return &#39;async1 end&#39;\\n}\\nconsole.log(&#39;srcipt start&#39;)\\nasync1().then(res =&gt; console.log(res))\\nconsole.log(&#39;srcipt end&#39;)\\n</code></pre>\\n<p>现在Promise有了返回值了，因此await后面的内容将会被执行：</p>\\n<pre class=\\"language-text\\"><code>&#39;script start&#39;\\n&#39;async1 start&#39;\\n&#39;promise1&#39;\\n&#39;script end&#39;\\n&#39;promise1 resolve&#39;\\n&#39;async1 success&#39;\\n&#39;async1 end&#39;\\n</code></pre>\\n<h3 id=\\"_5-7-题目七\\"><a class=\\"header-anchor\\" href=\\"#_5-7-题目七\\">#</a> 5.7 题目七</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>await</code> 接收内部 Promise 的成功值，但 async 函数最终值由自己的 <code>return</code> 决定</li>\\n<li>其他 Promise executor 仍同步执行，定时器进入后续宏任务</li>\\n<li><code>async1().then</code> 收到的是 <code>async1 end</code>，不是内部 <code>resolve</code> 的文本</li>\\n</ul>\\n<p><strong>内部 <code>Promise</code> 的 <code>resolve(\'promise resolve\')</code> 只负责让 <code>await</code> 获得成功结果，并不会直接决定 <code>async1().then</code> 收到什么。</strong> 外层 <code>then</code> 最终拿到的是 <code>async1</code> 自己 <code>return</code> 的 <code>async1 end</code>。另一个 <code>Promise</code> 的执行函数会同步打印 <code>promise2</code>，而其中的 <code>setTimeout</code> 要等后续任务再执行。简单来说，先看到同步日志，再恢复 <code>async1</code> 输出成功信息和返回值，最后才打印 <code>timer</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>async function async1 () {\\n  console.log(&#39;async1 start&#39;);\\n  await new Promise(resolve =&gt; {\\n    console.log(&#39;promise1&#39;)\\n    resolve(&#39;promise resolve&#39;)\\n  })\\n  console.log(&#39;async1 success&#39;);\\n  return &#39;async1 end&#39;\\n}\\nconsole.log(&#39;srcipt start&#39;)\\nasync1().then(res =&gt; {\\n  console.log(res)\\n})\\nnew Promise(resolve =&gt; {\\n  console.log(&#39;promise2&#39;)\\n  setTimeout(() =&gt; {\\n    console.log(&#39;timer&#39;)\\n  })\\n})\\n</code></pre>\\n<p>这道题应该也不难，不过有一点需要注意的，在async1中的new Promise它的resovle的值和async1().then()里的值是没有关系的，很多小伙伴可能看到resovle(\'promise resolve\')就会误以为是async1().then()中的返回值。</p>\\n<p>因此这里的执行结果为：</p>\\n<pre class=\\"language-js\\"><code>&#39;script start&#39;\\n&#39;async1 start&#39;\\n&#39;promise1&#39;\\n&#39;promise2&#39;\\n&#39;async1 success&#39;\\n&#39;sync1 end&#39;\\n&#39;timer&#39;\\n</code></pre>\\n<h3 id=\\"_5-8-题目八\\"><a class=\\"header-anchor\\" href=\\"#_5-8-题目八\\">#</a> 5.8 题目八</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>同步阶段依次输出 script start、async1 start、async2、promise1 和 script end</li>\\n<li><code>await async2()</code> 的续体与显式 <code>Promise.then</code> 都属于微任务</li>\\n<li>当前环境下 async1 的续体先入队，所以先输出 async1 end，再输出 promise2，最后执行定时器</li>\\n</ul>\\n<p><strong>输出顺序是 <code>script start</code>、<code>async1 start</code>、<code>async2</code>、<code>promise1</code>、<code>script end</code>、<code>async1 end</code>、<code>promise2</code>，最后是 <code>setTimeout</code>。</strong> 前五项都在同步阶段完成，<code>await async2()</code> 后面的代码和显式的 <code>then</code> 回调会进入微任务队列。当前题目所用环境中，<code>async1</code> 的续体更早入队，所以它先于 <code>promise2</code> 执行；定时器属于后续任务，因此最后输出。</p>\\n</section>\\n<p>我们再来看一道头条曾经的面试题：</p>\\n<pre class=\\"language-js\\"><code>async function async1() {\\n  console.log(&quot;async1 start&quot;);\\n  await async2();\\n  console.log(&quot;async1 end&quot;);\\n}\\n\\nasync function async2() {\\n  console.log(&quot;async2&quot;);\\n}\\n\\nconsole.log(&quot;script start&quot;);\\n\\nsetTimeout(function() {\\n  console.log(&quot;setTimeout&quot;);\\n}, 0);\\n\\nasync1();\\n\\nnew Promise(function(resolve) {\\n  console.log(&quot;promise1&quot;);\\n  resolve();\\n}).then(function() {\\n  console.log(&quot;promise2&quot;);\\n});\\nconsole.log(&#39;script end&#39;)\\n</code></pre>\\n<pre class=\\"language-text\\"><code>&#39;script start&#39;\\n&#39;async1 start&#39;\\n&#39;async2&#39;\\n&#39;promise1&#39;\\n&#39;script end&#39;\\n&#39;async1 end&#39;\\n&#39;promise2&#39;\\n&#39;setTimeout&#39;\\n</code></pre>\\n<p>(这道题最后async1 end和promise2的顺序其实在网上饱受争议，我这里使用浏览器Chrome V80，Node v12.16.1的执行结果都是上面这个答案)</p>\\n<h3 id=\\"_5-9-题目九\\"><a class=\\"header-anchor\\" href=\\"#_5-9-题目九\\">#</a> 5.9 题目九</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>每个 <code>await</code> 都把后续代码切成微任务，多个 await 会分多轮恢复</li>\\n<li>async 函数返回普通值或 <code>Promise.resolve</code> 最终都会得到 Promise</li>\\n<li>推演时先列完整同步输出，再按微任务入队顺序逐个执行</li>\\n</ul>\\n<p><strong>最终依次输出 <code>test start...</code>、<code>执行testSometing</code>、<code>promise start...</code>、<code>test end...</code>、<code>testSometing</code>、<code>执行testAsync</code>、<code>promise</code>、<code>hello async</code>，最后输出两个结果。</strong> 本质上，每遇到一个 <code>await</code>，当前函数都会暂停，并把后续执行放到微任务中恢复。同步代码全部跑完后，再按照微任务的入队顺序处理，所以 <code>promise</code> 会夹在两次 <code>await</code> 的恢复过程之间。无论 <code>async</code> 返回普通值还是 <code>Promise.resolve</code>，调用方最终拿到的都是 <code>Promise</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>async function testSometing() {\\n  console.log(&quot;执行testSometing&quot;);\\n  return &quot;testSometing&quot;;\\n}\\n\\nasync function testAsync() {\\n  console.log(&quot;执行testAsync&quot;);\\n  return Promise.resolve(&quot;hello async&quot;);\\n}\\n\\nasync function test() {\\n  console.log(&quot;test start...&quot;);\\n  const v1 = await testSometing();\\n  console.log(v1);\\n  const v2 = await testAsync();\\n  console.log(v2);\\n  console.log(v1, v2);\\n}\\n\\ntest();\\n\\nvar promise = new Promise(resolve =&gt; {\\n  console.log(&quot;promise start...&quot;);\\n  resolve(&quot;promise&quot;);\\n});\\npromise.then(val =&gt; console.log(val));\\n\\nconsole.log(&quot;test end...&quot;);\\n</code></pre>\\n<p>答案：</p>\\n<pre class=\\"language-js\\"><code>&#39;test start...&#39;\\n&#39;执行testSometing&#39;\\n&#39;promise start...&#39;\\n&#39;test end...&#39;\\n&#39;testSometing&#39;\\n&#39;执行testAsync&#39;\\n&#39;promise&#39;\\n&#39;hello async&#39;\\n&#39;testSometing&#39; &#39;hello async&#39;\\n</code></pre>\\n<h2 id=\\"_6-async处理错误\\"><a class=\\"header-anchor\\" href=\\"#_6-async处理错误\\">#</a> 6. async处理错误</h2>\\n<h3 id=\\"_6-1-题目一\\"><a class=\\"header-anchor\\" href=\\"#_6-1-题目一\\">#</a> 6.1 题目一</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>在async中，如果 await后面的内容是一个异常或者错误的话，会怎样呢？</li>\\n<li>例如这道题中，await后面跟着的是一个状态为rejected的promise</li>\\n<li>如果在async函数中抛出了错误，则终止错误结果，不会继续向下执行</li>\\n</ul>\\n<p><strong><code>await</code> 遇到 <code>rejected</code> <code>Promise</code> 时，会像函数内部抛出异常一样中断当前 <code>async</code> 函数，后面的代码不会继续执行。</strong> 这道题先同步输出 <code>async2</code>，随后拒绝原因 <code>error</code> 会让 <code>async1()</code> 返回的 <code>Promise</code> 也变成 <code>rejected</code>。由于这里只注册了成功回调，没有处理拒绝，因此会看到 <code>Uncaught (in promise) error</code>。直接在 <code>async</code> 函数里执行 <code>throw new Error(...)</code>，结果也是函数提前结束并返回 <code>rejected</code> <code>Promise</code>。</p>\\n</section>\\n<blockquote>\\n<p>在async中，如果 await后面的内容是一个异常或者错误的话，会怎样呢？</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>async function async1 () {\\n  await async2();\\n  console.log(&#39;async1&#39;);\\n  return &#39;async1 success&#39;\\n}\\nasync function async2 () {\\n  return new Promise((resolve, reject) =&gt; {\\n    console.log(&#39;async2&#39;)\\n    reject(&#39;error&#39;)\\n  })\\n}\\nasync1().then(res =&gt; console.log(res))\\n</code></pre>\\n<p>例如这道题中，await后面跟着的是一个状态为rejected的promise。</p>\\n<p>如果在async函数中抛出了错误，则终止错误结果，不会继续向下执行。</p>\\n<p>所以答案为：</p>\\n<pre class=\\"language-text\\"><code>&#39;async2&#39;\\nUncaught (in promise) error\\n</code></pre>\\n<blockquote>\\n<p>如果改为throw new Error也是一样的：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>async function async1 () {\\n  console.log(&#39;async1&#39;);\\n  throw new Error(&#39;error!!!&#39;)\\n  return &#39;async1 success&#39;\\n}\\nasync1().then(res =&gt; console.log(res))\\n</code></pre>\\n<p>结果为：</p>\\n<pre class=\\"language-text\\"><code>&#39;async1&#39;\\nUncaught (in promise) Error: error!!!\\n</code></pre>\\n<h3 id=\\"_6-2-题目二\\"><a class=\\"header-anchor\\" href=\\"#_6-2-题目二\\">#</a> 6.2 题目二</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>await</code> 的 rejected Promise 会像同步异常一样中断当前 async 函数</li>\\n<li>用 <code>try...catch</code> 捕获后，函数可以继续执行后续语句</li>\\n<li>也可以在被等待的 Promise 上先接 <code>catch</code>，把拒绝转换为已处理结果</li>\\n</ul>\\n<p><strong>想让 <code>rejected</code> <code>Promise</code> 不影响 <code>async</code> 函数后续执行，可以用 <code>try...catch</code> 捕获 <code>await</code> 抛出的异常。</strong> 题目中同步代码先输出 <code>script start</code>，微任务恢复后依次输出 <code>error!!!</code>、<code>async1</code> 和 <code>async1 success</code>。也可以直接给 <code>Promise.reject(...)</code> 接一个 <code>catch</code>，先把拒绝处理掉，再让 <code>await</code> 继续往下走。两种写法都能达到相同效果，区别只是错误处理放在 <code>await</code> 外层还是被等待的 <code>Promise</code> 上。</p>\\n</section>\\n<blockquote>\\n<p>如果想要使得错误的地方不影响async函数后续的执行的话，可以使用try catch</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>async function async1 () {\\n  try {\\n    await Promise.reject(&#39;error!!!&#39;)\\n  } catch(e) {\\n    console.log(e)\\n  }\\n  console.log(&#39;async1&#39;);\\n  return Promise.resolve(&#39;async1 success&#39;)\\n}\\nasync1().then(res =&gt; console.log(res))\\nconsole.log(&#39;script start&#39;)\\n</code></pre>\\n<p>这里的结果为：</p>\\n<pre class=\\"language-text\\"><code>&#39;script start&#39;\\n&#39;error!!!&#39;\\n&#39;async1&#39;\\n&#39;async1 success&#39;\\n</code></pre>\\n<blockquote>\\n<p>或者你可以直接在Promise.reject后面跟着一个catch()方法：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>async function async1 () {\\n  // try {\\n  //   await Promise.reject(&#39;error!!!&#39;)\\n  // } catch(e) {\\n  //   console.log(e)\\n  // }\\n  await Promise.reject(&#39;error!!!&#39;)\\n    .catch(e =&gt; console.log(e))\\n  console.log(&#39;async1&#39;);\\n  return Promise.resolve(&#39;async1 success&#39;)\\n}\\nasync1().then(res =&gt; console.log(res))\\nconsole.log(&#39;script start&#39;)\\n</code></pre>\\n<h2 id=\\"_7-综合题\\"><a class=\\"header-anchor\\" href=\\"#_7-综合题\\">#</a> 7. 综合题</h2>\\n<h3 id=\\"_7-1-题目一\\"><a class=\\"header-anchor\\" href=\\"#_7-1-题目一\\">#</a> 7.1 题目一</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>第一段代码定义的是一个函数，所以我们得看看它是在哪执行的，发现它在4之前，所以可以来看看first函数里面的内容了。(这一步有点类似于题目1.5)</li>\\n<li>函数first返回的是一个new Promise()，因此先执行里面的同步代码3</li>\\n<li>接着又遇到了一个new Promise()，直接执行里面的同步代码7</li>\\n<li>执行完7之后，在p中，遇到了一个定时器，先将它放到下一个宏任务队列里不管它，接着向下走</li>\\n<li>碰到了resolve(1)，这里就把p的状态改为了resolved，且返回值为1，不过这里也先不执行</li>\\n</ul>\\n<p><strong>这段代码依次输出 <code>3、7、4、1、2、5</code>，最后打印状态为 <code>fulfilled</code>、值为 <code>1</code> 的 <code>p</code>。</strong> 调用 <code>first()</code> 时会立即执行两个 <code>Promise</code> 构造器里的同步代码，所以先看到 <code>3</code> 和 <code>7</code>，随后定时器进入后续宏任务，外层同步代码输出 <code>4</code>。两个 <code>then</code> 回调按注册顺序进入微任务队列，因此接着输出 <code>1</code> 和 <code>2</code>。定时器最后执行，虽然调用了 <code>resolve(6)</code>，但 <code>p</code> 已被 <code>resolve(1)</code> 确定状态，这次调用不会再改变结果。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const first = () =&gt; (new Promise((resolve, reject) =&gt; {\\n    console.log(3);\\n    let p = new Promise((resolve, reject) =&gt; {\\n        console.log(7);\\n        setTimeout(() =&gt; {\\n            console.log(5);\\n            resolve(6);\\n          \\tconsole.log(p)\\n        }, 0)\\n        resolve(1);\\n    });\\n    resolve(2);\\n    p.then((arg) =&gt; {\\n        console.log(arg);\\n    });\\n\\n}));\\n\\nfirst().then((arg) =&gt; {\\n    console.log(arg);\\n});\\nconsole.log(4);\\n</code></pre>\\n<ul>\\n<li>第一段代码定义的是一个函数，所以我们得看看它是在哪执行的，发现它在4之前，所以可以来看看first函数里面的内容了。(这一步有点类似于题目1.5)</li>\\n<li>函数first返回的是一个new Promise()，因此先执行里面的同步代码3</li>\\n<li>接着又遇到了一个new Promise()，直接执行里面的同步代码7</li>\\n<li>执行完7之后，在p中，遇到了一个定时器，先将它放到下一个宏任务队列里不管它，接着向下走</li>\\n<li>碰到了resolve(1)，这里就把p的状态改为了resolved，且返回值为1，不过这里也先不执行</li>\\n<li>跳出p，碰到了resolve(2)，这里的resolve(2)，表示的是把first函数返回的那个Promise的状态改了，也先不管它。</li>\\n<li>然后碰到了p.then，将它加入本次循环的微任务列表，等待执行</li>\\n<li>跳出first函数，遇到了first().then()，将它加入本次循环的微任务列表(p.then的后面执行)</li>\\n<li>然后执行同步代码4</li>\\n<li>本轮的同步代码全部执行完毕，查找微任务列表，发现p.then和first().then()，依次执行，打印出1和2</li>\\n<li>本轮任务执行完毕了，发现还有一个定时器没有跑完，接着执行这个定时器里的内容，执行同步代码5</li>\\n<li>然后又遇到了一个<code>resolve(6)</code>，它是放在p里的，但是p的状态在之前已经发生过改变了，因此这里就不会再改变，也就是说<code>resolve(6)</code>相当于没任何用处，因此打印出来的p为<code>Promise{&lt;resolved&gt;: 1}</code>。(这一步类似于题目3.1)</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>3\\n7\\n4\\n1\\n2\\n5\\nPromise{&lt;resolved&gt;: 1}\\n</code></pre>\\n<h3 id=\\"_7-2-题目二\\"><a class=\\"header-anchor\\" href=\\"#_7-2-题目二\\">#</a> 7.2 题目二</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>未完成的 <code>await new Promise</code> 会让 async 函数永久暂停，因此不会输出 async1 end</li>\\n<li>传给 <code>then</code> 的非函数参数发生值穿透，最终仍输出原值 <code>1</code></li>\\n<li>两个定时器按延迟时间执行，<code>1s</code> 的 timer2 早于 <code>2s</code> 的 timer1</li>\\n</ul>\\n<p><strong>最终依次输出 <code>script start、async1、promise1、script end、1、timer2、timer1</code>，不会出现 <code>async1 end</code>。</strong> <code>await</code> 等待的 <code>Promise</code> 从未调用 <code>resolve</code>，所以 <code>async1</code> 会一直暂停，后面的日志和返回值都不会产生。<code>then</code> 接收的 <code>2</code> 和 <code>Promise</code> 对象都不是函数，会发生值穿透，因此链尾仍然拿到 <code>1</code>。两个定时器互不等待，延迟 <code>1000ms</code> 的 <code>timer2</code> 会早于延迟 <code>2000ms</code> 的 <code>timer1</code>。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const async1 = async () =&gt; {\\n  console.log(&#39;async1&#39;);\\n  setTimeout(() =&gt; {\\n    console.log(&#39;timer1&#39;)\\n  }, 2000)\\n  await new Promise(resolve =&gt; {\\n    console.log(&#39;promise1&#39;)\\n  })\\n  console.log(&#39;async1 end&#39;)\\n  return &#39;async1 success&#39;\\n} \\nconsole.log(&#39;script start&#39;);\\nasync1().then(res =&gt; console.log(res));\\nconsole.log(&#39;script end&#39;);\\nPromise.resolve(1)\\n  .then(2)\\n  .then(Promise.resolve(3))\\n  .catch(4)\\n  .then(res =&gt; console.log(res))\\nsetTimeout(() =&gt; {\\n  console.log(&#39;timer2&#39;)\\n}, 1000)\\n</code></pre>\\n<p><strong>注意的知识点：</strong></p>\\n<ul>\\n<li>async函数中await的new Promise要是没有返回值的话则不执行后面的内容(类似题5.5)</li>\\n<li>.then函数中的参数期待的是函数，如果不是函数的话会发生穿透(类似题3.8 )</li>\\n<li>注意定时器的延迟时间</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&#39;script start&#39;\\n&#39;async1&#39;\\n&#39;promise1&#39;\\n&#39;script end&#39;\\n1\\n&#39;timer2&#39;\\n&#39;timer1&#39;\\n</code></pre>\\n<h3 id=\\"_7-3-题目三\\"><a class=\\"header-anchor\\" href=\\"#_7-3-题目三\\">#</a> 7.3 题目三</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>同一个 Promise 多次 <code>resolve</code> 只有第一次有效，所以保持 <code>resolve1</code></li>\\n<li><code>then</code> 没有显式返回值，新 Promise 会以 <code>undefined</code> fulfilled</li>\\n<li><code>finally</code> 收不到链上的值，定时器日志属于后续宏任务</li>\\n</ul>\\n<p><strong>这段代码依次输出 <code>resolve1</code>、<code>finally undefined</code>、<code>timer1</code>，之后打印 <code>fulfilled</code> 且值为 <code>undefined</code> 的 <code>p1</code>。</strong> <code>Promise</code> 状态只能确定一次，首次 <code>resolve(\'resovle1\')</code> 生效后，后续的 <code>resolve(\'resolve2\')</code> 和定时器中的 <code>resolve(\'resolve3\')</code> 都不会改变结果。<code>then</code> 回调没有显式返回值，所以它生成的新 <code>Promise</code> 会以 <code>undefined</code> 完成，而 <code>finally</code> 回调本身也接收不到链上的结果。定时器属于后续宏任务，因此 <code>timer1</code> 会在微任务中的 <code>then</code> 和 <code>finally</code> 执行完之后输出。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>const p1 = new Promise((resolve) =&gt; {\\n  setTimeout(() =&gt; {\\n    resolve(&#39;resolve3&#39;);\\n    console.log(&#39;timer1&#39;)\\n  }, 0)\\n  resolve(&#39;resovle1&#39;);\\n  resolve(&#39;resolve2&#39;);\\n}).then(res =&gt; {\\n  console.log(res)\\n  setTimeout(() =&gt; {\\n    console.log(p1)\\n  }, 1000)\\n}).finally(res =&gt; {\\n  console.log(&#39;finally&#39;, res)\\n})\\n</code></pre>\\n<p><strong>注意的知识点：</strong></p>\\n<ul>\\n<li>Promise的状态一旦改变就无法改变(类似题目3.5)</li>\\n<li>finally不管Promise的状态是resolved还是rejected都会执行，且它的回调函数是没有参数的(类似3.10)</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&#39;resolve1&#39;\\n&#39;finally&#39; undefined\\n&#39;timer1&#39;\\nPromise{&lt;resolved&gt;: undefined}\\n</code></pre>\\n<h2 id=\\"_8-几道大厂的面试题\\"><a class=\\"header-anchor\\" href=\\"#_8-几道大厂的面试题\\">#</a> 8. 几道大厂的面试题</h2>\\n<h3 id=\\"_8-1-使用promise实现每隔1秒输出1-2-3\\"><a class=\\"header-anchor\\" href=\\"#_8-1-使用promise实现每隔1秒输出1-2-3\\">#</a> 8.1 使用Promise实现每隔1秒输出1,2,3</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>用 <code>reduce</code> 从 <code>Promise.resolve()</code> 开始串联每个延时任务</li>\\n<li>每个 <code>then</code> 必须返回新的延时 Promise，下一项才会等待上一项完成</li>\\n<li>串行链保证 <code>1、2、3</code> 各间隔约一秒输出，而不是同时启动三个定时器</li>\\n</ul>\\n<p><strong>可以用 <code>reduce</code> 从 <code>Promise.resolve()</code> 开始串联任务，让 <code>1、2、3</code> 每隔约一秒依次输出。</strong> 每次 <code>then</code> 都返回一个新的 <code>Promise</code>，并在 <code>setTimeout</code> 到期后完成，这样下一轮才会继续执行。本质上是把三个延时任务连成串行链；如果只是循环创建三个定时器，它们会几乎同时开始计时，无法形成逐项间隔。实际编写时要保留 <code>return</code>，否则 <code>Promise</code> 链不会等待当前定时任务结束。</p>\\n</section>\\n<blockquote>\\n<p>这道题比较简单的一种做法是可以用Promise配合着reduce不停的在promise后面叠加.then，请看下面的代码：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const arr = [1, 2, 3]\\narr.reduce((p, x) =&gt; {\\n  return p.then(() =&gt; {\\n    return new Promise(r =&gt; {\\n      setTimeout(() =&gt; r(console.log(x)), 1000)\\n    })\\n  })\\n}, Promise.resolve())\\n</code></pre>\\n<p>或者你可以更简单一点写：</p>\\n<pre class=\\"language-text\\"><code>const arr = [1, 2, 3]\\narr.reduce((p, x) =&gt; p.then(() =&gt; new Promise(r =&gt; setTimeout(() =&gt; r(console.log(x)), 1000))), Promise.resolve())\\n</code></pre>\\n<h3 id=\\"_8-2-使用promise实现红绿灯交替重复亮\\"><a class=\\"header-anchor\\" href=\\"#_8-2-使用promise实现红绿灯交替重复亮\\">#</a> 8.2 使用Promise实现红绿灯交替重复亮</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>把单次亮灯封装为返回 Promise 的 <code>light(timer, callback)</code></li>\\n<li>红、绿、黄通过连续 <code>then</code> 串行执行，各自等待不同时间</li>\\n<li>一轮结束后递归启动下一轮，并需要考虑停止信号和定时器清理</li>\\n</ul>\\n<p><strong>把一次亮灯封装成返回 <code>Promise</code> 的 <code>light</code>，再用连续的 <code>then</code> 按红、绿、黄顺序串起来。</strong> 每个 <code>light</code> 内部通过 <code>setTimeout</code> 等待指定时间，执行亮灯函数后调用 <code>resolve</code>，所以下一个灯只会在当前灯结束后亮起。完成一轮后再次调用 <code>step</code>，就能让三个灯不断交替重复。实际使用时还要考虑如何停止循环，以及停止后清理尚未执行的定时器。</p>\\n</section>\\n<p>红灯3秒亮一次，黄灯2秒亮一次，绿灯1秒亮一次；如何让三个灯不断交替重复亮灯？（用Promise实现）三个亮灯函数已经存在：</p>\\n<pre class=\\"language-js\\"><code>function red() {\\n    console.log(&#39;red&#39;);\\n}\\nfunction green() {\\n    console.log(&#39;green&#39;);\\n}\\nfunction yellow() {\\n    console.log(&#39;yellow&#39;);\\n}\\n</code></pre>\\n<p>答案：</p>\\n<pre class=\\"language-js\\"><code>function red() {\\n  console.log(&quot;red&quot;);\\n}\\nfunction green() {\\n  console.log(&quot;green&quot;);\\n}\\nfunction yellow() {\\n  console.log(&quot;yellow&quot;);\\n}\\nconst light = function (timer, cb) {\\n  return new Promise(resolve =&gt; {\\n    setTimeout(() =&gt; {\\n      cb()\\n      resolve()\\n    }, timer)\\n  })\\n}\\nconst step = function () {\\n  Promise.resolve().then(() =&gt; {\\n    return light(3000, red)\\n  }).then(() =&gt; {\\n    return light(2000, green)\\n  }).then(() =&gt; {\\n    return light(1000, yellow)\\n  }).then(() =&gt; {\\n    return step()\\n  })\\n}\\n\\nstep();\\n</code></pre>\\n<h3 id=\\"_8-3-实现mergepromise函数\\"><a class=\\"header-anchor\\" href=\\"#_8-3-实现mergepromise函数\\">#</a> 8.3 实现mergePromise函数</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>定义一个数组data用于保存所有异步操作的结果</li>\\n<li>初始化一个const promise = Promise.resolve()，然后循环遍历数组，在promise后面添加执行ajax任务，同时要将添加的结果重新赋值到promise上</li>\\n<li>实现mergePromise函数，把传进去的数组按顺序先后执行，并且把返回的数据先后放到数组data中</li>\\n<li>这道题有点类似于Promise.all()，不过.all()不需要管执行顺序，只需要并发执行就行了</li>\\n<li>但是这里需要等上一个执行完毕之后才能执行下一个</li>\\n</ul>\\n<p><strong><code>mergePromise</code> 可以从 <code>Promise.resolve()</code> 开始，把任务函数逐个接到同一条 <code>then</code> 链上，并按完成顺序收集结果。</strong> 每次先调用当前 <code>ajax</code>，等它返回的 <code>Promise</code> 完成后，再把结果放进 <code>data</code>，因此任务会严格串行执行。循环中必须把新的链重新赋给 <code>promise</code>，否则后续任务不会接在前一个任务后面。它和 <code>Promise.all()</code> 的区别是这里强调依次执行，而不是同时发起。</p>\\n</section>\\n<blockquote>\\n<p>实现mergePromise函数，把传进去的数组按顺序先后执行，并且把返回的数据先后放到数组data中。</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const time = (timer) =&gt; {\\n  return new Promise(resolve =&gt; {\\n    setTimeout(() =&gt; {\\n      resolve()\\n    }, timer)\\n  })\\n}\\nconst ajax1 = () =&gt; time(2000).then(() =&gt; {\\n  console.log(1);\\n  return 1\\n})\\nconst ajax2 = () =&gt; time(1000).then(() =&gt; {\\n  console.log(2);\\n  return 2\\n})\\nconst ajax3 = () =&gt; time(1000).then(() =&gt; {\\n  console.log(3);\\n  return 3\\n})\\n\\nfunction mergePromise () {\\n  // 在这里写代码\\n}\\n\\nmergePromise([ajax1, ajax2, ajax3]).then(data =&gt; {\\n  console.log(&quot;done&quot;);\\n  console.log(data); // data 为 [1, 2, 3]\\n});\\n\\n// 要求分别输出\\n// 1\\n// 2\\n// 3\\n// done\\n// [1, 2, 3]\\n</code></pre>\\n<p>这道题有点类似于Promise.all()，不过.all()不需要管执行顺序，只需要并发执行就行了。但是这里需要等上一个执行完毕之后才能执行下一个。</p>\\n<p><strong>解题思路：</strong></p>\\n<ul>\\n<li>定义一个数组data用于保存所有异步操作的结果</li>\\n<li>初始化一个const promise = Promise.resolve()，然后循环遍历数组，在promise后面添加执行ajax任务，同时要将添加的结果重新赋值到promise上</li>\\n</ul>\\n<p>答案：</p>\\n<pre class=\\"language-js\\"><code>function mergePromise (ajaxArray) {\\n  // 存放每个ajax的结果\\n  const data = [];\\n  let promise = Promise.resolve();\\n  ajaxArray.forEach(ajax =&gt; {\\n  \\t// 第一次的then为了用来调用ajax\\n  \\t// 第二次的then是为了获取ajax的结果\\n    promise = promise.then(ajax).then(res =&gt; {\\n      data.push(res);\\n      return data; // 把每次的结果返回\\n    })\\n  })\\n  // 最后得到的promise它的值就是data\\n  return promise;\\n}\\n</code></pre>\\n<h3 id=\\"_8-4-封装一个异步加载图片的方法\\"><a class=\\"header-anchor\\" href=\\"#_8-4-封装一个异步加载图片的方法\\">#</a> 8.4 封装一个异步加载图片的方法</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>图片加载封装为 Promise，在 <code>onload</code> 中 <code>resolve(img)</code></li>\\n<li>网络或解码失败时必须在 <code>onerror</code> 中 <code>reject</code>，不能让 Promise 永久 pending</li>\\n<li>生产实现还应处理超时、取消、跨域和重复 URL 缓存</li>\\n</ul>\\n<p><strong>创建一个 <code>Image</code> 对象并用 <code>Promise</code> 包起来，在 <code>onload</code> 中执行 <code>resolve(img)</code>，就能得到异步加载图片的方法。</strong> 设置 <code>img.src</code> 后，浏览器会开始加载，成功时调用方可以通过 <code>then</code> 拿到图片对象。加载失败时要在 <code>onerror</code> 中执行 <code>reject</code>，否则这个 <code>Promise</code> 会一直处于等待状态。工程里还可以按需要补上超时、取消、跨域处理或重复地址缓存。</p>\\n</section>\\n<p>这个相对简单一些，只需要在图片的onload函数中，使用resolve返回一下就可以了。\\n来看看具体代码：</p>\\n<pre class=\\"language-js\\"><code>function loadImg(url) {\\n  return new Promise((resolve, reject) =&gt; {\\n    const img = new Image();\\n    img.onload = function() {\\n      console.log(&quot;一张图片加载完成&quot;);\\n      resolve(img);\\n    };\\n    img.onerror = function() {\\n    \\treject(new Error(&#39;Could not load image at&#39; + url));\\n    };\\n    img.src = url;\\n  });\\n}\\n</code></pre>\\n<p>上面是最小实现。生产中还要把超时和取消纳入同一个完成出口，并在完成后清理 <code>onload</code>、<code>onerror</code>、定时器和 <code>abort</code> 监听。<code>Promise</code> 状态只能确定一次，并不代表其他引用会自动清理。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品详情页里有一张地址失效的主图，代码只在 <code>img.onload</code> 中调用 <code>resolve(img)</code>，骨架屏一直不消失；你会怎样指出这个最小封装的缺陷？</strong></p>\\n<p>封装缺少 <code>onerror</code> 出口，加载失败后 <code>Promise</code> 会永久停在 <code>pending</code>，调用方既无法展示占位图，也无法结束加载态。应在赋值 <code>src</code> 前同时注册 <code>onload</code> 和 <code>onerror</code>，失败时用包含 <code>url</code> 的 <code>Error</code> 执行 <code>reject</code>；这仍不能覆盖网络长时间无响应。</p>\\n</li>\\n<li>\\n<p><strong>相册页一次预加载 30 张缩略图，产品要求单张超过指定时限就显示占位图；你会怎样把超时加入 <code>loadImg</code>，又避免回调重复执行？</strong></p>\\n<p>我会让成功、失败和超时都进入同一个 <code>finish</code> 函数，由它检查完成标记后只执行一次 <code>resolve</code> 或 <code>reject</code>。定时器触发时返回超时错误，图片先完成时则清除定时器；仅依赖 <code>Promise</code> 只能防止状态二次改变，不能自动停止遗留回调。</p>\\n</li>\\n<li>\\n<p><strong>用户在图片预览页关闭弹窗后，调用方通过 <code>AbortSignal</code> 取消加载；原来的 <code>onload</code> 随后仍可能触发，你会怎样设计取消语义？</strong></p>\\n<p>取消时应通过统一完成出口执行 <code>reject</code>，并移除 <code>abort</code> 监听、清除定时器以及解绑 <code>onload</code>、<code>onerror</code>。这样能阻止已取消任务继续影响界面；但取消 <code>Promise</code> 的等待不等于浏览器一定终止底层图片传输，不能把两者当成同一保证。</p>\\n</li>\\n<li>\\n<p><strong>线上监控发现图片请求已经超时并展示占位图，几秒后控制台却仍打印“一张图片加载完成”，而且弹窗反复打开后监听越来越多；你先查哪段代码？</strong></p>\\n<p>我会先检查超时、成功、失败和取消分支是否都调用同一个清理逻辑，尤其是 <code>img.onload</code>、<code>img.onerror</code>、定时器及 <code>abort</code> 监听是否解除。<code>Promise</code> 虽然只会确定一次状态，但旧闭包和事件引用仍可能存活；若只在成功分支清理，失败与取消路径仍会泄漏副作用。</p>\\n</li>\\n<li>\\n<p><strong>基础组件负责人想把 <code>loadImg</code> 保持为只接收 <code>url</code> 的十行函数，业务负责人却要求超时和取消都内置；你会如何划分接口而不让调用方各自补丁？</strong></p>\\n<p>我会保留默认行为简洁，同时让接口接收可选的超时时间和 <code>AbortSignal</code>，内部仍统一管理完成与清理。这样业务无需重复编写竞态处理，基础场景也不增加调用负担；代价是错误类型和取消约定必须稳定，否则调用方难以区分加载失败、超时与主动取消。</p>\\n</li>\\n<li>\\n<p><strong>代码评审里有人说“<code>Promise</code> 已经只会成功或失败一次，所以超时后没必要清理 <code>img.onload</code>”；预览页每次切图都会新建任务，你怎么反驳？</strong></p>\\n<p>状态只能确定一次，只保证后续 <code>resolve</code> 或 <code>reject</code> 不再改变结果，并不负责释放事件处理器、定时器或外部监听。超时后保留的 <code>onload</code> 仍可能执行日志、闭包逻辑或持有引用，因此所有出口都要清理；若回调还包含界面操作，风险会从资源残留扩大为过期任务干扰当前页面。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_8-5-限制异步操作的并发个数并尽可能快的完成全部\\"><a class=\\"header-anchor\\" href=\\"#_8-5-限制异步操作的并发个数并尽可能快的完成全部\\">#</a> 8.5 限制异步操作的并发个数并尽可能快的完成全部</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>分批 <code>Promise.all()</code> 只能限流，批内慢任务会让已空闲的槽位白白等待</li>\\n<li>正确模型是固定数量的 <code>worker</code> 共享任务游标：一个任务结束，立即领取下一个</li>\\n<li>正在执行数始终不超过 <code>limit</code>，结果按原索引写入，完成顺序不会打乱返回顺序</li>\\n<li>失败要明确是首错拒绝还是收集全部结果，并处理取消、重试和同步抛错</li>\\n</ul>\\n<p><strong>这题要用动态任务池，而不是把请求每三个分一批。</strong> 我会启动最多 <code>limit</code> 个 <code>worker</code>，任何一个任务结束后立即补上下一个，让并发槽位尽量不空转。结果按任务原索引保存，不依赖完成顺序。上线前还要明确失败是立即中断还是继续收集，以及是否需要 <code>AbortSignal</code>、重试和超时。</p>\\n</section>\\n<p>有8个图片资源的url，已经存储在数组urls中。</p>\\n<blockquote>\\n<p>urls类似于[\'<a href=\\"https://image1.png\\">https://image1.png</a>\', \'<a href=\\"https://image2.png\\">https://image2.png</a>\', ....]</p>\\n</blockquote>\\n<p>而且已经有一个函数function loadImg，输入一个url链接，返回一个Promise，该Promise在图片下载完成的时候resolve，下载失败则reject。</p>\\n<p>但有一个要求，任何时刻同时下载的链接数量不可以超过3个。</p>\\n<p>请写一段代码实现这个需求，要求尽可能快速地将所有图片下载完成。</p>\\n<pre class=\\"language-js\\"><code>var urls = [\\n  &quot;https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/AboutMe-painting1.png&quot;,\\n  &quot;https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/AboutMe-painting2.png&quot;,\\n  &quot;https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/AboutMe-painting3.png&quot;,\\n  &quot;https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/AboutMe-painting4.png&quot;,\\n  &quot;https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/AboutMe-painting5.png&quot;,\\n  &quot;https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/bpmn6.png&quot;,\\n  &quot;https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/bpmn7.png&quot;,\\n  &quot;https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/bpmn8.png&quot;,\\n];\\nfunction loadImg(url) {\\n  return new Promise((resolve, reject) =&gt; {\\n    const img = new Image();\\n    img.onload = function() {\\n      console.log(&quot;一张图片加载完成&quot;);\\n      resolve(img);\\n    };\\n    img.onerror = function() {\\n    \\treject(new Error(&#39;Could not load image at&#39; + url));\\n    };\\n    img.src = url;\\n  });\\n</code></pre>\\n<p>看到这道题时，我最开始的想法是：</p>\\n<p>拿到urls，然后将这个数组每3个<code>url</code>一组创建成一个二维数组\\n然后用<code>Promise.all()</code>每次加载一组url（也就是并发3个），这一组加载完再加载下一组。</p>\\n<blockquote>\\n<p>这个想法从技术上说并不难实现，有点类似于第三题。不过缺点也明显，那就是每次都要等到上一组全部加载完之后，才加载下一组，那如果上一组有2个已经加载完了，还有1个特别慢，还在加载，要等这个慢的也加载完才能进入下一组。这明显会照常卡顿，影响加载效率\\n`</p>\\n</blockquote>\\n<p>想法一💡：</p>\\n<pre class=\\"language-js\\"><code>function limitLoad (urls, handler, limit) {\\n  const data = []; // 存储所有的加载结果\\n  let p = Promise.resolve();\\n  const handleUrls = (urls) =&gt; { // 这个函数是为了生成3个url为一组的二维数组\\n    const doubleDim = [];\\n    const len = Math.ceil(urls.length / limit); // Math.ceil(8 / 3) = 3\\n    console.log(len) // 3, 表示二维数组的长度为3\\n    for (let i = 0; i &lt; len; i++) {\\n      doubleDim.push(urls.slice(i * limit, (i + 1) * limit))\\n    }\\n    return doubleDim;\\n  }\\n  const ajaxImage = (urlCollect) =&gt; { // 将一组字符串url 转换为一个加载图片的数组\\n    console.log(urlCollect)\\n    return urlCollect.map(url =&gt; handler(url))\\n  }\\n  const doubleDim = handleUrls(urls); // 得到3个url为一组的二维数组\\n  doubleDim.forEach(urlCollect =&gt; {\\n    p = p.then(() =&gt; Promise.all(ajaxImage(urlCollect))).then(res =&gt; {\\n      data.push(...res); // 将每次的结果展开，并存储到data中 (res为：[img, img, img])\\n      return data;\\n    })\\n  })\\n  return p;\\n}\\nlimitLoad(urls, loadImg, 3).then(res =&gt; {\\n  console.log(res); // 最终得到的是长度为8的img数组: [img, img, img, ...]\\n  res.forEach(img =&gt; {\\n    document.body.appendChild(img);\\n  })\\n});\\n</code></pre>\\n<p>上面是易于理解的分批方案，它能保证并发数不超标，却不符合“尽可能快”。更合适的实现是让固定数量的 <code>worker</code> 动态领取任务：</p>\\n<pre class=\\"language-js\\"><code>async function mapLimit(items, limit, task) {\\n  if (!Number.isInteger(limit) || limit &lt; 1) {\\n    throw new RangeError(&#39;limit 必须是正整数&#39;)\\n  }\\n\\n  const results = new Array(items.length)\\n  let cursor = 0\\n\\n  async function worker() {\\n    while (true) {\\n      const index = cursor++\\n      if (index &gt;= items.length) return\\n\\n      // 同时把 task 的同步抛错转成 Promise 拒绝\\n      results[index] = await Promise.resolve().then(() =&gt; task(items[index], index))\\n    }\\n  }\\n\\n  const workerCount = Math.min(limit, items.length)\\n  await Promise.all(Array.from({ length: workerCount }, worker))\\n  return results\\n}\\n\\nconst images = await mapLimit(urls, 3, loadImg)\\n</code></pre>\\n<p>每个 <code>worker</code> 只有在上一个 <code>await</code> 完成后才会领取新索引，因此正在执行的任务不会超过 <code>limit</code>。结果写入原索引，所以即使完成顺序不同，返回数组仍与输入对齐。时间复杂度是 <code>O(n)</code>，结果空间是 <code>O(n)</code>，调度器中活跃的执行单元为 <code>O(limit)</code>。</p>\\n<p>这份代码采用首错拒绝语义，但 <code>Promise.all</code> 不会自动取消已启动任务。如果首错后必须停止其他请求，需要向 <code>task</code> 传入共享的 <code>AbortSignal</code>。如果要等全部完成再收集成败，则在 <code>worker</code> 内捕获错误，像 <code>Promise.allSettled()</code> 一样保存 <code>{ status, value, reason }</code>。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>有 <code>8</code> 张图片，并发上限为 <code>3</code>；第一批前两张很快完成，第三张长期未返回，而后面还有 <code>5</code> 张待下载，用每三张一次 <code>Promise.all()</code> 为什么不够快？</strong></p>\\n<p>分批方案会让两个已经空闲的并发槽位等待第三张慢图，后续任务无法及时启动。应创建至多 <code>3</code> 个 <code>worker</code> 动态领取下一个索引，使任一任务完成后立即补位；这样保证上限的同时避免批次屏障造成空转。</p>\\n</li>\\n<li>\\n<p><strong>你要把图片下载器封装成 <code>mapLimit(items, limit, task)</code>，调用方要求结果顺序与输入一致，但网络完成顺序随机；代码结构怎么设计？</strong></p>\\n<p>先按输入长度创建结果数组，每个 <code>worker</code>领取索引后把结果写回对应位置，而不是按完成时间 <code>push</code>。共享游标负责分配尚未执行的任务，<code>worker</code>在一次 <code>await</code>结束后再领取下一项；活跃执行单元最多为 <code>limit</code>。</p>\\n</li>\\n<li>\\n<p><strong>调用方传入 <code>limit = 0</code>、空数组，或者 <code>task</code> 在返回 <code>Promise</code> 前同步抛错；你的通用调度器应怎样处理这些边界？</strong></p>\\n<p><code>limit</code>不是正整数时应立即抛出 <code>RangeError</code>，空数组则直接得到空结果。调用任务时可用 <code>Promise.resolve().then(() =&gt; task(...))</code>统一同步抛错与异步拒绝；否则同步异常可能绕开预期的异步错误路径。</p>\\n</li>\\n<li>\\n<p><strong>线上第 <code>4</code> 张图片加载失败后，外层 <code>Promise.all</code> 已经拒绝，但抓包显示另外两张仍在下载；这是并发控制失效了吗，怎么处理？</strong></p>\\n<p>这不是并发上限失效，而是 <code>Promise</code>拒绝不会自动取消已经启动的操作。若业务要求首错停止，应让调度器停止领取新任务，并向所有已启动任务传入共享的 <code>AbortSignal</code>；底层任务不支持取消时，只能忽略其结果。</p>\\n</li>\\n<li>\\n<p><strong>批量处理 <code>1000</code> 个资源时，产品既要保留全部成功结果，也要展示每个失败原因；你会保留首错拒绝语义还是改造结果模型？</strong></p>\\n<p>应在 <code>worker</code>内部捕获每项错误，按原索引保存类似 <code>{ status, value, reason }</code> 的结果，等待所有任务结束后统一返回。首错拒绝更适合任一失败即整体无效的流程；全量收集会继续消耗资源，但能支持局部展示和后续重试。</p>\\n</li>\\n<li>\\n<p><strong>评审中有人建议用 <code>Promise.race()</code>维护运行队列，另一人建议固定数量的 <code>worker</code>；在只要求限流、保序和尽快完成时你选哪种？</strong></p>\\n<p>固定 <code>worker</code>通常更直接：创建 <code>Math.min(limit, items.length)</code> 个执行单元，共享游标并把结果写回原索引即可。<code>Promise.race()</code>也能补位，但需要维护和移除运行中的任务；需求没有动态优先级时，它会增加状态管理复杂度。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);