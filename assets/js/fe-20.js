(window.webpackJsonp=window.webpackJsonp||[]).push([[20],{1127:function(e){e.exports=JSON.parse('{"html":"<blockquote>\\n<p>环形链表是链表中的一类特殊问题，它和链表反转一样，有着相对恒定的解题思路和适当的变体。如果你对它的特性和解法没有预先的了解和把握，那么前期的推导可能会花去你大量的时间。反过来看，只要我们能够掌握其核心思路，那么不管它怎么变化，大家都能在瞬间找到解题的“抓手”、进而给出正确的解答。</p>\\n</blockquote>\\n<h2 id=\\"环形链表基本问题-如何判断链表是否成环\\"><a class=\\"header-anchor\\" href=\\"#环形链表基本问题-如何判断链表是否成环\\">#</a> 环形链表基本问题——如何判断链表是否成环？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>不修改链表时首选 Floyd 快慢指针：<code>slow</code> 每次一步，<code>fast</code> 每次两步。</li>\\n<li>有环时快指针会在环内追上慢指针；无环时 <code>fast</code> 或 <code>fast.next</code> 先到 <code>null</code>。</li>\\n<li>时间 <code>O(n)</code>、额外空间 <code>O(1)</code>；<code>Set</code> 法更直观但空间为 <code>O(n)</code>。</li>\\n<li>循环条件先保护 <code>fast &amp;&amp; fast.next</code>，比较的是节点引用而不是节点值。</li>\\n</ul>\\n<p><strong>不修改链表的情况下，我一般用 <code>Floyd</code> 快慢指针判断是否有环。</strong> <code>slow</code> 每轮走一步，<code>fast</code> 每轮走两步；进入有限长度的环后，快指针会不断缩短与慢指针的相对距离，最终相遇。若 <code>fast</code> 或 <code>fast.next</code> 先变成 <code>null</code>，则说明没有环；实现时比较节点引用而不是节点值，时间为 <code>O(n)</code>、额外空间为 <code>O(1)</code>。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“我先确认不能修改节点。然后用 Floyd 判环：快慢指针进入有限长度的环后，相对位置每轮前进一格，所以必然相遇；若快指针抵达 null，则无环。”</p>\\n</blockquote>\\n<blockquote>\\n<p>真题描述：给定一个链表，判断链表中是否有环。</p>\\n</blockquote>\\n<p>示例 1：</p>\\n<blockquote>\\n<p>输入：[3,2,0,4]（链表结构如下图） 输出：true</p>\\n</blockquote>\\n<p>解释：链表中存在一个环</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122601.png\\" alt=\\"\\"></p>\\n<p><strong>思路解读</strong></p>\\n<p>其实链表成环的特征非常明显，大家可以结合一个现实中的例子来理解：</p>\\n<p>假如现实中有一个长跑爱好者李雷，这货很狂，他立了一个 flag，说要徒步环游世界：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122609.png\\" alt=\\"\\"></p>\\n<p>地球的周长围出来的这个圆，它就是一个“环”。李雷现在就想围着这个环跑上一圈，说他狂，他也没那么狂——他觉得自己最多跑一圈，为了防止自己跑过界，他决定在出发的地方立一个 flag：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122618.png\\" alt=\\"\\"></p>\\n<p>这样，不管李雷走完这个环用了多少年，世事如何变迁，只要他的 flag 还没有倒，那么李雷就一定能回到自己梦开始的地方：）。</p>\\n<p>换个角度看：只要李雷在闷头前进的过程中，发现了 flag 的存在，那么就意味着，李雷确实走了一个环。毕竟若这是一条线，他将永远无法回到起点。</p>\\n<p>回到链表的世界里，也是一个道理。一个环形链表的基本修养，是能够让遍历它的游标回到原点</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122629.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>从 flag 出发，只要我能够再回到 flag 处，那么就意味着，我正在遍历一个环形链表。</p>\\n</blockquote>\\n<p>我们按照这个思路来做题：</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {ListNode} head\\n * @return {boolean}\\n */\\n// 入参是头结点 \\nconst hasCycle = function(head) {\\n    // 只要结点存在，那么就继续遍历\\n    while(head){\\n        // 如果 flag 已经立过了，那么说明环存在\\n        if(head.flag){\\n            return true;\\n        }else{\\n            // 如果 flag 没立过，就立一个 flag 再往\\n            下走\\n            head.flag = true;\\n            head = head.next;\\n        }\\n    }\\n    return false;\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>调试页里两个不同节点的 <code>value</code> 都是 <code>7</code>，判环函数因此返回 <code>true</code>；为什么比较节点值会产生误判？</strong></p>\\n<p>成环意味着沿 <code>next</code> 再次到达同一个节点对象，而不是再次看到相同的业务值。判定时必须比较节点引用；两个独立节点即使 <code>value</code>、时间戳等字段完全相同，也不能证明链表存在环。</p>\\n</li>\\n<li>\\n<p><strong>共享缓存中的链表节点被冻结且会被多个请求复用，开发者提出写入 <code>flag</code> 判环；你会接受吗？</strong></p>\\n<p>不应直接写入 <code>flag</code>，冻结节点可能让赋值失败，共享结构还会把一次遍历留下的标记带给后续请求。应使用 <code>Floyd</code> 快慢指针保持 <code>O(1)</code> 额外空间；若允许额外内存，也可用按引用存储的 <code>Set</code>。</p>\\n</li>\\n<li>\\n<p><strong>数据导入链表可能包含上百万节点，产品要求不修改输入且额外空间保持常数级，你会如何判断是否成环？</strong></p>\\n<p>应让 <code>slow</code> 每轮走一步、<code>fast</code> 每轮走两步，并在移动前检查 <code>fast</code> 和 <code>fast.next</code>。若二者引用相同则存在环，若快指针到达 <code>null</code> 则无环；该方案时间为 <code>O(n)</code>，且不会随节点数增加额外集合空间。</p>\\n</li>\\n<li>\\n<p><strong>线上单节点链表执行判环后偶发 <code>Cannot read properties of null</code>，代码直接写了 <code>fast = fast.next.next</code>；你会检查什么？</strong></p>\\n<p>应先确认每轮移动前是否同时判断 <code>fast</code> 与 <code>fast.next</code>，任一为空都应立即返回无环。单节点无环、空链表和尾部只剩一个节点都会触发该边界；仅在移动之后检查已经来不及阻止空引用访问。</p>\\n</li>\\n<li>\\n<p><strong>评审中一方选择 <code>Set</code>，另一方选择 <code>Floyd</code>；链表来自只读 <code>SDK</code> 且长度不可预估，你会支持哪一个？</strong></p>\\n<p>更适合选择 <code>Floyd</code>，因为它不修改 <code>SDK</code> 节点，也只占用常数额外空间，长度不可预估时内存风险更低。<code>Set</code> 的实现更直观并能保留访问痕迹，但需要 <code>O(n)</code> 空间，只有诊断需求高于内存约束时才更合适。</p>\\n</li>\\n</ul>\\n</section>\\n<pre class=\\"language-js\\"><code>function hasCycle(head) {\\n  let slow = head\\n  let fast = head\\n  while (fast &amp;&amp; fast.next) {\\n    slow = slow.next\\n    fast = fast.next.next\\n    if (slow === fast) return true\\n  }\\n  return false\\n}\\n</code></pre>\\n<h2 id=\\"环形链表衍生问题-定位环的起点\\"><a class=\\"header-anchor\\" href=\\"#环形链表衍生问题-定位环的起点\\">#</a> 环形链表衍生问题——定位环的起点</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>第一阶段让快慢指针相遇，只证明“有环”。</li>\\n<li>第二阶段把一个指针放回头节点，两者都每次一步；再次相遇处就是环入口。</li>\\n<li>设头到入口为 <code>a</code>、入口到首次相遇为 <code>b</code>、环长为 <code>c</code>，可推出头到入口与相遇点到入口的剩余距离同余。</li>\\n<li>无环必须返回 <code>null</code>，不能在第一阶段退出后继续访问相遇点。</li>\\n</ul>\\n<p><strong>我会先用 <code>Floyd</code> 快慢指针找到环内相遇点，再让一个指针回到头节点，与另一个指针同步前进，再次相遇的位置就是环入口。</strong> 本质上，快指针路程是慢指针的两倍，可以推出头节点到入口的距离，与相遇点继续绕到入口的距离对环长同余。若第一阶段发现 <code>fast</code> 或 <code>fast.next</code> 为 <code>null</code>，必须直接返回 <code>null</code>，不能继续访问所谓的相遇点。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“先用 Floyd 找相遇点。由快指针路程是慢指针两倍可推出，头到入口的距离等于相遇点继续绕到入口的距离模环长，因此两个指针同步走会在入口相遇。”</p>\\n</blockquote>\\n<blockquote>\\n<p>真题描述：给定一个链表，返回链表开始入环的第一个结点。 如果链表无环，则返回 null。</p>\\n</blockquote>\\n<p>示例 1：</p>\\n<blockquote>\\n<p>输入：head = [3,2,0,-4]（如下图） 输出：tail connects to node index 1 解释：链表中有一个环，其尾部连接到第二个结点。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905122641.png\\" alt=\\"\\"></p>\\n<p>示例 2：</p>\\n<ul>\\n<li>输入：head = [1,2]（如下图）</li>\\n<li>输出：tail connects to node index 0</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210905122705.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>解释：链表中有一个环，其尾部连接到第一个结点。 链表成环2</p>\\n</blockquote>\\n<p>示例 3：</p>\\n<ul>\\n<li>输入：head = [1]（如下图）</li>\\n<li>输出：no cycle</li>\\n</ul>\\n<p>解释：链表中没有环。</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122716.png\\" alt=\\"\\"></p>\\n<p><strong>思路解读</strong></p>\\n<blockquote>\\n<p>这道题在上道题的基础上，仅仅增加了一个“返回链表的成环起点”，其难度定义就从 easy 上升到了 medium。不过对于掌握了关键解题思路的各位来说，这道题仍然是 easy——因为如果一个结点是环形链表成环的起点，那么它一定是第一个被发现 flag 标志已存在的结点：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905122722.png\\" alt=\\"\\"></p>\\n<p>这一点不难理解，我们试想如果从头开始遍历一个链表，假如途中进入了一个环，那么首先被打上 flag 标签的其实就是环的起点。待我们遍历完这个环时，即便环上所有的结点都已经被立了 flag，但起点处的 flag 一定最先被我们定位到。因此，我们只需要在第一次发现 flag 已存在时，将对应的结点返回即可：</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {ListNode} head\\n * @return {ListNode}\\n */\\nconst detectCycle = function(head) {\\n    while(head){\\n        if(head.flag){\\n            return head;\\n        }else{\\n            head.flag = true;\\n            head = head.next;\\n        }\\n    }\\n    return null;\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>链表 <code>A→B→C→D→C</code> 中，代码在快慢指针首次相遇时直接返回相遇节点；页面却要求返回 <code>C</code>，错在哪里？</strong></p>\\n<p>首次相遇只能证明存在环，相遇位置并不保证就是入口 <code>C</code>。应把一个指针重置到 <code>head</code>，另一个留在相遇点，再让二者每次各走一步；下一次引用相同的位置才是入环的第一个节点。</p>\\n</li>\\n<li>\\n<p><strong>日志采集链表禁止增加字段，接口还要求无环时返回 <code>null</code>；你会怎样实现入口定位并处理空链表？</strong></p>\\n<p>应先用 <code>Floyd</code> 完成判环，移动前检查 <code>fast</code> 和 <code>fast.next</code>，到达尾部就返回 <code>null</code>。相遇后再启动从 <code>head</code> 出发的 <code>finder</code> 与慢指针同步前进，最终返回共同节点，全程不需要修改输入。</p>\\n</li>\\n<li>\\n<p><strong>安全审计要求在找到入口后断开环，但该链表还被另一个读线程共享；你会直接修改入口节点吗？</strong></p>\\n<p>不能直接修改入口，因为要断环的是环内满足 <code>node.next === entry</code> 的前驱节点，将其 <code>next</code> 设为 <code>null</code>。共享读线程可能同时观察到结构突变，必须先确认所有权或加同步措施；若无修改授权，只应返回入口而不改链表。</p>\\n</li>\\n<li>\\n<p><strong>线上实现对部分长链表一直循环，监控显示已找到首次相遇点，但第二阶段一个指针走一步、另一个走两步；你会如何修复？</strong></p>\\n<p>第二阶段必须让从 <code>head</code> 出发的指针和相遇点指针都每次走一步。入口推导依赖两段剩余距离在相同速度下抵消，改变速度后不再保证在入口相遇，甚至可能只在环内周期性错过。</p>\\n</li>\\n<li>\\n<p><strong>内存充足的后台任务想用 <code>Set</code> 直接返回首个重复节点，核心库坚持 <code>Floyd</code>；两种方案如何取舍？</strong></p>\\n<p>从头遍历并按引用加入 <code>Set</code> 时，首次重复的节点确实是入口，实现直观但需要 <code>O(n)</code> 额外空间。<code>Floyd</code> 只占常数空间且不修改节点，更适合通用核心库；若任务更重视可观测性并接受内存增长，<code>Set</code> 也成立。</p>\\n</li>\\n<li>\\n<p><strong>定位入口后，排障人员还想知道环长以评估损坏范围；你会复用哪个状态继续计算？</strong></p>\\n<p>可从首次相遇点出发，固定一个指针，让另一个每次走一步，直到再次回到该点，累计步数就是环长。该过程仍为 <code>O(1)</code> 额外空间，但必须在已确认有环后执行，否则沿无环链表计数会遇到 <code>null</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"快慢指针的思路\\"><a class=\\"header-anchor\\" href=\\"#快慢指针的思路\\">#</a> 快慢指针的思路</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>慢指针每轮一步、快指针每轮两步；无环时快指针先到 <code>null</code>，有环时二者最终必在环内相遇</li>\\n<li>循环条件要同时检查 <code>fast</code> 和 <code>fast.next</code>，否则读取 <code>fast.next.next</code> 会越界</li>\\n<li>比较的是结点引用而不是结点值，相同值不代表走到了同一个位置</li>\\n</ul>\\n<p><strong>快慢指针的核心是让 <code>slow</code> 每轮走一步、<code>fast</code> 每轮走两步，用二者是否相遇来判断链表有没有环。</strong> 有环时，两者进入环后会因速度差最终相遇；无环时，快指针会更早走到链表末尾。循环中要检查 <code>fast</code> 和 <code>fast.next</code>，否则读取 <code>fast.next.next</code> 可能出错；同时必须比较节点引用，相同的节点值并不代表处在同一位置。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>function detectCycle(head) {\\n  let slow = head\\n  let fast = head\\n  do {\\n    if (!fast || !fast.next) return null\\n    slow = slow.next\\n    fast = fast.next.next\\n  } while (slow !== fast)\\n\\n  let finder = head\\n  while (finder !== slow) {\\n    finder = finder.next\\n    slow = slow.next\\n  }\\n  return finder\\n}\\n</code></pre>\\n<p>这道题还有一个公认的比较经典的思路，就是用快慢指针来做：</p>\\n<p>定义慢指针 slow，快指针 fast。两者齐头并进， slow 一次走一步、fast 一次 走两步。这样如果它们是在一个有环的链表里移动，一定有相遇的时刻。这个原理证明起来也比较简单：我们假设移动的次数为 t，slow 移动的路程就是t，fast 移动的路程为2t，假如环的长度为 s，那么当下面这个条件：</p>\\n<pre class=\\"language-text\\"><code>2t - t = s\\n</code></pre>\\n<p>也就是：</p>\\n<pre class=\\"language-text\\"><code>t = s\\n</code></pre>\\n<blockquote>\\n<p>满足时，slow 和 fast 就一定会相遇。反之，如果两者没有相遇，同时 fast 遍历到了链表的末尾，发现 next 指针指向 null，则链表中不存在环。</p>\\n</blockquote>\\n<p>有兴趣的同学，可以尝试用双指针法实现一遍上面的判定。不过我更加推荐的仍然是“立flag”法，理解难度和编码难度上来说都更加友好，有利于大家实现题目的“秒杀”。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审中有人用公式 <code>2t-t=s</code> 断言快慢指针一定恰好绕一圈后相遇，但链表进入环前还有一段前缀；你会怎样纠正？</strong></p>\\n<p>不能把相遇条件限定为差值恰好等于一个环长，因为进入环前两指针已走过不同距离。进入有限环后，只需观察二者的相对位置：快指针每轮多走一步，相对距离按环长取模，最终必然变为零。</p>\\n</li>\\n<li>\\n<p><strong>消息链表可能为空或只有一个节点，服务端希望用 <code>do...while</code> 写 <code>Floyd</code>；循环体里检查应放在哪里？</strong></p>\\n<p>应在执行 <code>fast.next.next</code> 前先检查 <code>fast</code> 和 <code>fast.next</code>，任一为空就返回 <code>null</code>。<code>do...while</code> 能让同从 <code>head</code> 出发的两个指针先移动再比较，避免在初始位置被误判相遇，但空引用防护不能省略。</p>\\n</li>\\n<li>\\n<p><strong>候选人把快指针改为每次走三步，声称速度更快且其余入口代码完全不变；你会要求他证明什么？</strong></p>\\n<p>必须证明速度差与环长取模后一定能覆盖相对位置，并重新推导第二阶段为何仍能落在入口。一步与两步的经典方案使相对位置每轮只前进一格，证明和入口恢复都稳定；改成三步不能直接沿用原结论。</p>\\n</li>\\n<li>\\n<p><strong>线上判环函数对无环链表占满 <code>CPU</code>，采样显示 <code>fast</code> 已到尾节点却没有退出；你会优先查看哪处控制流？</strong></p>\\n<p>应检查循环是否在每次移动前处理 <code>!fast || !fast.next</code>，并确认无环分支确实 <code>return null</code> 而非只跳过赋值。还要核对指针是否每轮推进；若某个分支保留原引用，即使输入有限也可能形成代码层面的死循环。</p>\\n</li>\\n<li>\\n<p><strong>列表页需要找中点复用快慢指针，但偶数长度时设计稿未说明取左中点还是右中点；你会直接提交实现吗？</strong></p>\\n<p>不应在未约定语义时直接提交，因为不同初始化和循环条件会得到不同中点。常见的 <code>slow=head</code>、<code>fast=head</code> 且快指针能走两步就推进，会偏向右中点；若页面需要左中点，应调整停止条件并补充偶数样例。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"弦外之音\\"><a class=\\"header-anchor\\" href=\\"#弦外之音\\">#</a> 弦外之音</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>快慢指针不是“链表专用 API”，而是利用不同速度制造位置关系，常用于判环、找中点和定位周期</li>\\n<li>解题时要明确速度、起点、终止条件及相遇后的含义，不能看到链表就机械套模板</li>\\n<li>所有证明都应落到距离或模运算；只凭动画直觉无法覆盖不同环长和入口距离</li>\\n</ul>\\n<p><strong>做环形链表题，真正的难点是想清楚成环过程和环的特性，代码实现反而通常只有几行。</strong> 算法题不像前端业务开发，提交给 <code>OJ</code> 前没有直观反馈帮你及时纠偏，思路一乱，代码往往越改越乱。面试时我会先把逻辑和边界在纸上捋顺，确认每一步成立后再编码，避免盲写增加修复成本，也让面试官看到稳妥的分析过程。</p>\\n</section>\\n<p>在这一节，大家会发现一个非常有趣的现象——做环形链表的系列题目，难点其实在于你怎么去想明白这个成环的过程、怎么把握成环后的特性。真正编码实现的时候，寥寥数行就可以搞定。这其实也是我想要向大家传达的一个重要的解题习惯——做算法题时，不要急于下手写代码，而应该先静下心来，稳住神、一步一步捋清楚你自己的思路。</p>\\n<p>之所以要把这点单独拎出来讲，是因为我知道很多同学平时写业务代码比较多。前端业务代码是什么特征？干就完了，对吧？反正就算代码有问题，也可以通过直观的视觉反馈及时发现、及时修复。在肉眼可见的反馈的指导下，你基本不会出什么方向性的问题。</p>\\n<p>做算法题就大不一样了，真正提交给 OJ 运行之前，除了你自己的逻辑判断之外、没有任何直观的线索能够帮你明确问题的所在。也就是说，如果你一开始压根没想清楚、脑子里本来就是一团乱麻，那么直接开干后往往是越写越乱、最后代码的修复成本也会变得非常高。</p>\\n<p>盲写代码、乱写代码，不仅容易扰乱自己的思路，也会给面试官留下“这个人怎么这么冒失”一类的负面印象。所以大家一定要尽量规避这种行为，如果实在对自己的思路感到不确定、不自信，这时候可以问对方要张纸、先线下梳理一下。真正面试的时候，我们对于自己敲在屏幕上的每一行代码，都应该抱有敬畏之心。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在共享屏幕中还没说明终止条件就开始写循环，随后不断给 <code>null</code> 判断打补丁；作为面试官你会让他先补出什么？</strong></p>\\n<p>应先让他明确循环不变量、指针每轮如何推进，以及有环和无环分别凭什么终止。算法题缺少页面反馈，边界条件若未在编码前理清，补丁容易改变主路径语义；纸上画出前缀、入口和环通常更可靠。</p>\\n</li>\\n<li>\\n<p><strong>支付链路排障脚本处理十万级节点时卡住，开发者只凭几组手工数据确认过逻辑；重新运行前你会要求哪些最小验证？</strong></p>\\n<p>应至少覆盖空链表、单节点无环、单节点自环、长前缀小环和完全无环，并为遍历设置仅用于诊断的步数或超时保护。样例通过不能证明终止性；重新运行前还要说明正式算法为何必然相遇或抵达 <code>null</code>。</p>\\n</li>\\n<li>\\n<p><strong>白板推导采用给节点写 <code>flag</code>，但真正接口把节点声明为只读；在不改变目标的情况下你会怎样重做方案？</strong></p>\\n<p>约束变化后应改用 <code>Floyd</code> 快慢指针，先以一步和两步速度判断是否相遇，再按需求定位入口。原先的 <code>flag</code> 思路依赖可写节点，强行保留会污染或违反输入契约；换方案前应重新写清终止条件。</p>\\n</li>\\n<li>\\n<p><strong>线上修复把判环、求入口和断环揉进一个循环，结果无环输入偶发空引用；你会怎样拆解排查？</strong></p>\\n<p>应先把任务拆为判环、定位入口、查找环内前驱三个可验证阶段，并分别记录指针状态与退出条件。无环路径只能在判环阶段返回，后两阶段必须建立在已确认存在环之上；混合控制流会让这一前置条件难以保证。</p>\\n</li>\\n<li>\\n<p><strong>团队争论先写出代码再靠测试修正，还是先完成推导再实现；面对无法直观看到结果的链表算法，你支持哪种流程？</strong></p>\\n<p>应先完成关键推导和边界约定，再把它们逐行映射为代码与测试。测试适合验证实现是否符合推导，却不能替代终止性和正确性说明；代价是前期多花一点梳理时间，但能降低反复改写主逻辑的风险。</p>\\n</li>\\n<li>\\n<p><strong>已经求得环长 <code>k</code>，另一位工程师不想保存首次相遇点；还能怎样定位入口？</strong></p>\\n<p>可让两个指针都从 <code>head</code> 出发，其中一个先走 <code>k</code> 步，再让二者同步前进，相遇处就是入口。两指针始终相隔一个环长，进入环后会指向同一节点；该方法依赖环长计算正确，且无环输入不能进入此阶段。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);