(window.webpackJsonp=window.webpackJsonp||[]).push([[75],{1181:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：虚拟节点是对界面结构的描述，渲染器通过挂载与更新把声明式描述映射为宿主环境操作</li>\\n<li>原理主线：围绕 「什么是 Virtual DOM」、「为什么使用 Virtual DOM」、「虚拟 DOM 的作用」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：介绍了虚拟DOM的原理、作用及其在提升前端渲染性能、简化DOM操作、实现视图与状态同步等方面的优势，并对主流Virtual DOM库进行了简要说明，适合前端开发者深入理解虚拟DOM技术。</li>\\n<li>边界与代价：<code>key</code>、节点类型和稳定序列决定复用边界；虚拟 DOM 解决可预测更新，不保证永远比手写 DOM 快</li>\\n<li>工程落地：性能分析要看实际提交次数、节点移动和组件边界，不能只用“减少 DOM 操作”解释</li>\\n</ul>\\n<p><strong>虚拟 <code>DOM</code> 本质上是用普通 <code>JavaScript</code> 对象描述界面结构，再通过前后状态对比更新真实 <code>DOM</code>。</strong> 状态变化时，框架先生成新的虚拟树，与上一次结果做 <code>diff</code>，然后只执行必要的宿主环境操作。这样既能维护视图与状态的关系，也能减少复杂场景下手动操作 <code>DOM</code> 的负担。它还可用于 <code>SSR</code>、原生应用和小程序，但具体实现会随框架版本变化，落地前要先确认版本与项目约束。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 原理文章中的代码代表特定实现与写作时间。应用到当前项目时，应先确认浏览器、框架或工具的主版本，再区分稳定的规范语义、可变化的内部实现和项目自身约束。</p>\\n<h2 id=\\"什么是-virtual-dom\\"><a class=\\"header-anchor\\" href=\\"#什么是-virtual-dom\\">#</a> 什么是 Virtual DOM</h2>\\n<ol>\\n<li><code>Virtual DOM</code>(虚拟 DOM)，是由普通的 <code>JS</code> 对象来描述 <code>DOM</code> 对象，因为不是真实的 <code>DOM</code> 对象，所以叫 <code>Virtual DOM</code></li>\\n</ol>\\n<pre class=\\"language-js\\"><code>let element = document.querySelector(&#39;#app&#39;) \\nlet s = &#39;&#39;\\nfor (var key in element) {\\n  s += key + &#39;,&#39; \\n}\\nconsole.log(s)\\n\\n// 打印结果 align,title,lang,translate,dir,hidden,accessKey,draggable,spellcheck,aut ocapitalize,contentEditable,isContentEditable,inputMode,offsetParent,off setTop,offsetLeft,offsetWidth,offsetHeight,style,innerText,outerText,onc opy,oncut,onpaste,onabort,onblur,oncancel,oncanplay,oncanplaythrough,onc hange,onclick,onclose,oncontextmenu,oncuechange,ondblclick,ondrag,ondrag end,ondragenter,ondragleave,ondragover,ondragstart,ondrop,ondurationchan ge,onemptied,onended,onerror,onfocus,oninput,oninvalid,onkeydown,onkeypr ess,onkeyup,onload,onloadeddata,onloadedmetadata,onloadstart,onmousedown ,onmouseenter,onmouseleave,onmousemove,onmouseout,onmouseover,onmouseup, onmousewheel,onpause,onplay,onplaying,onprogress,onratechange,onreset,on resize,onscroll,onseeked,onseeking,onselect,onstalled,onsubmit,onsuspend ,ontimeupdate,ontoggle,onvolumechange,onwaiting,onwheel,onauxclick,ongot pointercapture,onlostpointercapture,onpointerdown,onpointermove,onpointe rup,onpointercancel,onpointerover,onpointerout,onpointerenter,onpointerl eave,onselectstart,onselectionchange,onanimationend,onanimationiteration ,onanimationstart,ontransitionend,dataset,nonce,autofocus,tabIndex,click ,focus,blur,enterKeyHint,onformdata,onpointerrawupdate,attachInternals,n amespaceURI,prefix,localName,tagName,id,className,classList,slot,part,at tributes,shadowRoot,assignedSlot,innerHTML,outerHTML,scrollTop,scrollLef t,scrollWidth,scrollHeight,clientTop,clientLeft,clientWidth,clientHeight ,attributeStyleMap,onbeforecopy,onbeforecut,onbeforepaste,onsearch,eleme ntTiming,previousElementSibling,nextElementSibling,children,firstElement Child,lastElementChild,childElementCount,onfullscreenchange,onfullscreen error,onwebkitfullscreenchange,onwebkitfullscreenerror,setPointerCapture ,releasePointerCapture,hasPointerCapture,hasAttributes,getAttributeNames ,getAttribute,getAttributeNS,setAttribute,setAttributeNS,removeAttribute ,removeAttributeNS,hasAttribute,hasAttributeNS,toggleAttribute,getAttrib uteNode,getAttributeNodeNS,setAttributeNode,setAttributeNodeNS,removeAtt ributeNode,closest,matches,webkitMatchesSelector,attachShadow,getElement sByTagName,getElementsByTagNameNS,getElementsByClassName,insertAdjacentE lement,insertAdjacentText,insertAdjacentHTML,requestPointerLock,getClien tRects,getBoundingClientRect,scrollIntoView,scroll,scrollTo,scrollBy,scr ollIntoViewIfNeeded,animate,computedStyleMap,before,after,replaceWith,re move,prepend,append,querySelector,querySelectorAll,requestFullscreen,web kitRequestFullScreen,webkitRequestFullscreen,createShadowRoot,getDestina tionInsertionPoints,ELEMENT_NODE,ATTRIBUTE_NODE,TEXT_NODE,CDATA_SECTION_ NODE,ENTITY_REFERENCE_NODE,ENTITY_NODE,PROCESSING_INSTRUCTION_NODE,COMME NT_NODE,DOCUMENT_NODE,DOCUMENT_TYPE_NODE,DOCUMENT_FRAGMENT_NODE,NOTATION _NODE,DOCUMENT_POSITION_DISCONNECTED,DOCUMENT_POSITION_PRECEDING,DOCUMEN T_POSITION_FOLLOWING,DOCUMENT_POSITION_CONTAINS,DOCUMENT_POSITION_CONTAI NED_BY,DOCUMENT_POSITION_IMPLEMENTATION_SPECIFIC,nodeType,nodeName,baseU RI,isConnected,ownerDocument,parentNode,parentElement,childNodes,firstCh ild,lastChild,previousSibling,nextSibling,nodeValue,textContent,hasChild Nodes,getRootNode,normalize,cloneNode,isEqualNode,isSameNode,compareDocu mentPosition,contains,lookupPrefix,lookupNamespaceURI,isDefaultNamespace ,insertBefore,appendChild,replaceChild,removeChild,addEventListener,remo veEventListener,dispatchEvent\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>可以使用 <code>Virtual DOM</code> 来描述真实 <code>DOM</code></li>\\n</ol>\\n<pre class=\\"language-js\\"><code>{\\n  sel: &quot;div&quot;,\\n  data: {},\\n  children: undefined,\\n  text: &quot;Hello Virtual DOM&quot;,\\n  elm: undefined,\\n  key: undefined\\n}\\n</code></pre>\\n<h2 id=\\"为什么使用-virtual-dom\\"><a class=\\"header-anchor\\" href=\\"#为什么使用-virtual-dom\\">#</a> 为什么使用 Virtual DOM</h2>\\n<ul>\\n<li>手动操作 <code>DOM</code> 比较麻烦，还需要考虑浏览器兼容性问题，虽然有 <code>jQuery</code> 等库简化 <code>DOM</code> 操作，但是随着项目的复杂 DOM 操作复杂提升</li>\\n<li>为了简化 <code>DOM</code> 的复杂操作于是出现了各种 <code>MVVM</code> 框架，<code>MVVM</code> 框架解决了视图和状态的同步问题</li>\\n<li>为了简化视图的操作我们可以使用模板引擎，但是模板引擎没有解决跟踪状态变化的问题，于是<code>Virtual DOM</code> 出现了</li>\\n<li><code>Virtual DOM</code> 的好处是当状态改变时不需要立即更新 DOM，只需要创建一个虚拟树来描述<code>DOM</code>，<code>Virtual DOM</code> 内部将弄清楚如何有效(<code>diff</code>)的更新 <code>DOM</code></li>\\n<li>虚拟 <code>DOM</code> 可以维护程序的状态，跟踪上一次的状态</li>\\n<li>通过比较前后两次状态的差异更新真实 <code>DOM</code></li>\\n</ul>\\n<h2 id=\\"虚拟-dom-的作用\\"><a class=\\"header-anchor\\" href=\\"#虚拟-dom-的作用\\">#</a> 虚拟 DOM 的作用</h2>\\n<ul>\\n<li>维护视图和状态的关系</li>\\n<li>复杂视图情况下提升渲染性能</li>\\n<li>除了渲染 <code>DOM</code> 以外，还可以实现 <code>SSR(Nuxt.js/Next.js)</code>、原生应用(<code>Weex/React Native</code>)、小程序(<code>mpvue/uni-app</code>)等</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210328112610.png\\" alt=\\"\\"></p>\\n<h2 id=\\"virtual-dom-库\\"><a class=\\"header-anchor\\" href=\\"#virtual-dom-库\\">#</a> Virtual DOM 库</h2>\\n<ul>\\n<li><a href=\\"https://github.com/snabbdom/snabbdom\\">Snabbdom</a>\\n<ul>\\n<li><code>Vue 2.x</code> 内部使用的 <code>Virtual DOM</code> 就是改造的 <code>Snabbdom</code></li>\\n<li>通过模块可扩展</li>\\n<li>源码使用 <code>TypeScript</code> 开发</li>\\n<li>最快的 <code>Virtual DOM</code> 之一</li>\\n</ul>\\n</li>\\n<li><a href=\\"https://github.com/Matt-Esch/virtual-dom\\">virtual-dom</a></li>\\n</ul>\\n<h2 id=\\"snabbdom-基本使用\\"><a class=\\"header-anchor\\" href=\\"#snabbdom-基本使用\\">#</a> Snabbdom 基本使用</h2>\\n<h3 id=\\"创建项目\\"><a class=\\"header-anchor\\" href=\\"#创建项目\\">#</a> 创建项目</h3>\\n<pre class=\\"language-bash\\"><code># 创建项目目录\\nmd snabbdom-demo\\n# 进入项目目录\\ncd snabbdom-demo\\n# 创建 package.json yarn init -y\\n# 本地安装 parcel\\nyarn add parcel-bundler\\n</code></pre>\\n<p>配置 <code>package.json</code> 的 <code>scripts</code></p>\\n<pre class=\\"language-text\\"><code>&quot;scripts&quot;: {\\n&quot;dev&quot;: &quot;parcel index.html --open&quot;, &quot;build&quot;: &quot;parcel build index.html&quot;\\n}\\n</code></pre>\\n<p>创建目录结构</p>\\n<p><img src=\\"https://s.poetries.top/images/20210328123653.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-text\\"><code>yarn add snabbdom\\n</code></pre>\\n<pre class=\\"language-text\\"><code>import{init,h,thunk}from&#39;snabbdom&#39;\\n</code></pre>\\n<blockquote>\\n<p><code>snabbdom</code> 的核心仅提供最基本的功能，只导出了三个函数 <code>init()</code>、<code>h()</code>、<code>thunk()</code></p>\\n</blockquote>\\n<ul>\\n<li><code>init()</code> 是一个高阶函数，返回 <code>patch()</code></li>\\n<li><code>h()</code> 返回虚拟节点 <code>VNode</code>，这个函数我们在使用 <code>Vue.js</code> 的时候见过</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>new Vue({\\n  router,\\n  store,\\n  render: h =&gt; h(App) \\n}).$mount(&#39;#app&#39;)\\n</code></pre>\\n<ul>\\n<li><code>thunk()</code> 是一种优化策略，可以在处理不可变数据时使用</li>\\n</ul>\\n<blockquote>\\n<p>注意:导入时候不能使用 <code>import snabbdom from \'snabbdom\'</code>。原因:<code>node_modules/src/snabbdom.ts</code> 末尾导出使用的语法是 <code>export</code> 导出 <code>API</code>，没有使用 <code>export default</code> 导出默认输出</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210328124133.png\\" alt=\\"\\"></p>\\n<h3 id=\\"基本使用\\"><a class=\\"header-anchor\\" href=\\"#基本使用\\">#</a> 基本使用</h3>\\n<p><strong>例子1</strong></p>\\n<pre class=\\"language-js\\"><code>import { h, init } from &#39;snabbdom&#39;\\n\\n// 1. hello world\\n// 参数：数组，模块\\n// 返回值：patch函数，作用对比两个vnode的差异更新到真实DOM\\nlet patch = init([])\\n// 第一个参数：标签+选择器\\n// 第二个参数：如果是字符串的话就是标签中的内容\\nlet vnode = h(&#39;div#container.cls&#39;, { \\n  hook: {\\n    init (vnode) {\\n      console.log(vnode.elm)\\n    },\\n    create (emptyVnode, vnode) {\\n      console.log(vnode.elm)\\n    }\\n  }\\n}, &#39;Hello World&#39;)\\n\\nlet app = document.querySelector(&#39;#app&#39;)\\n// 第一个参数：可以是DOM元素，内部会把DOM元素转换成VNode\\n// 第二个参数：VNode\\n// 返回值：VNde\\nlet oldVnode = patch(app, vnode)\\n\\n// 假设的时刻\\nvnode = h(&#39;div&#39;, &#39;Hello Snabbdom&#39;)\\n\\npatch(oldVnode, vnode)\\n</code></pre>\\n<p><strong>例子2</strong></p>\\n<pre class=\\"language-js\\"><code>// 2. div中放置子元素 h1,p\\nimport { h, init } from &#39;snabbdom&#39;\\n\\nlet patch = init([])\\n\\nlet vnode = h(&#39;div#container&#39;, [\\n  h(&#39;h1&#39;, &#39;Hello Snabbdom&#39;),\\n  h(&#39;p&#39;, &#39;这是一个p标签&#39;)\\n])\\n\\nlet app = document.querySelector(&#39;#app&#39;)\\n\\nlet oldVnode = patch(app, vnode)\\n\\nsetTimeout(() =&gt; {\\n  vnode = h(&#39;div#container&#39;, [\\n    h(&#39;h1&#39;, &#39;Hello World&#39;),\\n    h(&#39;p&#39;, &#39;Hello P&#39;)\\n  ])\\n  patch(oldVnode, vnode)\\n\\n  // 清空页面元素 -- 错误\\n  // patch(oldVnode, null)\\n  patch(oldVnode, h(&#39;!&#39;))\\n}, 2000);\\n</code></pre>\\n<p><strong>例子3 debug-patchVnode</strong></p>\\n<pre class=\\"language-js\\"><code>import { h, init } from &#39;snabbdom&#39;\\n\\nlet patch = init([])\\n\\n// 首次渲染\\nlet vnode = h(&#39;div&#39;, &#39;Hello World&#39;)\\nlet app = document.querySelector(&#39;#app&#39;)\\nlet oldVnode = patch(app, vnode)\\n\\n// patchVnode 的执行过程\\nvnode = h(&#39;div&#39;, &#39;Hello Snabbdom&#39;)\\npatch(oldVnode, vnode)\\n</code></pre>\\n<p><strong>例子4 debug-updateChildren</strong></p>\\n<pre class=\\"language-js\\"><code>import { h, init } from &#39;snabbdom&#39;\\n\\nlet patch = init([])\\n\\n// 首次渲染\\nlet vnode = h(&#39;ul&#39;, [\\n  h(&#39;li&#39;, &#39;首页&#39;),\\n  h(&#39;li&#39;, &#39;视频&#39;),\\n  h(&#39;li&#39;, &#39;微博&#39;)\\n])\\nlet app = document.querySelector(&#39;#app&#39;)\\nlet oldVnode = patch(app, vnode)\\n\\n// updateChildren 的执行过程\\nvnode = h(&#39;ul&#39;, [\\n  h(&#39;li&#39;, &#39;首页&#39;),\\n  h(&#39;li&#39;, &#39;微博&#39;),\\n  h(&#39;li&#39;, &#39;视频&#39;)\\n])\\npatch(oldVnode, vnode)\\n</code></pre>\\n<p><strong>例子5 debug-updateChildren-key</strong></p>\\n<pre class=\\"language-js\\"><code>import { h, init } from &#39;snabbdom&#39;\\n\\nlet patch = init([])\\n\\n// 首次渲染\\nlet vnode = h(&#39;ul&#39;, [\\n  h(&#39;li&#39;, { key: &#39;a&#39; }, &#39;首页&#39;),\\n  h(&#39;li&#39;, { key: &#39;b&#39; }, &#39;视频&#39;),\\n  h(&#39;li&#39;, { key: &#39;c&#39; }, &#39;微博&#39;)\\n])\\nlet app = document.querySelector(&#39;#app&#39;)\\nlet oldVnode = patch(app, vnode)\\n\\n// updateChildren 的执行过程\\nvnode = h(&#39;ul&#39;, [\\n  h(&#39;li&#39;, { key: &#39;a&#39; }, &#39;首页&#39;),\\n  h(&#39;li&#39;, { key: &#39;c&#39; }, &#39;微博&#39;),\\n  h(&#39;li&#39;, { key: &#39;b&#39; }, &#39;视频&#39;)\\n])\\npatch(oldVnode, vnode)\\n</code></pre>\\n<h3 id=\\"模块\\"><a class=\\"header-anchor\\" href=\\"#模块\\">#</a> 模块</h3>\\n<blockquote>\\n<p><code>Snabbdom</code> 的核心库并不能处理元素的<code>属性/样式/事件</code>等，如果需要处理的话，可以使用模块</p>\\n</blockquote>\\n<p><strong>常用模块</strong></p>\\n<p>官方提供了 6 个模块</p>\\n<ul>\\n<li><code>attributes</code>\\n<ul>\\n<li>设置 <code>DOM</code> 元素的属性，使用 <code>setAttribute ()</code></li>\\n<li>处理布尔类型的属性</li>\\n</ul>\\n</li>\\n<li><code>props</code>\\n<ul>\\n<li>和 <code>attributes</code> 模块相似，设置 <code>DOM</code> 元素的属性 <code>element[attr] = value</code></li>\\n<li>不处理布尔类型的属性</li>\\n</ul>\\n</li>\\n<li><code>class</code>\\n<ul>\\n<li>切换类样式</li>\\n<li>注意:给元素设置类样式是通过 <code>sel</code> 选择器</li>\\n</ul>\\n</li>\\n<li><code>dataset</code>\\n<ul>\\n<li>设置 <code>data-*</code> 的自定义属性 <code>eventlisteners</code></li>\\n<li>注册和移除事件</li>\\n</ul>\\n</li>\\n<li><code>style</code>\\n<ul>\\n<li>设置行内样式，支持动画</li>\\n<li><code>delayed/remove/destroy</code></li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>模块使用</strong></p>\\n<blockquote>\\n<p>模块使用步骤:</p>\\n</blockquote>\\n<ul>\\n<li>导入需要的模块</li>\\n<li><code>init()</code> 中注册模块</li>\\n<li>使用 <code>h()</code> 函数创建 <code>VNode</code> 的时候，可以把第二个参数设置为对象，其他参数往后移</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>import { init, h } from &#39;snabbdom&#39;\\n// 1. 导入模块\\nimport style from &#39;snabbdom/modules/style&#39;\\nimport eventlisteners from &#39;snabbdom/modules/eventlisteners&#39;\\n// 2. 注册模块\\nlet patch = init([\\n  style,\\n  eventlisteners\\n])\\n// 3. 使用 h() 函数的第二个参数传入模块需要的数据（对象）\\nlet vnode = h(&#39;div&#39;, {\\n  style: {\\n    backgroundColor: &#39;red&#39;\\n  },\\n  on: {\\n    click: eventHandler\\n  }\\n}, [\\n  h(&#39;h1&#39;, &#39;Hello Snabbdom&#39;),\\n  h(&#39;p&#39;, &#39;这是p标签&#39;)\\n])\\n\\nfunction eventHandler () {\\n  console.log(&#39;点击我了&#39;)\\n}\\n\\nlet app = document.querySelector(&#39;#app&#39;)\\n\\nlet oldVnode = patch(app, vnode)\\n\\n\\nvnode = h(&#39;div&#39;, &#39;hello&#39;)\\npatch(oldVnode, vnode)\\n</code></pre>\\n<h2 id=\\"snabbdom-源码解析\\"><a class=\\"header-anchor\\" href=\\"#snabbdom-源码解析\\">#</a> Snabbdom 源码解析</h2>\\n<h3 id=\\"snabbdom-的核心\\"><a class=\\"header-anchor\\" href=\\"#snabbdom-的核心\\">#</a> Snabbdom 的核心</h3>\\n<ul>\\n<li>使用 <code>h()</code> 函数创建 JavaScript 对象(<code>VNode</code>)描述真实 DOM</li>\\n<li><code>init()</code> 设置模块，创建 <code>patch()</code></li>\\n<li><code>patch()</code> 比较新旧两个 <code>VNode</code></li>\\n<li>把变化的内容更新到真实 <code>DOM</code> 树上</li>\\n</ul>\\n<p><strong>Snabbdom 源码</strong></p>\\n<blockquote>\\n<p>源码地址: <a href=\\"https://github.com/snabbdom/snabbdom\\">https://github.com/snabbdom/snabbdom</a></p>\\n</blockquote>\\n<p>src 目录结构</p>\\n<p><img src=\\"https://s.poetries.top/images/20210328125857.png\\" alt=\\"\\"></p>\\n<h3 id=\\"h-函数\\"><a class=\\"header-anchor\\" href=\\"#h-函数\\">#</a> h 函数</h3>\\n<ul>\\n<li><code>h()</code> 函数介绍: 在使用 <code>Vue</code> 的时候见过 <code>h()</code> 函数</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>new Vue({\\n  router,\\n  store,\\n  render: h =&gt; h(App) \\n}).$mount(&#39;#app&#39;)\\n</code></pre>\\n<ul>\\n<li><code>h()</code> 函数最早见于 <code>hyperscript</code>，使用 JavaScript 创建超文本</li>\\n<li>Snabbdom 中的 <code>h()</code> 函数不是用来创建超文本，而是创建 <code>VNode</code></li>\\n</ul>\\n<p><strong>函数重载</strong></p>\\n<ul>\\n<li>概念</li>\\n<li>参数个数或类型不同的函数</li>\\n<li><code>JavaScript</code> 中没有重载的概念</li>\\n<li><code>TypeScript</code> 中有重载，不过重载的实现还是通过代码调整参数</li>\\n<li>重载的示意</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>function add (a, b) { \\n  console.log(a + b)\\n}\\nfunction add (a, b, c) {\\n  console.log(a + b + c) \\n}\\nadd(1, 2)\\nadd(1, 2, 3)\\n</code></pre>\\n<blockquote>\\n<p>源码位置:<code>src/h.ts</code></p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// h函数的重载\\nexport function h (sel: string): VNode\\nexport function h (sel: string, data: VNodeData | null): VNode\\nexport function h (sel: string, children: VNodeChildren): VNode\\nexport function h (sel: string, data: VNodeData | null, children: VNodeChildren): VNode\\nexport function h (sel: any, b?: any, c?: any): VNode {\\n  let data: VNodeData = {}\\n  let children: any\\n  let text: any\\n  let i: number\\n  // 处理参数，实现重载的机制\\n  if (c !== undefined) {\\n    // 处理三个参数的情况\\n    // sel、data、children/text\\n    if (b !== null) {\\n      data = b\\n    }\\n    if (is.array(c)) {\\n      children = c\\n      // 如果 c 是字符串或者数字\\n    } else if (is.primitive(c)) {\\n      text = c\\n    } else if (c &amp;&amp; c.sel) {\\n      children = [c]\\n    }\\n  } else if (b !== undefined &amp;&amp; b !== null) {\\n    // 处理两个参数的情况\\n    if (is.array(b)) {\\n      children = b\\n      // 如果 b 是字符串或者数字\\n    } else if (is.primitive(b)) {\\n      text = b\\n      // 如果 b 是 VNode\\n    } else if (b &amp;&amp; b.sel) {\\n      children = [b]\\n    } else { data = b }\\n  }\\n  if (children !== undefined) {\\n    // 处理 children 中的原始值(string/number)\\n    for (i = 0; i &lt; children.length; ++i) {\\n      // 如果 child 是 string/number，创建文本节点\\n      if (is.primitive(children[i])) children[i] = vnode(undefined, undefined, undefined, children[i], undefined)\\n    }\\n  }\\n  if (\\n    sel[0] === &#39;s&#39; &amp;&amp; sel[1] === &#39;v&#39; &amp;&amp; sel[2] === &#39;g&#39; &amp;&amp;\\n    (sel.length === 3 || sel[3] === &#39;.&#39; || sel[3] === &#39;#&#39;)\\n  ) {\\n    // 如果是 svg，添加命名空间\\n    addNS(data, children, sel)\\n  }\\n  // 返回 VNode\\n  return vnode(sel, data, children, text, undefined)\\n};\\n\\n// 导出模块\\nexport default h;\\n</code></pre>\\n<h3 id=\\"vnode\\"><a class=\\"header-anchor\\" href=\\"#vnode\\">#</a> VNode</h3>\\n<blockquote>\\n<p>一个 <code>VNode</code> 就是一个虚拟节点用来描述一个 <code>DOM</code> 元素，如果这个 <code>VNode</code> 有 <code>children</code> 就是 <code>Virtual DOM</code></p>\\n</blockquote>\\n<p>源码位置: <code>src/vnode.ts</code></p>\\n<pre class=\\"language-js\\"><code>export interface VNodeData {\\n  props?: Props\\n  attrs?: Attrs\\n  class?: Classes\\n  style?: VNodeStyle\\n  dataset?: Dataset\\n  on?: On\\n  hero?: Hero\\n  attachData?: AttachData\\n  hook?: Hooks\\n  key?: Key\\n  ns?: string // for SVGs\\n  fn?: () =&gt; VNode // for thunks\\n  args?: any[] // for thunks\\n  is?: string // for custom elements v1\\n  [key: string]: any // for any other 3rd party module\\n}\\n\\nexport interface VNode {\\n  // 选择器\\n  sel: string | undefined\\n  // 节点数据:属性/样式/事件等\\n  data: VNodeData | undefined\\n  // 子节点，和 text 只能互斥\\n  children: Array&lt;VNode | string&gt; | undefined\\n  // 记录 vnode 对应的真实 DOM\\n  elm: Node | undefined\\n  // 节点中的内容，和 children 只能互斥\\n  text: string | undefined\\n  // 优化用\\n  key: Key | undefined\\n}\\n\\nexport function vnode (sel: string | undefined,\\n  data: any | undefined,\\n  children: Array&lt;VNode | string&gt; | undefined,\\n  text: string | undefined,\\n  elm: Element | Text | undefined): VNode {\\n  const key = data === undefined ? undefined : data.key\\n  return { sel, data, children, text, elm, key }\\n}\\n</code></pre>\\n<h3 id=\\"snabbdom\\"><a class=\\"header-anchor\\" href=\\"#snabbdom\\">#</a> snabbdom</h3>\\n<ul>\\n<li><code>patch(oldVnode, newVnode)</code></li>\\n<li>打补丁，把新节点中变化的内容渲染到真实 <code>DOM</code>，最后返回新节点作为下一次处理的旧节点</li>\\n<li>对比新旧 <code>VNode</code> 是否相同节点(节点的 <code>key</code> 和 <code>sel</code> 相同)</li>\\n<li>如果不是相同节点，删除之前的内容，重新渲染</li>\\n<li>如果是相同节点，再判断新的 <code>VNode</code> 是否有 <code>text</code>，如果有并且和 <code>oldVnode</code> 的 <code>text</code> 不同，直接更 新文本内容</li>\\n<li>如果新的 <code>VNode</code> 有 <code>children</code>，判断子节点是否有变化，判断子节点的过程使用的就是 <code>diff</code> 算法</li>\\n<li><code>diff</code> 过程只进行同层级比较</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210329091820.png\\" alt=\\"\\"></p>\\n<h3 id=\\"init\\"><a class=\\"header-anchor\\" href=\\"#init\\">#</a> init</h3>\\n<ul>\\n<li>功能: <code>init(modules, domApi)</code>，返回 <code>patch()</code>函数(高阶函数)</li>\\n<li>为什么要使用高阶函数?\\n<ul>\\n<li>因为 <code>patch()</code> 函数再外部会调用多次，每次调用依赖一些参数，比如: <code>modules/domApi/cbs</code></li>\\n<li>通过高阶函数让 <code>init()</code> 内部形成闭包，返回的 <code>patch()</code> 可以访问到 <code>modules/domApi/cbs</code>，而 不需要重新创建</li>\\n</ul>\\n</li>\\n<li><code>init()</code> 在返回 <code>patch()</code> 之前，首先收集了所有模块中的钩子函数存储到 <code>cbs</code> 对象中</li>\\n</ul>\\n<blockquote>\\n<p>源码位置:<code>src/init.ts</code></p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>export function init (modules: Array&lt;Partial&lt;Module&gt;&gt;, domApi?: DOMAPI) {\\n  let i: number\\n  let j: number\\n  const cbs: ModuleHooks = {\\n    create: [],\\n    update: [],\\n    remove: [],\\n    destroy: [],\\n    pre: [],\\n    post: []\\n  }\\n\\n  const api: DOMAPI = domApi !== undefined ? domApi : htmlDomApi\\n\\n  for (i = 0; i &lt; hooks.length; ++i) {\\n    cbs[hooks[i]] = []\\n    for (j = 0; j &lt; modules.length; ++j) {\\n      const hook = modules[j][hooks[i]]\\n      if (hook !== undefined) {\\n        (cbs[hooks[i]] as any[]).push(hook)\\n      }\\n    }\\n  }\\n  ...\\n\\n  return function patch (oldVnode: VNode | Element, vnode: VNode): VNode {\\n    let i: number, elm: Node, parent: Node\\n    const insertedVnodeQueue: VNodeQueue = []\\n    for (i = 0; i &lt; cbs.pre.length; ++i) cbs.pre[i]()\\n\\n    if (!isVnode(oldVnode)) {\\n      oldVnode = emptyNodeAt(oldVnode)\\n    }\\n\\n    if (sameVnode(oldVnode, vnode)) {\\n      patchVnode(oldVnode, vnode, insertedVnodeQueue)\\n    } else {\\n      elm = oldVnode.elm!\\n      parent = api.parentNode(elm) as Node\\n\\n      createElm(vnode, insertedVnodeQueue)\\n\\n      if (parent !== null) {\\n        api.insertBefore(parent, vnode.elm!, api.nextSibling(elm))\\n        removeVnodes(parent, [oldVnode], 0, 0)\\n      }\\n    }\\n\\n    for (i = 0; i &lt; insertedVnodeQueue.length; ++i) {\\n      insertedVnodeQueue[i].data!.hook!.insert!(insertedVnodeQueue[i])\\n    }\\n    for (i = 0; i &lt; cbs.post.length; ++i) cbs.post[i]()\\n    return vnode\\n  }\\n}\\n</code></pre>\\n<h3 id=\\"patch\\"><a class=\\"header-anchor\\" href=\\"#patch\\">#</a> patch</h3>\\n<ul>\\n<li>功能:\\n<ul>\\n<li>传入新旧 <code>VNode</code>，对比差异，把差异渲染到 <code>DOM</code></li>\\n<li>返回新的 <code>VNode</code>，作为下一次 <code>patch()</code> 的 <code>oldVnode</code></li>\\n</ul>\\n</li>\\n<li>执行过程:\\n<ul>\\n<li>首先执行模块中的钩子函数 <code>pre</code></li>\\n<li>如果 <code>oldVnode</code> 和 <code>vnode</code> 相同(<code>key</code> 和 <code>sel</code> 相同)\\n<ul>\\n<li>调用 <code>patchVnode()</code>，找节点的差异并更新 <code>DOM</code></li>\\n</ul>\\n</li>\\n<li>如果 <code>oldVnode</code> 是 <code>DOM</code> 元素\\n<ul>\\n<li>把 <code>DOM</code> 元素转换成 <code>oldVnode</code></li>\\n<li>调用 <code>createElm()</code> 把 <code>vnode</code> 转换为真实 <code>DOM</code>，记录到 <code>vnode.elm</code></li>\\n<li>把刚创建的 <code>DOM</code> 元素插入到 <code>parent</code> 中</li>\\n<li>移除老节点</li>\\n<li>触发用户设置的 <code>create</code> 钩子函数</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n<p>源码位置:<code>src/snabbdom.ts</code></p>\\n<pre class=\\"language-js\\"><code>return function patch (oldVnode: VNode | Element, vnode: VNode): VNode {\\n  let i: number, elm: Node, parent: Node\\n  // 保存新插入节点的队列，为了触发钩子函数\\n  const insertedVnodeQueue: VNodeQueue = []\\n  // 执行模块的 pre 钩子函数\\n  for (i = 0; i &lt; cbs.pre.length; ++i) cbs.pre[i]()\\n\\n  // 如果 oldVnode 不是 VNode，创建 VNode 并设置 elm\\n  if (!isVnode(oldVnode)) {\\n    // 把 DOM 元素转换成空的 VNode\\n    oldVnode = emptyNodeAt(oldVnode)\\n  }\\n  // 如果新旧节点是相同节点(key 和 sel 相同)\\n  if (sameVnode(oldVnode, vnode)) {\\n    // 找节点的差异并更新 DOM\\n    patchVnode(oldVnode, vnode, insertedVnodeQueue)\\n  } else {\\n    // 如果新旧节点不同，vnode 创建对应的 DOM\\n    // 获取当前的 DOM 元素\\n    elm = oldVnode.elm!\\n    parent = api.parentNode(elm) as Node\\n    // 触发 init/create 钩子函数,创建 DOM\\n    createElm(vnode, insertedVnodeQueue)\\n\\n    if (parent !== null) {\\n      // 如果父节点不为空，把 vnode 对应的 DOM 插入到文档中\\n      api.insertBefore(parent, vnode.elm!, api.nextSibling(elm))\\n      // 移除老节点\\n      removeVnodes(parent, [oldVnode], 0, 0)\\n    }\\n  }\\n  // 执行用户设置的 insert 钩子函数 \\n  for (i = 0; i &lt; insertedVnodeQueue.length; ++i) {\\n    insertedVnodeQueue[i].data!.hook!.insert!(insertedVnodeQueue[i])\\n  }\\n  // 执行模块的 post 钩子函数\\n  for (i = 0; i &lt; cbs.post.length; ++i) cbs.post[i]()\\n  // 返回 vnode\\n  return vnode\\n}\\n</code></pre>\\n<h3 id=\\"createelm\\"><a class=\\"header-anchor\\" href=\\"#createelm\\">#</a> createElm</h3>\\n<ul>\\n<li>\\n<p>功能:</p>\\n<ul>\\n<li><code>createElm(vnode, insertedVnodeQueue)</code>，返回创建的 <code>DOM</code> 元素</li>\\n<li>创建 <code>vnode</code> 对应的 <code>DOM</code> 元素</li>\\n</ul>\\n</li>\\n<li>\\n<p>执行过程:</p>\\n</li>\\n<li>\\n<p>首先触发用户设置的 <code>init</code> 钩子函数</p>\\n</li>\\n<li>\\n<p>如果选择器是<code>!</code>，创建评论节点</p>\\n</li>\\n<li>\\n<p>如果选择器为空，创建文本节点</p>\\n</li>\\n<li>\\n<p>如果选择器不为空</p>\\n<ul>\\n<li>解析选择器，设置标签的 <code>id</code> 和 <code>class</code> 属性</li>\\n<li>执行模块的 <code>create</code> 钩子函数</li>\\n<li>如果 <code>vnode</code> 有 <code>children</code>，创建子 <code>vnode</code> 对应的 <code>DOM</code>，追加到 <code>DOM</code> 树</li>\\n<li>如果 <code>vnode</code> 的 <code>text</code> 值是 <code>string/number</code>，创建文本节点并追击到 <code>DOM</code> 树</li>\\n<li>执行用户设置的 <code>create</code> 钩子函数</li>\\n<li>如果有用户设置的 <code>insert</code> 钩子函数，把 <code>vnode</code> 添加到队列中</li>\\n</ul>\\n</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>function createElm (vnode: VNode, insertedVnodeQueue: VNodeQueue): Node {\\n  let i: any\\n  let data = vnode.data\\n  if (data !== undefined) {\\n    // 执行用户设置的 init 钩子函数\\n    const init = data.hook?.init\\n    if (isDef(init)) {\\n      init(vnode)\\n      data = vnode.data\\n    }\\n  }\\n  const children = vnode.children\\n  const sel = vnode.sel\\n  if (sel === &#39;!&#39;) {\\n    // 如果选择器是!，创建评论节点\\n    if (isUndef(vnode.text)) {\\n      vnode.text = &#39;&#39;\\n    }\\n    vnode.elm = api.createComment(vnode.text!)\\n  } else if (sel !== undefined) {\\n    // Parse selector\\n    // 如果选择器不为空\\n    // 解析选择器\\n    // Parse selector\\n    const hashIdx = sel.indexOf(&#39;#&#39;)\\n    const dotIdx = sel.indexOf(&#39;.&#39;, hashIdx)\\n    const hash = hashIdx &gt; 0 ? hashIdx : sel.length\\n    const dot = dotIdx &gt; 0 ? dotIdx : sel.length\\n    const tag = hashIdx !== -1 || dotIdx !== -1 ? sel.slice(0, Math.min(hash, dot)) : sel\\n    const elm = vnode.elm = isDef(data) &amp;&amp; isDef(i = data.ns)\\n      ? api.createElementNS(i, tag, data)\\n      : api.createElement(tag, data)\\n    if (hash &lt; dot) elm.setAttribute(&#39;id&#39;, sel.slice(hash + 1, dot))\\n    if (dotIdx &gt; 0) elm.setAttribute(&#39;class&#39;, sel.slice(dot + 1).replace(/\\\\./g, &#39; &#39;))\\n    // 执行模块的 create 钩子函数\\n    for (i = 0; i &lt; cbs.create.length; ++i) cbs.create[i](emptyNode, vnode)\\n    // 如果 vnode 中有子节点，创建子 vnode 对应的 DOM 元素并追加到 DOM 树上\\n    if (is.array(children)) {\\n      for (i = 0; i &lt; children.length; ++i) {\\n        const ch = children[i]\\n        if (ch != null) {\\n          api.appendChild(elm, createElm(ch as VNode, insertedVnodeQueue))\\n        }\\n      }\\n    } else if (is.primitive(vnode.text)) {\\n      // 如果 vnode 的 text 值是 string/number，创建文本节点并追加到 DOM 树\\n      api.appendChild(elm, api.createTextNode(vnode.text))\\n    }\\n    const hook = vnode.data!.hook\\n    if (isDef(hook)) {\\n      // 执行用户传入的钩子 create\\n      hook.create?.(emptyNode, vnode)\\n      if (hook.insert) {\\n        // 把 vnode 添加到队列中，为后续执行 insert 钩子做准备\\n        insertedVnodeQueue.push(vnode)\\n      }\\n    }\\n  } else {\\n    // 如果选择器为空，创建文本节点\\n    vnode.elm = api.createTextNode(vnode.text!)\\n  }\\n  // 返回新创建的 DOM\\n  return vnode.elm\\n}\\n</code></pre>\\n<h3 id=\\"patchvnode\\"><a class=\\"header-anchor\\" href=\\"#patchvnode\\">#</a> patchVnode</h3>\\n<ul>\\n<li>功能:\\n<ul>\\n<li><code>patchVnode(oldVnode, vnode, insertedVnodeQueue)</code></li>\\n<li>对比 <code>oldVnode</code> 和 <code>vnode</code> 的差异，把差异渲染到 <code>DOM</code></li>\\n</ul>\\n</li>\\n<li>执行过程:\\n<ul>\\n<li>首先执行用户设置的 <code>prepatch</code> 钩子函数</li>\\n<li>执行 <code>create</code> 钩子函数\\n<ul>\\n<li>首先执行模块的 <code>create</code>钩子函数</li>\\n<li>然后执行用户设置的 <code>create</code> 钩子函数</li>\\n</ul>\\n</li>\\n<li>如果 <code>vnode.text</code> 未定义\\n<ul>\\n<li>如果 <code>oldVnode.children</code> 和 <code>vnode.children</code> 都有值\\n<ul>\\n<li>调用 <code>updateChildren()</code></li>\\n<li>使用 <code>diff</code> 算法对比子节点，更新子节点</li>\\n</ul>\\n</li>\\n<li>如果 <code>vnode.children</code> 有值， <code>oldVnode.children</code> 无值\\n<ul>\\n<li>清空 <code>DOM</code> 元素</li>\\n<li>调用 <code>addVnodes()</code> ，批量添加子节点</li>\\n</ul>\\n</li>\\n<li>如果 <code>oldVnode.children</code> 有值， <code>vnode.children</code> 无值\\n<ul>\\n<li>调用 <code>removeVnodes()</code> ，批量移除子节点</li>\\n</ul>\\n</li>\\n<li>如果 <code>oldVnode.text</code> 有值\\n<ul>\\n<li>清空 <code>DOM</code> 元素的内容</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li>如果设置了 <code>vnode.text</code> 并且和和 <code>oldVnode.text</code> 不等\\n<ul>\\n<li>如果老节点有子节点，全部移除</li>\\n<li>设置 <code>DOM</code> 元素的 <code>textContent</code> 为 <code>vnode.text</code></li>\\n</ul>\\n</li>\\n<li>最后执行用户设置的 <code>postpatch</code> 钩子函数</li>\\n</ul>\\n</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>function patchVnode (oldVnode: VNode, vnode: VNode, insertedVnodeQueue: VNodeQueue) {\\n  const hook = vnode.data?.hook\\n  // 首先执行用户设置的 prepatch 钩子函数\\n  hook?.prepatch?.(oldVnode, vnode)\\n  const elm = vnode.elm = oldVnode.elm!\\n  const oldCh = oldVnode.children as VNode[]\\n  const ch = vnode.children as VNode[]\\n  // 如果新老 vnode 相同返回\\n  if (oldVnode === vnode) return\\n  if (vnode.data !== undefined) {\\n    // 执行模块的 update 钩子函数\\n    for (let i = 0; i &lt; cbs.update.length; ++i) cbs.update[i](oldVnode, vnode)\\n    // 执行用户设置的 update 钩子函数\\n    vnode.data.hook?.update?.(oldVnode, vnode)\\n  }\\n  // 如果 vnode.text 未定义\\n  if (isUndef(vnode.text)) {\\n    // 如果新老节点都有 children\\n    if (isDef(oldCh) &amp;&amp; isDef(ch)) {\\n      // 使用 diff 算法对比子节点，更新子节点\\n      if (oldCh !== ch) updateChildren(elm, oldCh, ch, insertedVnodeQueue)\\n    } else if (isDef(ch)) {\\n      // 如果新节点有 children，老节点没有 children\\n      // 如果老节点有text，清空dom 元素的内容\\n      if (isDef(oldVnode.text)) api.setTextContent(elm, &#39;&#39;)\\n      // 批量添加子节点\\n      addVnodes(elm, null, ch, 0, ch.length - 1, insertedVnodeQueue)\\n    } else if (isDef(oldCh)) {\\n      // 如果老节点有children，新节点没有children\\n      // 批量移除子节点\\n      removeVnodes(elm, oldCh, 0, oldCh.length - 1)\\n    } else if (isDef(oldVnode.text)) {\\n      // 如果老节点有 text，清空 DOM 元素\\n      api.setTextContent(elm, &#39;&#39;)\\n    }\\n  } else if (oldVnode.text !== vnode.text) {\\n    // 如果没有设置 vnode.text\\n    if (isDef(oldCh)) {\\n      // 如果老节点有 children，移除\\n      removeVnodes(elm, oldCh, 0, oldCh.length - 1)\\n    }\\n    // 设置 DOM 元素的 textContent 为 vnode.text\\n    api.setTextContent(elm, vnode.text!)\\n  }\\n  // 最后执行用户设置的 postpatch 钩子函数\\n  hook?.postpatch?.(oldVnode, vnode)\\n}\\n</code></pre>\\n<h3 id=\\"updatechildren\\"><a class=\\"header-anchor\\" href=\\"#updatechildren\\">#</a> updateChildren</h3>\\n<ul>\\n<li>功能:\\n<ul>\\n<li><code>diff</code> 算法的核心，对比新旧节点的 <code>children</code>，更新 <code>DOM</code></li>\\n</ul>\\n</li>\\n<li>执行过程:</li>\\n<li>要对比两棵树的差异，我们可以取第一棵树的每一个节点依次和第二课树的每一个节点比 较，但是这样的时间复杂度为 <code>O(n^3)</code></li>\\n<li>在 <code>DOM</code> 操作的时候我们很少很少会把一个父节点移动/更新到某一个子节点</li>\\n<li>因此只需要找同级别的子节点依次比较，然后再找下一级别的节点比较，这样算法的时间复 杂度为 <code>O(n)</code></li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210329092027.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>在进行同级别节点比较的时候，首先会对新老节点数组的开始和结尾节点设置标记索引，遍 历的过程中移动索引</li>\\n<li>在对开始和结束节点比较的时候，总共有四种情况\\n<ul>\\n<li><code>oldStartVnode / newStartVnode</code> (旧开始节点 / 新开始节点)</li>\\n<li><code>oldEndVnode / newEndVnode</code> (旧结束节点 / 新结束节点)</li>\\n<li><code>oldStartVnode / oldEndVnode</code> (旧开始节点 / 新结束节点)</li>\\n<li><code>oldEndVnode / newStartVnode</code> (旧结束节点 / 新开始节点)</li>\\n</ul>\\n</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210329092301.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>开始节点和结束节点比较，这两种情况类似\\n<ul>\\n<li><code>oldStartVnode / newStartVnode</code> (旧开始节点 / 新开始节点)</li>\\n<li><code>oldEndVnode / newEndVnode</code> (旧结束节点 / 新结束节点)</li>\\n</ul>\\n</li>\\n<li>如果 <code>oldStartVnode</code> 和 <code>newStartVnode</code> 是 <code>sameVnode</code> (<code>key</code> 和 <code>sel</code> 相同)\\n<ul>\\n<li>调用 <code>patchVnode()</code> 对比和更新节点</li>\\n<li>把旧开始和新开始索引往后移动 <code>oldStartIdx++ / oldEndIdx++</code></li>\\n</ul>\\n</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210329092430.png\\" alt=\\"\\"></p>\\n<ul>\\n<li><code>oldStartVnode / newEndVnode</code> (旧开始节点 / 新结束节点) 相同\\n<ul>\\n<li>调用 <code>patchVnode()</code> 对比和更新节点</li>\\n<li>把 <code>oldStartVnode</code> 对应的 <code>DOM</code> 元素，移动到右边\\n<ul>\\n<li>更新索引</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210329092820.png\\" alt=\\"\\"></p>\\n<ul>\\n<li><code>oldEndVnode / newStartVnode</code> (旧结束节点 / 新开始节点) 相同\\n<ul>\\n<li>调用 <code>patchVnode()</code> 对比和更新节点</li>\\n<li>把 <code>oldEndVnode</code> 对应的 <code>DOM</code> 元素，移动到左边</li>\\n<li>更新索引</li>\\n</ul>\\n</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210329093025.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>如果不是以上四种情况\\n<ul>\\n<li>遍历新节点，使用 <code>newStartNode</code> 的 <code>key</code> 在老节点数组中找相同节点</li>\\n<li>如果没有找到，说明 <code>newStartNode</code> 是新节点\\n<ul>\\n<li>创建新节点对应的 DOM 元素，插入到 DOM 树中</li>\\n</ul>\\n</li>\\n<li>如果找到了\\n<ul>\\n<li>判断新节点和找到的老节点的 <code>sel</code> 选择器是否相同</li>\\n<li>如果不相同，说明节点被修改了\\n<ul>\\n<li>重新创建对应的 <code>DOM</code> 元素，插入到 DOM 树中</li>\\n</ul>\\n</li>\\n<li>如果相同，把 <code>elmToMove</code> 对应的 DOM 元素，移动到左边</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210329093137.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>循环结束\\n<ul>\\n<li>当老节点的所有子节点先遍历完 (<code>oldStartIdx &gt; oldEndIdx</code>)，循环结束</li>\\n<li>新节点的所有子节点先遍历完 (<code>newStartIdx &gt; newEndIdx</code>)，循环结束</li>\\n</ul>\\n</li>\\n<li>如果老节点的数组先遍历完(<code>oldStartIdx &gt; oldEndIdx</code>)，说明新节点有剩余，把剩余节点批量插入到右边</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210329093302.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>如果新节点的数组先遍历完(<code>newStartIdx &gt; newEndIdx</code>)，说明老节点有剩余，把剩余节点批 量删除</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210329093414.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个父节点下有 300 个带 <code>key</code> 的列表项，同事准备让新旧树任意跨层两两比较，声称这样最准确，你会接受吗？</strong></p>\\n<p>不会，<code>updateChildren</code> 利用了 <code>DOM</code> 更新很少跨父子层级移动的前提，只比较同级子节点，再逐层处理。任意节点两两甚至继续向下比较会把复杂度推高到文中所述的 <code>O(n^3)</code>；同级比较可降到 <code>O(n)</code>，代价是不能把跨层复用当作常规能力。</p>\\n</li>\\n<li>\\n<p><strong>消息列表从 <code>[A,B,C,D]</code> 更新为 <code>[D,A,B,C]</code>，四项的 <code>key</code> 和 <code>sel</code> 都稳定，<code>updateChildren</code> 会怎样减少创建节点？</strong></p>\\n<p>旧尾节点 <code>D</code> 与新首节点 <code>D</code> 满足 <code>sameVnode</code>，算法先调用 <code>patchVnode()</code>，再把 <code>D</code> 对应的真实 <code>DOM</code> 移到左侧并更新索引。其余节点继续通过首尾标记比较，因此主要发生移动和局部更新，而不是销毁后重建整列。</p>\\n</li>\\n<li>\\n<p><strong>表格原来有 <code>[A,B]</code>，更新后变成 <code>[A,B,C,D]</code>；另一轮又从 <code>[A,B,C]</code> 变成 <code>[A]</code>，循环结束后的收尾逻辑分别是什么？</strong></p>\\n<p>旧子节点先遍历完时，说明新数组仍有节点，剩余的 <code>C</code>、<code>D</code> 应批量插入右侧。新子节点先遍历完时，则说明旧数组还有冗余节点，需要批量删除；收尾条件依赖 <code>oldStartIdx &gt; oldEndIdx</code> 或 <code>newStartIdx &gt; newEndIdx</code>，边界写错容易漏增漏删。</p>\\n</li>\\n<li>\\n<p><strong>线上排序后某一行内容正确但 <code>DOM</code> 类型不对，日志显示新首节点的 <code>key</code> 能在旧数组找到，而两者 <code>sel</code> 不同，你会怎样定位？</strong></p>\\n<p>先确认 <code>key</code> 查找命中了哪一个旧节点，再核对新旧节点的 <code>sel</code>，因为只有两者同时匹配才适合移动复用。当前场景应创建新的 <code>DOM</code> 并插入，而不能搬用旧元素；若强行复用，节点类型或属性更新就可能落在错误载体上。</p>\\n</li>\\n<li>\\n<p><strong>团队争论列表更新只做首尾四种比较，还是一开始就为全部旧节点建索引，你会怎样解释当前算法的取舍？</strong></p>\\n<p>首尾四种比较优先覆盖头对头、尾对尾以及两类首尾交叉移动，命中后只需 <code>patchVnode()</code>、移动 <code>DOM</code> 和推进索引。四种情况都不命中时，再用 <code>newStartVnode.key</code> 到旧数组查找；这样保留常见序列变化的快速路径，但 <code>key</code> 和 <code>sel</code> 必须稳定。</p>\\n</li>\\n<li>\\n<p><strong>新首节点的 <code>key</code> 在旧子节点中完全找不到，和找到同 <code>key</code> 但不同 <code>sel</code>，<code>updateChildren</code> 的处理为什么不能合并？</strong></p>\\n<p>找不到 <code>key</code> 表示它是新增节点，应创建对应 <code>DOM</code> 后插入；找到 <code>key</code> 却 <code>sel</code> 不同，则表示原身份线索存在但节点类型已改变，同样需要新建 <code>DOM</code>。两者创建原因不同，前者是新增，后者是不可复用；区分日志有助于排查错误 <code>key</code> 和结构变更。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);