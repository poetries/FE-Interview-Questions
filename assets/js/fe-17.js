(window.webpackJsonp=window.webpackJsonp||[]).push([[17],{1124:function(e){e.exports=JSON.parse('{"html":"<p>字符串在算法面试中，单独考察的机会并不多，同样倾向于和一些经典算法（后面会讲的）结合来体现区分度。步子不能跨太大，不然容易扯着x。本节我们照样是先解决只需要数据结构知识做基础就可以解决的字符串问题。</p>\\n<p>在讲题之前，我首先要给大家点拨两个字符串相关的“基本算法技能”。这两个技能偶尔也会单独命题，但整体来看在综合性题目中的考察频率较高，需要大家着重熟悉、反复练习和记忆，确保真正做题时万无一失。</p>\\n<h2 id=\\"基本算法技能\\"><a class=\\"header-anchor\\" href=\\"#基本算法技能\\">#</a> 基本算法技能</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>JavaScript 字符串不可变，所谓原地交换通常要先转字符数组，完成后再 <code>join(\'\')</code></li>\\n<li>字符串题先确认比较单位是 UTF-16 code unit、Unicode code point 还是用户看到的字素簇，emoji 可能占多个 code unit</li>\\n<li>反转、回文常用左右指针把额外空间降为 <code>O(1)</code>（对字符数组而言），并把边界写成 <code>left &lt; right</code></li>\\n</ul>\\n<p><strong>做字符串算法前，先要明确题目中的“字符”和“相等”按什么口径计算，再选择双指针、计数或滑动窗口。</strong> 只处理英文小写字母时按索引访问通常够用，但遇到 <code>emoji</code>、组合音标或多语言文本，就要区分 <code>UTF-16 code unit</code>、<code>Unicode</code> 码点和字素簇。反转或回文判断常用左右指针；由于 <code>JavaScript</code> 字符串不可变，需要修改时通常先转成字符数组，处理后再 <code>join(\'\')</code>。空串、单字符、重复字符和超长输入也都应该覆盖。</p>\\n</section>\\n<p>字符串算法的第一步不是立刻写循环，而是定义“字符”和“相等”。只处理英文小写字母时，按索引读取通常足够；一旦输入包含 emoji、组合音标或多语言文本，就要区分 <code>UTF-16 code unit</code>、<code>Unicode code point</code> 与用户看到的字素簇。这个口径会直接决定长度、切分、反转和回文判断是否正确。</p>\\n<p>确定字符语义后，再根据访问模式选工具：两端向中间收缩用双指针，统计出现次数用 <code>Map</code>，连续区间约束用滑动窗口，复杂模式匹配则考虑状态机或动态规划。每个方案都要补空串、单字符、重复字符和超长输入，不能只验证 ASCII 样例。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>用户资料页限制昵称最多 10 个“字符”，但 <code>\'😀\'.length</code> 得到 <code>2</code>；产品、前端和后端在评审时必须先统一什么口径？</strong></p>\\n<p>必须先明确“字符”指 <code>UTF-16 code unit</code>、<code>Unicode</code> <code>code</code> <code>point</code>，还是用户看到的字素簇。<code>JavaScript</code> 的 <code>length</code> 按 <code>code</code> <code>unit</code> 计数，<code>Array.from()</code> 按 <code>code</code> <code>point</code> 拆分；面向视觉字符的限制通常还需 <code>Intl.Segmenter</code>。</p>\\n</li>\\n<li>\\n<p><strong>搜索框只处理英文小写字母，候选人仍为每次输入建立复杂分词对象；你会如何权衡正确性与实现成本？</strong></p>\\n<p>若输入契约确实限定为英文小写字母，按索引读取已经能满足字符语义，没有必要引入字素簇分段。关键是把限制写进校验和测试；一旦允许 <code>emoji</code>、组合音标或多语言文本，就必须重新选择切分口径。</p>\\n</li>\\n<li>\\n<p><strong>文本审核功能从“判断是否回文”扩展为“找出最长的不重复连续片段”，原来的两端双指针还能直接复用吗？</strong></p>\\n<p>两端收缩适合比较首尾关系，不能直接维护任意连续区间内的重复约束。应改用滑动窗口，并借助 <code>Map</code> 记录字符位置或计数；窗口中的字符单位仍须与产品定义保持一致，否则 <code>Unicode</code> 输入会产生偏差。</p>\\n</li>\\n<li>\\n<p><strong>线上只有带组合重音的姓名出现长度校验和回文判断不一致，<code>ASCII</code> 与普通 <code>emoji</code> 测试都通过；你会怎样定位？</strong></p>\\n<p>先构造由基础字符和组合标记组成的最小样例，分别打印 <code>length</code>、<code>Array.from()</code> 结果与字素簇分段结果。若两个功能采用了不同字符口径，应统一到同一分段层；规范化是否需要引入，则要由“相等”的业务定义决定。</p>\\n</li>\\n<li>\\n<p><strong>日志分析要统计超长文本中每种符号的出现次数，团队在普通对象与 <code>Map</code> 之间选型；你会提醒哪些边界？</strong></p>\\n<p><code>Map</code> 能直接以分段后的字符为键，并避免普通对象原型键带来的额外处理，适合表达频次表。真正影响结果的仍是分段单位和输入规模；完整统计需要随不同字符数增长的空间，无法仅靠换容器消除。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"反转字符串\\"><a class=\\"header-anchor\\" href=\\"#反转字符串\\">#</a> 反转字符串</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>JavaScript 字符串不可变，反转会创建新结果；最常见实现是拆分字符、原地交换、再拼接</li>\\n<li><code>split(\'\')</code> 按 UTF-16 code unit 拆分，会破坏由代理对组成的 emoji；<code>Array.from()</code> 或 <code>[...text]</code> 至少按 code point 拆分</li>\\n<li>code point 仍不等于用户看到的字素簇，组合音标、家庭 emoji 等要用 <code>Intl.Segmenter</code></li>\\n<li>双指针交换时间 <code>O(n)</code>、额外空间 <code>O(n)</code>，因为字符串不可变且需要字符数组</li>\\n<li>面试前先确认题目处理 ASCII、小写字母、Unicode code point，还是用户可见字符</li>\\n</ul>\\n<p><strong>反转字符串通常先转成字符数组，再用左右指针交换，最后通过 <code>join(\'\')</code> 拼回字符串。</strong> 因为 <code>JavaScript</code> 字符串不可变，这个过程的时间和额外空间都是 <code>O(n)</code>。纯英文可以用 <code>split(\'\')</code>，但它会拆坏 <code>emoji</code> 的代理对；处理 <code>Unicode</code> <code>code</code> <code>point</code> 时应改用 <code>Array.from()</code>。如果要求按用户看到的字符反转，组合音标和家庭 <code>emoji</code> 还要通过 <code>Intl.Segmenter</code> 按字素簇分段。</p>\\n</section>\\n<p>原文的 <code>split(\'\').reverse()</code> 对纯英文成立，但会把 emoji 的代理对拆开。若题目只要求按 Unicode code point 反转，可以这样写：</p>\\n<pre class=\\"language-js\\"><code>function reverseText(text) {\\n  const chars = Array.from(text)\\n  let left = 0\\n  let right = chars.length - 1\\n  while (left &lt; right) {\\n    ;[chars[left], chars[right]] = [chars[right], chars[left]]\\n    left += 1\\n    right -= 1\\n  }\\n  return chars.join(&#39;&#39;)\\n}\\n\\nconsole.log(reverseText(&#39;A😀B&#39;)) // B😀A\\n</code></pre>\\n<p><code>Array.from()</code> 能保住单个 emoji，却不能保证组合字素。例如 <code>e</code> 加组合重音可能仍被拆开；面向用户文本时用 <code>new Intl.Segmenter(locale, { granularity: \'grapheme\' })</code> 分段。算法时间 <code>O(n)</code>，字符数组和结果占 <code>O(n)</code> 空间。</p>\\n<p>在 JS 中，反转字符串我们直接调相关 API 即可，相信不少同学都能手到擒来：</p>\\n<pre class=\\"language-js\\"><code>// 定义被反转的字符串 \\nconst str = &#39;juejin&#39;  \\n// 定义反转后的字符串\\nconst res = str.split(&#39;&#39;).reverse().join(&#39;&#39;)\\n\\nconsole.log(res) // nijeuj\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>昵称编辑页把 <code>A😀B</code> 用 <code>split(\'\').reverse().join(\'\')</code> 处理后出现乱码，而纯英文测试正常；你会怎样解释这段代码的失败点？</strong></p>\\n<p><code>split(\'\')</code> 按 <code>UTF-16 code unit</code> 拆分，部分 <code>emoji</code> 由代理对组成，反转后代理对顺序被破坏。若需求是按 <code>Unicode</code> <code>code</code> <code>point</code> 反转，可先用 <code>Array.from(text)</code>；这仍不保证组合字素完整。</p>\\n</li>\\n<li>\\n<p><strong>前端需要实现按 <code>code</code> <code>point</code> 反转，代码评审要求避免不断执行 <code>result = char + result</code>；你会给出什么实现？</strong></p>\\n<p>先用 <code>Array.from(text)</code> 得到字符数组，再以 <code>left</code>、<code>right</code> 双指针原地交换，最后执行一次 <code>join(\'\')</code>。算法遍历为 <code>O(n)</code>，数组与结果占 <code>O(n)</code> 空间；<code>JavaScript</code> 字符串不可变，不能承诺真正的零额外输出空间。</p>\\n</li>\\n<li>\\n<p><strong>产品把“反转昵称”改成按用户看到的字符反转，输入可能包含 <code>e</code> 加组合重音；仅把 <code>split(\'\')</code> 换成 <code>Array.from()</code> 足够吗？</strong></p>\\n<p>不够，<code>Array.from()</code> 保护的是 <code>Unicode</code> <code>code</code> <code>point</code>，而组合重音可能与基础字符属于同一个字素簇。应使用 <code>Intl.Segmenter(locale, { granularity: \'grapheme\' })</code> 分段后再反转；具体 <code>locale</code> 需要与产品文本规则一致。</p>\\n</li>\\n<li>\\n<p><strong>线上故障只发生在少量多语言昵称，排查时你发现存储值、页面显示和反转结果的长度各不相同；应该先收集什么证据？</strong></p>\\n<p>先保留原始字符串，并分别查看 <code>code</code> <code>unit</code>、<code>code</code> <code>point</code> 和字素簇三种切分结果，避免只依据页面视觉判断。再核对校验、存储和反转是否使用同一字符口径；若上游还改变了文本表示，需要单独确认相等规则。</p>\\n</li>\\n<li>\\n<p><strong>面试官要求“<code>O(1)</code> 额外空间反转字符串”，候选人直接在 <code>JavaScript</code> 字符串上交换索引；你会如何纠正这个约束冲突？</strong></p>\\n<p><code>JavaScript</code> 字符串不可变，索引位置不能被原地交换，生成反转字符串必然需要新的结果存储。若题目把输入改为可变字符数组，双指针交换可做到算法辅助空间 <code>O(1)</code>；但字符数组和最终输出本身仍占空间。</p>\\n</li>\\n</ul>\\n</section>\\n<p>（这段代码需要你非常熟悉，一些公司一面为了试水，有时会单独考这个操作）。</p>\\n<h3 id=\\"判断一个字符串是否是回文字符串\\"><a class=\\"header-anchor\\" href=\\"#判断一个字符串是否是回文字符串\\">#</a> 判断一个字符串是否是回文字符串</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>回文判断使用左右指针比较对称位置，首次不等即可返回 <code>false</code>，无需真的构造完整反转串</li>\\n<li>时间 <code>O(n)</code>、辅助空间按可索引字符表示而定；ASCII 字符串可直接索引，Unicode 文本先明确字符口径</li>\\n<li>“忽略大小写、空格和标点”不是回文定义自带条件，必须由题目契约决定并在比较前规范化</li>\\n<li>规范化可能改变字符数量与语义，例如 <code>é</code> 有预组合和组合形式，可按需求使用 <code>normalize(\'NFC\')</code></li>\\n<li>空串和单字符通常视为回文，但应在函数契约或测试中明确</li>\\n</ul>\\n<p><strong>判断回文字符串，我一般用左右指针比较对称位置，遇到第一处不同就直接返回 <code>false</code>。</strong> 这样不需要构造反转字符串，时间复杂度是 <code>O(n)</code>；<code>ASCII</code> 字符串可以直接按下标访问，<code>Unicode</code> 文本则要先明确比较口径。忽略大小写、空格和标点并不是回文判断的默认规则，只有题目明确要求时才做预处理。像 <code>é</code> 还可能存在不同编码形式，可按需求先调用 <code>normalize(\'NFC\')</code>，空串和单字符通常视为回文。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>function isPalindrome(text) {\\n  const chars = Array.from(text.normalize(&#39;NFC&#39;))\\n  let left = 0\\n  let right = chars.length - 1\\n  while (left &lt; right) {\\n    if (chars[left] !== chars[right]) return false\\n    left += 1\\n    right -= 1\\n  }\\n  return true\\n}\\n</code></pre>\\n<p>进入每轮时，区间外的字符已经成对相等，答案只取决于 <code>[left, right]</code>。每轮区间缩小两个位置，所以时间 <code>O(n)</code>。这里没有擅自忽略标点和大小写；若产品要求“上海自来水来自海上”和英文短语规范化，应把过滤规则作为独立、可测试的预处理函数。</p>\\n<p>回文字符串，就是正着读和倒着读都一🐱一样的字符串，比如这样的：</p>\\n<pre class=\\"language-text\\"><code>&#39;yessey&#39;\\n</code></pre>\\n<p>结合这个定义，我们不难写出一个判定回文字符串的方法：</p>\\n<pre class=\\"language-js\\"><code>function isPalindrome(str) {\\n    // 先反转字符串\\n    const reversedStr = str.split(&#39;&#39;).reverse().join(&#39;&#39;)\\n    // 判断反转前后是否相等\\n    return reversedStr === str\\n}\\n</code></pre>\\n<blockquote>\\n<p>同时，回文字符串还有另一个特性：如果从中间位置“劈开”，那么两边的两个子串在内容上是完全对称的。因此我们也可以结合对称性来做判断：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function isPalindrome(str) {\\n    // 缓存字符串的长度\\n    const len = str.length\\n    // 遍历前半部分，判断和后半部分是否对称\\n    for(let i=0;i&lt;len/2;i++) {\\n        if(str[i]!==str[len-i-1]) {\\n            return false\\n        }\\n    }\\n    return true\\n}\\n</code></pre>\\n<p>（谨记这个对称的特性，非常容易用到）</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>评论区输入 <code>A man, a plan, a canal: Panama</code>，产品认定它是回文，但当前 <code>isPalindrome</code> 返回 <code>false</code>；你会直接在双指针循环里跳过标点并转小写吗？</strong></p>\\n<p>不会直接改核心循环，而会先把“忽略哪些字符、是否忽略大小写”写成文本契约，再实现独立且可测试的预处理。双指针只负责比较规范化后的字符，否则过滤、大小写和边界逻辑交织后，很容易误删非 <code>ASCII</code> 字母或改变其他页面的原有语义。</p>\\n</li>\\n<li>\\n<p><strong>用户昵称页面里两个看起来相同的 <code>é</code> 参与回文判断，一个是预组合字符，另一个是 <code>e</code> 加组合音标，为什么 <code>chars[left] !== chars[right]</code> 仍可能成立？</strong></p>\\n<p>两者可能具有不同的 <code>Unicode</code> 码点序列，因此直接比较字符序列会判为不等。若业务把规范等价视为相同，应先调用 <code>text.normalize(\'NFC\')</code>，再用 <code>Array.from</code> 遍历码点；若还要求按视觉字素簇比较，则需另行明确文本契约和分词能力。</p>\\n</li>\\n<li>\\n<p><strong>日志平台要判断包含 <code>emoji</code> 的短文本是否回文，旧实现用 <code>split(\'\').reverse()</code>，而新实现用 <code>Array.from</code> 加双指针；这次替换解决了什么，又没有解决什么？</strong></p>\\n<p><code>Array.from</code> 按 <code>Unicode</code> 码点展开，能避免把常见代理对 <code>emoji</code> 拆成两个 <code>UTF-16</code> 代码单元，双指针也不必构造反转字符串。它仍不等于按字素簇处理，带组合符号或由多个码点组成的可见字符是否应视为整体，必须由产品规则决定。</p>\\n</li>\\n<li>\\n<p><strong>审核服务一次接收长度为十万字符的文本，团队有人坚持用 <code>split(\'\').reverse().join(\'\') === text</code>，你为什么更倾向当前双指针实现？</strong></p>\\n<p>双指针在第一次不匹配时即可返回，并且只维护左右下标，辅助状态更少；完整反转则必须生成字符数组和新字符串后才能比较。两者时间量级都是 <code>O(n)</code>，但双指针更容易陈述“区间外已成对相等”的不变量，代价是代码稍长且仍需先确定 <code>Unicode</code> 迭代单位。</p>\\n</li>\\n<li>\\n<p><strong>线上偶发报告“中文回文正常、带组合音标失败”，你会怎样定位是规范化遗漏、过滤规则错误，还是双指针边界错误？</strong></p>\\n<p>先固定最小样例，分别打印原文、<code>NFC</code> 后的码点序列和预处理输出，再单测空串、单字符、奇偶长度及首尾冲突。若规范化后序列已一致而结果仍错，再检查 <code>left &lt; right</code>、指针递增递减和过滤函数；不要只看页面渲染，因为视觉相同不能证明底层序列相同。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"高频真题解读\\"><a class=\\"header-anchor\\" href=\\"#高频真题解读\\">#</a> 高频真题解读</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>真题先把自然语言约束翻译成可验证不变量：允许删除几个字符、是否忽略标点、模式是否必须覆盖整个字符串</li>\\n<li>暴力解用于确认正确性和测试小输入，再根据重复计算选择双指针、动态规划、滑动窗口或预处理</li>\\n<li>代码必须覆盖空串、单字符、重复字符、Unicode 和极端长度，不能只通过题目示例</li>\\n</ul>\\n<p><strong>做字符串真题时，关键是先把自然语言限制变成明确、可验证的函数契约。</strong> 比如允许删除几个字符、是否忽略标点，以及模式匹配整个字符串还是任意子串，这些条件不同，实际解决的就是不同问题。我会先用暴力解确认小规模输入的正确结果，再根据重复扫描、重复状态或单调性选择双指针、动态规划等优化方式。最后还要覆盖空串、单字符、重复字符、<code>Unicode</code> 和极端长度，不能只验证题目给出的示例。</p>\\n</section>\\n<p>真题阶段要把题目里的自然语言逐项翻译成契约：是否忽略大小写与标点、允许修改几个字符、匹配整个字符串还是任意子串、失败时返回布尔值还是位置。契约没钉死，同一段代码可能在一个样例里正确，换一组输入就变成另一道题。</p>\\n<p>优化应从暴力解中推导，而不是直接背模板。先用小规模实现建立正确性基线，再找重复扫描、重复状态或单调性；最后用固定边界用例和随机差分测试对照优化前后结果。这样双指针、动态规划或状态机的每一步都有来源，也更容易向面试官解释。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>搜索框需求只写“判断模式是否匹配字符串”，开发按任意子串实现，验收却拿完整字符串匹配来判错；你在写算法前会追问哪些契约？</strong></p>\\n<p>我会先确认匹配范围是整个字符串还是任意子串，并明确大小写、标点、空输入和允许字符。还要钉死失败时返回布尔值还是位置，以及是否允许修改字符；这些条件会直接决定双指针、状态机或其他模型，不能靠样例猜测。</p>\\n</li>\\n<li>\\n<p><strong>候选人看到“最多删除一个字符后成为回文”就背出双指针，但解释不了为什么只检查两条区间；你会如何判断这是不是可靠优化？</strong></p>\\n<p>我会要求他从暴力删除每个位置的基线推导：第一处左右冲突出现时，外侧已经对称，合法答案必须删除当前冲突字符之一。于是只需验证 <code>[left+1,right]</code> 与 <code>[left,right-1]</code>，若说不清这个必要性，模板即使通过样例也缺少可迁移的正确性依据。</p>\\n</li>\\n<li>\\n<p><strong>字符串规则优化后通过了十几个固定用例，却在随机输入上偶发与旧实现结果不同；你会让团队怎样缩小语义回归范围？</strong></p>\\n<p>保留只处理小规模输入的暴力实现，把它当作正确性基线，与优化版进行随机差分测试，并记录首个反例。随后把反例缩减并加入固定边界集，重点覆盖空串、单字符、临界修改次数和首尾冲突；前提是两份实现遵循同一份已确认契约。</p>\\n</li>\\n<li>\\n<p><strong>接口从“返回是否匹配”改成“返回第一处失败位置”，原方案是一条复杂正则；你还会坚持正则选型吗？</strong></p>\\n<p>通常不会直接坚持，因为失败位置要求保留扫描状态，复杂正则即使能判断真假，也未必能稳定表达定位语义。显式扫描或状态机更容易记录当前位置和停止原因；代价是代码量增加，但状态转移、复杂度和边界行为更便于测试与解释。</p>\\n</li>\\n<li>\\n<p><strong>页面输入规模从几百字符提升到数万字符，当前实现存在重复扫描；你会先换动态规划模板，还是先证明哪部分能够消除？</strong></p>\\n<p>我会先标出重复访问的区间、重复状态以及是否存在单调性，再决定用双指针、动态规划或状态机。优化必须能从基线实现推导，并用复杂度分析和差分测试验证；直接换模板可能降低复杂度，也可能悄悄改变匹配范围或修改次数等语义。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"回文字符串的衍生问题\\"><a class=\\"header-anchor\\" href=\\"#回文字符串的衍生问题\\">#</a> 回文字符串的衍生问题</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先用左右指针跳过所有相等对；第一次冲突才是唯一需要使用“删除额度”的位置</li>\\n<li>冲突时只需验证 <code>[left+1, right]</code> 与 <code>[left, right-1]</code> 两个区间，不必尝试删除每个字符</li>\\n<li>两条验证路径各线性扫描一次，总时间仍为 <code>O(n)</code>，辅助空间取决于字符表示</li>\\n<li>不能真的切片再递归尝试，否则频繁复制字符串会增加时间和空间</li>\\n<li>“最多删除一个”包含零次删除，原本就是回文的输入必须返回 <code>true</code></li>\\n</ul>\\n<p><strong>最多删除一个字符后判断回文，可以先用左右指针跳过相等字符，在第一次冲突处分别尝试跳过左边或右边。</strong> 因为冲突位置外侧已经对称，合法结果必须删除当前两个不等字符中的一个，删除其他位置无法解决这对冲突。两个候选区间各自最多线性检查一次，所以总时间仍是 <code>O(n)</code>，没必要切片后递归尝试每个删除位置。还要注意“最多删除一个”包含不删除，因此原字符串已经是回文时也应返回 <code>true</code>。</p>\\n</section>\\n<p>原文思路正确，关键是把“第一处冲突为什么只需两条分支”说清：外侧都已对称，若最终存在合法答案，当前不等的两个字符至少要删掉一个；删除其他位置无法消除这对冲突。</p>\\n<pre class=\\"language-js\\"><code>function validPalindrome(text) {\\n  const chars = Array.from(text)\\n  const isRangePalindrome = (left, right) =&gt; {\\n    while (left &lt; right) {\\n      if (chars[left] !== chars[right]) return false\\n      left += 1\\n      right -= 1\\n    }\\n    return true\\n  }\\n\\n  let left = 0\\n  let right = chars.length - 1\\n  while (left &lt; right &amp;&amp; chars[left] === chars[right]) {\\n    left += 1\\n    right -= 1\\n  }\\n  return isRangePalindrome(left + 1, right) || isRangePalindrome(left, right - 1)\\n}\\n</code></pre>\\n<blockquote>\\n<p>真题描述：给定一个非空字符串 s，最多删除一个字符。判断是否能成为回文字符串。</p>\\n</blockquote>\\n<p>示例 1:</p>\\n<pre class=\\"language-text\\"><code>输入: &quot;aba&quot;\\n输出: True\\n示例 2:\\n输入: &quot;abca&quot;\\n输出: True\\n解释: 你可以删除c字符。\\n注意: 字符串只包含从 a-z 的小写字母。字符串的最大长度是50000。\\n</code></pre>\\n<p><strong>思路分析</strong></p>\\n<p>这道题很多同学第一眼看过去，可能本能地会想到这样一种解法：若字符串本身不回文，则直接遍历整个字符串。遍历到哪个字符就删除哪个字符、同时对删除该字符后的字符串进行是否回文的判断，看看存不存在删掉某个字符后、字符串能够满足回文的这种情况。</p>\\n<p>这个思路真的实现起来的话，在判题系统眼里其实也是没啥毛病的。但是在面试官看来，就有点问题了——这不是一个高效的解法。\\n如何判断自己解决回文类问题的解法是否“高效”？其中一个很重要的标准，就是看你对回文字符串的对称特性利用得是否彻底。</p>\\n<p>字符串题干中若有“回文”关键字，那么做题时脑海中一定要冒出两个关键字——对称性 和 双指针。这两个工具一起上，足以解决大部分的回文字符串衍生问题。</p>\\n<p>回到这道题上来，我们首先是初始化两个指针，一个指向字符串头部，另一个指向尾部：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114114.png\\" alt=\\"\\"></p>\\n<p>如果两个指针所指的字符恰好相等，那么这两个字符就符合了回文字符串对对称性的要求，跳过它们往下走即可。如果两个指针所指的字符串不等，比如这样：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114121.png\\" alt=\\"\\"></p>\\n<p>那么就意味着不对称发生了，意味着这是一个可以“删掉试试看”的操作点。我们可以分别对左指针字符和右指针字符尝试进行“跳过”，看看区间在 <code>[left+1, right]</code> 或 <code>[left, right-1]</code> 的字符串是否回文。如果是的话，那么就意味着如果删掉被“跳过”那个字符，整个字符串都将回文：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114132.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>比如说这里我们跳过了 b，[left+1, right] 的区间就是 [2, 2]，它对应 c 这个字符，单个字符一定回文。这样一来，删掉 b 之后，左右指针所指的内部区间是回文的，外部区间也是回文的，可以认为整个字符串就是一个回文字符串了。</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>const validPalindrome = function(s) {\\n    // 缓存字符串的长度\\n    const len = s.length\\n\\n    // i、j分别为左右指针\\n    let i=0, j=len-1\\n    \\n    // 当左右指针均满足对称时，一起向中间前进\\n    while(i&lt;j&amp;&amp;s[i]===s[j]) {\\n        i++ \\n        j--\\n    }\\n    \\n    // 尝试判断跳过左指针元素后字符串是否回文\\n    if(isPalindrome(i+1,j)) {\\n      return true\\n    }\\n    // 尝试判断跳过右指针元素后字符串是否回文\\n    if(isPalindrome(i,j-1)) {\\n        return true\\n    }\\n    \\n    // 工具方法，用于判断字符串是否回文\\n    function isPalindrome(st, ed) {\\n        while(st&lt;ed) {\\n            if(s[st] !== s[ed]) {\\n                return false\\n            }\\n            st++\\n            ed--\\n        } \\n        return true\\n    }\\n    \\n    // 默认返回 false\\n    return false \\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>接口输入 <code>abca</code> 时第一次冲突是 <code>b</code> 和 <code>c</code>，同事为了少一次扫描固定删除左侧字符；为什么这个贪心规则不能作为通用实现？</strong></p>\\n<p>当前冲突只能说明左右字符至少要删除一个，无法仅凭局部信息确定删哪一侧；其他输入可能只有删除右侧才成立。应分别验证 <code>[left+1,right]</code> 和 <code>[left,right-1]</code>，任一为回文即可，固定方向会漏掉合法答案。</p>\\n</li>\\n<li>\\n<p><strong>移动端校验最长五万字符的字符串，代码用两次 <code>slice()</code> 构造候选串再判断；你会怎样改，复杂度有什么变化？</strong></p>\\n<p>我会让辅助函数接收左右下标，直接在原字符序列上验证两个候选区间，避免为每条分支创建子串。总体时间仍是 <code>O(n)</code>，辅助扫描只发生在第一处冲突之后；若先用 <code>Array.from</code> 处理 <code>Unicode</code>，则字符数组本身仍需要线性空间。</p>\\n</li>\\n<li>\\n<p><strong>评审时有人质疑：第一次不相等后，为什么删除中间某个字符不可能修复回文，而只能删当前左右字符之一？</strong></p>\\n<p>进入冲突位置前，区间外的字符已经成对相等，而当前 <code>chars[left]</code> 与 <code>chars[right]</code> 必须在最终序列中互相对应。若删除的是更内部字符，这两个不等字符仍同时保留并继续相对，冲突不会消失；所以一次删除额度只能用于左右端之一。</p>\\n</li>\\n<li>\\n<p><strong>需求从“最多删除一个字符”改成“最多删除 <code>k</code> 个”，字符串长度仍可能达到五万；你会继续递归展开左右两条分支吗？</strong></p>\\n<p>不会直接展开，因为每次冲突都可能产生两条路径，朴素递归最坏会形成指数级分支。可考虑带剩余额度的记忆化搜索、区间动态规划，或用最长回文子序列是否至少为 `n-k<code>建模；最终选择受</code>n<code>、</code>k`` 和内存限制约束，不能只替换一个参数。</p>\\n</li>\\n<li>\\n<p><strong>线上版本把空串、单字符和本身已回文的输入都交给 <code>isRangePalindrome(left + 1, right)</code>，出现负下标告警；主循环退出后该怎样处理才稳妥？</strong></p>\\n<p>当主循环因 <code>left &gt;= right</code> 退出时，原串已经是回文，应直接返回 <code>true</code>，无需消费删除额度。若仍复用区间函数，也必须保证空区间按回文处理且不读取越界元素；显式提前返回更清楚，也能区分“无需删除”和“尝试删除后成功”。</p>\\n</li>\\n<li>\\n<p><strong>产品又要求返回被删除字符的下标，而不是只返回布尔值；现有两分支验证需要怎样调整才能处理两边都可行的情况？</strong></p>\\n<p>辅助函数仍可验证两个区间，但主流程要保存左、右候选是否成功，并按产品约定返回对应下标。若两边都成功，算法本身无法替产品决定优先删左、删右或返回全部候选；必须先约定确定性规则，否则相同输入可能在重构后得到不同结果。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"字符串匹配问题-正则表达式初相见\\"><a class=\\"header-anchor\\" href=\\"#字符串匹配问题-正则表达式初相见\\">#</a> 字符串匹配问题——正则表达式初相见</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>题目里的 <code>.</code> 是“单字符通配符”，不是让调用方传任意正则；实现必须限制语法，避免把输入直接当正则执行</li>\\n<li>按长度分桶可缩小候选集，但最坏仍要扫描同长度单词；Trie 能沿字符层级剪枝</li>\\n<li>普通字符只走对应子结点，<code>.</code> 要遍历当前结点所有孩子；到模式末尾还要检查完整单词标记</li>\\n<li>搜索复杂度取决于通配符数量：普通词接近 <code>O(m)</code>，多个 <code>.</code> 可能扩展到大量分支</li>\\n<li>生产接口还要限制单词长度、字符集和通配符数量，防止资源耗尽</li>\\n</ul>\\n<p><strong>这道题里的 <code>.</code> 只是单字符通配符，我会用 <code>Trie</code> 显式处理，而不是把输入直接交给 <code>RegExp</code>。</strong> 普通字符只查对应子节点，遇到 <code>.</code> 才遍历当前节点的所有孩子，模式结束时还要确认命中了完整单词。没有通配符时查询接近 <code>O(m)</code>，但多个 <code>.</code> 会扩展出大量分支。线上还应限制模式长度、字符集和通配符数量，避免资源被耗尽。</p>\\n</section>\\n<p>原文把用户模式交给 <code>new RegExp(word)</code>，不仅缺少 <code>^...$</code> 锚点，而且把未来新增字符误当成完整正则语法。更稳的实现是显式解释唯一允许的 <code>.</code>：</p>\\n<pre class=\\"language-js\\"><code>class WordDictionary {\\n  constructor() { this.root = { children: new Map(), end: false } }\\n  addWord(word) {\\n    let node = this.root\\n    for (const char of word) {\\n      if (!node.children.has(char)) node.children.set(char, { children: new Map(), end: false })\\n      node = node.children.get(char)\\n    }\\n    node.end = true\\n  }\\n  search(pattern) {\\n    const chars = Array.from(pattern)\\n    const visit = (node, index) =&gt; {\\n      if (index === chars.length) return node.end\\n      const char = chars[index]\\n      if (char !== &#39;.&#39;) return node.children.has(char) &amp;&amp; visit(node.children.get(char), index + 1)\\n      for (const child of node.children.values()) if (visit(child, index + 1)) return true\\n      return false\\n    }\\n    return visit(this.root, 0)\\n  }\\n}\\n</code></pre>\\n<p>添加单词时间 <code>O(m)</code>。无通配符查询也是 <code>O(m)</code>；每个 <code>.</code> 都可能探索多个孩子，最坏复杂度与 Trie 分支数相关，因此线上必须限制模式长度和通配符数量。</p>\\n<p>接下来我们来看一道综合性比较强的字符串大题：</p>\\n<blockquote>\\n<p>真题描述： 设计一个支持以下两种操作的数据结构：</p>\\n</blockquote>\\n<ul>\\n<li><code>void addWord(word)</code></li>\\n<li><code>bool search(word)</code></li>\\n<li><code>search(word)</code> 可以搜索文字或正则表达式字符串，字符串只包含字母 <code>.</code> 或 <code>a-z</code> 。</li>\\n<li><code>.</code> 可以表示任何一个字母。</li>\\n</ul>\\n<p>示例:</p>\\n<pre class=\\"language-text\\"><code>addWord(&quot;bad&quot;)\\naddWord(&quot;dad&quot;)\\naddWord(&quot;mad&quot;)\\nsearch(&quot;pad&quot;) -&gt; false\\nsearch(&quot;bad&quot;) -&gt; true\\nsearch(&quot;.ad&quot;) -&gt; true\\nsearch(&quot;b..&quot;) -&gt; true\\n</code></pre>\\n<p>说明:</p>\\n<p>你可以假设所有单词都是由小写字母 <code>a-z</code> 组成的。</p>\\n<p><strong>思路分析</strong></p>\\n<p>这道题要求字符串既可以被添加、又可以被搜索，这就意味着字符串在添加时一定要被存在某处。键值对存储，我们用 Map（或对象字面量来模拟 Map）。</p>\\n<p>注意，这里为了降低查找时的复杂度，我们可以考虑以字符串的长度为 key，相同长度的字符串存在一个数组中，这样可以提高我们后续定位的效率。</p>\\n<blockquote>\\n<p>难点在于 search 这个 API，它既可以搜索文字，又可以搜索正则表达式。因此我们在搜索前需要额外判断一下，传入的到底是普通字符串，还是正则表达式。若是普通字符串，则直接去 Map 中查找是否有这个 key；若是正则表达式，则创建一个正则表达式对象，判断 Map 中相同长度的字符串里，是否存在一个能够与这个正则相匹配。</p>\\n</blockquote>\\n<p>这里需要大家复习一下正则表达式的创建，以及用于测试某个字符串是否与之匹配的方法：</p>\\n<pre class=\\"language-js\\"><code>/**\\n * 构造函数\\n */\\nconst WordDictionary = function () {\\n  // 初始化一个对象字面量，承担 Map 的角色\\n  this.words = {}\\n};\\n\\n/**\\n  添加字符串的方法\\n */\\nWordDictionary.prototype.addWord = function (word) {\\n  // 若该字符串对应长度的数组已经存在，则只做添加\\n  if (this.words[word.length]) {\\n    this.words[word.length].push(word)\\n  } else {\\n    // 若该字符串对应长度的数组还不存在，则先创建\\n    this.words[word.length] = [word]\\n  }\\n\\n};\\n\\n/**\\n  搜索方法\\n */\\nWordDictionary.prototype.search = function (word) {\\n  // 若该字符串长度在 Map 中对应的数组根本不存在，则可判断该字符串不存在\\n  if (!this.words[word.length]) {\\n    return false\\n  }\\n  // 缓存目标字符串的长度\\n  const len = word.length\\n  // 如果字符串中不包含‘.’，那么一定是普通字符串\\n  if (!word.includes(&#39;.&#39;)) {\\n    // 定位到和目标字符串长度一致的字符串数组，在其中查找是否存在该字符串\\n    return this.words[len].includes(word)\\n\\n  }\\n\\n  // 否则是正则表达式，要先创建正则表达式对象\\n  const reg = new RegExp(word)\\n\\n  // 只要数组中有一个匹配正则表达式的字符串，就返回true\\n  return this.words[len].some((item) =&gt; {\\n    return reg.test(item)\\n  })\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>词典中只有 <code>badly</code>，查询页输入 <code>bad</code> 时 <code>Trie</code> 能走完三个字符，却必须返回 <code>false</code>；你会检查哪个状态？</strong></p>\\n<p>应在模式消费完时返回当前结点的 <code>end</code>，不能只判断路径是否存在。<code>Trie</code> 中可达路径可能只是更长单词的前缀，<code>end</code> 才表示该位置确实添加过完整词条；遗漏它会把所有已存在的前缀误报为命中。</p>\\n</li>\\n<li>\\n<p><strong>旧搜索接口把用户输入直接交给 <code>new RegExp(pattern)</code>，产品契约却只允许 <code>.</code> 表示单个字母；除了缺少 <code>^</code> 和 <code>$</code>，这还会制造什么问题？</strong></p>\\n<p>用户会意外获得 <code>*</code>、<code>+</code>、分组等完整正则语义，既可能改变匹配结果，也可能构造高回溯模式阻塞事件循环。应显式解释普通字符和 <code>.</code>，例如使用 <code>Trie</code> 遍历；同时限制模式长度与通配符数量，避免把输入当成任意程序执行。</p>\\n</li>\\n<li>\\n<p><strong>词库有大量同长度单词，查询模式包含十个连续 <code>.</code>，监控显示 <code>CPU</code> 明显升高；<code>Trie</code> 不是按字符查找吗，为什么仍会退化？</strong></p>\\n<p>普通字符只沿一个孩子前进，但每个 <code>.</code> 都要枚举当前结点的所有孩子，连续通配符会让分支数量相乘。最坏代价取决于 <code>Trie</code> 的实际分支和可达结点数，因此线上应限制通配符数量、先按长度排除不可能结果，并设置访问结点预算。</p>\\n</li>\\n<li>\\n<p><strong>业务只存小写 <code>a-z</code>，查询量不大，但需要最快上线；团队在“按长度分桶后逐词匹配”和 <code>Trie</code> 之间争论，你怎么选？</strong></p>\\n<p>数据量和查询压力有限时，按长度分桶实现更短，先定位同长度集合后逐词比较即可，且应手写 <code>.</code> 的单字符语义而非执行任意正则。<code>Trie</code> 更适合大量共享前缀和高频查询，但实现、内存占用及通配符分支控制更复杂，需由真实规模决定。</p>\\n</li>\\n<li>\\n<p><strong>新增词 <code>bad</code> 后立刻搜索 <code>b.d</code> 偶发失败，而搜索 <code>bad</code> 正常；你会按什么顺序排查 <code>Trie</code> 的添加和通配符递归？</strong></p>\\n<p>先确认 <code>addWord</code> 为每个字符复用或创建正确孩子，并在末结点设置 <code>end=true</code>，再跟踪 <code>visit(node,index)</code> 的索引推进。重点检查 <code>.</code> 分支是否遍历所有孩子、递归后是否短路返回，以及模式耗尽时是否检查 <code>end</code>；普通查询正常并不能证明通配符分支正确。</p>\\n</li>\\n<li>\\n<p><strong>接口准备支持空字符串和空模式，<code>Trie</code> 根结点的 <code>end</code> 应该如何定义，查询 <code>&quot;&quot;</code> 又该返回什么？</strong></p>\\n<p>若契约允许添加空字符串，<code>addWord(&quot;&quot;)</code> 应把根结点的 <code>end</code> 设为 <code>true</code>，随后空模式查询才能命中。若业务禁止空词，则入口应拒绝或固定返回 <code>false</code>；不能让递归基线自行决定，因为根结点同时承担结构起点而非默认词条。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"正则表达式更进一步-字符串与数字之间的转换问题\\"><a class=\\"header-anchor\\" href=\\"#正则表达式更进一步-字符串与数字之间的转换问题\\">#</a> 正则表达式更进一步——字符串与数字之间的转换问题</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>atoi</code> 是确定状态机：跳前导空格 → 读一个可选符号 → 连续读数字 → 首个非数字处停止</li>\\n<li>无有效数字返回 <code>0</code>；符号后若没有数字也不能算成功解析</li>\\n<li>累计用 <code>value = value * 10 + digit</code>，每步检查 32 位边界，避免先溢出再截断</li>\\n<li>正确范围是 <code>[-2^31, 2^31-1]</code>，负数一侧多一个绝对值，不能只对称使用 <code>MAX</code></li>\\n<li>不能直接用 <code>parseInt</code> 代替题目实现；生产中还要明确 Unicode 空白、进制和大整数策略</li>\\n</ul>\\n<p><strong><code>atoi</code> 本质上是按“跳过前导空格、读取可选符号、连续读取数字、遇到非数字停止”的规则扫描字符串。</strong> 没读到有效数字就返回 <code>0</code>，累计时用 <code>value = value * 10 + digit</code>，并在每一步检查边界。结果必须截断到 <code>[-2^31, 2^31-1]</code>，尤其要注意负数下界的绝对值比正数上界多一。扫描复杂度是 <code>O(n)</code>、空间是 <code>O(1)</code>；超长输入还要考虑 <code>Number</code> 的精度问题，可改用乘法前阈值判断或 <code>BigInt</code>。</p>\\n</section>\\n<p>手写扫描比“先正则提取、再转 Number”更能展示边界控制：</p>\\n<pre class=\\"language-js\\"><code>function atoi(text) {\\n  const MIN = -(2 ** 31)\\n  const MAX = 2 ** 31 - 1\\n  let index = 0\\n  let sign = 1\\n  let value = 0\\n  while (text[index] === &#39; &#39;) index += 1\\n  if (text[index] === &#39;+&#39; || text[index] === &#39;-&#39;) sign = text[index++] === &#39;-&#39; ? -1 : 1\\n  while (index &lt; text.length &amp;&amp; text[index] &gt;= &#39;0&#39; &amp;&amp; text[index] &lt;= &#39;9&#39;) {\\n    value = value * 10 + Number(text[index++])\\n    if (sign * value &lt;= MIN) return MIN\\n    if (sign * value &gt;= MAX) return MAX\\n  }\\n  return sign * value\\n}\\n</code></pre>\\n<p>扫描最多访问每个相关字符一次，时间 <code>O(n)</code>、辅助空间 <code>O(1)</code>。若运行环境的 Number 已可能在超长输入上失去整数精度，可在乘法前用阈值比较，或使用 <code>BigInt</code> 累计后截断；面试中应主动指出这个生产边界。</p>\\n<blockquote>\\n<p>真题描述：请你来实现一个 atoi 函数，使其能将字符串转换成整数。</p>\\n</blockquote>\\n<p>首先，该函数会根据需要丢弃无用的开头空格字符，直到寻找到第一个非空格的字符为止。</p>\\n<p>当我们寻找到的第一个非空字符为正或者负号时，则将该符号与之后面尽可能多的连续数字组合起来，作为该整数的正负号；假如第一个非空字符是数字，则直接将其与之后连续的数字字符组合起来，形成整数。</p>\\n<p>该字符串除了有效的整数部分之后也可能会存在多余的字符，这些字符可以被忽略，它们对于函数不应该造成影响。</p>\\n<blockquote>\\n<p>注意：假如该字符串中的第一个非空格字符不是一个有效整数字符、字符串为空或字符串仅包含空白字符时，则你的函数不需要进行转换。</p>\\n</blockquote>\\n<p>在任何情况下，若函数不能进行有效的转换时，请返回 0。</p>\\n<blockquote>\\n<p>说明：假设我们的环境只能存储 32 位大小的有符号整数，那么其数值范围为 [−2^31,  2^31 − 1]。如果数值超过这个范围，请返回  INT_MAX (2^31 − 1) 或 INT_MIN (−2^31) 。</p>\\n</blockquote>\\n<blockquote>\\n<p>示例 1:</p>\\n</blockquote>\\n<ul>\\n<li>输入: &quot;42&quot;</li>\\n<li>输出: 42</li>\\n</ul>\\n<blockquote>\\n<p>示例 2:</p>\\n</blockquote>\\n<ul>\\n<li>输入: &quot; -42&quot;</li>\\n<li>输出: -42</li>\\n</ul>\\n<p>解释: 第一个非空白字符为 \'-\', 它是一个负号。</p>\\n<p>我们尽可能将负号与后面所有连续出现的数字组合起来，最后得到 -42 。</p>\\n<blockquote>\\n<p>示例 3:</p>\\n</blockquote>\\n<ul>\\n<li>输入: &quot;4193 with words&quot;</li>\\n<li>输出: 4193</li>\\n</ul>\\n<p>解释: 转换截止于数字 \'3\' ，因为它的下一个字符不为数字。</p>\\n<blockquote>\\n<p>示例 4:</p>\\n</blockquote>\\n<ul>\\n<li>输入: &quot;words and 987&quot;</li>\\n<li>输出: 0</li>\\n</ul>\\n<p>解释: 第一个非空字符是 \'w\', 但它不是数字或正、负号。 因此无法执行有效的转换。</p>\\n<blockquote>\\n<p>示例 5:</p>\\n</blockquote>\\n<ul>\\n<li>输入: &quot;-91283472332&quot;</li>\\n<li>输出: -2147483648</li>\\n</ul>\\n<p>解释: 数字 &quot;-91283472332&quot; 超过 32 位有符号整数范围。因此返回 INT_MIN (−2^31) 。</p>\\n<p><strong>思路解读</strong></p>\\n<blockquote>\\n<p>这道题乍一看比较唬人，毕竟题干这么长，首先会刷掉一波没耐心读完的火大老哥。我在实际的面试情景下，见过题没读完就掀桌走人的.....嗨，这里特别提醒大家，千万别冲动：小孩子才害怕读题，成年人都偷着乐——你得知道，一般来说，题干越长，题目越好做。</p>\\n</blockquote>\\n<ul>\\n<li>为啥这样说？大家想想，我们做题靠的是什么？自身的知识储备+题目提供的信息。题干长意味着什么？意味着它提供的信息相对丰富、细节描述相对到位，甚至很有可能，这个题的答案都藏在题里了！</li>\\n<li>就拿这道题开刀，我把其中比较关键的句子摘出来给大家翻译翻译：</li>\\n<li>该函数会根据需要丢弃无用的开头空格字符，直到寻找到第一个非空格的字符为止——暗示你拿到字符串先去空格；</li>\\n<li>当我们寻找到的第一个非空字符为正或者负号时，则将该符号与之后面尽可能多的连续数字组合起来，作为该整数的正负号——暗示你识别开头的“+”字符和“-”字符；</li>\\n<li>该字符串除了有效的整数部分之后也可能会存在多余的字符，这些字符可以被忽略，它们对于函数不应该造成影响——暗示你见到非整数字符就刹车；</li>\\n<li>说明： 假设我们的环境只能存储 32 位大小的有符号整数，那么其数值范围为 <code>[−2^31, 2^31 − 1]</code>。如果数值超过这个范围，请返回 <code>INT_MAX (2^31 − 1)</code> 或 <code>INT_MIN (−2^31)</code> ——暗示......这都不是暗示了，这是明示啊！直接告诉你先把这俩边界值算出来，摆在那做卡口就完了。</li>\\n</ul>\\n<p><strong>Step1：计算卡口</strong></p>\\n<p>所以说不管这道题你用啥方法做，这个卡口计算肯定是没跑了。计算某个数的 n 次方，我们要用到 Math.pow 这个方法：</p>\\n<pre class=\\"language-text\\"><code>// 计算最大值\\nconst max = Math.pow(2,31) - 1\\n// 计算最小值\\nconst min = -max - 1\\n</code></pre>\\n<p><strong>Step2：解析字符串</strong></p>\\n<p>这道题其实有很多种解法，不同解法之间的区别就在于解析字符串的方式不同。</p>\\n<p>最直接的解法，是对字符串进行遍历，在遍历的过程中，按照上文我给大家提取的 1、2、3 这三点暗示，逐个地去对每个遍历对象进行判断，从而提取出符合题目要求的数字字符串，再把它转换成数字。</p>\\n<p>逐字符扫描是这道题更稳妥的主解：它能在累计过程中提前判断溢出，也能清楚展示“空格、符号、数字、停止”四个状态。下面保留正则方案作为字符串提取练习，但面试时应先给出上方状态机实现，并说明正则版需要先生成完整数字才能截断，处理超长输入和扩展规则时不如扫描直观。</p>\\n<p>现在我们决定了用正则来做这道题，能不能做对它，就要看咱们正则表达式能不能写对了。</p>\\n<p>对于正则表达式，大多数的团队不会有特别强硬的要求，不会期望你一定要多么多么精通、能不靠 Google 徒手写多么复杂的表达式出来啥的——这样搞其实也没有意义。但是必要的基础你是要有的，这道题目涉及到的正则其实就在这个“必要”的范围里，我们一起来分析一下，首先是看回这三个约束条件，我重新给大家翻译一下：</p>\\n<ul>\\n<li>该函数会根据需要丢弃无用的开头空格字符，直到寻找到第一个非空格的字符为止——允许字符串头部出现空格，但是你在处理的时候要想办法把它摘出去，不要让它干扰你的计算</li>\\n<li>当我们寻找到的第一个非空字符为正或者负号时，则将该符号与之后面尽可能多的连续数字组合起来，作为该整数的正负号——允许字符串的第一个有效字符为“+”或者“—”，不要摘它出去，它对你的计算是有意义的</li>\\n<li>该字符串除了有效的整数部分之后也可能会存在多余的字符，这些字符可以被忽略，它们对于函数不应该造成影响——匹配的时候，连续整数之外的部分都应该被摘除</li>\\n</ul>\\n<p><strong>通过以上分析，我们可以形成以下思路：</strong></p>\\n<blockquote>\\n<p>首先，摘除空格：有两个方法，一个是直接使用 string 的 trim 方法，它是 JavaScript 的一个原生方法，可以去除字符串的头尾空格:</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>let str = &#39;      +10086&#39;\\nstr.trim() // &#39;+10086&#39;\\n</code></pre>\\n<p>另一个方法是在匹配的时候，匹配空格（正则匹配符为 \\\\s*，意味着匹配 0 个或多个空格），但是不把它放在捕获组里——这种方法会更加通用，正则表达式匹配过程中，所有的“摘除”动作都可以通过将匹配到的结果排除在捕获组之外来实现，</p>\\n<blockquote>\\n<p>什么是捕获组？其实就是正则表达式中被小括号括住的部分。在这道题里，我们需要从字符串中提取的其实只有“+/-”符号以及其后面的数字而已，同时这个字符串需要满足 可能存在的空格+正负号+数字字符串+其它字符内容 这样的格式才算合法，那我们就可以通过这样写正则表达式，实现“匹配”和“提取”的双重目的：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>/\\\\s*([-\\\\+]?[0-9]*).*/\\n</code></pre>\\n<p>针对正则基础比较薄弱的同学，我来解释一下上面这个正则表达式：</p>\\n<ul>\\n<li>首先，<code>\\\\s</code> 这个符号，意味着空字符，它可以用来匹配回车、空格、换行等空白区域，这里，它用来被匹配空格。<em>这个符号，跟在其它符号后面，意味着“前面这个符号可以出现0次或多次。\\\\s</em>，这里的意思就是空格出现0次或多次，都可被匹配到。</li>\\n<li>接着 <code>()</code> 出现了。<code>()</code> 圈住的内容，就是我们要捕获起来额外存储的东西。</li>\\n<li><code>[]</code>中的匹配符之间是“或”的关系，也就是说只要能匹配上其中一个就行了。这里<code>[]</code>中包括了<code>-</code>和<code>\\\\+</code>，<code>-</code>不必说匹配的是对应字符，这个<code>\\\\+</code>之所以加了一个斜杠符，是<code>-</code> 因为<code>+</code>本身是一个有特殊作用的正则匹配符，这里我们要让它回归<code>+</code>字符的本义，所以要用一个<code>\\\\</code>来完成转义。</li>\\n<li><code>[0-9]*</code>结合咱们前面铺陈的知识，这个就不难理解了，它的意思是 <code>0-9</code> 之间的整数，能匹配到0个或多个就算匹配成功。</li>\\n<li>最后的 <code>.</code>这个是任意字符的意思，<code>.*</code>用于字符串尾部匹配非数字的任意字符。我们看到<code>.*</code>是被排除捕获组之外的，所以说这个东西其实也不会被额外存储，它被“摘除”了。</li>\\n</ul>\\n<p><strong>Step3：获取捕获结果</strong></p>\\n<blockquote>\\n<p>JS 的正则相关方法中， test()方法返回的是一个布尔值，单纯判断“是否匹配”。要想获取匹配的结果，我们需要调度match()方法：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>const reg = /\\\\s*([-\\\\+]?[0-9]*).*/\\nconst groups = str.match(reg)\\n</code></pre>\\n<p>match() 方法是一个在字符串中执行查找匹配的String方法，它返回一个数组，在未匹配到时会返回 null。</p>\\n<p>如果我们的正则表达式尾部有 g 标志，match()会返回与完整正则表达式匹配的所有结果，但不会返回捕获组。</p>\\n<p>这里我们没有使用g标志，match()就会返回第一个完整匹配（作为数组的第0项）及其相关的捕获组（作为数组的第1及第1+项）。</p>\\n<p>这里我们只定义了一个捕获组，因此可以从 groups[1] 里拿到我们捕获的结果。</p>\\n<p><strong>Step4：判断卡口</strong></p>\\n<p>最后一步，就是把捕获的结果转换成数字，看看是否超出了题目要求的范围。这一步比较简单，无需多言。</p>\\n<p>编码实现</p>\\n<p>分析了这么多，我们终于可以写代码啦：</p>\\n<pre class=\\"language-js\\"><code>// 入参是一个字符串\\nconst myAtoi = function(str) {\\n    // 编写正则表达式\\n    const reg = /\\\\s*([-\\\\+]?[0-9]*).*/\\n    // 得到捕获组\\n    const groups = str.match(reg)\\n    // 计算最大值\\n    const max = Math.pow(2,31) - 1\\n    // 计算最小值\\n    const min = -max - 1\\n    // targetNum 用于存储转化出来的数字\\n    let targetNum = 0\\n    // 如果匹配成功\\n    if(groups) {\\n        // 尝试转化捕获到的结构\\n        targetNum = +groups[1]\\n        // 注意，即便成功，也可能出现非数字的情况，比如单一个&#39;+&#39;\\n        if(isNaN(targetNum)) {\\n            // 不能进行有效的转换时，请返回 0\\n            targetNum = 0\\n        }\\n    }\\n    // 卡口判断\\n    if(targetNum &gt; max) {\\n        return max\\n    } else if( targetNum &lt; min) {\\n        return min\\n    }\\n    // 返回转换结果\\n    return targetNum\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>表单输入只有 <code>-</code> 或 <code>+</code>，扫描器已经记录了符号；为什么接口仍应返回 <code>0</code>，而不能把它当成有效的正负零？</strong></p>\\n<p>符号只改变后续数字的符号，本身不构成整数；没有消费任何数字时，解析应判为无效并按契约返回 <code>0</code>。实现可记录是否读到至少一位数字，避免把状态推进到符号后就误认为转换成功，也要留意 <code>JavaScript</code> 的 <code>-0</code> 表现。</p>\\n</li>\\n<li>\\n<p><strong>订单导入服务收到数万位数字字符串，代码先完整转成 <code>Number</code> 再截断到 32 位边界；为什么这个顺序不稳妥？</strong></p>\\n<p>超长输入在完成转换前就可能超过安全整数精度甚至成为 <code>Infinity</code>，原始整数信息已经丢失。应逐位累计，并在乘十前用阈值比较，或以 <code>BigInt</code> 累计后截断；前者还能在必然越界时提前返回，避免继续扫描无意义的尾部数字。</p>\\n</li>\\n<li>\\n<p><strong>解析页面输入 <code>4193 with words</code> 得到 <code>4193</code>，输入 <code>words and 987</code> 却必须得到 <code>0</code>；扫描器的停止规则怎样同时满足这两个样例？</strong></p>\\n<p>跳过前导空格和可选符号后，只消费紧邻的连续数字，遇到首个非数字字符便停止并返回已累计值。若第一个有效字符既不是符号也不是数字，或者符号后没有数字，则没有合法数字前缀，应返回 <code>0</code>，不能继续向后搜索数字。</p>\\n</li>\\n<li>\\n<p><strong>接口原来只跳过普通空格 <code>\' \'</code>，国际化页面传入制表符或其他空白字符后结果与产品预期不一致；你会直接改成 <code>trim()</code> 吗？</strong></p>\\n<p>不能在未确认契约时直接扩大空白范围，因为题目实现只明确展示了前导空格处理，而 <code>trim()</code> 会接受更广的 <code>Unicode</code> 空白。应先和产品确认允许的空白集合，再统一入口扫描规则；扩展后要补充空串、全空白及空白后符号等固定用例。</p>\\n</li>\\n<li>\\n<p><strong>线上接口要支持 64 位订单号，开发只把 <code>MIN</code>、<code>MAX</code> 从 32 位常量换成 64 位常量，返回类型仍是 <code>JavaScript</code> <code>Number</code>；评审时你会阻止吗？</strong></p>\\n<p>会阻止，因为超过 <code>Number.MAX_SAFE_INTEGER</code> 后整数不能保证精确，仅更换边界常量无法保住 64 位订单号。应选择字符串或 <code>BigInt</code> 作为返回协议，并同步确认 <code>JSON</code> 序列化、数据库驱动和前端展示链路；<code>BigInt</code> 不能直接按普通 <code>JSON</code> 数字传输。</p>\\n</li>\\n<li>\\n<p><strong>重构后的 <code>atoi</code> 把 <code>-2147483648</code> 错截成 <code>-2147483647</code>，而正数上界用例正常；你会优先检查哪段边界逻辑？</strong></p>\\n<p>先检查上下界是否分别定义为 <code>-(2 ** 31)</code> 与 <code>2 ** 31 - 1</code>，二者绝对值并不相同。再跟踪符号参与比较的时机，确认负数累计到 <code>2147483648</code> 时允许返回 <code>MIN</code>；若统一按正数上界判断，通常会误伤最小负数这一格。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);