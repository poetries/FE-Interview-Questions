(window.webpackJsonp=window.webpackJsonp||[]).push([[123],{1230:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：理解原理要建立输入、状态变化、输出和失败边界，而不是只记 API 与结论</li>\\n<li>原理主线：围绕 「组件类」、「工具类」、「react-hooks」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：系统梳理了 React 的全部核心 API，包括组件类（Component、PureComponent）、内置组件（Fragment、StrictMode）、高阶组件（forwardRef、memo）等，详细解析每个 API 的原理、用法及</li>\\n<li>边界与代价：文章中的实现通常有版本和场景前提，迁移到当前项目时必须先核对运行环境与依赖</li>\\n<li>工程落地：面试回答按核心机制、执行链路、边界反例和工程验证展开，最后说明取舍</li>\\n</ul>\\n<p><strong><code>React</code> 的 <code>API</code> 本质上是在控制组件描述、更新边界和渲染时机，不能只记调用方式。</strong> 比如 <code>PureComponent</code> 浅比较 <code>props</code> 与 <code>state</code>，<code>React.memo</code> 只比较 <code>props</code>，而自定义比较函数返回 <code>true</code> 反而表示跳过渲染。<code>forwardRef</code> 用于转发特殊处理的 <code>ref</code>，<code>lazy</code> 通常配合 <code>Suspense</code> 处理异步组件。涉及旧生命周期或 <code>ReactDOM.render</code> 时，还要区分历史实现与当前版本行为。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文若分析 <code>ReactDOM.render</code>、旧生命周期或栈调和，应把它视为理解架构演进的历史路径。<code>React 19</code> 已移除 <code>ReactDOM.render</code>，当前客户端入口使用 <code>createRoot</code>；并发渲染也必须区分可中断的渲染阶段与同步提交阶段。迁移前对照 <a href=\\"https://react.dev/blog/2024/04/25/react-19-upgrade-guide\\">React 19 官方升级指南</a>。</p>\\n<h2 id=\\"组件类\\"><a class=\\"header-anchor\\" href=\\"#组件类\\">#</a> 组件类</h2>\\n<p>组件类，详细分的话有三种类，第一类说白了就是我平时用于继承的基类组件<code>Component</code>,<code>PureComponent</code>,还有就是<code>react</code>提供的内置的组件，比如<code>Fragment</code>,<code>StrictMode</code>,另一部分就是高阶组件<code>forwardRef</code>,<code>memo</code>等。</p>\\n<p><img src=\\"https://s.poetries.top/images/20210504203949.png\\" alt=\\"\\"></p>\\n<h3 id=\\"component\\"><a class=\\"header-anchor\\" href=\\"#component\\">#</a> Component</h3>\\n<p><code>Component</code>是<code>class</code>组件的根基。\\b类组件一切始于<code>Component</code>。对于<code>React.Component</code>使用，我们没有什么好讲的。我们这里重点研究一下<code>react</code>对<code>Component</code>做了些什么。</p>\\n<pre class=\\"language-js\\"><code>react/src/ReactBaseClasses.js\\nfunction Component(props, context, updater) {\\n  this.props = props;\\n  this.context = context;\\n  this.refs = emptyObject;\\n  this.updater = updater || ReactNoopUpdateQueue;\\n}\\n\\n</code></pre>\\n<p>这就是<code>Component</code>函数，其中<code>updater</code>对象上保存着更新组件的方法。</p>\\n<p><strong>我们声明的类组件是什么时候以何种形式被实例化的呢？</strong></p>\\n<pre class=\\"language-text\\"><code>react-reconciler/src/ReactFiberClassComponent.js\\n</code></pre>\\n<p><strong>constructClassInstance</strong></p>\\n<pre class=\\"language-js\\"><code>function constructClassInstance(\\n    workInProgress,\\n    ctor,\\n    props\\n){\\n   const instance = new ctor(props, context);\\n    instance.updater = {\\n        isMounted,\\n        enqueueSetState(){\\n            /* setState 触发这里面的逻辑 */\\n        },\\n        enqueueReplaceState(){},\\n        enqueueForceUpdate(){\\n            /* forceUpdate 触发这里的逻辑 */\\n        }\\n    }\\n}\\n\\n</code></pre>\\n<p>对于<code>Component</code>， <code>react</code> 处理逻辑还是很简单的，实例化我们类组件，然后赋值<code>updater</code>对象，负责组件的更新。然后在组件各个阶段，执行类组件的<code>render</code>函数，和对应的生命周期函数就可以了。</p>\\n<h3 id=\\"purecomponent\\"><a class=\\"header-anchor\\" href=\\"#purecomponent\\">#</a> PureComponent</h3>\\n<p><code>PureComponent</code>和 <code>Component</code>用法，差不多一样，唯一不同的是，纯组件<code>PureComponent</code>会浅比较，<code>props</code>和<code>state</code>是否相同，来决定是否重新渲染组件。所以一般用于<strong>性能调优</strong>，减少<strong>render</strong>次数。</p>\\n<p>什么叫做<strong>浅比较</strong>，我这里举个列子：</p>\\n<pre class=\\"language-js\\"><code>class Index extends React.PureComponent{\\n    constructor(props){\\n        super(props)\\n        this.state={\\n           data:{\\n              name:&#39;alien&#39;,\\n              age:28\\n           }\\n        }\\n    }\\n    handerClick= () =&gt;{\\n        const { data } = this.state\\n        data.age++\\n        this.setState({ data })\\n    }\\n    render(){\\n        const { data } = this.state\\n        return &lt;div className=&quot;box&quot; &gt;\\n        &lt;div className=&quot;show&quot; &gt;\\n            &lt;div&gt; 你的姓名是: { data.name } &lt;/div&gt;\\n            &lt;div&gt; 年龄： { data.age  }&lt;/div&gt;\\n            &lt;button onClick={ this.handerClick } &gt;age++&lt;/button&gt;\\n        &lt;/div&gt;\\n    &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p><img src=\\"!%5B%5D(https://s.poetries.top/images/202203211401980.png)\\" alt=\\"pureComponent.gif\\"></p>\\n<p><strong>点击按钮，没有任何反应</strong>，因为<code>PureComponent</code>会比较两次<code>data</code>对象，都指向同一个<code>data</code>,没有发生改变，所以不更新视图。</p>\\n<p>解决这个问题很简单，只需要在<code>handerClick</code>事件中这么写：</p>\\n<pre class=\\"language-js\\"><code> this.setState({ data:{...data} })\\n\\n</code></pre>\\n<p><strong>浅拷贝</strong>就能根本解决问题。</p>\\n<h3 id=\\"memo\\"><a class=\\"header-anchor\\" href=\\"#memo\\">#</a> memo</h3>\\n<p><code>React.memo</code>和<code>PureComponent</code>作用类似，可以用作性能优化，<code>React.memo</code> 是高阶组件，函数组件和类组件都可以使用， 和区别<code>PureComponent</code>是 <code>React.memo</code>只能对<code>props</code>的情况确定是否渲染，而<code>PureComponent</code>是针对<code>props</code>和<code>state</code>。</p>\\n<p><code>React.memo</code> 接受两个参数，第一个参数原始组件本身，第二个参数，可以根据一次更新中<code>props</code>是否相同决定原始组件是否重新渲染。是一个返回布尔值，<code>true</code> 证明组件无须重新渲染，<code>false</code>证明组件需要重新渲染，这个和类组件中的<code>shouldComponentUpdate()</code>正好相反 。</p>\\n<p><strong>React.memo: 第二个参数 返回 <code>true</code> 组件不渲染 ， 返回 <code>false</code> 组件重新渲染。</strong> <strong>shouldComponentUpdate: 返回 <code>true</code> 组件渲染 ， 返回 <code>false</code> 组件不渲染。</strong></p>\\n<p>接下来我们做一个场景，控制组件在仅此一个<code>props</code>数字变量，一定范围渲染。</p>\\n<p>例子🌰：</p>\\n<p>控制 <code>props</code> 中的 <code>number</code> ：</p>\\n<ul>\\n<li>1 只有 <code>number</code> 更改，组件渲染。</li>\\n<li>2 只有 <code>number</code> 小于 5 ，组件渲染。</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>function TextMemo(props){\\n    console.log(&#39;子组件渲染&#39;)\\n    if(props)\\n    return &lt;div&gt;hello,world&lt;/div&gt; \\n}\\n\\nconst controlIsRender = (pre,next)=&gt;{\\n   if(pre.number === next.number  ){ // number 不改变 ，不渲染组件\\n       return true \\n   }else if(pre.number !== next.number &amp;&amp; next.number &gt; 5 ) { // number 改变 ，但值大于5 ， 不渲染组件\\n       return true\\n   }else { // 否则渲染组件\\n       return false\\n   }\\n}\\n\\nconst NewTexMemo = memo(TextMemo,controlIsRender)\\nclass Index extends React.Component{\\n    constructor(props){\\n        super(props)\\n        this.state={\\n            number:1,\\n            num:1\\n        }\\n    }\\n    render(){\\n        const { num , number }  = this.state\\n        return &lt;div&gt;\\n            &lt;div&gt;\\n                改变num：当前值 { num }  \\n                &lt;button onClick={ ()=&gt;this.setState({ num:num + 1 }) } &gt;num++&lt;/button&gt;\\n                &lt;button onClick={ ()=&gt;this.setState({ num:num - 1 }) } &gt;num--&lt;/button&gt;  \\n            &lt;/div&gt;\\n            &lt;div&gt;\\n                改变number： 当前值 { number } \\n                &lt;button onClick={ ()=&gt;this.setState({ number:number + 1 }) } &gt; number ++&lt;/button&gt;\\n                &lt;button onClick={ ()=&gt;this.setState({ number:number - 1 }) } &gt; number -- &lt;/button&gt;  \\n            &lt;/div&gt;\\n            &lt;NewTexMemo num={ num } number={number}  /&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p><strong>效果：</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211401466.png\\" alt=\\"\\"></p>\\n<p>完美达到了效果，<code>React.memo</code>一定程度上，可以等价于组件外部使用<code>shouldComponentUpdate</code> ，用于拦截新老<code>props</code>，确定组件是否更新。</p>\\n<h3 id=\\"forwardref\\"><a class=\\"header-anchor\\" href=\\"#forwardref\\">#</a> forwardRef</h3>\\n<p>官网对<code>forwardRef</code>的概念和用法很笼统，也没有给定一个具体的案例。很多同学不知道 <code>forwardRef</code>具体怎么用，下面我结合具体例子给大家讲解<code>forwardRef</code>应用场景。</p>\\n<p><strong>1 转发引入Ref</strong></p>\\n<p>这个场景实际很简单，比如父组件想获取孙组件，某一个<code>dom</code>元素。这种隔代<code>ref</code>获取引用，就需要<code>forwardRef</code>来助力。</p>\\n<pre class=\\"language-js\\"><code>function Son (props){\\n    const { grandRef } = props\\n    return &lt;div&gt;\\n        &lt;div&gt; i am alien &lt;/div&gt;\\n        &lt;span ref={grandRef} &gt;这个是想要获取元素&lt;/span&gt;\\n    &lt;/div&gt;\\n}\\n\\nclass Father extends React.Component{\\n    constructor(props){\\n        super(props)\\n    }\\n    render(){\\n        return &lt;div&gt;\\n            &lt;Son grandRef={this.props.grandRef}  /&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n\\nconst NewFather = React.forwardRef((props,ref)=&gt;&lt;Father grandRef={ref}  {...props} /&gt;  )\\n\\nclass GrandFather extends React.Component{\\n    constructor(props){\\n        super(props)\\n    }\\n    node = null \\n    componentDidMount(){\\n        console.log(this.node)\\n    }\\n    render(){\\n        return &lt;div&gt;\\n            &lt;NewFather ref={(node)=&gt; this.node = node } /&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p><strong>效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/20210504204037.png\\" alt=\\"\\"></p>\\n<p><code>react</code>不允许<code>ref</code>通过<code>props</code>传递，因为组件上已经有 <code>ref</code> 这个属性,在组件调和过程中，已经被特殊处理，<code>forwardRef</code>出现就是解决这个问题，把<code>ref</code>转发到自定义的<code>forwardRef</code>定义的属性上，让<code>ref</code>，可以通过<code>props</code>传递。</p>\\n<p><strong>2 高阶组件转发Ref</strong></p>\\n<p>一文吃透<code>hoc</code>文章中讲到，由于属性代理的<code>hoc</code>，被包裹一层，所以如果是类组件，是通过<code>ref</code>拿不到原始组件的实例的，不过我们可以通过<code>forWardRef</code>转发<code>ref</code>。</p>\\n<pre class=\\"language-js\\"><code>function HOC(Component){\\n  class Wrap extends React.Component{\\n     render(){\\n        const { forwardedRef ,...otherprops  } = this.props\\n        return &lt;Component ref={forwardedRef}  {...otherprops}  /&gt;\\n     }\\n  }\\n  return  React.forwardRef((props,ref)=&gt; &lt;Wrap forwardedRef={ref} {...props} /&gt; ) \\n}\\nclass Index extends React.Component{\\n  componentDidMount(){\\n      console.log(666)\\n  }\\n  render(){\\n    return &lt;div&gt;hello,world&lt;/div&gt;\\n  }\\n}\\nconst HocIndex =  HOC(Index,true)\\nexport default ()=&gt;{\\n  const node = useRef(null)\\n  useEffect(()=&gt;{\\n     /* 就可以跨层级，捕获到 Index 组件的实例了 */ \\n    console.log(node.current.componentDidMount)\\n  },[])\\n  return &lt;div&gt;&lt;HocIndex ref={node}  /&gt;&lt;/div&gt;\\n}\\n\\n\\n</code></pre>\\n<p>如上，解决了高阶组件引入<code>Ref</code>的问题。</p>\\n<h3 id=\\"lazy\\"><a class=\\"header-anchor\\" href=\\"#lazy\\">#</a> lazy</h3>\\n<blockquote>\\n<p>React.lazy 和 Suspense 技术还不支持服务端渲染。如果你想要在使用服务端渲染的应用中使用，我们推荐 Loadable Components 这个库</p>\\n</blockquote>\\n<p><code>React.lazy</code>和<code>Suspense</code>配合一起用，能够有动态加载组件的效果。<code>React.lazy</code> 接受一个函数，这个函数需要动态调用 <code>import()</code>。它必须返回一个 <code>Promise</code> ，该 <code>Promise</code> 需要 <code>resolve</code> 一个 <code>default export</code> 的 <code>React</code> 组件。</p>\\n<p>我们模拟一个动态加载的场景。</p>\\n<p><strong>父组件</strong></p>\\n<pre class=\\"language-js\\"><code>import Test from &#39;./comTest&#39;\\nconst LazyComponent =  React.lazy(()=&gt; new Promise((resolve)=&gt;{\\n      setTimeout(()=&gt;{\\n          resolve({\\n              default: ()=&gt; &lt;Test /&gt;\\n          })\\n      },2000)\\n}))\\nclass index extends React.Component{   \\n    render(){\\n        return &lt;div className=&quot;context_box&quot;  style={ { marginTop :&#39;50px&#39; } }   &gt;\\n           &lt;React.Suspense fallback={ &lt;div className=&quot;icon&quot; &gt;&lt;SyncOutlined  spin  /&gt;&lt;/div&gt; } &gt;\\n               &lt;LazyComponent /&gt;\\n           &lt;/React.Suspense&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p>我们用<code>setTimeout</code>来模拟<code>import</code>异步引入效果。</p>\\n<p><strong>Test</strong></p>\\n<pre class=\\"language-js\\"><code>class Test extends React.Component{\\n    constructor(props){\\n        super(props)\\n    }\\n    componentDidMount(){\\n        console.log(&#39;--componentDidMount--&#39;)\\n    }\\n    render(){\\n        return &lt;div&gt;\\n            &lt;img src={alien}  className=&quot;alien&quot; /&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p><strong>效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211402827.png\\" alt=\\"\\"></p>\\n<h3 id=\\"suspense\\"><a class=\\"header-anchor\\" href=\\"#suspense\\">#</a> Suspense</h3>\\n<p>何为<code>Suspense</code>, <code>Suspense</code> 让组件“等待”某个异步操作，直到该异步操作结束即可渲染。</p>\\n<p>用于数据获取的 <code>Suspense</code> 是一个新特性，你可以使用 <code>&lt;Suspense&gt;</code> 以声明的方式来“等待”任何内容，包括数据。本文重点介绍它在数据获取的用例，它也可以用于等待图像、脚本或其他异步的操作。</p>\\n<p>上面讲到高阶组件<code>lazy</code>时候，已经用 <code>lazy</code> + <code>Suspense</code>模式，构建了异步渲染组件。我们看一下官网文档中的案例：</p>\\n<pre class=\\"language-js\\"><code>const ProfilePage = React.lazy(() =&gt; import(&#39;./ProfilePage&#39;)); // 懒加载\\n&lt;Suspense fallback={&lt;Spinner /&gt;}&gt;\\n  &lt;ProfilePage /&gt;\\n&lt;/Suspense&gt;\\n\\n</code></pre>\\n<h3 id=\\"fragment\\"><a class=\\"header-anchor\\" href=\\"#fragment\\">#</a> Fragment</h3>\\n<p><code>react</code>不允许一个组件返回多个节点元素，比如说如下情况</p>\\n<pre class=\\"language-js\\"><code>render(){\\n    return &lt;li&gt; 🍎🍎🍎 &lt;/li&gt;\\n           &lt;li&gt; 🍌🍌🍌 &lt;/li&gt;\\n           &lt;li&gt; 🍇🍇🍇 &lt;/li&gt;\\n}\\n\\n</code></pre>\\n<p>如果我们想解决这个情况，很简单，只需要在外层套一个容器元素。</p>\\n<pre class=\\"language-js\\"><code>render(){\\n    return &lt;div&gt;\\n           &lt;li&gt; 🍎🍎🍎 &lt;/li&gt;\\n           &lt;li&gt; 🍌🍌🍌 &lt;/li&gt;\\n           &lt;li&gt; 🍇🍇🍇 &lt;/li&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p>但是我们不期望，增加额外的<code>dom</code>节点，所以<code>react</code>提供<code>Fragment</code>碎片概念，能够让一个组件返回多个元素。 所以我们可以这么写</p>\\n<pre class=\\"language-js\\"><code>&lt;React.Fragment&gt;\\n    &lt;li&gt; 🍎🍎🍎 &lt;/li&gt;\\n    &lt;li&gt; 🍌🍌🍌 &lt;/li&gt;\\n    &lt;li&gt; 🍇🍇🍇 &lt;/li&gt;\\n&lt;/React.Fragment&gt;\\n\\n</code></pre>\\n<p>还可以简写成：</p>\\n<pre class=\\"language-js\\"><code>&lt;&gt;\\n    &lt;li&gt; 🍎🍎🍎 &lt;/li&gt;\\n    &lt;li&gt; 🍌🍌🍌 &lt;/li&gt;\\n    &lt;li&gt; 🍇🍇🍇 &lt;/li&gt;\\n&lt;/&gt;\\n\\n</code></pre>\\n<p>和<code>Fragment</code>区别是，<code>Fragment</code>可以支持<code>key</code>属性。<code>&lt;&gt;&lt;/&gt;</code>不支持<code>key</code>属性。</p>\\n<p><strong>温馨提示</strong>。我们通过<code>map</code>遍历后的元素，<code>react</code>底层会处理，默认在外部嵌套一个<code>&lt;Fragment&gt;</code>。</p>\\n<p>比如：</p>\\n<pre class=\\"language-js\\"><code>{\\n   [1,2,3].map(item=&gt;&lt;span key={item.id} &gt;{ item.name }&lt;/span&gt;)\\n}\\n\\n</code></pre>\\n<p><code>react</code>底层处理之后，等价于：</p>\\n<pre class=\\"language-html\\"><code>&lt;Fragment&gt;\\n   &lt;span&gt;&lt;/span&gt;\\n   &lt;span&gt;&lt;/span&gt;\\n   &lt;span&gt;&lt;/span&gt;\\n&lt;/Fragment&gt;\\n\\n</code></pre>\\n<h3 id=\\"profiler\\"><a class=\\"header-anchor\\" href=\\"#profiler\\">#</a> Profiler</h3>\\n<p><code>Profiler</code>这个<code>api</code>一般用于开发阶段，性能检测，检测一次<code>react</code>组件渲染用时，性能开销。</p>\\n<p><code>Profiler</code> 需要两个参数：</p>\\n<p>第一个参数：是 <code>id</code>，用于表识唯一性的<code>Profiler</code>。</p>\\n<p>第二个参数：<code>onRender</code>回调函数，用于渲染完成，接受渲染参数。</p>\\n<p><strong>实践：</strong></p>\\n<pre class=\\"language-js\\"><code>const index = () =&gt; {\\n  const callback = (...arg) =&gt; console.log(arg)\\n  return &lt;div &gt;\\n    &lt;div &gt;\\n      &lt;Profiler id=&quot;root&quot; onRender={ callback }  &gt;\\n        &lt;Router  &gt;\\n          &lt;Meuns/&gt;\\n          &lt;KeepaliveRouterSwitch withoutRoute &gt;\\n              { renderRoutes(menusList) }\\n          &lt;/KeepaliveRouterSwitch&gt;\\n        &lt;/Router&gt;\\n      &lt;/Profiler&gt; \\n    &lt;/div&gt;\\n  &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p><strong>结果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211402516.png\\" alt=\\"\\"></p>\\n<p>onRender</p>\\n<ul>\\n<li>0 -id: <code>root</code>  -&gt;  <code>Profiler</code> 树的 <code>id</code> 。</li>\\n<li>1 -phase: <code>mount</code> -&gt;  <code>mount</code> 挂载 ， <code>update</code> 渲染了。</li>\\n<li>2 -actualDuration: <code>6.685000262223184</code>  -&gt; 更新 <code>committed</code> 花费的渲染时间。</li>\\n<li>3 -baseDuration:  <code>4.430000321008265</code>  -&gt; 渲染整颗子树需要的时间</li>\\n<li>4 -startTime : <code>689.7299999836832</code> -&gt;  本次更新开始渲染的时间</li>\\n<li>5 -commitTime : <code>698.5799999674782</code> -&gt;  本次更新committed 的时间</li>\\n<li>6 -interactions: <code>set{}</code> -&gt; 本次更新的 <code>interactions</code> 的集合</li>\\n</ul>\\n<blockquote>\\n<p>尽管 Profiler 是一个轻量级组件，我们依然应该在需要时才去使用它。对一个应用来说，每添加一些都会给 CPU 和内存带来一些负担。</p>\\n</blockquote>\\n<h3 id=\\"strictmode\\"><a class=\\"header-anchor\\" href=\\"#strictmode\\">#</a> StrictMode</h3>\\n<p><code>StrictMode</code>见名知意，严格模式，用于检测<code>react</code>项目中的潜在的问题，。与 <code>Fragment</code> 一样， <code>StrictMode</code> 不会渲染任何可见的 <code>UI</code> 。它为其后代元素触发额外的检查和警告。</p>\\n<blockquote>\\n<p>严格模式检查仅在开发模式下运行；它们不会影响生产构建。</p>\\n</blockquote>\\n<p><code>StrictMode</code>目前有助于：</p>\\n<ul>\\n<li>①识别不安全的生命周期。</li>\\n<li>②关于使用过时字符串 <code>ref API</code> 的警告</li>\\n<li>③关于使用废弃的 <code>findDOMNode</code> 方法的警告</li>\\n<li>④检测意外的副作用</li>\\n<li>⑤检测过时的 <code>context API</code></li>\\n</ul>\\n<p><strong>实践:识别不安全的生命周期</strong></p>\\n<p>对于不安全的生命周期，指的是<code>UNSAFE_componentWillMount</code>，<code>UNSAFE_componentWillReceiveProps</code> , <code>UNSAFE_componentWillUpdate</code></p>\\n<pre class=\\"language-text\\"><code>外层开启严格模式：\\n&lt;React.StrictMode&gt; \\n    &lt;Router  &gt;\\n        &lt;Meuns/&gt;\\n        &lt;KeepaliveRouterSwitch withoutRoute &gt;\\n            { renderRoutes(menusList) }\\n        &lt;/KeepaliveRouterSwitch&gt;\\n    &lt;/Router&gt;\\n&lt;/React.StrictMode&gt;\\n\\n我们在内层组件中，使用不安全的生命周期:\\nclass Index extends React.Component{    \\n    UNSAFE_componentWillReceiveProps(){\\n    }\\n    render(){      \\n        return &lt;div className=&quot;box&quot; /&gt;   \\n    }\\n}\\n\\n效果：\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/images/202203211402367.png\\" alt=\\"\\"></p>\\n<h2 id=\\"工具类\\"><a class=\\"header-anchor\\" href=\\"#工具类\\">#</a> 工具类</h2>\\n<p>接下来我们一起来探究一下<code>react</code>工具类函数的用法。</p>\\n<p><img src=\\"https://s.poetries.top/images/202203211402146.png\\" alt=\\"\\"></p>\\n<h3 id=\\"createelement\\"><a class=\\"header-anchor\\" href=\\"#createelement\\">#</a> createElement</h3>\\n<p>一提到<code>createElement</code>，就不由得和<code>JSX</code>联系一起。我们写的<code>jsx</code>，最终会被 <code>babel</code>，用<code>createElement</code>编译成<code>react</code>元素形式。我写一个组件，我们看一下会被编译成什么样子，</p>\\n<p>如果我们在<code>render</code>里面这么写：</p>\\n<pre class=\\"language-js\\"><code>render(){\\n    return &lt;div className=&quot;box&quot; &gt;\\n        &lt;div className=&quot;item&quot;  &gt;生命周期&lt;/div&gt;\\n        &lt;Text  mes=&quot;hello,world&quot;  /&gt;\\n        &lt;React.Fragment&gt; Flagment &lt;/React.Fragment&gt;\\n        { /*  */ }\\n        text文本\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p>会被编译成这样：</p>\\n<pre class=\\"language-js\\"><code>render() {\\n    return React.createElement(&quot;div&quot;, { className: &quot;box&quot; },\\n            React.createElement(&quot;div&quot;, { className: &quot;item&quot; }, &quot;\\\\u751F\\\\u547D\\\\u5468\\\\u671F&quot;),\\n            React.createElement(Text, { mes: &quot;hello,world&quot; }),\\n            React.createElement(React.Fragment, null, &quot; Flagment &quot;),\\n            &quot;text\\\\u6587\\\\u672C&quot;);\\n    }\\n\\n</code></pre>\\n<p>当然我们可以不用<code>jsx</code>模式，而是直接通过<code>createElement</code>进行开发。</p>\\n<p><strong><code>createElement</code>模型:</strong></p>\\n<pre class=\\"language-js\\"><code>React.createElement(\\n  type,\\n  [props],\\n  [...children]\\n)\\n\\n</code></pre>\\n<p><code>createElement</code>参数：</p>\\n<p>**第一个参数:**如果是组件类型，会传入组件，如果是<code>dom</code>元素类型，传入<code>div</code>或者<code>span</code>之类的字符串。</p>\\n<p><strong>第二个参数:</strong>:第二个参数为一个对象，在<code>dom</code>类型中为<strong>属性</strong>，在<code>组件</code>类型中为<strong>props</strong>。</p>\\n<p><strong>其他参数:</strong>，依次为<code>children</code>，根据顺序排列。</p>\\n<p><strong>createElement做了些什么？</strong></p>\\n<p>经过<code>createElement</code>处理，最终会形成 <code>$$typeof = Symbol(react.element)</code>对象。对象上保存了该<code>react.element</code>的信息。</p>\\n<h3 id=\\"cloneelement\\"><a class=\\"header-anchor\\" href=\\"#cloneelement\\">#</a> cloneElement</h3>\\n<p>可能有的同学还傻傻的分不清楚<code>cloneElement</code>和<code>createElement</code>区别和作用。</p>\\n<p><code>createElement</code>把我们写的<code>jsx</code>，变成<code>element</code>对象;  而<code>cloneElement</code>的作用是以 <code>element</code> 元素为样板克隆并返回新的 <code>React</code> 元素。返回元素的 <code>props</code> 是将新的 <code>props</code> 与原始元素的 <code>props</code> 浅层合并后的结果。</p>\\n<p>那么<code>cloneElement</code>感觉在我们实际业务组件中，可能没什么用，但是在<strong>一些开源项目，或者是公共插槽组件中</strong>用处还是蛮大的，比如说，我们可以在组件中，劫持<code>children element</code>，然后通过<code>cloneElement</code>克隆<code>element</code>，混入<code>props</code>。经典的案例就是 <code>react-router</code>中的<code>Swtich</code>组件，通过这种方式，来匹配唯一的 <code>Route</code>并加以渲染。</p>\\n<p>我们设置一个场景，在组件中，去劫持<code>children</code>，然后给<code>children</code>赋能一些额外的<code>props</code>:</p>\\n<pre class=\\"language-js\\"><code>function FatherComponent({ children }){\\n    const newChildren = React.cloneElement(children, { age: 18})\\n    return &lt;div&gt; { newChildren } &lt;/div&gt;\\n}\\n\\nfunction SonComponent(props){\\n    console.log(props)\\n    return &lt;div&gt;hello,world&lt;/div&gt;\\n}\\n\\nclass Index extends React.Component{    \\n    render(){      \\n        return &lt;div className=&quot;box&quot; &gt;\\n            &lt;FatherComponent&gt;\\n                &lt;SonComponent name=&quot;alien&quot;  /&gt;\\n            &lt;/FatherComponent&gt;\\n        &lt;/div&gt;   \\n    }\\n}\\n\\n</code></pre>\\n<p><strong>打印：</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211402099.png\\" alt=\\"\\"></p>\\n<p>完美达到了效果！</p>\\n<h3 id=\\"createcontext\\"><a class=\\"header-anchor\\" href=\\"#createcontext\\">#</a> createContext</h3>\\n<p><code>createContext</code>用于创建一个<code>Context</code>对象，<code>createContext</code>对象中，包括用于传递 <code>Context</code> 对象值 <code>value</code>的<code>Provider</code>，和接受<code>value</code>变化订阅的<code>Consumer</code>。</p>\\n<pre class=\\"language-js\\"><code>const MyContext = React.createContext(defaultValue)\\n\\n</code></pre>\\n<p><code>createContext</code>接受一个参数<code>defaultValue</code>，如果<code>Consumer</code>上一级一直没有<code>Provider</code>,则会应用<code>defaultValue</code>作为<code>value</code>。<strong>只有</strong>当组件所处的树中没有匹配到 <code>Provider</code> 时，其 <code>defaultValue</code> 参数才会生效。</p>\\n<p>我们来模拟一个 <code>Context.Provider</code>和<code>Context.Consumer</code>的例子：</p>\\n<pre class=\\"language-js\\"><code>function ComponentB(){\\n    /* 用 Consumer 订阅， 来自 Provider 中 value 的改变  */\\n    return &lt;MyContext.Consumer&gt;\\n        { (value) =&gt; &lt;ComponentA  {...value} /&gt; }\\n    &lt;/MyContext.Consumer&gt;\\n}\\n\\nfunction ComponentA(props){\\n    const { name , mes } = props\\n    return &lt;div&gt; \\n            &lt;div&gt; 姓名： { name }  &lt;/div&gt;\\n            &lt;div&gt; 想对大家说： { mes }  &lt;/div&gt;\\n         &lt;/div&gt;\\n}\\n\\nfunction index(){\\n    const [ value , ] = React.useState({\\n        name:&#39;alien&#39;,\\n        mes:&#39;let us learn React &#39;\\n    })\\n    return &lt;div style={{ marginTop:&#39;50px&#39; }} &gt;\\n        &lt;MyContext.Provider value={value}  &gt;\\n          &lt;ComponentB /&gt;\\n    &lt;/MyContext.Provider&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p><strong>打印结果：</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211402455.png\\" alt=\\"\\"></p>\\n<p><code>Provider</code>和<code>Consumer</code>的良好的特性，可以做数据的<strong>存</strong>和<strong>取</strong>，<code>Consumer</code>一方面传递<code>value</code>,另一方面可以订阅<code>value</code>的改变。</p>\\n<p><code>Provider</code>还有一个特性可以层层传递<code>value</code>，这种特性在<code>react-redux</code>中表现的淋漓尽致。</p>\\n<h3 id=\\"createfactory\\"><a class=\\"header-anchor\\" href=\\"#createfactory\\">#</a> createFactory</h3>\\n<pre class=\\"language-js\\"><code>React.createFactory(type)\\n\\n</code></pre>\\n<p>返回用于生成指定类型 React 元素的函数。类型参数既可以是标签名字符串（像是 \'<code>div</code>\' 或 \'<code>span</code>\'），也可以是 React 组件 类型 （ <code>class</code> 组件或函数组件），或是 <code>React fragment</code> 类型。</p>\\n<p>使用：</p>\\n<pre class=\\"language-js\\"><code> const Text = React.createFactory(()=&gt;&lt;div&gt;hello,world&lt;/div&gt;) \\nfunction Index(){  \\n    return &lt;div style={{ marginTop:&#39;50px&#39;  }} &gt;\\n        &lt;Text/&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p><strong>效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211402510.png\\" alt=\\"\\"></p>\\n<p>报出警告，这个<code>api</code>将要被废弃，我们这里就不多讲了，如果想要达到同样的效果，请用<code>React.createElement</code></p>\\n<h3 id=\\"createref\\"><a class=\\"header-anchor\\" href=\\"#createref\\">#</a> createRef</h3>\\n<p><code>createRef</code>可以创建一个 <code>ref</code> 元素，附加在<code>react</code>元素上。</p>\\n<p><strong>用法：</strong></p>\\n<pre class=\\"language-js\\"><code>class Index extends React.Component{\\n    constructor(props){\\n        super(props)\\n        this.node = React.createRef()\\n    }\\n    componentDidMount(){\\n        console.log(this.node)\\n    }\\n    render(){\\n        return &lt;div ref={this.node} &gt; my name is alien &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p>个人觉得<code>createRef</code>这个方法，很鸡肋，我们完全可以<code>class</code>类组件中这么写，来捕获<code>ref</code>。</p>\\n<pre class=\\"language-js\\"><code>class Index extends React.Component{\\n    node = null\\n    componentDidMount(){\\n        console.log(this.node)\\n    }\\n    render(){\\n        return &lt;div ref={(node)=&gt; this.node } &gt; my name is alien &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p>或者在<code>function</code>组件中这么写：</p>\\n<pre class=\\"language-js\\"><code>function Index(){\\n    const node = React.useRef(null)\\n    useEffect(()=&gt;{\\n        console.log(node.current)\\n    },[])\\n    return &lt;div ref={node} &gt;  my name is alien &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<h3 id=\\"isvalidelement\\"><a class=\\"header-anchor\\" href=\\"#isvalidelement\\">#</a> isValidElement</h3>\\n<p>这个方法可以用来检测是否为<code>react element</code>元素,接受待验证对象，返回<code>true</code>或者<code>false</code>。这个api可能对于业务组件的开发，作用不大，因为对于组件内部状态，都是已知的，我们根本就不需要去验证，是否是<code>react element</code> 元素。 但是，对于一起公共组件或是开源库，<code>isValidElement</code>就很有作用了。</p>\\n<p><strong>实践</strong></p>\\n<p>我们做一个场景，验证容器组件的所有子组件，过滤到非<code>react element</code>类型。</p>\\n<p>没有用<code>isValidElement</code>验证之前：</p>\\n<pre class=\\"language-js\\"><code>const Text = () =&gt; &lt;div&gt;hello,world&lt;/div&gt; \\nclass WarpComponent extends React.Component{\\n    constructor(props){\\n        super(props)\\n    }\\n    render(){\\n        return this.props.children\\n    }\\n}\\nfunction Index(){\\n    return &lt;div style={{ marginTop:&#39;50px&#39; }} &gt;\\n        &lt;WarpComponent&gt;\\n            &lt;Text/&gt;\\n            &lt;div&gt; my name is alien &lt;/div&gt;\\n            Let&#39;s learn react together!\\n        &lt;/WarpComponent&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p><strong>过滤之前的效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211403570.png\\" alt=\\"\\"></p>\\n<p><strong>我们用<code>isValidElement</code>进行<code>react element</code>验证:</strong></p>\\n<pre class=\\"language-js\\"><code>class WarpComponent extends React.Component{\\n    constructor(props){\\n        super(props)\\n        this.newChidren = this.props.children.filter(item =&gt; React.isValidElement(item) )\\n    }\\n    render(){\\n        return this.newChidren\\n    }\\n}\\n\\n</code></pre>\\n<p><strong>过滤之后效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211403268.png\\" alt=\\"\\"></p>\\n<p>过滤掉了非<code>react element</code> 的 <code>Let\'s learn react together!</code>。</p>\\n<h3 id=\\"children-map\\"><a class=\\"header-anchor\\" href=\\"#children-map\\">#</a> Children.map</h3>\\n<p>接下来的五个<code>api</code>都是和<code>react.Chidren</code>相关的，我们来分别介绍一下，我们先来看看官网的描述，<code>React.Children</code> 提供了用于处理 <code>this.props.children</code> 不透明数据结构的实用方法。</p>\\n<p>有的同学会问遍历 <code>children</code>用数组方法,<code>map</code> ，<code>forEach</code> 不就可以了吗？ 请我们注意一下<code>不透明数据结构</code>,什么叫做不透明结构?</p>\\n<p><strong>我们先看一下透明的结构：</strong></p>\\n<pre class=\\"language-js\\"><code>class Text extends React.Component{\\n    render(){\\n        return &lt;div&gt;hello,world&lt;/div&gt;\\n    }\\n}\\nfunction WarpComponent(props){\\n    console.log(props.children)\\n    return props.children\\n}\\nfunction Index(){\\n    return &lt;div style={{ marginTop:&#39;50px&#39; }} &gt;\\n        &lt;WarpComponent&gt;\\n            &lt;Text/&gt;\\n            &lt;Text/&gt;\\n            &lt;Text/&gt;\\n            &lt;span&gt;hello,world&lt;/span&gt;\\n        &lt;/WarpComponent&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p><strong>打印</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211403738.png\\" alt=\\"\\"></p>\\n<p>但是我们把<code>Index</code>结构改变一下：</p>\\n<pre class=\\"language-js\\"><code>function Index(){\\n    return &lt;div style={{ marginTop:&#39;50px&#39; }} &gt;\\n        &lt;WarpComponent&gt;\\n            { new Array(3).fill(0).map(()=&gt;&lt;Text/&gt;) }\\n            &lt;span&gt;hello,world&lt;/span&gt;\\n        &lt;/WarpComponent&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p><strong>打印</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211403397.png\\" alt=\\"\\"></p>\\n<p>这个数据结构，我们不能正常的遍历了，即使遍历也不能遍历，每一个子元素。此时就需要 <code>react.Chidren</code> 来帮忙了。</p>\\n<p>但是我们把<code>WarpComponent</code>组件用<code>react.Chidren</code>处理<code>children</code>:</p>\\n<pre class=\\"language-js\\"><code>function WarpComponent(props){\\n    const newChildren = React.Children.map(props.children,(item)=&gt;item)\\n    console.log(newChildren)\\n    return newChildren\\n} \\n\\n</code></pre>\\n<p>此时就能正常遍历了，达到了预期效果。</p>\\n<p><img src=\\"https://s.poetries.top/images/202203211403428.png\\" alt=\\"\\"></p>\\n<p><strong>注意</strong> 如果 <code>children</code> 是一个 <code>Fragment</code> 对象，它将被视为单一子节点的情况处理，而不会被遍历。</p>\\n<h3 id=\\"children-foreach\\"><a class=\\"header-anchor\\" href=\\"#children-foreach\\">#</a> Children.forEach</h3>\\n<p><code>Children.forEach</code>和<code>Children.map</code> 用法类似，<code>Children.map</code>可以返回新的数组，<code>Children.forEach</code>仅停留在遍历阶段。</p>\\n<p>我们将上面的<code>WarpComponent</code>方法，用<code>Children.forEach</code>改一下。</p>\\n<pre class=\\"language-js\\"><code>function WarpComponent(props){\\n    React.Children.forEach(props.children,(item)=&gt;console.log(item))\\n    return props.children\\n}   \\n\\n</code></pre>\\n<h3 id=\\"children-count\\"><a class=\\"header-anchor\\" href=\\"#children-count\\">#</a> Children.count</h3>\\n<p><code>children</code> 中的组件总数量，等同于通过 <code>map</code> 或 <code>forEach</code> 调用回调函数的次数。对于更复杂的结果，<code>Children.count</code>可以返回同一级别子组件的数量。</p>\\n<p>我们还是把上述例子进行改造：</p>\\n<pre class=\\"language-js\\"><code>function WarpComponent(props){\\n    const childrenCount =  React.Children.count(props.children)\\n    console.log(childrenCount,&#39;childrenCount&#39;)\\n    return props.children\\n}   \\nfunction Index(){\\n    return &lt;div style={{ marginTop:&#39;50px&#39; }} &gt;\\n        &lt;WarpComponent&gt;\\n            { new Array(3).fill(0).map((item,index) =&gt; new Array(2).fill(1).map((item,index1)=&gt;&lt;Text key={index+index1} /&gt;)) }\\n            &lt;span&gt;hello,world&lt;/span&gt;\\n        &lt;/WarpComponent&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p><strong>效果:</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211403851.png\\" alt=\\"\\"></p>\\n<h3 id=\\"children-toarray\\"><a class=\\"header-anchor\\" href=\\"#children-toarray\\">#</a> Children.toArray</h3>\\n<p><code>Children.toArray</code>返回，<code>props.children</code>扁平化后结果。</p>\\n<pre class=\\"language-js\\"><code>function WarpComponent(props){\\n    const newChidrenArray =  React.Children.toArray(props.children)\\n    console.log(newChidrenArray,&#39;newChidrenArray&#39;)\\n    return newChidrenArray\\n}   \\nfunction Index(){\\n    return &lt;div style={{ marginTop:&#39;50px&#39; }} &gt;\\n        &lt;WarpComponent&gt;\\n            { new Array(3).fill(0).map((item,index)=&gt;new Array(2).fill(1).map((item,index1)=&gt;&lt;Text key={index+index1} /&gt;)) }\\n            &lt;span&gt;hello,world&lt;/span&gt;\\n        &lt;/WarpComponent&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p><strong>效果：</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211403763.png\\" alt=\\"\\"></p>\\n<p><strong>newChidrenArray</strong> ,就是扁平化的数组结构。<code>React.Children.toArray()</code> 在拉平展开子节点列表时，更改 <code>key</code> 值以保留嵌套数组的语义。也就是说， <code>toArray</code> 会为返回数组中的每个 <code>key</code> 添加前缀，以使得每个元素 <code>key</code> 的范围都限定在此函数入参数组的对象内。</p>\\n<h3 id=\\"children-only\\"><a class=\\"header-anchor\\" href=\\"#children-only\\">#</a> Children.only</h3>\\n<p>验证 <code>children</code> 是否只有一个子节点（一个 <code>React</code> 元素），如果有则返回它，否则此方法会抛出错误。</p>\\n<p><strong>不唯一</strong></p>\\n<pre class=\\"language-js\\"><code>function WarpComponent(props){\\n    console.log(React.Children.only(props.children))\\n    return props.children\\n}   \\nfunction Index(){\\n    return &lt;div style={{ marginTop:&#39;50px&#39; }} &gt;\\n        &lt;WarpComponent&gt;\\n            { new Array(3).fill(0).map((item,index)=&gt;&lt;Text key={index} /&gt;) }\\n            &lt;span&gt;hello,world&lt;/span&gt;\\n        &lt;/WarpComponent&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p><strong>效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211403735.png\\" alt=\\"\\"></p>\\n<p><strong>唯一</strong></p>\\n<pre class=\\"language-js\\"><code>function WarpComponent(props){\\n    console.log(React.Children.only(props.children))\\n    return props.children\\n}   \\nfunction Index(){\\n    return &lt;div style={{ marginTop:&#39;50px&#39; }} &gt;\\n        &lt;WarpComponent&gt;\\n           &lt;Text/&gt;\\n        &lt;/WarpComponent&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<p><strong>效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211403294.png\\" alt=\\"\\"></p>\\n<p><code>React.Children.only()</code> 不接受 <code>React.Children.map()</code> 的返回值，因为它是一个数组而并不是 <code>React</code> 元素。</p>\\n<h2 id=\\"react-hooks\\"><a class=\\"header-anchor\\" href=\\"#react-hooks\\">#</a> react-hooks</h2>\\n<p>对于<code>react-hooks</code>,我已经写了三部曲，介绍了<code>react-hooks</code>使用，自定义<code>hooks</code>，以及<code>react-hooks</code>原理，感兴趣的同学可以去看看，文章末尾有链接，对于常用的<code>api</code>，我这里参考了<code>react-hooks</code>如何使用那篇文章。并做了相应精简化和一些内容的补充。</p>\\n<p><img src=\\"https://s.poetries.top/images/202203211403098.png\\" alt=\\"\\"></p>\\n<h3 id=\\"usestate\\"><a class=\\"header-anchor\\" href=\\"#usestate\\">#</a> useState</h3>\\n<p><code>useState</code>可以弥补函数组件没有<code>state</code>的缺陷。<code>useState</code>可以接受一个初识值，也可以是一个函数<code>action</code>，<code>action</code>返回值作为新的<code>state</code>。返回一个数组，第一个值为<code>state</code>读取值，第二个值为改变<code>state</code>的<code>dispatchAction</code>函数。</p>\\n<p>我们看一个例子：</p>\\n<pre class=\\"language-jsx\\"><code>const DemoState = (props) =&gt; {\\n   /* number为此时state读取值 ，setNumber为派发更新的函数 */\\n   let [number, setNumber] = useState(0) /* 0为初始值 */\\n   return (&lt;div&gt;\\n       &lt;span&gt;{ number }&lt;/span&gt;\\n       &lt;button onClick={ ()=&gt; {\\n         setNumber(number+1) /* 写法一 */\\n         setNumber(number=&gt;number + 1 ) /* 写法二 */\\n         console.log(number) /* 这里的number是不能够即时改变的  */\\n       } } &gt;num++&lt;/button&gt;\\n   &lt;/div&gt;)\\n}\\n\\n</code></pre>\\n<h3 id=\\"useeffect\\"><a class=\\"header-anchor\\" href=\\"#useeffect\\">#</a> useEffect</h3>\\n<p><code>useEffect</code>可以弥补函数组件没有生命周期的缺点。我们可以在<code>useEffect</code>第一个参数回调函数中，做一些请求数据，事件监听等操作，第二个参数作为<code>dep</code>依赖项，当依赖项发生变化，重新执行第一个函数。</p>\\n<p><strong>useEffect可以用作数据交互。</strong></p>\\n<pre class=\\"language-jsx\\"><code>/* 模拟数据交互 */\\nfunction getUserInfo(a){\\n    return new Promise((resolve)=&gt;{\\n        setTimeout(()=&gt;{ \\n           resolve({\\n               name:a,\\n               age:16,\\n           }) \\n        },500)\\n    })\\n}\\nconst DemoEffect = ({ a }) =&gt; {\\n    const [ userMessage , setUserMessage ] :any= useState({})\\n    const div= useRef()\\n    const [number, setNumber] = useState(0)\\n    /* 模拟事件监听处理函数 */\\n    const handleResize =()=&gt;{}\\n    /* useEffect使用 ，这里如果不加限制 ，会是函数重复执行，陷入死循环*/\\n    useEffect(()=&gt;{\\n        /* 请求数据 */\\n       getUserInfo(a).then(res=&gt;{\\n           setUserMessage(res)\\n       })\\n       /* 操作dom  */\\n       console.log(div.current) /* div */\\n       /* 事件监听等 */\\n        window.addEventListener(&#39;resize&#39;, handleResize)\\n    /* 只有当props-&gt;a和state-&gt;number改变的时候 ,useEffect副作用函数重新执行 ，如果此时数组为空[]，证明函数只有在初始化的时候执行一次相当于componentDidMount */\\n    },[ a ,number ])\\n    return (&lt;div ref={div} &gt;\\n        &lt;span&gt;{ userMessage.name }&lt;/span&gt;\\n        &lt;span&gt;{ userMessage.age }&lt;/span&gt;\\n        &lt;div onClick={ ()=&gt; setNumber(1) } &gt;{ number }&lt;/div&gt;\\n    &lt;/div&gt;)\\n}\\n\\n\\n</code></pre>\\n<p><strong>useEffect可以用作事件监听，还有一些基于<code>dom</code>的操作。</strong>,别忘了在<code>useEffect</code>第一个参数回调函数，返一个函数用于清除事件监听等操作。</p>\\n<pre class=\\"language-jsx\\"><code>const DemoEffect = ({ a }) =&gt; {\\n    /* 模拟事件监听处理函数 */\\n    const handleResize =()=&gt;{}\\n    useEffect(()=&gt;{\\n       /* 定时器 延时器等 */\\n       const timer = setInterval(()=&gt;console.log(666),1000)\\n       /* 事件监听 */\\n       window.addEventListener(&#39;resize&#39;, handleResize)\\n       /* 此函数用于清除副作用 */\\n       return function(){\\n           clearInterval(timer) \\n           window.removeEventListener(&#39;resize&#39;, handleResize)\\n       }\\n    },[ a ])\\n    return (&lt;div  &gt;\\n    &lt;/div&gt;)\\n}\\n\\n\\n</code></pre>\\n<h3 id=\\"usememo\\"><a class=\\"header-anchor\\" href=\\"#usememo\\">#</a> useMemo</h3>\\n<p><code>useMemo</code>接受两个参数，第一个参数是一个函数，返回值用于产生<strong>保存值</strong>。 第二个参数是一个数组，作为<code>dep</code>依赖项，数组里面的依赖项发生变化，重新执行第一个函数，产生<strong>新的值</strong>。</p>\\n<p>应用场景： <strong>1 缓存一些值，避免重新执行上下文</strong></p>\\n<pre class=\\"language-js\\"><code>const number = useMemo(()=&gt;{\\n    /** ....大量的逻辑运算 **/\\n   return number\\n},[ props.number ]) // 只有 props.number 改变的时候，重新计算number的值。\\n\\n</code></pre>\\n<p><strong>2 减少不必要的<code>dom</code>循环</strong></p>\\n<pre class=\\"language-js\\"><code>/* 用 useMemo包裹的list可以限定当且仅当list改变的时候才更新此list，这样就可以避免selectList重新循环 */\\n {useMemo(() =&gt; (\\n      &lt;div&gt;{\\n          selectList.map((i, v) =&gt; (\\n              &lt;span\\n                  className={style.listSpan}\\n                  key={v} &gt;\\n                  {i.patentName} \\n              &lt;/span&gt;\\n          ))}\\n      &lt;/div&gt;\\n), [selectList])}\\n\\n\\n</code></pre>\\n<p><strong>3 减少子组件渲染</strong></p>\\n<pre class=\\"language-js\\"><code>/* 只有当props中，list列表改变的时候，子组件才渲染 */\\nconst  goodListChild = useMemo(()=&gt; &lt;GoodList list={ props.list } /&gt; ,[ props.list ])\\n\\n</code></pre>\\n<h3 id=\\"usecallback\\"><a class=\\"header-anchor\\" href=\\"#usecallback\\">#</a> useCallback</h3>\\n<p><code>useMemo</code> 和 <code>useCallback</code> 接收的参数都是一样，都是在其依赖项发生变化后才执行，都是返回缓存的值，区别在于 <code>useMemo</code> 返回的是函数运行的结果， <code>useCallback</code> 返回的是函数。 返回的<code>callback</code>可以作为<code>props</code>回调函数传递给子组件。</p>\\n<pre class=\\"language-js\\"><code>/* 用react.memo */\\nconst DemoChildren = React.memo((props)=&gt;{\\n   /* 只有初始化的时候打印了 子组件更新 */\\n    console.log(&#39;子组件更新&#39;)\\n   useEffect(()=&gt;{\\n       props.getInfo(&#39;子组件&#39;)\\n   },[])\\n   return &lt;div&gt;子组件&lt;/div&gt;\\n})\\nconst DemoUseCallback=({ id })=&gt;{\\n    const [number, setNumber] = useState(1)\\n    /* 此时usecallback的第一参数 (sonName)=&gt;{ console.log(sonName) }\\n     经过处理赋值给 getInfo */\\n    const getInfo  = useCallback((sonName)=&gt;{\\n          console.log(sonName)\\n    },[id])\\n    return &lt;div&gt;\\n        {/* 点击按钮触发父组件更新 ，但是子组件没有更新 */}\\n        &lt;button onClick={ ()=&gt;setNumber(number+1) } &gt;增加&lt;/button&gt;\\n        &lt;DemoChildren getInfo={getInfo} /&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<h3 id=\\"useref\\"><a class=\\"header-anchor\\" href=\\"#useref\\">#</a> useRef</h3>\\n<p><code>useRef</code>的作用：</p>\\n<ul>\\n<li>一 是可以用来获取<code>dom</code>元素，或者<code>class</code>组件实例 。</li>\\n<li>二 <code>react-hooks原理</code>文章中讲过，创建<code>useRef</code>时候，会创建一个原始对象，只要函数组件不被销毁，原始对象就会一直存在，那么我们可以利用这个特性，来通过<code>useRef</code>保存一些数据。</li>\\n</ul>\\n<pre class=\\"language-jsx\\"><code>const DemoUseRef = ()=&gt;{\\n    const dom= useRef(null)\\n    const handerSubmit = ()=&gt;{\\n        /*  &lt;div &gt;表单组件&lt;/div&gt;  dom 节点 */\\n        console.log(dom.current)\\n    }\\n    return &lt;div&gt;\\n        {/* ref 标记当前dom节点 */}\\n        &lt;div ref={dom} &gt;表单组件&lt;/div&gt;\\n        &lt;button onClick={()=&gt;handerSubmit()} &gt;提交&lt;/button&gt; \\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<h3 id=\\"uselayouteffect\\"><a class=\\"header-anchor\\" href=\\"#uselayouteffect\\">#</a> useLayoutEffect</h3>\\n<p><strong><code>useEffect</code>执行顺序:</strong> 组件更新挂载完成 -&gt; 浏览器 <code>dom</code> 绘制完成 -&gt; 执行 <code>useEffect</code> 回调。 <strong><code>useLayoutEffect</code> 执行顺序:</strong> 组件更新挂载完成 -&gt;  执行 <code>useLayoutEffect</code> 回调-&gt; 浏览器<code>dom</code>绘制完成。</p>\\n<p>所以说 <code>useLayoutEffect</code> 代码可能会阻塞浏览器的绘制 。我们写的 <code>effect</code>和 <code>useLayoutEffect</code>，<code>react</code>在底层会被分别打上<code>PassiveEffect</code>，<code>HookLayout</code>，在<code>commit</code>阶段区分出，在什么时机执行。</p>\\n<pre class=\\"language-jsx\\"><code>const DemoUseLayoutEffect = () =&gt; {\\n    const target = useRef()\\n    useLayoutEffect(() =&gt; {\\n        /*我们需要在dom绘制之前，移动dom到制定位置*/\\n        const { x ,y } = getPositon() /* 获取要移动的 x,y坐标 */\\n        animate(target.current,{ x,y })\\n    }, []);\\n    return (\\n        &lt;div &gt;\\n            &lt;span ref={ target } className=&quot;animate&quot;&gt;&lt;/span&gt;\\n        &lt;/div&gt;\\n    )\\n}\\n\\n</code></pre>\\n<h3 id=\\"usereducer\\"><a class=\\"header-anchor\\" href=\\"#usereducer\\">#</a> useReducer</h3>\\n<p>在<code>react-hooks</code>原理那篇文章中讲解到，<code>useState</code>底层就是一个简单版的<code>useReducer</code></p>\\n<p><code>useReducer</code> 接受的第一个参数是一个函数，我们可以认为它就是一个 <code>reducer</code> , <code>reducer</code> 的参数就是常规 <code>reducer</code> 里面的 <code>state</code> 和  <code>action</code> ,返回改变后的 <code>state</code> , <code>useReducer</code> 第二个参数为 <code>state</code> 的初始值 返回一个数组，数组的第一项就是更新之后 <code>state</code> 的值 ，第二个参数是派发更新的 <code>dispatch</code> 函数。</p>\\n<p>我们来看一下<code>useReducer</code>如何使用：</p>\\n<pre class=\\"language-js\\"><code>const DemoUseReducer = ()=&gt;{\\n    /* number为更新后的state值,  dispatchNumbner 为当前的派发函数 */\\n   const [ number , dispatchNumbner ] = useReducer((state,action)=&gt;{\\n       const { payload , name  } = action\\n       /* return的值为新的state */\\n       switch(name){\\n           case &#39;add&#39;:\\n               return state + 1\\n           case &#39;sub&#39;:\\n               return state - 1 \\n           case &#39;reset&#39;:\\n             return payload       \\n       }\\n       return state\\n   },0)\\n   return &lt;div&gt;\\n      当前值：{ number }\\n      { /* 派发更新 */ }\\n      &lt;button onClick={()=&gt;dispatchNumbner({ name:&#39;add&#39; })} &gt;增加&lt;/button&gt;\\n      &lt;button onClick={()=&gt;dispatchNumbner({ name:&#39;sub&#39; })} &gt;减少&lt;/button&gt;\\n      &lt;button onClick={()=&gt;dispatchNumbner({ name:&#39;reset&#39; ,payload:666 })} &gt;赋值&lt;/button&gt;\\n      { /* 把dispatch 和 state 传递给子组件  */ }\\n      &lt;MyChildren  dispatch={ dispatchNumbner } State={{ number }} /&gt;\\n   &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<h3 id=\\"usecontext\\"><a class=\\"header-anchor\\" href=\\"#usecontext\\">#</a> useContext</h3>\\n<p>我们可以使用 <code>useContext</code> ，来获取父级组件传递过来的 <code>context</code> 值，这个当前值就是最近的父级组件 <code>Provider</code> 设置的 <code>value</code> <code>值，useContext</code> 参数一般是由 <code>createContext</code> 方式引入 ,也可以父级上下文 <code>context</code> 传递 ( 参数为 <code>context</code> )。<code>useContext</code> 可以代替 <code>context.Consumer</code> 来获取 <code>Provider</code> 中保存的 <code>value</code> 值</p>\\n<pre class=\\"language-jsx\\"><code>/* 用useContext方式 */\\nconst DemoContext = ()=&gt; {\\n    const value:any = useContext(Context)\\n    /* my name is alien */\\nreturn &lt;div&gt; my name is { value.name }&lt;/div&gt;\\n}\\n/* 用Context.Consumer 方式 */\\nconst DemoContext1 = ()=&gt;{\\n    return &lt;Context.Consumer&gt;\\n         {/*  my name is alien  */}\\n        { (value)=&gt; &lt;div&gt; my name is { value.name }&lt;/div&gt; }\\n    &lt;/Context.Consumer&gt;\\n}\\n\\nexport default ()=&gt;{\\n    return &lt;div&gt;\\n        &lt;Context.Provider value={{ name:&#39;alien&#39; , age:18 }} &gt;\\n            &lt;DemoContext /&gt;\\n            &lt;DemoContext1 /&gt;\\n        &lt;/Context.Provider&gt;\\n    &lt;/div&gt;\\n}\\n\\n</code></pre>\\n<h3 id=\\"useimperativehandle\\"><a class=\\"header-anchor\\" href=\\"#useimperativehandle\\">#</a> useImperativeHandle</h3>\\n<p><code>useImperativeHandle</code> 可以配合 <code>forwardRef</code>自定义暴露给父组件的实例值。这个很有用，我们知道，对于子组件，如果是<code>class</code>类组件，我们可以通过<code>ref</code>获取类组件的实例，但是在子组件是函数组件的情况，如果我们不能直接通过<code>ref</code>的，那么此时<code>useImperativeHandle</code>和 <code>forwardRef</code>配合就能达到效果。</p>\\n<p><code>useImperativeHandle</code>接受三个参数：</p>\\n<ul>\\n<li>第一个参数ref: 接受 <code>forWardRef</code> 传递过来的 <code>ref</code>。</li>\\n<li>第二个参数 <code>createHandle</code> ：处理函数，返回值作为暴露给父组件的<code>ref</code>对象。</li>\\n<li>第三个参数 <code>deps</code>:依赖项 <code>deps</code>，依赖项更改形成新的<code>ref</code>对象。</li>\\n</ul>\\n<p><strong>我们来模拟给场景，用<code>useImperativeHandle</code>，使得父组件能让子组件中的<code>input</code>自动赋值并聚焦。</strong></p>\\n<pre class=\\"language-js\\"><code>function Son (props,ref) {\\n    console.log(props)\\n    const inputRef = useRef(null)\\n    const [ inputValue , setInputValue ] = useState(&#39;&#39;)\\n    useImperativeHandle(ref,()=&gt;{\\n       const handleRefs = {\\n           /* 声明方法用于聚焦input框 */\\n           onFocus(){\\n              inputRef.current.focus()\\n           },\\n           /* 声明方法用于改变input的值 */\\n           onChangeValue(value){\\n               setInputValue(value)\\n           }\\n       }\\n       return handleRefs\\n    },[])\\n    return &lt;div&gt;\\n        &lt;input\\n            placeholder=&quot;请输入内容&quot;\\n            ref={inputRef}\\n            value={inputValue}\\n        /&gt;\\n    &lt;/div&gt;\\n}\\n\\nconst ForwarSon = forwardRef(Son)\\n\\nclass Index extends React.Component{\\n    cur = null\\n    handerClick(){\\n       const { onFocus , onChangeValue } =this.cur\\n       onFocus()\\n       onChangeValue(&#39;let us learn React!&#39;)\\n    }\\n    render(){\\n        return &lt;div style={{ marginTop:&#39;50px&#39; }} &gt;\\n            &lt;ForwarSon ref={cur =&gt; (this.cur = cur)} /&gt;\\n            &lt;button onClick={this.handerClick.bind(this)} &gt;操控子组件&lt;/button&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p><strong>效果:</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211404678.png\\" alt=\\"\\"></p>\\n<h3 id=\\"usedebugvalue\\"><a class=\\"header-anchor\\" href=\\"#usedebugvalue\\">#</a> useDebugValue</h3>\\n<pre class=\\"language-text\\"><code>useDebugValue` 可用于在 `React` 开发者工具中显示自定义 `hook` 的标签。这个`hooks`目的就是检查自定义`hooks\\nfunction useFriendStatus(friendID) {\\n  const [isOnline, setIsOnline] = useState(null);\\n  // ...\\n  // 在开发者工具中的这个 Hook 旁边显示标签\\n  // e.g. &quot;FriendStatus: Online&quot;\\n  useDebugValue(isOnline ? &#39;Online&#39; : &#39;Offline&#39;);\\n\\n  return isOnline;\\n}\\n\\n</code></pre>\\n<blockquote>\\n<p>我们不推荐你向每个自定义 Hook 添加 debug 值。当它作为共享库的一部分时才最有价值。在某些情况下，格式化值的显示可能是一项开销很大的操作。除非需要检查 Hook，否则没有必要这么做。因此，useDebugValue 接受一个格式化函数作为可选的第二个参数。该函数只有在 Hook 被检查时才会被调用。它接受 debug 值作为参数，并且会返回一个格式化的显示值。</p>\\n</blockquote>\\n<h3 id=\\"usetransition\\"><a class=\\"header-anchor\\" href=\\"#usetransition\\">#</a> useTransition</h3>\\n<p><code>useTransition</code>允许延时由<code>state</code>改变而带来的视图渲染。避免不必要的渲染。它还允许组件将速度较慢的数据获取更新推迟到随后渲染，以便能够立即渲染更重要的更新。</p>\\n<pre class=\\"language-js\\"><code>const TIMEOUT_MS = { timeoutMs: 2000 }\\nconst [startTransition, isPending] = useTransition(TIMEOUT_MS)\\n\\n</code></pre>\\n<ul>\\n<li><code>useTransition</code> 接受一个对象， <code>timeoutMs</code>代码需要延时的时间。</li>\\n<li>返回一个数组。<strong>第一个参数：</strong>  是一个接受回调的函数。我们用它来告诉 <code>React</code> 需要推迟的 <code>state</code> 。 <strong>第二个参数：</strong> 一个布尔值。表示是否正在等待，过度状态的完成(延时<code>state</code>的更新)。</li>\\n</ul>\\n<p>下面我们引入官网的列子，来了解<code>useTransition</code>的使用。</p>\\n<pre class=\\"language-js\\"><code>const SUSPENSE_CONFIG = { timeoutMs: 2000 };\\n\\nfunction App() {\\n  const [resource, setResource] = useState(initialResource);\\n  const [startTransition, isPending] = useTransition(SUSPENSE_CONFIG);\\n  return (\\n    &lt;&gt;\\n      &lt;button\\n        disabled={isPending}\\n        onClick={() =&gt; {\\n          startTransition(() =&gt; {\\n            const nextUserId = getNextId(resource.userId);\\n            setResource(fetchProfileData(nextUserId));\\n          });\\n        }}\\n      &gt;\\n        Next\\n      &lt;/button&gt;\\n      {isPending ? &quot; 加载中...&quot; : null}\\n      &lt;Suspense fallback={&lt;Spinner /&gt;}&gt;\\n        &lt;ProfilePage resource={resource} /&gt;\\n      &lt;/Suspense&gt;\\n    &lt;/&gt;\\n  );\\n}\\n\\n</code></pre>\\n<p>在这段代码中，我们使用 <code>startTransition</code> 包装了我们的数据获取。这使我们可以立即开始获取用户资料的数据，同时推迟下一个用户资料页面以及其关联的 <code>Spinner</code> 的渲染 2 秒钟（ <code>timeoutMs</code>  中显示的时间）。</p>\\n<p>这个<code>api</code>目前处于实验阶段，没有被完全开放出来。</p>\\n<h2 id=\\"react-dom\\"><a class=\\"header-anchor\\" href=\\"#react-dom\\">#</a> react-dom</h2>\\n<p>接下来，我们来一起研究<code>react-dom</code>中比较重要的<code>api</code>。</p>\\n<p><img src=\\"https://s.poetries.top/images/202203211404500.png\\" alt=\\"\\"></p>\\n<h3 id=\\"render\\"><a class=\\"header-anchor\\" href=\\"#render\\">#</a> render</h3>\\n<p><code>render</code> 是我们最常用的<code>react-dom</code>的 <code>api</code>，用于渲染一个<code>react</code>元素，一般<code>react</code>项目我们都用它，渲染根部容器<code>app</code>。</p>\\n<pre class=\\"language-js\\"><code>ReactDOM.render(element, container[, callback])\\n\\n</code></pre>\\n<p><strong>使用</strong></p>\\n<pre class=\\"language-jsx\\"><code>ReactDOM.render(\\n    &lt; App / &gt;,\\n    document.getElementById(&#39;app&#39;)\\n)\\n\\n</code></pre>\\n<p><code>ReactDOM.render</code>会控制<code>container</code>容器节点里的内容，但是不会修改容器节点本身。</p>\\n<h3 id=\\"hydrate\\"><a class=\\"header-anchor\\" href=\\"#hydrate\\">#</a> hydrate</h3>\\n<p>服务端渲染用<code>hydrate</code>。用法与 <code>render()</code> 相同，但它用于在 <code>ReactDOMServer</code> 渲染的容器中对 <code>HTML</code> 的内容进行 <code>hydrate</code> 操作。</p>\\n<pre class=\\"language-js\\"><code>ReactDOM.hydrate(element, container[, callback])\\n\\n</code></pre>\\n<h3 id=\\"createportal\\"><a class=\\"header-anchor\\" href=\\"#createportal\\">#</a> createPortal</h3>\\n<p><code>Portal</code> 提供了一种将子节点渲染到存在于父组件以外的 <code>DOM</code> 节点的优秀的方案。<code>createPortal</code> 可以把当前组件或 <code>element</code> 元素的子节点，渲染到组件之外的其他地方。</p>\\n<p>那么具体应用到什么场景呢？</p>\\n<p>比如一些全局的弹窗组件<code>model</code>,<code>&lt;Model/&gt;</code>组件一般都写在我们的组件内部，倒是真正挂载的<code>dom</code>，都是在外层容器，比如<code>body</code>上。此时就很适合<code>createPortal</code>API。</p>\\n<p><code>createPortal</code>接受两个参数：</p>\\n<pre class=\\"language-js\\"><code>ReactDOM.createPortal(child, container)\\n\\n</code></pre>\\n<p>第一个： <code>child</code> 是任何可渲染的 <code>React</code> 子元素 第二个： <code>container</code>是一个 <code>DOM</code> 元素。</p>\\n<p>接下来我们实践一下：</p>\\n<pre class=\\"language-js\\"><code>function WrapComponent({ children }){\\n    const domRef = useRef(null)\\n    const [ PortalComponent, setPortalComponent ] = useState(null)\\n    React.useEffect(()=&gt;{\\n        setPortalComponent( ReactDOM.createPortal(children,domRef.current) )\\n    },[])\\n    return &lt;div&gt; \\n        &lt;div className=&quot;container&quot; ref={ domRef } &gt;&lt;/div&gt;\\n        { PortalComponent }\\n     &lt;/div&gt;\\n}\\n\\nclass Index extends React.Component{\\n    render(){\\n        return &lt;div style={{ marginTop:&#39;50px&#39; }} &gt;\\n             &lt;WrapComponent&gt;\\n               &lt;div  &gt;hello,world&lt;/div&gt;\\n            &lt;/WrapComponent&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p><strong>效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211404332.png\\" alt=\\"\\"></p>\\n<p>我们可以看到，我们<code>children</code>实际在<code>container</code> 之外挂载的，但是已经被<code>createPortal</code>渲染到<code>container</code>中。</p>\\n<h3 id=\\"unstable-batchedupdates\\"><a class=\\"header-anchor\\" href=\\"#unstable-batchedupdates\\">#</a> unstable_batchedUpdates</h3>\\n<p>在<code>react-legacy</code>模式下，对于事件，<code>react</code>事件有批量更新来处理功能,但是这一些非常规的事件中，批量更新功能会被打破。所以我们可以用<code>react-dom</code>中提供的<code>unstable_batchedUpdates</code> 来进行批量更新。</p>\\n<p><strong>一次点击实现的批量更新</strong></p>\\n<pre class=\\"language-js\\"><code>class Index extends React.Component{\\n    constructor(props){\\n       super(props)\\n       this.state={\\n           numer:1,\\n       }\\n    }\\n    handerClick=()=&gt;{\\n        this.setState({ numer : this.state.numer + 1 })\\n        console.log(this.state.numer)\\n        this.setState({ numer : this.state.numer + 1 })\\n        console.log(this.state.numer)\\n        this.setState({ numer : this.state.numer + 1 })\\n        console.log(this.state.numer)\\n    }\\n    render(){\\n        return &lt;div  style={{ marginTop:&#39;50px&#39; }} &gt; \\n            &lt;button onClick={ this.handerClick } &gt;click me&lt;/button&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p><strong>效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211404594.png\\" alt=\\"\\"></p>\\n<p>渲染次数一次。</p>\\n<p><strong>批量更新条件被打破</strong></p>\\n<pre class=\\"language-js\\"><code> handerClick=()=&gt;{\\n    Promise.resolve().then(()=&gt;{\\n        this.setState({ numer : this.state.numer + 1 })\\n        console.log(this.state.numer)\\n        this.setState({ numer : this.state.numer + 1 })\\n        console.log(this.state.numer)\\n        this.setState({ numer : this.state.numer + 1 })\\n        console.log(this.state.numer)\\n    })\\n  }\\n\\n</code></pre>\\n<p><strong>效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211404673.png\\" alt=\\"\\"></p>\\n<p>渲染次数三次。</p>\\n<p><strong>unstable_batchedUpdate助力</strong></p>\\n<pre class=\\"language-js\\"><code> handerClick=()=&gt;{\\n        Promise.resolve().then(()=&gt;{\\n            ReactDOM.unstable_batchedUpdates(()=&gt;{\\n                this.setState({ numer : this.state.numer + 1 })\\n                console.log(this.state.numer)\\n                this.setState({ numer : this.state.numer + 1 })\\n                console.log(this.state.numer)\\n                this.setState({ numer : this.state.numer + 1 })\\n                console.log(this.state.numer)\\n            }) \\n        })\\n    }\\n\\n</code></pre>\\n<p>渲染次数一次,完美解决批量更新问题。</p>\\n<h3 id=\\"flushsync\\"><a class=\\"header-anchor\\" href=\\"#flushsync\\">#</a> flushSync</h3>\\n<p><code>flushSync</code> 可以将回调函数中的更新任务，放在一个较高的优先级中。我们知道<code>react</code>设定了很多不同优先级的更新任务。如果一次更新任务在<code>flushSync</code>回调函数内部，那么将获得一个较高优先级的更新。比如</p>\\n<pre class=\\"language-js\\"><code>ReactDOM.flushSync(()=&gt;{\\n    /* 此次更新将设置一个较高优先级的更新 */\\n    this.setState({ name: &#39;alien&#39;  })\\n})\\n\\n</code></pre>\\n<p>为了让大家理解<code>flushSync</code>，我这里做一个<code>demo</code>奉上，</p>\\n<pre class=\\"language-js\\"><code>/* flushSync */\\nimport ReactDOM from &#39;react-dom&#39;\\nclass Index extends React.Component{\\n    state={ number:0 }\\n    handerClick=()=&gt;{\\n        setTimeout(()=&gt;{\\n            this.setState({ number: 1  })\\n        })\\n        this.setState({ number: 2  })\\n        ReactDOM.flushSync(()=&gt;{\\n            this.setState({ number: 3  })\\n        })\\n        this.setState({ number: 4  })\\n    }\\n    render(){\\n        const { number } = this.state\\n        console.log(number) // 打印什么？？\\n        return &lt;div&gt;\\n            &lt;div&gt;{ number }&lt;/div&gt;\\n            &lt;button onClick={this.handerClick} &gt;测试flushSync&lt;/button&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p>先不看答案，点击一下按钮，打印什么呢？</p>\\n<p><strong>我们来点击一下看看</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211404922.png\\" alt=\\"\\"></p>\\n<p>打印 0 3 4 1 ，相信不难理解为什么这么打印了。</p>\\n<ul>\\n<li>首先 <code>flushSync</code> <code>this.setState({ number: 3 })</code>设定了一个高优先级的更新，所以3 先被打印</li>\\n<li>2 4 被批量更新为 4</li>\\n</ul>\\n<p>相信这个<code>demo</code>让我们更深入了解了<code>flushSync</code>。</p>\\n<h3 id=\\"finddomnode\\"><a class=\\"header-anchor\\" href=\\"#finddomnode\\">#</a> findDOMNode</h3>\\n<p><code>findDOMNode</code>用于访问组件<code>DOM</code>元素节点，<code>react</code>推荐使用<code>ref</code>模式，不期望使用<code>findDOMNode</code>。</p>\\n<pre class=\\"language-js\\"><code>ReactDOM.findDOMNode(component)\\n\\n</code></pre>\\n<p>注意的是：</p>\\n<ul>\\n<li>1 <code>findDOMNode</code>只能用在已经挂载的组件上。</li>\\n<li>2  如果组件渲染内容为 <code>null</code> 或者是 <code>false</code>，那么 <code>findDOMNode</code>返回值也是 <code>null</code>。</li>\\n<li>3 <code>findDOMNode</code> 不能用于函数组件。</li>\\n</ul>\\n<p>接下来让我们看一下，<code>findDOMNode</code>具体怎么使用的：</p>\\n<pre class=\\"language-js\\"><code>class Index extends React.Component{\\n    handerFindDom=()=&gt;{\\n        console.log(ReactDOM.findDOMNode(this))\\n    }\\n    render(){\\n        return &lt;div style={{ marginTop:&#39;100px&#39; }} &gt;\\n            &lt;div&gt;hello,world&lt;/div&gt;\\n            &lt;button onClick={ this.handerFindDom } &gt;获取容器dom&lt;/button&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n\\n</code></pre>\\n<p><strong>效果：</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211404848.png\\" alt=\\"\\"></p>\\n<p>我们完全可以将外层容器用<code>ref</code>来标记，获取捕获原生的<code>dom</code>节点。</p>\\n<h3 id=\\"unmountcomponentatnode\\"><a class=\\"header-anchor\\" href=\\"#unmountcomponentatnode\\">#</a> unmountComponentAtNode</h3>\\n<p>从 <code>DOM</code> 中卸载组件，会将其事件处理器和 <code>state</code> 一并清除。 如果指定容器上没有对应已挂载的组件，这个函数什么也不会做。如果组件被移除将会返回 <code>true</code> ，如果没有组件可被移除将会返回  <code>false</code> 。</p>\\n<p>我们来简单举例看看<code>unmountComponentAtNode</code>如何使用？</p>\\n<pre class=\\"language-js\\"><code>function Text(){\\n    return &lt;div&gt;hello,world&lt;/div&gt;\\n}\\n\\nclass Index extends React.Component{\\n    node = null\\n    constructor(props){\\n       super(props)\\n       this.state={\\n           numer:1,\\n       }\\n    }\\n    componentDidMount(){\\n        /*  组件初始化的时候，创建一个 container 容器 */\\n        ReactDOM.render(&lt;Text/&gt; , this.node )\\n    }\\n    handerClick=()=&gt;{\\n       /* 点击卸载容器 */ \\n       const state =  ReactDOM.unmountComponentAtNode(this.node)\\n       console.log(state)\\n    }\\n    render(){\\n        return &lt;div  style={{ marginTop:&#39;50px&#39; }}  &gt; \\n             &lt;div ref={ ( node ) =&gt; this.node = node  }  &gt;&lt;/div&gt;  \\n            &lt;button onClick={ this.handerClick } &gt;click me&lt;/button&gt;\\n        &lt;/div&gt;\\n    }\\n}\\n</code></pre>\\n<p><strong>效果</strong></p>\\n<p><img src=\\"https://s.poetries.top/images/202203211405130.png\\" alt=\\"\\">\\n<img src=\\"https://s.poetries.top/images/202203211405780.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>弹窗关闭时，业务代码已经执行 <code>container.remove()</code>，同事认为这等同于 <code>unmountComponentAtNode</code>；如果弹窗里还有点击事件和组件 <code>state</code>，你怎么判断？</strong></p>\\n<p>直接移除容器不等同于通过 <code>React</code> 卸载组件，因为后者还会清除组件关联的事件处理器和 <code>state</code>。应先对挂载容器调用 <code>ReactDOM.unmountComponentAtNode(container)</code>，再按需移除容器；绕开卸载流程可能遗留资源，源码未覆盖的外部订阅仍需组件自行清理。</p>\\n</li>\\n<li>\\n<p><strong>微前端基座反复打开和关闭同一个 <code>React</code> 子应用，容器节点会复用；你会怎样把 <code>unmountComponentAtNode</code> 接进销毁流程？</strong></p>\\n<p>销毁阶段应保存实际执行过 <code>ReactDOM.render</code> 的容器引用，并对同一节点调用 <code>ReactDOM.unmountComponentAtNode(container)</code>。返回 <code>true</code> 说明本次确实移除了已挂载组件，返回 <code>false</code> 则应记录容器或生命周期是否错配；它不能替代业务创建的定时器和外部监听清理。</p>\\n</li>\\n<li>\\n<p><strong>组件最初渲染到 <code>nodeA</code>，重构后 <code>ref</code> 指向了 <code>nodeB</code>，关闭页面时 <code>unmountComponentAtNode(nodeB)</code> 返回 <code>false</code>；你会如何解释这个结果？</strong></p>\\n<p>返回 <code>false</code> 只表示指定容器上没有可移除的已挂载组件，不代表原组件已经卸载。应核对 <code>ReactDOM.render</code> 与卸载调用是否使用同一个 <code>DOM</code> 容器，并检查 <code>ref</code> 更新时机；错误容器会让旧挂载继续存在，不能把布尔值当作全局销毁状态。</p>\\n</li>\\n<li>\\n<p><strong>线上抽屉关闭后界面消失，但再次进入时旧组件行为仍然存在，日志显示卸载结果为 <code>false</code>；你会按什么顺序排查？</strong></p>\\n<p>先确认界面消失是 <code>React</code> 卸载还是父级 <code>DOM</code> 被直接隐藏或移除，再比对挂载与卸载时记录的容器引用。随后用最小复现验证第一次调用应返回 <code>true</code>、重复调用应返回 <code>false</code>；若 <code>React</code> 卸载已成功，残留行为更可能来自组件之外的订阅或资源。</p>\\n</li>\\n<li>\\n<p><strong>团队准备把所有局部隐藏都改成 <code>unmountComponentAtNode</code> 来降低内存占用，但产品要求再次展开时保留表单内容，你会接受吗？</strong></p>\\n<p>不会把隐藏和卸载视为同一种操作，因为卸载会清除组件 <code>state</code>，再次挂载不能自然保留表单现场。需要保留状态时可继续挂载并控制可见性，或先把状态提升到外部再卸载；前者保留资源，后者增加状态同步和恢复成本。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);