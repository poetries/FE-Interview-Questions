(window.webpackJsonp=window.webpackJsonp||[]).push([[19],{1127:function(e){e.exports=JSON.parse('{"html":"<h2 id=\\"快慢指针与多指针\\"><a class=\\"header-anchor\\" href=\\"#快慢指针与多指针\\">#</a> 快慢指针与多指针</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>快慢指针不是固定速度名词，而是用两个位置编码距离、阶段或环内相对运动</li>\\n<li>固定间距可定位倒数位置，速度差可检测环，同速多指针可维护待处理区间和已处理前缀</li>\\n<li>每个指针都要有一句语义，例如 fast 指向前方第 n 个位置、slow 指向目标前驱，不能只背变量名</li>\\n<li>移动前先检查可达性；链表边界常见 <code>fast</code>、<code>fast.next</code> 与 <code>fast.next.next</code> 三种条件，不能互换</li>\\n<li>多指针通常只用 <code>O(1)</code> 引用空间，但仍可能走 <code>O(n)</code> 时间；空间换时间并不是所有双指针题的准确描述</li>\\n</ul>\\n<p><strong>快慢指针和多指针的本质，是用几个指针的相对位置保存链表遍历过程中的状态。</strong> 因为链表不能按下标随机访问，写代码前要说清每个指针的不变量，例如删除倒数结点时，<code>fast</code> 与 <code>slow</code> 的间距始终为 <code>n</code>。遇到反转时，也可以让 <code>prev</code> 表示已反转前缀的头，<code>current</code> 表示尚未处理后缀的头，避免多个指针发生错位。移动前还要按实际访问层级检查 <code>fast</code>、<code>fast.next</code> 等边界；这类方法通常只占 <code>O(1)</code> 引用空间，但遍历时间仍可能是 <code>O(n)</code>。</p>\\n</section>\\n<p>链表无法按下标随机访问，指针的相对位置就是算法状态。写代码前应先说出不变量：例如删除倒数结点时 fast 与 slow 的间距始终为 n；反转时 prev 是已反转前缀的头，current 是未处理后缀的头。若说不出这句话，代码里的三四个指针很容易错位。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在删除倒数第 <code>n</code> 个结点时固定让 <code>fast</code> 每轮走两步、<code>slow</code> 走一步；你会如何指出这不是通用的快慢指针规则？</strong></p>\\n<p>删除倒数结点需要的是 <code>fast</code> 与 <code>slow</code> 始终保持 <code>n</code> 个结点的间距，因此应先建立间距，再让两者同速前进。<code>2:1</code> 速度常服务于环检测，机械套用会破坏当前任务的不变量并删错位置。</p>\\n</li>\\n<li>\\n<p><strong>一个链表编辑页面要原地删除倒数结点，代码评审时你会要求开发者在循环旁写清哪条不变量？</strong></p>\\n<p>应写清 <code>fast</code> 与 <code>slow</code> 的间距始终为 <code>n</code>，以及当 <code>fast</code> 到达链表末端时 <code>slow</code> 对应待删除位置的关系。代码中的初始化、提前移动和终止条件都要围绕这句话验证，否则头结点等边界容易错位。</p>\\n</li>\\n<li>\\n<p><strong>需求从删除倒数结点改成原地反转链表，原来的双指针角色还能保持不变吗？</strong></p>\\n<p>不能只替换变量名，反转时需要维护“<code>prev</code> 是已反转前缀的头，<code>current</code> 是未处理后缀的头”这一状态。更新 <code>current.next</code> 前还要暂存后继结点，否则未处理部分会丢失；指针数量相近不代表不变量相同。</p>\\n</li>\\n<li>\\n<p><strong>线上偶发链表断裂，日志显示执行过 <code>current.next = prev</code>；你会优先检查哪一个更新顺序？</strong></p>\\n<p>优先确认修改 <code>current.next</code> 之前是否先保存原后继结点，以及随后是否按保存值推进 <code>current</code>。如果先改指向再读取后继，未处理后缀会丢失；若 <code>prev</code> 更新过早，也可能形成错误自环或跳过结点。</p>\\n</li>\\n<li>\\n<p><strong>评审者认为使用三个指针就不再是 <code>O(1)</code> 空间，要求改成递归以“减少变量”；你会怎样裁决？</strong></p>\\n<p>固定数量的指针引用不会随链表长度增长，辅助空间仍是 <code>O(1)</code>，无需为了少一个局部变量改成递归。递归调用栈通常会随结点数增长，还引入深度风险；复杂度应看存储规模，而不是变量表面数量。</p>\\n</li>\\n<li>\\n<p><strong>候选人同时做环检测、删除倒数结点和链表反转，你会要求他用什么共同视角串联三段代码？</strong></p>\\n<p>共同视角是把指针的相对位置和所指区间视为算法状态，并在每次移动后维持明确不变量。环检测关注速度关系，删除关注固定间距，反转关注已处理与未处理边界；三者共享方法，但不能共享同一移动规则。</p>\\n</li>\\n</ul>\\n</section>\\n<p>链表题目中，有一类会涉及到反复的遍历。涉及反复遍历的题目，题目本身虽然不会直接跟你说“你好，我是一道需要反复遍历的题目”，但只要你尝试用常规的思路分析它，你会发现它一定涉及反复遍历；同时，涉及反复遍历的题目，还有一个更明显的特征，就是它们往往会涉及相对复杂的链表操作，比如反转、指定位置的删除等等。</p>\\n<blockquote>\\n<p>解决这类问题，我们用到的是双指针中的“快慢指针”。快慢指针指的是两个一前一后的指针，两个指针往同一个方向走，只是一个快一个慢。快慢指针严格来说只能有俩，不过实际做题中，可能会出现一前、一中、一后的三个指针，这种超过两个指针的解题方法也叫“多指针法”。</p>\\n</blockquote>\\n<p>快慢指针+多指针，双管齐下，可以帮助我们解决链表中的大部分复杂操作问题。</p>\\n<h2 id=\\"快慢指针-删除链表的倒数第-n-个结点\\"><a class=\\"header-anchor\\" href=\\"#快慢指针-删除链表的倒数第-n-个结点\\">#</a> 快慢指针——删除链表的倒数第 N 个结点</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>dummy 让删除头结点也拥有前驱；fast 与 slow 都从 dummy 开始</li>\\n<li>fast 先走 n 步，再让两者同速前进；不变量是 fast 始终领先 slow n 个结点</li>\\n<li>当 fast 到最后一个结点时，slow 正好位于倒数第 n 个结点的前驱，执行 <code>slow.next = slow.next.next</code></li>\\n<li>一趟扫描时间 <code>O(L)</code>、额外空间 <code>O(1)</code>；两趟“先数长度再定位”也为 <code>O(L)</code>，区别是遍历次数和流式能力</li>\\n<li>生产函数要处理 n 非正、n 大于长度、空链和成环输入，不能只依赖题目“n 有效”</li>\\n</ul>\\n<p><strong>我会让 <code>fast</code> 和 <code>slow</code> 都从 <code>dummy</code> 出发，先让 <code>fast</code> 走 <code>n</code> 步，再同步移动，最后删除 <code>slow.next</code>。</strong> 因为两者始终相差 <code>n</code> 个结点，所以 <code>fast</code> 到达链尾时，<code>slow</code> 正好停在待删结点的前驱。<code>dummy</code> 能统一处理删除头结点的情况，整套操作时间是 <code>O(L)</code>、额外空间是 <code>O(1)</code>。公共函数还要明确处理空链、非法 <code>n</code>、<code>n</code> 超过长度以及成环输入。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>function removeNthFromEnd(head, n) {\\n  if (!Number.isInteger(n) || n &lt;= 0) throw new RangeError(&#39;n must be positive&#39;)\\n  const dummy = { next: head }\\n  let fast = dummy\\n  let slow = dummy\\n  for (let step = 0; step &lt; n; step += 1) {\\n    fast = fast.next\\n    if (fast === null) throw new RangeError(&#39;n exceeds list length&#39;)\\n  }\\n  while (fast.next !== null) {\\n    fast = fast.next\\n    slow = slow.next\\n  }\\n  slow.next = slow.next.next\\n  return dummy.next\\n}\\n</code></pre>\\n<p>使用 dummy 后，长度为 L 且 n=L 时，fast 走到原链尾，slow 仍停在 dummy，恰好删除原 head。题库通常保证 n 有效；公共函数必须明确非法输入是抛错、返回原链还是结构化失败。</p>\\n<blockquote>\\n<p>真题描述：给定一个链表，删除链表的倒数第 n 个结点，并且返回链表的头结点。</p>\\n</blockquote>\\n<p>示例：</p>\\n<ul>\\n<li>给定一个链表: 1-&gt;2-&gt;3-&gt;4-&gt;5, 和 n = 2.</li>\\n<li>当删除了倒数第二个结点后，链表变为 1-&gt;2-&gt;3-&gt;5.</li>\\n</ul>\\n<blockquote>\\n<p>说明：给定的 n 保证是有效的。</p>\\n</blockquote>\\n<p><strong>思路分析</strong></p>\\n<p>小贴士：dummy 结点的使用</p>\\n<blockquote>\\n<p>上一节我给大家介绍了 dummy 结点：它可以帮我们处理掉头结点为空的边界问题，帮助我们简化解题过程。因此涉及链表操作、尤其是涉及结点删除的题目（对前驱结点的存在性要求比较高），我都建议大家写代码的时候直接把 dummy 给用起来，建立好的编程习惯：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const dummy = new ListNode()\\n// 这里的 head 是链表原有的第一个结点\\ndummy.next = head\\n</code></pre>\\n<p><strong>“倒数”变“正数”</strong></p>\\n<p>链表的删除我们上节已经讲过，相信都难不倒大家。这道题的难点实际在于这个“倒数第 N 个”如何定位。</p>\\n<p>考虑到咱们的遍历不可能从后往前走，因此这个“倒数第 N 个” 咱们完全可以转换为“正数第 len - n + 1&quot;个。这里这个 len 代表链表的总长度，比如说咱们链表长为 7，那么倒数第 1 个就是正数第 7 个。按照这个思路往下分析，如果走直接遍历这条路，那么这个 len 就非常关键了。</p>\\n<p>我们可以直接遍历两趟：第一趟，设置一个变量 count = 0，每遍历到一个不为空的结点，count 就加 1，一直遍历到链表结束为止，得出链表的总长度 len；根据这个总长度，咱们就可以算出倒数第 n 个到底是正数第几个了（M = len - n + 1），那么我们遍历到第 M - 1（也就是 len - n） 个结点的时候就可以停下来，执行删除操作（想一想，为什么是第 M-1 个，而不是第 M 个？如果你认真读了我们前面的章节，心中一定会有一个清晰的答案^_^）</p>\\n<p>不过这种超过一次的遍历必然需要引起我们的注意，我们应该主动去思考，“如果一次遍历来解决这个问题，我可以怎么做？”，这时候，就要请双指针法来帮忙了。</p>\\n<p><strong>快慢指针登场</strong></p>\\n<p>按照我们已经预告过的思路，首先两个指针 slow 和 fast，全部指向链表的起始位——dummy 结点：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114323.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>快指针先出发！闷头走上 n 步，在第 n 个结点处打住，这里 n=2：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905114335.png\\" alt=\\"\\"></p>\\n<p>然后，快慢指针一起前进，当快指针前进到最后一个结点处时，两个指针再一起停下来：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114400.png\\" alt=\\"\\">\\n<img src=\\"https://s.poetries.top/images/20210905114410.png\\" alt=\\"\\"></p>\\n<p>此时，慢指针所指的位置，就是倒数第 n 个结点的前一个结点：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114536.png\\" alt=\\"\\"></p>\\n<p>我们基于这个结点来做删除，可以说是手到擒来：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114546.png\\" alt=\\"\\"></p>\\n<p>到这里，我们总结一下：</p>\\n<p>链表删除问题中，若走两次遍历，我们做了两件事：</p>\\n<ol>\\n<li>求长度</li>\\n<li>做减法，找定位。</li>\\n</ol>\\n<blockquote>\\n<p>若用快慢指针，我们其实是把做减法和找定位这个过程给融合了。通过快指针先行一步、接着快慢指针一起前进这个操作，巧妙地把两个指针之间的差值保持在了“n”上（用空间换时间，本质上其实就是对关键信息进行提前记忆，这里咱们相当于用两个指针对差值实现了记忆），这样当快指针走到链表末尾（第 len 个）时，慢指针刚好就在 len - n 这个地方稳稳落地。</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {ListNode} head\\n * @param {number} n\\n * @return {ListNode}\\n */\\nconst removeNthFromEnd = function(head, n) {\\n    // 初始化 dummy 结点\\n    const dummy = new ListNode()\\n    // dummy指向头结点\\n    dummy.next = head\\n    // 初始化快慢指针，均指向dummy\\n    let fast = dummy\\n    let slow = dummy\\n\\n    // 快指针闷头走 n 步\\n    while(n!==0){\\n        fast = fast.next\\n        n--\\n    }\\n    \\n    // 快慢指针一起走\\n    while(fast.next){\\n        fast = fast.next\\n        slow = slow.next\\n    }\\n    \\n    // 慢指针删除自己的后继结点\\n    slow.next = slow.next.next\\n    // 返回头结点\\n    return dummy.next\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审页上有人把同步循环改成 <code>while (fast !== null)</code>，但仍让 <code>fast</code> 从 <code>dummy</code> 先走 <code>n</code> 步；输入 <code>1-&gt;2-&gt;3</code>、<code>n=1</code> 时会出现什么现象？</strong></p>\\n<p>这会让 <code>slow</code> 多走一步，最终停在待删结点而非其前驱，随后删除错误结点，甚至访问空引用。当前写法必须配合 <code>while (fast.next !== null)</code>，指针起点、先行步数和终止条件不能拆开替换。</p>\\n</li>\\n<li>\\n<p><strong>公共工具库接入这段代码后，调用方可能传入 <code>n=0</code>、小数或超过链表长度的值，你会怎样定义接口行为？</strong></p>\\n<p>入口应先用 <code>Number.isInteger(n)</code> 和 <code>n &gt; 0</code> 校验，建立间距时再检测 <code>fast === null</code>，统一抛出语义明确的 <code>RangeError</code>。若业务希望返回原链或结构化失败，也必须写进契约，不能把偶发的 <code>TypeError</code> 暴露给调用方。</p>\\n</li>\\n<li>\\n<p><strong>需求改成允许空链表，而页面仍声称 <code>n</code> 保证有效；当 <code>head=null</code>、<code>n=1</code> 时，这份公共函数应该怎样处理？</strong></p>\\n<p>空链表不存在可删除结点，因此在快指针第一次前进后就会命中 <code>null</code>，应按非法范围处理。若保留当前公共函数契约，可抛出 <code>RangeError</code>；只有产品明确把空操作定义为成功时，才适合直接返回 <code>null</code>，否则会掩盖上游数据异常。</p>\\n</li>\\n<li>\\n<p><strong>线上告警显示删除长度为 <code>L</code> 的链表倒数第 <code>L</code> 个结点时总是错删第二个结点，你会优先检查哪几处？</strong></p>\\n<p>先检查 <code>fast</code> 和 <code>slow</code> 是否都从 <code>dummy</code> 出发，以及 <code>fast</code> 是否恰好先走 <code>n</code> 步。正确状态是 <code>fast</code> 到原链尾、同步循环零次、<code>slow</code> 留在 <code>dummy</code>，再通过 <code>slow.next = slow.next.next</code> 删除原头；任何起点或步数偏移都会破坏这一边界。</p>\\n</li>\\n<li>\\n<p><strong>团队有人主张先遍历求长度再删除，另一个人坚持一次扫描；在可读性和约束没有额外要求时你怎么选？</strong></p>\\n<p>两趟法先求 <code>len</code>，再定位第 <code>len-n</code> 个前驱，推导直观但需要重复遍历。快慢指针把差值 <code>n</code> 保存在两个引用的位置关系中，一趟即可完成；若团队难以稳定维护其边界条件，两趟法也可接受，但两者都应明确非法 <code>n</code> 的处理。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"多指针法-链表的反转\\"><a class=\\"header-anchor\\" href=\\"#多指针法-链表的反转\\">#</a> 多指针法——链表的反转</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>prev</code> 指向已反转前缀头，<code>current</code> 指向未处理后缀头，<code>next</code> 临时保存唯一的后继入口</li>\\n<li>每轮顺序固定：保存 next → 改 current.next → 推进 prev → 推进 current，顺序错会丢链或成环</li>\\n<li>循环结束 current 为 null，prev 是新头；空链返回 null，单结点返回自身</li>\\n<li>时间 <code>O(n)</code>、辅助空间 <code>O(1)</code>；递归版时间相同但调用栈 <code>O(n)</code></li>\\n<li>原地反转改变所有共享结点的观察结果，使用前要明确链表所有权和是否允许修改</li>\\n</ul>\\n<p><strong>反转链表时，我会用 <code>previous</code> 记录已反转部分，用 <code>current</code> 处理剩余结点，并提前保存 <code>next</code>。</strong> 每轮先保留后继，再把 <code>current.next</code> 改指向 <code>previous</code>，否则原链后半段会丢失，顺序不对还可能形成环。循环结束后，<code>previous</code> 就是新头，空链和单结点也能自然返回。迭代写法时间为 <code>O(n)</code>、辅助空间为 <code>O(1)</code>，但原地修改前要确认链表是否被其他地方共享。</p>\\n</section>\\n<p>原文实现的循环不变量是：<code>prev</code> 可遍历到已经反转完成的前缀，<code>current</code> 可遍历到尚未处理的原后缀，并且两部分结点集合不重叠、不丢失。保存 <code>next</code> 后才能安全覆盖 <code>current.next</code>。</p>\\n<pre class=\\"language-js\\"><code>function reverseList(head) {\\n  let previous = null\\n  let current = head\\n  while (current !== null) {\\n    const next = current.next\\n    current.next = previous\\n    previous = current\\n    current = next\\n  }\\n  return previous\\n}\\n</code></pre>\\n<p>完全反转一个链表</p>\\n<blockquote>\\n<p>真题描述：定义一个函数，输入一个链表的头结点，反转该链表并输出反转后链表的头结点。</p>\\n</blockquote>\\n<p>示例:</p>\\n<ul>\\n<li>输入: <code>1-&gt;2-&gt;3-&gt;4-&gt;5-&gt;NULL</code></li>\\n<li>输出: <code>5-&gt;4-&gt;3-&gt;2-&gt;1-&gt;NULL</code></li>\\n</ul>\\n<p><strong>思路解读</strong></p>\\n<blockquote>\\n<p>这道题虽然是一道新题，但你要说你完全没思路，我真的哭了orz。老哥，我真想把这句话刻你显示器上——处理链表的本质，是处理链表结点之间的指针关系。</p>\\n</blockquote>\\n<p>我啥也不说，就给你一张链表的结构图：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122437.png\\" alt=\\"\\"></p>\\n<p>来，你告诉我，我如何把这货颠倒个顺序呢？</p>\\n<p>是不是想办法把每个结点 next 指针的指向给反过来就行了：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122445.png\\" alt=\\"\\"></p>\\n<p>你只要能想到这一步，就说明你对链表操作类题目已经有了最关键的感知，给你双击666~</p>\\n<p>接下来我们需要琢磨的是如何去反转指针的指向，这里我们需要用到三个指针，它们分别指向目标结点（cur）、目标结点的前驱结点（pre）、目标结点的后继结点（next）。这里咱们随便找个结点来开刀：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122455.png\\" alt=\\"\\"></p>\\n<p>这里我只需要一个简单的cur.next = pre，就做到了 next 指针的反转：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122502.png\\" alt=\\"\\"></p>\\n<p>有同学会说：那 next 不是完全没用到吗？当然有用，你瞅瞅，咱们反转完链表变成啥样了：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122509.png\\" alt=\\"\\"></p>\\n<p>这会儿我要是不用 next 给你指着 cur 原本的后继结点，你上哪去定位下一个结点呢？遍历都没法继续了嗷。</p>\\n<p>咱们从第一个结点开始，每个结点都给它进行一次 next 指针的反转。到最后一个结点时，整个链表就已经被我们彻底反转掉了。</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {ListNode} head\\n * @return {ListNode}\\n */\\nconst reverseList = function(head) {\\n    // 初始化前驱结点为 null\\n    let pre = null;\\n    // 初始化目标结点为头结点\\n    let cur = head;\\n    // 只要目标结点不为 null，遍历就得继续\\n    while (cur !== null) {\\n        // 记录一下 next 结点\\n        let next = cur.next;\\n        // 反转指针\\n        cur.next = pre;\\n        // pre 往前走一步\\n        pre = cur;\\n        // cur往前走一步\\n        cur = next;\\n    }\\n    // 反转结束后，pre 就会变成新链表的头结点\\n    return pre\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>调试页面里有人把循环体写成 <code>current.next = previous</code> 后才执行 <code>const next = current.next</code>；输入 <code>1-&gt;2-&gt;3</code> 时为什么最终拿不到完整反转链？</strong></p>\\n<p>覆盖 <code>current.next</code> 后，变量读取到的是反转后的前驱，而不是原后继，尚未处理的 <code>2-&gt;3</code> 入口已经丢失。必须先保存原 <code>next</code>，再改指向并移动两个游标；单链表没有其他路径可帮助恢复被覆盖的后缀。</p>\\n</li>\\n<li>\\n<p><strong>共享状态模块把链表头传给 <code>reverseList</code>，另一个组件仍持有原来的中间结点引用；上线前你会怎样说明副作用？</strong></p>\\n<p>函数复用原结点并原地改写每个 <code>next</code>，所以其他组件看到的拓扑也会同步变化，原中间结点可能只通向新的后缀。接口应明确这是破坏性操作；共享方若必须保留旧结构，就要先复制结点或采用不可变表示，但会增加分配成本。</p>\\n</li>\\n<li>\\n<p><strong>链表长度由外部数据决定，产品又要求改成递归实现以追求代码更短，你会接受这个选型吗？</strong></p>\\n<p>长度不受控时不宜仅为简短改用递归，因为每个结点都会占用一个调用栈帧，长链存在栈溢出风险，辅助空间也是 <code>O(n)</code>。迭代版只维护 <code>previous</code>、<code>current</code> 和 <code>next</code> 三个引用，适合作为默认实现；递归只宜用于长度边界明确的场景。</p>\\n</li>\\n<li>\\n<p><strong>线上出现反转后链表只剩原头结点，日志又没有异常；你会用什么不变量定位是哪一步写错了？</strong></p>\\n<p>逐轮检查 <code>previous</code> 是否能遍历到已反转前缀，<code>current</code> 是否仍指向未处理的原后缀，并确认两部分结点不重叠、不丢失。若首轮后 <code>current</code> 变为 <code>null</code> 或退回 <code>previous</code>，通常是保存 <code>next</code> 的时机或 <code>current = next</code> 写错；继续运行只会扩大结构损坏。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中有人提议先把结点值拷进数组、倒序后再重建链表，和原地三指针方案相比你怎么取舍？</strong></p>\\n<p>若题意要求反转现有链表，三指针法直接改写链接，遍历一次且只使用固定数量的引用，更贴合约束。数组重建会分配新结构，并改变结点对象身份，外部持有的旧引用不会指向新链；只有明确需要保留原链时，复制方案才有合理性。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"局部反转一个链表\\"><a class=\\"header-anchor\\" href=\\"#局部反转一个链表\\">#</a> 局部反转一个链表</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>局部反转除了区间内部指针，还必须保存区间前驱 <code>before</code>、原区间头 <code>start</code> 和区间后继 <code>after</code></li>\\n<li>dummy 统一 m=1 的头部反转；先移动到第 m-1 个位置，再反转 n-m+1 个结点</li>\\n<li>反转完成后有两条接缝：<code>before.next</code> 接新头，<code>start.next</code> 接 after，漏一条就会断链或成环</li>\\n<li>一趟扫描时间 <code>O(L)</code>、辅助空间 <code>O(1)</code>；m=n 时应保持原链不变</li>\\n<li>公共函数要校验 <code>1 ≤ m ≤ n ≤ length</code>，题目保证不能替代生产契约</li>\\n</ul>\\n<p><strong>局部反转要同时处理区间内部和两侧接缝，我会用 <code>dummy</code> 找到区间前驱，再把指定范围内的结点反转。</strong> 完成后，需要让 <code>before.next</code> 接上反转后的新头，并让原区间头 <code>start.next</code> 接回后缀，少接一处都会断链或成环。<code>dummy</code> 还能统一处理从第一个结点开始反转的场景，整体时间为 <code>O(L)</code>、辅助空间为 <code>O(1)</code>。当 <code>m=n</code> 时链表应保持不变，公共函数还要校验区间是否合法。</p>\\n</section>\\n<p>头插法可以在同一局部窗口内完成反转，<code>before</code> 始终不动，反复把 <code>start.next</code> 摘下插到区间前端：</p>\\n<pre class=\\"language-js\\"><code>function reverseBetween(head, left, right) {\\n  const dummy = { next: head }\\n  let before = dummy\\n  for (let i = 1; i &lt; left; i += 1) before = before.next\\n  const start = before.next\\n  for (let i = 0; i &lt; right - left; i += 1) {\\n    const moved = start.next\\n    start.next = moved.next\\n    moved.next = before.next\\n    before.next = moved\\n  }\\n  return dummy.next\\n}\\n</code></pre>\\n<p>每轮后，<code>before.next</code> 是已反转局部的新头，<code>start</code> 是局部尾且仍连接未处理后缀。m=n 时循环零次自然返回原链。若 left/right 越界，访问 null 会报错，生产实现要提前验证或在移动中检测。</p>\\n<blockquote>\\n<p>反转链表真是座金矿，反转完整体反转局部，反转完局部还能每 k 个一组花式反转（最后这个略难，我们会放在真题训练环节来做）。虽然难度依次进阶，但只要把握住核心思想就没问题，下面咱们来看看如何反转局部：</p>\\n</blockquote>\\n<blockquote>\\n<p>真题描述：反转从位置 m 到 n 的链表。请使用一趟扫描完成反转。</p>\\n</blockquote>\\n<p>说明: <code>1 ≤ m ≤ n ≤ 链表长度</code>。</p>\\n<p>示例:</p>\\n<ul>\\n<li>输入: 1-&gt;2-&gt;3-&gt;4-&gt;5-&gt;NULL, m = 2, n = 4</li>\\n<li>输出: 1-&gt;4-&gt;3-&gt;2-&gt;5-&gt;NULL</li>\\n</ul>\\n<p><strong>思路解读</strong></p>\\n<p>我们仍然是从指针反转来入手：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122523.png\\" alt=\\"\\"></p>\\n<p>按照题中的示例，假如我们需要反转的是链表的第 2-4 之间的结点，那么对应的指针逆序后会是这个样子：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122529.png\\" alt=\\"\\"></p>\\n<p>4指3，3指2，这都没问题，关键在于，如何让1指向4、让2指向5呢？这就要求我们在单纯的重复“逆序”这个动作之外，还需要对被逆序的区间前后的两个结点做额外的处理：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122536.png\\" alt=\\"\\"></p>\\n<p>由于我们遍历链表的顺序是从前往后遍历，那么为了避免结点1和结点2随着遍历向后推进被遗失，我们需要提前把1结点缓存下来。而结点5就没有这么麻烦了：随着遍历的进行，当我们完成了结点4的指针反转后，此时 cur 指针就恰好指在结点5上：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905122543.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>此时我们直接将结点2的 next 指针指向 cur、将结点1的 next 指针指向 pre 即可。</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {ListNode} head\\n * @param {number} m\\n * @param {number} n\\n * @return {ListNode}\\n */\\n// 入参是头结点、m、n\\nconst reverseBetween = function(head, m, n) {\\n    // 定义pre、cur，用leftHead来承接整个区间的前驱结点\\n    let pre,cur,leftHead\\n    // 别忘了用 dummy 嗷\\n    const dummy = new ListNode()  \\n    // dummy后继结点是头结点\\n    dummy.next = head\\n    // p是一个游标，用于遍历，最初指向 dummy\\n    let p = dummy  \\n    // p往前走 m-1 步，走到整个区间的前驱结点处\\n    for(let i=0;i&lt;m-1;i++){\\n        p = p.next\\n    }\\n    // 缓存这个前驱结点到 leftHead 里\\n    leftHead = p\\n    // start 是反转区间的第一个结点\\n    let start = leftHead.next  \\n    // pre 指向start\\n    pre = start\\n    // cur 指向 start 的下一个结点\\n    cur = pre.next\\n    // 开始重复反转动作\\n    for(let i=m;i&lt;n;i++){\\n        let next = cur.next\\n        cur.next = pre\\n        pre = cur\\n        cur = next\\n    }\\n    //  leftHead 的后继结点此时为反转后的区间的第一个结点\\n    leftHead.next = pre\\n    // 将区间内反转后的最后一个结点 next 指向 cur\\n    start.next=cur\\n    // dummy.next 永远指向链表头结点\\n    return dummy.next\\n};\\n</code></pre>\\n<p>小贴士：楼上的两道反转题目，都可以用递归来实现，你试试？</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>演示页把 <code>1-&gt;2-&gt;3-&gt;4-&gt;5</code> 的 <code>[2,4]</code> 反转后，只执行 <code>before.next = newHead</code>，为什么结果不是完整的 <code>1-&gt;4-&gt;3-&gt;2-&gt;5</code>？</strong></p>\\n<p>原区间头 <code>2</code> 在反转后成为区间尾，还必须重新连接结点 <code>5</code>；只接左边会使后缀失联，残留旧指向时还可能形成环。头插法通过每轮更新 <code>start.next</code> 持续连接未处理后缀，传统反转法则要显式执行 <code>start.next = after</code>。</p>\\n</li>\\n<li>\\n<p><strong>表单允许用户提交 <code>left</code>、<code>right</code>，后端没有保证 <code>1 ≤ left ≤ right ≤ 链表长度</code>；公共实现应在哪里拦截越界？</strong></p>\\n<p>应先验证位置为满足顺序约束的正整数，并在寻找 <code>before</code>、移动 <code>start.next</code> 时检测空引用。失败时按接口契约抛出范围错误或返回结构化失败，不能让访问 <code>null.next</code> 成为业务反馈；若追求严格原子性，还要避免校验途中已修改链表。</p>\\n</li>\\n<li>\\n<p><strong>需求改成反转 <code>[3,3]</code>，评审者要求增加专门的提前返回分支；保留统一循环是否仍然正确？</strong></p>\\n<p>统一实现仍然正确，因为循环次数是 <code>right-left</code>，此时为零，链表不会发生任何指针改写并直接返回 <code>dummy.next</code>。提前返回只能算微小优化，不应成为正确性的依赖；这个用例反而适合检查循环次数是否多执行了一轮。</p>\\n</li>\\n<li>\\n<p><strong>线上偶发局部反转后遍历无法结束，故障样本集中在 <code>[2,4]</code>；你会怎样检查头插法的三条赋值？</strong></p>\\n<p>逐轮确认先保存 <code>moved = start.next</code>，再用 <code>start.next = moved.next</code> 摘除结点，最后把 <code>moved</code> 插到 <code>before.next</code> 前。若先覆盖 <code>moved.next</code> 或漏掉摘除动作，旧链接可能闭合成环；还应验证 <code>before</code> 始终不动、<code>start</code> 始终作为局部尾连接剩余后缀。</p>\\n</li>\\n<li>\\n<p><strong>团队在“头插法原地完成”和“先完整反转区间再接回两端”之间争论，你会依据什么选？</strong></p>\\n<p>头插法让 <code>before</code> 固定，每轮把 <code>start.next</code> 移到区间前端，左右连接在循环中持续保持，最终无需单独寻找区间后继。传统三指针法也正确，但结束后必须同时接回 <code>before.next</code> 与 <code>start.next</code>；团队若更熟悉其不变量可以采用，代价是接线遗漏风险更集中。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);