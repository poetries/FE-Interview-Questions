(window.webpackJsonp=window.webpackJsonp||[]).push([[14],{1121:function(o){o.exports=JSON.parse('{"html":"<p>我们之前学过数组的遍历、链表的遍历，这些线性结构的遍历考起来没有什么难度，可以理解为基本技能，一般也不会单独出题。</p>\\n<p>但是二叉树可不一样了，这一“开叉”，它的遍历难度陡然上了一个台阶。在面试中，二叉树的各种姿势的遍历，是非常容易作为独立命题点来考察的，而且这个考察的频率极高极高。\\n因此对于有志于在算法面试上求稳的同学，本节涉及的编码内容，你千万不要沉溺在“我看懂了”、“我理解了”、“我知道你说的是啥意思了”这种虚无的成就感中——假的，都是假的，只有自己写出来的代码才是真的！</p>\\n<p>理解只是记忆的前提，只吹理解不记忆，不如回家去种地：）。</p>\\n<p>这里我对大家的要求就是“在理解的基础上记忆”。如果你真的暂时理解不了，背也要先给你自己背下来，然后带着对正确思路的记忆，重新去看解析部分里的图文（尤其是图）、反复去理解，这么整下来你不可能学不会。\\n面试时见到二叉树的遍历，你不能再去想太多——没有那么多时间给你现场推理，这么熟悉的题目你没必要现场推理，你要做的是默写！默写啊！老哥们！！（捶胸顿足）</p>\\n<h2 id=\\"二叉树的遍历-命题思路解读\\"><a class=\\"header-anchor\\" href=\\"#二叉树的遍历-命题思路解读\\">#</a> 二叉树的遍历——命题思路解读</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>遍历要按确定顺序访问每个结点一次；二叉树常见先序、中序、后序和层序</li>\\n<li>先、中、后描述根结点相对左右子树的访问时机，三者默认都保持左子树先于右子树</li>\\n<li>深度优先可用递归或显式栈，层序使用队列；选顺序取决于结果对父子结点的依赖</li>\\n<li>复制常用先序，搜索树有序输出用中序，删除或汇总子树用后序，最浅层问题常用层序</li>\\n<li>四者时间都是 <code>O(n)</code>；深搜辅助空间 <code>O(h)</code>，层序队列最坏保存树的最大宽度 <code>O(w)</code></li>\\n</ul>\\n<p><strong>二叉树遍历就是按确定顺序访问每个结点一次，常见方式是先序、中序、后序和层序。</strong> 前三种本质上区别在于根结点相对左右子树何时处理，可用递归或显式栈；层序则用队列逐层访问。选择顺序要看数据依赖，例如搜索树有序输出适合中序，父结点依赖子树结果时适合后序。它们时间都是 <code>O(n)</code>，深度优先空间为 <code>O(h)</code>，层序最坏为树的最大宽度 <code>O(w)</code>。</p>\\n</section>\\n<p>遍历方式应从数据依赖反推。若父结点结果依赖左右子树，就要先得到孩子再处理父亲，对应后序；若利用二叉搜索树“左小、根中、右大”的约束输出有序值，则用中序。层序让距离根相同的结点连续出现，适合最短层数、逐层聚合和宽度问题。</p>\\n<p>四种遍历都至少访问每个结点一次，时间为 <code>O(n)</code>。深度优先保存当前根到叶的路径，辅助空间 <code>O(h)</code>；广度优先保存当前层的候选结点，最坏 <code>O(w)</code>。完全二叉树最后一层宽度接近 <code>n/2</code>，所以层序空间不能简单说成 <code>O(log n)</code>。</p>\\n<blockquote>\\n<p>以一定的顺序规则，逐个访问二叉树的所有结点，这个过程就是二叉树的遍历。按照顺序规则的不同，遍历方式有以下四种：</p>\\n</blockquote>\\n<ul>\\n<li>先序遍历</li>\\n<li>中序遍历</li>\\n<li>后序遍历</li>\\n<li>层次遍历</li>\\n</ul>\\n<p>按照实现方式的不同，遍历方式又可以分为以下两种：</p>\\n<ul>\\n<li>递归遍历（先、中、后序遍历）</li>\\n<li>迭代遍历（层次遍历）</li>\\n</ul>\\n<blockquote>\\n<p>层次遍历的考察相对比较孤立，我们会把它放在后续的真题归纳解读环节来讲。这里我们重点要看的是先、中、后序遍历三兄弟——由于同时纠结了二叉树和“递归”两个大热命题点，又不属于“偏难怪”之流，遍历三兄弟一直是前端算法面试官们的心头好，考察热度经久不衰。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>权限树页面要求父节点状态由左右子树的计算结果决定，候选人却先处理父节点再递归孩子，你为什么会要求改成后序遍历？</strong></p>\\n<p>父节点依赖两个子树的结果时，应先计算左、右子树，再处理根节点，这正是后序顺序。先序处理根时依赖数据尚未产生，只能额外回填或重复计算；若父节点信息反过来决定孩子处理方式，先序才可能更自然。</p>\\n</li>\\n<li>\\n<p><strong>搜索结果存成二叉搜索树，接口要按从小到大输出全部键值，为什么中序遍历比先序更贴合约束？</strong></p>\\n<p>二叉搜索树满足左侧值较小、根居中、右侧值较大的结构约束，按“左—根—右”的中序遍历可直接得到有序序列。先序或后序仍会访问全部结点，但输出天然不保证有序；若树不满足搜索树约束，中序也不能凭空产生排序结果。</p>\\n</li>\\n<li>\\n<p><strong>组织架构页要找距离 <code>CEO</code> 最近的异常节点，并在首次命中后停止，团队在 <code>DFS</code> 与层序遍历之间如何选？</strong></p>\\n<p>层序遍历会让距离根相同的结点连续处理，因此首次命中的异常节点具有最小层数，更符合需求。<code>DFS</code> 可能先深入一条很长的分支后才发现更浅答案；代价是队列需要保存当前候选层，宽树上的峰值空间可能明显更大。</p>\\n</li>\\n<li>\\n<p><strong>一棵接近完全二叉树有一百万个结点，线上层序任务出现内存峰值，而开发者认为树高只有 <code>O(log n)</code>，你会怎样定位误判？</strong></p>\\n<p>树高为 <code>O(log n)</code> 只约束深度优先遍历保存的路径长度，不约束层序队列的宽度。完全二叉树最后一层宽度可接近 <code>n/2</code>，层序辅助空间最坏是 <code>O(w)</code>；应检查队列峰值，并评估能否改用 <code>O(h)</code> 的深度优先方案。</p>\\n</li>\\n<li>\\n<p><strong>统计树的最大宽度与计算树高分别要上线，负责人要求统一一种遍历模板，你会接受吗？</strong></p>\\n<p>不会仅为模板统一牺牲数据依赖：最大宽度需要识别每一层的结点数量，层序遍历更直接；树高可用深度优先沿路径计算，辅助空间为 <code>O(h)</code>。两者时间都至少访问每个结点一次，即 <code>O(n)</code>，差异主要在中间状态和空间峰值。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"递归遍历初相见\\"><a class=\\"header-anchor\\" href=\\"#递归遍历初相见\\">#</a> 递归遍历初相见</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>递归函数必须有可达终止条件；树题常以空结点为边界</li>\\n<li>每次调用都有独立栈帧，保存参数、局部变量和返回位置，递归栈必须计入空间</li>\\n<li>先定义函数对一棵子树返回什么，再假设左右子树正确返回，当前层只负责组合</li>\\n<li>递归深度等于树高 <code>h</code>：平衡树约 <code>O(log n)</code>，退化链为 <code>O(n)</code>，JavaScript 可能栈溢出</li>\\n<li>有环或共享结点的数据不是普通树；不可信输入要检测对象身份</li>\\n</ul>\\n<p><strong>二叉树递归遍历的关键，是先约定函数如何处理一棵子树，并用空结点作为可达的终止条件。</strong> 每次递归都有独立栈帧，当前层只需要相信左右子树能正确返回，再组合结果。每个结点处理一次，时间是 <code>O(n)</code>，调用栈空间是 <code>O(h)</code>，不能算成 <code>O(1)</code>。平衡树深度约为 <code>O(log n)</code>，退化成链时会到 <code>O(n)</code>；面对超深树或可能含环的输入，我会改用显式栈并检测对象身份。</p>\\n</section>\\n<p>以求高度为例，函数契约是“返回以 node 为根的子树高度，空树高度为 <code>-1</code>”。当前层无需知道子树内部细节，只取左右返回值的较大者加一：</p>\\n<pre class=\\"language-js\\"><code>function height(root) {\\n  if (root === null) return -1\\n  return 1 + Math.max(height(root.left), height(root.right))\\n}\\n</code></pre>\\n<p>每个结点计算一次，时间 <code>O(n)</code>；调用栈空间 <code>O(h)</code>。不能因为局部变量少就说空间 <code>O(1)</code>，未返回的栈帧会随深度增长。用户可构造的超深树应改用显式栈并限制最大结点数和深度。</p>\\n<blockquote>\\n<p>编程语言中，函数Func(Type a,……)直接或间接调用函数本身，则该函数称为递归函数。</p>\\n</blockquote>\\n<p>简单来说，当我们看到一个函数反复调用它自己的时候，递归就发生了。“递归”就意味着“反复”，像咱们之前对二叉树的定义，就可以理解为是一个递归式的定义：</p>\\n<ul>\\n<li>它可以没有根结点，作为一棵空树存在</li>\\n<li>如果它不是空树，那么必须由根结点、左子树和右子树组成，且左右子树都是二叉树。</li>\\n</ul>\\n<blockquote>\\n<p>这个定义有着这样的内涵：如果我们想要创建一个二叉树结点作为根结点，那么它左侧的子结点和右侧的子结点也都必须符合二叉树结点的定义，这意味着我们要反复地执行“创建一个由数据域、左右子树组成的结点”这个动作，直到数据被分配完为止。</p>\\n</blockquote>\\n<p>结合这个定义来看，每一棵二叉树都应该由这三部分组成：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905110927.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>对树的遍历，就可以看做是对这三个部分的遍历。这里就引出一个问题：三个部分中，到底先遍历哪个、后遍历哪个呢？我们此处其实可以穷举一下，假如在保证“左子树一定先于右子树遍历”这个前提，那么遍历的可能顺序也不过三种：</p>\\n</blockquote>\\n<ul>\\n<li>根结点 -&gt; 左子树 -&gt; 右子树</li>\\n<li>左子树 -&gt; 根结点 -&gt; 右子树</li>\\n<li>左子树 -&gt; 右子树 -&gt; 根结点</li>\\n</ul>\\n<p>上述三个遍历顺序，就分别对应了二叉树的先序遍历、中序遍历和后序遍历规则。</p>\\n<p>在这三种顺序中，根结点的遍历分别被安排在了首要位置、中间位置和最后位置。</p>\\n<p>所谓的“先序”、“中序”和“后序”，“先”、“中”、“后”其实就是指根结点的遍历时机。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在白板上写出 <code>height(root)</code>，空节点返回 <code>0</code>、叶子返回 <code>1</code>，而接口文档规定空树高度为 <code>-1</code>，你会让他怎样修正？</strong></p>\\n<p>必须先统一高度按边数还是结点数计数；既然契约规定空树为 <code>-1</code>，叶子就应返回 <code>0</code>。实现可写成空节点直接返回 <code>-1</code>，非空节点返回左右高度最大值加一；混用两套约定会造成稳定的偏一错误。</p>\\n</li>\\n<li>\\n<p><strong>树形评论页只有几十行递归代码，却在十万层链式脏数据上栈溢出，为什么局部变量少也救不了它？</strong></p>\\n<p>每次递归调用都保留未返回的栈帧，链式结构使深度 <code>h</code> 接近结点数 <code>n</code>，辅助空间因此是 <code>O(n)</code>。应改用显式栈迭代，并对不可信输入限制结点数和最大深度；显式栈仍占空间，但不会依赖有限的语言调用栈。</p>\\n</li>\\n<li>\\n<p><strong>计算高度时产品把空树高度从 <code>-1</code> 改成 <code>0</code>，现有 <code>1 + Math.max(...)</code> 递归还能原样保留吗？</strong></p>\\n<p>递推结构可以保留，但所有高度会改为按结点数计数，叶子高度由 <code>0</code> 变成 <code>1</code>。调用方若把高度用于边数、缩进或阈值判断，也必须同步调整；只修改基础返回值而不核对契约消费者，会引入偏一故障。</p>\\n</li>\\n<li>\\n<p><strong>线上高度接口返回异常缓慢，你看到递归函数对每个节点只取一次左右子树结果，应该怎样判断是算法问题还是输入形状问题？</strong></p>\\n<p>正常树上每个结点只计算一次，时间复杂度是 <code>O(n)</code>，不存在因树退化而重复访问结点的问题。退化形状主要把调用栈空间推到 <code>O(n)</code>，可能导致栈溢出；应核对实际结点数、最大深度和是否存在异常引用，再决定改迭代还是限制输入。</p>\\n</li>\\n<li>\\n<p><strong>团队争论递归版与显式栈版的树高计算，数据既可能平衡也可能由用户构造，你会如何定方案？</strong></p>\\n<p>可信且深度受控的数据可优先使用递归版，它直接表达“子树高度取最大值再加一”的契约。用户可构造超深树时，显式栈更稳妥，并应同时限制最大深度和结点数；两者时间通常都是 <code>O(n)</code>，选择差异集中在栈安全与实现复杂度。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"遍历方法图解与编码实现\\"><a class=\\"header-anchor\\" href=\\"#遍历方法图解与编码实现\\">#</a> 遍历方法图解与编码实现</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先序根左右、中序左根右、后序左右根；只移动“处理 root”的位置，不改变左右关系</li>\\n<li>返回数组比函数内 <code>console.log</code> 更易测试；大结果可用生成器按需产出</li>\\n<li>中序只对二叉搜索树产生有序结果，普通二叉树没有该保证</li>\\n<li>递归和显式栈版本的辅助空间都是 <code>O(h)</code>，迭代不会凭空消掉空间</li>\\n<li>测试覆盖空树、单结点、单侧链和普通分叉树，并确认输入未被修改</li>\\n</ul>\\n<p><strong>三种遍历只是在调整访问根结点的时机：先序是根左右，中序是左根右，后序是左右根。</strong> 左子树始终先于右子树，因此实现时只要移动处理 <code>root</code> 的那一行，不必改递归结构。我一般返回数组而不是直接 <code>console.log</code>，这样更方便测试；如果只找首个结果，可以短路返回或使用生成器。三种方式的时间都是 <code>O(n)</code>，递归栈为 <code>O(h)</code>，还要单独计算结果数组的 <code>O(n)</code> 空间。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>function traverse(root, order = &#39;pre&#39;) {\\n  if (![&#39;pre&#39;, &#39;in&#39;, &#39;post&#39;].includes(order)) throw new TypeError(&#39;unknown order&#39;)\\n  const result = []\\n  const visit = (node) =&gt; {\\n    if (node === null) return\\n    if (order === &#39;pre&#39;) result.push(node.val)\\n    visit(node.left)\\n    if (order === &#39;in&#39;) result.push(node.val)\\n    visit(node.right)\\n    if (order === &#39;post&#39;) result.push(node.val)\\n  }\\n  visit(root)\\n  return result\\n}\\n</code></pre>\\n<p>每个结点只在选定时机写入一次，时间 <code>O(n)</code>，递归栈 <code>O(h)</code>，结果数组 <code>O(n)</code>。若调用方只找第一个匹配项，应返回命中结果逐层短路或用生成器，避免继续遍历和收集完整数组。</p>\\n<h3 id=\\"先序遍历\\"><a class=\\"header-anchor\\" href=\\"#先序遍历\\">#</a> 先序遍历</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先序顺序是“根 → 左 → 右”，访问当前结点发生在递归两个子树之前</li>\\n<li>递归终止条件是空结点直接返回；每个结点恰好进入一次，因此时间 <code>O(n)</code>，额外栈空间为 <code>O(h)</code></li>\\n<li>先序天然适合复制树、序列化结构和从父结点向子结点传递路径状态</li>\\n</ul>\\n<p><strong>先序遍历按照“根结点、左子树、右子树”的顺序执行，也就是先访问当前结点，再递归处理两棵子树。</strong> 递归边界是 <code>root</code> 为空时直接返回，否则输出 <code>root.val</code>，随后调用左、右子树。每个结点只会进入一次，所以时间复杂度是 <code>O(n)</code>，调用栈空间取决于树高，为 <code>O(h)</code>。这种先处理父结点的顺序，适合复制或序列化树，也方便把路径状态从父结点传给子结点。</p>\\n</section>\\n<p>先序遍历的“旅行路线”如下图红色数字 所示：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905110939.png\\" alt=\\"\\"></p>\\n<p>如果说有 N 多个子树，那么我们在每一棵子树内部，都要重复这个“旅行路线”，动画演示如下：</p>\\n<p><img src=\\"https://p1-jj.byteimg.com/tos-cn-i-t2oaga2asx/gold-user-assets/2020/4/6/1714ec42acc57e04~tplv-t2oaga2asx-watermark.awebp\\" alt=\\"\\"></p>\\n<p>这个“重复”，我们就用递归来实现。</p>\\n<p>注：上面这个二叉树的结构，大家可以试着用我们前面学过的知识编码实现一把。这里为了方便大家理解，我直接给出来了哈（记得自己回去也要试着手写一遍）：</p>\\n<pre class=\\"language-js\\"><code>const root = {\\n  val: &quot;A&quot;,\\n  left: {\\n    val: &quot;B&quot;,\\n    left: {\\n      val: &quot;D&quot;\\n    },\\n    right: {\\n      val: &quot;E&quot;\\n    }\\n  },\\n  right: {\\n    val: &quot;C&quot;,\\n    right: {\\n      val: &quot;F&quot;\\n    }\\n  }\\n};\\n</code></pre>\\n<p><strong>递归函数的编写要点</strong></p>\\n<blockquote>\\n<p>编写一个递归函数之前，大家首先要明确两样东西：</p>\\n</blockquote>\\n<ul>\\n<li>递归式</li>\\n<li>递归边界</li>\\n</ul>\\n<blockquote>\\n<p>递归式，它指的是你每一次重复的内容是什么。在这里，我们要做先序遍历，那么每一次重复的其实就是 <code>根结点 -&gt; 左子树 -&gt; 右子树</code> 这个旅行路线。</p>\\n</blockquote>\\n<p>递归边界，它指的是你什么时候停下来。</p>\\n<blockquote>\\n<p>在遍历的场景下，当我们发现遍历的目标树为空的时候，就意味着旅途已达终点、需要画上句号了。这个“画句号”的方式，在编码实现里对应着一个 return 语句——这就是二叉树遍历的递归边界。</p>\\n</blockquote>\\n<p><strong>第一个递归遍历函数</strong></p>\\n<p>上面咱们已经捋清楚思路，接下来话不多说，先序遍历的编码实现：</p>\\n<pre class=\\"language-js\\"><code>// 所有遍历函数的入参都是树的根结点对象\\nfunction preorder(root) {\\n    // 递归边界，root 为空\\n    if(!root) {\\n        return \\n    }\\n     \\n    // 输出当前遍历的结点值\\n    console.log(&#39;当前遍历的结点值是：&#39;, root.val)  \\n    // 递归遍历左子树 \\n    preorder(root.left)  \\n    // 递归遍历右子树  \\n    preorder(root.right)\\n}\\n</code></pre>\\n<p>不熟悉这种写法？不用怕，我们接下来一行一行把这段代码跑完，你就知道它在干啥了：</p>\\n<p><strong>图解先序遍历的完整过程</strong></p>\\n<p>各位现在完全可以再回过头来看一下我们前面示例的这棵二叉树：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111045.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>我们直接把它套进 preorder 函数里，一步一步来认清楚先序遍历的每一步做了什么：</p>\\n</blockquote>\\n<ol>\\n<li>调用 <code>preorder(root)</code>，这里 root 就是 A，它非空，所以进入递归式，输出 A 值。接着优先遍历左子树，<code>preorder(root.left)</code>此时为 <code>preorder(B)</code> ：</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/images/20210905111053.png\\" alt=\\"\\"></p>\\n<ol start=\\"2\\">\\n<li>进入 <code>preorder(B)</code> 的逻辑： 入参为结点 B，非空，进入递归式，输出 B 值。接着优先遍历 B 的左子树，<code>preorder(root.left)</code> 此时为 <code>preorder(D)</code> ：</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/images/20210905111106.png\\" alt=\\"\\"></p>\\n<ol start=\\"3\\">\\n<li>进入 <code>preorder(D)</code> 的逻辑： 入参为结点 D，非空，进入递归式，输出 D 值。接着优先遍历 D 的左子树，<code>preorder(root.left)</code> 此时为 <code>preorder(null)</code>：</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/images/20210905111116.png\\" alt=\\"\\"></p>\\n<ol start=\\"4\\">\\n<li>进入<code>preorder(null)</code> ，发现抵达了递归边界，直接 return 掉。紧接着是 <code>preorder(D)</code> 的逻辑往下走，走到了 <code>preorder(root.right)</code> ：</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/images/20210905111125.png\\" alt=\\"\\"></p>\\n<ol start=\\"5\\">\\n<li>再次进入<code>preorder(null)</code> ，发现抵达了递归边界，直接 <code>return</code> 掉，回到<code>preorder(D)</code> 里。接着 <code>preorder(D)</code> 的逻辑往下走，发现 preorder(D) 已经执行完了。于是返回，回到<code>preorder(B)</code> 里，接着<code>preorder(B)</code> 往下走，进入 <code>preorder(root.right)</code> ，也就是 <code>preorder(E)</code></li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/images/20210905111137.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>E 不为空，进入递归式，输出 E 值。接着优先遍历 E 的左子树，preorder(root.left) 此时为 preorder(null)，触碰递归边界，直接返回 preorder(E)；继续preorder(E)执行下去，是preorder(root.right) ，这里 E 的 right 同样是 null，故直接返回。如此一来，preorder(E)就执行完了，回到preorder(B)里去；发现preorder(B)也执行完了，于是回到preorder(A)里去，执行preorder(A)中的 preorder(root.right)。</p>\\n</blockquote>\\n<ol start=\\"6\\">\\n<li>root 是A，<code>root.right</code> 就是 C 了，进入<code>preorder(C)</code>的逻辑：</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/images/20210905111145.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>C 不为空，进入递归式，输出 C 值。接着优先遍历 C 的左子树，preorder(root.left) 此时为 preorder(null)，触碰递归边界，直接返回。继续preorder(C)执行下去，是preorder(root.right) ，这里 C 的 right 是 F：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905111154.png\\" alt=\\"\\"></p>\\n<ol start=\\"7\\">\\n<li>进入<code>preorder(F)</code>的逻辑，F 不为空，进入递归式，输出 F 值。接着优先遍历 F 的左子树，preorder(root.left) 此时为 preorder(null)，触碰递归边界，直接返回 <code>preorder(F)</code>；继续preorder(F)执行下去，是preorder(root.right) ，这里 F 的 right 同样是 null，故直接返回preorder(F)。此时preorder(F)已经执行完了，返回preorder(C)；发现preorder(C)也执行完了，就回到 preorder(A)；发现preorder(A)作为递归入口，它的逻辑也已经执行完了，于是我们的递归活动就正式画上了句号。到此为止，6个结点也已全部按照先序遍历顺序输出：</li>\\n</ol>\\n<pre class=\\"language-text\\"><code>当前遍历的结点值是： A\\n当前遍历的结点值是： B\\n当前遍历的结点值是： D\\n当前遍历的结点值是： E\\n当前遍历的结点值是： C\\n当前遍历的结点值是： F\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品分类页要按先序展示树，接口返回示例中的 <code>A</code> 到 <code>F</code>；同事却得到 <code>D、E、B、F、C、A</code>，你会先检查哪段代码？</strong></p>\\n<p>这组结果实际符合后序，而不是先序，说明处理 <code>root.val</code> 的语句被放到了两个递归调用之后。应把当前结点的输出或业务动作移到 <code>preorder(root.left)</code> 之前，并保留空结点直接 <code>return</code>；只调整左右子树顺序仍无法修正根结点的位置。</p>\\n</li>\\n<li>\\n<p><strong>导航菜单页面收到这棵 <code>A-B-C</code> 示例树，要求渲染顺序为 <code>A、B、D、E、C、F</code>，递归函数里应怎样组织代码？</strong></p>\\n<p>进入非空结点后应先处理 <code>root.val</code>，再依次调用 <code>preorder(root.left)</code> 和 <code>preorder(root.right)</code>。空结点必须作为递归边界立即 <code>return</code>，否则读取 <code>root.val</code>、<code>root.left</code> 时会报错；这种实现表达的是根、左、右的固定访问顺序。</p>\\n</li>\\n<li>\\n<p><strong>代码评审时有人把先序递归改成显式栈，但页面顺序变成了根、右、左；在不改变数据结构的前提下应怎样修正？</strong></p>\\n<p>弹出根结点后，应先把右孩子压栈，再把左孩子压栈，使后进的左孩子优先被处理。若先压左再压右，栈的后进先出会让右子树先访问；修正入栈顺序后仍要跳过空孩子，避免无意义处理。</p>\\n</li>\\n<li>\\n<p><strong>线上某棵分类树出现缺失字段，先序遍历偶发报出读取 <code>val</code> 失败；你会怎样定位递归边界是否写错？</strong></p>\\n<p>先确认每次进入函数时是否在访问 <code>root.val</code> 前执行 <code>if (!root) return</code>，再沿报错路径检查哪个 <code>left</code> 或 <code>right</code> 为空。若边界写在输出之后，叶子结点递归到空孩子就会失败；若数据中存在非树形引用，仅靠空值边界也不能保证终止。</p>\\n</li>\\n<li>\\n<p><strong>埋点团队要求“进入页面节点时立即记录”，而统计团队要求“等子页面结果汇总后再记录”；两种需求都适合继续用先序吗？</strong></p>\\n<p>立即记录适合先序，因为当前结点动作发生在左右子树之前；等待两个子树完成后记录则应采用后序结构。两者只是业务动作在递归式中的位置不同，但不能随意移动，否则记录时刻与依赖关系会被改变。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"中序遍历\\"><a class=\\"header-anchor\\" href=\\"#中序遍历\\">#</a> 中序遍历</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>中序顺序是“左 → 根 → 右”，当前结点要等左子树完全处理后才访问</li>\\n<li>对二叉搜索树做中序遍历会得到非递减序列，这是验证 BST、找第 k 小值的核心性质</li>\\n<li>迭代写法不断把左链压栈，弹栈访问后再转向右子树</li>\\n</ul>\\n<p><strong>中序遍历的顺序是“左子树、根结点、右子树”，当前结点必须等左子树全部处理完后再访问。</strong> 它和先序遍历的递归边界相同，区别只是把处理 <code>root.val</code> 的位置移到左右递归之间。对二叉搜索树使用中序遍历会得到非递减序列，因此常用于验证结构或寻找第 <code>k</code> 小的值。如果改成迭代写法，就持续把左链压栈，弹栈访问结点后再转向右子树。</p>\\n</section>\\n<p>理解了先序遍历的过程，中序遍历就不是什么难题。唯一的区别只是把遍历顺序调换了<code>左子树 -&gt; 根结点 -&gt; 右子树</code>：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111204.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>若有多个子树，那么我们在每一棵子树内部，都要重复这个“旅行路线”，这个过程用动画表示如下：</p>\\n</blockquote>\\n<p><img src=\\"https://p1-jj.byteimg.com/tos-cn-i-t2oaga2asx/gold-user-assets/2020/4/6/1714f098b2bd1f9a~tplv-t2oaga2asx-watermark.awebp\\" alt=\\"\\"></p>\\n<p>递归边界照旧，唯一发生改变的是递归式里调用递归函数的顺序——左子树的访问会优先于根结点。我们参考先序遍历的分析思路，来写中序遍历的代码：</p>\\n<pre class=\\"language-js\\"><code>// 所有遍历函数的入参都是树的根结点对象\\nfunction inorder(root) {\\n    // 递归边界，root 为空\\n    if(!root) {\\n        return \\n    }\\n     \\n    // 递归遍历左子树 \\n    inorder(root.left)  \\n    // 输出当前遍历的结点值\\n    console.log(&#39;当前遍历的结点值是：&#39;, root.val)  \\n    // 递归遍历右子树  \\n    inorder(root.right)\\n}\\n</code></pre>\\n<blockquote>\\n<p>按照中序遍历的逻辑，同样的一棵二叉树，结点内容的输出顺序如下：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>当前遍历的结点值是： D\\n当前遍历的结点值是： B\\n当前遍历的结点值是： E\\n当前遍历的结点值是： A\\n当前遍历的结点值是： C\\n当前遍历的结点值是： F\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>权限树页面做中序遍历后得到一串数值，同事据此断言它必然升序；面对一棵普通二叉树，你会怎么反驳？</strong></p>\\n<p>中序只规定左子树、根结点、右子树的访问次序，并不规定结点值之间的大小关系。普通二叉树即使严格按中序输出，也可能得到任意数值顺序；只有额外存在键值约束时，才能进一步讨论是否有序。</p>\\n</li>\\n<li>\\n<p><strong>调试面板要让示例树输出 <code>D、B、E、A、C、F</code>，你会把日志放在递归函数的哪个位置？</strong></p>\\n<p>日志应放在 <code>inorder(root.left)</code> 之后、<code>inorder(root.right)</code> 之前，使左子树处理完才输出当前结点。函数入口仍需对空 <code>root</code> 直接 <code>return</code>；若把日志移到第一个递归调用之前，结果就会变为先序。</p>\\n</li>\\n<li>\\n<p><strong>设计稿把“处理当前节点”的动作从左子树之后挪到右子树之后，但产品仍要求示例输出 <code>D、B、E、A、C、F</code>，你会接受这次改动吗？</strong></p>\\n<p>不能接受，因为动作移到两个子树之后会把访问方式改成后序，示例将输出 <code>D、E、B、F、C、A</code>。中序的关键不在函数名称，而在三条语句的相对位置；若动作确实依赖右子树结果，产品要求也必须同步调整。</p>\\n</li>\\n<li>\\n<p><strong>线上调试时，中序遍历只打印了 <code>D、B、E</code>，右半棵 <code>C、F</code> 完全缺失；在根结点存在的情况下应优先检查什么？</strong></p>\\n<p>应先检查输出根结点后是否遗漏了 <code>inorder(root.right)</code>，以及调用参数是否误写成了 <code>root.left</code>。示例左半树恰好对应 <code>D、B、E</code>，说明递归边界大概率正常而右子树入口未执行；仍需结合日志确认 <code>A.right</code> 确实指向 <code>C</code>。</p>\\n</li>\\n<li>\\n<p><strong>搜索组件只想在中序访问到目标值时停止，同事却先收集完整数组再查找；你会怎样权衡？</strong></p>\\n<p>完整收集会遍历并保存所有已输出结点，而命中即停可让递归返回状态并逐层停止后续右子树访问。短路实现需要每一层都检查并传播命中结果，否则祖先仍会继续递归；若调用方确实需要完整中序序列，数组方案更直接。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"后序遍历\\"><a class=\\"header-anchor\\" href=\\"#后序遍历\\">#</a> 后序遍历</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>后序顺序是“左 → 右 → 根”，父结点在两个子问题都完成后才得到处理</li>\\n<li>求树高、判断平衡、删除整棵树等“结果由孩子汇总到父亲”的问题最适合后序</li>\\n<li>迭代实现可用双栈，或把“根 → 右 → 左”的结果反转为“左 → 右 → 根”</li>\\n</ul>\\n<p><strong>后序遍历的访问顺序是“左子树 → 右子树 → 根结点”，也就是两个子问题处理完后再处理父结点。</strong> 递归实现时，遇到空结点直接返回，随后依次调用左、右子树的遍历函数，最后访问当前结点。它适合求树高、判断平衡这类需要把子树结果向父结点汇总的问题；改成迭代时，可以使用双栈，或者将“根 → 右 → 左”的结果反转。</p>\\n</section>\\n<p>在后序遍历中，我们<code>先访问左子树</code>，<code>再访问右子树</code>，<code>最后访问根结点</code>：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111309.png\\" alt=\\"\\"></p>\\n<p>若有多个子树，那么我们在每一棵子树内部，都要重复这个“旅行路线”：</p>\\n<p><img src=\\"https://p1-jj.byteimg.com/tos-cn-i-t2oaga2asx/gold-user-assets/2020/4/6/1714efce7db2cdff~tplv-t2oaga2asx-watermark.awebp\\" alt=\\"\\"></p>\\n<p>在编码实现的时候，递归边界照旧，唯一发生改变的仍然是是递归式里调用递归函数的顺序：</p>\\n<pre class=\\"language-js\\"><code>function postorder(root) {\\n    // 递归边界，root 为空\\n    if(!root) {\\n        return \\n    }\\n     \\n    // 递归遍历左子树 \\n    postorder(root.left)  \\n    // 递归遍历右子树  \\n    postorder(root.right)\\n    // 输出当前遍历的结点值\\n    console.log(&#39;当前遍历的结点值是：&#39;, root.val)  \\n}\\n</code></pre>\\n<p>按照后序遍历的逻辑，同样的一棵二叉树，结点内容的输出顺序如下：</p>\\n<pre class=\\"language-text\\"><code>当前遍历的结点值是： D\\n当前遍历的结点值是： E\\n当前遍历的结点值是： B\\n当前遍历的结点值是： F\\n当前遍历的结点值是： C\\n当前遍历的结点值是： A\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>资源回收页面要求先释放子节点再释放父节点，同事使用根、左、右的先序逻辑；哪种具体故障可能出现，顺序应怎样改？</strong></p>\\n<p>父节点会在孩子之前被处理，后续子节点操作可能失去所依赖的父级上下文。应依次递归左子树、右子树，最后处理 <code>root.val</code>；若左右子树之间还有业务依赖，仅满足后序仍不足以决定二者次序。</p>\\n</li>\\n<li>\\n<p><strong>构建任务树中，父任务必须等左右子任务完成后再汇总；递归代码怎样保证示例树最终输出 <code>D、E、B、F、C、A</code>？</strong></p>\\n<p>对非空结点先调用 <code>postorder(root.left)</code>，再调用 <code>postorder(root.right)</code>，最后执行当前结点的汇总动作。叶子继续递归到空孩子时直接 <code>return</code>，随后才处理叶子自身；若把汇总放在任一递归调用之前，就破坏了父任务等待子任务的约束。</p>\\n</li>\\n<li>\\n<p><strong>产品把任务树改成可能只有右孩子的结构，例如 <code>C</code> 只有 <code>F</code>；后序函数需要为这种单边树增加特殊分支吗？</strong></p>\\n<p>不需要，空左子树会命中递归边界并立即返回，随后右子树 <code>F</code> 仍被完整处理，最后才处理 <code>C</code>。统一的空值边界已经覆盖叶子和单边结点；若孩子字段不是空值而是异常对象，则仍需在数据入口另做校验。</p>\\n</li>\\n<li>\\n<p><strong>线上日志显示示例树输出为 <code>D、E、B、C、F、A</code>，只有 <code>C</code> 与 <code>F</code> 的相对顺序错误；你会锁定哪一处代码？</strong></p>\\n<p>应重点检查处理 <code>C</code> 时，当前结点日志是否写在 <code>postorder(root.right)</code> 之前。因为左半树和根 <code>A</code> 的位置均正确，错误更像是右子树分支局部提前处理当前结点；修复后还应以只有右孩子的结点做回归。</p>\\n</li>\\n<li>\\n<p><strong>同事主张所有树操作统一用先序，避免维护三套模板；面对“父结果依赖两个孩子结果”的计算，你会如何取舍？</strong></p>\\n<p>应保留后序结构，因为父结点动作必须发生在左右子树完成之后，先序无法直接满足这一依赖。三种遍历的递归边界相同，主要差别只是业务动作的位置；强行统一会把依赖管理转移到额外状态中，反而增加出错风险。</p>\\n</li>\\n</ul>\\n</section>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码走查时函数名写着 <code>postorder</code>，实际输出却是 <code>A、B、D、E、C、F</code>；你怎样用代码现象判断它实现了什么遍历？</strong></p>\\n<p>这组结果表明当前结点在子树之前被处理，实际实现的是先序。应检查 <code>console.log(root.val)</code> 是否位于两个递归调用之前，并将它移动到左右子树调用之后；仅修改函数名不会改变访问次序。</p>\\n</li>\\n<li>\\n<p><strong>文件树删除流程要求目录必须晚于其所有后代处理，示例数据中 <code>A</code> 是根；递归动作应该放在哪里？</strong></p>\\n<p>删除或处理当前目录的动作应放在 <code>postorder(root.left)</code> 与 <code>postorder(root.right)</code> 都返回之后，因此 <code>A</code> 最后执行。入口遇到空结点应立即 <code>return</code>，使叶子能够自然结束；若真实删除还受权限或外部资源约束，需要额外处理失败回滚。</p>\\n</li>\\n<li>\\n<p><strong>接口把部分叶子节点的 <code>left</code>、<code>right</code> 省略为 <code>undefined</code>，后序遍历还能沿用 <code>if (!root) return</code> 吗？</strong></p>\\n<p>可以，递归到缺失孩子时传入 <code>undefined</code>，会命中该空值边界并返回，随后继续处理另一孩子和当前结点。该判断也会接受 <code>null</code>；但若接口可能用其他无效占位对象表示空节点，就不能假定这一边界足够。</p>\\n</li>\\n<li>\\n<p><strong>监控中发现每个叶子都打印正常，但父节点从不打印，代码没有抛错；你会怎样排查后序函数的控制流？</strong></p>\\n<p>应检查左右递归调用之后是否遗漏当前结点动作，或是否在第一次递归返回后又无条件 <code>return</code>。后序的父节点输出必须位于两个子调用之后，任何提前返回都会让父级处理被跳过；可用单个根节点作为最小用例验证。</p>\\n</li>\\n<li>\\n<p><strong>评审会上有人建议把后序递归改成“根、右、左”收集后再反转，你会接受吗？</strong></p>\\n<p>若最终只需要完整输出序列，这种方式能够得到左、右、根，但会引入保存结果和反转的额外步骤。直接递归更贴合父结点等待两个孩子的执行语义；若业务动作必须实时发生而不能延后批量执行，反转方案并不等价。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"结语\\"><a class=\\"header-anchor\\" href=\\"#结语\\">#</a> 结语</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>三种递归遍历的代码骨架相同，差异只在“访问当前结点”放在左右递归的前、中还是后</li>\\n<li>时间都为 <code>O(n)</code>，空间由树高 <code>h</code> 决定；真正选型依据是信息沿树向下传还是向上汇总</li>\\n<li>写递归前先明确函数返回什么、终止条件是什么、当前层如何组合子问题，避免只背模板</li>\\n</ul>\\n<p><strong>二叉树的先序、中序和后序遍历共用同一套递归骨架，区别只是当前结点在左右子树递归之前、中间还是之后访问。</strong> 三者都要访问每个结点，因此时间复杂度是 <code>O(n)</code>，递归空间则由树高 <code>h</code> 决定。面试时最好熟练到可以直接写出，同时明确函数返回值、终止条件和当前层如何组合子问题，把时间留给层次遍历或更综合的题目。</p>\\n</section>\\n<blockquote>\\n<p>对于二叉树的先、中、后序遍历，各位只要掌握了其中一种的思路，就可以举一反三、顺势推导其它三种思路。不过，我个人的建议，仍然是以“默写”的标准来要求自己，面试时不要指望“推导”，而应该有条件反射。这样才可以尽量地提高你做题的效率，为后面真正的难题、综合性题目腾出时间。</p>\\n</blockquote>\\n<p>关于二叉树遍历类题目的讨论，这里只是一个开始。二叉树的先、中、后包括层次遍历的玩法，还有很多很多，我们在后续的真题归纳解读专题、包括末尾的大规模刷题训练中，会带大家认识更多新奇好玩的东西。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在白板上说“先、中、后序只是名字不同”，但把日志始终写在递归函数第一行；你会让他用什么代码现象证明差异？</strong></p>\\n<p>让他分别把日志放在左递归之前、左右递归之间、右递归之后，并对同一棵树比较输出。三者对应先序、中序和后序，差异来自当前结点动作的相对位置；若空结点边界缺失，三种写法都会在叶子之后失败。</p>\\n</li>\\n<li>\\n<p><strong>面试只剩十分钟，候选人需要连续写出三种递归遍历；怎样组织模板才能减少现场推导和手误？</strong></p>\\n<p>先固定 <code>if (!root) return</code>，再固定左右子树的递归调用，只移动处理 <code>root.val</code> 的语句。放在两次调用前是先序，中间是中序，之后是后序；熟练模板能节省时间，但仍应拿一个小树快速核对输出。</p>\\n</li>\\n<li>\\n<p><strong>页面需求从“进入节点就埋点”改成“左侧渲染完成后再展示标题”，再改成“两个子区块完成后汇总”；遍历选型应如何变化？</strong></p>\\n<p>三个时机分别对应先序、中序和后序：当前动作位于左右递归之前、之间或之后。选型应跟随动作依赖，而不是沿用旧函数名；若页面还要求同层依次处理，则需要考虑层次遍历，不能硬套深度优先顺序。</p>\\n</li>\\n<li>\\n<p><strong>线上树数据出现空孩子后遍历报错，三套函数都有人分别修补；你会要求团队统一检查哪条不变量？</strong></p>\\n<p>三套递归都应在函数入口先判断 <code>root</code> 是否为空并立即 <code>return</code>，再访问 <code>val</code>、<code>left</code> 或 <code>right</code>。它们共享同一递归边界，区别仅在当前结点动作的位置；若数据可能形成循环引用，空值判断无法阻止无限递归。</p>\\n</li>\\n<li>\\n<p><strong>团队准备把所有遍历结果先存数组，另一方坚持边访问边处理；在日志展示与父子汇总两类需求中怎样取舍？</strong></p>\\n<p>只需按顺序输出或执行动作时，可在遍历位置直接处理，避免额外维护完整结果；调用方需要重复读取或统一展示时，数组更方便。父子汇总更应关注动作是否位于后序位置，数组不能替代依赖顺序；保存完整结果还会增加空间占用。</p>\\n</li>\\n<li>\\n<p><strong>面试题从先、中、后序继续追到层次遍历时，候选人仍只调整递归语句顺序；你会指出什么边界？</strong></p>\\n<p>调整当前结点动作的位置只能在深度优先的先序、中序和后序之间切换，无法自然得到逐层访问顺序。层次遍历关注同一深度的结点批次，需要不同的状态组织；因此掌握三种递归模板只是树遍历题的起点。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);