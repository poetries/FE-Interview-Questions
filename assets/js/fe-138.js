(window.webpackJsonp=window.webpackJsonp||[]).push([[138],{1245:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：状态管理的核心是把状态变化约束成可追踪的数据流，中间件在派发边界扩展异步、日志和副作用</li>\\n<li>原理主线：围绕 「一、初始vuex」、「二、modules」、「三、mutations」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：介绍了Vuex的核心概念、原理及其在Vue项目中的状态管理应用，涵盖state、getters、mutations、actions和modules等模块，帮助开发者理解和高效使用Vuex进行全局状态管理。</li>\\n<li>边界与代价：集中式状态不是越多越好；可变性、订阅粒度和异步取消决定性能与可维护性</li>\\n<li>工程落地：选型要从状态归属、更新频率、调试需求和团队约束出发，而不是只比较 API 数量</li>\\n</ul>\\n<p><strong><code>Vuex</code> 本质上是把多个组件共享的状态集中管理，并规定统一的数据读取和修改路径。</strong> 状态放在 <code>state</code>，公共派生数据交给 <code>getters</code>，同步修改通过 <code>mutations</code>，异步流程或多个修改则由 <code>actions</code> 调度。组件可用 <code>mapState</code>、<code>mapGetters</code> 和 <code>mapActions</code> 减少重复代码，状态较多时再用 <code>modules</code> 拆分。小型应用不必过度分模块，但应避免组件绕过 <code>commit</code> 随意修改共享状态。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文出现 <code>Object.defineProperty</code>、<code>Dep</code>、<code>Watcher</code> 和双端 <code>Diff</code> 时，主要描述 <code>Vue 2</code> 实现；<code>Vue 3</code> 使用 <code>Proxy</code>、<code>effect</code> 与新的渲染器路径。<code>Vue 2</code> 已于 <code>2023-12-31</code> 结束维护，新项目应以 <code>Vue 3</code> 为基线，旧项目参考 <a href=\\"https://v2.vuejs.org/eol/\\">Vue 2 EOL 官方说明</a> 制定迁移与安全策略。</p>\\n<blockquote>\\n<p>了解vuex核心概念请移步 <a href=\\"https://vuex.vuejs.org/zh/\\">https://vuex.vuejs.org/zh/</a></p>\\n</blockquote>\\n<p><img src=\\"https://vuex.vuejs.org/vuex.png\\" alt=\\"img\\"></p>\\n<h2 id=\\"一、初始vuex\\"><a class=\\"header-anchor\\" href=\\"#一、初始vuex\\">#</a> 一、初始vuex</h2>\\n<h3 id=\\"_1-1-vuex是什么\\"><a class=\\"header-anchor\\" href=\\"#_1-1-vuex是什么\\">#</a> 1.1 vuex是什么</h3>\\n<blockquote>\\n<ul>\\n<li>\\n<pre class=\\"language-text\\"><code>vuex\\n</code></pre>\\n<p>就是把需要共享的变量全部存储在一个对象里面，然后将这个对象放在顶层组件中供其他组件使用</p>\\n<ul>\\n<li>父子组件通信时，我们通常会采用 <code>props + emit</code> 这种方式。但当通信双方不是父子组件甚至压根不存在相关联系，或者一个状态需要共享给多个组件时，就会非常麻烦，数据也会相当难维护</li>\\n</ul>\\n</li>\\n</ul>\\n</blockquote>\\n<h3 id=\\"_1-2-vuex中有什么\\"><a class=\\"header-anchor\\" href=\\"#_1-2-vuex中有什么\\">#</a> 1.2 vuex中有什么</h3>\\n<pre class=\\"language-text\\"><code>const store = new Vuex.Store({\\n    state: {\\n        name: &#39;weish&#39;,\\n        age: 22\\n    },\\n    getters: {\\n        personInfo(state) {\\n            return `My name is ${state.name}, I am ${state.age}`;\\n        }\\n    }\\n    mutations: {\\n        SET_AGE(state, age) {\\n            commit(age, age);\\n        }\\n    },\\n    actions: {\\n        nameAsyn({commit}) {\\n            setTimeout(() =&gt; {\\n                commit(&#39;SET_AGE&#39;, 18);\\n            }, 1000);\\n        }\\n    },\\n    modules: {\\n        a: modulesA\\n    }\\n}\\n</code></pre>\\n<blockquote>\\n<p>个就是最基本也是完整的<code>vuex</code>代码；<code>vuex</code> 包含有五个基本的对象</p>\\n</blockquote>\\n<ul>\\n<li><code>state</code>：存储状态。也就是变量；</li>\\n<li><code>getters</code>：派生状态。也就是<code>set</code>、<code>get</code>中的<code>get</code>，有两个可选参数：<code>state</code>、<code>getters</code>分别可以获取<code>state</code>中的变量和其他的<code>getters</code>。外部调用方式：<code>store.getters.personInfo()</code>。就和<code>vue</code>的<code>computed</code>差不多；</li>\\n<li><code>mutations</code>：提交状态修改。也就是<code>set</code>、<code>get</code>中的<code>set</code>，这是<code>vuex</code>中唯一修改<code>state</code>的方式，但不支持异步操作。第一个参数默认是<code>state</code>。外部调用方式：<code>store.commit(\'SET_AGE\', 18)</code>。和<code>vue</code>中的<code>methods</code>类似。</li>\\n<li><code>actions</code>：和<code>mutations</code>类似。不过<code>actions</code>支持异步操作。第一个参数默认是和<code>store</code>具有相同参数属性的对象。外部调用方式：<code>store.dispatch(\'nameAsyn\')</code>。</li>\\n<li><code>modules</code>：<code>store</code>的子模块，内容就相当于是<code>store</code>的一个实例。调用方式和前面介绍的相似，只是要加上当前子模块名，如：<code>store.a.getters.xxx()</code></li>\\n</ul>\\n<h3 id=\\"_1-3-vue-cli中使用vuex的方式\\"><a class=\\"header-anchor\\" href=\\"#_1-3-vue-cli中使用vuex的方式\\">#</a> 1.3 vue-cli中使用vuex的方式</h3>\\n<p><strong>目录结构</strong></p>\\n<pre class=\\"language-text\\"><code>├── index.html\\n├── main.js\\n├── components\\n└── store\\n    ├── index.js          # 我们组装模块并导出 store 的地方\\n    ├── state.js          # 跟级别的 state\\n    ├── getters.js        # 跟级别的 getter\\n    ├── mutation-types.js # 根级别的mutations名称（官方推荐mutions方法名使用大写）\\n    ├── mutations.js      # 根级别的 mutation\\n    ├── actions.js        # 根级别的 action\\n    └── modules\\n        ├── m1.js         # 模块1\\n        └── m2.js         # 模块2\\n</code></pre>\\n<p><strong>state示例</strong></p>\\n<pre class=\\"language-text\\"><code>const state = {\\n    name: &#39;weish&#39;,\\n    age: 22\\n};\\n\\nexport default state;\\n</code></pre>\\n<p><strong>getter示例</strong></p>\\n<blockquote>\\n<p><code>getters.js</code>示例（我们一般使用<code>getters</code>来获取<code>state</code>的状态，而不是直接使用<code>state</code>）</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>export const name = (state) =&gt; {\\n    return state.name;\\n}\\n\\nexport const age = (state) =&gt; {\\n    return state.age\\n}\\n\\nexport const other = (state) =&gt; {\\n    return `My name is ${state.name}, I am ${state.age}.`;\\n}\\n</code></pre>\\n<p><strong>mutation-type示例</strong></p>\\n<blockquote>\\n<p>将所有<code>mutations</code>的函数名放在这个文件里</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>export const SET_NAME = &#39;SET_NAME&#39;;\\nexport const SET_AGE = &#39;SET_AGE&#39;;\\n</code></pre>\\n<p><strong>mutations示例</strong></p>\\n<pre class=\\"language-text\\"><code>import * as types from &#39;./mutation-type.js&#39;;\\n\\nexport default {\\n    [types.SET_NAME](state, name) {\\n        state.name = name;\\n    },\\n    [types.SET_AGE](state, age) {\\n        state.age = age;\\n    }\\n};\\n</code></pre>\\n<p><strong>actions示例</strong></p>\\n<blockquote>\\n<p>异步操作、多个<code>commit</code>时</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>import * as types from &#39;./mutation-type.js&#39;;\\n\\nexport default {\\n    nameAsyn({commit}, {age, name}) {\\n        commit(types.SET_NAME, name);\\n        commit(types.SET_AGE, age);\\n    }\\n}\\n</code></pre>\\n<p><strong>modules–m1.js示例</strong></p>\\n<blockquote>\\n<p>如果不是很复杂的应用，一般来讲是不会分模块的</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>export default {\\n    state: {},\\n    getters: {},\\n    mutations: {},\\n    actions: {}\\n}\\n</code></pre>\\n<p><strong>index.js示例（组装vuex)</strong></p>\\n<pre class=\\"language-text\\"><code>import vue from &#39;vue&#39;;\\nimport vuex from &#39;vuex&#39;;\\nimport state from &#39;./state.js&#39;;\\nimport * as getters from &#39;./getters.js&#39;;\\nimport mutations from &#39;./mutations.js&#39;;\\nimport actions from &#39;./actions.js&#39;;\\nimport m1 from &#39;./modules/m1.js&#39;;\\nimport m2 from &#39;./modules/m2.js&#39;;\\nimport createLogger from &#39;vuex/dist/logger&#39;; // 修改日志\\n\\nvue.use(vuex);\\n\\nconst debug = process.env.NODE_ENV !== &#39;production&#39;; // 开发环境中为true，否则为false\\n\\nexport default new vuex.Store({\\n    state,\\n    getters,\\n    mutations,\\n    actions,\\n    modules: {\\n        m1,\\n        m2\\n    },\\n    plugins: debug ? [createLogger()] : [] // 开发环境下显示vuex的状态修改\\n});\\n</code></pre>\\n<blockquote>\\n<p>最后将<code>store</code>实例挂载到<code>main.js</code>里面的<code>vue</code>上去就行了</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>import store from &#39;./store/index.js&#39;;\\n\\nnew Vue({\\n  el: &#39;#app&#39;,\\n  store,\\n  render: h =&gt; h(App)\\n});\\n</code></pre>\\n<blockquote>\\n<p>在<code>vue</code>组件中使用时，我们通常会使用<code>mapGetters</code>、<code>mapActions</code>、<code>mapMutations</code>，然后就可以按照<code>vue</code>调用<code>methods</code>和<code>computed</code>的方式去调用这些变量或函数，示例如</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>import {mapGetters, mapMutations, mapActions} from &#39;vuex&#39;;\\n\\n/* 只写组件中的script部分 */\\nexport default {\\n    computed: {\\n        ...mapGetters([\\n            name,\\n            age\\n        ])\\n    },\\n    methods: {\\n        ...mapMutations({\\n            setName: &#39;SET_NAME&#39;,\\n            setAge: &#39;SET_AGE&#39;\\n        }),\\n        ...mapActions([\\n            nameAsyn\\n        ])\\n    }\\n};\\n</code></pre>\\n<h2 id=\\"二、modules\\"><a class=\\"header-anchor\\" href=\\"#二、modules\\">#</a> 二、modules</h2>\\n<blockquote>\\n<p>在 src 目录下 , 新建一个 store 文件夹 , 然后在里面新建一个 index.js</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>import Vue from &#39;vue&#39;\\nimport vuex from &#39;vuex&#39;\\nVue.use(vuex);\\n\\nexport default new vuex.Store({\\n    state:{\\n        show:false\\n    }\\n})\\n</code></pre>\\n<blockquote>\\n<p>在 <code>main.js</code> 里的代码应该改成,在实例化 <code>Vue</code>对象时加入 <code>store</code> 对象</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>//vuex\\nimport store from &#39;./store&#39;\\n\\nnew Vue({\\n  el: &#39;#app&#39;,\\n  router,\\n  store,//使用store\\n  template: &#39;&lt;App/&gt;&#39;,\\n  components: { App }\\n})\\n</code></pre>\\n<blockquote>\\n<p>这样就把 <code>store</code> 分离出去了 , 那么还有一个问题是 : 这里 <code>$store.state.show</code> 无论哪个组件都可以使用 , 那组件多了之后 , 状态也多了 , 这么多状态都堆在 store 文件夹下的 <code>index.js</code> 不好维护怎么办 ?</p>\\n</blockquote>\\n<ul>\\n<li>我们可以使用 <code>vuex</code> 的 <code>modules</code> , 把 <code>store</code> 文件夹下的 <code>index.js</code> 改成</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>import Vue from &#39;vue&#39;\\nimport vuex from &#39;vuex&#39;\\nVue.use(vuex);\\n\\nimport dialog_store from &#39;../components/dialog_store.js&#39;;//引入某个store对象\\n\\nexport default new vuex.Store({\\n    modules: {\\n        dialog: dialog_store\\n    }\\n})\\n</code></pre>\\n<blockquote>\\n<p>这里我们引用了一个 <code>dialog_store.js</code> , 在这个 <code>js</code>文件里我们就可以单独写 <code>dialog</code> 组件的状态了</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>export default {\\n    state:{\\n        show:false\\n    }\\n}\\n</code></pre>\\n<blockquote>\\n<p>做出这样的修改之后 , 我们将之前我们使用的 <code>$store.state.show</code> 统统改为 <code>$store.state.dialog.show</code> 即可</p>\\n</blockquote>\\n<ul>\\n<li>如果还有其他的组件需要使用 <code>vuex</code> , 就新建一个对应的状态文件 , 然后将他们加入 <code>store</code>文件夹下的 <code>index.js</code>文件中的 <code>modules</code> 中</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>modules: {\\n    dialog: dialog_store,\\n    other: other,//其他组件\\n}\\n</code></pre>\\n<h2 id=\\"三、mutations\\"><a class=\\"header-anchor\\" href=\\"#三、mutations\\">#</a> 三、mutations</h2>\\n<blockquote>\\n<p>对<code>vuex</code> 的依赖仅仅只有一个 <code>$store.state.dialog.show</code> 一个状态 , 但是如果我们要进行一个操作 , 需要依赖很多很多个状态 , 那管理起来又麻烦了</p>\\n</blockquote>\\n<ul>\\n<li><code>mutations</code>里的操作必须是同步的</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>export default {\\n    state:{//state\\n        show:false\\n    },\\n    mutations:{\\n        switch_dialog(state){//这里的state对应着上面这个state\\n            state.show = state.show?false:true;\\n            //你还可以在这里执行其他的操作改变state\\n        }\\n    }\\n}\\n</code></pre>\\n<blockquote>\\n<p>使用 <code>mutations</code> 后 , 原先我们的父组件可以改为</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>&lt;template&gt;\\n  &lt;div id=&quot;app&quot;&gt;\\n    &lt;a href=&quot;javascript:;&quot; @click=&quot;$store.commit(&#39;switch_dialog&#39;)&quot;&gt;点击&lt;/a&gt;\\n    &lt;t-dialog&gt;&lt;/t-dialog&gt;\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport dialog from &#39;./components/dialog.vue&#39;\\nexport default {\\n  components:{\\n    &quot;t-dialog&quot;:dialog\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<blockquote>\\n<p>使用 <code>$store.commit(\'switch_dialog\')</code> 来触发 <code>mutations</code> 中的 <code>switch_dialog</code> 方法</p>\\n</blockquote>\\n<h2 id=\\"四、actions\\"><a class=\\"header-anchor\\" href=\\"#四、actions\\">#</a> 四、actions</h2>\\n<blockquote>\\n<p>多个 <code>state</code> 的操作 , 使用 <code>mutations</code>会来触发会比较好维护 , 那么需要执行多个 <code>mutations</code> 就需要用 <code>action</code> 了</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>export default {\\n    state:{//state\\n        show:false\\n    },\\n    mutations:{\\n        switch_dialog(state){//这里的state对应着上面这个state\\n            state.show = state.show?false:true;\\n            //你还可以在这里执行其他的操作改变state\\n        }\\n    },\\n    actions:{\\n        switch_dialog(context){//这里的context和我们使用的$store拥有相同的对象和方法\\n            context.commit(&#39;switch_dialog&#39;);\\n            //你还可以在这里触发其他的mutations方法\\n        },\\n    }\\n}\\n</code></pre>\\n<blockquote>\\n<p>那么 , 在之前的父组件中 , 我们需要做修改 , 来触发 <code>action</code> 里的 <code>switch_dialog</code> 方法</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>&lt;template&gt;\\n  &lt;div id=&quot;app&quot;&gt;\\n    &lt;a href=&quot;javascript:;&quot; @click=&quot;$store.dispatch(&#39;switch_dialog&#39;)&quot;&gt;点击&lt;/a&gt;\\n    &lt;t-dialog&gt;&lt;/t-dialog&gt;\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport dialog from &#39;./components/dialog.vue&#39;\\nexport default {\\n  components:{\\n    &quot;t-dialog&quot;:dialog\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<ul>\\n<li>使用 <code>$store.dispatch(\'switch_dialog\')</code> 来触发 <code>action</code> 中的 <code>switch_dialog</code> 方法。</li>\\n<li>官方推荐 , 将异步操作放在 <code>action</code>中</li>\\n</ul>\\n<h2 id=\\"五、getters\\"><a class=\\"header-anchor\\" href=\\"#五、getters\\">#</a> 五、getters</h2>\\n<blockquote>\\n<p><code>getters</code>和 <code>vue</code> 中的<code>computed</code> 类似 , 都是用来计算 <code>state</code> 然后生成新的数据 ( 状态 ) 的</p>\\n</blockquote>\\n<ul>\\n<li>假如我们需要一个与状态 <code>show</code> 刚好相反的状态 , 使用 <code>vue</code> 中的 <code>computed</code> 可以这样算出来</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>computed(){\\n    not_show(){\\n        return !this.$store.state.dialog.show;\\n    }\\n}\\n</code></pre>\\n<blockquote>\\n<p>那么 , 如果很多很多个组件中都需要用到这个与 <code>show</code>刚好相反的状态 , 那么我们需要写很多很多个 <code>not_show</code>, 使用 <code>getters</code>就可以解决这种问题</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>export default {\\n    state:{//state\\n        show:false\\n    },\\n    getters:{\\n        not_show(state){//这里的state对应着上面这个state\\n            return !state.show;\\n        }\\n    },\\n    mutations:{\\n        switch_dialog(state){//这里的state对应着上面这个state\\n            state.show = state.show?false:true;\\n            //你还可以在这里执行其他的操作改变state\\n        }\\n    },\\n    actions:{\\n        switch_dialog(context){//这里的context和我们使用的$store拥有相同的对象和方法\\n            context.commit(&#39;switch_dialog&#39;);\\n            //你还可以在这里触发其他的mutations方法\\n        },\\n    }\\n}\\n</code></pre>\\n<blockquote>\\n<p>我们在组件中使用 <code>$store.state.dialog.show</code> 来获得状态 <code>show</code> , 类似的 , 我们可以使用 <code>$store.getters.not_show</code> 来获得状态 <code>not_show</code></p>\\n</blockquote>\\n<ul>\\n<li>注意 : <code>$store.getters.not_show</code> 的值是不能直接修改的 , 需要对应的 <code>state</code> 发生变化才能修改</li>\\n</ul>\\n<h2 id=\\"六、mapstate、mapgetters、mapactions\\"><a class=\\"header-anchor\\" href=\\"#六、mapstate、mapgetters、mapactions\\">#</a> 六、mapState、mapGetters、mapActions</h2>\\n<blockquote>\\n<p>很多时候 , <code>$store.state.dialog.show</code> 、<code>$store.dispatch(\'switch_dialog\')</code> 这种写法很不方便</p>\\n</blockquote>\\n<ul>\\n<li>使用 <code>mapState</code>、<code>mapGetters</code>、<code>mapActions</code> 就不会这么复杂了</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&lt;template&gt;\\n  &lt;el-dialog :visible.sync=&quot;show&quot;&gt;&lt;/el-dialog&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport {mapState} from &#39;vuex&#39;;\\nexport default {\\n  computed:{\\n\\n    //这里的三点叫做 : 扩展运算符\\n    ...mapState({\\n      show:state=&gt;state.dialog.show\\n    }),\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p>相当于</p>\\n<pre class=\\"language-text\\"><code>&lt;template&gt;\\n  &lt;el-dialog :visible.sync=&quot;show&quot;&gt;&lt;/el-dialog&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport {mapState} from &#39;vuex&#39;;\\nexport default {\\n  computed:{\\n    show(){\\n        return this.$store.state.dialog.show;\\n    }\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<blockquote>\\n<p><code>mapGetters</code>、<code>mapActions</code> 和 <code>mapState</code> 类似 , <code>mapGetters</code> 一般也写在 <code>computed</code> 中 , <code>mapActions</code> 一般写在 <code>methods</code> 中</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>订单弹窗只读取 <code>state.dialog.show</code>，同事却说用了 <code>mapState</code> 就不再依赖 <code>Vuex</code> 状态，你会如何纠正？</strong></p>\\n<p><code>mapState</code> 只是生成对应的计算属性，底层仍然读取 <code>this.$store.state.dialog.show</code>，并没有改变状态来源。它减少模板和组件中的重复路径，但组件依旧与该 <code>store</code> 字段耦合；模块结构改名时映射也必须同步调整。</p>\\n</li>\\n<li>\\n<p><strong>一个页面有五个本地计算属性，还要映射 <code>dialog.show</code>，你会怎样写 <code>computed</code> 才不覆盖原有逻辑？</strong></p>\\n<p>在 <code>computed</code> 中使用展开语法合并 <code>mapState</code> 的返回对象，例如映射 <code>show: state =&gt; state.dialog.show</code>，原有计算属性保留在同一对象中。映射名会成为组件属性，因此要避免与现有字段重名；冲突时后写的键可能遮蔽前面的定义。</p>\\n</li>\\n<li>\\n<p><strong>组件原先直接写 <code>$store.dispatch(\'switch_dialog\')</code>，现在模板交互增多，团队要求统一使用辅助函数，你会把 <code>mapActions</code> 放在哪里？</strong></p>\\n<p>我会把 <code>mapActions</code> 展开到 <code>methods</code>，让事件处理通过组件方法触发对应 <code>action</code>；状态与 <code>getter</code> 的映射则放在 <code>computed</code>。这种调整主要改善调用表达和重复代码，不改变 <code>action</code> 的实际执行语义，也不会自动解决命名冲突或错误 <code>action</code> 名称。</p>\\n</li>\\n<li>\\n<p><strong>线上弹窗始终不显示，模板绑定 <code>show</code>，组件用了 <code>...mapState({ show: state =&gt; state.dialog.visible })</code>，而仓库真实字段是 <code>dialog.show</code>，你怎么排查？</strong></p>\\n<p>先展开映射关系，直接对照 <code>state</code> 的实际结构和原先可工作的 <code>$store.state.dialog.show</code> 路径。当前映射读取了错误字段，计算属性会得到非预期值；修正为 <code>state =&gt; state.dialog.show</code> 后，还需确认模板绑定的属性名仍是 <code>show</code>。</p>\\n</li>\\n<li>\\n<p><strong>团队认为所有 <code>$store</code> 调用都应替换为 <code>mapState</code>，包括 <code>getter</code> 和 <code>dispatch</code>，你会接受这个统一规则吗？</strong></p>\\n<p>不会，三类辅助函数对应不同职责：<code>mapState</code> 映射状态，<code>mapGetters</code> 映射 <code>getter</code>，<code>mapActions</code> 映射 <code>action</code>。前两者通常进入 <code>computed</code>，<code>mapActions</code> 通常进入 <code>methods</code>；为了表面统一而混用会模糊读取与行为调用的边界。</p>\\n</li>\\n<li>\\n<p><strong>公共组件只有一处读取和一次派发，负责人坚持引入三个映射辅助函数来追求“标准化”，你如何权衡？</strong></p>\\n<p>若直接访问路径清晰且只出现一次，辅助函数的收益主要是书写便利，并非功能必需；不必为了形式统一增加别名和跳转。出现多处状态、<code>getter</code> 或 <code>action</code> 映射时再集中展开更合适，但无论选择哪种写法，都应保持来源和名称可追踪。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);