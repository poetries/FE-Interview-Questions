(window.webpackJsonp=window.webpackJsonp||[]).push([[53],{1160:function(e){e.exports=JSON.parse('{"html":"<h2 id=\\"_1-性能优化方式\\"><a class=\\"header-anchor\\" href=\\"#_1-性能优化方式\\">#</a> 1 性能优化方式</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>按用户感知的时间线分四段：<strong>加载</strong>（网络+体积）、<strong>渲染</strong>（首屏绘制）、<strong>交互</strong>（响应延迟）、<strong>运行时</strong>（长任务、内存）</li>\\n<li>加载：路由懒加载、<code>CDN</code>、<code>gzip</code>/<code>br</code>、关键资源 <code>preload</code>、图片按设备出尺寸 + 现代格式</li>\\n<li>渲染：关键 <code>CSS</code> 内联、<code>JS</code> 加 <code>defer</code>、骨架屏、避免首屏图懒加载（会拖垮 <code>LCP</code>）</li>\\n<li>交互与运行时：长任务分片或搬进 <code>Worker</code>、动画只动 <code>transform</code>/<code>opacity</code>、长列表虚拟滚动</li>\\n<li><strong>先量再改</strong>：<code>Performance</code> 看阶段耗时、<code>Coverage</code> 看代码使用率、线上看 <code>LCP</code>/<code>INP</code>/<code>CLS</code> 的 <strong><code>P75</code></strong></li>\\n</ul>\\n<p><strong>性能优化要结合具体加载环节处理，当前资料明确给出的方式是对外部域名进行 <code>DNS</code> 预解析。</strong> 浏览器访问域名前需要先把域名解析成对应的 <code>IP</code>，这个过程本身会消耗时间。通过提前声明 <code>dns-prefetch</code>，可以让浏览器预先完成解析，为后续资源请求减少这部分等待。它适合页面确定会访问某个外部域名的场景，其他优化方式还需要结合资源和页面情况判断。</p>\\n</section>\\n<h3 id=\\"_1-1-dns-预解析\\"><a class=\\"header-anchor\\" href=\\"#_1-1-dns-预解析\\">#</a> 1.1 DNS 预解析</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>dns-prefetch</code> 只提前把域名解析成 <code>IP</code>，不建立连接</li>\\n<li>它适合页面后续确定会访问的第三方域名，当前主域通常已在导航时解析</li>\\n<li>域名过多会抢占网络与解析资源，隐私敏感场景还会提前暴露访问意图</li>\\n<li>需要连 <code>TCP/TLS</code> 一并提前建立时用 <code>preconnect</code>，但只保留少量关键源</li>\\n</ul>\\n<p><strong><code>DNS</code> 预解析就是让浏览器提前把目标域名解析成对应的 <code>IP</code>，减少真正请求资源时的等待。</strong> 使用时可以在页面中添加 <code>&lt;link rel=&quot;dns-prefetch&quot; href=&quot;//blog.poetries.top&quot;&gt;</code>，提示浏览器提前处理这个域名。它优化的是域名解析阶段，并不代表资源内容已经被下载。一般用于页面后续确定会访问的外部域名，没有相关请求时则不会带来实际收益。</p>\\n</section>\\n<ul>\\n<li><code>DNS</code> 解析也是需要时间的，可以通过预解析的方式来预先获得域名所对应的 <code>IP</code></li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;link rel=&quot;dns-prefetch&quot; href=&quot;//blog.poetries.top&quot;&gt;\\n</code></pre>\\n<h3 id=\\"_1-2-缓存\\"><a class=\\"header-anchor\\" href=\\"#_1-2-缓存\\">#</a> 1.2 缓存</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先判强缓存：<code>Cache-Control: max-age</code> 新鲜时直接用本地副本，不发请求</li>\\n<li>过期后走协商缓存：<code>ETag/If-None-Match</code> 或 <code>Last-Modified/If-Modified-Since</code></li>\\n<li>未变化返回 <code>304</code> 复用响应体，已变化返回 <code>200</code> 和新内容</li>\\n<li>带内容哈希的静态资源适合长期缓存，<code>HTML</code> 通常需要及时验证</li>\\n<li><code>no-cache</code> 是“可以存但每次验证”，<code>no-store</code> 才是“完全不存”</li>\\n</ul>\\n<p><strong>浏览器缓存分为强缓存和协商缓存，前者在有效期内直接使用本地资源，后者会向服务端确认资源是否变化。</strong> 强缓存通常由 <code>Cache-Control: max-age</code> 控制，且 <code>Cache-Control</code> 的优先级高于 <code>Expires</code>。缓存过期后，可通过 <code>ETag</code> 与 <code>If-None-Match</code>，或 <code>Last-Modified</code> 与 <code>If-Modified-Since</code> 进行协商；资源未变时返回 <code>304</code>。频繁变化的资源可用 <code>no-cache</code> 配合 <code>ETag</code>，完全不需要缓存时才使用 <code>no-store</code>，带文件指纹的代码文件则适合长期缓存。</p>\\n</section>\\n<ul>\\n<li>缓存对于前端性能优化来说是个很重要的点，良好的缓存策略可以降低资源的重复加载提高网页的整体加载速度</li>\\n<li>通常浏览器缓存策略分为两种：强缓存和协商缓存</li>\\n</ul>\\n<p><strong>强缓存</strong></p>\\n<blockquote>\\n<p>实现强缓存可以通过两种响应头实现：<code>Expires</code>和 <code>Cache-Control</code> 。强缓存表示在缓存期间不需要请求，<code>state code</code>为 <code>200</code></p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>Expires: Wed, 22 Oct 2018 08:41:00 GMT\\n</code></pre>\\n<blockquote>\\n<p><code>Expires</code> 是 <code>HTTP / 1.0</code> 的产物，表示资源会在 <code>Wed, 22 Oct 2018 08:41:00 GMT</code> 后过期，需要再次请求。并且 <code>Expires</code> 受限于本地时间，如果修改了本地时间，可能会造成缓存失效</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>Cache-control: max-age=30\\n</code></pre>\\n<blockquote>\\n<p><code>Cache-Control</code> 出现于 <code>HTTP / 1.1</code>，优先级高于 <code>Expires</code> 。该属性表示资源会在 <code>30</code> 秒后过期，需要再次请求</p>\\n</blockquote>\\n<p><strong>协商缓存</strong></p>\\n<ul>\\n<li>如果缓存过期了，我们就可以使用协商缓存来解决问题。协商缓存需要请求，如果缓存有效会返回 304</li>\\n<li>协商缓存需要客户端和服务端共同实现，和强缓存一样，也有两种实现方式</li>\\n</ul>\\n<p><code>Last-Modified</code> 和 <code>If-Modified-Since</code></p>\\n<ul>\\n<li><code>Last-Modified</code> 表示本地文件最后修改日期，<code>If-Modified-Since</code> 会将 <code>Last-Modified</code>的值发送给服务器，询问服务器在该日期后资源是否有更新，有更新的话就会将新的资源发送回来</li>\\n<li>但是如果在本地打开缓存文件，就会造成 <code>Last-Modified</code> 被修改，所以在 <code>HTTP / 1.1</code> 出现了 <code>ETag</code></li>\\n</ul>\\n<p><code>ETag</code> 和 <code>If-None-Match</code></p>\\n<ul>\\n<li><code>ETag</code> 类似于文件指纹，<code>If-None-Match</code> 会将当前 <code>ETag</code> 发送给服务器，询问该资源 ETag 是否变动，有变动的话就将新的资源发送回来。并且 <code>ETag</code> 优先级比 <code>Last-Modified</code> 高</li>\\n</ul>\\n<p><strong>选择合适的缓存策略</strong></p>\\n<blockquote>\\n<p>对于大部分的场景都可以使用强缓存配合协商缓存解决，但是在一些特殊的地方可能需要选择特殊的缓存策略</p>\\n</blockquote>\\n<ul>\\n<li>对于某些不需要缓存的资源，可以使用 <code>Cache-control: no-store</code> ，表示该资源不需要缓存</li>\\n<li>对于频繁变动的资源，可以使用 <code>Cache-Control: no-cache</code> 并配合 <code>ETag</code> 使用，表示该资源已被缓存，但是每次都会发送请求询问资源是否更新。</li>\\n<li>对于代码文件来说，通常使用 <code>Cache-Control: max-age=31536000</code> 并配合策略缓存使用，然后对文件进行指纹处理，一旦文件名变动就会立刻下载新的文件</li>\\n</ul>\\n<h3 id=\\"_1-3-使用-http-2-0\\"><a class=\\"header-anchor\\" href=\\"#_1-3-使用-http-2-0\\">#</a> 1.3 使用 HTTP / 2.0</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>HTTP/2</code> 把请求响应拆成带 <code>Stream ID</code> 的帧，在一条连接上交错传输</li>\\n<li>多路复用解决 <code>HTTP/1.1</code> 应用层队头阻塞，减少为并发而建立的连接数</li>\\n<li><code>HPACK</code> 压缩重复请求头，但业务响应体仍需 <code>gzip/br</code></li>\\n<li>底层仍是有序 <code>TCP</code>，丢包时所有流都会等待；<code>HTTP/3</code> 用 <code>QUIC</code> 改善这一点</li>\\n<li>升级后不应继续用多域名分片，否则会失去连接复用与头部压缩收益</li>\\n</ul>\\n<p><strong><code>HTTP/2</code> 通过一条 <code>TCP</code> 连接承载多条流，让多个请求可以并行传输。</strong> 相比 <code>HTTP/1.1</code> 频繁建立连接带来的 <code>RTT</code> 和慢启动成本，多路复用能减少连接开销，数据帧再按 <code>Stream ID</code> 重组。它还支持请求头压缩，可以进一步降低传输的数据量。不过底层仍是 <code>TCP</code>，丢包时依然可能出现连接层的队头阻塞。</p>\\n</section>\\n<ul>\\n<li>因为浏览器会有并发请求限制，在 <code>HTTP / 1.1</code> 时代，每个请求都需要建立和断开，消耗了好几个 <code>RTT</code> 时间，并且由于 <code>TCP</code> 慢启动的原因，加载体积大的文件会需要更多的时间</li>\\n<li>在 <code>HTTP / 2.0</code> 中引入了多路复用，能够让多个请求使用同一个 <code>TCP</code> 链接，极大的加快了网页的加载速度。并且还支持 <code>Header</code> 压缩，进一步的减少了请求的数据大小</li>\\n</ul>\\n<h3 id=\\"_1-4-预加载\\"><a class=\\"header-anchor\\" href=\\"#_1-4-预加载\\">#</a> 1.4 预加载</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>preload</code> 提高当前页面关键资源的发现时间与优先级，不负责自动执行资源</li>\\n<li>必须写正确的 <code>as</code>，字体等跨源资源还要匹配 <code>crossorigin</code>，否则可能重复下载</li>\\n<li>适合关键字体、首屏主图、晚发现的关键脚本或样式</li>\\n<li>滥用会挤占首屏带宽，把真正关键资源拖慢，并产生“预加载但未使用”警告</li>\\n</ul>\\n<p><strong><code>preload</code> 是用声明式方式让浏览器提前请求当前页面确定会用到的资源。</strong> 它本质上类似提前发起 <code>fetch</code>，资源可以更早进入加载流程，而且不会阻塞 <code>onload</code> 事件。比如可以通过 <code>&lt;link rel=&quot;preload&quot; href=&quot;http://example.com&quot;&gt;</code> 预加载重要文件，从而在一定程度上降低首屏等待时间。使用时要确认资源确实需要，同时留意浏览器兼容性。</p>\\n</section>\\n<ul>\\n<li>在开发中，可能会遇到这样的情况。有些资源不需要马上用到，但是希望尽早获取，这时候就可以使用预加载</li>\\n<li>预加载其实是声明式的 <code>fetch</code> ，强制浏览器请求资源，并且不会阻塞 <code>onload</code> 事件，可以使用以下代码开启预加载</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;link rel=&quot;preload&quot; href=&quot;http://example.com&quot;&gt;\\n</code></pre>\\n<blockquote>\\n<p>预加载可以一定程度上降低首屏的加载时间，因为可以将一些不影响首屏但重要的文件延后加载，唯一缺点就是兼容性不好</p>\\n</blockquote>\\n<h3 id=\\"_1-5-预渲染\\"><a class=\\"header-anchor\\" href=\\"#_1-5-预渲染\\">#</a> 1.5 预渲染</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>预渲染提前准备用户下一步高概率访问的页面，比预取资源更重</li>\\n<li>只有导航意图足够确定时才值得做，否则浪费流量、CPU 和内存</li>\\n<li>页面可能包含埋点、登录态或副作用，必须确认预渲染阶段不会误触业务逻辑</li>\\n<li>现代浏览器应优先评估 <code>Speculation Rules API</code>，旧式 <code>&lt;link rel=&quot;prerender&quot;&gt;</code> 支持与行为不一致</li>\\n</ul>\\n<p><strong>预渲染是提前下载并在后台渲染用户接下来可能访问的页面。</strong> 当用户真正打开该页面时，前置工作已经完成，因此页面展示会更快，例如可通过 <code>&lt;link rel=&quot;prerender&quot; href=&quot;http://poetries.com&quot;&gt;</code> 开启。它的取舍也很直接：只有后续访问几乎确定时才值得使用。判断失误就会白白消耗下载和渲染资源。</p>\\n</section>\\n<blockquote>\\n<p>可以通过预渲染将下载的文件预先在后台渲染，可以使用以下代码开启预渲染</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;link rel=&quot;prerender&quot; href=&quot;http://poetries.com&quot;&gt;\\n</code></pre>\\n<ul>\\n<li>预渲染虽然可以提高页面的加载速度，但是要确保该页面百分百会被用户在之后打开，否则就白白浪费资源去渲染</li>\\n</ul>\\n<h3 id=\\"_1-6-懒执行与懒加载\\"><a class=\\"header-anchor\\" href=\\"#_1-6-懒执行与懒加载\\">#</a> 1.6 懒执行与懒加载</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>懒加载延后网络与模块初始化，懒执行延后已经拥有代码中的计算工作</li>\\n<li>图片和组件可用原生 <code>loading=&quot;lazy&quot;</code>、动态 <code>import</code> 或 <code>IntersectionObserver</code></li>\\n<li>要在真正进入视口前留预加载距离，不能等用户看到空白才开始请求</li>\\n<li>首屏 <code>LCP</code> 资源不能懒加载；占位时要预留尺寸避免 <code>CLS</code></li>\\n<li>触发后要取消观察、处理失败与重试，避免重复加载和监听器泄漏</li>\\n</ul>\\n<p><strong>懒执行是推迟非必要计算，懒加载是推迟非关键资源请求，两者都是把首屏暂时用不到的工作往后放。</strong> 懒执行通常等定时器或事件触发后再运行耗时逻辑；懒加载则只在资源进入可视区域或即将进入时开始加载。图片可以先使用占位图，把真实地址放在自定义属性中，满足条件后再替换到 <code>src</code>。同样的思路也能用于视频，例如进入可视区域后再播放。</p>\\n</section>\\n<p><strong>懒执行</strong></p>\\n<ul>\\n<li>懒执行就是将某些逻辑延迟到使用时再计算。该技术可以用于首屏优化，对于某些耗时逻辑并不需要在首屏就使用的，就可以使用懒执行。懒执行需要唤醒，一般可以通过定时器或者事件的调用来唤醒</li>\\n</ul>\\n<p><strong>懒加载</strong></p>\\n<ul>\\n<li>懒加载就是将不关键的资源延后加载</li>\\n</ul>\\n<blockquote>\\n<p>懒加载的原理就是只加载自定义区域（通常是可视区域，但也可以是即将进入可视区域）内需要加载的东西。对于图片来说，先设置图片标签的 <code>src</code> 属性为一张占位图，将真实的图片资源放入一个自定义属性中，当进入自定义区域时，就将自定义属性替换为 <code>src</code> 属性，这样图片就会去下载资源，实现了图片懒加载</p>\\n</blockquote>\\n<ul>\\n<li>懒加载不仅可以用于图片，也可以使用在别的资源上。比如进入可视区域才开始播放视频等</li>\\n</ul>\\n<h3 id=\\"_1-7-文件优化\\"><a class=\\"header-anchor\\" href=\\"#_1-7-文件优化\\">#</a> 1.7 文件优化</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>图片优化同时看格式、像素尺寸、质量、响应式候选和加载优先级</li>\\n<li><code>JavaScript</code> 用路由拆包、动态导入和 <code>Tree Shaking</code> 减少下载与执行</li>\\n<li><code>CSS</code> 保证关键样式尽早到达，清理未使用规则，避免超大通用样式包</li>\\n<li>文本资源使用 <code>Brotli/gzip</code>，带哈希文件长期缓存，入口文档及时验证</li>\\n<li>字体要子集化、限制字重并设置合理 <code>font-display</code>，避免阻塞与布局跳动</li>\\n</ul>\\n<p><strong>文件优化的核心是减少资源体积和加载阻塞，让浏览器更快完成下载、解析与渲染。</strong> 图片要按场景选格式和尺寸，照片用 <code>JPEG</code>，图标优先考虑 <code>SVG</code>，支持时可用 <code>WebP</code>，移动端则请求裁剪后的图片。<code>CSS</code> 放在 <code>head</code>，脚本可放在 <code>body</code> 底部或使用 <code>defer</code>、<code>async</code>，耗时计算可交给 <code>Web Worker</code>。静态资源适合通过不同域名的 <code>CDN</code> 分发，避免携带主站 <code>Cookie</code>，但小图是否使用 <code>base64</code> 或雪碧图仍要结合请求和体积取舍。</p>\\n</section>\\n<p><strong>图片优化</strong></p>\\n<blockquote>\\n<p>对于如何优化图片，有 2 个思路</p>\\n</blockquote>\\n<ul>\\n<li>减少像素点</li>\\n<li>减少每个像素点能够显示的颜色</li>\\n</ul>\\n<p><strong>图片加载优化</strong></p>\\n<ul>\\n<li>不用图片。很多时候会使用到很多修饰类图片，其实这类修饰图片完全可以用 <code>CSS</code> 去代替。</li>\\n<li>对于移动端来说，屏幕宽度就那么点，完全没有必要去加载原图浪费带宽。一般图片都用 CDN 加载，可以计算出适配屏幕的宽度，然后去请求相应裁剪好的图片</li>\\n<li>小图使用 <code>base64</code>格式</li>\\n<li>将多个图标文件整合到一张图片中（雪碧图）</li>\\n<li>选择正确的图片格式：\\n<ul>\\n<li>对于能够显示 <code>WebP</code> 格式的浏览器尽量使用 <code>WebP</code> 格式。因为 <code>WebP</code> 格式具有更好的图像数据压缩算法，能带来更小的图片体积，而且拥有肉眼识别无差异的图像质量，缺点就是兼容性并不好</li>\\n<li>小图使用 <code>PNG</code>，其实对于大部分图标这类图片，完全可以使用 <code>SVG</code> 代替</li>\\n<li>照片使用 <code>JPEG</code></li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>其他文件优化</strong></p>\\n<ul>\\n<li><code>CSS</code>文件放在 <code>head</code> 中</li>\\n<li>服务端开启文件压缩功能</li>\\n<li>将 <code>script</code> 标签放在 <code>body</code> 底部，因为 <code>JS</code> 文件执行会阻塞渲染。当然也可以把 <code>script</code> 标签放在任意位置然后加上 <code>defer</code> ，表示该文件会并行下载，但是会放到 <code>HTML</code> 解析完成后顺序执行。对于没有任何依赖的 <code>JS</code>文件可以加上 <code>async</code> ，表示加载和渲染后续文档元素的过程将和 <code>JS</code> 文件的加载与执行并行无序进行。\\n执行 <code>JS</code>代码过长会卡住渲染，对于需要很多时间计算的代码</li>\\n<li>可以考虑使用 <code>Webworker</code>。<code>Webworker</code>可以让我们另开一个线程执行脚本而不影响渲染。</li>\\n</ul>\\n<p><strong>CDN</strong></p>\\n<blockquote>\\n<p>静态资源尽量使用 <code>CDN</code> 加载，由于浏览器对于单个域名有并发请求上限，可以考虑使用多个 <code>CDN</code> 域名。对于 <code>CDN</code> 加载静态资源需要注意 <code>CDN</code> 域名要与主站不同，否则每次请求都会带上主站的 <code>Cookie</code></p>\\n</blockquote>\\n<h3 id=\\"_1-8-其他\\"><a class=\\"header-anchor\\" href=\\"#_1-8-其他\\">#</a> 1.8 其他</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>构建侧关注产物体积与缓存命中，运行侧关注用户下载、解析和执行成本</li>\\n<li><code>CDN</code> 通过就近节点与边缘缓存降低往返，但要一起看命中率和回源策略</li>\\n<li>错误监控要覆盖同步错误、<code>Promise</code> 拒绝、资源加载失败，并用 <code>Source Map</code> 还原</li>\\n<li>第三方脚本常是隐藏长任务，应延迟加载、隔离并设置性能预算</li>\\n<li>所有优化都要用真实用户指标回归，不能只看一次本地跑分</li>\\n</ul>\\n<p><strong>其他性能工作主要落在构建优化和线上监控两端，既要减少无效代码，也要保证问题能够定位。</strong> 构建时可用 <code>production</code> 模式压缩代码，借助 <code>ES6</code> 模块做 <code>tree shaking</code>，再通过路由拆分实现按需加载，并给文件名加哈希以利用缓存。运行错误通常用 <code>window.onerror</code>、<code>Promise.catch</code> 或 <code>try...catch</code> 捕获，跨域脚本还要设置 <code>crossorigin</code>。线上代码被压缩后需要配套 <code>sourceMap</code> 辅助调试，捕获结果再上传服务器。</p>\\n</section>\\n<p><strong>使用 Webpack 优化项目</strong></p>\\n<ul>\\n<li>对于 <code>Webpack4</code>，打包项目使用 <code>production</code> 模式，这样会自动开启代码压缩</li>\\n<li>使用 <code>ES6</code> 模块来开启 <code>tree shaking</code>，这个技术可以移除没有使用的代码</li>\\n<li>优化图片，对于小图可以使用 <code>base64</code> 的方式写入文件中</li>\\n<li>按照路由拆分代码，实现按需加载</li>\\n<li>给打包出来的文件名添加哈希，实现浏览器缓存文件</li>\\n</ul>\\n<p><strong>监控</strong></p>\\n<blockquote>\\n<p>对于代码运行错误，通常的办法是使用 <code>window.onerror</code> 拦截报错。该方法能拦截到大部分的详细报错信息，但是也有例外</p>\\n</blockquote>\\n<ul>\\n<li>对于跨域的代码运行错误会显示 <code>Script error</code>. 对于这种情况我们需要给 <code>script</code> 标签添加 <code>crossorigin</code> 属性</li>\\n<li>对于某些浏览器可能不会显示调用栈信息，这种情况可以通过 <code>arguments.callee.caller</code> 来做栈递归</li>\\n<li>对于异步代码来说，可以使用 <code>catch</code> 的方式捕获错误。比如 <code>Promise</code> 可以直接使用 catch 函数，<code>async await</code> 可以使用 <code>try catch</code></li>\\n<li>但是要注意线上运行的代码都是压缩过的，需要在打包时生成 <code>sourceMap</code> 文件便于 <code>debug</code>。</li>\\n<li>对于捕获的错误需要上传给服务器，通常可以通过 <code>img</code> 标签的 <code>src</code>发起一个请求</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>Webpack</code> 项目切到 <code>production</code> 后产物已经压缩，负责人仍把“删掉未引用代码”归功于压缩器；你会怎样区分这两类优化？</strong></p>\\n<p>代码压缩主要缩短已有代码的表示形式，未使用模块代码的移除则依赖 <code>ES6</code> 模块带来的 <code>tree shaking</code>。应检查模块是否采用可静态分析的导入导出，并分析最终产物；仅设置 <code>production</code> 不能替代正确的模块组织。</p>\\n</li>\\n<li>\\n<p><strong>单页应用有 30 个路由，首页只用其中两个，构建负责人主张把全部代码打成一个压缩包以减少请求数，应该如何取舍？</strong></p>\\n<p>应按路由拆分代码，让首页只加载首屏依赖，其余路由在需要时按需加载。单包虽减少请求数量，却会把非首屏代码的传输和解析成本提前；拆分过细也会增加调度开销，因此边界应围绕实际路由依赖设计。</p>\\n</li>\\n<li>\\n<p><strong>发布后所有静态文件内容都变了，但文件名保持 <code>app.js</code>，用户偶发读取旧代码并出现接口不兼容，构建层该怎么修？</strong></p>\\n<p>应让构建产物文件名携带内容哈希，使内容变化时 <code>URL</code> 随之变化，从而避开旧缓存。未变化资源可继续复用缓存，变化资源会请求新地址；同时必须保证 <code>HTML</code> 引用与本次产物一致，否则仍可能出现版本错配。</p>\\n</li>\\n<li>\\n<p><strong>监控平台只收到跨域脚本的 <code>Script error</code>，没有文件位置和调用栈，而同源错误信息正常，页面侧优先检查什么？</strong></p>\\n<p>优先检查跨域脚本标签是否设置了 <code>crossorigin</code>，并确认资源响应允许相应的跨域访问，否则浏览器会隐藏详细错误。线上代码经过压缩时还要生成可用于定位的 <code>sourceMap</code>；缺少映射即使收到位置，也难还原源码。</p>\\n</li>\\n<li>\\n<p><strong>异步支付流程偶发报错，团队只接入 <code>window.onerror</code> 并用图片请求上报，为什么仍可能缺少关键故障？</strong></p>\\n<p>异步链路还应在 <code>Promise</code> 上使用 <code>catch</code>，并在 <code>async</code>/<code>await</code> 边界使用 <code>try catch</code>，不能只依赖全局错误入口。图片 <code>src</code> 可用于发送简短上报，但详细错误、环境信息及压缩代码定位仍需与服务端和 <code>sourceMap</code> 配合。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_2-首屏渲染优化\\"><a class=\\"header-anchor\\" href=\\"#_2-首屏渲染优化\\">#</a> 2 首屏渲染优化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先定位：<code>Performance</code> 看 <code>HTML</code> 响应结束到首次绘制之间卡在哪 —— 是等 <code>CSS</code>、等同步 <code>JS</code>、还是等接口</li>\\n<li>减体积：路由懒加载、<code>splitChunks</code>、按需引入组件库、<code>Coverage</code> 找未使用代码</li>\\n<li>加速度：<code>CDN</code> + <code>gzip</code>/<code>brotli</code>、关键资源 <code>preload</code>、接口域名 <code>preconnect</code>、<code>HTTP/2</code></li>\\n<li>改感知：骨架屏、关键 <code>CSS</code> 内联（首屏不再等一次网络往返）</li>\\n<li>⚠️ <strong>首屏主图绝不能加 <code>loading=&quot;lazy&quot;</code></strong> —— 懒加载要等布局算完才请求，直接拖垮 <code>LCP</code>；该配 <code>fetchpriority=&quot;high&quot;</code></li>\\n</ul>\\n<p><strong>首屏渲染优化本质上是让关键内容先到、先执行、先显示，非关键资源延后处理。</strong> 我一般会拆分 <code>CSS</code> 和 <code>JS</code>、内联首屏关键代码，并用异步加载、懒加载及 <code>preload</code>、<code>preconnect</code> 等资源提示减少阻塞。字体包要控制体积和加载时机，数据与资源则可结合 <code>LocalStorage</code>、<code>Service Worker</code> 或服务端缓存复用。首屏仍需兼顾感知体验，可使用骨架屏或过渡动画；执行时间较长的任务应拆分异步执行，必要时交给 <code>Web Worker</code>。</p>\\n</section>\\n<ul>\\n<li><code>css</code> / <code>js</code> 分割，使首屏依赖的文件体积最小，内联首屏关键 <code>css</code> / <code>js</code>；</li>\\n<li>非关键性的文件尽可能的 异步加载和懒加载，避免阻塞首页渲染；</li>\\n<li>使用<code>dns-prefetch</code> / <code>preconnect</code> / <code>prefetch</code> / preload等浏览器提供的资源提示，加快文件传输；</li>\\n<li>谨慎控制好 Web字体，一个大字体包足够让你功亏一篑\\n<ul>\\n<li>控制字体包的加载时机；</li>\\n<li>如果使用的字体有限，那尽可能只将使用的文字单独打包，能有效减少体积；\\n合理利用 <code>Localstorage</code> / <code>services worker</code> 等存储方式进行 数据与资源缓存</li>\\n</ul>\\n</li>\\n<li><strong>分清轻重缓急</strong>\\n<ul>\\n<li>重要的元素优先渲染；</li>\\n<li>视窗内的元素优先渲染</li>\\n</ul>\\n</li>\\n<li><strong>服务端渲染(SSR)</strong>:\\n<ul>\\n<li>减少首屏需要的数据量，剔除冗余数据和请求；</li>\\n<li>控制好缓存，对数据/页面进行合理的缓存；</li>\\n<li>页面的请求使用流的形式进行传递；</li>\\n</ul>\\n</li>\\n<li><strong>优化用户感知</strong>\\n<ul>\\n<li>利用一些动画 过渡效果，能有效减少用户对卡顿的感知；</li>\\n<li>尽可能利用 骨架屏(<code>Placeholder</code>) / <code>Loading</code> 等减少用户对白屏的感知；</li>\\n<li>动画帧数尽量保证在 <code>30帧</code> 以上，低帧数、卡顿的动画宁愿不要；</li>\\n<li>js 执行时间避免超过 <code>100ms</code>，超过的话就需要做\\n<ul>\\n<li>寻找可 缓存 的点</li>\\n<li>任务的 分割异步 或 <code>web worker</code> 执行</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>移动端的性能优化</strong></p>\\n<ol>\\n<li>首屏加载和按需加载，懒加载</li>\\n<li>资源预加载</li>\\n<li>图片压缩处理，使用<code>base64</code>内嵌图片</li>\\n<li>合理缓存<code>dom</code>对象</li>\\n<li>使用<code>touchstart</code>代替<code>click</code>（<code>click 300</code>毫秒的延迟）</li>\\n<li>利用<code>transform:translateZ(0)</code>，开启硬件GUP加速</li>\\n<li>不滥用web字体，不滥用<code>float</code>（布局计算消耗性能），减少<code>font-size</code>声明</li>\\n<li>使用<code>viewport</code>固定屏幕渲染，加速页面渲染内容</li>\\n<li>尽量使用事件代理，避免直接事件绑定</li>\\n</ol>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>资讯首页把所有 <code>CSS</code>、<code>JS</code> 都内联进 <code>HTML</code>，首屏不再等待外链请求，但文档体积明显膨胀；“内联首屏资源”为什么不能理解成全部内联？</strong></p>\\n<p>内联目标应是首屏关键 <code>CSS</code> 和必要 <code>JS</code>，而非整站资源；其余代码仍应分割并异步或懒加载。全部内联会放大 <code>HTML</code>，并削弱独立资源缓存的价值；关键内容的边界应按首屏真实依赖确定。</p>\\n</li>\\n<li>\\n<p><strong>移动首页首屏只展示头图和导航，却同步加载了后续十个区块的脚本与图片，工程上该怎样调整加载优先级？</strong></p>\\n<p>应优先传输和渲染视窗内的重要元素，把非关键脚本、图片及后续区块改为异步加载或懒加载。还可对确定的关键资源使用 <code>preload</code> 或连接提示；提示使用过多会争抢带宽，因此只能覆盖明确的首屏依赖。</p>\\n</li>\\n<li>\\n<p><strong>品牌团队要求首屏加载完整 <code>Web</code> 字体包，但页面实际只使用少量固定文案，前端和设计该如何达成折中？</strong></p>\\n<p>应控制字体包的加载时机，并在字符集合固定时只打包实际使用的文字，以减少首屏传输体积。若文案会动态变化，过度裁剪可能导致字符缺失或字体回退；此时应扩大字符范围或延后加载完整字体。</p>\\n</li>\\n<li>\\n<p><strong>首页接口数量不多，白屏仍很明显，性能记录显示一段主线程 <code>JS</code> 连续执行超过 <code>100ms</code>，下一步怎么拆？</strong></p>\\n<p>先定位长任务中可缓存的重复计算，再把可分割工作切成异步小任务；与 <code>DOM</code> 无关的重计算可考虑交给 <code>Web Worker</code>。拆分会增加调度和通信成本，依赖同步 <code>DOM</code> 状态的逻辑也不能直接搬入 <code>Worker</code>。</p>\\n</li>\\n<li>\\n<p><strong><code>SSR</code> 首页已经返回完整结构，但仍串行等待冗余数据后才开始发送，后端认为“用了 <code>SSR</code> 就足够快”，你会要求改什么？</strong></p>\\n<p>应减少首屏所需数据和请求，剔除非关键内容，并结合页面或数据缓存降低重复计算。响应还可采用流式传递，让已准备好的重要内容先到达；流式方案会增加服务端编排和客户端接续复杂度，不能替代依赖治理。</p>\\n</li>\\n<li>\\n<p><strong>产品希望用骨架屏掩盖白屏，技术负责人认为这等同于完成性能优化，应该怎样划清边界？</strong></p>\\n<p>骨架屏和过渡动画只能改善等待期间的用户感知，不能减少资源体积、阻塞任务或数据请求。应同时优化首屏依赖、缓存和渲染优先级；动画若帧率过低或自身引入长任务，反而会强化卡顿感。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_3-页面基础优化\\"><a class=\\"header-anchor\\" href=\\"#_3-页面基础优化\\">#</a> 3 页面基础优化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>HTML</code>：语义化、<code>img</code> 写 <code>width</code>/<code>height</code> 防 <code>CLS</code>、<code>script</code> 加 <code>defer</code>、避免同步阻塞</li>\\n<li><code>CSS</code>：关键样式内联、非关键异步加载（<code>media=&quot;print&quot; onload</code>）、避免超长选择器、删无用样式</li>\\n<li><code>JS</code>：减少主线程长任务、事件用委托、高频回调节流、避免布局抖动（读写分离）</li>\\n<li>资源：图片按设备出尺寸 + 现代格式、字体子集化 + <code>font-display: swap</code>、第三方脚本延后加载</li>\\n<li>缓存：<code>HTML</code> 用 <code>no-cache</code>、带 <code>hash</code> 的静态资源用 <code>max-age=31536000, immutable</code></li>\\n</ul>\\n<p><strong>页面基础优化就是减少阻塞、请求和资源体积，同时把可复用的内容缓存起来。</strong> <code>CSS</code> 通常放在 <code>head</code>，普通脚本放在 <code>body</code> 底部；影响首屏的高优先级脚本可以提前引入甚至内联。构建侧可通过 <code>tree shaking</code>、压缩和代码拆分删除冗余，并按浏览器能力提供 <code>ES6</code>、<code>ES5</code> 或动态 <code>polyfill</code>。图片应根据尺寸、质量和透明需求选择格式，静态资源带哈希放到 <code>CDN</code>，而页面、接口数据还可分别利用浏览器、服务器或数据缓存。</p>\\n</section>\\n<ul>\\n<li>引入位置: css 文件<code>&lt;head&gt;</code>中引入， js 文件<code>&lt;body&gt;</code>底部引入\\n<ul>\\n<li>影响首屏的，优先级很高的 js 也可以头部引入，甚至内联</li>\\n</ul>\\n</li>\\n<li><strong>减少请求</strong> (http 1.0 - 1.1)，合并请求，正确设置 http 缓存</li>\\n<li><strong>减少文件体积</strong>\\n<ul>\\n<li><strong>删除多余代码:</strong>\\n<ul>\\n<li><code>tree-shaking</code></li>\\n<li><code>UglifyJs</code></li>\\n<li><code>code-spliting</code></li>\\n</ul>\\n</li>\\n<li>混淆 / 压缩代码，开启 <code>gzip</code> 压缩；</li>\\n<li>多份编译文件按条件引入\\n<ul>\\n<li>针对现代浏览器直接给 ES6 文件，只针对低端浏览器引用编译后的 ES5 文件</li>\\n<li>可以利用<code>&lt;script type=&quot;module&quot;&gt; / &lt;script type=&quot;module&quot;&gt;</code>进行条件引入用</li>\\n</ul>\\n</li>\\n<li>动态 <code>polyfill</code>，只针对不支持的浏览器引入 <code>polyfill；</code></li>\\n</ul>\\n</li>\\n<li>图片优化:\\n<ul>\\n<li>根据业务场景，与UI探讨选择 合适质量，合适尺寸；</li>\\n<li>根据需求和平台，选择 合适格式，例如非透明时可用 <code>jpg</code>；非苹果端，使用 <code>webp</code>；</li>\\n<li>小图片合成 雪碧图，低于 <code>5K</code> 的图片可以转换成 <code>base64</code> 内嵌</li>\\n<li>合适场景下，使用 <code>iconfont</code> 或者 <code>svg</code>；</li>\\n</ul>\\n</li>\\n<li><strong>使用缓存</strong>\\n<ul>\\n<li><strong>浏览器缓存</strong>: 通过设置请求的过期时间，合理运用浏览器缓存；</li>\\n<li><strong>CDN缓存</strong>: 静态文件合理使用 <code>CDN</code> 缓存技术\\n<ul>\\n<li><code>HTML</code> 放于自己的服务器上；</li>\\n<li>打包后的图片 / <code>js</code> / <code>css</code> 等资源上传到 <code>CDN</code> 上，文件带上 <code>hash</code> 值；</li>\\n<li>由于浏览器对单个域名请求的限制，可以将资源放在多个不同域的 CDN 上，可以绕开该限制；</li>\\n</ul>\\n</li>\\n<li><strong>服务器缓存</strong>: 将不变的数据、页面缓存到 内存 或 远程存储(redis等) 上</li>\\n<li><strong>数据缓存</strong>: 通过各种存储将不常变的数据进行缓存，缩短数据的获取时间</li>\\n</ul>\\n</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>活动页把 <code>CSS</code> 放到 <code>body</code> 底部、把统计脚本同步放在 <code>head</code>，首屏先裸奔再停住；开发说“文件最终都会下载，位置没区别”，你怎么判断？</strong></p>\\n<p>位置会改变解析与渲染时序，不能只看文件是否最终下载。关键 <code>CSS</code> 应在 <code>head</code> 引入，普通 <code>JS</code> 放到 <code>body</code> 底部；真正影响首屏且优先级很高的脚本才考虑头部引入或内联，否则会阻塞页面解析并延迟首屏。</p>\\n</li>\\n<li>\\n<p><strong>一个旧版 <code>HTTP/1.1</code> 商城首页有几十个零散 <code>CSS</code>、<code>JS</code> 和小图标请求，你接手后会怎样落地基础优化？</strong></p>\\n<p>先按资源类型和更新频率盘点请求，合并适合共同发布的文件，并为稳定资源设置合理的浏览器缓存。小图可按场景合成雪碧图，低于约 <code>5K</code> 的资源可评估转成 <code>base64</code>；内联会增大宿主文件，频繁复用的图片不宜盲目处理。</p>\\n</li>\\n<li>\\n<p><strong>同一站点既要支持现代浏览器，又不能放弃少量只认旧语法的终端，构建产物应该统一下发编译后的 <code>ES5</code> 吗？</strong></p>\\n<p>不宜让所有终端都承担兼容产物的体积和解析成本，应按能力条件下发多份编译文件。现代浏览器优先获得 <code>ES6</code> 模块，旧终端再加载兼容构建和必要的 <code>polyfill</code>；代价是构建、缓存与回归矩阵都会更复杂。</p>\\n</li>\\n<li>\\n<p><strong>静态资源已经压缩并上传 <code>CDN</code>，但每次发版后仍有人下载旧 <code>JS</code>、有人请求新 <code>HTML</code> 配旧代码，你会从哪里排查？</strong></p>\\n<p>先检查 <code>HTML</code> 与带 <code>hash</code> 静态资源的缓存策略是否混用，并核对页面实际引用的文件名。<code>HTML</code> 通常由业务服务器及时更新，图片、<code>JS</code>、<code>CSS</code> 用内容 <code>hash</code> 上传 <code>CDN</code> 并长期缓存；若资源名不随内容变化，缓存越强越容易产生版本错配。</p>\\n</li>\\n<li>\\n<p><strong>设计师要求所有商品图无损导出，前端又想把每张图都转成 <code>base64</code> 来“减少请求”，移动端首屏明显变大时你怎么取舍？</strong></p>\\n<p>图片方案应同时按展示尺寸、可接受质量、透明需求和目标平台选择，不能只追求请求数。非透明图片可评估 <code>jpg</code>，支持条件合适时可用 <code>webp</code>，图标则考虑 <code>SVG</code> 或 <code>iconfont</code>；大图内联会放大页面文件，也失去独立缓存收益。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_4-性能优化方向\\"><a class=\\"header-anchor\\" href=\\"#_4-性能优化方向\\">#</a> 4 性能优化方向</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>三个层次：<strong>指标层</strong>（用什么衡量）、<strong>手段层</strong>（怎么改）、<strong>制度层</strong>（怎么不劣化）</li>\\n<li>指标层用 <strong>Core Web Vitals</strong>：<code>LCP</code>（首屏可见）、<code>INP</code>（交互响应，已取代 <code>FID</code>）、<code>CLS</code>（布局偏移），一律看 <strong><code>P75</code></strong></li>\\n<li>手段层按用户感知时间线分：加载 → 渲染 → 交互 → 运行时</li>\\n<li>制度层最容易被忽略：把<strong>包体积阈值写进 <code>CI</code></strong>、关键页面定时巡检、线上 <code>Web Vitals</code> 设告警</li>\\n<li>没有制度层，一次性专项优化的成果通常半年就被新需求吃光</li>\\n</ul>\\n<p><strong>前端性能优化可以分成工程化和细节两个方向，同时要用指标判断效果。</strong> 工程化解决构建、交付和监控等系统性问题，细节优化则处理页面关键路径上的具体瓶颈。简单来说，优化不能只靠一次人工排查，还要关注 <code>P75</code> 等指标，并在 <code>CI</code> 中设置性能红线。这样既能取得优化收益，也能避免后续迭代把性能重新拖回去。</p>\\n</section>\\n<blockquote>\\n<p>前端性能优化分为两个方向，一是工程化方向，另一个是细节方向</p>\\n</blockquote>\\n<h3 id=\\"_4-1-工程化方向\\"><a class=\\"header-anchor\\" href=\\"#_4-1-工程化方向\\">#</a> 4.1 工程化方向</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>设性能预算：入口包、路由包、图片和字体超过阈值就让 <code>CI</code> 告警或失败</li>\\n<li>构建阶段做代码分割、<code>Tree Shaking</code>、压缩、现代/兼容产物分层与依赖分析</li>\\n<li>分发阶段使用 <code>CDN</code>、内容哈希、长期缓存和 <code>Brotli/gzip</code></li>\\n<li>渲染架构按业务选择 <code>SSR/SSG/CSR</code>，并控制服务端耗时、水合和缓存成本</li>\\n<li>线上持续采集 <code>Web Vitals</code>，按版本、设备、网络和路由定位回归</li>\\n</ul>\\n<p><strong>工程化优化的核心，是把资源体积、加载链路和渲染方式统一纳入构建与交付流程。</strong> 资源侧可以用 <code>Gzip</code>、<code>Tree Shaking</code>、动态 <code>Import</code> 和拆包减少传输内容，图片则优先考虑 <code>WebP</code> 并做好兼容性嗅探。加载侧可结合 <code>CDN</code>、浏览器缓存、数据预取和减少重定向，渲染侧再按场景选择服务端渲染、预渲染或骨架图。具体手段不必全部启用，应根据项目瓶颈取舍。</p>\\n</section>\\n<ul>\\n<li>客户端Gzip离线包，服务器资源Gzip压缩。</li>\\n<li>JS瘦身，<code>Tree shaking</code>，<code>ES Module</code>，动态<code>Import</code>，动态<code>Polyfill</code></li>\\n<li>图片加载优化，<code>Webp</code>，考虑兼容性，可以提前加载一张图片，嗅探是否支持<code>Webp</code></li>\\n<li>服务端渲染，客户端预渲染</li>\\n<li><code>CDN</code>静态资源</li>\\n<li><code>Webpack Dll</code>，通用优先打包抽离，利用浏览器缓存</li>\\n<li>骨架图</li>\\n<li>数据预取，包括接口数据，和加载详情页图片</li>\\n<li>Webpack本身提供的优化，Base64，资源压缩，Tree shaking，拆包chunk</li>\\n<li>减少重定向</li>\\n</ul>\\n<h3 id=\\"_4-2-细节方向\\"><a class=\\"header-anchor\\" href=\\"#_4-2-细节方向\\">#</a> 4.2 细节方向</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>首屏资源用 <code>preload/preconnect</code> 提前发现，未来页面资源才考虑 <code>prefetch</code></li>\\n<li>脚本按依赖选择 <code>defer/async</code>，避免同步脚本阻塞解析</li>\\n<li>图片写尺寸、按设备输出候选并区分首屏优先与视口外懒加载</li>\\n<li><code>DOM</code> 更新批量读写，动画优先 <code>transform/opacity</code>，减少布局与绘制范围</li>\\n<li>细节优化先用 trace 证明瓶颈，微优化不能以可读性和正确性为代价</li>\\n</ul>\\n<p><strong>细节优化要盯住当前页面真正的阻塞点，再选择加载、缓存或渲染手段。</strong> 当前页关键资源可用 <code>preload</code> 提高请求优先级，未来页面可能用到的资源则用 <code>prefetch</code> 在浏览器空闲时加载，两者不能混用。脚本可按执行时机选择 <code>async</code> 或 <code>defer</code>，同时合理设置服务端缓存并减少首屏接口和不可视组件。涉及滚动或频繁更新时，还要减少 <code>DOM</code> 操作、重排重绘和渲染抖动。</p>\\n</section>\\n<ul>\\n<li>图片，图片占位，图片懒加载。 雪碧图</li>\\n<li>使用 <code>prefetch / preload</code> 预加载等新特性\\n<ul>\\n<li><code>Preload</code> 来告诉浏览器预先请求当前页需要的资源，从而提高这些资源的请求优先级。比如，对于那些本来请求优先级较低的关键请求，我们可以通过设置 <code>Preload</code> 来提升这些请求的优先级</li>\\n<li><code>Prefetch</code> 来告诉浏览器用户将来可能在其他页面（非本页面）可能使用到的资源，那么浏览器会在空闲时，就去预先加载这些资源放在 <code>http</code> 缓存内，最常见的 <code>dns-prefetch</code>。比如，当我们在浏览A页面，如果会通过A页面中的链接跳转到B页面，而B页面中我们有些资源希望尽早提前加载，那么我们就可以在A页面里添加这些资源 <code>Prefetch</code> ，那么当浏览器空闲时，就会去加载这些资源</li>\\n<li>所以，对于那些可能在当前页面使用到的资源可以利用 <code>Preload</code>，而对一些可能在将来的某些页面中被使用的资源可以利用 <code>Prefetch</code>。如果从加载优先级上看，<code>Preload</code> 会提升请求优先级；而<code>Prefetch</code>会把资源的优先级放在最低，当浏览器空闲时才去预加载</li>\\n</ul>\\n</li>\\n<li>服务器合理设置缓存策略</li>\\n<li><code>async</code>（加载完当前js立即执行）/ <code>defer</code>(所有资源加载完之后执行js)</li>\\n<li>减少<code>Dom</code>的操作，减少重排重绘</li>\\n<li>从客户端层面，首屏减少和客户端交互，合并接口请求</li>\\n<li>数据缓存</li>\\n<li>首页不加载不可视组件</li>\\n<li>防止渲染抖动，控制时序</li>\\n<li>减少组件层级</li>\\n<li>优先使用<code>Flex</code>布局</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>详情页把下一页推荐图设成 <code>preload</code>，却把当前首屏字体设成 <code>prefetch</code>；开发认为两者都是“提前下载”，这个配置错在哪里？</strong></p>\\n<p>两者表达的使用时机和优先级不同，不能互换。当前页确定需要且原本优先级不足的关键资源适合 <code>preload</code>，未来页面可能使用的资源适合 <code>prefetch</code>，后者通常在浏览器空闲时以较低优先级加载；滥用 <code>preload</code> 会争抢当前页带宽。</p>\\n</li>\\n<li>\\n<p><strong>资讯首页首屏只展示两个模块，代码却一次挂载十几个不可视组件并请求各自接口，你会怎样改造加载链路？</strong></p>\\n<p>首屏不应加载当前不可视组件，可将其渲染和数据请求延后，并配合图片占位与懒加载稳定页面。客户端交互能减少就减少，相关接口可在职责允许时合并；延后过度会让用户滚动后等待，因此触发距离和缓存策略需要结合访问路径调整。</p>\\n</li>\\n<li>\\n<p><strong>广告脚本要求尽早执行，但它不能阻塞正文解析；团队在 <code>async</code> 和 <code>defer</code> 之间争执，你会依据什么选？</strong></p>\\n<p>若脚本下载完成后即可独立执行且不依赖文档顺序，可考虑 <code>async</code>；若它依赖完整解析或与其他脚本存在执行顺序，则更适合 <code>defer</code>。两者都改变同步阻塞行为，但 <code>async</code> 的完成时机不稳定，存在依赖关系时容易引发竞态。</p>\\n</li>\\n<li>\\n<p><strong>列表页滚动时偶发跳动，录屏显示图片加载后内容下移，接口返回后组件又连续重排；你会怎样定位并止血？</strong></p>\\n<p>先固定图片容器或设置占位，避免资源到达后改变既有布局，再检查接口回填和组件挂载时序。将可合并的 <code>DOM</code> 更新批量执行，减少反复操作造成的重排重绘；占位尺寸若偏离真实比例，仍可能产生可见位移。</p>\\n</li>\\n<li>\\n<p><strong>路由入口既能给下一页资源加 <code>prefetch</code>，也能直接在首页加载下一页组件；产品要求跳转快但首页不能变慢，你怎么选？</strong></p>\\n<p>优先保留当前首屏资源预算，把确定属于未来页面的资源交给 <code>prefetch</code> 在空闲期处理，而不是直接挂载不可视组件。对高概率跳转路径可适度预取，但弱网、低命中率或资源过大时会浪费流量，需要结合缓存和真实访问路径收缩范围。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_5-长列表优化\\"><a class=\\"header-anchor\\" href=\\"#_5-长列表优化\\">#</a> 5 长列表优化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心手段是<strong>虚拟滚动</strong>：只渲染视口内的十几二十条 + 上下缓冲区，滚动时复用节点只换内容</li>\\n<li>收益是数量级的 —— 一万条数据的 <code>DOM</code> 节点从一万降到几十，首屏渲染和滚动帧率都天差地别</li>\\n<li>定高列表最简单（<code>总高度 = 条数 × 行高</code>，用除法直接算出起始索引）</li>\\n<li><strong>不定高</strong>要维护高度缓存 + 前缀和，用<strong>二分查找</strong>定位起始索引（<code>O(log n)</code>），并在渲染后回填真实高度</li>\\n<li>配套：数据层用 <code>shallowRef</code>/<code>Object.freeze</code> 跳过深度响应式、滚动回调用 <code>rAF</code> 节流、别在滚动中读 <code>getBoundingClientRect</code></li>\\n</ul>\\n<p><strong>长列表通常用虚拟滚动优化，只渲染视口附近的数据，并在滚动时复用节点。</strong> 定高列表可以根据总条数和行高直接计算占位高度与起始索引，实现相对简单。不定高列表需要缓存每项高度、维护前缀和，再用二分查找定位起始位置，并在渲染后回填真实高度。配套上可用 <code>shallowRef</code> 或 <code>Object.freeze</code> 避免深度响应式，用 <code>requestAnimationFrame</code> 节流滚动回调，并避免滚动中频繁读取布局信息。</p>\\n</section>\\n<h3 id=\\"vue-virtual-scroll-list优化长列表\\"><a class=\\"header-anchor\\" href=\\"#vue-virtual-scroll-list优化长列表\\">#</a> vue-virtual-scroll-list优化长列表</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>虚拟列表只渲染视口与缓冲区内元素，用一个占位容器保持完整滚动高度</li>\\n<li>定高列表可用 <code>scrollTop / itemHeight</code> 直接计算起始索引</li>\\n<li>组件滚动时复用少量节点并更新内容，<code>DOM</code> 数量从万级降到几十</li>\\n<li>缓冲区太小会快速滚动白屏，太大又会增加渲染成本</li>\\n<li>仍需稳定 <code>key</code>、轻量单项组件和节流后的滚动状态更新</li>\\n</ul>\\n<p><strong><code>vue-virtual-scroll-list</code> 的核心是只渲染可视区域内的 <code>DOM</code> 节点，用虚拟滚动承载海量数据。</strong> 不可见的数据会被暂时收起，因此页面中的实际节点数量不会随着数据总量一起增长。简单来说，它减少的是同时参与渲染的节点，而不是删减原始数据，适合数据很多、但用户一次只能看到少量内容的列表。</p>\\n</section>\\n<blockquote>\\n<p>虚拟列表的实现原理：只渲染可视区的 dom 节点，其余不可见的数据卷起来，只会渲染可视区域的 dom 节点，提高渲染性能及流畅性，优点是支持海量数据的渲染；</p>\\n</blockquote>\\n<p>github地址：<a href=\\"https://github.com/tangbc/vue-virtual-scroll-list\\">https://github.com/tangbc/vue-virtual-scroll-list</a></p>\\n<h3 id=\\"object-freeze优化长列表\\"><a class=\\"header-anchor\\" href=\\"#object-freeze优化长列表\\">#</a> Object.freeze优化长列表</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Object.freeze</code> 让对象不可增删改，适合加载后只展示、不再更新的大数据</li>\\n<li>在 <code>Vue 2</code> 中它可阻止对整棵对象递归安装 <code>getter/setter</code>，降低初始化成本</li>\\n<li>它是浅冻结，嵌套对象不会自动冻结；业务仍修改数据时会造成更新失效</li>\\n<li><code>Vue 3</code> 基于 <code>Proxy</code>，更常用 <code>markRaw</code>、<code>shallowRef</code> 或只替换顶层引用控制深度</li>\\n<li>冻结只能减少响应式开销，解决不了视口中真实 <code>DOM</code> 过多，仍需虚拟列表</li>\\n</ul>\\n<p><strong>对于不会再修改的大数组或对象，可以用 <code>Object.freeze()</code> 跳过 <code>Vue</code> 的响应式转换，从而降低初始化开销。</strong> 冻结后不能新增、删除或修改已有属性，<code>Vue</code> 也不会再为这些数据转换 <code>getter</code> 和 <code>setter</code>。它优化的是响应式处理，不会减少页面实际渲染的节点，所以数据量很大时仍可配合虚拟列表使用。</p>\\n</section>\\n<ul>\\n<li><code>Object.freeze()</code>方法可以冻结一个对象。一个被冻结的对象再也不能被修改；冻结了一个对象则不能向这个对象添加新的属性，不能删除已有属性，不能修改该对象已有属性的可枚举性、可配置性、可写性，以及不能修改已有属性的值。</li>\\n<li>对于<code>data()</code>或vuex中冻结的对象，vue不会做<code>getter</code>和<code>setter</code>的转换。因此对于一个不变的、大数据量的数组或Object数据来说，使用<code>Object.freeze()</code>可以有效地提升性能。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个 <code>Vue</code> 报表把数万条只读记录放进 <code>data()</code>，开发只禁止了界面编辑，却仍让框架转换全部字段；这和 <code>Object.freeze()</code> 等价吗？</strong></p>\\n<p>不等价，业务上“不编辑”并不会阻止 <code>Vue</code> 为普通对象转换 <code>getter</code> 和 <code>setter</code>。对确定不会变化的大数组或对象使用 <code>Object.freeze()</code>，可跳过这部分响应式转换开销；冻结后属性也无法增删改，后续若需要局部更新就不适用。</p>\\n</li>\\n<li>\\n<p><strong>权限审计页一次返回大批历史记录，数据只展示、不编辑，你会把 <code>Object.freeze()</code> 放在接口层、组件层还是模板里？</strong></p>\\n<p>应在数据进入 <code>data()</code> 或 <code>vuex</code> 的响应式管理前完成冻结，再把冻结后的引用交给页面展示。这样框架能够识别其不可变性并避免属性转换；若先完成深层响应式处理再冻结，前面的转换成本已经发生，优化价值会明显降低。</p>\\n</li>\\n<li>\\n<p><strong>产品临时要求长列表支持行内改备注，原方案冻结了整个数组；你会继续保留 <code>Object.freeze()</code> 吗？</strong></p>\\n<p>整份数据需要原地修改时不应继续冻结，否则属性写入、添加和删除都会受限。可将稳定的大数据保持冻结，把可编辑备注拆到独立的小型响应式映射中；这种拆分增加了数据合并与提交逻辑，需要保证记录标识稳定。</p>\\n</li>\\n<li>\\n<p><strong>页面接入 <code>Object.freeze()</code> 后首次加载变快，但筛选结果不再随字段修改刷新；你会先查什么？</strong></p>\\n<p>先确认筛选逻辑是否仍假设冻结对象能够触发 <code>Vue</code> 的响应式更新，再检查代码是否直接修改了被冻结的字段。冻结数据适合只读展示，变化应通过生成新数据并替换引用，或把可变状态单独维护；若业务依赖深层变更通知，冻结方案本身就选错了。</p>\\n</li>\\n<li>\\n<p><strong>团队准备同时上虚拟列表和 <code>Object.freeze()</code>，有人认为二者作用重复；面对数万条只读数据你怎么解释取舍？</strong></p>\\n<p>二者解决的成本不同：虚拟列表控制实际渲染的节点数量，<code>Object.freeze()</code> 减少大对象进入响应式系统时的转换。只读长列表可能同时受益，但冻结不能替代渲染裁剪，虚拟化也不会自动消除数据转换成本；是否叠加仍应依据瓶颈测量。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_6-卡顿问题解决\\"><a class=\\"header-anchor\\" href=\\"#_6-卡顿问题解决\\">#</a> 6 卡顿问题解决</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>本质：主线程被占住，来不及在 <strong>16.7ms</strong> 内产出下一帧</li>\\n<li>四类分别看 <code>Performance</code> 面板的颜色：黄色 <code>Scripting</code> 长条 = <strong><code>JS</code> 长任务</strong>、紫色 <code>Layout</code> 密集 = <strong>重排</strong>、绿色 <code>Paint</code> 块大 = <strong>重绘</strong>、<code>Memory</code> 只涨不降 = <strong>泄漏</strong></li>\\n<li>长任务（&gt;50ms）解法三级：<strong>能不算就不算</strong>（缓存/<code>computed</code>）→ <strong>分片</strong>（<code>requestIdleCallback</code>）→ <strong>搬进 <code>Worker</code></strong></li>\\n<li>⚠️ 最隐蔽的是<strong>布局抖动</strong>：循环里读 <code>offsetWidth</code> 再写样式 = 每轮强制同步布局；解法是<strong>读写分离</strong></li>\\n<li>动画只动 <code>transform</code>/<code>opacity</code>（走合成线程）；<code>will-change</code> 用完要移除，每层占 <code>宽×高×4</code> 字节显存</li>\\n<li>排查顺序：录一段 → 看哪个颜色的块最长 → 对号入座 → 改完再录一次对比</li>\\n</ul>\\n<p><strong>页面卡顿本质上是主线程被占用，无法及时完成下一帧，排查时要区分 <code>JavaScript</code> 长任务、重排、重绘和内存泄漏。</strong> 我一般先录制 <code>Performance</code>，根据黄色 <code>Scripting</code>、紫色 <code>Layout</code> 和绿色 <code>Paint</code> 的耗时定位问题；越用越卡则对比堆快照。长计算可缓存、分片或交给 <code>Web Worker</code>，循环中的布局读取和样式写入要分开，避免强制同步布局。动画优先改 <code>transform</code> 和 <code>opacity</code>，<code>will-change</code> 用完及时移除，修改后再录制一次验证。</p>\\n</section>\\n<p>卡顿的本质是<strong>主线程被占住，来不及在 16.7ms 内产出下一帧</strong>。排查要先分清是哪一类。</p>\\n<p><strong>四类卡顿及其定位方法：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>现象</th>\\n<th>原因</th>\\n<th><code>Performance</code> 面板特征</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>点击后半天没反应</td>\\n<td><strong><code>JS</code> 长任务</strong></td>\\n<td>黄色 <code>Scripting</code> 长条（&gt;50ms 会被标红）</td>\\n</tr>\\n<tr>\\n<td>滚动/动画掉帧</td>\\n<td><strong>重排（Layout）</strong></td>\\n<td>紫色 <code>Layout</code> 密集出现</td>\\n</tr>\\n<tr>\\n<td>大面积变化时卡</td>\\n<td><strong>重绘（Paint）</strong></td>\\n<td>绿色 <code>Paint</code> 块大</td>\\n</tr>\\n<tr>\\n<td>越用越卡</td>\\n<td><strong>内存泄漏</strong></td>\\n<td><code>Memory</code> 曲线只涨不降</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>① <code>JS</code> 长任务</strong></p>\\n<p>任何超过 50ms 的任务都会明显影响交互响应（<code>INP</code> 指标）。解法按优先级：</p>\\n<ul>\\n<li><strong>能不算就不算</strong> —— 结果缓存、<code>computed</code>、避免在渲染中做重计算</li>\\n<li><strong>分片</strong> —— 用 <code>requestIdleCallback</code> 或 <code>scheduler.yield</code> 把长任务切成小段，中间让出主线程</li>\\n<li><strong>搬走</strong> —— <code>Web Worker</code> 处理纯计算（数据聚合、加解密、大 <code>JSON</code> 解析）</li>\\n</ul>\\n<p><strong>② 布局抖动（layout thrashing）—— 最常见也最隐蔽</strong></p>\\n<pre class=\\"language-js\\"><code>// ❌ 每次循环都强制同步布局：写 → 读 → 写 → 读\\nitems.forEach((el) =&gt; { el.style.width = el.offsetWidth + 10 + &#39;px&#39; })\\n\\n// ✅ 读写分离\\nconst widths = items.map((el) =&gt; el.offsetWidth)     // 先全部读\\nitems.forEach((el, i) =&gt; { el.style.width = widths[i] + 10 + &#39;px&#39; })   // 再全部写\\n</code></pre>\\n<p>读取 <code>offsetWidth</code>/<code>scrollTop</code>/<code>getBoundingClientRect</code>/<code>getComputedStyle</code> 时，如果有待处理的样式变更，浏览器必须<strong>立刻重新计算布局</strong>才能给出准确值。在循环里读写交替，就是每轮都强制一次布局。<code>Performance</code> 面板会明确标出 &quot;Forced reflow&quot;。</p>\\n<p><strong>③ 动画掉帧</strong></p>\\n<p>只动 <code>transform</code> 和 <code>opacity</code>——它们能在合成线程完成，跳过布局和绘制。动 <code>left</code>/<code>top</code>/<code>width</code> 每帧都要重排。需要时用 <code>will-change</code> 提前提层，但<strong>动画结束要移除</strong>（每层占 <code>宽×高×4</code> 字节显存）。</p>\\n<p><strong>④ 内存泄漏导致的&quot;越用越卡&quot;</strong></p>\\n<p><code>SPA</code> 里页面不卸载，泄漏会累积。排查用堆快照对比，重点看 <code>Detached HTMLElement</code> 和闭包对象的增量。九成源于组件销毁时没清定时器、没解绑 <code>window</code> 上的监听。</p>\\n<p><strong>通用排查顺序</strong>：<code>Performance</code> 录一段 → 看哪个颜色的块最长 → 按上表对号入座 → 改完再录一次对比。凭感觉优化经常改错地方。</p>\\n<p>下面是更多说明：</p>\\n<ul>\\n<li>CSS动画效率比JS高，<code>css</code>可以用<code>GPU</code>加速，<code>3d</code>加速。如果非要用JS动画，可以用<code>requestAnimationFrame</code></li>\\n<li>批量进行DOM操作，固定图片容器大小，避免屏幕抖动</li>\\n<li>减少重绘重排</li>\\n<li>节流和防抖</li>\\n<li>减少临时大对象产生，利用对象缓存，主要是减少内存碎片</li>\\n<li>异步操作，<code>IntersectionObserver</code>，<code>PostMessage</code>，<code>RequestIdleCallback</code></li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>搜索页点击筛选后停顿明显，开发看到页面没动画就断定不是掉帧；<code>Performance</code> 里出现超过 <code>50ms</code> 的黄色长条，你怎么判断？</strong></p>\\n<p>这是主线程被 <code>JS</code> 长任务占用导致的交互延迟，即使没有动画也会表现为点击迟迟不响应。应展开黄色 <code>Scripting</code> 区段定位重计算、解析或渲染逻辑，再优先消除或缓存计算；只给按钮加加载态不会释放主线程。</p>\\n</li>\\n<li>\\n<p><strong>表格批量调整列宽时，循环里先写 <code>style.width</code> 又读 <code>offsetWidth</code>，数据一多就卡；你会怎样改这段代码？</strong></p>\\n<p>这是读写交替触发的布局抖动，应先集中读取所有 <code>offsetWidth</code> 并缓存，再统一写入新宽度。读取 <code>getBoundingClientRect</code>、<code>getComputedStyle</code> 等准确布局值也有相同风险；若其他代码在两阶段之间修改布局，缓存值可能失效，需要控制执行时序。</p>\\n</li>\\n<li>\\n<p><strong>数据聚合持续占用主线程，结果不能缓存；低端设备又不保证有充足空闲时间，你会在分片和 <code>Web Worker</code> 之间怎么选？</strong></p>\\n<p>需要持续推进且属于纯计算时，优先评估 <code>Web Worker</code>，把聚合、加解密或大 <code>JSON</code> 处理移出主线程。若任务必须频繁访问页面状态，则可用 <code>scheduler.yield</code> 或时间切片主动让出执行权；<code>requestIdleCallback</code> 依赖空闲窗口，繁忙页面上可能迟迟得不到机会。</p>\\n</li>\\n<li>\\n<p><strong>轮播动画只在大面积卡片移动时掉帧，<code>Performance</code> 里绿色 <code>Paint</code> 很大，代码每帧修改 <code>left</code>；你会如何止损？</strong></p>\\n<p>应把位移动画改为 <code>transform</code>，透明变化使用 <code>opacity</code>，尽量让合成线程处理并跳过布局与绘制。必要时可临时设置 <code>will-change</code> 提前创建图层，但动画结束要移除；图层会占用约 <code>宽×高×4</code> 字节显存，不能全站常驻。</p>\\n</li>\\n<li>\\n<p><strong>单页应用刚打开流畅，来回进入详情页几十次后越来越卡，<code>Memory</code> 曲线只涨不降；你会沿什么证据链排查？</strong></p>\\n<p>先录制内存变化并对比堆快照，重点观察 <code>Detached HTMLElement</code> 和闭包对象是否持续增加。随后检查组件销毁时是否清理定时器、解绑挂在 <code>window</code> 上的监听；仅减少临时对象只能缓解分配压力，无法修复仍被引用的泄漏。</p>\\n</li>\\n<li>\\n<p><strong>线上有人建议把所有动画改成 <code>CSS</code>，另有人坚持统一用 <code>requestAnimationFrame</code>；拖拽和纯视觉过渡分别怎么选？</strong></p>\\n<p>纯视觉过渡优先使用适合合成的 <code>CSS</code> 动画，通常能减少主线程上的逐帧工作。必须由手势或计算实时驱动时再用 <code>requestAnimationFrame</code>，并避免每帧触发布局和大面积绘制；<code>requestAnimationFrame</code> 只对齐刷新节奏，不会自动让昂贵逻辑变快。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_7-编码优化\\"><a class=\\"header-anchor\\" href=\\"#_7-编码优化\\">#</a> 7 编码优化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>循环与遍历：避免在循环里做重复计算（提到循环外）、避免多次链式遍历产生中间数组、高频存在性判断用 <code>Set</code> 而非 <code>includes</code></li>\\n<li><code>DOM</code>：批量操作用 <code>DocumentFragment</code>、读写分离避免布局抖动、事件用委托而非逐个绑定</li>\\n<li>计算：结果缓存（<code>computed</code>/<code>memo</code>/记忆化）、高频回调用防抖节流、大计算搬 <code>Worker</code></li>\\n<li>内存：及时解绑监听和定时器、<code>WeakMap</code>/<code>WeakSet</code> 存对象关联数据、避免闭包意外持有大对象</li>\\n<li>⚠️ 别为了微优化牺牲可读性 —— 几百条数据的规模上，<code>for</code> 和 <code>map</code> 的差别可以忽略，可读性优先</li>\\n</ul>\\n<p><strong>编码优化要优先处理会被频繁执行的循环、数据读取和 <code>DOM</code> 操作，同时避免为了细小收益破坏可读性。</strong> 循环里应减少遍历量、及时结束并缓存重复计算；常用的深层属性也可以先存到局部变量，减少作用域链和嵌套读取。操作页面时尽量合并更新，用 <code>DocumentFragment</code> 等方式批量处理，并通过事件委托减少大量监听。除此之外，还应控制 <code>DOM</code> 层级、选择器复杂度和 <code>cookie</code> 体积，但是否采用要结合真实热点判断。</p>\\n</section>\\n<blockquote>\\n<p>编码优化，指的就是 在代码编写时的，通过一些 最佳实践，提升代码的执行性能。通常这并不会带来非常大的收益，但这属于 程序猿的自我修养，而且这也是面试中经常被问到的一个方面，考察自我管理与细节的处理。</p>\\n</blockquote>\\n<p><strong>数据读取:</strong></p>\\n<ul>\\n<li>通过作用域链 / 原型链 读取变量或方法时，需要更多的耗时，且越长越慢</li>\\n<li>对象嵌套越深，读取值也越慢；</li>\\n<li>最佳实践\\n<ul>\\n<li>尽量在局部作用域中进行 变量缓存；</li>\\n<li>避免嵌套过深的数据结构，数据扁平化 有利于数据的读取和维护</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>循环</strong>: 循环通常是编码性能的关键点；</p>\\n<ul>\\n<li>代码的性能问题会再循环中被指数倍放大</li>\\n<li>最佳实践\\n<ul>\\n<li>尽可能 减少循环次数\\n<ul>\\n<li>减少遍历的数据量；</li>\\n<li>完成目的后马上结束循环</li>\\n</ul>\\n</li>\\n<li>避免在循环中执行大量的运算，避免重复计算，相同的执行结果应该使用缓存；</li>\\n<li>js 中使用 倒序循环 会略微提升性能；</li>\\n<li>尽量避免使用 for-in 循环，因为它会枚举原型对象，耗时大于普通循环；</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>条件流程性能</strong>: Map / Object &gt; switch &gt; if-else</p>\\n<pre class=\\"language-js\\"><code>// 使用 if-else\\nif(type === 1) {\\n\\n} else if (type === 2) {\\n\\n} else if (type === 3) {\\n\\n}\\n\\n// 使用 switch\\nswitch (type) {\\n\\tcase 1:\\n\\t\\tbreak;4\\n\\tcase 2:\\n\\t\\tbreak;\\n\\tcase 3:\\n\\t\\tbreak;\\n    default:\\n        break;\\n}\\n\\n// 使用 Map\\nconst map = new Map([\\n\\t[1, () =&gt; {}],\\n\\t[2, () =&gt; {}],\\n\\t[3, () =&gt; {}],\\n])\\nmap.get(type)()\\n\\n// 使用 Objext\\nconst obj = {\\n\\t1: () =&gt; {},\\n\\t2: () =&gt; {},\\n\\t3: () =&gt; {},\\n}\\nobj[type]()\\n</code></pre>\\n<p><strong>减少 cookie 体积</strong>: 能有效减少每次请求的体积和响应时间；</p>\\n<ul>\\n<li>去除不必要的 <code>cookie</code>；</li>\\n<li>压缩 <code>cookie</code> 大小；</li>\\n<li>设置 <code>domain</code> 与 过期时间；</li>\\n</ul>\\n<p><strong>dom 优化:</strong></p>\\n<ul>\\n<li>减少访问 dom 的次数，如需多次，将 dom 缓存于变量中；</li>\\n<li>减少重绘与回流:\\n<ul>\\n<li>多次操作合并为一次；</li>\\n<li>减少对计算属性的访问\\n<ul>\\n<li>例如 offsetTop， getComputedStyle 等</li>\\n<li>因为浏览器需要获取最新准确的值，因此必须立即进行重排，这样会破坏了浏览器的队列整合，尽量将值进行缓存使用；</li>\\n</ul>\\n</li>\\n<li>大量操作时，可将 dom 脱离文档流或者隐藏，待操作完成后再重新恢复；</li>\\n<li>使用DocumentFragment / cloneNode / replaceChild进行操作；</li>\\n</ul>\\n</li>\\n<li>使用事件委托，避免大量的事件绑定；</li>\\n</ul>\\n<p><strong>css 优化:</strong></p>\\n<ul>\\n<li>层级扁平，避免过于多层级的选择器嵌套；</li>\\n<li>特定的选择器 好过一层一层查找:  <code>.xxx-child-text{}</code> - 优于 <code>.xxx .child .text{}</code></li>\\n<li>减少使用通配符与属性选择器；</li>\\n<li>减少不必要的多余属性；</li>\\n<li>使用 动画属性实现动画，动画时脱离文档流，开启硬件加速，优先使用 css 动画；</li>\\n<li>使用 <code>&lt;link&gt;</code> 替代原生 <code>@import</code>；</li>\\n</ul>\\n<p><strong>html 优化:</strong></p>\\n<ul>\\n<li>减少 dom 数量，避免不必要的节点或嵌套</li>\\n<li>避免<code>&lt;img src=&quot;&quot; /&gt;</code>空标签，能减少服务器压力，因为 src 为空时，浏览器仍然会发起请求\\n<ul>\\n<li>IE 向页面所在的目录发送请求；</li>\\n<li>Safari、Chrome、Firefox 向页面本身发送请求；</li>\\n<li>Opera 不执行任何操作。</li>\\n</ul>\\n</li>\\n<li>图片提前 指定宽高 或者 脱离文档流，能有效减少因图片加载导致的页面回流；</li>\\n<li>语义化标签 有利于 SEO 与浏览器的解析时间；</li>\\n<li>减少使用 table 进行布局，避免使用<code>&lt;br /&gt;</code>与<code>&lt;hr /&gt;</code></li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>热路径里用 <code>for-in</code> 遍历数组，开发认为它和普通循环只差写法；当对象原型上还有扩展属性时，你会指出什么风险？</strong></p>\\n<p><code>for-in</code> 会枚举可枚举属性，并可能沿原型链拿到额外成员，不适合按数组元素处理的热点循环。应换成边界明确的普通循环，并在满足条件后立即结束；倒序循环即使可能略有收益，也不应掩盖枚举语义错误。</p>\\n</li>\\n<li>\\n<p><strong>一个批量校验页面对同一深层配置和计算结果反复读取数万次，你会怎样落地编码层优化？</strong></p>\\n<p>把稳定的深层值和重复计算结果缓存到当前局部作用域，缩短作用域链、原型链和对象嵌套读取路径。还应减少参与遍历的数据量，并在找到目标后立即退出；缓存只适用于值在循环期间不变，否则会引入过期数据。</p>\\n</li>\\n<li>\\n<p><strong>路由分发只有三个分支时，有人要求强制把 <code>if-else</code> 改成 <code>Map</code>，理由是性能排序更高；你接受吗？</strong></p>\\n<p>不应脱离分支规模和可读性机械替换，少量条件下实际收益未必构成工程理由。分支持续增长、键到处理器的映射稳定时，<code>Map</code> 或对象更便于直接查找和维护；还要处理未命中项，直接调用 <code>map.get(type)()</code> 可能抛错。</p>\\n</li>\\n<li>\\n<p><strong>后台一次新增上千个节点，每个节点都绑定点击事件并反复插入文档，操作时明显卡顿；你会怎么重构？</strong></p>\\n<p>先用 <code>DocumentFragment</code> 等方式在文档外批量构建，再一次性插入，减少重复回流和重绘。点击处理可提升到稳定父节点做事件委托，避免大量独立监听；若子节点事件需要阻止冒泡或行为差异很大，委托逻辑会更复杂。</p>\\n</li>\\n<li>\\n<p><strong>页面滚动时频繁读取 <code>getComputedStyle</code>，同时修改元素类名，<code>Performance</code> 出现密集重排；排查时你会改哪条调用链？</strong></p>\\n<p>应把布局与计算样式读取集中到写操作之前，并将结果缓存供本轮逻辑复用，再批量修改类名或样式。浏览器为返回最新准确值，可能被迫立即完成布局；若读取依赖刚写入的结果，就需拆分到明确的后续帧，而不是继续交替执行。</p>\\n</li>\\n<li>\\n<p><strong>一个营销页用多层选择器、<code>@import</code> 和大量无意义容器，团队只想优化 <code>JS</code>；你会怎样说明相邻层面的成本？</strong></p>\\n<p>编码优化不只发生在 <code>JS</code>，过深的 <code>DOM</code> 和选择器层级也会增加解析、匹配与布局维护成本。应减少不必要节点和嵌套，使用更明确的选择器，并以 <code>&lt;link&gt;</code> 替代 <code>@import</code>；语义和样式边界不能为了扁平化被破坏。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_8-如何根据chrome的timing优化\\"><a class=\\"header-anchor\\" href=\\"#_8-如何根据chrome的timing优化\\">#</a> 8 如何根据chrome的timing优化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Network</code> 面板的 <code>Timing</code> 分段：<code>Queueing</code>（排队）→ <code>Stalled</code> → <code>DNS</code> → <code>Initial connection</code> → <code>SSL</code> → <code>Request sent</code> → <strong><code>Waiting (TTFB)</code></strong> → <code>Content Download</code></li>\\n<li><strong><code>Stalled</code> 长</strong> = 请求在排队 → <code>HTTP/1.1</code> 同域并发只有 6 个，升 <code>HTTP/2</code> 或减少首屏资源数</li>\\n<li><strong><code>DNS</code>/<code>Connection</code>/<code>SSL</code> 长</strong> = 建连成本高 → 收敛域名、用 <code>preconnect</code> 提前握手</li>\\n<li><strong><code>TTFB</code> 长</strong> = 服务端慢（大头在后端），前端能做的是让它不阻塞首屏（骨架屏 + 异步加载）</li>\\n<li><strong><code>Content Download</code> 长</strong> = 响应体太大 → 压缩（<code>gzip</code>/<code>br</code>）、分页、精简字段</li>\\n</ul>\\n<p><strong>根据 <code>Chrome</code> <code>Timing</code> 优化，关键是先看耗时落在哪个阶段，再针对瓶颈处理。</strong> <code>Stalled</code> 长通常说明请求排队，可减少首屏资源或升级 <code>HTTP/2</code>；<code>DNS</code>、连接和 <code>SSL</code> 长，则应收敛域名并使用 <code>preconnect</code>。<code>TTFB</code> 长主要是服务端响应慢，前端可用骨架屏和异步加载降低阻塞感。<code>Content Download</code> 长说明响应体偏大，可采用 <code>gzip</code>、<code>br</code>、分页或精简字段。</p>\\n</section>\\n<h3 id=\\"_8-1-性能优化api\\"><a class=\\"header-anchor\\" href=\\"#_8-1-性能优化api\\">#</a> 8.1 性能优化API</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>PerformanceObserver</code> 适合持续监听 <code>LCP</code>、长任务、资源与布局偏移等条目</li>\\n<li><code>performance.mark/measure</code> 用于给业务流程打点，测量自定义阶段耗时</li>\\n<li><code>performance.getEntriesByType(\'resource\')</code> 可读取资源时序，但跨源详细数据需要 <code>Timing-Allow-Origin</code></li>\\n<li><code>requestAnimationFrame</code> 对齐下一次绘制，<code>requestIdleCallback</code> 只适合可延后的低优先级工作</li>\\n<li>采集必须控制采样、批量上报并避免监控代码自己制造长任务</li>\\n</ul>\\n<p><strong>性能优化 <code>API</code> 主要用来记录加载时序、观察页面变化，并把耗时任务移出主线程。</strong> <code>performance.now()</code> 提供相对页面加载时刻的高精度时间，但不一定适合直接衡量单页应用的完整生命周期。首屏判断可结合 <code>load</code>、<code>DOMContentLoaded</code> 和图片 <code>onload</code>，调度任务则可用 <code>requestAnimationFrame</code> 或 <code>requestIdleCallback</code>。页面变化可交给 <code>IntersectionObserver</code>、<code>MutationObserver</code> 观察，重计算任务可放进 <code>Web Worker</code>。</p>\\n</section>\\n<ul>\\n<li><code>Performance</code>。<code>performance.now()</code>与<code>new Date()</code>区别，它是高精度的，且是相对时间，相对于页面加载的那一刻。但是不一定适合单页面场景</li>\\n<li><code>window.addEventListener(&quot;load&quot;, &quot;&quot;);</code> <code>window.addEventListener(&quot;domContentLoaded&quot;, &quot;&quot;);</code></li>\\n<li><code>Img</code>的<code>onload</code>事件，监听首屏内的图片是否加载完成，判断首屏事件</li>\\n<li><code>RequestFrameAnmation</code> 和 <code>RequestIdleCallback</code></li>\\n<li><code>IntersectionObserver</code>、<code>MutationObserver</code>，<code>PostMessage</code></li>\\n<li><code>Web Worker</code>，耗时任务放在里面执行</li>\\n</ul>\\n<h3 id=\\"_8-2-检测工具\\"><a class=\\"header-anchor\\" href=\\"#_8-2-检测工具\\">#</a> 8.2 检测工具</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Lighthouse</code> 提供可复现的实验室诊断，适合发现机会但不代表真实用户分布</li>\\n<li><code>DevTools Performance</code> 用火焰图定位主线程长任务、布局、绘制和事件时序</li>\\n<li><code>Network</code> 看优先级、缓存、连接与传输，<code>Coverage</code> 看首屏未使用代码</li>\\n<li>包分析器用于定位重复依赖和异常大模块，不直接代表运行时性能</li>\\n<li>线上 <code>RUM</code> 才能按设备、网络、路由和版本判断优化是否真实生效</li>\\n</ul>\\n<p><strong>性能检测要把本地诊断和线上真实数据结合起来，单看一个工具很容易误判。</strong> <code>Chrome DevTools</code> 可从 <code>Performance</code>、<code>Network</code> 和 <code>Coverage</code> 定位长任务、请求瓶颈及未使用代码，<code>Lighthouse</code>、<code>PageSpeed</code> 更适合做可复现的实验室检查。<code>JSPerf</code> 可用于比较特定代码写法，但局部跑分不等于页面整体更快。最终仍要结合线上 <code>RUM</code>，按设备、网络、路由和版本确认优化是否真实生效。</p>\\n</section>\\n<ul>\\n<li><code>Chrome Dev Tools</code></li>\\n<li><code>Page Speed</code></li>\\n<li><code>Jspref</code></li>\\n</ul>\\n<h3 id=\\"_8-3-前端指标\\"><a class=\\"header-anchor\\" href=\\"#_8-3-前端指标\\">#</a> 8.3 前端指标</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>LCP</code> 衡量最大主要内容出现时间，优先排查服务端、关键资源和主图优先级</li>\\n<li><code>INP</code> 衡量交互全过程的高分位延迟，重点看事件处理、主线程排队与下一帧呈现</li>\\n<li><code>CLS</code> 衡量意外布局偏移，图片尺寸、动态插入和字体替换是高频来源</li>\\n<li>核心指标看真实用户 <code>P75</code>，并按移动/桌面分别统计</li>\\n<li>指标必须关联页面版本和业务动作，否则只能知道“慢”，不知道改哪里</li>\\n</ul>\\n<p><strong>前端性能指标既要看页面关键阶段的耗时，也要关注真实用户的加载、交互和视觉稳定体验。</strong> 可通过 <code>performance.timing</code> 计算 <code>DNS</code>、<code>TCP</code>、请求、<code>DOM</code> 解析、白屏、<code>DOMContentLoaded</code> 和 <code>load</code> 时间，并结合内存占用排查问题。现代体验指标可重点观察 <code>LCP</code>、<code>INP</code>、<code>CLS</code> 的真实用户 <code>P75</code>。发现请求耗时后，可优先使用缓存、压缩、拆包和懒加载；域名较多时再考虑 <code>dns-prefetch</code>，避免把预解析当成通用方案。</p>\\n</section>\\n<p><img src=\\"https://s.poetries.top/images/image-20210307184052955.png\\" alt=\\"image-20210307184052955\\"></p>\\n<pre class=\\"language-js\\"><code>window.onload = function(){\\n    setTimeout(function(){\\n        let t = performance.timing\\n        console.log(&#39;DNS查询耗时 ：&#39; + (t.domainLookupEnd - t.domainLookupStart).toFixed(0))\\n        console.log(&#39;TCP链接耗时 ：&#39; + (t.connectEnd - t.connectStart).toFixed(0))\\n        console.log(&#39;request请求耗时 ：&#39; + (t.responseEnd - t.responseStart).toFixed(0))\\n        console.log(&#39;解析dom树耗时 ：&#39; + (t.domComplete - t.domInteractive).toFixed(0))\\n        console.log(&#39;白屏时间 ：&#39; + (t.responseStart - t.navigationStart).toFixed(0))\\n        console.log(&#39;domready时间 ：&#39; + (t.domContentLoadedEventEnd - t.navigationStart).toFixed(0))\\n        console.log(&#39;onload时间 ：&#39; + (t.loadEventEnd - t.navigationStart).toFixed(0))\\n\\n        if(t = performance.memory){\\n            console.log(&#39;js内存使用占比 ：&#39; + (t.usedJSHeapSize / t.totalJSHeapSize * 100).toFixed(2) + &#39;%&#39;)\\n        }\\n    })\\n}\\n</code></pre>\\n<p><strong>DNS预解析优化</strong></p>\\n<blockquote>\\n<p>dns解析是很耗时的，因此如果解析域名过多，会让首屏加载变得过慢，可以考虑dns-prefetch优化</p>\\n</blockquote>\\n<p><code>DNS Prefetch</code> 应该尽量的放在网页的前面，推荐放在 后面。具体使用方法如下：</p>\\n<pre class=\\"language-html\\"><code>&lt;meta http-equiv=&quot;x-dns-prefetch-control&quot; content=&quot;on&quot;&gt;\\n&lt;link rel=&quot;dns-prefetch&quot; href=&quot;//www.zhix.net&quot;&gt;\\n&lt;link rel=&quot;dns-prefetch&quot; href=&quot;//api.share.zhix.net&quot;&gt;\\n&lt;link rel=&quot;dns-prefetch&quot; href=&quot;//bdimg.share.zhix.net&quot;&gt;\\n</code></pre>\\n<p><strong>request请求耗时</strong></p>\\n<ul>\\n<li>不请求，用<code>cache</code>（最好的方式就是尽量引用公共资源，同时设置缓存，不去重新请求资源，也可以运用PWA的离线缓存技术，可以帮助wep实现离线使用）</li>\\n<li>前端打包时压缩</li>\\n<li>服务器上的<code>zip</code>压缩</li>\\n<li>图片压缩（比如<code>tiny</code>），使用<code>webp</code>等高压缩比格式</li>\\n<li>把过大的包，拆分成多个较少的包，防止单个资源耗时过大</li>\\n<li>同一时间针对同一域名下的请求有一定数量限制，超过限制数目的请求会被阻塞。如果资源来自于多个域下，可以增大并行请求和下载速度</li>\\n<li>延迟、异步、预加载、懒加载</li>\\n<li>对于非首屏的资源，可以使用 <code>defer</code> 或 <code>async</code> 的方式引入</li>\\n<li>也可以按需加载，在逻辑中，只有执行到时才做请求</li>\\n<li>对于多屏页面，滚动时才动态载入图片</li>\\n</ul>\\n<p><strong>解析dom树耗时</strong></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>首页白屏明显，但监控只显示 <code>onload</code> 很慢，产品就认定是接口拖累；仅凭 <code>performance.timing</code> 里的哪个差值能反驳或支持他？</strong></p>\\n<p>不能只凭 <code>onload</code> 判断接口慢，应拆看 <code>responseStart - navigationStart</code>、<code>responseEnd - responseStart</code> 与 <code>loadEventEnd - navigationStart</code>。前两项分别近似反映白屏前等待和响应接收阶段，若主要耗时落在资源加载或事件完成阶段，接口并非唯一原因；这些旧式时间点也不足以还原全部用户视觉体验。</p>\\n</li>\\n<li>\\n<p><strong>活动页首屏引用了十几个不同业务域名，瀑布图中 <code>DNS</code> 时间累计很高，你会怎样落地 <code>dns-prefetch</code>，而不是把所有域名都预解析？</strong></p>\\n<p>只对首屏即将访问且稳定的外部域名，在文档前部加入 <code>x-dns-prefetch-control</code> 和对应的 <code>&lt;link rel=&quot;dns-prefetch&quot;&gt;</code>。应先按瀑布图筛出真正存在解析耗时的关键域名，非首屏或低概率域名继续按需解析；预解析过多会产生无效网络工作，也无法改善服务器响应耗时。</p>\\n</li>\\n<li>\\n<p><strong>新闻站把图片、脚本拆到多个域名以提高并行下载，后来基础设施升级了传输协议，前端还要继续扩大域名数量吗？</strong></p>\\n<p>不能把增加域名当成永久策略，它主要用于缓解同域请求数量受限时的阻塞。协议与连接复用条件变化后，应重新观察请求排队、连接建立和下载耗时，再决定是否保留域名拆分；域名越多还会增加 <code>DNS</code> 与连接成本，结论必须以当前瀑布图为准。</p>\\n</li>\\n<li>\\n<p><strong>线上多屏商品页滚动卡顿，瀑布图显示首屏阶段已下载全部大图，同时 <code>request</code> 耗时很高，你会先改图片格式、压缩包，还是加载时机？</strong></p>\\n<p>应先阻止非首屏图片在初始化阶段请求，改为滚动接近可视区域时动态加载，再压缩图片并采用 <code>WebP</code> 等高压缩比格式。这样同时减少首屏请求数和传输体积；若首屏图片本身仍大，再继续压缩，但懒加载会带来占位布局和触发时机管理成本。</p>\\n</li>\\n<li>\\n<p><strong>一个管理后台把主包拆成大量小包后，单包下载更快，但首屏总耗时反而上升，构建负责人坚持继续细拆，你如何判断该停在哪里？</strong></p>\\n<p>单个资源变小不代表整体更快，应结合瀑布图检查同域请求是否出现阻塞，以及新增请求是否推迟关键资源。优先缓存公共资源、压缩产物，并只对非首屏代码做异步或按需加载；拆包过细会增加调度与请求成本，最终边界应由首屏关键链路而非文件大小决定。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_9-vue性能优化\\"><a class=\\"header-anchor\\" href=\\"#_9-vue性能优化\\">#</a> 9 Vue性能优化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>编码层：<code>v-for</code> 用稳定 <code>key</code>、<code>v-if</code>/<code>v-show</code> 按切换频率选、<code>computed</code> 缓存重计算、<code>v-if</code> 与 <code>v-for</code> 不同元素</li>\\n<li>数据层：纯展示的大数据用 <code>Object.freeze</code>（<code>Vue 2</code>）或 <code>shallowRef</code>/<code>markRaw</code>（<code>Vue 3</code>）<strong>跳过深度响应式劫持</strong></li>\\n<li>渲染层：长列表虚拟滚动、大组件拆小让更新局部化、<code>v-once</code>/<code>v-memo</code> 跳过静态部分</li>\\n<li>加载层：路由和组件懒加载、按需引入 <code>UI</code> 库、<code>bundle-analyzer</code> 找体积大头</li>\\n<li>销毁层：定时器/监听/连接必须清理 —— <code>SPA</code> 里泄漏会累积成&quot;越用越卡&quot;</li>\\n</ul>\\n<p><strong><code>Vue</code> 性能优化可以从加载、编码、渲染、缓存和用户体验五个层面处理，其中还要关注如何跳过不必要的响应式劫持。</strong> 数据不需要深度响应时，可考虑 <code>Object.freeze</code> 或 <code>shallowRef</code>，减少代理、依赖收集和后续更新成本。它们只适合明确不会修改或只需浅层响应的数据；如果业务依赖深层属性变化，就不能为了性能直接套用。</p>\\n</section>\\n<h3 id=\\"_9-1-vue首屏加载优化有哪些方案么\\"><a class=\\"header-anchor\\" href=\\"#_9-1-vue首屏加载优化有哪些方案么\\">#</a> 9.1 vue首屏加载优化有哪些方案么</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>路由级动态导入拆开非首屏页面，组件库与重依赖按需加载</li>\\n<li>关键资源走 <code>preload/preconnect</code>，静态资源压缩、哈希缓存并由 <code>CDN</code> 分发</li>\\n<li>首屏数据减少串行瀑布，能并行就并行，稳定内容可在服务端或构建期生成</li>\\n<li>大组件延后挂载，但首屏主图和主要内容不能误用懒加载</li>\\n<li>用 <code>Coverage</code>、包分析和真实 <code>LCP/INP</code> 验证，而不是只看打包总大小</li>\\n</ul>\\n<p><strong><code>Vue</code> 首屏优化本质上是减少初次下载和执行的代码，并让关键资源与页面内容更早到达。</strong> 我一般会先做路由懒加载、异步组件和第三方库按需加载，再配合 <code>CDN</code> 与 <code>gzip</code> 缩小传输成本。关键资源可用 <code>preload</code> 提高下载优先级，后续页面资源则适合用 <code>prefetch</code>；需要更早输出内容时可考虑 <code>SSR</code>。<code>Service Worker</code> 更偏向缓存和再次访问优化，不能替代首次加载治理。</p>\\n</section>\\n<ul>\\n<li><code>Vue-Router</code>路由懒加载（利用<code>Webpack</code>的代码切割）</li>\\n<li>使用<code>CDN</code>加速，将通用的库从<code>vendor</code>进行抽离</li>\\n<li><code>Nginx</code>的<code>gzip</code>压缩</li>\\n<li><code>Vue</code>异步组件</li>\\n<li>服务端渲染<code>SSR</code></li>\\n<li>如果使用了一些<code>UI</code>库，采用按需加载</li>\\n<li><code>Webpack</code>开启<code>gzip</code>压缩</li>\\n<li><code>Service Worker</code>缓存文件处理</li>\\n<li>使用<code>link</code>标签的<code>rel</code>属性设置   <code>prefetch</code>（这段资源将会在未来某个导航或者功能要用到，但是本资源的下载顺序权重比较低，<code>prefetch</code>通常用于加速下一次导航）、<code>preload</code>（<code>preload</code>将会把资源得下载顺序权重提高，使得关键数据提前下载好，优化页面打开速度）</li>\\n</ul>\\n<h3 id=\\"_9-2-编码阶段\\"><a class=\\"header-anchor\\" href=\\"#_9-2-编码阶段\\">#</a> 9.2 编码阶段</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>列表使用稳定唯一 <code>key</code>，避免无意义节点复用或整片重建</li>\\n<li>大数据按使用方式选择 <code>shallowRef/markRaw</code>，不要默认深度响应式</li>\\n<li>稳定子树用 <code>v-once</code>，条件稳定的大块内容可评估 <code>v-memo</code></li>\\n<li><code>computed</code> 用于可缓存派生值，模板和频繁事件里避免重复重计算</li>\\n<li>组件拆分要服务更新边界，层级过深和碎片过多同样会增加成本</li>\\n</ul>\\n<p><strong>编码阶段要尽量缩小响应式数据、虚拟节点对比和重复渲染的范围。</strong> 不参与视图更新的数据不要随意放进 <code>data</code>，列表要提供唯一 <code>key</code>，批量事件可用事件代理，避免产生过多监听。页面切换频繁时可用 <code>keep-alive</code> 缓存组件，路由和第三方模块则按需加载。长列表、图片和高频交互分别考虑动态加载、懒加载以及防抖或节流；<code>v-if</code> 与 <code>v-show</code> 仍要结合切换频率取舍。</p>\\n</section>\\n<ul>\\n<li>尽量减少<code>data</code>中的数据，<code>data</code>中的数据都会增加<code>getter</code>和<code>setter</code>，会收集对应的<code>watcher</code>；</li>\\n<li>如果需要使用<code>v-for</code>给每项元素绑定事件时使用事件代理；</li>\\n<li><code>SPA</code> 页面采用<code>keep-alive</code>缓存组件；</li>\\n<li>在更多的情况下，使用<code>v-if</code>替代<code>v-show</code>；</li>\\n<li><code>key</code>保证唯一；</li>\\n<li>使用路由懒加载、异步组件；</li>\\n<li>防抖、节流；</li>\\n<li>第三方模块按需导入；</li>\\n<li>长列表滚动到可视区域动态加载；</li>\\n<li>图片懒加载；</li>\\n</ul>\\n<h3 id=\\"_9-3-用户体验\\"><a class=\\"header-anchor\\" href=\\"#_9-3-用户体验\\">#</a> 9.3 用户体验：</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>骨架屏和渐进展示只能改善感知，不能掩盖真实的长任务和网络瀑布</li>\\n<li>路由切换立即给反馈，长操作展示进度并允许取消或安全重试</li>\\n<li>图片、广告和异步内容提前预留空间，避免用户点击目标突然移动</li>\\n<li>低端设备与弱网要保留核心能力，动效和非关键装饰可降级</li>\\n<li>加载态、空态、失败态和离线态都应有明确下一步，而不是永久转圈</li>\\n</ul>\\n<p><strong>用户体验优化既要降低真实等待成本，也要让暂时无法消除的等待变得可感知、可预期。</strong> 首屏内容尚未就绪时可以使用骨架屏，避免页面长时间空白；符合场景时也可通过 <code>PWA</code> 改善缓存和访问体验。客户端缓存、服务端缓存以及服务端 <code>gzip</code> 能继续减少资源获取成本，但骨架屏只是等待反馈，不能代替这些实际性能优化。</p>\\n</section>\\n<ul>\\n<li>骨架屏；</li>\\n<li><code>PWA</code>；</li>\\n<li>还可以使用缓存(客户端缓存、服务端缓存)优化、服务端开启<code>gzip</code>压缩等。</li>\\n</ul>\\n<h3 id=\\"_9-4-seo优化\\"><a class=\\"header-anchor\\" href=\\"#_9-4-seo优化\\">#</a> 9.4 SEO优化</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>搜索引擎首先要拿到稳定、可索引的正文，关键内容不要只依赖客户端异步渲染</li>\\n<li>每页提供准确的 <code>title</code>、<code>description</code>、规范链接和语义化标题层级</li>\\n<li><code>SSR/SSG</code> 有利于首屏内容，但仍要控制 <code>TTFB</code>、水合体积和缓存</li>\\n<li>图片提供有意义的 <code>alt</code>，内部链接可爬取，结构化数据必须与可见内容一致</li>\\n<li><code>Core Web Vitals</code> 影响用户体验与搜索表现，但不能替代内容质量和可发现性</li>\\n</ul>\\n<p><strong><code>SEO</code> 优化的核心，是让页面内容能够被稳定获取和展示，可采用预渲染或服务端渲染 <code>SSR</code>。</strong> 预渲染适合提前生成页面内容，服务端渲染则在服务端输出页面结果，两者都能避免内容完全依赖客户端执行后才出现。实际选择时要看页面的生成方式和内容特点，不必为了 <code>SEO</code> 把所有页面都改成同一种渲染模式。</p>\\n</section>\\n<ul>\\n<li>预渲染；</li>\\n<li>服务端渲染SSR；</li>\\n</ul>\\n<h3 id=\\"_9-5-打包优化\\"><a class=\\"header-anchor\\" href=\\"#_9-5-打包优化\\">#</a> 9.5 打包优化</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先用包分析器定位大模块、重复版本和意外进入客户端的服务端依赖</li>\\n<li>使用静态 <code>ESM</code>、正确的 <code>sideEffects</code> 声明和生产模式保证 <code>Tree Shaking</code></li>\\n<li>按路由和重交互组件动态拆包，避免把所有异步块切得过碎</li>\\n<li>现代浏览器优先发送现代语法，兼容代码与 <code>polyfill</code> 按需提供</li>\\n<li>缓存构建结果提升开发速度；代码分割、压缩和删减才直接影响用户</li>\\n</ul>\\n<p><strong>打包优化本质上是减少无用、重复和首屏暂时不需要的代码，再通过压缩与缓存降低加载成本。</strong> 我一般会先用 <code>Tree Shaking</code>、<code>Scope Hoisting</code> 整理模块，再通过 <code>splitChunks</code> 抽离公共文件，避免相同依赖反复进入产物。第三方模块也可以交给 <code>CDN</code>，构建较慢时再考虑 <code>HappyPack</code> 多线程打包。<code>sourceMap</code> 要按使用场景取舍，不能只追求调试方便而忽略产物影响。</p>\\n</section>\\n<ul>\\n<li>压缩代码；</li>\\n<li><code>Tree Shaking/Scope Hoisting</code>；\\n<ul>\\n<li><code>scope hoisting</code> 是 <code>webpack3</code> 的新功能，直译过来就是「作用域提升」。熟悉 JavaScript 都应该知道「函数提升」和「变量提升」，JavaScript 会把函数和变量声明提升到当前作用域的顶部。「作用域提升」也类似于此，webpack 会把引入的 js 文件“提升到”它的引入者顶部</li>\\n</ul>\\n</li>\\n<li>使用cdn加载第三方模块；</li>\\n<li>多线程打包<code>happypack</code>；</li>\\n<li><code>splitChunks</code>抽离公共文件；</li>\\n<li><code>sourceMap</code>优化；</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>生产包已经做了代码压缩，负责人因此认为没有继续优化空间，但分析图里第三方库仍占大头；这时压缩为什么不等于完成打包优化？</strong></p>\\n<p>压缩只减少已有代码的表示体积，不会自动移除未使用模块，也不会改变第三方依赖的装载方式。还应检查 <code>Tree Shaking</code>、<code>Scope Hoisting</code> 是否生效，并评估把稳定第三方模块改由 <code>CDN</code> 加载；若依赖存在副作用或模块边界不利于静态分析，删除效果会受限。</p>\\n</li>\\n<li>\\n<p><strong>一个多入口后台让每个页面都打进同一份工具库，改一行公共代码就产生多份重复产物，你会怎样用 <code>splitChunks</code> 收敛？</strong></p>\\n<p>应通过 <code>splitChunks</code> 把被多个入口复用的模块抽成公共文件，让页面共享同一份产物并配合缓存复用。落地后要用产物分析确认重复模块确实消失，而不是只看配置存在；公共包过大也会让只访问单页的用户下载无关代码，因此抽离范围需要控制。</p>\\n</li>\\n<li>\\n<p><strong>团队准备同时采用 <code>CDN</code> 外置第三方库和 <code>splitChunks</code> 抽取依赖，架构师要求所有依赖统一走 <code>CDN</code>，你会怎样划分边界？</strong></p>\\n<p>稳定、通用且具备可靠托管条件的第三方模块可考虑从包中剔除并交给 <code>CDN</code>，业务内部复用代码更适合由 <code>splitChunks</code> 抽取。统一外置会引入额外域名、版本一致性和可用性约束；若资源并不公共或缓存命中有限，<code>CDN</code> 未必比随应用发布更合适。</p>\\n</li>\\n<li>\\n<p><strong>构建机 <code>CPU</code> 长时间满载，团队加了 <code>happypack</code> 后总构建仍未明显缩短，你会先查哪里，而不是继续加线程？</strong></p>\\n<p>先确认主要耗时是否真的来自可并行处理的转换任务，再检查压缩、<code>sourceMap</code> 生成和插件阶段是否才是瓶颈。多线程打包只对适合拆分的工作有效，还会增加任务调度与进程通信成本；若瓶颈位于串行插件或磁盘读写，继续加线程不会解决根因。</p>\\n</li>\\n<li>\\n<p><strong>线上需要定位压缩代码报错，安全负责人又禁止公开源码；<code>sourceMap</code> 优化与排障需求冲突时怎么选？</strong></p>\\n<p>应保留满足内部定位需求的 <code>sourceMap</code> 产物，但不要把可还原源码的映射文件直接公开部署。构建侧需要在生成耗时、映射精度和发布安全之间选择合适配置，并验证错误平台能取得映射；完全关闭会降低排障能力，公开发布则可能暴露源码结构。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_10-vue1-x-vue2-x-vue3-框架分析性能\\"><a class=\\"header-anchor\\" href=\\"#_10-vue1-x-vue2-x-vue3-框架分析性能\\">#</a> 10 vue1.X，vue2.X，vue3 框架分析性能</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><strong><code>Vue 1</code></strong>：每个绑定一个 <code>Watcher</code>（细粒度到属性），更新精确但 <code>Watcher</code> 数量随模板复杂度暴涨，内存吃紧</li>\\n<li><strong><code>Vue 2</code></strong>：改成<strong>组件级 <code>Watcher</code></strong> + 虚拟 <code>DOM</code> diff —— <code>Watcher</code> 数量降到组件数量级，代价是组件内部要 diff</li>\\n<li><strong><code>Vue 3</code></strong>：响应式换 <code>Proxy</code>（可拦新增属性、惰性代理），并把优化重心移到<strong>编译期</strong></li>\\n<li><code>Vue 3</code> 的提速主要来自编译期而非算法：<strong>静态提升</strong>（静态节点不参与 diff）、<strong><code>PatchFlag</code></strong>（只 diff 会变的属性）、<strong>事件缓存</strong></li>\\n<li>所以 <code>Vue 3</code> 快在&quot;<strong>少 diff</strong>&quot;而不是&quot;diff 得更聪明&quot;——真正的算法改进只有乱序区换成最长递增子序列</li>\\n</ul>\\n<p><strong>三代 <code>Vue</code> 的性能取舍，是从细粒度响应式更新，转向组件级更新，再把更多优化前移到编译阶段。</strong> <code>Vue 1.x</code> 每个绑定对应一个 <code>Watcher</code>，更新精确，但模板复杂后数量和内存压力会上升；<code>Vue 2.x</code> 改为组件级 <code>Watcher</code> 配合虚拟 <code>DOM</code>，减少了 <code>Watcher</code>，代价是组件内部需要执行 <code>diff</code>。<code>Vue 3</code> 使用 <code>Proxy</code>，并通过静态提升、<code>PatchFlag</code> 和事件缓存减少无效比较。简单来说，它更多是让运行时少做 <code>diff</code>，乱序区域才使用最长递增子序列改进算法。</p>\\n</section>\\n<h3 id=\\"_10-1-vue1-x-特点-响应式\\"><a class=\\"header-anchor\\" href=\\"#_10-1-vue1-x-特点-响应式\\">#</a> 10.1 Vue1.x （特点：响应式）</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Vue 1.x</code> 更接近细粒度依赖更新，绑定关系直接驱动对应 <code>DOM</code> 变化</li>\\n<li>小页面更新路径直接，但大量绑定会产生较多 watcher 与管理成本</li>\\n<li>当应用规模扩大，逐绑定维护与调度的复杂度会明显上升</li>\\n<li>它没有后来组件级虚拟 <code>DOM diff</code> 与编译期静态标记这套优化路径</li>\\n</ul>\\n<p><strong><code>Vue 1.x</code> 没有虚拟 <code>DOM</code>，主要依靠细粒度响应式和大量 <code>Watcher</code> 直接驱动页面更新。</strong> 数据发生变化时，响应式通知机制会让对应的 <code>Watcher</code> 工作，因此更新目标比较明确。问题在于项目规模和模板复杂度上升后，<code>Watcher</code> 数量也会持续增加，管理与运行开销可能成为性能瓶颈。它更适合放在三代框架的演进中理解：用更多监听对象换取更直接的更新。</p>\\n</section>\\n<blockquote>\\n<p>没有vdom，完全的响应式，每个数据变化，都通过响应式通知机制来新建Watcher干活，项目规模变大后，过多的Watcher，会导致性能的瓶颈。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/image-20210307183730134.png\\" alt=\\"image-20210307183730134\\"></p>\\n<h3 id=\\"_10-2-vue2-x-特点-组件级响应式-组件内部vdom-diff\\"><a class=\\"header-anchor\\" href=\\"#_10-2-vue2-x-特点-组件级响应式-组件内部vdom-diff\\">#</a> 10.2 Vue2.x （特点：组件级响应式，组件内部vdom diff）</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Vue 2</code> 通过 <code>Object.defineProperty</code> 收集依赖，更新以组件 watcher 为主要边界</li>\\n<li>组件被通知后重新执行渲染函数，再通过虚拟 <code>DOM diff</code> 找出真实改动</li>\\n<li>新增/删除对象属性不能被旧式劫持自动发现，需要 <code>Vue.set/delete</code></li>\\n<li>大组件依赖过多时一次状态变化可能触发较大范围重新渲染</li>\\n</ul>\\n<p><strong><code>Vue 2</code> 的更新机制可以概括为组件级响应式通知，再在组件内部做 <code>Virtual DOM diff</code>。</strong> 这样把更新粒度控制在组件范围，既避免给每个绑定都创建大量 <code>watcher</code>，也不会让参与比较的虚拟节点规模过大。代价是组件更新时仍有运行时比较成本，所以组件边界划分是否合理会影响实际表现。</p>\\n</section>\\n<blockquote>\\n<p>引入<code>vdom</code>，控制了颗粒度，组件层面走<code>watcher</code>通知， 组件内部走<code>vdom</code>做<code>diff</code>，既不会有太多watcher，也不会让vdom的规模过大，diff超过16ms，真是优秀。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/image-20210307183810844.png\\" alt=\\"image-20210307183810844\\"></p>\\n<h3 id=\\"_10-3-vue3-特点-proxy做响应式-静态标记、按需更新\\"><a class=\\"header-anchor\\" href=\\"#_10-3-vue3-特点-proxy做响应式-静态标记、按需更新\\">#</a> 10.3 Vue3 （特点：proxy做响应式：静态标记、按需更新）</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Vue 3</code> 用 <code>Proxy</code> 覆盖属性新增、删除、索引等更多操作，并支持按访问懒代理</li>\\n<li>编译器把静态节点提升，给动态节点生成 <code>Patch Flag</code>，运行时只比较可能变化的部分</li>\\n<li><code>Block Tree</code> 收集动态子节点，减少在静态结构中递归遍历</li>\\n<li>性能提升来自“少创建、少遍历、少 diff”，不是单纯说 <code>Proxy</code> 更快</li>\\n<li>大数据仍要用 <code>shallowRef/markRaw</code> 控制响应式深度，代理本身不是零成本</li>\\n</ul>\\n<p><strong><code>Vue 3</code> 通过 <code>Proxy</code> 响应式、组件内部 <code>Virtual DOM</code> 和静态标记，把更新范围缩小到真正动态的内容。</strong> 静态节点在编译阶段被识别后，运行时就不必反复比较，因此更新性能可提升约 <code>1.3～2</code> 倍，<code>SSR</code> 可提升约 <code>2～3</code> 倍。任务粒度已经控制得较细，所以通常不太依赖 <code>time-slice</code>。</p>\\n</section>\\n<blockquote>\\n<p>先说结论，静态标记，upadte性能提升1.3~2倍，ssr提升2~3倍。</p>\\n</blockquote>\\n<p>Vue3通过<code>Proxy响应式+组件内部vdom+静态标记</code>，把任务颗粒度控制的足够细致，所以也不太需要<code>time-slice</code>了。</p>\\n<p><img src=\\"https://s.poetries.top/images/image-20210307183845241.png\\" alt=\\"image-20210307183845241\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个几乎全是静态文案的详情页仍在每次状态变化时遍历整棵虚拟树，开发者说用了 <code>Proxy</code> 就已经获得 <code>Vue 3</code> 的全部更新性能，你会指出哪里判断错了？</strong></p>\\n<p><code>Proxy</code> 只覆盖响应式这一侧，组件更新仍要依赖虚拟 <code>DOM</code> 与编译期静态标记缩小处理范围。静态内容应由编译结果标识，使更新时聚焦真正动态的节点；若模板写法阻碍静态分析，运行时仍可能承担额外比较，不能把收益全部归因于响应式实现。</p>\\n</li>\\n<li>\\n<p><strong>仪表盘组件有大量固定标题，只有数值和状态类名变化，你会怎样验证“静态标记、按需更新”确实落到了生成代码里？</strong></p>\\n<p>应检查模板编译产物是否区分固定节点与动态节点，并在状态变化时观察组件内部实际参与更新的范围。数值和类名应成为明确的动态部分，固定标题不应被重复处理；若大量内容由难以静态分析的动态结构生成，编译器能提供的优化会减少。</p>\\n</li>\\n<li>\\n<p><strong>团队在长列表卡顿后主张给 <code>Vue 3</code> 引入通用 <code>time-slice</code>，另一方认为框架已经不需要它；你如何裁决这个选型冲突？</strong></p>\\n<p>先判断卡顿是否来自组件更新颗粒度，而不是直接引入调度层。<code>Proxy</code>、组件内部虚拟 <code>DOM</code> 和静态标记已把常规更新控制得更细，因此通常不必用 <code>time-slice</code> 补救；但这不代表超大规模渲染天然无成本，数据量与节点量仍需单独治理。</p>\\n</li>\\n<li>\\n<p><strong>迁移后某组件更新没有预期提升，线上采样显示状态变化时大量子节点仍被处理，你会沿哪条链路排查？</strong></p>\\n<p>先确认响应式数据是否导致组件整体进入更新，再检查模板中哪些节点被识别为动态内容、静态标记是否形成。随后定位是否存在大范围动态属性或结构变化，使编译器无法跳过节点；若主要成本是业务计算或真实 <code>DOM</code> 数量，响应式与标记优化也无法独自消除。</p>\\n</li>\\n<li>\\n<p><strong>技术负责人只允许在“升级 <code>Proxy</code> 响应式”和“改造模板以获得静态标记”中选一项，目标是降低频繁更新成本，你如何说明两者不能简单互换？</strong></p>\\n<p>两者作用阶段不同：<code>Proxy</code> 负责依赖变化的响应式基础，静态标记负责组件更新时跳过无关虚拟节点。频繁更新且模板静态内容较多时，静态标记直接影响按需更新范围；数据访问与初始化特征明显时响应式机制也重要，取舍必须依据瓶颈而非统一结论。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);