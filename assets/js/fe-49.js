(window.webpackJsonp=window.webpackJsonp||[]).push([[49],{1157:function(e){e.exports=JSON.parse('{"html":"<h2 id=\\"_1-移动端适配\\"><a class=\\"header-anchor\\" href=\\"#_1-移动端适配\\">#</a> 1 移动端适配</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>前提是 <code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;</code>，没有它一切适配都不成立</li>\\n<li>两套主流方案：<strong><code>rem</code> + 动态根字号</strong>（配 <code>postcss-pxtorem</code> 自动转换）、<strong><code>vw</code> 直接换算</strong>（纯 <code>CSS</code> 无需 <code>JS</code>）</li>\\n<li>都要<strong>限制上下限</strong>，否则在平板和折叠屏上元素会被撑到荒唐尺寸 —— 用 <code>clamp()</code> 或容器 <code>max-width</code></li>\\n<li>字号建议保留可缩放能力：别把根字号写死成 <code>px</code>，那会覆盖用户在浏览器里设置的字号偏好（无障碍问题）</li>\\n<li>现代补充：<code>100dvh</code> 解决地址栏导致的 <code>100vh</code> 溢出、<code>env(safe-area-inset-*)</code> 处理刘海屏和底部横条</li>\\n</ul>\\n<p><strong>移动端适配的核心，是让页面在不同屏幕宽度和像素比下保持合理的布局比例。</strong> 页面要先设置 <code>viewport</code>，再根据业务选择百分比布局或动态 <code>rem</code>：前者适合结构均匀的页面，后者更方便按设计稿等比缩放。使用 <code>rem</code> 时，需要根据屏幕宽度计算根字号，并限制最大适配宽度，避免平板上被无限放大。设备像素相关问题还要结合 <code>window.devicePixelRatio</code>，区分物理像素和 <code>CSS</code> 像素。</p>\\n</section>\\n<p><strong>为什么要做适配</strong></p>\\n<ul>\\n<li>为了适应各种移动端设备，完美呈现应有的布局效果</li>\\n<li>各个移动端设备，分辨率大小不一致，网页想铺满整个屏幕，并在各种分辨下等比缩放</li>\\n</ul>\\n<h3 id=\\"适配方案\\"><a class=\\"header-anchor\\" href=\\"#适配方案\\">#</a> 适配方案</h3>\\n<ul>\\n<li>固定高度，宽度百分比适配-布局非常均匀，适合百分比布局</li>\\n<li>固定宽度，改变缩放比例适配-什么情况都可以</li>\\n<li>Rem适配</li>\\n<li>像素比适配</li>\\n</ul>\\n<p><strong>单位</strong></p>\\n<ul>\\n<li><code>em</code>根据元素自身的字体大小计算,元素自身 <code>16px 1em=16px</code></li>\\n<li><code>Rem R -&gt; root</code> 根节点( html ) 根据html的字体大小计算其他元素尺寸</li>\\n</ul>\\n<p><strong>百分比适配</strong></p>\\n<blockquote>\\n<p>固定高度，宽度百分比适配</p>\\n</blockquote>\\n<ul>\\n<li>根据设置的大小去设置高度，单位可以用<code>px</code> 百分比 <code>auto</code></li>\\n<li>常用<code>Flex</code>布局</li>\\n<li>百分比宽度</li>\\n</ul>\\n<blockquote>\\n<p>以<code>640</code>设计稿为例，在外层容器上设置最大以及最小的宽</p>\\n</blockquote>\\n<pre class=\\"language-css\\"><code>#wrapper {\\n    max-width: 640px; /*设置设计稿的宽度*/\\n    min-width: 300px;\\n    margin: 0 auto;\\n}\\n</code></pre>\\n<blockquote>\\n<p>后面的区块布局都用百分比，具体元素大小用<code>px</code>计算</p>\\n</blockquote>\\n<p><strong>Rem适配(常用)</strong></p>\\n<ul>\\n<li>根据屏幕的分辨率动态设置<code>html</code>的文字大小，达到等比缩放的功能</li>\\n<li>保证<code>html</code>最终算出来的字体大小，不能小于<code>12px</code></li>\\n<li>在不同的移动端显示不同的元素比例效果</li>\\n<li>如果<code>html</code>的<code>font-size:20px</code>的时候，那么此时的<code>1rem = 20px</code></li>\\n<li>把设计图的宽度分成多少分之一，根据实际情况</li>\\n<li><code>rem</code>做盒子的宽度，<code>viewport</code>缩放</li>\\n</ul>\\n<blockquote>\\n<p><code>head</code>加入常见的<code>meta</code>属性</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;meta name=&quot;format-detection&quot; content=&quot;telephone=no&quot;&gt;\\n&lt;meta name=&quot;apple-mobile-web-app-capable&quot; content=&quot;yes&quot;&gt;\\n&lt;meta name=&quot;apple-mobile-web-app-status-bar-style&quot; content=&quot;black&quot;&gt;\\n&lt;!--这个是关键--&gt;\\n&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0，minimum-scale=1.0&quot;&gt;\\n</code></pre>\\n<blockquote>\\n<p>把这段代码加入<code>head</code>中的<code>script</code>预先加载</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// rem适配用这段代码动态计算html的font-size大小\\n(function(win) {\\n    var docEl = win.document.documentElement;\\n    var timer = &#39;&#39;;\\n\\n    function changeRem() {\\n        var width = docEl.getBoundingClientRect().width;\\n        if (width &gt; 750) { // 750是设计稿大小\\n            width = 750;\\n        }\\n        var fontS = width / 10; // 把设备宽度十等分 1rem&lt;=75px\\n        docEl.style.fontSize = fontS + &quot;px&quot;;\\n    }\\n    win.addEventListener(&quot;resize&quot;, function() {\\n        clearTimeout(timer);\\n        timer = setTimeout(changeRem, 30);\\n    }, false);\\n    win.addEventListener(&quot;pageshow&quot;, function(e) {\\n        if (e.persisted) { //清除缓存\\n            clearTimeout(timer);\\n            timer = setTimeout(changeRem, 30);\\n        }\\n    }, false);\\n    changeRem();\\n})(window)\\n</code></pre>\\n<p><strong>像素比适配</strong></p>\\n<ul>\\n<li><code>window.devicePixelRatio</code></li>\\n<li>物理像素是手机屏幕分辨率</li>\\n<li>独立像素 指<code>css</code>像素 屏幕宽度</li>\\n<li>像素比 = 物理像素 / <code>css</code>宽度</li>\\n<li>获取设备的像素比 <code>window.devicePixelRatio</code></li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>设计稿宽度是 640 像素，开发把所有模块都写成固定像素宽，页面在 320 像素手机上横向溢出，这算移动端适配吗？</strong></p>\\n<p>这只是照搬设计稿尺寸，没有形成随屏幕变化的布局。外层容器可设置 <code>max-width: 640px</code>、合理的 <code>min-width</code> 并居中，区块宽度使用百分比或 <code>Flex</code>；具体元素仍可按设计需要保留部分像素尺寸。</p>\\n</li>\\n<li>\\n<p><strong>一套 750 像素设计稿要用 <code>rem</code> 等比缩放，团队约定把视口十等分，根字号脚本应怎样计算并避免平板无限放大？</strong></p>\\n<p>读取根元素的视口宽度，将超过 750 像素的值封顶，再设置 <code>html</code> 的 <code>font-size</code> 为宽度除以 10，因此设计宽度下 <code>1rem</code> 为 75 像素。还需监听 <code>resize</code> 和 <code>pageshow</code>，并用防抖避免频繁重算。</p>\\n</li>\\n<li>\\n<p><strong>移动页面从后台返回时尺寸错乱，只有浏览器命中往返缓存时出现，<code>resize</code> 又没有触发，怎么修？</strong></p>\\n<p>应监听 <code>pageshow</code>，并在 <code>event.persisted</code> 为真时重新执行根字号计算，因为页面可能从往返缓存恢复而未走常规初始化。重算仍要经过短暂防抖；若只依赖 <code>resize</code>，该恢复路径可能一直保留旧尺寸。</p>\\n</li>\\n<li>\\n<p><strong>同一台手机上 <code>CSS</code> 宽度是 390 像素，截图物理宽度明显更高，测试同学认为页面缩放算法失效，你会查什么？</strong></p>\\n<p>先检查 <code>window.devicePixelRatio</code>，它表示物理像素与 <code>CSS</code> 独立像素之间的比例，两种宽度本来就不必相等。布局应主要依据 <code>CSS</code> 视口尺寸，物理像素更多影响资源清晰度；把截图像素直接当布局宽度会造成错误判断。</p>\\n</li>\\n<li>\\n<p><strong>电商 <code>H5</code> 既要求各模块比例接近设计稿，又要求列表宽度灵活、超宽屏不能继续放大，固定宽度、百分比和 <code>rem</code> 怎么取舍？</strong></p>\\n<p>可用百分比或 <code>Flex</code> 处理列表等流式区块，用 <code>rem</code> 表达需要随屏幕等比变化的尺寸，并在外层设置最大宽度封顶。完全固定宽度容易在窄屏溢出，完全等比缩放又会在宽屏过大，混合方案的代价是规则需要统一维护。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_2-移动端300ms延迟\\"><a class=\\"header-anchor\\" href=\\"#_2-移动端300ms延迟\\">#</a> 2 移动端300ms延迟</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>起因：早期移动浏览器要等 <code>300ms</code> 判断用户是不是<strong>双击缩放</strong>，所以 <code>click</code> 被延迟触发</li>\\n<li>⚠️ <strong>现在已经不需要处理了</strong>：只要正确设置了 <code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width&quot;&gt;</code>，浏览器就知道页面不需要双击缩放，会去掉这个延迟</li>\\n<li>所以 <code>FastClick</code> 已经停止维护、也不该再引入 —— 继续用反而可能引发点击穿透等新问题</li>\\n<li>相关的&quot;点击穿透&quot;是另一个问题：<code>touch</code> 事件触发后元素消失，<code>300ms</code> 后合成的 <code>click</code> 落到了下层元素上</li>\\n<li>现代方案：用 <code>pointer</code> 事件统一处理、或给遮罩加 <code>touch-action</code>，别再用 <code>touchstart</code> 模拟点击</li>\\n</ul>\\n<p><strong>移动端早期的 <code>300ms</code> 点击延迟，本质上是浏览器在等待用户是否会进行第二次点击，从而判断是不是双击缩放。</strong> 因此一次触摸结束后，<code>click</code> 不会立刻触发，事件顺序通常是 <code>touchstart</code>、<code>touchmove</code>、<code>touchend</code>，最后才到 <code>click</code>。传统方案是通过 <code>viewport</code> 禁用缩放，或者使用 <code>FastClick</code> 在 <code>touchend</code> 时主动派发点击并拦截延迟到来的原生事件。现代页面一般优先正确设置 <code>viewport</code>，不必再默认引入 <code>FastClick</code>。</p>\\n</section>\\n<blockquote>\\n<p>由来：300毫秒延迟解决的是双击缩放。双击缩放，手指在屏幕快速点击两次。safari浏览器就会将网页缩放值原始比例。由于用户可以双击缩放或者是滚动的操作，</p>\\n</blockquote>\\n<p>当用户点击屏幕一次之后，浏览器并不会判断用户确实要打开至这个链接，还是想要进行双击操作\\n因此，safair浏览器就会等待300ms，用来判断用户是否在次点击了屏幕</p>\\n<p><strong>解决方案</strong>：</p>\\n<ol>\\n<li>禁用缩放，设置meta标签 <code>user-scalable=no</code></li>\\n<li><code>fastclick.js</code></li>\\n</ol>\\n<blockquote>\\n<p>原理：FastClick的实现原理是在检查到touchend事件的时候，会通过dom自定义事件立即发出click事件，并把浏览器在<code>300ms</code>之后真正的click事件阻止掉。fastclick.js还可以解决穿透问题</p>\\n</blockquote>\\n<ul>\\n<li>fastclick可以解决在手机上点击事件的<code>300ms</code>延迟</li>\\n<li>zepto的touch模块，tap事件也是为了解决在click的延迟问题</li>\\n</ul>\\n<p><strong>触摸事件的响应顺序</strong></p>\\n<ul>\\n<li><code>ontouchstart</code></li>\\n<li><code>ontouchmove</code></li>\\n<li><code>ontouchend</code></li>\\n<li><code>onclick</code></li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品详情页只监听了 <code>click</code>，用户单击“立即购买”后约 <code>300ms</code> 才响应；同事却说这是接口慢，你怎么现场证明延迟发生在浏览器事件阶段？</strong></p>\\n<p>应先记录 <code>touchstart</code>、<code>touchend</code>、<code>click</code> 和请求发起的时间，确认停顿位于 <code>touchend</code> 到 <code>click</code> 之间，而不是网络阶段。浏览器等待期间是在判断用户是否还会再次点击以触发双击缩放；若 <code>click</code> 后请求才变慢，再转查接口链路。</p>\\n</li>\\n<li>\\n<p><strong>一个活动页既允许双击缩放，又要求领取按钮触摸结束后立即反馈，直接设置 <code>user-scalable=no</code> 合适吗？</strong></p>\\n<p>不合适，因为禁用缩放虽然能消除浏览器为双击判断保留的等待，却同时取消了用户的缩放能力。应先确认页面是否允许牺牲缩放；必须保留时，可评估用 <code>FastClick</code> 在 <code>touchend</code> 后派发模拟 <code>click</code>，但要验证事件冲突。</p>\\n</li>\\n<li>\\n<p><strong>旧项目同时引入 <code>FastClick</code> 和 <code>Zepto touch</code>，一个按钮既绑定 <code>tap</code> 又绑定 <code>click</code>，线上偶尔提交两次订单，你会怎样收敛事件模型？</strong></p>\\n<p>应只保留一条点击链路，因为 <code>tap</code> 与 <code>FastClick</code> 都在绕开原生 <code>click</code> 的等待，同时绑定容易让一次触摸进入多套处理逻辑。先给提交动作加幂等保护，再移除重复监听，并在真机核对 <code>touchend</code>、模拟 <code>click</code> 与原生 <code>click</code> 是否只触发一次。</p>\\n</li>\\n<li>\\n<p><strong>遮罩关闭逻辑从 <code>click</code> 改到 <code>touchend</code> 后，下面的商品链接也被打开了；为什么提速会引入这个线上故障？</strong></p>\\n<p>因为 <code>touchend</code> 先隐藏了遮罩，而浏览器稍后仍可能生成原生 <code>click</code>，此时原位置已经暴露出下层链接，事件便落到新的目标上。若使用模拟 <code>click</code>，必须阻止随后到来的原生事件；处理不完整就会形成穿透或重复触发。</p>\\n</li>\\n<li>\\n<p><strong>支付确认按钮准备采用 <code>user-scalable=no</code> 或 <code>FastClick</code>，产品要求响应快，无障碍同事要求页面可缩放，你会怎样取舍？</strong></p>\\n<p>应优先保留缩放能力，不能只为缩短点击等待就默认禁用用户缩放。若目标环境确有延迟，可在明确兼容范围后使用 <code>FastClick</code> 的 <code>touchend</code> 模拟机制，并重点回归表单、链接和遮罩；代价是引入额外事件层及冲突风险。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_3-如何解决移动端-retina-屏-1px-像素问题\\"><a class=\\"header-anchor\\" href=\\"#_3-如何解决移动端-retina-屏-1px-像素问题\\">#</a> 3 如何解决移动端 Retina 屏 1px 像素问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>成因：<code>CSS</code> 的 <code>1px</code> 是<strong>逻辑像素</strong>，<code>DPR=2</code> 时被渲染成 2 个物理像素，所以看起来比设计稿粗</li>\\n<li><strong>首选方案</strong>：伪元素 + <code>transform: scaleY(0.5)</code>，必须配 <code>transform-origin: 0 0</code> 否则位置偏移；兼容性最好，值得封成 <code>mixin</code></li>\\n<li><code>0.5px</code> 写法简洁但<strong>部分安卓会渲染成 0</strong>（边框直接消失），必须配 <code>@media (-webkit-min-device-pixel-ratio: 2)</code> 并做回退</li>\\n<li><code>box-shadow</code> 模拟最简单但颜色会因抗锯齿偏淡；<code>border-image</code> 维护成本高已基本弃用</li>\\n<li>⚠️ <code>postcss-pxtorem</code> 要用 <code>propList: [\'!border*\']</code> <strong>排除边框</strong>，否则转成 <code>rem</code> 后可能算出不足 1 物理像素被渲染成 0</li>\\n<li>圆角边框用 <code>scaleY</code> 会失真；另外值得先和设计确认&quot;是不是真的需要 0.5px&quot;</li>\\n</ul>\\n<p><strong><code>Retina</code> 屏上的 <code>1px</code> 变粗，是因为它代表一个 <code>CSS</code> 逻辑像素，在高 <code>DPR</code> 设备上会对应多个物理像素。</strong> 我一般用伪元素画 <code>1px</code> 线，再通过 <code>transform: scaleY(0.5)</code> 缩小，并设置 <code>transform-origin</code> 防止位置偏移。直接写 <code>0.5px</code> 更省事，但部分安卓设备可能渲染成 <code>0</code>，需要媒体查询和回退。圆角边框不适合简单纵向缩放，同时使用 <code>postcss-pxtorem</code> 时也应排除边框属性。</p>\\n</section>\\n<p><strong>问题成因</strong>：<code>CSS</code> 里的 <code>1px</code> 是<strong>逻辑像素</strong>，在 <code>DPR = 2</code> 的屏幕上会被渲染成 <strong>2 个物理像素</strong>，所以边框看起来比设计稿粗一倍。</p>\\n<p><strong>几种解法，按推荐度排：</strong></p>\\n<p><strong>① <code>transform: scaleY(0.5)</code> + 伪元素（最通用）</strong></p>\\n<pre class=\\"language-css\\"><code>.line::after {\\n  position: absolute;\\n  bottom: 0; left: 0;\\n  width: 100%; height: 1px;\\n  background: #e5e5e5;\\n  transform: scaleY(0.5);\\n  transform-origin: 0 0;    /* 必须，否则位置会偏移 */\\n  content: &#39;&#39;;\\n}\\n</code></pre>\\n<p>四条边都要的话用 <code>scale(0.5)</code> 配 <code>width: 200%; height: 200%</code> 再整体缩小。这个方案兼容性最好，是团队里最值得封成 <code>mixin</code> 的一个。</p>\\n<p><strong>② 直接写 <code>0.5px</code>（简洁但有兼容风险）</strong></p>\\n<pre class=\\"language-css\\"><code>@media (-webkit-min-device-pixel-ratio: 2) {\\n  .line { border-width: 0.5px; }\\n}\\n</code></pre>\\n<p>现代 <code>iOS Safari</code> 支持，但<strong>部分安卓会把 <code>0.5px</code> 渲染成 0</strong>，边框直接消失 —— 所以必须配媒体查询并做好回退。</p>\\n<p><strong>③ <code>box-shadow</code> 模拟</strong></p>\\n<pre class=\\"language-css\\"><code>.line { box-shadow: 0 0.5px 0 0 #e5e5e5; }\\n</code></pre>\\n<p>写法简单，但颜色会因为抗锯齿而偏淡。</p>\\n<p><strong>④ <code>border-image</code> / 背景渐变</strong> —— 能做但维护成本高，现在基本不用了。</p>\\n<p><strong>几个实践要点：</strong></p>\\n<ul>\\n<li><strong><code>postcss-pxtorem</code> 要排除边框</strong>：<code>1px</code> 转成 <code>rem</code> 后可能算出不足 1 物理像素的值被渲染成 0。在 <code>propList</code> 里用 <code>\'!border*\'</code> 排除。</li>\\n<li><strong>圆角边框用 <code>scaleY</code> 方案会失真</strong> —— 那种情况更适合用 <code>0.5px</code> 或直接接受 <code>1px</code>。</li>\\n<li><strong>先问设计要不要</strong>：很多时候设计稿标的 <code>1px</code> 只是习惯，真实视觉效果上 <code>1</code> 物理像素在高 <code>DPR</code> 屏上可能太细以至于看不清。这是个值得和设计确认的问题，而不是无脑实现。</li>\\n</ul>\\n<p>下面是更多说明：</p>\\n<ul>\\n<li><code>伪元素 + transform scaleY(.5)</code></li>\\n<li><code>border-image</code></li>\\n<li><code>background-image</code></li>\\n<li><code>box-shadow</code></li>\\n</ul>\\n<blockquote>\\n<p>一般来说，在PC端浏览器中，设备像素比（dpr）等于1，1个css像素就代表1个物理像素；但是在<code>retina</code>屏幕中，dpr普遍是2或3，1个css像素不再等于1个物理像素，因此比实际设计稿看起来粗不少</p>\\n</blockquote>\\n<ol>\\n<li>伪元素+scale</li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n    .box{\\n        width: 100%;\\n        height: 1px;\\n        margin: 20px 0;\\n        position: relative;\\n    }\\n    .box::after{\\n        content: &#39;&#39;;\\n        position: absolute;\\n        bottom: 0;\\n        width: 100%;\\n        height: 1px;\\n        transform: scaleY(0.5);\\n        transform-origin: 0 0; \\n        background: red;\\n    }\\n&lt;/style&gt;\\n\\n&lt;div class=&quot;box&quot;&gt;&lt;/div&gt;\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>border-image</li>\\n</ol>\\n<pre class=\\"language-css\\"><code>div{\\n    border-width: 1px 0px;\\n    border-image: url(border.png) 2 0 stretch;\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>设计师说列表分割线必须是一个物理像素，开发却直接写了 <code>border-bottom: 1px</code>；在 <code>DPR = 2</code> 的手机上为什么会显得更粗？</strong></p>\\n<p>因为 <code>CSS</code> 中的 <code>1px</code> 是逻辑像素，在 <code>DPR = 2</code> 的屏幕上会覆盖两个物理像素，并不等于设计要求的一条物理像素线。应先区分设计稿的视觉意图与 <code>CSS</code> 单位；若设计并不要求极细线，也不必机械缩放。</p>\\n</li>\\n<li>\\n<p><strong>消息列表有上千个条目，需要统一实现底部分割线，你会怎样把伪元素方案封装，避免每个业务组件各写一份？</strong></p>\\n<p>可把 <code>::after</code> 的绝对定位、<code>height: 1px</code>、背景色、<code>scaleY(0.5)</code> 和 <code>transform-origin: 0 0</code> 封成统一 <code>mixin</code>。宿主需要建立定位上下文，并允许配置方向和颜色；大量节点仍会增加伪元素数量，复杂卡片应评估维护成本。</p>\\n</li>\\n<li>\\n<p><strong>同一套样式要覆盖 <code>DPR = 2</code> 的 <code>iPhone</code> 和部分会把 <code>0.5px</code> 渲染成 <code>0</code> 的安卓机，设计坚持直接写 <code>border-width: 0.5px</code>，你怎么处理？</strong></p>\\n<p>不能把 <code>0.5px</code> 作为无条件方案，应放在设备像素比媒体查询中，并为不可靠环境保留可见回退。更稳妥的是使用 <code>1px</code> 伪元素再纵向缩放；直接写小数更简洁，但边框在部分安卓设备上可能完全消失。</p>\\n</li>\\n<li>\\n<p><strong>上线后只有部分机型看不到分割线，代码里写的是 <code>border-bottom: 1px</code>，构建产物却没有这个值，你先查哪里？</strong></p>\\n<p>先检查 <code>postcss-pxtorem</code> 的构建结果，确认边框是否被换算成不足一个物理像素的 <code>rem</code> 值并最终渲染为 <code>0</code>。应在 <code>propList</code> 中用 <code>!border*</code> 排除边框，再真机复测；只改组件源码而不查产物会漏掉构建阶段的原因。</p>\\n</li>\\n<li>\\n<p><strong>设置面板既有直线分隔，也有带圆角的卡片边框，能否全部用 <code>width: 200%; height: 200%; transform: scale(0.5)</code> 统一实现？</strong></p>\\n<p>不宜全部统一，四边框可用双倍尺寸后整体 <code>scale(0.5)</code>，但圆角在缩放方案下可能失真。圆角卡片更适合评估 <code>0.5px</code> 并提供回退，或接受普通 <code>1px</code>；统一代码的收益不能覆盖视觉变形和兼容风险。</p>\\n</li>\\n<li>\\n<p><strong>设计评审时视觉同事要求所有高 <code>DPR</code> 设备都使用一个物理像素细线，你为什么不会立刻照单实现？</strong></p>\\n<p>应先确认实际观看效果，因为一个物理像素在高 <code>DPR</code> 屏上可能细到难以辨认，设计稿标注的 <code>1px</code> 也未必代表物理像素。确认确有需要后再选伪元素方案；否则为技术上的精确付出兼容和维护成本并不划算。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_4-如何解决移动端击穿-穿透-问题\\"><a class=\\"header-anchor\\" href=\\"#_4-如何解决移动端击穿-穿透-问题\\">#</a> 4 如何解决移动端击穿（穿透）问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>成因：用 <code>touchstart</code>/<code>touchend</code> 关闭遮罩后元素立刻消失，浏览器约 <code>300ms</code> 后合成的 <code>click</code> 落到了<strong>下层元素</strong>上</li>\\n<li>最直接的解法：<strong>别用 <code>touch</code> 事件模拟点击</strong> —— 现代浏览器设了 <code>viewport</code> 后 <code>click</code> 已经没有延迟，直接用 <code>click</code> 即可</li>\\n<li>必须用 <code>touch</code> 时：在 <code>touchend</code> 里 <code>preventDefault()</code> 阻止合成 <code>click</code>（注意不能是 <code>passive</code> 监听器）</li>\\n<li>或者延迟隐藏：关闭动画留 <code>300ms</code> 以上，让合成的 <code>click</code> 落在还在的遮罩上</li>\\n<li>另一类&quot;穿透&quot;是<strong>滚动穿透</strong>：弹层滚到底后继续滑动会带动底层页面 —— 用 <code>overscroll-behavior: contain</code> 解决</li>\\n</ul>\\n<p><strong>移动端点击穿透的核心解法是统一使用 <code>click</code>，必须监听触摸事件时，则在 <code>touchend</code> 中调用 <code>preventDefault()</code>。</strong> 遮罩若在 <code>touchstart</code> 或 <code>touchend</code> 后立即消失，浏览器稍后合成的 <code>click</code> 就可能落到下层元素。监听器不能设为 <code>passive</code>，也可以延迟约 <code>300ms</code> 隐藏遮罩，但交互会变慢。若遇到的是滚动穿透，可用 <code>overscroll-behavior: contain</code> 限制滚动传递。</p>\\n</section>\\n<blockquote>\\n<p>在移动端开发的时候，我们有时候会遇到这样一个bug：点击关闭遮罩层的时候，遮罩层下面的带有点击的元素也会被触发，给人一种击穿了页面的感觉，这是为什么呢？</p>\\n</blockquote>\\n<ul>\\n<li>点击“打开弹框”按钮，显示遮罩层</li>\\n<li>点击“关闭弹框”按钮，遮罩层消失，底下的连接被触发</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>var show = document.getElementById(&#39;show&#39;) // 打开按钮\\nvar mask = document.getElementById(&#39;mask&#39;) // 遮罩层\\nvar btn = document.getElementById(&#39;btn&#39;)   // 关闭按钮\\n\\nshow.onclick = function () {\\n    mask.style.display = &#39;block&#39;\\n}\\n\\nbtn.addEventListener(&#39;touchstart&#39;, function () {\\n    mask.style.display = &#39;none&#39;\\n}, false)\\n</code></pre>\\n<ul>\\n<li>这样问题的形成原因是什么呢？</li>\\n<li>我们先来看一段代码：(以下代码需在移动端上运行)</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>&lt;div id=&quot;btn&quot;&gt;我是一个按钮&lt;/div&gt;\\nvar btn = document.getElementById(&#39;btn&#39;)\\nbtn.addEventListener(&#39;touchstart&#39;, function () {\\n    console.log(&#39;start&#39;)    \\n}, false)\\n\\nbtn.addEventListener(&#39;touchmove&#39;, function () {\\n    console.log(&#39;move&#39;)\\n}, false)\\n\\nbtn.addEventListener(&#39;touchend&#39;, function () {\\n    console.log(&#39;touchend&#39;)\\n}, false)\\n\\nbtn.addEventListener(&#39;click&#39;, function () {\\n    console.log(&#39;click&#39;)\\n}, false)\\n</code></pre>\\n<blockquote>\\n<p>以上代码会出现2种运行情况</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>start ===&gt; move ===&gt; end\\nstart ===&gt; end ===&gt; click\\n</code></pre>\\n<blockquote>\\n<p>看到这里相信大家都明白了，由于「关闭弹框」按钮绑定的事件是<code>touch</code>，a标签是<code>click</code>事件，在<code>touch</code>事件触发后，我们弹出框的遮罩层就消失了，这时候的<code>click</code>事件就被a标签给捕获到了，形成了击穿的效果</p>\\n</blockquote>\\n<p><strong>方法一、阻止默认事件</strong></p>\\n<pre class=\\"language-js\\"><code>btn.addEventListener(&#39;touchend&#39;, function (e) {\\n    mask.style.display = &#39;none&#39;\\n    e.preventDefault()\\n}, false)\\n</code></pre>\\n<blockquote>\\n<p>在执行 touchstart 和 touchend 事件时，隐藏执行完隐藏命令后，立即阻止后续事件(推荐在touchend时，阻止后续的默认事件)</p>\\n</blockquote>\\n<p><strong>方法二、统一使用click事件</strong></p>\\n<pre class=\\"language-js\\"><code>btn.addEventListener(&#39;click&#39;, function () {\\n    mask.style.display = &#39;none&#39;\\n}, false)\\n</code></pre>\\n<blockquote>\\n<p>这个方法简单，就是交互的效率没有<code>click</code>事件高，另外，用户在<code>touch</code>的时候，有可能微微滑动了一下，就会无法触发点击事件。影响用户体验。</p>\\n</blockquote>\\n<p><strong>方法三、延迟执行</strong></p>\\n<pre class=\\"language-js\\"><code>btn.addEventListener(&#39;touchend&#39;, function () {\\n    setTimeout(function () {\\n        mask.style.display = &#39;none&#39;  // 可以使用fadeOut动画\\n    }, 300)\\n}, false)\\n</code></pre>\\n<blockquote>\\n<p>点击之后，我们不立即隐藏。让遮罩在350ms毫秒内淡出消失。（我为了演示方便就没有添加动画了，采用了定时器方法。）</p>\\n</blockquote>\\n<p><strong>方法四、 css属性pointer-events</strong></p>\\n<pre class=\\"language-js\\"><code>click.setAttribute(&#39;style&#39;, &#39;pointer-events:none&#39;)\\nmask.style.display = &#39;none&#39;\\nsetTimeout(function () {\\n    click.setAttribute(&#39;style&#39;, &#39;pointer-events:auto&#39;)\\n}, 350)\\n</code></pre>\\n<blockquote>\\n<p>这样做法是，在遮罩消失之前，先让a标签忽略点击事件，这样遮罩层的点击事件，就不会被a标签捕获到。还是等350毫秒之后，再次赋予a标签的点击能力。这个方法跟方法三原理相似，只是利用了不同的css属性而已。个人觉得方法三比较好一点。方法四有明显的2个缺点：</p>\\n</blockquote>\\n<ul>\\n<li>遮罩层下面可能有多个带有事件的元素，那么你需要给所有可点击元素添加<code>pointer-events</code>属性 然后删除。不仅容易出错，还影响性能</li>\\n<li>如果用户在<code>350</code>毫秒内点击了元素，会造成页面失效的错觉，影响体验。</li>\\n</ul>\\n<p><strong>方法五、fastClick库</strong></p>\\n<blockquote>\\n<p>这个库的引用方法，在我上一篇文章中已经讲到。fastClick的原理就是使用了方法一的做法。fastClick 在 touchend 阶段 调用 event.preventDefault，通过 document.createEvent 创建一个 MouseEvents，然后 通过 <code>eventTarget.dispatchEvent</code> 触发对应目标元素上绑定的 click 事件</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>登录弹层的关闭按钮绑定 <code>touchstart</code>，正下方恰好是“删除账号”链接；用户只点了一次却触发删除，事件链上发生了什么？</strong></p>\\n<p><code>touchstart</code> 先执行并立即移除弹层，随后同一次触摸仍可能经历 <code>touchend</code> 并产生延迟的 <code>click</code>，下层链接此时已暴露而接到点击。它不是事件冒泡到父元素，而是前后两个事件面对了不同页面状态。</p>\\n</li>\\n<li>\\n<p><strong>促销弹层每天有大量关闭操作，团队决定统一用 <code>touchend</code>，你会怎样落地以避免下层链接被触发？</strong></p>\\n<p>可在关闭按钮的 <code>touchend</code> 处理器中先隐藏遮罩，再调用 <code>e.preventDefault()</code> 阻止后续默认点击行为。还要确认监听器允许取消默认行为，并回归轻微滑动、链接和表单控件；滥用阻止默认事件可能破坏原生交互。</p>\\n</li>\\n<li>\\n<p><strong>页面必须同时兼容只写 <code>click</code> 的业务组件和使用 <code>touchend</code> 的旧弹层，短期不能重构全部代码，你会怎样隔离穿透风险？</strong></p>\\n<p>短期可让遮罩延迟约 <code>300ms</code> 后再移除，使后续 <code>click</code> 仍命中遮罩，而不是下层组件；也可临时禁用明确的下层点击目标。前者会让关闭反馈变慢，后者在目标很多时容易漏配并影响正常点击，都只适合作为过渡。</p>\\n</li>\\n<li>\\n<p><strong>线上反馈只有弹层覆盖链接时会误跳转，覆盖纯文本时正常；你如何用日志和最小复现确认不是路由重复跳转？</strong></p>\\n<p>应同时记录关闭按钮的 <code>touchstart</code>、<code>touchend</code>、遮罩移除时刻和下层链接的 <code>click</code>，观察是否出现 <code>start → end → click</code>。再把下层链接替换为无点击元素作对照；若误跳随链接消失，便应沿触摸后生成点击的链路排查。</p>\\n</li>\\n<li>\\n<p><strong>修复会上有人主张给页面所有可点击元素设置 <code>pointer-events: none</code>，<code>350ms</code> 后恢复；你为什么不会把它作为长期方案？</strong></p>\\n<p>这种做法需要枚举并切换全部下层交互元素，页面越复杂越容易遗漏，也会增加样式操作和维护负担。用户若在禁用窗口内再次点击，还会感到页面失效；相比之下，统一使用 <code>click</code> 或在 <code>touchend</code> 精确阻止后续事件更可控。</p>\\n</li>\\n<li>\\n<p><strong>旧项目已经接入 <code>FastClick</code>，弹层又手写了 <code>touchend</code> 和 <code>click</code> 两套关闭逻辑，偶发执行两次，你会怎样判断该删哪一层？</strong></p>\\n<p>先确认 <code>FastClick</code> 是否已在 <code>touchend</code> 阶段阻止原生事件并派发模拟 <code>click</code>，再统计两套监听各自的触发顺序。业务关闭逻辑应收敛到单一入口并保证幂等；保留多条事件链会继续制造重复执行和穿透边界。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_5-移动端的兼容问题\\"><a class=\\"header-anchor\\" href=\\"#_5-移动端的兼容问题\\">#</a> 5 移动端的兼容问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>兼容基线先由<strong>真实用户分布</strong>决定：拉埋点看机型和浏览器占比，写进 <code>browserslist</code> 让整条工具链共用</li>\\n<li>高频问题：<code>iOS</code> 输入框聚焦自动放大（字号设 <code>16px</code>）、<code>100vh</code> 被地址栏顶出（用 <code>100dvh</code>）、<code>position: fixed</code> 遇软键盘错位</li>\\n<li><code>iOS</code> 的 <code>Safari</code> 因为系统绑定升级慢，是现在真正的兼容长尾 —— 比&quot;老安卓&quot;更值得关注</li>\\n<li>用<strong>特性检测</strong>不用 <code>UA</code> 嗅探；<code>CSS</code> 用 <code>@supports</code> 做渐进增强</li>\\n<li>必须在<strong>真机</strong>上测关键路径，模拟器测不出滚动性能、软键盘行为、以及国产 <code>WebView</code> 的差异</li>\\n</ul>\\n<p><strong>移动端兼容应先按真实用户的机型和浏览器分布确定基线，再通过 <code>browserslist</code> 统一构建配置。</strong> 常见问题集中在 <code>iOS Safari</code>：输入框聚焦放大可将字号设为 <code>16px</code>，地址栏影响视口高度时可用 <code>100dvh</code>，软键盘还可能让 <code>position: fixed</code> 错位。判断能力时优先做特性检测，并用 <code>@supports</code> 渐进增强，不要过度依赖 <code>UA</code>。关键流程仍要上真机验证，因为模拟器难以还原滚动、软键盘及不同 <code>WebView</code> 的表现。</p>\\n</section>\\n<ul>\\n<li>给移动端添加点击事件会有<code>300S</code>的延迟 如果用点击事件，需要引一个<code>fastclick.js</code>文件，解决<code>300s</code>的延迟 一般在移动端用<code>ontouchstart</code>、<code>ontouchmove</code>、<code>ontouchend</code></li>\\n<li>移动端点透问题,<code>touchstart</code> 早于 <code>touchend</code> 早于<code>click</code>,<code>click</code>的触发是有延迟的，这个时间大概在<code>300ms</code>左右，也就是说我们<code>tap</code>触发之后蒙层隐藏， 此时 <code>click</code>还没有触发，300ms之后由于蒙层隐藏，我们的click触发到了下面的a链接上尽量都使用<code>touch</code>事件来替换<code>click</code>事件。例如用touchend事件(推荐)。用<code>fastclick</code>，<code>github.com/ftlabs/fast…</code>用<code>preventDefault</code>阻止<code>a</code>标签的<code>click</code>消除 <code>IE10</code> 里面的那个叉号<code>input:-ms-clear{display:none;}</code></li>\\n<li>设置缓存 手机页面通常在第一次加载后会进行缓存，然后每次刷新会使用缓存而不是去重新向服务器发送请求。如果不希望使用缓存可以设置<code>no-cache</code>。</li>\\n<li>圆角<code>BUG</code> 某些Android手机圆角失效 <code>background-clip: padding-box;</code> 防止手机中网页放大和缩小 这点是最基本的，做为手机网站开发者来说应该都知道的，就是设置<code>meta</code>中的<code>viewport</code></li>\\n<li>设置用户截止缩放，一般写视口的时候就已经写好了</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>测试说移动端所有按钮都该把 <code>click</code> 换成 <code>touchstart</code>，因为这样没有约 <code>300ms</code> 延迟；页面里还有可滚动列表，你会直接接受吗？</strong></p>\\n<p>不会直接替换，<code>touchstart</code> 在用户尚未完成点击意图时就执行，滚动手势也可能从它开始，并且与后续 <code>click</code> 混用会带来点透。应按目标环境验证延迟，再统一事件策略；盲目替换会把响应速度换成误触和事件冲突。</p>\\n</li>\\n<li>\\n<p><strong>营销页弹层关闭后会误打开下方广告链接，现有代码是遮罩监听 <code>touchend</code>、广告监听 <code>click</code>，你会怎样做最小改动？</strong></p>\\n<p>可在遮罩的 <code>touchend</code> 中执行关闭后调用 <code>preventDefault()</code>，阻止同次触摸继续产生下层链接的默认点击。也可统一改用同类事件或由 <code>FastClick</code> 管理；若页面还有需要默认触摸行为的控件，必须缩小拦截范围。</p>\\n</li>\\n<li>\\n<p><strong>某些 <code>Android</code> <code>WebView</code> 中卡片圆角外仍露出背景色，设计要求不改 <code>DOM</code> 结构，你先尝试哪条 <code>CSS</code> 修复？</strong></p>\\n<p>可先给圆角容器增加 <code>background-clip: padding-box</code>，限制背景绘制区域，避免背景越过圆角边界。随后要在出现故障的真实 <code>WebView</code> 中验证，因为该修复针对特定圆角渲染现象；若问题来自子元素溢出，还需另查裁剪关系。</p>\\n</li>\\n<li>\\n<p><strong>客服反馈用户刷新活动页后仍看到旧文案，接口已经返回新数据，开发说“手机浏览器缓存太强”；你会如何区分页面缓存和接口问题？</strong></p>\\n<p>先对比首次加载、刷新时的网络请求及响应内容，确认浏览器是否根本没有重新向服务器请求资源。确属缓存时再为相应资源设置合适的 <code>no-cache</code> 策略；全站粗暴禁用缓存会增加加载成本，静态资源更应通过版本化更新。</p>\\n</li>\\n<li>\\n<p><strong>产品要求移动站彻底禁止放大缩小，开发只在局部组件里阻止手势事件，这个选型有什么问题？</strong></p>\\n<p>缩放策略应由页面级 <code>viewport</code> 配置统一表达，而不是让各组件分别拦截触摸手势。局部处理容易遗漏、干扰滚动和点击链路；同时禁用缩放会限制用户查看内容的能力，实施前应确认这是明确的产品约束。</p>\\n</li>\\n<li>\\n<p><strong>同一页面同时出现输入框的 <code>IE10</code> 清除叉号、<code>Android</code> 圆角异常和移动端点透，你会不会引入一个全局脚本一次解决？</strong></p>\\n<p>不应把不同层次的兼容现象塞进同一个脚本：清除叉号可用 <code>input:-ms-clear { display: none; }</code>，圆角先用 <code>background-clip</code>，点透则处理事件顺序。按故障层分开修复更易验证；全局补丁可能扩大影响范围并掩盖真实原因。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_6-jsbridge原理是什么-如何设计一个jsbridge\\"><a class=\\"header-anchor\\" href=\\"#_6-jsbridge原理是什么-如何设计一个jsbridge\\">#</a> 6 JSBridge原理是什么？如何设计一个JSBridge？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>JSBridge</code> 是 <code>H5</code> 和原生 <code>App</code> 的<strong>双向通信通道</strong>，解决 <code>Web</code> 拿不到原生能力（相机、定位、支付、存储）的问题</li>\\n<li><code>JS</code> → 原生两种方式：<strong>拦截 <code>URL Scheme</code></strong>（创建隐藏 <code>iframe</code> 跳转，兼容性好但有长度限制）、<strong>注入 API</strong>（<code>iOS</code> 的 <code>WKScriptMessageHandler</code>、安卓的 <code>addJavascriptInterface</code>，现在主流）</li>\\n<li>原生 → <code>JS</code>：原生直接执行 <code>webView.evaluateJavascript(\'window.xxx(...)\')</code></li>\\n<li>因为通信是<strong>异步</strong>的，要设计一套<strong>回调管理</strong>：每次调用生成唯一 <code>callbackId</code>，存进全局映射表，原生回调时按 id 取出执行</li>\\n<li>设计要点：<code>Promise</code> 化封装、超时兜底、协议版本协商（老 App 装不了新方法）、<strong>安全白名单</strong>（限制可调用的方法和来源域名）</li>\\n</ul>\\n<p><strong><code>JSBridge</code> 本质上是 <code>H5</code> 与原生应用之间的双向通信层，让网页能够调用相机、定位等原生能力，也让原生端执行页面里的 <code>JavaScript</code>。</strong> 网页调用原生可拦截自定义 <code>URL Scheme</code>，或由原生向 <code>window</code> 注入 <code>API</code>；原生调用网页则可使用 <code>evaluateJavascript()</code>。设计时应为每次异步调用生成唯一 <code>callbackId</code>，通过映射表找到对应回调，并封装成 <code>Promise</code>。还要补上超时、协议版本协商及方法和来源域名白名单，兼顾旧版应用与安全边界。</p>\\n</section>\\n<h3 id=\\"_6-1-jsbridge原理\\"><a class=\\"header-anchor\\" href=\\"#_6-1-jsbridge原理\\">#</a> 6.1 JSBridge原理</h3>\\n<blockquote>\\n<p><code>JSBridge</code>的作用就是让<code>native</code>可以调用<code>web</code>的<code>js</code>代码，让<code>web</code>可以调用原生的代码，实现数据通信，它在做<code>native</code>代码和js代码相互转换的事情。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210319155725.png\\" alt=\\"\\"></p>\\n<p><strong>实现数据间的通讯关键是以下两点：</strong></p>\\n<ul>\\n<li>将<code>Native</code>端的接口封装成js接口</li>\\n<li>将<code>Web</code>端js接口封装成原生接口</li>\\n</ul>\\n<h3 id=\\"_6-2-jsbridge的核心\\"><a class=\\"header-anchor\\" href=\\"#_6-2-jsbridge的核心\\">#</a> 6.2 JsBridge的核心</h3>\\n<ul>\\n<li>拦截<code>Url</code></li>\\n<li><code>load url(&quot;javascript:js_method()&quot;);</code></li>\\n</ul>\\n<h3 id=\\"_6-3-为什么是-js-bridge\\"><a class=\\"header-anchor\\" href=\\"#_6-3-为什么是-js-bridge\\">#</a> 6.3 为什么是‘JS’Bridge</h3>\\n<blockquote>\\n<p>因为Web端支持JavaScript，而<code>Native(iOS/Android)</code>端的<code>Webview</code>控件对JavaScript也有所支持，页面加载完成后调用页面的JavaScript代码</p>\\n</blockquote>\\n<h3 id=\\"_6-4-应用场景\\"><a class=\\"header-anchor\\" href=\\"#_6-4-应用场景\\">#</a> 6.4 应用场景</h3>\\n<blockquote>\\n<p>它有什么用？我们在使用混合开发模式(<code>Hybrid App</code>)混合使用<code>Native</code>和<code>Web</code>技术用到。例如目前的使用此技术的主流框架<code>React Native</code>、<code>Weex</code>、微信小程序等</p>\\n</blockquote>\\n<h3 id=\\"_6-5-jsbridge实现-native端调用web端代码\\"><a class=\\"header-anchor\\" href=\\"#_6-5-jsbridge实现-native端调用web端代码\\">#</a> 6.5 JSBridge实现 —— Native端调用Web端代码</h3>\\n<blockquote>\\n<p><code>WebView</code>是<code>Native</code>中加载网页的一个控件，该组件提供一个<code>evaluateJavascript()</code>方法运行JS代码。我们要做的是在Native端执行一个js方法，在Web端进行监听</p>\\n</blockquote>\\n<p><strong>1. 执行一段JS代码</strong></p>\\n<pre class=\\"language-js\\"><code>webView.evaluateJavascript(&quot;window.showWebDialog(&#39;123&#39;)&quot;,null);\\n</code></pre>\\n<p><strong>2. Web端进行监听</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;script&gt;\\n    window.showWebDialog = text =&gt; window.alert(text);\\n&lt;/script&gt;\\n</code></pre>\\n<h3 id=\\"_6-6-jsbridge实现-web端调用native端代码-拦截url-schema\\"><a class=\\"header-anchor\\" href=\\"#_6-6-jsbridge实现-web端调用native端代码-拦截url-schema\\">#</a> 6.6 JSBridge实现 —— Web端调用Native端代码（拦截URL Schema）</h3>\\n<blockquote>\\n<p>当Web端要请求<code>Native</code>端的方法时，我们首先要自定义一个<code>URL Schema</code>，向Native端发起一个请求，最后在<code>Native</code>端的<code>WebView</code>进行监听，下面我们看看具体实现：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210319160140.png\\" alt=\\"\\"></p>\\n<p><strong>1. URL schema介绍</strong></p>\\n<blockquote>\\n<p><code>URL schema</code> 是类<code>URL</code>的请求格式，如：<code>&lt;protocol&gt;://&lt;domain&gt;/&lt;path&gt;?&lt;query&gt;</code></p>\\n</blockquote>\\n<p>接下来可以自定义通信的<code>URL schema</code>，如：</p>\\n<pre class=\\"language-text\\"><code>jsbridge://&lt;method&gt;?&lt;params&gt;\\njsbridge://showToast?text=hello&amp;a=b\\n</code></pre>\\n<p><strong>2. 发送URL schema请求</strong></p>\\n<blockquote>\\n<p>请求自定义<code>URL Schema</code>方法：<code>jsbridge://showToast?text=</code></p>\\n</blockquote>\\n<p>向Native端发起请求：</p>\\n<pre class=\\"language-html\\"><code>&lt;script&gt;\\n    function showNativeDialog(text) {\\n        window.alert(&#39;jsbridge://showToast?text=&#39; + text);\\n    }\\n&lt;/script&gt;\\n</code></pre>\\n<p><strong>3. Native端实现监听</strong></p>\\n<pre class=\\"language-js\\"><code>   webView.setWebChromeClient(new WebChromeClient() {\\n        @Override\\n        public boolean onJsAlert(WebView view, String url, String message, JsResult result) {\\n            if (!message.startsWith(&quot;jsbridge://&quot;)) {\\n                return super.onJsAlert(view, url, message, result);\\n            }\\n\\n            UrlSchema urlschema = new UrlSchema(message);\\n            if (&quot;showToast&quot;.equals(urlchema.getMethodName())) {\\n                String text = urlschema.getParams(&quot;text&quot;);\\n                Toast.makeText(mContext, text, Toast.LENGTH_LONG).show();\\n            }\\n\\n            result.confirm();\\n            return true;\\n        }\\n    }\\n</code></pre>\\n<h3 id=\\"_6-7-jsbridge实现-web端调用native端代码-注入api\\"><a class=\\"header-anchor\\" href=\\"#_6-7-jsbridge实现-web端调用native端代码-注入api\\">#</a> 6.7 JSBridge实现 —— Web端调用Native端代码（注入api)</h3>\\n<blockquote>\\n<p>注入API 方式的是Native端通过 WebView 提供的接口，向 JavaScript 的 <code>Context（window）</code>中注入对象。在Web中通过注入的对象调用Native方法</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210319160503.png\\" alt=\\"\\"></p>\\n<p><strong>1. 向WebView注入JS对象</strong></p>\\n<p>创建一个JS对象，并实现监听的方法</p>\\n<pre class=\\"language-js\\"><code>class NativeBridge{\\n    private Context context;\\n\\n    NativeBridge(Context context){\\n        this.context = context;\\n    }\\n\\n    @JavascriptInterface\\n    public void showNativeDialog(String text){\\n        Toast.makeText(context,text,Toast.LENGTH_LONG).show();\\n    }\\n}\\n</code></pre>\\n<p>Native端通过WebView的接口注入JS对象</p>\\n<pre class=\\"language-js\\"><code>webView.addJavascriptInterface(new NativeBridge(mContext),&quot;NativeBridge&quot;);\\n</code></pre>\\n<p><strong>2. 通过注入的JS对象调用Native代码</strong></p>\\n<p>Web中获取JS对象，调用Native代码：</p>\\n<pre class=\\"language-html\\"><code>&lt;script&gt;\\n    function showNativeDialog(text) {\\n        //window.alert(&#39;jsbridge://showToast?text=&#39; + text);\\n        window.NativeBridge.showNativeDialog(text);\\n    }\\n&lt;/script&gt;\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>活动页代码直接调用 <code>window.NativeBridge.showNativeDialog(\'成功\')</code>，在普通手机浏览器里却报 <code>NativeBridge is undefined</code>；这能说明原生注入失败吗？</strong></p>\\n<p>不能，普通浏览器本来没有 <code>WebView</code> 注入的 <code>NativeBridge</code> 对象，只有 <code>Native</code> 调用注入接口后它才会出现在 <code>window</code> 上。<code>Web</code> 端应先做存在性判断并准备浏览器降级；只有目标 <code>App</code> 的 <code>WebView</code> 中仍缺失时才排查注入。</p>\\n</li>\\n<li>\\n<p><strong><code>App</code> 内有十几个 <code>H5</code> 页面都要调用原生提示框，你会怎样封装 <code>window.NativeBridge.showNativeDialog(text)</code>，避免业务代码到处直接访问全局对象？</strong></p>\\n<p>应封装统一的 <code>Bridge</code> 入口，由它检查 <code>window.NativeBridge</code> 和方法是否存在，再负责参数转换、错误提示与降级。业务层只调用稳定接口，<code>Native</code> 仍通过 <code>addJavascriptInterface</code> 注入对象；代价是两端必须共同维护方法名和参数约定。</p>\\n</li>\\n<li>\\n<p><strong><code>Android</code> 团队把注入名称从 <code>NativeBridge</code> 改成 <code>AppBridge</code>，但部分旧 <code>H5</code> 仍在缓存中，发布窗口又不能同步，你如何控制兼容范围？</strong></p>\\n<p><code>Native</code> 可在过渡期同时注入旧名称和新名称，或让 <code>Web</code> 封装按明确顺序探测两者，再逐步迁移调用方。不能直接假设所有缓存页面立即更新；双名称会扩大暴露面，因此应设定清理时点并保持方法集合一致。</p>\\n</li>\\n<li>\\n<p><strong>线上只有 <code>App</code> 内打开页面时点击“显示提示”无反应，浏览器控制台也没有明显报错，你按什么顺序排查注入链路？</strong></p>\\n<p>先确认页面确实运行在目标 <code>WebView</code>，再检查 <code>Native</code> 是否执行了 <code>addJavascriptInterface(new NativeBridge(...), &quot;NativeBridge&quot;)</code>。随后验证 <code>window.NativeBridge</code>、<code>showNativeDialog</code> 名称及参数类型，最后查看方法是否带 <code>@JavascriptInterface</code>；任一环节不一致都会导致调用不可达。</p>\\n</li>\\n<li>\\n<p><strong>原生同事建议为省事把几十个设备能力都挂到一个注入对象上，<code>Web</code> 团队则希望按业务随时增加方法，你会如何取舍？</strong></p>\\n<p>应只注入 <code>H5</code> 确实需要且边界清晰的方法，并把调用集中到稳定对象上，而不是无限扩大全局能力集合。注入 <code>API</code> 的优点是 <code>Web</code> 调用直接，但两端耦合于对象名、方法名和参数；能力越多，兼容维护与输入校验负担越大。</p>\\n</li>\\n<li>\\n<p><strong>同一个 <code>H5</code> 既要运行在 <code>App</code> <code>WebView</code>，也要运行在微信和系统浏览器，提示能力该怎样与普通 <code>window.alert</code> 串联？</strong></p>\\n<p>可由封装层优先检测 <code>window.NativeBridge.showNativeDialog</code>，存在时调用原生能力，否则退回 <code>Web</code> 提示组件或 <code>window.alert</code>。这样业务代码不依赖具体容器；但降级交互与原生弹窗表现不同，不能承诺所有环境具有完全一致的能力。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);