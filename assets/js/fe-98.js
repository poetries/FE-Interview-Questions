(window.webpackJsonp=window.webpackJsonp||[]).push([[98],{1205:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：前端路由的本质是监听地址变化、匹配路由表，再驱动视图切换而不请求整页文档</li>\\n<li>原理主线：围绕 「一、React Router基础之his」、「二、react-router的基本原理」、「核心机制」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：深入解析React Router的原理，包括history的实现方式、不同环境下的路由管理、location对象的结构与作用，帮助前端开发者全面理解React Router的底层机制与SEO优化相关知识。</li>\\n<li>边界与代价：<code>hash</code> 与 <code>history</code> 的事件和服务端回退要求不同，鉴权、懒加载与滚动恢复属于路由之上的工程能力</li>\\n<li>工程落地：选型时同时验证首屏直达、前进后退、刷新回源、动态参数和未匹配路由</li>\\n</ul>\\n<p><strong><code>React</code> <code>Router</code> 的本质是监听地址变化、匹配路由规则，再驱动组件树更新，而不是重新请求整页文档。</strong> <code>hash</code> 模式通过 <code>hashchange</code> 感知变化，<code>history</code> 模式依赖 <code>pushState</code>、<code>replaceState</code> 和 <code>popstate</code>，内存模式则自行维护记录。用户点击 <code>Link</code> 时，默认跳转会被拦截，新的 <code>location</code> 经过路由匹配后触发视图重渲染。选择模式时要考虑运行环境，并把文章中的旧组件和生命周期写法视为历史实现。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文若分析 <code>ReactDOM.render</code>、旧生命周期或栈调和，应把它视为理解架构演进的历史路径。<code>React 19</code> 已移除 <code>ReactDOM.render</code>，当前客户端入口使用 <code>createRoot</code>；并发渲染也必须区分可中断的渲染阶段与同步提交阶段。迁移前对照 <a href=\\"https://react.dev/blog/2024/04/25/react-19-upgrade-guide\\">React 19 官方升级指南</a>。</p>\\n<h2 id=\\"一、react-router基础之history\\"><a class=\\"header-anchor\\" href=\\"#一、react-router基础之history\\">#</a> 一、React Router基础之history</h2>\\n<h3 id=\\"_1-1-history介绍\\"><a class=\\"header-anchor\\" href=\\"#_1-1-history介绍\\">#</a> 1.1 History介绍</h3>\\n<blockquote>\\n<p><code>history</code>是一个独立的第三方js库，可以用来兼容在不同浏览器、不同环境下对历史记录的管理，拥有统一的<code>API</code>。具体来说里面的<code>history</code>分为三类</p>\\n</blockquote>\\n<ul>\\n<li>老浏览器的<code>history</code>: 主要通过<code>hash</code>来实现，对应<code>createHashHistory</code></li>\\n<li>高版本浏览器: 通过<code>html5</code>里面的<code>history</code>，对应<code>createBrowserHistory</code></li>\\n<li><code>node</code>环境下: 主要存储在<code>memeory</code>里面，对应<code>createMemoryHistory</code></li>\\n</ul>\\n<blockquote>\\n<p>上面针对不同的环境提供了三个<code>API</code>，但是三个<code>API</code>有一些共性的操作，将其抽象了一个公共的文件<code>createHistory</code></p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// 内部的抽象实现\\nfunction createHistory(options={}) {\\n  ...\\n  return {\\n    listenBefore, // 内部的hook机制，可以在location发生变化前执行某些行为，AOP的实现\\n    listen, // location发生改变时触发回调\\n    transitionTo, // 执行location的改变\\n    push, // 改变location\\n    replace,\\n    go,\\n    goBack,\\n    goForward,\\n    createKey, // 创建location的key，用于唯一标示该location，是随机生成的\\n    createPath,\\n    createHref,\\n    createLocation, // 创建location\\n  }\\n}\\n</code></pre>\\n<blockquote>\\n<p>上述这些方式是<code>history</code>内部最基础的方法，<code>createHashHistory</code>、<code>createBrowserHistory</code>、<code>createMemoryHistory</code>只是覆盖其中的某些方法而已。其中需要注意的是，此时的<code>location</code>跟浏览器原生的<code>location</code>是不相同的，最大的区别就在于里面多了<code>key</code>字段，<code>history</code>内部通过<code>key</code>来进行<code>location</code>的操作</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>function createLocation() {\\n  return {\\n    pathname, // url的基本路径\\n    search, // 查询字段\\n    hash, // url中的hash值\\n    state, // url对应的state字段\\n    action, // 分为 push、replace、pop三种\\n    key // 生成方法为: Math.random().toString(36).substr(2, length)\\n  }\\n}\\n</code></pre>\\n<h3 id=\\"_1-2-内部解析\\"><a class=\\"header-anchor\\" href=\\"#_1-2-内部解析\\">#</a> 1.2 内部解析</h3>\\n<blockquote>\\n<p>三个<code>API</code>的大致的技术实现如下</p>\\n</blockquote>\\n<ul>\\n<li><code>createBrowserHistory</code>: 利用<code>HTML5</code>里面的<code>history</code></li>\\n<li><code>createHashHistory</code>: 通过<code>hash</code>来存储在不同状态下的<code>history</code>信息</li>\\n<li><code>createMemoryHistory</code>: 在内存中进行历史记录的存储`</li>\\n</ul>\\n<h4 id=\\"_1-2-1-执行url前进\\"><a class=\\"header-anchor\\" href=\\"#_1-2-1-执行url前进\\">#</a> 1.2.1 执行URL前进</h4>\\n<ul>\\n<li><code>createBrowserHistory</code>: <code>pushState</code>、<code>replaceState</code></li>\\n<li><code>createHashHistory</code>: <code>location.hash=*** location.replace()</code></li>\\n<li><code>createMemoryHistory</code>: 在内存中进行历史记录的存储</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// 伪代码\\n\\n// createBrowserHistory(HTML5)中的前进实现\\nfunction finishTransition(location) {\\n  ...\\n  const historyState = { key };\\n  ...\\n  if (location.action === &#39;PUSH&#39;) ) {\\n    window.history.pushState(historyState, null, path);\\n  } else {\\n    window.history.replaceState(historyState, null, path)\\n  }\\n}\\n// createHashHistory的内部实现\\nfunction finishTransition(location) {\\n  ...\\n  if (location.action === &#39;PUSH&#39;) ) {\\n    window.location.hash = path;\\n  } else {\\n    window.location.replace(\\n    window.location.pathname + window.location.search + &#39;#&#39; + path\\n  );\\n  }\\n}\\n// createMemoryHistory的内部实现\\nentries = [];\\nfunction finishTransition(location) {\\n  ...\\n  switch (location.action) {\\n    case &#39;PUSH&#39;:\\n      entries.push(location);\\n      break;\\n    case &#39;REPLACE&#39;:\\n      entries[current] = location;\\n      break;\\n}\\n</code></pre>\\n<h4 id=\\"_1-2-2-检测url回退\\"><a class=\\"header-anchor\\" href=\\"#_1-2-2-检测url回退\\">#</a> 1.2.2 检测URL回退</h4>\\n<ul>\\n<li><code>createBrowserHistory</code>: <code>popstate</code></li>\\n<li><code>createHashHistory</code>: <code>hashchange</code></li>\\n<li><code>createMemoryHistory</code>:因为是在内存中操作，跟浏览器没有关系，不涉及<code>UI</code>层面的事情，所以可以直接进行历史信息的回退</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// 伪代码\\n\\n// createBrowserHistory(HTML5)中的后退检测\\nfunction startPopStateListener({ transitionTo }) {\\n  function popStateListener(event) {\\n    ...\\n    transitionTo( getCurrentLocation(event.state) );\\n  }\\n  addEventListener(window, &#39;popstate&#39;, popStateListener);\\n  ...\\n}\\n \\n// createHashHistory的后退检测\\nfunction startPopStateListener({ transitionTo }) {\\n  function hashChangeListener(event) {\\n    ...\\n    transitionTo( getCurrentLocation(event.state) );\\n  }\\n  addEventListener(window, &#39;hashchange&#39;, hashChangeListener);\\n  ...\\n}\\n// createMemoryHistory的内部实现\\nfunction go(n) {\\n  if (n) {\\n    ...\\n    current += n;\\n  const currentLocation = getCurrentLocation();\\n  // change action to POP\\n  history.transitionTo({ ...currentLocation, action: POP });\\n  }\\n}\\n</code></pre>\\n<h4 id=\\"_1-2-3-state的存储\\"><a class=\\"header-anchor\\" href=\\"#_1-2-3-state的存储\\">#</a> 1.2.3 state的存储</h4>\\n<blockquote>\\n<p>为了维护<code>state</code>的状态，将其存储在<code>sessionStorage</code>里面:</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// createBrowserHistory/createHashHistory中state的存储\\nfunction saveState(key, state) {\\n  ...\\n  window.sessionStorage.setItem(createKey(key), JSON.stringify(state));\\n}\\nfunction readState(key) {\\n  ...\\n  json = window.sessionStorage.getItem(createKey(key));\\n  return JSON.parse(json);\\n}\\n// createMemoryHistory仅仅在内存中，所以操作比较简单\\nconst storage = createStateStorage(entries); // storage = {entry.key: entry.state}\\n \\nfunction saveState(key, state) {\\n  storage[key] = state\\n}\\nfunction readState(key) {\\n  return storage[key]\\n}\\n</code></pre>\\n<h3 id=\\"_1-3-小结\\"><a class=\\"header-anchor\\" href=\\"#_1-3-小结\\">#</a> 1.3 小结</h3>\\n<p><strong>路由原理</strong></p>\\n<blockquote>\\n<p>前端路由实现起来其实很简单，本质就是监听 <code>URL</code> 的变化，然后匹配路由规则，显示相应的页面，并且无须刷新。目前单页面使用的路由就只有两种实现方式</p>\\n</blockquote>\\n<ul>\\n<li><code>hash</code> 模式</li>\\n<li><code>history</code> 模式</li>\\n</ul>\\n<blockquote>\\n<ul>\\n<li><code>www.test.com/##/</code> 就是 <code>Hash URL</code>，当 <code>##</code> 后面的哈希值发生变化时，不会向服务器请求数据，可以通过 <code>hashchange</code> 事件来监听到 <code>URL</code> 的变化，从而进行跳转页面。</li>\\n<li><code>History</code>模式是 <code>HTML5</code>新推出的功能，比之 <code>Hash URL</code> 更加美观</li>\\n</ul>\\n</blockquote>\\n<h2 id=\\"二、react-router的基本原理\\"><a class=\\"header-anchor\\" href=\\"#二、react-router的基本原理\\">#</a> 二、react-router的基本原理</h2>\\n<blockquote>\\n<p>实现<code>URL</code>与<code>UI</code>界面的同步。其中在<code>react-router</code>中，<code>URL</code>对应<code>Location</code>对象，而<code>UI</code>是由<code>react components</code>来决定的，这样就转变成<code>location</code>与<code>components</code>之间的同步问题</p>\\n</blockquote>\\n<h3 id=\\"_2-1-优点\\"><a class=\\"header-anchor\\" href=\\"#_2-1-优点\\">#</a> 2.1 优点</h3>\\n<ul>\\n<li>与<code>React</code>融为一体,专为<code>react</code>量身打造，编码风格与<code>react</code>保持一致，例如路由的配置可以通过<code>component</code>来实现</li>\\n<li>不需要手工维护路由<code>state</code>，使代码变得简单</li>\\n<li>强大的路由管理机制，体现在如下方面\\n<ul>\\n<li>路由配置: 可以通过组件、配置对象来进行路由的配置</li>\\n<li>路由切换: 可以通过<code>&lt;Link&gt;</code> <code>Redirect</code>进行路由的切换</li>\\n<li>路由加载: 可以同步记载，也可以异步加载，这样就可以实现按需加载</li>\\n</ul>\\n</li>\\n<li>使用方式: 不仅可以在浏览器端的使用，而且可以在服务器端的使用</li>\\n</ul>\\n<h3 id=\\"_2-2-react-router具体实现\\"><a class=\\"header-anchor\\" href=\\"#_2-2-react-router具体实现\\">#</a> 2.2 react-router具体实现</h3>\\n<blockquote>\\n<p><code>react-router</code>在<code>history</code>库的基础上，实现了<code>URL</code>与<code>UI</code>的同步，分为两个层次来描述具体的实现。</p>\\n</blockquote>\\n<p><strong>组件层面描述实现过程</strong></p>\\n<blockquote>\\n<p>在<code>react-router</code>中最主要的<code>component</code>是<code>Router RouterContext Link</code>，<code>history</code>库起到了中间桥梁的作用</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/12/9df13e6c236538bd.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>以<code>browserHistory</code>(一种<code>history</code>类型:一个 <code>history</code> 知道如何去监听浏览器地址栏的变化， 并解析这个 <code>URL</code> 转化为 <code>location</code> 对象)为例 :</p>\\n</blockquote>\\n<ul>\\n<li><code>browserHistory</code>进行路由<code>state</code>管理,主要通过<code>sessionStorage</code></li>\\n</ul>\\n<pre class=\\"language-js\\"><code>//保存　路由state(router state)\\nfunction saveState(key, state) {\\n  ...\\n  window.sessionStorage.setItem(createKey(key), JSON.stringify(state));\\n}\\n//读取路由state\\nfunction readState(key) {\\n  ...\\n  json = window.sessionStorage.getItem(createKey(key));\\n  return JSON.parse(json);\\n}\\n</code></pre>\\n<blockquote>\\n<p>其中<code>saveState</code>函数传进来的<code>state</code>是个<code>json</code>对象，如：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>{route: &#39;/about&#39;} ///假设此时的location为&#39;/about&#39;\\n</code></pre>\\n<blockquote>\\n<p>进行路由匹配，最终渲染对应的组件</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const About = React.createClass({/*...*/}) //About 组件\\nconst Inbox = React.createClass({/*...*/}) //Inbox 组件\\nconst Home = React.createClass({/*...*/}) //Home组件\\n render() {\\n    let Child\\n    switch (this.state.route) {\\n      case &#39;/about&#39;: Child = About; break;\\n      case &#39;/inbox&#39;: Child = Inbox; break;\\n      default:      Child = Home;\\n    }\\n\\n    return (\\n      &lt;div&gt;\\n        &lt;h1&gt;App&lt;/h1&gt;\\n        &lt;ul&gt;\\n          &lt;li&gt;&lt;a href=&quot;#/about&quot;&gt;About&lt;/a&gt;&lt;/li&gt;\\n          &lt;li&gt;&lt;a href=&quot;#/inbox&quot;&gt;Inbox&lt;/a&gt;&lt;/li&gt;\\n        &lt;/ul&gt;\\n        &lt;Child/&gt;\\n      &lt;/div&gt;\\n    )\\n  }\\n})\\n\\nReact.render(&lt;App /&gt;, document.body)\\n</code></pre>\\n<p><strong>API层面描述实现过程</strong></p>\\n<blockquote>\\n<p>为了简单说明，只描述使用<code>browserHistory</code>的实现，<code>hashHistory</code>的实现过程是类似的，就不在说明</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/12/50910ca45a7ce35a.png\\" alt=\\"\\"></p>\\n<h3 id=\\"_2-3-用户点击了link组件后路由系统中到底发生了哪些变化\\"><a class=\\"header-anchor\\" href=\\"#_2-3-用户点击了link组件后路由系统中到底发生了哪些变化\\">#</a> 2.3 用户点击了Link组件后路由系统中到底发生了哪些变化</h3>\\n<p><img src=\\"https://s.poetries.top/gitee/2019/10/429.png\\" alt=\\"img\\"></p>\\n<blockquote>\\n<p><code>Link</code> 组件最终会渲染为 <code>HTML</code> 标签 <code>&lt;a&gt;</code>，它的 <code>to</code>、<code>query</code>、<code>hash</code>属性会被组合在一起并渲染为 <code>href</code> 属性。虽然 <code>Link</code> 被渲染为超链接，但在内部实现上使用脚本拦截了浏览器的默认行为，然后调用了<code>history.pushState</code> 方法</p>\\n</blockquote>\\n<ul>\\n<li>系统会将上述 <code>location</code>对象作为参数传入到 <code>TransitionTo</code>方法中，然后调用 <code>window.location.hash</code> 或者<code>window.history.pushState()</code> 修改了应用的 <code>URL</code>，这取决于你创建<code>history</code>对象的方式。同时会触发<code>history.listen</code> 中注册的事件监听器。</li>\\n<li>接下来请看路由系统内部是如何修改<code>UI</code> 的。在得到了新的<code>location</code>对象后，系统内部的 <code>matchRoutes</code> 方法会匹配出<code>Route</code> 组件树中与当前<code>location</code>对象匹配的一个子集，并且得到了 <code>nextState</code>，具体的匹配算法不在这里讲解，感兴趣的同学可以点击查看，<code>state</code> 的结构如下</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>nextState = {\\n  location, // 当前的 location 对象\\n  routes, // 与 location 对象匹配的 Route 树的子集，是一个数组\\n  params, // 传入的 param，即 URL 中的参数\\n  components, // routes 中每个元素对应的组件，同样是数组\\n};\\n</code></pre>\\n<blockquote>\\n<p>在 <code>Router</code> 组件的 <code>componentWillMount</code> 生命周期方法中调用了 <code>history.listen(listener)</code> 方法。<code>listener</code> 会在上述 <code>matchRoutes</code> 方法执行成功后执行<code>listener(nextState)</code>，<code>nextState</code>对象每个属性的具体含义已经在上述代码中注释，接下来执行<code>this.setState(nextState)</code> 就可以实现重新渲染 <code>Router</code>组件。举个简单的例子，当 URL（准确的说应该是 <code>location.pathname</code>） 为 <code>/archives/posts</code> 时，应用的匹配结果如下图所示</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2019/10/430.png\\" alt=\\"img\\"></p>\\n<blockquote>\\n<p>到这里，系统已经完成了当用户点击一个由 <code>Link</code> 组件渲染出的超链接到页面刷新的全过程</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品页里的 <code>Link</code> 最终渲染成 <code>&lt;a href=&quot;/orders?status=paid#detail&quot;&gt;</code>，同事认为点击后一定会整页刷新；你会怎样说明实际链路？</strong></p>\\n<p><code>Link</code> 虽然最终是 <code>&lt;a&gt;</code>，且 <code>to</code>、<code>query</code>、<code>hash</code> 会组合进 <code>href</code>，但内部脚本会拦截浏览器默认跳转。随后路由系统根据 <code>history</code> 的创建方式修改 <code>window.location.hash</code> 或调用 <code>window.history.pushState()</code>，再由监听器推进匹配与 <code>UI</code> 更新；拦截未生效时才可能退回普通导航。</p>\\n</li>\\n<li>\\n<p><strong>后台新增订单详情路由后，地址栏已经变成 <code>/orders/42</code>，但页面仍停在列表；你会沿着哪些节点检查？</strong></p>\\n<p>先确认 <code>URL</code> 变化是否触发了 <code>history.listen</code> 注册的监听器，再检查新的 <code>location</code> 是否进入 <code>matchRoutes</code>。随后核对产出的 <code>nextState.routes</code>、<code>params</code> 和 <code>components</code>，以及 <code>listener(nextState)</code> 是否执行到 <code>this.setState(nextState)</code>；任一环节中断都可能造成地址与 <code>UI</code> 脱节。</p>\\n</li>\\n<li>\\n<p><strong>应用从 <code>hash</code> 路由改成 <code>history</code> 路由后，业务方要求点击 <code>Link</code> 的后续逻辑完全不变；哪些部分可复用，哪些部分必须重新确认？</strong></p>\\n<p>后续仍是由新 <code>location</code> 触发监听、路由匹配并生成 <code>nextState</code>，再更新 <code>Router</code> 完成重渲染。变化主要在 <code>URL</code> 修改方式从 <code>window.location.hash</code> 转为 <code>window.history.pushState()</code>；还应重新验证监听是否触发，文中没有覆盖服务端直达回退等部署约束。</p>\\n</li>\\n<li>\\n<p><strong>线上偶发点击导航后 <code>URL</code> 正确、路由参数却还是上一次的值，你会如何制作最小证据链，而不是先怀疑页面组件？</strong></p>\\n<p>应为一次点击串起 <code>Link</code> 生成的目标地址、传入 <code>TransitionTo</code> 的 <code>location</code>、<code>matchRoutes</code> 输出的 <code>nextState.params</code> 和监听器收到的对象。若参数在匹配阶段已经错误，就排查 <code>location</code> 构造与路由配置；若 <code>nextState</code> 正确而页面陈旧，再检查 <code>setState</code> 后的组件消费链路。</p>\\n</li>\\n<li>\\n<p><strong>设计路由状态时，有人建议页面组件只读取 <code>window.location.pathname</code>，不再使用 <code>Router</code> 提供的匹配结果；你会接受吗？</strong></p>\\n<p>不建议绕开路由系统，因为一次匹配不仅产生 <code>location</code>，还会得到匹配的 <code>routes</code>、<code>URL</code> <code>params</code> 和对应 <code>components</code>。这些数据作为同一个 <code>nextState</code> 进入 <code>Router</code> 更新，能保持视图与路由结果一致；页面自行解析 <code>pathname</code> 会复制匹配逻辑并增加状态不一致风险。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);