(window.webpackJsonp=window.webpackJsonp||[]).push([[130],{1237:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：虚拟节点是对界面结构的描述，渲染器通过挂载与更新把声明式描述映射为宿主环境操作</li>\\n<li>原理主线：围绕 「自定义渲染器的原理」、「自定义渲染器的应用」、「References」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：解析Vue自定义渲染器的实现原理，讲解如何通过抽象接口实现跨平台渲染，并介绍异步渲染的机制和应用场景，帮助开发者理解和扩展Vue的渲染能力。</li>\\n<li>边界与代价：<code>key</code>、节点类型和稳定序列决定复用边界；虚拟 DOM 解决可预测更新，不保证永远比手写 DOM 快</li>\\n<li>工程落地：性能分析要看实际提交次数、节点移动和组件边界，不能只用“减少 DOM 操作”解释</li>\\n</ul>\\n<p><strong>自定义渲染器的本质，是把平台相关操作从 <code>mount</code> 和 <code>patch</code> 中抽出来，让核心渲染算法不再直接依赖浏览器 <code>DOM</code>。</strong> <code>createRenderer</code> 通过 <code>nodeOps</code> 接收节点创建、插入、删除和查询能力，再通过 <code>patchData</code> 处理元素自身属性，因此换一套实现就能面向不同宿主环境。<code>Web</code> 端可以把这些接口映射到 <code>createElement</code>、<code>insertBefore</code> 等 <code>API</code>，无 <code>DOM</code> 测试环境也可以用普通对象表示元素。异步渲染还需要调度器决定执行时机，而渲染器负责把组件挂载、卸载、<code>DOM</code> 操作及 <code>ref</code> 等后置工作安排到合适队列。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文出现 <code>Object.defineProperty</code>、<code>Dep</code>、<code>Watcher</code> 和双端 <code>Diff</code> 时，主要描述 <code>Vue 2</code> 实现；<code>Vue 3</code> 使用 <code>Proxy</code>、<code>effect</code> 与新的渲染器路径。<code>Vue 2</code> 已于 <code>2023-12-31</code> 结束维护，新项目应以 <code>Vue 3</code> 为基线，旧项目参考 <a href=\\"https://v2.vuejs.org/eol/\\">Vue 2 EOL 官方说明</a> 制定迁移与安全策略。</p>\\n<blockquote>\\n<p>在本章之前，我们花费了很大的篇幅全面的讲解了一个普通渲染器的实现原理，它可以将 <code>Virtual DOM</code> 渲染为 Web 平台的真实 DOM。本章我们将在上一章的基础上讲解更加高级的渲染器：自定义渲染器(<code>Custom renderer</code>)以及异步渲染。</p>\\n</blockquote>\\n<h2 id=\\"自定义渲染器的原理\\"><a class=\\"header-anchor\\" href=\\"#自定义渲染器的原理\\">#</a> 自定义渲染器的原理</h2>\\n<p>渲染器是围绕 <code>Virtual DOM</code> 而存在的，在 Web 平台下它能够把 <code>Virtual DOM</code> 渲染为浏览器中的真实 DOM 对象，通过前面几章的讲解，相信你已经能够认识到渲染器的实现原理，为了能够将 <code>Virtual DOM</code> 渲染为真实 DOM，渲染器内部需要调用浏览器提供的 DOM 编程接口，下面罗列了在出上一章中我们曾经使用到的那些浏览器为我们提供的 DOM 编程接口：</p>\\n<ul>\\n<li><code>document.createElement / createElementNS</code>：创建标签元素。</li>\\n<li><code>document.createTextNode</code>：创建文本元素。</li>\\n<li><code>el.nodeValue</code>：修改文本元素的内容。</li>\\n<li><code>el.removeChild</code>：移除 DOM 元素。</li>\\n<li><code>el.insertBefore</code>：插入 DOM 元素。</li>\\n<li><code>el.appendChild</code>：追加 DOM 元素。</li>\\n<li><code>el.parentNode</code>：获取父元素。</li>\\n<li><code>el.nextSibling</code>：获取下一个兄弟元素。</li>\\n<li><code>document.querySelector</code>：挂载 <code>Portal</code> 类型的 <code>VNode</code> 时，用它查找挂载点。</li>\\n</ul>\\n<p>这些 DOM 编程接口完成了 Web 平台(或者说浏览器)下对 DOM 的增加、删除、查找的工作，它是 Web 平台独有的，所以如果渲染器自身强依赖于这些方法(函数)，那么这个渲染器也只能够运行在浏览器中，它不具备跨平台的能力。换句话说，如果想要实现一个平台无关的渲染器，那么渲染器自身必须不能强依赖于任何一个平台下特有的接口，而是应该提供一个抽象层，将 “DOM” 的增加、删除、查找等操作使用抽象接口实现，具体到某个平台下时，由开发者决定如何使用该平台下的接口实现这个抽象层，这就是自定义渲染器的本质。</p>\\n<div class=\\"tip\\">\\n<p>在下文中，我们将使用 “元素” 一词指代所有平台中的元素对象，例如在 Web 平台下 “元素” 一词指的就是 DOM 元素。</p>\\n</div>\\n<p>渲染器除了负责对元素的增加、删除、查找之外，它还负责修改某个特定元素自身的属性/特性，例如 Web 平台中元素具有 <code>id</code>、<code>href</code> 等属性/特性。在上一章中，我们使用 <code>patchData</code> 函数来完成元素自身属性/特性的更新，如下代码用于修改一个元素的类名列表(<code>class</code>)：</p>\\n<pre class=\\"language-js\\"><code>// patchData.js\\ncase &#39;class&#39;:\\n  el.className = nextValue\\n  break\\n</code></pre>\\n<p>这段代码同样也只能运行在浏览器中，为了渲染器能够跨平台，那么修改一个元素自身的属性/特性的工作也应该作为可自定义的一部分才行，因此，一个跨平台的渲染器应该至少包含两个可自定义的部分：<strong>可自定义元素的增加、删除、查找等操作</strong>、<strong>可自定义元素自身属性/特性的修改操作</strong>。这样对于任何一个元素来说，它的增删改查都已经变成了可自定义的部分，我们只需要“告知”渲染器在对元素进行增删改查时应该做哪些具体的操作即可。</p>\\n<p>接下来我们就着手将一个普通渲染器修改为拥有自定义能力的渲染器，在之前的讲解中，我们将渲染器的代码存放在了 <code>render.js</code> 文件中，如下是整个 <code>render.js</code> 文件的核心代码：</p>\\n<pre class=\\"language-js\\"><code>// 导出渲染器\\nexport default function render(vnode, container) { /* ... */ }\\n\\n// ========== 挂载 ==========\\n\\nfunction mount(vnode, container, isSVG, refNode) { /* ... */ }\\n\\nfunction mountElement(vnode, container, isSVG, refNode) { /* ... */ }\\n\\nfunction mountText(vnode, container) { /* ... */ }\\n\\nfunction mountFragment(vnode, container, isSVG) { /* ... */ }\\n\\nfunction mountPortal(vnode, container) { /* ... */ }\\n\\nfunction mountComponent(vnode, container, isSVG) { /* ... */ }\\n\\nfunction mountStatefulComponent(vnode, container, isSVG) { /* ... */ }\\n\\nfunction mountFunctionalComponent(vnode, container, isSVG) { /* ... */ }\\n\\n// ========== patch ==========\\n\\nfunction patch(prevVNode, nextVNode, container) { /* ... */ }\\n\\nfunction replaceVNode(prevVNode, nextVNode, container) { /* ... */ }\\n\\nfunction patchElement(prevVNode, nextVNode, container) { /* ... */ }\\n\\nfunction patchChildren(\\n  prevChildFlags,\\n  nextChildFlags,\\n  prevChildren,\\n  nextChildren,\\n  container\\n) { /* ... */ }\\n\\nfunction patchText(prevVNode, nextVNode) { /* ... */ }\\n\\nfunction patchFragment(prevVNode, nextVNode, container) { /* ... */ }\\n\\nfunction patchPortal(prevVNode, nextVNode) { /* ... */ }\\n\\nfunction patchComponent(prevVNode, nextVNode, container) { /* ... */ }\\n\\n// https://en.wikipedia.org/wiki/Longest_increasing_subsequence\\nfunction lis(arr) { /* ... */ }\\n</code></pre>\\n<p>观察如上代码结构，可以发现一个渲染器由两部分组成：<code>mount</code> 和 <code>patch</code>。在 <code>mount</code> 和 <code>patch</code> 中都会调用浏览器提供的 DOM 编程接口来完成真正的渲染工作。为了将浏览器提供的 DOM 编程接口与渲染器的代码分离，我们可以将如上代码封装到一个叫做 <code>createRenderer</code> 的函数中，如下代码所示：</p>\\n<pre class=\\"language-js\\"><code>export default function createRenderer(options) {\\n  function render(vnode, container) { /* ... */ }\\n\\n  // ========== 挂载 ==========\\n\\n  function mount(vnode, container, isSVG, refNode) { /* ... */ }\\n\\n  function mountElement(vnode, container, isSVG, refNode) { /* ... */ }\\n\\n  function mountText(vnode, container) { /* ... */ }\\n\\n  function mountFragment(vnode, container, isSVG) { /* ... */ }\\n\\n  function mountPortal(vnode, container) { /* ... */ }\\n\\n  function mountComponent(vnode, container, isSVG) { /* ... */ }\\n\\n  function mountStatefulComponent(vnode, container, isSVG) { /* ... */ }\\n\\n  function mountFunctionalComponent(vnode, container, isSVG) { /* ... */ }\\n\\n  // ========== patch ==========\\n\\n  function patch(prevVNode, nextVNode, container) { /* ... */ }\\n\\n  function replaceVNode(prevVNode, nextVNode, container) { /* ... */ }\\n\\n  function patchElement(prevVNode, nextVNode, container) { /* ... */ }\\n\\n  function patchChildren(\\n    prevChildFlags,\\n    nextChildFlags,\\n    prevChildren,\\n    nextChildren,\\n    container\\n  ) { /* ... */ }\\n\\n  function patchText(prevVNode, nextVNode) { /* ... */ }\\n\\n  function patchFragment(prevVNode, nextVNode, container) { /* ... */ }\\n\\n  function patchPortal(prevVNode, nextVNode) { /* ... */ }\\n\\n  function patchComponent(prevVNode, nextVNode, container) { /* ... */ }\\n\\n  return { render }\\n}\\n\\n// https://en.wikipedia.org/wiki/Longest_increasing_subsequence\\nfunction lis(arr) { /* ... */ }\\n</code></pre>\\n<p><code>createRenderer</code> 函数的返回值就是之前的 <code>render</code> 函数，也就是说调用 <code>createRenderer</code> 函数可以创建一个渲染器。<code>createRenderer</code> 函数接收一个参数 <code>options</code>，该参数的作用是为了允许外界有能力将操作元素的具体实现以选项的方式传递进来。</p>\\n<p>那么 <code>options</code> 参数中应该包含哪些选项呢？其实前面我们已经分析过了，只要是需要自定义的部分就应该作为选项传递进来，所以参数 <code>options</code> 中至少要包含两部分：一部分是元素的增加、删除、查找；另外一部分是元素的修改，即 <code>patchData</code> 函数。如下代码所示：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  // nodeOps 是一个对象，该对象包含了所有用于操作节点的方法\\n  nodeOps: {\\n    createElement() { /* ... */ },\\n    createText() { /* ... */ }\\n    // more...\\n  },\\n  patchData\\n})\\n</code></pre>\\n<p>基于此，在 <code>createRenderer</code> 函数内部我们就可以通过解构的方式从 <code>options</code> 参数中得到具体的方法：</p>\\n<pre class=\\"language-js\\"><code>export default function createRenderer(options) {\\n  // options.nodeOps 选项中包含了本章开头罗列的所有操作 DOM 的方法\\n  // options.patchData 选项就是 patchData 函数\\n  const {\\n    nodeOps: {\\n      createElement: platformCreateElement,\\n      createText: platformCreateText,\\n      setText: platformSetText, // 等价于 Web 平台的 el.nodeValue\\n      appendChild: platformAppendChild,\\n      insertBefore: platformInsertBefore,\\n      removeChild: platformRemoveChild,\\n      parentNode: platformParentNode,\\n      nextSibling: platformNextSibling,\\n      querySelector: platformQuerySelector\\n    },\\n    patchData: platformPatchData\\n  } = options\\n\\n  function render(vnode, container) { /* ... */ }\\n\\n  // ========== 挂载 ==========\\n  // 省略...\\n\\n  // ========== patch ==========\\n  // 省略...\\n\\n  return { render }\\n}\\n</code></pre>\\n<p>如上代码所示，<code>options.nodeOps</code> 选项是一个对象，它包含了所有用于对元素进行增、删、查的操作，<code>options.patchData</code> 选项是一个函数，用于处理某个特定元素上的属性/特定，这些内容都是在创建渲染器时由外界来决定的。</p>\\n<p>接下来我们要做的就是将渲染器中原本使用了 Web 平台进行 DOM 操作的地方修改成使用通过解构得到的函数进行替代，例如在创建 DOM 元素时，原来的实现如下：</p>\\n<pre class=\\"language-js\\"><code>function mountElement(vnode, container, isSVG, refNode) {\\n  isSVG = isSVG || vnode.flags &amp; VNodeFlags.ELEMENT_SVG\\n  const el = isSVG\\n    ? document.createElementNS(&#39;http://www.w3.org/2000/svg&#39;, vnode.tag)\\n    : document.createElement(vnode.tag)\\n  // 省略...\\n}\\n</code></pre>\\n<p>现在我们应该使用 <code>platformCreateElement</code> 函数替代 <code>document.createElement(NS)</code>：</p>\\n<pre class=\\"language-js\\"><code>function mountElement(vnode, container, isSVG, refNode) {\\n  isSVG = isSVG || vnode.flags &amp; VNodeFlags.ELEMENT_SVG\\n  const el = platformCreateElement(vnode.tag, isSVG)\\n  // 省略...\\n}\\n</code></pre>\\n<p>类似的，其他所有涉及 DOM 操作的地方都应该使用这些通过解构得到的抽象接口替代。当这部分工作完成之后，接下来要做的就是对这些用于操作节点的抽象方法进行实现，如下代码所示，我们实现了 Web 平台下创建 DOM 节点的方法：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag, isSVG) {\\n      return isSVG\\n        ? document.createElementNS(&#39;http://www.w3.org/2000/svg&#39;, tag)\\n        : document.createElement(tag)\\n    }\\n  }\\n})\\n</code></pre>\\n<p>再举一个例子，下面这条语句是我们之前实现的渲染器中用于移除旧 <code>children</code> 中节点的代码：</p>\\n<pre class=\\"language-js\\"><code>container.removeChild(prevChildren.el)\\n</code></pre>\\n<p>现在我们将之替换为 <code>platformRemoveChild</code> 函数：</p>\\n<pre class=\\"language-js\\"><code>platformRemoveChild(container, prevVNode.el)\\n</code></pre>\\n<p>为了让这段代码在 Web 平台正常工作，我们需要在创建渲染器时实现 <code>nodeOps.removeChild</code> 函数：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag, isSVG) {\\n      return isSVG\\n        ? document.createElementNS(&#39;http://www.w3.org/2000/svg&#39;, tag)\\n        : document.createElement(tag)\\n    },\\n    removeChild(parent, child) {\\n      parent.removeChild(child)\\n    }\\n  }\\n})\\n</code></pre>\\n<p>也许你已经想到了，当我们实现了所有 <code>nodeOps</code> 下的规定的抽象接口之后，实际上就完成了一个面向 Web 平台的渲染器，如下代码所示：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag, isSVG) {\\n      return isSVG\\n        ? document.createElementNS(&#39;http://www.w3.org/2000/svg&#39;, tag)\\n        : document.createElement(tag)\\n    },\\n    removeChild(parent, child) {\\n      parent.removeChild(child)\\n    },\\n    createText(text) {\\n      return document.createTextNode(text)\\n    },\\n    setText(node, text) {\\n      node.nodeValue = text\\n    },\\n    appendChild(parent, child) {\\n      parent.appendChild(child)\\n    },\\n    insertBefore(parent, child, ref) {\\n      parent.insertBefore(child, ref)\\n    },\\n    parentNode(node) {\\n      return node.parentNode\\n    },\\n    nextSibling(node) {\\n      return node.nextSibling\\n    },\\n    querySelector(selector) {\\n      return document.querySelector(selector)\\n    }\\n  }\\n})\\n</code></pre>\\n<p>当然了，如上代码所创建的渲染器只能够完成 Web 平台中对 DOM 的增加、删除和查找的功能，为了能够修改 DOM 元素自身的属性和特性，我们还需要在创建渲染器时将 <code>patchData</code> 函数作为选项传递过去，好在我们之前已经封装了 <code>patchData</code> 函数，现在直接拿过来用即可：</p>\\n<pre class=\\"language-js\\"><code>import { patchData } from &#39;./patchData&#39;\\nconst { render } = createRenderer({\\n  nodeOps: {\\n    // 省略...\\n  },\\n  patchData\\n})\\n</code></pre>\\n<div class=\\"tip\\">\\n<p>完整代码&amp;在线体验地址：<a href=\\"https://codesandbox.io/s/mq8v65qyry\\">https://codesandbox.io/s/mq8v65qyry</a></p>\\n</div>\\n<p>以上我们就完成了对渲染器的抽象，使它成为一个平台无关的工具。并基于此实现了一个 Web 平台的渲染器，专门用于浏览器环境。</p>\\n<h2 id=\\"自定义渲染器的应用\\"><a class=\\"header-anchor\\" href=\\"#自定义渲染器的应用\\">#</a> 自定义渲染器的应用</h2>\\n<p><code>Vue3</code> 提供了一个叫做 <code>@vue/runtime-test</code> 的包，其作用是方便开发者在无 DOM 环境时有能力对组件的渲染内容进行测试，这实际上就是对自定义渲染器的应用。本节我们尝试来实现与 <code>@vue/runtime-test</code> 具有相同功能的渲染器。</p>\\n<p>原理其实很简单，如下代码所示，这是用于 Web 平台下创建真实 DOM 元素的代码：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag, isSVG) {\\n      return isSVG\\n        ? document.createElementNS(&#39;http://www.w3.org/2000/svg&#39;, tag)\\n        : document.createElement(tag)\\n    }\\n  }\\n})\\n</code></pre>\\n<p>其中 <code>nodeOps.createElement</code> 函数会返回一个真实的 DOM 对象，在其内部调用的是浏览器为我们提供的 <code>document.createElement/NS</code> 函数。实际上 <code>nodeOps.createElement</code> 函数的真正意图是：<strong>创建一个元素</strong>，然而并没有规定这个元素应该由谁来创建，或这个元素应该具有什么样的特征，这就是自定义的核心所在。因此，我们完全使 <code>nodeOps.createElement</code> 函数返回一个普通对象来代指一个元素，后续的所有操作都是基于我们所规定的元素而进行，如下代码所示：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag) {\\n      const customElement = {\\n        type: &#39;ELEMENT&#39;,\\n        tag\\n      }\\n      return customElement\\n    }\\n  }\\n})\\n</code></pre>\\n<p>在这段代码中，我们自行规定了 <code>nodeOps.createElement</code> 函数所返回的元素的格式，即 <code>customElement</code> 对象，它包含两个属性，分别是 用来代表元素类型的 <code>type</code> 属性以及用来代表元素名称的 <code>tag</code> 属性。虽然看上去很奇怪，但这确实是一个完全符合要求的实现。这么做的结果就是：<strong><code>nodeOps.createElement</code> 函数所创建的元素不来自于浏览器的 DOM 编程接口，更不来自于任何其他平台的 API</strong>，因此，如上代码所创建的渲染器也将是一个平台无关的渲染器。这就是为什么 <code>@vue/runtime-test</code> 可以运行在 <code>NodeJs</code> 中的原因。</p>\\n<p>当然了，如上代码中 <code>customElement</code> 只有两个属性，实际上这并不能满足需求，即使元素的格式由我们自行定义，但还是要有一定的限制，例如元素会有子节点，子节点也需要保存对父节点的引用，元素自身也会有属性/特性等等。一个最小且完整的元素定义应该包含以下属性：</p>\\n<pre class=\\"language-js\\"><code>const customElement = {\\n  type, // 元素的类型：ELEMENT ---&gt; 标签元素；TEXT ---&gt; 文本\\n  tag, // 当 type === &#39;ELEMENT&#39; 时，tag 属性为标签名字\\n  parentNode, // 对父节点的引用\\n  children, // 子节点\\n  props,  // 当 type === &#39;ELEMENT&#39; 时，props 中存储着元素的属性/特性\\n  eventListeners,  // 当 type === &#39;ELEMENT&#39; 时，eventListeners 中存储着元素的事件信息\\n  text  // 当 type === &#39;TEXT&#39; 时，text 存储着文本内容\\n}\\n</code></pre>\\n<p>现在 <code>customElement</code> 就是一个能完全代替真实 DOM 对象的模拟实现了，我们用它修改之前的代码：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag) {\\n      const customElement = {\\n        type: &#39;ELEMENT&#39;,\\n        tag,\\n        parentNode: null,\\n        children: [],\\n        props: {},\\n        eventListeners: {},\\n        text: null\\n      }\\n      return customElement\\n    }\\n  }\\n})\\n</code></pre>\\n<p>如上代码所示，由于 <code>nodeOps.createElement</code> 函数用于创建元素节点，因此 <code>type</code> 属性的值为 <code>\'ELEMENT\'</code>；刚刚创建的元素还不能确定其父节点，因此 <code>parentNode</code> 为 <code>null</code>；用于存储子节点的 <code>children</code> 属性被初始化为一个数组，<code>props</code> 属性和 <code>eventListeners</code> 被初始化为空对象；最后的 <code>text</code> 为 <code>null</code>，因为它不是一个文本节点。</p>\\n<p>现在创建元素节点的功能已经实现，那么创建文本节点呢？如下：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag) {\\n      const customElement = {/* 省略... */}\\n      return customElement\\n    },\\n    createText(text) {\\n      const customElement = {\\n        type: &#39;TEXT&#39;,\\n        parentNode: null,\\n        text: text\\n      }\\n      return customElement\\n    }\\n  }\\n})\\n</code></pre>\\n<p>文本元素的 <code>type</code> 类型值为 <code>\'TEXT\'</code>，<code>parentNode</code> 同样被初始化为 <code>unll</code>，<code>text</code> 属性存储着文本节点的内容。由于文本元素没有子节点、属性/特性、事件等信息，因此不需要其他描述信息。</p>\\n<p>文本节点与元素节点的创建都已经实现，接下来我们看看当元素被追加时应该如何处理，即 <code>nodeOps.appendChild</code> 函数的实现：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag) {\\n      const customElement = {/* 省略... */}\\n      return customElement\\n    },\\n    createText(text) {\\n      const customElement = {/* 省略... */}\\n      return customElement\\n    },\\n    appendChild(parent, child) {\\n      // 简历父子关系\\n      child.parentNode = parent\\n      parent.children.push(child)\\n    }\\n  }\\n})\\n</code></pre>\\n<p>如上高亮代码所示，追加节点时我们要做的就是建立节点间正确的父子关系，在 Web 平台下，当我们调用 <code>el.appendChild</code> 函数时，父子关系是由浏览器负责建立的，但在模拟实现中，这个关系需要我们自己来维护。不过好在这很简单，让子元素的 <code>parentNode</code> 指向父元素，同时将子元素添加到父元素的 <code>children</code> 数组中即可。</p>\\n<p>类似的，如下是 <code>nodeOps.removeChild</code> 函数的实现：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag) {/* 省略... */},\\n    createText(text) {/* 省略... */},\\n    appendChild(parent, child) {\\n      // 简历父子关系\\n      child.parentNode = parent\\n      parent.children.push(child)\\n    },\\n    removeChild(parent, child) {\\n      // 找到将要移除的元素 child 在父元素的 children 中的位置\\n      const i = parent.children.indexOf(child)\\n      if (i &gt; -1) {\\n        // 如果找到了，则将其删除\\n        parent.children.splice(i, 1)\\n      } else {\\n        // 没找到，说明渲染器出了问题，例如没有在 nodeOps.appendChild 函数中维护正确的父子关系等\\n        // 这时需要打印错误信息，以提示开发者\\n        console.error(&#39;target: &#39;, child)\\n        console.error(&#39;parent: &#39;, parent)\\n        throw Error(&#39;target 不是 parent 的子节点&#39;)\\n      }\\n      // 清空父子链\\n      child.parentNode = null\\n    }\\n  }\\n})\\n</code></pre>\\n<p>如上高亮代码所示，在移除节点时，思路也很简单，首先需要在父节点的 <code>children</code> 属性中查找即将要被移除的节点的位置索引，如果找到了，那么就直接将其从父节点的 <code>children</code> 数组中移除即可。如果没有找到则说明渲染器出问题了，例如在你实现自定义渲染器时没有在 <code>nodeOps.appendChild</code> 函数或 <code>nodeOps.insertBefore</code> 函数中维护正确的父子关系，这时我们需要打印错误信息以提示开发者。最后不要忘记清空父子链。</p>\\n<p>通过如上的讲解，你可能已经领会到了，我们所做的其实就是在模拟 Web 平台在操作元素时的行为，并且这个模拟的思路也及其简单。实际上，当我们实现了所有 <code>nodeOps</code> 下的抽象函数之后，那么这个类似于 <code>@vue/runtime-test</code> 的自定义渲染器就基本完成了。当然，不要忘记的是我们还需要实现 <code>patchData</code> 函数，这可能比你想象的要简单的多，如下高亮代码所示：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag) {/* 省略... */},\\n    createText(text) {/* 省略... */},\\n    appendChild(parent, child) {/* 省略... */},\\n    removeChild(parent, child) {/* 省略... */}\\n    // 其他 nodeOps 函数的实现\\n  },\\n  patchData(\\n    el,\\n    key,\\n    prevValue,\\n    nextValue\\n  ) {\\n    // 将属性添加到元素的 props 对象下\\n    el.props[key] = nextValue\\n    // 我们将属性名字中前两个字符是 &#39;o&#39; 和 &#39;n&#39; 的属性认为是事件绑定\\n    if (key[0] === &#39;o&#39; &amp;&amp; key[1] === &#39;n&#39;) {\\n      // 如果是事件，则将事件添加到元素的 eventListeners 对象下\\n      const event = key.slice(2).toLowerCase()\\n      ;(el.eventListeners || (el.eventListeners = {}))[event] = nextValue\\n    }\\n  }\\n})\\n</code></pre>\\n<p>在创建渲染器时我们需要实现 <code>patchData</code> 函数的功能，它的功能是用来更新元素自身的属性/特性的，在之前的讲解中我们实现了 Web 平台中 <code>patchData</code> 函数，然而在这个模拟实现中，我们要做的事情就少了很多。只需要把元素的属性添加到元素的 <code>props</code> 对象中即可，同时如果是事件的话，我们也只需要将其添加到元素的 <code>eventListeners</code> 对象中就可以了。</p>\\n<p>实际上，本节我们所实现的自定义渲染器，就能够满足我们对组件测试的需求，我们可以利用它来测试组件所渲染内容的正确性。如果你想要进一步提升该自定义渲染器的能力，例如希望该渲染器有能力在控制台中打印出操作元素的信息，也很简单，我们以创建元素为例，如下代码所示：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag) {\\n      const customElement = {\\n        type: &#39;ELEMENT&#39;,\\n        tag,\\n        parentNode: null,\\n        children: [],\\n        props: {},\\n        eventListeners: {},\\n        text: null\\n      }\\n\\n      console.table({\\n        type: &#39;CREATE ELEMENT&#39;,\\n        targetNode: customElement\\n      })\\n\\n      return customElement\\n    }\\n  }\\n})\\n</code></pre>\\n<p>只需要在 <code>nodeOps.createElement</code> 函数中调用 <code>console.table</code> 进行打印你想要的信息即可，例如我们打印了一个对象，该对象包含 <code>type</code> 属性用于指示当前操作元素的类型，所以对于创建元素来说，我们为 <code>type</code> 属性赋值了字符串 <code>\'CREATE ELEMENT\'</code>，同时将目标节点也打印了出来(即 <code>targetNode</code>)。类似的，追加节点可以打印如下信息：</p>\\n<pre class=\\"language-js\\"><code>const { render } = createRenderer({\\n  nodeOps: {\\n    createElement(tag) {/* 省略... */},\\n    appendChild(parent, child) {\\n      // 简历父子关系\\n      child.parentNode = parent\\n      parent.children.push(child)\\n\\n      console.table({\\n        type: &#39;APPEND&#39;,\\n        targetNode: child,\\n        parentNode: parent\\n      })\\n    }\\n  }\\n})\\n</code></pre>\\n<p>怎么样，是不是很简单。当然了这只是自定义渲染器的应用之一，对于自定义渲染器来说，它可发挥的空间还是非常大的，举几个例子：</p>\\n<ul>\\n<li>渲染到 <code>PDF</code>，我们可以实现一个自定义渲染器如 <code>vue-pdf-renderer</code>，它能够将 <code>Vue</code> 组件渲染为 <code>PDF</code> 文件。</li>\\n<li>渲染到文件系统，我们可以实现一个 <code>vue-file-renderer</code>，它可以根据 <code>VNode</code> 的结构在本地渲染与该结构相同的文件目录。</li>\\n<li><code>canvas</code> 渲染器，我们可以实现一个 <code>vue-canvas-renderer</code>，它可以从渲染器的层面渲染 <code>canvas</code>，而非组件层面。</li>\\n</ul>\\n<p>以上仅仅是简单的列了几个小想法，实际上由于自定义渲染器本身就是平台无关的，很多事情需要看特定平台的能力，渲染器为你提供的就是在组件层面的抽象能力以及虚拟 DOM 的更新算法，剩下的就靠社区的想象力和实现能力了。</p>\\n<div class=\\"tip\\">\\n<p>完整代码&amp;在线体验地址：<a href=\\"https://codesandbox.io/s/v39py7lxq5\\">https://codesandbox.io/s/v39py7lxq5</a></p>\\n</div>\\n<h2 id=\\"references\\"><a class=\\"header-anchor\\" href=\\"#references\\">#</a> References</h2>\\n<ul>\\n<li><a href=\\"https://www.youtube.com/watch?reload=9&amp;v=cCOL7MC4Pl0&amp;feature=youtu.be\\">Jake Archibald: In The Loop - JSConf.Asia 2018</a></li>\\n<li><a href=\\"https://stackoverflow.com/questions/25915634/difference-between-microtask-and-macrotask-within-an-event-loop-context\\">https://stackoverflow.com/questions/25915634/difference-between-microtask-and-macrotask-within-an-event-loop-context</a></li>\\n<li><a href=\\"https://html.spec.whatwg.org/#event-loop-processing-model\\">https://html.spec.whatwg.org/#event-loop-processing-model</a></li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>搜索页在一次点击中依次执行同步状态修改、<code>Promise.then</code> 和 <code>setTimeout</code>，同事断言定时器一定先于界面更新，你怎么判断这句话？</strong></p>\\n<p>不能只凭代码书写顺序判断，浏览器会先执行当前任务，再处理微任务，渲染机会由事件循环处理模型决定，之后才会进入后续任务。<code>Promise.then</code> 通常进入微任务队列，<code>setTimeout</code> 安排后续任务；但是否已发生绘制还受浏览器渲染时机影响，不能把状态提交等同于屏幕已经更新。</p>\\n</li>\\n<li>\\n<p><strong>自定义 <code>Canvas</code> 渲染器连续收到 <code>500</code> 次坐标修改，你会怎样把异步调度接入更新流程，避免每次都立即绘制？</strong></p>\\n<p>我会让状态变更只把渲染作业放入队列，以作业身份去重，并在约定的异步检查点批量执行，确保同一轮多次修改只提交最终必要结果。宿主绘制接口仍由渲染器负责；批处理会降低中间状态的可见性，因此读取布局或要求立即反馈的路径需要明确同步出口。</p>\\n</li>\\n<li>\\n<p><strong>通知面板原来只更新文本，现在新增“修改后立刻测量高度并定位浮层”，继续把更新统一放进微任务会有什么约束？</strong></p>\\n<p>状态更新进入微任务并不保证像素已经绘制，测量动作必须放在宿主节点更新完成之后，并区分“补丁已提交”和“浏览器已完成一帧”。若定位依赖最终布局，可能还需要等待合适的渲染时机；盲目增加 <code>setTimeout</code> 只能改变排队顺序，不能形成可靠契约。</p>\\n</li>\\n<li>\\n<p><strong>线上偶发加载动画不出现，日志显示先设为 <code>true</code>，随后在一个已兑现的 <code>Promise</code> 中又设为 <code>false</code>，你会怎么排查？</strong></p>\\n<p>我会按当前任务、微任务检查点、渲染机会和下一任务还原时间线，并记录两次状态修改何时进入及清空渲染队列。若两次更新在绘制前被合并，页面只会呈现最终的 <code>false</code>；若业务必须让加载态可见，就要调整任务边界或交互策略，但强制多一次绘制会增加闪烁和延迟。</p>\\n</li>\\n<li>\\n<p><strong>组件库作者想用 <code>setTimeout</code> 实现所有异步刷新，业务方则要求更新后尽快拿到稳定视图，你会如何权衡？</strong></p>\\n<p>统一使用 <code>setTimeout</code> 会把工作推到后续任务，语义直观但增加不必要的等待，也可能让多段更新穿插。微任务更适合同一轮状态的集中清空，但长微任务链会推迟渲染和输入响应；应公开明确的刷新完成契约，并限制单次队列工作量，而非依赖某种队列天然更快。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);