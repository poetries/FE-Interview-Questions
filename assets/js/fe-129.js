(window.webpackJsonp=window.webpackJsonp||[]).push([[129],{1237:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：虚拟节点是对界面结构的描述，渲染器通过挂载与更新把声明式描述映射为宿主环境操作</li>\\n<li>原理主线：围绕 「在VNode创建时确定其类型 - fla」、「在VNode创建时确定其children」、「使用 h 函数创建 VNode」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：介绍了Vue中用于辅助创建VNode的h函数的原理、用法及其在虚拟DOM渲染流程中的作用，帮助开发者理解h函数如何提升开发效率及其与模板、JSX的关系。</li>\\n<li>边界与代价：<code>key</code>、节点类型和稳定序列决定复用边界；虚拟 DOM 解决可预测更新，不保证永远比手写 DOM 快</li>\\n<li>工程落地：性能分析要看实际提交次数、节点移动和组件边界，不能只用“减少 DOM 操作”解释</li>\\n</ul>\\n<p><strong><code>h</code> 函数是创建 <code>VNode</code> 的运行时封装，它根据 <code>tag</code>、<code>data</code> 和 <code>children</code> 生成带有明确类型信息的虚拟节点。</strong> 它会从 <code>tag</code> 判断普通元素、组件、<code>Fragment</code> 或 <code>Portal</code>，再根据 <code>children</code> 区分无子节点、单节点和多节点，并把普通文本转换成文本 <code>VNode</code>。多个子节点还会经过规范化处理，为缺少 <code>key</code> 的节点补充标识。需要注意，组件的子内容应转成 <code>slots</code>，不能完全套用普通元素的子节点处理方式。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文出现 <code>Object.defineProperty</code>、<code>Dep</code>、<code>Watcher</code> 和双端 <code>Diff</code> 时，主要描述 <code>Vue 2</code> 实现；<code>Vue 3</code> 使用 <code>Proxy</code>、<code>effect</code> 与新的渲染器路径。<code>Vue 2</code> 已于 <code>2023-12-31</code> 结束维护，新项目应以 <code>Vue 3</code> 为基线，旧项目参考 <a href=\\"https://v2.vuejs.org/eol/\\">Vue 2 EOL 官方说明</a> 制定迁移与安全策略。</p>\\n<blockquote>\\n<p>自从有了 <code>VNode</code> ，开发页面的方式就变成了书写 <code>VNode</code>，但如果日常开发中需要手写 <code>VNode</code> ，那绝对是反人类的，在“组件的本质”一章中我们使用了 <code>snabbdom</code> 的 <code>h</code> 函数来辅助讲解一些小例子，<code>h</code> 函数作为创建 <code>VNode</code> 对象的函数封装，在一定程度上改善了这个问题，但却没有解决本质问题，这也是为什么我们需要模板或 <code>jsx</code> 的原因。但 <code>h</code> 函数依然很重要，因为无论是模板还是 <code>jsx</code> 都需要经过编译，那么是直接编译成 <code>VNode</code> 树好呢？还是编译成由 <code>h</code> 函数组成的调用集合好呢？这个其实很难说，但可以肯定的一点是，我们将可<strong>公用、灵活、复杂的逻辑封装成函数，并交给运行时</strong>，这能够大大降低编译器的书写难度，甚至经过编译器生成的代码也具有一定的可读性，而 <code>h</code> 函数就是众多运行时函数中的一个。</p>\\n</blockquote>\\n<h2 id=\\"在vnode创建时确定其类型-flags\\"><a class=\\"header-anchor\\" href=\\"#在vnode创建时确定其类型-flags\\">#</a> 在VNode创建时确定其类型 - flags</h2>\\n<p>一个最简单的 <code>h</code> 函数如下：</p>\\n<pre class=\\"language-js\\"><code>function h() {\\n  return {\\n    _isVNode: true,\\n    flags: VNodeFlags.ELEMENT_HTML,\\n    tag: &#39;h1&#39;,\\n    data: null,\\n    children: null,\\n    childFlags: ChildrenFlags.NO_CHILDREN,\\n    el: null\\n  }\\n}\\n</code></pre>\\n<p>这个 <code>h</code> 函数只能用来创建一个空的 <code>&lt;h1&gt;&lt;/h1&gt;</code> 标签，可以说没有任何意义。为了让 <code>h</code> 函数更加灵活，我们可以增加一些参数，问题是哪些内容应该提取到参数中呢？如果提取的参数多了，就会导致函数的使用不便，如果提取的参数少了又会导致函数的功能不足，所以这也是一个探索的过程。实际上只需要把 <code>tag</code>、<code>data</code> 和 <code>children</code> 提取为参数即可：</p>\\n<pre class=\\"language-js\\"><code>function h(tag, data = null, children = null) {\\n  //...\\n}\\n</code></pre>\\n<p>我们来看看为什么三个参数就能满足需求，对于 <code>_isVNode</code> 属性，它的值始终都为 <code>true</code>，所以不需要提取到参数中。对于 <code>flags</code> 属性，我们可以通过检查 <code>tag</code> 属性值的特征来确定该 <code>VNode</code> 的 <code>flags</code> 属性值，如下：</p>\\n<pre class=\\"language-js\\"><code>function h(tag, data = null, children = null) {\\n  let flags = null\\n  if (typeof tag === &#39;string&#39;) {\\n    flags = tag === &#39;svg&#39; ? VNodeFlags.ELEMENT_SVG : VNodeFlags.ELEMENT_HTML\\n  }\\n}\\n</code></pre>\\n<p>如上代码所示，如果 <code>tag</code> 是字符串则可以确定该 <code>VNode</code> 是标签元素，再次通过 <code>tag === \'svg\'</code> 进一步判断是否是 <code>SVG</code> 标签，从而确定了该 <code>VNode</code> 的类型。</p>\\n<div class=\\"tip\\">\\n<p>请注意区分下文中出现的 <code>tag</code>，有时指的是 <code>VNode</code> 对象的 <code>tag</code> 属性，有时指的是 <code>h</code> 函数的第一个参数。</p>\\n</div>\\n<p>对于 <code>Fragment</code> 类型的 <strong><code>VNode</code></strong>，它的 <code>tag</code> 属性值为 <code>null</code>，但是纯文本类型的 <code>VNode</code> 其 <code>tag</code> 属性值也是 <code>null</code>，所以为了区分，我们可以增加一个唯一的标识，当 <code>h</code> 函数的第一个参数(<code>tag</code>)的值等于该标识的时候，则意味着创建的是 <code>Fragment</code> 类型的 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>// 唯一标识\\nexport const Fragment = Symbol()\\nfunction h(tag, data = null, children = null) {\\n  let flags = null\\n  if (typeof tag === &#39;string&#39;) {\\n    flags = tag === &#39;svg&#39; ? VNodeFlags.ELEMENT_SVG : VNodeFlags.ELEMENT_HTML\\n  } else if (tag === Fragment) {\\n    flags = VNodeFlags.FRAGMENT\\n  }\\n}\\n</code></pre>\\n<p>这时我们可以像如下这样调用 <code>h</code> 函数创建 <code>Fragment</code>：</p>\\n<pre class=\\"language-js\\"><code>import { h, Fragment } from &#39;vue&#39;\\n\\nh(Fragment, null, children)\\n</code></pre>\\n<p>类似的，对于<code>Portal</code> 类型的 <strong><code>VNode</code></strong>，它的 <code>tag</code> 属性值也可以是字符串，这就会与普通标签元素类型的 <code>VNode</code> 冲突，导致无法区分一个 <code>VNode</code> 到底是普通标签元素还是 <code>Portal</code>，所以我们参考 <code>Fragment</code> 的实现方式，增加一个 <code>Portal</code> 标识：</p>\\n<pre class=\\"language-js\\"><code>export const Fragment = Symbol()\\nexport const Portal = Symbol()\\nfunction h(tag, data = null, children = null) {\\n  let flags = null\\n  if (typeof tag === &#39;string&#39;) {\\n    flags = tag === &#39;svg&#39; ? VNodeFlags.ELEMENT_SVG : VNodeFlags.ELEMENT_HTML\\n  } else if (tag === Fragment) {\\n    flags = VNodeFlags.FRAGMENT\\n  } else if (tag === Portal) {\\n    flags = VNodeFlags.PORTAL\\n    tag = data &amp;&amp; data.target\\n  }\\n}\\n</code></pre>\\n<p>这里需要注意的一点是，类型为 <code>Portal</code> 的 <code>VNode</code>，其 <code>tag</code> 属性值存储的是 <code>Portal</code> 挂载的目标，即 <code>target</code>。通常模板在经过编译后，我们把 <code>target</code> 数据存储在 <code>VNodeData</code> 中，所以我们看到如上代码中包含 <code>tag = data &amp;&amp; data.target</code>。</p>\\n<p>如果一个 <code>VNode</code> 对象的 <code>tag</code> 属性值不满足以上全部条件，那只有一种可能了，即该 <code>VNode</code> 是组件。有的同学可能会说，也有可能是文本节点啊，没错，但是我们很少会用 <code>h</code> 函数去创建一个文本节点，而是单纯的使用字符串，然后在内部实现中检测到该字符串的寓意是文本节点的时候会为其自动创建一个纯文本的 <code>VNode</code> 对象，例如：</p>\\n<pre class=\\"language-js\\"><code>h(\\n  &#39;div&#39;,\\n  {\\n    style: { color: &#39;red&#39; }\\n  },\\n  &#39;我是文本&#39;\\n)\\n</code></pre>\\n<p>如上代码所示，<code>h</code> 函数的第三个参数 <code>children</code> 的值是一段文本字符串，这时在 <code>h</code> 函数内部会为这段文本字符串创建一个与之相符的纯文本 <code>VNode</code> 对象。</p>\\n<p>我们回过头来，继续讨论当一个 <code>VNode</code> 是组件时，如何根据 <code>tag</code> 属性确定该 <code>VNode</code> 对象的 <code>flags</code> 值，很简单如下：</p>\\n<pre class=\\"language-js\\"><code>// 省略...\\nfunction h(tag, data = null, children = null) {\\n  let flags = null\\n  if (typeof tag === &#39;string&#39;) {\\n    flags = tag === &#39;svg&#39; ? VNodeFlags.ELEMENT_SVG : VNodeFlags.ELEMENT_HTML\\n  } else if (tag === Fragment) {\\n    flags = VNodeFlags.FRAGMENT\\n  } else if (tag === Portal) {\\n    flags = VNodeFlags.PORTAL\\n    tag = data &amp;&amp; data.target\\n  } else {\\n    // 兼容 Vue2 的对象式组件\\n    if (tag !== null &amp;&amp; typeof tag === &#39;object&#39;) {\\n      flags = tag.functional\\n        ? VNodeFlags.COMPONENT_FUNCTIONAL       // 函数式组件\\n        : VNodeFlags.COMPONENT_STATEFUL_NORMAL  // 有状态组件\\n    } else if (typeof tag === &#39;function&#39;) {\\n      // Vue3 的类组件\\n      flags = tag.prototype &amp;&amp; tag.prototype.render\\n        ? VNodeFlags.COMPONENT_STATEFUL_NORMAL  // 有状态组件\\n        : VNodeFlags.COMPONENT_FUNCTIONAL       // 函数式组件\\n    }\\n  }\\n}\\n</code></pre>\\n<p>在 <code>Vue2</code> 中用一个对象作为组件的描述，而在 <code>Vue3</code> 中，有状态组件是一个继承了基类的类。所以如果是 <code>Vue2</code> 的对象式组件，我们通过检查该对象的 <code>functional</code> 属性的真假来判断该组件是否是函数式组件。在 <code>Vue3</code> 中，因为有状态组件会继承基类，所以通过原型链判断其原型中是否有 <code>render</code> 函数的定义来确定该组件是否是有状态组件。</p>\\n<p>一旦确定了一个 <code>VNode</code> 的类型，那么 <code>h</code> 函数就可返回带有正确类型的 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>function h(tag, data = null, children = null) {\\n  let flags = null\\n  // 省略用来确定 flags 的代码\\n\\n  return {\\n    flags,\\n    // 其他属性...\\n  }\\n}\\n</code></pre>\\n<h2 id=\\"在vnode创建时确定其children的类型\\"><a class=\\"header-anchor\\" href=\\"#在vnode创建时确定其children的类型\\">#</a> 在VNode创建时确定其children的类型</h2>\\n<p>上文通过 <strong>检测 <code>tag</code> 属性值</strong> 来确定一个 <code>VNode</code> 对象的 <code>flags</code> 属性值。类似地，可以通过 <strong>检测 <code>children</code></strong> 来确定 <code>childFlags</code> 的值。根据 <code>h</code> 函数的调用方式可以很容易地确定参数 <code>children</code> 包含哪些值：</p>\\n<ul>\\n<li>1、<code>children</code> 是一个数组：</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>h(&#39;ul&#39;, null, [\\n  h(&#39;li&#39;),\\n  h(&#39;li&#39;)\\n])\\n</code></pre>\\n<ul>\\n<li>2、<code>children</code> 是一个 <code>VNode</code> 对象：</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>h(&#39;div&#39;, null, h(&#39;span&#39;))\\n</code></pre>\\n<ul>\\n<li>3、没有 <code>children</code>：</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>h(&#39;div&#39;)\\n</code></pre>\\n<ul>\\n<li>4、<code>children</code> 是一个普通文本字符串：</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>h(&#39;div&#39;, null, &#39;我是文本&#39;)\\n</code></pre>\\n<p>以上是四种常见的 <code>h</code> 函数的调用方式，根据这四种调用方式中 <code>children</code> 的不同形式即可确定一个 <code>VNode</code> 对象的 <code>childFlags</code>：</p>\\n<pre class=\\"language-js\\"><code>function h(tag, data = null, children = null) {\\n  // 省略用于确定 flags 相关的代码\\n\\n  let childFlags = null\\n  if (Array.isArray(children)) {\\n    const { length } = children\\n    if (length === 0) {\\n      // 没有 children\\n      childFlags = ChildrenFlags.NO_CHILDREN\\n    } else if (length === 1) {\\n      // 单个子节点\\n      childFlags = ChildrenFlags.SINGLE_VNODE\\n      children = children[0]\\n    } else {\\n      // 多个子节点，且子节点使用key\\n      childFlags = ChildrenFlags.KEYED_VNODES\\n      children = normalizeVNodes(children)\\n    }\\n  } else if (children == null) {\\n    // 没有子节点\\n    childFlags = ChildrenFlags.NO_CHILDREN\\n  } else if (children._isVNode) {\\n    // 单个子节点\\n    childFlags = ChildrenFlags.SINGLE_VNODE\\n  } else {\\n    // 其他情况都作为文本节点处理，即单个子节点，会调用 createTextVNode 创建纯文本类型的 VNode\\n    childFlags = ChildrenFlags.SINGLE_VNODE\\n    children = createTextVNode(children + &#39;&#39;)\\n  }\\n}\\n</code></pre>\\n<p>首先，如果 <code>children</code> 是数组，则根据数组的长度来判断 <code>children</code> 的类型到底是 <code>ChildrenFlags.NO_CHILDREN</code>、<code>ChildrenFlags.SINGLE_VNODE</code> 还是 <code>ChildrenFlags.KEYED_VNODES</code>。这里大家可能会有疑问：“为什么多个子节点时会直接被当做使用了 <code>key</code> 的子节点？”，这是因为 <code>key</code> 是可以人为创造的，如下是 <code>normalizeVNodes</code> 函数的简化：</p>\\n<pre class=\\"language-js\\"><code>function normalizeVNodes(children) {\\n  const newChildren = []\\n  // 遍历 children\\n  for (let i = 0; i &lt; children.length; i++) {\\n    const child = children[i]\\n    if (child.key == null) {\\n      // 如果原来的 VNode 没有key，则使用竖线(|)与该VNode在数组中的索引拼接而成的字符串作为key\\n      child.key = &#39;|&#39; + i\\n    }\\n    newChildren.push(child)\\n  }\\n  // 返回新的children，此时 children 的类型就是 ChildrenFlags.KEYED_VNODES\\n  return newChildren\\n}\\n</code></pre>\\n<p>如上， <code>normalizeVNodes</code> 函数接收 <code>children</code> 数组作为参数，并遍历该数组，对于数组中没有 <code>key</code> 的 <code>VNode</code> 对象，为其添加一个由竖线(<code>|</code>)与该 <code>VNode</code> 在数组中的索引拼接而成的字符串作为该 <code>VNode</code> 的 <code>key</code>。</p>\\n<p>如果 <code>children</code> 不是数组，则判断 <code>children</code> 是否为 <code>null/undefined</code>，条件为真则说明没有子节点，所以 <code>childFlags</code> 的值为 <code>ChildrenFlags.NO_CHILDREN</code>。如果 <code>children</code> 不为 <code>null/undefined</code>，并且 <code>children._isVNode</code> 为真，则说明 <code>children</code> 就是单个 <code>VNode</code> 对象，即单个子节点 <code>ChildrenFlags.SINGLE_VNODE</code>。</p>\\n<p>最后，如果 <code>children</code> 不满足以上任何条件，则会把 <code>children</code> 作为纯文本节点的文本内容处理，这时会使用 <code>createTextVNode</code> 函数创建一个纯文本类型的 <code>VNode</code>，<code>createTextVNode</code> 函数接收一个字符串作为参数，创建一个与之相符的纯文本类型的 <code>VNode</code>，如下：</p>\\n<pre class=\\"language-js\\"><code>function createTextVNode(text) {\\n  return {\\n    _isVNode: true,\\n    // flags 是 VNodeFlags.TEXT\\n    flags: VNodeFlags.TEXT,\\n    tag: null,\\n    data: null,\\n    // 纯文本类型的 VNode，其 children 属性存储的是与之相符的文本内容\\n    children: text,\\n    // 文本节点没有子节点\\n    childFlags: ChildrenFlags.NO_CHILDREN,\\n    el: null\\n  }\\n}\\n</code></pre>\\n<p><strong>这里再次强调！！！</strong> ，以上用于确定 <code>childFlags</code> 的代码仅限于非组件类型的 <code>VNode</code>，因为对于组件类型的 <code>VNode</code> 来说，它并没有子节点，所有子节点都应该作为 <code>slots</code> 存在，所以如果使用 <code>h</code> 函数创建一个组件类型的 <code>VNode</code>，那么我们应该把 <code>children</code> 的内容转化为 <code>slots</code>，然后再把 <code>children</code> 置为 <code>null</code>，这些内容我们会在合适的章节讲解。</p>\\n<h2 id=\\"使用-h-函数创建-vnode\\"><a class=\\"header-anchor\\" href=\\"#使用-h-函数创建-vnode\\">#</a> 使用 h 函数创建 VNode</h2>\\n<div class=\\"tip\\">\\n<p>本章关于 <code>h</code> 函数的完整代码&amp;在线体验地址：<a href=\\"https://codesandbox.io/s/6x2nvmmxn3\\">https://codesandbox.io/s/6x2nvmmxn3</a></p>\\n</div>\\n<p>假设有如下模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;div&gt;\\n    &lt;span&gt;&lt;/span&gt;\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>用 <code>h</code> 函数来创建与之相符的 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>const elementVNode = h(&#39;div&#39;, null, h(&#39;span&#39;))\\n</code></pre>\\n<p>得到的 <code>VNode</code> 对象如下：</p>\\n<pre class=\\"language-js\\"><code>const elementVNode = {\\n  _isVNode: true,\\n  flags: 1, // VNodeFlags.ELEMENT_HTML\\n  tag: &#39;div&#39;,\\n  data: null,\\n  children: {\\n    _isVNode: true,\\n    flags: 1, // VNodeFlags.ELEMENT_HTML\\n    tag: &#39;span&#39;,\\n    data: null,\\n    children: null,\\n    childFlags: 1, // ChildrenFlags.NO_CHILDREN\\n    el: null\\n  },\\n  childFlags: 2, // ChildrenFlags.SINGLE_VNODE\\n  el: null\\n}\\n</code></pre>\\n<blockquote>\\n<p>---- 我是一条分割线(^o^)/~ ---</p>\\n</blockquote>\\n<p>假设有如下模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;div&gt;我是文本&lt;/div&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>用 <code>h</code> 函数来创建与之相符的 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>const elementWithTextVNode = h(&#39;div&#39;, null, &#39;我是文本&#39;)\\n</code></pre>\\n<p>得到的 <code>VNode</code> 对象如下：</p>\\n<pre class=\\"language-js\\"><code>const elementWithTextVNode = {\\n  _isVNode: true,\\n  flags: 1, // VNodeFlags.ELEMENT_HTML\\n  tag: &#39;div&#39;,\\n  data: null,\\n  children: {\\n    _isVNode: true,\\n    flags: 64,  // VNodeFlags.TEXT\\n    tag: null,\\n    data: null,\\n    children: &#39;我是文本&#39;,\\n    childFlags: 1, // ChildrenFlags.NO_CHILDREN\\n    el: null\\n  },\\n  childFlags: 2, // ChildrenFlags.SINGLE_VNODE\\n  el: null\\n}\\n</code></pre>\\n<blockquote>\\n<p>---- 我是一条分割线(^o^)/~ ---</p>\\n</blockquote>\\n<p>假设有如下模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;td&gt;&lt;/td&gt;\\n  &lt;td&gt;&lt;/td&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>我们在之前的章节中曾经遇到过此模板，我们知道这种模板拥有多个根节点，它是一个 <code>Fragment</code>，我们可以像如下这样使用 <code>h</code> 函数来创建 <code>Fragment</code> 类型的 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>import { h, Fragment } from &#39;./h&#39;\\nconst fragmentVNode = h(Fragment, null, [\\n  h(&#39;td&#39;), h(&#39;td&#39;)\\n])\\n</code></pre>\\n<p>得到的 <code>VNode</code> 对象如下：</p>\\n<pre class=\\"language-js\\"><code>const fragmentVNode = {\\n  _isVNode: true,\\n  flags: 128, // VNodeFlags.FRAGMENT\\n  data: null,\\n  children: [\\n    {\\n      _isVNode: true,\\n      flags: 1, // VNodeFlags.ELEMENT_HTML\\n      tag: &#39;td&#39;,\\n      data: null,\\n      children: null,\\n      childFlags: 1,  // ChildrenFlags.NO_CHILDREN\\n      key: &#39;|0&#39;, // 自动生成的 key\\n      el: null\\n    },\\n    {\\n      _isVNode: true,\\n      flags: 1, // VNodeFlags.ELEMENT_HTML\\n      tag: &#39;td&#39;,\\n      data: null,\\n      children: null,\\n      childFlags: 1,  // ChildrenFlags.NO_CHILDREN\\n      key: &#39;|1&#39;, // 自动生成的 key\\n      el: null\\n    }\\n  ],\\n  childFlags: 4, // ChildrenFlags.KEYED_VNODES\\n  el: null\\n}\\n</code></pre>\\n<p>观察如上 <code>VNode</code> 对象可以发现，<code>children</code> 数组中的每一个 <code>VNode</code> 都自动添加了 <code>key</code> 属性。</p>\\n<blockquote>\\n<p>---- 我是一条分割线(^o^)/~ ---</p>\\n</blockquote>\\n<p>假设有如下模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;Portal target=&quot;#box&quot;&gt;\\n    &lt;h1&gt;&lt;/h1&gt;\\n  &lt;/Portal&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>这段模板是一个 <code>Portal</code>，并且会将其内容渲染到 <code>id=&quot;box&quot;</code> 的元素下。我们可以像如下这样使用 <code>h</code> 函数来创建 <code>Portal</code> 类型的 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>import { h, Portal } from &#39;./h&#39;\\nconst portalVNode = h(\\n  Portal,\\n  {\\n    target: &#39;#box&#39;\\n  },\\n  h(&#39;h1&#39;)\\n)\\n</code></pre>\\n<p>得到的 <code>VNode</code> 对象如下：</p>\\n<pre class=\\"language-js\\"><code>const portalVNode = {\\n  _isVNode: true,\\n  flags: 256, // VNodeFlags.PORTAL\\n  tag: &#39;#box&#39;,  // 类型为 Portal 的 VNode，其 tag 属性值等于 data.target\\n  data: { target: &#39;#box&#39; },\\n  children: {\\n    _isVNode: true,\\n    flags: 1, // VNodeFlags.ELEMENT_HTML\\n    tag: &#39;h1&#39;,\\n    data: null,\\n    children: null,\\n    childFlags: 1, // ChildrenFlags.NO_CHILDREN\\n    el: null\\n  },\\n  childFlags: 2, // ChildrenFlags.SINGLE_VNODE\\n  el: null\\n}\\n</code></pre>\\n<p>如上 <code>VNode</code> 对象所示，类型为 <code>Portal</code> 的 <code>VNode</code> 其 <code>tag</code> 属性值等于 <code>data.target</code>。</p>\\n<blockquote>\\n<p>---- 我是一条分割线(^o^)/~ ---</p>\\n</blockquote>\\n<p>假设有如下模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;MyFunctionalComponent&gt;\\n    &lt;div&gt;&lt;/div&gt;\\n  &lt;/MyFunctionalComponent&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>这段模板中包含了一个函数式组件，并为该组件提供了一个空的 <code>div</code> 标签作为默认的插槽内容，我们尝试用 <code>h</code> 函数创建与该模板相符的 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>// 一个函数式组件\\nfunction MyFunctionalComponent() {}\\n\\n// 传递给 h 函数的第一个参数就是组件函数本身\\nconst functionalComponentVNode = h(MyFunctionalComponent, null, h(&#39;div&#39;))\\n</code></pre>\\n<p>来看一下我们最终得到的 <code>VNode</code> 对象：</p>\\n<pre class=\\"language-js\\"><code>const functionalComponentVNode = {\\n  _isVNode: true,\\n  flags: 32,  // VNodeFlags.COMPONENT_FUNCTIONAL\\n  tag: MyFunctionalComponent, // tag 属性值引用组件函数\\n  data: null,\\n  children: {\\n    _isVNode: true,\\n    flags: 1,\\n    tag: &#39;div&#39;,\\n    data: null,\\n    children: null,\\n    childFlags: 1,\\n    el: null\\n  },\\n  childFlags: 2, // ChildrenFlags.SINGLE_VNODE\\n  el: null\\n}\\n</code></pre>\\n<p>大家观察如上 <code>VNode</code> 对象，其中 <code>tag</code> 属性的值就是组件函数本身，另外 <code>functionalComponentVNode.children</code> 的值就是作为默认插槽的空 <code>div</code> 标签的 <code>VNode</code>，我们暂且这样设计，因为它不会影响我们对渲染器的理解，等到讲解插槽的章节时再来说明：为什么我们不使用 <code>children</code> 属性来存储插槽内容，以及我们应该如何使用 <code>VNode</code> 来描述插槽。</p>\\n<blockquote>\\n<p>---- 我是一条分割线(^o^)/~ ---</p>\\n</blockquote>\\n<p>我们再来使用 <code>h</code> 函数创建一个有状态组件的 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>// 有状态组件\\nclass MyStatefulComponent {}\\nconst statefulComponentVNode = h(MyStatefulComponent, null, h(&#39;div&#39;))\\n</code></pre>\\n<p>我们将得到如下 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>const statefulComponentVNode = {\\n  _isVNode: true,\\n  flags: 32,  // VNodeFlags.COMPONENT_FUNCTIONAL\\n  tag: MyStatefulComponent,\\n  data: null,\\n  children: {\\n    _isVNode: true,\\n    flags: 1,\\n    tag: &#39;div&#39;,\\n    data: null,\\n    children: null,\\n    childFlags: 1,\\n    el: null\\n  },\\n  childFlags: 2,\\n  el: null\\n}\\n</code></pre>\\n<p>观察 <code>statefulComponentVNode.flags</code> 属性的值，我们明明使用 <code>h</code> 创建的是有状态组件的 <code>VNode</code>，为什么最终产生的 <code>VNode</code> 是函数式组件呢？别急，大家还记得 <code>h</code> 函数是如何区分有状态组件和函数式组件的吗？如下是我们之前编写的 <code>h</code> 中的一段用来区分函数式组件和有状态组件的代码：</p>\\n<pre class=\\"language-js\\"><code>// Vue3 的类组件\\nflags =\\n  tag.prototype &amp;&amp; tag.prototype.render\\n    ? VNodeFlags.COMPONENT_STATEFUL_NORMAL // 有状态组件\\n    : VNodeFlags.COMPONENT_FUNCTIONAL // 函数式组件\\n</code></pre>\\n<p>只有当组件的原型上拥有 <code>render</code> 函数时才会把它当作有状态组件，这里我们再次说明为什么要这样设计，实际上我们在编写有状态组件时，通常需要继承一个框架提供好的基础组件，如下：</p>\\n<pre class=\\"language-js\\"><code>class MyStatefulComponent extends Component {}\\n</code></pre>\\n<p>其中 <code>Component</code> 组件就是基础组件，而基础组件中会包含 <code>render</code> 函数，如下是 <code>Component</code> 组件的实现：</p>\\n<pre class=\\"language-js\\"><code>class Component {\\n  render() {}\\n}\\n</code></pre>\\n<p>那么基础组件的 <code>render</code> 函数有什么用呢？我们知道对于一个组件来说它的 <code>render</code> 函数就是它的一切，假设一个组件没有 <code>render</code> 函数，那么请问该组件存在的意义是什么？它要渲染的又是什么？所以在设计框架的时候，组件是必须要拥有 <code>render</code> 函数的，如果没有 <code>render</code> 函数我们需要打印错误信息来提示用户，这个“警示”的工作就是由基础组件的 <code>render</code> 函数来完成的，如下：</p>\\n<pre class=\\"language-js\\"><code>class Component {\\n  render() {\\n    throw &#39;组件缺少 render 函数&#39;\\n  }\\n}\\n</code></pre>\\n<p>它是如何起作用的呢？还记得我们在“组件的本质”一章中曾经讲到过的挂载组件的 <code>mountComponent</code> 函数吗：</p>\\n<pre class=\\"language-js\\"><code>function mountComponent(vnode, container) {\\n  // 创建组件实例\\n  const instance = new vnode.tag()\\n  // 渲染\\n  instance.$vnode = instance.render()\\n  // 挂载\\n  mountElement(instance.$vnode, container)\\n}\\n</code></pre>\\n<p>在挂载组件时我们会创建组件实例，并调用组件的 <code>render</code> 函数，由于任何组件都会继承基础组件，所以一旦组件没有 <code>render</code> 函数，那么基础组件的 <code>render</code> 函数将被调用，此时就会抛出一个异常提示用户：“你的组件缺少 <code>render</code> 函数”。</p>\\n<p>实际上，基础组件还会做更多的任务，本章不会展开讨论。大家只需要知道<strong>在设计有状态组件时，我们会设计一个基础组件，所有组件都会继承基础组件，并且基础组件拥有用来报告错误信息的 <code>render</code> 函数</strong>，这就是我们可以通过以下代码来区分函数式组件和有状态组件的原因：</p>\\n<pre class=\\"language-js\\"><code>// Vue3 的类组件\\nflags =\\n  tag.prototype &amp;&amp; tag.prototype.render\\n    ? VNodeFlags.COMPONENT_STATEFUL_NORMAL // 有状态组件\\n    : VNodeFlags.COMPONENT_FUNCTIONAL // 函数式组件\\n</code></pre>\\n<p>了解了这些，我们再来使用 <code>h</code> 函数创建有状态组件的 <code>VNode</code>，如下：</p>\\n<pre class=\\"language-js\\"><code>// 有状态组件应该继承 Component\\nclass MyStatefulComponent extends Component {}\\nconst statefulComponentVNode = h(MyStatefulComponent, null, h(&#39;div&#39;))\\n</code></pre>\\n<p>此时我们得到的 <code>VNode</code> 对象如下：</p>\\n<pre class=\\"language-js\\"><code>const statefulComponentVNode = {\\n  _isVNode: true,\\n  flags: 4, // VNodeFlags.COMPONENT_STATEFUL_NORMAL\\n  data: null,\\n  children: {\\n    _isVNode: true,\\n    flags: 1,\\n    tag: &#39;div&#39;,\\n    data: null,\\n    children: null,\\n    childFlags: 1,\\n    el: null\\n  },\\n  childFlags: 2,\\n  el: null\\n}\\n</code></pre>\\n<p>可以看到 <code>statefulComponentVNode.flags</code> 的值已经修正了。</p>\\n<p>至此，我们的 <code>h</code> 函数已经可以创建任何类型的 <code>VNode</code> 对象了，有了 <code>VNode</code> 对象，我们下一步要做的就是将 <code>VNode</code> 对象渲染成真实 DOM，下一章我们将开启渲染器之旅。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>有人说 <code>h(\'div\', null, \'我是文本\')</code> 的 <code>children</code> 直接保存字符串即可，不需要文本 <code>VNode</code>；你会怎样评审？</strong></p>\\n<p>按当前设计，字符串会被规范化为 <code>VNodeFlags.TEXT</code> 的文本 <code>VNode</code>，文本内容放在其 <code>children</code> 中，父节点标记为 <code>ChildrenFlags.SINGLE_VNODE</code>。统一成 <code>VNode</code> 后，渲染器能按节点类型处理；若直接混用字符串，就要在后续流程反复增加分支。</p>\\n</li>\\n<li>\\n<p><strong>表格组件调用 <code>h(Fragment, null, [h(\'td\'), h(\'td\')])</code>，你预期生成结构里哪些字段决定后续更新？</strong></p>\\n<p>根节点应标记为 <code>VNodeFlags.FRAGMENT</code>，两个 <code>td</code> 保存在数组中，并把 <code>childFlags</code> 设为 <code>ChildrenFlags.KEYED_VNODES</code>。当前实现还会为数组子节点生成类似 <code>|0</code>、<code>|1</code> 的 <code>key</code>；这种位置型键能完成规范化，但重排时未必表达稳定业务身份。</p>\\n</li>\\n<li>\\n<p><strong>弹层原本渲染在当前位置，现在产品要求内容挂到 <code>#box</code>，调用改为 <code>h(Portal, { target: \'#box\' }, h(\'h1\'))</code> 后，<code>VNode</code> 应怎样变化？</strong></p>\\n<p>它应被标记为 <code>VNodeFlags.PORTAL</code>，并将 <code>tag</code> 设置为 <code>data.target</code>，即 <code>#box</code>，子节点仍是单个 <code>h1</code> <code>VNode</code>。渲染器据此把内容挂到目标容器；若目标选择器不存在，当前材料没有规定恢复策略，实现必须显式处理失败边界。</p>\\n</li>\\n<li>\\n<p><strong>线上某个类组件被当成函数式组件执行，代码是 <code>class Panel {}</code> 后传给 <code>h(Panel)</code>，你会优先检查什么？</strong></p>\\n<p>先检查 <code>Panel.prototype.render</code> 是否存在，因为当前 <code>h</code> 以该条件区分有状态组件和函数式组件。空类没有 <code>render</code> 会被标记为 <code>VNodeFlags.COMPONENT_FUNCTIONAL</code>；让它继承带 <code>render</code> 的基础 <code>Component</code>，或自行实现该方法，才能进入有状态组件路径。</p>\\n</li>\\n<li>\\n<p><strong>框架团队争论应该靠类名、构造函数名称还是 <code>prototype.render</code> 判断有状态组件，你会选哪一个并说明代价？</strong></p>\\n<p>在当前设计约束下应使用 <code>tag.prototype &amp;&amp; tag.prototype.render</code>，因为有状态组件统一继承提供 <code>render</code> 的基础类，函数式组件则是普通函数。基础 <code>render</code> 还能在子类缺失实现时抛出明确错误；代价是未遵循该继承或方法约定的类会被错误分类。</p>\\n</li>\\n<li>\\n<p><strong>一个函数式组件通过 <code>h(MyFunctionalComponent, null, h(\'div\'))</code> 接收默认插槽，设计评审却要求立刻建设完整 <code>slots</code> 系统，你会怎么取舍？</strong></p>\\n<p>当前阶段可暂时把默认插槽 <code>VNode</code> 放在组件 <code>VNode</code> 的 <code>children</code> 中，因为这足以支撑渲染器基础流程。完整插槽语义最终不应只靠 <code>children</code> 表达，但应在插槽需求明确后再扩展数据结构；过早加入会把本章的节点分类与渲染职责复杂化。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);