(window.webpackJsonp=window.webpackJsonp||[]).push([[129],{1235:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：虚拟节点是对界面结构的描述，渲染器通过挂载与更新把声明式描述映射为宿主环境操作</li>\\n<li>原理主线：围绕 「用 VNode 描述真实 DOM」、「用 VNode 描述抽象内容」、「VNode 的种类」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：系统讲解了Vue中VNode（虚拟DOM节点）的设计原理，包括如何用VNode描述真实DOM结构、属性、事件和子节点，以及VNode设计对渲染性能和框架架构的影响，帮助开发者深入理解Vue底层渲染机制。</li>\\n<li>边界与代价：<code>key</code>、节点类型和稳定序列决定复用边界；虚拟 DOM 解决可预测更新，不保证永远比手写 DOM 快</li>\\n<li>工程落地：性能分析要看实际提交次数、节点移动和组件边界，不能只用“减少 DOM 操作”解释</li>\\n</ul>\\n<p><strong><code>VNode</code> 本质上是对元素、组件、文本及 <code>Fragment</code>、<code>Portal</code> 等界面内容的结构化描述，渲染器再把它转换成真实宿主操作。</strong> 不同节点需要的字段不同，例如元素保存标签和子节点，组件引用组件本身，<code>Portal</code> 还要记录渲染目标。创建时用位标识记录节点类别，可以让挂载和更新阶段直接分派处理逻辑，减少反复猜测类型。这里的双端 <code>Diff</code> 等内容主要对应 <code>Vue 2</code>；新项目应以 <code>Vue 3</code> 的渲染器实现为基线。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文出现 <code>Object.defineProperty</code>、<code>Dep</code>、<code>Watcher</code> 和双端 <code>Diff</code> 时，主要描述 <code>Vue 2</code> 实现；<code>Vue 3</code> 使用 <code>Proxy</code>、<code>effect</code> 与新的渲染器路径。<code>Vue 2</code> 已于 <code>2023-12-31</code> 结束维护，新项目应以 <code>Vue 3</code> 为基线，旧项目参考 <a href=\\"https://v2.vuejs.org/eol/\\">Vue 2 EOL 官方说明</a> 制定迁移与安全策略。</p>\\n<p>上一章讲述了组件的本质，知道了一个组件的产出是 <code>VNode</code>，渲染器(<code>Renderer</code>)的渲染目标也是 <code>VNode</code>。可见 <code>VNode</code> 在框架设计的整个环节中都非常重要，甚至<strong>设计 <code>VNode</code> 本身就是在设计框架</strong>，<code>VNode</code> 的设计还会对后续算法的性能产生影响。本章我们就着手对 <code>VNode</code> 进行一定的设计，尝试用 <code>VNode</code> 描述各类渲染内容。</p>\\n<h2 id=\\"用-vnode-描述真实-dom\\"><a class=\\"header-anchor\\" href=\\"#用-vnode-描述真实-dom\\">#</a> 用 VNode 描述真实 DOM</h2>\\n<p>一个 <code>html</code> 标签有它的名字、属性、事件、样式、子节点等诸多信息，这些内容都需要在 <code>VNode</code> 中体现，我们可以用如下对象来描述一个红色背景的正方形 <code>div</code> 元素：</p>\\n<pre class=\\"language-js\\"><code>const elementVNode = {\\n  tag: &#39;div&#39;,\\n  data: {\\n    style: {\\n      width: &#39;100px&#39;,\\n      height: &#39;100px&#39;,\\n      backgroundColor: &#39;red&#39;\\n    }\\n  }\\n}\\n</code></pre>\\n<p>我们使用 <code>tag</code> 属性来存储标签的名字，用 <code>data</code> 属性来存储该标签的附加信息，比如 <code>style</code>、<code>class</code>、事件等，通常我们把一个 <code>VNode</code> 对象的 <code>data</code> 属性称为 <code>VNodeData</code>。</p>\\n<p>为了描述子节点，我们需要给 <code>VNode</code> 对象添加 <code>children</code> 属性，如下 <code>VNode</code> 对象用来描述一个有子节点的 <code>div</code> 元素：</p>\\n<pre class=\\"language-js\\"><code>const elementVNode = {\\n  tag: &#39;div&#39;,\\n  data: null,\\n  children: {\\n    tag: &#39;span&#39;,\\n    data: null\\n  }\\n}\\n</code></pre>\\n<p>若有多个子节点，则可以把 <code>children</code> 属性设计为一个数组：</p>\\n<pre class=\\"language-js\\"><code>const elementVNode = {\\n  tag: &#39;div&#39;,\\n  data: null,\\n  children: [\\n    {\\n      tag: &#39;h1&#39;,\\n      data: null\\n    },\\n    {\\n      tag: &#39;p&#39;,\\n      data: null\\n    }\\n  ]\\n}\\n</code></pre>\\n<p>除了标签元素之外，DOM 中还有文本节点，我们可以用如下 <code>VNode</code> 对象来描述一个文本节点：</p>\\n<pre class=\\"language-js\\"><code>const textVNode = {\\n  tag: null,\\n  data: null,\\n  children: &#39;文本内容&#39;\\n}\\n</code></pre>\\n<p>如上，由于文本节点没有标签名字，所以它的 <code>tag</code> 属性值为 <code>null</code>。由于文本节点也无需用额外的 <code>VNodeData</code> 来描述附加属性，所以其 <code>data</code> 属性值也是 <code>null</code>。</p>\\n<p>唯一需要注意的是我们使用 <code>children</code> 属性来存储一个文本节点的文本内容。有的同学可能会问：“可不可以新建一个属性 <code>text</code> 来存储文本内容呢？”</p>\\n<pre class=\\"language-js\\"><code>const textVNode = {\\n  tag: null,\\n  data: null,\\n  children: null,\\n  text: &#39;文本内容&#39;\\n}\\n</code></pre>\\n<p>这完全没有问题，这取决于你如何设计，但是<strong>尽可能的在保证语义能够说得通的情况下复用属性，会使 <code>VNode</code> 对象更加轻量</strong>，所以我们采取使用 <code>children</code> 属性来存储文本内容的方案。</p>\\n<p>如下是一个以文本节点作为子节点的 <code>div</code> 标签的 <code>VNode</code> 对象：</p>\\n<pre class=\\"language-js\\"><code>const elementVNode = {\\n  tag: &#39;div&#39;,\\n  data: null,\\n  children: {\\n    tag: null,\\n    data: null,\\n    children: &#39;文本内容&#39;\\n  }\\n}\\n</code></pre>\\n<h2 id=\\"用-vnode-描述抽象内容\\"><a class=\\"header-anchor\\" href=\\"#用-vnode-描述抽象内容\\">#</a> 用 VNode 描述抽象内容</h2>\\n<p>什么是抽象内容呢？组件就属于抽象内容，比如你在 模板 或 <code>jsx</code> 中使用了一个组件，如下：</p>\\n<pre class=\\"language-html\\"><code>&lt;div&gt;\\n  &lt;MyComponent /&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p>你的意图并不是要在页面中渲染一个名为 <code>MyComponent</code> 的标签元素，而是要渲染 <code>MyComponent</code> 组件所产出的内容。</p>\\n<p>但我们仍然需要使用 <code>VNode</code> 来描述 <code>&lt;MyComponent/&gt;</code>，并给此类用来描述组件的 <code>VNode</code> 添加一个标识，以便在挂载的时候有办法区分一个 <code>VNode</code> 到底是普通的 <code>html</code> 标签还是组件。</p>\\n<p>我们可以使用如下 <code>VNode</code> 对象来描述上面的模板：</p>\\n<pre class=\\"language-js\\"><code>const elementVNode = {\\n  tag: &#39;div&#39;,\\n  data: null,\\n  children: {\\n    tag: MyComponent,\\n    data: null\\n  }\\n}\\n</code></pre>\\n<p>如上，用来描述组件的 <code>VNode</code> 其 <code>tag</code> 属性值引用的就是组件类(或函数)本身，而不是标签名称字符串。所以理论上：<strong>我们可以通过检查 <code>tag</code> 属性值是否是字符串来确定一个 <code>VNode</code> 是否是普通标签</strong>。</p>\\n<p>除了组件之外，还有两种抽象的内容需要描述，即 <code>Fragment</code> 和 <code>Portal</code>。我们先来了解一下什么是 <code>Fragment</code> 以及它所解决的问题。</p>\\n<p><code>Fragment</code> 的寓意是要渲染一个片段，假设我们有如下模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;table&gt;\\n    &lt;tr&gt;\\n      &lt;Columns /&gt;\\n    &lt;/tr&gt;\\n  &lt;/table&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>组件 <code>Columns</code> 会返回多个 <code>&lt;td&gt;</code> 元素：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;td&gt;&lt;/td&gt;\\n  &lt;td&gt;&lt;/td&gt;\\n  &lt;td&gt;&lt;/td&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>大家思考一个问题，如上模板的 <code>VNode</code> 如何表示？如果模板中只有一个 <code>td</code> 标签，即只有一个根元素，这很容易表示：</p>\\n<pre class=\\"language-js\\"><code>const elementVNode = {\\n  tag: &#39;td&#39;,\\n  data: null\\n}\\n</code></pre>\\n<p>但是模板中不仅仅只有一个 <code>td</code> 标签，而是有多个 <code>td</code> 标签，即多个根元素，这如何表示？此时我们就需要引入一个抽象元素，也就是我们要介绍的 <code>Fragment</code>。</p>\\n<pre class=\\"language-js\\"><code>const Fragment = Symbol()\\nconst fragmentVNode = {\\n  // tag 属性值是一个唯一标识\\n  tag: Fragment,\\n  data: null,\\n  children: [\\n    {\\n      tag: &#39;td&#39;,\\n      data: null\\n    },\\n    {\\n      tag: &#39;td&#39;,\\n      data: null\\n    },\\n    {\\n      tag: &#39;td&#39;,\\n      data: null\\n    }\\n  ]\\n}\\n</code></pre>\\n<p>如上，我们把所有 <code>td</code> 标签都作为 <code>fragmentVNode</code> 的子节点，根元素并不是一个实实在在的真实 DOM，而是一个抽象的标识，即 <code>Fragment</code>。</p>\\n<p>当渲染器在渲染 <code>VNode</code> 时，如果发现该 <code>VNode</code> 的类型是 <code>Fragment</code>，就只需要把该 <code>VNode</code> 的子节点渲染到页面。</p>\\n<div class=\\"tip\\">\\n<p>在上面的代码中 <code>fragmentVNode.tag</code> 属性的值是一个通过 <code>Symbol</code> 创建的唯一标识，但实际上我们更倾向于给 <code>VNode</code> 对象添加一个 <code>flags</code> 属性，用来代表该 <code>VNode</code> 的类型，这在本章的后面会详细说明。</p>\\n</div>\\n<p>再来看看 <code>Portal</code>，什么是 <code>Portal</code> 呢？</p>\\n<p>一句话：它允许你把内容渲染到任何地方。其应用场景是，假设你要实现一个蒙层组件 <code>&lt;Overlay/&gt;</code>，要求是该组件的 <code>z-index</code> 的层级最高，这样无论在哪里使用都希望它能够遮住全部内容，你可能会将其用在任何你需要蒙层的地方。</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;div id=&quot;box&quot; style=&quot;z-index: -1;&quot;&gt;\\n    &lt;Overlay /&gt;\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>如上，不幸的事情发生了，在没有 <code>Portal</code> 的情况下，上面的 <code>&lt;Overlay/&gt;</code> 组件的内容只能渲染到 <code>id=&quot;box&quot;</code> 的 <code>div</code> 标签下，这就会导致蒙层的层级失效甚至布局都可能会受到影响。</p>\\n<p>其实解决办法也很简单，假如 <code>&lt;Overlay/&gt;</code> 组件要渲染的内容不受 DOM 层级关系限制，即可以渲染到任何位置，该问题将迎刃而解。</p>\\n<p>使用 <code>Portal</code> 可以这样编写 <code>&lt;Overlay/&gt;</code> 组件的模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;Portal target=&quot;#app-root&quot;&gt;\\n    &lt;div class=&quot;overlay&quot;&gt;&lt;/div&gt;\\n  &lt;/Portal&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>其最终效果是，无论你在何处使用 <code>&lt;Overlay/&gt;</code> 组件，它都会把内容渲染到 <code>id=&quot;app-root&quot;</code> 的元素下。由此可知，所谓 <code>Portal</code> 就是把子节点渲染到给定的目标，我们可以使用如下 <code>VNode</code> 对象来描述上面这段模板：</p>\\n<pre class=\\"language-js\\"><code>const Portal = Symbol()\\nconst portalVNode = {\\n  tag: Portal,\\n  data: {\\n    target: &#39;#app-root&#39;\\n  },\\n  children: {\\n    tag: &#39;div&#39;,\\n    data: {\\n      class: &#39;overlay&#39;\\n    }\\n  }\\n}\\n</code></pre>\\n<p><code>Portal</code> 类型的 <code>VNode</code> 与 <code>Fragment</code> 类型的 <code>VNode</code> 类似，都需要一个唯一的标识，来区分其类型，目的是告诉渲染器如何渲染该 <code>VNode</code>。</p>\\n<h2 id=\\"vnode-的种类\\"><a class=\\"header-anchor\\" href=\\"#vnode-的种类\\">#</a> VNode 的种类</h2>\\n<p>当 <code>VNode</code> 描述不同的事物时，其属性的值也各不相同。比如一个 <code>VNode</code> 对象是 <code>html</code> 标签的描述，那么其 <code>tag</code> 属性值就是一个字符串，即标签的名字；如果是组件的描述，那么其 <code>tag</code> 属性值则引用组件类(或函数)本身；如果是文本节点的描述，那么其 <code>tag</code> 属性值为 <code>null</code>。</p>\\n<p>最终我们发现，<strong>不同类型的 <code>VNode</code> 拥有不同的设计</strong>，这些差异积少成多，所以我们完全可以将它们分门别类。</p>\\n<p>总的来说，我们可以把 <code>VNode</code> 分成五类，分别是：<strong><code>html/svg</code> 元素</strong>、<strong>组件</strong>、<strong>纯文本</strong>、<strong>Fragment</strong> 以及 <strong>Portal</strong>：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2024/02/91644f2f0ad1d19b.png\\" alt=\\"\\"></p>\\n<p>如上图所示，我们可以把组件细分为 <strong>有状态组件</strong> 和 <strong>函数式组件</strong>。同时有状态组件还可以细分为三部分：<strong>普通的有状态组件</strong>、<strong>需要被 keepAlive 的有状态组件</strong> 以及 <strong>已经被 keepAlive 的有状态组件</strong> 。</p>\\n<p>但无论是普通的有状态组件还是 <code>keepAlive</code> 相关的有状态组件，它们都是<strong>有状态组件</strong>。所以我们在设计 <code>VNode</code> 时可以将它们作为一类看待。</p>\\n<h2 id=\\"使用-flags-作为-vnode-的标识\\"><a class=\\"header-anchor\\" href=\\"#使用-flags-作为-vnode-的标识\\">#</a> 使用 flags 作为 VNode 的标识</h2>\\n<p>既然 <code>VNode</code> 有类别之分，我们就有必要使用一个唯一的标识，来标明某一个 <code>VNode</code> 属于哪一类。同时给 <code>VNode</code> 添加 <code>flags</code> 也是 <code>Virtual DOM</code> 算法的优化手段之一。</p>\\n<p>比如在 <code>Vue2</code> 中区分 <code>VNode</code> 是 <code>html</code> 元素还是组件亦或是普通文本，是这样做的：</p>\\n<ul>\\n<li>1、拿到 <code>VNode</code> 后先尝试把它当作组件去处理，如果成功地创建了组件，那说明该 <code>VNode</code> 就是组件的 <code>VNode</code></li>\\n<li>2、如果没能成功地创建组件，则检查 <code>vnode.tag</code> 是否有定义，如果有定义则当作普通标签处理</li>\\n<li>3、如果 <code>vnode.tag</code> 没有定义则检查是否是注释节点</li>\\n<li>4、如果不是注释节点，则会把它当作文本节点对待</li>\\n</ul>\\n<p>以上这些判断都是在挂载(或<code>patch</code>)阶段进行的，换句话说，一个 <code>VNode</code> 到底描述的是什么是在挂载或 <code>patch</code> 的时候才知道的。这就带来了两个难题：<strong>无法从 <code>AOT</code> 的层面优化</strong>、<strong>开发者无法手动优化</strong>。</p>\\n<p>为了解决这个问题，我们的思路是在 <code>VNode</code> 创建的时候就把该 <code>VNode</code> 的类型通过 <code>flags</code> 标明，这样在挂载或 <code>patch</code> 阶段通过 <code>flags</code> 可以直接避免掉很多消耗性能的判断，我们先提前感受一下渲染器的代码：</p>\\n<pre class=\\"language-js\\"><code>if (flags &amp; VNodeFlags.ELEMENT) {\\n  // VNode 是普通标签\\n  mountElement(/* ... */)\\n} else if (flags &amp; VNodeFlags.COMPONENT) {\\n  // VNode 是组件\\n  mountComponent(/* ... */)\\n} else if (flags &amp; VNodeFlags.TEXT) {\\n  // VNode 是纯文本\\n  mountText(/* ... */)\\n}\\n</code></pre>\\n<p>如上，采用了位运算，在一次挂载任务中如上判断很可能大量的进行，使用位运算在一定程度上再次拉升了运行时性能。</p>\\n<div class=\\"tip\\">\\n<p>实际上 <code>Vue3</code> 在 <code>Virtual DOM</code> 的优化上采用的就是 <a href=\\"https://github.com/infernojs/inferno\\">inferno</a> 的手段。具体如何做我们会在后面的章节介绍。</p>\\n</div>\\n<p>这就意味着我们在设计 <code>VNode</code> 对象时，应该包含 <code>flags</code> 字段：</p>\\n<pre class=\\"language-js\\"><code>// VNode 对象\\n{\\n  flags: ...\\n}\\n</code></pre>\\n<h2 id=\\"枚举值-vnodeflags\\"><a class=\\"header-anchor\\" href=\\"#枚举值-vnodeflags\\">#</a> 枚举值 VNodeFlags</h2>\\n<p>那么一个 <code>VNode</code> 对象的 <code>flags</code> 可以是哪些值呢？那就看 <code>VNode</code> 有哪些种类就好了，每一个 <code>VNode</code> 种类我们都为其分配一个 <code>flags</code> 值即可，我们把它设计成一个枚举值并取名为 <code>VNodeFlags</code>，在 <code>javascript</code> 里就用一个对象来表示即可：</p>\\n<pre class=\\"language-js\\"><code>const VNodeFlags = {\\n  // html 标签\\n  ELEMENT_HTML: 1,\\n  // SVG 标签\\n  ELEMENT_SVG: 1 &lt;&lt; 1,\\n\\n  // 普通有状态组件\\n  COMPONENT_STATEFUL_NORMAL: 1 &lt;&lt; 2,\\n  // 需要被keepAlive的有状态组件\\n  COMPONENT_STATEFUL_SHOULD_KEEP_ALIVE: 1 &lt;&lt; 3,\\n  // 已经被keepAlive的有状态组件\\n  COMPONENT_STATEFUL_KEPT_ALIVE: 1 &lt;&lt; 4,\\n  // 函数式组件\\n  COMPONENT_FUNCTIONAL: 1 &lt;&lt; 5,\\n\\n  // 纯文本\\n  TEXT: 1 &lt;&lt; 6,\\n  // Fragment\\n  FRAGMENT: 1 &lt;&lt; 7,\\n  // Portal\\n  PORTAL: 1 &lt;&lt; 8\\n}\\n</code></pre>\\n<p>如上这些枚举属性所代表的意义能够与下面的图片一一对应上：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2024/02/91644f2f0ad1d19b.png\\" alt=\\"\\"></p>\\n<p>我们注意到，这些枚举属性的值基本都是通过将十进制数字 <code>1</code> 左移不同的位数得来的。根据这些基本的枚举属性值，我们还可以派生出额外的三个标识：</p>\\n<pre class=\\"language-js\\"><code>// html 和 svg 都是标签元素，可以用 ELEMENT 表示\\nVNodeFlags.ELEMENT = VNodeFlags.ELEMENT_HTML | VNodeFlags.ELEMENT_SVG\\n// 普通有状态组件、需要被keepAlive的有状态组件、已经被keepAlice的有状态组件 都是“有状态组件”，统一用 COMPONENT_STATEFUL 表示\\nVNodeFlags.COMPONENT_STATEFUL =\\n  VNodeFlags.COMPONENT_STATEFUL_NORMAL |\\n  VNodeFlags.COMPONENT_STATEFUL_SHOULD_KEEP_ALIVE |\\n  VNodeFlags.COMPONENT_STATEFUL_KEPT_ALIVE\\n// 有状态组件 和  函数式组件都是“组件”，用 COMPONENT 表示\\nVNodeFlags.COMPONENT = VNodeFlags.COMPONENT_STATEFUL | VNodeFlags.COMPONENT_FUNCTIONAL\\n</code></pre>\\n<p>其中 <code>VNodeFlags.ELEMENT</code>、<code>VNodeFlags.COMPONENT_STATEFUL</code> 以及 <code>VNodeFlags.COMPONENT</code> 是由基本标识通过<code>按位或(|)</code>运算得到的，这三个派生值将用于辅助判断。</p>\\n<p>有了这些 <code>flags</code> 之后，我们在创建 <code>VNode</code> 的时候就可以预先为其打上 <code>flags</code>，以标明该 <code>VNode</code> 的类型：</p>\\n<pre class=\\"language-js\\"><code>// html 元素节点\\nconst htmlVnode = {\\n  flags: VNodeFlags.ELEMENT_HTML,\\n  tag: &#39;div&#39;,\\n  data: null\\n}\\n\\n// svg 元素节点\\nconst svgVnode = {\\n  flags: VNodeFlags.ELEMENT_SVG,\\n  tag: &#39;svg&#39;,\\n  data: null\\n}\\n\\n// 函数式组件\\nconst functionalComponentVnode = {\\n  flags: VNodeFlags.COMPONENT_FUNCTIONAL,\\n  tag: MyFunctionalComponent\\n}\\n\\n// 普通的有状态组件\\nconst normalComponentVnode = {\\n  flags: VNodeFlags.COMPONENT_STATEFUL_NORMAL,\\n  tag: MyStatefulComponent\\n}\\n\\n// Fragment\\nconst fragmentVnode = {\\n  flags: VNodeFlags.FRAGMENT,\\n  // 注意，由于 flags 的存在，我们已经不需要使用 tag 属性来存储唯一标识\\n  tag: null\\n}\\n\\n// Portal\\nconst portalVnode = {\\n  flags: VNodeFlags.PORTAL,\\n  // 注意，由于 flags 的存在，我们已经不需要使用 tag 属性来存储唯一标识，tag 属性用来存储 Portal 的 target\\n  tag: target\\n}\\n</code></pre>\\n<p>如下是利用 <code>VNodeFlags</code> 判断 <code>VNode</code> 类型的例子，比如判断一个 <code>VNode</code> 是否是组件：</p>\\n<pre class=\\"language-js\\"><code>// 使用按位与(&amp;)运算\\nfunctionalComponentVnode.flags &amp; VNodeFlags.COMPONENT // 真\\nnormalComponentVnode.flags &amp; VNodeFlags.COMPONENT // 真\\nhtmlVnode.flags &amp; VNodeFlags.COMPONENT // 假\\n</code></pre>\\n<p>熟悉位运算的话，理解起来很简单。这实际上是多种位运算技巧中的一个小技巧。我们可以列一个表格：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>VNodeFlags</th>\\n<th>左移运算</th>\\n<th>32 位的 bit 序列(出于简略，只用 9 位表示)</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>ELEMENT_HTML</td>\\n<td>无</td>\\n<td>00000000<code>1</code></td>\\n</tr>\\n<tr>\\n<td>ELEMENT_SVG</td>\\n<td>1 &lt;&lt; 1</td>\\n<td>0000000<code>1</code>0</td>\\n</tr>\\n<tr>\\n<td>COMPONENT_STATEFUL_NORMAL</td>\\n<td>1 &lt;&lt; 2</td>\\n<td>000000<code>1</code>00</td>\\n</tr>\\n<tr>\\n<td>COMPONENT_STATEFUL_SHOULD_KEEP_ALIVE</td>\\n<td>1 &lt;&lt; 3</td>\\n<td>00000<code>1</code>000</td>\\n</tr>\\n<tr>\\n<td>COMPONENT_STATEFUL_KEPT_ALIVE</td>\\n<td>1 &lt;&lt; 4</td>\\n<td>0000<code>1</code>0000</td>\\n</tr>\\n<tr>\\n<td>COMPONENT_FUNCTIONAL</td>\\n<td>1 &lt;&lt; 5</td>\\n<td>000<code>1</code>00000</td>\\n</tr>\\n<tr>\\n<td>TEXT</td>\\n<td>1 &lt;&lt; 6</td>\\n<td>00<code>1</code>000000</td>\\n</tr>\\n<tr>\\n<td>FRAGMENT</td>\\n<td>1 &lt;&lt; 7</td>\\n<td>0<code>1</code>0000000</td>\\n</tr>\\n<tr>\\n<td>PORTAL</td>\\n<td>1 &lt;&lt; 8</td>\\n<td><code>1</code>00000000</td>\\n</tr>\\n</tbody>\\n</table>\\n<p>根据上表展示的基本 <code>flags</code> 值可以很容易地得出下表：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>VNodeFlags</th>\\n<th>32 位的比特序列(出于简略，只用 9 位表示)</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>ELEMENT</td>\\n<td>0000000<code>1</code> <code>1</code></td>\\n</tr>\\n<tr>\\n<td>COMPONENT_STATEFUL</td>\\n<td>0000<code>1</code> <code>1</code> <code>1</code>00</td>\\n</tr>\\n<tr>\\n<td>COMPONENT</td>\\n<td>000<code>1</code> <code>1</code> <code>1</code> <code>1</code>00</td>\\n</tr>\\n</tbody>\\n</table>\\n<p>所以很自然的，只有 <code>VNodeFlags.ELEMENT_HTML</code> 和 <code>VNodeFlags.ELEMENT_SVG</code> 与 <code>VNodeFlags.ELEMENT</code> 进行按位与(<code>&amp;</code>)运算才会得到非零值，即为真。</p>\\n<h2 id=\\"children-和-childrenflags\\"><a class=\\"header-anchor\\" href=\\"#children-和-childrenflags\\">#</a> children 和 ChildrenFlags</h2>\\n<p>DOM 是一棵树早已家至人说，既然 <code>VNode</code> 是真实渲染内容的描述，那么它必然也是一棵树。在之前的设计中，我们给 <code>VNode</code> 定义了 <code>children</code> 属性，用来存储子 <code>VNode</code>。大家思考一下，一个标签的子节点会有几种情况？</p>\\n<p>总的来说无非有以下几种：</p>\\n<ul>\\n<li>没有子节点</li>\\n<li>只有一个子节点</li>\\n<li>多个子节点\\n<ul>\\n<li>有 <code>key</code></li>\\n<li>无 <code>key</code></li>\\n</ul>\\n</li>\\n<li>不知道子节点的情况</li>\\n</ul>\\n<p>我们可以用一个叫做 <code>ChildrenFlags</code> 的对象来枚举出以上这些情况，作为一个 <code>VNode</code> 的子节点的类型标识：</p>\\n<pre class=\\"language-js\\"><code>const ChildrenFlags = {\\n  // 未知的 children 类型\\n  UNKNOWN_CHILDREN: 0,\\n  // 没有 children\\n  NO_CHILDREN: 1,\\n  // children 是单个 VNode\\n  SINGLE_VNODE: 1 &lt;&lt; 1,\\n\\n  // children 是多个拥有 key 的 VNode\\n  KEYED_VNODES: 1 &lt;&lt; 2,\\n  // children 是多个没有 key 的 VNode\\n  NONE_KEYED_VNODES: 1 &lt;&lt; 3\\n}\\n</code></pre>\\n<p>由于 <code>ChildrenFlags.KEYED_VNODES</code> 和 <code>ChildrenFlags.NONE_KEYED_VNODES</code> 都属于多个 <code>VNode</code>，所以我们可以派生出一个“多节点”标识，以方便程序的判断：</p>\\n<pre class=\\"language-js\\"><code>ChildrenFlags.MULTIPLE_VNODES = ChildrenFlags.KEYED_VNODES | ChildrenFlags.NONE_KEYED_VNODES\\n</code></pre>\\n<p>这样我们判断一个 <code>VNode</code> 的子节点是否是多个子节点就变得容易多了：</p>\\n<pre class=\\"language-js\\"><code>someVNode.childFlags &amp; ChildrenFlags.MULTIPLE_VNODES\\n</code></pre>\\n<div class=\\"tip\\">\\n<p>为什么 <code>children</code> 也需要标识呢？原因只有一个：<strong>为了优化</strong>。在后面讲解 <code>diff</code> 算法的章节中你将会意识到，这些信息是至关重要的。</p>\\n</div>\\n<p>在一个 <code>VNode</code> 对象中，我们使用 <code>flags</code> 属性来存储该 <code>VNode</code> 的类型，类似的，我们将使用 <code>childFlags</code> 来存储子节点的类型，我们来举一些实际的例子：</p>\\n<pre class=\\"language-js\\"><code>// 没有子节点的 div 标签\\nconst elementVNode = {\\n  flags: VNodeFlags.ELEMENT_HTML,\\n  tag: &#39;div&#39;,\\n  data: null,\\n  children: null,\\n  childFlags: ChildrenFlags.NO_CHILDREN\\n}\\n\\n// 文本节点的 childFlags 始终都是 NO_CHILDREN\\nconst textVNode = {\\n  tag: null,\\n  data: null,\\n  children: &#39;我是文本&#39;,\\n  childFlags: ChildrenFlags.NO_CHILDREN\\n}\\n\\n// 拥有多个使用了key的 li 标签作为子节点的 ul 标签\\nconst elementVNode = {\\n  flags: VNodeFlags.ELEMENT_HTML,\\n  tag: &#39;ul&#39;,\\n  data: null,\\n  childFlags: ChildrenFlags.KEYED_VNODES,\\n  children: [\\n    {\\n      tag: &#39;li&#39;,\\n      data: null,\\n      key: 0\\n    },\\n    {\\n      tag: &#39;li&#39;,\\n      data: null,\\n      key: 1\\n    }\\n  ]\\n}\\n\\n// 只有一个子节点的 Fragment\\nconst elementVNode = {\\n  flags: VNodeFlags.FRAGMENT,\\n  tag: null,\\n  data: null,\\n  childFlags: ChildrenFlags.SINGLE_VNODE,\\n  children: {\\n    tag: &#39;p&#39;,\\n    data: null\\n  }\\n}\\n</code></pre>\\n<p>但并非所有类型的 <code>VNode</code> 的 <code>children</code> 属性都是用来存储子 <code>VNode</code>，比如组件的“子 <code>VNode</code>”其实不应该作为 <code>children</code> 而是应该作为 <code>slots</code>，所以我们会定义 <code>VNode.slots</code> 属性来存储这些子 <code>VNode</code>，不过目前来说我们还不需要深入探讨有关插槽的知识。</p>\\n<h2 id=\\"vnodedata\\"><a class=\\"header-anchor\\" href=\\"#vnodedata\\">#</a> VNodeData</h2>\\n<p>前面提到过，<code>VNodeData</code> 指的是 <code>VNode</code> 的 <code>data</code> 属性，它是一个对象：</p>\\n<pre class=\\"language-js\\"><code>{\\n  flags: ...,\\n  tag: ...,\\n  // VNodeData\\n  data: {\\n    ...\\n  }\\n}\\n</code></pre>\\n<p><code>VNodeData</code> 顾名思义，它就是 <code>VNode</code> 数据，用于对 <code>VNode</code> 进行描述。举个例子，假如一个 <code>VNode</code> 的类型是 <code>html</code> 标签，则 <code>VNodeData</code> 中可以包含 <code>class</code>、<code>style</code> 以及一些事件，这样渲染器在渲染此 <code>VNode</code> 时，才知道这个标签的背景颜色、字体大小以及监听了哪些事件等等。所以从设计角度来讲，任何可以对 <code>VNode</code> 进行描述的内容，我们都可以将其存放到 <code>VNodeData</code> 对象中，如：</p>\\n<pre class=\\"language-js\\"><code>{\\n  flags: VNodeFlags.ELEMENT_HTML,\\n  tag: &#39;div&#39;,\\n  data: {\\n    class: [&#39;class-a&#39;, &#39;active&#39;],\\n    style: {\\n      background: &#39;red&#39;,\\n      color: &#39;green&#39;\\n    },\\n    // 其他数据...\\n  }\\n}\\n</code></pre>\\n<p>如果 <code>VNode</code> 的类型是组件，那么我们同样可以用 <code>VNodeData</code> 来描述组件，比如组件的事件、组件的 <code>props</code> 等等，假设有如下模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;MyComponent @some-event=&quot;handler&quot; prop-a=&quot;1&quot; /&gt;\\n</code></pre>\\n<p>则其对应的 <code>VNodeData</code> 应为：</p>\\n<pre class=\\"language-js\\"><code>{\\n  flags: VNodeFlags.COMPONENT_STATEFUL,\\n  tag: &#39;div&#39;,\\n  data: {\\n    on: {\\n      &#39;some-event&#39;: handler\\n    },\\n    propA: &#39;1&#39;\\n    // 其他数据...\\n  }\\n}\\n</code></pre>\\n<p>当然了，只要能够正确地对 <code>VNode</code> 进行描述，具体的数据结构你可以随意设计。我们暂且不限制 <code>VNodeData</code> 的固定格式。</p>\\n<p>在后续章节中，我们会根据需求逐渐地完善 <code>VNodeData</code> 的设计。</p>\\n<p>至此，我们已经对 <code>VNode</code> 完成了一定的设计，目前为止我们所设计的 <code>VNode</code> 对象如下：</p>\\n<pre class=\\"language-js\\"><code>export interface VNode {\\n  // _isVNode 属性在上文中没有提到，它是一个始终为 true 的值，有了它，我们就可以判断一个对象是否是 VNode 对象\\n  _isVNode: true\\n  // el 属性在上文中也没有提到，当一个 VNode 被渲染为真实 DOM 之后，el 属性的值会引用该真实DOM\\n  el: Element | null\\n  flags: VNodeFlags\\n  tag: string | FunctionalComponent | ComponentClass | null\\n  data: VNodeData | null\\n  children: VNodeChildren\\n  childFlags: ChildrenFlags\\n}\\n</code></pre>\\n<p>其中 <code>_isVNode</code> 属性和 <code>el</code> 属性在上文中没有提到，<code>_isVNode</code> 属性是一个始终为 <code>true</code> 的值，有了它，我们就可以判断一个对象是否是 <code>VNode</code> 对象。<code>el</code> 属性的值在 <code>VNode</code> 被渲染为真实 DOM 之前一直都是 <code>null</code>，当 <code>VNode</code> 被渲染为真实 DOM 之后，<code>el</code> 属性的值会引用该真实 DOM。</p>\\n<p>实际上，如果你看过 <code>Vue3</code> 的源码，你会发现在源码中一个 <code>VNode</code> 对象除了包含本节我们所讲到的这些属性之外，还包含诸如 <code>handle</code> 和 <code>contextVNode</code>、<code>parentVNode</code>、<code>key</code>、<code>ref</code>、<code>slots</code> 等其他额外的属性。</p>\\n<p>我们之所以没有在本章中包含这些内容，是因为目前来讲，我们根本不需要这些属性，比如 <code>handle</code> 属性仅用于函数式组件，所以我们会在函数式组件原理相关的章节再讲。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>组件库把按钮的 <code>class</code>、<code>style</code>、<code>DOM</code> 事件和组件 <code>props</code> 全塞进 <code>children</code>，渲染器再猜字段含义，你会怎样反驳这套结构？</strong></p>\\n<p>这些描述信息应归入 <code>VNodeData</code>，<code>children</code> 用来表达子节点，两者职责混合会让渲染器难以稳定解释 <code>VNode</code>。<code>HTML</code> 元素可在 <code>data</code> 中记录 <code>class</code>、<code>style</code> 和事件，组件则可记录组件事件与 <code>props</code>；具体字段格式可演进，但语义边界应清楚。</p>\\n</li>\\n<li>\\n<p><strong>你在实现一个 <code>div</code> 的 <code>VNode</code>，需要带两个类名、红色背景和点击监听，评审要求说明渲染器靠什么恢复这些信息？</strong></p>\\n<p>我会把标签类型放在 <code>flags</code>，标签名放在 <code>tag</code>，并将类名、样式和事件写入 <code>data</code>，也就是 <code>VNodeData</code>。渲染器读取这些描述后才能设置真实 <code>DOM</code>；如果 <code>data</code> 为 <code>null</code>，就不能凭空推导相关表现和监听器。</p>\\n</li>\\n<li>\\n<p><strong>同一个 <code>VNode</code> 结构现在既要描述原生标签，也要描述有状态组件，<code>VNodeData</code> 是否必须为两类节点强制使用完全相同的字段？</strong></p>\\n<p>不必强制完全相同，<code>VNodeData</code> 的目标是准确描述当前 <code>VNode</code>，原生标签与组件天然需要不同信息。标签侧关注 <code>class</code>、<code>style</code> 和 <code>DOM</code> 事件，组件侧可放组件事件与 <code>props</code>；统一容器便于扩展，但渲染器必须结合 <code>flags</code> 解释字段。</p>\\n</li>\\n<li>\\n<p><strong>线上渲染器拿到一个普通对象后误当作 <code>VNode</code>，随后访问 <code>el</code> 时状态混乱；同时部分新建节点的 <code>el</code> 不是 <code>null</code>，你会查哪两个约定？</strong></p>\\n<p>先检查对象是否具有恒为 <code>true</code> 的 <code>_isVNode</code>，这是区分普通对象与 <code>VNode</code> 的显式标记。再确认新建 <code>VNode</code> 的 <code>el</code> 初始为 <code>null</code>，只有渲染成真实 <code>DOM</code> 后才写入元素引用；提前复用旧 <code>el</code> 会破坏虚拟节点与实际节点的对应关系。</p>\\n</li>\\n<li>\\n<p><strong>框架作者想一次性把 <code>handle</code>、<code>contextVNode</code>、<code>parentVNode</code>、<code>slots</code> 等字段全部加入最小 <code>VNode</code>，你支持吗？</strong></p>\\n<p>我会按当前渲染需求逐步加入，而不是仅因其他实现存在这些字段就全部复制。现阶段核心结构由 <code>_isVNode</code>、<code>el</code>、<code>flags</code>、<code>tag</code>、<code>data</code>、<code>children</code> 和 <code>childFlags</code> 构成；额外字段会扩大维护面，应等函数式组件或插槽等能力真正需要时再设计。</p>\\n</li>\\n<li>\\n<p><strong>自研渲染器收到 <code>&lt;MyComponent @some-event=&quot;handler&quot; prop-a=&quot;1&quot; /&gt;</code>，有人把 <code>some-event</code> 当 <code>DOM</code> 监听器直接绑到组件根元素，你会如何划分语义？</strong></p>\\n<p>该 <code>VNode</code> 的 <code>data</code> 描述的是组件，<code>on.some-event</code> 应作为组件事件信息，<code>propA</code> 则作为组件输入，而不是默认下沉成根元素属性。渲染器要先依据 <code>flags</code> 判断节点类型，再解释 <code>VNodeData</code>；否则组件封装边界会被意外穿透。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);