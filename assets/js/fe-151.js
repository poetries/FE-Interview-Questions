(window.webpackJsonp=window.webpackJsonp||[]).push([[151],{1259:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：<code>HMR</code> 通过文件监听、增量编译、更新通知和运行时模块替换保留页面状态</li>\\n<li>原理主线：围绕 「Vite 的定义」、「快速上手」、「实现原理」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：系统介绍了 Vite 的核心原理、与 Webpack 等传统构建工具的差异、上手体验及其在现代前端开发中的优势，帮助开发者理解 Vite 如何通过原生 ES Modules 实现更快的冷启动和高效的热更新，提升开发效率。</li>\\n<li>边界与代价：依赖边界拒绝更新或副作用无法安全重放时必须回退刷新；开发期热替换不属于生产运行逻辑</li>\\n<li>工程落地：排障应沿文件是否被监听、哈希是否变化、客户端是否收到更新、accept 边界是否成立逐段检查</li>\\n</ul>\\n<p><strong><code>Vite</code> 在开发阶段利用浏览器原生 <code>ES Modules</code> 按请求编译模块，因此启动和热更新不必先打完整个应用。</strong> 它把项目目录作为静态服务根目录，拦截依赖和 <code>.vue</code> 文件请求，并通过 <code>WebSocket</code> 推送更新。第三方依赖可以提前优化成 <code>ESM bundle</code>，业务文件则按需处理，避免编译暂时用不到的模块。生产环境仍通过 <code>vite build</code> 交给 <code>Rollup</code> 打包，所以“开发时不打包”不等于上线时完全不构建。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 旧文中的 <code>webpack 4</code>、<code>JSONP</code> 更新清单或 <code>react-hot-loader</code> 代码用于解释历史链路，不应直接复制到新项目。当前 <code>webpack-dev-server 4+</code> 默认启用 <code>HMR</code>，严格 <code>ESM</code> 可使用 <code>import.meta.webpackHot</code>；生产环境不得携带 <code>HMR runtime</code>。以 <a href=\\"https://webpack.js.org/guides/hot-module-replacement/\\">webpack HMR 官方指南</a> 和项目锁定版本为准。</p>\\n<h2 id=\\"vite-的定义\\"><a class=\\"header-anchor\\" href=\\"#vite-的定义\\">#</a> Vite 的定义</h2>\\n<p>Vite 是面向现代浏览器的一个更轻、更快的 Web 应用开发工具，核心基于 ECMAScript 标准原生模块系统（ES Modules）实现。</p>\\n<p>表象功能上看，Vite 可以取代基于 Webpack 的 vue-cli 或者 cra 的集成式开发工具，提供全新的一种开发体验。</p>\\n<p>具体细节往下看。</p>\\n<h3 id=\\"vite-的由来\\"><a class=\\"header-anchor\\" href=\\"#vite-的由来\\">#</a> Vite 的由来</h3>\\n<p>在此之前，如果我们所开发的应用比较复杂（代码量偏大），使用 Webpack 的开发过程相对没有那么「丝滑」，具体表现为以下两点：</p>\\n<ul>\\n<li>Webpack Dev Server 冷启动时间会比较长，稍大一点的项目启动开发服务都需要等待 10 - 20 秒；</li>\\n<li>Webpack HMR 热更新的反应速度比较慢，修改完代码需要等待编译器全部编译完成才能开始同步到浏览器；</li>\\n</ul>\\n<h2 id=\\"快速上手\\"><a class=\\"header-anchor\\" href=\\"#快速上手\\">#</a> 快速上手</h2>\\n<p>这里我们话不多说，先上手体验一下 Vite，然后再来分析其内部的思路和想法。</p>\\n<p>Vite 官方目前提供了一个比较简单的脚手架：create-vite-app，可以使用这个脚手架快速创建一个使用 Vite 构建的 Vue.js 应用</p>\\n<pre class=\\"language-shell\\"><code>$ npm init vite-app &lt;project-name&gt;\\n$ cd &lt;project-name&gt;\\n$ npm install\\n$ npm run dev\\n</code></pre>\\n<p>如果使用 Yarn：</p>\\n<pre class=\\"language-shell\\"><code>$ yarn create vite-app &lt;project-name&gt;\\n$ cd &lt;project-name&gt;\\n$ yarn\\n$ yarn dev\\n</code></pre>\\n<blockquote>\\n<p>P.S. <code>npm init</code> 或者 <code>yarn create</code> 是这两个包管理工具提供的新功能， 其内部就是自动去安装一个 <code>create-&lt;xxx&gt;</code> 的模块（临时），然后自动执行这个模块中的 bin。 例如: <code>yarn create react-app my-react-app</code> 就相当于先 <code>yarn global add create-react-app</code>，然后自动执行 <code>create-react-app my-react-app</code></p>\\n</blockquote>\\n<h3 id=\\"对比差异点\\"><a class=\\"header-anchor\\" href=\\"#对比差异点\\">#</a> 对比差异点</h3>\\n<p>打开生成的项目过后，你会发现就是一个很普通的 Vue.js 应用，没有太多特殊的地方。</p>\\n<p>不过相比于之前 vue-cli 创建的项目或者是基于 Webpack 搭建的 Vue.js 项目，这里的开发依赖非常简单，只有 <code>vite</code> 和 <code>@vue/compiler-sfc</code>。</p>\\n<pre class=\\"language-json\\"><code>{\\n  &quot;name&quot;: &quot;vite-demo&quot;,\\n  &quot;version&quot;: &quot;0.0.0&quot;,\\n  &quot;scripts&quot;: {\\n    &quot;dev&quot;: &quot;vite&quot;,\\n    &quot;build&quot;: &quot;vite build&quot;\\n  },\\n  &quot;dependencies&quot;: {\\n    &quot;vue&quot;: &quot;^3.0.0-rc.1&quot;\\n  },\\n  &quot;devDependencies&quot;: {\\n    &quot;vite&quot;: &quot;^1.0.0-rc.1&quot;,\\n    &quot;@vue/compiler-sfc&quot;: &quot;^3.0.0-rc.1&quot;\\n  }\\n}\\n</code></pre>\\n<p><code>vite</code> 就是我们今天要介绍的主角，而 <code>@vue/compiler-sfc</code> 就是用来编译我们项目中 <code>.vue</code> 结尾的单文件组件（SFC），它取代的就是 Vue.js 2.x 时使用的 <code>vue-template-compiler</code>。</p>\\n<p>再者就是 Vue.js 的版本是 3.0。这里尤其需要注意：<strong>Vite 目前只支持 Vue.js 3.0 版本。</strong></p>\\n<blockquote>\\n<p>如果你想，在后面介绍完实现原理过后，你也可以改造 Vite 让它支持 Vue.js 2.0。</p>\\n</blockquote>\\n<h3 id=\\"基础体验\\"><a class=\\"header-anchor\\" href=\\"#基础体验\\">#</a> 基础体验</h3>\\n<p>这里我们所安装的 vite 模块提供了两个子命令：</p>\\n<ul>\\n<li><code>serve</code>：启动一个用于开发的服务器</li>\\n<li><code>build</code>：构建整个项目（上线）</li>\\n</ul>\\n<p>当我们执行 <code>vite serve</code> 的时候，你会发现响应速度非常快，几乎就是秒开。</p>\\n<p>可能单独体验你不会有太明显的感觉，你可以对比使用 <code>vue-cli-service</code>（内部还是 Webpack）启动开发服务器，</p>\\n<p>当我们对比使用 <code>vue-cli-service serve</code> 的时候，你会有更明显的感觉。</p>\\n<p>因为 Webpack Dev Server 在启动时，需要先 build 一遍，而 build 的过程是需要耗费很多时间的。</p>\\n<p><img src=\\"https://s.poetries.top/images/20210503215021.png\\" alt=\\"\\"></p>\\n<p>而 Vite 则完全不同，当我们执行 <code>vite serve</code> 时，内部直接启动了 Web Server，并不会先编译所有的代码文件。</p>\\n<p>那仅仅是启动 Web Server，速度上自然就快了很多。</p>\\n<p><img src=\\"https://s.poetries.top/images/20210503215029.png\\" alt=\\"\\"></p>\\n<p>但是像 Webpack 这类工具的做法是将所有模块提前编译、打包进 bundle 里，换句话说，不管模块是否会被执行，都要被编译和打包到 bundle 里。随着项目越来越大打包后的 bundle 也越来越大，打包的速度自然也就越来越慢。</p>\\n<p>Vite 利用现代浏览器原生支持 ESM 特性，省略了对模块的打包。</p>\\n<p>对于需要编译的文件，Vite 采用的是另外一种模式：即时编译。</p>\\n<p>也就是说，只有具体去请求某个文件时才会编译这个文件。</p>\\n<p>所以，这种「即时编译」的好处主要体现在：按需编译。</p>\\n<h3 id=\\"optimize\\"><a class=\\"header-anchor\\" href=\\"#optimize\\">#</a> Optimize</h3>\\n<p>Vite 还提供了一个目前在帮助列表中并没有呈现的一个子命令：optimize。</p>\\n<p>这个命令的作用就是单独的去「优化依赖」。</p>\\n<p>所谓的「优化依赖」，指的就是自动去把代码中依赖的第三方模块提前编译出来。</p>\\n<p>例如，我们在代码中通过 <code>import</code> 载入了 vue 这个模块，那通过这个命令就会自动将这个模块打包成一个单独的 ESM bundle, 放到 <code>node_modules/.vite_opt_cache</code> 目录中。</p>\\n<p>这样后续请求这个文件时就不需要再即时去加载了。</p>\\n<h3 id=\\"hmr\\"><a class=\\"header-anchor\\" href=\\"#hmr\\">#</a> HMR</h3>\\n<p>同样也是模式的问题，热更新的时候，Vite 只需要立即编译当前所修改的文件即可，所以响应速度非常快。</p>\\n<p>而 Webpack 修改某个文件过后，会自动以这个文件为入口重写 build 一次，所有的涉及到的依赖也都会被加载一遍，所以反应速度会慢很多。</p>\\n<h3 id=\\"build\\"><a class=\\"header-anchor\\" href=\\"#build\\">#</a> Build</h3>\\n<p>Vite 在生产模式下打包，需要使用 <code>vite build</code> 命令。</p>\\n<p>这个命令内部采用的是 Rollup 完成的应用打包，最终还是会把文件都提前编译并打包到一起。</p>\\n<p>对于 Code Splitting 需求，Vite 内部采用的就是原生 Dynamic imports 特性实现的，所以打包结果还是只能够支持现代浏览器。</p>\\n<p>不过好在 Dynamic imports 特性是可以有 Polyfill 的：<a href=\\"https://github.com/GoogleChromeLabs/dynamic-import-polyfill\\">https://github.com/GoogleChromeLabs/dynamic-import-polyfill</a> ，也就是说，只要你想，它也可以运行在相对低版本的浏览器中。</p>\\n<h3 id=\\"打包-or-不打包\\"><a class=\\"header-anchor\\" href=\\"#打包-or-不打包\\">#</a> 打包 or 不打包</h3>\\n<p>Vite 的出现，引发了另外一个值得我们思考的问题：究竟还有没有必要打包应用？</p>\\n<p>之前我们使用 Webpack 打包应用代码，使之成为一个 bundle.js，主要有两个原因：</p>\\n<ol>\\n<li>浏览器环境并不支持模块化</li>\\n<li>零散的模块文件会产生大量的 HTTP 请求</li>\\n</ol>\\n<p>随着浏览器的对 ES 标准支持的逐渐完善，第一个问题已经慢慢不存在了。现阶段绝大多数浏览器都是支持 ES Modules 的。</p>\\n<p><img src=\\"https://s.poetries.top/images/20210503215039.png\\" alt=\\"\\"></p>\\n<p>零散模块文件确实会产生大量的 HTTP 请求，而大量的 HTTP 请求在浏览器端就会并发请求资源的问题；</p>\\n<p><img src=\\"https://s.poetries.top/images/20210503215054.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>如上图所示，红色圈出来的请求就是并行请求，但是后面的请求就因为域名链接数已超过限制，而被挂起等待了一段时间。</p>\\n</blockquote>\\n<p>在 HTTP 1.1 的标准下，每次请求都需要单独建立 TCP 链接，经过完整的通讯过程，非常耗时；</p>\\n<p><img src=\\"https://s.poetries.top/images/20210503215110.png\\" alt=\\"\\"></p>\\n<p>而且每次请求除了请求体中的内容，请求头中也会包含很多数据，大量请求的情况下也会浪费很多资源。</p>\\n<p>但是这些问题随着 HTTP 2 的出现，也就不复存在了。</p>\\n<p><img src=\\"https://s.poetries.top/images/20210503215117.png\\" alt=\\"\\"></p>\\n<p>关于 HTTP 1.1 与 HTTP 2 之间的差异，可以通过这个链接体验：<a href=\\"https://http2.akamai.com/demo\\">https://http2.akamai.com/demo</a> ，直观感受下 HTTP/2 比 HTTP/1 到底快了多少。</p>\\n<p>而且不打包也有一个好处，就是可以把按需加载实现到极致。</p>\\n<blockquote>\\n<p>关于 HTTP 2 的详细介绍，可以参考：</p>\\n<ul>\\n<li><a href=\\"https://blog.fundebug.com/2019/03/07/understand-http2-and-http3/\\">https://blog.fundebug.com/2019/03/07/understand-http2-and-http3/</a></li>\\n<li><a href=\\"https://www.digitalocean.com/community/tutorials/http-1-1-vs-http-2-what-s-the-difference\\">https://www.digitalocean.com/community/tutorials/http-1-1-vs-http-2-what-s-the-difference</a></li>\\n</ul>\\n</blockquote>\\n<h3 id=\\"开箱即用\\"><a class=\\"header-anchor\\" href=\\"#开箱即用\\">#</a> 开箱即用</h3>\\n<ul>\\n<li>TypeScript - 内置支持</li>\\n<li>less/sass/stylus/postcss - 内置支持（需要单独安装所对应的编译器）</li>\\n</ul>\\n<h3 id=\\"特性小结\\"><a class=\\"header-anchor\\" href=\\"#特性小结\\">#</a> 特性小结</h3>\\n<p>Vite 带来的优势主要体现在提升开发者在开发过程中的体验。</p>\\n<ul>\\n<li>Dev Server 无需等待，即时启动；</li>\\n<li>几乎实时的模块热更新；</li>\\n<li>所需文件按需编译，避免编译用不到的文件；</li>\\n<li>开箱即用，避免各种 Loader 和 Plugin 的配置；</li>\\n</ul>\\n<h2 id=\\"实现原理\\"><a class=\\"header-anchor\\" href=\\"#实现原理\\">#</a> 实现原理</h2>\\n<p>Vite 的核心功能：Static Server + Compile + HMR</p>\\n<p>核心思路：</p>\\n<ol>\\n<li>将当前项目目录作为静态文件服务器的根目录</li>\\n<li>拦截部分文件请求\\n<ol>\\n<li>处理代码中 import node_modules 中的模块</li>\\n<li>处理 vue 单文件组件（SFC）的编译</li>\\n</ol>\\n</li>\\n<li>通过 WebSocket 实现 HMR</li>\\n</ol>\\n<h3 id=\\"手写实现\\"><a class=\\"header-anchor\\" href=\\"#手写实现\\">#</a> 手写实现</h3>\\n<p>详细参考 <a href=\\"https://github.com/zce/vite-essentials\\">https://github.com/zce/vite-essentials</a></p>\\n<pre class=\\"language-javascript\\"><code>#!/usr/bin/env node\\n\\nconst path = require(&#39;path&#39;)\\nconst { Readable } = require(&#39;stream&#39;)\\nconst Koa = require(&#39;koa&#39;)\\nconst send = require(&#39;koa-send&#39;)\\nconst compilerSfc = require(&#39;@vue/compiler-sfc&#39;)\\n\\nconst cwd = process.cwd()\\n\\nconst streamToString = stream =&gt;\\n  new Promise((resolve, reject) =&gt; {\\n    const chunks = []\\n    stream.on(&#39;data&#39;, chunk =&gt; chunks.push(chunk))\\n    stream.on(&#39;end&#39;, () =&gt; resolve(Buffer.concat(chunks).toString(&#39;utf8&#39;)))\\n    stream.on(&#39;error&#39;, reject)\\n  })\\n\\nconst app = new Koa()\\n\\n// 重写请求路径，/@modules/xxx =&gt; /node_modules/\\napp.use(async (ctx, next) =&gt; {\\n  if (ctx.path.startsWith(&#39;/@modules/&#39;)) {\\n    const moduleName = ctx.path.substr(10) // =&gt; vue\\n    const modulePkg = require(path.join(cwd, &#39;node_modules&#39;, moduleName, &#39;package.json&#39;))\\n    ctx.path = path.join(&#39;/node_modules&#39;, moduleName, modulePkg.module)\\n  }\\n  await next()\\n})\\n\\n// 根据请求路径得到相应文件 /index.html\\napp.use(async (ctx, next) =&gt; {\\n  // ctx.path // http://localhost:3080/\\n  // ctx.body = &#39;my-vite&#39;\\n  await send(ctx, ctx.path, { root: cwd, index: &#39;index.html&#39; }) // 有可能还需要额外处理相应结果\\n  await next()\\n})\\n\\n// .vue 文件请求的处理，即时编译\\napp.use(async (ctx, next) =&gt; {\\n  if (ctx.path.endsWith(&#39;.vue&#39;)) {\\n    const contents = await streamToString(ctx.body)\\n    const { descriptor } = compilerSfc.parse(contents)\\n    let code\\n\\n    if (ctx.query.type === undefined) {\\n      code = descriptor.script.content\\n      code = code.replace(/export\\\\s+default\\\\s+/, &#39;const __script = &#39;)\\n      code += `\\n  import { render as __render } from &quot;${ctx.path}?type=template&quot;\\n  __script.render = __render\\n  export default __script`\\n      // console.log(code)\\n      ctx.type = &#39;application/javascript&#39;\\n      ctx.body = Readable.from(Buffer.from(code))\\n    } else if (ctx.query.type === &#39;template&#39;) {\\n      const templateRender = compilerSfc.compileTemplate({\\n        source: descriptor.template.content\\n      })\\n      code = templateRender.code\\n    }\\n\\n    ctx.type = &#39;application/javascript&#39;\\n    ctx.body = Readable.from(Buffer.from(code))\\n  }\\n  await next()\\n})\\n\\n// 替换代码中特殊位置\\napp.use(async (ctx, next) =&gt; {\\n  if (ctx.type === &#39;application/javascript&#39;) {\\n    const contents = await streamToString(ctx.body)\\n    ctx.body = contents\\n      .replace(/(from\\\\s+[&#39;&quot;])(?![\\\\.\\\\/])/g, &#39;$1/@modules/&#39;)\\n      .replace(/process\\\\.env\\\\.NODE_ENV/g, &#39;&quot;production&quot;&#39;)\\n  }\\n})\\n\\napp.listen(3080)\\n\\nconsole.log(&#39;Server running @ http://localhost:3080&#39;)\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>手写开发服务器访问 <code>/@modules/vue</code> 时直接读取包入口，某个依赖没有预期字段便返回 500，你会指出这段实现依赖了什么假设？</strong></p>\\n<p>这段重写逻辑假设模块名可直接映射到 <code>node_modules</code> 下的 <code>package.json</code>，且包中存在可用的 <code>module</code> 入口。工程化实现必须处理入口缺失、子路径和非法路径等情况；示例适合解释基本链路，不能等同于完整解析器。</p>\\n</li>\\n<li>\\n<p><strong>项目根目录有五百个文件，<code>Koa</code> 中间件先执行静态发送，再处理 <code>.vue</code> 即时编译，页面却收到原始单文件组件，你如何判断中间件顺序是否有问题？</strong></p>\\n<p>先检查静态中间件是否已经设置 <code>ctx.body</code>，以及后续 <code>.vue</code> 中间件拿到的是流还是已结束的响应。示例依赖 <code>await next()</code> 继续进入编译逻辑，但真实服务器还需保证响应未提前提交；顺序或流处理错误都会让浏览器收到原始内容。</p>\\n</li>\\n<li>\\n<p><strong>一个 <code>.vue</code> 文件同时含脚本和模板，浏览器为什么要再次请求 <code>?type=template</code>，而不是服务器一次返回全部结果？</strong></p>\\n<p>首次请求会把默认导出改写为 <code>__script</code>，再插入对同一路径 <code>?type=template</code> 的导入，并把返回的 <code>render</code> 绑定到脚本对象。这样脚本与模板能按请求类型分别处理；若查询参数分支或导出拼接错误，组件会缺少渲染函数。</p>\\n</li>\\n<li>\\n<p><strong>模板请求偶发空白，但脚本请求正常，你会在 <code>compilerSfc.parse</code> 之后记录哪些事实来定位？</strong></p>\\n<p>应确认 <code>descriptor</code> 是否包含模板、当前 <code>ctx.query.type</code> 是否确为 <code>template</code>，以及模板编译结果是否生成可导入的 <code>render</code>。还要检查响应类型是否为 <code>JavaScript</code>、流内容是否完整；示例代码在模板分支并不完整，不能假定解析成功就能直接运行。</p>\\n</li>\\n<li>\\n<p><strong>架构评审想把这段几十行 <code>Koa</code> 示例直接作为团队开发服务器，以减少构建依赖，你会否决还是接受？</strong></p>\\n<p>我会把它限定为展示路径重写、静态服务和 <code>Vue</code> <code>SFC</code> 即时编译的原理样例，不直接当作完整工具。真实项目还需补齐模块解析、错误处理、缓存、文件变化和更新机制；继续自研会获得控制力，也会承担这些边界的长期维护成本。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);