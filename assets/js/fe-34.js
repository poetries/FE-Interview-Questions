(window.webpackJsonp=window.webpackJsonp||[]).push([[34],{1140:function(o){o.exports=JSON.parse('{"html":"<h1 id=\\"前端算法面试专题学习路线\\"><a class=\\"header-anchor\\" href=\\"#前端算法面试专题学习路线\\">#</a> 前端算法面试专题学习路线</h1>\\n<h2 id=\\"从读题到可证明实现\\"><a class=\\"header-anchor\\" href=\\"#从读题到可证明实现\\">#</a> 从读题到可证明实现</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先明确输入规模、数据范围、是否有序、能否修改原数据和重复值语义，再选择数据结构与算法</li>\\n<li>面试表达按「暴力基线 → 可利用性质 → 优化方案 → 循环不变量 / 递归定义 → 复杂度」推进</li>\\n<li>数组常配双指针、滑窗和哈希；树图先定遍历顺序与 visited；最短路、回溯、动态规划各有明确适用信号</li>\\n<li>写代码时先约定函数契约和边界用例，维护可说清的循环不变量，避免“凭感觉把样例跑通”</li>\\n<li>复杂度要说明最坏/均摊条件与额外空间，递归栈、切片、排序和哈希冲突都不能从成本里消失</li>\\n</ul>\\n<p><strong>拿到算法题，我会先确认输入输出、规模、数据范围、有序性、重复值语义以及能否修改原数据，再决定算法和数据结构。</strong> 先写出正确的暴力解，才能看清重复计算或无效扫描，从而自然推导出优化方案。编码时要说清循环不变量或递归返回值，例如 <code>twoSum</code> 中，进入第 <code>i</code> 轮时，<code>seen</code> 始终保存 <code>[0, i)</code> 的值与索引。最后补齐空输入、重复值、极端规模等边界，并说明最坏或均摊复杂度，递归栈、排序和哈希代价也要算进去。</p>\\n</section>\\n<p>拿到题目先复述输入输出，并主动问清规模和边界。规模决定 <code>O(n²)</code> 是否可接受，有序性决定能否二分，值域决定能否计数或用位图，是否允许原地修改则影响空间方案。先给出正确的暴力解能建立基线，再指出重复计算、无效扫描或可复用状态，优化才有推导过程。</p>\\n<p>编码时要能说出不变量。例如快速排序分区过程中，可以约定 <code>[lo, i)</code> 始终小于 pivot，<code>[i, j)</code> 是已扫描但不小于 pivot 的区间，<code>[j, hi)</code> 尚未处理；每一步交换都维护这个命题。这样正确性来自可检查的状态，而不是“最后看起来排好了”。上面的播放器允许替换输入、逐帧、回退和拖动进度，代码行、指针与数组状态来自同一次真实算法演算。</p>\\n<p>测试至少覆盖空输入、单元素、全相等、已排序、逆序、重复值和极端规模。复杂度回答也必须带条件：快排平均 <code>O(n log n)</code>、最坏 <code>O(n²)</code>，递归栈平均 <code>O(log n)</code>；若 pivot 选择或输入分布会触发退化，就要说明随机化、三数取中或改用 introsort 等工程策略。</p>\\n<pre class=\\"language-js\\"><code>function twoSum(nums, target) {\\n  const seen = new Map()\\n  for (let i = 0; i &lt; nums.length; i += 1) {\\n    const need = target - nums[i]\\n    if (seen.has(need)) return [seen.get(need), i]\\n    seen.set(nums[i], i)\\n  }\\n  return []\\n}\\n</code></pre>\\n<p>这段实现的不变量是：进入第 <code>i</code> 轮时，<code>seen</code> 保存了 <code>[0, i)</code> 的值到索引映射，所以只需检查当前值的补数是否已经出现。时间复杂度期望为 <code>O(n)</code>，额外空间为 <code>O(n)</code>；若题目要求所有答案、排序后下标或值域极大，还要重新确认返回契约与哈希结构的代价。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人拿到订单去重题就说用 <code>Set</code> 做到 <code>O(n)</code>，但页面没有说明订单量、值域和是否允许改变顺序；你会追问他先补齐哪些契约？</strong></p>\\n<p>应先确认输入规模、重复判定规则、返回顺序、内存限制以及能否原地修改，再讨论 <code>Set</code> 是否合适。复杂度必须附带结构和数据分布条件；如果必须稳定保序或内存受限，排序、位图或原地方案的结论都会不同。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中，开发者说快速排序分区“跑完自然就有序”，你要求他为区间 <code>[lo, hi)</code> 写出一个可检查的不变量，他该怎么描述？</strong></p>\\n<p>可以约定 <code>[lo, i)</code> 始终小于 <code>pivot</code>，<code>[i, j)</code> 已扫描且不小于 <code>pivot</code>，<code>[j, hi)</code> 尚未处理。每轮比较或交换都必须维持这三个区间的含义，终止时未处理区间为空；若边界采用其他开闭约定，下标更新也必须同步推导。</p>\\n</li>\\n<li>\\n<p><strong>搜索页面的两数和从“返回任意一组下标”改成“返回全部不重复值对”，原来的 <code>Map</code> 实现为什么不能只改 <code>return</code> 为 <code>push</code>？</strong></p>\\n<p>原实现的不变量只保证当前元素能找到此前出现的一个补数，并未定义重复值如何去重，也没有控制全部结果的输出规模。需要重新约定按值还是按下标去重，并调整映射结构或改用排序双指针；结果数量过大时还要考虑分页或输出上限。</p>\\n</li>\\n<li>\\n<p><strong>线上排序任务只在已升序的百万级输入上栈溢出，随机小数组测试都通过；你会沿着哪条证据链定位快速排序退化？</strong></p>\\n<p>先记录每次分区后的左右区间长度与递归深度，检查固定首尾 <code>pivot</code> 是否持续产生极不平衡分区。若深度随输入接近线性增长，就应采用随机化、三数取中或 <code>introsort</code>，并优先递归较短一侧；这些策略降低风险，但不能替代边界正确性验证。</p>\\n</li>\\n<li>\\n<p><strong>支付页查找补数可选期望 <code>O(n)</code> 的 <code>Map</code>，也可复制后排序再双指针；业务要求返回原下标、输入不可修改且内存紧张，你如何解释没有绝对最优方案？</strong></p>\\n<p><code>Map</code> 直接保留下标且无需排序，但额外空间为 <code>O(n)</code>；复制排序同样需要保存值与原索引，时间还增加到 <code>O(n log n)</code>。若输入本来有序，可用双指针降低状态开销；否则必须在内存、时间和返回契约之间明确优先级。</p>\\n</li>\\n</ul>\\n</section>\\n<p>建议按本目录的 22 篇顺序学习：先补数据结构和复杂度，再练数组、字符串、链表、栈队列、树与遍历，最后进入回溯、排序和动态规划。每完成一种范式，都用“适用信号、状态定义、不变量、复杂度、退化条件”复盘一次。</p>\\n"}')}}]);