(window.webpackJsonp=window.webpackJsonp||[]).push([[114],{1220:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：<code>Fiber</code> 把不可中断的递归遍历改造成可保存进度的工作单元，使渲染阶段能够调度、暂停和恢复</li>\\n<li>原理主线：围绕 「调和（Reconciliation）过程」、「Diff 策略的设计思想」、「把握三个“要点”，图解 Diff 逻辑」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：解析了 React 15 中的“栈调和”算法，包括调和（Reconciliation）与 Diff 算法的区别、源码结构、Diff 策略设计思想及其高性能实现原理，帮助读者深入理解 React 虚拟 DOM 与真实 DOM 同步的核心机制。</li>\\n<li>边界与代价：可中断的是渲染计算，提交阶段仍需保持一致性；<code>React 18+</code> 并发能力也不等于所有更新都并行执行</li>\\n<li>工程落地：优化时先减少无效工作和长任务，再结合优先级、过渡更新和切片机制改善响应性</li>\\n</ul>\\n<p><strong><code>React 15</code> 的栈调和会同步递归比较新旧虚拟 <code>DOM</code>，再把差异更新到真实 <code>DOM</code>。</strong> 它只比较同一层级，把原本昂贵的树比较降为一次自上而下的遍历；节点类型不同就直接替换，类型相同才继续深入。列表更新时，稳定且唯一的 <code>key</code> 能帮助复用同层节点，避免插入一项却连带重建后续节点。跨层移动仍会被当成删除后重建，而且递归过程不可暂停，这也是后来引入 <code>Fiber</code> 调和的重要背景。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文若分析 <code>ReactDOM.render</code>、旧生命周期或栈调和，应把它视为理解架构演进的历史路径。<code>React 19</code> 已移除 <code>ReactDOM.render</code>，当前客户端入口使用 <code>createRoot</code>；并发渲染也必须区分可中断的渲染阶段与同步提交阶段。迁移前对照 <a href=\\"https://react.dev/blog/2024/04/25/react-19-upgrade-guide\\">React 19 官方升级指南</a>。</p>\\n<p>理解 React 15 的“栈调和”算法。</p>\\n<h2 id=\\"调和-reconciliation-过程与-diff-算法\\"><a class=\\"header-anchor\\" href=\\"#调和-reconciliation-过程与-diff-算法\\">#</a> 调和（Reconciliation）过程与 Diff 算法</h2>\\n<p>“调和”又译为“协调”，协调过程的官方定义，藏在 React 官网对虚拟 DOM 这一概念的解释中，原文如下：</p>\\n<blockquote>\\n<p>Virtual DOM 是一种编程概念。在这个概念里，UI 以一种理想化的，或者说“虚拟的”表现形式被保存于内存中，并通过如 ReactDOM 等类库使之与“真实的” DOM 同步。这一过程叫作协调（调和）。</p>\\n</blockquote>\\n<p>我来划一下这段话里的重点：<code>通过如 ReactDOM 等类库使虚拟 DOM 与“真实的” DOM 同步，这一过程叫作协调（调和）</code></p>\\n<p>说人话：<code>调和指的是将虚拟 DOM映射到真实 DOM 的过程</code>。因此严格来说，<code>调和过程并不能和 Diff 画等号</code>。<code>调和是“使一致”的过程</code>，而 <code>Diff 是“找不同”的过程</code>，它只是“使一致”过程中的一个环节</p>\\n<blockquote>\\n<p><code>React 的源码结构佐证了这一点</code>：React 从大的板块上将源码划分为了 <code>Core</code>、<code>Renderer</code> 和 <code>Reconciler</code> 三部分。其中 <code>Reconciler</code>（调和器）的源码位于<a href=\\"https://github.com/facebook/react/tree/15-stable/src/renderers/shared/stack/reconciler\\">src/renderers/shared/stack/reconcile</a>r这个路径，<code>调和器所做的工作是一系列的，包括组件的挂载、卸载、更新等过程，其中更新过程涉及对 Diff 算法的调用</code>。</p>\\n</blockquote>\\n<p>所以说<code>调和 !== Diff</code> 这个结论，是站得住脚的，如果你持有这个观点，说明你很专业</p>\\n<p>但是！在如今大众的认知里，当我们讨论调和的时候，其实就是在讨论 Diff。</p>\\n<p>这样的认知也有其合理性，因为<code>Diff 确实是调和过程中最具代表性的一环</code>：根据 <code>Diff</code> 实现形式的不同，调和过程被划分为了以 <code>React 15</code> 为代表的“栈调和”以及 <code>React 16</code> 以来的“Fiber 调和”。在实际的面试过程中，当面试官抛出 <code>Reconciliation</code> 相关问题时，也多半是为了了解候选人对 <code>Diff</code> 的掌握程度。因此在本讲中，“栈调和”指的就是 <code>React 15 的 Diff 算法</code>。</p>\\n<h2 id=\\"diff-策略的设计思想\\"><a class=\\"header-anchor\\" href=\\"#diff-策略的设计思想\\">#</a> Diff 策略的设计思想</h2>\\n<p>在展开讲解 Diff 算法的具体逻辑之前，我们首先从整体上把握一下 Diff 的设计思想。</p>\\n<p>前面我们已经提到，<code>Diff 算法其实就是“找不同”的过程</code>。在计算机科学领域，要想找出两个树结构之间的不同, 传统的计算方法是通过循环递归进行树节点的一一对比,  这个过程的算法复杂度是 <code>O (n3)</code> 。尽管这个算法本身已经是几代程序员持续优化的结果，但对计算能力有限的浏览器来说，<code>O (n3)</code> 仍然意味着一场性能灾难。</p>\\n<p>具体来说，若一张页面中有 100 个节点（这样的情况在实际开发中并不少见），1003 算下来就有十万次操作了，这还只是一次 Diff 的开销；若应用规模更大一点，维护 1000 个节点，那么操作次数将会直接攀升到 10 亿的量级。</p>\\n<p>经常做算法题的人都知道，OJ 中相对理想的时间复杂度一般是 O(1) 或 O(n)。当复杂度攀升至 O(n2) 时，我们就会本能地寻求性能优化的手段，更不必说是人神共愤的 O(n3) 了！我们看不下去，React 自然也看不下去。<code>React 团队结合设计层面的一些推导，总结了以下两个规律， 为将 O (n3) 复杂度转换成 O (n) 复杂度确立了大前提：</code></p>\\n<ul>\\n<li>若两个组件属于同一个类型，那么它们将拥有相同的 <code>DOM</code>树形结构；</li>\\n<li>处于同一层级的一组子节点，可用通过设置 <code>key</code> 作为唯一标识，从而维持各个节点在不同渲染过程中的稳定性。</li>\\n</ul>\\n<p>除了这两个“板上钉钉”的规律之外，还有一个和实践结合比较紧密的规律，它为 React 实现高效的 Diff 提供了灵感：<code>DOM 节点之间的跨层级操作并不多，同层级操作是主流</code>。</p>\\n<p>接下来我们就一起看看 React 是如何巧用这 3 个规律，打造高性能 Diff 的。</p>\\n<h2 id=\\"把握三个-要点-图解-diff-逻辑\\"><a class=\\"header-anchor\\" href=\\"#把握三个-要点-图解-diff-逻辑\\">#</a> 把握三个“要点”，图解 Diff 逻辑</h2>\\n<p>对于 Diff 逻辑的拆分与解读，社区目前已经有过许多版本，不同版本的解读姿势和角度各有不同。但说到底，真正需要你把握的要点无非下面这 3 个：</p>\\n<ol>\\n<li><code>Diff</code> 算法性能突破的关键点在于“分层对比”；</li>\\n<li>类型一致的节点才有继续 <code>Diff</code> 的必要性；</li>\\n<li><code>key</code> 属性的设置，可以帮我们尽可能重用同一层级内的节点。</li>\\n</ol>\\n<p>这 3 个要点各自呼应着上文的 3 个规律，我们逐个来看。</p>\\n<h3 id=\\"_1-改变时间复杂度量级的决定性思路-分层对比\\"><a class=\\"header-anchor\\" href=\\"#_1-改变时间复杂度量级的决定性思路-分层对比\\">#</a> 1. 改变时间复杂度量级的决定性思路：分层对比</h3>\\n<p>结合“DOM 节点之间的跨层级操作并不多，<code>同层级操作是主流</code>”这一规律，React 的 Diff 过程直接放弃了跨层级的节点比较，它只针对相同层级的节点作对比，如下图所示。这样一来，<code>只需要从上到下的一次遍历，就可以完成对整棵树的对比</code>，这是降低复杂度量级方面的一个最重要的设计。</p>\\n<blockquote>\\n<p>需要注意的是：<code>虽然栈调和将传统的树对比算法优化为了分层对比</code>，但整个算法仍然是以递归的形式运转的，<code>分层递归也是递归</code>。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210429221559.png\\" alt=\\"\\"></p>\\n<p>那么如果真的发生了跨层级的节点操作（比如将以 B 节点为根节点的子树从 A 节点下面移动到 C 节点下面，如下图所示）会怎样呢？很遗憾，作为“次要矛盾”，在这种情况下 React 并不能够判断出“移动”这个行为，它只能机械地认为移出子树那一层的组件消失了，对应子树需要被销毁；而移入子树的那一层新增了一个组件，需要重新为其创建一棵子树。</p>\\n<blockquote>\\n<p><code>销毁 + 重建</code>的代价是昂贵的，因此 React 官方也建议开发者<code>不要做跨层级的操作</code>，尽量保持 DOM 结构的稳定性。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210429221957.png\\" alt=\\"\\"></p>\\n<h3 id=\\"_2-减少递归的-一刀切-策略-类型的一致性决定递归的必要性\\"><a class=\\"header-anchor\\" href=\\"#_2-减少递归的-一刀切-策略-类型的一致性决定递归的必要性\\">#</a> 2. 减少递归的“一刀切”策略：类型的一致性决定递归的必要性</h3>\\n<p>结合“若两个组件属于同一个类型，那么它们将拥有相同的 DOM 树形结构”这一规律，我们虽不能直接反推出“不同类型的组件 DOM 结构不同”，但在大部分的情况下，这个结论都是成立的。毕竟，实际开发中遇到两个 DOM 结构完全一致、而类型不一致的组件的概率确实太低了。</p>\\n<p>React 认为，<code>只有同类型的组件，才有进一步对比的必要性</code>；若参与 <code>Diff</code> 的两个组件类型不同，那么直接放弃比较，原地替换掉旧的节点，如下图所示。只有确认组件类型相同后，React 才会在保留组件对应 DOM 树（或子树）的基础上，尝试向更深层次去 Diff</p>\\n<p>这样一来，便能够从很大程度上减少 <code>Diff</code> 过程中冗余的递归操作。</p>\\n<p><img src=\\"https://s.poetries.top/images/20210429222131.png\\" alt=\\"\\"></p>\\n<h3 id=\\"_3-重用节点的好帮手-key-属性帮-react-记住-节点\\"><a class=\\"header-anchor\\" href=\\"#_3-重用节点的好帮手-key-属性帮-react-记住-节点\\">#</a> 3. 重用节点的好帮手：key 属性帮 React “记住”节点</h3>\\n<p>我们提到了“key 属性能够帮助维持节点的稳定性”，这个结论从何而来呢？首先，我们来看看 React 对 key 属性的定义：</p>\\n<blockquote>\\n<p><code>key</code> 是用来帮助 React <code>识别哪些内容被更改、添加或者删除</code>。key 需要写在用数组渲染出来的元素内部，并且需要赋予其一个稳定的值。稳定在这里很重要，因为如果 <code>key</code> 值发生了变更，React 则会触发 UI 的重渲染。这是一个非常有用的特性。</p>\\n</blockquote>\\n<p>它试图解决的是<code>同一层级下节点的重用问题</code>。在展开分析之前，我们先结合到现在为止对 <code>Diff</code> 过程的理解，来思考这样一种情况，如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210429222302.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>图中 A 组件在保持类型和其他属性均不变的情况下，在两个子节点（B 和 D）之间插入了一个新的节点（C）。按照已知的 Diff 原则，两棵树之间的 Diff 过程应该是这样的：</p>\\n</blockquote>\\n<ul>\\n<li>首先对比位于<code>第 1 层的节点</code>，发现两棵树的节点类型是一致的（都是 A），于是进一步 <code>Diff</code>；</li>\\n<li>开始对比位于<code>第 2 层的节点</code>，第 1 个接受比较的是 B 这个位置，对比下来发现两棵树这个位置上的节点都是 B，没毛病，放过它；</li>\\n<li>第 2 个接受比较的是 D 这个位置，对比 D 和 C，发现前后的类型不一致，直接删掉 D 重建 C；</li>\\n<li>第 3 个接受比较的是 E 这个位置，对比 E 和 D，发现前后的类型不一致，直接删掉 E 重建 D；</li>\\n<li>最后接受“比较”的是树 2 的 E 节点这个位置，这个位置在树 1 里是空的，也就是说树 2 的E 是一个新增节点，所以<code>新增一个 E</code>。</li>\\n</ul>\\n<p>奇怪的事情发生了：C、D、E 三个节点，其实都是可以直接拿来用的。原本新增 1 个节点就能搞定的事情，现在却<code>又是删除又是重建</code>地搞了半天，这也太蠢了吧？而且这个蠢操作和跨层级移动节点还不太一样，后者本来就属于低频操作，加以合理的最佳实践约束一下基本上可以完全规避掉；但图示的这种插入节点的形式，可是实打实的高频操作，你怎么躲也躲不过的。频繁增删节点必定拖垮性能，<code>这时候就需要请出 key 属性来帮我们重用节点了</code></p>\\n<blockquote>\\n<p>key 属性的形式，我们肯定都不陌生。在基于数组动态生成节点时，我们一般都会给每个节点加装一个 key 属性（下面是一段代码示例）：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const todoItems = todos.map((todo) =&gt;\\n  &lt;li key={todo.id}&gt;\\n    {todo.text}\\n  &lt;/li&gt;\\n)\\n</code></pre>\\n<p>如果你忘记写 key，React 虽然不至于因此报错，但控制台标红是难免的，它会给你抛出一个“请给列表元素补齐 key 属性”的 warning，这个常见的 warning 也从侧面反映出了 key 的重要性。事实上，<code>当我们没有设定 key 值的时候，Diff 的过程就正如上文所描述的一样惨烈</code>。但只要你按照规范加装一个合适的 key，这个 key 就会像一个记号一样，帮助 React “记住”某一个节点，从而在后续的更新中实现对这个节点的追踪。比如说刚刚那棵虚拟 DOM 树，若我们给位于第 2 层的每一个子节点一个 key 值，如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210429222911.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>这个 key 就充当了每个节点的 ID（唯一标识），有了这个标识之后，当 C 被插入到 B 和 D 之间时，React 并不会再认为 C、D、E 这三个坑位都需要被重建——它会通过识别 ID，意识到 D 和 E 并没有发生变化（D 的 ID 仍然是 1，E 的 ID 仍然是 2），而只是被调整了顺序而已。接着，React 便能够轻松地重用它“追踪”到旧的节点，将 D 和 E 转移到新的位置，并完成对 C 的插入。这样一来，同层级下元素的操作成本便大大降低。</p>\\n</blockquote>\\n<p><strong>注：作为一个节点的唯一标识，在使用 key 之前，请务必确认 key 的唯一和稳定</strong></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>购物车列表用数组下标作 <code>key</code>，在第 1 项后插入新商品时，后续行里的输入状态发生串位；为什么“每个 <code>key</code> 都不重复”仍然不够？</strong></p>\\n<p><code>key</code> 不仅要在同层级中唯一，还必须在多次渲染之间稳定，数组下标会随插入位置改变。<code>React</code> 因而可能把旧节点身份对应到另一条商品数据，无法按业务实体正确追踪和重用；应使用稳定的商品标识，缺少稳定标识时需先修正数据模型。</p>\\n</li>\\n<li>\\n<p><strong>消息列表原来是 <code>B、D、E</code>，现在在 <code>B</code> 后插入 <code>C</code>；如果没有 <code>key</code>，<code>React</code> 可能执行哪些多余操作，你会怎样修正？</strong></p>\\n<p>按位置比较时，<code>React</code> 会先把原位置的 <code>D</code> 当作与 <code>C</code> 类型不同而删除重建，再把 <code>E</code> 替换成 <code>D</code>，最后新增 <code>E</code>。给同层节点配置唯一且稳定的 <code>key</code> 后，<code>React</code> 能识别 <code>D</code>、<code>E</code> 只是移动并予以重用，只需完成顺序调整和 <code>C</code> 的插入。</p>\\n</li>\\n<li>\\n<p><strong>表格数据从 20 行增长到 3000 行，接口每次返回时都给行生成新的随机 <code>key</code>；页面没有警告，但更新越来越重，你会如何解释？</strong></p>\\n<p>随机值虽然可能满足单次渲染的唯一性，却破坏了跨渲染的稳定性，<code>React</code> 会把原有行视为身份已改变并触发重渲染或重建。应改用数据中持久且唯一的行 <code>ID</code>，并对比更新前后的 <code>key</code> 映射；数据确实没有稳定身份时，节点复用能力就受到限制。</p>\\n</li>\\n<li>\\n<p><strong>线上待办列表拖拽排序后，部分行的本地编辑内容跟着位置走了；你会怎样区分是业务状态写错，还是 <code>key</code> 导致的节点复用错误？</strong></p>\\n<p>先打印排序前后的业务 <code>ID</code>、数组位置和实际 <code>key</code>，检查同一业务项是否始终保有同一个标识。再用稳定 <code>ID</code> 替换当前位置或易变字段做最小对照，若编辑状态随正确节点恢复即可定位；若映射一直稳定，则继续排查状态是否错误绑定到索引。</p>\\n</li>\\n<li>\\n<p><strong>同事为了强制重置支付表单，主动修改组件的 <code>key</code>；这和列表里保持稳定 <code>key</code> 是否矛盾，你会批准吗？</strong></p>\\n<p>不矛盾，<code>key</code> 变化会让 <code>React</code> 认为节点身份改变并触发重新渲染或重建，这可被有意识地用于重置组件。列表重排时则要保持身份稳定以便追踪和复用；只有产品确实需要丢弃旧节点状态时才应改 <code>key</code>，否则会牺牲复用并造成状态意外丢失。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中有人把 <code>key</code> 写在列表项内部的子元素上，而 <code>map</code> 直接返回的元素没有 <code>key</code>；为什么控制台仍可能告警？</strong></p>\\n<p><code>key</code> 需要放在数组动态生成并参与同层比较的元素上，才能标识该层每个节点的身份。写到更深的子元素并不能为外层数组项建立追踪关系，因此 <code>React</code> 仍可能提示补齐 <code>key</code>；标识的作用域应与发生列表 <code>Diff</code> 的层级一致。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);