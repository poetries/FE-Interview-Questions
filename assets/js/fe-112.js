(window.webpackJsonp=window.webpackJsonp||[]).push([[112],{1219:function(o){o.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：虚拟节点是对界面结构的描述，渲染器通过挂载与更新把声明式描述映射为宿主环境操作</li>\\n<li>原理主线：围绕 「快速搞定虚拟 DOM 的两个“大问题”」、「虚拟 DOM 是如何解决问题的」、「React 选用虚拟 DOM，真的是为了」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：解析虚拟DOM的原理、工作机制及其在React中的应用，阐述虚拟DOM如何通过JS对象描述真实DOM结构，实现高效的差量更新，并探讨虚拟DOM对前端开发体验和性能优化的实际影响，帮助开发者全面理解虚拟DOM的价值与局限。</li>\\n<li>边界与代价：<code>key</code>、节点类型和稳定序列决定复用边界；虚拟 DOM 解决可预测更新，不保证永远比手写 DOM 快</li>\\n<li>工程落地：性能分析要看实际提交次数、节点移动和组件边界，不能只用“减少 DOM 操作”解释</li>\\n</ul>\\n<p><strong>虚拟 <code>DOM</code> 是用 <code>JavaScript</code> 对象描述界面结构，再由渲染器把这份声明映射到真实宿主环境。</strong> 挂载时会创建并渲染整棵描述树，更新时则比较前后结构，把需要变化的部分应用到真实 <code>DOM</code>。它的核心价值是声明式开发和跨平台抽象，并不保证在所有场景下都比直接操作 <code>DOM</code> 更快。若界面几乎全量变化，<code>diff</code> 的计算成本可能抵消差量更新收益，因此性能结论必须结合具体场景验证。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 原理文章中的代码代表特定实现与写作时间。应用到当前项目时，应先确认浏览器、框架或工具的主版本，再区分稳定的规范语义、可变化的内部实现和项目自身约束。</p>\\n<h2 id=\\"快速搞定虚拟-dom-的两个-大问题\\"><a class=\\"header-anchor\\" href=\\"#快速搞定虚拟-dom-的两个-大问题\\">#</a> 快速搞定虚拟 DOM 的两个“大问题”</h2>\\n<blockquote>\\n<p>虚拟 DOM（Virtual DOM）<code>本质上是JS 和 DOM 之间的一个映射缓存</code>，它在形态上表现为一个能够描述 DOM 结构及其属性信息的 JS 对象</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210428200221.png\\" alt=\\"\\"></p>\\n<p>就这个示例来说，你需要把握住以下两点：</p>\\n<ul>\\n<li>虚拟 DOM 是 JS 对象</li>\\n<li>虚拟 DOM 是对真实 DOM 的描述</li>\\n</ul>\\n<p>我们看看 React 中的虚拟 DOM 大致是如何工作的</p>\\n<ul>\\n<li><strong>挂载阶段</strong>，React 将结合 JSX 的描述，构建出虚拟 DOM 树，然后通过 <code>ReactDOM.render</code> 实现虚拟 DOM 到真实 DOM 的映射（触发渲染流水线）；</li>\\n<li><strong>更新阶段</strong>，页面的变化在作用于真实 DOM 之前，会先作用于虚拟 <code>DOM</code>，虚拟 <code>DOM</code> 将在 JS 层借助算法先对比出具体有哪些真实 DOM 需要被改变，然后再将这些改变作用于真实 DOM。</li>\\n</ul>\\n<h2 id=\\"虚拟-dom-是如何解决问题的\\"><a class=\\"header-anchor\\" href=\\"#虚拟-dom-是如何解决问题的\\">#</a> 虚拟 DOM 是如何解决问题的</h2>\\n<p><img src=\\"https://s.poetries.top/images/20210428200431.png\\" alt=\\"\\"></p>\\n<p>而在虚拟 DOM 的加持下，事情变成了这样：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210428200450.png\\" alt=\\"\\"></p>\\n<p>注意图中的“模板”二字加了引号，这是因为虚拟 DOM 在实现上并不总是借助模板。比如 React 就使用了 JSX，前面咱们着重讲过，JSX 本质不是模板，而是一种使用体验和模板相似的 JS 语法糖</p>\\n<blockquote>\\n<p><code>区别就在于多出了一层虚拟 DOM 作为缓冲层</code>。这个缓冲层带来的利好是：<code>当 DOM 操作（渲染更新）比较频繁时，它会先将前后两次的虚拟 DOM 树进行对比，定位出具体需要更新的部分，生成一个“补丁集”，最后只把“补丁”打在需要更新的那部分真实 DOM 上，实现精准的“差量更新”</code>。这个过程对应的虚拟 DOM 工作流如下图所示：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210428200541.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>注：图中的 <code>diff 和 patch</code> 其实都是函数名，这些函数取材于一个独立的虚拟 DOM 库</p>\\n</blockquote>\\n<p>还需要说明的一点是， 虚拟 DOM 和 Redux 一样，不依附于任何具体的框架。学习虚拟 DOM，实际上可以完全不借助 React；但学习 React，就必须了解虚拟 DOM</p>\\n<h2 id=\\"react-选用虚拟-dom-真的是为了更好的性能吗\\"><a class=\\"header-anchor\\" href=\\"#react-选用虚拟-dom-真的是为了更好的性能吗\\">#</a> React 选用虚拟 DOM，真的是为了更好的性能吗？</h2>\\n<blockquote>\\n<p>在整个 DOM 操作的演化过程中，主要矛盾并不在于性能，而在于开发者写得爽不爽，在于研发体验/研发效率。<code>虚拟 DOM 不是别的，正是前端开发们为了追求更好的研发体验和研发效率而创造出来的高阶产物</code>。</p>\\n</blockquote>\\n<p>虚拟 DOM 并不一定会带来更好的性能，React 官方也从来没有把虚拟 DOM 作为性能层面的卖点对外输出过。<code>虚拟 DOM 的优越之处在于，它能够在提供更爽、更高效的研发模式（也就是函数式的 UI 编程方式）的同时，仍然保持一个还不错的性能</code></p>\\n<p>性能问题属于前端领域复杂度比较高的问题。当我们量化性能的时候，往往并不能只追求一个单一的数据，而是需要结合具体的参照物、渲染的阶段、数据的吞吐量等各种要素来作分情况的讨论。</p>\\n<p>拿前面讲过的模板渲染来举例，我们可以对比一下它和虚拟 DOM 在性能开销上的差异。两者的渲染工作流对比如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210428200801.png\\" alt=\\"\\"></p>\\n<p>从图中可以看出，模板渲染的步骤1，和虚拟 DOM 渲染的步骤1、2都属于 JS 范畴的行为，这两者是具备可比性的，我们放在一起来看：动态生成 HTML 字符串的过程本质是对字符串的拼接，对性能的消耗是有限的；而虚拟 DOM 的构建和 diff 过程逻辑则相对复杂，它不可避免地涉及递归、遍历等耗时操作。因此在 JS 行为这个层面，模板渲染胜出</p>\\n<p>模板渲染的步骤3，和虚拟 DOM 的步骤3 都属于 DOM 范畴的行为，两者具备可比性，因此我们仍然可以愉快地对比下去：<code>模板渲染是全量更新，而虚拟 DOM 是差量更新</code>。</p>\\n<p>乍一看好像差量更新一定比全量更新高效，但你需要考虑这样一种情况：数据内容变化非常大（或者说整个发生了改变），促使差量更新计算出来的结果和全量更新极为接近（或者说完全一样）。</p>\\n<p>在这种情况下，DOM 更新的工作量基本一致，而虚拟 DOM 却伴随着开销更大的 JS 计算，此时会出现的一种现象就是模板渲染和虚拟 DOM 在整体性能上难分伯仲：若两者最终计算出的 DOM 更新内容完全一致，那么虚拟 DOM 大概率不敌模板渲染；但只要两者在最终 DOM 操作量上拉开那么一点点的差距，虚拟 DOM 就将具备战胜模板渲染的底气。<code>因为虚拟 DOM 的劣势主要在于 JS 计算的耗时，而 DOM 操作的能耗和 JS 计算的能耗根本不在一个量级</code>，极少量的 DOM 操作耗费的性能足以支撑大量的 JS 计算。</p>\\n<blockquote>\\n<p>当然，上面讨论的这种情况相对来说比较极端。在实际的开发中，更加高频的场景是这样的：我每次 setState 的时候只修改少量的数据，比如一个对象中的某几个属性，再比如一个数组中的某几个元素。在这样的场景下，模板渲染和虚拟 DOM 之间 DOM 操作量级的差距就完全拉开了，虚拟 DOM 将在性能上具备绝对的优势。</p>\\n</blockquote>\\n<blockquote>\\n<p>注意，此处的结论是“在 XXX 场景下，虚拟 DOM 相对于 XXX 具备性能优势”，它是有严格限定条件的。有人不到黄河心不死，可能又要问“那虚拟 DOM 对比 jQuery 呢？”“那虚拟 DOM 对比原生 DOM 呢？”。我想说的是，性能问题不能一概而论，而且咱都讲到这个份上了，就不要再钻性能这个牛角尖了。jQuery、原生 DOM 在思维模式上来说和虚拟 DOM 截然不同，强行比较意义不大。</p>\\n</blockquote>\\n<p>前面又是分析又是举例地说了这么多，其实我最终希望你明白的事情只有一件：<code>虚拟 DOM 的价值不在性能，而在别处</code>。因此想要从性能角度来把握虚拟 DOM 的优势，无异于南辕北辙。偏偏在面试场景下，10 个人里面有 9 个都走这条歧路，最后9个人里面自然没有一个能自圆其说，实在让人惋惜。</p>\\n<h2 id=\\"那么虚拟-dom-的价值到底是什么呢\\"><a class=\\"header-anchor\\" href=\\"#那么虚拟-dom-的价值到底是什么呢\\">#</a> 那么虚拟 DOM 的价值到底是什么呢？</h2>\\n<p><strong>虚拟 DOM 解决的关键问题有以下两个。</strong></p>\\n<ul>\\n<li><code>研发体验/研发效率的问题</code>：DOM 操作模式的每一次革新，背后都是前端对效率和体验的进一步追求。虚拟 DOM 的出现，为数据驱动视图这一思想提供了高度可用的载体，使得前端开发能够基于函数式 UI 的编程方式实现高效的声明式编程。</li>\\n<li><code>跨平台的问题</code>：虚拟 DOM 是对真实渲染内容的一层抽象。若没有这一层抽象，那么视图层将和渲染平台紧密耦合在一起，为了描述同样的视图内容，你可能要分别在 Web 端和 Native 端写完全不同的两套甚至多套代码。但现在中间多了一层描述性的虚拟 DOM，它描述的东西可以是真实 DOM，也可以是iOS 界面、安卓界面、小程序......同一套虚拟 DOM，可以对接不同平台的渲染逻辑，从而实现“一次编码，多端运行”，如下图所示。其实说到底，跨平台也是研发提效的一种手段，它在思想上和1是高度呼应的。</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210428201522.png\\" alt=\\"\\"></p>\\n<p>除了差量更新以外，“批量更新”也是虚拟 <code>DOM</code> 在性能方面所做的一个重要努力：“批量更新”在通用虚拟 DOM 库里是由 <code>batch</code> 函数来处理的。在差量更新速度非常快的情况下（比如极短的时间里多次操作同一个 <code>DOM</code>），用户实际上只能看到最后一次更新的效果。这种场景下，前面几次的更新动作虽然意义不大，但都会触发重渲染流程，带来大量不必要的高耗能操作</p>\\n<p>这时就需要请 <code>batch</code> 来帮忙了，<code>batch</code> 的作用是缓冲每次生成的补丁集，它会把收集到的多个补丁集暂存到队列中，再将最终的结果交给渲染函数，最终实现集中化的 <code>DOM</code> 批量更新</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>评审会上有人说引入虚拟 <code>DOM</code> 的唯一理由是“它操作 <code>DOM</code> 一定比手写更快”，而页面每次只更新一个文本节点；你会怎么反驳？</strong></p>\\n<p>不能把虚拟 <code>DOM</code> 的价值归结为单次 <code>DOM</code> 操作必然更快，它首先为数据驱动视图和声明式、函数式 <code>UI</code> 提供了可用载体。差量与批量更新能减少部分无意义渲染，但简单场景下不能据此断言胜过精准手写 <code>DOM</code>；抽象层本身也有计算成本。</p>\\n</li>\\n<li>\\n<p><strong>行情页在极短时间内连续收到多次价格变化，用户只看得到最后一个值，但每次变化都触发渲染；你会如何利用文中机制改造？</strong></p>\\n<p>应把连续更新产生的补丁集先交给 <code>batch</code> 缓冲，在队列中合并这一时段的更新，再把最终结果集中交给渲染函数。这样能避免用户不可见的中间状态反复进入高耗能渲染流程；若每个中间状态都必须展示或触发业务动作，就不能盲目批处理。</p>\\n</li>\\n<li>\\n<p><strong>同一套商品卡片要同时运行在 <code>Web</code>、<code>iOS</code> 和小程序，负责人希望“有虚拟 <code>DOM</code> 就完全不用写平台代码”；你会怎样划清边界？</strong></p>\\n<p>虚拟 <code>DOM</code> 能作为真实渲染内容的描述层，让同一套视图描述对接 <code>DOM</code>、原生界面或小程序等不同渲染逻辑。它降低的是视图层与平台的耦合，并不意味着各平台渲染器和能力差异自动消失；平台特有交互仍需适配，复用程度取决于抽象边界。</p>\\n</li>\\n<li>\\n<p><strong>搜索页一次输入触发了多轮状态更新，线上出现明显卡顿，团队直接把原因归结为“虚拟 <code>DOM</code> 的 <code>Diff</code> 太慢”；你会怎样验证？</strong></p>\\n<p>先记录每次状态变化、补丁生成与实际渲染的次数，确认是否存在短时间内多个最终不可见的中间更新。再启用或模拟批量收集，对照集中提交后渲染次数是否下降；若耗时来自业务计算或最终渲染本身，<code>batch</code> 只能减少重复流程，不能消除单次成本。</p>\\n</li>\\n<li>\\n<p><strong>一个只服务 <code>Web</code> 的小型后台准备为“未来可能跨端”重写整套视图层，你会如何做选型？</strong></p>\\n<p>当前没有明确跨端目标时，不应仅凭“一次编码，多端运行”投入重写，而应先衡量声明式开发效率和现有维护成本。虚拟 <code>DOM</code> 的抽象确实为未来接入不同渲染器留出空间，但跨端收益依赖真实平台计划；提前重写会引入迁移和适配成本。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);