(window.webpackJsonp=window.webpackJsonp||[]).push([[18],{1126:function(n){n.exports=JSON.parse('{"html":"<p>链表结构相对数组、字符串来说，稍微有那么一些些复杂，所以针对链表的真题戏份也相对比较多。\\n前面咱们说过，数组、字符串若想往难了出，那一定是要结合一些超越数据结构本身的东西——比如排序算法、二分思想、动态规划思想等等。因此，这部分对应的难题、综合题，我们需要等知识体系完全构建起来之后，在真题训练环节重新复盘。</p>\\n<p>但是链表可不一样了。如果说在命题时，数组和字符串的角色往往是“算法思想的载体”，那么链表本身就可以被认为是“命题的目的”。单在真题归纳解读环节，我们能讲的技巧、能做的题目已经有很多。结合实际面试中的命题规律，我把这些题目分为以下三类：</p>\\n<ul>\\n<li>链表的处理：合并、删除等（删除操作画个记号，重点中的重点！）</li>\\n<li>链表的反转及其衍生题目</li>\\n<li>链表成环问题及其衍生题目</li>\\n</ul>\\n<p>本节我们就以链表的处理为切入点，一步一步走进链表的世界。</p>\\n<h2 id=\\"链表的合并\\"><a class=\\"header-anchor\\" href=\\"#链表的合并\\">#</a> 链表的合并</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>两条输入链都有序，比较当前头结点，把较小者接到结果尾部并推进对应指针</li>\\n<li><code>dummy</code> 固定结果入口，<code>tail</code> 始终指向已合并前缀最后一个结点，避免单独处理第一个结点</li>\\n<li>一侧耗尽后，另一侧剩余部分本来有序，可以整段接上，不必逐结点复制</li>\\n<li>时间 <code>O(m+n)</code>、迭代辅助空间 <code>O(1)</code>；实现复用并重连原结点，会改变输入链结构</li>\\n<li>相等值取左还是取右决定稳定性；若不能修改输入，需要创建新结点并承担 <code>O(m+n)</code> 空间</li>\\n</ul>\\n<p><strong>合并两条有序链表时，持续比较当前头结点，把较小的结点接到结果链表尾部即可。</strong> <code>dummy</code> 用来固定结果入口，<code>tail</code> 始终指向已合并部分的末尾，因此不用单独处理第一个结点。一条链表耗尽后，另一条剩余部分本身有序，可以直接整体接上，时间复杂度是 <code>O(m+n)</code>，辅助空间是 <code>O(1)</code>。这种写法会重连原结点；如果输入链不能被修改，就需要复制结点并使用 <code>O(m+n)</code> 空间。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>function mergeTwoLists(left, right) {\\n  const dummy = { next: null }\\n  let tail = dummy\\n  while (left !== null &amp;&amp; right !== null) {\\n    if (left.val &lt;= right.val) {\\n      tail.next = left\\n      left = left.next\\n    } else {\\n      tail.next = right\\n      right = right.next\\n    }\\n    tail = tail.next\\n  }\\n  tail.next = left ?? right\\n  return dummy.next\\n}\\n</code></pre>\\n<p>循环前，<code>dummy.next..tail</code> 已经包含两条链中被消费的最小元素且保持有序；<code>left</code>、<code>right</code> 分别指向未处理部分最小值。每轮至少推进一个指针，必然终止。该版本会重连原结点；如果其他调用方仍持有旧链并假设其结构不变，应复制结点或明确转移所有权。</p>\\n<blockquote>\\n<p>真题描述：将两个有序链表合并为一个新的有序链表并返回。新链表是通过拼接给定的两个链表的所有结点组成的。</p>\\n</blockquote>\\n<p>示例：</p>\\n<pre class=\\"language-text\\"><code>输入：1-&gt;2-&gt;4, 1-&gt;3-&gt;4 输出：1-&gt;1-&gt;2-&gt;3-&gt;4-&gt;4\\n</code></pre>\\n<p><strong>思路分析</strong></p>\\n<blockquote>\\n<p>做链表处理类问题，大家要把握住一个中心思想——处理链表的本质，是处理链表结点之间的指针关系。</p>\\n</blockquote>\\n<p>这道题也不例外，我们先来看看处理前两个链表的情况：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114154.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>两个链表如果想要合并为一个链表，我们恰当地补齐双方之间结点 next 指针的指向关系，就能达到目的。</li>\\n<li>如果这么说仍然让你觉得抽象，那么大家不妨把图上的6个结点想象成6个扣子：现在的情况是，6个扣子被分成了两拨，各自由一根线把它们穿起来。而我们的目的是让这六个扣子按照一定的顺序，串到一根线上去。这时候需要咱们做的就是一个穿针引线的活儿，现在线有了，咱缺的是一根针</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210905114202.png\\" alt=\\"\\"></p>\\n<p>这根针每次钻进扣子眼儿之前，要先比较一下它眼前的两个扣子，选择其中值较小的那个，优先把它串进去。一次串一个，直到所有的扣子都被串进一条线为止（下图中红色箭头表明穿针的过程与方向）：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114209.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>同时我们还要考虑 l1 和 l2 两个链表长度不等的情况：若其中一个链表已经完全被串进新链表里了，而另一个链表还有剩余结点，考虑到该链表本身就是有序的，我们可以直接把它整个拼到目标链表的尾部。</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {ListNode} l1\\n * @param {ListNode} l2\\n * @return {ListNode}\\n */\\nconst mergeTwoLists = function(l1, l2) {\\n  // 定义头结点，确保链表可以被访问到\\n  let head = new ListNode()\\n  // cur 这里就是咱们那根“针”\\n  let cur = head\\n  // “针”开始在 l1 和 l2 间穿梭了\\n  while(l1 &amp;&amp; l2) {\\n      // 如果 l1 的结点值较小\\n      if(l1.val&lt;=l2.val) {\\n          // 先串起 l1 的结点\\n          cur.next = l1\\n          // l1 指针向前一步\\n          l1 = l1.next\\n      } else {\\n          // l2 较小时，串起 l2 结点\\n          cur.next = l2\\n          // l2 向前一步\\n          l2 = l2.next\\n      }\\n      \\n      // “针”在串起一个结点后，也会往前一步\\n      cur = cur.next \\n\\n  }\\n  \\n  // 处理链表不等长的情况\\n  cur.next = l1!==null?l1:l2\\n  // 返回起始结点\\n  return head.next\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人说合并后的链表是“新的”，所以实现一定不会修改两条输入链；面对 <code>tail.next = left</code> 这行代码，你会怎么反驳？</strong></p>\\n<p>“新的有序链表”不代表结点必须新建，这段实现复用原结点并改写它们的 <code>next</code> 关系。调用方若仍按旧结构持有输入链，观察到的连接会发生变化；需要保留原链时必须复制结点，代价是额外的线性空间。</p>\\n</li>\\n<li>\\n<p><strong>订单页面合并两条已按时间排序的链表，相同时间的订单还要保持左侧数据源优先，比较条件该写 <code>&lt;</code> 还是 <code>&lt;=</code>？</strong></p>\\n<p>应使用 <code>&lt;=</code>，相等时先连接左链结点，才能维持约定的跨数据源稳定顺序。改成 <code>&lt;</code> 仍能保证键值有序，却会让右链相等元素排在前面；结点携带来源或创建次序时，这种差异会影响业务展示。</p>\\n</li>\\n<li>\\n<p><strong>接口从合并两条有序链改成合并 <code>k</code> 条有序链，继续逐条调用 <code>mergeTwoLists</code> 有什么取舍？</strong></p>\\n<p>逐条合并实现简单并能复用现有代码，但早期已合并出的长链会被反复遍历，链数增大时不够均衡。可按两两分治合并，或用最小堆维护各链表头；后者需要额外结构，具体选择还取决于链数和依赖约束。</p>\\n</li>\\n<li>\\n<p><strong>线上任务合并时 <code>CPU</code> 持续占用且请求不返回，采样显示 <code>while (left !== null &amp;&amp; right !== null)</code> 一直运行，你会优先排查什么？</strong></p>\\n<p>优先检查输入链是否成环，以及结点的 <code>next</code> 是否在进入函数前已被并发或其他逻辑破坏；合法无环输入下，每轮必然推进一个指针。对不可信入口可先做环检测或设置访问上限，但会增加遍历成本或引入截断策略。</p>\\n</li>\\n<li>\\n<p><strong>内存敏感的服务要求低额外空间，而另一个调用方要求输入链完全不可变，两项约束冲突时你怎样定接口？</strong></p>\\n<p>复用结点可保持常量级辅助空间，但必然重连原链；复制结点能保护输入结构，却需要与结点数量同阶的新内存。接口应明确“消费输入”还是“只读输入”，无法同时满足时不能把所有权变化藏在实现细节里。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"链表结点的删除\\"><a class=\\"header-anchor\\" href=\\"#链表结点的删除\\">#</a> 链表结点的删除</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>题目要求有序链表“每个值保留一个”，重复值必然连续，只需比较 <code>current</code> 与 <code>current.next</code></li>\\n<li>相等时执行 <code>current.next = current.next.next</code>，但不能移动 current，以便继续删除第三、第四个副本</li>\\n<li>不相等时才能推进 current；这条分支决定循环是否既不漏删又能终止</li>\\n<li>时间 <code>O(n)</code>、辅助空间 <code>O(1)</code>，原地修改链表；空链和单结点自然返回</li>\\n<li>若输入无序，相同值不连续，该算法不正确，需要哈希集合或先排序，并承担相应代价</li>\\n</ul>\\n<p><strong>有序链表中的重复值连续出现，所以比较 <code>current</code> 和 <code>current.next</code> 就能完成去重。</strong> 两者相等时让 <code>current.next</code> 指向下下个结点，但不要移动 <code>current</code>，否则像 <code>1→1→1</code> 这样的连续重复可能漏删。只有值不相等时才向后推进，这样每个结点最多处理一次，时间复杂度是 <code>O(n)</code>，辅助空间是 <code>O(1)</code>。该方法会原地修改链表，而且只适用于有序输入；无序链表需要额外的哈希集合或先排序。</p>\\n</section>\\n<p>原文代码的关键不是 <code>while</code> 本身，而是相等分支不推进 <code>cur</code>。输入 <code>1→1→1</code> 时，第一次删掉第二个 <code>1</code> 后，当前结点还要与新的后继继续比较：</p>\\n<pre class=\\"language-js\\"><code>function keepOneDuplicate(head) {\\n  let current = head\\n  while (current?.next) {\\n    if (current.val === current.next.val) current.next = current.next.next\\n    else current = current.next\\n  }\\n  return head\\n}\\n</code></pre>\\n<p>每个被保留或删除的结点最多处理一次，时间 <code>O(n)</code>。被跳过结点在没有其他引用时会由垃圾回收处理；若结点持有文件、订阅等外部资源，链表断开不会自动释放那些资源，需要业务层显式清理。</p>\\n<p>我们先来看一道基础题目：</p>\\n<blockquote>\\n<p>真题描述：给定一个排序链表，删除所有重复的元素，使得每个元素只出现一次。</p>\\n</blockquote>\\n<p>示例 1:</p>\\n<ul>\\n<li>输入: 1-&gt;1-&gt;2</li>\\n<li>输出: 1-&gt;2</li>\\n</ul>\\n<p>示例 2:</p>\\n<ul>\\n<li>输入: 1-&gt;1-&gt;2-&gt;3-&gt;3</li>\\n<li>输出: 1-&gt;2-&gt;3</li>\\n</ul>\\n<p><strong>思路分析</strong></p>\\n<blockquote>\\n<p>链表的删除是一个基础且关键的操作，我们在数据结构部分就已经对该操作的编码实现进行过介绍，这里直接复用大家已经学过的删除能力，将需要删除的目标结点的前驱结点 next 指针往后指一格：</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {ListNode} head\\n * @return {ListNode}\\n */\\nconst deleteDuplicates = function(head) {\\n    // 设定 cur 指针，初始位置为链表第一个结点\\n    let cur = head;\\n    // 遍历链表\\n    while(cur != null &amp;&amp; cur.next != null) {\\n        // 若当前结点和它后面一个结点值相等（重复）\\n        if(cur.val === cur.next.val) {\\n            // 删除靠后的那个结点（去重）\\n            cur.next = cur.next.next;\\n        } else {\\n            // 若不重复，继续遍历\\n            cur = cur.next;\\n        }\\n    }\\n    return head;\\n};\\n</code></pre>\\n<p>大家不要小看了这么一道简简单单的基础题目，在实际面试中，下不了笔的、写不囫囵的、写了跑不起来的，大有人在。</p>\\n<p>一道题之所以能够成为面试题，一定有其考察意义在。拿这道题来说，既能考察你链表的遍历（while循环），又能考察你链表的 CRUD 中最热门的删除操作，候选人做这道题的情况，一定程度上可以反馈其基本功的扎实度。做对了是正常，如果做不对，那么在算法和数据结构这个考察环节，你的处境就有点危险了。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在 <code>1→1→1</code> 上每次删除重复后都执行 <code>current = current.next</code>，页面结果为什么会错误地留下两个 <code>1</code>？</strong></p>\\n<p>第一次跨过第二个 <code>1</code> 后，当前结点的新后继仍然是重复值，此时推进 <code>current</code> 就跳过了再次比较。相等分支必须停在原结点并继续删除后继，只有值不同时才能前进；否则连续重复组会漏删。</p>\\n</li>\\n<li>\\n<p><strong>缓存服务维护一条已排序的键链，只想保留每个键的一个结点，原地删除时怎样说明复杂度和资源处理边界？</strong></p>\\n<p>单次遍历中，每个结点至多被保留或跳过一次，因此时间为 <code>O(n)</code>，只使用当前指针时辅助空间为 <code>O(1)</code>。被断开的普通对象可由垃圾回收处理；若结点持有文件、订阅等外部资源，还需业务层显式释放。</p>\\n</li>\\n<li>\\n<p><strong>输入链不再保证排序，但产品仍要求删除后续重复值并保留首次出现顺序，原来的相邻比较为什么失效？</strong></p>\\n<p>原实现只能发现相邻的相同值，无序链中的重复项可能相隔多个结点，因此会被遗漏。可用 <code>Set</code> 记录已见值并原地跳过后续副本，期望时间为 <code>O(n)</code>、空间为 <code>O(n)</code>；先排序则会改变原顺序。</p>\\n</li>\\n<li>\\n<p><strong>线上偶发只删除每组的第二个重复结点，日志中 <code>current</code> 在相等分支也发生了移动，你会怎样加最小化用例定位？</strong></p>\\n<p>先用 <code>1→1→1</code> 和 <code>1→1→2→2→2</code> 复现，逐轮记录 <code>current.val</code>、<code>current.next?.val</code> 及链表快照。若删除后立即推进指针，就能直接看到新后继未经比较；修复后还应覆盖空链、单结点和无重复链。</p>\\n</li>\\n<li>\\n<p><strong>评审中有人要求每次删除都把被跳过结点的 <code>next</code> 设为 <code>null</code>，你会无条件接受吗？</strong></p>\\n<p>不会，算法正确性并不依赖显式置空，没有其他引用时垃圾回收会处理不可达结点。若对象池、调试或所有权协议要求完全脱链，可以先保存被删结点及其后继再置空；操作顺序错误会丢失剩余链表。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"删除问题的延伸-dummy-结点登场\\"><a class=\\"header-anchor\\" href=\\"#删除问题的延伸-dummy-结点登场\\">#</a> 删除问题的延伸——dummy 结点登场</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>本题不是“重复值保留一个”，而是重复组全部删除；必须先确认一组长度，再决定保留或跨过</li>\\n<li><code>dummy.next = head</code> 为原头结点补出统一前驱，使头部重复组和中间重复组使用同一逻辑</li>\\n<li><code>previous</code> 指向已确认结果尾部；发现重复组时它不动，只把 <code>previous.next</code> 接到组后第一个不同值</li>\\n<li>时间 <code>O(n)</code>、辅助空间 <code>O(1)</code>；所有结点都重复时返回 <code>null</code></li>\\n<li>dummy 是哨兵，不属于业务结果；返回永远是 <code>dummy.next</code>，不能误返回 dummy</li>\\n</ul>\\n<p><strong>这道题要把重复值对应的整组结点全部删除，不能只留下一个。</strong> 我一般会用 <code>dummy.next = head</code> 给头结点补一个统一前驱，再让 <code>previous</code> 指向已经确认的结果尾部，由 <code>cursor</code> 找到当前同值组的末尾。组内有多个结点时，<code>previous</code> 不动，只把 <code>previous.next</code> 接到下一组；只有当前组长度为一时才向后推进。这样时间复杂度是 <code>O(n)</code>、辅助空间是 <code>O(1)</code>，即使所有结点都重复也能通过返回 <code>dummy.next</code> 得到 <code>null</code>，而不是误返回哨兵结点。</p>\\n</section>\\n<pre class=\\"language-js\\"><code>function removeAllDuplicates(head) {\\n  const dummy = { next: head }\\n  let previous = dummy\\n  while (previous.next !== null) {\\n    let cursor = previous.next\\n    while (cursor.next !== null &amp;&amp; cursor.next.val === cursor.val) cursor = cursor.next\\n    if (cursor !== previous.next) previous.next = cursor.next\\n    else previous = previous.next\\n  }\\n  return dummy.next\\n}\\n</code></pre>\\n<p>进入每轮时，dummy 到 previous 是已经确认不会重复的结果前缀。<code>cursor</code> 找到当前同值组末尾；组长大于一就整段跨过，组长为一才推进 previous。输入 <code>1→1→1</code> 时 previous 始终是 dummy，最后 <code>dummy.next</code> 变为 null。</p>\\n<blockquote>\\n<p>真题描述：给定一个排序链表，删除所有含有重复数字的结点，只保留原始链表中 没有重复出现的数字。</p>\\n</blockquote>\\n<p>示例 1:</p>\\n<ul>\\n<li>输入: 1-&gt;2-&gt;3-&gt;3-&gt;4-&gt;4-&gt;5</li>\\n<li>输出: 1-&gt;2-&gt;5</li>\\n</ul>\\n<p>示例 2:</p>\\n<ul>\\n<li>输入: 1-&gt;1-&gt;1-&gt;2-&gt;3</li>\\n<li>输出: 2-&gt;3</li>\\n</ul>\\n<p><strong>思路分析</strong></p>\\n<blockquote>\\n<p>我们先来分析一下这道题和上道题有什么异同哈：相同的地方比较明显，都是删除重复元素。不同的地方在于，楼上我们删到没有重复元素就行了，可以留个“独苗”；但现在，题干要求我们只要一个元素发生了重复，就要把它彻底从链表中干掉，一个不留。</p>\\n</blockquote>\\n<p>这带来了一个什么问题呢？我们回顾一下前面咱们是怎么做删除的：在遍历的过程中判断当前结点和后继结点之间是否存在值相等的情况，若有，直接对后继结点进行删除：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114238.png\\" alt=\\"\\"></p>\\n<p>这个过程非常自然，为啥？因为咱们要删除某一个目标结点时，必须知道它的前驱结点。在上图中，我们本来就是站在前驱结点的位置，对其后继结点进行删除，只需要将前驱结点的 next 指针往后挪一位就行了。</p>\\n<p>但是现在，咱们要做的事情变成了把前驱和后继一起删掉，前面两个值为1的结点要一起狗带才行，起始结点直接变成了第三个：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114246.png\\" alt=\\"\\">\\n如果继续沿用刚才的思路，我们会发现完全走不通。因为我们的 cur 指针就是从图中第一个结点出发开始遍历的，无法定位到第一个结点的前驱结点，删除便无法完成。</p>\\n<p>其实在链表题中，经常会遇到这样的问题：链表的第一个结点，因为没有前驱结点，导致我们面对它无从下手。这时我们就可以用一个 dummy 结点来解决这个问题。\\n所谓 dummy 结点，就是咱们人为制造出来的第一个结点的前驱结点，这样链表中所有的结点都能确保有一个前驱结点，也就都能够用同样的逻辑来处理了。\\ndummy 结点能够帮助我们降低链表处理过程的复杂度，处理链表时，不设 dummy 结点思路可能会打不开；设了 dummy 结点的话，就算不一定用得上，也不会出错。所以笔者个人非常喜欢用 dummy 结点。有心的同学可能也会注意到，在本节的第一题“链表的合并”中，其实也有 dummy 结点的身影。</p>\\n<p>回到这道题上来，我们首先要做的就是定义一个 dummy 结点，指向链表的起始位置：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905114254.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>这样一来，如果想要删除两个连续重复的值为 1 的结点，我们只需要把 dummy 结点的 next 指针直接指向 2：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905114301.png\\" alt=\\"\\"></p>\\n<p>如此一来，就大功告成啦~</p>\\n<blockquote>\\n<p>注意：由于重复的结点可能不止一个两个，我们这里需要用一个 <code>while</code> 循环来反复地进行重复结点的判断和删除操作。</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {ListNode} head\\n * @return {ListNode}\\n */\\nconst deleteDuplicates = function(head) {\\n    // 极端情况：0个或1个结点，则不会重复，直接返回\\n    if(!head || !head.next) {\\n        return head\\n    }\\n    // dummy 登场\\n    let dummy = new ListNode() \\n    // dummy 永远指向头结点\\n    dummy.next = head   \\n    // cur 从 dummy 开始遍历\\n    let cur = dummy \\n    // 当 cur 的后面有至少两个结点时\\n    while(cur.next &amp;&amp; cur.next.next) {\\n        // 对 cur 后面的两个结点进行比较\\n        if(cur.next.val === cur.next.next.val) {\\n            // 若值重复，则记下这个值\\n            let val = cur.next.val\\n            // 反复地排查后面的元素是否存在多次重复该值的情况\\n            while(cur.next &amp;&amp; cur.next.val===val) {\\n                // 若有，则删除\\n                cur.next = cur.next.next \\n            }\\n        } else {\\n            // 若不重复，则正常遍历\\n            cur = cur.next\\n        }\\n    }\\n    // 返回链表的起始结点\\n    return dummy.next;\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在用户标签链 <code>1→1→2</code> 上复用上一题“相等就删后一个”的代码，产品却要求重复标签全部消失；页面为什么仍会显示一个 <code>1</code>？</strong></p>\\n<p>上一题的语义是每组重复值保留一个，因此只删除当前结点的重复后继，组内第一个 <code>1</code> 必然留下。本题必须先识别完整同值组，再让该组前驱的 <code>next</code> 跨到组后；若没有保存前驱，就无法统一删除头部重复组。</p>\\n</li>\\n<li>\\n<p><strong>订单链以 <code>1→1→1→2</code> 开头，不使用 <code>dummy</code> 时，工程代码会额外承担哪些头结点分支？</strong></p>\\n<p>代码必须单独更新 <code>head</code>，并在删除后继续判断新的头结点是否仍属于重复组，容易与中间结点逻辑分叉。<code>dummy</code> 人为提供稳定前驱，使头部和中间删除都归一为修改 <code>previous.next</code>；代价只是一个哨兵对象。</p>\\n</li>\\n<li>\\n<p><strong>数据模型允许任意整数，有人建议把 <code>dummy.val</code> 设为 <code>-1</code> 并参与相等比较，这个选型有什么风险？</strong></p>\\n<p><code>-1</code> 可能本身就是合法数据，一旦哨兵值参与比较，头部结点可能被误判为与哨兵重复。这里的 <code>dummy</code> 只需要保存 <code>next</code>，算法不应读取其 <code>val</code>；依赖特殊值会把数据域限制偷偷带入正确性条件。</p>\\n</li>\\n<li>\\n<p><strong>线上输入 <code>1→1→1</code> 后仍残留一个结点，断点显示发现重复组后 <code>previous</code> 被推进了，你会检查哪条循环不变式？</strong></p>\\n<p>应确认 <code>dummy</code> 到 <code>previous</code> 始终是已确定不会重复的结果前缀，重复组尚未处理完时不能推进 <code>previous</code>。让 <code>cursor</code> 先走到同值组末尾，再一次执行 <code>previous.next = cursor.next</code>；若组长为一，才把 <code>previous</code> 前移。</p>\\n</li>\\n<li>\\n<p><strong>团队在“逐个删除重复结点”和“先找组尾再整段跨过”之间争论，你会怎样结合可读性和正确性取舍？</strong></p>\\n<p>两种写法都能成立，但必须保证重复组的第一个结点也被删除，并且前驱在整组处理期间保持不动。先找组尾能直接表达“组长大于一则整组移除”，循环不变式更清晰；逐个删除若状态设计不严谨，更容易误留首结点。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"小结\\"><a class=\\"header-anchor\\" href=\\"#小结\\">#</a> 小结</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>链表题先画结点和箭头，所有修改都要确保未处理链路仍可达；必要时先保存 <code>next</code></li>\\n<li>合并靠双指针选择较小结点，删除靠前驱跨越目标，头结点边界用 dummy 统一</li>\\n<li>复杂度要说明每个结点访问次数与是否复用原结点；原地重连通常时间 <code>O(n)</code>、额外空间 <code>O(1)</code></li>\\n</ul>\\n<p><strong>处理链表题时，我会先画清结点和指针关系，保证每次修改后未处理的链路仍然可达。</strong> 如果后续还要访问某个结点，改动指针前就要先保存它的 <code>next</code>，否则很容易把剩余链表弄丢。合并链表本质上是用双指针选择较小结点，删除结点则依赖前驱跨过目标，涉及头结点时可以用 <code>dummy</code> 统一边界。复用原结点并原地重连时，通常每个结点只需访问有限次数，因此时间为 <code>O(n)</code>、额外空间为 <code>O(1)</code>。</p>\\n</section>\\n<p>在本节，我们对链表真题大军中的“小可爱”流派进行了剖析。通过对“小可爱”流派的学习，能够帮助大家巩固对链表数据结构特性的认知，同时强化基本的硬编码能力。</p>\\n<p>从下节开始，我们将用两个专题的时间，分解链表中的“不可爱”流派。接下来的学习或许不像本节一样轻松，但相信一定能给大家带来更重磅的收获~~</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>复盘页面把本节链表题都标成“简单”，新人因此认为只要背下代码就算掌握；你会用什么代码现象反驳他？</strong></p>\\n<p>能背出代码不等于掌握链表，结点引用、指针推进和边界处理稍有偏差，代码就可能断链、漏结点或无法结束。复盘时应让他脱离模板说明每次连接后的链表状态；若只能复述写法，本节强化数据结构认知与硬编码能力的目标仍未达到。</p>\\n</li>\\n<li>\\n<p><strong>团队准备把本节题目做成新人训练页，你会怎样设计验收，避免页面只检查最终输出？</strong></p>\\n<p>验收应同时检查结果和链表操作过程，让新人解释当前结点、下一结点以及连接完成后的结构变化。还应加入空链表、单结点和短链表等边界输入；只比较最终数组会掩盖指针操作错误，也无法判断基本编码能力是否真正稳定。</p>\\n</li>\\n<li>\\n<p><strong>候选人做完本节基础题后，要求直接跳过后续两个链表专题，因为他认为题型都只是指针重连；你会怎么判断？</strong></p>\\n<p>不应仅凭完成基础题就跳过后续专题，本节定位是巩固链表特性与基本硬编码能力，不能代表更复杂链表题也已掌握。可先用后续专题的入口题验证其建模和边界分析；若仍依赖模板，应按原学习路径继续，而不是用基础题表现外推。</p>\\n</li>\\n<li>\\n<p><strong>训练平台线上出现同一道基础链表题时而超时、时而丢失尾结点，你会要求维护者先排查什么？</strong></p>\\n<p>先检查指针是否在每个分支都推进，以及重连前是否保存了后续结点，因为这两类错误分别容易造成循环无法结束和剩余链表丢失。随后用极短输入逐步打印结点关系；若只增加超时阈值或补结果特判，结构性错误仍会保留。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);