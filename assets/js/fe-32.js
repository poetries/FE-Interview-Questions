(window.webpackJsonp=window.webpackJsonp||[]).push([[32],{1140:function(o){o.exports=JSON.parse('{"html":"<p>在上一节，我们掌握了求解动态规划问题的通用套路。但正如我们前面所说，通用思路存在一定的局限性——它提供给大家的毕竟是一种方向性的引导，至于能不能实实在在地用自己的双手解决掉一道具体的题目，更多还是看同学们自己的“造化”。</p>\\n<blockquote>\\n<p>所谓“造化”，其实并不神秘，它就是指我们自己对题目和知识点的思考深度和吸收程度。“造化”是否到位，并不取决于天赋，而是取决于每一位同学自身对题目的量的积累和对解题技术的总结。</p>\\n</blockquote>\\n<p>作为对通用解题套路的补充，本节笔者基于自己接触过的海量动态规划真题，结合个人对面试命题倾向的观察和思考，提取了两个学习性价比极高的重点解题模型。\\n模型可以帮助我们迅速地识别并解决掉一类问题，通过对重点模型进行学习，大家可以在实战中做到举一反N，大大提升我们做题的效率和专业程度。</p>\\n<h2 id=\\"_0-1背包模型\\"><a class=\\"header-anchor\\" href=\\"#_0-1背包模型\\">#</a> 0-1背包模型</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>dp[c]</code> 表示容量不超过 <code>c</code> 时的最大价值。</li>\\n<li>每件物品只有选/不选两种；一维压缩后容量必须倒序，避免同一物品本轮重复使用。</li>\\n<li>转移：<code>dp[c] = max(dp[c], dp[c-weight] + value)</code>。</li>\\n<li>时间 <code>O(nC)</code>、空间 <code>O(C)</code>；容量巨大时要考虑值域 DP 或其他算法。</li>\\n</ul>\\n<p><strong><code>0-1</code> 背包用 <code>dp[c]</code> 表示容量不超过 <code>c</code> 时能取得的最大价值，每件物品只能选择一次或不选。</strong> 一维状态的转移是 <code>dp[c] = max(dp[c], dp[c-weight] + value)</code>，它比较不放当前物品和放入当前物品两种结果。容量必须从大到小更新，因为正序会读到本轮刚写入的状态，让同一件物品被重复使用，问题就变成了完全背包。该做法时间复杂度为 <code>O(nC)</code>、空间复杂度为 <code>O(C)</code>；如果容量特别大，还要考虑值域 <code>DP</code> 或其他算法。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“0-1 的关键是每件只能用一次，所以一维 dp 倒序更新；若正序，同一物品的新状态会被本轮再次读取，悄悄变成完全背包。”</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审里有人把容量循环改成从小到大，声称 <code>Math.max</code> 会自动避免重复选择；用一个重量能被容量容纳多次的物品，结果会发生什么？</strong></p>\\n<p>结果可能在同一轮多次计入该物品，因为较大容量会读取本轮刚更新的 <code>dp[c-weight]</code>。<code>Math.max</code> 只比较候选价值，并不限制物品使用次数；这段代码的语义已经悄悄接近完全背包，违反每件物品只能选一次的约束。</p>\\n</li>\\n<li>\\n<p><strong>商品组合页有数百件候选商品，前端只需计算容量上限内的最大价值，不展示具体清单；你会怎样落地 <code>0-1</code> 背包状态？</strong></p>\\n<p>可用长度为容量加一的一维 <code>dp</code> 数组，外层逐件遍历商品，内层容量从上限倒序到当前重量。这样每次转移仍读取上一轮语义的状态，并省去完整二维表；若之后要恢复商品清单，仅保留价值数组就不够，需要追加来源记录。</p>\\n</li>\\n<li>\\n<p><strong>产品经理把规则改成同一种商品可选任意件，但开发仍坚持倒序容量更新；页面会漏掉哪类最优组合？</strong></p>\\n<p>倒序更新会让本轮无法复用刚写入的状态，因此同一商品仍最多贡献一次，可能漏掉重复选择该商品形成的更高价值组合。规则变成可重复选取后应改为正序容量更新；代价是模型语义变为完全背包，不能与原 <code>0-1</code> 约束混用。</p>\\n</li>\\n<li>\\n<p><strong>线上组合结果异常偏大，日志显示只有一件重量为 <code>2</code>、价值为 <code>3</code> 的商品，容量为 <code>6</code> 时却得到 <code>9</code>；你会先查哪一层循环？</strong></p>\\n<p>先查容量循环是否从 <code>2</code> 正序走到 <code>6</code>，因为该顺序会依次读到本轮生成的 <code>dp[2]</code> 和 <code>dp[4]</code>，把同一件商品累计三次。修复为从 <code>6</code> 倒序到 <code>2</code>，再用单物品用例回归；若商品本就允许重复，则异常来自需求模型选错。</p>\\n</li>\\n</ul>\\n</section>\\n<blockquote>\\n<p>0-1背包问题是一个基本问题，基于这个基本问题，可以衍生出千姿百态的变种问题，这种题目就非常适合拿来构造解题模型。</p>\\n</blockquote>\\n<p>0-1背包问题说的是这么回事儿：</p>\\n<blockquote>\\n<p>有 n 件物品，物品体积用一个名为 w 的数组存起来，物品的价值用一个名为 value 的数组存起来；每件物品的体积用 w[i] 来表示，每件物品的价值用 value[i] 来表示。现在有一个容量为 c 的背包，问你如何选取物品放入背包，才能使得背包内的物品总价值最大？</p>\\n</blockquote>\\n<p>注意：每种物品都只有1件</p>\\n<h3 id=\\"思路分析\\"><a class=\\"header-anchor\\" href=\\"#思路分析\\">#</a> 思路分析</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>每件物品只能选一次，状态可定义为容量为 <code>c</code> 时前若干物品能取得的最大价值</li>\\n<li>处理完第 <code>i</code> 件物品后，<code>dp[c]</code> 只代表前 <code>i</code> 件物品的最优解</li>\\n<li>压成一维后容量必须从大到小遍历，否则本轮刚更新的状态会让同一物品被重复使用</li>\\n</ul>\\n<p><strong>这道题适合用动态规划，因为每件物品只有选和不选两种状态，而直接枚举会产生 <code>2^n</code> 种组合。</strong> 可以定义 <code>dp[i][c]</code> 为只看前 <code>i</code> 件物品、容量为 <code>c</code> 时的最大价值，再比较不选第 <code>i</code> 件的 <code>dp[i-1][c]</code>，以及选它后的 <code>dp[i-1][c-w[i]] + value[i]</code>。由于当前行只依赖上一行，二维数组可以压缩成一维数组。压缩后容量必须倒序遍历，确保读取的仍是上一轮状态，避免当前物品在同一轮被重复选中。</p>\\n</section>\\n<blockquote>\\n<p>这道题如果全靠本能来做，相信不少同学会联想到“暴力枚举法”：暴力枚举每一件物品放或者不放进背包的情况。考虑到每一种物品都面临“放”和“不放”两种选择，因此 n 个物品就对应 2^n 种情况，进而会带来高达 O(2^n)的时间复杂度。这个时间复杂度是众多复杂度中相对来说比较恐怖的“指数量级”，我们是万万不能让这种东西出现在面试题解中的，因此果断放弃它。</p>\\n</blockquote>\\n<p>现在我们放弃本能，回归理智，开始调度自己的智慧来做题：这道题最后问了“如何才能使背包内的物品总价值最大？”，我们前面讲过，遇到最值问题，一定要在可能的解题方案中给动态优化留下一席之地。事实上，背包系列问题，正是动态规划的标准对口问题。</p>\\n<p>下面我们基于通用解题思路来梳理一下这道题：</p>\\n<p>“倒推”法明确状态间关系</p>\\n<blockquote>\\n<p>现在，假设背包已满，容量已经达到了 c。站在c这个容量终点往后退，考虑从中取出一样物品，那么可能被取出的物品就有 i 种可能性。我们现在尝试表达“取出一件”这个动作对应的变化，我用 f(i, c) 来表示前 i 件物品恰好装入容量为 c 的背包中所能获得的最大价值。现在假设我试图取出的物品是 i，那么只有两种可能：</p>\\n</blockquote>\\n<ul>\\n<li>第 i 件物品在背包里</li>\\n<li>第 i 件物品不在背包里</li>\\n</ul>\\n<p>如果说本来这个背包中就没有 i 这个东西，那么尝试取之前和尝试取之后，背包中的价值总量是不会发生变化的。：</p>\\n<pre class=\\"language-text\\"><code>f(i, c) = f(i-1, c)\\n</code></pre>\\n<blockquote>\\n<p>但如果背包中是有 i 的，那么取出这个动作就会带来价值量和体积量的减少：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>f(i, c) - value[i] = f(i-1, c-w[i])\\n</code></pre>\\n<p>把这个减法关系稍微转化一下，变为加法关系：</p>\\n<pre class=\\"language-text\\"><code>f(i, c) = f(i-1, c-w[i]) + value[i]\\n</code></pre>\\n<blockquote>\\n<p>可以看出，想要求出 f(i, c)，我们只要定位到正确的 f(i-1, c) 和 f(i-1, c-w[i]) + value[i] 的值，并且取出两者中较大的值就可以了。如此，我们便明确出了这道题的状态转移关系。现在我们需要思考的是如何把这种关系用代码的形式表达出来。</p>\\n</blockquote>\\n<p>首先，基于上面的分析，我们抽取出自变量和因变量：自变量是物品的索引（假设为i）和当前背包内物品的总体积（假设为 v），因变量是总价值。我们仍然是用一个数组来记忆不同状态下的总价值，考虑到这道题中存在两个自变量，我们需要开辟的是一个二维数组。现在我利用二维数组来将上述的状态关系编码化：</p>\\n<pre class=\\"language-text\\"><code>dp[i][v] = Math.max(dp[i-1][v], dp[i-1][v-w[i]] + c[i])\\n</code></pre>\\n<blockquote>\\n<p>以上便是这道题对应的状态转移方程。你会发现我前面真没忽悠你——只要能够利用“倒推”法明确出状态转移关系，我们根本没有必要去构造一个完整而复杂的树形思维模型，直接把状态转移方程往循环里塞就行。</p>\\n</blockquote>\\n<blockquote>\\n<p>为什么我这么笃定？别忘了，动态规划的关键特性就是“最优子结构”（对这个概念感到模糊的同学，赶紧回到上一节去复习一下）。这道题符合最优子结构的特征——dp[i]只和它之前的状态dp[i-1]有关。最优子结构允许我们推导一次就知晓全局，就是这么爽。</p>\\n</blockquote>\\n<p>现在我们来瞅瞅这个状态转移方程怎么往循环里塞才合适。仍然是从变量入手：变量是 i 和 v，但本质上来说 v 其实也是随着 i 的变化而变化的，因此我们可以在外层遍历i、在内层遍历 v。明白了这一点，我们就可以编码如下：</p>\\n<pre class=\\"language-text\\"><code>for(let i=1;i&lt;=n;i++) {\\n    for(let v=w[i]; v&lt;=c;v++) {\\n      dp[i][v] = Math.max(dp[i-1][v], dp[i-1][v-w[i]]+value[i])\\n    }\\n}\\n</code></pre>\\n<blockquote>\\n<p>现在，时间复杂度已经被我们优化到了 O(n)的水平，相当不错。但是空间复杂度其实还可以抢救一下。不过不着急，初学背包问题，我们先站在巩固思路的角度，重现一下这个二维数组的填充过程</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123925.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<ul>\\n<li>从图中我们可以看出，计算 dp[i][v] 的时候，其实只需要图中标红位置的数据就可以了（这与我们前面讲解过的最优子结构特性不谋而合），也就是说未标红的地方对于 dp[i][v] 的计算来说都属于冗余数据。实际上，对于第 i 行的计算来说，只有第 i-1 行的数据是有意义的，更早的数据它都不关心。也就是说我们其实根本不需要记录所有的数据，理论上只要保留当前行和上一行的数据就足够了。</li>\\n<li>一些教材或许会教你通过优化二位数组来节省空间上的开销，但这种手段在笔者看来无异于隔靴搔痒——要优化就优化到底，我们干脆把二维数组干掉，用一维数组来做。</li>\\n</ul>\\n</blockquote>\\n<p><strong>插播小知识——滚动数组</strong></p>\\n<blockquote>\\n<p>这里要给大家介绍的是一种叫做“滚动数组”的编码思想——所谓“滚动数组”，顾名思义，就是让数组“滚动”起来：固定一块存储空间，滚动更新这块存储空间的内容，确保每个时刻空间内的数据都是当前真正会用到的最新数据，从而达到节约内存的效果，这种手段就叫做滚动数组。</p>\\n</blockquote>\\n<p><strong>用滚动数组来优化状态转移方程</strong></p>\\n<p>我可以只定义一个一维数组，通过倒着遍历v的方法来实现数组的滚动更新：</p>\\n<pre class=\\"language-js\\"><code>for(let i=1;i&lt;=n;i++) {\\n    for(let v=c;v&gt;=w[i];v--) {\\n        dp[v] = Math.max(dp[v], d[v-w[i]]+value[i])\\n    }\\n}\\n</code></pre>\\n<blockquote>\\n<p>这个方法到底骚在哪里？它为什么可以实现数组的滚动更新？接下来咱们就一起来瞅瞅这个数组是怎么“滚”的：</p>\\n</blockquote>\\n<p>拿第 i-1行和第i行来举例，首先我肯定是刷刷刷地用第 i-1 行的数据把一维数组给填满了（这里我保留了对关键计算线索的高亮）：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123932.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>接下来我尝试用第 i 行的数据更新它。当数据更新走到 dp[i][v] 这里的时候，dp[i-1][v] 和 dp[i-1][v-w[i]] 都是存在的状态（分别对应一维数组中现在的 dp[v]和dp[v-w[i]]的值，完全可以满足我们的计算需要：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123944.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>当我们计算出 dp[i][v] 的值以后，dp[i-1][v-w[i]]可能还会在以后的计算中用到，但dp[i-1][v]其实已经完全用不到了（这个点对初学的同学来说可能会有点绕，不要慌，你品，你细品。注意这里dp[i][v]已经求解出来了，对于 i 这个索引来说只需要求解 dp[i][v-1]到dp[i][w[i]]之间的值，仔细想想，求解这些值是不是完全用不到dp[i-1][v]？）。</p>\\n</blockquote>\\n<ul>\\n<li>此时我们刚好用 dp[i][v] 去更新了 dp[v] 的值，把用不到的数据给及时地替换掉了，岂不美滋滋？</li>\\n<li>基于上面的分析，我们可以写出背包问题的完整求解代码：</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// 入参是物品的个数和背包的容量上限，以及物品的重量和价值数组\\nfunction knapsack(n, c, w, value) {\\n    // dp是动态规划的状态保存数组\\n    const dp = (new Array(c+1)).fill(0)  \\n    // res 用来记录所有组合方案中的最大值\\n    let res = -Infinity\\n    for(let i=1;i&lt;=n;i++) {\\n        for(let v=c;v&gt;=w[i];v--) {\\n            // 写出状态转移方程\\n            dp[v] = Math.max(dp[v], dp[v-w[i]] + value[i])\\n            // 即时更新最大值\\n            if(dp[v] &gt; res) {\\n                res = dp[v]\\n            }\\n        }\\n    }\\n    return res\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>评审页面里有人宣称“把所有物品的放与不放都枚举出来最稳”，当物品数继续增长时，你会怎样指出方案缺陷？</strong></p>\\n<p>每件物品都有放与不放两种选择，完整枚举会形成 <code>2^n</code> 种组合，物品数增长后成本呈指数级上升。更合适的做法是用 <code>f(i,c)</code> 表示前 <code>i</code> 件物品在容量 <code>c</code> 下的最大价值；若状态定义与容量约束不一致，转移仍会失真。</p>\\n</li>\\n<li>\\n<p><strong>购物组合页只展示最大价值，容量上限较大且浏览器内存受限，你会保留二维 <code>dp[i][v]</code>，还是改成一维滚动数组？</strong></p>\\n<p>只需要最大价值时可改用一维 <code>dp[v]</code>，因为计算第 <code>i</code> 件物品只依赖上一行的 <code>dp[i-1][v]</code> 与 <code>dp[i-1][v-w[i]]</code>。容量必须倒序遍历以维持上一行语义；若页面还要回溯具体选择，单数组会丢失路径信息。</p>\\n</li>\\n<li>\\n<p><strong>业务把“容量不超过 <code>c</code>”改成“总体积必须恰好为 <code>c</code>”，但代码仍把整个 <code>dp</code> 初始化为 <code>0</code>；会产生什么误判？</strong></p>\\n<p>全量初始化为 <code>0</code> 会把尚不可达的容量伪装成合法状态，转移后可能返回一个并未恰好装满的组合。应区分可达与不可达状态，只让容量 <code>0</code> 作为初始可达点；具体哨兵值还要避免与有效价值冲突，并处理无解结果。</p>\\n</li>\\n<li>\\n<p><strong>线上结果比预期大，代码是 <code>dp[v] = Math.max(dp[v], dp[v-w[i]] + value[i])</code>，单件物品也能贡献多次；你如何定位？</strong></p>\\n<p>先核对内层是否从小容量向大容量遍历，这会让 <code>dp[v-w[i]]</code> 读到同一轮刚更新的值，从而重复使用第 <code>i</code> 件物品。改为从容量上限倒序至 <code>w[i]</code>，并用单物品、多倍容量用例验证；若仍异常，再检查重量和值的索引是否错位。</p>\\n</li>\\n<li>\\n<p><strong>候选人主张二维表更易解释，工程负责人坚持一维数组更省空间；在面试现场你如何裁决这组选型冲突？</strong></p>\\n<p>两种写法使用同一状态转移，二维表直接保留各物品阶段，适合讲清依赖或保留更多中间信息；一维滚动数组只保留当前必要状态，空间更紧凑。若只求最大值可优先一维，若需要调试转移或恢复选择，二维表通常更直观，但占用更多空间。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"最长上升子序列模型\\"><a class=\\"header-anchor\\" href=\\"#最长上升子序列模型\\">#</a> 最长上升子序列模型</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>dp[i]</code> 表示“必须以 <code>nums[i]</code> 结尾”的 LIS 长度，初值为 1。</li>\\n<li>枚举 <code>j &lt; i</code>，若 <code>nums[j] &lt; nums[i]</code>，用 <code>dp[j]+1</code> 更新。</li>\\n<li>答案是 <code>max(dp)</code>，不是必然 <code>dp[n-1]</code>；空数组返回 0。</li>\\n<li>基础 DP 为 <code>O(n²)</code>；维护最小结尾数组可优化到 <code>O(n log n)</code>。</li>\\n</ul>\\n<p><strong>这个模型用 <code>dp[i]</code> 表示必须以 <code>nums[i]</code> 结尾的最长严格上升子序列长度。</strong> 每个位置初始为 <code>1</code>，枚举所有 <code>j &lt; i</code>，只要 <code>nums[j] &lt; nums[i]</code>，就用 <code>dp[j] + 1</code> 更新当前状态。最终答案要取 <code>max(dp)</code>，因为最长序列不一定结束在数组末尾，空数组则返回 <code>0</code>。基础实现是 <code>O(n²)</code>，维护最小结尾数组可以优化到 <code>O(n log n)</code>。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“为了让转移可定义，我把状态限定为以 i 结尾。所有更小的前驱都能接到 i 后面，最后再对所有结尾取最大值。”</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在序列 <code>[3,1,2]</code> 上把 <code>dp[i]</code> 定义成“前 <code>i</code> 个元素的最长长度”，却直接写 <code>dp[i]=dp[i-1]+1</code>；反例暴露了什么？</strong></p>\\n<p>前缀最优值没有说明最优子序列以哪个元素结尾，因此无法判断当前元素能否接到它后面。应把状态限定为以索引 <code>i</code> 的元素结尾，再枚举更小的前驱；最终答案还要对所有结尾取最大值，而不能只读取最后一项。</p>\\n</li>\\n<li>\\n<p><strong>趋势分析页只需返回一个长度，输入是一段普通规模的数值序列；你会怎样实现可解释的基础版本？</strong></p>\\n<p>为每个位置初始化 <code>dp[i]=1</code>，表示该元素单独构成子序列，再检查所有 <code>j&lt;i</code> 且 <code>nums[j]&lt;nums[i]</code> 的位置并更新 <code>dp[i]</code>。遍历时维护全局最大值；这种写法逻辑直观，但双层遍历的成本会随序列长度明显增长。</p>\\n</li>\\n<li>\\n<p><strong>需求从“严格上升”改成“允许相等的非递减”，代码仍用 <code>nums[j] &lt; nums[i]</code>；包含大量重复值的页面结果会怎样？</strong></p>\\n<p>重复值仍无法相互连接，因此页面返回的长度会小于非递减规则下的正确结果。基础动态规划应把前驱条件改为 <code>nums[j] &lt;= nums[i]</code>，同时同步测试与文案；若只改比较符而遗漏规则说明，后续维护者可能再次按严格上升修回。</p>\\n</li>\\n<li>\\n<p><strong>线上数组 <code>[10,9,2,5,3,7,101,18]</code> 返回 <code>4</code>，但另一个实现直接返回 <code>dp[len-1]</code> 后在部分输入上变小；你会查什么？</strong></p>\\n<p>应检查实现是否把最后一个元素误当成最长上升子序列的必然结尾，实际上最长结果可能结束在任意索引。正确做法是计算所有以 <code>i</code> 结尾的状态并维护最大值；修复后还要用末尾骤降的序列回归，避免偶然由最后一项得到正确答案。</p>\\n</li>\\n</ul>\\n</section>\\n<blockquote>\\n<p>该模型对口的其实是动态规划中一类非常经典的问题——“序列型动态规划”。在形态各异的序列型动态规划问题中，“最长上升子序列”问题可以说是相当热门的，其解法也是比较具有代表性的。接下来我们就以这个问题为抓手，提取其对应的的解题模型。</p>\\n</blockquote>\\n<blockquote>\\n<p>题目描述：给定一个无序的整数数组，找到其中最长上升子序列的长度。</p>\\n</blockquote>\\n<p>示例: 输入: [10,9,2,5,3,7,101,18]</p>\\n<p>输出: 4\\n解释: 最长的上升子序列是 [2,3,7,101]，它的长度是 4。</p>\\n<p>说明:</p>\\n<ul>\\n<li>可能会有多种最长上升子序列的组合，你只需要输出对应的长度即可。 你算法的时间复杂度应该为 O(n^2) 。</li>\\n<li>进阶: 你能将算法的时间复杂度降低到 O(n log n) 吗?</li>\\n</ul>\\n<h3 id=\\"思路分析-2\\"><a class=\\"header-anchor\\" href=\\"#思路分析-2\\">#</a> 思路分析</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>dp[i]</code> 表示以 <code>nums[i]</code> 结尾的最长严格上升子序列长度，答案是所有 <code>dp[i]</code> 的最大值</li>\\n<li>计算 <code>dp[i]</code> 时，所有更早位置的最优结尾状态已经确定，可从满足 <code>nums[j] &lt; nums[i]</code> 的位置转移</li>\\n<li>每个元素单独就构成长度 1；“严格上升”使用 <code>&lt;</code>，非递减版本才使用 <code>&lt;=</code></li>\\n</ul>\\n<p><strong>关键是把 <code>dp[i]</code> 定义成以 <code>nums[i]</code> 结尾的最长严格上升子序列长度。</strong> 这样遍历到位置 <code>i</code> 时，只需回看前面的元素：如果 <code>nums[j] &lt; nums[i]</code>，当前元素就能接在对应子序列后面。每个位置至少能单独组成长度为 <code>1</code> 的子序列，所以状态统一初始化为 <code>1</code>。遍历完成后取所有 <code>dp[i]</code> 的最大值；如果输入为空，直接返回 <code>0</code>。</p>\\n</section>\\n<blockquote>\\n<p>在做一道题之前，首先要知道题目到底在说啥，这里可能会对初学者构成理解障碍的关键字有两个：“子序列”和“上升”。\\n啥是“子序列”？它指的是在原有序列的基础上，删除0个或者多个数，其他数的顺序保持不变得到的结果。拿示例的这个序列来说：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[10,9,2,5,3,7,101,18]    \\n</code></pre>\\n<blockquote>\\n<p>随便拿掉0个数或多个数：比如说我去掉第一个数字10，去掉最后一个数18，但是不打乱这个序列的顺序，那么我得到的新的序列就是该序列的一个子序列：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[9,2,5,3,7,101]    \\n</code></pre>\\n<blockquote>\\n<p>啥是“上升”？这个就比较好理解了，它指的是排在后面的元素总是要大于排在前面的元素。</p>\\n</blockquote>\\n<p>在上一节，我们曾经学习过一种“通用解题思路”。通用解题思路的核心，是利用递归思想，以“倒推”为抓手，快速地明确状态与状态间的关系。当我们面对一道题目，无从下手的时候，这个思维工具往往能够帮到我们很大的忙。</p>\\n<p>然而，事事无绝对，学习动态规划并不是掌握一个套路就可以高枕无忧的“一锤子买卖”。对于最长上升子序列问题来说，通用解题思路所描述的这种“自顶向下”的思维方式，并不好使。</p>\\n<blockquote>\\n<p>事实上，做这道题，大家要把握住一个关键的特征，那就是“序列”。</p>\\n</blockquote>\\n<p>这道题比较直白，直接把“序列”二字写在了题干里。后面我们会做到一些更有趣（同时也更拧巴）的题目，这些题目不会直接跟你说它是序列类型题目，但是通过对题干进行分析，你会发现它实际上仍然会涉及到对一个序列的遍历，并且序列中的每一个元素都能够对应到一个有利于求解出最终结果的状态值，这类题目也符合“序列”的特征。</p>\\n<p>对于序列类题目，我们并没有一套固定的解题模型可以直接套，一般来说只能见招拆招，针对不同的题型分支套不同的解法（所以确实需要大家有一定程度的题量上的积累）。解法虽有不同，背后的思想却是一而贯之的，那就是关注到序列中元素的索引，尝试寻找不同索引对应的元素之间的关系、并以索引为线索去构造一维或二维的状态数组。</p>\\n<p>拿“最长上升子序列”这个问题分支来说，这里我们关注到的就是“以序列中第 i 个元素为结尾的前i个元素的状态”。\\n我们用f(i)来表示前i个元素中最长上升子序列的长度。若想基于 f(i) 求解出 f(i+1)，我们需要关注到的是第 i+1 个元素和前 i 个元素范围内的最长上升子序列的关系，它们之间的关系有两种可能：</p>\\n<ul>\\n<li>若第i+1个元素比前i个元素中某一个元素要大，此时我们就可以在这个元素所在的上升子序列的末尾追加第i+1个元素（延长原有的子序列），得到一个新的上升子序列。</li>\\n<li>若第i+1个元素并不比前i个元素中所涵盖的最长上升子序列中的某一个元素大，则维持原状，子序列不延长。\\n这个过程形容起来可能比较抽象，下面我们用一个示例来理解它。</li>\\n</ul>\\n<blockquote>\\n<p>拿我们题目示例中的数组[10,9,2,5,3,7,101,18] 来举例。在算法的初始态，我们还没有进行任何的遍历和计算，此时对于每一个索引位来说，它都只与一个长度为1的子序列有关——那就是只有它自己存在的子序列。因此每一个索引位对应的状态初始值都是1：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123955.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>同时对于索引位为0的元素来说，由于以它为结尾的子序列有且仅有 [10] 这一个，因此它的状态值时一开始就明确的，那就是1：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>f(0) = 1\\n</code></pre>\\n<blockquote>\\n<p>下面基于 f(0) 对 f(1) 求解，比较两个索引位上元素的大小：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905124008.png\\" alt=\\"\\"></p>\\n<p>发现9比10小，没办法延长原有的子序列，因此啥也不干。继续往下遍历，遇到了2，发现2比前两个数都小，仍然没法延长任何一个子序列，继续啥也不干。</p>\\n<p>再往下遍历，遇到了5，对比5和前面三个元素，发现它比2大，可以延长2所在的那个最长上升子序列，延长后，以 5 为结尾的最长上升子序列的长度就得到了更新：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905124017.png\\" alt=\\"\\"></p>\\n<p>重复上面这个“遍历新元素+回头看”的逻辑，直到整个数组被完全遍历，我们就能拿到以每一个索引位元素为结尾的最长上升子序列的长度值。从这些长度值中筛选出最大值，我们也就得到了问题的解。</p>\\n<blockquote>\\n<p>我们基于这个思路进行编码：</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {number[]} nums\\n * @return {number}\\n */\\n// 入参是一个数字序列\\nconst lengthOfLIS = function(nums) {\\n  // 缓存序列的长度\\n  const len = nums.length  \\n  // 处理边界条件\\n  if(!len) {\\n      return 0\\n  }\\n  // 初始化数组里面每一个索引位的状态值\\n  const dp = (new Array(len)).fill(1)\\n  // 初始化最大上升子序列的长度为1\\n  let maxLen = 1 \\n  // 从第2个元素开始，遍历整个数组\\n  for(let i=1;i&lt;len;i++) {\\n      // 每遍历一个新元素，都要“回头看”，看看能不能延长原有的上升子序列\\n      for(let j=0;j&lt;i;j++) {  \\n          // 若遇到了一个比当前元素小的值，则意味着遇到了一个可以延长的上升子序列，故更新当前元素索引位对应的状态\\n          if(nums[j]&lt;nums[i]) {\\n              dp[i] = Math.max(dp[i], dp[j] + 1)  \\n          }\\n      }\\n      // 及时更新上升子序列长度的最大值\\n      if(dp[i] &gt; maxLen) {\\n          maxLen = dp[i]\\n      }\\n  }\\n  // 遍历完毕，最后到手的就是最大上升子序列的长度\\n  return maxLen\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>页面示例 <code>[10,9,2,5,3,7,101,18]</code> 中，开发把“子序列”当成连续片段处理；为什么即使代码找到 <code>[2,5]</code>，结果仍可能偏小？</strong></p>\\n<p>子序列允许删除若干元素但必须保持原顺序，并不要求被选元素在原数组中连续。只扫描连续上升片段会错过跨越 <code>5</code>、选择 <code>3</code> 后再连接 <code>7</code> 等组合；应按索引枚举合法前驱，而不是遇到下降就把状态整体清零。</p>\\n</li>\\n<li>\\n<p><strong>面试编码时要求写一个便于讲清正确性的版本，你会如何定义 <code>dp[i]</code> 并组织两层循环？</strong></p>\\n<p>令 <code>dp[i]</code> 表示以 <code>nums[i]</code> 结尾的最长严格上升子序列长度，初值统一为 <code>1</code>。外层按 <code>i</code> 向后遍历，内层查看所有 <code>j&lt;i</code>，满足 <code>nums[j]&lt;nums[i]</code> 时用 <code>dp[j]+1</code> 更新；最后取所有状态最大值。</p>\\n</li>\\n<li>\\n<p><strong>数据规则改为允许相等元素相接，输入 <code>[2,2,2]</code> 仍返回 <code>1</code>；你会改动哪里并警惕什么语义风险？</strong></p>\\n<p>当前判断使用严格条件 <code>nums[j] &lt; nums[i]</code>，所以相等元素不会产生转移；非递减规则下应改为 <code>nums[j] &lt;= nums[i]</code>。比较符改变了题目语义，接口名称、测试断言和产品文案必须同步，否则调用方仍可能把结果理解为严格上升长度。</p>\\n</li>\\n<li>\\n<p><strong>线上空数组返回 <code>1</code>，而单元素数组也返回 <code>1</code>；维护者只想统一把 <code>maxLen</code> 初始化成 <code>0</code>，你认可吗？</strong></p>\\n<p>仅修改 <code>maxLen</code> 不足以覆盖全部流程，应先对 <code>nums.length===0</code> 明确返回 <code>0</code>，再让非空序列的每个 <code>dp[i]</code> 初始化为 <code>1</code>。这样单元素自然返回 <code>1</code>；若缺少空输入分支，后续访问状态或依赖默认最大值的代码仍可能产生错误。</p>\\n</li>\\n<li>\\n<p><strong>性能评审中，一方要求保留双层 <code>dp</code> 便于排查，另一方要求立即换更复杂的优化算法；你会依据什么取舍？</strong></p>\\n<p>当前实现逐个位置回看所有更早索引，状态含义直接，适合验证严格比较、初始化和全局最大值等正确性。是否换算法应由实际输入规模与性能证据决定；来源只给出了该动态规划实现，缺乏约束时不能声称复杂方案必然更优，还会增加调试成本。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"问题复盘\\"><a class=\\"header-anchor\\" href=\\"#问题复盘\\">#</a> 问题复盘</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>dp[i]</code> 表示以 <code>nums[i]</code> 结尾的最长严格上升子序列长度，答案是所有 <code>dp[i]</code> 的最大值</li>\\n<li>计算 <code>dp[i]</code> 时，所有更早位置的最优结尾状态已经确定，可从满足 <code>nums[j] &lt; nums[i]</code> 的位置转移</li>\\n<li>每个元素单独就构成长度 1；“严格上升”使用 <code>&lt;</code>，非递减版本才使用 <code>&lt;=</code></li>\\n</ul>\\n<p><strong>最长上升子序列更适合按索引建立状态，没必要枚举每个元素取或不取的所有组合。</strong> 枚举法确实可以写成递归并配合记忆化搜索，但这类序列问题已经有更直接的模型：记录每个位置结尾时的最优长度，再从前面的可接元素转移。本质上，递归只是寻找状态关系的一种手段，动态规划并不等于记忆化搜索的迭代版本。遇到新问题时，仍要关注重叠子问题和最优子结构，而不是强行套递归。</p>\\n</section>\\n<blockquote>\\n<p>对于这道题，其实也可以用最原始的办法来枚举每种情况：对于每个元素，考虑“取”和“不取”两种选择，得到对应的序列，进而判断这个序列是否是一个上升序列。若得到的是上升序列，则计算并更新最大长度；若不是，则啥也不干。\\n基于这个枚举的思路，我们不难想出递归的解法，然后再结合记忆化搜索，这道题似乎也能用“自顶向下”的求解思路做出来。\\n这里不推荐大家延续“自顶向下”的思维方式，原因有两个：</p>\\n</blockquote>\\n<p>这类题目有稳定的解题模型，可以帮助我们更好更快地解决问题，大可不必舍近求远</p>\\n<p>帮助大家完成从递归思想向动态规划思想的过渡，避免思维定式。</p>\\n<blockquote>\\n<p>在初学动态规划时，为了避免架空式地理解一个全新思想所带来的不适，我们需要借助已经学过的递归思想作为“垫脚石”来辅助我们的理解，从而跨过最难的第一道门。在基于递归思想的解题模板的帮助下，相信大家都对“倒推”法明确状态转移关系这个套路有了深刻的理解，同时也难免会有这样的困惑——难道动态规划就是记忆化搜索的迭代实现吗？这跟脱裤子放屁有什么区别？</p>\\n</blockquote>\\n<blockquote>\\n<p>如果你曾经有过这样的困惑，相信本节的学习已经为你解开心结——动态规划并不是任何一种其他算法思想的“替代品”，它有着自己独特的思路和作用。我们之所以在前面的几道题中会用到递归，是因为递归能够帮助我们又好又快地定位状态转移关系——递归是手段，而不是目的。在最大上升子序列这个问题中，我们可以看出，状态转移关系虽然总是涉及到对前后两个状态的分析，却并不总是依赖递归。</p>\\n</blockquote>\\n<p>在后续的“大厂真题训练”环节中，大家会见识到更多千姿百态的动态规划题目。其中有一部分，可以完全借助我们这两节所讨论过的经验来解决；还有一部分，需要你“见招拆招”。不过不要怕，只要你把握住了“重叠子问题”和“最优子结构”两个关键特征，把握住了动态规划的核心解题逻辑，那么再新的题目也只不过是对你已经掌握的动态规划之“道”的验证，是对解题之“术”的拓展。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在白板上把最长上升子序列写成“每个元素取或不取”的递归，还说加缓存后就是动态规划，你会怎样指出这句话不够准确？</strong></p>\\n<p>加缓存的递归属于自顶向下的记忆化搜索，能够消除大量重复计算，但不能据此断言动态规划只是它的迭代改写。递归在这里是定位状态转移关系的手段；当状态依赖顺序清晰时，也可以直接自底向上推导，代价是需要先把状态含义和遍历方向说明白。</p>\\n</li>\\n<li>\\n<p><strong>评审一段最长上升子序列代码时，页面只展示了两层循环和 <code>dp</code> 数组，没有递归推导，你会要求开发者补充哪项工程说明？</strong></p>\\n<p>需要补充 <code>dp[i]</code> 的精确定义、合法转移条件以及最终答案如何汇总，例如它是否表示“以第 <code>i</code> 个元素结尾”的最优长度。随后说明为何只读取更早的状态，以及严格上升应使用 <code>nums[j] &lt; nums[i]</code>；否则代码即使通过样例，也难以验证正确性。</p>\\n</li>\\n<li>\\n<p><strong>产品把最长“严格上升”子序列改成最长“非递减”子序列，现有 <code>dp</code> 转移只改初始化值能交付吗？</strong></p>\\n<p>不能只改初始化值，决定序列合法性的比较条件也必须由 <code>&lt;</code> 调整为 <code>&lt;=</code>，重复元素才允许接入已有序列。状态定义和自底向上的求解框架仍可保留，但要用全重复数组等用例重新验证；若遗漏比较语义，结果会系统性低估。</p>\\n</li>\\n<li>\\n<p><strong>线上判题只有包含大量重复值的用例失败，而单调递增、单调递减和单元素输入都通过，你会先查最长上升子序列实现的哪一处？</strong></p>\\n<p>先检查转移条件是否把严格上升误写成了 <code>&lt;=</code>，以及每个 <code>dp[i]</code> 是否以长度 <code>1</code> 初始化。再确认最终结果取所有结尾状态的最大值，而不是直接返回最后一项；前两类错误会在重复值上暴露，后一类则可能被有序样例掩盖。</p>\\n</li>\\n<li>\\n<p><strong>团队争论最长上升子序列应统一采用记忆化递归还是自底向上 <code>dp</code>，代码规范该强制其中一种吗？</strong></p>\\n<p>不宜仅凭名称强制统一，两者都能利用重叠子问题和最优子结构，关键是状态表达与依赖关系是否清晰。此类题已有稳定的自底向上模型，迭代写法通常更直接；记忆化搜索可用于辅助推导，但会保留递归调用及缓存管理的代价。</p>\\n</li>\\n<li>\\n<p><strong>候选人做完最长上升子序列后，把所有能写递归的题都归为动态规划，你会用什么判据继续追问？</strong></p>\\n<p>应要求他确认是否同时存在重叠子问题和最优子结构，而不是看到递归树就直接套用动态规划。递归只描述求解过程，子问题若彼此独立更接近分治；若最优解无法由相容的子问题最优解推出，缓存结果也不能建立正确转移。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);