(window.webpackJsonp=window.webpackJsonp||[]).push([[58],{1164:function(n){n.exports=JSON.parse('{"html":"<p><img src=\\"https://s.poetries.top/images/20210330120838.png\\" alt=\\"\\"></p>\\n<h2 id=\\"冒泡排序\\"><a class=\\"header-anchor\\" href=\\"#冒泡排序\\">#</a> 冒泡排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>思路：相邻比较、逆序交换，每轮把最大值&quot;冒&quot;到末尾，<code>n-1</code> 轮后有序</li>\\n<li>复杂度：平均/最坏 <code>O(n²)</code>，<strong>加了提前退出后最好情况 <code>O(n)</code></strong>；空间 <code>O(1)</code></li>\\n<li><strong>稳定</strong>——但前提是相等时不交换（比较用 <code>&gt;</code> 而不是 <code>&gt;=</code>），写错就变成不稳定</li>\\n<li>两个必加优化：<code>swapped</code> 标志位提前退出、内层循环边界减去已排好的 <code>i</code></li>\\n<li>面试价值不在实用性，而在于它是理解&quot;稳定性&quot;和&quot;最好/最坏差异&quot;最简单的载体</li>\\n</ul>\\n<p><strong>冒泡排序就是反复比较相邻元素并交换逆序项，每轮把未排序区间的最大值推到末尾。</strong> 它平均和最坏是 <code>O(n²)</code>，空间是 <code>O(1)</code>；加上 <code>swapped</code> 标记后，已有序数组可以提前结束，最好达到 <code>O(n)</code>。比较条件必须用 <code>&gt;</code> 而不是 <code>&gt;=</code>，否则相等元素也会交换，稳定性就被破坏了。工程中很少手写它，但它很适合说明提前退出优化以及稳定排序的边界。</p>\\n</section>\\n<p><strong>思路</strong>：相邻两个元素比较，逆序就交换。每一轮把当前未排序区间里最大的元素&quot;冒&quot;到末尾，n-1 轮后有序。</p>\\n<table>\\n<thead>\\n<tr>\\n<th>指标</th>\\n<th>值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>时间复杂度</td>\\n<td>平均/最坏 <code>O(n²)</code>，最好 <code>O(n)</code>（已有序且做了提前退出优化）</td>\\n</tr>\\n<tr>\\n<td>空间复杂度</td>\\n<td><code>O(1)</code>（原地）</td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td><strong>稳定</strong>（相等时不交换）</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>必须加的两个优化：</strong></p>\\n<pre class=\\"language-js\\"><code>function bubbleSort(arr) {\\n  for (let i = 0; i &lt; arr.length - 1; i++) {\\n    let swapped = false                       // ① 提前退出\\n    for (let j = 0; j &lt; arr.length - 1 - i; j++) {\\n      if (arr[j] &gt; arr[j + 1]) {              // ② 用 &gt; 而不是 &gt;=，保证稳定\\n        ;[arr[j], arr[j + 1]] = [arr[j + 1], arr[j]]\\n        swapped = true\\n      }\\n    }\\n    if (!swapped) break                       // 这一轮没换过 → 已经有序\\n  }\\n  return arr\\n}\\n</code></pre>\\n<ul>\\n<li><strong>提前退出</strong>：没有它，已排好序的数组仍然要跑满 <code>O(n²)</code>。加上之后最好情况降到 <code>O(n)</code>，这是面试官常追问的点。</li>\\n<li><strong>比较用 <code>&gt;</code> 不是 <code>&gt;=</code></strong>：相等时不交换才能保持稳定性 —— 写成 <code>&gt;=</code> 就把稳定排序变成了不稳定的，这是个很隐蔽的错误。</li>\\n</ul>\\n<p><strong>为什么面试还问它？</strong> 不是因为它实用（工程上没人手写冒泡），而是因为它是理解<strong>稳定性</strong>和<strong>最好/最坏情况差异</strong>最简单的载体。能主动说出&quot;稳定性取决于相等时换不换&quot;和&quot;提前退出让最好情况变 <code>O(n)</code>&quot;，比默写代码更有价值。</p>\\n<p>下面是具体实现：</p>\\n<blockquote>\\n<p>通过相邻元素比较和交换，使得每一趟循环都能找到未排序的子数组。</p>\\n</blockquote>\\n<h3 id=\\"实现\\"><a class=\\"header-anchor\\" href=\\"#实现\\">#</a> 实现</h3>\\n<pre class=\\"language-js\\"><code>function bubbleSort(list) {\\n  var n = list.length\\n  if(!n) return []\\n  \\n  for(var i = 0; i &lt; n; i++) {\\n    for(var j = 0; j &lt; n - i - 1; j++) {\\n      if(list[j] &gt; list[j + 1]) {\\n        var temp = list[j + 1]\\n        list[j + 1] = list[j]\\n        list[j] = temp\\n      }\\n    }\\n  }\\n  return list\\n}\\n</code></pre>\\n<h3 id=\\"优化\\"><a class=\\"header-anchor\\" href=\\"#优化\\">#</a> 优化</h3>\\n<h4 id=\\"单向冒泡\\"><a class=\\"header-anchor\\" href=\\"#单向冒泡\\">#</a> 单向冒泡</h4>\\n<blockquote>\\n<p>标记在一轮比较汇总中，如果没有需要交换的数据，说明数组已经是有序的，可以减少排序循环的次数。</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function bubbleSort(list) {\\n  var n = list.length\\n  if(!n) return []\\n  \\n  for(var i = 0; i &lt; n; i++) {\\n    let mark = true // 如果在一轮比较中没有出现需要交换的数据，说明数组已经是有序的\\n    for(let j = 0; j &lt; n - i - 1; j++) {\\n      if(list[j] &gt; list[j + 1]) {\\n        var temp = list[j + 1]\\n        list[j + 1] = list[j]\\n        list[j] = temp\\n        mark = false\\n      }\\n    }\\n    if(mark) break \\n  }\\n  \\n  return list\\n}\\n</code></pre>\\n<h4 id=\\"双向冒泡\\"><a class=\\"header-anchor\\" href=\\"#双向冒泡\\">#</a> 双向冒泡</h4>\\n<blockquote>\\n<p>普通冒泡排序一轮只找到最大值或最小值其中之一，双向冒泡则多一轮筛选，既可以找到最大值，也可以找到最小值。</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function bubbleSort(list) {\\n  var low = 0\\n  var high = list.length - 1\\n  while(low &lt; high) {\\n    var mark = true\\n    // 找到最大值放在右边\\n    for(var i = low; i &lt; high; i++) {\\n      if(list[i] &gt; list[i + 1]) {\\n        var temp = list[i + 1]\\n        list[i + 1] = list[i]\\n        list[i] = temp\\n        mark = false\\n      }\\n    }\\n    high--\\n    // 找到最小值放在左边\\n    for(var j = high; j &gt; low; j--) {\\n      if(list[j] &lt; list[j - 1]) {\\n        var temp = list[j - 1]\\n        list[j - 1] = list[j]\\n        list[j] = temp\\n        mark = false\\n      }\\n    }\\n    low++\\n    if(mark) break\\n  }\\n  return list\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>订单列表包含两个比较值相同但创建顺序不同的对象，双向冒泡的两个判断都从严格比较改成 <code>&gt;=</code> 或 <code>&lt;=</code>，页面顺序可能发生什么变化？</strong></p>\\n<p>相等元素也会被交换，原有相对顺序可能被打乱，排序因此失去稳定性。代码应分别保留 <code>list[i] &gt; list[i + 1]</code> 与 <code>list[j] &lt; list[j - 1]</code>；若业务依赖同值记录的既有顺序，非严格比较会制造难以察觉的抖动。</p>\\n</li>\\n<li>\\n<p><strong>一个几乎有序的几十项菜单每次拖拽后都重新排序，候选人删除了 <code>mark</code> 提前退出逻辑；结果仍正确，但你如何评价这次改动？</strong></p>\\n<p>结果仍能排好，但失去了在某一轮完全没有交换时提前结束的能力，对近乎有序的数据会继续做无效扫描。<code>mark</code> 必须在每轮开始设为 <code>true</code>，任一方向发生交换就置为 <code>false</code>；它只改善特定输入，最坏情况仍是二次复杂度。</p>\\n</li>\\n<li>\\n<p><strong>双向冒泡的正向扫描结束后，代码先执行 <code>high--</code> 再反向扫描；评审要求把边界更新挪到整轮最后，你会接受吗？</strong></p>\\n<p>不能直接挪动，因为正向扫描已经把当前最大值固定到旧的 <code>high</code>，反向阶段无需再次比较该位置，先收缩边界正是为了跳过它。若改动边界时没有同步检查循环起止条件，可能只是增加比较，也可能造成漏扫或越界。</p>\\n</li>\\n<li>\\n<p><strong>线上发现长度为 <code>2</code> 的倒序数组偶尔没有排好，你看到反向循环被改成 <code>for (j = high; j &gt;= low; j--)</code>，会怎样定位？</strong></p>\\n<p>先用空数组、单元素、两元素正序和倒序用例逐步检查 <code>low</code>、<code>high</code> 与相邻下标。反向比较访问的是 <code>list[j - 1]</code>，因此条件必须保证 <code>j &gt; low</code>；写成 <code>j &gt;= low</code> 会在边界读取越界位置，并掩盖真正的循环不变量。</p>\\n</li>\\n<li>\\n<p><strong>前端要给十万条日志在主线程排序，有人主张用双向冒泡，因为一轮能同时找到最大值和最小值；你会如何取舍？</strong></p>\\n<p>不应因为双向扫描就选择它，大规模输入下仍需要大量比较与交换，无法改变二次复杂度的上界。应优先使用成熟排序实现，并评估是否应减少前端数据量或移出主线程；双向冒泡更适合作为算法与边界控制练习。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"选择排序\\"><a class=\\"header-anchor\\" href=\\"#选择排序\\">#</a> 选择排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>思路：每轮从未排序区间找最小值，与该区间第一个元素交换</li>\\n<li>时间复杂度<strong>恒定 <code>O(n²)</code></strong>——最好最坏都一样，没有提前退出的余地（这点不如冒泡）</li>\\n<li><strong>不稳定</strong>：因为交换是&quot;跨越式&quot;的，<code>[5a, 5b, 2]</code> 排完变成 <code>[2, 5b, 5a]</code>，两个 5 的顺序被颠倒</li>\\n<li>唯一优势是<strong>交换次数最多 <code>n-1</code> 次</strong>（冒泡最坏 <code>O(n²)</code> 次），元素很大或写入成本高时反而划算</li>\\n<li>和冒泡的核心差别：冒泡是相邻交换所以稳定，选择是跨越交换所以不稳定</li>\\n</ul>\\n<p><strong>选择排序每轮从未排序区间找出最小值，再把它与区间首元素交换。</strong> 无论数据是否已有序，它都要完成查找，所以最好、平均和最坏时间复杂度都是 <code>O(n²)</code>，空间复杂度为 <code>O(1)</code>。跨位置交换会破坏相等元素的先后顺序，例如 <code>[5a, 5b, 2]</code> 会变成 <code>[2, 5b, 5a]</code>，因此它不稳定。它的取舍是最多只交换 <code>n-1</code> 次，在元素很大或写入成本较高时，可能比交换频繁的冒泡排序更合适。</p>\\n</section>\\n<p><strong>思路</strong>：每一轮从未排序区间里找出最小的元素，和该区间的第一个元素交换。</p>\\n<table>\\n<thead>\\n<tr>\\n<th>指标</th>\\n<th>值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>时间复杂度</td>\\n<td><strong>恒定 <code>O(n²)</code></strong>（最好最坏都一样，没有提前退出的余地）</td>\\n</tr>\\n<tr>\\n<td>空间复杂度</td>\\n<td><code>O(1)</code></td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td><strong>不稳定</strong></td>\\n</tr>\\n<tr>\\n<td>交换次数</td>\\n<td><strong>最多 <code>n-1</code> 次</strong>（这是它唯一的优点）</td>\\n</tr>\\n</tbody>\\n</table>\\n<pre class=\\"language-js\\"><code>function selectionSort(arr) {\\n  for (let i = 0; i &lt; arr.length - 1; i++) {\\n    let min = i\\n    for (let j = i + 1; j &lt; arr.length; j++) {\\n      if (arr[j] &lt; arr[min]) min = j\\n    }\\n    if (min !== i) [arr[i], arr[min]] = [arr[min], arr[i]]\\n  }\\n  return arr\\n}\\n</code></pre>\\n<p><strong>为什么它不稳定？</strong> 因为交换是&quot;跨越式&quot;的。举个例子：</p>\\n<pre class=\\"language-text\\"><code>[5a, 5b, 2]  →  第一轮找到最小值 2，和第一个 5a 交换\\n[2, 5b, 5a]  →  两个 5 的相对顺序被颠倒了 ❌\\n</code></pre>\\n<p>这是选择排序区别于冒泡和插入的关键 —— 后两者都是相邻交换，天然稳定。</p>\\n<p><strong>它唯一的优势：交换次数少。</strong></p>\\n<p>冒泡最坏要交换 <code>O(n²)</code> 次，选择排序<strong>最多只交换 <code>n-1</code> 次</strong>。当&quot;交换&quot;这个操作的成本特别高时（比如元素是大对象、或者数据在慢速存储上），选择排序反而更划算。</p>\\n<p><strong>和冒泡的对比是高频追问：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th>冒泡</th>\\n<th>选择</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>最好情况</td>\\n<td><code>O(n)</code>（可提前退出）</td>\\n<td><code>O(n²)</code>（无法优化）</td>\\n</tr>\\n<tr>\\n<td>交换次数</td>\\n<td>最多 <code>O(n²)</code></td>\\n<td>最多 <code>n-1</code></td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td>稳定</td>\\n<td><strong>不稳定</strong></td>\\n</tr>\\n</tbody>\\n</table>\\n<p>下面是具体实现：</p>\\n<blockquote>\\n<p>依次找到剩余元素的最小值或者最大值，放置末尾或者开头。</p>\\n</blockquote>\\n<h3 id=\\"实现-2\\"><a class=\\"header-anchor\\" href=\\"#实现-2\\">#</a> 实现</h3>\\n<pre class=\\"language-js\\"><code>function selectSort(list) {\\n  var n = list.length\\n  var minIndex\\n  \\n  for(var i = 0; i &lt; n - 1; i++) {\\n    minIndex = i\\n    for(var j = i + 1; j &lt; n; j++) {\\n      if(list[j] &lt; list[minIndex]) {\\n        minIndex = j\\n      }\\n    }\\n    var temp = list[i]\\n\\t\\tlist[i] = list[minIndex]\\n    list[minIndex] = temp\\n  }\\n  return list\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>员工表按部门排序时要求同部门成员保持原始入职顺序，选择排序处理 <code>[5a, 5b, 2]</code> 后为什么可能破坏这个要求？</strong></p>\\n<p>首轮会把最小值 <code>2</code> 与 <code>5a</code> 直接交换，结果成为 <code>[2, 5b, 5a]</code>，两个相等的 <code>5</code> 相对次序被颠倒。标准选择排序的跨位置交换并不稳定，若页面依赖既有次序，应选择稳定方案或把次级字段纳入比较。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中有人把内层比较写成 <code>list[j] &lt;= list[minIndex]</code>，商品列表存在大量同价记录时会带来什么变化？</strong></p>\\n<p>相等时不断更新 <code>minIndex</code>，每轮会倾向选择最后出现的同值元素，再与当前位置交换，进一步破坏同价商品的相对顺序。改回严格小于能减少这种额外扰动，但标准选择排序的最终交换仍不能保证整体稳定。</p>\\n</li>\\n<li>\\n<p><strong>一个嵌入式页面更在意写入次数而不是比较次数，数组规模不大且元素交换会触发昂贵持久化；选择排序有什么可辩护的价值？</strong></p>\\n<p>选择排序每轮只在确定最小元素后执行一次交换，因此交换次数至多与数组长度同阶，适合交换成本明显高于比较成本的约束。它仍要完成二次量级的比较，数据规模扩大后不能仅凭写入少就忽略总耗时。</p>\\n</li>\\n<li>\\n<p><strong>线上监控发现已排好序的列表仍执行了多次无意义写入，代码每轮都交换 <code>list[i]</code> 与 <code>list[minIndex]</code>；你会怎样修正？</strong></p>\\n<p>当 <code>minIndex === i</code> 时无需交换，可增加条件跳过对同一位置的重复赋值，减少无效写入。该优化不会减少寻找最小值所需的比较，也不会把选择排序变成对有序输入自适应的算法。</p>\\n</li>\\n<li>\\n<p><strong>候选人为了让选择排序稳定，提出找到最小值后不要交换，而是把区间元素整体右移再插入；这个方案是否成立？</strong></p>\\n<p>方案可以保持被跨越元素的相对顺序：先保存最小值，再把 <code>[i, minIndex)</code> 整体右移，最后写入 <code>i</code>。代价是每轮可能产生线性移动，放弃标准选择排序交换次数少的优势，因此要根据稳定性与写入成本取舍。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"插入排序\\"><a class=\\"header-anchor\\" href=\\"#插入排序\\">#</a> 插入排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>思路：像理扑克牌 —— 取未排序区第一个元素，在已排序区从后往前找位置插入</li>\\n<li>复杂度：平均/最坏 <code>O(n²)</code>，<strong>最好 <code>O(n)</code></strong>（已有序时每个元素只比一次）；空间 <code>O(1)</code>；<strong>稳定</strong></li>\\n<li>内层用&quot;<strong>后移</strong>&quot;而不是&quot;交换&quot;，一次写操作 vs 三次 —— 常数比冒泡小一半</li>\\n<li>它是三个 <code>O(n²)</code> 算法里<strong>最实用</strong>的：近乎有序时极快、常数小、稳定原地在线</li>\\n<li><strong>生产代码真在用</strong>：<code>V8</code> 的 TimSort 和多数快排实现在子数组小于 10~16 时都切换成插入排序</li>\\n<li>希尔排序就是它的改进版 —— 先让数组大致有序，再用插入排序收尾</li>\\n</ul>\\n<p><strong>插入排序会把数组分成已排序区和未排序区，每次取一个新元素，从后往前找到位置并插入。</strong> 它平均和最坏是 <code>O(n²)</code>，但数据已有序时每个元素只需比较一次，最好能到 <code>O(n)</code>，同时保持 <code>O(1)</code> 空间和稳定性。实现时把较大元素依次后移，比连续交换写入更少，相等时不移动则能保留原顺序。它尤其适合小数组或近乎有序的数据，因此一些复合排序会在子数组较小时切换到插入排序。</p>\\n</section>\\n<p><strong>思路</strong>：把数组看成&quot;已排序区&quot;和&quot;未排序区&quot;，每次取未排序区的第一个元素，在已排序区里从后往前找到合适位置插入 —— 就像打牌时理手牌。</p>\\n<table>\\n<thead>\\n<tr>\\n<th>指标</th>\\n<th>值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>时间复杂度</td>\\n<td>平均/最坏 <code>O(n²)</code>，<strong>最好 <code>O(n)</code></strong>（已有序时每个元素只比较一次）</td>\\n</tr>\\n<tr>\\n<td>空间复杂度</td>\\n<td><code>O(1)</code></td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td><strong>稳定</strong></td>\\n</tr>\\n</tbody>\\n</table>\\n<pre class=\\"language-js\\"><code>function insertionSort(arr) {\\n  for (let i = 1; i &lt; arr.length; i++) {\\n    const cur = arr[i]\\n    let j = i - 1\\n    // 用 &gt; 而不是 &gt;=，相等时停下来插在后面 → 保证稳定\\n    while (j &gt;= 0 &amp;&amp; arr[j] &gt; cur) {\\n      arr[j + 1] = arr[j]     // 后移而不是交换，比冒泡省一半写操作\\n      j--\\n    }\\n    arr[j + 1] = cur\\n  }\\n  return arr\\n}\\n</code></pre>\\n<p><strong>它是三个 <code>O(n²)</code> 算法里最实用的一个</strong>，原因有三：</p>\\n<ol>\\n<li><strong>近乎有序的数据上极快</strong> —— 数据基本有序时接近 <code>O(n)</code>，而选择排序仍是 <code>O(n²)</code></li>\\n<li><strong>常数小</strong> —— 内层是&quot;后移&quot;不是&quot;交换&quot;，一次写操作 vs 三次</li>\\n<li><strong>稳定 + 原地 + 在线</strong>（可以边接收数据边排序）</li>\\n</ol>\\n<p><strong>所以它被真正用在了生产代码里</strong>：<code>V8</code> 的 <code>Array.prototype.sort</code>（TimSort）和大多数快排实现，在子数组长度小于某个阈值（通常 10~16）时都会切换成插入排序 —— 因为小数组上它的常数优势超过了 <code>O(n log n)</code> 算法的递归开销。</p>\\n<p><strong>希尔排序就是它的改进版</strong>：先让数组&quot;大致有序&quot;，再用插入排序收尾，正是利用了&quot;近乎有序时极快&quot;这个特性。</p>\\n<p>下面是具体实现：</p>\\n<blockquote>\\n<p>以第一个元素为有序数组，其后的元素通过在这个已有序的数组中找到合适的元素并插入。</p>\\n</blockquote>\\n<h3 id=\\"实现-3\\"><a class=\\"header-anchor\\" href=\\"#实现-3\\">#</a> 实现</h3>\\n<pre class=\\"language-js\\"><code>function insertSort(list) {\\n  var n = list.length\\n  var preIndex\\n  var current\\n  \\n  for(var i = 1; i &lt; n; i++) {\\n    preIndex = i - 1\\n    current = list[i]\\n    \\n    while(preIndex &gt;=0 &amp;&amp; list[preIndex] &gt; current) {\\n      list[preIndex + 1] = list[preIndex]\\n      preIndex--\\n    }\\n    list[preIndex + 1] = current\\n  }\\n  return list\\n}\\n</code></pre>\\n<h3 id=\\"优化-2\\"><a class=\\"header-anchor\\" href=\\"#优化-2\\">#</a> 优化</h3>\\n<h4 id=\\"拆半插入\\"><a class=\\"header-anchor\\" href=\\"#拆半插入\\">#</a> 拆半插入</h4>\\n<pre class=\\"language-js\\"><code>function insertSort(list) {\\n  var low\\n  var high\\n  var j\\n  var temp\\n  for (var i = 1; i &lt; list.length; i++) {\\n    if (list[i] &lt; list[i - 1]) {\\n      temp = list[i]\\n      low = 0\\n      high = i - 1\\n      while (low &lt;= high) {\\n        let mid = Math.floor((low + high) / 2)\\n        if (temp &gt; list[mid]) {\\n          low = mid + 1\\n        } else {\\n          high = mid - 1\\n        }\\n      }\\n      for (j = i; j &gt; low; --j) {\\n        list[j] = list[j - 1]\\n      }\\n      list[j] = temp\\n    }\\n  }\\n  return list\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>成绩列表中相同分数必须保持录入顺序，但代码在二分时遇到相等值就执行 <code>high = mid - 1</code>；最终插入位置会有什么风险？</strong></p>\\n<p>新元素会被定位到已有相等元素之前，后续整体右移会改变相等记录的原始顺序，因此这版拆半插入不稳定。若要保持稳定，应把新元素插到相等区间之后，并相应调整二分条件与边界。</p>\\n</li>\\n<li>\\n<p><strong>候选人声称拆半插入把插入排序从 <code>O(n²)</code> 降成了 <code>O(n log n)</code>，页面一次排序五万项数据；你会怎样纠正？</strong></p>\\n<p>二分只降低了寻找插入位置的比较次数，每轮找到位置后仍可能移动线性数量的元素，所以整体移动成本仍可能达到二次量级。五万项数据不能仅凭二分查找就认定可接受，还需选择更合适的排序方案并避免阻塞页面。</p>\\n</li>\\n<li>\\n<p><strong>数据已经基本有序，代码仍为每个元素执行完整二分查找；源码中的 <code>if (list[i] &lt; list[i - 1])</code> 为什么值得保留？</strong></p>\\n<p>该判断能直接跳过已经不小于前驱的元素，近乎有序时可避免二分定位和数组搬移。删除它通常不影响最终有序性，却丢失了插入排序对局部有序输入的适应性；若比较器复杂，无效比较的代价会更明显。</p>\\n</li>\\n<li>\\n<p><strong>线上只有长度为 <code>2</code> 的倒序数组出现元素重复，排查发现后移循环结束后写成了 <code>list[i] = temp</code>，而不是 <code>list[j] = temp</code>；错误在哪里？</strong></p>\\n<p>后移完成后真正空出的槽位是二分得到的 <code>low</code>，循环结束时 <code>j</code> 正好落在该位置，必须写入 <code>list[j]</code>。若仍写回原来的 <code>i</code>，会覆盖刚移动的元素，造成重复并丢失待插入值原本应占的位置。</p>\\n</li>\\n<li>\\n<p><strong>对象列表的比较器代价很高，但移动数组元素相对便宜，团队在普通插入与拆半插入之间选型；拆半版本的优势和边界是什么？</strong></p>\\n<p>拆半插入能减少在已排序区间内定位位置所需的比较次数，因此比较器昂贵时更有价值。它不会减少将中间元素整体后移的数量，且稳定性取决于相等值的落点规则；移动昂贵或数据很大时优势会被削弱。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"希尔排序\\"><a class=\\"header-anchor\\" href=\\"#希尔排序\\">#</a> 希尔排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>思路：插入排序的改进 —— 先按大间隔 gap 分组做插入排序，逐步缩小 gap，最后 gap=1 再完整插入一次</li>\\n<li>为什么快：插入排序只能一格格挪，希尔让元素<strong>大步跨越</strong>快速接近位置；gap 缩小时数组已近乎有序，而插入排序此时接近 <code>O(n)</code></li>\\n<li>复杂度<strong>取决于 gap 序列</strong>：朴素的除以 2 最坏仍是 <code>O(n²)</code>，Hibbard 约 <code>O(n^1.5)</code>，Sedgewick 约 <code>O(n^1.3)</code></li>\\n<li>至今<strong>没有人从理论上完全分析清最优 gap 序列</strong>——这在算法里很少见，可以主动提一句</li>\\n<li><strong>不稳定</strong>：相同元素可能被分到不同子序列，各自排序时相对顺序被打乱（分组破坏了插入排序原有的稳定性）</li>\\n<li>工程上已很少用 —— 大数据量有 <code>O(n log n)</code> 的算法，小数据量直接用插入排序</li>\\n</ul>\\n<p><strong>希尔排序本质上是带间隔的插入排序：先按较大的 <code>gap</code> 分组排序，再逐步缩小到 <code>1</code>。</strong> 大间隔让元素能跨越移动，等数组接近有序后，插入排序的效率也会接近 <code>O(n)</code>。它的时间复杂度取决于间隔序列，朴素序列最坏仍是 <code>O(n²)</code>，而且分组会打乱相同元素的顺序，所以不稳定。工程里大数据通常选 <code>O(n log n)</code> 算法，小数据直接用插入排序，希尔排序更多用于理解这种渐进优化思路。</p>\\n</section>\\n<p><strong>思路</strong>：插入排序的改进版。先按一个较大的间隔（gap）把数组分成若干子序列分别做插入排序，然后逐步缩小 gap，最后 gap = 1 时再做一次完整的插入排序。</p>\\n<p><strong>为什么这样更快？</strong></p>\\n<p>插入排序的弱点是<strong>元素只能一格一格地移动</strong> —— 如果最小的元素在最末尾，它要一路挪到最前面，代价是 <code>O(n)</code>。</p>\\n<p>希尔排序让元素在大 gap 时能<strong>跨越式地大步移动</strong>，快速接近最终位置；等到 gap 缩小时数组已经&quot;大致有序&quot;，而插入排序在近乎有序的数据上接近 <code>O(n)</code>——两个特性正好互补。</p>\\n<pre class=\\"language-js\\"><code>function shellSort(arr) {\\n  const n = arr.length\\n  for (let gap = Math.floor(n / 2); gap &gt; 0; gap = Math.floor(gap / 2)) {\\n    // 对每个间隔为 gap 的子序列做插入排序\\n    for (let i = gap; i &lt; n; i++) {\\n      const cur = arr[i]\\n      let j = i - gap\\n      while (j &gt;= 0 &amp;&amp; arr[j] &gt; cur) {\\n        arr[j + gap] = arr[j]\\n        j -= gap\\n      }\\n      arr[j + gap] = cur\\n    }\\n  }\\n  return arr\\n}\\n</code></pre>\\n<table>\\n<thead>\\n<tr>\\n<th>指标</th>\\n<th>值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>时间复杂度</td>\\n<td><strong>取决于 gap 序列</strong>，从 <code>O(n²)</code> 到约 <code>O(n^1.3)</code> 不等</td>\\n</tr>\\n<tr>\\n<td>空间复杂度</td>\\n<td><code>O(1)</code></td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td><strong>不稳定</strong></td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>两个考点：</strong></p>\\n<p><strong>① 复杂度取决于 gap 序列，这是它最特别的地方。</strong> 上面用的是最朴素的&quot;每次除以 2&quot;（Shell 原始序列），最坏仍是 <code>O(n²)</code>。改用 Hibbard 序列（<code>2^k - 1</code>）能到 <code>O(n^1.5)</code>，Sedgewick 序列约 <code>O(n^1.3)</code>。目前<strong>还没有人从理论上完全分析清楚最优 gap 序列</strong> —— 这在算法里很少见，值得提一句。</p>\\n<p><strong>② 为什么不稳定？</strong> 因为相同的元素可能被分到不同的子序列里，各自独立排序时相对顺序就被打乱了。插入排序本身是稳定的，但&quot;分组&quot;这个动作破坏了稳定性。</p>\\n<p><strong>工程现状</strong>：希尔排序在实践中已经很少用了 —— <code>O(n log n)</code> 的算法（快排、归并、TimSort）在大数据量上稳定占优，而小数据量直接用插入排序即可。它的价值主要在教学上：<strong>展示了&quot;让算法先接近目标状态再收尾&quot;这个优化思路</strong>。</p>\\n<p>下面是具体实现：</p>\\n<blockquote>\\n<p>通过某个增量 gap，将整个序列分给若干组，从后往前进行组内成员的比较和交换，随后逐步缩小增量至 1。希尔排序类似于插入排序，只是一开始向前移动的步数从 1 变成了 gap。</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function shellSort(list) {\\n  var n = list.length\\n  var gap = parseInt(n / 2) // 初始化步数\\n  while(gap) { // 逐步缩小步数\\n    for(var i = gap; i &lt; n; i++) {\\n      // 逐步和前面其他成员比较交换\\n      for(var j = i - gap; j &gt;=0; j -= gap) {\\n        if(list[j] &gt; list[j + gap]) {\\n          var temp = list[j + gap]\\n          list[j + gap] = list[j]\\n          list[j] = temp\\n        } else {\\n          break\\n        }\\n      }\\n    }\\n    gap = parseInt(gap / 2)\\n  }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表里两个价格相同的商品原本按上架时间排列，执行这段希尔排序后顺序反了；组内用的插入排序明明稳定，问题出在哪里？</strong></p>\\n<p>问题出在不同 <code>gap</code> 子序列之间的跨步移动，相等商品可能被分到不同组，最终相对位置被间接颠倒。组内插入排序只保证本子序列中相等元素不交换，无法约束跨组顺序；若业务依赖稳定性，应改用稳定排序。</p>\\n</li>\\n<li>\\n<p><strong>一个前端页面要对几千条已接近有序的数据排序，同事主张引入希尔排序以优化插入排序，你会批准这个选型吗？</strong></p>\\n<p>通常不会仅凭“接近有序”引入希尔排序，因为最后一轮 <code>gap = 1</code> 本就是插入排序，而页面规模下额外的增量循环未必带来可验证收益。通用代码优先使用运行时排序；只有受限环境明确要求原地、低额外空间时，才值得基准验证。</p>\\n</li>\\n<li>\\n<p><strong>嵌入式脚本只能使用 <code>O(1)</code> 额外空间，又不能递归，待排数组有数万项；希尔排序相较归并排序还剩什么优势和风险？</strong></p>\\n<p>希尔排序可原地执行且不需要递归栈，符合 <code>O(1)</code> 额外空间约束，元素还能借助大 <code>gap</code> 快速接近目标位置。风险是耗时高度依赖增量序列，最坏可能达到 <code>O(n²)</code>，并且不稳定，不能用于依赖同值元素原顺序的数据。</p>\\n</li>\\n<li>\\n<p><strong>线上排序耗时随数组长度波动明显，代码使用 <code>gap = Math.floor(gap / 2)</code>；你会先检查比较函数，还是先检查增量序列和输入分布？</strong></p>\\n<p>应同时记录输入规模和各轮 <code>gap</code>，重点检查增量序列，因为希尔排序的复杂度直接受它影响，朴素折半序列最坏仍可能是 <code>O(n²)</code>。比较函数若正确且一致，波动更可能来自数据分布与移动次数；更换序列也只能改善边界，不能获得统一的 <code>O(n log n)</code> 保证。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中有人要把折半增量换成 <code>Hibbard</code> 或 <code>Sedgewick</code>，并宣称这样就能保证最优复杂度，你会怎么回应？</strong></p>\\n<p>可以把更好的增量序列作为实验候选，但不能宣称已经得到理论最优解。资料只支持 <code>Hibbard</code> 约 <code>O(n^1.5)</code>、<code>Sedgewick</code> 约 <code>O(n^1.3)</code>，而最优 <code>gap</code> 序列尚未被完全分析；选型仍需结合实现复杂度、数据分布和基准结果。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"归并排序\\"><a class=\\"header-anchor\\" href=\\"#归并排序\\">#</a> 归并排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>思路：分治 —— 从中间一分为二，递归排好左右两半，再<strong>合并</strong>两个有序数组</li>\\n<li>复杂度 <strong>稳定 <code>O(n log n)</code></strong>（最好最坏平均都一样，不受输入分布影响）；空间 <code>O(n)</code>；<strong>稳定</strong></li>\\n<li>⚠️ 合并时用 <code>&lt;=</code> 而不是 <code>&lt;</code>（相等优先取左边）才保证稳定 —— 一个字符决定稳定性</li>\\n<li>两个不可替代之处：<strong>不怕最坏情况</strong>（快排可能退化到 <code>O(n²)</code>）、<strong>是唯一常用的稳定 <code>O(n log n)</code> 排序</strong></li>\\n<li>所以 <code>V8</code> 的 TimSort、<code>Java</code> 的 <code>Collections.sort</code>、<code>Python</code> 的 <code>sorted</code> 都是它的改良版</li>\\n<li>独有能力：<strong>外部排序</strong> —— 只需顺序读取，能处理内存装不下的超大数据（分块排序 + 多路归并）</li>\\n</ul>\\n<p><strong>归并排序就是把数组递归拆成两半，分别排好后，再合并两个有序序列。</strong> 它最好、平均和最坏都是 <code>O(n log n)</code>，不受输入分布影响，但合并需要 <code>O(n)</code> 额外空间。合并时相等元素要优先取左侧，也就是比较使用 <code>&lt;=</code>，否则会失去稳定性。它适合要求稳定或不能接受最坏退化的场景，也能通过分块排序和多路归并处理内存放不下的大文件。</p>\\n</section>\\n<p><strong>思路</strong>：典型的分治 —— 把数组从中间一分为二，递归排好左右两半，再把两个有序数组<strong>合并</strong>成一个。</p>\\n<pre class=\\"language-js\\"><code>function mergeSort(arr) {\\n  if (arr.length &lt;= 1) return arr\\n  const mid = arr.length &gt;&gt; 1\\n  const left = mergeSort(arr.slice(0, mid))\\n  const right = mergeSort(arr.slice(mid))\\n  return merge(left, right)\\n}\\n\\nfunction merge(a, b) {\\n  const res = []\\n  let i = 0, j = 0\\n  while (i &lt; a.length &amp;&amp; j &lt; b.length) {\\n    // ⚠️ 用 &lt;= 而不是 &lt;：相等时优先取左边 → 保证稳定\\n    if (a[i] &lt;= b[j]) res.push(a[i++])\\n    else res.push(b[j++])\\n  }\\n  return res.concat(a.slice(i)).concat(b.slice(j))\\n}\\n</code></pre>\\n<table>\\n<thead>\\n<tr>\\n<th>指标</th>\\n<th>值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>时间复杂度</td>\\n<td><strong>稳定 <code>O(n log n)</code></strong>——最好最坏平均都一样，不受输入分布影响</td>\\n</tr>\\n<tr>\\n<td>空间复杂度</td>\\n<td><code>O(n)</code>（需要额外数组做合并）</td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td><strong>稳定</strong>（合并时相等取左边）</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>它的两个不可替代之处：</strong></p>\\n<p><strong>① 复杂度稳定，不怕最坏情况。</strong> 快排在数据已有序或大量重复时可能退化到 <code>O(n²)</code>，归并排序<strong>永远是 <code>O(n log n)</code></strong>。所以对&quot;最坏情况有硬性要求&quot;的场景（比如实时系统）会选它。</p>\\n<p><strong>② 是唯一常用的稳定 <code>O(n log n)</code> 排序。</strong> 需要多字段排序、或者排序结果要可复现时，稳定性是硬需求。<code>Java</code> 的 <code>Collections.sort</code>、<code>Python</code> 的 <code>sorted</code>、<code>V8</code> 的 <code>Array.sort</code> 用的 TimSort 都是归并排序的改良版，正是因为这一条。</p>\\n<p><strong>它的代价是 <code>O(n)</code> 额外空间</strong>（虽然有原地归并的变体，但常数太大不实用）。所以在内存紧张时快排更受欢迎。</p>\\n<p><strong>归并排序还有一个快排做不到的能力：外部排序。</strong></p>\\n<p>数据量大到内存装不下时（比如排序一个 100GB 的日志文件），可以把数据分块读进内存排好、写回磁盘，最后多路归并 —— 因为归并只需要顺序读取，对磁盘和网络这类&quot;顺序快、随机慢&quot;的介质非常友好。快排的随机访问模式做不到这一点。</p>\\n<p><strong>面试里能加分的一句</strong>：<code>merge</code> 里用 <code>&lt;=</code> 而不是 <code>&lt;</code>，这一个字符决定了整个算法稳不稳定。</p>\\n<p>下面是具体实现：</p>\\n<blockquote>\\n<p>递归将数组分成两个序列，有序合并这两个序列。作为一种典型的分治法思想算法应用，归并排序的实现有两种方法：</p>\\n<ul>\\n<li>自上而下的递归</li>\\n<li>自下而上的迭代</li>\\n</ul>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function mergeSort(list) {\\n  var n = list.length\\n  if(n &lt; 2) return list\\n  \\n  var mid = Math.floor(n / 2)\\n  var left = list.slice(0, mid)\\n  var right = list.slice(mid)\\n  \\n  return merge(mergeSort(left), mergeSort(right))\\n}\\n\\nfunction merge(left, right) {\\n  var result = []\\n  while(left.length &amp;&amp; right.length) {\\n    if(left[0] &lt;= right[0]) {\\n      result.push(left.shift())\\n    } else {\\n      result.push(right.shift())\\n    }\\n  }\\n  while(left.length) {\\n    result.push(left.shift())\\n  }\\n  while(right.length) {\\n    result.push(right.shift())\\n  }\\n  return result\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>订单页先按创建时间排过一次，再按状态做归并排序；<code>merge</code> 中把 <code>&lt;=</code> 改成 <code>&lt;</code> 后，同状态订单的时间顺序乱了，为什么只改一个字符就会出故障？</strong></p>\\n<p>使用 <code>&lt;</code> 时左右两边键值相等会先取右侧元素，使原本靠后的订单越过左侧同值订单，稳定性因此被破坏。保留 <code>&lt;=</code> 可在相等时优先取左边，维持前一轮排序结果；前提是左右子数组本身也按同一规则稳定生成。</p>\\n</li>\\n<li>\\n<p><strong>浏览器要排序一份接近内存上限的大数组，架构师要求最坏时间仍为 <code>O(n log n)</code>；归并排序满足时间要求后，还要评估什么风险？</strong></p>\\n<p>归并排序的最好、平均和最坏时间都为 <code>O(n log n)</code>，不会因已有序或重复值而退化。主要风险是合并需要 <code>O(n)</code> 额外数组，示例中的 <code>slice</code>、<code>concat</code> 还会产生更多临时对象；若内存是硬约束，稳定时间保证与空间成本存在直接冲突。</p>\\n</li>\\n<li>\\n<p><strong>要给无法一次装入内存的 <code>100GB</code> 日志文件排序，快排和归并都有人支持；你会怎样设计归并流程？</strong></p>\\n<p>应把文件切成可放入内存的块，逐块排序并写回外部存储，再对这些有序块执行多路归并。归并阶段主要顺序读取，适合磁盘和网络这类随机访问代价较高的介质；代价是需要临时文件、额外写入以及归并过程的资源管理。</p>\\n</li>\\n<li>\\n<p><strong>线上用示例代码处理几十万项时频繁出现内存压力，火焰图里 <code>shift</code>、<code>slice</code> 和 <code>concat</code> 很显眼；算法复杂度没变，应该怎么改？</strong></p>\\n<p>先把 <code>merge</code> 改为用下标扫描左右数组，避免反复调用 <code>shift</code> 修改数组头部，并减少连续的 <code>slice</code>、<code>concat</code> 临时分配。可复用一个辅助缓冲区来承载合并结果；这仍无法消除归并排序本身的 <code>O(n)</code> 辅助空间，只能降低实现常数。</p>\\n</li>\\n<li>\\n<p><strong>实时任务要求排序耗时不能出现输入相关的平方级退化，同时业务又要保留相等记录的原顺序；快排与归并之间怎么选？</strong></p>\\n<p>应优先选择稳定归并，因为它能同时提供稳定性和不受输入分布影响的 <code>O(n log n)</code> 时间上界。普通快排既可能退化到 <code>O(n²)</code>，也不天然稳定；归并的代价是 <code>O(n)</code> 额外空间，若内存同样是硬限制，就需要重新协调约束。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"快速排序\\"><a class=\\"header-anchor\\" href=\\"#快速排序\\">#</a> 快速排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>思路：分治 —— 选基准 pivot 分区成&quot;小于/大于&quot;两部分，再递归排序两边</li>\\n<li>复杂度：平均 <code>O(n log n)</code>，<strong>最坏 <code>O(n²)</code></strong>；空间 <code>O(log n)</code> 递归栈、原地；<strong>不稳定</strong></li>\\n<li>退化条件：每次 pivot 都是最值 —— 最典型是<strong>对已有序数组固定取首/尾元素</strong>，而&quot;数据已有序&quot;在真实业务里非常常见</li>\\n<li>三个必备优化：<strong>随机/三数取中选 pivot</strong>、<strong>小数组（&lt;10~16）切插入排序</strong>、<strong>三路划分</strong>处理大量重复元素</li>\\n<li>它实践中最快的原因是<strong>缓存局部性好</strong>（分区是顺序扫描），这比渐进复杂度的影响还大</li>\\n<li>需要稳定性选归并；不能接受最坏 <code>O(n²)</code> 时，可像 C++ <code>introsort</code> 那样递归过深自动切堆排序</li>\\n</ul>\\n<p><strong>快速排序通过选取基准值 <code>pivot</code> 完成分区，再递归排序基准值两侧的数据。</strong> 它平均是 <code>O(n log n)</code>，原地实现的递归栈通常为 <code>O(log n)</code>，但分区中的跨越交换决定了它不稳定。若有序数组总取首尾元素作基准，或大量重复值仍用普通两路划分，就可能退化到 <code>O(n²)</code>。实际实现一般用随机或三数取中、小数组切插入排序，并用三路划分处理重复值；若不能接受最坏情况，可以用堆排序兜底。</p>\\n</section>\\n<p><strong>思路</strong>：分治。选一个基准值 pivot，把数组分成&quot;比它小的&quot;和&quot;比它大的&quot;两部分（这一步叫<strong>分区 partition</strong>），再递归排序两边。</p>\\n<pre class=\\"language-js\\"><code>function quickSort(arr, lo = 0, hi = arr.length - 1) {\\n  if (lo &gt;= hi) return arr\\n  const p = partition(arr, lo, hi)\\n  quickSort(arr, lo, p - 1)\\n  quickSort(arr, p + 1, hi)\\n  return arr\\n}\\n\\nfunction partition(arr, lo, hi) {\\n  // ① 三数取中：避免对已有序数组退化\\n  const mid = (lo + hi) &gt;&gt; 1\\n  if (arr[mid] &lt; arr[lo]) [arr[lo], arr[mid]] = [arr[mid], arr[lo]]\\n  if (arr[hi] &lt; arr[lo]) [arr[lo], arr[hi]] = [arr[hi], arr[lo]]\\n  if (arr[hi] &lt; arr[mid]) [arr[mid], arr[hi]] = [arr[hi], arr[mid]]\\n  ;[arr[mid], arr[hi]] = [arr[hi], arr[mid]]   // 中位数放到末尾当 pivot\\n\\n  const pivot = arr[hi]\\n  let i = lo\\n  for (let j = lo; j &lt; hi; j++) {\\n    if (arr[j] &lt; pivot) [arr[i], arr[j]] = [arr[j], arr[i]], i++\\n  }\\n  ;[arr[i], arr[hi]] = [arr[hi], arr[i]]\\n  return i\\n}\\n</code></pre>\\n<table>\\n<thead>\\n<tr>\\n<th>指标</th>\\n<th>值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>时间复杂度</td>\\n<td>平均 <code>O(n log n)</code>，<strong>最坏 <code>O(n²)</code></strong></td>\\n</tr>\\n<tr>\\n<td>空间复杂度</td>\\n<td><code>O(log n)</code>（递归栈），原地</td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td><strong>不稳定</strong>（分区时是跨越式交换）</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>什么时候退化到 <code>O(n²)</code>？</strong></p>\\n<p>当每次选的 pivot 都是当前区间的最值时，分区极度不平衡（一边 0 个、一边 n-1 个），递归深度变成 <code>n</code>。最典型的触发场景是 <strong>对已经有序的数组固定取第一个（或最后一个）元素做 pivot</strong> —— 而&quot;数据已经有序&quot;在真实业务里非常常见（比如从数据库按 id 顺序取出来的数据）。</p>\\n<p><strong>三个让它能上生产的优化：</strong></p>\\n<p><strong>① 选 pivot：随机 或 三数取中。</strong> 这是防退化的根本手段。上面代码用的是三数取中（首、中、尾取中位数），比纯随机更稳定且没有随机数开销。</p>\\n<p><strong>② 小数组切插入排序。</strong> 子数组长度小于 10~16 时递归开销超过收益，直接插入排序。所有工业级实现都有这一步。</p>\\n<p><strong>③ 三路划分处理大量重复元素。</strong> 普通两路分区遇到&quot;全是相同值&quot;的数组时，等于每次只能确定一个位置，退化成 <code>O(n²)</code>。三路划分把数组分成&quot;小于 / 等于 / 大于&quot;三段，等于的部分直接跳过，把这种场景降到 <code>O(n)</code>。</p>\\n<p><strong>它为什么在实践中最快？</strong></p>\\n<p>原地（不像归并要 <code>O(n)</code> 额外空间）、常数小、<strong>缓存局部性极好</strong>——分区是顺序扫描数组，<code>CPU</code> 缓存命中率高。这一点在实测中比渐进复杂度的影响还大，也是堆排序虽然最坏更好却更慢的原因。</p>\\n<p><strong>什么时候不该选它</strong>：需要<strong>稳定性</strong>（选归并）、或者<strong>不能接受最坏 <code>O(n²)</code></strong>（实时系统选归并或堆排序）。C++ 的 <code>introsort</code> 的做法是：快排为主，递归太深时自动切换成堆排序兜底 —— 两全其美。</p>\\n<p>下面是更多实现细节：</p>\\n<blockquote>\\n<p>选择一个元素作为基数，把比基数小的元素放在它左边，比它大的放在右边（相当于二分），再不断递归基数左右的序列。快速排序是一种分而治之思想在排序算法上的典型应用。本质上来看，快速排序应该算是在冒泡排序基础上递归分治法。快速排序的名字起的是简单粗暴，因为一听到这个名字你就知道它存在的意义，它是处理大数据最快的排序算法之一。</p>\\n</blockquote>\\n<h3 id=\\"实现一\\"><a class=\\"header-anchor\\" href=\\"#实现一\\">#</a> 实现一</h3>\\n<pre class=\\"language-js\\"><code>function quickSort(arr) {\\n  if (arr.length&lt;=1){\\n    return arr;\\n  }\\n  var baseIndex = Math.floor(arr.length/2);//向下取整，选取基准点\\n  var base = arr.splice(baseIndex,1)[0];//取出基准点的值，\\n  // splice 通过删除或替换现有元素或者原地添加新的元素来修改数组,并以数组形式返回被修改的内容。此方法会改变原数组。\\n  // slice方法返回一个新的数组对象,不会更改原数组\\n  //这里不能直接base=arr[baseIndex],因为base代表的每次都删除的那个数\\n  var left=[];\\n  var right=[];\\n  for (var i = 0; i&lt;arr.length; i++){\\n    // 这里的length是变化的，因为splice会改变原数组。\\n    if (arr[i] &lt; base){\\n      left.push(arr[i]);//比基准点小的放在左边数组，\\n    }\\n  }else{\\n    right.push(arr[i]);//比基准点大的放在右边数组，\\n  }\\n  return quickSort(left).concat([base],quickSort(right));\\n}\\n</code></pre>\\n<h3 id=\\"实现二\\"><a class=\\"header-anchor\\" href=\\"#实现二\\">#</a> 实现二</h3>\\n<pre class=\\"language-js\\"><code>function quickSort(list, left = 0, right = list.length - 1) {\\n  var n = list.length\\n\\tif(left &lt; right) {\\n    var index = left - 1\\n    for(var i = left; i &lt;= right; i++) {\\n      if(list[i] &lt;= list[right]) {\\n        index++\\n        var temp = list[index]\\n        list[index] = list[i]\\n        list[i] = temp\\n      }\\n    }\\n    quickSort(list, left, index - 1)\\n    quickSort(list, index + 1, right)\\n  }\\n  \\n  return list\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>接口返回的数据已按 <code>id</code> 升序排列，这段快排却固定用 <code>list[right]</code> 做 <code>pivot</code>；数组增大后递归很深，分区过程发生了什么？</strong></p>\\n<p>升序输入会让末尾 <code>pivot</code> 每次都是区间最大值，所有元素都进入左侧，分区规模变成 <code>n-1</code> 和 <code>0</code>。递归深度因此接近 <code>n</code>，总比较量退化到 <code>O(n²)</code>；随机选 <code>pivot</code> 或采用三数取中只能降低触发概率，不能让普通快排天然稳定。</p>\\n</li>\\n<li>\\n<p><strong>埋点数组里只有少数几种状态码，代码判断条件是 <code>list[i] &lt;= list[right]</code>；为什么大量重复值也可能让这版快排变慢？</strong></p>\\n<p>相等元素都会被划入 <code>pivot</code> 的同一侧，数据大量重复时仍可能形成极不平衡的递归区间。可改用三路划分，把区间拆成小于、等于和大于 <code>pivot</code> 三段，并跳过等值段递归；实现更复杂，且快排的不稳定性不会因此消失。</p>\\n</li>\\n<li>\\n<p><strong>代码评审发现循环写成 <code>i &lt;= right</code>，连 <code>pivot</code> 自己也参与比较；这是越界式错误，还是这版 <code>Lomuto</code> 分区的有意写法？</strong></p>\\n<p>这是有意让 <code>pivot</code> 在最后一次比较时被交换到 <code>index</code> 位置，并未访问区间之外。递归随后排 <code>[left, index - 1]</code> 和 <code>[index + 1, right]</code>，也正确排除了 <code>pivot</code>；真正需要警惕的是 <code>&lt;=</code> 对重复值造成的单侧聚集。</p>\\n</li>\\n<li>\\n<p><strong>线上偶发 <code>Maximum call stack size exceeded</code>，输入通常已经部分有序，日志显示每次分区后的右区间几乎为空；你会怎样定位和止血？</strong></p>\\n<p>先记录每轮 <code>pivot</code> 位置和左右区间长度，确认是否持续出现 <code>n-1</code> 对 <code>0</code> 的极端分区。止血可随机化 <code>pivot</code>、三数取中，并优先递归较小分区而循环处理较大分区以控制栈深；这些措施仍需通过最坏输入测试验证。</p>\\n</li>\\n<li>\\n<p><strong>内存受限的工具函数要排序普通数值数组，同事在快排和归并之间争执；这段实现能提供哪些取舍依据？</strong></p>\\n<p>这版快排原地交换，除递归栈外不需要归并所需的 <code>O(n)</code> 辅助数组，通常更符合内存受限场景。代价是排序不稳定，固定末尾 <code>pivot</code> 还可能触发 <code>O(n²)</code> 和深递归；若业务要求稳定或硬性最坏时间上界，应选择归并。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"堆排序\\"><a class=\\"header-anchor\\" href=\\"#堆排序\\">#</a> 堆排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>思路：先原地建<strong>大顶堆</strong>，再反复&quot;堆顶与末尾交换 + 范围减一 + 下沉调整&quot;，<code>n-1</code> 次后有序</li>\\n<li>复杂度 <strong>稳定 <code>O(n log n)</code></strong>（不受输入影响）；空间 <strong><code>O(1)</code></strong> 完全原地；<strong>不稳定</strong></li>\\n<li><strong>建堆是 <code>O(n)</code> 不是 <code>O(n log n)</code></strong>：越靠底层节点越多但下沉距离越短，求和收敛到 <code>2n</code>——这是高频追问</li>\\n<li>独特价值：<strong>唯一同时满足&quot;最坏 <code>O(n log n)</code> + 原地 <code>O(1)</code>&quot;</strong>（归并要 <code>O(n)</code> 空间、快排最坏 <code>O(n²)</code>）</li>\\n<li>但实践中比快排慢 —— <code>siftDown</code> 是跳跃访问，<strong><code>CPU</code> 缓存不友好</strong>；所以常作为快排的兜底（C++ <code>introsort</code>）</li>\\n<li>真正的主场是 <strong><code>TopK</code></strong>：维护大小为 k 的堆，<code>O(n log k)</code> 时间、<code>O(k)</code> 空间</li>\\n</ul>\\n<p><strong>堆排序先原地构建大顶堆，再反复交换堆顶与末尾元素，并缩小堆范围后继续下沉调整。</strong> 它始终是 <code>O(n log n)</code>，额外空间为 <code>O(1)</code>，但交换可能改变相同元素的相对顺序，因此不稳定。建堆是 <code>O(n)</code> 而不是 <code>O(n log n)</code>，因为越靠近底部的节点越多，可下沉的距离却越短。它兼顾最坏复杂度和原地排序，不过跳跃访问对缓存不友好，通常作为快速排序的兜底；处理 <code>TopK</code> 时则可用大小为 <code>k</code> 的堆做到 <code>O(n log k)</code>。</p>\\n</section>\\n<p><strong>思路</strong>：利用堆这个数据结构 —— 先把数组原地建成<strong>大顶堆</strong>（每个父节点都不小于子节点），然后反复做三步：把堆顶（最大值）和当前堆的最后一个元素交换、堆的有效范围减一、对新堆顶做下沉调整。<code>n-1</code> 次之后数组就有序了。</p>\\n<pre class=\\"language-js\\"><code>function heapSort(arr) {\\n  const n = arr.length\\n  // ① 建堆：从最后一个非叶节点开始倒着下沉 —— 这一步是 O(n) 不是 O(n log n)\\n  for (let i = (n &gt;&gt; 1) - 1; i &gt;= 0; i--) siftDown(arr, i, n)\\n  // ② 反复取堆顶放到末尾\\n  for (let i = n - 1; i &gt; 0; i--) {\\n    ;[arr[0], arr[i]] = [arr[i], arr[0]]\\n    siftDown(arr, 0, i)\\n  }\\n  return arr\\n}\\n\\nfunction siftDown(arr, i, size) {\\n  while (true) {\\n    let max = i\\n    const l = 2 * i + 1, r = 2 * i + 2\\n    if (l &lt; size &amp;&amp; arr[l] &gt; arr[max]) max = l\\n    if (r &lt; size &amp;&amp; arr[r] &gt; arr[max]) max = r\\n    if (max === i) break\\n    ;[arr[i], arr[max]] = [arr[max], arr[i]]\\n    i = max\\n  }\\n}\\n</code></pre>\\n<table>\\n<thead>\\n<tr>\\n<th>指标</th>\\n<th>值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>时间复杂度</td>\\n<td><strong>稳定 <code>O(n log n)</code></strong>（不受输入分布影响）</td>\\n</tr>\\n<tr>\\n<td>空间复杂度</td>\\n<td><strong><code>O(1)</code></strong>（完全原地，连递归栈都不需要）</td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td><strong>不稳定</strong></td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>一个高频追问：为什么建堆是 <code>O(n)</code> 而不是 <code>O(n log n)</code>？</strong></p>\\n<p>直觉上 n 个节点每个下沉 <code>log n</code> 层应该是 <code>O(n log n)</code>，但实际上：<strong>越靠近底部的节点越多，而它们能下沉的距离越短</strong>。第 h 层有约 <code>n/2^h</code> 个节点，每个最多下沉 <code>h</code> 层，求和 <code>Σ (n/2^h) × h</code> 收敛到 <code>2n</code>——所以是 <code>O(n)</code>。能推出这一步是很好的加分点。</p>\\n<p><strong>它的独特价值：唯一同时满足&quot;最坏 <code>O(n log n)</code> + 原地 <code>O(1)</code> 空间&quot;的排序。</strong></p>\\n<ul>\\n<li>归并：<code>O(n log n)</code> 但要 <code>O(n)</code> 额外空间</li>\\n<li>快排：原地但最坏 <code>O(n²)</code></li>\\n<li>堆排序：两个都满足</li>\\n</ul>\\n<p><strong>但实践中它通常比快排慢</strong> —— 因为 <code>siftDown</code> 是跳跃式访问（<code>i → 2i+1 → 4i+3</code>），对 <code>CPU</code> 缓存极不友好；快排的分区是顺序扫描，缓存命中率高得多，实测常常快好几倍。</p>\\n<p>所以堆排序在通用排序里的定位是<strong>兜底</strong>：C++ 的 <code>introsort</code> 就是快排为主、递归深度超过阈值时切换成堆排序，既拿到快排的速度又保住了最坏情况。</p>\\n<p><strong>它真正的主场是 <code>TopK</code></strong>：维护一个大小为 k 的堆遍历数据，复杂度 <code>O(n log k)</code>、空间 <code>O(k)</code>——这是全排序做不到的。</p>\\n<p>下面是具体实现：</p>\\n<blockquote>\\n<p>说到堆排序，首先需要了解一种数据结构--堆。堆是一种完全二叉树，这种结构通常可以用数组表示。在实际应用中，堆又可以分为最小堆和最大堆，两者区别如下：</p>\\n<ul>\\n<li>-max-heap property：对于所有除了根节点的节点i，A[Parent(i)] &gt;= A[i]</li>\\n<li>-min-heap property：对于除了根节点的节点i，A[Parent(i)] &lt;= A[i]</li>\\n</ul>\\n<p>堆排序可以说是一种利用堆的概念来排序的选择排序，分为两种方法：</p>\\n<ul>\\n<li>大顶堆：每个节点的值都大于或等于其子节点的值，在堆排序算法中用于升序排序；</li>\\n<li>小顶堆：每个节点的值都小于或等于其子节点的值，在堆排序算法中用于降序排序。</li>\\n</ul>\\n</blockquote>\\n<h3 id=\\"实现-4\\"><a class=\\"header-anchor\\" href=\\"#实现-4\\">#</a> 实现</h3>\\n<pre class=\\"language-js\\"><code>function heapSort(list) {\\n  buildHeap(list) \\n  // 循环 n-1 次，每次循环后交换堆顶元素和堆底元素并重新调整堆结构\\n  for(var i = list.length - 1; i &gt; 0; i--) {\\n    [nums[i], nums[0]] = [nums[0], nums[i]]\\n    adjustHeap(nums, 0, i)\\n  }\\n  return list\\n}\\n\\nfunction buildHeap(nums) {\\n  // 注意这里的头节点是从0开始的，所以最后一个非叶子节点结果是 parseInt(nums.length / 2) - 1\\n  var start = parseInt(nums.length / 2) - 1\\n  var size = nums.length\\n  // 从最后一个非叶子节点开始调整，直至堆顶\\n  for(var i = start; i &gt;= 0; i--) {\\n    adjustHeap(nums, i, size)\\n  }\\n}\\n\\nfunction adjustHeap(nums, index, size) {\\n  // 交换后可能会破坏堆结构，需要循环使得每一个父节点都大于左右节点\\n  while(true) {\\n    var max = index\\n    var left = index * 2 + 1 // 左节点\\n    var right = index * 2 + 2 // 右节点\\n    if(left &lt; size &amp;&amp; nums[max] &lt; nums[left]) max = left\\n    if(right &lt; size &amp;&amp; nums[max] &lt; nums[right]) max = right\\n    // 如果左右节点大雨当前节点则交换，并在循环一遍判断交换后是否破坏堆结构\\n    if(index !== max) {\\n      [nums[index], nums[max]] = [nums[max], nums[index]]\\n      index = max\\n    } else {\\n      break\\n    }\\n  }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>把这段 <code>heapSort(list)</code> 直接复制到页面后报 <code>ReferenceError: nums is not defined</code>，但 <code>buildHeap</code> 已经执行；故障点具体在哪里？</strong></p>\\n<p>主函数参数名是 <code>list</code>，交换和调整阶段却引用了未定义的 <code>nums</code>，因此第一次循环就会抛错。应统一为 <code>list</code>，并调用 <code>adjustHeap(list, 0, i)</code>；修复后还要用空数组、单元素和重复值验证边界。</p>\\n</li>\\n<li>\\n<p><strong>监控平台要从一亿条指标中找最大的 <code>100</code> 条，团队有人准备先执行完整堆排序；你会怎样改方案？</strong></p>\\n<p>应维护容量为 <code>100</code> 的小顶堆，遍历数据时只在新值大于堆顶时替换并下沉调整。这样时间为 <code>O(n log k)</code>、额外空间为 <code>O(k)</code>，无需完整排序全部数据；如果还要求最终有序，再对堆中的 <code>100</code> 项排序即可。</p>\\n</li>\\n<li>\\n<p><strong>内存只能保存少量候选值，而且指标流持续到达；“最大的 <code>100</code> 个”为什么要用小顶堆，而示例堆排序构建的却是大顶堆？</strong></p>\\n<p>流式 <code>Top</code> <code>K</code> 需要小顶堆，让当前候选中最小的值位于堆顶，遇到更大值时能以 <code>O(log k)</code> 淘汰它。完整升序堆排序则构建大顶堆，把最大值逐次交换到数组尾部；两者目标不同，不能仅凭“求最大值”混用。</p>\\n</li>\\n<li>\\n<p><strong>同事估算 <code>buildHeap</code> 为 <code>O(n log n)</code>，理由是每个节点都可能下沉 <code>log n</code> 层；你会用代码中的自底向上过程怎样反驳？</strong></p>\\n<p>该估算把所有节点都按根节点的最大高度计算了，而代码从最后一个非叶子节点开始下沉，底层大量节点最多只移动一两层。按高度汇总“节点数乘下沉距离”的级数收敛到线性量级，所以自底向上建堆是 <code>O(n)</code>；逐个插入建堆才是 <code>O(n log n)</code>。</p>\\n</li>\\n<li>\\n<p><strong>商品排序要求相同价格保持原始上架顺序，内存又很紧，评审会上有人建议直接采用这版堆排序；你会接受吗？</strong></p>\\n<p>不能直接接受，因为堆顶与堆尾的远距离交换可能改变相等商品的相对顺序，堆排序不稳定。它具备原地和稳定 <code>O(n log n)</code> 时间的优势，但不满足稳定性要求；若稳定顺序是硬约束，需要换稳定排序或引入原序号作为显式次关键字。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"计数排序\\"><a class=\\"header-anchor\\" href=\\"#计数排序\\">#</a> 计数排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>思路：<strong>不比较</strong>，统计每个值出现的次数，再按值从小到大输出 —— 靠&quot;按值索引&quot;绕开 <code>O(n log n)</code> 下界</li>\\n<li>复杂度 <code>O(n + k)</code>（k 是值域大小），空间 <code>O(n + k)</code>，<strong>稳定</strong>（前提是倒序回填）</li>\\n<li>⚠️ 关键限制：<strong>k 不能远大于 n</strong> —— 排 10 个百万范围的数要开百万数组，还不如直接排序</li>\\n<li>适用场景：整数且值域集中 —— 年龄、分数、状态码、月份</li>\\n<li>两个细节：<strong>做前缀和</strong>是为了保留原始元素（不只是值）、<strong>必须倒序回填</strong>才稳定</li>\\n<li>基数排序依赖它的稳定性才能工作，所以倒序回填不是可选项</li>\\n</ul>\\n<p><strong>计数排序不比较元素，而是统计每个整数值出现的次数，再按值从小到大回填结果。</strong> 它用按值索引绕过比较排序的下界，时间复杂度是 <code>O(n + k)</code>，空间复杂度是 <code>O(n + k)</code>。为了保留关联数据，需要先算前缀和，并倒序回填来保证稳定性，这也是基数排序能正确工作的前提。它适合年龄、分数这类值域集中的数据；如果 <code>k</code> 远大于 <code>n</code>，时间和内存开销反而不划算。</p>\\n</section>\\n<p><strong>思路</strong>：不比较元素，而是统计每个值出现的次数，再按值从小到大依次输出。</p>\\n<pre class=\\"language-js\\"><code>function countingSort(arr) {\\n  if (!arr.length) return arr\\n  const min = Math.min(...arr)\\n  const max = Math.max(...arr)\\n  const count = new Array(max - min + 1).fill(0)\\n\\n  for (const v of arr) count[v - min]++        // ① 统计次数\\n  // ② 累加成&quot;前缀和&quot;，得到每个值在结果里的结束位置\\n  for (let i = 1; i &lt; count.length; i++) count[i] += count[i - 1]\\n\\n  const res = new Array(arr.length)\\n  // ③ ⚠️ 必须倒序遍历原数组才能保证稳定\\n  for (let i = arr.length - 1; i &gt;= 0; i--) {\\n    res[--count[arr[i] - min]] = arr[i]\\n  }\\n  return res\\n}\\n</code></pre>\\n<table>\\n<thead>\\n<tr>\\n<th>指标</th>\\n<th>值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>时间复杂度</td>\\n<td><code>O(n + k)</code>，k 是值域大小</td>\\n</tr>\\n<tr>\\n<td>空间复杂度</td>\\n<td><code>O(n + k)</code></td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td><strong>稳定</strong>（前提是倒序回填）</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>它为什么能突破 <code>O(n log n)</code>？</strong></p>\\n<p>因为它<strong>不做比较</strong>。基于比较的排序有 <code>O(n log n)</code> 的理论下界（决策树模型可以证明），而计数排序是&quot;按值索引&quot;，绕开了这个下界 —— 代价是只能用于<strong>整数或可映射为整数</strong>的数据。</p>\\n<p><strong>关键限制：值域 k 不能太大。</strong></p>\\n<p>排序 <code>[1, 1000000]</code> 范围内的 10 个数，要开一百万长度的数组 —— 时间和空间都被 k 主导，还不如直接排序。所以适用条件是 <strong>k 与 n 同数量级</strong>，比如年龄（0~150）、分数（0~100）、状态码。</p>\\n<p><strong>两个容易答错的细节：</strong></p>\\n<p><strong>① 为什么要做前缀和？</strong> 直接按次数输出也能排序，但那样会<strong>丢失原始元素</strong>——只知道&quot;3 出现了两次&quot;，不知道这两个 3 分别关联着什么数据。做前缀和 + 回填才能保留完整对象，才能作为基数排序的子过程使用。</p>\\n<p><strong>② 为什么必须倒序回填？</strong> 正序回填会让相同值的元素顺序颠倒，破坏稳定性。而基数排序<strong>依赖计数排序的稳定性</strong>才能工作（低位排好的顺序要在高位排序时保留下来），所以这不是可选项。</p>\\n<p>下面是具体实现：</p>\\n<blockquote>\\n<p>以数组元素值为键，出现次数为值存进一个临时数组，最后再遍历这个临时数组还原回原数组。因为 JS 的数组下标是以字符串形式存储的，所以计数排序可以用来排列负数，但是不可以排列小数。</p>\\n</blockquote>\\n<h3 id=\\"实现-5\\"><a class=\\"header-anchor\\" href=\\"#实现-5\\">#</a> 实现</h3>\\n<pre class=\\"language-js\\"><code>function countingSort(nums) {\\n  var list = []\\n  var max = Math.max(...nums)\\n  var min = Math.min(...nums)\\n  \\n  for(var i = 0; i &lt; nums.length; i++) {\\n    var temp = nums[i]\\n    list[temp] = list[temp] + 1 || 1\\n  }\\n  \\n  var index = 0\\n  for(var i = min; i &lt;= max; i++) {\\n    while(list[i] &gt; 0) {\\n      nums[index++] = i\\n      list[i]--\\n    }\\n  }\\n  \\n  return list\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>用户表按年龄排序时还要保留同龄用户的注册顺序，这段只统计次数再回写数值的实现能宣称稳定吗？</strong></p>\\n<p>不能直接宣称稳定，因为实现只回写年龄值，原记录身份已经丢失，相等元素的先后关系在结果中不可观察。要稳定排序对象，需要累计计数得到位置，并按原数组倒序放入独立输出数组；代价是额外的 <code>O(n)</code> 输出空间。</p>\\n</li>\\n<li>\\n<p><strong>页面要排序一千万个整数，但值域从 <code>-10^9</code> 到 <code>10^9</code>，产品经理看到线性复杂度就指定计数排序；你会否决吗？</strong></p>\\n<p>应否决直接按完整值域分配计数数组的方案，因为空间取决于值域跨度而非元素数量，稀疏大范围会造成不可接受的内存开销。计数排序适合有限且较密集的整数值域；这里应选择比较排序，或仅在可证明的稠密分段内计数。</p>\\n</li>\\n<li>\\n<p><strong>测试传入负数后，代码用 <code>list[temp]</code> 计数但最终结果异常；在 <code>JavaScript</code> 数组语义下，负下标为什么是隐患？</strong></p>\\n<p>负数键会成为数组对象的普通属性，而不是参与正常长度和索引存储的数组槽位，稀疏大正数也会带来不合适的结构。应使用偏移量 <code>value - min</code> 映射到从零开始的计数下标；仍需先检查 <code>max - min</code> 是否适合分配。</p>\\n</li>\\n<li>\\n<p><strong>线上接口偶尔返回空数组，这段代码在 <code>Math.max(...nums)</code> 和 <code>Math.min(...nums)</code> 后行为异常；你会怎么修并排查更大规模的风险？</strong></p>\\n<p>应在函数开头处理 <code>nums.length &lt;= 1</code>，避免空输入产生无意义的边界值。还要检查展开运算符处理超大数组时的调用参数风险，并改为循环求 <code>min</code>、<code>max</code>；随后验证值域跨度，防止计数结构因极端值膨胀。</p>\\n</li>\\n<li>\\n<p><strong>同事说比较排序有 <code>O(n log n)</code> 下界，所以这段计数排序标成线性复杂度一定是错的；你如何解释两者并指出隐藏成本？</strong></p>\\n<p><code>O(n log n)</code> 下界约束的是基于元素比较的通用排序，而计数排序按整数值直接索引计数，没有走比较决策树。其时间更准确地写作 <code>O(n + k)</code>，<code>k</code> 是值域跨度；当 <code>k</code> 远大于 <code>n</code> 时，时间和空间优势都会消失。</p>\\n</li>\\n<li>\\n<p><strong>团队准备把计数排序作为基数排序的每一位排序器，却沿用当前“按值重复回写”的代码；为什么接口层面就不够用？</strong></p>\\n<p>基数排序每一轮必须保留上一轮形成的相对顺序，因此内部计数排序必须稳定地搬运完整记录，而不能只重建当前位的数值。应通过累计计数确定输出位置，并倒序扫描输入写入输出数组；若正序放置且位置递减，相同键的顺序会被反转。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"桶排序\\"><a class=\\"header-anchor\\" href=\\"#桶排序\\">#</a> 桶排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>思路：按值域把数据<strong>均匀分到若干桶</strong>，桶内单独排序（通常用插入排序），最后按桶序拼接</li>\\n<li>复杂度：<strong>理想 <code>O(n + k)</code></strong>，<strong>最坏 <code>O(n²)</code></strong>（全落进一个桶）；空间 <code>O(n + k)</code></li>\\n<li>稳定性<strong>取决于桶内算法</strong> —— 用插入排序就稳定</li>\\n<li>⚠️ 核心前提是<strong>数据均匀分布</strong>，这是它和计数排序最大的区别（后者只要求值域有限、不管分布）</li>\\n<li>桶数经验值：让平均每桶 1~10 个元素（约 <code>n/5</code>）；分布已知时按分位数划分比等宽更均匀</li>\\n<li>三者关系：<strong>计数排序是桶排序在&quot;一个桶只放一个值&quot;时的特例</strong>；基数排序是按位分桶排 d 轮</li>\\n</ul>\\n<p><strong>桶排序是把数据按值域分进多个桶，桶内分别排序，再按桶的顺序拼接。</strong> 数据分布均匀且桶数合适时，时间复杂度接近 <code>O(n + k)</code>；如果元素全挤进一个桶，使用插入排序时会退化到 <code>O(n²)</code>。稳定性取决于桶内算法，用稳定的插入排序就能保持稳定。它适合分布较均匀的浮点数或大值域数据，分布已知时按分位数划桶通常比等宽划分更合适。</p>\\n</section>\\n<p><strong>思路</strong>：把数据按值域<strong>均匀分配到若干个桶</strong>里，每个桶内部单独排序，最后按桶的顺序依次拼接。</p>\\n<pre class=\\"language-js\\"><code>function bucketSort(arr, bucketCount = 10) {\\n  if (arr.length &lt;= 1) return arr\\n  const min = Math.min(...arr), max = Math.max(...arr)\\n  if (min === max) return arr\\n\\n  const buckets = Array.from({ length: bucketCount }, () =&gt; [])\\n  const size = (max - min) / bucketCount\\n\\n  for (const v of arr) {\\n    // 注意边界：最大值要落进最后一个桶，否则索引越界\\n    const idx = Math.min(Math.floor((v - min) / size), bucketCount - 1)\\n    buckets[idx].push(v)\\n  }\\n\\n  // 桶内用插入排序 —— 桶小时最快，而且它稳定\\n  return buckets.flatMap((b) =&gt; insertionSort(b))\\n}\\n</code></pre>\\n<table>\\n<thead>\\n<tr>\\n<th>指标</th>\\n<th>值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>时间复杂度</td>\\n<td><strong>理想 <code>O(n + k)</code></strong>（数据均匀、桶数合适）；<strong>最坏 <code>O(n²)</code></strong>（全落进一个桶）</td>\\n</tr>\\n<tr>\\n<td>空间复杂度</td>\\n<td><code>O(n + k)</code></td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td><strong>取决于桶内排序算法</strong>（用插入排序就稳定）</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>核心前提：数据必须均匀分布。</strong></p>\\n<p>这是它和计数排序最大的区别 —— 计数排序只要求&quot;值域有限&quot;，不管分布如何；桶排序的性能<strong>完全押注在分布均匀性</strong>上。如果数据全挤进一个桶，就退化成&quot;对整个数组做一次插入排序&quot;，即 <code>O(n²)</code>。</p>\\n<p><strong>桶数怎么选？</strong></p>\\n<ul>\\n<li>太少 → 每个桶元素太多，退化成桶内排序的复杂度</li>\\n<li>太多 → 空桶的遍历开销和内存浪费</li>\\n</ul>\\n<p>经验值是让<strong>平均每个桶 1~10 个元素</strong>，也就是桶数取 <code>n/5</code> 左右。数据分布已知时可以按分位数划分桶边界，比等宽划分均匀得多。</p>\\n<p><strong>它和计数排序、基数排序的关系：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th>桶的含义</th>\\n<th>适用数据</th>\\n<th>依赖</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>计数排序</td>\\n<td>一个值一个桶</td>\\n<td>值域有限的整数</td>\\n<td>值域不能太大</td>\\n</tr>\\n<tr>\\n<td>桶排序</td>\\n<td>一个区间一个桶</td>\\n<td>浮点数、值域大的数据</td>\\n<td><strong>分布均匀</strong></td>\\n</tr>\\n<tr>\\n<td>基数排序</td>\\n<td>按位分桶，排 d 轮</td>\\n<td>可按位拆分的数据</td>\\n<td>子过程必须稳定</td>\\n</tr>\\n</tbody>\\n</table>\\n<p>可以说<strong>计数排序是桶排序在&quot;每个桶只放一个值&quot;时的特例</strong>。</p>\\n<p><strong>实际项目里的价值</strong>：直接手写桶排序很少，但&quot;先分桶再局部处理&quot;这个思路到处都是 —— 订单按金额分档统计、日志按时间窗口分片、<code>MapReduce</code> 的 shuffle、数据库的分区表。理解它的价值不在实现，而在这个思路。</p>\\n<p>下面是具体实现：</p>\\n<blockquote>\\n<p>取 n 个桶，根据数组的最大值和最小值确认每个桶存放的数的区间，将元素插入到相应的桶里，最后再合并各个桶。</p>\\n<p>桶排序是计数排序的升级版。它利用了函数的映射关系，高效与否的关键就在于这个映射函数的确定。 为了使桶排序更加高效，我们需要做到这两点：</p>\\n<ul>\\n<li>在额外空间充足的情况下，尽量增大桶的数量。</li>\\n<li>使用的映射函数能够将输入的N个数据均匀的分配到K个桶中。</li>\\n</ul>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function bucketSort(nums) {\\n  // 桶的个数，只要是正数都行\\n  var num = 5\\n  var max = Math.max(...nums)\\n  var min = Math.min(...nums)\\n  // 计算每个桶存放的数值范围，至少为 1\\n  var range = Math.ceil((max - min) / num) || 1\\n  // 创建二维数组，第一维表示第几个桶，第二维表示桶里放的数\\n\\tvar arr = Array.from(Array(num)).map(() =&gt; Array().fill(0))\\n  nums.forEach(val =&gt; {\\n  \\t// 计算元素应该分布在哪个桶\\n   \\tlet index = parseInt((val - min) / range);\\n    // 防止index越界，例如当[5,1,1,2,0,0]时index会出现5\\n    index = index &gt;= num ? num - 1 : index;\\n    let temp = arr[index];\\n    // 插入排序，将元素有序插入到桶中\\n    let j = temp.length - 1;\\n    while (j &gt;= 0 &amp;&amp; val &lt; temp[j]) {\\n    \\ttemp[j + 1] = temp[j];\\n      j--;\\n    }\\n    temp[j + 1] = val;\\n\\t});\\n  // 修改回原数组\\n  var res = [].concat.apply([], arr);\\n  nums.forEach((val, i) =&gt; {\\n  \\tnums[i] = res[i];\\n  });\\n  return nums;\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>风控页面要排序一批金额数据，候选人说“桶排序就是把每个金额当成一个桶”，这段描述哪里混淆了计数排序，遇到浮点金额时会怎样？</strong></p>\\n<p>桶排序的一个桶对应一段值域，桶内仍需排序；每个值对应一个计数位置描述的是计数排序。浮点金额可以按区间进入桶，而不能直接作为有限整数下标；若区间映射设计不当，桶排序仍可能严重失衡。</p>\\n</li>\\n<li>\\n<p><strong>报表页要在浏览器中排序 <code>10 万</code> 条近似均匀分布的评分，采用等宽桶并在桶内用插入排序，你会怎样确定桶数和处理最大值边界？</strong></p>\\n<p>可先以平均每桶约 <code>1～10</code> 个元素为目标估算桶数，再结合内存与空桶遍历成本调整。索引应写成 <code>Math.min(Math.floor((v - min) / size), bucketCount - 1)</code>，否则最大值会得到越界下标；桶过多也会浪费空间。</p>\\n</li>\\n<li>\\n<p><strong>同一套排序从均匀评分改成大量集中在 <code>99～100</code> 的活动分数，产品又要求保持稳定性，原实现还能直接沿用吗？</strong></p>\\n<p>不能只沿用等宽分桶，因为数据会集中到少数桶，桶内插入排序可能接近对整个数组排序，最坏达到 <code>O(n²)</code>。可依据已知分布按分位数设置桶边界，并保留元素入桶顺序、使用稳定的桶内排序；稳定性最终取决于桶内算法。</p>\\n</li>\\n<li>\\n<p><strong>线上排序偶发丢失最大金额，日志显示分配阶段出现 <code>buckets[bucketCount]</code>，同时全相等数据有时得到除零结果，你会先检查哪两处？</strong></p>\\n<p>先检查最大值映射是否被钳制到 <code>bucketCount - 1</code>，因为 <code>(max - min) / size</code> 会正好等于桶数。再检查 <code>min === max</code> 是否提前返回，否则桶宽为 <code>0</code> 会让索引计算失效；修复边界后还应验证负数、浮点数和空数组。</p>\\n</li>\\n<li>\\n<p><strong>数据平台在计数排序与桶排序之间争论：输入是值域很大的浮点数，但分布大致均匀，你会支持哪一个，代价是什么？</strong></p>\\n<p>更适合选择桶排序，因为计数排序要求可直接索引的有限整数值域，面对大值域浮点数并不合适。桶排序能按区间映射这些数据，但性能押注于分布和桶数选择，还需要 <code>O(n + k)</code> 额外空间；分布未知时不能承诺线性表现。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"基数排序\\"><a class=\\"header-anchor\\" href=\\"#基数排序\\">#</a> 基数排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>思路：按数位排序 —— <strong>从最低位到最高位</strong>（LSD），每位用一次<strong>稳定的计数排序</strong>分配，排完 d 轮即有序</li>\\n<li>复杂度 <code>O(d × (n + k))</code>（d 是位数，k 是每位取值范围）；空间 <code>O(n + k)</code>；<strong>稳定</strong></li>\\n<li>⚠️ <strong>子过程必须稳定，这是正确性前提不是优化</strong> —— 高位排序时要保留低位已排好的顺序</li>\\n<li>为什么从低位开始：低位先排、高位后排时，高位相同的元素自动保持低位顺序，一遍走完即正确、无需回溯</li>\\n<li><code>MSD</code>（从高位开始）需要对每个分组递归处理，实现复杂，但处理变长字符串时能提前剪枝</li>\\n<li>什么时候比快排快：<strong><code>d</code> 远小于 <code>log n</code> 时</strong> —— 排一亿个 11 位手机号是 <code>O(11n)</code> vs 快排约 <code>O(27n)</code></li>\\n</ul>\\n<p><strong>基数排序会从最低位到最高位逐轮排序，每一轮都必须使用稳定的排序过程。</strong> 因为处理高位时要保留低位已经排好的相对顺序，所以稳定性是正确性前提，不只是性能优化。它的时间复杂度是 <code>O(d × (n + k))</code>，空间复杂度是 <code>O(n + k)</code>，适合整数、定长字符串和日期等可按位拆分的数据。当 <code>d</code> 较小且数据量大时可能比快排更合适，但它需要额外空间，也不适合位数很长或无法按位拆分的数据。</p>\\n</section>\\n<p><strong>思路</strong>：不整体比较，而是<strong>按数位一轮轮排序</strong>。从最低位（个位）开始，每一位用一次<strong>稳定的计数排序</strong>做分配，全部位排完后整体有序。这叫 <code>LSD</code>（Least Significant Digit）方式。</p>\\n<pre class=\\"language-js\\"><code>function radixSort(arr) {\\n  if (arr.length &lt;= 1) return arr\\n  const max = Math.max(...arr)\\n  for (let exp = 1; Math.floor(max / exp) &gt; 0; exp *= 10) {\\n    arr = countingSortByDigit(arr, exp)\\n  }\\n  return arr\\n}\\n\\nfunction countingSortByDigit(arr, exp) {\\n  const count = new Array(10).fill(0)\\n  for (const v of arr) count[Math.floor(v / exp) % 10]++\\n  for (let i = 1; i &lt; 10; i++) count[i] += count[i - 1]\\n\\n  const res = new Array(arr.length)\\n  // ⚠️ 必须倒序 —— 保证稳定性，这是基数排序正确性的前提\\n  for (let i = arr.length - 1; i &gt;= 0; i--) {\\n    const d = Math.floor(arr[i] / exp) % 10\\n    res[--count[d]] = arr[i]\\n  }\\n  return res\\n}\\n</code></pre>\\n<table>\\n<thead>\\n<tr>\\n<th>指标</th>\\n<th>值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>时间复杂度</td>\\n<td><code>O(d × (n + k))</code>，d = 最大位数，k = 每位取值范围（十进制为 10）</td>\\n</tr>\\n<tr>\\n<td>空间复杂度</td>\\n<td><code>O(n + k)</code></td>\\n</tr>\\n<tr>\\n<td>稳定性</td>\\n<td><strong>稳定</strong></td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>两个必须讲清的点：</strong></p>\\n<p><strong>① 为什么从低位开始（LSD）？</strong></p>\\n<p>因为低位先排、高位后排时，<strong>高位相同的元素会自动保持低位排好的顺序</strong> —— 这正是稳定排序的定义。所以一遍走下来结果就是正确的，不需要任何回溯。</p>\\n<p>如果从高位开始（<code>MSD</code>），高位排完后必须对每个分组<strong>递归</strong>处理低位，实现复杂得多（但 <code>MSD</code> 在处理变长字符串时有优势，可以提前剪枝）。</p>\\n<p><strong>② 为什么子过程必须是稳定排序？</strong></p>\\n<p>这是基数排序<strong>正确性的前提</strong>，不是优化选项。高位那一轮排序时必须保留低位已排好的相对顺序 —— 比如 <code>23</code> 和 <code>21</code> 在个位排序后 <code>21</code> 在前，十位都是 2 时这个顺序不能被打乱。用不稳定的排序（快排）做子过程，整个算法直接失效。</p>\\n<p>这也解释了为什么计数排序回填时&quot;必须倒序遍历&quot;——在这里它是硬性要求。</p>\\n<p><strong>什么时候比快排快？</strong></p>\\n<p>当 <code>d</code> 远小于 <code>log n</code> 时。排一亿个手机号（11 位定长）：基数排序 <code>O(11n)</code>，快排约 <code>O(27n)</code>——基数明显更快。但如果是长度不一的长字符串，<code>d</code> 很大就不划算了。</p>\\n<p><strong>实际限制</strong>：需要 <code>O(n)</code> 额外空间、缓存局部性不如快排、只适用于可按位拆分的数据（整数、定长字符串、日期）。所以通用场景仍是快排/TimSort 的天下。</p>\\n<p>下面是具体实现：</p>\\n<blockquote>\\n<p>使用十个桶 0-9，把每个数从低位到高位根据位数放到相应的桶里，以此循环最大值的位数次。但只能排列正整数，因为遇到负号和小数点无法进行比较。</p>\\n<p>基数排序有两种方法：</p>\\n<ul>\\n<li>MSD 从高位开始进行排序</li>\\n<li>LSD 从低位开始进行排序</li>\\n</ul>\\n<p>基数排序 vs 计数排序 vs 桶排序：</p>\\n<p>这三种排序算法都利用了桶的概念，但对桶的使用方法上有明显差异：</p>\\n<ul>\\n<li>基数排序：根据键值的每位数字来分配桶</li>\\n<li>计数排序：每个桶只存储单一键值</li>\\n<li>桶排序：每个桶存储一定范围的数值</li>\\n</ul>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function radixSort(nums) {\\n  // 计算位数\\n  function getDigits(n) {\\n  \\tvar sum = 0;\\n    while (n) {\\n    \\tsum++;\\n      n = parseInt(n / 10);\\n    }\\n    return sum;\\n  }\\n  // 第一维表示位数即0-9，第二维表示里面存放的值\\n  var arr = Array.from(Array(10)).map(() =&gt; Array());\\n  var max = Math.max(...nums);\\n  var maxDigits = getDigits(max);\\n  for (var i = 0, len = nums.length; i &lt; len; i++) {\\n  \\t// 用0把每一个数都填充成相同的位数\\n    nums[i] = (nums[i] + &#39;&#39;).padStart(maxDigits, 0);\\n    // 先根据个位数把每一个数放到相应的桶里\\n    var temp = nums[i][nums[i].length - 1];\\n    arr[temp].push(nums[i]);\\n  }\\n  // 循环判断每个位数\\n  for (var i = maxDigits - 2; i &gt;= 0; i--) {\\n  \\t// 循环每一个桶\\n    for (var j = 0; j &lt;= 9; j++) {\\n    \\tvar temp = arr[j]\\n      var len = temp.length;\\n      // 根据当前的位数i把桶里的数放到相应的桶里\\n      \\twhile (len--) {\\n          var str = temp[0];\\n          temp.shift();\\n          arr[str[i]].push(str);\\n       \\t}\\n      }\\n    }\\n    // 修改回原数组\\n    var res = [].concat.apply([], arr);\\n    nums.forEach((val, index) =&gt; {\\n    \\tnums[index] = +res[index];\\n    });\\n    return nums;\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>订单页用 <code>LSD</code> 基数排序处理编号，候选人想把每一位的计数排序替换成更熟悉的快排；十位相同但个位不同的编号会出现什么风险？</strong></p>\\n<p>不能直接替换成不稳定的快排，因为处理高位时必须保留低位已经形成的相对顺序。十位相同的编号若被任意交换，个位轮次的结果就会丢失，最终整体次序可能错误；稳定子过程是正确性前提，不是性能优化。</p>\\n</li>\\n<li>\\n<p><strong>前端工具要排序一批非负整数，计数回填代码却从数组头部向后遍历，你会怎样修改，并说明它为什么影响整个基数排序？</strong></p>\\n<p>应从原数组末尾向前回填，并用累计计数确定结果位置，例如写入 <code>res[--count[d]]</code>。这样同一数位的元素才能保持原有先后关系，供下一轮高位排序继承；正向回填若未另行保证稳定性，会破坏 <code>LSD</code> 链条。</p>\\n</li>\\n<li>\\n<p><strong>数据源从固定长度的正整数改成变长字符串，团队仍坚持从最低位补齐后逐轮排序，你会考虑切到 <code>MSD</code> 吗？</strong></p>\\n<p>可以评估 <code>MSD</code>，因为它从高位分组后递归处理低位，对变长字符串可能提前剪枝。代价是实现和分组递归明显更复杂，不再是简单地循环全部位；若继续使用 <code>LSD</code>，还必须明确定义缺失位、字符顺序及稳定分配规则。</p>\\n</li>\\n<li>\\n<p><strong>线上任务把负数和小数直接交给当前十进制实现，结果出现错误桶号或字符位比较异常，你会如何定位适用边界？</strong></p>\\n<p>先确认当前取位逻辑 <code>Math.floor(v / exp) % 10</code> 只按非负整数设计，负号与小数点并不是可直接排序的数字位。应在入口限制数据，或额外设计符号、整数位和小数位的规范化方案；没有完整映射规则时，不应宣称实现支持负数或小数。</p>\\n</li>\\n<li>\\n<p><strong>基础设施组要在通用排序和基数排序间选型：键是定长手机号，但内存预算紧张，你会如何做取舍？</strong></p>\\n<p>定长、可按位拆分的手机号符合基数排序条件，其复杂度为 <code>O(d × (n + k))</code>，有机会避免通用比较排序的比较成本。它仍需 <code>O(n + k)</code> 辅助空间，且缓存局部性可能不如通用排序；内存预算紧张时，这些代价可能比理论时间优势更关键。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);