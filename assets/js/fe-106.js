(window.webpackJsonp=window.webpackJsonp||[]).push([[106],{1213:function(o){o.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：状态管理的核心是把状态变化约束成可追踪的数据流，中间件在派发边界扩展异步、日志和副作用</li>\\n<li>原理主线：围绕 「一、定义constants」、「二、定义actionCreator」、「三、定义reducer」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：讲解了如何在React项目中结合Redux进行状态管理，通过TODO应用实例，介绍了常量、action creator、reducer等核心概念及实现步骤，帮助开发者掌握React与Redux集成的实战方法。</li>\\n<li>边界与代价：集中式状态不是越多越好；可变性、订阅粒度和异步取消决定性能与可维护性</li>\\n<li>工程落地：选型要从状态归属、更新频率、调试需求和团队约束出发，而不是只比较 API 数量</li>\\n</ul>\\n<p><strong><code>React</code> 结合 <code>Redux</code> 的关键，是让组件只负责交互和展示，把状态变化统一交给 <code>action</code>、<code>reducer</code> 与 <code>store</code>。</strong> 用户操作经 <code>dispatch</code> 发出 <code>action</code>，<code>reducer</code> 根据类型返回新状态，<code>connect</code> 再把所需状态和操作映射成组件属性。以 <code>TODO</code> 为例，列表和筛选条件可拆成两个 <code>reducer</code>，再用 <code>combineReducers</code> 合并。日志等能力放在中间件层，展示组件本身不必直接管理这些数据。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文若分析 <code>ReactDOM.render</code>、旧生命周期或栈调和，应把它视为理解架构演进的历史路径。<code>React 19</code> 已移除 <code>ReactDOM.render</code>，当前客户端入口使用 <code>createRoot</code>；并发渲染也必须区分可中断的渲染阶段与同步提交阶段。迁移前对照 <a href=\\"https://react.dev/blog/2024/04/25/react-19-upgrade-guide\\">React 19 官方升级指南</a>。</p>\\n<blockquote>\\n<p>以<code>TODO</code>为例分析，实际开发中并不是那么简单，下面的原型只是开发中的一个原型，这个简单的例子，有助于掌握数据处理传递的原则。</p>\\n</blockquote>\\n<h2 id=\\"一、定义constants\\"><a class=\\"header-anchor\\" href=\\"#一、定义constants\\">#</a> 一、定义constants</h2>\\n<blockquote>\\n<p>这一步不是必须的</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>/**\\n * 常量统一保存，便于管理\\n */\\nexport const ADD_TODO = &#39;ADD_TODO&#39;;\\nexport const TOGGLE_TODO = &#39;TOGGLE_TODO&#39;;\\nexport const SET_VISIBILITY = &#39;SET_VISIBILITY&#39;;\\n\\n//controll todo wheher show or hide\\nexport const SHOW_ALL = &#39;SHOW_ALL&#39;;\\nexport const SHOW_ACTIVE = &#39;SHOW_ACTIVE&#39;;\\nexport const SHOW_COMPLETED = &#39;SHOW_COMPLETED&#39;;\\n</code></pre>\\n<h2 id=\\"二、定义actioncreator\\"><a class=\\"header-anchor\\" href=\\"#二、定义actioncreator\\">#</a> 二、定义actionCreator</h2>\\n<pre class=\\"language-javascript\\"><code>/**\\n * 定义action creator\\n */\\n\\nimport * as actionType from &#39;../constant/index&#39;;\\n\\nlet nextTodo = 0;\\n\\nexport const addTodo = (text)=&gt;({\\n  type:actionType.ADD_TODO,\\n  id:nextTodo++,\\n  text\\n})\\n\\nexport const toggleTodo = (id)=&gt;({\\n  type:actionType.TOGGLE_TODO,\\n  id\\n})\\n\\nexport const setVisibilityFilter = (filter)=&gt;{\\n  return {\\n    type:actionType.SET_VISIBILITY,\\n    filter\\n  }\\n}\\n</code></pre>\\n<h2 id=\\"三、定义reducer\\"><a class=\\"header-anchor\\" href=\\"#三、定义reducer\\">#</a> 三、定义reducer</h2>\\n<p><strong>拆分reducer</strong></p>\\n<ul>\\n<li><code>SetVisibility.js</code></li>\\n</ul>\\n<pre class=\\"language-javascript\\"><code>/**\\n * 处理TODO可见与不可见的reducer\\n */\\nimport * as actionType from &#39;../constant/index&#39;;\\n\\n// 初始状态是自己设置的 后面的状态会转化\\n// 接收当前状态(设置默认的过滤SHOW_ALL，如设置某些选项卡的active一样)，和action返回新的state\\nexport const visibilityFilter = (state=&#39;SHOW_ALL&#39;,action)=&gt;{\\n  switch(action.type) {\\n    case actionType.SET_VISIBILITY:\\n      return action.filter;\\n    default:\\n      return state;\\n  }\\n}\\n</code></pre>\\n<ul>\\n<li><code>addTodo.js</code></li>\\n</ul>\\n<pre class=\\"language-javascript\\"><code>/**\\n * 定义处理action的reducers\\n */\\nimport * as actionType from &#39;../constant/index&#39;;\\n\\n//传入当前的状态空数、action\\nexport const todos = (state = [],action)=&gt;{\\n  switch(action.type){ // 匹配用户触发的actionType\\n    case actionType.ADD_TODO:\\n    // 合并上一次的状态和当前的状态 返回todos数组\\n      return [\\n        ...state,//把数组展开合并\\n        {\\n          id:action.id,\\n          text:action.text,\\n          completed:false//用户控制TODO是否处于点击完成的状态 默认false 没点击\\n        }\\n      ]\\n      // TODO列表来回切换 遍历add_todo返回的数组 通过completed来判断\\n    case actionType.TOGGLE_TODO:\\n      return state.map(todo=&gt;(todo.id===action.id)?{...todo,completed:!todo.completed}:todo)\\n    default:\\n      return state;//匹配不到返回state\\n  }\\n}\\n</code></pre>\\n<p><strong>合并reducer</strong></p>\\n<pre class=\\"language-javascript\\"><code>/**\\n * 合并reducers\\n */\\n\\nimport { combineReducers } from &#39;redux&#39;\\nimport {todos} from &#39;./addTodo&#39;;\\nimport {visibilityFilter} from &#39;./SetVisibility&#39;;\\n\\nexport default combineReducers({\\n  todos, //这些键其实就是被拆分的状态，后面在容器组件中需要通过connect链接\\n  visibilityFilter\\n});\\n</code></pre>\\n<h2 id=\\"四、定义store\\"><a class=\\"header-anchor\\" href=\\"#四、定义store\\">#</a> 四、定义store</h2>\\n<pre class=\\"language-javascript\\"><code>import { applyMiddleware, createStore } from &#39;redux&#39;;\\nimport reducer from &#39;../reducers/index&#39;;\\nimport logger from &#39;redux-logger&#39;;\\n\\n// 创建store 用来存储状态\\nexport const store = createStore(\\n  reducer,\\n  applyMiddleware(logger) //处理日志中间件\\n)\\n</code></pre>\\n<h2 id=\\"五、结合react-redux\\"><a class=\\"header-anchor\\" href=\\"#五、结合react-redux\\">#</a> 五、结合react-redux</h2>\\n<blockquote>\\n<p>这里忽略展示组件，完成源码看文章结尾</p>\\n</blockquote>\\n<ul>\\n<li>首先我们在<code>container</code>组件中处理好之前分解合并的那些<code>reducer</code>的键，然后在通过<code>connect</code>链接，传递给展示组件的属性使用</li>\\n</ul>\\n<p><strong>容器组件处理</strong></p>\\n<blockquote>\\n<p>react-todos/src/container/FilterLink.js</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>// 处理数组过滤\\n\\nimport { connect } from &#39;react-redux&#39;;\\nimport Link from &#39;../components/Link&#39;;\\nimport { setVisibilityFilter } from &#39;../actions/index&#39;;\\n\\n// 这里的ownProps指的是 FilterLink\\n// 这里的state其实就是之前分解的todos，visibilityFilter\\nconst mapStateToProps = (state, ownProps) =&gt; {\\n  return {\\n    active:ownProps.filter === state.visibilityFilter\\n  }\\n}\\n\\n/**\\n 如果mapDispatchToProps是一个函数，会得到dispatch和ownProps（容器组件的props对象）两个参数。\\n */\\nconst mapDispatchToProps = (dispatch, ownProps) =&gt; ({\\n// 这里处理对应的事件，传递给展示组件的属性\\n  onClick: () =&gt; {\\n      dispatch(setVisibilityFilter(ownProps.filter))\\n  }\\n})\\n\\nexport default connect(\\n  mapStateToProps,\\n  mapDispatchToProps\\n)(Link);\\n</code></pre>\\n<ul>\\n<li>分析\\n<ul>\\n<li>这里通过<code>connect</code>组件把之前<code>reducer</code>处理的那些状态链接</li>\\n</ul>\\n</li>\\n</ul>\\n<pre class=\\"language-javascript\\"><code>\\nexport default combineReducers({\\n  todos, //这些键其实就是被拆分的状态，后面在容器组件中需要通过connect链接\\n  visibilityFilter\\n});\\n\\n</code></pre>\\n<blockquote>\\n<p>react-todos/src/container/VisibilityTodoList.js</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>/**\\n * 处理可见于不可见组件的逻辑\\n */\\nimport {connect} from &#39;react-redux&#39;;\\nimport TodoList from &#39;../components/TodoList&#39;;\\nimport {toggleTodo} from &#39;../actions/index&#39;;\\nimport * as actionType from &#39;../constant/index&#39;;\\nconsole.log(toggleTodo)\\n\\n// todos是返回的数组，filter是过滤的选项如SHOW_ALL SHOW_ACTIVE.\\nconst getVisibilityTodos = (todos,filter)=&gt;{\\n  switch(filter) {\\n    case actionType.SHOW_ALL:\\n      return todos;\\n    case actionType.SHOW_ACTIVE:\\n      return todos.filter(t =&gt; !t.completed);\\n    case actionType.SHOW_COMPLETED:\\n      return todos.filter(t =&gt; t.completed);\\n    default:\\n      throw new Error(&#39;未知的&#39;+filter);\\n  }\\n}\\n\\n// 把状态转化为展示组件的属性转递过去\\n/**\\n *\\n * @param {*} state 也就是\\n * export default CombineReducers({\\n  Todos,\\n  SetVisibility\\n  *});\\n * @param {*} ownProps 返回的容器组件本身的参数 如&lt;Filter name=&quot;poetries&quot;&gt;此时的ownProps就是name了\\n */\\n\\nconst mapStateToProps = (state) =&gt; {\\n  return {\\n    todos: getVisibilityTodos(state.todos,state.visibilityFilter),\\n    count:state.todos.length\\n  }\\n}\\n\\n/**\\n * 如果mapDispatchToProps是一个对象，它的每个键名也是对应 UI 组件的同名参数，键值应该是一个函数，会被当作 Action creator ，返回的 Action 会由 Redux 自动发出\\n */\\nconst mapDispatchToProps = {\\n    onTodoClick: toggleTodo\\n}\\n\\nexport default connect(\\n  mapStateToProps,\\n  mapDispatchToProps\\n)(TodoList)\\n\\n</code></pre>\\n<blockquote>\\n<p>react-todos/src/container/addTodos.js</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>/**\\n * Addtodo的处逻辑\\n */\\nimport React, { Component } from &#39;react&#39;;\\nimport {addTodo} from &#39;../actions/index&#39;;\\nimport AddTask from &#39;../components/addTodo&#39;;\\nimport {connect} from &#39;react-redux&#39;;\\n\\nconst mapStateToProps = (state, ownProps) =&gt; ({\\n  \\n})\\n\\nconst mapDispatchToProps = {\\n  addTodo:addTodo\\n}\\n\\nexport default connect(\\n  mapStateToProps,\\n  mapDispatchToProps\\n)(AddTask);\\n\\n</code></pre>\\n<blockquote>\\n<p>到此分析完毕，展示组件就不分析了，展示组件本身是没有数据的，需要container处理传递</p>\\n</blockquote>\\n<p><strong>完整的源码</strong></p>\\n<blockquote>\\n<p><a href=\\"https://github.com/poetries/react-todos\\">https://github.com/poetries/react-todos</a></p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>Todo</code> 页把 <code>combineReducers</code> 后的状态当成数组传给 <code>connect</code>，导致 <code>state.filter</code> 为 <code>undefined</code>；你会怎样指出状态读取错在哪里？</strong></p>\\n<p>容器应按 <code>reducer</code> 注册时的键读取状态，例如 <code>state.todos</code> 与 <code>state.visibilityFilter</code>，不能忽略组合后的对象结构。<code>combineReducers</code> 的键就是容器选择状态的边界；键名或层级不一致时，展示组件拿到的派生属性必然错误。</p>\\n</li>\\n<li>\\n<p><strong>筛选栏有三个 <code>FilterLink</code>，每个链接都要判断自身是否激活并派发筛选动作，你会怎样使用 <code>ownProps</code>？</strong></p>\\n<p>在 <code>mapStateToProps</code> 中比较 <code>ownProps.filter</code> 与 <code>state.visibilityFilter</code>，生成展示组件需要的 <code>active</code>。在函数形式的 <code>mapDispatchToProps</code> 中闭包使用同一 <code>ownProps.filter</code> 并派发 <code>setVisibilityFilter</code>；复用容器时要保证该属性稳定存在。</p>\\n</li>\\n<li>\\n<p><strong>团队把 <code>Todo</code> 列表从两个筛选项扩展到“全部、未完成、已完成”，有人想把过滤逻辑塞进无数据的展示组件，你如何划分职责？</strong></p>\\n<p>过滤逻辑应留在容器的 <code>getVisibilityTodos</code> 和 <code>mapStateToProps</code> 中，展示组件只接收筛选后的 <code>todos</code> 与 <code>count</code>。未知筛选值应显式报错，而不是静默展示错误集合；代价是容器需要维护状态到视图属性的映射。</p>\\n</li>\\n<li>\\n<p><strong>点击 <code>Todo</code> 后 <code>action</code> 已派发，但列表状态没有变化；你会沿 <code>connect</code> 链路检查哪些位置？</strong></p>\\n<p>先确认对象形式的 <code>mapDispatchToProps</code> 是否把 <code>toggleTodo</code> 暴露为展示组件实际调用的 <code>onTodoClick</code>，再检查 <code>reducer</code> 是否更新 <code>state.todos</code>。随后核对 <code>mapStateToProps</code> 是否以新 <code>todos</code> 和 <code>visibilityFilter</code> 重新计算列表，避免只盯着展示组件。</p>\\n</li>\\n<li>\\n<p><strong>新增任务组件不读取任何状态，只需要派发 <code>addTodo</code>；还要不要保留空的 <code>mapStateToProps</code>？</strong></p>\\n<p>不需要为形式完整而保留返回空对象的映射，可以让 <code>connect</code> 只承担 <code>action</code> <code>creator</code> 到属性的绑定。对象形式的 <code>mapDispatchToProps</code> 会把 <code>addTodo</code> 包装为可直接调用的属性；若组件后来需要状态，再显式增加选择逻辑。</p>\\n</li>\\n<li>\\n<p><strong>同事主张所有容器都使用函数形式的 <code>mapDispatchToProps</code>，但列表只需绑定 <code>toggleTodo</code>，筛选链接还依赖 <code>ownProps</code>，你怎么选？</strong></p>\\n<p>列表可用对象形式 <code>{ onTodoClick: toggleTodo }</code>，让 <code>React</code> <code>Redux</code> 自动派发 <code>action</code> <code>creator</code> 的返回值。筛选链接因动作参数来自 <code>ownProps.filter</code>，函数形式更直接；统一写法不应牺牲参数来源和代码意图。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);