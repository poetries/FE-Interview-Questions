(window.webpackJsonp=window.webpackJsonp||[]).push([[41],{1147:function(e){e.exports=JSON.parse('{"html":"<h2 id=\\"_1-如何理解html语义化\\"><a class=\\"header-anchor\\" href=\\"#_1-如何理解html语义化\\">#</a> 1 如何理解HTML语义化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>语义化是用元素表达内容角色，而不是只用 <code>div</code> 配样式</li>\\n<li>对用户：辅助技术能识别地标、标题层级和控件含义；对机器：搜索引擎更容易理解页面结构</li>\\n<li>对团队：结构自解释，默认行为和键盘能力也更完整</li>\\n<li>选标签看内容含义，不要为了 <code>SEO</code> 滥用 <code>h1</code>、<code>article</code> 或 <code>nav</code></li>\\n</ul>\\n<p><strong><code>HTML</code> 语义化就是让标签直接表达内容和控件的角色，而不是所有结构都用 <code>div</code> 拼出来。</strong> 例如用 <code>button</code> 做提交控件，会自然获得焦点、键盘操作和读屏器可识别的按钮角色，比 <code>&lt;div onclick&gt;</code> 更完整。<code>nav</code>、标题层级和 <code>article</code> 也能帮助辅助技术、搜索引擎及维护者理解页面结构。纯布局容器仍该用 <code>div</code>，不要为了 <code>SEO</code> 滥用 <code>h1</code> 或 <code>section</code>，能用原生元素时也不应只靠 <code>ARIA</code> 补语义。</p>\\n</section>\\n<p>语义化的核心是<strong>让标签本身携带含义</strong>，而不是清一色 <code>div</code> 再靠 <code>class</code> 名去暗示用途。浏览器、搜索引擎和辅助技术读到 <code>&lt;nav&gt;</code> 就知道这是导航，读到 <code>&lt;button&gt;</code> 就知道它可点击、可聚焦、能用回车和空格触发 —— 这些行为是元素自带的，不是你写 <code>JS</code> 补出来的。</p>\\n<p><strong>三层价值，面试按这个顺序讲：</strong></p>\\n<ol>\\n<li><strong>可访问性</strong>：读屏器靠语义构建&quot;地标列表&quot;，用户能直接跳到导航或主内容。<code>&lt;div onclick&gt;</code> 在读屏器里就是一段普通文本，键盘 <code>Tab</code> 也停不上去；换成 <code>&lt;button&gt;</code>，焦点、角色、键盘操作三样全都免费拿到。</li>\\n<li><strong>SEO</strong>：爬虫用标签推断内容权重和结构。<code>&lt;h1&gt;</code> 到 <code>&lt;h6&gt;</code> 的层级、<code>&lt;article&gt;</code> 的边界、<code>&lt;time datetime&gt;</code> 的机器可读时间，都会影响索引和摘要展示。</li>\\n<li><strong>可维护性</strong>：结构自解释。接手的人看到 <code>&lt;aside&gt;</code> 就知道这是次要内容，不用去猜 <code>class=&quot;box2&quot;</code> 是什么。</li>\\n</ol>\\n<p><strong>一个具体对比：</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;!-- ❌ 键盘用不了、读屏器读不出、没有 disabled 语义 --&gt;\\n&lt;div class=&quot;btn&quot; onclick=&quot;submit()&quot;&gt;提交&lt;/div&gt;\\n\\n&lt;!-- ✅ 免费获得：可聚焦、回车/空格触发、disabled 状态、读屏器播报&quot;按钮&quot; --&gt;\\n&lt;button type=&quot;submit&quot;&gt;提交&lt;/button&gt;\\n</code></pre>\\n<p><strong>边界 —— 语义化不等于标签越多越好：</strong></p>\\n<ul>\\n<li>纯粹为了布局包的容器就该用 <code>div</code>，强行套 <code>&lt;section&gt;</code> 反而会在辅助技术里多出一堆无名地标。</li>\\n<li><code>&lt;section&gt;</code> 必须配标题才有意义，没有标题时用 <code>div</code>。</li>\\n<li>别为了 <code>SEO</code> 堆 <code>&lt;h1&gt;</code>：一个页面多个 <code>h1</code> 在 <code>HTML5</code> 里合法，但标题层级错乱会让大纲失效。</li>\\n<li><code>ARIA</code> 是补丁不是替代品 —— 官方规则第一条就是&quot;能用原生元素就别用 <code>role</code>&quot;，因为 <code>role=&quot;button&quot;</code> 只改了播报，键盘行为还得自己补。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>提交按钮用 <code>&lt;div onclick&gt;</code> 实现，鼠标操作正常，但客服反馈键盘按 <code>Tab</code> 找不到它；你会怎样改，为什么只补 <code>role=&quot;button&quot;</code> 不够？</strong></p>\\n<p>应优先换成 <code>&lt;button type=&quot;submit&quot;&gt;</code>，它原生具备焦点、按钮角色、回车与空格触发以及禁用语义。<code>role=&quot;button&quot;</code> 主要改变辅助技术的播报，不会自动补齐焦点和键盘行为；坚持自定义元素就必须自行实现并长期维护这些交互。</p>\\n</li>\\n<li>\\n<p><strong>内容页为了“更语义化”，开发把每个布局容器都改成没有标题的 <code>&lt;section&gt;</code>；代码评审时你会要求回退哪些部分？</strong></p>\\n<p>纯布局容器以及没有独立主题和标题的区域应回退为 <code>&lt;div&gt;</code>，因为 <code>&lt;section&gt;</code> 需要表达有标题的内容分区。滥用结构标签会产生无名区域，反而干扰辅助技术理解页面；语义化追求准确含义，不是特殊标签数量。</p>\\n</li>\\n<li>\\n<p><strong>中后台没有公开搜索入口，产品因此认为 <code>&lt;nav&gt;</code>、标题层级和 <code>&lt;button&gt;</code> 都可以省略；你会用哪些页面角色说明价值？</strong></p>\\n<p>即使不考虑 <code>SEO</code>，键盘用户和读屏器用户仍依赖原生控件、标题层级与地标在复杂后台中移动。开发维护者也能从 <code>&lt;nav&gt;</code>、<code>&lt;aside&gt;</code> 等结构直接判断职责；但纯布局包装仍应保留 <code>&lt;div&gt;</code>，避免为了可读性制造错误语义。</p>\\n</li>\\n<li>\\n<p><strong>文章详情页的读屏器地标列表出现大量无名区域，视觉展示却完全正常；你会按什么顺序排查 <code>HTML</code>？</strong></p>\\n<p>先检查是否存在没有标题的 <code>&lt;section&gt;</code>、重复或错乱的标题层级，以及本应是布局容器却被标成地标的节点。再确认导航、主内容和次要内容的标签是否与职责匹配；视觉正常不能证明语义树正确，修复时也不要靠堆叠 <code>ARIA</code> 掩盖结构错误。</p>\\n</li>\\n<li>\\n<p><strong>设计系统团队坚持用一个 <code>&lt;div&gt;</code> 组件模拟按钮、链接和开关，只通过 <code>role</code> 切换；你会如何评价这项统一方案？</strong></p>\\n<p>这种统一只减少了表面组件数量，却把每种控件的焦点、键盘操作、禁用状态和播报规则都转成自研责任。应以原生 <code>&lt;button&gt;</code>、<code>&lt;a&gt;</code> 等元素作为底座，再统一外观和公共接口；只有原生语义确实无法表达时才谨慎补充 <code>ARIA</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_2-h5的新特性有哪些\\"><a class=\\"header-anchor\\" href=\\"#_2-h5的新特性有哪些\\">#</a> 2 H5的新特性有哪些</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>结构层新增 <code>header</code>、<code>nav</code>、<code>main</code>、<code>article</code> 等语义元素，表单补充新类型和原生校验</li>\\n<li>能力层包括音视频、<code>Canvas</code>、<code>Web Storage</code>、<code>Web Worker</code>、拖放和通信能力</li>\\n<li>面试不要只报 API 名，按「语义、表单、多媒体、存储、线程/通信」分类，并各举一个场景</li>\\n</ul>\\n<p><strong><code>HTML5</code> 的新特性可以归为语义结构、媒体展示、浏览器存储和 <code>Web</code> 应用能力。</strong> 典型能力包括 <code>header</code>、<code>nav</code> 等语义标签，<code>audio</code>、<code>video</code>、<code>Canvas</code>，以及 <code>localStorage</code> 和 <code>sessionStorage</code>。应用层面还增加了 <code>Web Worker</code>、<code>WebSocket</code>、<code>Geolocation</code> 与 <code>History API</code>，其中 <code>Worker</code> 可把复杂计算放到后台线程，再通过 <code>postMessage</code> 返回结果。<code>pushState</code> 和 <code>replaceState</code> 能修改历史记录而不刷新页面，<code>WebSocket</code> 则适合客户端与服务端相互推送消息。</p>\\n</section>\\n<ul>\\n<li>画布(<code>Canvas</code>) API</li>\\n<li>地理(<code>Geolocation</code>) API</li>\\n<li>音频、视频API(<code>audio</code>,<code>video</code>)</li>\\n<li><code>localStorage</code>和<code>sessionStorage</code></li>\\n<li><code>webworker</code>, <code>websocket</code></li>\\n<li>新的一套标签 <code>header</code>,<code>nav</code>,<code>footer</code>,<code>aside</code>,<code>article</code>,<code>section</code></li>\\n<li><code>web worker</code>是运行在浏览器后台的js程序，他不影响主程序的运行，是另开的一个js线程，可以用这个线程执行复杂的数据操作，然后把操作结果通过<code>postMessage</code>传递给主线程，这样在进行复杂且耗时的操作时就不会阻塞主线程了</li>\\n<li><code>HTML5 History</code>两个新增的API：<code>history.pushState</code> 和 <code>history.replaceState</code>，两个 <code>API</code> 都会操作浏览器的历史记录，而不会引起页面的刷新</li>\\n</ul>\\n<blockquote>\\n<p><code>Hash</code>就是<code>url</code> 中看到 <code>#</code> ,我们需要一个根据监听哈希变化触发的事件( <code>hashchange</code>) 事件。我们用 <code>window.location</code>处理哈希的改变时不会重新渲染页面，而是当作新页面加到历史记录中，这样我们跳转页面就可以在 <code>hashchange</code> 事件中注册 <code>ajax</code> 从而改变页面内容。 可以为<code>hash</code>的改变添加监听事件：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>window.addEventListener(&quot;hashchange&quot;, funcRef, false)\\n</code></pre>\\n<ul>\\n<li><code>WebSocket</code> 使用<code>ws</code>或<code>wss</code>协议，<code>Websocket</code>是一个持久化的协议，相对于<code>HTTP</code>这种非持久的协议来说。<code>WebSocket API</code>最伟大之处在于服务器和客户端可以在给定的时间范围内的任意时刻，相互推送信息。<code>WebSocket</code>并不限于以<code>Ajax</code>(或<code>XHR</code>)方式通信，因为<code>Ajax</code>技术需要客户端发起请求，而<code>WebSocket</code>服务器和客户端可以彼此相互推送信息；<code>XHR</code>受到域的限制，而<code>WebSocket</code>允许跨域通信</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// 创建一个Socket实例\\nvar socket = new WebSocket(&#39;ws://localhost:8080&#39;);\\n// 打开Socket\\nsocket.onopen = function(event) {\\n  // 发送一个初始化消息\\n  socket.send(&#39;I am the client and I\\\\&#39;m listening!&#39;);\\n  // 监听消息\\n  socket.onmessage = function(event) {\\n    console.log(&#39;Client received a message&#39;,event);\\n  };\\n  // 监听Socket的关闭\\n  socket.onclose = function(event) {\\n    console.log(&#39;Client notified socket has closed&#39;,event);\\n  };\\n  // 关闭Socket....\\n  //socket.close()\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人把 <code>header</code>、<code>localStorage</code>、<code>WebSocket</code> 都称为“让页面更好看 的 <code>H5</code> 标签”；如果报表页同时用了这三类能力，你会让他怎样纠正分类？</strong></p>\\n<p><code>header</code> 属于语义化元素，<code>localStorage</code> 是客户端存储能力，<code>WebSocket</code> 则提供客户端与服务器的双向持久通信，它们不是同一类“标签”。工程选型要按结构、存储和通信目标分别判断；把名称混为一谈会掩盖兼容、容量和连接管理等不同风险。</p>\\n</li>\\n<li>\\n<p><strong>报表页需要在浏览器中处理大量计算，执行期间输入框明显失去响应；你会把什么工作迁到 <code>Web Worker</code>，主线程怎样拿结果？</strong></p>\\n<p>应把可独立执行的复杂数据计算放进 <code>Web Worker</code>，避免它长期占用页面主线程。工作线程完成后通过 <code>postMessage</code> 把结果传回，主线程只负责更新界面；<code>Worker</code> 适合计算隔离，但任务拆分和消息传递本身也有成本。</p>\\n</li>\\n<li>\\n<p><strong>单页应用点击筛选项后既要改变地址，又不能刷新整页，用户还要求浏览器后退可恢复上一步；你会在 <code>hashchange</code> 与 <code>History</code> <code>API</code> 之间怎样选？</strong></p>\\n<p>若路由状态放在 <code>#</code> 后，可监听 <code>hashchange</code> 并据此更新内容，哈希变化不会触发整页刷新。需要操作正常路径形态时可用 <code>history.pushState</code> 或 <code>replaceState</code> 写入历史记录而不刷新；两种方案都必须同步页面状态与前进后退行为。</p>\\n</li>\\n<li>\\n<p><strong>客服聊天页发送成功，但服务端主动推送的新消息偶发收不到；代码只在 <code>onopen</code> 里调用一次 <code>send</code>，你会检查哪些 <code>WebSocket</code> 生命周期事件？</strong></p>\\n<p>先确认连接是否真正触发 <code>onopen</code>，再检查 <code>onmessage</code> 是否持续注册并正确处理服务端推送，同时通过 <code>onclose</code> 判断连接是否已断开。<code>WebSocket</code> 允许双方主动发送，不代表连接永不失效；生产环境还需围绕关闭状态设计保守的重连和状态恢复策略。</p>\\n</li>\\n<li>\\n<p><strong>实时通知模块在 <code>Ajax</code> 轮询和 <code>WebSocket</code> 之间争论：通知必须由服务端随时推送，但部署环境未必始终支持长连接，你会怎么取舍？</strong></p>\\n<p>需要服务端在任意时刻主动推送时，<code>WebSocket</code> 更贴合双向持续通信，而 <code>Ajax</code> 通常仍由客户端发起请求。若运行环境无法稳定建立 <code>ws</code> 或 <code>wss</code> 连接，就应保留可接受的请求式降级路径；这会增加两套状态同步与异常处理成本。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_3-说一下-html5-drag-api\\"><a class=\\"header-anchor\\" href=\\"#_3-说一下-html5-drag-api\\">#</a> 3 说一下 HTML5 drag api</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>拖动源监听 <code>dragstart</code>，目标区必须在 <code>dragover</code> 调用 <code>preventDefault</code> 才能接收 <code>drop</code></li>\\n<li>数据通过 <code>DataTransfer</code> 传递，文件拖入还能读取 <code>files</code></li>\\n<li>原生拖放在移动端和可访问性上体验有限，复杂交互常改用指针事件实现</li>\\n</ul>\\n<p><strong><code>HTML5 Drag API</code> 的核心是拖动源在 <code>dragstart</code> 写入数据，目标区允许放置后在 <code>drop</code> 读取数据。</strong> 拖动源还会触发 <code>drag</code> 和 <code>dragend</code>，目标区则会经历 <code>dragenter</code>、<code>dragover</code>、<code>dragleave</code> 与 <code>drop</code>。最容易漏的是在 <code>dragover</code> 中调用 <code>preventDefault()</code>，否则浏览器默认不允许放置，<code>drop</code> 就不会触发。应用内数据可通过 <code>DataTransfer</code> 的 <code>setData</code>、<code>getData</code> 传递，桌面文件则从 <code>files</code> 获取；移动端和键盘操作支持较弱时，更适合用 <code>Pointer Events</code> 并提供替代操作。</p>\\n</section>\\n<p>原生拖放有七个事件，分属两个主体，这是最容易记混的地方：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>事件</th>\\n<th>触发主体</th>\\n<th>时机</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>dragstart</code></td>\\n<td>被拖动元素</td>\\n<td>开始拖动，在这里写入数据</td>\\n</tr>\\n<tr>\\n<td><code>drag</code></td>\\n<td>被拖动元素</td>\\n<td>拖动过程中持续触发</td>\\n</tr>\\n<tr>\\n<td><code>dragend</code></td>\\n<td>被拖动元素</td>\\n<td>整个拖放结束（无论成功与否）</td>\\n</tr>\\n<tr>\\n<td><code>dragenter</code></td>\\n<td>目标区</td>\\n<td>拖入目标区</td>\\n</tr>\\n<tr>\\n<td><code>dragover</code></td>\\n<td>目标区</td>\\n<td>在目标区内移动，<strong>必须 <code>preventDefault</code></strong></td>\\n</tr>\\n<tr>\\n<td><code>dragleave</code></td>\\n<td>目标区</td>\\n<td>移出目标区</td>\\n</tr>\\n<tr>\\n<td><code>drop</code></td>\\n<td>目标区</td>\\n<td>在目标区释放，在这里读取数据</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>最关键的一个坑：<code>dragover</code> 里必须调 <code>preventDefault()</code>，否则 <code>drop</code> 永远不会触发。</strong>\\n原因是浏览器对拖放的默认行为是&quot;不允许放置&quot;，你必须显式取消这个默认行为来声明&quot;这里可以放&quot;。九成的&quot;拖过去没反应&quot;都是漏了这一行。</p>\\n<pre class=\\"language-js\\"><code>const box = document.getElementById(&#39;drop-zone&#39;)\\n\\n// 拖动源：把数据写进 DataTransfer\\nitem.addEventListener(&#39;dragstart&#39;, (e) =&gt; {\\n  e.dataTransfer.setData(&#39;text/plain&#39;, item.dataset.id)\\n  e.dataTransfer.effectAllowed = &#39;move&#39;\\n})\\n\\n// 目标区：不 preventDefault 就收不到 drop\\nbox.addEventListener(&#39;dragover&#39;, (e) =&gt; {\\n  e.preventDefault()\\n  e.dataTransfer.dropEffect = &#39;move&#39;\\n})\\n\\nbox.addEventListener(&#39;drop&#39;, (e) =&gt; {\\n  e.preventDefault()               // 阻止浏览器默认打开文件/链接\\n  const id = e.dataTransfer.getData(&#39;text/plain&#39;)\\n  const files = e.dataTransfer.files   // 从桌面拖入的文件在这里\\n})\\n</code></pre>\\n<p><strong><code>DataTransfer</code> 的两个用法</strong>：<code>setData</code>/<code>getData</code> 传应用内的数据（注意 <code>getData</code> 只能在 <code>drop</code> 里读，<code>dragover</code> 时出于安全读不到）；从桌面拖文件进来时用 <code>files</code> 直接拿 <code>FileList</code>，这是做拖拽上传的标准姿势。</p>\\n<p><strong>边界 —— 什么时候不该用原生拖放：</strong></p>\\n<ul>\\n<li><strong>移动端基本不可用</strong>：触摸屏不触发这套事件，要另写一套 <code>Pointer Events</code>。</li>\\n<li><strong>可访问性差</strong>：键盘用户完全没法操作，必须额外提供&quot;上移/下移&quot;按钮之类的替代路径。</li>\\n<li><strong>拖动预览难定制</strong>：只能用 <code>setDragImage</code> 换一张图，做不了复杂的跟随效果。</li>\\n</ul>\\n<p>所以列表排序、看板拖拽这类交互，成熟库基本都改用 <code>Pointer Events</code> 自己实现；原生拖放的主场是<strong>跨应用</strong>的场景 —— 从桌面拖文件进来，或者拖到另一个窗口。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>看板卡片已经设置了 <code>draggable=&quot;true&quot;</code>，拖进目标列时 <code>dragenter</code>、<code>dragover</code> 都有日志，松手却始终没有 <code>drop</code>，你先查哪一行代码？</strong></p>\\n<p>先查目标列的 <code>dragover</code> 是否调用了 <code>preventDefault()</code>，缺少它时浏览器会把该区域视为不可放置，因而不会触发 <code>drop</code>。<code>drop</code> 中通常也要取消默认行为，避免拖入文件或链接时被浏览器直接打开。</p>\\n</li>\\n<li>\\n<p><strong>同事想在 <code>dragover</code> 中通过 <code>dataTransfer.getData(\'text/plain\')</code> 读取卡片编号，并据此实时高亮目标列，为什么一直拿到空字符串？</strong></p>\\n<p>应用内数据应在 <code>dragstart</code> 用 <code>setData</code> 写入，并到 <code>drop</code> 阶段再用 <code>getData</code> 读取；拖动途中出于安全限制通常读不到。实时高亮应依据内存状态或元素标记判断，不能把 <code>dragover</code> 读取传输数据作为前提。</p>\\n</li>\\n<li>\\n<p><strong>文件上传区既支持从桌面拖入文件，也支持站内拖动附件卡片，<code>drop</code> 回调怎样区分这两类数据？</strong></p>\\n<p>桌面文件从 <code>e.dataTransfer.files</code> 获取 <code>FileList</code>，站内卡片则读取 <code>setData</code> 写入的自定义标识。两条路径都应在 <code>dragover</code> 和 <code>drop</code> 取消默认行为，并校验文件类型或卡片标识，不能仅凭拖放动作就信任输入。</p>\\n</li>\\n<li>\\n<p><strong>设计稿要求拖动卡片经过内部按钮时，投放区高亮不能反复闪烁，但线上日志显示 <code>dragleave</code> 被连续触发，你会怎么处理？</strong></p>\\n<p>这是拖动指针跨越目标区子元素时产生的边界事件，不代表真的离开整个投放区。可用进入、离开计数器维护状态，或结合 <code>relatedTarget</code> 判断是否仍在目标区内部；层级复杂时必须覆盖嵌套元素进行验证。</p>\\n</li>\\n<li>\\n<p><strong>同一套看板在桌面浏览器可用，到了触屏平板完全拖不动，同时产品还要求键盘用户能排序，你还会坚持原生拖放吗？</strong></p>\\n<p>不适合继续只用原生拖放，因为触摸屏基本不触发这套事件，键盘用户也没有天然操作路径。应以 <code>Pointer Events</code> 或成熟拖拽库统一鼠标和触摸输入，并提供上移、下移等键盘替代操作，代价是命中检测与状态管理要自行实现。</p>\\n</li>\\n<li>\\n<p><strong>产品同时要做看板排序和“把桌面文件拖进网页上传”，你会统一成一套自研拖拽，还是拆成两套实现？</strong></p>\\n<p>应按场景拆分：看板排序更适合基于 <code>Pointer Events</code> 的实现，桌面文件上传则保留原生拖放并从 <code>DataTransfer.files</code> 取文件。强行统一会丢掉原生跨应用传输能力，或让排序继续受移动端、键盘操作和拖动预览限制。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_4-iframe有那些缺点\\"><a class=\\"header-anchor\\" href=\\"#_4-iframe有那些缺点\\">#</a> 4 iframe有那些缺点</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>iframe</code> 是独立浏览上下文，会额外消耗连接、内存和渲染资源</li>\\n<li>父子页面通信受同源策略限制，跨域通常要用 <code>postMessage</code></li>\\n<li>高度同步、路由历史、焦点、打印和移动端适配都更难处理</li>\\n<li>但它的隔离能力也有价值，第三方支付、地图、沙箱预览仍是合理场景</li>\\n</ul>\\n<p><strong><code>iframe</code> 是独立的浏览上下文，缺点是会增加性能、通信和页面管理成本。</strong> 它有自己的文档与渲染流程，会占用内存、连接等资源，还可能拖延父页面的 <code>load</code> 事件。跨域时父子页面只能通过 <code>postMessage</code> 通信，并且必须校验来源，高度同步、路由历史和移动端滚动也更难处理。需要嵌入支付、地图或沙箱预览时，它的样式和脚本隔离反而很有价值，本质上是用这些成本换强隔离。</p>\\n</section>\\n<p><code>iframe</code> 的所有问题都来自同一个事实：<strong>它是一个独立的浏览上下文</strong>，有自己的 <code>window</code>、<code>document</code>、事件循环和渲染流程。</p>\\n<p><strong>性能成本：</strong></p>\\n<ul>\\n<li>会阻塞父页面的 <code>load</code> 事件 —— 父页面必须等所有 <code>iframe</code> 加载完才算 <code>load</code>，这会拖慢依赖 <code>load</code> 的逻辑和性能指标统计。</li>\\n<li>同域下和主页面共享连接池，<code>iframe</code> 里的请求会挤占主页面的并发名额。</li>\\n<li>每个 <code>iframe</code> 都要一整套渲染资源，内存开销是实打实的；开启站点隔离后跨站 <code>iframe</code> 还会额外占一个进程。</li>\\n</ul>\\n<p><strong>通信成本：</strong></p>\\n<p>同源时可以直接 <code>iframe.contentWindow.xxx</code> 互相访问；跨域就只能 <code>postMessage</code>，而且必须严格校验来源，否则就是安全漏洞：</p>\\n<pre class=\\"language-js\\"><code>// 父页面接收\\nwindow.addEventListener(&#39;message&#39;, (e) =&gt; {\\n  // ⚠️ 这一行不能省，否则任何页面都能给你发消息\\n  if (e.origin !== &#39;https://trusted.example.com&#39;) return\\n  handle(e.data)\\n})\\n\\n// 子页面发送：目标源也要写具体值，别用 &#39;*&#39;\\nparent.postMessage({ type: &#39;resize&#39;, height: document.body.scrollHeight }, &#39;https://parent.example.com&#39;)\\n</code></pre>\\n<p><strong>工程上的麻烦：</strong></p>\\n<ul>\\n<li><strong>高度自适应</strong>：跨域拿不到内容高度，只能靠子页面主动 <code>postMessage</code> 上报，还得配 <code>ResizeObserver</code> 持续同步。</li>\\n<li><strong>路由和历史</strong>：<code>iframe</code> 内的导航会进浏览器历史，用户点后退可能只退了 <code>iframe</code> 而整页不动，体验很怪。</li>\\n<li><strong>SEO</strong>：搜索引擎基本不把 <code>iframe</code> 内容算作当前页内容。</li>\\n<li><strong>移动端适配</strong>：<code>iOS</code> 上 <code>iframe</code> 的滚动行为一直有各种历史问题。</li>\\n</ul>\\n<p><strong>但它的隔离能力是真价值</strong>，别一棍子打死：</p>\\n<p>第三方支付、地图、广告、富文本预览、微前端沙箱都在用它 —— 因为独立上下文意味着<strong>样式不互相污染、<code>JS</code> 崩了不影响主应用、配合 <code>sandbox</code> 属性还能精确限制权限</strong>：</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- 只允许脚本执行，禁止表单提交、弹窗、顶层导航 --&gt;\\n&lt;iframe src=&quot;preview.html&quot; sandbox=&quot;allow-scripts&quot;&gt;&lt;/iframe&gt;\\n</code></pre>\\n<p>所以面试时的正确姿势不是背缺点清单，而是说清&quot;成本换隔离&quot;这个取舍：<strong>需要强隔离且能接受通信成本时，<code>iframe</code> 依然是最省事的方案</strong>；只是想复用一段 UI 就别用它。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>业务负责人说 <code>iframe</code> 只是页面里的一个标签，嵌入五个跨站报表不会明显增加成本；你会用什么工程事实反驳？</strong></p>\\n<p>每个 <code>iframe</code> 都是独立浏览上下文，拥有自己的 <code>window</code>、<code>document</code>、事件循环和渲染资源，因此内存与渲染成本真实存在。跨站场景开启站点隔离后还可能使用额外进程，并且其请求会与主页面争用资源。</p>\\n</li>\\n<li>\\n<p><strong>跨域报表内容会随筛选结果增高，父页面不能读取 <code>contentDocument</code>，又不能接受双滚动条，你会设计怎样的高度同步协议？</strong></p>\\n<p>让子页面用 <code>ResizeObserver</code> 监听内容高度，并通过 <code>postMessage</code> 主动上报；父页面校验消息后更新 <code>iframe</code> 高度。双方都应约定消息类型和高度边界，若第三方页面无法配合，就只能接受固定高度或内部滚动。</p>\\n</li>\\n<li>\\n<p><strong>支付页通过 <code>postMessage</code> 把支付结果发给商城，评审发现接收端只判断了 <code>e.data.type</code>，发送端的目标源还是 <code>\'*\'</code>，风险在哪里？</strong></p>\\n<p>任意页面都可能向接收窗口发送同类型消息，缺少 <code>e.origin</code> 校验会让伪造数据进入业务逻辑；发送时使用 <code>\'*\'</code> 也可能把敏感信息交给错误来源。父页面应匹配可信源，子页面也应写明具体目标源，但消息内容仍需业务校验。</p>\\n</li>\\n<li>\\n<p><strong>用户在内嵌后台里连续跳转三页后点击浏览器后退，外层产品页没变化，只是 <code>iframe</code> 退了一页，产品认为路由坏了，你怎么判断？</strong></p>\\n<p>这是两个浏览上下文各自导航造成的历史记录体验，不一定是路由代码失效。若要求外层地址与子页同步，需要双方通过消息协议显式同步路由和回退行为；协议越复杂，循环更新与状态不一致的风险越高。</p>\\n</li>\\n<li>\\n<p><strong>微前端评审中，安全团队要求第三方代码不能污染主站，体验团队又要求下拉层能跨出子应用边界，你怎么做取舍？</strong></p>\\n<p>安全隔离优先时应选 <code>iframe</code>，它能隔离样式和脚本，并可通过 <code>sandbox</code> 限制表单、弹窗或顶层导航等权限。跨边界浮层和无缝路由会因此变难；若改用共享上下文沙箱换取体验，就不能再声称具有同等级的浏览器隔离。</p>\\n</li>\\n<li>\\n<p><strong>内容团队准备把核心商品介绍放进跨域 <code>iframe</code>，同时要求搜索引擎把这些文字计入商品详情页，你会批准吗？</strong></p>\\n<p>不应把需要归属于当前页面的核心 <code>SEO</code> 内容只放在 <code>iframe</code> 中，因为搜索引擎通常不会将其视为宿主页正文。可嵌入第三方支付、地图或预览等强隔离模块，但核心商品内容应由主文档直接输出。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_5-如何实现浏览器内多个标签页之间的通信\\"><a class=\\"header-anchor\\" href=\\"#_5-如何实现浏览器内多个标签页之间的通信\\">#</a> 5 如何实现浏览器内多个标签页之间的通信</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>同源页面首选 <code>BroadcastChannel</code>，接口直接、消息语义清晰</li>\\n<li><code>storage</code> 事件兼容性广，但只通知其他页面且要借助 <code>localStorage</code></li>\\n<li>共享状态可用 <code>SharedWorker</code>；服务端实时同步可用 <code>WebSocket</code> 或 <code>SSE</code></li>\\n<li>选型先看是否同源、是否需要持久化、是否要跨设备</li>\\n</ul>\\n<p><strong>同源标签页只传通知时优先用 <code>BroadcastChannel</code>，需要兼容旧环境可用 <code>storage</code> 事件。</strong> <code>BroadcastChannel</code> 语义直接，而修改 <code>localStorage</code> 只会通知其他同源页面，连续发送相同值时还要附带时间戳。若多个页面需要共享状态或复用一条 <code>WebSocket</code>，可以使用 <code>SharedWorker</code>，但调试和兼容性成本更高。只有跨设备、跨浏览器同步才需要服务端通过 <code>WebSocket</code> 或 <code>SSE</code> 中转，没必要为本机标签页额外增加服务端压力。</p>\\n</section>\\n<p>先分清两类场景：<strong>同源标签页之间的纯浏览器通信</strong>，和<strong>需要服务端中转的跨设备同步</strong>。面试时先说这个分类，再展开方案。</p>\\n<p><strong>一、<code>BroadcastChannel</code>（同源首选）</strong></p>\\n<p>语义最直接：同一个频道名的所有同源上下文都能收到广播，发送方自己不会收到。</p>\\n<pre class=\\"language-js\\"><code>const channel = new BroadcastChannel(&#39;auth&#39;)\\n\\n// A 标签页退出登录\\nchannel.postMessage({ type: &#39;logout&#39; })\\n\\n// B 标签页监听\\nchannel.onmessage = (e) =&gt; {\\n  if (e.data.type === &#39;logout&#39;) location.href = &#39;/login&#39;\\n}\\n\\n// 页面卸载时关闭\\nwindow.addEventListener(&#39;pagehide&#39;, () =&gt; channel.close())\\n</code></pre>\\n<p><strong>二、<code>storage</code> 事件（兼容性最广）</strong></p>\\n<p>改 <code>localStorage</code> 时，<strong>其他</strong>同源标签页会触发 <code>storage</code> 事件（自己不会触发，这点经常被忘）：</p>\\n<pre class=\\"language-js\\"><code>localStorage.setItem(&#39;msg&#39;, JSON.stringify({ type: &#39;logout&#39;, t: Date.now() }))\\n\\nwindow.addEventListener(&#39;storage&#39;, (e) =&gt; {\\n  if (e.key !== &#39;msg&#39;) return\\n  const data = JSON.parse(e.newValue)\\n})\\n</code></pre>\\n<p>两个坑：一是<strong>同值不触发</strong>，连发两次一样的内容第二次收不到，所以要带时间戳；二是它借用了存储来传消息，本质是&quot;用副作用做通信&quot;，能用 <code>BroadcastChannel</code> 就别用它。</p>\\n<p><strong>三、<code>SharedWorker</code>（需要共享状态时）</strong></p>\\n<p>多个标签页连到同一个 <code>Worker</code> 实例，适合&quot;多个页面共用一条 <code>WebSocket</code> 连接&quot;这类需求 —— 避免开十个标签就建十条连接。代价是调试麻烦、<code>Safari</code> 支持长期缺失。</p>\\n<p><strong>四、服务端通道（<code>WebSocket</code> / <code>SSE</code>）</strong></p>\\n<p>只有在需要<strong>跨设备、跨浏览器</strong>同步时才用。同一台机器的标签页之间用它属于杀鸡用牛刀，还多了一份服务端压力。</p>\\n<p><strong>已经过时的写法</strong>：<code>cookie</code> + <code>setInterval</code> 轮询。它的问题是有延迟、持续消耗 <code>CPU</code>、<code>cookie</code> 还会跟着每个请求上传浪费带宽。老资料里常见，但现在没有理由再用。</p>\\n<p><strong>选型速查</strong>：同源且只是通知 → <code>BroadcastChannel</code>；要兼容很老的浏览器 → <code>storage</code>；要共享一条长连接 → <code>SharedWorker</code>；跨设备 → 服务端推送。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>登录系统用 <code>BroadcastChannel(\'auth\')</code> 广播退出，开发在发送页也等 <code>onmessage</code> 清理状态，结果当前页仍显示已登录，原因是什么？</strong></p>\\n<p><code>BroadcastChannel</code> 的发送方不会收到自己发出的消息，所以当前页不能依赖该回调完成退出。发送页应直接清理本地状态并跳转，再广播通知其他同源标签页；其他窗口未运行或已断开时仍需由鉴权失败兜底。</p>\\n</li>\\n<li>\\n<p><strong>旧浏览器兼容方案用 <code>localStorage.setItem(\'auth-event\',\'logout\')</code> 连续广播两次，第二次其他标签页没有任何反应，你怎么修？</strong></p>\\n<p><code>storage</code> 事件只在其他同源页面触发，而且写入与原值相同的内容不会产生第二次通知。消息中应加入时间戳或唯一编号，使每次写入都变化；它本质上借存储副作用通信，能用 <code>BroadcastChannel</code> 时语义更清晰。</p>\\n</li>\\n<li>\\n<p><strong>客服后台打开十个标签页后建立了十条相同的 <code>WebSocket</code>，服务端要求每个浏览器实例尽量复用一条连接，你会选什么机制？</strong></p>\\n<p>可让多个同源标签页连接同一个 <code>SharedWorker</code>，由它维护共享的 <code>WebSocket</code>，再通过各页面端口分发消息。方案能集中连接和共享状态，但调试更复杂，并且需要评估目标浏览器对 <code>SharedWorker</code> 的支持。</p>\\n</li>\\n<li>\\n<p><strong>用户在普通窗口退出登录，但同一浏览器的无痕窗口和另一台手机仍保持登录，产品认为 <code>BroadcastChannel</code> 丢消息，你怎么解释边界？</strong></p>\\n<p>纯前端标签页通信只覆盖同一浏览器环境内的同源上下文，不能跨普通与无痕隔离边界，也不能跨浏览器或设备。若要求所有终端同步失效，需要服务端撤销会话并通过 <code>WebSocket</code>、<code>SSE</code> 或后续请求鉴权传递状态。</p>\\n</li>\\n<li>\\n<p><strong>线上偶发出现 <code>B</code> 标签页没收到退出广播，之后请求全部返回 <code>401</code>；只增加广播重试能保证正确性吗？</strong></p>\\n<p>不能，广播只能改善标签页间的即时体验，页面休眠、关闭或未监听时都可能错过消息。请求层仍要统一处理 <code>401</code>，清除失效状态并引导重新登录；服务端鉴权才是最终约束，不能把安全正确性押在前端通知上。</p>\\n</li>\\n<li>\\n<p><strong>架构评审有人提议用 <code>cookie</code> 配合每秒 <code>setInterval</code> 检查登录变化，以兼容所有页面，你为什么会否决？</strong></p>\\n<p>这种轮询有同步延迟并持续消耗 <code>CPU</code>，而 <code>cookie</code> 还会随请求上传，增加无关带宽开销。同源通知优先用 <code>BroadcastChannel</code>，兼容旧环境可退到 <code>storage</code>；只有跨设备需求才值得引入服务端通道。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_6-简述一下src与href的区别\\"><a class=\\"header-anchor\\" href=\\"#_6-简述一下src与href的区别\\">#</a> 6 简述一下src与href的区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>src</code> 表示当前元素要嵌入或执行的资源，如图片、脚本、媒体</li>\\n<li><code>href</code> 表示当前文档与外部资源之间的引用关系，如链接和样式表</li>\\n<li>是否阻塞解析不能只看属性名，还要结合元素类型以及 <code>async</code>、<code>defer</code> 等加载策略</li>\\n</ul>\\n<p><strong>简单来说，<code>src</code> 会把外部资源嵌入当前元素，<code>href</code> 则用于建立当前文档或元素与目标资源的关联。</strong> 像图片、脚本和框架会通过 <code>src</code> 下载并应用到标签所在位置，其中脚本的加载、编译和执行可能暂停文档的后续处理。<code>href</code> 常见于锚点和样式表链接，例如 <code>&lt;link&gt;</code> 引用的 <code>CSS</code> 可以并行下载，不会停止当前文档的处理。</p>\\n</section>\\n<ul>\\n<li><code>src</code>用于替换当前元素，<code>href</code>用于在当前文档和引用资源之间确立联系。</li>\\n<li><code>src</code>是<code>source</code>的缩写，指向外部资源的位置，指向的内容将会嵌入到文档中当前标签所在位置；在请求<code>src</code>资源时会将其指向的资源下载并应用到文档内，例如<code>js</code>脚本，<code>img</code>图片和<code>frame</code>等元素</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&lt;script src =”js.js”&gt;&lt;/script&gt;\\n</code></pre>\\n<blockquote>\\n<p>当浏览器解析到该元素时，会暂停其他资源的下载和处理，直到将该资源加载、编译、执行完毕，图片和框架等元素也如此，类似于将所指向资源嵌入当前标签内。这也是为什么将<code>js</code>脚本放在底部而不是头部</p>\\n</blockquote>\\n<ul>\\n<li><code>href</code>是<code>Hypertext Reference</code>的缩写，指向网络资源所在位置，建立和当前元素（锚点）或当前文档（链接）之间的链接，如果我们在文档中添加</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>&lt;link href=”common.css” rel=”stylesheet”/&gt;\\n</code></pre>\\n<blockquote>\\n<p>那么浏览器会识别该文档为css文件，就会并行下载资源并且不会停止对当前文档的处理。这也是为什么建议使用<code>link</code>方式来加载css，而不是使用<code>@import</code>方式。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审里有人说 <code>src</code> 和 <code>href</code> 都只是“资源地址”，所以 <code>&lt;script href=&quot;app.js&quot;&gt;</code> 与 <code>&lt;link src=&quot;theme.css&quot;&gt;</code> 理论上等价，你怎么纠正？</strong></p>\\n<p>两者表达的关系不同：<code>src</code> 指向将嵌入或替换当前元素内容的资源，<code>href</code> 建立当前文档或元素与外部资源的关联。具体标签只识别规范规定的属性，不能因为值都是 <code>URL</code> 就互换使用。</p>\\n</li>\\n<li>\\n<p><strong>首页把同步 <code>&lt;script src=&quot;app.js&quot;&gt;</code> 放在 <code>head</code>，网络稍慢时后面的页面结构迟迟不出现；这和 <code>src</code> 的处理方式有什么关系？</strong></p>\\n<p>解析器遇到普通外部脚本时，需要加载并执行脚本后才能继续处理后续文档，因此头部同步脚本会延后页面结构解析。可调整脚本位置或采用适合依赖关系的异步加载策略，但必须确认执行顺序和 <code>DOM</code> 使用时机。</p>\\n</li>\\n<li>\\n<p><strong>设计系统通过 <code>&lt;link href=&quot;common.css&quot; rel=&quot;stylesheet&quot;&gt;</code> 引入样式，<code>HTML</code> 仍在继续解析，团队便断言它不会影响首屏，这个结论哪里不完整？</strong></p>\\n<p><code>link</code> 建立外部样式表关联，资源可在文档继续处理时并行下载，但样式何时就绪仍会影响页面渲染。不能把“不停止 <code>HTML</code> 处理”直接等同于“不影响首屏”，大型关键样式仍需控制体积和加载时机。</p>\\n</li>\\n<li>\\n<p><strong>线上首屏样式总比预期晚出现，排查发现 <code>main.css</code> 内部又用 <code>@import</code> 引入 <code>theme.css</code>；为什么改成两个 <code>&lt;link&gt;</code> 更合理？</strong></p>\\n<p><code>@import</code> 只有在外层 <code>CSS</code> 被下载并解析到对应规则后才会发现内层资源，容易形成串行请求。两个 <code>&lt;link&gt;</code> 能在 <code>HTML</code> 处理阶段更早暴露资源并行下载；若构建工具已在编译期合并，则不属于浏览器运行时的这类问题。</p>\\n</li>\\n<li>\\n<p><strong>组件负责人为了“统一资源加载 <code>API</code>”，想用 <code>JavaScript</code> 动态创建标签来替代所有 <code>src</code> 和 <code>href</code>，你会接受吗？</strong></p>\\n<p>不应只为表面统一而抹平语义，脚本、图片、框架是嵌入型资源，样式表和超链接则是关联关系，浏览器会据标签与属性采取不同处理。动态创建还会延后资源发现时机，除非确有条件加载需求，否则没有工程收益。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_7-知道的网页制作会用到的图片格式有哪些\\"><a class=\\"header-anchor\\" href=\\"#_7-知道的网页制作会用到的图片格式有哪些\\">#</a> 7 知道的网页制作会用到的图片格式有哪些</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>照片优先 <code>AVIF</code> / <code>WebP</code>，需要广泛兼容时保留 <code>JPEG</code> 回退</li>\\n<li>图标和线稿适合 <code>SVG</code>，透明截图可用 <code>PNG</code>，简单动画可选 <code>WebP</code> 或视频</li>\\n<li>选型看内容类型、透明/动画需求、体积、解码成本和浏览器兼容，而不是只看清晰度</li>\\n</ul>\\n<p><strong>网页常用 <code>AVIF</code>、<code>WebP</code>、<code>JPEG</code>、<code>PNG</code> 和 <code>SVG</code>，具体选择要看内容、体积、透明或动画需求以及兼容性。</strong> 照片和复杂渐变可优先用 <code>AVIF</code> 或 <code>WebP</code>，并保留 <code>JPEG</code> 回退；图标、线稿和 <code>Logo</code> 更适合可无损缩放的 <code>SVG</code>。需要透明时可在 <code>WebP</code> 与 <code>PNG</code> 之间取舍，短动画则优先考虑 <code>WebP</code> 或视频，避免 <code>GIF</code> 的颜色和体积限制。我一般会用 <code>&lt;picture&gt;</code> 提供多格式回退，同时通过 <code>srcset</code> 输出不同尺寸，并给图片写明宽高以减少 <code>CLS</code>。</p>\\n</section>\\n<p>图片选型是在<strong>画质、体积、功能（透明/动画）、兼容性、解码成本</strong>五个维度上取舍，不存在&quot;哪个最好&quot;。</p>\\n<p><strong>按内容类型选：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>内容</th>\\n<th>首选</th>\\n<th>回退</th>\\n<th>理由</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>照片、复杂渐变</td>\\n<td><code>AVIF</code></td>\\n<td><code>WebP</code> → <code>JPEG</code></td>\\n<td>同画质下 <code>AVIF</code> 比 <code>JPEG</code> 小 50% 上下</td>\\n</tr>\\n<tr>\\n<td>图标、线稿、Logo</td>\\n<td><code>SVG</code></td>\\n<td><code>PNG</code></td>\\n<td>矢量无损缩放，能用 <code>CSS</code> 改色，体积极小</td>\\n</tr>\\n<tr>\\n<td>需要透明的截图/插图</td>\\n<td><code>WebP</code></td>\\n<td><code>PNG</code></td>\\n<td><code>PNG</code> 无损但体积大，<code>WebP</code> 有损也支持透明</td>\\n</tr>\\n<tr>\\n<td>短动画</td>\\n<td><code>WebP</code> / 视频</td>\\n<td><code>GIF</code></td>\\n<td><code>GIF</code> 只有 256 色且体积巨大，同样内容视频能小十倍</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>标准写法是让浏览器自己挑：</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;picture&gt;\\n  &lt;source srcset=&quot;hero.avif&quot; type=&quot;image/avif&quot;&gt;\\n  &lt;source srcset=&quot;hero.webp&quot; type=&quot;image/webp&quot;&gt;\\n  &lt;img src=&quot;hero.jpg&quot; alt=&quot;产品主图&quot; width=&quot;1200&quot; height=&quot;600&quot;&gt;\\n&lt;/picture&gt;\\n</code></pre>\\n<p>浏览器取<strong>第一个它认识的</strong> <code>source</code>，都不认就落到 <code>&lt;img&gt;</code>。这套写法不需要任何 <code>JS</code>，也不需要 <code>UA</code> 判断。</p>\\n<p><strong>几个容易忽略的点：</strong></p>\\n<ul>\\n<li><strong><code>width</code>/<code>height</code> 必须写</strong>，哪怕用 <code>CSS</code> 控制实际尺寸 —— 它让浏览器提前算出宽高比预留空间，直接决定 <code>CLS</code> 指标。</li>\\n<li><strong>首屏主图不要加 <code>loading=&quot;lazy&quot;</code></strong>，懒加载会推迟它的请求，反而拖垮 <code>LCP</code>；该给它加 <code>fetchpriority=&quot;high&quot;</code>。</li>\\n<li><strong><code>AVIF</code> 的代价是解码慢</strong>：体积赢了，但在低端安卓上解码时间可能吃掉一部分收益，大图要实测。</li>\\n<li><strong><code>srcset</code> 比格式更值钱</strong>：手机上下 <code>1920</code> 宽的原图，换什么格式都救不回来。先按设备出多档尺寸，再谈格式。</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;img src=&quot;p-800.jpg&quot;\\n     srcset=&quot;p-400.jpg 400w, p-800.jpg 800w, p-1600.jpg 1600w&quot;\\n     sizes=&quot;(max-width: 600px) 100vw, 50vw&quot;\\n     alt=&quot;商品图&quot; width=&quot;800&quot; height=&quot;600&quot; loading=&quot;lazy&quot;&gt;\\n</code></pre>\\n<p>面试官问这题想听的是&quot;你有没有关注过新格式&quot;，但真正加分的是你能说出<strong>格式之外的那几件事</strong>（尺寸自适应、<code>CLS</code>、<code>LCP</code>），因为那才是线上真正的收益来源。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>运营说 <code>AVIF</code> 体积通常更小，所以商品站所有照片、图标和动图都统一转成 <code>AVIF</code>，你会批准这个方案吗？</strong></p>\\n<p>不会按单一格式覆盖所有内容：照片和复杂渐变适合优先尝试 <code>AVIF</code>，图标与 <code>Logo</code> 更适合 <code>SVG</code>，透明插图可选 <code>WebP</code>，短动画还应比较视频。格式选择还受透明、动画、兼容性和解码成本约束，需保留回退。</p>\\n</li>\\n<li>\\n<p><strong>商品主图要同时覆盖支持 <code>AVIF</code>、只支持 <code>WebP</code> 和只能显示 <code>JPEG</code> 的浏览器，又不允许写 <code>UA</code> 判断，你会怎样组织标签？</strong></p>\\n<p>用 <code>&lt;picture&gt;</code> 按 <code>AVIF</code>、<code>WebP</code> 顺序声明带 <code>type</code> 的 <code>&lt;source&gt;</code>，最后用 <code>&lt;img src=&quot;hero.jpg&quot;&gt;</code> 提供 <code>JPEG</code> 回退。浏览器会选择第一个支持的来源，关键是保留 <code>img</code>、准确的 <code>alt</code> 以及明确的宽高。</p>\\n</li>\\n<li>\\n<p><strong>移动端商品列表仍下载 <code>1600px</code> 原图，团队却准备继续调低 <code>WebP</code> 质量来救流量，你会先改什么？</strong></p>\\n<p>先用 <code>srcset</code> 提供多档宽度，并用 <code>sizes</code> 描述图片在不同视口中的实际占用，让浏览器选择合适尺寸。手机持续下载大图时，尺寸浪费往往不是继续换格式能补救的；压低质量还会直接损害画面。</p>\\n</li>\\n<li>\\n<p><strong>上线后商品卡片图片加载时页面不断跳动，图片已经压成很小的 <code>WebP</code>，为什么体积优化没有解决故障？</strong></p>\\n<p>跳动属于布局空间未预留的问题，与文件体积小并不等价。应在 <code>&lt;img&gt;</code> 上写准确的 <code>width</code> 和 <code>height</code>，让浏览器提前计算宽高比；若 <code>CSS</code> 改变展示尺寸，也要保持比例与布局规则一致。</p>\\n</li>\\n<li>\\n<p><strong>性能同学发现首页最大内容图使用了 <code>loading=&quot;lazy&quot;</code>，下载开始得很晚，却有人建议继续压缩图片，你会如何调整？</strong></p>\\n<p>首屏主图不应懒加载，应移除 <code>loading=&quot;lazy&quot;</code>，并可用 <code>fetchpriority=&quot;high&quot;</code> 表达其优先级。之后再比较响应式尺寸与格式收益；<code>AVIF</code> 虽可能更小，但低端设备上的解码成本仍要通过真实环境验证。</p>\\n</li>\\n<li>\\n<p><strong>用户可上传头像，前端为了支持主题换色准备把用户提供的 <code>SVG</code> 直接内联进 <code>DOM</code>，这个选型有什么风险？</strong></p>\\n<p>不应直接内联不可信 <code>SVG</code>，其中可能包含脚本或事件属性，进入 <code>DOM</code> 后会形成脚本执行风险。若不需要操控内部样式，可通过 <code>&lt;img src&gt;</code> 隔离引用；确需内联改色时必须先净化内容并限制来源。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_8-script标签中defer和async的区别\\"><a class=\\"header-anchor\\" href=\\"#_8-script标签中defer和async的区别\\">#</a> 8 script标签中defer和async的区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>两者都会并行下载，不会像普通脚本那样阻塞解析阶段的下载</li>\\n<li><code>async</code> 下载完立刻执行，顺序不可保证，适合无依赖的统计脚本</li>\\n<li><code>defer</code> 等文档解析完成后按文档顺序执行，并早于 <code>DOMContentLoaded</code></li>\\n<li><code>type=&quot;module&quot;</code> 默认具有类似 <code>defer</code> 的行为</li>\\n</ul>\\n<p><strong><code>defer</code> 和 <code>async</code> 都会并行下载外部脚本，区别主要在执行时机和顺序。</strong> <code>async</code> 下载完就执行，可能打断 <code>HTML</code> 解析，也不保证多个脚本的先后顺序，适合统计埋点这类独立脚本。<code>defer</code> 会等文档解析完成，再按文档顺序执行，并且早于 <code>DOMContentLoaded</code>，所以业务脚本通常更适合它。<code>type=&quot;module&quot;</code> 默认具有类似 <code>defer</code> 的行为，而这两个属性对没有 <code>src</code> 的内联脚本无效。</p>\\n</section>\\n<p>三种加载方式的区别，本质是<strong>下载和执行分别在什么时候发生</strong>：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>写法</th>\\n<th>下载</th>\\n<th>执行时机</th>\\n<th>顺序保证</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>&lt;script&gt;</code></td>\\n<td>阻塞 <code>HTML</code> 解析</td>\\n<td>下载完立即执行，执行完才继续解析</td>\\n<td>按文档顺序</td>\\n</tr>\\n<tr>\\n<td><code>&lt;script async&gt;</code></td>\\n<td>并行，不阻塞解析</td>\\n<td><strong>下载完立刻执行</strong>，会打断解析</td>\\n<td>❌ 谁先下完谁先执行</td>\\n</tr>\\n<tr>\\n<td><code>&lt;script defer&gt;</code></td>\\n<td>并行，不阻塞解析</td>\\n<td>等 <code>HTML</code> 解析完再执行，早于 <code>DOMContentLoaded</code></td>\\n<td>✅ 按文档顺序</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><img src=\\"https://s.poetries.top/images/20210314221335.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>蓝色是网络下载，红色是脚本执行，绿色是 <code>HTML</code> 解析</p>\\n</blockquote>\\n<p><strong>为什么普通 <code>script</code> 要阻塞？</strong>\\n因为脚本里可能有 <code>document.write</code>，能改变后面的文档结构，浏览器不敢在脚本执行完之前继续解析。这个历史包袱直接催生了&quot;脚本放 <code>body</code> 底部&quot;这条老规矩 —— 而有了 <code>defer</code> 之后，放 <code>head</code> 里加 <code>defer</code> 反而更好：<strong>下载更早开始，执行时机一样</strong>。</p>\\n<p><strong>怎么选：</strong></p>\\n<ul>\\n<li><strong>有依赖关系、需要操作 <code>DOM</code></strong> → <code>defer</code>。这是业务脚本的默认选择。</li>\\n<li><strong>完全独立、越早跑越好</strong> → <code>async</code>。典型是统计埋点、错误监控 SDK。</li>\\n<li><strong>既想早执行又不能乱序</strong>？没有这种组合，只能拆成&quot;一个 <code>async</code> 的加载器 + 内部自己保证顺序&quot;。</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;head&gt;\\n  &lt;!-- 埋点：独立无依赖，越早越好，乱序无所谓 --&gt;\\n  &lt;script async src=&quot;/analytics.js&quot;&gt;&lt;/script&gt;\\n  &lt;!-- 业务代码：有依赖，要按顺序，要等 DOM --&gt;\\n  &lt;script defer src=&quot;/vendor.js&quot;&gt;&lt;/script&gt;\\n  &lt;script defer src=&quot;/app.js&quot;&gt;&lt;/script&gt;\\n&lt;/head&gt;\\n</code></pre>\\n<p><strong>几个容易踩的点：</strong></p>\\n<ul>\\n<li><code>type=&quot;module&quot;</code> <strong>默认就是 <code>defer</code> 行为</strong>，不用再写 <code>defer</code>；模块脚本加 <code>async</code> 才有意义。</li>\\n<li>两个属性对<strong>内联脚本无效</strong> —— 只有带 <code>src</code> 的外部脚本才认。</li>\\n<li><code>defer</code> 脚本执行在 <code>DOMContentLoaded</code> <strong>之前</strong>，所以 <code>defer</code> 脚本里注册的 <code>DOMContentLoaded</code> 监听器仍然会被触发。</li>\\n<li><code>async</code> 脚本可能在 <code>DOM</code> 还没构建完时就执行，里面直接 <code>document.querySelector</code> 很可能拿到 <code>null</code>。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品详情页在 <code>head</code> 里依次写了 <code>vendor.js</code> 和 <code>app.js</code>，两者都加上 <code>async</code> 后偶发出现 <code>React is not defined</code>，但刷新又可能恢复，为什么？</strong></p>\\n<p>两个 <code>async</code> 脚本按下载完成时间执行，不再保证文档中的先后顺序，所以 <code>app.js</code> 可能抢在 <code>vendor.js</code> 前运行。存在依赖关系的业务脚本应改用 <code>defer</code>，既能并行下载，又会在解析完成后按文档顺序执行；继续用 <code>async</code> 就必须由加载器自行管理依赖。</p>\\n</li>\\n<li>\\n<p><strong>首屏业务脚本原来放在 <code>body</code> 底部，前端负责人想迁到 <code>head</code> 并加 <code>defer</code>，担心脚本会更早执行导致查不到商品节点，这个改动是否安全？</strong></p>\\n<p>带 <code>defer</code> 的外部脚本会在 <code>HTML</code> 解析完成后执行，因此读取页面节点通常是安全的，而且放进 <code>head</code> 能让下载更早开始。多个 <code>defer</code> 脚本仍按文档顺序运行，并且全部早于 <code>DOMContentLoaded</code>；前提是脚本带有 <code>src</code>，因为该属性对内联脚本无效。</p>\\n</li>\\n<li>\\n<p><strong>监控 <code>SDK</code> 必须尽早初始化，但它初始化后还要加载两个有先后依赖的插件，架构师要求所有脚本既尽早执行又绝不乱序，只靠 <code>async</code> 或 <code>defer</code> 能满足吗？</strong></p>\\n<p>单靠一个属性无法同时获得“下载完立即执行”和“跨脚本严格有序”两种语义。可让独立的加载器使用 <code>async</code> 尽早启动，再由加载器按依赖关系串行加载或初始化插件；若插件必须等完整 <code>DOM</code>，则更适合统一改用按顺序执行的 <code>defer</code>。</p>\\n</li>\\n<li>\\n<p><strong>埋点脚本加了 <code>async</code> 后，线上只有部分用户的商品 <code>ID</code> 是空值，网络面板显示脚本请求成功，应该从哪里排查？</strong></p>\\n<p>请求成功不代表执行时页面节点已经存在，<code>async</code> 会在下载完成后立刻执行，并可能打断尚未完成的 <code>HTML</code> 解析。应记录脚本执行时的 <code>document.readyState</code>，并检查目标节点当时是否已生成；读取页面数据的逻辑可等待 <code>DOMContentLoaded</code>，或由业务代码在数据就绪后主动上报。</p>\\n</li>\\n<li>\\n<p><strong>新项目全部使用 <code>&lt;script type=&quot;module&quot;&gt;</code>，代码评审里有人要求每个标签再补 <code>defer</code>，另一个人主张给入口统一加 <code>async</code>，你会怎么定？</strong></p>\\n<p>模块脚本默认已经具有类似 <code>defer</code> 的行为，再写 <code>defer</code> 没有必要。只有明确接受下载完成即执行及顺序变化时，给模块脚本加 <code>async</code> 才有意义；业务入口通常存在依赖和页面初始化顺序，贸然统一添加会把稳定的执行顺序变成网络竞争结果。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_9-说一下-web-worker\\"><a class=\\"header-anchor\\" href=\\"#_9-说一下-web-worker\\">#</a> 9 说一下 web worker</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Web Worker</code> 在后台线程执行脚本，适合重计算，不能直接操作 <code>DOM</code></li>\\n<li>主线程和工作线程用 <code>postMessage</code> 通信，数据默认结构化克隆，也可转移所有权</li>\\n<li>它不能自动让网络更快；任务过小还可能被创建与通信成本抵消</li>\\n</ul>\\n<p><strong><code>Web Worker</code> 可以把重计算放到独立线程执行，避免长时间占用主线程，但不能直接操作 <code>DOM</code>。</strong> 主线程与工作线程通过 <code>postMessage</code> 通信，数据默认使用结构化克隆；传递大块二进制数据时，也可以转移所有权来减少复制成本。它适合数据聚合、加解密、压缩或图像处理，计算完成后仍要交给主线程渲染。任务只有几毫秒时，我一般不会使用，因为创建线程和通信的成本可能抵消收益，它也不会让网络请求本身变快。</p>\\n</section>\\n<p><code>Web Worker</code> 是浏览器提供的<strong>真·多线程</strong>能力：一段 <code>JS</code> 跑在独立线程里，不占用主线程，所以再重的计算也不会卡住渲染和交互。</p>\\n<p><strong>基本用法：</strong></p>\\n<pre class=\\"language-js\\"><code>// main.js\\nconst worker = new Worker(&#39;/worker.js&#39;)\\nworker.postMessage({ type: &#39;calc&#39;, rows: bigArray })\\nworker.onmessage = (e) =&gt; render(e.data)\\nworker.onerror = (e) =&gt; console.error(e.message)\\n// 用完要关，否则线程一直占着\\nworker.terminate()\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// worker.js —— 这里没有 window、没有 document\\nself.onmessage = (e) =&gt; {\\n  const result = heavyCalc(e.data.rows)\\n  self.postMessage(result)\\n}\\n</code></pre>\\n<p><strong>它能做什么、不能做什么，是这题的核心：</strong></p>\\n<ul>\\n<li>✅ 能：大量计算（数据聚合、加解密、压缩、图像处理）、<code>fetch</code> 请求、<code>IndexedDB</code>、<code>WebAssembly</code></li>\\n<li>❌ 不能：碰 <code>DOM</code>、读 <code>window</code>/<code>document</code>、直接改页面</li>\\n</ul>\\n<p>不能碰 <code>DOM</code> 不是遗漏，是<strong>刻意的设计</strong> —— 多线程同时改 <code>DOM</code> 会引入锁和竞态，那正是浏览器想避免的复杂度。所以 <code>Worker</code> 只能算完结果传回主线程，由主线程去渲染。</p>\\n<p><strong>通信成本是最容易被忽略的坑：</strong></p>\\n<p><code>postMessage</code> 默认走<strong>结构化克隆</strong>，大对象拷贝本身要时间，而且这次拷贝发生在<strong>主线程</strong>上。传一个几十兆的数组，光克隆就能卡出一帧。解法是转移所有权：</p>\\n<pre class=\\"language-js\\"><code>// 零拷贝：buffer 的所有权直接转移给 worker，主线程之后不能再用它\\nworker.postMessage(buffer, [buffer])\\n</code></pre>\\n<p><strong>什么时候不该用：</strong></p>\\n<ul>\\n<li>任务很小（几毫秒能算完）时，创建线程和通信的开销可能比计算本身还大。</li>\\n<li>它<strong>不会让网络变快</strong> —— 请求慢是网络问题，挪进 <code>Worker</code> 只是让主线程不等而已。</li>\\n<li>调试比主线程麻烦，报错栈也不如主线程直观。</li>\\n</ul>\\n<p><strong>实际判断标准</strong>：用 <code>Performance</code> 录一段，看到某个函数占用主线程超过 50ms 并造成掉帧或输入延迟，才值得搬进 <code>Worker</code>。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>运营后台把一个执行约 <code>5ms</code> 的筛选函数搬进 <code>Worker</code>，代码复杂了一倍，交互却没有明显改善，技术负责人还要继续推广这种改造，你会支持吗？</strong></p>\\n<p>这种短任务通常不值得迁移，因为创建线程、结构化克隆和消息往返也有成本，可能抵消计算收益。应先用 <code>Performance</code> 确认它是否形成超过 <code>50ms</code> 的主线程长任务并导致掉帧或输入延迟；没有此类证据时，保留主线程实现更容易维护和调试。</p>\\n</li>\\n<li>\\n<p><strong>数据看板要在浏览器里聚合几十万行记录，计算期间滚动和输入都会卡住，但最终还要更新图表 <code>DOM</code>，主线程与 <code>Worker</code> 应怎样分工？</strong></p>\\n<p>聚合计算应放进 <code>Worker</code>，完成后通过 <code>postMessage</code> 把结果返回，图表渲染仍由主线程执行。<code>Worker</code> 没有 <code>window</code> 和 <code>document</code>，不能直接操作页面，这是为了避免多线程修改 <code>DOM</code> 带来的锁与竞态；传回结果过大时还要评估通信成本。</p>\\n</li>\\n<li>\\n<p><strong>本地加密模块需要读取 <code>IndexedDB</code>、调用 <code>fetch</code> 获取密钥材料并运行 <code>WebAssembly</code>，安全同学担心 <code>Worker</code> 除了纯计算什么都不能做，这个方案是否可行？</strong></p>\\n<p>这些能力可以在 <code>Worker</code> 中使用，受限的核心是页面环境而不是所有浏览器接口。可让它读取 <code>IndexedDB</code>、发起 <code>fetch</code> 并运行 <code>WebAssembly</code>，再把计算结果发回主线程；如果流程依赖读取表单节点或直接更新提示文案，那部分仍须留在主线程。</p>\\n</li>\\n<li>\\n<p><strong>导入页已把大型数组解析放进 <code>Worker</code>，但点击按钮时仍会明显掉帧，火焰图显示耗时集中在 <code>worker.postMessage</code> 附近，下一步怎么处理？</strong></p>\\n<p>卡顿很可能来自主线程对大对象执行结构化克隆，而不是 <code>Worker</code> 内部的解析。若数据底层是 <code>ArrayBuffer</code>，应通过转移列表把所有权零拷贝交给 <code>Worker</code>，并让读取、解析尽量都在线程内完成；转移后主线程不能再使用原缓冲区，需要提前调整数据所有权。</p>\\n</li>\\n<li>\\n<p><strong>接口响应需要十几秒，产品经理要求把请求放进 <code>Worker</code> 来“让下载变快”，同时又希望页面关闭后任务继续，这个选型哪里有问题？</strong></p>\\n<p>把 <code>fetch</code> 放进 <code>Worker</code> 只能避免相关处理占用主线程，不能缩短网络请求本身的时间。普通 <code>Web Worker</code> 服务于当前页面，页面关闭后也不适合作为持续任务载体；若目标涉及离线缓存或独立于页面的网络代理，应评估 <code>Service Worker</code>，但它同样不能消除真实网络延迟。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_10-用一个div模拟textarea的实现\\"><a class=\\"header-anchor\\" href=\\"#_10-用一个div模拟textarea的实现\\">#</a> 10 用一个div模拟textarea的实现</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>使用 <code>contenteditable=&quot;true&quot;</code> 提供原生可编辑能力，再用样式模拟尺寸和占位提示</li>\\n<li>读取内容要明确用纯文本还是富文本，分别对应 <code>textContent</code> 与受控清洗后的 HTML</li>\\n<li>必须补键盘、焦点、粘贴清洗、输入法组合事件和表单提交等边界</li>\\n</ul>\\n<p><strong>用 <code>div</code> 模拟 <code>textarea</code>，基础做法是添加 <code>contenteditable=&quot;true&quot;</code>，但生产可用还要补齐输入语义和边界处理。</strong> 样式上可用 <code>white-space: pre-wrap</code> 保留换行，并自行实现占位符；取纯文本应使用 <code>textContent</code>，保留富文本则必须在服务端做白名单净化，避免 <code>XSS</code>。还要处理粘贴清洗、输入法组合、字数限制、表单同步和无障碍属性，因为这些能力不会像原生控件一样自动具备。除非需要提及高亮、标签等富内容，我一般会优先使用原生 <code>textarea</code>。</p>\\n</section>\\n<p>最小实现确实只有一行 —— 给元素加 <code>contenteditable</code>：</p>\\n<pre class=\\"language-html\\"><code>&lt;div class=&quot;editor&quot; contenteditable=&quot;true&quot;&gt;&lt;/div&gt;\\n</code></pre>\\n<p>但面试官问这题，想听的从来不是这一行，而是<strong>你知不知道离生产可用还差多少</strong>。</p>\\n<p><strong>一、样式层：把它做得像输入框</strong></p>\\n<pre class=\\"language-css\\"><code>.editor {\\n  min-height: 120px;\\n  padding: 8px 12px;\\n  overflow-y: auto;\\n  line-height: 1.6;\\n  white-space: pre-wrap;      /* 保留换行和连续空格，否则和 textarea 行为不一致 */\\n  word-break: break-word;\\n  border: 1px solid #d9d9d9;\\n  border-radius: 4px;\\n}\\n.editor:empty::before {\\n  color: #bfbfbf;\\n  content: attr(data-placeholder);   /* 原生 placeholder 不生效，只能自己造 */\\n  pointer-events: none;\\n}\\n.editor:focus { border-color: #1677ff; outline: none; }\\n</code></pre>\\n<p>注意 <code>:empty</code> 这个伪类很脆弱 —— 用户敲一个字再删光，浏览器往往会留下一个 <code>&lt;br&gt;</code>，元素就不再 <code>empty</code>，占位符消失不了。稳妥做法是在 <code>input</code> 事件里判断 <code>textContent.trim()</code> 自己加类名。</p>\\n<p><strong>二、取值层：纯文本还是富文本</strong></p>\\n<pre class=\\"language-js\\"><code>const text = el.textContent          // 纯文本，等价于 textarea.value\\nconst html = el.innerHTML            // 富文本，⚠️ 直接入库就是 XSS 入口\\n</code></pre>\\n<p>只要纯文本就一路用 <code>textContent</code>；要保留格式就必须在<strong>服务端</strong>用白名单净化 <code>HTML</code>，前端过滤只是体验层。</p>\\n<p><strong>三、必须补的边界（这部分才是得分点）：</strong></p>\\n<ul>\\n<li>\\n<p><strong>粘贴清洗</strong>：从 Word 或网页复制过来会带一大堆 <code>style</code> 和标签，要拦 <code>paste</code> 事件只取纯文本：</p>\\n<pre class=\\"language-js\\"><code>el.addEventListener(&#39;paste&#39;, (e) =&gt; {\\n  e.preventDefault()\\n  const text = e.clipboardData.getData(&#39;text/plain&#39;)\\n  document.execCommand(&#39;insertText&#39;, false, text)\\n})\\n</code></pre>\\n</li>\\n<li>\\n<p><strong>输入法组合</strong>：中文输入过程中 <code>input</code> 事件会不断触发拿到拼音字母，做字数统计或联想搜索时必须用 <code>compositionstart</code>/<code>compositionend</code> 把这段过程排除掉。</p>\\n</li>\\n<li>\\n<p><strong>表单提交</strong>：<code>contenteditable</code> 的内容<strong>不会</strong>随 <code>form</code> 提交，得同步到一个 <code>&lt;input type=&quot;hidden&quot;&gt;</code>。</p>\\n</li>\\n<li>\\n<p><strong>可访问性</strong>：读屏器不会自动把它当输入框，要补 <code>role=&quot;textbox&quot;</code>、<code>aria-multiline=&quot;true&quot;</code> 和 <code>aria-label</code>。</p>\\n</li>\\n<li>\\n<p><strong>字数限制</strong>：<code>maxlength</code> 无效，只能自己在 <code>beforeinput</code> 里拦截。</p>\\n</li>\\n<li>\\n<p><strong>撤销栈</strong>：用 <code>JS</code> 直接改 <code>innerHTML</code> 会破坏浏览器原生的撤销记录，用户按 <code>Ctrl+Z</code> 行为会很怪。</p>\\n</li>\\n</ul>\\n<p><strong>结论也要说出来</strong>：这么多边界正是&quot;生产环境优先用原生 <code>&lt;textarea&gt;</code>&quot;的理由。真正需要 <code>contenteditable</code> 的场景只有一个 —— <strong>要在输入框里渲染富内容</strong>（<code>@</code> 提及高亮、话题标签、表情），那时候通常也不该手写，而是上 <code>Slate</code>/<code>ProseMirror</code> 这类专门处理光标和选区模型的编辑器框架。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>评论框把 <code>&lt;textarea&gt;</code> 换成了 <code>&lt;div contenteditable&gt;</code>，设计验收时发现连续空格被折叠、删除全部文字后占位符偶尔不回来，怎么补齐？</strong></p>\\n<p>编辑区应设置 <code>white-space: pre-wrap</code> 和合适的换行规则，才能接近 <code>&lt;textarea&gt;</code> 对空格与换行的呈现。占位符不能只依赖 <code>:empty</code>，因为清空后浏览器可能残留 <code>&lt;br&gt;</code>；应在 <code>input</code> 中检查 <code>textContent.trim()</code>，据此切换专用的空状态类名。</p>\\n</li>\\n<li>\\n<p><strong>活动页表单里有一个 <code>contenteditable</code> 评论框，用户输入正常，但提交到服务端时字段始终为空，后端认为前端漏传了参数，应该怎么落地？</strong></p>\\n<p><code>contenteditable</code> 的内容不会像表单控件的 <code>value</code> 一样自动随 <code>form</code> 提交，需要在输入或提交前同步到隐藏字段。纯文本场景读取 <code>textContent</code>，不要保存 <code>innerHTML</code>；若业务确实保留富文本，服务端还必须按白名单净化，前端清洗不能作为安全边界。</p>\\n</li>\\n<li>\\n<p><strong>搜索联想框用 <code>contenteditable</code> 模拟输入，中文用户输入拼音时每敲一个字母都会请求接口，产品又要求限制最多 <code>100</code> 个字符，应该改哪两处？</strong></p>\\n<p>联想请求要在 <code>compositionstart</code> 后暂停，并在 <code>compositionend</code> 后基于最终文本触发，避免把输入法组合中的拼音当成正式内容。<code>contenteditable</code> 不支持 <code>maxlength</code>，字数限制应结合 <code>beforeinput</code> 主动拦截；仍需处理粘贴和选区替换，否则限制容易被绕过。</p>\\n</li>\\n<li>\\n<p><strong>客服后台从 <code>Word</code> 粘贴回复后，数据库里出现大量 <code>style</code> 和未知标签，其他页面展示时样式错乱，前端只加 <code>all: unset</code> 能解决吗？</strong></p>\\n<p><code>all: unset</code> 只能改变当前页面的显示，脏 <code>HTML</code> 仍会被保存并在其他渲染入口产生风险。纯文本回复应拦截 <code>paste</code>，读取 <code>clipboardData.getData(\'text/plain\')</code> 后插入；若必须保留格式，则前端做体验清洗，服务端再用严格标签与属性白名单净化。</p>\\n</li>\\n<li>\\n<p><strong>社交产品要求编辑框支持 <code>@</code> 标签、整块退格删除、跨标签复制和稳定的 <code>Ctrl+Z</code>，团队想继续直接修改 <code>innerHTML</code>，你会怎么取舍？</strong></p>\\n<p>继续直接改 <code>innerHTML</code> 会破坏浏览器撤销栈，而且光标可能进入标签内部，删除和跨节点选区也很难保持一致。此类富内容需求应采用 <code>Slate</code> 或 <code>ProseMirror</code> 等维护文档与选区模型的编辑器；如果只是多行纯文本，应退回原生 <code>&lt;textarea&gt;</code>，成本和可访问性都更可控。</p>\\n</li>\\n<li>\\n<p><strong>无障碍测试发现读屏器只把自制评论框念成普通区域，而且前端提交后才发现它没有原生校验能力，最低限度要补什么？</strong></p>\\n<p>编辑区至少要声明 <code>role=&quot;textbox&quot;</code>、<code>aria-multiline=&quot;true&quot;</code> 和可理解的 <code>aria-label</code>，让辅助技术识别其用途。提交数据仍需同步到真正的表单字段，并自行实现字数等约束；即使补齐这些属性，它仍不自动获得原生 <code>&lt;textarea&gt;</code> 的完整语义和行为。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_11-介绍下资源预加载-prefetch-preload\\"><a class=\\"header-anchor\\" href=\\"#_11-介绍下资源预加载-prefetch-preload\\">#</a> 11 介绍下资源预加载 prefetch/preload</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>preload</code> 获取当前页面很快会用到的关键资源，优先级高且必须写对 <code>as</code></li>\\n<li><code>prefetch</code> 为可能访问的后续页面低优先级预取，浏览器可在资源紧张时忽略</li>\\n<li>跨域字体等资源要正确设置 <code>crossorigin</code>，否则可能重复下载</li>\\n</ul>\\n<p><strong><code>preload</code> 用来提前加载当前页面马上需要的关键资源，<code>prefetch</code> 则低优先级预取后续页面可能使用的资源。</strong> <code>preload</code> 会与正常请求竞争带宽，因此只适合首屏字体、<code>LCP</code> 主图或关键样式，而且必须正确填写 <code>as</code>，否则资源可能无法复用并被重复下载。跨域字体还要匹配 <code>crossorigin</code>，因为预加载请求与实际字体请求的模式不同也会造成重复请求。<code>prefetch</code> 更适合下一页的代码块，但弱网或省流模式下可能被浏览器忽略，不能把它当成可用性保证。</p>\\n</section>\\n<p>两个都是给浏览器的<strong>加载提示</strong>，区别在于&quot;这次导航要不要用&quot;。</p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th><code>preload</code></th>\\n<th><code>prefetch</code></th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>用途</td>\\n<td>当前页<strong>马上要用</strong>的关键资源</td>\\n<td><strong>下一次导航</strong>可能要用的资源</td>\\n</tr>\\n<tr>\\n<td>优先级</td>\\n<td>高，和正常请求竞争带宽</td>\\n<td>最低，只在浏览器空闲时下载</td>\\n</tr>\\n<tr>\\n<td>不用会怎样</td>\\n<td>控制台警告&quot;preloaded but not used&quot;</td>\\n<td>静默丢弃，无成本</td>\\n</tr>\\n<tr>\\n<td>典型场景</td>\\n<td>首屏字体、<code>LCP</code> 主图、关键 <code>CSS</code></td>\\n<td>用户大概率会点的下一页 <code>chunk</code></td>\\n</tr>\\n</tbody>\\n</table>\\n<pre class=\\"language-html\\"><code>&lt;!-- 当前页首屏字体：必须写 as，跨域字体必须写 crossorigin --&gt;\\n&lt;link rel=&quot;preload&quot; href=&quot;/fonts/main.woff2&quot; as=&quot;font&quot; type=&quot;font/woff2&quot; crossorigin&gt;\\n\\n&lt;!-- 首屏主图：配 fetchpriority 抢占优先级 --&gt;\\n&lt;link rel=&quot;preload&quot; href=&quot;/hero.avif&quot; as=&quot;image&quot; fetchpriority=&quot;high&quot;&gt;\\n\\n&lt;!-- 下一步可能访问的页面 --&gt;\\n&lt;link rel=&quot;prefetch&quot; href=&quot;/js/checkout.chunk.js&quot;&gt;\\n</code></pre>\\n<p><strong><code>as</code> 属性不是可选项</strong>，它决定三件事：请求的优先级、发送哪些请求头（<code>Accept</code>）、以及资源下载后能不能被正确复用。写错或不写，浏览器会重新下载一遍，等于负优化。可选值包括 <code>script</code>、<code>style</code>、<code>font</code>、<code>image</code>、<code>fetch</code>、<code>video</code>、<code>audio</code>、<code>document</code>、<code>worker</code> 等。</p>\\n<p><strong>跨域字体的 <code>crossorigin</code> 是最经典的坑</strong>：字体请求天生是匿名跨域模式，<code>preload</code> 时不写 <code>crossorigin</code> 就会形成两条独立的缓存条目 —— 预加载那份用不上，实际渲染时再下一次。控制台会明确警告，看到就是这个原因。</p>\\n<p><strong>几个实践要点：</strong></p>\\n<ul>\\n<li><code>preload</code> 只管&quot;下载早&quot;，不管&quot;什么时候用&quot;。字体还得配 <code>font-display: swap</code> 才不会白等；脚本还得自己写 <code>&lt;script src&gt;</code> 去执行。</li>\\n<li>别滥用 <code>preload</code>：它是高优先级，预加载十个资源等于把带宽从真正的关键请求那里抢走。一般首屏只值得 preload 两三个。</li>\\n<li><code>prefetch</code> 的实际策略由浏览器决定，弱网或省流模式下可能直接忽略，所以不能依赖它保证可用性。</li>\\n<li>现代框架（<code>Next.js</code>、<code>Nuxt</code>）已经内置了路由级 <code>prefetch</code>：链接进入视口就自动预取对应 <code>chunk</code>，通常不需要手写。</li>\\n</ul>\\n<p><strong>还有两个相关的提示</strong>：<code>preconnect</code> 提前完成 <code>DNS + TCP + TLS</code>（适合 <code>CDN</code>、接口域名，最多用两三个）；<code>dns-prefetch</code> 只解析 <code>DNS</code>，成本极低，可以给非关键第三方域名兜底。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>结算页把下一页报表脚本写成 <code>preload</code>，同时把当前首屏字体写成 <code>prefetch</code>，弱网下首屏反而更慢，这两个提示错在哪里？</strong></p>\\n<p>两者的使用方向颠倒了：当前页马上使用的关键字体适合 <code>preload</code>，下一次导航才可能用到的脚本适合 <code>prefetch</code>。<code>preload</code> 优先级高，会与真实关键请求竞争带宽；把非当前页资源放进去可能拖慢首屏，而 <code>prefetch</code> 也不能保证浏览器一定下载。</p>\\n</li>\\n<li>\\n<p><strong>首屏使用 <code>/fonts/main.woff2</code>，团队加了 <code>preload</code> 后网络面板仍出现两次字体请求，标签只写了 <code>rel</code> 和 <code>href</code>，应该怎么改？</strong></p>\\n<p>字体预加载必须声明正确的 <code>as=&quot;font&quot;</code>、对应的 <code>type</code>，并补上 <code>crossorigin</code>，使预加载与实际字体请求采用一致的请求模式和缓存键。<code>as</code> 还会影响优先级与 <code>Accept</code> 请求头；缺失或写错都可能导致资源无法复用，最终再次下载。</p>\\n</li>\\n<li>\\n<p><strong>首页已经 <code>preload</code> 了首屏字体、主图和入口脚本，开发认为资源下载后会自动应用字体并执行脚本，这个理解对吗？</strong></p>\\n<p><code>preload</code> 只提示浏览器提前下载，不负责消费资源，因此字体仍要由样式引用，脚本仍要有对应的 <code>&lt;script src&gt;</code> 才会执行。字体若要避免渲染长时间等待，还应结合 <code>font-display</code>；预加载了却未及时使用，浏览器会给出警告并浪费高优先级带宽。</p>\\n</li>\\n<li>\\n<p><strong>线上控制台持续出现 <code>preloaded but not used</code>，页面同时有十多个 <code>preload</code>，<code>LCP</code> 主图反而变慢，你会按什么顺序排查？</strong></p>\\n<p>先核对每个资源是否确实在当前页很快被消费，再检查 <code>as</code>、资源地址以及字体的 <code>crossorigin</code> 是否与实际请求一致。随后从网络优先级和瀑布图确认是否发生重复下载或关键资源竞争；通常只保留少量首屏关键资源，不能把 <code>preload</code> 当作通用加速开关。</p>\\n</li>\\n<li>\\n<p><strong>内容站想给所有文章链接自动 <code>prefetch</code>，运营担心移动流量浪费，性能负责人又希望提高跳转速度，策略该怎么收敛？</strong></p>\\n<p>应只预取用户高概率访问的下一页资源，并接受浏览器在弱网或省流模式下忽略提示。可根据 <code>navigator.connection.saveData</code> 与 <code>effectiveType</code> 跳过预取，或在鼠标悬停、触摸开始时再触发；全面预取命中率低，还会消耗用户流量和缓存空间。</p>\\n</li>\\n<li>\\n<p><strong>首屏接口和图片来自同一个第三方 <code>CDN</code>，但资源路径要等运行时配置才能确定，架构评审在 <code>preload</code>、<code>preconnect</code> 和 <code>dns-prefetch</code> 之间怎么选？</strong></p>\\n<p>资源地址尚未确定时无法准确 <code>preload</code>，但可以用 <code>preconnect</code> 提前完成目标域名的 <code>DNS</code>、<code>TCP</code> 与 <code>TLS</code> 建连。关键外部域名应控制在少量范围内；若域名只是低概率使用，可退到成本更低的 <code>dns-prefetch</code>，它只提前完成域名解析。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_12-介绍下-viewport\\"><a class=\\"header-anchor\\" href=\\"#_12-介绍下-viewport\\">#</a> 12 介绍下 viewport</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>移动浏览器区分布局视口、视觉视口和理想视口，不设置时常用较宽布局视口缩放页面</li>\\n<li>常用配置是 <code>width=device-width, initial-scale=1</code></li>\\n<li>不要随意禁用缩放，<code>user-scalable=no</code> 会伤害低视力用户的可访问性</li>\\n</ul>\\n<p><strong><code>viewport</code> 用来告诉移动浏览器按设备宽度排版，而不是先按接近桌面的宽度布局后再整体缩小。</strong> 移动端可分为布局视口、视觉视口和理想视口，其中 <code>width=device-width</code> 把布局视口设为设备宽度，<code>initial-scale=1</code> 指定初始缩放比例。常用配置是 <code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;</code>。工程中还要注意别用 <code>user-scalable=no</code> 禁止缩放；全屏布局可用 <code>100dvh</code> 跟随地址栏变化，并用 <code>100vh</code> 兼容旧浏览器。</p>\\n</section>\\n<p>移动端有<strong>三个视口</strong>，先分清它们，配置才讲得明白：</p>\\n<ul>\\n<li><strong>布局视口（layout viewport）</strong>：<code>CSS</code> 排版参照的宽度。不做任何配置时，移动浏览器会假装自己有 <code>980px</code> 宽（各家略有差异），好让桌面站能完整排版。</li>\\n<li><strong>视觉视口（visual viewport）</strong>：用户实际看到的那块区域，缩放时会变。</li>\\n<li><strong>理想视口（ideal viewport）</strong>：等于设备宽度，也就是&quot;不需要缩放就能正常阅读&quot;的宽度。</li>\\n</ul>\\n<p>不加 <code>meta viewport</code> 的页面在手机上是这样的：先按 <code>980px</code> 排好版，再整体缩小塞进屏幕 —— 结果就是字小得看不清，需要双指放大。</p>\\n<p><strong>标准配置只有一行：</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;\\n</code></pre>\\n<p><code>width=device-width</code> 把布局视口设成设备宽度（即理想视口），<code>initial-scale=1</code> 让初始缩放为 1。两个都写是历史经验 —— 早期 <code>iOS</code> 横竖屏切换时只写一个会有问题。</p>\\n<p><strong>各个参数：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>参数</th>\\n<th>说明</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>width</code></td>\\n<td>布局视口宽度，可以是数字或 <code>device-width</code></td>\\n</tr>\\n<tr>\\n<td><code>initial-scale</code></td>\\n<td>初始缩放比</td>\\n</tr>\\n<tr>\\n<td><code>minimum-scale</code> / <code>maximum-scale</code></td>\\n<td>缩放范围</td>\\n</tr>\\n<tr>\\n<td><code>user-scalable</code></td>\\n<td>是否允许用户缩放</td>\\n</tr>\\n<tr>\\n<td><code>viewport-fit=cover</code></td>\\n<td>刘海屏下让内容延伸到安全区外，配合 <code>env(safe-area-inset-*)</code> 使用</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>两个必须知道的坑：</strong></p>\\n<p><strong>1. 不要禁用缩放。</strong> <code>user-scalable=no</code> 会卡死视力不好的用户，无障碍规范明确反对，而且 <strong><code>iOS</code> 从 10 开始已经直接忽略它</strong> —— 写了也没用。想解决&quot;输入框聚焦时页面被自动放大&quot;，正确做法是把输入框字号设成 <code>16px</code> 及以上，<code>iOS</code> 就不会触发自动放大。</p>\\n<p><strong>2. <code>100vh</code> 在移动端不等于屏幕高度。</strong> <code>vh</code> 算的是地址栏收起时的高度，地址栏一出现内容就被挡住。现在用动态视口单位：</p>\\n<pre class=\\"language-css\\"><code>.full-screen {\\n  height: 100vh;   /* 老浏览器兜底 */\\n  height: 100dvh;  /* 跟随地址栏变化 */\\n}\\n</code></pre>\\n<p>还有 <code>svh</code>（最小视口高度）和 <code>lvh</code>（最大视口高度）可选。要兼容更老的环境就用 <code>JS</code> 监听 <code>resize</code> 写一个 <code>--vh</code> 自定义属性。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个没有 <code>meta viewport</code> 的旧活动页在桌面正常，到了手机上却像把 <code>980px</code> 页面整体缩小，文字很小但布局没有变窄，浏览器在做什么？</strong></p>\\n<p>移动浏览器会先使用较宽的布局视口排版，再把整页缩小到视觉视口中，因此桌面布局看似完整，内容却难以阅读。应加入 <code>width=device-width, initial-scale=1</code>，让布局视口采用设备宽度并以 <code>1</code> 倍初始缩放；仅靠媒体查询不能纠正错误的视口基准。</p>\\n</li>\\n<li>\\n<p><strong>新建 <code>H5</code> 订单页时，团队在 <code>width=device-width</code> 和 <code>initial-scale=1</code> 中只想保留一个，认为现代手机效果一样，你会怎么定标准？</strong></p>\\n<p>标准配置应同时保留 <code>width=device-width, initial-scale=1</code>，前者设置布局视口宽度，后者明确初始缩放比例。两项共同表达页面按理想视口以 <code>1</code> 倍显示，也能规避早期移动浏览器横竖屏处理差异；没有明确兼容性验证时，不值得为少几个字符删减。</p>\\n</li>\\n<li>\\n<p><strong>设计要求登录页输入框使用 <code>14px</code> 字号，测试发现 <code>iPhone</code> 聚焦时页面会自动放大，产品要求加 <code>user-scalable=no</code> 禁止缩放，应该接受吗？</strong></p>\\n<p>不应以禁用用户缩放解决，因为这会损害低视力用户的可访问性，而且较新的 <code>iOS</code> 会忽略该声明。应把 <code>input</code>、<code>textarea</code> 或 <code>select</code> 的实际字号设为至少 <code>16px</code>；若必须呈现更小的视觉尺寸，可谨慎缩放外观，但要复核布局和点击区域。</p>\\n</li>\\n<li>\\n<p><strong>聊天页容器写了 <code>height: 100vh</code>，移动端地址栏展开后底部发送栏被遮住，收起地址栏又恢复，<code>CSS</code> 应怎样调整？</strong></p>\\n<p>移动端传统 <code>100vh</code> 往往按较大的视口高度计算，地址栏出现时可见区域变小，底部内容便可能被遮挡。可先保留 <code>height: 100vh</code> 作为旧浏览器兜底，再覆盖为 <code>height: 100dvh</code> 跟随动态视觉区域；更老环境需要监听 <code>resize</code> 写入自定义高度变量。</p>\\n</li>\\n<li>\\n<p><strong>全面屏手机上，底部购买栏要贴到屏幕边缘又不能被系统横条遮挡，页面已经有标准 <code>viewport</code>，还缺哪些配置？</strong></p>\\n<p>需要在视口声明中加入 <code>viewport-fit=cover</code>，允许页面延伸到安全区边缘，再用 <code>env(safe-area-inset-bottom)</code> 给底栏增加安全内边距。通常应通过 <code>calc()</code> 叠加设计间距并提供回退值；只开启 <code>cover</code> 而不消费安全区变量，反而可能让可操作内容落入遮挡区域。</p>\\n</li>\\n<li>\\n<p><strong>地图页要做全屏工具面板，设计师在 <code>100svh</code>、<code>100lvh</code> 和 <code>100dvh</code> 之间争论，地址栏展开和收起时都不能遮住按钮，该怎么选？</strong></p>\\n<p>需要高度随浏览器工具栏变化时，优先选择 <code>100dvh</code>，它会跟随动态视口更新。<code>svh</code> 对应较小视口，适合宁可留白也要始终完整可见的区域；<code>lvh</code> 对应较大视口，工具栏展开时更容易产生遮挡，选型仍需结合是否允许布局随地址栏变化而重排。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_13-如何解决a标点击后hover事件失效的问题\\"><a class=\\"header-anchor\\" href=\\"#_13-如何解决a标点击后hover事件失效的问题\\">#</a> 13 如何解决a标点击后hover事件失效的问题?</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>历史口诀是链接伪类按 <code>link → visited → hover → active</code> 排列，避免同权重规则互相覆盖</li>\\n<li>现代代码更应明确交互状态并保持选择器权重一致，而不是死背顺序</li>\\n<li>触屏设备没有稳定的悬停语义，关键功能不能只放在 <code>hover</code></li>\\n</ul>\\n<p><strong>链接点击后 <code>hover</code> 看似失效，通常是同权重的伪类规则被后写的样式覆盖了。</strong> <code>:link</code>、<code>:visited</code>、<code>:hover</code>、<code>:active</code> 权重相同，因此一般按 <code>LVHA</code> 顺序书写，让悬停状态覆盖已访问状态、按下状态再覆盖悬停状态。实际排查时我会直接看开发者工具中哪条规则被划掉，因为选择器权重不同时，书写顺序未必是关键。触屏设备没有稳定的悬停语义，可用 <code>@media (hover: hover)</code> 限定效果，同时为键盘用户补上 <code>:focus-visible</code>。</p>\\n</section>\\n<p>所谓&quot;<code>hover</code> 失效&quot;，根本不是 <code>hover</code> 坏了，而是<strong>同权重的伪类被后面的规则覆盖了</strong>。</p>\\n<p><code>a:link</code>、<code>a:visited</code>、<code>a:hover</code>、<code>a:active</code> 这四个选择器权重完全相同（都是 0,1,1）。权重相同时，<code>CSS</code> 按<strong>书写顺序</strong>取最后一条。所以下面这段里，链接被访问过之后，<code>a:visited</code> 排在 <code>a:hover</code> 后面就会一直赢：</p>\\n<pre class=\\"language-css\\"><code>a:hover  { color: green; }\\na:visited{ color: red; }   /* 访问过之后，鼠标移上去也是红的 —— hover &quot;失效&quot;了 */\\n</code></pre>\\n<p><strong>正确顺序 —— <code>LoVe HAte</code> 口诀：</strong></p>\\n<pre class=\\"language-text\\"><code>:link → :visited → :hover → :active\\n L        V          H        A\\n</code></pre>\\n<p>这个顺序不是随便定的，它符合状态的<strong>包含关系</strong>：所有链接都可能被访问（<code>visited</code> 覆盖 <code>link</code>），访问过的链接也能被悬停（<code>hover</code> 覆盖 <code>visited</code>），悬停时才可能按下（<code>active</code> 覆盖 <code>hover</code>）。越具体的状态排越后，才能在发生时赢过前面的。</p>\\n<p><strong>四个伪类的含义：</strong></p>\\n<ul>\\n<li><code>a:link</code> —— 未访问过（通常直接写成 <code>a</code>）</li>\\n<li><code>a:visited</code> —— 已访问过</li>\\n<li><code>a:hover</code> —— 鼠标悬停</li>\\n<li><code>a:active</code> —— 鼠标按下的那一瞬间</li>\\n</ul>\\n<p><strong>现代代码里更该关心的三件事：</strong></p>\\n<ol>\\n<li>\\n<p><strong>别死背顺序，理解权重。</strong> 一旦你写的是 <code>.nav a:hover</code> 和 <code>a:visited</code>，权重不同了，顺序就不再是决定因素。真正的排查方法是打开 <code>DevTools</code> 的 <code>Styles</code> 面板，看哪条规则被划掉了 —— 一眼就知道是被谁覆盖的。</p>\\n</li>\\n<li>\\n<p><strong><code>:visited</code> 能改的属性极少。</strong> 出于隐私考虑（防止用它探测用户浏览历史），浏览器只允许 <code>:visited</code> 修改颜色类属性（<code>color</code>、<code>background-color</code>、<code>border-color</code>），改字号、改 <code>display</code> 都无效，<code>getComputedStyle</code> 读到的也是未访问时的值。</p>\\n</li>\\n<li>\\n<p><strong>触屏没有稳定的 <code>hover</code>。</strong> 手机上点击后 <code>hover</code> 样式可能一直粘着不消失。关键信息不能只放在 <code>hover</code> 里，要用媒体查询把悬停效果限定在真正支持悬停的设备上：</p>\\n<pre class=\\"language-css\\"><code>@media (hover: hover) {\\n  a:hover { text-decoration: underline; }\\n}\\n</code></pre>\\n</li>\\n<li>\\n<p><strong>别忘了 <code>:focus-visible</code>。</strong> 键盘用户是靠焦点环导航的，只写 <code>hover</code> 等于把他们排除在外。<code>:focus-visible</code> 的好处是鼠标点击时不显示焦点环、键盘 <code>Tab</code> 时才显示，两边都照顾到。</p>\\n</li>\\n</ol>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>资讯页里已访问链接是红色，鼠标移上去应变绿色；代码却写成 <code>a:hover { color: green }</code> 后接 <code>a:visited { color: red }</code>，测试说点击返回后悬停失效，你怎么解释？</strong></p>\\n<p>不是 <code>:hover</code> 没触发，而是同权重的 <code>:visited</code> 写在后面，按层叠顺序覆盖了它。把规则调整为 <code>:link</code>、<code>:visited</code>、<code>:hover</code>、<code>:active</code>，悬停状态就能覆盖访问状态；若选择器权重不同，则不能只靠调整顺序。</p>\\n</li>\\n<li>\\n<p><strong>导航组件同时存在 <code>.nav a:hover</code> 和全局 <code>a:visited</code>，设计师要求已访问链接仍保留导航悬停色，你会怎样落地而不污染其他页面？</strong></p>\\n<p>应把链接各状态限定在导航组件作用域内，并尽量保持选择器权重一致，例如统一写成 <code>.nav a:visited</code> 与 <code>.nav a:hover</code>。这样既能按状态顺序覆盖，也不会靠 <code>!important</code> 强压全局规则；组件层级继续变化时仍要复查实际权重。</p>\\n</li>\\n<li>\\n<p><strong>同一套文章列表要兼容桌面鼠标和手机触屏，移动端点击链接后悬停底色偶尔一直残留，关键信息目前只在 <code>:hover</code> 中出现，你会怎么改？</strong></p>\\n<p>把纯悬停装饰放进 <code>@media (hover: hover)</code>，只让真正支持稳定悬停的设备应用它。关键信息还要在默认状态、点击状态或可操作控件中直接呈现，因为触屏没有可靠的 <code>hover</code>，不能用它承载唯一入口。</p>\\n</li>\\n<li>\\n<p><strong>线上只有部分已读文章出现“悬停不变色”，<code>CSS</code> 文件经过多轮覆盖且选择器混有类名，你会让开发怎样定位，而不是先背 <code>LoVe HAte</code> 改顺序？</strong></p>\\n<p>先在 <code>DevTools</code> 的 <code>Styles</code> 面板选中异常链接，确认 <code>:hover</code> 规则是否命中以及被哪条声明划掉，再比较来源、权重和书写顺序。若是 <code>.list a:visited</code> 压过 <code>a:hover</code>，仅交换顺序仍无效，应统一作用域或提高悬停规则的合理权重。</p>\\n</li>\\n<li>\\n<p><strong>产品想用 <code>:visited::after</code> 给上千篇文章追加“已读”角标，并要求脚本还能统计角标数量；你会接受这个方案吗？</strong></p>\\n<p>不应接受，<code>:visited</code> 受浏览历史隐私保护，只允许有限的颜色类属性变化，不能可靠修改 <code>content</code>、布局或供脚本读取真实状态。已读角标应由业务阅读记录生成类名或数据属性；代价是需要存储和同步状态，但行为可测且能跨设备一致。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_14-点击一个input依次触发的事件\\"><a class=\\"header-anchor\\" href=\\"#_14-点击一个input依次触发的事件\\">#</a> 14 点击一个input依次触发的事件</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>未聚焦输入框通常经历指针/鼠标按下、获得焦点、抬起、<code>click</code></li>\\n<li>文本变化时还会出现 <code>beforeinput</code>、<code>input</code>，失焦时可能触发 <code>change</code></li>\\n<li>具体序列受输入类型、设备和浏览器影响，面试应说明用监听器实测，别给绝对顺序</li>\\n</ul>\\n<p><strong>鼠标点击一个未聚焦的 <code>input</code>，常见主线是指针进入、按下、获得焦点、抬起，最后触发 <code>click</code>。</strong> 对应常见顺序可写成 <code>mouseenter → mousedown → focus → mouseup → click</code>，其中聚焦是 <code>mousedown</code> 默认行为产生的，所以在该阶段调用 <code>preventDefault()</code> 可以阻止获得焦点。值变化时还会触发 <code>beforeinput</code> 和 <code>input</code>，文本框的 <code>change</code> 通常要等失焦且值确实改变。输入类型、触屏设备和浏览器都可能改变细节，中文输入法场景还应结合组合事件处理，不能把这串顺序当成绝对规则。</p>\\n</section>\\n<p>先看这段代码，它是这道题的标准形态：</p>\\n<pre class=\\"language-javascript\\"><code>const text = document.getElementById(&#39;text&#39;)\\ntext.onmouseenter = () =&gt; console.log(&#39;onmouseenter&#39;)\\ntext.onmousedown  = () =&gt; console.log(&#39;onmousedown&#39;)\\ntext.onfocus      = () =&gt; console.log(&#39;onfocus&#39;)\\ntext.onclick      = () =&gt; console.log(&#39;onclick&#39;)\\n</code></pre>\\n<p>用鼠标点一下这个未聚焦的输入框，输出顺序是：</p>\\n<pre class=\\"language-text\\"><code>onmouseenter → onmousedown → onfocus → onclick\\n</code></pre>\\n<p><strong>为什么是这个顺序？</strong></p>\\n<ol>\\n<li><code>mouseenter</code> —— 指针进入元素区域，此时还没按下。</li>\\n<li><code>mousedown</code> —— 按键按下。</li>\\n<li><code>focus</code> —— <strong>焦点转移发生在 <code>mousedown</code> 的默认行为里</strong>。这是关键：聚焦不是一个独立的用户动作，而是 <code>mousedown</code> 默认行为的产物。所以在 <code>mousedown</code> 里调 <code>preventDefault()</code> 可以<strong>阻止输入框获得焦点</strong> —— 做自定义下拉菜单时经常用这一招保持输入框焦点不丢。</li>\\n<li><code>click</code> —— 按下再抬起（<code>mouseup</code>）之后才合成 <code>click</code>。</li>\\n</ol>\\n<p><strong>完整的事件族分三组，别混为一谈：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>组别</th>\\n<th>事件</th>\\n<th>触发条件</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>指针</td>\\n<td><code>mouseenter</code> → <code>mousedown</code> → <code>mouseup</code> → <code>click</code></td>\\n<td>鼠标操作</td>\\n</tr>\\n<tr>\\n<td>焦点</td>\\n<td><code>focus</code> / <code>blur</code>（不冒泡）、<code>focusin</code> / <code>focusout</code>（冒泡）</td>\\n<td>焦点进出</td>\\n</tr>\\n<tr>\\n<td>输入</td>\\n<td><code>beforeinput</code> → <code>input</code> → <code>change</code></td>\\n<td>值发生变化</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong><code>input</code> 和 <code>change</code> 的区别是高频考点</strong>：<code>input</code> 在每次值变化时立即触发（每敲一个字符一次）；<code>change</code> 对文本框来说要等<strong>失焦且值确实变了</strong>才触发，对 <code>checkbox</code>/<code>select</code> 则是选中即触发。做实时搜索用 <code>input</code>，做&quot;填完再校验&quot;用 <code>change</code>。</p>\\n<p><strong>几个实际会踩的边界：</strong></p>\\n<ul>\\n<li>\\n<p><strong>中文输入法</strong>：拼音输入过程中 <code>input</code> 会不断触发拿到未上屏的字母，做联想搜索会发出一堆无效请求。要用 <code>compositionstart</code>/<code>compositionend</code> 把这段排除：</p>\\n<pre class=\\"language-js\\"><code>let composing = false\\ninput.addEventListener(&#39;compositionstart&#39;, () =&gt; (composing = true))\\ninput.addEventListener(&#39;compositionend&#39;, () =&gt; { composing = false; search(input.value) })\\ninput.addEventListener(&#39;input&#39;, () =&gt; { if (!composing) search(input.value) })\\n</code></pre>\\n</li>\\n<li>\\n<p><strong>顺序不是绝对的</strong>：不同输入类型（<code>checkbox</code>、<code>file</code>、<code>date</code>）、不同设备（触屏会先派发 <code>touch</code>/<code>pointer</code> 系列再合成鼠标事件）、不同浏览器都可能有差异。</p>\\n</li>\\n<li>\\n<p><strong>面试怎么答</strong>：说清&quot;指针 → 焦点 → 点击&quot;这条主线和 <code>focus</code> 来自 <code>mousedown</code> 默认行为这个因果，比背一串事件名有价值；再补一句&quot;具体顺序我会挂监听器实测&quot;，比给一个绝对答案更稳妥。</p>\\n</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>登录页的未聚焦文本框注册了 <code>mouseenter</code>、<code>mousedown</code>、<code>focus</code>、<code>click</code>，候选人坚持 <code>click</code> 一定先于 <code>focus</code>，你会让他解释一次普通鼠标点击的主线吗？</strong></p>\\n<p>普通鼠标进入并点击时，主线是 <code>mouseenter</code>、<code>mousedown</code>、<code>focus</code>、<code>mouseup</code>，随后合成 <code>click</code>。关键因果是聚焦属于 <code>mousedown</code> 的默认行为，并非由 <code>click</code> 触发；输入类型、设备和浏览器变化时仍应用监听器实测。</p>\\n</li>\\n<li>\\n<p><strong>自定义搜索下拉框有几百个候选项，点击某项时输入框先 <code>blur</code>，面板被关闭导致选项的 <code>click</code> 没执行，你会把修复放在哪个事件里？</strong></p>\\n<p>在选项的 <code>mousedown</code> 监听器中调用 <code>preventDefault()</code>，阻止焦点从输入框转移，再处理选项选择。等到 <code>click</code> 才阻止已经太晚，因为 <code>blur</code> 与面板关闭先发生；若选项本身必须获得焦点，就应重构关闭时机而非强行保焦。</p>\\n</li>\\n<li>\\n<p><strong>同一个表单既有文本框、复选框和日期输入，测试要求所有控件都按固定事件序列写断言，你会同意这种测试设计吗？</strong></p>\\n<p>不应把所有控件锁成同一条绝对序列，因为文本框、<code>checkbox</code>、<code>date</code> 等触发输入与变更的条件不同，触屏还会引入 <code>pointer</code> 或 <code>touch</code> 事件。测试应围绕业务结果和关键因果断言，并在目标浏览器上记录真实序列，避免依赖偶然实现细节。</p>\\n</li>\\n<li>\\n<p><strong>实时搜索框输入英文正常，但中文输入“上海”时日志先出现多次拼音请求，最终汉字有时又漏搜，你会怎样排查监听逻辑？</strong></p>\\n<p>检查是否在每次 <code>input</code> 都直接请求，以及 <code>compositionstart</code> 到 <code>compositionend</code> 期间是否正确维护组合态。组合期间跳过搜索，并在 <code>compositionend</code> 主动用当前值补触发一次；防抖仍需保留，但它只能降频，不能识别输入法是否完成上屏。</p>\\n</li>\\n<li>\\n<p><strong>结算表单要求文本框边输入边更新金额，同时在用户填完后做一次完整校验，团队争论只监听 <code>input</code> 还是只监听 <code>change</code>，你怎么定？</strong></p>\\n<p>金额预览监听 <code>input</code>，因为它会在值每次变化时及时触发；完整校验可在文本框失焦且值改变后由 <code>change</code> 执行。两者承担不同阶段，强行合并会牺牲即时反馈或完成态校验；中文输入还需额外处理组合事件。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_15-有写过原生的自定义事件吗\\"><a class=\\"header-anchor\\" href=\\"#_15-有写过原生的自定义事件吗\\">#</a> 15 有写过原生的自定义事件吗</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>用 <code>CustomEvent</code> 创建事件，通过 <code>detail</code> 携带业务数据</li>\\n<li>用 <code>dispatchEvent</code> 同步派发，监听方式仍是 <code>addEventListener</code></li>\\n<li>需要跨层冒泡时设置 <code>bubbles: true</code>，跨 <code>Shadow DOM</code> 边界还要考虑 <code>composed: true</code></li>\\n<li>自定义事件适合解耦同一页面组件，不是跨页面或跨进程通信方案</li>\\n</ul>\\n<p><strong>原生自定义事件通常用 <code>CustomEvent</code> 创建、<code>dispatchEvent</code> 派发，再通过 <code>addEventListener</code> 监听。</strong> 需要携带业务数据时，把参数放进 <code>detail</code>；只需要事件名称时也可以使用 <code>Event</code>。如果事件要经过父级统一处理，可以配置冒泡；是否允许取消默认行为则由 <code>cancelable</code> 控制。旧代码还可能通过 <code>document.createEvent(\'CustomEvent\')</code> 创建并初始化事件，但新代码直接使用构造函数更清楚，派发时传入的必须是事件对象而不是事件名称。</p>\\n</section>\\n<p><strong>创建自定义事件</strong></p>\\n<p>原生自定义事件有三种写法：</p>\\n<ol>\\n<li>使用<code>Event</code></li>\\n</ol>\\n<pre class=\\"language-javascript\\"><code>let myEvent = new Event(&#39;event_name&#39;);\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>使用<code>customEvent</code> （可以传参数）</li>\\n</ol>\\n<pre class=\\"language-javascript\\"><code>let myEvent = new CustomEvent(&#39;event_name&#39;, {\\n\\tdetail: {\\n\\t\\t// 将需要传递的参数放到这里\\n\\t\\t// 可以在监听的回调函数中获取到：event.detail\\n\\t}\\n})\\n</code></pre>\\n<ol start=\\"3\\">\\n<li>使用<code>document.createEvent(\'CustomEvent\')和initCustomEvent()</code></li>\\n</ol>\\n<pre class=\\"language-javascript\\"><code>let myEvent = document.createEvent(&#39;CustomEvent&#39;);// 注意这里是为&#39;CustomEvent&#39;\\nmyEvent.initEvent(\\n\\t// 1. event_name: 事件名称\\n\\t// 2. canBubble: 是否冒泡\\n\\t// 3. cancelable: 是否可以取消默认行为\\n)\\n</code></pre>\\n<ul>\\n<li><code>createEvent</code>：创建一个事件</li>\\n<li><code>initEvent</code>：初始化一个事件</li>\\n</ul>\\n<p>可以看到，<code>initEvent</code>可以指定3个参数。</p>\\n<p>（有些文章中会说还有第四个参数<code>detail</code>，但是我查看了<code>W3C</code>上并没有这个参数，而且实践了一下也没有效果）</p>\\n<p><strong>事件的监听</strong></p>\\n<p>自定义事件的监听其实和普通事件的一样，使用<code>addEventListener</code>来监听：</p>\\n<pre class=\\"language-javascript\\"><code>button.addEventListener(&#39;event_name&#39;, function (e) {})\\n</code></pre>\\n<p><strong>事件的触发</strong></p>\\n<p>触发自定义事件使用<code>dispatchEvent(myEvent)</code>。</p>\\n<p>注意⚠️，这里的参数是要自定义事件的对象(也就是<code>myEvent</code>)，而不是自定义事件的名称(<code>\'myEvent\'</code>)</p>\\n<p><strong>案例</strong></p>\\n<p>来看个案例吧：</p>\\n<pre class=\\"language-javascript\\"><code>// 1.\\n// let myEvent = new Event(&#39;myEvent&#39;);\\n// 2.\\n// let myEvent = new CustomEvent(&#39;myEvent&#39;, {\\n//   detail: {\\n//     name: &#39;lindaidai&#39;\\n//   }\\n// })\\n// 3.\\nlet myEvent = document.createEvent(&#39;CustomEvent&#39;);\\nmyEvent.initEvent(&#39;myEvent&#39;, true, true)\\n\\nlet btn = document.getElementsByTagName(&#39;button&#39;)[0]\\nbtn.addEventListener(&#39;myEvent&#39;, function (e) {\\n  console.log(e)\\n  console.log(e.detail)\\n})\\nsetTimeout(() =&gt; {\\n  btn.dispatchEvent(myEvent)\\n}, 2000)\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>页面创建了 <code>new Event(\'filter-change\')</code>，监听器却读取 <code>event.detail.keyword</code>，上线后拿到 <code>undefined</code>；筛选模块需要传递结构化参数，你会怎么改？</strong></p>\\n<p>改用 <code>new CustomEvent(\'filter-change\', { detail: { keyword } })</code>，监听端再从 <code>event.detail</code> 读取载荷。普通 <code>Event</code> 适合只表达“发生了什么”，不提供 <code>detail</code> 初始化能力；载荷对象仍应约定稳定结构，避免生产者和消费者暗中耦合。</p>\\n</li>\\n<li>\\n<p><strong>一个原生表格组件要通知宿主页面筛选条件变化，页面上有多个监听方，你会怎样定义、监听并触发这个事件？</strong></p>\\n<p>在组件节点上用 <code>addEventListener(\'filter-change\', handler)</code> 注册监听，再通过 <code>dispatchEvent(new CustomEvent(\'filter-change\', { detail }))</code> 触发。传给 <code>dispatchEvent</code> 的必须是事件对象而非事件名称；事件名和 <code>detail</code> 结构最好形成公开契约，否则多个监听方容易各自猜测。</p>\\n</li>\\n<li>\\n<p><strong>旧后台仍使用 <code>document.createEvent(\'CustomEvent\')</code> 和初始化方法，新模块准备全面改成构造器，评审中你会如何判断是否保留旧写法？</strong></p>\\n<p>新代码优先使用 <code>Event</code> 或 <code>CustomEvent</code> 构造器，表达更直接，也能清楚传入 <code>detail</code>。旧式创建与初始化方式主要用于历史兼容，只有项目明确受旧环境约束时才保留；不应在业务文件里长期混用两套风格增加维护成本。</p>\\n</li>\\n<li>\\n<p><strong>监控显示按钮确实执行了 <code>dispatchEvent(\'order-ready\')</code>，但任何监听器都没收到，事件名看起来完全一致，你先查哪一处？</strong></p>\\n<p>先检查 <code>dispatchEvent</code> 的实参，它要求一个事件对象，直接传字符串不是合法触发方式。应先构造 <code>new CustomEvent(\'order-ready\', { detail })</code> 再派发，同时确认监听器绑定在同一目标节点；目标不一致时，即使名称相同也不会自动送达。</p>\\n</li>\\n<li>\\n<p><strong>微前端宿主想让不同框架的三个子模块共享订单更新，团队在原生 <code>CustomEvent</code> 和各框架内部回调之间争论，你怎么划边界？</strong></p>\\n<p>跨框架或原生宿主边界可用 <code>CustomEvent</code> 作为低耦合协议，框架组件内部仍优先使用自身的回调或事件机制。这样不会让内部状态更新绕开框架，同时保留边界互操作性；代价是需要治理事件命名、载荷格式及监听器清理。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_16-addeventlistener和attachevent的区别\\"><a class=\\"header-anchor\\" href=\\"#_16-addeventlistener和attachevent的区别\\">#</a> 16 addEventListener和attachEvent的区别？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>attachEvent</code> 是旧版 <code>IE</code> 私有 API，只支持冒泡，事件名带 <code>on</code></li>\\n<li>它的 <code>this</code>、执行顺序和标准事件模型都有历史差异</li>\\n<li>现代浏览器统一使用 <code>addEventListener</code>，这题只需说明历史兼容背景，不再写双分支新代码</li>\\n</ul>\\n<p><strong><code>addEventListener</code> 是标准事件 <code>API</code>，<code>attachEvent</code> 则是 <code>IE8</code> 及更早版本的私有兼容方案。</strong> 前者的事件名不带 <code>on</code>，支持捕获和冒泡，回调里的 <code>this</code> 指向绑定元素；后者要写 <code>onclick</code>，只支持冒泡，<code>this</code> 指向 <code>window</code>。多个监听器同时存在时，两者的执行顺序也不同。现在的新代码不应再写 <code>attachEvent</code> 分支，老环境确实要兼容时，更适合用 <code>polyfill</code> 统一接口。</p>\\n</section>\\n<p><code>attachEvent</code> 是 <code>IE8</code> 及更早版本的私有 API，和标准的 <code>addEventListener</code> 有四处差异：</p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th><code>addEventListener</code>（标准）</th>\\n<th><code>attachEvent</code>（旧 IE）</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>事件名</td>\\n<td>不带 <code>on</code>，如 <code>\'click\'</code></td>\\n<td>带 <code>on</code>，如 <code>\'onclick\'</code></td>\\n</tr>\\n<tr>\\n<td>传播阶段</td>\\n<td>捕获、冒泡都支持</td>\\n<td><strong>只有冒泡</strong></td>\\n</tr>\\n<tr>\\n<td>回调里的 <code>this</code></td>\\n<td>绑定事件的那个元素</td>\\n<td><code>window</code></td>\\n</tr>\\n<tr>\\n<td>多个监听执行顺序</td>\\n<td>按注册顺序</td>\\n<td>逆序（后注册的先执行）</td>\\n</tr>\\n</tbody>\\n</table>\\n<pre class=\\"language-js\\"><code>// 标准\\nel.addEventListener(&#39;click&#39;, fn, false)\\nel.removeEventListener(&#39;click&#39;, fn, false)\\n\\n// 旧 IE\\nel.attachEvent(&#39;onclick&#39;, fn)\\nel.detachEvent(&#39;onclick&#39;, fn)\\n</code></pre>\\n<p><strong>这题现在的正确答法</strong>：说清差异，然后<strong>明确指出这是历史知识</strong>。<code>IE8</code> 早已停止支持，主流框架和构建工具都不再为它兼容，新代码里不该再出现 <code>attachEvent</code> 分支 —— 写了反而是噪音，还会误导后来的人以为项目要支持 IE。</p>\\n<p><strong>如果面试官追着问&quot;那老项目怎么办&quot;</strong>，答案是：真要支持这种环境，也不该手写双分支，而是引一个事件 polyfill 统一到标准 API，业务代码只写一套。手写兼容分支的问题在于它会散落在几十个文件里，将来放弃 IE 支持时清理成本极高。</p>\\n<p><strong>值得延伸的一点</strong>：<code>attachEvent</code> 里 <code>this</code> 指向 <code>window</code> 这个差异，在当年催生了大量 <code>var self = this</code> 的写法，也是很多老代码里 <code>bind</code> 满天飞的原因。理解这段历史，能帮你看懂老项目里那些看起来多余的包装。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>老 <code>CRM</code> 同时写了 <code>el.addEventListener(\'click\', fn)</code> 和 <code>el.attachEvent(\'click\', fn)</code>，旧 <code>IE</code> 分支始终没反应；不运行代码时你能指出哪个参数就错了吗？</strong></p>\\n<p><code>attachEvent</code> 的事件名必须带 <code>on</code>，应写成 <code>attachEvent(\'onclick\', fn)</code>，而标准 <code>API</code> 才使用 <code>\'click\'</code>。两者还不能被视为只差名称的等价接口，因为传播阶段、回调 <code>this</code> 和多监听器顺序也不同。</p>\\n</li>\\n<li>\\n<p><strong>遗留页面用事件代理处理上百行表格，业务要求捕获阶段先拦截子元素事件；现有兼容层底层仍是 <code>attachEvent</code>，你会怎么评估？</strong></p>\\n<p><code>attachEvent</code> 只支持冒泡阶段，无法满足真正的捕获监听，因此不能靠修改布尔参数补齐。若旧环境仍是硬约束，应由统一兼容层明确能力边界或调整交互设计；新代码则直接使用 <code>addEventListener</code>，避免业务散落双分支。</p>\\n</li>\\n<li>\\n<p><strong>旧 <code>IE</code> 模块迁到现代浏览器后，处理器里的 <code>this</code> 从 <code>window</code> 变成了按钮元素，原先依赖全局属性的逻辑异常，你会如何收敛差异？</strong></p>\\n<p>迁移时应显式决定处理器需要元素还是业务上下文，不再依赖两套 <code>API</code> 隐含的 <code>this</code> 差异。使用 <code>addEventListener</code> 时可直接用元素上下文，必要时通过闭包或明确绑定传入对象；盲目保留旧 <code>bind</code> 包装可能导致解绑引用不一致。</p>\\n</li>\\n<li>\\n<p><strong>线上同一按钮挂了三个监听器，现代浏览器按注册顺序执行，旧环境却表现为后注册先执行，导致校验晚于提交，你会从哪里定位？</strong></p>\\n<p>先确认旧分支是否使用 <code>attachEvent</code>，它的多个监听器可能按逆序执行，而 <code>addEventListener</code> 按注册顺序执行。业务正确性不应依赖多个独立监听器的偶然先后，可合并为一个明确流程；否则兼容层替换后仍可能暴露顺序耦合。</p>\\n</li>\\n<li>\\n<p><strong>接手项目后发现几十个文件手写 <code>addEventListener</code>/<code>attachEvent</code> 判断，但用户基线已经不要求旧 <code>IE</code>，团队担心直接删除风险太高，你怎么推进？</strong></p>\\n<p>先依据项目明确的浏览器支持范围和实际访问数据确认旧分支已无消费者，再集中删除兼容代码并回归关键交互。清理时同步处理 <code>window.event</code>、<code>var self = this</code> 等配套历史写法；若仍必须支持旧环境，应采用统一兼容层，而不是继续复制双分支。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_17-addeventlistener函数的第三个参数\\"><a class=\\"header-anchor\\" href=\\"#_17-addeventlistener函数的第三个参数\\">#</a> 17 addEventListener函数的第三个参数</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>布尔值只表示是否在捕获阶段触发；现代写法更推荐传配置对象</li>\\n<li><code>capture</code> 控制捕获阶段，<code>once</code> 自动移除，<code>passive</code> 承诺不调用 <code>preventDefault</code></li>\\n<li><code>signal</code> 可配合 <code>AbortController</code> 一次清理一组监听器</li>\\n<li>滚动触摸场景用 <code>passive: true</code> 能减少浏览器等待，但需要阻止默认行为时不能用</li>\\n</ul>\\n<p><strong><code>addEventListener</code> 的第三个参数既可以是表示是否捕获的布尔值，也可以是包含监听选项的对象。</strong> 常用选项可归成传播、生命周期和性能三类：<code>capture</code> 决定捕获或冒泡，<code>once</code> 负责触发后自动移除，<code>passive</code> 表示不会调用 <code>preventDefault()</code>。<code>signal</code> 则能配合 <code>AbortController</code> 一次清理一组监听器，组件卸载时很实用。需要阻止默认行为的手势场景不能设置 <code>passive: true</code>，否则调用 <code>preventDefault()</code> 不会生效。</p>\\n</section>\\n<p>第三个参数早已不只是&quot;是不是捕获&quot;。它有两种形态：</p>\\n<pre class=\\"language-js\\"><code>// 旧写法：布尔值，只表示 capture\\nel.addEventListener(&#39;click&#39;, fn, true)\\n\\n// 现代写法：配置对象\\nel.addEventListener(&#39;click&#39;, fn, {\\n  capture: false,   // 是否在捕获阶段触发\\n  once: true,       // 触发一次后自动移除\\n  passive: true,    // 承诺不调用 preventDefault\\n  signal,           // 配合 AbortController 批量清理\\n})\\n</code></pre>\\n<p><strong>四个选项各自解决什么问题：</strong></p>\\n<p><strong><code>capture</code></strong> —— 决定监听器在捕获阶段还是冒泡阶段执行。日常绝大多数场景用冒泡（默认 <code>false</code>）；需要&quot;抢在子元素之前拦截&quot;或者代理 <code>focus</code>/<code>blur</code> 这类不冒泡的事件时才用捕获。</p>\\n<p><strong><code>once</code></strong> —— 执行一次后自动移除，省掉手动 <code>removeEventListener</code>。做一次性引导、首次交互后初始化音频这类需求很顺手。</p>\\n<p><strong><code>passive</code></strong> —— 这是<strong>性能相关</strong>的一个。默认情况下，浏览器遇到 <code>touchstart</code>/<code>wheel</code> 监听器时必须先执行 <code>JS</code>，等它跑完才知道有没有调 <code>preventDefault</code>，才敢开始滚动 —— 这就是移动端滑动&quot;发涩&quot;的原因之一。加上 <code>passive: true</code> 等于提前承诺&quot;我不会阻止默认行为&quot;，浏览器可以立即滚动：</p>\\n<pre class=\\"language-js\\"><code>// 滚动监听一律加 passive\\nwindow.addEventListener(&#39;scroll&#39;, onScroll, { passive: true })\\nel.addEventListener(&#39;touchstart&#39;, onTouch, { passive: true })\\n</code></pre>\\n<p>代价是<strong>在 passive 监听器里调 <code>preventDefault()</code> 无效</strong>，控制台还会警告。所以需要阻止默认行为（比如自定义手势、阻止橡皮筋效果）的场景不能用它。注意 <code>Chrome</code> 已经把 <code>document</code>/<code>window</code> 上的 <code>touchstart</code>、<code>touchmove</code>、<code>wheel</code> 默认设成了 <code>passive: true</code>，老代码里靠它们阻止默认行为的逻辑可能已经悄悄失效了。</p>\\n<p><strong><code>signal</code></strong> —— 最实用的现代特性，一次清理一整组监听：</p>\\n<pre class=\\"language-js\\"><code>const ctrl = new AbortController()\\nconst { signal } = ctrl\\n\\nwindow.addEventListener(&#39;resize&#39;, onResize, { signal })\\ndocument.addEventListener(&#39;keydown&#39;, onKey, { signal })\\nel.addEventListener(&#39;click&#39;, onClick, { signal })\\n\\n// 组件卸载：一行清掉全部三个，不用记引用\\nctrl.abort()\\n</code></pre>\\n<p>这比逐个 <code>removeEventListener</code> 可靠得多 —— 后者最常见的失败原因就是传了个不同引用的函数，看起来移除了实际没有，日积月累就是内存泄漏。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审中有人说 <code>addEventListener</code> 第三个参数只能传 <code>true</code> 或 <code>false</code>，而组件传了 <code>{ once: true, signal }</code>；你会如何判断这段代码是否合理？</strong></p>\\n<p>第三个参数既可用布尔值表示 <code>capture</code>，也可传配置对象设置 <code>capture</code>、<code>once</code>、<code>passive</code> 和 <code>signal</code>。该写法适合一次性监听并随控制器统一清理；仍要确认目标运行环境支持所用选项，不能把现代对象形式误解成语法错误。</p>\\n</li>\\n<li>\\n<p><strong>图片预览层每次打开都会注册 <code>keydown</code>、<code>resize</code> 和按钮 <code>click</code>，连续开关后同一动作执行多次，你会怎样改造清理机制？</strong></p>\\n<p>每次挂载创建一个 <code>AbortController</code>，所有监听器都传入同一个 <code>{ signal }</code>，卸载时调用一次 <code>abort()</code>。这样无需逐个保存函数引用，可避免匿名函数或重新 <code>bind</code> 导致移除失败；控制器终止后不能复用，重新挂载必须新建。</p>\\n</li>\\n<li>\\n<p><strong>移动页面把 <code>touchmove</code> 监听统一改成 <code>{ passive: true }</code> 后滚动顺畅了，但自定义拖拽区域再也阻止不了页面滚动，你会如何处理冲突？</strong></p>\\n<p>需要调用 <code>preventDefault()</code> 的拖拽监听不能声明为 <code>passive</code>，否则阻止默认行为无效并可能产生警告。应把非被动监听限制在确实需要拦截的具体区域，其余滚动或触摸观察监听保持被动；扩大阻塞范围会重新影响滚动响应。</p>\\n</li>\\n<li>\\n<p><strong>线上反馈滚动监听已加 <code>{ passive: true }</code>，页面仍明显卡顿，开发据此认定不是事件问题；你会要求他继续查什么？</strong></p>\\n<p><code>passive</code> 只让浏览器无需等待监听器决定是否阻止默认滚动，并不会让处理器里的重计算或频繁 <code>DOM</code> 更新变快。应在性能面板检查监听执行时间、布局与绘制开销，再节流或减少工作量；不要把被动监听当成通用性能开关。</p>\\n</li>\\n<li>\\n<p><strong>表单容器想统一监听子控件的焦点进入，团队在 <code>focus</code> 加 <code>{ capture: true }</code> 和改用 <code>focusin</code> 之间选型，你会怎么取舍？</strong></p>\\n<p><code>focus</code> 本身不冒泡，可在容器捕获阶段监听；<code>focusin</code> 会冒泡，更符合常规事件代理模型。若只需统一处理后代聚焦，两者都可行，应按现有事件架构保持一致；混用会让处理顺序和停止传播行为更难推断。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_18-dom事件流是什么\\"><a class=\\"header-anchor\\" href=\\"#_18-dom事件流是什么\\">#</a> 18 DOM事件流是什么？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>一次事件传播分捕获、目标、冒泡三个阶段</li>\\n<li>捕获从 <code>window</code> 向目标下行，冒泡从目标向祖先上行</li>\\n<li><code>event.target</code> 是原始目标，<code>event.currentTarget</code> 是当前执行监听器的节点</li>\\n<li><code>stopPropagation</code> 停止继续传播，<code>preventDefault</code> 只阻止默认行为，两者不是一回事</li>\\n</ul>\\n<p><strong><code>DOM</code> 事件流是事件从外层节点向目标捕获，到达目标后再向外层冒泡的完整传播过程。</strong> 捕获阶段沿 <code>window</code>、<code>document</code> 和祖先节点向下传播，目标阶段执行目标元素的监听器，冒泡阶段再按相反方向返回。传播中 <code>event.target</code> 始终是原始触发元素，<code>event.currentTarget</code> 则是当前执行监听器的元素，这也是事件委托能工作的基础。<code>stopPropagation()</code> 用来停止传播，<code>preventDefault()</code> 只阻止默认行为，两者不能混为一谈。</p>\\n</section>\\n<p>一次事件的传播分<strong>三个阶段</strong>，这是 <code>W3C</code> 定义的标准模型：</p>\\n<ol>\\n<li><strong>捕获阶段</strong>：事件从 <code>window</code> 出发，沿着祖先链<strong>自上而下</strong>传到目标节点的父级。</li>\\n<li><strong>目标阶段</strong>：到达真正被点击的那个元素，执行它身上的监听器。</li>\\n<li><strong>冒泡阶段</strong>：从目标节点<strong>自下而上</strong>回传到 <code>window</code>。</li>\\n</ol>\\n<pre class=\\"language-text\\"><code>        │ 捕获 ↓                    ↑ 冒泡 │\\n   window → document → html → body → div → button（目标）\\n</code></pre>\\n<p><strong>为什么会有两个方向？</strong>\\n这是历史妥协的产物 —— 当年 <code>Netscape</code> 主张捕获、<code>IE</code> 主张冒泡，<code>W3C</code> 干脆两个都保留。所以你会看到 <code>addEventListener</code> 的第三个参数就是在选&quot;我要在哪个阶段被触发&quot;。</p>\\n<pre class=\\"language-js\\"><code>parent.addEventListener(&#39;click&#39;, fn, true)   // 捕获阶段触发\\nparent.addEventListener(&#39;click&#39;, fn, false)  // 冒泡阶段触发（默认）\\n</code></pre>\\n<p><strong>两个关键属性，面试常考：</strong></p>\\n<ul>\\n<li><code>event.target</code> —— <strong>事件的原始触发元素</strong>，整个传播过程中不变。</li>\\n<li><code>event.currentTarget</code> —— <strong>当前正在执行监听器的元素</strong>，等于你绑定监听的那个节点，随传播变化。</li>\\n</ul>\\n<p>事件委托能成立，靠的正是这两者的分离：监听绑在父级（<code>currentTarget</code> 是父级），但从 <code>target</code> 能知道用户实际点了哪一项。</p>\\n<p><strong>两个&quot;阻止&quot;方法不是一回事：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>方法</th>\\n<th>作用</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>stopPropagation()</code></td>\\n<td>阻止事件继续传播到下一个节点</td>\\n</tr>\\n<tr>\\n<td><code>stopImmediatePropagation()</code></td>\\n<td>在上面基础上，连<strong>当前节点上后注册的监听器</strong>也不执行</td>\\n</tr>\\n<tr>\\n<td><code>preventDefault()</code></td>\\n<td>只阻止默认行为（链接跳转、表单提交），<strong>不影响传播</strong></td>\\n</tr>\\n</tbody>\\n</table>\\n<p>常见误解是&quot;<code>preventDefault</code> 能阻止冒泡&quot; —— 不能，两者完全正交。判断有没有被阻止可以读 <code>event.defaultPrevented</code>。</p>\\n<p><strong>一个实际场景</strong>：弹窗组件在 <code>document</code> 上监听点击来实现&quot;点外面关闭&quot;，结果打开弹窗的那次点击本身就把它关了。原因是打开动作的 <code>click</code> 事件继续冒泡到了 <code>document</code>。解法要么在按钮的处理函数里 <code>stopPropagation</code>，要么把关闭监听注册到下一个事件循环（<code>setTimeout 0</code>），要么判断 <code>e.target</code> 是否在弹窗内。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>后台页面里父级和按钮都监听了 <code>click</code>，父级用捕获、按钮用冒泡；同事认为按钮离点击位置最近，所以一定先执行，你怎么判断？</strong></p>\\n<p>父级的捕获监听会先于目标按钮执行，距离目标近不代表执行更早。事件先沿 <code>window</code> 到目标父级完成捕获，再进入目标阶段，最后向上冒泡；只有结合监听阶段和传播路径，才能确定顺序。</p>\\n</li>\\n<li>\\n<p><strong>商品列表有一万个动态条目，只允许在容器上绑定一个监听器；点击按钮里的 <code>svg</code> 时，怎样准确找到商品并避免误处理容器外节点？</strong></p>\\n<p>在容器的冒泡监听器中从 <code>event.target</code> 调用 <code>closest()</code> 查找条目，再确认结果仍被 <code>event.currentTarget</code> 包含。前者代表原始触发元素，后者是绑定监听器的容器；若条目通过 <code>Portal</code> 渲染到别处，委托不会自动覆盖。</p>\\n</li>\\n<li>\\n<p><strong>弹窗打开按钮和“点击外部关闭”都依赖 <code>click</code>，关闭逻辑刚注册到 <code>document</code>，结果弹窗打开后立刻消失，你会怎么改？</strong></p>\\n<p>这是打开按钮的同一次 <code>click</code> 继续冒泡到 <code>document</code>，新注册的关闭逻辑把它当成外部点击。可延迟到下一轮事件循环注册，或根据 <code>event.target</code> 是否位于弹窗内过滤；使用 <code>stopPropagation()</code> 虽直接，但会切断埋点和其他委托。</p>\\n</li>\\n<li>\\n<p><strong>第三方脚本在 <code>document</code> 的捕获阶段调用了 <code>stopPropagation()</code>，上线后页面内所有按钮都失效；你会沿什么路径定位？</strong></p>\\n<p>先在捕获监听器处断点，确认事件是否在到达目标前被截断，再检查祖先链上的监听阶段和注册位置。捕获阶段停止传播会让事件根本到不了按钮；若无法修改脚本，只能在更上游节点提前处理关键逻辑，但这只是高耦合补丁。</p>\\n</li>\\n<li>\\n<p><strong>安全团队要求拦截未授权点击，数据团队又要求全局埋点完整；应该把拦截放在捕获阶段并调用哪种阻止方法？</strong></p>\\n<p>捕获阶段适合在业务监听器前做权限判断，但不应无条件调用 <code>stopImmediatePropagation()</code>。它不仅阻止后续节点，还会取消当前节点后注册的监听器；更稳妥的是缩小拦截范围，并让埋点在拦截前记录或采用不依赖该冒泡链的采集方案。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_19-冒泡和捕获的具体过程\\"><a class=\\"header-anchor\\" href=\\"#_19-冒泡和捕获的具体过程\\">#</a> 19 冒泡和捕获的具体过程</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>捕获阶段从 <code>window</code>、<code>document</code> 沿祖先链向目标节点传播</li>\\n<li>到达目标后执行目标监听器，再进入从目标祖先向外的冒泡阶段</li>\\n<li>事件委托依赖冒泡；需要提前拦截或处理不冒泡事件时才重点考虑捕获</li>\\n</ul>\\n<p><strong>点击目标元素时，事件会先从 <code>window</code> 沿祖先链向下捕获，到达目标后，再沿原路径向上冒泡。</strong> 例如点击 <code>button</code>，路径会经过 <code>document</code>、<code>html</code>、<code>body</code> 和各级父元素，因此捕获监听器通常先于目标和冒泡监听器执行。工程里常利用冒泡把监听器绑在父级，再通过 <code>event.target.closest()</code> 找到实际操作项，这样动态插入的节点也不用重复注册。捕获更适合代理 <code>focus</code>、<code>blur</code> 等不冒泡事件，但祖先若在捕获阶段调用 <code>stopPropagation()</code>，事件可能到不了目标。</p>\\n</section>\\n<p>用一个具体例子把两个方向走一遍：</p>\\n<pre class=\\"language-html\\"><code>&lt;ul onclick=&quot;alert(&#39;ul&#39;)&quot;&gt;\\n  &lt;li onclick=&quot;alert(&#39;li&#39;)&quot;&gt;\\n    &lt;button onclick=&quot;alert(&#39;button&#39;)&quot;&gt;点击&lt;/button&gt;\\n  &lt;/li&gt;\\n&lt;/ul&gt;\\n&lt;script&gt;\\n  window.addEventListener(&#39;click&#39;, () =&gt; alert(&#39;window&#39;), false)   // 冒泡\\n  document.addEventListener(&#39;click&#39;, () =&gt; alert(&#39;document&#39;), true) // 捕获\\n&lt;/script&gt;\\n</code></pre>\\n<p>点击按钮时，浏览器实际走的是一条<strong>完整的往返路径</strong>：</p>\\n<pre class=\\"language-text\\"><code>捕获（向下）: window → document → html → body → ul → li\\n目标        : button\\n冒泡（向上）: li → ul → body → html → document → window\\n</code></pre>\\n<p>上面代码的实际弹出顺序是：<code>document</code>（捕获）→ <code>button</code> → <code>li</code> → <code>ul</code> → <code>window</code>。\\n注意 <code>document</code> 那个因为注册在捕获阶段，所以<strong>最先</strong>执行，尽管它在 DOM 树上离得最远。</p>\\n<p><strong>一个容易记混的点</strong>：<code>onclick</code> 这种内联写法和 <code>addEventListener(fn, false)</code> 一样，都是冒泡阶段。想在捕获阶段执行只能用 <code>addEventListener</code> 传 <code>true</code>。</p>\\n<p><strong>冒泡的工程价值 —— 事件委托：</strong></p>\\n<pre class=\\"language-js\\"><code>// ❌ 一千行绑一千个监听器\\ndocument.querySelectorAll(&#39;tr&#39;).forEach(tr =&gt; tr.addEventListener(&#39;click&#39;, fn))\\n\\n// ✅ 只绑一个，靠冒泡拿到具体行\\ntbody.addEventListener(&#39;click&#39;, (e) =&gt; {\\n  const tr = e.target.closest(&#39;tr&#39;)\\n  if (!tr) return\\n  handle(tr.dataset.id)\\n})\\n</code></pre>\\n<p>三个收益：<strong>内存省</strong>（一个监听器 vs 一千个）、<strong>绑定快</strong>（不用遍历）、<strong>动态节点免注册</strong>（新插入的行天生就能用）。</p>\\n<p>用 <code>closest</code> 而不是直接判断 <code>e.target</code> 很关键 —— 用户可能点在行内的图标或 <code>span</code> 上，那时 <code>e.target</code> 是子元素，直接比较会漏掉。</p>\\n<p><strong>什么时候必须用捕获：</strong></p>\\n<ol>\\n<li><strong>代理不冒泡的事件</strong>：<code>focus</code>、<code>blur</code> 不冒泡，但它们<strong>有捕获阶段</strong>，所以在父级用捕获能代理到（或者改用天生冒泡的 <code>focusin</code>/<code>focusout</code>）。</li>\\n<li><strong>需要抢先拦截</strong>：比如全局的埋点或权限校验，要在业务监听器之前执行。</li>\\n</ol>\\n<p><strong>一个反直觉的细节</strong>：<code>stopPropagation()</code> 在捕获阶段调用，会导致事件<strong>根本到不了目标元素</strong>，目标自己的监听器也不会执行。很多&quot;按钮点了没反应&quot;的玄学问题，源头就是某个祖先在捕获阶段做了拦截。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>页面依次在 <code>document</code> 捕获、按钮目标、列表项冒泡、列表冒泡和 <code>window</code> 冒泡上注册日志，点击按钮时同事给出的顺序是从按钮一路向外，你会如何纠正？</strong></p>\\n<p>实际顺序应是 <code>document</code> 捕获先执行，然后是按钮目标监听，接着依次执行列表项、列表和 <code>window</code> 的冒泡监听。事件会先沿祖先链向下捕获，再从目标向上冒泡；未注册监听器的节点仍属于传播路径，只是不产生日志。</p>\\n</li>\\n<li>\\n<p><strong>订单表格从一百行增长到动态加载的一千行，团队在“每行绑定监听器”和“给 <code>tbody</code> 做委托”之间争论，你选哪种实现？</strong></p>\\n<p>应优先在 <code>tbody</code> 上绑定一个冒泡监听器，并用 <code>event.target.closest(\'tr\')</code> 找到实际订单行。这样无需遍历绑定，新增行也天然生效；仍要校验命中的行属于当前 <code>tbody</code>，否则嵌套结构可能带来误匹配。</p>\\n</li>\\n<li>\\n<p><strong>统一高亮几十个输入框时，架构规范只允许父级监听，但 <code>focus</code> 委托没有触发；在不改每个输入框代码的前提下怎么处理？</strong></p>\\n<p>可把事件改为会冒泡的 <code>focusin</code>，或在父级用捕获阶段监听 <code>focus</code>。两者都能覆盖动态输入框，前者更符合委托语义；若项目还要兼顾其他焦点逻辑，应统一事件类型，避免两套监听造成重复处理。</p>\\n</li>\\n<li>\\n<p><strong>列表只有部分删除按钮失效，失效行里按钮又包了一层 <code>svg</code>，还有少数行通过 <code>Portal</code> 渲染到 <code>body</code>；你会怎样区分两个根因？</strong></p>\\n<p>先看容器监听器是否收到事件：收到但分支未命中，应检查是否错误地直接比较 <code>event.target</code>，改用 <code>closest()</code> 处理 <code>svg</code> 子节点。完全收不到时再核对真实 <code>DOM</code> 祖先链和捕获拦截；<code>Portal</code> 节点不在该容器下，原委托无法覆盖。</p>\\n</li>\\n<li>\\n<p><strong>全局权限校验想在业务按钮前执行，组件团队担心一次误拦会让整页点击失效；捕获阶段是否值得用？</strong></p>\\n<p>需要抢先校验时可以使用捕获阶段，但监听范围和停止传播条件必须足够精确。祖先在捕获阶段调用 <code>stopPropagation()</code> 会让事件到不了目标元素，因此应优先只做判定或标记，把真正拦截限制在明确的未授权操作上。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_20-关于一些兼容性\\"><a class=\\"header-anchor\\" href=\\"#_20-关于一些兼容性\\">#</a> 20 关于一些兼容性</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>兼容性先由目标用户和浏览器基线决定，不要凭习惯支持已经退出市场的版本</li>\\n<li>语法转换交给 <code>Babel</code> / <code>TypeScript</code>，API 缺失用按需 <code>polyfill</code>，样式差异用渐进增强和特性查询</li>\\n<li>用真实设备、自动化测试和兼容数据库验证，避免堆浏览器嗅探与私有前缀</li>\\n</ul>\\n<p><strong>兼容性治理要先确定浏览器基线，再分别处理语法、<code>API</code>、样式和测试。</strong> 我一般会根据真实用户的浏览器分布配置 <code>browserslist</code>，让 <code>Babel</code>、<code>PostCSS</code> 和 <code>ESLint</code> 共用同一套范围。老项目里的 <code>window.event</code>、<code>srcElement</code> 等写法需要看得懂，但 <code>IE</code> 已停止支持，新代码没必要继续堆这些兼容分支。</p>\\n</section>\\n<p>先说结论：<strong>这些 <code>IE</code> 兼容写法今天已经不该出现在新代码里</strong>。<code>IE</code> 全系已停止支持，主流框架、构建工具、<code>npm</code> 包都不再为它兼容。但这题仍然值得答，因为你会在老项目里遇到它们，得看得懂。</p>\\n<p><strong>四组经典的历史兼容写法：</strong></p>\\n<pre class=\\"language-js\\"><code>// 1. 事件对象：老 IE 用全局 window.event\\nvar e = ev || window.event\\n\\n// 2. 事件源：老 IE 用 srcElement\\nvar target = e.target || e.srcElement\\n\\n// 3. 阻止冒泡\\ne.stopPropagation ? e.stopPropagation() : (window.event.cancelBubble = true)\\n\\n// 4. 阻止默认行为\\ne.preventDefault ? e.preventDefault() : (window.event.returnValue = false)\\n</code></pre>\\n<p><strong>现在该怎么做兼容性治理</strong>（这才是面试官真正想听的）：</p>\\n<p><strong>第一步：先定基线。</strong> 兼容范围由<strong>目标用户</strong>决定，不是凭习惯。拉埋点看真实的浏览器分布，用 <code>browserslist</code> 把结论写进配置，让整条工具链（<code>Babel</code>、<code>PostCSS</code>、<code>ESLint</code>）共用同一个基线：</p>\\n<pre class=\\"language-text\\"><code># .browserslistrc\\n&gt; 0.5%\\nlast 2 versions\\nnot dead\\n</code></pre>\\n<p><strong>第二步：分层处理。</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>差异类型</th>\\n<th>处理方式</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><strong>语法</strong>（箭头函数、可选链）</td>\\n<td><code>Babel</code> / <code>TypeScript</code> 编译降级</td>\\n</tr>\\n<tr>\\n<td><strong>API</strong>（<code>Promise</code>、<code>Array.at</code>）</td>\\n<td><code>core-js</code> 按需 polyfill（<code>useBuiltIns: \'usage\'</code>）</td>\\n</tr>\\n<tr>\\n<td><strong>CSS 属性</strong></td>\\n<td><code>Autoprefixer</code> 加前缀 + <code>@supports</code> 特性查询做渐进增强</td>\\n</tr>\\n<tr>\\n<td><strong>浏览器 bug</strong></td>\\n<td>具体问题具体处理，并写注释说明触发条件</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>第三步：用特性检测，不要 UA 嗅探。</strong></p>\\n<pre class=\\"language-js\\"><code>// ❌ UA 会被伪造、会变，永远维护不完\\nif (/MSIE/.test(navigator.userAgent)) { ... }\\n\\n// ✅ 直接问&quot;这个能力在不在&quot;\\nif (&#39;IntersectionObserver&#39; in window) { ... } else { /* 降级 */ }\\n</code></pre>\\n<p><code>CSS</code> 里对应的是 <code>@supports</code>：</p>\\n<pre class=\\"language-css\\"><code>.grid { display: flex; }                       /* 兜底 */\\n@supports (display: grid) { .grid { display: grid; } }\\n</code></pre>\\n<p><strong>第四步：验证。</strong> 用 <code>caniuse</code> 查支持度、用真机或 <code>BrowserStack</code> 跑关键路径，别只在自己的 <code>Chrome</code> 上测。最容易翻车的不是老 <code>IE</code>，而是<strong>低端安卓的自带浏览器</strong>和 <strong><code>iOS</code> 上落后一两个版本的 <code>Safari</code></strong> —— 后者因为系统绑定，用户升级很慢，是现在真正的兼容性长尾。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>项目写了 <code>.browserslistrc</code>，同事认为它会自动让所有新语法、新 <code>API</code> 和 <code>CSS</code> 在目标浏览器可用；构建评审会上你怎么反驳？</strong></p>\\n<p><code>.browserslistrc</code> 只描述目标浏览器范围，本身不会自动补齐兼容能力。语法要交给 <code>Babel</code> 或 <code>TypeScript</code> 降级，<code>API</code> 需要 <code>core-js</code> 等 <code>polyfill</code>，<code>CSS</code> 则由 <code>Autoprefixer</code> 和 <code>@supports</code> 处理；浏览器自身缺陷仍需单独修复。</p>\\n</li>\\n<li>\\n<p><strong>内容站准备采用 <code>&gt; 0.5%</code>、<code>last 2 versions</code>、<code>not dead</code>，产品又要求覆盖主要访问人群；你会怎样验证这份配置而不是直接接受？</strong></p>\\n<p>先把目标范围与站点真实访问数据和业务要求对照，再用 <code>caniuse</code> 检查关键能力是否覆盖。该配置只是通用基线，不能替代用户分布判断；尤其要用真机或 <code>BrowserStack</code> 验证低端安卓浏览器及较旧的 <code>iOS Safari</code> 关键路径。</p>\\n</li>\\n<li>\\n<p><strong>旧浏览器能解析打包后的可选链，却在运行 <code>Array.at()</code> 时白屏；构建负责人说“已经经过 <code>Babel</code>，不该有兼容问题”，你怎么处理？</strong></p>\\n<p>这是把语法降级和 <code>API</code> 补齐混为一谈，<code>Babel</code> 处理可选链并不必然提供 <code>Array.at()</code>。应依据目标浏览器配置启用按需 <code>polyfill</code>，例如 <code>core-js</code> 的 <code>useBuiltIns: \'usage\'</code>，同时检查最终产物，避免漏引或重复引入。</p>\\n</li>\\n<li>\\n<p><strong>设计稿要求使用新的 <code>CSS</code> 布局能力，但目标范围内部分浏览器不支持，设计师拒绝改视觉、研发又不想维护两套页面；你会给出什么落地方案？</strong></p>\\n<p>先提供可工作的基础样式，再用 <code>@supports</code> 为支持该能力的浏览器启用增强布局，并让 <code>Autoprefixer</code> 处理适用的前缀。渐进增强只能接受体验差异，若不支持会导致核心内容不可用，就必须调整方案或提供完整降级实现。</p>\\n</li>\\n<li>\\n<p><strong>线上只有某款内置浏览器出现功能空白，用户代理字符串又与常规浏览器相似；你会用 <code>UA</code> 分支热修还是改成能力检测？</strong></p>\\n<p>应优先检测实际能力，例如判断目标 <code>API</code> 是否存在，并在缺失时进入降级路径。<code>UA</code> 可能被伪装且规则会持续变化，容易把相同内核或新版本误判；若确认是特定浏览器缺陷，才增加范围明确且带触发条件说明的修复。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_21-如何阻止冒泡和默认事件-兼容写法\\"><a class=\\"header-anchor\\" href=\\"#_21-如何阻止冒泡和默认事件-兼容写法\\">#</a> 21 如何阻止冒泡和默认事件(兼容写法)</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>event.stopPropagation()</code> 阻止继续传播，<code>stopImmediatePropagation()</code> 还会阻止当前节点后续监听器</li>\\n<li><code>event.preventDefault()</code> 阻止链接跳转、表单提交等默认行为</li>\\n<li>被动监听器中不能阻止默认行为；旧 <code>cancelBubble</code>、<code>returnValue</code> 只作为历史兼容了解</li>\\n</ul>\\n<p><strong>阻止事件传播用 <code>stopPropagation()</code>，阻止浏览器默认行为用 <code>preventDefault()</code>，两者互不替代。</strong> 如果还要阻止同一节点后注册的监听器执行，可以用 <code>stopImmediatePropagation()</code>；而在 <code>addEventListener</code> 回调中写 <code>return false</code> 并不起作用。需要注意，<code>passive</code> 监听器或 <code>cancelable</code> 为 <code>false</code> 的事件无法通过 <code>preventDefault()</code> 阻止默认行为。实际开发也别随手停止冒泡，否则上层事件委托和埋点可能一起失效。</p>\\n</section>\\n<p><strong>先分清这是两件完全不同的事</strong>，这是这道题真正的考点：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>方法</th>\\n<th>作用</th>\\n<th>典型场景</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>stopPropagation()</code></td>\\n<td>阻止事件继续向上（或向下）传播</td>\\n<td>弹窗内部点击不触发&quot;点外面关闭&quot;</td>\\n</tr>\\n<tr>\\n<td><code>stopImmediatePropagation()</code></td>\\n<td>上面的基础上，同一节点上<strong>后注册的监听器</strong>也不执行</td>\\n<td>拦截第三方脚本的同名监听</td>\\n</tr>\\n<tr>\\n<td><code>preventDefault()</code></td>\\n<td>阻止浏览器的默认行为</td>\\n<td>阻止 <code>a</code> 跳转、阻止表单提交</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>它们互不影响</strong>：<code>preventDefault</code> 之后事件照样冒泡，<code>stopPropagation</code> 之后链接照样会跳。常见误解是以为 <code>return false</code> 能一次搞定 —— 在<strong>内联 <code>onclick</code></strong> 和 <strong>jQuery</strong> 里确实等价于两个都做，但在 <code>addEventListener</code> 的回调里 <code>return false</code> <strong>什么都不做</strong>。</p>\\n<pre class=\\"language-js\\"><code>// 现代写法，不需要任何兼容分支\\nel.addEventListener(&#39;click&#39;, (e) =&gt; {\\n  e.preventDefault()    // 不跳转\\n  e.stopPropagation()   // 不冒泡\\n})\\n</code></pre>\\n<p><strong>历史兼容写法</strong>（老项目里能见到，新代码不该再写）：</p>\\n<pre class=\\"language-javascript\\"><code>function stopBubble(e) {\\n  if (e &amp;&amp; e.stopPropagation) e.stopPropagation()\\n  else window.event.cancelBubble = true      // IE8-\\n}\\nfunction stopDefault(e) {\\n  if (e &amp;&amp; e.preventDefault) e.preventDefault()\\n  else window.event.returnValue = false      // IE8-\\n}\\n</code></pre>\\n<p><strong>三个容易踩的坑：</strong></p>\\n<p><strong>1. <code>passive</code> 监听器里 <code>preventDefault</code> 无效。</strong>\\n<code>Chrome</code> 已经把 <code>document</code>/<code>window</code> 上的 <code>touchstart</code>、<code>touchmove</code>、<code>wheel</code> 默认设成了 <code>passive: true</code>，所以老代码里靠这些事件&quot;阻止页面滚动&quot;的逻辑可能已经悄悄失效了。需要阻止就必须显式传 <code>{ passive: false }</code>。</p>\\n<p><strong>2. 不是所有默认行为都能阻止。</strong>\\n<code>event.cancelable</code> 为 <code>false</code> 的事件调 <code>preventDefault</code> 没有任何效果。另外浏览器的安全策略（比如 <code>Ctrl+W</code> 关标签页）也不受页面控制。</p>\\n<p><strong>3. <code>stopPropagation</code> 是把双刃剑。</strong>\\n它会切断事件流，导致<strong>上层的事件委托、埋点、全局快捷键统统失效</strong>。很多&quot;点击埋点丢了一部分&quot;的问题，根源就是某个组件里随手加了一句 <code>stopPropagation</code>。更稳妥的替代方案是在上层判断 <code>e.target</code> 是否在自己关心的范围内，而不是让下层去阻止传播。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>单页应用的链接监听器里只调用了 <code>stopPropagation()</code>，测试却发现浏览器仍然跳转；同事认为阻止冒泡就该连默认行为一起取消，你怎么解释并修复？</strong></p>\\n<p>传播与默认行为相互独立，<code>stopPropagation()</code> 只切断事件流，不会阻止链接跳转。若路由要接管导航，应另行调用 <code>preventDefault()</code>；反过来只阻止默认行为时，事件仍会继续冒泡到上层监听器。</p>\\n</li>\\n<li>\\n<p><strong>原生组件用 <code>addEventListener</code> 绑定提交事件，回调里 <code>return false</code> 后表单仍提交；迁移自 <code>jQuery</code> 的同事坚持写法没问题，你怎么改？</strong></p>\\n<p>原生 <code>addEventListener</code> 回调中的 <code>return false</code> 不会阻止传播或默认行为，应显式调用 <code>preventDefault()</code>，需要时再调用 <code>stopPropagation()</code>。只有内联 <code>onclick</code> 和 <code>jQuery</code> 等特定语境赋予了 <code>return false</code> 额外含义，迁移时不能照搬。</p>\\n</li>\\n<li>\\n<p><strong>移动端弹层打开后要禁止背景滚动，代码在 <code>document</code> 的 <code>touchmove</code> 中调用 <code>preventDefault()</code>，线上仍能滚动且控制台有被动监听提示，怎么处理？</strong></p>\\n<p>该监听很可能以 <code>passive: true</code> 执行，因此 <code>preventDefault()</code> 会被忽略，需要显式注册为 <code>{ passive: false }</code>。处理前还应检查 <code>event.cancelable</code>；持续拦截滚动可能影响性能，能用 <code>overflow: hidden</code> 或 <code>overscroll-behavior</code> 完成时更稳妥。</p>\\n</li>\\n<li>\\n<p><strong>卡片组件加了 <code>stopPropagation()</code> 后，挂在 <code>document</code> 的点击埋点和列表委托同时丢数据；线上排查时你会怎样确认并修复？</strong></p>\\n<p>先在卡片和 <code>document</code> 的监听器处记录传播路径，确认事件是否在组件内部被截断。通常应移除无差别的 <code>stopPropagation()</code>，让上层依据 <code>event.target.closest()</code> 判断是否处理；确需拦截时，要明确埋点补偿及受影响的全局逻辑。</p>\\n</li>\\n<li>\\n<p><strong>第三方脚本和业务代码在同一节点监听 <code>click</code>，安全团队要求让后注册的第三方回调不执行；该用 <code>stopPropagation()</code> 还是 <code>stopImmediatePropagation()</code>？</strong></p>\\n<p>若目标是连同一节点上后注册的监听器也不执行，需要 <code>stopImmediatePropagation()</code>，普通的 <code>stopPropagation()</code> 只阻止传播到后续节点。该做法依赖注册顺序且会误伤其他监听器，应限定到明确风险事件，不能作为通用隔离机制。</p>\\n</li>\\n<li>\\n<p><strong>维护一个仍含 <code>window.event.cancelBubble</code> 和 <code>returnValue</code> 的老后台时，新代码评审要求继续复制兼容分支；你会接受吗？</strong></p>\\n<p>若目标环境只需现代浏览器，新代码应直接使用 <code>stopPropagation()</code> 和 <code>preventDefault()</code>，不再扩散历史分支。旧写法可暂留在明确覆盖旧版 <code>IE</code> 的兼容层中；删除前仍要依据项目的浏览器目标和实际验证结果，而不能只凭代码年代判断。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_22-所有的事件都有冒泡吗\\"><a class=\\"header-anchor\\" href=\\"#_22-所有的事件都有冒泡吗\\">#</a> 22 所有的事件都有冒泡吗？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>不是所有事件都冒泡，是否冒泡可看事件的 <code>bubbles</code> 属性</li>\\n<li><code>focus</code> / <code>blur</code> 不冒泡，可用 <code>focusin</code> / <code>focusout</code> 或捕获阶段代理</li>\\n<li><code>mouseenter</code> / <code>mouseleave</code> 不冒泡，委托常改用 <code>mouseover</code> / <code>mouseout</code> 并判断关联目标</li>\\n</ul>\\n<p><strong>不是所有事件都会冒泡，是否冒泡应以事件的 <code>bubbles</code> 属性为准。</strong> 比如 <code>click</code> 会冒泡，但 <code>focus</code> 和 <code>mouseenter</code> 不会，因此直接依赖冒泡做事件委托可能收不到它们。焦点场景可以改用 <code>focusin</code>，也可以在捕获阶段监听 <code>focus</code>；悬停委托则可用 <code>mouseover</code>，并结合 <code>relatedTarget</code> 排除元素内部移动。简单来说，委托前要先确认事件特性，再决定换等价事件还是使用捕获。</p>\\n</section>\\n<p>不是。<strong>是否冒泡由事件自己的 <code>bubbles</code> 属性决定</strong>，可以直接在控制台验证：</p>\\n<pre class=\\"language-js\\"><code>el.addEventListener(&#39;focus&#39;, (e) =&gt; console.log(e.bubbles))  // false\\nel.addEventListener(&#39;click&#39;, (e) =&gt; console.log(e.bubbles))  // true\\n</code></pre>\\n<p><strong>常见的不冒泡事件：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>事件</th>\\n<th>冒泡版替代</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>focus</code> / <code>blur</code></td>\\n<td><code>focusin</code> / <code>focusout</code></td>\\n</tr>\\n<tr>\\n<td><code>mouseenter</code> / <code>mouseleave</code></td>\\n<td><code>mouseover</code> / <code>mouseout</code></td>\\n</tr>\\n<tr>\\n<td><code>load</code> / <code>error</code>（资源）</td>\\n<td>无，只能用捕获阶段代理</td>\\n</tr>\\n<tr>\\n<td><code>abort</code>、<code>unload</code>、<code>resize</code>（部分）</td>\\n<td>—</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>这件事的实际影响：事件委托会失效。</strong></p>\\n<pre class=\\"language-js\\"><code>// ❌ 收不到任何东西 —— focus 不冒泡\\nform.addEventListener(&#39;focus&#39;, highlight)\\n\\n// ✅ 方案一：用冒泡版本\\nform.addEventListener(&#39;focusin&#39;, highlight)\\n\\n// ✅ 方案二：用捕获阶段（不冒泡的事件仍然有捕获阶段）\\nform.addEventListener(&#39;focus&#39;, highlight, true)\\n</code></pre>\\n<p><strong><code>mouseenter</code> 和 <code>mouseover</code> 的区别值得单独说</strong>，它是这题的高频延伸：</p>\\n<ul>\\n<li><code>mouseover</code> <strong>冒泡</strong>，而且<strong>进入子元素时会重新触发</strong> —— 鼠标从卡片移到卡片里的按钮上，会先 <code>mouseout</code> 再 <code>mouseover</code>，做悬停高亮就会疯狂闪烁。</li>\\n<li><code>mouseenter</code> <strong>不冒泡</strong>，且<strong>只在真正进入元素边界时触发一次</strong>，移到子元素上不会重复触发，做悬停效果更省心。</li>\\n</ul>\\n<p>所以要委托就用 <code>mouseover</code> 并判断 <code>e.relatedTarget</code> 排除内部移动；只管一个元素就用 <code>mouseenter</code>。</p>\\n<pre class=\\"language-js\\"><code>list.addEventListener(&#39;mouseover&#39;, (e) =&gt; {\\n  const item = e.target.closest(&#39;.item&#39;)\\n  if (!item) return\\n  // 排除&quot;从 item 内部移到 item 内部&quot;这种情况\\n  if (item.contains(e.relatedTarget)) return\\n  highlight(item)\\n})\\n</code></pre>\\n<p><strong>面试怎么答</strong>：先说&quot;看 <code>bubbles</code>&quot;这个判断依据，再举 <code>focus</code> 和 <code>mouseenter</code> 两个例子，最后说清替代方案 —— 比背一张&quot;不冒泡事件表&quot;更能体现你真的用过。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>表单容器委托 <code>focus</code> 没收到事件，同事据此断言“没有冒泡就完全不能被祖先监听”，你会拿什么代码行为反驳？</strong></p>\\n<p><code>focus</code> 的 <code>bubbles</code> 为 <code>false</code>，但它仍有捕获阶段，因此父级使用 <code>addEventListener(\'focus\', handler, true)</code> 可以收到。也可改用会冒泡的 <code>focusin</code>；选择前应确认项目是否已监听另一种焦点事件，避免同一状态被更新两次。</p>\\n</li>\\n<li>\\n<p><strong>动态表单有几十个输入框，需要在聚焦和失焦时统一更新外层样式，又不允许逐个绑定监听器；你会怎样设计委托？</strong></p>\\n<p>可在表单容器上监听 <code>focusin</code> 和 <code>focusout</code>，根据 <code>event.target</code> 找到对应字段容器并更新样式。它们会冒泡，新增输入框无需重新注册；若改用 <code>focus</code> 和 <code>blur</code>，则必须明确启用捕获阶段。</p>\\n</li>\\n<li>\\n<p><strong>商品卡片列表用 <code>mouseover</code> 委托悬停效果，鼠标从卡片标题移到内部按钮时高亮反复闪烁；不能改成逐卡绑定时怎么修？</strong></p>\\n<p>继续使用会冒泡的 <code>mouseover</code>，但先用 <code>closest()</code> 找到卡片，再检查该卡片是否包含 <code>event.relatedTarget</code>。若包含，说明只是卡片内部移动，应直接忽略；单个元素监听才更适合使用不冒泡且不会因进入子元素重复触发的 <code>mouseenter</code>。</p>\\n</li>\\n<li>\\n<p><strong>图片列表在容器上用冒泡阶段委托 <code>error</code> 做占位图兜底，发布后所有破图都未被替换；你会怎样定位？</strong></p>\\n<p>资源 <code>error</code> 事件不冒泡，所以普通委托监听不会触发，应在容器或更上层节点使用捕获阶段监听。处理时还要检查 <code>event.target</code> 确实是目标资源元素；全局监听若不区分资源错误和脚本运行错误，容易误处理其他故障。</p>\\n</li>\\n<li>\\n<p><strong>组件库想统一规定“所有事件都用父级冒泡委托”，以减少监听器数量；评审时你会接受这条规范吗？</strong></p>\\n<p>不能把冒泡委托设为无条件规则，是否可用应先看事件的 <code>bubbles</code> 属性。<code>focus</code> 可换成 <code>focusin</code>，资源 <code>load</code> 或 <code>error</code> 只能借助捕获，而 <code>mouseenter</code> 若改成 <code>mouseover</code> 还需过滤内部移动；统一形式会隐藏语义和边界差异。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_23-拖拽有哪些知识点\\"><a class=\\"header-anchor\\" href=\\"#_23-拖拽有哪些知识点\\">#</a> 23 拖拽有哪些知识点</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>原生拖放关注 <code>dragstart</code>、<code>dragover</code>、<code>drop</code> 与 <code>DataTransfer</code></li>\\n<li>自定义拖拽通常用 <code>Pointer Events</code> 统一鼠标、触控和触笔，移动过程用坐标差更新位置</li>\\n<li>生产实现要处理指针捕获、边界限制、滚动、性能、键盘替代与释放监听器</li>\\n</ul>\\n<p><strong>拖拽通常分为原生 <code>HTML5</code> 拖放和基于 <code>Pointer Events</code> 的自定义拖动，两者适用场景不同。</strong> 从桌面拖文件或跨窗口传数据时，用 <code>dragstart</code>、<code>dragover</code>、<code>drop</code> 和 <code>DataTransfer</code>，其中 <code>dragover</code> 必须调用 <code>preventDefault()</code>，否则不会触发 <code>drop</code>。页面内部排序或面板移动更适合 <code>Pointer Events</code>，因为它能统一鼠标、触摸和触笔，并通过指针捕获避免移出元素后中断。生产实现还要用 <code>transform</code> 配合 <code>requestAnimationFrame</code> 更新位置，同时处理边界、滚动、<code>touch-action</code> 和键盘替代操作。</p>\\n</section>\\n<p>拖拽题要先分清两条完全不同的技术路线，答错方向会很吃亏：</p>\\n<p><strong>路线一：原生 HTML5 拖放（<code>draggable</code>）</strong></p>\\n<p>适合<strong>跨应用/跨窗口传递数据</strong>，尤其是从桌面拖文件进来。</p>\\n<pre class=\\"language-html\\"><code>&lt;div draggable=&quot;true&quot;&gt;可拖&lt;/div&gt;\\n</code></pre>\\n<p>（<code>img</code> 和 <code>a</code> 默认就可拖）</p>\\n<ul>\\n<li>拖动源事件：<code>dragstart</code> → <code>drag</code> → <code>dragend</code></li>\\n<li>目标区事件：<code>dragenter</code> → <code>dragover</code> → <code>dragleave</code> → <code>drop</code></li>\\n<li><strong><code>dragover</code> 里必须 <code>preventDefault()</code>，否则 <code>drop</code> 永远不触发</strong></li>\\n<li>数据通过 <code>e.dataTransfer</code> 传递，拖文件时读 <code>dataTransfer.files</code></li>\\n</ul>\\n<p>它的硬伤是：<strong>移动端完全不支持</strong>（触屏不派发这些事件）、拖动预览难定制、键盘用户无法操作。</p>\\n<p><strong>路线二：<code>Pointer Events</code> 自定义拖动</strong></p>\\n<p>看板排序、可拖拽面板这类交互都走这条路，因为它统一了鼠标、触摸、触笔：</p>\\n<pre class=\\"language-js\\"><code>el.addEventListener(&#39;pointerdown&#39;, (e) =&gt; {\\n  el.setPointerCapture(e.pointerId)   // 关键：指针移出元素也能继续收到事件\\n  startX = e.clientX; startY = e.clientY\\n})\\nel.addEventListener(&#39;pointermove&#39;, (e) =&gt; {\\n  if (!el.hasPointerCapture(e.pointerId)) return\\n  // 用 transform 而不是 left/top —— 前者不触发重排\\n  el.style.transform = `translate(${e.clientX - startX}px, ${e.clientY - startY}px)`\\n})\\nel.addEventListener(&#39;pointerup&#39;, (e) =&gt; el.releasePointerCapture(e.pointerId))\\n</code></pre>\\n<p><strong>生产实现必须处理的六件事：</strong></p>\\n<ol>\\n<li><strong>指针捕获</strong>：不调 <code>setPointerCapture</code>，鼠标一移出元素就丢事件，拖动&quot;断线&quot;。</li>\\n<li><strong>用 <code>transform</code> 位移</strong>：改 <code>left</code>/<code>top</code> 每帧触发重排，低端机必掉帧。</li>\\n<li><strong><code>touch-action: none</code></strong>：不设置的话浏览器会把手势当成滚动，<code>pointermove</code> 直接被抢走。</li>\\n<li><strong>边界与滚动</strong>：拖到容器边缘要自动滚动，还要限制不能拖出可视区。</li>\\n<li><strong>节流</strong>：<code>pointermove</code> 触发频率极高，用 <code>requestAnimationFrame</code> 保证一帧最多更新一次。</li>\\n<li><strong>键盘替代路径</strong>：只能鼠标拖的功能对键盘用户等于不存在，要提供&quot;上移/下移&quot;按钮或方向键操作。</li>\\n</ol>\\n<p><strong>怎么选</strong>：需要从桌面拖文件进来 → 原生拖放；页面内部的排序、拖动 → <code>Pointer Events</code>（实际项目里直接上 <code>dnd-kit</code>/<code>sortablejs</code>，它们把上面六件事都处理好了，还带无障碍支持）。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>文件上传页既要支持从桌面拖入文件，又要支持在手机上拖动附件调整顺序，产品坚持只维护一套拖拽代码，你会接受吗？</strong></p>\\n<p>不会把两种交互强行塞进同一路线：桌面文件拖入依赖原生 <code>HTML5</code> 拖放和 <code>dataTransfer.files</code>，附件排序则应使用 <code>Pointer Events</code>。原生拖放在触屏设备上不派发对应事件，统一实现会直接牺牲移动端排序能力，最多只能复用业务状态更新逻辑。</p>\\n</li>\\n<li>\\n<p><strong>看板卡片用 <code>pointerdown</code>、<code>pointermove</code>、<code>pointerup</code> 实现，鼠标快速移出卡片后拖动立刻断掉，但慢慢拖没有问题，你会改哪一处？</strong></p>\\n<p>应在 <code>pointerdown</code> 中调用 <code>setPointerCapture(e.pointerId)</code>，让卡片在指针移出自身后仍能接收后续事件，并在结束时释放捕获。还要按同一个 <code>pointerId</code> 判断拖动状态；只把监听器挂到更大的容器上不能完整替代指针捕获。</p>\\n</li>\\n<li>\\n<p><strong>移动端排班表已经改用 <code>Pointer Events</code>，但拖动卡片时页面仍跟着滚，偶尔还收不到后续 <code>pointermove</code>，事件里持续 <code>preventDefault()</code> 是否足够？</strong></p>\\n<p>应在可拖区域配置 <code>touch-action: none</code>，提前声明该手势不交给浏览器执行滚动或缩放，否则浏览器可能接管手势。事件回调里的临时阻止不能替代这项手势协商；若页面仍需纵向滚动，就要缩小禁用范围或设计专用拖动手柄。</p>\\n</li>\\n<li>\\n<p><strong>低端安卓上拖动两百张看板卡片明显掉帧，代码在每次 <code>pointermove</code> 中读取布局后修改 <code>left</code> 和 <code>top</code>，你会怎样定位并改造？</strong></p>\\n<p>先用性能面板确认高频回调中是否反复布局，再把拖动位移改为 <code>transform</code>，避免每帧通过 <code>left</code>、<code>top</code> 触发重排。位置更新用 <code>requestAnimationFrame</code> 合并到一帧一次，边界等稳定数据尽量在拖动开始时缓存；自动滚动时仍需谨慎刷新相关几何信息。</p>\\n</li>\\n<li>\\n<p><strong>运营后台只在桌面浏览器使用，团队在原生拖放与 <code>dnd-kit</code> 之间争论看板排序方案，你会依据什么拍板？</strong></p>\\n<p>页面内部排序仍应优先选择基于指针交互的成熟拖拽库，因为原生 <code>draggable</code> 的预览定制和键盘操作能力都较弱。库能集中处理捕获、边界滚动、节流和无障碍，但会引入依赖与适配成本；若只是接收桌面文件，原生拖放反而更直接。</p>\\n</li>\\n<li>\\n<p><strong>无障碍验收发现任务列表只能用鼠标排序，二十个条目没有任何键盘入口，你会如何补齐而不改变现有拖动手感？</strong></p>\\n<p>保留指针拖动，同时增加独立的键盘替代路径，例如聚焦条目后用方向键调整，或提供明确的“上移”“下移”按钮。状态变更应落到同一套排序逻辑，避免两种入口结果不一致；仅添加 <code>tabindex</code> 只能让元素获得焦点，并不等于可完成排序。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_24-offset、scroll、client的区别\\"><a class=\\"header-anchor\\" href=\\"#_24-offset、scroll、client的区别\\">#</a> 24 offset、scroll、client的区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>clientWidth</code> / <code>clientHeight</code> 包含内边距，不含边框和滚动条</li>\\n<li><code>offsetWidth</code> / <code>offsetHeight</code> 包含内边距、边框和滚动条，通常是布局占用尺寸</li>\\n<li><code>scrollWidth</code> / <code>scrollHeight</code> 表示完整可滚动内容尺寸，<code>scrollTop</code> / <code>scrollLeft</code> 表示滚动偏移</li>\\n<li>读取这些布局属性可能触发同步布局，循环中避免和样式写入交错</li>\\n</ul>\\n<p><strong><code>client</code> 表示可视区域，<code>offset</code> 表示元素布局尺寸和位置，<code>scroll</code> 表示滚动距离及完整内容尺寸。</strong> <code>clientWidth</code>、<code>clientHeight</code> 不包含边框，而 <code>clientLeft</code>、<code>clientTop</code> 可以取得边框宽度。<code>offsetWidth</code>、<code>offsetHeight</code> 包含边框，<code>offsetLeft</code>、<code>offsetTop</code> 描述元素相对页面的位置。内容发生溢出时，我一般用 <code>scrollTop</code>、<code>scrollLeft</code> 读取滚动距离，用 <code>scrollWidth</code>、<code>scrollHeight</code> 获取完整区域大小。</p>\\n</section>\\n<p><strong>client</strong>:</p>\\n<ul>\\n<li><code>oEvent.clientX</code>是指鼠标到可视区左边框的距离。</li>\\n<li><code>oEvent.clientY</code>是指鼠标到可视区上边框的距离。</li>\\n<li><code>clientWidth</code>是指可视区的宽度。</li>\\n<li><code>clientHeight</code>是指可视区的高度。</li>\\n<li><code>clientLeft</code>获取左边框的宽度。</li>\\n<li><code>clientTop</code>获取上边框的宽度。</li>\\n</ul>\\n<p><strong>offset</strong>:</p>\\n<ul>\\n<li><code>offsetWidth</code>是指div的宽度（包括div的边框）</li>\\n<li><code>offsetHeight</code>是指div的高度（包括div的边框）</li>\\n<li><code>offsetLeft</code>是指div到整个页面左边框的距离（不包括div的边框）</li>\\n<li><code>offsetTop</code>是指div到整个页面上边框的距离（不包括div的边框）</li>\\n</ul>\\n<p><strong>scroll</strong>:</p>\\n<ul>\\n<li><code>scrollTop</code>是指可视区顶部边框与整个页面上部边框的看不到的区域。</li>\\n<li><code>scrollLeft</code>是指可视区左边边框与整个页面左边边框的看不到的区域。</li>\\n<li><code>scrollWidth</code>是指左边看不到的区域加可视区加右边看不到的区域即整个页面的宽度（包括边框）</li>\\n<li><code>scrollHeight</code>是指上边看不到的区域加可视区加右边看不到的区域即整个页面的高度（包括边框）</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>弹窗内容区设置了 <code>border: 8px</code>，设计稿要求按可滚动内容的可视高度分页，同事却用 <code>offsetHeight</code> 计算每页高度，翻页为什么会逐渐错位？</strong></p>\\n<p><code>offsetHeight</code> 包含元素边框，不等于内容可滚动区域的可视高度，这里应以 <code>clientHeight</code> 为分页基准。每页都把边框计入位移会不断累积偏差；若还有内边距或动态尺寸变化，仍要结合实际盒模型重新测量。</p>\\n</li>\\n<li>\\n<p><strong>商品列表容器显示十条数据、总共加载了一百条，代码要判断是否滚到底部，你会如何组合 <code>scrollTop</code>、<code>clientHeight</code> 和 <code>scrollHeight</code>？</strong></p>\\n<p>判断关系应是已滚过的距离 <code>scrollTop</code> 加可视区高度 <code>clientHeight</code> 接近整体滚动高度 <code>scrollHeight</code>。工程上宜保留小容差，而不是依赖严格相等；内容继续插入、图片改变高度或用户反向滚动时，需要在新的布局状态下重新判断。</p>\\n</li>\\n<li>\\n<p><strong>画布编辑器要把鼠标落点换算成页面内坐标，开发直接把 <code>event.clientX</code> 当成页面横坐标，页面横向滚动三百像素后选区整体偏移，原因是什么？</strong></p>\\n<p><code>clientX</code> 表示指针到浏览器可视区左边缘的距离，不包含页面已经滚走的部分，所以滚动后不能直接作为页面坐标。需要结合相应滚动偏移完成坐标系转换；若目标位于独立滚动容器，还必须区分页面滚动与容器 <code>scrollLeft</code>。</p>\\n</li>\\n<li>\\n<p><strong>侧栏碰撞检测使用子元素的 <code>offsetLeft</code>，把组件移进新的定位容器后所有命中区域都偏了，排查时你先确认什么？</strong></p>\\n<p>先确认 <code>offsetLeft</code> 所依据的布局参照和当前元素层级，不能把它无条件理解为相对浏览器页面左边缘的绝对距离。组件更换父级或定位环境后，原有坐标假设可能失效；碰撞计算必须统一坐标系，并把边框与滚动偏移是否计入写清楚。</p>\\n</li>\\n<li>\\n<p><strong>虚拟列表需要持续获知滚动位置和容器尺寸，团队准备在每次 <code>scroll</code> 中读取整套 <code>offset</code>、<code>client</code>、<code>scroll</code> 属性，你会如何控制风险？</strong></p>\\n<p>只读取算法真正需要的 <code>scrollTop</code>、<code>clientHeight</code> 与 <code>scrollHeight</code>，并避免在同一高频回调里交替修改样式和读取布局属性。稳定尺寸可缓存，尺寸变化再重新计算；缓存过度则会在容器缩放或内容高度变化后产生陈旧数据。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_25-target-blank-有哪些问题\\"><a class=\\"header-anchor\\" href=\\"#_25-target-blank-有哪些问题\\">#</a> 25 target=&quot;_blank&quot;有哪些问题？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>新页面可能通过 <code>window.opener</code> 操作来源页面，形成反向标签劫持风险</li>\\n<li>外链应配 <code>rel=&quot;noopener noreferrer&quot;</code>；现代浏览器通常已对 <code>_blank</code> 隐式启用 <code>noopener</code>，显式写仍更清楚</li>\\n<li>新标签会改变返回路径和键盘体验，不应把所有链接都强制新开</li>\\n<li>只有用户需要保留当前上下文时才使用，并在界面上给出提示</li>\\n</ul>\\n<p><strong><code>target=&quot;_blank&quot;</code> 的主要问题是新窗口可能借助 <code>window.opener</code> 操作来源页面，并可能与原页面共用进程。</strong> 即使跨域访问受到限制，新页面仍可能修改 <code>window.opener.location</code>，把原页面跳转到伪装地址。性能较差的新页面如果与来源页面共用进程，也可能拖累原页面。确实需要新开窗口时，应加上 <code>rel=&quot;noopener&quot;</code> 或 <code>rel=&quot;noreferrer&quot;</code>，让新窗口的 <code>window.opener</code> 为 <code>null</code>。</p>\\n</section>\\n<p><strong>存在问题：</strong></p>\\n<ol>\\n<li>安全隐患：新打开的窗口可以通过<code>window.opener</code>获取到来源页面的<code>window</code>对象即使跨域也可以。某些属性的访问被拦截，是因为跨域安全策略的限制。 但是，比如修改<code>window.opener.location</code>的值，指向另外一个地址，这样新窗口有可能会把原来的网页地址改了并进行页面伪装来欺骗用户。</li>\\n<li>新打开的窗口与原页面窗口共用一个进程，若是新页面有性能不好的代码也会影响原页面</li>\\n</ol>\\n<p><strong>解决方案：</strong></p>\\n<ol>\\n<li>\\n<p>尽量不用<code>target=&quot;_blank&quot;</code></p>\\n</li>\\n<li>\\n<p>如果一定要用，需要加上<code>rel=&quot;noopener&quot;</code>或者<code>rel=&quot;noreferrer&quot;</code>。这样新窗口的<code>window.openner</code>就是<code>null</code>了，而且会让新窗口运行在独立的进程里，不会拖累原来页面的进程。(不过，有些浏览器对性能做了优化，即使不加这个属性，新窗口也会在独立进程打开。不过为了安全考虑，还是加上吧。)</p>\\n</li>\\n</ol>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>支付结果页把第三方帮助中心用 <code>&lt;a target=&quot;_blank&quot;&gt;</code> 打开，安全测试证明帮助页能把原支付页导航到仿冒地址，攻击链成立的关键是什么？</strong></p>\\n<p>关键是新窗口可经 <code>window.opener</code> 获得来源窗口引用，并可能修改其 <code>location</code>，即使跨域访问其他属性受到限制也不能排除这种导航风险。外链应增加 <code>rel=&quot;noopener&quot;</code> 或 <code>rel=&quot;noreferrer&quot;</code>，并检查最终渲染结果而不只检查组件入参。</p>\\n</li>\\n<li>\\n<p><strong>内容平台有八百个历史外链都要求新窗口打开，你会怎样做工程改造，避免业务开发每次手写安全属性？</strong></p>\\n<p>应在统一的链接组件或富文本渲染层中，为外部 <code>target=&quot;_blank&quot;</code> 链接自动补上 <code>rel=&quot;noopener&quot;</code>，再用测试覆盖生成结果。这样能集中治理存量和新增入口；仍需排查绕过组件的原生标签以及脚本调用 <code>window.open</code> 的路径。</p>\\n</li>\\n<li>\\n<p><strong>市场团队要求合作站点能识别导流来源，隐私团队却要求所有外链使用 <code>rel=&quot;noreferrer&quot;</code>，你会怎样说明两种属性的取舍？</strong></p>\\n<p>若只需要切断 <code>window.opener</code>，使用 <code>noopener</code> 更贴合安全目标；<code>noreferrer</code> 还会抑制来源信息，可能影响合作方的来源统计。最终要由隐私要求决定是否接受统计损失，不能为了方便把两者当成完全等价的属性。</p>\\n</li>\\n<li>\\n<p><strong>站点模板已经给所有外链加了 <code>rel=&quot;noopener&quot;</code>，线上抽查仍发现少数活动页存在反向劫持风险，你会从哪些入口继续查？</strong></p>\\n<p>先核对活动页最终 <code>DOM</code>，确认属性没有被模板覆盖、转义或在客户端重建时丢失，再搜索直接写出的 <code>&lt;a target=&quot;_blank&quot;&gt;</code>。还要单独审计 <code>window.open</code> 等脚本开窗路径，因为链接标签上的 <code>rel</code> 不会自动保护另一套调用入口。</p>\\n</li>\\n<li>\\n<p><strong>内部报表链接也使用 <code>target=&quot;_blank&quot;</code>，负责人认为同域页面可信所以无需保护，你会接受这个例外吗？</strong></p>\\n<p>不建议仅凭同域就放弃隔离，因为新页面中的脚本、第三方资源或后续功能变化仍可能通过 <code>window.opener</code> 影响原页。显式添加 <code>noopener</code> 成本很低，也能固定安全边界；若业务确实依赖窗口间控制，则应明确保留原因并改用受控通信方案。</p>\\n</li>\\n<li>\\n<p><strong>产品说新标签页可能与原页面共享进程，要求只靠 <code>rel=&quot;noopener&quot;</code> 解决所有卡顿，你会怎么校正这个预期？</strong></p>\\n<p><code>noopener</code> 的确定作用是切断 <code>window.opener</code>，并有助于降低新页面拖累原页的风险，但具体进程分配仍由浏览器实现决定。它不能替代对第三方页面性能和资源占用的评估，因此应把安全隔离与性能治理视为两件相关但不同的工作。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_26-children以及childnodes的区别\\"><a class=\\"header-anchor\\" href=\\"#_26-children以及childnodes的区别\\">#</a> 26 children以及childNodes的区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>children</code> 只返回元素节点，是动态的 <code>HTMLCollection</code></li>\\n<li><code>childNodes</code> 返回元素、文本、注释等所有子节点，是动态的 <code>NodeList</code></li>\\n<li>HTML 缩进换行也可能形成文本节点，因此用 <code>childNodes</code> 时不能假定相邻项都是元素</li>\\n</ul>\\n<p><strong><code>children</code> 只包含元素节点，<code>childNodes</code> 则包含元素、文本和注释等所有子节点。</strong> 前者返回实时的 <code>HTMLCollection</code>，后者返回实时的 <code>NodeList</code>；两者都是类数组，但可用的遍历方法不同。<code>HTML</code> 中的换行和缩进也会形成文本节点，所以 <code>firstChild</code> 或 <code>childNodes</code> 经常拿到空白，而不是预期元素。遍历普通子元素时我会优先用 <code>children</code>，只有处理文本高亮、注释或富文本节点模型时才选 <code>childNodes</code>。</p>\\n</section>\\n<p>核心差别一句话：<strong><code>children</code> 只要元素，<code>childNodes</code> 要所有节点</strong>。</p>\\n<pre class=\\"language-html\\"><code>&lt;p id=&quot;p1&quot;&gt;&lt;b&gt;node&lt;/b&gt; vs &lt;em&gt;element&lt;/em&gt;&lt;!--注释--&gt;&lt;/p&gt;\\n</code></pre>\\n<pre class=\\"language-js\\"><code>p1.children.length    // 2  →  &lt;b&gt;、&lt;em&gt;\\np1.childNodes.length  // 4  →  &lt;b&gt;、文本&quot; vs &quot;、&lt;em&gt;、注释\\n</code></pre>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th><code>children</code></th>\\n<th><code>childNodes</code></th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>包含</td>\\n<td>只有元素节点</td>\\n<td>元素 + 文本 + 注释</td>\\n</tr>\\n<tr>\\n<td>返回类型</td>\\n<td><code>HTMLCollection</code></td>\\n<td><code>NodeList</code></td>\\n</tr>\\n<tr>\\n<td>实时性</td>\\n<td>实时</td>\\n<td>实时（<code>childNodes</code> 这个是实时的）</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>最大的坑是空白文本节点。</strong> <code>HTML</code> 里的换行和缩进本身就是文本节点：</p>\\n<pre class=\\"language-html\\"><code>&lt;ul&gt;\\n  &lt;li&gt;A&lt;/li&gt;\\n  &lt;li&gt;B&lt;/li&gt;\\n&lt;/ul&gt;\\n</code></pre>\\n<p>这个 <code>ul</code> 的 <code>childNodes.length</code> 是 <strong>5</strong>（三个换行缩进产生的文本节点 + 两个 <code>li</code>），而 <code>children.length</code> 才是 2。所以凡是要遍历子元素的场景，<strong>默认就该用 <code>children</code></strong>，用 <code>childNodes</code> 必须自己判断 <code>nodeType === 1</code>。</p>\\n<p><strong>配套的一组属性同理：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>只要元素</th>\\n<th>包含所有节点</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>firstElementChild</code></td>\\n<td><code>firstChild</code></td>\\n</tr>\\n<tr>\\n<td><code>lastElementChild</code></td>\\n<td><code>lastChild</code></td>\\n</tr>\\n<tr>\\n<td><code>nextElementSibling</code></td>\\n<td><code>nextSibling</code></td>\\n</tr>\\n<tr>\\n<td><code>childElementCount</code></td>\\n<td><code>childNodes.length</code></td>\\n</tr>\\n</tbody>\\n</table>\\n<p>写 <code>el.firstChild.textContent</code> 这类代码时，如果标签之间有换行，拿到的往往是那个空白文本节点而不是你要的元素 —— 这是新手最常见的一个 bug。</p>\\n<p><strong>什么时候确实需要 <code>childNodes</code>：</strong></p>\\n<ul>\\n<li>要处理<strong>文本内容本身</strong>，比如做文本高亮、给节点里的关键词包 <code>&lt;mark&gt;</code>。</li>\\n<li>要清理注释节点。</li>\\n<li>实现富文本编辑器时需要精确的节点模型。</li>\\n</ul>\\n<p><strong>都不是数组</strong>，没有 <code>map</code>/<code>filter</code>。<code>NodeList</code> 有 <code>forEach</code>，<code>HTMLCollection</code> 连 <code>forEach</code> 都没有，要用先转：</p>\\n<pre class=\\"language-js\\"><code>;[...el.children].filter(x =&gt; x.classList.contains(&#39;active&#39;))\\nArray.from(el.children).map(x =&gt; x.dataset.id)\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>导航组件原来写成单行 <code>HTML</code> 时 <code>firstChild.classList.add(\'active\')</code> 正常，格式化后加入换行缩进就报错，你如何解释这个反例？</strong></p>\\n<p>格式化产生的换行和缩进会成为文本节点，<code>firstChild</code> 因而可能不再是元素，也就没有 <code>classList</code>。这里只关心导航元素，应改用 <code>firstElementChild</code>；若业务确实处理文本，则要先依据 <code>nodeType</code> 区分节点类型。</p>\\n</li>\\n<li>\\n<p><strong>富文本搜索要在一段包含链接、文本和注释的内容中给关键词包 <code>&lt;mark&gt;</code>，同事坚持遍历 <code>children</code>，为什么会漏结果？</strong></p>\\n<p><code>children</code> 只返回元素节点，元素之间直接存在的文本节点不会进入遍历，因此关键词可能完全看不到。这里应使用 <code>childNodes</code> 处理完整节点模型，并区分文本、元素和注释；递归改写文本时还要避免破坏已有标签结构。</p>\\n</li>\\n<li>\\n<p><strong>列表组件有五百个子元素，清理逻辑遍历 <code>el.children</code> 并逐个 <code>remove()</code>，线上发现总会残留一部分节点，你会怎样修？</strong></p>\\n<p><code>children</code> 返回实时 <code>HTMLCollection</code>，删除当前元素后后续索引会立即前移，递增下标因此会跳项。可先用 <code>Array.from(el.children)</code> 固化后删除，或持续删除第一个元素；固化数组会额外保存一份引用，但行为更容易推断。</p>\\n</li>\\n<li>\\n<p><strong>审查代码时看到 <code>el.children.map(x =&gt; x.dataset.id)</code>，开发说浏览器里能展开集合所以它就是数组，你会如何处理？</strong></p>\\n<p><code>children</code> 是 <code>HTMLCollection</code>，属于类数组集合，没有数组的 <code>map</code>、<code>filter</code>，应先用 <code>Array.from</code> 或展开语法转换。只读遍历可使用可迭代能力，但一旦需要数组方法或遍历期间修改 <code>DOM</code>，显式快照更稳妥。</p>\\n</li>\\n<li>\\n<p><strong>编辑器插件既要定位第一个元素块，又要保留块前的纯文本提示，你会选 <code>firstElementChild</code> 还是 <code>firstChild</code>？</strong></p>\\n<p>两个目标需要分别处理：元素块用 <code>firstElementChild</code>，块前提示则应从 <code>firstChild</code> 开始检查文本节点，不能用单一 <code>API</code> 同时表达。若直接给 <code>firstChild.textContent</code> 赋值，可能覆盖错误节点甚至破坏内容，必须先验证节点类型。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_27-htmlcollection和nodelist的区别\\"><a class=\\"header-anchor\\" href=\\"#_27-htmlcollection和nodelist的区别\\">#</a> 27 HTMLCollection和NodeList的区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>HTMLCollection</code> 只包含元素，通常是实时集合，还可按 <code>id</code> / <code>name</code> 访问</li>\\n<li><code>NodeList</code> 可包含任意节点；有的实时，如 <code>childNodes</code>，有的是静态快照，如 <code>querySelectorAll</code></li>\\n<li>两者都不是数组，需要时用 <code>Array.from()</code> 转换；遍历前先确认集合是否会随 DOM 修改</li>\\n</ul>\\n<p><strong><code>HTMLCollection</code> 只收集元素且总是实时更新，<code>NodeList</code> 可以包含任意节点，是否实时取决于产生它的 <code>API</code>。</strong> 例如 <code>children</code> 返回 <code>HTMLCollection</code>，<code>childNodes</code> 返回实时 <code>NodeList</code>，而 <code>querySelectorAll</code> 返回静态 <code>NodeList</code>。前者支持按名称访问但没有 <code>forEach</code>，后者通常可以用 <code>forEach</code>。修改实时集合时容易漏删或让集合不断增长，我一般先用 <code>Array.from</code> 固化后再遍历。</p>\\n</section>\\n<p>先理清类型关系：<strong><code>Node</code> 是基类，<code>Element</code> 继承自 <code>Node</code></strong>。文本节点、注释节点都是 <code>Node</code> 但不是 <code>Element</code>。</p>\\n<pre class=\\"language-text\\"><code>Node\\n├─ Document / DocumentFragment\\n├─ CharacterData ─┬─ Text（文本）\\n│                 └─ Comment（注释）\\n└─ Element ── HTMLElement ── HTMLDivElement / HTMLInputElement / ...\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2023/01/ede61848ee4d078c.png\\" alt=\\"\\"></p>\\n<p><strong>两个集合的区别：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th><code>HTMLCollection</code></th>\\n<th><code>NodeList</code></th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>元素类型</td>\\n<td>只有 <code>Element</code></td>\\n<td>任意 <code>Node</code>（含文本、注释）</td>\\n</tr>\\n<tr>\\n<td>实时性</td>\\n<td><strong>总是实时</strong></td>\\n<td><strong>看来源</strong>，见下</td>\\n</tr>\\n<tr>\\n<td>按名访问</td>\\n<td>支持 <code>list.namedItem(\'x\')</code> / <code>list[\'x\']</code></td>\\n<td>不支持</td>\\n</tr>\\n<tr>\\n<td><code>forEach</code></td>\\n<td>❌ 没有</td>\\n<td>✅ 有</td>\\n</tr>\\n<tr>\\n<td>典型来源</td>\\n<td><code>children</code>、<code>getElementsByTagName</code></td>\\n<td><code>childNodes</code>、<code>querySelectorAll</code></td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>最容易答错的是实时性 —— 不能简单背&quot;一个动态一个静态&quot;：</strong></p>\\n<pre class=\\"language-js\\"><code>const live = document.getElementsByTagName(&#39;li&#39;)  // HTMLCollection，实时\\nconst snap = document.querySelectorAll(&#39;li&#39;)      // NodeList，静态快照\\nconst kids = ul.childNodes                        // NodeList，但它是实时的\\n\\nul.appendChild(document.createElement(&#39;li&#39;))\\n\\nlive.length   // 变了 ✅\\nsnap.length   // 没变 ❌ 快照\\nkids.length   // 变了 ✅ —— 所以 NodeList 不一定是静态的\\n</code></pre>\\n<p><strong>正确的记法</strong>：<code>querySelectorAll</code> 返回静态快照，其余大多是实时集合。</p>\\n<p><strong>实时集合会咬人的两个场景：</strong></p>\\n<pre class=\\"language-js\\"><code>// ❌ 死循环：每 append 一个，集合就长一个\\nconst items = document.getElementsByTagName(&#39;li&#39;)\\nfor (let i = 0; i &lt; items.length; i++) ul.appendChild(document.createElement(&#39;li&#39;))\\n\\n// ❌ 漏删一半：删掉一个后面索引立刻前移\\nfor (let i = 0; i &lt; items.length; i++) items[i].remove()\\n\\n// ✅ 先固化成数组\\n;[...items].forEach(el =&gt; el.remove())\\n</code></pre>\\n<p>反过来，静态快照也有代价：<code>querySelectorAll</code> 之后 DOM 变了，快照里可能残留已经被移除的节点，对它们操作不会反映到页面上。</p>\\n<p><strong>两者都是类数组</strong>，要用数组方法先转：</p>\\n<pre class=\\"language-js\\"><code>Array.from(list)                  // 推荐\\n[...list]                         // 同上\\nArray.prototype.slice.call(list)  // 老写法\\n</code></pre>\\n<p><strong>性能上的一个小细节</strong>：实时集合的 <code>length</code> 每次读取都可能触发重新查询，所以循环条件里别直接写 <code>i &lt; list.length</code>，先缓存成局部变量。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审中有人断言“<code>HTMLCollection</code> 都是动态的，<code>NodeList</code> 都是静态的”，你会用哪段现有 <code>DOM</code> 场景当场反驳？</strong></p>\\n<p>可以对比 <code>querySelectorAll(\'li\')</code> 与 <code>ul.childNodes</code>：前者返回静态 <code>NodeList</code>，后者虽然也是 <code>NodeList</code>，却会随子节点变化实时更新。正确判断要看集合来源，不能只看返回类型；<code>HTMLCollection</code> 则是实时集合。</p>\\n</li>\\n<li>\\n<p><strong>批量下线页面中一百个 <code>.expired</code> 节点，循环 <code>getElementsByClassName(\'expired\')</code> 后逐项 <code>remove()</code>，结果只删除约一半，你会如何改代码？</strong></p>\\n<p>该查询返回实时 <code>HTMLCollection</code>，删除一个节点后后续项立即前移，递增索引会跳过新占据当前位置的元素。先用 <code>Array.from</code> 固化再遍历删除，或用 <code>while (list.length)</code> 始终删除首项；后者仍依赖实时更新特性。</p>\\n</li>\\n<li>\\n<p><strong>消息列表先执行 <code>querySelectorAll(\'.item\')</code>，随后框架重渲染了整个容器，再给快照中的节点加点击事件却毫无反应，故障点在哪里？</strong></p>\\n<p><code>querySelectorAll</code> 返回静态 <code>NodeList</code>，重渲染后变量仍指向旧节点，不会自动切换到页面上的新元素。应在新 <code>DOM</code> 就绪后重新查询，或把事件委托到稳定祖先；长期保存旧快照还会延长脱离节点的存活时间。</p>\\n</li>\\n<li>\\n<p><strong>模板分析器需要同时读取元素、文本和注释，业务列表却只需按 <code>id</code> 找子元素，两处都使用同一种集合是否合理？</strong></p>\\n<p>不合理：模板分析器需要能容纳任意 <code>Node</code> 的 <code>NodeList</code> 语义，例如遍历 <code>childNodes</code>；业务列表只处理元素时，<code>HTMLCollection</code> 更直接且支持按名称访问。即使类型选对，也要继续确认集合是实时还是静态，避免把内容范围与更新行为混为一谈。</p>\\n</li>\\n<li>\\n<p><strong>实时集合包含两千个节点，循环条件每次都读取 <code>list.length</code>，同时循环体还会向容器追加元素，页面出现无法结束的任务，你怎样定位？</strong></p>\\n<p>追加元素会让实时集合持续增长，<code>i &lt; list.length</code> 的终止边界也随之后移，循环可能一直追不上集合长度。进入循环前应先固化数组或确定固定上限；单纯缓存初始长度只能限制次数，却仍可能让索引对应的节点发生变化。</p>\\n</li>\\n<li>\\n<p><strong>团队选型时想统一用 <code>querySelectorAll</code>，理由是静态快照更安全，你会保留哪些反对意见？</strong></p>\\n<p>静态快照能避免遍历期间因实时更新而漏项或死循环，但 <code>DOM</code> 变化后它会过期，继续操作可能只影响已脱离文档的旧节点。是否采用应取决于需要快照还是持续观察，而不是追求单一 <code>API</code>；无论哪种集合，使用数组方法前都应显式转换。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);