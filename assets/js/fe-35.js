(window.webpackJsonp=window.webpackJsonp||[]).push([[35],{1142:function(o){o.exports=JSON.parse('{"html":"<h2 id=\\"一、创建对象和原型链\\"><a class=\\"header-anchor\\" href=\\"#一、创建对象和原型链\\">#</a> 一、创建对象和原型链</h2>\\n<h3 id=\\"_1-面向对象的三大特性\\"><a class=\\"header-anchor\\" href=\\"#_1-面向对象的三大特性\\">#</a> 1 面向对象的三大特性</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>封装：把状态收进对象内部只暴露方法，<code>JS</code> 里靠闭包、<code>WeakMap</code> 或 <code>#</code> 私有字段实现，下划线前缀只是约定</li>\\n<li>继承：把公共能力放在原型上共享，<code>class extends</code> 的本质是把子类原型接到父类原型上</li>\\n<li>多态：同一个调用点因实例不同走不同实现，<code>JS</code> 不需要额外语法，属性查找沿原型链先找到谁就用谁</li>\\n<li>三条在 <code>JS</code> 里全部落在原型链上，讲清「原型链怎么查找」比背定义值钱</li>\\n<li>加分点：<code>JS</code> 没有真正的类，<code>class</code> 只是构造函数的语法糖，主动点破这句能显出理解深度</li>\\n</ul>\\n<p><strong><code>JS</code> 的三大特性都是原型链的应用，不是语言内建的类机制</strong> —— 按「封装靠作用域、继承靠原型指向、多态靠查找顺序」三层讲。</p>\\n</section>\\n<ul>\\n<li>封装</li>\\n<li>继承</li>\\n<li>多态</li>\\n</ul>\\n<h4 id=\\"_1-1-原型链的知识\\"><a class=\\"header-anchor\\" href=\\"#_1-1-原型链的知识\\">#</a> 1.1 原型链的知识</h4>\\n<blockquote>\\n<p>原型链是面向对象的基础，是非常重要的部分。有以下几种知识：</p>\\n</blockquote>\\n<ul>\\n<li>创建对象有几种方法</li>\\n<li>原型、构造函数、实例、原型链</li>\\n<li><code>instanceof</code>的原理</li>\\n<li><code>new</code> 运算符</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>同事说 <code>JS</code> 没有真正的私有属性，加个下划线前缀就当私有了，你在 <code>review</code> 里会拦吗？</strong></p>\\n<p>会。下划线只是口头约定，外部照样能读能改。真要私有就用 <code>#</code> 私有字段，或者用 <code>WeakMap</code> 把状态存在实例外面。库代码尤其要用真私有 —— 用户一旦依赖了你的内部字段，这个字段你就再也改不动了。</p>\\n</li>\\n<li>\\n<p><strong>既然 <code>class</code> 只是语法糖，老项目里的构造函数直接改成 <code>class</code> 有风险吗？</strong></p>\\n<p>有三处行为差异：<code>class</code> 声明有暂时性死区不会提升、方法默认不可枚举所以 <code>for...in</code> 遍历不到、不加 <code>new</code> 调用直接抛 <code>TypeError</code>。老代码如果有遍历原型方法做序列化，或者有意无意地省了 <code>new</code>，迁移后当场就炸。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_2-创建对象有几种方法\\"><a class=\\"header-anchor\\" href=\\"#_2-创建对象有几种方法\\">#</a> 2 创建对象有几种方法</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>字面量 <code>{}</code> 和 <code>new Object()</code> 等价，原型都是 <code>Object.prototype</code></li>\\n<li>构造函数 <code>new Foo()</code>：实例属性各自一份，原型上的方法所有实例共享</li>\\n<li><code>Object.create(p)</code>：直接指定原型为 <code>p</code>，不经过构造函数，寄生组合继承靠的就是它</li>\\n<li><code>Object.create(null)</code>：造出没有原型的纯净字典，拿用户输入当 <code>key</code> 也不会撞上 <code>toString</code></li>\\n<li><code>class</code> 是构造函数的语法糖；工厂函数则每次返回全新对象、不共享原型</li>\\n<li>区分标准只有一条：新对象的原型指向谁，用 <code>Object.getPrototypeOf</code> 一验就知道</li>\\n</ul>\\n<p><strong>五种写法的差别不在语法，而在「新对象的原型指向谁」</strong> —— 按「字面量 → 构造函数 → <code>Object.create</code> → <code>class</code> → 工厂函数」顺着原型指向讲。</p>\\n</section>\\n<h4 id=\\"_2-1-方式一-字面量\\"><a class=\\"header-anchor\\" href=\\"#_2-1-方式一-字面量\\">#</a> 2.1 方式一：字面量</h4>\\n<pre class=\\"language-javascript\\"><code>    var obj11 = {name: &#39;smyh&#39;};\\n    var obj12 = new Object(name: `smyh`); //内置对象（内置的构造函数）\\n</code></pre>\\n<blockquote>\\n<p>上面的两种写法，效果是一样的。因为，第一种写法，<code>obj11</code>会指向<code>Object</code>。</p>\\n</blockquote>\\n<ul>\\n<li>第一种写法是：字面量的方式。</li>\\n<li>第二种写法是：内置的构造函数</li>\\n</ul>\\n<h4 id=\\"_2-2-方式二-通过构造函数\\"><a class=\\"header-anchor\\" href=\\"#_2-2-方式二-通过构造函数\\">#</a> 2.2 方式二：通过构造函数</h4>\\n<pre class=\\"language-javascript\\"><code>    var M = function (name) {\\n        this.name = name;\\n    }\\n    var obj3 = new M(&#39;smyhvae&#39;);\\n</code></pre>\\n<h4 id=\\"_2-3-方法三-object-create\\"><a class=\\"header-anchor\\" href=\\"#_2-3-方法三-object-create\\">#</a> 2.3 方法三：Object.create</h4>\\n<pre class=\\"language-javascript\\"><code>    var p = {name:&#39;poetry&#39;};\\n    var obj3 = Object.create(p);  //此方法创建的对象，是用原型链连接的\\n</code></pre>\\n<blockquote>\\n<p>第三种方法，很少有人能说出来。这种方式里，<code>obj3</code>是实例，<code>p</code>是<code>obj3的</code>原型（<code>name</code>是p原型里的属性），构造函数是<code>Object</code> 。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/18.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>配置对象要拿用户输入当 <code>key</code>，你会用 <code>{}</code> 还是 <code>Object.create(null)</code>？</strong></p>\\n<p>用 <code>Object.create(null)</code> 或者 <code>Map</code>。普通对象继承了 <code>toString</code>、<code>constructor</code>、<code>__proto__</code> 这些键，用户输入正好叫这几个名字时 <code>if (config[key])</code> 会莫名其妙为真，写 <code>__proto__</code> 还可能触发原型污染。</p>\\n</li>\\n<li>\\n<p><strong>同事把方法写成 <code>this.sayName = function () {}</code> 放在构造函数里，你怎么说服他挪到原型上？</strong></p>\\n<p>写在构造函数里等于每 <code>new</code> 一次就造一个新函数，一万个实例就是一万份相同代码；而且 <code>a.sayName === b.sayName</code> 是 <code>false</code>，做事件解绑或结果缓存时会踩坑。只有需要闭包捕获私有变量时，才值得留在构造函数里。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-原型、构造函数、实例-以及原型链\\"><a class=\\"header-anchor\\" href=\\"#_3-原型、构造函数、实例-以及原型链\\">#</a> 3 原型、构造函数、实例，以及原型链</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>三角关系背下来：<code>实例.__proto__ === 构造函数.prototype</code>，<code>构造函数.prototype.constructor === 构造函数</code></li>\\n<li>只有函数才有 <code>prototype</code>；<code>__proto__</code>（标准写法是 <code>Object.getPrototypeOf</code>）是每个对象都有的指针</li>\\n<li>属性查找沿 <code>__proto__</code> 一层层往上，找到就停，走到 <code>null</code> 还没有就返回 <code>undefined</code></li>\\n<li><code>instanceof</code> 判断的是「构造函数的 <code>prototype</code> 在不在实例的原型链上」，所以跨 <code>iframe</code> 会失效</li>\\n<li><code>hasOwnProperty</code> 只看自身属性，<code>in</code> 会连原型链一起查，这两个别混</li>\\n<li>加分点：运行时改原型（<code>Object.setPrototypeOf</code>）会让引擎的内联缓存失效，热路径上不要用</li>\\n</ul>\\n<p><strong>原型链就是一条 <code>__proto__</code> 指针串起来的查找链，继承和多态都是这条链的副产品</strong> —— 按「三角关系 → 查找规则 → <code>instanceof</code> 原理」讲。</p>\\n</section>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/19.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>PS：任何一个函数，如果在前面加了<code>new</code>，那就是构造函数。</p>\\n</blockquote>\\n<h4 id=\\"_3-1-原型、构造函数、实例三者之间的关系\\"><a class=\\"header-anchor\\" href=\\"#_3-1-原型、构造函数、实例三者之间的关系\\">#</a> 3.1 原型、构造函数、实例三者之间的关系</h4>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/20.png\\" alt=\\"\\"></p>\\n<ol>\\n<li>构造函数通过 <code>new</code> 生成实例</li>\\n<li>构造函数也是函数，构造函数的<code>prototype</code>指向原型。（所有的函数有<code>prototype</code>属性，但实例没有 <code>prototype</code>属性）</li>\\n<li>原型对象中有 <code>constructor</code>，指向该原型的构造函数。</li>\\n</ol>\\n<blockquote>\\n<p>上面的三行，代码演示：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>  var Foo = function (name) {\\n      this.name = name;\\n  }\\n\\n  var fn = new Foo(&#39;smyhvae&#39;);\\n</code></pre>\\n<blockquote>\\n<p>上面的代码中，<code>Foo.prototype.constructor === Foo</code>的结果是<code>true</code>：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/21.png\\" alt=\\"\\"></p>\\n<ol start=\\"4\\">\\n<li>实例的<code>__proto__</code>指向原型。也就是说，<code>Foo.__proto__ === M.prototype</code>。</li>\\n</ol>\\n<blockquote>\\n<p>声明：所有的<strong>引用类型</strong>（数组、对象、函数）都有<code>__proto__</code>这个属性。</p>\\n</blockquote>\\n<p><code>Foo.__proto__ === Function.prototype</code>的结果为true，说明<code>Foo</code>这个普通的函数，是<code>Function</code>构造函数的一个实例。</p>\\n<h4 id=\\"_3-2-原型链\\"><a class=\\"header-anchor\\" href=\\"#_3-2-原型链\\">#</a> 3.2 原型链</h4>\\n<p><strong>原型链的基本原理</strong>：任何一个<strong>实例</strong>，通过原型链，找到它上面的<strong>原型</strong>，该原型对象中的方法和属性，可以被所有的原型实例共享。</p>\\n<blockquote>\\n<p><code>Object</code>是原型链的顶端。</p>\\n</blockquote>\\n<ul>\\n<li>原型可以起到继承的作用。原型里的方法都可以被不同的实例共享：</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>  //给Foo的原型添加 say 函数\\n  Foo.prototype.say = function () {\\n      console.log(&#39;&#39;);\\n  }\\n</code></pre>\\n<p><strong>原型链的关键</strong>：在访问一个实例的时候，如果实例本身没找到此方法或属性，就往原型上找。如果还是找不到，继续往上一级的原型上找。</p>\\n<h4 id=\\"_3-3-instanceof的原理\\"><a class=\\"header-anchor\\" href=\\"#_3-3-instanceof的原理\\">#</a> 3.3 <code>instanceof</code>的原理</h4>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/22.png\\" alt=\\"\\"></p>\\n<ul>\\n<li><code>instanceof</code>的<strong>作用</strong>：用于判断<strong>实例</strong>属于哪个<strong>构造函数</strong>。</li>\\n<li><code>instanceof</code>的<strong>原理</strong>：判断实例对象的<code>__proto__</code>属性，和构造函数的<code>prototype</code>属性，是否为同一个引用（是否指向同一个地址）。</li>\\n</ul>\\n<blockquote>\\n<ul>\\n<li><strong>注意1</strong>：虽然说，实例是由构造函数 new 出来的，但是实例的<code>__proto__</code>属性引用的是构造函数的<code>prototype</code>。也就是说，实例的<code>__proto__</code>属性与构造函数本身无关。</li>\\n<li><strong>注意2</strong>：在原型链上，原型的上面可能还会有原型，以此类推往上走，继续找<code>__proto__</code>属性。这条链上如果能找到， instanceof 的返回结果也是 true。</li>\\n</ul>\\n</blockquote>\\n<p>比如说：</p>\\n<ul>\\n<li><code>foo instance of Foo</code>的结果为true，因为<code>foo.__proto__ === M.prototype</code>为<code>true</code>。</li>\\n<li><strong><code>foo instance of Objecet</code>的结果也为true</strong>，为<code>Foo.prototype.__proto__ === Object.prototype</code>为<code>true</code>。</li>\\n</ul>\\n<blockquote>\\n<p>但我们不能轻易的说：<code>foo</code> 一定是 由<code>Object</code>创建的实例`。这句话是错误的。我们来看下一个问题就明白了。</p>\\n</blockquote>\\n<h4 id=\\"_3-4-分析一个问题\\"><a class=\\"header-anchor\\" href=\\"#_3-4-分析一个问题\\">#</a> 3.4 分析一个问题</h4>\\n<p><strong>问题：<strong>已知A继承了B，B继承了C。怎么判断 a 是由A</strong>直接生成</strong>的实例，还是B直接生成的实例呢？还是C直接生成的实例呢？</p>\\n<blockquote>\\n<p>分析：这就要用到原型的<code>constructor</code>属性了。</p>\\n</blockquote>\\n<ul>\\n<li><code>foo.__proto__.constructor === M</code>的结果为<code>true</code>，但是 <code>foo.__proto__.constructor === Object</code>的结果为<code>false</code>。</li>\\n<li>所以，用 <code>consturctor</code>判断就比用 <code>instanceof</code>判断，更为严谨。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>用 <code>instanceof</code> 判断数组不靠谱，你平时用什么？</strong></p>\\n<p>用 <code>Array.isArray()</code>。<code>instanceof</code> 比的是原型链，跨 <code>iframe</code> 或跨 <code>realm</code> 时数组的原型来自另一个全局环境，<code>arr instanceof Array</code> 会是 <code>false</code>。要做通用类型判断则用 <code>Object.prototype.toString.call(v)</code>。</p>\\n</li>\\n<li>\\n<p><strong>有人为了「继承」直接写 <code>Child.prototype = Parent.prototype</code>，会出什么事？</strong></p>\\n<p>两个原型变成同一个对象，给子类加方法会污染父类，<code>constructor</code> 也指错。正确写法是 <code>Child.prototype = Object.create(Parent.prototype)</code>，再把 <code>constructor</code> 修回来。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_4-new-运算符\\"><a class=\\"header-anchor\\" href=\\"#_4-new-运算符\\">#</a> 4 new 运算符</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>四步：创建空对象 → 把它的原型指向 <code>构造函数.prototype</code> → 以它为 <code>this</code> 执行构造函数 → 返回它</li>\\n<li>第四步有例外：构造函数显式 <code>return</code> 一个对象时返回那个对象，返回原始值则忽略</li>\\n<li>不加 <code>new</code> 调用构造函数，非严格模式下 <code>this</code> 指向全局并静默污染，<code>class</code> 则直接抛 <code>TypeError</code></li>\\n<li><code>new.target</code> 能在函数内部判断自己是不是被 <code>new</code> 调用，可以做「忘了 <code>new</code> 也能工作」的兜底</li>\\n<li>箭头函数没有 <code>[[Construct]]</code>，不能被 <code>new</code>，也没有自己的 <code>prototype</code></li>\\n</ul>\\n<p><strong><code>new</code> 就是「建对象、连原型、绑 <code>this</code>、给返回值」四步</strong> —— 手写时按这四步写，再补一句「<code>return</code> 对象会覆盖默认返回值」。</p>\\n</section>\\n<blockquote>\\n<p>当<code>new Foo()</code>时发生了什么：</p>\\n</blockquote>\\n<ul>\\n<li>创建一个<strong>新的空对象实例</strong>。</li>\\n<li>将此空对象的隐式原型指向其构造函数的显示原型。</li>\\n<li>执行构造函数（传入相应的参数，如果没有参数就不用传），同时 <code>this</code> 指向这个新实例。</li>\\n<li>如果返回值是一个新对象，那么直接返回该对象；如果无返回值或者返回一个非对象值，那么就将步骤（1）创建的对象返回。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>手写 <code>new</code> 时最后一句为什么要判断返回值类型？</strong></p>\\n<p>因为规范规定构造函数返回对象时以它为准，返回原始值时忽略。很多单例和代理模式就是靠这一条实现的；漏了这句，遇到 <code>return new Proxy(...)</code> 这种写法结果就不对。</p>\\n</li>\\n<li>\\n<p><strong>箭头函数为什么不能 <code>new</code>？</strong></p>\\n<p>它没有 <code>[[Construct]]</code> 内部方法，也没有自己的 <code>this</code> 和 <code>prototype</code>，<code>new</code> 的第二步和第三步都无从谈起。所以构造函数和原型方法都不要写成箭头函数。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"二、面向对象\\"><a class=\\"header-anchor\\" href=\\"#二、面向对象\\">#</a> 二、面向对象</h2>\\n<h3 id=\\"_1-前言\\"><a class=\\"header-anchor\\" href=\\"#_1-前言\\">#</a> 1 前言</h3>\\n<blockquote>\\n<p>类与实例：</p>\\n</blockquote>\\n<ul>\\n<li>类的声明</li>\\n<li>生成实例</li>\\n</ul>\\n<p><strong>类与继承：</strong></p>\\n<ul>\\n<li>如何实现继承：继承的本质就是原型链</li>\\n<li>继承的几种方式</li>\\n</ul>\\n<h3 id=\\"_2-类的定义、实例化\\"><a class=\\"header-anchor\\" href=\\"#_2-类的定义、实例化\\">#</a> 2 类的定义、实例化</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>class</code> 只是构造函数的语法糖，<code>typeof Foo</code> 仍然是 <code>function</code></li>\\n<li>类声明有暂时性死区，不像 <code>function</code> 那样提升，声明之前使用直接抛 <code>ReferenceError</code></li>\\n<li>类体内部默认严格模式，不加 <code>new</code> 调用会抛 <code>TypeError</code></li>\\n<li>类的方法定义在 <code>Foo.prototype</code> 上且不可枚举，<code>for...in</code> 和 <code>Object.keys</code> 都看不到</li>\\n<li>实例属性写在 <code>constructor</code>（或类字段）里，共享方法写在类体里，别把方法塞进构造函数</li>\\n<li><code>static</code> 挂在类本身上；<code>get</code> / <code>set</code> 定义的是访问器属性，不是普通值</li>\\n</ul>\\n<p><strong><code>class</code> 等价于构造函数加四条约束：暂时性死区、必须 <code>new</code>、方法不可枚举、内部严格模式</strong> —— 按「本质是函数 → 四条约束 → 属性该写在哪」讲。</p>\\n</section>\\n<h4 id=\\"_2-1-类的定义-类的声明\\"><a class=\\"header-anchor\\" href=\\"#_2-1-类的定义-类的声明\\">#</a> 2.1 类的定义/类的声明</h4>\\n<p><strong>方式一</strong>：用构造函数模拟类（传统写法）</p>\\n<pre class=\\"language-js\\"><code>function Animal1() {\\n    this.name = &#39;smyhvae&#39;; //通过this，表明这是一个构造函数\\n}\\n</code></pre>\\n<p><strong>方式二</strong>：用 <code>class</code> 声明（<code>ES6</code>的写法）</p>\\n<pre class=\\"language-js\\"><code>class Animal2 {\\n    constructor() {  //可以在构造函数里写属性\\n        this.name = name;\\n    }\\n}\\n</code></pre>\\n<p>控制台的效果：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/41.png\\" alt=\\"\\"></p>\\n<h4 id=\\"_2-2-实例化\\"><a class=\\"header-anchor\\" href=\\"#_2-2-实例化\\">#</a> 2.2 实例化</h4>\\n<p>类的实例化很简单，直接 <code>new</code> 出来即可。</p>\\n<pre class=\\"language-js\\"><code>console.log(new Animal1(),new Animal2()); //实例化。如果括号里没有参数，则括号可以省略\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/41.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>老项目用 <code>for...in</code> 遍历实例做序列化，迁到 <code>class</code> 后方法「丢了」，这是 bug 吗？</strong></p>\\n<p>不是 bug，是 <code>class</code> 的方法默认不可枚举。真要拿方法名得用 <code>Object.getOwnPropertyNames(Foo.prototype)</code>。不过更该问的是：序列化为什么要遍历方法？正常只该序列化数据字段。</p>\\n</li>\\n<li>\\n<p><strong>类字段 <code>count = 0</code> 和写在 <code>constructor</code> 里的 <code>this.count = 0</code> 有区别吗？</strong></p>\\n<p>数据字段几乎没区别，类字段会被编译到构造函数开头。真正的差别在箭头函数字段：<code>handleClick = () =&gt; {}</code> 是每个实例一份、<code>this</code> 绑死；写成原型方法则所有实例共享一份、<code>this</code> 取决于调用方式。<code>React</code> 类组件里到处用前者，就是为了省掉手动 <code>bind</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-继承的几种方式\\"><a class=\\"header-anchor\\" href=\\"#_3-继承的几种方式\\">#</a> 3 继承的几种方式</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>借用构造函数（<code>Parent.call(this)</code>）：属性各自一份，但拿不到原型上的方法，<code>instanceof</code> 也不认</li>\\n<li>原型链继承（<code>Child.prototype = new Parent()</code>）：方法有了，但引用类型属性被所有实例共享，还没法给父构造函数传参</li>\\n<li>组合继承：前两者相加，问题都解决了，代价是父构造函数被调用两次、原型上残留一份被遮蔽的属性</li>\\n<li>寄生组合继承（<code>Child.prototype = Object.create(Parent.prototype)</code>）：只调一次，是 <code>ES5</code> 的标准答案</li>\\n<li><code>class extends</code> 就是寄生组合的语法糖，还额外继承了静态方法（<code>Object.getPrototypeOf(Child) === Parent</code>）</li>\\n<li>每种写法都要把 <code>constructor</code> 修回来，否则 <code>instance.constructor</code> 会指向父类</li>\\n</ul>\\n<p><strong>继承方式是一条演进线，每一步都在补上一步的坑</strong> —— 按「借构造函数缺方法 → 原型链共享引用 → 组合调两次 → 寄生组合收官 → <code>class extends</code> 是它的语法糖」讲。</p>\\n</section>\\n<blockquote>\\n<p>继承的本质就是原型链。</p>\\n</blockquote>\\n<p><strong>继承的方式有几种？每种形式的优缺点是</strong>？这些问题必问的。其实就是考察你对原型链的掌握程度。</p>\\n<h4 id=\\"_3-1-方式一-借助构造函数\\"><a class=\\"header-anchor\\" href=\\"#_3-1-方式一-借助构造函数\\">#</a> 3.1 方式一：借助构造函数</h4>\\n<pre class=\\"language-javascript\\"><code>    function Parent1() {\\n        this.name = &#39;parent1 的属性&#39;;\\n    }\\n\\n    function Child1() {\\n        Parent1.call(this);         //【重要】此处用 call 或 apply 都行：改变 this 的指向\\n        this.type = &#39;child1 的属性&#39;;\\n    }\\n\\n    console.log(new Child1);\\n</code></pre>\\n<blockquote>\\n<p>【重要】上方代码中，最重要的那行代码：在子类的构造函数里写了<code>Parent1.call(this);</code>，意思是：<strong>让Parent的构造函数在child的构造函数中执行</strong>。发生的变化是：<strong>改变this的指向</strong>，parent的实例 --&gt; 改为指向child的实例。导致 parent的实例的属性挂在到了child的实例上，这就实现了继承。</p>\\n</blockquote>\\n<p>打印结果：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/43.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>上方结果表明：<code>child</code>先有了 <code>parent</code> 实例的属性（继承得以实现），再有了<code>child</code> 实例的属性。</p>\\n</blockquote>\\n<p><strong>分析</strong>：</p>\\n<blockquote>\\n<p>这种方式，虽然改变了 <code>this</code> 的指向，但是，<strong>Child1 无法继承 <code>Parent1</code> 的原型</strong>。也就是说，如果我给 <code>Parent1</code> 的原型增加一个方法：</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>    Parent1.prototype.say = function () {\\n    };\\n</code></pre>\\n<blockquote>\\n<p>上面这个方法是无法被 <code>Child1</code> 继承的。如下：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/44.png\\" alt=\\"\\"></p>\\n<h4 id=\\"_3-2-方法二-通过原型链实现继承\\"><a class=\\"header-anchor\\" href=\\"#_3-2-方法二-通过原型链实现继承\\">#</a> 3.2 方法二：通过原型链实现继承</h4>\\n<pre class=\\"language-javascript\\"><code>    /*\\n    通过原型链实现继承\\n     */\\n    function Parent() {\\n        this.name = &#39;Parent 的属性&#39;;\\n    }\\n\\n    function Child() {\\n        this.type = &#39;Child 的属性&#39;;\\n    }\\n\\n    Child.prototype = new Parent(); //【重要】\\n\\n    console.log(new Child());\\n</code></pre>\\n<p>打印结果：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/45.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>【重要】上方代码中，最重要的那行：每个函数都有<code>prototype</code>属性，于是，构造函数也有这个属性，这个属性是一个对象。现在，<strong>我们把<code>Parent</code>的实例赋值给了<code>Child</code>的<code>prototye</code></strong>，从而实现<strong>继承</strong>。此时，<code>Child</code>构造函数、<code>Parent</code>的实例、<code>Child</code>的实例构成一个三角关系。于是：</p>\\n</blockquote>\\n<ul>\\n<li><code>new Child.__proto__ === new Parent()</code>的结果为<code>true</code></li>\\n</ul>\\n<p><strong>分析：</strong></p>\\n<ul>\\n<li>这种继承方式，<strong>Child 可以继承 Parent 的原型</strong>，但有个缺点：</li>\\n</ul>\\n<blockquote>\\n<p>缺点是：<strong>如果修改 child1实例的name属性，child2实例中的name属性也会跟着改变</strong>。</p>\\n</blockquote>\\n<p>如下：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/46.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>上面的代码中， <code>child1</code>修改了<code>arr</code>属性，却发现，<code>child2</code>的<code>arr</code>属性也跟着改变了。这显然不太好，在业务中，两个子模块应该隔离才对。如果改了一个对象，另一个对象却发生了改变，就不太好。</p>\\n</blockquote>\\n<blockquote>\\n<p>造成这种缺点的原因是：<code>child1</code>和<code>child2</code>共用原型。即：<code>chi1d1.__proto__ === child2__proto__</code>是严格相同。而 arr方法是在 Parent 的实例上（即 Child实例的原型）的。</p>\\n</blockquote>\\n<h4 id=\\"_3-3-方式三-组合的方式-构造函数-原型链\\"><a class=\\"header-anchor\\" href=\\"#_3-3-方式三-组合的方式-构造函数-原型链\\">#</a> 3.3 方式三：组合的方式：构造函数 + 原型链</h4>\\n<p>就是把上面的两种方式组合起来：</p>\\n<pre class=\\"language-js\\"><code>/*\\n组合方式实现继承：构造函数、原型链\\n  */\\nfunction Parent3() {\\n    this.name = &#39;Parent 的属性&#39;;\\n    this.arr = [1, 2, 3];\\n}\\n\\nfunction Child3() {\\n    Parent3.call(this); //【重要1】执行 parent方法\\n    this.type = &#39;Child 的属性&#39;;\\n}\\nChild3.prototype = new Parent3(); //【重要2】第二次执行parent方法\\n\\nvar child = new Child3();\\n</code></pre>\\n<ul>\\n<li>这种方式，能解决之前两种方式的问题：既可以继承父类原型的内容，也不会造成原型里属性的修改。</li>\\n<li>这种方式的缺点是：让父亲<code>Parent</code>的构造方法执行了两次。</li>\\n<li><code>ES6</code>中的继承方式，一带而过即可，重点是要掌握<code>ES5</code>中的继承。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>面试官说「你就讲一种」，你讲哪一种？</strong></p>\\n<p>讲寄生组合，并顺带说明它为什么长这样：用 <code>Object.create(Parent.prototype)</code> 拿原型、用 <code>Parent.call(this)</code> 拿属性，把「要方法」和「要属性」两件事拆开，所以父构造函数只跑一次。讲清这个取舍，比背五种写法有用。</p>\\n</li>\\n<li>\\n<p><strong><code>class</code> 的 <code>super()</code> 为什么必须在用 <code>this</code> 之前调用？</strong></p>\\n<p><code>ES6</code> 的继承里子类没有自己的 <code>this</code>，对象是先由父类构造函数创建、再交给子类加工的。<code>super()</code> 没执行完 <code>this</code> 还不存在，提前访问会抛 <code>ReferenceError</code>。这也正是它和 <code>ES5</code>「先建 <code>this</code> 再借父构造函数」的本质区别。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"三、dom事件总结\\"><a class=\\"header-anchor\\" href=\\"#三、dom事件总结\\">#</a> 三、DOM事件总结</h2>\\n<p><strong>知识点主要包括以下几个方面：</strong></p>\\n<ul>\\n<li>基本概念：<code>DOM</code>事件的级别</li>\\n</ul>\\n<blockquote>\\n<p>面试不会直接问你，DOM有几个级别。但会在题目中体现：“请用<code>DOM2</code> ....”。</p>\\n</blockquote>\\n<ul>\\n<li><code>DOM</code>事件模型、<code>DOM</code>事件流</li>\\n</ul>\\n<blockquote>\\n<p>面试官如果问你“<strong>DOM事件模型</strong>”，你不一定知道怎么回事。其实说的就是<strong>捕获和冒泡</strong>。</p>\\n</blockquote>\\n<p><strong>DOM事件流</strong>，指的是事件传递的<strong>三个阶段</strong>。</p>\\n<ul>\\n<li>描述<code>DOM</code>事件捕获的具体流程</li>\\n</ul>\\n<blockquote>\\n<p>讲的是事件的传递顺序。参数为<code>false</code>（默认）、参数为<code>true</code>，各自代表事件在什么阶段触发。</p>\\n</blockquote>\\n<p>能回答出来的人，寥寥无几。也许有些人可以说出一大半，但是一字不落的人，极少。</p>\\n<ul>\\n<li><code>Event</code>对象的常见应用（<code>Event</code>的常用<code>api</code>方法）</li>\\n</ul>\\n<blockquote>\\n<p><code>DOM</code>事件的知识点，一方面包括事件的流程；另一方面就是：怎么去注册事件，也就是监听用户的交互行为。第三点：在响应时，<code>Event</code>对象是非常重要的。</p>\\n</blockquote>\\n<p><strong>自定义事件（非常重要）</strong></p>\\n<blockquote>\\n<p>一般人可以讲出事件和注册事件，但是如果让你讲<strong>自定义事件</strong>，能知道的人，就更少了。</p>\\n</blockquote>\\n<h3 id=\\"_1-dom-事件级别有哪些-dom0-和-dom2-有什么区别\\"><a class=\\"header-anchor\\" href=\\"#_1-dom-事件级别有哪些-dom0-和-dom2-有什么区别\\">#</a> 1 DOM 事件级别有哪些，DOM0 和 DOM2 有什么区别</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>DOM0</code>：<code>element.onclick = fn</code>，同名只能挂一个，后赋值覆盖先赋值的，解绑写 <code>null</code></li>\\n<li><code>DOM2</code>：<code>addEventListener(type, fn, useCapture)</code>，可以挂任意多个，还能指定在捕获还是冒泡阶段触发</li>\\n<li>解绑必须传同一个函数引用，匿名函数和 <code>bind()</code> 的返回值都解不掉</li>\\n<li>第三个参数现在推荐传配置对象：<code>{ capture, once, passive, signal }</code></li>\\n<li><code>DOM3</code> 没改机制，只是补了键盘、鼠标滚轮、复合输入等事件类型；没有 <code>DOM1</code> 是因为那版标准根本没涉及事件</li>\\n<li><code>passive: true</code> 让浏览器不必等监听器执行完就能滚动，是移动端滚动优化的常用手段</li>\\n</ul>\\n<p><strong><code>DOM0</code> 是一个属性一个处理函数，<code>DOM2</code> 是一个事件类型一串监听器</strong> —— 按「写法 → 能不能多绑 → 怎么解绑 → <code>DOM3</code> 只是扩类型」讲。</p>\\n</section>\\n<p><strong>DOM事件的级别</strong></p>\\n<blockquote>\\n<p><code>DOM</code>事件的级别，准确来说，是<strong>DOM标准</strong>定义的级别。包括：</p>\\n</blockquote>\\n<p><strong>DOM0的写法：</strong></p>\\n<pre class=\\"language-javascript\\"><code>  element.onclick = function () {\\n\\n  }\\n</code></pre>\\n<blockquote>\\n<p>上面的代码是在 <code>js</code> 中的写法；如果要在<code>html</code>中写，写法是：在<code>onclick</code>属性中，加 <code>js</code> 语句。</p>\\n</blockquote>\\n<p><strong>DOM2的写法：</strong></p>\\n<pre class=\\"language-javascript\\"><code>  element.addEventListener(&#39;click&#39;, function () {\\n\\n  }, false);\\n</code></pre>\\n<blockquote>\\n<p>【重要】上面的第三参数中，<strong>true</strong>表示事件在<strong>捕获阶段</strong>触发，<strong>false</strong>表示事件在<strong>冒泡阶段</strong>触发（默认）。如果不写，则默认为false。</p>\\n</blockquote>\\n<p><strong>DOM3的写法：</strong></p>\\n<pre class=\\"language-javascript\\"><code>    element.addEventListener(&#39;keyup&#39;, function () {\\n\\n    }, false);\\n</code></pre>\\n<blockquote>\\n<p><code>DOM3</code>中，增加了很多事件类型，比如鼠标事件、键盘事件等。</p>\\n</blockquote>\\n<blockquote>\\n<p>PS：为何事件没有<code>DOM1</code>的写法呢？因为，<code>DOM1</code>标准制定的时候，没有涉及与事件相关的内容。</p>\\n</blockquote>\\n<p><strong>总结</strong>：关于“DOM事件的级别”，能回答出以上内容即可，不会出题目让你做。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>同事写 <code>el.addEventListener(\'click\', this.onClick.bind(this))</code>，卸载时用同样一句 <code>removeEventListener</code>，能解掉吗？</strong></p>\\n<p>解不掉。<code>bind</code> 每次都返回新函数，两次传的根本不是同一个引用，监听器会一直留着，路由来回切几次就积累一堆。要么先把绑定后的函数存下来复用，要么注册时传 <code>{ signal: controller.signal }</code>，卸载时 <code>controller.abort()</code> 一次性清理。</p>\\n</li>\\n<li>\\n<p><strong>移动端页面滚动卡顿，同事在 <code>touchmove</code> 里做了节流还是卡，你会看什么？</strong></p>\\n<p>先看有没有加 <code>{ passive: true }</code>。默认情况下浏览器必须等监听器执行完、确认没调用 <code>preventDefault</code> 才敢滚动，节流救不了这段等待。标记 <code>passive</code> 后滚动和监听器并行，代价是里面不能再调 <code>preventDefault</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_2-dom-事件模型和事件流是怎么回事-捕获顺序是什么\\"><a class=\\"header-anchor\\" href=\\"#_2-dom-事件模型和事件流是怎么回事-捕获顺序是什么\\">#</a> 2 DOM 事件模型和事件流是怎么回事，捕获顺序是什么</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>事件流三个阶段：捕获（从 <code>window</code> 往下到目标）、目标阶段、冒泡（从目标往上回到 <code>window</code>）</li>\\n<li>捕获顺序完整版：<code>window</code> → <code>document</code> → <code>html</code>（<code>document.documentElement</code>）→ <code>body</code> → 父级 → 目标元素，第一个拿到事件的是 <code>window</code> 而不是 <code>body</code></li>\\n<li><code>addEventListener</code> 第三个参数为 <code>true</code> 在捕获阶段触发，<code>false</code>（默认）在冒泡阶段触发</li>\\n<li>不是所有事件都冒泡：<code>focus</code>、<code>blur</code>、资源的 <code>error</code> 和 <code>load</code> 都不冒泡，对应的 <code>focusin</code> / <code>focusout</code> 才冒泡</li>\\n<li>事件委托就靠冒泡：监听挂父级、用 <code>event.target</code> 判断点了谁，动态子元素不用重新绑</li>\\n<li><code>event.eventPhase</code> 能直接读出当前处于哪个阶段（<code>1</code> 捕获 / <code>2</code> 目标 / <code>3</code> 冒泡）</li>\\n</ul>\\n<p><strong>一次点击其实走了「从上往下捕获、到达目标、再从下往上冒泡」一整趟</strong> —— 按「三个阶段 → 完整节点顺序 → 第三个参数决定在哪一段触发」讲。</p>\\n</section>\\n<p><strong>DOM事件模型</strong></p>\\n<blockquote>\\n<p><code>DOM</code>事件模型讲的就是<strong>捕获和冒泡</strong>，一般人都能回答出来。</p>\\n</blockquote>\\n<ul>\\n<li>捕获：从上往下。</li>\\n<li>冒泡：从下（目标元素）往上。</li>\\n</ul>\\n<p><strong>DOM事件流</strong></p>\\n<blockquote>\\n<p><code>DOM</code>事件流讲的就是：浏览器在于当前页面做交互时，这个事件是怎么传递到页面上的。</p>\\n</blockquote>\\n<p><strong>完整的事件流，分三个阶段：</strong></p>\\n<ol>\\n<li>捕获：从 <code>window</code> 对象传到 目标元素。</li>\\n<li>目标阶段：事件通过捕获，到达目标元素，这个阶段就是目标阶段。</li>\\n<li>冒泡：从<strong>目标元素</strong>传到 <code>Window</code> 对象。</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/58.png\\" alt=\\"\\">\\n<img src=\\"https://s.poetries.top/gitee/2020/07/59.png\\" alt=\\"\\"></p>\\n<p><strong>描述DOM事件捕获的具体流程</strong></p>\\n<blockquote>\\n<p>很少有人能说完整。</p>\\n</blockquote>\\n<p><strong>捕获的流程</strong></p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/60.png\\" alt=\\"\\"></p>\\n<p><strong>说明</strong>：捕获阶段，事件依次传递的顺序是：<code>window</code> --&gt; <code>document</code> --&gt; <code>html</code>--&gt; <code>body</code> --&gt; 父元素、子元素、目标元素。</p>\\n<ul>\\n<li>PS1：第一个接收到事件的对象是 <strong>window</strong>（有人会说<code>body</code>，有人会说<code>html</code>，这都是错误的）。</li>\\n<li>PS2：<code>JS</code>中涉及到<code>DOM</code>对象时，有两个对象最常用：<code>window</code>、<code>doucument</code>。它们俩也是最先获取到事件的。</li>\\n</ul>\\n<p>代码如下：</p>\\n<pre class=\\"language-javascript\\"><code>    window.addEventListener(&quot;click&quot;, function () {\\n        alert(&quot;捕获 window&quot;);\\n    }, true);\\n\\n    document.addEventListener(&quot;click&quot;, function () {\\n        alert(&quot;捕获 document&quot;);\\n    }, true);\\n\\n    document.documentElement.addEventListener(&quot;click&quot;, function () {\\n        alert(&quot;捕获 html&quot;);\\n    }, true);\\n\\n    document.body.addEventListener(&quot;click&quot;, function () {\\n        alert(&quot;捕获 body&quot;);\\n    }, true);\\n\\n    fatherBox.addEventListener(&quot;click&quot;, function () {\\n        alert(&quot;捕获 father&quot;);\\n    }, true);\\n\\n    childBox.addEventListener(&quot;click&quot;, function () {\\n        alert(&quot;捕获 child&quot;);\\n    }, true);\\n\\n</code></pre>\\n<p><strong>补充一个知识点：</strong></p>\\n<blockquote>\\n<p>在 <code>js</code>中：</p>\\n</blockquote>\\n<ul>\\n<li>如果想获取 <code>body</code> 节点，方法是：<code>document.body</code>；</li>\\n<li>但是，如果想获取 <code>html</code>节点，方法是<code>document.documentElement</code>。</li>\\n</ul>\\n<p><strong>冒泡的流程</strong></p>\\n<blockquote>\\n<p>与捕获的流程相反</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>列表有 1000 行、每行三个按钮，你会怎么绑事件？</strong></p>\\n<p>在列表容器上绑一次，用 <code>event.target.closest(\'[data-action]\')</code> 判断点的是哪个按钮。好处是只有一个监听器、新增行不用重新绑、删除行也不用解绑。代价要说清楚：不冒泡的事件（<code>focus</code>、资源 <code>error</code>）委托不了，子元素里如果有人调了 <code>stopPropagation</code>，委托也会被打断。</p>\\n</li>\\n<li>\\n<p><strong>弹窗「点击外部关闭」失效了：点按钮打开弹窗，弹窗立刻又被关掉，为什么？</strong></p>\\n<p>打开弹窗的那次点击继续冒泡到了 <code>document</code>，被「点击外部」的监听器接住了。稳妥的修法是在下一个宏任务再注册外部点击监听，或者判断时排除触发元素；直接在按钮上 <code>stopPropagation</code> 也能解决，但会顺带打断页面上其它的事件委托。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-event-对象有哪些常用-api\\"><a class=\\"header-anchor\\" href=\\"#_3-event-对象有哪些常用-api\\">#</a> 3 Event 对象有哪些常用 API</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>preventDefault()</code> 只拦默认行为（跳转、提交、右键菜单），不影响传播；<code>event.defaultPrevented</code> 能查是否已被拦</li>\\n<li><code>stopPropagation()</code> 只拦传播，不影响默认行为；<code>stopImmediatePropagation()</code> 连同一元素上后注册的监听器一起拦</li>\\n<li><code>target</code> 是真正被操作的元素，<code>currentTarget</code> 是当前监听器挂在哪个元素上，事件委托全靠这个区别</li>\\n<li><code>currentTarget</code> 只在事件处理期间有效，异步回调里再读会是 <code>null</code>，要先用变量存下来</li>\\n<li><code>event.isTrusted</code> 区分真实用户操作和脚本 <code>dispatchEvent</code> 派发的事件</li>\\n<li><code>passive: true</code> 的监听器里调 <code>preventDefault()</code> 无效，控制台还会给警告</li>\\n</ul>\\n<p><strong><code>preventDefault</code> 管「做不做」，<code>stopPropagation</code> 管「传不传」，两件事互不影响</strong> —— 再补 <code>target</code> 与 <code>currentTarget</code> 的区别，这两条覆盖了这题九成考点。</p>\\n</section>\\n<p><strong>Event对象的常见 api 方法</strong></p>\\n<blockquote>\\n<p>用户做的是什么操作（比如，是敲键盘了，还是点击鼠标了），这些事件基本都是通过<code>Event</code>对象拿到的。这些都比较简单，我们就不讲了。我们来看看下面这几个方法：</p>\\n</blockquote>\\n<p><strong>方法一</strong></p>\\n<pre class=\\"language-javascript\\"><code>    event.preventDefault();\\n</code></pre>\\n<ul>\\n<li>解释：阻止默认事件。</li>\\n<li>比如，已知<code>&lt;a&gt;</code>标签绑定了click事件，此时，如果给<code>&lt;a&gt;</code>设置了这个方法，就阻止了链接的默认跳转。</li>\\n</ul>\\n<p><strong>方法二：阻止冒泡</strong></p>\\n<blockquote>\\n<p>这个在业务中很常见。</p>\\n</blockquote>\\n<blockquote>\\n<p>有的时候，业务中不需要事件进行冒泡。比如说，业务这样要求：单击子元素做事件<code>A</code>，单击父元素做事件B，如果不阻止冒泡的话，出现的问题是：单击子元素时，子元素和父元素都会做事件<code>A</code>。这个时候，就要用到阻止冒泡了。</p>\\n</blockquote>\\n<blockquote>\\n<p><code>w3c</code>的方法：（火狐、谷歌、<code>IE11</code>）</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>    event.stopPropagation();\\n</code></pre>\\n<blockquote>\\n<p><code>IE10</code>以下则是：</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>\\tevent.cancelBubble = true;\\n</code></pre>\\n<blockquote>\\n<p>兼容代码如下：</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>   box3.onclick = function (event) {\\n\\n        alert(&quot;child&quot;);\\n\\n        //阻止冒泡\\n        event = event || window.event;\\n\\n        if (event &amp;&amp; event.stopPropagation) {\\n            event.stopPropagation();\\n        } else {\\n            event.cancelBubble = true;\\n        }\\n    }\\n</code></pre>\\n<blockquote>\\n<p>上方代码中，我们对<code>box3</code>进行了阻止冒泡，产生的效果是：事件不会继续传递到 <code>father</code>、<code>grandfather</code>、<code>body</code>了。</p>\\n</blockquote>\\n<p><strong>方法三：设置事件优先级</strong></p>\\n<pre class=\\"language-javascript\\"><code>    event.stopImmediatePropagation();\\n</code></pre>\\n<p>这个方法比较长，一般人没听说过。解释如下：</p>\\n<blockquote>\\n<p>比如说，我用<code>addEventListener</code>给某按钮同时注册了事件<code>A</code>、事件<code>B</code>。此时，如果我单击按钮，就会依次执行事件A和事件<code>B</code>。现在要求：单击按钮时，只执行事件A，不执行事件<code>B</code>。该怎么做呢？这是时候，就可以用到<code>stopImmediatePropagation</code>方法了。做法是：在事件A的响应函数中加入这句话。</p>\\n</blockquote>\\n<blockquote>\\n<p>大家要记住 <code>event</code> 有这个方法。</p>\\n</blockquote>\\n<p><strong>属性4、属性5（事件委托中用到）</strong></p>\\n<pre class=\\"language-javascript\\"><code>\\n    event.currentTarget   //当前所绑定的事件对象。在事件委托中，指的是【父元素】。\\n\\n    event.target  //当前被点击的元素。在事件委托中，指的是【子元素】。\\n\\n</code></pre>\\n<p>上面这两个属性，在事件委托中经常用到。</p>\\n<blockquote>\\n<p><strong>总结</strong>：上面这几项，非常重要，但是容易弄混淆。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>表单提交想自己发 <code>Ajax</code>，同事在监听器里写了 <code>return false</code>，为什么没用？</strong></p>\\n<p><code>return false</code> 只在 <code>DOM0</code> 的 <code>onsubmit</code> 里等价于 <code>preventDefault</code> 加 <code>stopPropagation</code>，<code>addEventListener</code> 注册的监听器返回值会被直接忽略。必须显式调 <code>event.preventDefault()</code>。</p>\\n</li>\\n<li>\\n<p><strong>在事件处理函数里 <code>setTimeout</code> 之后读 <code>event.currentTarget</code> 拿到 <code>null</code>，怎么回事？</strong></p>\\n<p>事件派发结束后浏览器会把 <code>currentTarget</code> 重置。需要的话在同步阶段先 <code>const el = event.currentTarget</code> 存下来。顺带一提，<code>React 17</code> 之前的合成事件还有事件池复用，异步访问整个事件对象都会失效，<code>React 17</code> 起事件池已经移除。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_4-自定义事件怎么创建和派发\\"><a class=\\"header-anchor\\" href=\\"#_4-自定义事件怎么创建和派发\\">#</a> 4 自定义事件怎么创建和派发</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>创建用 <code>new CustomEvent(type, { detail, bubbles, cancelable })</code>，业务数据放在 <code>detail</code> 里</li>\\n<li>派发用 <code>el.dispatchEvent(event)</code>，参数是事件对象不是事件名，这是最常见的手误</li>\\n<li><code>dispatchEvent</code> 返回 <code>false</code> 表示有监听器调用了 <code>preventDefault</code>，派发方可以据此中止后续动作</li>\\n<li><code>bubbles</code> 默认是 <code>false</code>，不写的话父级监听不到</li>\\n<li><code>dispatchEvent</code> 是同步的：所有监听器执行完它才返回，别在里面做重活</li>\\n<li>典型用途是跨框架、微前端之间的解耦通信，比自己造事件总线更贴近平台</li>\\n</ul>\\n<p><strong>自定义事件 = <code>CustomEvent</code> 造事件 + <code>dispatchEvent</code> 派发 + <code>detail</code> 带数据</strong> —— 再补 <code>bubbles</code> 默认关闭、返回值表示是否被取消这两个易错点。</p>\\n</section>\\n<p><strong>自定义事件</strong></p>\\n<blockquote>\\n<p>自定义事件的代码如下：</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>    var myEvent = new Event(&#39;clickTest&#39;);\\n    element.addEventListener(&#39;clickTest&#39;, function () {\\n        console.log(&#39;smyhvae&#39;);\\n    });\\n\\n\\t//元素注册事件\\n    element.dispatchEvent(myEvent); //注意，参数是写事件对象 myEvent，不是写 事件名 clickTest\\n\\n</code></pre>\\n<blockquote>\\n<p>上面这个事件是定义完了之后，就直接自动触发了。在正常的业务中，这个事件一般是和别的事件结合用的。比如延时器设置按钮的动作：</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>    var myEvent = new Event(&#39;clickTest&#39;);\\n\\n    element.addEventListener(&#39;clickTest&#39;, function () {\\n        console.log(&#39;smyhvae&#39;);\\n    });\\n\\n    setTimeout(function () {\\n        element.dispatchEvent(myEvent); //注意，参数是写事件对象 myEvent，不是写 事件名 clickTest\\n    }, 1000);\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>微前端主应用要通知子应用刷新，你用自定义事件还是全局变量？</strong></p>\\n<p>用挂在 <code>window</code> 上的自定义事件。它天然支持多个监听者、能带 <code>detail</code>、子应用卸载时 <code>removeEventListener</code> 就干净了；全局变量得自己实现订阅和清理，还容易被别人覆盖。实践中要约定事件名前缀，避免和第三方库撞车。</p>\\n</li>\\n<li>\\n<p><strong>自定义事件派发之后马上读状态，会不会监听器还没执行完？</strong></p>\\n<p>不会。<code>dispatchEvent</code> 是同步执行的，它返回时所有监听器都跑完了（监听器内部又开了异步那是另一回事）。这也是它和 <code>postMessage</code> 的关键区别 —— 后者是异步投递的。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"四、event-loop详细版\\"><a class=\\"header-anchor\\" href=\\"#四、event-loop详细版\\">#</a> 四、Event Loop详细版</h2>\\n<h3 id=\\"_1-为什么-gui-渲染线程与-js-引擎线程互斥\\"><a class=\\"header-anchor\\" href=\\"#_1-为什么-gui-渲染线程与-js-引擎线程互斥\\">#</a> 1 为什么 GUI 渲染线程与 JS 引擎线程互斥</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>原因只有一条：<code>JS</code> 能改 <code>DOM</code>。两个线程同时跑，渲染线程可能读到改了一半的 <code>DOM</code>，结果不可预期</li>\\n<li>所以 <code>JS</code> 执行期间渲染被挂起，<code>DOM</code> 改动排进队列，等主线程空闲再统一渲染</li>\\n<li>直接后果：长任务会卡住动画、输入和点击，也就是俗称的页面假死</li>\\n<li>同一个宏任务里对同一元素改十次样式，用户只会看到最后一次结果，中间态根本没机会上屏</li>\\n<li>应对手段是让出主线程：任务切片、<code>requestIdleCallback</code>、把纯计算挪进 <code>Web Worker</code></li>\\n<li><code>Web Worker</code> 拿不到 <code>DOM</code>，正是这条互斥规则的延伸</li>\\n</ul>\\n<p><strong>互斥是因为 <code>JS</code> 能改 <code>DOM</code>，同时跑就会渲染出不可预期的中间态</strong> —— 按「原因 → 表现（卡顿、看不到中间态）→ 怎么让出主线程」讲。</p>\\n</section>\\n<ul>\\n<li>这是由于 JS 是可以操作 DOM 的，如果同时修改元素属性并同时渲染界面(即 JS线程和 UI线程同时运行)</li>\\n<li>那么渲染线程前后获得的元素就可能不一致了</li>\\n<li>因此，为了防止渲染出现不可预期的结果，浏览器设定 GUI渲染线程和 JS引擎线程为互斥关系</li>\\n<li>当 JS引擎线程执行时 GUI渲染线程会被挂起，GUI更新则会被保存在一个队列中等待 JS引擎线程空闲时立即被执行</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>首屏要处理两万条数据再渲染，页面白屏三秒，你怎么改？</strong></p>\\n<p>先分清是计算慢还是渲染慢。纯计算就挪进 <code>Web Worker</code>，主线程只等结果；必须留在主线程就切片，每片控制在 <code>5ms</code> 内，用 <code>requestIdleCallback</code> 或 <code>scheduler.postTask</code> 排队，让浏览器有机会插入渲染帧。如果瓶颈是节点太多，那要上虚拟列表，优化计算没用。</p>\\n</li>\\n<li>\\n<p><strong>既然 <code>Web Worker</code> 不能操作 <code>DOM</code>，它还能干什么？</strong></p>\\n<p>能干所有不碰 <code>DOM</code> 的重活：大数据解析排序、加解密、图片像素处理、<code>diff</code> 计算、埋点批量压缩。结果用 <code>postMessage</code> 传回主线程再渲染。传大对象时用 <code>Transferable</code> 转移所有权，否则光是结构化克隆就够卡一下的。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_2-从-event-loop-看-js-的运行机制\\"><a class=\\"header-anchor\\" href=\\"#_2-从-event-loop-看-js-的运行机制\\">#</a> 2 从 Event Loop 看 JS 的运行机制</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>同步任务在执行栈上跑；异步任务的回调由各自的线程（定时器线程、异步 <code>HTTP</code> 线程）在时机到了之后塞进任务队列</li>\\n<li>执行栈清空后主线程才去任务队列取回调执行，如此循环，这就是事件循环</li>\\n<li>注意区分：<code>setTimeout</code> / <code>fetch</code> 这些函数的调用本身是同步的，只有回调才是异步的</li>\\n<li>单线程指的是 <code>JS</code> 引擎线程只有一个，浏览器本身是多线程的（渲染、定时器、网络、事件触发各一套）</li>\\n<li><code>setTimeout(fn, 0)</code> 不是立刻执行，是「尽快排进下一轮宏任务」，实测通常还有 <code>4ms</code> 左右的最小延迟</li>\\n<li>浏览器和 <code>Node</code> 不是同一套：<code>Node</code> 分 <code>timers</code>、<code>poll</code>、<code>check</code> 等阶段，还多一个 <code>process.nextTick</code></li>\\n</ul>\\n<p><strong>事件循环就是「执行栈清空 → 从任务队列取一个回调 → 再清空」的循环，异步能力来自浏览器的其它线程</strong> —— 按「谁在执行 → 谁在计时 → 谁在派发」三方分工讲。</p>\\n</section>\\n<p><strong>先理解一些概念：</strong></p>\\n<ul>\\n<li><code>JS</code> 分为同步任务和异步任务</li>\\n<li>同步任务都在JS引擎线程上执行，形成一个 执行栈</li>\\n<li>事件触发线程管理一个 任务队列，异步任务触发条件达成，将回调事件放到 任务队列中</li>\\n<li>执行栈中所有同步任务执行完毕，此时JS引擎线程空闲，系统会读取 任务队列，将可运行的异步任务回调事件添加到 执行栈中，开始执行</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/69.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>前端开发中我们会通过 <code>setTimeout/setInterval</code> 来指定定时任务，会通过 <code>XHR/fetch</code>发送网络请求</li>\\n<li>接下来简述一下 <code>setTimeout/setInterval</code>和 <code>XHR/fetch</code>到底做了什么事</li>\\n<li>我们知道，不管是 <code>setTimeout/setInterval</code>和 <code>XHR/fetch</code>代码，在这些代码执行时，\\n本身是同步任务，而其中的回调函数才是异步任务</li>\\n<li>当代码执行到 <code>setTimeout/setInterval</code>时，实际上是 JS引擎线程通知 定时触发器线程，间隔一个时间后，会触发一个回调事件</li>\\n<li>而定时触发器线程在接收到这个消息后，会在等待的时间后，将回调事件放入到由 事件触发线程所管理的事件队列中</li>\\n<li>当代码执行到 <code>XHR/fetch</code>时，实际上是 <code>JS</code>引擎线程通知 异步<code>http</code>请求线程，发送一个网络请求，并制定请求完成后的回调事件</li>\\n<li>而异步<code>http</code>请求线程在接收到这个消息后，会在请求成功后，将回调事件放入到由 事件触发线程所管理的 事件队列中</li>\\n<li>当我们的同步任务执行完，<code>JS</code>引擎线程会询问事件触发线程，在 事件队列中是否有待执行的回调函数，如果有就会加入到执行栈中交给 JS引擎线程执行</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/70.png\\" alt=\\"\\">\\n<img src=\\"https://s.poetries.top/gitee/20190928/3.png\\" alt=\\"\\"></p>\\n<p><strong>总结一下：</strong></p>\\n<ul>\\n<li>JS引擎线程只执行执行栈中的事件</li>\\n<li>执行栈中的代码执行完毕，就会读取事件队列中的事件</li>\\n<li>事件队列中的回调事件，是由各自线程插入到事件队列中的</li>\\n<li>如此循环</li>\\n</ul>\\n<blockquote>\\n<p>当我们基本了解了什么是执行栈，什么是事件队列之后，我们深入了解一下事件循环中 宏任务、 微任务</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>setTimeout(fn, 1000)</code> 一定在 1 秒后执行吗？</strong></p>\\n<p>不一定，只能保证「至少 1 秒后」。到点时回调只是被放进任务队列，还得等执行栈清空；主线程正忙就会继续延后。所以做动画不要靠 <code>setTimeout</code> 累加计时，用 <code>requestAnimationFrame</code>，或者每次都读真实时间戳来校正。</p>\\n</li>\\n<li>\\n<p><strong>页面切到后台再切回来，倒计时走慢了，怎么回事？</strong></p>\\n<p>浏览器会对隐藏标签页的定时器降频（通常降到一秒一次，甚至冻结），是省电策略。靠定时器次数累加的倒计时必然走偏，正确做法是记录起始时间戳、每次用 <code>Date.now()</code> 算真实差值，并在 <code>visibilitychange</code> 时重新校正。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-宏任务和微任务有什么区别-执行顺序是什么\\"><a class=\\"header-anchor\\" href=\\"#_3-宏任务和微任务有什么区别-执行顺序是什么\\">#</a> 3 宏任务和微任务有什么区别，执行顺序是什么</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>一轮循环是：取一个宏任务执行 → 清空整个微任务队列 → 需要就渲染一帧 → 再取下一个宏任务</li>\\n<li>宏任务有：<code>script</code> 整体、<code>setTimeout</code> / <code>setInterval</code>、<code>I/O</code>、<code>UI</code> 事件回调、<code>MessageChannel</code></li>\\n<li>微任务有：<code>Promise.then</code> / <code>catch</code> / <code>finally</code>、<code>queueMicrotask</code>、<code>MutationObserver</code>（<code>Node</code> 还有 <code>process.nextTick</code>）</li>\\n<li>微任务是「一次清空」：清空过程中新产生的微任务也在本轮跑完，所以它总能插队到渲染之前</li>\\n<li>推论一：同一个宏任务里改多次样式只渲染一次；拆成两个宏任务用户就能看到中间态</li>\\n<li>推论二：微任务里递归产生新微任务会饿死渲染，页面直接卡死，比 <code>setTimeout</code> 死循环还狠</li>\\n</ul>\\n<p><strong>宏任务之间可以插入渲染，微任务只能在本轮宏任务末尾、渲染之前一次性清空</strong> —— 按「一轮循环四步 → 各自都有谁 → 两条工程推论」讲。</p>\\n</section>\\n<h4 id=\\"_3-1-什么是宏任务\\"><a class=\\"header-anchor\\" href=\\"#_3-1-什么是宏任务\\">#</a> 3.1 什么是宏任务</h4>\\n<ul>\\n<li>我们可以将每次执行栈执行的代码当做是一个宏任务（包括每次从事件队列中获取一个事件回调并放到执行栈中执行）</li>\\n<li>每一个宏任务会从头到尾执行完毕，不会执行其他。</li>\\n</ul>\\n<blockquote>\\n<p>我们前文提到过 JS引擎线程和 GUI渲染线程是互斥的关系，浏览器为了能够使 宏任务和 DOM任务有序的进行，会在一个 宏任务执行结果后，在下一个 宏任务执行前， GUI渲染线程开始工作，对页面进行渲染。</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>// 宏任务--&gt;渲染--&gt;宏任务--&gt;渲染--&gt;渲染．．\\n</code></pre>\\n<blockquote>\\n<p>主代码块，<code>setTimeout</code>，<code>setInterval</code>等，都属于宏任务</p>\\n</blockquote>\\n<p><strong>第一个例子：</strong></p>\\n<p>我们可以将这段代码放到浏览器的控制台执行以下，看一下效果：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/20190928/4.png\\" alt=\\"\\"></p>\\n<p><img src=\\"https://s.poetries.top/gitee/20190928/5.gif\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>我们会看到的结果是，页面背景会在瞬间变成白色，以上代码属于同一次 宏任务，所以全部执行完才触发 页面渲染，渲染时 GUI线程会将所有UI改动优化合并，所以视觉效果上，只会看到页面变成灰色</p>\\n</blockquote>\\n<p><strong>第二个例子：</strong></p>\\n<p><img src=\\"https://s.poetries.top/gitee/20190928/6.png\\" alt=\\"\\"></p>\\n<p><img src=\\"https://s.poetries.top/gitee/20190928/7.gif\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>我会看到，页面先显示成蓝色背景，然后瞬间变成了黑色背景，这是因为以上代码属于两次 宏任务，第一次 宏任务执行的代码是将背景变成蓝色，然后触发渲染，将页面变成蓝色，再触发第二次宏任务将背景变成黑色</p>\\n</blockquote>\\n<h4 id=\\"_3-2-什么是微任务\\"><a class=\\"header-anchor\\" href=\\"#_3-2-什么是微任务\\">#</a> 3.2 什么是微任务</h4>\\n<ul>\\n<li>我们已经知道 宏任务结束后，会执行渲染，然后执行下一个 宏任务，</li>\\n<li>而微任务可以理解成在当前 宏任务执行后立即执行的任务。</li>\\n<li>也就是说，<strong>当 宏任务执行完，会在渲染前，将执行期间所产生的所有 微任务都执行完</strong>。</li>\\n</ul>\\n<blockquote>\\n<p><code>Promise</code>，<code>process.nextTick</code>等，属于 微任务。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/20190928/8.png\\" alt=\\"\\"></p>\\n<p><img src=\\"https://s.poetries.top/gitee/20190928/9.gif\\" alt=\\"\\"></p>\\n<ul>\\n<li>控制台输出 1 3 2 , 是因为 promise 对象的 then 方法的回调函数是异步执行，所以 2 最后输出</li>\\n<li>页面的背景色直接变成黑色，没有经过蓝色的阶段，是因为，我们在宏任务中将背景设置为蓝色，但在进行渲染前执行了微任务，在微任务中将背景变成了黑色，然后才执行的渲染</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/gitee/20190928/10.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>上面代码共包含两个 <code>setTimeout</code> ，也就是说除主代码块外，共有两个 宏任务，\\n其中第一个 宏任务执行中，输出 1 ，并且创建了 微任务队列，所以在下一个 宏任务队列执行前，先执行 微任务，在 微任务执行中，输出 3 ，微任务执行后，执行下一次 宏任务，执行中输出 2</p>\\n</blockquote>\\n<h4 id=\\"_3-3-一轮事件循环的完整顺序\\"><a class=\\"header-anchor\\" href=\\"#_3-3-一轮事件循环的完整顺序\\">#</a> 3.3 一轮事件循环的完整顺序</h4>\\n<ul>\\n<li>执行一个 宏任务（栈中没有就从 事件队列中获取）</li>\\n<li>执行过程中如果遇到 微任务，就将它添加到 微任务的任务队列中</li>\\n<li>宏任务执行完毕后，立即执行当前 微任务队列中的所有 微任务（依次执行）</li>\\n<li>当前 宏任务执行完毕，开始检查渲染，然后 GUI线程接管渲染</li>\\n<li>渲染完毕后， JS线程继续接管，开始下一个 宏任务（从事件队列中获取）</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/gitee/20190928/11.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>await</code> 后面的代码算宏任务还是微任务？</strong></p>\\n<p>微任务。<code>await</code> 之前的代码是同步执行的，<code>await</code> 会把函数剩下的部分包成一个微任务挂在那个 <code>Promise</code> 后面。所以 <code>async</code> 函数不等于「整体异步」，这是笔试题最爱考的点。</p>\\n</li>\\n<li>\\n<p><strong>要在 <code>DOM</code> 更新后立刻量元素尺寸，你用 <code>setTimeout(0)</code> 还是 <code>requestAnimationFrame</code>？</strong></p>\\n<p>都不是首选。<code>Vue</code> 用 <code>nextTick</code>（微任务）、<code>React</code> 用 <code>useLayoutEffect</code>，它们保证在渲染提交后、浏览器绘制前同步执行。<code>requestAnimationFrame</code> 是下一帧渲染前触发，<code>setTimeout(0)</code> 时机更不确定，两者都可能量到旧布局或者白多等一帧。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"五、css盒模型及bfc\\"><a class=\\"header-anchor\\" href=\\"#五、css盒模型及bfc\\">#</a> 五、CSS盒模型及BFC</h2>\\n<p><strong>题目：谈一谈你对CSS盒模型的认识</strong></p>\\n<blockquote>\\n<p>专业的面试，一定会问 <code>CSS</code> 盒模型。对于这个题目，我们要回答一下几个方面：</p>\\n</blockquote>\\n<ol>\\n<li>基本概念：<code>content</code>、<code>padding</code>、<code>margin</code></li>\\n<li>标准盒模型、<code>IE</code>盒模型的区别。不要漏说了<code>IE</code>盒模型，通过这个问题，可以筛选一部分人</li>\\n<li><code>CSS</code>如何设置这两种模型（即：如何设置某个盒子为其中一个模型）？如果回答了上面的第二条，还会继续追问这一条。</li>\\n<li><code>JS</code>如何设置、获取盒模型对应的宽和高？这一步，已经有很多人答不上来了。</li>\\n<li>实例题：根据盒模型解释<strong>边距重叠</strong>。</li>\\n</ol>\\n<blockquote>\\n<p>前四个方面是逐渐递增，第五个方面，却鲜有人知。</p>\\n</blockquote>\\n<ol start=\\"6\\">\\n<li><code>BFC</code>（边距重叠解决方案）或<code>IFC</code>。</li>\\n</ol>\\n<blockquote>\\n<p>如果能回答第五条，就会引出第六条。<code>BFC</code>是面试频率较高的。</p>\\n</blockquote>\\n<p><strong>总结</strong>：以上几点，从上到下，知识点逐渐递增，知识面从理论、<code>CSS</code>、<code>JS</code>，又回到<code>CSS</code>理论</p>\\n<p>接下来，我们把上面的六条，依次讲解。</p>\\n<h3 id=\\"_1-谈一谈你对-css-盒模型的认识\\"><a class=\\"header-anchor\\" href=\\"#_1-谈一谈你对-css-盒模型的认识\\">#</a> 1 谈一谈你对 CSS 盒模型的认识</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>一个盒子从内到外是 <code>content</code>、<code>padding</code>、<code>border</code>、<code>margin</code> 四层</li>\\n<li>标准盒模型（<code>content-box</code>）里 <code>width</code> 只算内容区；<code>IE</code> 盒模型（<code>border-box</code>）里 <code>width</code> 含 <code>padding</code> 和 <code>border</code></li>\\n<li>用 <code>box-sizing</code> 切换，默认是 <code>content-box</code>；现代项目一般全局设成 <code>border-box</code>，定宽时不用自己减</li>\\n<li><code>margin</code> 永远不算进盒子尺寸，它描述的是盒子之间的距离</li>\\n<li>行内元素的 <code>width</code> / <code>height</code> 不生效，垂直 <code>padding</code> / <code>margin</code> 也不撑开行，这是高频陷阱</li>\\n<li>答完这题面试官通常顺着问 <code>JS</code> 怎么取宽高、<code>margin</code> 重叠和 <code>BFC</code>，提前把这条线串好</li>\\n</ul>\\n<p><strong>盒模型的核心差别只有一句：<code>width</code> 到底算不算 <code>padding</code> 和 <code>border</code></strong> —— 按「四层结构 → 两种模型 → <code>box-sizing</code> 切换 → <code>margin</code> 不计入」讲。</p>\\n</section>\\n<p><strong>标准盒模型和IE盒子模型</strong></p>\\n<p>标准盒子模型：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/47.png\\" alt=\\"\\"></p>\\n<p><code>IE</code>盒子模型：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/48.png\\" alt=\\"\\"></p>\\n<p>上图显示：</p>\\n<blockquote>\\n<p>在 <code>CSS</code> 盒子模型 (<code>Box Model</code>) 规定了元素处理元素的几种方式：</p>\\n</blockquote>\\n<ul>\\n<li><code>width</code>和<code>height</code>：<strong>内容</strong>的宽度、高度（不是盒子的宽度、高度）。</li>\\n<li><code>padding</code>：内边距。</li>\\n<li><code>border</code>：边框。</li>\\n<li><code>margin</code>：外边距。</li>\\n</ul>\\n<blockquote>\\n<p><code>CSS</code>盒模型和<code>IE</code>盒模型的区别：</p>\\n</blockquote>\\n<ul>\\n<li>\\n<p>在<strong>标准盒子模型</strong>中，<strong>width 和 height 指的是内容区域</strong>的宽度和高度。增加内边距、边框和外边距不会影响内容区域的尺寸，但是会增加元素框的总尺寸。</p>\\n</li>\\n<li>\\n<p><strong>IE盒子模型</strong>中，<strong>width 和 height 指的是内容区域+border+padding</strong>的宽度和高度。</p>\\n</li>\\n</ul>\\n<p><strong>CSS如何设置这两种模型</strong></p>\\n<p>代码如下：</p>\\n<pre class=\\"language-javascript\\"><code>/* 设置当前盒子为 标准盒模型（默认） */\\nbox-sizing: content-box;\\n\\n/* 设置当前盒子为 IE盒模型 */\\nbox-sizing: border-box;\\n</code></pre>\\n<blockquote>\\n<p>备注：盒子默认为标准盒模型。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>为什么现在的项目普遍在全局写 <code>* { box-sizing: border-box }</code>？</strong></p>\\n<p>因为布局时我们想控制的是「这个盒子占多宽」，而不是「内容区多宽」。用 <code>content-box</code> 时加一点 <code>padding</code> 就得手动把 <code>width</code> 减回去，栅格和百分比宽度尤其难算。全局设 <code>border-box</code> 后 <code>width: 50%</code> 就是真的占一半，代价是要留意第三方组件是否依赖默认值。</p>\\n</li>\\n<li>\\n<p><strong>给一个 <code>span</code> 设了 <code>width: 200px; height: 100px</code>，为什么没效果？</strong></p>\\n<p>行内元素的宽高不生效，垂直方向的 <code>padding</code> 和 <code>margin</code> 也不参与布局（背景会画出来但不撑开行）。要么 <code>display: inline-block</code>，要么改成 <code>block</code>。这也是为什么 <code>img</code>、<code>input</code> 这类替换元素的表现和普通行内元素不一样。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_2-js-如何设置、获取盒模型对应的宽和高\\"><a class=\\"header-anchor\\" href=\\"#_2-js-如何设置、获取盒模型对应的宽和高\\">#</a> 2 JS 如何设置、获取盒模型对应的宽和高</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>设置只有一条主路：<code>el.style.width = \'100px\'</code>，写的是行内样式</li>\\n<li><code>el.style.width</code> 只读得到行内样式，样式写在 <code>CSS</code> 文件里时它是空字符串</li>\\n<li><code>getComputedStyle(el).width</code> 拿的是最终计算值，永远是内容区宽度，不含 <code>padding</code> 和 <code>border</code></li>\\n<li><code>clientWidth</code> = <code>content</code> + <code>padding</code>；<code>offsetWidth</code> = <code>content</code> + <code>padding</code> + <code>border</code>，两者都取整</li>\\n<li><code>getBoundingClientRect()</code> 返回带小数、且算上 <code>transform</code> 缩放的实际渲染尺寸，量元素统一用它</li>\\n<li>连续读这些属性会强制同步布局（<code>layout thrashing</code>），要把「读」和「写」分批，别在循环里交替</li>\\n</ul>\\n<p><strong>六个 <code>API</code> 量的根本不是同一段距离，先问清楚要的是哪一段</strong> —— 按「设置只有行内样式 → 读取分四档 → 实测统一用 <code>getBoundingClientRect</code> → 注意强制回流」讲。</p>\\n</section>\\n<p><strong>取值方式一览</strong></p>\\n<blockquote>\\n<p>方式一：通过<code>DOM</code>节点的 <code>style</code> 样式获取</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>element.style.width/height;\\n</code></pre>\\n<blockquote>\\n<p>缺点：通过这种方式，只能获取<strong>行内样式</strong>，不能获取<code>内嵌</code>的样式和<code>外链</code>的样式。</p>\\n</blockquote>\\n<p>这种方式有局限性，但应该了解。</p>\\n<blockquote>\\n<p>方式二（通用型）</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>window.getComputedStyle(element).width/height;\\n</code></pre>\\n<blockquote>\\n<p>方式二能兼容 <code>Chrome</code>、火狐。是通用型方式。</p>\\n</blockquote>\\n<blockquote>\\n<p>方式三（IE独有的）</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>\\telement.currentStyle.width/height;\\n</code></pre>\\n<blockquote>\\n<p>和方式二相同，但这种方式只有IE独有。获取到的即时运行完之后的宽高（三种css样式都可以获取）。</p>\\n</blockquote>\\n<blockquote>\\n<p>方式四</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>\\telement.getBoundingClientRect().width/height;\\n</code></pre>\\n<blockquote>\\n<p>此 <code>api</code> 的作用是：获取一个元素的绝对位置。绝对位置是视窗 <code>viewport</code> 左上角的绝对位置。此 <code>api</code> 可以拿到四个属性：<code>left</code>、<code>top</code>、<code>width</code>、<code>height</code>。</p>\\n</blockquote>\\n<p><strong>总结：</strong></p>\\n<blockquote>\\n<p>上面的四种方式，要求能说出来区别，以及哪个的通用型更强。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>要判断元素有没有进入视口，你用 <code>getBoundingClientRect</code> 还是别的？</strong></p>\\n<p>首选 <code>IntersectionObserver</code>。<code>getBoundingClientRect</code> 得绑在滚动事件上反复调用，每次都触发强制布局，长列表里必卡；<code>IntersectionObserver</code> 由浏览器在主线程之外算好再回调，还能配 <code>rootMargin</code> 提前触发，做懒加载和曝光埋点都用它。</p>\\n</li>\\n<li>\\n<p><strong>循环里先读 <code>offsetHeight</code> 再写 <code>style.height</code>，为什么特别慢？</strong></p>\\n<p>读几何属性时浏览器必须保证布局是最新的，而上一次的写让布局失效了，于是每次读都强制同步重排，<code>N</code> 次循环就是 <code>N</code> 次重排。改法是先把要读的全部读完存进数组，再统一写；或者干脆改用 <code>transform</code> 这类不触发布局的属性。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-什么是-margin-塌陷-外边距重叠-怎么解决\\"><a class=\\"header-anchor\\" href=\\"#_3-什么是-margin-塌陷-外边距重叠-怎么解决\\">#</a> 3 什么是 margin 塌陷（外边距重叠），怎么解决</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>只发生在标准流的<strong>竖直</strong>方向，水平方向的 <code>margin</code> 从不合并</li>\\n<li>三种场景：相邻兄弟之间、父子之间（父元素没有 <code>border</code> / <code>padding</code> / <code>BFC</code> 隔开）、空元素自身的上下 <code>margin</code></li>\\n<li>合并规则是取较大值；有负值时是「最大正值加最小负值」，全是负值则取绝对值最大的那个</li>\\n<li>父子塌陷的解法：给父元素加 <code>padding</code> 或 <code>border</code>，或让父元素成为 <code>BFC</code>（<code>display: flow-root</code> 最干净）</li>\\n<li>更省事的做法是用父元素的 <code>padding</code> 表达父子间距，<code>margin</code> 只用来表达兄弟之间的距离</li>\\n<li><code>flex</code> / <code>grid</code> 容器的子项之间不会发生外边距重叠，用 <code>gap</code> 就彻底绕开了这个问题</li>\\n</ul>\\n<p><strong>外边距重叠只在标准流的竖直方向发生，本质是相邻的两段 <code>margin</code> 合并成一段</strong> —— 按「三种场景 → 取值规则 → 用 <code>BFC</code> 或 <code>padding</code> 隔开」讲。</p>\\n</section>\\n<p><strong>margin塌陷/margin重叠</strong></p>\\n<p><strong>标准文档流中，竖直方向的margin不叠加，只取较大的值作为margin</strong>(水平方向的<code>margin</code>是可以叠加的，即水平方向没有塌陷现象)。</p>\\n<blockquote>\\n<p>PS：如果不在标准流，比如盒子都浮动了，那么两个盒子之间是没有<code>margin</code>重叠的现象的。</p>\\n</blockquote>\\n<blockquote>\\n<p>我们来看几个例子。</p>\\n</blockquote>\\n<p><strong>兄弟元素之间</strong></p>\\n<p>如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/49.png\\" alt=\\"\\"></p>\\n<p><strong>子元素和父元素之间</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n&lt;head&gt;\\n    &lt;meta charset=&quot;UTF-8&quot;&gt;\\n    &lt;title&gt;Document&lt;/title&gt;\\n    &lt;style&gt;\\n\\n        * {\\n            margin: 0;\\n            padding: 0;\\n        }\\n\\n        .father {\\n            background: green;\\n\\n        }\\n\\n        /* 给儿子设置margin-top为10像素 */\\n        .son {\\n            height: 100px;\\n            margin-top: 10px;\\n            background: red;\\n        }\\n\\n    &lt;/style&gt;\\n&lt;/head&gt;\\n&lt;body&gt;\\n&lt;div class=&quot;father&quot;&gt;\\n    &lt;div class=&quot;son&quot;&gt;&lt;/div&gt;\\n&lt;/div&gt;\\n&lt;/body&gt;\\n&lt;/html&gt;\\n\\n</code></pre>\\n<blockquote>\\n<p>上面的代码中，儿子的<code>height</code>是 <code>100p</code>x，<code>magin-top</code> 是<code>10px</code>。注意，此时父亲的 <code>height</code> 是<code>100</code>，而不是<code>110</code>。因为儿子和父亲在竖直方向上，共一个<code>margin</code>。</p>\\n</blockquote>\\n<p>儿子这个盒子：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/50.png\\" alt=\\"\\"></p>\\n<p>父亲这个盒子：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/51.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>上方代码中，如果我们给父亲设置一个属性：<code>overflow: hidden</code>，就可以避免这个问题，此时父亲的高度是110px，这个用到的就是BFC（下一段讲解）。</p>\\n</blockquote>\\n<p><strong>善于使用父亲的padding，而不是儿子的margin</strong></p>\\n<blockquote>\\n<p>其实，这一小段讲的内容与上一小段相同，都是讲父子之间的margin重叠。</p>\\n</blockquote>\\n<p>我们来看一个奇怪的现象。现在有下面这样一个结构：（<code>div</code>中放一个<code>p</code>）</p>\\n<pre class=\\"language-html\\"><code>\\t&lt;div&gt;\\n\\t\\t&lt;p&gt;&lt;/p&gt;\\n\\t&lt;/div&gt;\\n</code></pre>\\n<blockquote>\\n<p>上面的结构中，我们尝试通过给儿子<code>p</code>一个<code>margin-top:50px;</code>的属性，让其与父亲保持50px的上边距。结果却看到了下面的奇怪的现象：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/52.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>此时我们给父亲<code>div</code>加一个<code>border</code>属性，就正常了：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/53.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>如果父亲没有<code>border</code>，那么儿子的<code>margin</code>实际上踹的是“流”，踹的是这“行”。所以，父亲整体也掉下来了。</p>\\n</blockquote>\\n<p><strong>margin这个属性，本质上描述的是兄弟和兄弟之间的距离； 最好不要用这个marign表达父子之间的距离。</strong></p>\\n<blockquote>\\n<p>所以，如果要表达父子之间的距离，我们一定要善于使用父亲的padding，而不是儿子的`margin。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>父元素没设高，子元素加了 <code>margin-top: 50px</code>，结果整个父元素往下跑了 50px，怎么修？</strong></p>\\n<p>这是父子塌陷。最干净的是给父元素 <code>display: flow-root</code>，它的语义就是建立块格式化上下文，没有副作用；加 <code>padding-top</code> 或 <code>border-top</code> 也行，但会改变视觉。别用 <code>overflow: hidden</code> —— 它会顺手裁掉子元素的阴影和弹层。</p>\\n</li>\\n<li>\\n<p><strong>两个 <code>flex</code> 子项之间的 <code>margin</code> 为什么不合并？</strong></p>\\n<p>外边距重叠是块格式化上下文里的规则，<code>flex</code> 容器建立的是弹性格式化上下文，子项的 <code>margin</code> 不参与合并。所以 <code>flex</code> 布局里的间距是可预期的，直接用 <code>gap</code> 比给每个子项加 <code>margin</code> 更好维护。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_4-bfc-是什么-怎么触发-能解决什么问题\\"><a class=\\"header-anchor\\" href=\\"#_4-bfc-是什么-怎么触发-能解决什么问题\\">#</a> 4 BFC 是什么，怎么触发，能解决什么问题</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>BFC</code> 是块级格式化上下文，一块独立的布局区域，内部布局不影响外部</li>\\n<li>三个作用：包含内部浮动（清浮动）、阻止外部浮动侵入（做自适应两栏）、阻止父子外边距合并</li>\\n<li>触发方式：<code>display: flow-root</code>、<code>overflow</code> 不为 <code>visible</code>、浮动、绝对定位、<code>inline-block</code>、<code>flex</code> / <code>grid</code> 子项、表格单元格</li>\\n<li>首选 <code>display: flow-root</code>，语义就是「建立新的块格式化上下文」，且没有副作用</li>\\n<li><code>overflow: hidden</code> 虽然也能触发，但会裁掉阴影、下拉菜单和溢出内容，属于历史写法</li>\\n<li>也要说清它解决不了什么：不影响行内布局，也不改变水平方向 <code>margin</code> 的行为</li>\\n</ul>\\n<p><strong><code>BFC</code> 是一块「自扫门前雪」的独立布局区域，价值在于把浮动、高度计算和相邻区域隔离开</strong> —— 按「定义 → 三个作用 → <code>flow-root</code> 是首选触发方式」讲。</p>\\n</section>\\n<p><strong>BFC（边距重叠解决方案）</strong></p>\\n<blockquote>\\n<p><code>BFC（Block Formatting Context）</code>：块级格式化上下文。你可以把它理解成一个独立的区域。</p>\\n</blockquote>\\n<p>另外还有个概念叫<code>IFC</code>。不过，<code>BFC</code>问得更多。</p>\\n<p><strong>BFC 的原理/BFC的布局规则【非常重要】</strong></p>\\n<blockquote>\\n<p><code>BFC</code> 的原理，其实也就是 <code>BFC</code> 的渲染规则（能说出以下四点就够了）。包括：</p>\\n</blockquote>\\n<ol>\\n<li>BFC <strong>内部的</strong>子元素，在垂直方向，<strong>边距会发生重叠</strong>。</li>\\n<li>BFC在页面中是独立的容器，外面的元素不会影响里面的元素，反之亦然。（稍后看<code>举例1</code>）</li>\\n<li><strong>BFC区域不与旁边的<code>float box</code>区域重叠</strong>。（可以用来清除浮动带来的影响）。（稍后看<code>举例2</code>）</li>\\n<li>计算<code>BFC</code>的高度时，浮动的子元素也参与计算。（稍后看<code>举例3</code>）</li>\\n</ol>\\n<p><strong>如何生成BFC</strong></p>\\n<blockquote>\\n<p>有以下几种方法：</p>\\n</blockquote>\\n<ul>\\n<li>方法1：<code>overflow</code>: 不为<code>visible</code>，可以让属性是 <code>hidden</code>、<code>auto</code>。【最常用】</li>\\n<li>方法2：浮动中：<code>float</code>的属性值不为<code>none</code>。意思是，只要设置了浮动，当前元素就创建了<code>BFC</code>。</li>\\n<li>方法3：定位中：只要<code>posiiton</code>的值不是 s<code>tatic</code>或者是<code>relative</code>即可，可以是<code>absolute</code>或<code>fixed</code>，也就生成了一个<code>BFC</code>。</li>\\n<li>方法4：<code>display</code>为<code>inline-block</code>, <code>table-cell</code>, <code>table-caption</code>, <code>flex</code>, <code>inline-flex</code></li>\\n</ul>\\n<p><strong>BFC 的应用</strong></p>\\n<p>**举例1：**解决 margin 重叠</p>\\n<blockquote>\\n<p>当父元素和子元素发生 <code>margin</code> 重叠时，解决办法：<strong>给子元素或父元素创建BFC</strong>。</p>\\n</blockquote>\\n<p>比如说，针对下面这样一个 <code>div</code> 结构：</p>\\n<pre class=\\"language-html\\"><code>&lt;div class=&quot;father&quot;&gt;\\n    &lt;p class=&quot;son&quot;&gt;\\n    &lt;/p&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<blockquote>\\n<p>上面的<code>div</code>结构中，如果父元素和子元素发生<code>margin</code>重叠，我们可以给子元素创建一个 <code>BFC</code>，就解决了：</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;div class=&quot;father&quot;&gt;\\n    &lt;p class=&quot;son&quot; style=&quot;overflow: hidden&quot;&gt;\\n    &lt;/p&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<blockquote>\\n<p>因为<strong>第二条：BFC区域是一个独立的区域，不会影响外面的元素</strong>。</p>\\n</blockquote>\\n<p><strong>举例2</strong>：BFC区域不与float区域重叠：</p>\\n<p>针对下面这样一个div结构；</p>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n&lt;head&gt;\\n    &lt;meta charset=&quot;UTF-8&quot;&gt;\\n    &lt;title&gt;Document&lt;/title&gt;\\n    &lt;style&gt;\\n\\n        .father-layout {\\n            background: pink;\\n        }\\n\\n        .father-layout .left {\\n            float: left;\\n            width: 100px;\\n            height: 100px;\\n            background: green;\\n        }\\n\\n        .father-layout .right {\\n            height: 150px;  /*右侧标准流里的元素，比左侧浮动的元素要高*/\\n            background: red;\\n        }\\n\\n    &lt;/style&gt;\\n&lt;/head&gt;\\n&lt;body&gt;\\n\\n&lt;section class=&quot;father-layout&quot;&gt;\\n    &lt;div class=&quot;left&quot;&gt;\\n        左侧，生命壹号\\n    &lt;/div&gt;\\n    &lt;div class=&quot;right&quot;&gt;\\n        右侧，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，\\n    &lt;/div&gt;\\n&lt;/section&gt;\\n\\n&lt;/body&gt;\\n&lt;/html&gt;\\n</code></pre>\\n<p>效果如下：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/54.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>上图中，由于右侧标准流里的元素，比左侧浮动的元素要高，导致右侧有一部分会跑到左边的下面去。</p>\\n</blockquote>\\n<p><strong>如果要解决这个问题，可以将右侧的元素创建BFC</strong>，因为<strong>第三条：BFC区域不与<code>float box</code>区域重叠</strong>。解决办法如下：（将right区域添加overflow属性）</p>\\n<pre class=\\"language-html\\"><code>    &lt;div class=&quot;right&quot; style=&quot;overflow: hidden&quot;&gt;\\n        右侧，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，smyhvae，\\n    &lt;/div&gt;\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/55.png\\" alt=\\"\\"></p>\\n<p>上图表明，解决之后，<code>father-layout</code>的背景色显现出来了，说明问题解决了。</p>\\n<p>**举例3：**清除浮动</p>\\n<p>现在有下面这样的结构：</p>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n&lt;head&gt;\\n    &lt;meta charset=&quot;UTF-8&quot;&gt;\\n    &lt;title&gt;Document&lt;/title&gt;\\n    &lt;style&gt;\\n\\n        .father {\\n            background: pink;\\n        }\\n\\n        .son {\\n            float: left;\\n            background: green;\\n        }\\n\\n    &lt;/style&gt;\\n&lt;/head&gt;\\n&lt;body&gt;\\n\\n&lt;section class=&quot;father&quot;&gt;\\n    &lt;div class=&quot;son&quot;&gt;\\n        生命壹号\\n    &lt;/div&gt;\\n\\n&lt;/section&gt;\\n&lt;/body&gt;\\n&lt;/html&gt;\\n</code></pre>\\n<p>效果如下：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/56.png\\" alt=\\"\\"></p>\\n<p>上面的代码中，儿子浮动了，但由于父亲没有设置高度，导致看不到父亲的背景色（此时父亲的高度为0）。正所谓<strong>有高度的盒子，才能关住浮动</strong>。</p>\\n<blockquote>\\n<p>如果想要清除浮动带来的影响，方法一是给父亲设置高度，然后采用隔墙法。方法二是 BFC：给父亲增加 <code>overflow=hidden</code>属性即可， 增加之后，效果如下：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/57.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>为什么父元素成为BFC之后，就有了高度呢？这就回到了<strong>第四条：计算BFC的高度时，浮动元素也参与计算</strong>。意思是，<strong>在计算BFC的高度时，子元素的float box也会参与计算</strong></p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>父元素只有浮动子元素、高度塌成 0，你有几种修法，选哪个？</strong></p>\\n<p>常见三种：<code>display: flow-root</code>、伪元素 <code>::after { content:\'\'; display:block; clear:both }</code>、<code>overflow: hidden</code>。选第一种，一行搞定且没副作用；伪元素清浮动是兼容老浏览器时的写法；<code>overflow: hidden</code> 不推荐，裁剪问题太多。</p>\\n</li>\\n<li>\\n<p><strong>左侧固定宽度浮动、右侧自适应，右侧文字为什么会绕到左侧下面？</strong></p>\\n<p>普通块盒会与浮动元素发生环绕。让右侧建立 <code>BFC</code>（<code>display: flow-root</code> 或 <code>overflow: auto</code>）它就会避开浮动边界。不过新项目直接用 <code>flex</code> 更合适，结构意图清楚，也不用惦记清浮动。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"六、页面布局\\"><a class=\\"header-anchor\\" href=\\"#六、页面布局\\">#</a> 六、页面布局</h2>\\n<h3 id=\\"_1-三栏布局有哪几种写法-左右定宽、中间自适应\\"><a class=\\"header-anchor\\" href=\\"#_1-三栏布局有哪几种写法-左右定宽、中间自适应\\">#</a> 1 三栏布局有哪几种写法（左右定宽、中间自适应）</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>五种写法要能一口气说全：浮动、绝对定位、<code>flex</code>、<code>table</code>、<code>grid</code></li>\\n<li>浮动版的 <code>DOM</code> 顺序必须是「左、右、中」，中间栏靠 <code>margin</code> 让开两侧，最后还得清浮动</li>\\n<li>绝对定位版三栏全部脱离文档流，父容器高度塌陷，只适合高度真的固定的场景</li>\\n<li><code>flex</code> 是现代默认解：<code>.center { flex: 1 }</code> 吃掉剩余空间，天然等高，记得补 <code>min-width: 0</code> 防止被内容撑破</li>\\n<li><code>grid</code> 一行 <code>grid-template-columns: 300px 1fr 300px</code> 就够了，二维布局无可替代</li>\\n<li>面试官必追一句「高度不确定怎么办」：浮动和绝对定位都会垮，<code>flex</code> / <code>table</code> / <code>grid</code> 天然等高</li>\\n</ul>\\n<p><strong>五种写法都要答得出，但选型只有一句：一维用 <code>flex</code>，二维用 <code>grid</code>，其余是兼容老浏览器的历史方案</strong> —— 按「五种写法 → 各自代价 → 高度未知时谁还活着」讲。</p>\\n</section>\\n<blockquote>\\n<p>问题：假设高度默认<code>100px</code> ，请写出三栏布局，其中左栏、右栏各为<code>300px</code>，中间自适应。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/25.png\\" alt=\\"\\"></p>\\n<p>分析：</p>\\n<p>初学者想到的答案有两种：</p>\\n<ul>\\n<li>方法1：浮动</li>\\n<li>方法2：绝对定位</li>\\n</ul>\\n<blockquote>\\n<p>但要求你能至少写出三四种方法，才算及格。剩下的方法如下：</p>\\n</blockquote>\\n<ul>\\n<li>方法3：<code>flexbox</code>。移动开发里经常用到。</li>\\n<li>方法4：表格布局<code>table</code>。虽然已经淘汰了，但也应该了解。</li>\\n<li>方法5：网格布局 <code>grid</code></li>\\n</ul>\\n<p><strong>方法1、浮动：</strong></p>\\n<blockquote>\\n<p>左侧设置左浮动，右侧设置右浮动即可，中间会自动地自适应。</p>\\n</blockquote>\\n<p><strong>方法2、绝对定位：</strong></p>\\n<blockquote>\\n<p>左侧设置为绝对定位， <code>left：0px</code>。右侧设置为绝对定位， <code>right：0px</code>。中间设置为绝对定位，<code>left</code>和<code>right</code> 都为<code>300px</code>，即可。中间的宽度会自适应。</p>\\n</blockquote>\\n<blockquote>\\n<p>使用<code>article</code>标签作为容器，包裹左、中、右三个部分。</p>\\n</blockquote>\\n<blockquote>\\n<p>方法1 和方法2 的代码如下：</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n\\n&lt;head&gt;\\n    &lt;meta charset=&quot;UTF-8&quot;&gt;\\n    &lt;title&gt;Document&lt;/title&gt;\\n    &lt;style&gt;\\n        html * {\\n            padding: 0px;\\n            margin: 0px;\\n        }\\n\\n        .layout {\\n            margin-bottom: 150px;\\n        }\\n\\n\\n        .layout article div { /*注意，这里是设置每个小块儿的高度为100px，而不是设置大容器的高度。大容器的高度要符合响应式*/\\n            height: 100px;\\n        }\\n\\n        /* 方法一 start */\\n\\n        .layout.float .left {\\n            float: left;\\n            width: 300px;\\n            background: red;\\n        }\\n\\n        .layout.float .right {\\n            float: right;\\n            width: 300px;\\n            background: blue;\\n        }\\n\\n        .layout.float .center {\\n            background: green;\\n\\n        }\\n\\n        /* 方法一 end */\\n\\n\\n        /* 方法二 start */\\n        .layout.absolute .left-center-right {\\n            position: relative;\\n        }\\n\\n        .layout.absolute .left {\\n            position: absolute;\\n            left: 0;\\n            width: 300px;\\n            background: red;\\n        }\\n\\n        /* 【重要】中间的区域，左侧定位300px，右侧定位为300px，即可完成。宽度会自使用 */\\n        .layout.absolute .center {\\n            position: absolute;\\n            left: 300px;\\n            right: 300px;\\n            background: green;\\n        }\\n\\n        .layout.absolute .right {\\n            position: absolute;\\n            right: 0;\\n            width: 300px;\\n            background: blue;\\n        }\\n\\n\\n        /* 方法二 end */\\n    &lt;/style&gt;\\n&lt;/head&gt;\\n\\n&lt;body&gt;\\n\\n    &lt;!-- 方法一：浮动 start --&gt;\\n    &lt;!-- 输入 section.layout.float，即可生成  --&gt;\\n    &lt;section class=&quot;layout float&quot;&gt;\\n        &lt;!-- 用  article 标签包裹左、中、右三个部分 --&gt;\\n        &lt;article class=&quot;left-right-center&quot;&gt;\\n            &lt;!-- 输入 div.left+div.right+div.center，即可生成 --&gt;\\n            &lt;div class=&quot;left&quot;&gt;\\n                我是 left\\n            &lt;/div&gt;\\n            &lt;div class=&quot;right&quot;&gt;\\n                我是 right\\n            &lt;/div&gt;\\n            &lt;div class=&quot;center&quot;&gt;\\n                浮动解决方案\\n                我是 center\\n            &lt;/div&gt;\\n\\n        &lt;/article&gt;\\n\\n    &lt;/section&gt;\\n    &lt;!-- 方法一：浮动 end --&gt;\\n\\n    &lt;section class=&quot;layout absolute&quot;&gt;\\n        &lt;article class=&quot;left-center-right&quot;&gt;\\n            &lt;div class=&quot;left&quot;&gt;\\n                我是 left\\n            &lt;/div&gt;\\n            &lt;div class=&quot;right&quot;&gt;\\n                我是 right\\n            &lt;/div&gt;\\n            &lt;div class=&quot;center&quot;&gt;\\n                &lt;h1&gt;绝对定位解决方案&lt;/h1&gt;\\n                我是 center\\n            &lt;/div&gt;\\n        &lt;/article&gt;\\n    &lt;/section&gt;\\n&lt;/body&gt;\\n&lt;/html&gt;\\n\\n</code></pre>\\n<p>效果如下：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/26.gif\\" alt=\\"\\"></p>\\n<p><strong>方法3、flexbox布局</strong></p>\\n<blockquote>\\n<p>将左中右所在的容器设置为<code>display: flex</code>，设置两侧的宽度后，然后让中间的<code>flex = 1</code>，即可。</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n\\n&lt;head&gt;\\n    &lt;meta charset=&quot;UTF-8&quot;&gt;\\n    &lt;title&gt;Document&lt;/title&gt;\\n    &lt;style&gt;\\n        html * {\\n            padding: 0;\\n            margin: 0;\\n        }\\n\\n        .layout article div {\\n            height: 100px;\\n        }\\n\\n        .left-center-right {\\n            display: flex;\\n        }\\n\\n        .layout.flex .left {\\n            width: 300px;\\n            background: red;\\n        }\\n\\n        .layout.flex .center {\\n            flex: 1;\\n            background: green;\\n        }\\n\\n        .layout.flex .right {\\n            width: 300px;\\n            background: blue;\\n        }\\n    &lt;/style&gt;\\n\\n&lt;/head&gt;\\n\\n&lt;body&gt;\\n    &lt;section class=&quot;layout flex&quot;&gt;\\n        &lt;article class=&quot;left-center-right-&quot;&gt;\\n            &lt;div class=&quot;left&quot;&gt;\\n                我是 left\\n            &lt;/div&gt;\\n            &lt;div class=&quot;center&quot;&gt;\\n                &lt;h1&gt;flex布局解决方案&lt;/h1&gt;\\n                我是 center\\n            &lt;/div&gt;\\n            &lt;div class=&quot;right&quot;&gt;\\n                我是 right\\n            &lt;/div&gt;\\n\\n        &lt;/article&gt;\\n    &lt;/section&gt;\\n\\n&lt;/body&gt;\\n\\n&lt;/html&gt;\\n\\n\\n</code></pre>\\n<p>效果如下：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/27.gif\\" alt=\\"\\"></p>\\n<p><strong>方法4、表格布局 table</strong></p>\\n<blockquote>\\n<p>设置整个容器的宽度为<code>100%</code>，设置三个部分均为表格，然后左边的单元格为 <code>300px</code>，右边的单元格为 <code>300px</code>，即可。中间的单元格会自适应。</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n\\n&lt;head&gt;\\n    &lt;meta charset=&quot;UTF-8&quot;&gt;\\n    &lt;title&gt;Document&lt;/title&gt;\\n    &lt;style&gt;\\n        html * {\\n            padding: 0;\\n            margin: 0;\\n        }\\n\\n        .layout.table div {\\n            height: 100px;\\n        }\\n\\n        /* 重要：设置容器为表格布局，宽度为100% */\\n        .layout.table .left-center-right {\\n            width: 100%;\\n            display: table;\\n            height: 100px;\\n\\n        }\\n\\n        .layout.table .left-center-right div {\\n            display: table-cell; /* 重要：设置三个模块为表格里的单元*/\\n        }\\n\\n        .layout.table .left {\\n            width: 300px;\\n            background: red;\\n        }\\n\\n        .layout.table .center {\\n            background: green;\\n        }\\n\\n        .layout.table .right {\\n            width: 300px;\\n            background: blue;\\n        }\\n    &lt;/style&gt;\\n\\n&lt;/head&gt;\\n\\n&lt;body&gt;\\n    &lt;section class=&quot;layout table&quot;&gt;\\n        &lt;article class=&quot;left-center-right&quot;&gt;\\n            &lt;div class=&quot;left&quot;&gt;\\n                我是 left\\n            &lt;/div&gt;\\n            &lt;div class=&quot;center&quot;&gt;\\n                &lt;h1&gt;表格布局解决方案&lt;/h1&gt;\\n                我是 center\\n            &lt;/div&gt;\\n            &lt;div class=&quot;right&quot;&gt;\\n                我是 right\\n            &lt;/div&gt;\\n\\n        &lt;/article&gt;\\n    &lt;/section&gt;\\n\\n&lt;/body&gt;\\n\\n&lt;/html&gt;\\n\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/28.gif\\" alt=\\"\\"></p>\\n<p><strong>方法5、网格布局 grid</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n\\n&lt;head&gt;\\n    &lt;meta charset=&quot;UTF-8&quot;&gt;\\n    &lt;title&gt;Document&lt;/title&gt;\\n    &lt;style&gt;\\n        html * {\\n            padding: 0;\\n            margin: 0;\\n        }\\n\\n        /* 重要：设置容器为网格布局，宽度为100% */\\n        .layout.grid .left-center-right {\\n            display: grid;\\n            width: 100%;\\n            grid-template-rows: 100px;\\n            grid-template-columns: 300px auto 300px;  /* 重要：设置网格为三列，并设置每列的宽度。即可。*/\\n\\n        }\\n\\n        .layout.grid .left {\\n            background: red;\\n        }\\n\\n        .layout.grid .center {\\n            background: green;\\n        }\\n\\n        .layout.grid .right {\\n            background: blue;\\n        }\\n    &lt;/style&gt;\\n\\n&lt;/head&gt;\\n\\n&lt;body&gt;\\n    &lt;section class=&quot;layout grid&quot;&gt;\\n        &lt;article class=&quot;left-center-right&quot;&gt;\\n            &lt;div class=&quot;left&quot;&gt;\\n                我是 left\\n            &lt;/div&gt;\\n            &lt;div class=&quot;center&quot;&gt;\\n                &lt;h1&gt;网格布局解决方案&lt;/h1&gt;\\n                我是 center\\n            &lt;/div&gt;\\n            &lt;div class=&quot;right&quot;&gt;\\n                我是 right\\n            &lt;/div&gt;\\n\\n        &lt;/article&gt;\\n    &lt;/section&gt;\\n\\n&lt;/body&gt;\\n\\n&lt;/html&gt;\\n</code></pre>\\n<p>效果：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/29.gif\\" alt=\\"\\"></p>\\n<p><strong>延伸：五种方法的对比</strong></p>\\n<blockquote>\\n<p>五种方法的优缺点</p>\\n</blockquote>\\n<ul>\\n<li>考虑中间模块的高度问题</li>\\n<li>兼容性问题：实际开发中，哪个最实用？</li>\\n</ul>\\n<p>方法1：浮动：</p>\\n<ul>\\n<li>优点：兼容性好。</li>\\n<li>缺点：浮动会脱离标准文档流，因此要清除浮动。我们解决好这个问题即可。</li>\\n</ul>\\n<p>方法:2：绝对定位</p>\\n<ul>\\n<li>优点：快捷。</li>\\n<li>缺点：导致子元素也脱离了标准文档流，可实用性差。</li>\\n</ul>\\n<p>方法3：flex 布局（CSS3中出现的）</p>\\n<ul>\\n<li>优点：解决上面两个方法的不足，flex布局比较完美。移动端基本用 flex布局。</li>\\n</ul>\\n<p>方法4：表格布局</p>\\n<ul>\\n<li>优点：表格布局在很多场景中很实用，兼容性非常好。因为IE8不支持 flex，此时可以尝试表格布局</li>\\n<li>缺点：因为三个部分都当成了<strong>单元格</strong>来对待，此时，如果中间的部分变高了，其会部分也会被迫调整高度。但是，在很多场景下，我们并不需要两侧的高度增高。</li>\\n</ul>\\n<blockquote>\\n<p>什么时候用 <code>flex</code>布局 or 表格布局，看具体的场景。二者没有绝对的优势，也没有绝对的不足。</p>\\n</blockquote>\\n<p>方法5：网格布局</p>\\n<ul>\\n<li>CSS3中引入的布局，很好用。代码量简化了很多。</li>\\n</ul>\\n<blockquote>\\n<p>PS：面试提到网格布局，说明我们对新技术是有追求的。</p>\\n</blockquote>\\n<p><strong>延伸：如果题目中去掉高度已知</strong></p>\\n<blockquote>\\n<p>问题：题目中，如果去掉高度已知，我们往中间的模块里塞很多内容，让中间的模块撑开。会发生什么变化？哪个布局就不能用了？</p>\\n</blockquote>\\n<p>分析：其实可以这样理解，我们回去看上面的动画效果，当中间的模块变得很挤时，会发生什么效果？就是我们想要的答案。</p>\\n<blockquote>\\n<p>答案是：<strong>flex 布局和表格布局可以通用</strong>，其他三个布局都不能用了。</p>\\n</blockquote>\\n<p><strong>总结</strong></p>\\n<blockquote>\\n<p>涉及到的知识点：</p>\\n</blockquote>\\n<ul>\\n<li>语义化掌握到位：每个区域用<code>section</code>、<code>article</code>代表容器、<code>div</code>代表块儿。如果通篇都用 div，那就是语义化没掌握好。</li>\\n<li>页面布局理解深刻。</li>\\n<li><code>CSS</code>基础知识扎实。</li>\\n<li>思维灵活且积极上进。题目中可以通过<code>网格布局</code>来体现。</li>\\n<li>代码书写规范。注意命名。上面的代码中，没有一行代码是多的。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>面试官接着问「圣杯布局和双飞翼布局有什么区别」，你怎么答？</strong></p>\\n<p>两者都是在浮动方案上解决「中间栏优先渲染」的问题：<code>DOM</code> 写成中、左、右，中间栏 <code>width: 100%</code>，再用负 <code>margin</code> 把左右栏拉到两侧。区别在中间栏怎么避开两侧 —— 圣杯给父容器加 <code>padding</code> 再用相对定位把左右栏推出去，双飞翼在中间栏里再套一层 <code>div</code> 用 <code>margin</code> 让开。有了 <code>flex</code> 和 <code>grid</code> 之后，这两种写法基本只剩面试价值。</p>\\n</li>\\n<li>\\n<p><strong>中间栏放了一个超长英文单词或一张大图，把布局撑破了，怎么修？</strong></p>\\n<p><code>flex</code> 子项默认 <code>min-width: auto</code>，不会小于内容宽度，所以要显式写 <code>min-width: 0</code>（<code>grid</code> 里同理）。再配 <code>overflow: hidden</code> 或 <code>word-break</code> 处理长文本，图片加 <code>max-width: 100%</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"七、安全问题-csrf和xss\\"><a class=\\"header-anchor\\" href=\\"#七、安全问题-csrf和xss\\">#</a> 七、安全问题：CSRF和XSS</h2>\\n<h3 id=\\"_1-前言-2\\"><a class=\\"header-anchor\\" href=\\"#_1-前言-2\\">#</a> 1 前言</h3>\\n<blockquote>\\n<p>面试中的安全问题，明确来说，就两个方面：</p>\\n</blockquote>\\n<ul>\\n<li><code>CSRF</code>：基本概念、攻击方式、防御措施</li>\\n<li><code>XSS</code>：基本概念、攻击方式、防御措施</li>\\n</ul>\\n<blockquote>\\n<p>这两个问题，一般不会问太难。</p>\\n</blockquote>\\n<blockquote>\\n<p>有人问：<code>SQL</code>注入算吗？答案：这个其实跟前端的关系不是很大。</p>\\n</blockquote>\\n<h3 id=\\"_2-csrf\\"><a class=\\"header-anchor\\" href=\\"#_2-csrf\\">#</a> 2 CSRF</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>CSRF</code>（<code>Cross-site request forgery</code>）跨站请求伪造：诱导已登录用户的浏览器向目标站点发出写请求</li>\\n<li>利用的是 cookie 会自动携带这一机制，所以受害者必须处于登录态</li>\\n<li>关键特征是「发得出请求，但读不到响应」，因此只对有副作用的接口有威胁</li>\\n<li>防御按优先级：写操作不用 <code>GET</code> → 一次性 <code>CSRF Token</code>（攻击页面受同源策略限制读不到）→ <code>SameSite=Lax</code> 或 <code>Strict</code> → 校验 <code>Origin</code> / <code>Referer</code></li>\\n<li><code>SameSite=Lax</code> 已是现代浏览器的默认值，挡掉了大部分场景，但不能只靠它（老浏览器、必须用 <code>None</code> 的三方场景）</li>\\n<li>验证码和二次密码只适合转账这类高危操作，不能当通用防御</li>\\n</ul>\\n<p><strong><code>CSRF</code> 是借你的身份发请求，攻击者全程读不到你的数据</strong> —— 按「原理（cookie 自动带）→ 三种攻击载体 → 四道防线的优先级」讲。</p>\\n</section>\\n<blockquote>\\n<p>问的不难，一般问：</p>\\n</blockquote>\\n<ul>\\n<li><code>CSRF</code>的基本概念、缩写、全称</li>\\n<li>攻击原理</li>\\n<li>防御措施</li>\\n</ul>\\n<blockquote>\\n<p>如果把<strong>攻击原理</strong>和<strong>防御措施</strong>掌握好，基本没什么问题。</p>\\n</blockquote>\\n<h4 id=\\"_2-1-csrf的基本概念、缩写、全称\\"><a class=\\"header-anchor\\" href=\\"#_2-1-csrf的基本概念、缩写、全称\\">#</a> 2.1 CSRF的基本概念、缩写、全称</h4>\\n<blockquote>\\n<p><code>CSRF</code>（<code>Cross-site request forgery</code>）：<strong>跨站请求伪造</strong>。</p>\\n</blockquote>\\n<p>PS：中文名一定要记住。英文全称，如果记不住也拉倒。</p>\\n<h4 id=\\"_2-2-csrf的攻击原理\\"><a class=\\"header-anchor\\" href=\\"#_2-2-csrf的攻击原理\\">#</a> 2.2 CSRF的攻击原理</h4>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/17.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>用户是网站A的注册用户，且登录进去，于是网站A就给用户下发<code>cookie</code>。</p>\\n</blockquote>\\n<blockquote>\\n<p>从上图可以看出，要完成一次<code>CSRF</code>攻击，受害者必须满足两个必要的条件：</p>\\n</blockquote>\\n<ol>\\n<li>登录受信任网站<code>A</code>，并在本地生成<code>Cookie</code>。（如果用户没有登录网站<code>A</code>，那么网站<code>B</code>在诱导的时候，请求网站<code>A</code>的<code>api</code>接口时，会提示你登录）</li>\\n<li>在不登出<code>A</code>的情况下，访问危险网站<code>B</code>（其实是利用了网站<code>A</code>的漏洞）。</li>\\n</ol>\\n<blockquote>\\n<p>我们在讲<code>CSRF</code>时，一定要把上面的两点说清楚。</p>\\n</blockquote>\\n<blockquote>\\n<p>温馨提示一下，<code>cookie</code>保证了用户可以处于登录状态，但网站<code>B</code>其实拿不到 <code>cookie</code>。</p>\\n</blockquote>\\n<blockquote>\\n<p>举个例子，前段时间里，微博网站有个<code>api</code>接口有漏洞，导致很多用户的粉丝暴增。</p>\\n</blockquote>\\n<h4 id=\\"_2-3-csrf如何防御\\"><a class=\\"header-anchor\\" href=\\"#_2-3-csrf如何防御\\">#</a> 2.3 CSRF如何防御</h4>\\n<p><strong>方法一、Token 验证：</strong>（用的最多）</p>\\n<ol>\\n<li>服务器发送给客户端一个<code>token</code>；</li>\\n<li>客户端提交的表单中带着这个<code>token</code>。</li>\\n<li>如果这个 <code>token</code> 不合法，那么服务器拒绝这个请求。</li>\\n</ol>\\n<p><strong>方法二：隐藏令牌：</strong></p>\\n<ul>\\n<li>把 <code>token</code> 隐藏在 <code>http</code> 的 <code>head</code>头中。</li>\\n</ul>\\n<blockquote>\\n<p>方法二和方法一有点像，本质上没有太大区别，只是使用方式上有区别。</p>\\n</blockquote>\\n<p><strong>方法三、Referer 验证：</strong></p>\\n<blockquote>\\n<p><code>Referer</code> 指的是页面请求来源。意思是，<strong>只接受本站的请求，服务器才做响应</strong>；如果不是，就拦截。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>接口从 <code>GET</code> 改成 <code>POST</code> 就安全了吗？</strong></p>\\n<p>不安全。攻击者可以在自己页面上放一个隐藏表单自动提交，跨站表单 <code>POST</code> 照样带 cookie。改 <code>POST</code> 只是挡掉了 <code>&lt;img src&gt;</code> 这种最省事的载体，真正的防线是 <code>Token</code> 和 <code>SameSite</code>。</p>\\n</li>\\n<li>\\n<p><strong>站点同时存在 <code>XSS</code> 漏洞和完善的 <code>CSRF Token</code>，够安全吗？</strong></p>\\n<p>不够，而且 <code>Token</code> 会一起失效。有 <code>XSS</code> 就意味着攻击脚本运行在你的域下，能直接把页面里的 <code>Token</code> 读出来再发请求，所有 <code>CSRF</code> 防御一并被绕过。所以修复顺序永远是先堵 <code>XSS</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-xss\\"><a class=\\"header-anchor\\" href=\\"#_3-xss\\">#</a> 3 XSS</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>XSS</code>（<code>Cross-site scripting</code>）跨站脚本攻击：用户可控的内容被当成代码在你的域下执行</li>\\n<li>三种类型：存储型（存进数据库、影响所有访客，危害最大）、反射型（参数原样回显，靠诱导点链接）、<code>DOM</code> 型（前端自己把参数写进 <code>innerHTML</code>）</li>\\n<li>得手后能读 <code>document.cookie</code>、以用户身份发请求、篡改页面做钓鱼</li>\\n<li>防御主线是输出编码：默认用 <code>textContent</code> 或框架插值（<code>React</code> 的 <code>{}</code>、<code>Vue</code> 的 <code>{<span></span>{ }<span></span>}</code> 都自动转义）</li>\\n<li>必须渲染富文本时用白名单库净化（例如 <code>DOMPurify</code>），不要用黑名单过滤关键字</li>\\n<li>配套手段：<code>CSP</code> 限制脚本来源、cookie 加 <code>HttpOnly</code> 让脚本读不到会话、<code>URL</code> 进 <code>href</code> 前校验协议防 <code>javascript:</code></li>\\n</ul>\\n<p><strong><code>XSS</code> 的本质是「数据被当成代码执行」，防御的落点在输出而不是输入</strong> —— 按「三种类型 → 危害 → 输出编码为主、<code>CSP</code> 和 <code>HttpOnly</code> 兜底」讲。</p>\\n</section>\\n<h4 id=\\"_3-1-xss的基本概念\\"><a class=\\"header-anchor\\" href=\\"#_3-1-xss的基本概念\\">#</a> 3.1 XSS的基本概念</h4>\\n<blockquote>\\n<p><code>XSS（Cross Site Scripting）</code>：<strong>跨域脚本攻击</strong>。</p>\\n</blockquote>\\n<ul>\\n<li>接下来，我们详细讲一下 <code>XSS</code> 的内容。</li>\\n</ul>\\n<blockquote>\\n<p>预备知识：<code>HTTP</code>、<code>Cookie</code>、<code>Ajax</code>。</p>\\n</blockquote>\\n<h4 id=\\"_3-2-xss的攻击原理\\"><a class=\\"header-anchor\\" href=\\"#_3-2-xss的攻击原理\\">#</a> 3.2 XSS的攻击原理</h4>\\n<blockquote>\\n<p><code>XSS</code>攻击的核心原理是：不需要你做任何的登录认证，它会通过合法的操作（比如在<code>url</code>中输入、在评论框中输入），向你的页面注入脚本（可能是<code>js</code>、<code>hmtl</code>代码块等）。</p>\\n</blockquote>\\n<blockquote>\\n<p>最后导致的结果可能是：</p>\\n</blockquote>\\n<ul>\\n<li>盗用<code>Cookie</code></li>\\n<li>破坏页面的正常结构，插入广告等恶意内容</li>\\n<li><code>D-doss</code>攻击</li>\\n</ul>\\n<h4 id=\\"_3-3-xss的攻击方式\\"><a class=\\"header-anchor\\" href=\\"#_3-3-xss的攻击方式\\">#</a> 3.3 XSS的攻击方式</h4>\\n<ol>\\n<li>反射型</li>\\n</ol>\\n<blockquote>\\n<p>发出请求时，<code>XSS</code>代码出现在<code>url</code>中，作为输入提交到服务器端，服务器端解析后响应，<code>XSS</code>代码随响应内容一起传回给浏览器，最后浏览器解析执行<code>XSS</code>代码。这个过程像一次反射，所以叫反射型<code>XSS</code>。</p>\\n</blockquote>\\n<ol start=\\"2\\">\\n<li>存储型</li>\\n</ol>\\n<blockquote>\\n<p>存储型<code>XSS</code>和反射型<code>XSS</code>的差别在于，提交的代码会存储在服务器端（数据库、内存、文件系统等），下次请求时目标页面时不用再提交XSS代码。</p>\\n</blockquote>\\n<h4 id=\\"_3-4-xss的防范措施-encode-过滤\\"><a class=\\"header-anchor\\" href=\\"#_3-4-xss的防范措施-encode-过滤\\">#</a> 3.4 XSS的防范措施（encode + 过滤）</h4>\\n<p><strong>XSS的防范措施主要有三个：</strong></p>\\n<p><strong>1. 编码</strong>：</p>\\n<blockquote>\\n<p>对用户输入的数据进行<code>HTML Entity</code>编码。</p>\\n</blockquote>\\n<p>如上图所示，把字符转换成 转义字符。</p>\\n<blockquote>\\n<p><code>Encode</code>的作用是将<code>$var</code>等一些字符进行转化，使得浏览器在最终输出结果上是一样的。</p>\\n</blockquote>\\n<p>比如说这段代码：</p>\\n<pre class=\\"language-html\\"><code>&lt;script&gt;alert(1)&lt;/script&gt;\\n</code></pre>\\n<blockquote>\\n<p>若不进行任何处理，则浏览器会执行alert的js操作，实现XSS注入。</p>\\n</blockquote>\\n<blockquote>\\n<p>进行编码处理之后，L在浏览器中的显示结果就是<code>&lt;script&gt;alert(1)&lt;/script&gt;</code>，实现了将<code>$var</code>作为纯文本进行输出，且不引起J<code>avaScript</code>的执行。</p>\\n</blockquote>\\n<p><strong>2、过滤：</strong></p>\\n<ul>\\n<li>移除用户输入的和事件相关的属性。如<code>onerror</code>可以自动触发攻击，还有<code>onclick</code>等。（总而言是，过滤掉一些不安全的内容）</li>\\n<li>移除用户输入的<code>Style</code>节点、<code>Script</code>节点、<code>Iframe</code>节点。（尤其是<code>Script</code>节点，它可是支持跨域的呀，一定要移除）。</li>\\n</ul>\\n<p><strong>3、校正</strong></p>\\n<ul>\\n<li>避免直接对<code>HTML Entity</code>进行解码。</li>\\n<li>使用<code>DOM Parse</code>转换，校正不配对的<code>DOM</code>标签。</li>\\n</ul>\\n<blockquote>\\n<p>备注：我们应该去了解一下<code>DOM Parse</code>这个概念，它的作用是把文本解析成<code>DOM</code>结构。</p>\\n</blockquote>\\n<p>比较常用的做法是，通过第一步的编码转成文本，然后第三步转成<code>DOM</code>对象，然后经过第二步的过滤。</p>\\n<p><strong>还有一种简洁的答案：</strong></p>\\n<p>首先是encode，如果是富文本，就白名单。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>同事写了个过滤函数，把 <code>&lt;script&gt;</code> 关键字替换掉就上线了，你怎么说服他改？</strong></p>\\n<p>黑名单挡不住。<code>&lt;img src=x onerror=...&gt;</code>、<code>&lt;svg onload=...&gt;</code>、<code>javascript:</code> 伪协议都不带 <code>script</code> 字样；更讽刺的是，用 <code>innerHTML</code> 插入的 <code>&lt;script&gt;</code> 本来就不执行，他拦的恰恰是唯一无害的那个。正确做法是输出时统一转义，富文本走白名单净化。</p>\\n</li>\\n<li>\\n<p><strong><code>React</code> 项目里还会有 <code>XSS</code> 吗？</strong></p>\\n<p>会。<code>React</code> 的插值默认转义，但 <code>dangerouslySetInnerHTML</code>、把用户输入拼进 <code>href</code> / <code>src</code>、以及各种动态渲染 <code>HTML</code> 的第三方组件都会绕过它。审计时重点搜这几个点，再叠一层 <code>CSP</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_4-csrf-和-xss-的区别\\"><a class=\\"header-anchor\\" href=\\"#_4-csrf-和-xss-的区别\\">#</a> 4 CSRF 和 XSS 的区别</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>XSS</code> 是代码注入：攻击者的脚本跑在你的域下，能读能写；<code>CSRF</code> 是身份冒用：脚本跑在攻击者自己域下，只能写不能读</li>\\n<li><code>XSS</code> 不需要受害者处于登录态也有价值；<code>CSRF</code> 必须借用一个已登录的会话</li>\\n<li>防御方向不同：<code>XSS</code> 防输入输出（转义、白名单净化、<code>CSP</code>），<code>CSRF</code> 防请求来源与凭证（<code>Token</code>、<code>SameSite</code>、<code>Origin</code> 校验）</li>\\n<li>两者有依赖关系：站点存在 <code>XSS</code> 时，<code>CSRF Token</code> 会被脚本直接读走，所有 <code>CSRF</code> 防御失效</li>\\n<li>所以修复顺序是先堵 <code>XSS</code>；<code>HttpOnly</code> 也只能防脚本读 cookie，防不住脚本以用户身份直接发请求</li>\\n</ul>\\n<p><strong>一句话分清：<code>XSS</code> 是在你的地盘上执行代码，<code>CSRF</code> 是在别人的地盘上冒用你的身份</strong> —— 按「攻击位置 → 能读还是只能写 → 防御方向 → 谁优先修」讲。</p>\\n</section>\\n<blockquote>\\n<p>面试官还可能喜欢问二者的区别。</p>\\n</blockquote>\\n<p><strong>区别一：</strong></p>\\n<ul>\\n<li><code>CSRF</code>：需要用户先登录网站<code>A</code>，获取 <code>cookie</code></li>\\n<li><code>XSS</code>：不需要登录。</li>\\n</ul>\\n<p><strong>区别二：（原理的区别）</strong></p>\\n<ul>\\n<li><code>CSRF</code>：是利用网站<code>A</code>本身的漏洞，去请求网站<code>A</code>的<code>api</code>。</li>\\n<li><code>XSS</code>：是向网站 <code>A</code> 注入 <code>JS</code>代码，然后执行 <code>JS</code> 里的代码，篡改网站<code>A</code>的内容。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>面试官问「只做 <code>CSRF</code> 防护够不够」，你怎么答？</strong></p>\\n<p>不够，而且顺序反了。<code>CSRF</code> 防护建立在「页面里的 <code>Token</code> 攻击者读不到」这个前提上，而这个前提由同源策略保证；有 <code>XSS</code> 就等于同源策略被打穿，<code>Token</code> 唾手可得。所以加固要先做输出编码和 <code>CSP</code>，再谈 <code>CSRF</code>。</p>\\n</li>\\n<li>\\n<p><strong><code>HttpOnly</code> 的 cookie 能防住哪种攻击？</strong></p>\\n<p>只能防 <code>XSS</code> 脚本读取 cookie 内容。它防不住 <code>CSRF</code>（浏览器照样自动带上），也防不住 <code>XSS</code> 脚本直接以用户身份发请求。它是降低损失的手段，不是解决方案。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"八、跨域通信类\\"><a class=\\"header-anchor\\" href=\\"#八、跨域通信类\\">#</a> 八、跨域通信类</h2>\\n<h3 id=\\"_1-前言-3\\"><a class=\\"header-anchor\\" href=\\"#_1-前言-3\\">#</a> 1 前言</h3>\\n<p>从本章起，对代码的要求没之前那么高了，但是，要求你对知识面的掌握要足够宽。</p>\\n<p><strong>前端通信类的问题，主要包括以下内容</strong>：</p>\\n<ol>\\n<li>什么是<strong>同源策略</strong>及限制</li>\\n</ol>\\n<blockquote>\\n<p>同源策略是一个概念，就一句话。有什么限制，就三句话。能说出来即可。</p>\\n</blockquote>\\n<ol start=\\"2\\">\\n<li><strong>前后端如何通信</strong></li>\\n</ol>\\n<blockquote>\\n<p>如果你不准备，估计也就只能说出<code>ajax</code>。这个可以考察出知识面。</p>\\n</blockquote>\\n<ol start=\\"3\\">\\n<li>如何创建<strong>Ajax</strong></li>\\n</ol>\\n<blockquote>\\n<p><code>Ajax</code>在前后端通信中经常用到。做业务时，可以借助第三方的库，比如<code>vue</code>框架里的库、<code>jQuery</code>也有封装好的方法。但如果让你用原生的<code>js</code>去实现，该怎么做？</p>\\n</blockquote>\\n<p>这就是考察你的动手能力，以及框架原理的掌握。如果能写出来，可以体现出你的基本功。是加分项。</p>\\n<ol start=\\"4\\">\\n<li><strong>跨域通信</strong>的几种方式</li>\\n</ol>\\n<blockquote>\\n<p>这部分非常重要。无非就是问你：什么是跨域、跨域有什么限制、<strong>跨域有几种方式</strong>。</p>\\n</blockquote>\\n<p>下面分别讲解。</p>\\n<h3 id=\\"_2-同源策略的概念和具体限制\\"><a class=\\"header-anchor\\" href=\\"#_2-同源策略的概念和具体限制\\">#</a> 2 同源策略的概念和具体限制</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>同源 = 协议、域名（主机）、端口三者完全相同，路径和查询串不参与判定</li>\\n<li>限制三件事：拿不到对方的 <code>Cookie</code> / <code>LocalStorage</code> / <code>IndexedDB</code>、操作不了对方文档的 <code>DOM</code>、<code>Ajax</code> 拿不到响应</li>\\n<li>措辞要准确：跨域的 <code>Ajax</code> 请求其实发出去了，服务端也执行了，只是响应被浏览器拦住不给 <code>JS</code> 读</li>\\n<li><code>&lt;img&gt;</code> / <code>&lt;script&gt;</code> / <code>&lt;link&gt;</code> / <code>&lt;iframe&gt;</code> 加载资源不受同源策略限制，<code>JSONP</code> 就是钻这个空子</li>\\n<li>子域名不同（<code>www</code> 与 <code>api</code>）也算跨域；默认端口省略和显式写出算同一个源</li>\\n<li>它是浏览器的安全策略，服务端之间互相调用不存在跨域，所以代理能绕开</li>\\n</ul>\\n<p><strong>同源策略限制的是「读」，不是「发」</strong> —— 按「三要素判定 → 三类限制 → 标签加载不受限（<code>JSONP</code> 的由来）」讲。</p>\\n</section>\\n<blockquote>\\n<p><strong>同源策略</strong>：限制从一个源加载的文档或脚本如何与来自另一个源的资源进行交互。这是一个用于隔离潜在恶意文件的关键的安全机制。（来自MDN官方的解释）</p>\\n</blockquote>\\n<p><strong>具体解释：</strong></p>\\n<ol>\\n<li><code>源</code>包括三个部分：协议、域名、端口（<code>http</code>协议的默认端口是<code>80</code>）。如果有任何一个部分不同，则<code>源</code>不同，那就是跨域了。</li>\\n<li><code>限制</code>：这个源的文档没有权利去操作另一个源的文档。这个限制体现在：（要记住）</li>\\n</ol>\\n<ul>\\n<li><code>Cookie</code>、<code>LocalStorage</code>和<code>IndexDB</code>无法获取。</li>\\n<li>无法获取和操作<code>DOM</code>。</li>\\n<li>不能发送<code>Ajax</code>请求。我们要注意，<code>Ajax</code>只适合<strong>同源</strong>的通信。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>接口报跨域错误，后端说「我这边收到请求了，日志都有」，谁的问题？</strong></p>\\n<p>两边说的都对。浏览器确实发出了请求、服务端也执行了，只是响应缺少 <code>Access-Control-Allow-Origin</code> 头，浏览器不把结果交给 <code>JS</code>。所以是服务端补 <code>CORS</code> 响应头（或前端走同域代理）。顺便提醒后端：既然请求真的执行了，写操作接口更要防 <code>CSRF</code>。</p>\\n</li>\\n<li>\\n<p><strong>本地开发用 <code>Vite</code> 的 <code>proxy</code> 就好了，为什么上线还要配 <code>CORS</code>？</strong></p>\\n<p><code>proxy</code> 是开发服务器在 <code>Node</code> 侧转发，服务端之间不存在同源策略，所以本地看不到问题。上线后浏览器直连接口，同源策略重新生效。要么线上也走同域网关反向代理，要么服务端老老实实配 <code>CORS</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-前后端如何通信\\"><a class=\\"header-anchor\\" href=\\"#_3-前后端如何通信\\">#</a> 3 前后端如何通信</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Ajax</code>（<code>XMLHttpRequest</code> / <code>fetch</code>）：最常用，受同源策略限制，跨域要靠 <code>CORS</code></li>\\n<li><code>WebSocket</code>：一次 <code>HTTP Upgrade</code> 之后升级成全双工长连接，不受同源策略限制，适合实时推送</li>\\n<li><code>CORS</code> 不是另一种通信方式，而是让浏览器放行跨域响应的一套协议，可以理解成「同时支持同源和跨域的 <code>Ajax</code>」</li>\\n<li>单向推送还可以用 <code>SSE</code>（<code>EventSource</code>）：比 <code>WebSocket</code> 轻、自带重连，但只能服务端往客户端发</li>\\n<li>老方案 <code>JSONP</code> 只能 <code>GET</code>、错误处理弱、有安全风险，现在只在维护老系统时才会遇到</li>\\n<li>选型看通信方向和实时性：一问一答用 <code>Ajax</code>，服务端单向推用 <code>SSE</code>，双向高频用 <code>WebSocket</code></li>\\n</ul>\\n<p><strong>按「通信方向 + 实时性」选：一问一答 <code>Ajax</code>、单向推送 <code>SSE</code>、双向实时 <code>WebSocket</code></strong> —— 再补一句 <code>CORS</code> 是让跨域 <code>Ajax</code> 合法化的协议，不是一种通信方式。</p>\\n</section>\\n<p><strong>主要有以下几种方式：</strong></p>\\n<ul>\\n<li><code>Ajax</code>：不支持跨域。</li>\\n<li><code>WebSocket</code>：不受同源策略的限制，支持跨域</li>\\n<li><code>CORS</code>：不受同源策略的限制，支持跨域。一种新的通信协议标准。可以理解成是：<strong>同时支持同源和跨域的Ajax</strong>。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>做一个消息通知的小需求，你用轮询、<code>SSE</code> 还是 <code>WebSocket</code>？</strong></p>\\n<p>先问频率和方向。只是服务端偶尔推几条，<code>SSE</code> 最划算：基于 <code>HTTP</code>、自带断线重连、代理兼容性好、几行代码就能跑。要双向（在线聊天、协同编辑）才上 <code>WebSocket</code>，它得自己做心跳、退避重连和断线补消息。轮询只在两者都用不了时兜底。</p>\\n</li>\\n<li>\\n<p><strong><code>WebSocket</code> 不受同源策略限制，那它安全吗？</strong></p>\\n<p>正因为不受限制，服务端必须自己校验握手时的 <code>Origin</code> 头并做鉴权，否则任何站点都能连上你的服务。握手时 cookie 一样会带上，所以还要防跨站 <code>WebSocket</code> 劫持（<code>CSWSH</code>），通常做法是用一次性 <code>token</code> 而不是纯靠 cookie 鉴权。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_4-如何创建ajax\\"><a class=\\"header-anchor\\" href=\\"#_4-如何创建ajax\\">#</a> 4 如何创建Ajax</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>五步：<code>new XMLHttpRequest()</code> → 注册 <code>onreadystatechange</code> → <code>open(method, url, async)</code> → 需要就 <code>setRequestHeader</code> → <code>send(body)</code></li>\\n<li><code>readyState</code> 五个值要能背：<code>0</code> 未初始化、<code>1</code> 已 <code>open</code>、<code>2</code> 收到响应头、<code>3</code> 正在接收响应体、<code>4</code> 完成</li>\\n<li>判断成功不能只看 <code>readyState === 4</code>，还要看 <code>status</code> 是不是 <code>2xx</code>；<code>status</code> 为 <code>0</code> 通常是跨域被拦或请求被中断</li>\\n<li><code>setRequestHeader</code> 必须在 <code>open</code> 之后、<code>send</code> 之前调用，顺序错了会抛错</li>\\n<li>兼容性写法要判断 <code>window.XMLHttpRequest</code>，老 <code>IE</code> 用 <code>new ActiveXObject(\'Microsoft.XMLHTTP\')</code></li>\\n<li>现在业务里用 <code>fetch</code> / <code>axios</code>，但要知道 <code>fetch</code> 默认不带 cookie（要写 <code>credentials</code>）、<code>HTTP</code> 错误码不会 <code>reject</code></li>\\n</ul>\\n<p><strong>原生 <code>Ajax</code> 就是「建对象 → 注册回调 → <code>open</code> → <code>send</code>」四步加一个 <code>readyState</code> 状态机</strong> —— 再补「成功判定要同时看 <code>readyState</code> 和 <code>status</code>」这个最常见的错。</p>\\n</section>\\n<blockquote>\\n<p>在回答 <code>Ajax</code> 的问题时，要回答以下几个方面：</p>\\n</blockquote>\\n<ol>\\n<li><code>XMLHttpRequest</code> 的工作原理</li>\\n<li>兼容性处理</li>\\n</ol>\\n<blockquote>\\n<p><code>XMLHttpRequest</code>只有在高级浏览器中才支持。在回答问题时，这个兼容性问题不要忽略。</p>\\n</blockquote>\\n<ol start=\\"3\\">\\n<li>事件的触发条件</li>\\n<li>事件的触发顺序</li>\\n</ol>\\n<blockquote>\\n<p><code>XMLHttpRequest</code>有很多触发事件，每个事件是怎么触发的。</p>\\n</blockquote>\\n<h4 id=\\"_4-1-发送-ajax-请求的五个步骤-xmlhttprequest的工作原理\\"><a class=\\"header-anchor\\" href=\\"#_4-1-发送-ajax-请求的五个步骤-xmlhttprequest的工作原理\\">#</a> 4.1 发送 Ajax 请求的五个步骤（XMLHttpRequest的工作原理）</h4>\\n<ol>\\n<li>创建<code>XMLHttpRequest</code> 对象。</li>\\n<li>使用<code>open</code>方法设置请求的参数。<code>open(method, url, 是否异步)</code></li>\\n<li>发送请求。</li>\\n<li>注册事件。 注册<code>onreadystatechange</code>事件，状态改变时就会调用。</li>\\n</ol>\\n<blockquote>\\n<p>如果要在数据完整请求回来的时候才调用，我们需要手动写一些判断的逻辑。</p>\\n</blockquote>\\n<ol start=\\"5\\">\\n<li>获取返回的数据，更新UI。</li>\\n</ol>\\n<h4 id=\\"_4-2-发送-get-请求和-post-请求\\"><a class=\\"header-anchor\\" href=\\"#_4-2-发送-get-请求和-post-请求\\">#</a> 4.2 发送 get 请求和 post 请求</h4>\\n<blockquote>\\n<p><code>get</code>请求举例：</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n&lt;head&gt;\\n    &lt;meta charset=&quot;UTF-8&quot;&gt;\\n    &lt;title&gt;Document&lt;/title&gt;\\n&lt;/head&gt;\\n&lt;body&gt;\\n&lt;h1&gt;Ajax 发送 get 请求&lt;/h1&gt;\\n&lt;input type=&quot;button&quot; value=&quot;发送get_ajax请求&quot; id=&#39;btnAjax&#39;&gt;\\n\\n&lt;script type=&quot;text/javascript&quot;&gt;\\n    // 绑定点击事件\\n    document.querySelector(&#39;#btnAjax&#39;).onclick = function () {\\n        // 发送ajax 请求 需要 五步\\n\\n        // （1）创建异步对象\\n        var ajaxObj = new XMLHttpRequest();\\n\\n        // （2）设置请求的参数。包括：请求的方法、请求的url。\\n        ajaxObj.open(&#39;get&#39;, &#39;02-ajax.php&#39;);\\n\\n        // （3）发送请求\\n        ajaxObj.send();\\n\\n        //（4）注册事件。 onreadystatechange事件，状态改变时就会调用。\\n        //如果要在数据完整请求回来的时候才调用，我们需要手动写一些判断的逻辑。\\n        ajaxObj.onreadystatechange = function () {\\n            // 为了保证 数据 完整返回，我们一般会判断 两个值\\n            if (ajaxObj.readyState == 4 &amp;&amp; ajaxObj.status == 200) {\\n                // 如果能够进到这个判断 说明 数据 完美的回来了,并且请求的页面是存在的\\n                // 5.在注册的事件中 获取 返回的 内容 并修改页面的显示\\n                console.log(&#39;数据返回成功&#39;);\\n\\n                // 数据是保存在 异步对象的 属性中\\n                console.log(ajaxObj.responseText);\\n\\n                // 修改页面的显示\\n                document.querySelector(&#39;h1&#39;).innerHTML = ajaxObj.responseText;\\n            }\\n        }\\n    }\\n&lt;/script&gt;\\n&lt;/body&gt;\\n&lt;/html&gt;\\n</code></pre>\\n<blockquote>\\n<p><code>post</code> 请求举例：</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n&lt;head&gt;\\n    &lt;meta charset=&quot;UTF-8&quot;&gt;\\n    &lt;title&gt;Document&lt;/title&gt;\\n&lt;/head&gt;\\n&lt;body&gt;\\n&lt;h1&gt;Ajax 发送 get 请求&lt;/h1&gt;\\n&lt;input type=&quot;button&quot; value=&quot;发送put_ajax请求&quot; id=&#39;btnAjax&#39;&gt;\\n&lt;script type=&quot;text/javascript&quot;&gt;\\n\\n    // 异步对象\\n    var xhr = new XMLHttpRequest();\\n\\n    // 设置属性\\n    xhr.open(&#39;post&#39;, &#39;02.post.php&#39;);\\n\\n    // 如果想要使用post提交数据,必须添加此行\\n    xhr.setRequestHeader(&quot;Content-type&quot;, &quot;application/x-www-form-urlencoded&quot;);\\n\\n    // 将数据通过send方法传递\\n    xhr.send(&#39;name=fox&amp;age=18&#39;);\\n\\n    // 发送并接受返回值\\n    xhr.onreadystatechange = function () {\\n        // 这步为判断服务器是否正确响应\\n        if (xhr.readyState == 4 &amp;&amp; xhr.status == 200) {\\n            alert(xhr.responseText);\\n        }\\n    };\\n&lt;/script&gt;\\n&lt;/body&gt;\\n&lt;/html&gt;\\n</code></pre>\\n<h4 id=\\"_4-3-onreadystatechange-事件\\"><a class=\\"header-anchor\\" href=\\"#_4-3-onreadystatechange-事件\\">#</a> 4.3 onreadystatechange 事件</h4>\\n<blockquote>\\n<p>注册 <code>onreadystatechange</code> 事件后，每当 <code>readyState</code> 属性改变时，就会调用 <code>onreadystatechange</code> 函数。</p>\\n</blockquote>\\n<blockquote>\\n<p><code>readyState</code>：（存有 <code>XMLHttpRequest</code> 的状态。从 <code>0</code> 到 <code>4</code> 发生变化）</p>\\n</blockquote>\\n<ul>\\n<li><code>0</code>: 请求未初始化</li>\\n<li><code>1</code>: 服务器连接已建立</li>\\n<li><code>2</code>: 请求已接收</li>\\n<li><code>3</code>: 请求处理中</li>\\n<li><code>4</code>: 请求已完成，且响应已就绪</li>\\n</ul>\\n<h4 id=\\"_4-4-事件的触发条件\\"><a class=\\"header-anchor\\" href=\\"#_4-4-事件的触发条件\\">#</a> 4.4 事件的触发条件</h4>\\n<blockquote>\\n<p><code>onreadystatechange</code> 的触发条件：<strong>只要 <code>readyState</code> 的值发生改变，就会触发一次 <code>onreadystatechange</code> 事件</strong>。<code>readyState</code> 从 <code>0 → 1 → 2 → 3 → 4</code> 每变化一步都会触发，所以一次完整的请求过程中该回调<strong>会被多次调用</strong>（正常情况约 4 次，<code>readyState</code> 停在 3 时若有多段数据到达还可能多次触发 3）。</p>\\n</blockquote>\\n<p>真正判断「请求成功完成」要<strong>同时满足两个条件</strong>：<code>xhr.readyState === 4</code>（请求已完成、响应就绪）<strong>且</strong> <code>xhr.status === 200</code>（或 2xx 区间）——前者表示「传输结束」，后者表示「服务端返回成功」，两者缺一不可。</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/23.png\\" alt=\\"\\"></p>\\n<h4 id=\\"_4-5-事件的触发顺序\\"><a class=\\"header-anchor\\" href=\\"#_4-5-事件的触发顺序\\">#</a> 4.5 事件的触发顺序</h4>\\n<blockquote>\\n<p>触发顺序对应 <code>readyState</code> 的递增：<code>0</code>（未初始化，<code>new XMLHttpRequest</code> 后）→ <code>1</code>（已调用 <code>open</code>，连接建立）→ <code>2</code>（已调用 <code>send</code>，响应头已收到）→ <code>3</code>（响应体正在下载，数据部分可用）→ <code>4</code>（下载完成，响应就绪）。每进入一个新状态触发一次 <code>onreadystatechange</code>。</p>\\n</blockquote>\\n<blockquote>\\n<p>补充：现代开发已很少直接写 <code>XMLHttpRequest</code> + <code>onreadystatechange</code>，更多用 <code>fetch</code>（基于 Promise、API 更简洁）或 axios（自动 JSON、拦截器、超时）。这里的 <code>readyState</code> 机制作为「AJAX 底层原理」了解即可，<code>fetch</code> 用 <code>response.ok</code> / <code>response.status</code> 判断成功、用 <code>AbortController</code> 取消。</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/24.png\\" alt=\\"\\"></p>\\n<h4 id=\\"_4-6-实际开发中用的-原生ajax请求\\"><a class=\\"header-anchor\\" href=\\"#_4-6-实际开发中用的-原生ajax请求\\">#</a> 4.6 实际开发中用的 原生Ajax请求</h4>\\n<pre class=\\"language-javascript\\"><code>\\n    var util = {};\\n\\n    //获取 ajax 请求之后的json\\n    util.json = function (options) {\\n\\n        var opt = {\\n            url: &#39;&#39;,\\n            type: &#39;get&#39;,\\n            data: {},\\n            success: function () {\\n            },\\n            error: function () {\\n            },\\n\\n        };\\n        util.extend(opt, options);\\n        if (opt.url) {\\n            //IE兼容性处理：浏览器特征检查。检查该浏览器是否存在XMLHttpRequest这个api，没有的话，就用IE的api\\n            var xhr = XMLHttpRequest ? new XMLHttpRequest() : new window.ActiveXObject(&#39;Microsoft.XMLHTTP&#39;);\\n\\n            var data = opt.data,\\n                url = opt.url,\\n                type = opt.type.toUpperCase();\\n            dataArr = [];\\n        }\\n\\n        for (var key in data) {\\n            dataArr.push(key + &#39;=&#39; + data[key]);\\n        }\\n\\n        if (type === &#39;GET&#39;) {\\n            url = url + &#39;?&#39; + dataArr.join(&#39;&amp;&#39;);\\n            xhr.open(type, url.replace(/\\\\?$/g, &#39;&#39;), true);\\n            xhr.send();\\n        }\\n\\n        if (type === &#39;POST&#39;) {\\n            xhr.open(type, url, true);\\n            // 如果想要使用post提交数据,必须添加此行\\n            xhr.setRequestHeader(&quot;Content-type&quot;, &quot;application/x-www-form-urlencoded&quot;);\\n            xhr.send(dataArr.join(&#39;&amp;&#39;));\\n        }\\n\\n        xhr.onload = function () {\\n            if (xhr.status === 200 || xhr.status === 304) { //304表示：用缓存即可。206表示获取媒体资源的前面一部分\\n                var res;\\n                if (opt.success &amp;&amp; opt.success instanceof Function) {\\n                    res = xhr.responseText;\\n                    if (typeof res === &#39;string&#39;) {\\n                        res = JSON.parse(res);  //将字符串转成json\\n                        opt.success.call(xhr, res);\\n                    }\\n                }\\n            } else {\\n                if (opt.error &amp;&amp; opt.error instanceof Function) {\\n                    opt.error.call(xhr, res);\\n                }\\n            }\\n        };\\n    }\\n\\n\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>fetch</code> 请求返回 404，为什么 <code>catch</code> 里没进去？</strong></p>\\n<p><code>fetch</code> 只在网络层失败（断网、<code>DNS</code> 失败、被 <code>CORS</code> 拦住）时才 <code>reject</code>，<code>HTTP</code> 错误码在它看来属于「请求成功完成了」。必须自己判断 <code>response.ok</code> 或 <code>status</code> 再抛错 —— 这也是大家都要在 <code>fetch</code> 外面封装一层 <code>request</code> 的原因之一。</p>\\n</li>\\n<li>\\n<p><strong>要给请求加超时和取消，原生怎么做？</strong></p>\\n<p><code>XMLHttpRequest</code> 有 <code>timeout</code> 属性和 <code>abort()</code> 方法；<code>fetch</code> 没有内置超时，要用 <code>AbortController</code>，配合 <code>AbortSignal.timeout(ms)</code> 或自己 <code>setTimeout</code> 后调 <code>controller.abort()</code>。搜索联想、页面切换这类场景必须取消上一个请求，否则会出现旧响应覆盖新结果。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_5-跨域通信的几种方式\\"><a class=\\"header-anchor\\" href=\\"#_5-跨域通信的几种方式\\">#</a> 5 跨域通信的几种方式</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>CORS</code> 是标准答案：服务端返回 <code>Access-Control-Allow-Origin</code>，浏览器才把响应交给 <code>JS</code></li>\\n<li>非简单请求（自定义头、<code>application/json</code>、<code>PUT</code> / <code>DELETE</code>）会先发一次 <code>OPTIONS</code> 预检，这就是「一个请求发两次」的真相</li>\\n<li>带 cookie 要同时满足三点：前端写 <code>credentials: \'include\'</code>、服务端返回 <code>Allow-Credentials: true</code>、且 <code>Allow-Origin</code> 不能是 <code>*</code></li>\\n<li><code>JSONP</code> 利用 <code>&lt;script&gt;</code> 不受同源限制，只能 <code>GET</code>，要清理全局回调，安全性差</li>\\n<li>页面之间通信用 <code>postMessage</code>，收消息一定要校验 <code>event.origin</code>，不校验等于开了后门</li>\\n<li>开发环境用 <code>devServer.proxy</code>、生产用 <code>Nginx</code> 反向代理，本质是把跨域挪到服务端之间，那里没有同源策略</li>\\n</ul>\\n<p><strong>生产上只有两条路：服务端配 <code>CORS</code>，或者用同域网关代理转发</strong> —— 其余（<code>JSONP</code>、<code>document.domain</code>、<code>hash</code>、<code>postMessage</code>）要么是历史方案，要么解决的是页面间通信而不是接口跨域。</p>\\n</section>\\n<blockquote>\\n<p>方式如下：</p>\\n</blockquote>\\n<ol>\\n<li><code>JSONP</code></li>\\n<li><code>WebSocket</code></li>\\n<li><code>CORS</code></li>\\n<li><code>Hash</code></li>\\n<li><code>postMessage</code></li>\\n</ol>\\n<blockquote>\\n<p>上面这五种方式，在面试时，都要说出来。</p>\\n</blockquote>\\n<h4 id=\\"_5-1-jsonp\\"><a class=\\"header-anchor\\" href=\\"#_5-1-jsonp\\">#</a> 5.1 JSONP</h4>\\n<blockquote>\\n<p>面试会问：<code>JSONP</code>的原理是什么？怎么实现的？</p>\\n</blockquote>\\n<ul>\\n<li>在<code>CORS</code>和<code>postMessage</code>以前，我们一直都是通过<code>JSONP</code>来做跨域通信的。</li>\\n</ul>\\n<blockquote>\\n<p><strong>JSONP的原理</strong>：通过<code>&lt;script&gt;</code>标签的异步加载来实现的。比如说，实际开发中，我们发现，<code>head</code>标签里，可以通过<code>&lt;script&gt;</code>标签的<code>src</code>，里面放<code>url</code>，加载很多在线的插件。这就是用到了<code>JSONP</code>。</p>\\n</blockquote>\\n<p><strong>JSONP的实现：</strong></p>\\n<blockquote>\\n<p>比如说，客户端这样写：</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>    &lt;script src=&quot;http://www.smyhvae.com/?data=name&amp;callback=myjsonp&quot;&gt;&lt;/script&gt;\\n</code></pre>\\n<blockquote>\\n<p>上面的<code>src</code>中，<code>data=name</code>是get请求的参数，<code>myjsonp</code>是和后台约定好的函数名。\\n服务器端这样写：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>  myjsonp({\\n      data: {}\\n\\n  })\\n</code></pre>\\n<blockquote>\\n<p>于是，本地要求创建一个<code>myjsonp</code> 的<strong>全局函数</strong>，才能将返回的数据执行出来。</p>\\n</blockquote>\\n<p><strong>实际开发中，前端的JSONP是这样实现的：</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;script&gt;\\n\\n    var util = {};\\n\\n    //定义方法：动态创建 script 标签\\n    /**\\n     * [function 在页面中注入js脚本]\\n     * @param  {[type]} url     [description]\\n     * @param  {[type]} charset [description]\\n     * @return {[type]}         [description]\\n     */\\n    util.createScript = function (url, charset) {\\n        var script = document.createElement(&#39;script&#39;);\\n        script.setAttribute(&#39;type&#39;, &#39;text/javascript&#39;);\\n        charset &amp;&amp; script.setAttribute(&#39;charset&#39;, charset);\\n        script.setAttribute(&#39;src&#39;, url);\\n        script.async = true;\\n        return script;\\n    };\\n\\n\\n    /**\\n     * [function 处理jsonp]\\n     * @param  {[type]} url      [description]\\n     * @param  {[type]} onsucess [description]\\n     * @param  {[type]} onerror  [description]\\n     * @param  {[type]} charset  [description]\\n     * @return {[type]}          [description]\\n     */\\n    util.jsonp = function (url, onsuccess, onerror, charset) {\\n        var callbackName = util.getName(&#39;tt_player&#39;); //事先约定好的 函数名\\n        window[callbackName] = function () {      //根据回调名称注册一个全局的函数\\n            if (onsuccess &amp;&amp; util.isFunction(onsuccess)) {\\n                onsuccess(arguments[0]);\\n            }\\n        };\\n        var script = util.createScript(url + &#39;&amp;callback=&#39; + callbackName, charset);   //动态创建一个script标签\\n        script.onload = script.onreadystatechange = function () {   //监听加载成功的事件，获取数据\\n            if (!script.readyState || /loaded|complete/.test(script.readyState)) {\\n                script.onload = script.onreadystatechange = null;\\n                // 移除该script的 DOM 对象\\n                if (script.parentNode) {\\n                    script.parentNode.removeChild(script);\\n                }\\n                // 删除函数或变量\\n                window[callbackName] = null;  //最后不要忘了删除\\n            }\\n        };\\n        script.onerror = function () {\\n            if (onerror &amp;&amp; util.isFunction(onerror)) {\\n                onerror();\\n            }\\n        };\\n        document.getElementsByTagName(&#39;head&#39;)[0].appendChild(script); //往html中增加这个标签，目的是把请求发送出去\\n    };\\n\\n&lt;/script&gt;\\n\\n</code></pre>\\n<h4 id=\\"_5-2-websocket\\"><a class=\\"header-anchor\\" href=\\"#_5-2-websocket\\">#</a> 5.2 WebSocket</h4>\\n<blockquote>\\n<p><code>WebSocket</code>的用法如下：</p>\\n</blockquote>\\n<pre class=\\"language-javascript\\"><code>    //\\n\\n    var ws = new WebSocket(&#39;wss://echo.websocket.org&#39;); //创建WebSocket的对象。参数可以是 ws 或 wss，后者表示加密。\\n\\n    //把请求发出去\\n    ws.onopen = function (evt) {\\n        console.log(&#39;Connection open ...&#39;);\\n        ws.send(&#39;Hello WebSockets!&#39;);\\n    };\\n\\n\\n    //对方发消息过来时，我接收\\n    ws.onmessage = function (evt) {\\n        console.log(&#39;Received Message: &#39;, evt.data);\\n        ws.close();\\n    };\\n\\n    //关闭连接\\n    ws.onclose = function (evt) {\\n        console.log(&#39;Connection closed.&#39;);\\n    };\\n</code></pre>\\n<blockquote>\\n<p>面试一般不会让你写这个代码，一般是考察你是否了解 <code>WebSocket</code>概念，知道有这么回事即可。</p>\\n</blockquote>\\n<h4 id=\\"_5-3-cors\\"><a class=\\"header-anchor\\" href=\\"#_5-3-cors\\">#</a> 5.3 CORS</h4>\\n<blockquote>\\n<p><code>CORS</code> 可以理解成是<strong>既可以同步、也可以异步</strong>的Ajax。</p>\\n</blockquote>\\n<ul>\\n<li>fetch<code>是一个比较新的</code>API<code>，用来实现</code>CORS`通信。用法如下：</li>\\n</ul>\\n<pre class=\\"language-javascript\\"><code>      // url（必选），options（可选）\\n      fetch(&#39;/some/url/&#39;, {\\n          method: &#39;get&#39;,\\n      }).then(function (response) {  //类似于 ES6中的promise\\n\\n      }).catch(function (err) {\\n        // 出错了，等价于 then 的第二个参数，但这样更好用更直观\\n      });\\n</code></pre>\\n<blockquote>\\n<p>另外，如果面试官问：“CORS为什么支持跨域的通信？”</p>\\n</blockquote>\\n<blockquote>\\n<p>答案：跨域时，浏览器会拦截<code>Ajax</code>请求，并在<code>http</code>头中加<code>Origin</code>。</p>\\n</blockquote>\\n<h4 id=\\"_5-4-hash\\"><a class=\\"header-anchor\\" href=\\"#_5-4-hash\\">#</a> 5.4 Hash</h4>\\n<ul>\\n<li><code>url</code>的<code>#</code>后面的内容就叫<code>Hash</code>。<strong>Hash的改变，页面不会刷新</strong>。这就是用 <code>Hash</code> 做跨域通信的基本原理。</li>\\n</ul>\\n<blockquote>\\n<p>补充：<code>url</code>的<code>?</code>后面的内容叫<code>Search</code>。<code>Search</code>的改变，会导致页面刷新，因此不能做跨域通信。</p>\\n</blockquote>\\n<p><strong>使用举例：</strong></p>\\n<p><strong>场景</strong>：我的页面 <code>A</code> 通过<code>iframe</code>或<code>frame</code>嵌入了跨域的页面 <code>B</code>。</p>\\n<blockquote>\\n<p>现在，我这个<code>A</code>页面想给<code>B</code>页面发消息，怎么操作呢？</p>\\n</blockquote>\\n<ol>\\n<li>首先，在我的<code>A</code>页面中：</li>\\n</ol>\\n<pre class=\\"language-javascript\\"><code>    //伪代码\\n    var B = document.getElementsByTagName(&#39;iframe&#39;);\\n    B.src = B.src + &#39;#&#39; + &#39;jsonString&#39;;  //我们可以把JS 对象，通过 JSON.stringify()方法转成 json字符串，发给 B\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>然后，在<code>B</code>页面中：</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>// B中的伪代码\\nwindow.onhashchange = function () {  //通过onhashchange方法监听，url中的 hash 是否发生变化\\n  var data = window.location.hash;\\n};\\n</code></pre>\\n<h4 id=\\"_5-5-postmessage-方法\\"><a class=\\"header-anchor\\" href=\\"#_5-5-postmessage-方法\\">#</a> 5.5 postMessage()方法</h4>\\n<blockquote>\\n<p><code>H5</code>中新增的<code>postMessage()</code>方法，可以用来做跨域通信。既然是H5中新增的，那就一定要提到。</p>\\n</blockquote>\\n<p><strong>场景</strong>：窗口 A (<code>http:A.com</code>)向跨域的窗口 B (<code>http:B.com</code>)发送信息。步骤如下</p>\\n<ol>\\n<li>在<code>A</code>窗口中操作如下：向<code>B</code>窗口发送数据：</li>\\n</ol>\\n<pre class=\\"language-javascript\\"><code>\\t// 窗口A(http:A.com)向跨域的窗口B(http:B.com)发送信息\\n \\tBwindow.postMessage(&#39;data&#39;, &#39;http://B.com&#39;); //这里强调的是B窗口里的window对象\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>在<code>B</code>窗口中操作如下：</li>\\n</ol>\\n<pre class=\\"language-javascript\\"><code>    // 在窗口B中监听 message 事件\\n    Awindow.addEventListener(&#39;message&#39;, function (event) {   //这里强调的是A窗口里的window对象\\n        console.log(event.origin);  //获取 ：url。这里指：http://A.com\\n        console.log(event.source);  //获取：A window对象\\n        console.log(event.data);    //获取传过来的数据\\n    }, false);\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>接口配了 <code>Access-Control-Allow-Origin: *</code>，但带 cookie 还是失败，为什么？</strong></p>\\n<p>带凭证时规范不允许 <code>Allow-Origin</code> 写通配符，必须回显具体的源，同时返回 <code>Access-Control-Allow-Credentials: true</code>，前端也要显式写 <code>credentials: \'include\'</code>。三者缺一不可。</p>\\n</li>\\n<li>\\n<p><strong>预检请求每次都发，接口耗时翻倍，怎么优化？</strong></p>\\n<p>让服务端返回 <code>Access-Control-Max-Age</code>，把预检结果缓存一段时间（各浏览器有自己的上限）。更彻底的做法是把请求降级成简单请求，或者干脆走同域代理，从根上不产生预检。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"九、前端错误监控\\"><a class=\\"header-anchor\\" href=\\"#九、前端错误监控\\">#</a> 九、前端错误监控</h2>\\n<h3 id=\\"_1-前言-4\\"><a class=\\"header-anchor\\" href=\\"#_1-前言-4\\">#</a> 1 前言</h3>\\n<blockquote>\\n<p>错误监控包含的内容是：</p>\\n</blockquote>\\n<ul>\\n<li>前端错误的分类</li>\\n<li>每种错误的捕获方式</li>\\n<li>上报错误的基本原理</li>\\n</ul>\\n<blockquote>\\n<p>面试时，可能有两种问法：</p>\\n</blockquote>\\n<ul>\\n<li>如何监测 <code>js</code> 错误？（开门见山的方式）</li>\\n<li>如何保证<strong>产品质量</strong>？（其实问的也是错误监控）</li>\\n</ul>\\n<h3 id=\\"_2-前端错误的分类\\"><a class=\\"header-anchor\\" href=\\"#_2-前端错误的分类\\">#</a> 2 前端错误的分类</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>按来源分两大类：即时运行错误（代码执行时抛出）和资源加载错误（<code>img</code> / <code>script</code> / <code>css</code> 没加载成功）</li>\\n<li>运行错误里还要单独拎出「未处理的 <code>Promise</code> 拒绝」，它不触发 <code>error</code> 事件，得单独监听</li>\\n<li>接口错误也算一类：<code>HTTP</code> 状态码非 <code>2xx</code>、超时、业务错误码，要在请求封装层统一采集</li>\\n<li>还有一类容易被忽略：白屏和页面崩溃，靠错误监听根本抓不到，得用心跳或存活探测</li>\\n<li>按可观测性还能再分：能拿到堆栈的、只有一句 <code>Script error.</code> 的（跨域脚本）、以及静默失败的</li>\\n<li>分类的目的是决定用哪套捕获手段，不是为了背名词</li>\\n</ul>\\n<p><strong>先按「运行时 / 资源 / <code>Promise</code> / 接口 / 崩溃」五类分，每一类对应一套不同的捕获方式</strong> —— 分类讲完自然引出下一题的捕获手段。</p>\\n</section>\\n<p>包括两种：</p>\\n<ul>\\n<li>即时运行错误（代码错误）</li>\\n<li>资源加载错误</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>面试官问「如何保证产品质量」，你会从错误监控讲起吗？</strong></p>\\n<p>会，但要讲成闭环：采集（分类覆盖全）→ 上报（采样、去重、不拖慢业务）→ 还原（<code>sourcemap</code> 还原堆栈）→ 告警和归因（按版本、路由、用户分组）→ 回归验证。只说「我加了 <code>window.onerror</code>」显得没做过线上系统。</p>\\n</li>\\n<li>\\n<p><strong>接口返回 200 但业务码是失败，这算前端错误吗？</strong></p>\\n<p>算异常但不算 <code>JS</code> 错误，要单独归一类。它抓不到堆栈，靠请求封装层统一采集业务码和 <code>traceId</code>，和后端日志对齐才有排查价值。混进 <code>JS</code> 错误里只会让告警噪音失控。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-每种错误的捕获方式\\"><a class=\\"header-anchor\\" href=\\"#_3-每种错误的捕获方式\\">#</a> 3 每种错误的捕获方式</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>同步运行错误：<code>window.onerror</code> 或 <code>window.addEventListener(\'error\')</code>，能拿到 <code>message</code>、文件、行列号和 <code>error</code> 对象（含堆栈）</li>\\n<li>资源加载错误不冒泡，只能用 <code>window.addEventListener(\'error\', handler, true)</code> 在捕获阶段拦，并用 <code>event.target</code> 判断</li>\\n<li>未处理的 <code>Promise</code> 拒绝要单独监听 <code>unhandledrejection</code>；<code>async/await</code> 忘了 <code>try/catch</code> 也归这一类</li>\\n<li><code>try/catch</code> 只能兜同步代码，异步回调里抛的异常它抓不到；而且抓到之后错误就被消费，全局监听不会再收到</li>\\n<li>跨域脚本只给一句 <code>Script error.</code>，要给 <code>&lt;script&gt;</code> 加 <code>crossorigin</code> 并让 <code>CDN</code> 返回 <code>Access-Control-Allow-Origin</code> 才有堆栈</li>\\n<li>框架还有自己的兜底：<code>React</code> 的 <code>Error Boundary</code>、<code>Vue</code> 的 <code>errorHandler</code>，要和全局监听一起上</li>\\n</ul>\\n<p><strong>不同错误走不同通道，一套监听兜不住</strong> —— 按「运行时 → 资源（捕获阶段）→ <code>Promise</code> → <code>try/catch</code> 的边界 → 跨域脚本」五条线讲。</p>\\n</section>\\n<h4 id=\\"_3-1-即时运行错误的捕获方式\\"><a class=\\"header-anchor\\" href=\\"#_3-1-即时运行错误的捕获方式\\">#</a> 3.1 即时运行错误的捕获方式</h4>\\n<p><strong>方式1</strong>：<code>try ... catch</code>。</p>\\n<blockquote>\\n<p>这种方式要部署在代码中。</p>\\n</blockquote>\\n<p><strong>方式2：</strong><code>window.onerror</code>函数。这个函数是全局的。</p>\\n<pre class=\\"language-js\\"><code>\\twindow.onerror = function(msg, url, row, col, error) { ... }\\n</code></pre>\\n<blockquote>\\n<p>参数解释：</p>\\n</blockquote>\\n<ul>\\n<li><code>msg</code>为异常基本信息</li>\\n<li><code>source</code>为发生异常<code>Javascript</code>文件的<code>url</code></li>\\n<li><code>row</code>为发生错误的行号</li>\\n</ul>\\n<blockquote>\\n<p>方式二中的<code>window.onerror</code>是属于DOM0的写法，我们也可以用DOM2的写法：<code>window.addEventListener(&quot;error&quot;, fn);</code>也可以。</p>\\n</blockquote>\\n<p><strong>问题延伸1：</strong></p>\\n<p><code>window.onerror</code>默认无法捕获<strong>跨域</strong>的<code>js</code>运行错误。捕获出来的信息如下：（基本属于无效信息）</p>\\n<blockquote>\\n<p>比如说，我们的代码想引入<code>B</code>网站的<code>b.js</code>文件，怎么捕获它的异常呢？</p>\\n</blockquote>\\n<p><strong>解决办法</strong>：在方法二的基础之上，做如下操作：</p>\\n<ol>\\n<li>在<code>b.js</code>文件里，加入如下 <code>response</code> <code>header</code>，表示允许跨域：（或者世界给静态资源<code>b.js</code>加这个 response header）</li>\\n</ol>\\n<pre class=\\"language-js\\"><code>\\tAccess-Control-Allow-Origin: *\\n</code></pre>\\n<ol start=\\"2\\">\\n<li>引入第三方的文件<code>b.js</code>时，在<code>&lt;script&gt;</code>标签中增加<code>crossorigin</code>属性；</li>\\n</ol>\\n<p><strong>问题延伸2：</strong></p>\\n<blockquote>\\n<p>只靠方式二中的<code>window.onerror</code>是不够的，因为我们无法获取文件名是什么，不知道哪里出了错误。解决办法：把<strong>堆栈</strong>信息作为msg打印出来，堆栈里很详细。</p>\\n</blockquote>\\n<h4 id=\\"_3-2-资源加载错误的捕获方式\\"><a class=\\"header-anchor\\" href=\\"#_3-2-资源加载错误的捕获方式\\">#</a> 3.2 资源加载错误的捕获方式</h4>\\n<blockquote>\\n<p>上面的<code>window.onerror</code>只能捕获即时运行错误，无法捕获资源加载错误。原理是：资源加载错误，并不会向上冒泡，<code>object.onerror</code>捕获后就会终止（不会冒泡给<code>window</code>），所以<code>window.onerror</code>并不能捕获资源加载错误。</p>\\n</blockquote>\\n<ul>\\n<li><strong>方式1</strong>：<code>object.onerror</code>。<code>img</code>标签、<code>script</code>标签等节点都可以添加<code>onerror</code>事件，用来捕获资源加载的错误。</li>\\n<li><strong>方式2</strong>：performance.getEntries。可以获取所有已加载资源的加载时长，通过这种方式，可以间接的拿到没有加载的资源错误。</li>\\n</ul>\\n<p>举例：</p>\\n<blockquote>\\n<p>浏览器打开一个网站，在<code>Console</code>控制台下，输入：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>\\tperformance.getEntries().forEach(function(item){console.log(item.name)})\\n</code></pre>\\n<p>或者输入：</p>\\n<pre class=\\"language-js\\"><code>\\tperformance.getEntries().forEach(item=&gt;{console.log(item.name)})\\n</code></pre>\\n<blockquote>\\n<p>上面这个<code>api</code>，返回的是数组，既然是数组，就可以用<code>forEach</code>遍历。打印出来的资源就是<strong>已经成功加载</strong>的资源。；</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/30.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>再入<code>document.getElementsByTagName(\'img\')</code>，就会显示出所有<strong>需要加载</strong>的的img集合。</p>\\n</blockquote>\\n<blockquote>\\n<p>于是，<code>document.getElementsByTagName(\'img\')</code>获取的资源数组减去通过<code>performance.getEntries()</code>获取的资源数组，剩下的就是没有成功加载的，这种方式可以间接捕获到资源加载错误。</p>\\n</blockquote>\\n<p>这种方式非常有用，一定要记住。</p>\\n<p>**方式3；**Error事件捕获。</p>\\n<blockquote>\\n<p>源加载错误，虽然会阻止冒泡，但是不会阻止捕获。我们可以在捕获阶段绑定<code>error</code>事件。例如：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/31.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>**总结：**如果我们能回答出后面的两种方式，面试官对我们的印象会大大增加。既可以体现出我们对错误监控的了解，还可以体现出我们对事件模型的掌握。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>线上错误全是 <code>Script error.</code>，怎么办？</strong></p>\\n<p>说明报错来自跨域加载的脚本。给 <code>&lt;script&gt;</code> 标签加 <code>crossorigin=&quot;anonymous&quot;</code>，同时让静态资源 <code>CDN</code> 返回 <code>Access-Control-Allow-Origin</code>，两者都做到才能拿到真实的 <code>message</code> 和堆栈。</p>\\n</li>\\n<li>\\n<p><strong><code>window.onerror</code> 和 <code>addEventListener(\'error\')</code> 该用哪个？</strong></p>\\n<p>两者都能抓同步运行错误，但只有 <code>addEventListener</code> 支持捕获阶段，所以资源错误必须用它。<code>window.onerror</code> 的独门优势是返回 <code>true</code> 可以阻止错误继续打到控制台。实践中两个一起注册，资源错误那条走捕获阶段并判断 <code>event.target</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_4-错误上报的两种方式\\"><a class=\\"header-anchor\\" href=\\"#_4-错误上报的两种方式\\">#</a> 4 错误上报的两种方式</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>图片打点（<code>new Image().src = url</code>）：没有跨域限制、不用配 <code>CORS</code>，但只能 <code>GET</code>，数据受 <code>URL</code> 长度限制</li>\\n<li><code>navigator.sendBeacon()</code>：浏览器在后台异步发送，页面关掉也能送达，最适合卸载时上报；只能 <code>POST</code>、拿不到响应、有体积上限</li>\\n<li>第三条路是 <code>fetch(..., { keepalive: true })</code>：最灵活，能带自定义头也能读响应，但要配 <code>CORS</code></li>\\n<li>上报必须做采样和去重，否则一个死循环就能把日志打爆，也会拖慢用户页面</li>\\n<li>上报代码本身要用 <code>try/catch</code> 包住：监控挂了不能把业务带崩</li>\\n<li>配合 <code>sourcemap</code> 才有意义，否则线上只有 <code>a.js:1:2345</code> 这种没法看的堆栈</li>\\n</ul>\\n<p><strong>上报选型看两点：要不要跨域配置，以及页面会不会当场关掉</strong> —— 按「图片打点 → <code>sendBeacon</code> → <code>keepalive</code>，再补采样、去重、<code>sourcemap</code> 三件工程事」讲。</p>\\n</section>\\n<ul>\\n<li><strong>方式一</strong>：采用Ajax通信的方式上报（此方式虽然可以上报错误，但是我们并不采用这种方式）</li>\\n<li>**方式二：**利用Image对象上报（推荐。网站的监控体系都是采用的这种方式）</li>\\n</ul>\\n<blockquote>\\n<p>方式二的实现方式如下：</p>\\n</blockquote>\\n<pre class=\\"language-html\\"><code>&lt;!DOCTYPE html&gt;\\n&lt;html lang=&quot;en&quot;&gt;\\n&lt;head&gt;\\n    &lt;meta charset=&quot;UTF-8&quot;&gt;\\n    &lt;title&gt;Title&lt;/title&gt;\\n&lt;/head&gt;\\n&lt;body&gt;\\n\\n&lt;script&gt;\\n\\t//通过Image对象进行错误上报\\n    (new Image()).src = &#39;http://blog.com/myPath?badjs=msg&#39;;   // myPath表示上报的路径（我要上报到哪里去）。后面的内容是自己加的参数。\\n&lt;/script&gt;\\n&lt;/body&gt;\\n&lt;/html&gt;\\n\\n</code></pre>\\n<blockquote>\\n<p>打开浏览器，效果如下：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/32.png\\" alt=\\"\\"></p>\\n<p>上图中，红色那一栏表明，我的请求已经发出去了。点进去看看：</p>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/33.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>这种方式，不需要借助第三方的库，一行代码即可搞定。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>用户点了「退出」马上关标签页，这时候的上报经常丢，怎么修？</strong></p>\\n<p>用 <code>navigator.sendBeacon</code>，它把请求交给浏览器进程在后台发，页面销毁也不影响。监听时机选 <code>visibilitychange</code> 变为 <code>hidden</code>，不要用 <code>unload</code> —— 移动端和 <code>bfcache</code> 场景下 <code>unload</code> 经常不触发。</p>\\n</li>\\n<li>\\n<p><strong>上报量太大服务端扛不住，你会怎么削？</strong></p>\\n<p>三板斧：按错误指纹（<code>message</code> 加堆栈首帧）去重，同一个错误一个会话只报一次并带计数；按用户或会话做采样，比如只留 <code>10%</code>；把多条错误攒成一批延迟发送，页面隐藏时用 <code>sendBeacon</code> 冲刷剩余队列。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"十、http协议\\"><a class=\\"header-anchor\\" href=\\"#十、http协议\\">#</a> 十、HTTP协议</h2>\\n<blockquote>\\n<p>一面中，如果有笔试，考HTTP协议的可能性较大。</p>\\n</blockquote>\\n<h3 id=\\"_1-前言-5\\"><a class=\\"header-anchor\\" href=\\"#_1-前言-5\\">#</a> 1 前言</h3>\\n<p>一面要讲的内容：</p>\\n<ul>\\n<li><code>HTTP</code>协议的主要特点</li>\\n<li><code>HTTP</code>报文的组成部分</li>\\n<li><code>HTTP</code>方法</li>\\n<li><code>get</code> 和 <code>post</code>的区别</li>\\n<li><code>HTTP</code>状态码</li>\\n<li>什么是持久连接</li>\\n<li>什么是管线化</li>\\n</ul>\\n<p>二面要讲的内容；</p>\\n<ul>\\n<li>缓存</li>\\n<li><code>CSRF</code>攻击</li>\\n</ul>\\n<h3 id=\\"_2-http协议的主要特点\\"><a class=\\"header-anchor\\" href=\\"#_2-http协议的主要特点\\">#</a> 2 HTTP协议的主要特点</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>四个特点：简单快速、灵活、无连接、无状态；记不住至少要说出后两个</li>\\n<li>无连接说的是「每次连接只处理一个请求、处理完就断开」，这是 <code>HTTP/1.0</code> 的行为，<code>HTTP/1.1</code> 默认 <code>keep-alive</code> 已经改掉了</li>\\n<li>无状态指服务端不保存请求之间的关联，靠 cookie / <code>session</code> / <code>token</code> 在应用层补状态</li>\\n<li>灵活体现在 <code>Content-Type</code> 可以传任意类型数据，头字段也可以扩展</li>\\n<li>说完四点最好主动补版本演进：<code>HTTP/1.1</code> 持久连接、<code>HTTP/2</code> 多路复用与头部压缩、<code>HTTP/3</code> 换 <code>QUIC</code> 解决 <code>TCP</code> 队头阻塞</li>\\n<li>别把「无连接」说成「不需要连接」，<code>HTTP</code> 始终跑在 <code>TCP</code>（或 <code>QUIC</code>）之上</li>\\n</ul>\\n<p><strong>四个特点里真正要讲透的是「无连接」和「无状态」，而且要说清它们各自是怎么被后来的技术弥补的</strong> —— 按「四点 → 无连接被 <code>keep-alive</code> 改写 → 无状态被 cookie / <code>token</code> 补上」讲。</p>\\n</section>\\n<ul>\\n<li>简单快速</li>\\n<li>灵活</li>\\n<li><strong>无连接</strong></li>\\n<li><strong>无状态</strong></li>\\n</ul>\\n<blockquote>\\n<p>通常我们要答出以上四个内容。如果实在记不住，一定要记得后面的两个：<strong>无连接、无状态</strong>。</p>\\n</blockquote>\\n<p>我们分别来解释一下。</p>\\n<h4 id=\\"_2-1-简单快速\\"><a class=\\"header-anchor\\" href=\\"#_2-1-简单快速\\">#</a> 2.1 简单快速</h4>\\n<blockquote>\\n<p><strong>简单</strong>：每个资源（比如图片、页面）都通过 url 来定位。这都是固定的，在<code>http</code>协议中，处理起来也比较简单，想访问什么资源，直接输入url即可。</p>\\n</blockquote>\\n<h4 id=\\"_2-2-灵活\\"><a class=\\"header-anchor\\" href=\\"#_2-2-灵活\\">#</a> 2.2 灵活</h4>\\n<blockquote>\\n<p><code>http</code>协议的头部有一个<code>数据类型</code>，通过<code>http</code>协议，就可以完成不同数据类型的传输。</p>\\n</blockquote>\\n<h4 id=\\"_2-3-无连接\\"><a class=\\"header-anchor\\" href=\\"#_2-3-无连接\\">#</a> 2.3 无连接</h4>\\n<blockquote>\\n<p>连接一次，就会断开，不会继续保持连接。</p>\\n</blockquote>\\n<h4 id=\\"_2-4-无状态\\"><a class=\\"header-anchor\\" href=\\"#_2-4-无状态\\">#</a> 2.4 无状态</h4>\\n<blockquote>\\n<p>客户端和服务器端是两种身份。第一次请求结束后，就断开了，第二次请求时，<strong>服务器端并没有记住之前的状态</strong>，也就是说，服务器端无法区分客户端是否为同一个人、同一个身份。</p>\\n</blockquote>\\n<blockquote>\\n<p>有的时候，我们访问网站时，网站能记住我们的账号，这个是通过其他的手段（比如 <code>session</code>）做到的，并不是<code>http</code>协议能做到的。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>既然 <code>HTTP</code> 是无状态的，登录态是怎么保持的？</strong></p>\\n<p>在应用层补：服务端下发 <code>session id</code> 存进 cookie，或者下发 <code>token</code> 由前端存起来每次带上。协议本身仍然无状态，这也正是服务端能水平扩容的前提 —— 状态放在 cookie、<code>Redis</code> 或 <code>token</code> 里，不绑在某条连接上。</p>\\n</li>\\n<li>\\n<p><strong>「无连接」和 <code>HTTP/1.1</code> 的 <code>keep-alive</code> 矛盾吗？</strong></p>\\n<p>不矛盾，是历史表述。无连接描述的是 <code>HTTP/1.0</code> 的默认行为：一次请求响应完就断开。<code>HTTP/1.1</code> 默认开启持久连接，一条 <code>TCP</code> 上可以串行跑多个请求，但请求之间服务端依然不保存任何状态，所以「无状态」这条始终成立。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-http报文的组成部分\\"><a class=\\"header-anchor\\" href=\\"#_3-http报文的组成部分\\">#</a> 3 HTTP报文的组成部分</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>请求报文四段：请求行（方法 + <code>URL</code> + 版本）、请求头、空行、请求体</li>\\n<li>响应报文四段：状态行（版本 + 状态码 + 状态短语）、响应头、空行、响应体</li>\\n<li>那个空行是硬边界：解析器读到连续的 <code>CRLF</code> 才切换去读正文，头里出现裸换行会导致请求走私</li>\\n<li>正文长度由 <code>Content-Length</code> 或 <code>Transfer-Encoding: chunked</code> 决定，两者不能同时可信</li>\\n<li><code>GET</code> 语法上允许带请求体，但规范没有定义它的语义，很多中间件会直接丢掉，所以别用</li>\\n<li><code>HTTP/2</code> 之后改成二进制分帧、头部用 <code>HPACK</code> 压缩，但语义层还是这四段</li>\\n</ul>\\n<p><strong>请求和响应都是「起始行 + 头部 + 空行 + 正文」四段，空行是区分头和体的硬边界</strong> —— 再补一句 <code>HTTP/2</code> 换成了二进制分帧但语义没变。</p>\\n</section>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/34.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>在回答此问题时，我们要按照顺序回答：</p>\\n</blockquote>\\n<ul>\\n<li>先回答的是，<code>http</code>报文包括：<strong>请求报文</strong>和<strong>响应报文</strong>。</li>\\n<li>再回答的是，每个报文包含什么部分。</li>\\n<li>最后回答，每个部分的内容是什么</li>\\n</ul>\\n<h4 id=\\"_3-1-请求报文包括\\"><a class=\\"header-anchor\\" href=\\"#_3-1-请求报文包括\\">#</a> 3.1 请求报文包括：</h4>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/35.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>请求行：包括请求方法、请求的<code>url</code>、<code>http</code>协议及版本。</li>\\n<li>请求头：一大堆的键值对。</li>\\n<li><strong>空行</strong>指的是：当服务器在解析请求头的时候，如果遇到了空行，则表明，后面的内容是请求体</li>\\n<li>请求体：数据部分。</li>\\n</ul>\\n<h4 id=\\"_3-2-响应报文包括\\"><a class=\\"header-anchor\\" href=\\"#_3-2-响应报文包括\\">#</a> 3.2 响应报文包括：</h4>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/36.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>状态行：<code>http</code>协议及版本、状态码及状态描述。</li>\\n<li>响应头</li>\\n<li>空行</li>\\n<li>响应体</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>抓包看到同时有 <code>Content-Length</code> 和 <code>Transfer-Encoding: chunked</code>，以哪个为准？</strong></p>\\n<p>规范上不允许同时生效，同时出现时应以 <code>Transfer-Encoding</code> 为准，稳妥的实现会直接拒绝该请求。前后两台服务器对这条规则理解不一致，正是 <code>HTTP</code> 请求走私攻击的成因。</p>\\n</li>\\n<li>\\n<p><strong>为什么说 <code>HTTP/2</code> 之后「报文」这个词要改口？</strong></p>\\n<p>因为 <code>HTTP/2</code> 把消息拆成了带流 <code>ID</code> 的二进制帧（<code>HEADERS</code>、<code>DATA</code> 等）在连接上传输，不再是可读的文本行。但语义模型没变 —— 方法、路径、头字段、正文都还在，只是编码方式换了，所以面试里按四段回答语义结构仍然正确。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_4-http方法\\"><a class=\\"header-anchor\\" href=\\"#_4-http方法\\">#</a> 4 HTTP方法</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>至少说全五个：<code>GET</code> 获取、<code>POST</code> 提交、<code>PUT</code> 整体更新、<code>DELETE</code> 删除、<code>HEAD</code> 只要响应头</li>\\n<li>两个定义先背下来：安全 = 不改服务端状态；幂等 = 执行 1 次和 <code>N</code> 次最终状态相同</li>\\n<li><code>GET</code> / <code>HEAD</code> 安全且幂等；<code>PUT</code> / <code>DELETE</code> 幂等但不安全；<code>POST</code> 两者都不是</li>\\n<li><code>PATCH</code> 是局部更新且不保证幂等，<code>PUT</code> 整体覆盖所以幂等，这是两者的关键区别</li>\\n<li><code>OPTIONS</code> 用于查询服务端支持的能力，跨域预检用的就是它</li>\\n<li>工程价值：只有幂等的方法，网关和客户端才敢自动重试</li>\\n</ul>\\n<p><strong>安全看改不改服务端状态，幂等看做 <code>N</code> 次等不等于做 1 次</strong> —— 按「五个方法 → 两个定义 → 幂等决定能不能自动重试」讲。</p>\\n</section>\\n<p>包括：</p>\\n<ul>\\n<li><code>GET</code>：获取资源</li>\\n<li><code>POST</code>：传输资源</li>\\n<li><code>put</code>：更新资源</li>\\n<li><code>DELETE</code>：删除资源</li>\\n<li><code>HEAD</code>：获得报文首部</li>\\n</ul>\\n<blockquote>\\n<p><code>HTTP</code>方法有很多，但是上面这五个方法，要求在面试时全部说出来，不要漏掉。</p>\\n</blockquote>\\n<ul>\\n<li><code>get</code> <code>和</code>post` 比较常见。</li>\\n<li><code>put</code> 和 <code>delete</code> 在实际应用中用的很少。况且，业务中，一般不删除服务器端的资源。</li>\\n<li><code>head</code> 可能偶尔用的到。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>DELETE</code> 第一次返回 200、第二次返回 404，响应都不一样了，还叫幂等吗？</strong></p>\\n<p>叫。幂等看的是服务端最终状态，不是响应码。第一次删完资源就不存在了，之后再删它依然不存在，状态没有继续变化。</p>\\n</li>\\n<li>\\n<p><strong>下单页用户网络卡时连点两次，出了两个订单，前端能兜住吗？</strong></p>\\n<p>兜不住。按钮置灰只是体验层，刷新和网络层重试都能绕过去。可靠做法是前端生成一个幂等键放在请求头里，后端拿它去重 —— 这正是「只有幂等的接口才敢自动重试」的实际落地。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_5-get-和-post的区别\\"><a class=\\"header-anchor\\" href=\\"#_5-get-和-post的区别\\">#</a> 5 get 和 post的区别</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>语义是根本：<code>GET</code> 安全幂等、可缓存、可被浏览器预取；<code>POST</code> 不幂等，回退时浏览器会提示重新提交</li>\\n<li>参数位置：<code>GET</code> 在 <code>URL</code> 上（进历史记录、服务器日志、<code>CDN</code> 缓存键），<code>POST</code> 在请求体里</li>\\n<li>缓存：<code>GET</code> 会被浏览器和代理主动缓存，<code>POST</code> 默认不缓存</li>\\n<li>「<code>GET</code> 有 <code>2KB</code> 长度限制」不是 <code>HTTP</code> 规范规定的，是浏览器和服务器对 <code>URL</code> 长度的实现限制</li>\\n<li>「<code>POST</code> 更安全」只是相对的：参数不在 <code>URL</code> 上而已，明文照样能抓包，真正的保护是 <code>HTTPS</code></li>\\n<li>「<code>POST</code> 发两次包」说的是跨域非简单请求的 <code>OPTIONS</code> 预检，和方法本身无关</li>\\n</ul>\\n<p><strong>别背八条差异，抓住一条：<code>GET</code> 是读、<code>POST</code> 是写，其它差异都是这条语义的推论</strong> —— 再主动澄清长度限制和安全性这两个常见误解。</p>\\n</section>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/37.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>区别有很多，如果记不住，面试时，至少要任意答出其中的三四条。</li>\\n<li>有一点要强调，<strong>get是相对不隐私的，而post是相对隐私的</strong>。</li>\\n</ul>\\n<blockquote>\\n<p>我们大概要记住以下几点：</p>\\n</blockquote>\\n<ol>\\n<li>浏览器在回退时，<code>get</code> <strong>不会重新请求</strong>，但是<code>post</code>会重新请求。【重要】</li>\\n<li><code>get</code>请求会被浏览器<strong>主动缓存</strong>，而<code>post</code>不会。【重要】</li>\\n<li><code>get</code>请求的参数，会报<strong>保留</strong>在浏览器的<strong>历史记录</strong>里，而<code>post</code>不会。做业务时要注意。为了防止<code>CSRF</code>攻击，很多公司把<code>get</code>统一改成了<code>post</code>。</li>\\n<li><code>get</code>请求在<code>url</code>中<code>传递的参数有大小限制，基本是</code>2kb`，不同的浏览器略有不同。而post没有注意。</li>\\n<li><code>get</code>的参数是直接暴露在<code>url</code>上的，相对不安全。而<code>post</code>是放在请求体中的。</li>\\n</ol>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>公司为了防 <code>CSRF</code> 把所有 <code>GET</code> 都改成了 <code>POST</code>，你怎么看？</strong></p>\\n<p>方向不对。<code>POST</code> 挡不住跨站表单自动提交，防 <code>CSRF</code> 要靠 <code>Token</code> 和 <code>SameSite</code>。而且把读接口改成 <code>POST</code> 会丢掉浏览器缓存、<code>CDN</code> 缓存和可重试性，收益为负。真正该做的是把「有副作用的 <code>GET</code>」改成 <code>POST</code>。</p>\\n</li>\\n<li>\\n<p><strong>搜索页参数太多，<code>URL</code> 被服务端截断了，怎么办？</strong></p>\\n<p>先确认是哪一层的限制（浏览器、<code>Nginx</code> 的请求头缓冲、网关）。能压缩就压缩参数，比如用短 key、把多选项编码成位图；实在放不下再改成 <code>POST</code> 查询接口，但要接受失去缓存的代价。折中办法是先把查询条件存一次拿到 <code>queryId</code>，再用 <code>GET</code> 拉结果，缓存和分享都还在。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_6-http状态码\\"><a class=\\"header-anchor\\" href=\\"#_6-http状态码\\">#</a> 6 http状态码</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>五类先背：<code>1xx</code> 信息、<code>2xx</code> 成功、<code>3xx</code> 重定向、<code>4xx</code> 客户端错误、<code>5xx</code> 服务端错误</li>\\n<li>常考的：<code>200</code>、<code>204</code> 无内容、<code>206</code> 范围请求（视频拖进度条）、<code>301</code> 永久重定向、<code>302</code> 临时、<code>304</code> 命中协商缓存</li>\\n<li><code>400</code> 参数错、<code>401</code> 未认证（该去登录）、<code>403</code> 已认证但没权限、<code>404</code> 资源不存在、<code>405</code> 方法不允许、<code>429</code> 被限流</li>\\n<li><code>500</code> 服务端异常、<code>502</code> 网关拿到无效响应、<code>503</code> 服务不可用、<code>504</code> 网关超时</li>\\n<li><code>301</code> 会被浏览器长期缓存，配错了很难回滚，不确定就先用 <code>302</code></li>\\n<li><code>304</code> 不是错误：它表示「你的缓存还能用」，响应没有 body，所以特别快</li>\\n</ul>\\n<p><strong>按五类分档，再挑三对容易混的讲透：<code>301</code> / <code>302</code>、<code>401</code> / <code>403</code>、<code>502</code> / <code>504</code></strong> —— 说清每一对的判断依据，比背一长串号码有用。</p>\\n</section>\\n<blockquote>\\n<p><code>http</code>状态码分类：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/38.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>常见的<code>http</code>状态码：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/39.png\\" alt=\\"\\"></p>\\n<p><strong>部分解释</strong>：</p>\\n<ul>\\n<li><code>206</code>的应用：<code>range</code>指的是请求的范围，客户端只请求某个大文件里的一部分内容。比如说，如果播放视频地址或音频地址的前面一部分，可以用到<code>206</code>。</li>\\n<li><code>301</code>：重定向（永久）。</li>\\n<li><code>302</code>：重定向（临时）。</li>\\n<li><code>304</code>：我这个服务器告诉客户端，你已经有缓存了，不需要从我这里取了。</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/07/40.png\\" alt=\\"\\"></p>\\n<ul>\\n<li><code>400</code>和<code>401</code>用的不多,未授权。<code>403</code>指的是请求被拒绝。<code>404</code>指的是资源不存在。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>线上把某个页面配成了 <code>301</code> 跳转，改回来后老用户还是跳，怎么办？</strong></p>\\n<p><code>301</code> 会被浏览器长期缓存，而且很多实现不看缓存头。补救办法是让旧地址继续返回 <code>301</code>、但指向新的正确地址（再跳一次），同时配 <code>Cache-Control: no-store</code> 慢慢稀释。这也是为什么不确定时先上 <code>302</code>。</p>\\n</li>\\n<li>\\n<p><strong>接口偶发 <code>504</code>，运维说服务是好的，你怎么定位？</strong></p>\\n<p><code>504</code> 是网关等上游超时，说明请求到了网关但后端没在时限内回。先看网关超时配置和后端 <code>P99</code> 耗时，再看是不是某个慢查询或下游依赖抖动。和 <code>502</code> 要区分开 —— <code>502</code> 是上游返回了无效响应，通常是进程崩了或协议不匹配。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_7-持久链接-http长连接\\"><a class=\\"header-anchor\\" href=\\"#_7-持久链接-http长连接\\">#</a> 7 持久链接/http长连接</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>HTTP/1.0</code> 每个请求都要重新三次握手，<code>HTTP/1.1</code> 默认 <code>Connection: keep-alive</code> 复用同一条 <code>TCP</code></li>\\n<li>收益是省掉每次建连的 <code>RTT</code> 和 <code>TCP</code> 慢启动，<code>HTTPS</code> 还能省掉一次 <code>TLS</code> 握手</li>\\n<li>复用是串行的：一条连接上仍然一问一答，所以浏览器对同一域名会开 6 个左右的并发连接</li>\\n<li><code>Keep-Alive</code> 不是永久：有 <code>timeout</code> 和 <code>max</code> 上限，服务端和中间代理都会回收空闲连接</li>\\n<li>每个请求仍要各自发一整套请求头，头部冗余问题要到 <code>HTTP/2</code> 的 <code>HPACK</code> 才解决</li>\\n<li>别和 <code>WebSocket</code> 混：<code>keep-alive</code> 只是复用连接，仍是客户端发起的一问一答，服务端不能主动推</li>\\n</ul>\\n<p><strong>持久连接解决的是「每次请求都重新建连」的浪费，不是「并发」</strong> —— 按「<code>1.0</code> 每次重连 → <code>1.1</code> 默认复用 → 仍然串行，所以才有域名分片和 <code>HTTP/2</code> 多路复用」讲。</p>\\n</section>\\n<blockquote>\\n<p>如果你能答出持久链接，这是面试官很想知道的一个点。</p>\\n</blockquote>\\n<ul>\\n<li><strong>轮询</strong>：<code>http1.0</code>中，客户端每隔很短的时间，都会对服务器发出请求，查看是否有新的消息，只要轮询速度足够快，例如<code>1</code>秒，就能给人造成交互是实时进行的印象。这种做法是无奈之举，实际上对服务器、客户端双方都造成了大量的性能浪费。</li>\\n<li><strong>长连接</strong>：<code>HTTP1.1</code>中，通过使用<code>Connection:keep-alive</code>进行长连接，。客户端只请求一次，但是服务器会将继续保持连接，当再次请求时，避免了重新建立连接。</li>\\n</ul>\\n<blockquote>\\n<p>注意，<code>HTTP 1.1</code>默认进行持久连接。在一次 <code>TCP</code> 连接中可以完成多个 <code>HTTP</code> 请求，但是对<strong>每个请求仍然要单独发 header</strong>，<code>Keep-Alive</code>不会永久保持连接，它有一个保持时间，可以在不同的服务器软件（如<code>Apache</code>）中设定这个时间。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>既然有了 <code>keep-alive</code>，为什么还要做域名分片（把静态资源拆到多个子域名）？</strong></p>\\n<p>因为持久连接是串行复用，浏览器对单域名的并发连接数有上限（通常 6 个）。分成多个域名就能拿到更多并发。但这是 <code>HTTP/1.1</code> 时代的优化，<code>HTTP/2</code> 多路复用之后应该反过来收敛域名 —— 分片会白白增加 <code>DNS</code>、建连和握手成本。</p>\\n</li>\\n<li>\\n<p><strong><code>keep-alive</code> 和 <code>WebSocket</code> 有什么区别？</strong></p>\\n<p><code>keep-alive</code> 只是复用 <code>TCP</code> 连接，通信模型还是客户端请求、服务端响应，服务端不能主动发消息；<code>WebSocket</code> 借一次 <code>HTTP</code> 握手升级成全双工协议，之后双方都能随时发帧。需要服务端推送就必须用 <code>WebSocket</code> 或 <code>SSE</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_8-长连接中的管线化\\"><a class=\\"header-anchor\\" href=\\"#_8-长连接中的管线化\\">#</a> 8 长连接中的管线化</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>管线化是 <code>HTTP/1.1</code> 的设计：不等响应就在同一条连接上连续发多个请求</li>\\n<li>它失败了，主流浏览器默认关闭，原因有三条</li>\\n<li>第一，响应必须按请求顺序返回，第一个慢就堵住后面全部，这就是应用层队头阻塞</li>\\n<li>第二，只对幂等请求安全，<code>POST</code> 不能管线化（重发会有副作用）</li>\\n<li>第三，很多代理和老服务器的实现有 <code>bug</code>，浏览器为稳妥干脆不开</li>\\n<li>它想解决的「一条连接上并发多请求」，直到 <code>HTTP/2</code> 的多路复用才真正实现；<code>TCP</code> 层的队头阻塞则要靠 <code>HTTP/3</code> 的 <code>QUIC</code></li>\\n</ul>\\n<p><strong>管线化是个「看起来能提速、实际因队头阻塞和兼容性而失败」的方案</strong> —— 按「它想干什么 → 三条失败原因 → 最终由 <code>HTTP/2</code> 多路复用解决」讲，能自然带出下一个考点。</p>\\n</section>\\n<blockquote>\\n<p>如果能答出<strong>管线化</strong>，则属于加分项。</p>\\n</blockquote>\\n<h4 id=\\"_8-1-管线化的原理\\"><a class=\\"header-anchor\\" href=\\"#_8-1-管线化的原理\\">#</a> 8.1 管线化的原理</h4>\\n<blockquote>\\n<p>长连接时，<strong>默认</strong>的请求这样的：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>请求1 --&gt; 响应1 --&gt;请求2 --&gt; 响应2 --&gt; 请求3 --&gt; 响应3\\n</code></pre>\\n<blockquote>\\n<p>管线化就是，我把现在的请求打包，一次性发过去，你也给我一次响应回来。</p>\\n</blockquote>\\n<h4 id=\\"_8-2-管线化的注意事项\\"><a class=\\"header-anchor\\" href=\\"#_8-2-管线化的注意事项\\">#</a> 8.2 管线化的注意事项</h4>\\n<p>管线化（HTTP pipelining）虽然能把多个请求一次性打包发出，但有几个致命限制，导致它<strong>实际上从未被广泛采用、现代浏览器基本都默认关闭甚至移除了它</strong>：</p>\\n<ol>\\n<li><strong>响应必须按请求顺序返回</strong>：服务端要严格按请求发来的顺序返回响应，不能乱序。这带来最大的问题——<strong>队头阻塞（Head-of-Line Blocking）</strong>：如果第一个请求的响应很慢，后面即使已经处理好也必须排队等它，整体被拖慢。</li>\\n<li><strong>只对幂等请求安全</strong>：规范要求只对 GET/HEAD 等幂等请求做管线化，POST 等不能管线化（重发会有副作用）。</li>\\n<li><strong>中间代理/服务器支持不一致</strong>：很多代理和老服务器对管线化的实现有 bug,容易出错,所以浏览器为稳妥干脆不开。</li>\\n</ol>\\n<p><strong>所以管线化是个「看起来能提速、实际因队头阻塞和兼容性问题而失败」的方案。</strong> 它想解决的「一个连接上并发多请求」的问题,直到 <strong>HTTP/2 的多路复用</strong>才真正解决——HTTP/2 用二进制分帧,多个请求在一个连接上并发、响应可以乱序返回(用 stream id 标识),彻底消除了应用层队头阻塞(但 TCP 层队头阻塞仍在,由 HTTP/3 的 QUIC 解决)。</p>\\n<blockquote>\\n<p><strong>面试怎么答</strong>:「管线化是 HTTP/1.1 想在一个连接上并发多请求的早期尝试,但因为响应必须按序返回导致队头阻塞、且只能用于幂等请求、代理兼容性差,实际基本没被采用;这个需求最终由 HTTP/2 的多路复用真正解决。」——比「没研究过」有价值得多,还能自然带出 HTTP/2 多路复用这个高频考点。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>HTTP/2</code> 的多路复用为什么就不会队头阻塞了？</strong></p>\\n<p>因为它把消息拆成带 <code>Stream ID</code> 的二进制帧，多个请求的帧可以交错传输、响应可以乱序返回，接收端按 <code>ID</code> 重组，应用层的队头阻塞就此消失。但底层还是有序的 <code>TCP</code> 字节流，一旦丢包所有流都得等重传 —— 这个 <code>TCP</code> 层队头阻塞要靠 <code>HTTP/3</code> 的 <code>QUIC</code> 才解决。</p>\\n</li>\\n<li>\\n<p><strong>面试官问「你在项目里用过管线化吗」，怎么答比较真诚？</strong></p>\\n<p>直接说没有，主流浏览器默认关闭它，业务里遇不到。然后把话题引到真正做过的优化上：合并请求、开启 <code>HTTP/2</code>、收敛域名、用 <code>preconnect</code> 提前建连。承认没用过再给替代方案，比硬编经历可信得多。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"十一、css-独立自检题\\"><a class=\\"header-anchor\\" href=\\"#十一、css-独立自检题\\">#</a> 十一、CSS 独立自检题</h2>\\n<p>这一章由原“其他问题”页的 CSS 清单拆分而来，是快速自检用的短题；盒模型、外边距重叠和 <code>BFC</code> 的完整推演在「五、CSS盒模型及BFC」，布局选型在「六、页面布局」，想深挖回那两章看。</p>\\n<h3 id=\\"_1-css-选择器有哪些-优先级怎么计算\\"><a class=\\"header-anchor\\" href=\\"#_1-css-选择器有哪些-优先级怎么计算\\">#</a> 1 CSS 选择器有哪些，优先级怎么计算</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>常见选择器分为元素、类/属性/伪类、<code>ID</code>、组合器和伪元素；通配符与组合器本身不增加权重</li>\\n<li>优先级按 <code>(内联, ID, 类/属性/伪类, 元素/伪元素)</code> 四列从左到右比较，不是简单相加</li>\\n<li><code>!important</code> 先进入单独的优先层，再在同层内比较来源、级联层、权重和源码顺序</li>\\n<li><code>:where()</code> 权重恒为零；<code>:is()</code>、<code>:not()</code>、<code>:has()</code> 取参数列表中最高的权重</li>\\n<li>同权重时后声明胜出，但层叠来源、<code>@layer</code> 和重要性都在选择器权重之前判断</li>\\n</ul>\\n<p><strong>优先级不是背一个三位数，而是先走层叠规则，再逐列比较选择器权重</strong> —— 按「来源与重要性 → 级联层 → 权重 → 源码顺序」讲。</p>\\n</section>\\n<p>选择器首先决定“哪些元素能匹配”，级联算法再决定多个匹配声明中谁生效。常见选择器包括类型选择器 <code>div</code>、类选择器 <code>.card</code>、<code>ID</code> 选择器 <code>#app</code>、属性选择器 <code>[disabled]</code>、伪类 <code>:hover</code>、伪元素 <code>::before</code>，以及后代、子代、相邻兄弟和通用兄弟组合器。</p>\\n<p>计算时可以把权重记作四列：内联样式、<code>ID</code>、类/属性/伪类、元素/伪元素。<code>#app .card p</code> 是 <code>(0,1,1,1)</code>，会胜过 <code>.page .card p</code> 的 <code>(0,0,2,1)</code>；不要把它们转成十进制后相加，因为任意数量的类也不会“进位”成一个 <code>ID</code>。现代 CSS 还要留意 <code>:where()</code> 的零权重，以及 <code>@layer</code> 会先改变普通声明的层叠顺序。工程上优先用低权重、可组合的类，并通过级联层管理第三方样式；长期堆叠 <code>!important</code> 会让覆盖关系越来越难维护。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>组件库的样式总把业务类覆盖掉，你会直接再加一个 <code>!important</code> 吗？</strong></p>\\n<p>我会先在开发者工具的 <code>Computed</code> 面板确认输在来源、级联层还是权重。可控项目优先把第三方 CSS 放进较低优先级的 <code>@layer</code>，或提高业务规则的作用域；只有必须覆盖内联重要声明等极端场景才局部使用 <code>!important</code>。</p>\\n</li>\\n<li>\\n<p><strong>你在代码评审里看到 <code>#app .page .card .title</code>，为什么会拦？</strong></p>\\n<p>它把组件绑定到页面结构和 <code>ID</code>，复用时很难覆盖。更稳妥的是单一语义类或组件作用域类，让权重保持低且稳定，再靠源码组织或 <code>@layer</code> 解决顺序。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_2-什么是-bfc-有什么作用-如何形成\\"><a class=\\"header-anchor\\" href=\\"#_2-什么是-bfc-有什么作用-如何形成\\">#</a> 2 什么是 BFC，有什么作用，如何形成</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>BFC</code> 是块级格式化上下文，内部块盒按自己的布局规则参与排版，并与外部浮动和布局相对隔离</li>\\n<li>它能包含内部浮动、阻止外部浮动侵入，也能让相邻布局区域互不重叠</li>\\n<li>常见触发方式有 <code>display: flow-root</code>、非 <code>visible/clip</code> 的 <code>overflow</code>、浮动、绝对定位和部分布局容器</li>\\n<li>清浮动首选语义明确且副作用小的 <code>display: flow-root</code>，不要为了触发 <code>BFC</code> 随手裁剪内容</li>\\n<li><code>BFC</code> 不能解决所有外边距问题：父子外边距是否折叠还取决于边框、内边距和格式化上下文边界</li>\\n</ul>\\n<p><strong><code>BFC</code> 可以理解为一块独立的块布局区域，价值是把浮动、尺寸计算和相邻区域隔离开</strong> —— 按「定义 → 三个作用 → <code>flow-root</code> 实践」讲。</p>\\n</section>\\n<p>浏览器布局不是把所有块盒放在同一个全局规则里计算，而是建立多个格式化上下文。处在同一个 <code>BFC</code> 内的块级盒通常沿块轴排列；内部浮动参与该上下文高度计算，外部浮动也不会侵入一个新的 <code>BFC</code>。因此，父元素只有浮动子元素时设置 <code>display: flow-root</code>，父元素就能重新包住子元素；两栏布局中让右栏建立 <code>BFC</code>，它也不会覆盖左侧浮动栏。</p>\\n<p>过去常用 <code>overflow: hidden</code> 触发 <code>BFC</code>，但它同时会裁剪阴影、下拉菜单和超出内容。<code>display: flow-root</code> 直接表达“建立新的块格式化上下文”，副作用更少。<code>flex</code>、<code>grid</code> 的子项也会建立相应格式化上下文，但它们解决的是不同布局模型，不能只为了清浮动就把容器改成 <code>flex</code>。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>卡片里的下拉菜单被裁掉了，同事说这是用 <code>overflow: hidden</code> 清浮动的代价，你怎么改？</strong></p>\\n<p>把清浮动改为 <code>display: flow-root</code>，恢复默认溢出行为。它同样让容器包含浮动子项，却不会顺便裁剪弹层和阴影。</p>\\n</li>\\n<li>\\n<p><strong>一个左侧头像浮动、右侧正文自适应的评论项，正文为什么会钻到头像下面？</strong></p>\\n<p>普通块盒会与浮动发生环绕。让正文建立新的 <code>BFC</code>，例如 <code>display: flow-root</code> 或使用更合适的 <code>flex</code> 两栏布局，就能让正文区域避开浮动边界；新项目通常直接选 <code>flex</code>，因为结构意图更清楚。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-position-有哪些值-各自相对谁定位\\"><a class=\\"header-anchor\\" href=\\"#_3-position-有哪些值-各自相对谁定位\\">#</a> 3 position 有哪些值，各自相对谁定位</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>static</code> 走正常文档流，偏移属性不生效；<code>relative</code> 保留原占位并相对自身原位置偏移</li>\\n<li><code>absolute</code> 脱离常规流，相对最近的定位包含块计算；找不到时通常落到初始包含块</li>\\n<li><code>fixed</code> 通常相对视口固定，但祖先的 <code>transform</code>、<code>filter</code>、<code>perspective</code> 等可能改写它的包含块</li>\\n<li><code>sticky</code> 是相对定位与滚动约束的组合，需要偏移阈值，并受最近滚动容器和容器边界限制</li>\\n<li>定位解决坐标与覆盖关系，不应替代正常的一维或二维布局；常规排版优先 <code>flex</code> / <code>grid</code></li>\\n</ul>\\n<p><strong>定位题的关键不是枚举五个值，而是说清是否脱离文档流、占位是否保留、包含块是谁</strong> —— 按这三个问题逐个比较。</p>\\n</section>\\n<p><code>position</code> 的常用值是 <code>static</code>、<code>relative</code>、<code>absolute</code>、<code>fixed</code> 和 <code>sticky</code>。<code>relative</code> 常用于给绝对定位子元素建立包含块，它自身仍保留原来的布局空间；<code>absolute</code> 和 <code>fixed</code> 通常脱离常规流，不再为兄弟元素留位置。绝对定位元素的包含块并不只是“最近的 <code>position: relative</code> 祖先”，现代规范下部分 <code>transform</code>、<code>contain</code> 等属性也能建立包含块。</p>\\n<p><code>sticky</code> 只有在指定 <code>top</code>、<code>bottom</code> 等阈值后才会表现为吸附，并且不能越过自身容器的边界。最常见的失效原因是祖先意外形成了滚动容器、容器没有足够滚动距离，或伸展布局让粘性元素与容器等高。工程排障时应在开发者工具中逐层检查 <code>overflow</code> 和包含块，而不是盲目提高 <code>z-index</code>。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>弹窗里的 <code>position: fixed</code> 遮罩为什么只覆盖了一个卡片，没有铺满视口？</strong></p>\\n<p>先查卡片祖先是否设置了 <code>transform</code>、<code>filter</code> 或 <code>perspective</code>，这些属性可能让它成为固定定位的包含块。把遮罩通过 portal 挂到页面顶层，或移除不必要的包含块属性，通常比继续调宽高可靠。</p>\\n</li>\\n<li>\\n<p><strong>吸顶标题写了 <code>position: sticky; top: 0</code> 仍然不动，你怎么排查？</strong></p>\\n<p>从它向上找最近的滚动容器，检查祖先的 <code>overflow</code>、容器高度和是否有实际滚动距离，再看 <code>flex/grid</code> 拉伸是否让元素没有可移动空间。<code>z-index</code> 只影响覆盖顺序，不会让吸附机制生效。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_4-flex-布局有什么好处\\"><a class=\\"header-anchor\\" href=\\"#_4-flex-布局有什么好处\\">#</a> 4 Flex 布局有什么好处</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Flexbox</code> 是一维布局模型，擅长在一个主轴上分配剩余空间并控制交叉轴对齐</li>\\n<li>容器负责方向、换行、整体分布；项目负责增长、收缩、基准尺寸和单项对齐</li>\\n<li>它适合导航、工具栏、卡片行和未知尺寸居中，能减少浮动、行内块和负边距技巧</li>\\n<li><code>flex: 1</code> 常等价于可增长、可收缩且基准接近零；内容不肯收缩时要检查 <code>min-width: auto</code></li>\\n<li>二维行列强关联的页面骨架优先 <code>Grid</code>，不要把所有布局都塞进 <code>Flexbox</code></li>\\n</ul>\\n<p><strong><code>Flexbox</code> 的优势是让容器统一分配一条轴上的空间与对齐，项目只声明自己如何伸缩</strong> —— 按「主轴 → 交叉轴 → 伸缩 → 适用边界」讲。</p>\\n</section>\\n<p>传统布局依赖浮动、<code>inline-block</code> 或手算宽度时，未知尺寸、垂直居中和剩余空间分配都很麻烦。<code>display: flex</code> 把这些决策提升到容器：<code>justify-content</code> 管主轴分布，<code>align-items</code> 管交叉轴对齐，<code>gap</code> 管项目间距，子项再通过 <code>flex-grow</code>、<code>flex-shrink</code> 和 <code>flex-basis</code> 决定如何分享空间。</p>\\n<p>一个常见坑是文本列写了 <code>flex: 1</code> 仍把容器撑破，因为 flex 项目的默认最小尺寸是内容最小宽度；给该项设置 <code>min-width: 0</code> 才允许长文本真正收缩。另一个边界是 <code>Flexbox</code> 主要控制一维关系，如果每一行的列都要严格对齐，应使用 <code>Grid</code>，否则换行后各行会独立分配空间。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品卡片左边固定图片、右边标题很长并需要省略，为什么右栏 <code>flex: 1</code> 仍然溢出？</strong></p>\\n<p>右栏默认有 <code>min-width: auto</code>，最小宽度受内容约束，不能缩到容器内。给右栏加 <code>min-width: 0</code>，再对文本设置省略，才能让 flex 收缩与文本截断同时生效。</p>\\n</li>\\n<li>\\n<p><strong>后台表格头部有四列，第二行也必须与第一行列线严格对齐，你还会用可换行的 <code>Flexbox</code> 吗？</strong></p>\\n<p>不会。这里是二维对齐约束，<code>Grid</code> 的显式列轨道更合适；<code>Flexbox</code> 换行后每行独立计算，难保证跨行列线一致。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_5-介绍一下-css-盒模型\\"><a class=\\"header-anchor\\" href=\\"#_5-介绍一下-css-盒模型\\">#</a> 5 介绍一下 CSS 盒模型</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>每个盒子由内容区、内边距、边框和外边距组成；外边距不计入元素自身可见尺寸</li>\\n<li><code>content-box</code> 下声明的宽高只管内容区，实际占用还要加内边距和边框</li>\\n<li><code>border-box</code> 下声明宽高包含内容、内边距和边框，更适合组件与响应式布局</li>\\n<li>行内非替换元素的宽高行为与块盒不同；图片、表单控件等替换元素还有自身尺寸规则</li>\\n<li>排障时用 <code>getBoundingClientRect()</code> 看边框盒，用计算样式拆分各层，不凭肉眼猜</li>\\n</ul>\\n<p><strong>盒模型就是浏览器把一个元素拆成内容、内边距、边框、外边距四层，<code>box-sizing</code> 决定声明宽高量哪一层</strong> —— 先画四层，再比较两种尺寸算法。</p>\\n</section>\\n<p>默认 <code>box-sizing: content-box</code> 时，一个元素声明 <code>width: 200px; padding: 20px; border: 2px solid</code>，边框盒宽度会变成 <code>244px</code>。改为 <code>border-box</code> 后，<code>200px</code> 已包含左右内边距和边框，内容宽度由浏览器反推为 <code>156px</code>。因此项目通常在全局设置 <code>*, *::before, *::after { box-sizing: border-box; }</code>，让组件尺寸更可预测。</p>\\n<p>外边距描述盒子与外界的间隔，不属于元素背景和点击区域，还可能发生折叠。<code>getBoundingClientRect()</code> 返回的是变换后的边框盒几何信息；<code>offsetWidth</code> 通常是整数布局尺寸；计算样式能读取各层声明。理解这些差异能解释“明明写了 100% 为什么还横向溢出”——在 <code>content-box</code> 下额外的内边距和边框会叠加到百分比宽度之外。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>输入框写了 <code>width: 100%</code>，加左右内边距后为什么把表单撑出了容器？</strong></p>\\n<p>因为它仍是 <code>content-box</code>，<code>100%</code> 只计算内容区，内边距和边框继续向外增加。改成 <code>box-sizing: border-box</code>，或在全局统一盒模型即可。</p>\\n</li>\\n<li>\\n<p><strong>设计稿标注卡片宽 <code>320px</code>，你怎么验证浏览器最终是不是 <code>320px</code>？</strong></p>\\n<p>先确认设计标的是内容盒还是边框盒，再用 <code>getBoundingClientRect().width</code> 验证实际边框盒；同时在计算样式里检查 <code>box-sizing</code>、内边距和边框，避免只看声明的 <code>width</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_6-有哪些方式可以使-div-居中\\"><a class=\\"header-anchor\\" href=\\"#_6-有哪些方式可以使-div-居中\\">#</a> 6 有哪些方式可以使 div 居中</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>水平块级居中可用确定宽度配 <code>margin-inline: auto</code>，文本内容用 <code>text-align: center</code></li>\\n<li>未知尺寸的水平垂直居中首选 <code>display: grid; place-items: center</code> 或 <code>Flexbox</code></li>\\n<li>绝对定位可用 <code>inset: 50% auto auto 50%</code> 配 <code>transform: translate(-50%, -50%)</code></li>\\n<li>单行文本可用 <code>line-height</code>，但多行、字体缩放和国际化下不可靠</li>\\n<li>先问清“谁在谁里面居中、单轴还是双轴、尺寸是否已知”，再选方案</li>\\n</ul>\\n<p><strong>居中没有唯一答案，先确定轴、尺寸和是否脱离文档流，再选 <code>margin</code>、<code>Flexbox</code>、<code>Grid</code> 或定位</strong> —— 面试时用场景而不是背十种技巧。</p>\\n</section>\\n<p>对于普通块元素，已知宽度时 <code>margin-inline: auto</code> 能分配左右剩余空间；对于容器内任意尺寸的子元素，<code>Grid</code> 的 <code>place-items: center</code> 最简洁，<code>Flexbox</code> 的 <code>justify-content</code> 与 <code>align-items</code> 更适合还要控制兄弟元素排列的场景。绝对定位加百分比与 <code>transform</code> 适合浮层，但会脱离文档流，不能用来承担普通页面排版。</p>\\n<p>“垂直居中”常失败是因为容器没有可计算高度。百分比高度依赖包含块的明确高度，而 <code>min-height: 100vh</code>、<code>100dvh</code> 在移动端的浏览器工具栏语境也不同。工程上还要考虑内容变长、系统字体放大和安全区；方案必须在内容换行后仍成立，不能只对一行中文有效。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>登录页在手机浏览器里用 <code>height: 100vh</code> 居中，地址栏收起时为什么会跳？</strong></p>\\n<p>传统 <code>vh</code> 对动态浏览器工具栏的处理可能与视觉视口不同。现代浏览器可用 <code>100dvh</code> 跟随动态视口，并保留内容溢出和安全区处理；低版本再提供 <code>min-height</code> 回退。</p>\\n</li>\\n<li>\\n<p><strong>一个弹窗标题可能换成三行英文，你还会用固定 <code>line-height</code> 做垂直居中吗？</strong></p>\\n<p>不会。固定行高只适合单行且高度固定的文本，多行会破坏排版。弹窗主体使用 <code>Grid</code> 或 <code>Flexbox</code> 对齐，内容高度由文本自然撑开更稳妥。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_7-css-相关的性能优化有哪些\\"><a class=\\"header-anchor\\" href=\\"#_7-css-相关的性能优化有哪些\\">#</a> 7 CSS 相关的性能优化有哪些</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>优先减少无用 CSS 和阻塞首屏的样式体积，再讨论选择器微优化；网络与解析成本通常更可观</li>\\n<li>避免布局抖动：批量读写布局属性，不在循环里交替读尺寸、写样式</li>\\n<li>动画优先使用 <code>transform</code> 和 <code>opacity</code>，但不要无节制创建合成层</li>\\n<li>用 <code>content-visibility</code>、虚拟列表等减少不可见区域的布局与绘制，使用前评估可访问性和占位尺寸</li>\\n<li>性能优化必须用 <code>Performance</code>、渲染面板和真实指标验证，不能只靠“少写嵌套选择器”猜测</li>\\n</ul>\\n<p><strong>CSS 性能先看加载体积，再看布局与绘制，最后用工具验证；选择器通常不是第一瓶颈</strong> —— 按「下载解析 → 样式计算 → 布局 → 绘制合成」排查。</p>\\n</section>\\n<p>CSS 会参与关键渲染路径：体积过大或首屏引入大量未使用规则，会延迟样式树构建。工程上可按路由拆分样式、清除可靠识别的无用规则、压缩产物，并把首屏真正需要的样式优先交付。运行阶段则要避免频繁修改影响大范围布局的属性，尤其是在循环中读取 <code>offsetHeight</code> 后立即写 <code>style</code>，这可能强制浏览器同步完成布局。</p>\\n<p>动画使用 <code>transform</code> 与 <code>opacity</code> 往往能跳过布局和大部分绘制，交给合成阶段处理；但 <code>will-change</code> 会占用显存，只应在即将动画的少量元素上短期使用。长页面可以考虑 <code>content-visibility: auto</code>，同时用 <code>contain-intrinsic-size</code> 预留估算空间以减少滚动跳动。最终应在真实设备上记录样式计算、布局、绘制时间和 <code>Core Web Vitals</code>，确认改动确实改善用户体验。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>列表滚动时掉帧，你看到样式表有一万行，会先删 CSS 吗？</strong></p>\\n<p>我会先录制性能轨迹，确认主线程时间花在样式计算、布局、绘制还是脚本。若瓶颈是两千个 DOM 节点反复布局，虚拟列表和减少布局读写更有效；样式体积主要影响加载，不能凭文件行数判断运行时瓶颈。</p>\\n</li>\\n<li>\\n<p><strong>同事给页面上所有卡片都加了 <code>will-change: transform</code>，你会保留吗？</strong></p>\\n<p>不会。它可能让大量元素提前提升为合成层，增加显存和合成成本。只对即将动画、且性能数据证明需要的少量元素启用，动画结束后移除。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_8-双飞翼布局和圣杯布局有什么区别\\"><a class=\\"header-anchor\\" href=\\"#_8-双飞翼布局和圣杯布局有什么区别\\">#</a> 8 双飞翼布局和圣杯布局有什么区别</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>两者都是三栏布局的历史方案：中间栏优先出现在源码中，两侧固定，中间自适应</li>\\n<li>都依赖浮动与负外边距把左右栏拉到同一行，主要差别在“给中间内容腾空间”的位置</li>\\n<li>圣杯布局在外层容器加左右内边距，再相对定位两侧栏；双飞翼在中间栏内部增加包裹并设左右外边距</li>\\n<li>圣杯结构更少但窄屏下计算更脆弱；双飞翼多一层元素但通常不移动侧栏</li>\\n<li>现代项目优先使用 <code>Grid</code> 三列或 <code>Flexbox</code>，面试要说明历史价值与替代方案</li>\\n</ul>\\n<p><strong>两者目标相同，区别是圣杯给容器留内边距，双飞翼给中间内容包裹留外边距</strong> —— 最后补一句现代三栏首选 <code>Grid</code>。</p>\\n</section>\\n<p>圣杯布局与双飞翼布局诞生于 <code>Flexbox</code>、<code>Grid</code> 普及之前。它们把中间内容放在 DOM 前面，使主要内容优先下载和解析，再让三栏全部浮动。左右栏借助负外边距移动到中间栏两侧，中间栏宽度保持自适应。</p>\\n<p>圣杯布局在三栏父容器上设置左右 <code>padding</code>，再用相对定位把左右栏推进预留区域；当容器小于侧栏宽度组合时容易出现错位。双飞翼让中间栏内部多一层 <code>.main-inner</code>，通过它的左右 <code>margin</code> 为侧栏腾位置，因此不用相对定位侧栏。今天可以直接写 <code>grid-template-columns: 200px minmax(0, 1fr) 240px</code>，语义和边界都更清楚；维护老项目时仍需识别两种历史结构。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>维护十年前的活动页时，左右栏突然掉到下一行，你怎么判断是哪种布局并排查？</strong></p>\\n<p>先看中间栏内部是否有专门留外边距的包裹：有则多半是双飞翼；若父容器有左右内边距且侧栏相对定位，则是圣杯。再检查容器最小宽度、侧栏宽度和负外边距是否仍满足原公式。</p>\\n</li>\\n<li>\\n<p><strong>新做管理台三栏骨架，你会为了“中间内容优先”继续写双飞翼吗？</strong></p>\\n<p>不会。源码顺序仍可把主体放前面，再用 <code>Grid</code> 的命名区域或列轨道布局；这样无需浮动、负外边距和额外修正，响应式也更容易表达。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_9-浮动元素会造成什么影响-如何清除浮动\\"><a class=\\"header-anchor\\" href=\\"#_9-浮动元素会造成什么影响-如何清除浮动\\">#</a> 9 浮动元素会造成什么影响，如何清除浮动</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>浮动元素脱离常规块布局，但文字和行内内容会环绕它；它不是完全脱离所有排版影响</li>\\n<li>父元素只有浮动子项时可能高度塌陷，后续块盒会上移，背景和边框也包不住内容</li>\\n<li>推荐给父容器使用 <code>display: flow-root</code> 建立新的 <code>BFC</code></li>\\n<li>传统 <code>.clearfix::after</code> 仍适合维护旧代码；额外空标签和固定高度都不理想</li>\\n<li>新布局优先 <code>Flexbox</code> / <code>Grid</code>，浮动主要保留给图文环绕语义</li>\\n</ul>\\n<p><strong>浮动会让元素退出常规块流但仍参与文字环绕，清理的本质是让父容器重新包含浮动</strong> —— 首选 <code>flow-root</code>，旧项目再用 clearfix。</p>\\n</section>\\n<p><code>float</code> 最初用于图片与文字环绕。浮动盒会向行首或行尾移动，普通块盒可能延伸到它下面，而行内内容会绕开它。若父元素没有其他正常流内容，它的自动高度不会按浮动子元素撑开，于是出现背景消失、边框高度为零、后续内容上移等“高度塌陷”现象。</p>\\n<p>现代修复是父元素设置 <code>display: flow-root</code>，它建立新的 <code>BFC</code> 并包含内部浮动。旧代码常见 <code>.clearfix::after { content: \'\'; display: table; clear: both; }</code>，也能在浮动之后插入一个清除点。<code>overflow: hidden</code> 虽然常常有效，却可能裁剪弹层；给父元素写死高度则无法适应内容变化。若需求本质是左右排列，应迁移到 <code>Flexbox</code> 或 <code>Grid</code>。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>新闻详情页确实需要图片左浮、正文环绕，你会把浮动全部改成 <code>Flexbox</code> 吗？</strong></p>\\n<p>不会。图文环绕正是 <code>float</code> 的原生语义，保留更自然；只需让文章片段在结束处正确清除浮动，避免影响后续章节。</p>\\n</li>\\n<li>\\n<p><strong>父卡片背景高度为零，但不能裁剪内部下拉菜单，你怎么修？</strong></p>\\n<p>给父卡片加 <code>display: flow-root</code>。它能包含浮动子项，又不会像 <code>overflow: hidden</code> 一样裁剪下拉菜单。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_10-css-样式隔离有哪些手段\\"><a class=\\"header-anchor\\" href=\\"#_10-css-样式隔离有哪些手段\\">#</a> 10 CSS 样式隔离有哪些手段</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>命名约定如 <code>BEM</code> 成本最低，但依赖团队纪律，只降低冲突概率而非真正隔离</li>\\n<li><code>CSS Modules</code> 在构建期改写类名，适合组件级样式；它不隔离标签选择器、全局变量和继承</li>\\n<li><code>Shadow DOM</code> 提供真实的样式边界，外部选择器默认进不去，但主题、弹层和第三方组件集成更复杂</li>\\n<li>微前端可用前缀、构建时作用域或 <code>Shadow DOM</code>，还要处理运行时插入样式和公共弹层</li>\\n<li>隔离与主题能力存在取舍，应先明确要防命名冲突、继承污染还是第三方全局样式</li>\\n</ul>\\n<p><strong>样式隔离分“降低冲突概率、构建期改名、浏览器原生边界”三档</strong> —— 分别对应命名规范、<code>CSS Modules</code>、<code>Shadow DOM</code>。</p>\\n</section>\\n<p>样式冲突可能来自类名重名、过宽的标签选择器、继承属性、全局 CSS 变量以及运行时动态插入的样式。<code>BEM</code>、统一前缀适合简单项目，但任何人仍能写出覆盖全站的规则。<code>CSS Modules</code> 将本地类名编译成唯一标识，能解决大多数组件类名冲突；<code>scoped</code> CSS 也常通过属性选择器实现类似效果，但深度选择器和传送到根节点的弹层需要额外处理。</p>\\n<p><code>Shadow DOM</code> 是浏览器提供的封装边界，外部普通规则不会匹配内部节点，内部样式也不会泄漏出去。代价是全局主题、字体继承、测试选择器和组件库 portal 需要专门设计，可通过 CSS 自定义属性、<code>::part</code> 等暴露受控扩展点。微前端选型时还要验证子应用运行时注入的 <code>&lt;style&gt;</code>、模态框挂载位置和样式卸载，不能只给静态 CSS 加前缀。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>两个微前端都用了 <code>.ant-btn</code>，上线后样式串了，你只加应用前缀够吗？</strong></p>\\n<p>还不够。要检查组件库运行时样式插入位置、弹层 portal 是否挂到主应用根节点，以及子应用卸载时样式是否清理。前缀能处理静态选择器，运行时样式还需配置容器或采用更强的隔离边界。</p>\\n</li>\\n<li>\\n<p><strong>设计系统需要宿主自由换主题，你会直接把组件全部封进封闭的 <code>Shadow DOM</code> 吗？</strong></p>\\n<p>要谨慎。可以用 CSS 自定义属性传递主题，并用 <code>::part</code> 暴露有限部位；如果现有组件库高度依赖全局样式和 portal，<code>CSS Modules</code> 加受控主题变量可能集成成本更低。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_11-行内元素和块级元素有哪些-行为有什么不同\\"><a class=\\"header-anchor\\" href=\\"#_11-行内元素和块级元素有哪些-行为有什么不同\\">#</a> 11 行内元素和块级元素有哪些，行为有什么不同</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>块级盒通常在块轴上独占一行并尽量撑满可用宽度；行内盒参与当前行盒排版</li>\\n<li>常见块级元素有 <code>div</code>、<code>p</code>、标题、列表和区块元素；常见行内元素有 <code>span</code>、<code>a</code>、<code>strong</code>、<code>em</code></li>\\n<li>元素的 HTML 语义与 CSS 外部显示类型不是一回事，任何元素都可通过 <code>display</code> 改变布局角色</li>\\n<li>非替换行内元素设置宽高通常不按块盒方式生效，垂直外边距也不会推动上下行盒</li>\\n<li><code>img</code>、<code>input</code> 等替换元素虽可在行内排版，却有固有尺寸和不同的宽高行为</li>\\n</ul>\\n<p><strong>先区分 HTML 语义，再讨论 CSS 生成什么盒；“行内还是块级”不是元素永远不变的身份</strong> —— 用默认行为、可修改性和替换元素边界展开。</p>\\n</section>\\n<p>HTML 元素带有默认 UA 样式，所以 <code>div</code> 通常生成块级盒，<code>span</code> 通常生成行内盒。块级盒参与块格式化上下文，通常从新行开始；行内盒被放进行盒中，随文字换行。通过 <code>display: block</code>、<code>inline</code>、<code>inline-block</code>、<code>flex</code> 或 <code>grid</code> 可以改变生成盒的类型，但不应为了视觉效果滥用错误语义，例如把可点击操作写成 <code>div</code> 会丢失键盘与可访问性行为。</p>\\n<p>普通行内元素的 <code>width</code>、<code>height</code> 不按块盒方式控制，左右内外边距可见，垂直内边距可能绘制却不一定撑开行高。<code>inline-block</code> 则在外部参与行内排版、内部按块容器计算，并能设置宽高。图片和表单控件属于替换元素，浏览器用外部资源或控件替换其内容，具有固有尺寸，因此不能套用“行内元素都不能设宽高”的口诀。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审里有人为了让链接独占一行，把 <code>&lt;a&gt;</code> 改成 <code>&lt;div&gt;</code>，你会怎么改？</strong></p>\\n<p>保留 <code>&lt;a&gt;</code> 的链接语义和键盘行为，只设置 <code>display: block</code>。HTML 负责语义，CSS 负责盒类型，不应为布局牺牲可访问性。</p>\\n</li>\\n<li>\\n<p><strong>为什么图片底部常有几像素空隙，改成 <code>display: block</code> 就没了？</strong></p>\\n<p>图片默认作为行内替换元素按文本基线对齐，行盒会为字母下行部预留空间。可以按需求设置 <code>vertical-align</code>，或在确实不需要行内排版时改成块级。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_12-css3-有哪些重要新特性\\"><a class=\\"header-anchor\\" href=\\"#_12-css3-有哪些重要新特性\\">#</a> 12 CSS3 有哪些重要新特性</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>“CSS3” 已不再是单一整体版本，现代 CSS 由选择器、颜色、布局、变换等模块独立演进</li>\\n<li>高频能力包括圆角、阴影、渐变、自定义字体、变换、过渡、动画和媒体查询</li>\\n<li>布局主线是 <code>Flexbox</code>、<code>Grid</code>、多列与逻辑属性，响应式还可用容器查询</li>\\n<li>现代能力还有 CSS 自定义属性、<code>@supports</code>、<code>:has()</code>、级联层和原生嵌套</li>\\n<li>回答时要区分已稳定能力与渐进增强功能，并说明通过兼容表和特性查询确认支持范围</li>\\n</ul>\\n<p><strong>不要把 CSS3 背成 2010 年的功能清单，现代 CSS 是按模块演进的能力集合</strong> —— 按「视觉 → 布局 → 响应式 → 级联与工程化」归类回答。</p>\\n</section>\\n<p>过去面试中的“CSS3 新特性”通常指相对 CSS 2.1 新增的 <code>border-radius</code>、<code>box-shadow</code>、渐变、<code>@font-face</code>、<code>transform</code>、<code>transition</code>、<code>animation</code> 和媒体查询。今天更完整的回答应补上 <code>Flexbox</code>、<code>Grid</code>、自定义属性、逻辑属性、容器查询、<code>:has()</code>、<code>@layer</code> 等，同时说明 CSS 标准早已拆分为独立模块，不存在所有能力一起发布的单一“CSS3 版本”。</p>\\n<p>工程采用新特性时要看目标浏览器、失败时是否仍可用，以及构建工具能否安全降级。媒体查询可按视口响应，容器查询按组件所处空间响应，更适合可复用组件；自定义属性在运行时参与级联，和预处理器变量不是一回事。对于不支持也不影响核心流程的装饰能力，可以渐进增强；关键布局则要准备回退或通过 <code>@supports</code> 分支。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>需求说“必须兼容公司内嵌的旧 WebView”，你会直接上容器查询吗？</strong></p>\\n<p>先拿真实 UA 和版本查兼容性，再做最小可用回退。可以默认使用普通流或媒体查询，在 <code>@supports (container-type: inline-size)</code> 内增强为容器查询，保证不支持时功能仍可用。</p>\\n</li>\\n<li>\\n<p><strong>同事说 Sass 变量已经能换主题，为什么还需要 CSS 自定义属性？</strong></p>\\n<p>Sass 变量在构建时被替换，运行时不存在；CSS 自定义属性参与继承与级联，可以在不重新构建的情况下按主题、组件树或用户设置动态切换。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_13-层叠上下文是什么\\"><a class=\\"header-anchor\\" href=\\"#_13-层叠上下文是什么\\">#</a> 13 层叠上下文是什么</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>层叠上下文是一组元素在 Z 轴上的独立排序空间，内部子元素不会跨出去和外部兄弟直接比较</li>\\n<li>根元素、定位且 <code>z-index</code> 非 <code>auto</code>、<code>fixed/sticky</code>、透明度小于一、变换和隔离等都可能创建它</li>\\n<li><code>z-index</code> 很大仍被遮住，通常是祖先层叠上下文整体排在别人下面</li>\\n<li>同一上下文内还要按背景、负层级、块盒、浮动、行内内容、定位元素等绘制顺序判断</li>\\n<li>弹层优先通过 portal 进入统一顶层容器，并建立可维护的层级 token，而不是无限加数字</li>\\n</ul>\\n<p><strong><code>z-index</code> 只在当前层叠上下文里比较；子元素再大也翻不过祖先所在的层级</strong> —— 先找上下文，再比同层顺序。</p>\\n</section>\\n<p>层叠上下文类似一个局部坐标系：内部元素先排出顺序，整个上下文再作为一个整体参与父上下文排序。例如 A 的 <code>z-index: 1</code>，B 的 <code>z-index: 2</code>，即使 A 的子元素写 <code>z-index: 9999</code>，它仍不能盖过 B，因为 A 整体先输给了 B。除了定位与 <code>z-index</code>，<code>opacity: .99</code>、<code>transform</code>、<code>filter</code>、<code>isolation: isolate</code>、部分 <code>contain</code> 和 <code>will-change</code> 也可能创建新上下文。</p>\\n<p>排障时从被遮挡元素向上检查每个祖先是否创建层叠上下文，再与遮挡元素的祖先树比较，不要只盯着当前节点。模态框、气泡和通知适合通过 portal 挂到统一 overlay 根节点，并用有限的语义层级管理；这还能避免被局部 <code>overflow</code> 裁剪。层叠上下文解决的是绘制顺序，不会改变 DOM 语义和焦点顺序。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>Tooltip 写了 <code>z-index: 999999</code> 仍被顶部导航盖住，你怎么定位？</strong></p>\\n<p>检查 Tooltip 祖先是否因 <code>transform</code>、<code>opacity</code> 或定位创建了较低的层叠上下文。若是，把 Tooltip portal 到统一浮层根节点，并比较两个顶层上下文，而不是继续增加内部数字。</p>\\n</li>\\n<li>\\n<p><strong>把弹窗 portal 到 <code>body</code> 后视觉层级正常了，还要检查什么？</strong></p>\\n<p>还要处理焦点锁定、关闭后焦点归还、背景不可交互和语义属性。视觉置顶不等于可访问的模态交互，键盘与读屏顺序也必须正确。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_14-重排和重绘是什么-有什么区别\\"><a class=\\"header-anchor\\" href=\\"#_14-重排和重绘是什么-有什么区别\\">#</a> 14 重排和重绘是什么，有什么区别</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>重排也叫布局：几何信息变化后，浏览器重新计算元素尺寸与位置，可能影响后代和周围元素</li>\\n<li>重绘是像素外观变化后重新绘制，未必需要重新布局；颜色和阴影通常属于这一类</li>\\n<li>合成只重新组合已有图层，<code>transform</code>、<code>opacity</code> 动画常有机会停留在这一阶段</li>\\n<li>交替读取布局属性并写入样式可能触发强制同步布局，形成布局抖动</li>\\n<li>优化要先用性能面板确认 <code>Layout</code>、<code>Paint</code>、<code>Composite</code> 的实际耗时和影响范围</li>\\n</ul>\\n<p><strong>重排重新算几何，重绘重新画像素，合成重新组图层；成本通常依次降低但不是绝对</strong> —— 用渲染流水线和实际测量回答。</p>\\n</section>\\n<p>当元素宽高、字体、内容、窗口尺寸或布局关系改变时，浏览器可能把相关节点标记为需要布局，并在合适时机重新计算几何信息，这就是重排。背景色、文字颜色等不改变几何的属性通常只需重绘。若元素已在独立合成层，修改 <code>transform</code> 或 <code>opacity</code> 可能只需合成线程更新图层位置或透明度，但提升图层本身也有内存和管理成本。</p>\\n<p>浏览器会批量处理样式修改；问题出在代码写入样式后立刻读取 <code>offsetWidth</code>、<code>getBoundingClientRect()</code> 等布局相关数据，为返回最新值，浏览器可能被迫同步完成前面的样式计算和布局。循环中反复“写—读—写—读”就形成布局抖动。应把读取集中在前、写入集中在后，或使用 <code>requestAnimationFrame</code> 分帧调度，并以性能录制验证。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个 500 行表格逐行修改宽度并读取 <code>offsetHeight</code>，页面卡顿，你怎么改？</strong></p>\\n<p>先批量读取所需尺寸，再统一写样式，避免每一行都强制同步布局；如果节点量本身过大，再用虚拟列表减少参与布局的元素。改完用 Performance 轨迹确认 <code>Layout</code> 次数和耗时下降。</p>\\n</li>\\n<li>\\n<p><strong>把 <code>left</code> 动画改成 <code>transform</code> 就一定更快吗？</strong></p>\\n<p>通常更容易只走合成，但不是无条件保证。元素过多、图层过大或绘制内容频繁变化时仍可能昂贵；要看图层、绘制和合成数据，而不是把属性名当性能结论。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_15-动画性能如何优化\\"><a class=\\"header-anchor\\" href=\\"#_15-动画性能如何优化\\">#</a> 15 动画性能如何优化</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>优先动画 <code>transform</code> 和 <code>opacity</code>，避免每帧修改宽高、位置和会扩散布局的属性</li>\\n<li>动画前后控制图层生命周期，<code>will-change</code> 只短期用于少量已验证的热点元素</li>\\n<li>用 <code>requestAnimationFrame</code> 驱动必须由 JS 计算的视觉更新，并在页面不可见或组件卸载时停止</li>\\n<li>降低单帧工作量：减少大面积绘制、滤镜、阴影和同时动画的节点数量</li>\\n<li>尊重 <code>prefers-reduced-motion</code>，为用户提供无位移或关闭动画的路径</li>\\n</ul>\\n<p><strong>动画优化的目标是让每帧尽量只做合成，并把主线程工作稳定压进帧预算</strong> —— 按「属性选择 → 图层 → 调度 → 测量 → 无障碍」讲。</p>\\n</section>\\n<p>流畅动画需要在每一帧内完成脚本、样式计算、布局、绘制和合成。<code>60Hz</code> 屏幕一帧约 <code>16.7ms</code>，实际留给业务的时间更少；高刷新率设备预算还会缩短。位移、缩放尽量用 <code>transform</code>，淡入淡出用 <code>opacity</code>，可以避免反复改变几何。复杂阴影、模糊滤镜和大面积透明层可能带来高额绘制或合成开销，也要实际检查。</p>\\n<p>CSS 动画适合声明式、可由浏览器优化的过渡；需要逐帧计算时使用 <code>requestAnimationFrame</code>，不要用固定间隔假设屏幕刷新率。动画元素离开视口、页面隐藏或组件卸载时应暂停并清理调度。通过 <code>prefers-reduced-motion: reduce</code> 关闭非必要位移和连续运动，不仅是体验加分，也是无障碍要求。最后用 Performance 与 Rendering 面板检查长帧、绘制区域和图层数量。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>大屏看板同时让 200 个数字卡片闪烁，低配机器掉到 20 帧，你会先加 <code>will-change</code> 吗？</strong></p>\\n<p>不会。先减少同时动画的节点和大面积绘制，合并更新并停掉视口外动画；给 200 个节点升层可能让显存与合成压力更糟。只有轨迹证明某个热点适合独立合成时才局部使用。</p>\\n</li>\\n<li>\\n<p><strong>产品坚持保留强烈的页面切换动画，但用户开启了“减少动态效果”，怎么办？</strong></p>\\n<p>在 <code>prefers-reduced-motion: reduce</code> 下改为短时淡入或直接切换，保留状态反馈但移除大幅位移、缩放和循环动画。这个偏好应被视为用户需求，而不是可忽略的性能开关。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"十二、react-状态更新独立题\\"><a class=\\"header-anchor\\" href=\\"#十二、react-状态更新独立题\\">#</a> 十二、React 状态更新独立题</h2>\\n<p>以下三题是原自检页中真正需要就地作答的内容，按 <code>React 17</code> 及以前、<code>React 18</code> 与 <code>React 19</code> 的版本边界重新整理。</p>\\n<h3 id=\\"_1-setstate-是同步还是异步\\"><a class=\\"header-anchor\\" href=\\"#_1-setstate-是同步还是异步\\">#</a> 1 setState 是同步还是异步</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>setState</code> 调用会同步创建更新并入队，但读取到新状态要等 React 完成调度与提交，因此不能简单说“它是异步函数”</li>\\n<li><code>React 17</code> 及以前只在 React 管理的事件与生命周期中默认批处理，定时器、原生事件常逐次刷新</li>\\n<li><code>React 18</code> 使用 <code>createRoot</code> 后启用自动批处理，同一任务中的 Promise、定时器和原生事件更新也会合并</li>\\n<li><code>React 19</code> 延续自动批处理，旧的 <code>ReactDOM.render</code> 已移除；必须同步提交时才考虑 <code>flushSync</code></li>\\n<li>新值依赖旧值时始终使用函数式更新，不依赖“这次看起来同步”的运行环境</li>\\n</ul>\\n<p><strong><code>setState</code> 是同步入队、调度后提交；是否批处理取决于 React 版本和根节点模式</strong> —— 按「调用语义 → 版本分界 → <code>flushSync</code> 边界」讲。</p>\\n</section>\\n<p>状态更新不是把 <code>this.state</code> 当普通对象立即改写。调用 <code>setState</code> 时，React 创建更新记录、标记对应 Fiber 的更新优先级并安排渲染；组件能读到新值，要等更新被处理并提交。<code>React 17</code> 及以前通过是否处于 React 自己的批处理调用栈判断是否合并，所以 <code>setTimeout</code> 和原生事件里的更新常表现为逐次同步刷新。<code>React 18</code> 的 <code>createRoot</code> 开启自动批处理后，这些来源在同一任务内也通常合并。</p>\\n<p>批处理的目标是减少重复渲染并保持中间状态不泄露。若第三方 DOM 集成要求更新后立刻读取布局，可把最小范围更新包进 <code>flushSync</code>，但它会强制提前完成工作，频繁使用会损害并发调度与性能。更常见的做法是把依赖提交结果的副作用放进生命周期或 <code>useLayoutEffect</code> / <code>useEffect</code>，不要把“立即读到新 state”当默认保证。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>埋点 SDK 要在点击后立刻读取更新后的 DOM 尺寸，你会把整个点击处理器都放进 <code>flushSync</code> 吗？</strong></p>\\n<p>不会，只把必须先提交的那一次最小状态更新包进去，然后读取尺寸。更优先评估能否在 <code>useLayoutEffect</code> 中读取，避免把无关更新一起强制同步提交。</p>\\n</li>\\n<li>\\n<p><strong>老项目从 <code>ReactDOM.render</code> 迁到 <code>createRoot</code> 后，两次更新只触发一次渲染，这是 bug 吗？</strong></p>\\n<p>通常不是，而是 <code>React 18</code> 自动批处理范围扩大。要检查业务是否错误依赖了两次提交之间的中间 DOM；如果依赖，应重构状态与副作用，极少数集成场景再局部使用 <code>flushSync</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_2-为什么两次-setstate-有时只更新一次\\"><a class=\\"header-anchor\\" href=\\"#_2-为什么两次-setstate-有时只更新一次\\">#</a> 2 为什么两次 setState 有时只更新一次</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>两次对象式更新若都从同一旧快照计算，后一个结果会覆盖前一个，看起来只增加一次</li>\\n<li>函数式更新按队列顺序接收前一次计算结果，连续两次 <code>prev =&gt; prev + 1</code> 才稳定增加两次</li>\\n<li>批处理决定“何时统一渲染”，快照与更新队列决定“最终算出什么值”，这两件事不能混为一谈</li>\\n<li>函数组件每次渲染的状态是固定快照，事件闭包里的变量不会在调用 setter 后原地变化</li>\\n<li>只要下一状态依赖上一状态，就用函数式更新；这在并发渲染下也是正确写法</li>\\n</ul>\\n<p><strong>丢更新不是 React 少执行了一次，而是两次更新都拿同一个旧快照算出了同一个目标值</strong> —— 对比对象式与函数式更新队列即可说明。</p>\\n</section>\\n<p>类组件连续执行 <code>this.setState({ count: this.state.count + 1 })</code> 两次时，两次表达式可能都读取本轮尚未提交的旧 <code>count</code>。假设旧值为 <code>0</code>，队列里进入的是两个“把 count 设为 1”的更新，浅合并后结果当然仍是 <code>1</code>。改成 <code>this.setState(prev =&gt; ({ count: prev.count + 1 }))</code>，React 处理队列时会把第一项结果交给第二项，因此得到 <code>2</code>。</p>\\n<p>函数组件同理：<code>setCount(count + 1)</code> 捕获的是本次渲染快照，连写两次仍提交相同值；<code>setCount(prev =&gt; prev + 1)</code> 才把计算交给更新队列。即使没有批处理，依赖闭包快照的写法也很脆弱；并发渲染还可能暂停、重试渲染，因此函数式更新是语义保证，不只是性能技巧。对象 state 还要注意类组件 <code>setState</code> 会浅合并，而 <code>useState</code> setter 会整体替换。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>购物车“加 2 件”按钮偶发只加 1 件，你会从哪里查？</strong></p>\\n<p>先看是否连续调用了两次 <code>setCount(count + 1)</code>，或多个异步回调捕获了旧数量。把依赖旧值的计算改成函数式更新，并让服务端库存操作具备自己的并发与幂等校验，不能只靠前端状态正确。</p>\\n</li>\\n<li>\\n<p><strong>调用 setter 后马上 <code>console.log(count)</code> 还是旧值，说明更新失败了吗？</strong></p>\\n<p>不说明。当前事件处理器闭包拿的是本次渲染快照，setter 安排的是下一次渲染；要观察已提交的新值可在渲染结果或依赖该状态的 effect 中验证。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"_3-setstate-和-usestate-有什么区别\\"><a class=\\"header-anchor\\" href=\\"#_3-setstate-和-usestate-有什么区别\\">#</a> 3 setState 和 useState 有什么区别</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>类组件 <code>setState</code> 对对象状态做浅合并，<code>useState</code> setter 直接替换整个值</li>\\n<li>类组件通过实例读取已提交状态；函数组件每次渲染拥有独立状态快照，闭包会捕获该快照</li>\\n<li>类组件 <code>setState</code> 支持提交后回调，Hook 没有第二回调参数，副作用用 <code>useEffect</code> / <code>useLayoutEffect</code> 表达</li>\\n<li>两者都支持函数式更新，并遵循当前根节点的同一套调度、优先级与批处理规则</li>\\n<li><code>useState</code> 可按关注点拆分多个状态；复杂关联状态可用 <code>useReducer</code>，不要机械塞进一个大对象</li>\\n</ul>\\n<p><strong>核心差异是浅合并对整体替换、实例状态对渲染快照；调度和批处理机制则是共用的</strong> —— 再补副作用写法与状态拆分。</p>\\n</section>\\n<p>类组件通常把多个字段放在 <code>this.state</code> 对象中，<code>this.setState({ loading: true })</code> 只浅合并顶层字段，其他 key 会保留，但嵌套对象仍需显式复制。<code>useState</code> 的 setter 采用整体替换语义：若状态是 <code>{ name, age }</code>，调用 <code>setUser({ name })</code> 会丢失 <code>age</code>。函数组件因此常把无关状态拆开，或在确实作为整体变化时使用对象与 <code>useReducer</code>。</p>\\n<p>读取模型也不同。类实例上的 <code>this.state</code> 会在提交后更新；函数组件每次调用都形成一份状态快照和对应闭包，旧回调不会神奇地看到未来渲染的值。类组件可用 <code>setState(partial, callback)</code> 在提交后执行逻辑，Hook setter 没有这个参数，因为副作用应声明为 effect，并明确依赖。两者的函数式更新都可能被 React 调用来计算下一状态，更新函数必须保持纯净，不能在其中发送请求或修改外部对象。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>把类组件迁成 Hook 后，<code>setUser({ name: \'Ada\' })</code> 导致权限字段消失，为什么？</strong></p>\\n<p>类组件原来依赖 <code>setState</code> 的浅合并，<code>useState</code> setter 却整体替换。应按关注点拆分状态，或写 <code>setUser(prev =&gt; ({ ...prev, name: \'Ada\' }))</code>；若字段关系复杂，使用 reducer 更清晰。</p>\\n</li>\\n<li>\\n<p><strong>团队封装了 <code>setState(next, callback)</code> 风格的自定义 Hook，你会推荐吗？</strong></p>\\n<p>通常不推荐，它把命令式提交回调带回函数组件，容易隐藏依赖和闭包问题。提交后的副作用应由 <code>useEffect</code> 或 <code>useLayoutEffect</code> 按依赖表达；只有非常明确的基础设施场景才封装专用 API。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);