(window.webpackJsonp=window.webpackJsonp||[]).push([[25],{1133:function(n){n.exports=JSON.parse('{"html":"<p>二叉树在面试实战中，花样非常多。本节只是个开头，在后面几个专题、包括最后的大厂真题实战环节中，我们都不会停止对二叉树相关考点的学习和探讨。</p>\\n<blockquote>\\n<p>在本节，有以下三个命题方向需要大家重点掌握：</p>\\n</blockquote>\\n<ul>\\n<li>迭代法实现二叉树的先、中、后序遍历</li>\\n<li>二叉树层序遍历的衍生问题</li>\\n<li>翻转二叉树</li>\\n</ul>\\n<blockquote>\\n<p>这三个方向对应的考题都比较经典。与此同时，解决这些问题涉及到的思路和编码细节，也会成为各位日后解决更加复杂的问题的基石。因此，虽然本节篇幅略长，但还是希望各位能够倾注耐心，给自己充分的时间去理解和消化这些知识。</p>\\n</blockquote>\\n<h2 id=\\"遍历三兄弟-的迭代实现\\"><a class=\\"header-anchor\\" href=\\"#遍历三兄弟-的迭代实现\\">#</a> “遍历三兄弟”的迭代实现</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>递归隐含使用调用栈；改迭代就是显式保存“稍后还要处理”的节点。</li>\\n<li>前序 <code>根-左-右</code>：弹出根后先压右、再压左。</li>\\n<li>中序：一路压左，栈空前弹出访问，再转向右子树。</li>\\n<li>后序最容易错，可用 <code>根-右-左</code> 结果反转，或栈中保存 visited 状态。</li>\\n</ul>\\n<p><strong>三种遍历的迭代实现，本质上都是用栈安排结点的处理顺序，只是根结点被访问的时机不同。</strong> 前序遍历要得到“根、左、右”，弹出当前结点后应先压入右孩子，再压入左孩子，因为栈是后进先出。中序遍历则要沿左孩子一路入栈，走到最左侧后再逐个弹出并转向右子树。空树直接返回空数组，结果中保存的是结点值，而不是结点对象。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“三种遍历的区别只是访问根节点的时机。迭代实现要把递归栈里的返回点显式化，我会先说栈内节点代表什么，再写循环。”</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function inorder(root) {\\n  const result = []\\n  const stack = []\\n  let current = root\\n  while (current || stack.length) {\\n    while (current) {\\n      stack.push(current)\\n      current = current.left\\n    }\\n    current = stack.pop()\\n    result.push(current.val)\\n    current = current.right\\n  }\\n  return result\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>评论区有人说中序遍历也能像前序一样“根节点出栈就立刻输出”，你拿页面上的 <code>左 -&gt; 根 -&gt; 右</code> 规则怎么构造反例？</strong></p>\\n<p>只要根节点存在左孩子，根一出栈就输出便会早于左子树，直接违反 <code>左 -&gt; 根 -&gt; 右</code>。中序迭代必须先沿 <code>left</code> 一路压栈，直到没有左节点，再弹出并访问根；根的输出时机不能照搬前序框架。</p>\\n</li>\\n<li>\\n<p><strong>你在白板上写页面里的 <code>inorder(root)</code>，面试官追问 <code>stack</code> 中的节点究竟代表什么，你会怎样结合两个 <code>while</code> 解释？</strong></p>\\n<p><code>stack</code> 保存的是沿左链经过、但尚未输出的祖先节点，相当于显式记录递归返回点。内层 <code>while</code> 持续压入左侧路径，外层循环弹出最近祖先并访问，再把 <code>current</code> 转向其右子树；两部分共同完成回溯。</p>\\n</li>\\n<li>\\n<p><strong>组件树从普通深度变成一条只有右孩子的链，<code>current || stack.length</code> 这个循环条件还成立吗，流程会怎样变化？</strong></p>\\n<p>条件仍然成立，每轮内层循环只压入当前节点一次，随后立即弹出、输出并转向右孩子。即使 <code>stack</code> 暂时为空，只要 <code>current</code> 指向下一个节点，外层循环就必须继续；若只判断 <code>stack.length</code>，遍历会提前终止。</p>\\n</li>\\n<li>\\n<p><strong>线上埋点发现中序结果漏掉右子树，而左侧节点顺序正常；你会优先检查页面示例中的哪两行状态迁移？</strong></p>\\n<p>先检查弹栈输出之后是否执行了 <code>current = current.right</code>，以及外层条件是否保留 <code>current || stack.length</code>。前者缺失会永远不进入右子树，后者若只看栈可能在转向右孩子时提前结束；可用根节点带单个右孩子的最小用例复现。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中，一方主张分别维护三套遍历代码，另一方要求统一成 <code>{ node, visited }</code> 模板；就当前中序页面你如何取舍？</strong></p>\\n<p>页面代码直接把中序的“沿左链下探—弹栈访问—转向右侧”表达出来，状态含义清晰，适合单独维护和讲解。标记模板能统一形式，却引入额外栈帧状态；若团队更看重可读性，不必为了统一而隐藏中序的游标逻辑。</p>\\n</li>\\n</ul>\\n</section>\\n<p>经过第5节的学习，相信各位已经将二叉树先、中、后序遍历的递归实现吃得透透的了。在使用递归实现遍历的过程中，我们明显察觉到，“遍历三兄弟”的编码实现也宛如孪生兄弟一样，彼此之间只有代码顺序上的不同，整体内容基本是一致的。这正是递归思想的一个重要的优点——简单。</p>\\n<p>这里的“简单”并不是说学起来简单。相反，结合笔者早期的读者调研来看，大部分同学都认为递归学起来让人很难受（这也是正常的）。\\n初学递归的人排斥递归，大部分是出于对“函数调用自身”这种骚操作的不适应。但只要你能克服这种不适应，并且通过反复的演练去吸收这种解题方法，你就会发现递归真的是个好东西。因为通过使用递归，我们可以把原本复杂的东西，拆解成非常简单的、符合人类惯用脑回路的逻辑。</p>\\n<p>这样说可能还是有点抽象，不过没关系，接下来我会讲解“遍历三兄弟”对应的迭代解法。等我们学完这坨东西之后，心怀疑惑的同学不妨拿迭代法的代码和第5节中递归法的代码比较一下，相信你会毫不犹豫地回头对递归说上一句“真香！”。</p>\\n<p><strong>从先序遍历说起</strong></p>\\n<blockquote>\\n<p>题目描述：给定一个二叉树，返回它的前序（先序）遍历序列。</p>\\n</blockquote>\\n<p>示例:</p>\\n<pre class=\\"language-text\\"><code>输入: [1,null,2,3]\\n\\n1   \\n \\\\   \\n  2   \\n /  \\n3 \\n输出: [1,2,3]\\n</code></pre>\\n<p>进阶: 递归算法很简单，你可以通过迭代算法完成吗？</p>\\n<p><strong>思路分析</strong></p>\\n<p>注意最后那一行小字：“递归算法很简单，你可以通过迭代算法完成吗？”，对递归算法有疑问的同学，趁这个机会赶紧复习下第五小节，本节我们只讲迭代法。<br>\\n前面两个小节，我们一直在强调，递归和栈有着脱不开的干系。当一道本可以用递归做出来的题，突然不许你用递归了，此时我们本能的反应，就应该是往栈上想。</p>\\n<p>在基于栈来解决掉这个题之前，我要先跟平时用 leetcode 刷题的各位强调一个常识。</p>\\n<p>现在大家回头看这道题目给我们的输入和输出：输入看似是一个数组，实则不是。大家谨记，二叉树题目的输入只要没有额外强调，那么一般来说它都是基于这样的一个对象结构嵌套而来的：</p>\\n<pre class=\\"language-js\\"><code>function TreeNode(val) {\\n    this.val = val;\\n    this.left = this.right = null;\\n}\\n</code></pre>\\n<p>比如这样：</p>\\n<pre class=\\"language-js\\"><code>const root = {\\n  val: &quot;A&quot;,\\n  left: {\\n    val: &quot;B&quot;,\\n    left: {\\n      val: &quot;D&quot;\\n    },\\n    right: {\\n      val: &quot;E&quot;\\n    }\\n  },\\n  right: {\\n    val: &quot;C&quot;,\\n    right: {\\n      val: &quot;F&quot;\\n    }\\n  }\\n};\\n</code></pre>\\n<p>话说回来，为啥题上给的不是对象，而是这样的一个数组呢：</p>\\n<pre class=\\"language-text\\"><code>[1,null,2,3]\\n</code></pre>\\n<p>这其实是一种简化的写法，性质跟咱们写伪代码差不多。它的作用主要是描述二叉树的值，至于二叉树的结构，我们以题中给出的树形结构为准：</p>\\n<pre class=\\"language-text\\"><code>1\\n  \\\\\\n   2\\n  /\\n3\\n</code></pre>\\n<p>OK，了解了输入内容，现在再来看输出：</p>\\n<pre class=\\"language-text\\"><code>[1,2,3]\\n</code></pre>\\n<p>这里这个输出就简单多了，它是一个真数组。为什么可以判断它是一个真数组呢？因为题目中要求你输出的是一个遍历序列，而不是一个二叉树。因此大家最后需要塞入结果数组的不是结点对象，而是结点的值。</p>\\n<p><strong>注意：</strong></p>\\n<ul>\\n<li>以上的出参入参规律，是针对 leetcode 及其周边 OJ 来说的。OJ 中这样编写题目描述，是情理之中，因为 OJ 本身是支持多语言的，它只能对最通用的一部分信息进行透出。在面试场景下，不排除一些公司可能会贴心地把 JS 版本的出参和入参给出来，但更多的还是会直接复制粘贴 leetcode 或者其它一些算法书中的原题。如果你对题目的出参和入参有疑问，请大胆地对面试官说出你的困惑——没有一个正常面试官会在题目描述上为难你， 他比你更急切地想看到你刷刷写代码的英姿。</li>\\n<li>回到题目上来。我们接着栈往下说，题目中的出参是一个数组，大家仔细看这个数组，它像不像是一个栈的出栈序列？实际上，做这道题的一个根本思路，就是通过合理地安排入栈和出栈的时机、使栈的出栈序列符合二叉树的前序遍历规则。</li>\\n</ul>\\n<blockquote>\\n<p>前序遍历的规则是，先遍历根结点、然后遍历左孩子、最后遍历右孩子——这正是我们所期望的出栈序列。按道理，入栈序列和出栈序列相反，我们似乎应该按照 右-&gt;左-&gt;根 这样的顺序将结点入栈。不过需要注意的是，我们遍历的起点就是根结点，难道我们要假装没看到这个根结点、一鼓作气找到最右侧结点之后才开始进行入栈操作吗？答案当然是否定的，我们的出入栈顺序应该是这样的：</p>\\n</blockquote>\\n<ul>\\n<li>将根结点入栈</li>\\n<li>取出栈顶结点，将结点值 push 进结果数组</li>\\n<li>若栈顶结点有右孩子，则将右孩子入栈</li>\\n<li>若栈顶结点有左孩子，则将左孩子入栈</li>\\n</ul>\\n<blockquote>\\n<p>这整个过程，本质上是将当前子树的根结点入栈、出栈，随后再将其对应左右子树入栈、出栈的过程。</p>\\n</blockquote>\\n<p>重复 2、3、4 步骤，直至栈空，我们就能得到一个先序遍历序列。</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {TreeNode} root\\n * @return {number[]}\\n */\\nconst preorderTraversal = function(root) {\\n  // 定义结果数组\\n  const res = []  \\n  // 处理边界条件\\n  if(!root) {\\n      return res\\n  }\\n  // 初始化栈结构\\n  const stack = [] \\n  // 首先将根结点入栈\\n  stack.push(root)  \\n  // 若栈不为空，则重复出栈、入栈操作\\n  while(stack.length) {\\n      // 将栈顶结点记为当前结点\\n      const cur = stack.pop() \\n      // 当前结点就是当前子树的根结点，把这个结点放在结果数组的尾部\\n      res.push(cur.val)\\n      // 若当前子树根结点有右孩子，则将右孩子入栈\\n      if(cur.right) {\\n          stack.push(cur.right)\\n      }\\n      // 若当前子树根结点有左孩子，则将左孩子入栈\\n      if(cur.left) {\\n          stack.push(cur.left)\\n      }\\n  }\\n  // 返回结果数组\\n  return res\\n};\\n</code></pre>\\n<h3 id=\\"异曲同工的后序遍历迭代实现\\"><a class=\\"header-anchor\\" href=\\"#异曲同工的后序遍历迭代实现\\">#</a> 异曲同工的后序遍历迭代实现</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>后序要求左右子树都处理完才访问根，迭代时必须记录右子树是否已经访问</li>\\n<li>简洁方案是按“根 → 右 → 左”收集后整体反转，得到“左 → 右 → 根”；直接单栈方案则用 <code>lastVisited</code></li>\\n<li>每个结点有限次入栈出栈，时间 <code>O(n)</code>，栈空间 <code>O(h)</code> 到 <code>O(n)</code></li>\\n</ul>\\n<p><strong>后序遍历可以沿用前序遍历的栈框架，但要从结果数组的头部插入结点，最终得到“左、右、根”的顺序。</strong> 因为先弹出的结点会被后续元素不断向后推，所以根结点最终能落到结果末尾。入栈时先放左孩子、再放右孩子，利用后进先出让右孩子先弹出，头插后左孩子仍位于右孩子之前。空树是边界情况，直接返回空数组即可。</p>\\n</section>\\n<p><strong>思路分析</strong></p>\\n<blockquote>\\n<p>后序遍历的出栈序列，按照规则应该是 <code>左 -&gt; 右 -&gt; 根</code> 。这个顺序相对于先序遍历，最明显的变化就是根结点的位置从第一个变成了倒数第一个。<br>\\n如何做到这一点呢？与其从 stack 这个栈结构上入手，不如从 res 结果数组上入手：我们可以直接把 pop 出来的当前结点 unshift 进 res 的头部，改造后的代码会变成这样：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>while(stack.length) {\\n  // 将栈顶结点记为当前结点\\n  const cur = stack.pop() \\n  // 当前结点就是当前子树的根结点，把这个结点放在结果数组的头部\\n  res.unshift(cur.val)\\n  // 若当前子树根结点有右孩子，则将右孩子入栈\\n  if(cur.right) {\\n    stack.push(cur.right)\\n  }\\n  // 若当前子树根结点有左孩子，则将左孩子入栈\\n  if(cur.left) {\\n    stack.push(cur.left)\\n  }\\n}\\n</code></pre>\\n<blockquote>\\n<p>大家可以尝试在大脑里预判一下这个代码的执行顺序：由于我们填充 res 结果数组的顺序是从后往前填充（每次增加一个头部元素），因此先出栈的结点反而会位于 res 数组相对靠后的位置。出栈的顺序是 当前结点 -&gt; 当前结点的左孩子 -&gt; 当前结点的右孩子 ，其对应的 res 序列顺序就是 右 -&gt; 左 -&gt; 根 。这样一来， 根结点就成功地被我们转移到了遍历序列的最末尾。</p>\\n</blockquote>\\n<p>现在唯一让人看不顺眼的只剩下这个右孩子和左孩子的顺序了，这两个孩子结点进入结果数组的顺序与其被 pop 出栈的顺序是一致的，而出栈顺序又完全由入栈顺序决定，因此只需要相应地调整两个结点的入栈顺序就好了：</p>\\n<pre class=\\"language-js\\"><code>// 若当前子树根结点有左孩子，则将左孩子入栈\\nif(cur.left) {\\n  stack.push(cur.left)\\n}  \\n// 若当前子树根结点有右孩子，则将右孩子入栈\\nif(cur.right) {\\n  stack.push(cur.right)\\n}\\n</code></pre>\\n<p>如此一来，右孩子就会相对于左孩子优先出栈，进而被放在 res 结果数组相对靠后的位置， 左 -&gt; 右 -&gt;根 的排序规则就稳稳地实现出来了。</p>\\n<p>我们把以上两个改造点结合一下，就有了以下代码：</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {TreeNode} root\\n * @return {number[]}\\n */\\nconst postorderTraversal = function(root) {\\n  // 定义结果数组\\n  const res = []  \\n  // 处理边界条件\\n  if(!root) {\\n      return res\\n  }\\n  // 初始化栈结构\\n  const stack = [] \\n  // 首先将根结点入栈\\n  stack.push(root)  \\n  // 若栈不为空，则重复出栈、入栈操作\\n  while(stack.length) {\\n      // 将栈顶结点记为当前结点\\n      const cur = stack.pop() \\n      // 当前结点就是当前子树的根结点，把这个结点放在结果数组的头部\\n      res.unshift(cur.val)\\n      // 若当前子树根结点有左孩子，则将左孩子入栈\\n      if(cur.left) {\\n        stack.push(cur.left)\\n      }  \\n      // 若当前子树根结点有右孩子，则将右孩子入栈\\n      if(cur.right) {\\n        stack.push(cur.right)\\n      }\\n  }\\n  // 返回结果数组\\n  return res\\n};\\n</code></pre>\\n<p><strong>思路清奇的中序遍历迭代实现</strong></p>\\n<p>思路分析</p>\\n<p>经过上面的讲解，大家会发现先序遍历和后序遍历的编码实现其实是非常相似的，它们遵循的都是同一套基本框架。那么我们能否通过对这个基本框架进行微调、从而同样轻松地实现中序遍历呢？</p>\\n<p>答案是不能，为啥不能？因为先序遍历和后序遍历之所以可以用同一套代码框架来实现，本质上是因为两者的出栈、入栈逻辑差别不大——都是先处理根结点，然后处理孩子结点。而中序遍历中，根结点不再出现在遍历序列的边界、而是出现在遍历序列的中间。这就意味着无论如何我们不能再将根结点作为第一个被 pop 出来的元素来处理了——出栈的时机被改变了，这意味着入栈的逻辑也需要调整。这一次我们不能再通过对 res 动手脚来解决问题，而是需要和 stack 面对面 battle。</p>\\n<blockquote>\\n<p>中序遍历的序列规则是 <code>左 -&gt; 中 -&gt; 右</code>，这意味着我们必须首先定位到最左的叶子结点。在这个定位的过程中，必然会途径目标结点的父结点、爷爷结点和各种辈分的祖宗结点：</p>\\n</blockquote>\\n<p><img src=\\"https://p1-jj.byteimg.com/tos-cn-i-t2oaga2asx/gold-user-assets/2020/5/10/171fefdfeae2b8a3~tplv-t2oaga2asx-watermark.awebp\\" alt=\\"\\"></p>\\n<p>途径过的每一个结点，我们都要及时地把它入栈。这样当最左的叶子结点出栈时，第一个回溯到的就是它的父结点：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123421.png\\" alt=\\"\\"></p>\\n<p>有了父结点，就不愁找不到兄弟结点，遍历结果就变得唾手可得了~</p>\\n<p>基于这个思路，我们来写代码：</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {TreeNode} root\\n * @return {number[]}\\n */\\nconst inorderTraversal = function(root) {\\n  // 定义结果数组\\n  const res = []  \\n  // 初始化栈结构\\n  const stack = []   \\n  // 用一个 cur 结点充当游标\\n  let cur = root  \\n  // 当 cur 不为空、或者 stack 不为空时，重复以下逻辑\\n  while(cur || stack.length) {\\n      // 这个 while 的作用是把寻找最左叶子结点的过程中，途径的所有结点都记录下来 \\n      while(cur) {\\n          // 将途径的结点入栈\\n          stack.push(cur)  \\n          // 继续搜索当前结点的左孩子\\n          cur = cur.left  \\n      }\\n      // 取出栈顶元素\\n      cur = stack.pop()  \\n      // 将栈顶元素入栈\\n      res.push(cur.val)  \\n      // 尝试读取 cur 结点的右孩子\\n      cur = cur.right\\n  }\\n  // 返回结果数组\\n  return res\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>页面代码先压左孩子、再压右孩子，有同事认定出栈顺序因此就是后序的 <code>左 -&gt; 右 -&gt; 根</code>；你如何用一个根带左右孩子的树反驳？</strong></p>\\n<p>栈是后进先出，先压左再压右会让出栈顺序成为 <code>根 -&gt; 右 -&gt; 左</code>，并不是直接得到后序。代码依靠 <code>unshift</code> 把每个值放到结果头部，最终才反转为 <code>左 -&gt; 右 -&gt; 根</code>；忽略结果数组的填充方向就会误判。</p>\\n</li>\\n<li>\\n<p><strong>评审页面里的 <code>postorderTraversal</code> 时，面试官要求你用一句状态不变量解释为什么 <code>unshift</code> 能保证根节点最后出现，你会怎么说？</strong></p>\\n<p>每次弹出的节点都插到已有结果之前，因此越早弹出的节点在最终数组里越靠后。根节点最先出栈，最终自然位于末尾；同时右孩子先于左孩子出栈，经头插后顺序反转，才得到后序要求的左、右、根。</p>\\n</li>\\n<li>\\n<p><strong>接口把输出规模扩大到很多节点，性能负责人质疑循环里反复执行 <code>res.unshift</code>；在不改变后序结果的前提下你会怎样取舍？</strong></p>\\n<p>可先按 <code>根 -&gt; 右 -&gt; 左</code> 的目标组织出栈并用 <code>push</code> 追加，最后再整体反转为 <code>左 -&gt; 右 -&gt; 根</code>。这样避免每次头插可能带来的数组元素移动，但仍需要显式栈和结果数组；具体收益应结合运行环境测量，不能凭代码形态虚构数字。</p>\\n</li>\\n<li>\\n<p><strong>线上结果变成 <code>右 -&gt; 左 -&gt; 根</code>，根的位置正确但左右完全颠倒；你会优先核对哪段入栈代码？</strong></p>\\n<p>应核对是否误写成先压右孩子、再压左孩子，同时仍然使用 <code>unshift</code>。头插会反转出栈顺序，所以要得到 <code>左 -&gt; 右 -&gt; 根</code>，出栈必须是 <code>根 -&gt; 右 -&gt; 左</code>，对应入栈顺序应为左后右；用三节点树即可快速确认。</p>\\n</li>\\n<li>\\n<p><strong>候选人想把页面的头插法换成单栈加 <code>lastVisited</code>，理由是后序遍历就该模拟真实访问时机；你会怎样评价两种方案？</strong></p>\\n<p>头插法通过调整结果顺序实现后序，代码短，但阅读者必须同时推导入栈、出栈和反转关系。<code>lastVisited</code> 写法更贴近左、右、根的真实完成时机，却需要维护右子树是否处理过的状态；选择取决于可读性和操作成本，而非正确性高低。</p>\\n</li>\\n<li>\\n<p><strong>讲完后序头插法后，面试官让你解释为什么同一框架不能直接套到页面下方的中序遍历，你怎么串联？</strong></p>\\n<p>前序与这种后序写法都能先弹出根，再靠结果顺序调整根的位置；中序的根必须位于左、右子树之间，单纯头插或尾插无法表达这个访问时机。中序因此要把祖先压栈，先定位最左节点，再回溯访问根并进入右子树。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"编码复盘\\"><a class=\\"header-anchor\\" href=\\"#编码复盘\\">#</a> 编码复盘</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>迭代遍历要给栈中元素一个明确含义：待进入结点、待访问结点，或带处理阶段的帧</li>\\n<li>入栈顺序由目标输出顺序反推，栈后进先出，不能照着“左、右”文字顺序机械压栈</li>\\n<li>用空树、单结点、只有左链、只有右链验证，最容易暴露重复访问和死循环</li>\\n</ul>\\n<p><strong>迭代中序遍历的核心，是先沿左孩子不断入栈，再按“左、根、右”的顺序回溯访问。</strong> 内层 <code>while</code> 负责找到最左结点，并把沿途结点保存到栈里；外层循环则在出栈后记录当前值，再转向右子树。只要 <code>cur</code> 存在或栈不为空，遍历就还没有结束。理解后还要用空树、单结点和单侧链反复练习，避免写出遗漏或死循环。</p>\\n</section>\\n<p>读完这段编码示范，一部分同学可能已经开始懵逼了：看着前面给出的思路分析，似乎完全写不出上面这样的代码啊！所以这段代码到底在干嘛？？？<br>\\n如果你没有这样的困惑，说明你是一位悟性比较高的同学，可以直接跳过编码复盘部分往下读了（btw给你点个赞~）。</p>\\n<p>实不相瞒，如果你是初学者，这段代码可能确实需要大家在脑内反复运行、反复跑 demo 才能理解其中的逻辑。为了加快这个过程，我把其中看上去稍微拐弯抹角一点的逻辑摘出来，给大家点拨一下：</p>\\n<ol>\\n<li>两个 <code>while</code> ：内层的 <code>while</code> 的作用是在寻找最左叶子结点的过程中，把途径的所有结点都记录到 stack 里。记录工作完成后，才会走到外层 while 的剩余逻辑里——这部分逻辑的作用是从最左的叶子结点开始，一层层回溯遍历左孩子的父结点和右侧兄弟结点，进而完成整个中序遍历任务</li>\\n<li>外层 <code>while</code> 的两个条件： cur 的存在性和 <code>stack.length</code> 的存在性，各自是为了限制什么？</li>\\n</ol>\\n<ul>\\n<li>stack.length 的存在性比较好理解， stack 中存储的是没有被推入结果数组 res 的待遍历元素。只要 stack 不为空，就意味着遍历没有结束， 遍历动作需要继续重复</li>\\n<li>cur 的存在性就比较有趣了。它对应以下几种情况\\n<ul>\\n<li>初始态， cur 指向 root 结点，只要 root 不为空， cur 就不为空。此时判断了 cur 存在后，就会开始最左叶子结点的寻找之旅。这趟“一路向左”的旅途中， cur 始终指向当前遍历到的左孩子</li>\\n<li>第一波内层 while 循环结束， cur 开始承担中序遍历的遍历游标职责。 cur 始终会指向当前栈的栈顶元素，也就是“一路向左”过程中途径的某个左孩子，然后将这个左孩子作为中序遍历的第一个结果元素纳入结果数组。假如这个左孩子是一个叶子结点，那么尝试取其右孩子时就只能取到 null ，这个 null 的存在，会导致内层循环 while 被跳过，接着就直接回溯到了这个左孩子的父结点，符合 左-&gt;根  的序列规则</li>\\n<li>假如当前取到的栈顶元素不是叶子结点，同时有一个右孩子，那么尝试取其右孩子时就会取到一个存在的结点。 cur 存在，于是进入内层 while 循环，重复“一路向左”的操作，去寻找这个右孩子对应的子树里最靠左的结点，然后去重复刚刚这个或回溯、或“一路向左”的过程。如果这个右孩子对应的子树里没有左孩子，那么跳出内层 while 循环之后，紧接着被纳入 res 结果数组的就是这个右孩子本身，符合 根-&gt;右 的序列规则</li>\\n</ul>\\n</li>\\n</ul>\\n<p>结合上面的分析，大家会不会觉得中序遍历迭代法的这一通操作非常奇妙呢？短短的几行代码，里面竟然藏着这么广阔的乾坤，牛x、牛x。<br>\\n作为初学者，即便第一次写不出来上面的解法，也没什么好丧气的——大家谨记，关于二叉树的先、中、后序遍历，你对自己的要求应该是能够默写，也就是说要对上面这些逻辑充分熟悉、深刻记忆。</p>\\n<p>在熟悉和记忆的过程中，你会渐渐地对这些乍一看似乎很巧妙的操作产生一种“这也很自然嘛”的感觉，这种感觉就意味着你对这个思路的充分吸收。还是那句话，千万不要以为理解就是终点，你需要做的是记忆！记忆！理解是一种感觉，记忆却能保证你在做题时一秒钟映射到具体的套路和代码——只有靠自己的双手写出来的代码，才是最可靠的伙伴。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>有人看到外层 <code>while (cur || stack.length)</code>，认为 <code>cur</code> 为空就说明遍历结束；你用一个最左叶子节点的执行现场怎么反驳？</strong></p>\\n<p>到达最左叶子之后再执行 <code>cur = cur.left</code>，<code>cur</code> 必然变为 <code>null</code>，但栈里仍保存着叶子及其祖先，遍历远未结束。此时应跳过内层循环并弹栈访问；若把 <code>cur</code> 为空当终点，整条回溯路径都会丢失。</p>\\n</li>\\n<li>\\n<p><strong>白板实现中序遍历时，面试官指着内层 <code>while (cur)</code> 问它在工程上维护了什么状态，你会怎么回答？</strong></p>\\n<p>它把寻找最左节点途中经过的所有节点依次保存到 <code>stack</code>，这些节点都是尚未输出的回溯点。下探结束后，弹出的栈顶就是当前最靠左的待访问节点；访问后转向右孩子，再对该子树重复同样过程。</p>\\n</li>\\n<li>\\n<p><strong>测试树从平衡结构改成“某个左孩子还带右子树”，候选人的代码只会一路弹栈；哪一步必须恢复，才能满足中序顺序？</strong></p>\\n<p>每次弹出并输出节点后，必须令 <code>cur = cur.right</code>，让游标进入刚访问节点的右子树。若右孩子存在，下一轮还要沿它的左链继续压栈；若不存在，<code>cur</code> 为 <code>null</code>，外层循环依靠非空栈继续回溯祖先。</p>\\n</li>\\n<li>\\n<p><strong>线上中序结果在第一个叶子节点后就停止，但监控显示函数没有异常；你会怎样围绕两个循环条件排查？</strong></p>\\n<p>先检查外层条件是否被误写成只判断 <code>cur</code>，因为抵达最左端时 <code>cur</code> 会暂时为 <code>null</code>，却仍需依靠 <code>stack.length</code> 继续。再检查内层循环是否错误弹栈；用“根节点只有左孩子”的最小树即可验证是否丢失回溯阶段。</p>\\n</li>\\n<li>\\n<p><strong>团队争论是背熟这段代码，还是每次现场重新推导两个 <code>while</code>；你会如何制定面试准备策略？</strong></p>\\n<p>应先理解栈保存未访问祖先、<code>cur</code> 负责下探和转向右子树，再通过反复手写把这一状态机固化到能稳定复现。只背字符容易在条件变化时写错，只临场推导又增加停顿和遗漏风险；目标是理解支撑下的熟练默写。</p>\\n</li>\\n<li>\\n<p><strong>面试官把中序迭代与递归放在一起，要求你说明 <code>stack</code> 和递归调用栈的对应关系，你会怎样串联？</strong></p>\\n<p>显式 <code>stack</code> 保存的是沿左链尚未完成的祖先，对应递归调用中等待左子树返回的调用帧。弹栈访问相当于左递归结束后处理根，转向 <code>right</code> 则相当于进入右递归；迭代只是把隐式返回点改为手工维护。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"层序遍历的衍生问题\\"><a class=\\"header-anchor\\" href=\\"#层序遍历的衍生问题\\">#</a> 层序遍历的衍生问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>层序遍历使用队列；每轮固定读取开始时的 <code>levelSize</code>，这批节点属于同一层。</li>\\n<li>新发现的子节点只入队，不参与当前层循环。</li>\\n<li>时间 <code>O(n)</code>，空间最坏等于树的最大宽度 <code>O(w)</code>。</li>\\n<li>锯齿层序可按层决定结果写入方向，不必真的反转整条队列。</li>\\n</ul>\\n<p><strong>层序遍历用 <code>BFS</code> 和队列实现，每轮开始时缓存 <code>queue.length</code>，就能准确划分当前层。</strong> 循环过程中虽然会把子结点加入队尾，但缓存的长度不会变化，所以新结点只会在下一轮处理。当前这批结点统一写入一个 <code>level</code> 数组，处理完再放进结果数组。空树直接返回空数组；普通实现会用 <code>shift()</code> 取队头，也可以维护头指针。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“我用 BFS，并在每层开始缓存队列长度。这样即使循环中继续加入下一层节点，当前层边界也不会变化。”</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品分类页要按层展示目录树，同事在遍历当前层时持续向队列追加孩子，并用实时的 <code>queue.length</code> 作为循环上限，为什么页面会把下一层也混进来？</strong></p>\\n<p>循环上限会随入队操作增长，导致本层遍历越过原有边界并继续消费下一层节点。进入每层前应缓存固定的 <code>levelSize</code>，本轮只处理这批节点；若还用数组 <code>shift()</code> 出队，大数据下还可能产生额外搬移成本。</p>\\n</li>\\n<li>\\n<p><strong>组织架构页需要返回每一层的成员数组，你会怎样基于一个不断追加节点的队列实现，避免层级错位？</strong></p>\\n<p>每轮先记录当前待处理节点数 <code>levelSize</code>，再恰好出队或读取这么多个节点，并把它们的孩子追加到队尾。当前层结果独立收集后再写入总结果；若输入可能为空，入口处应直接返回空数组，避免制造虚假的第一层。</p>\\n</li>\\n<li>\\n<p><strong>风控服务只需要求一棵决策树的最小深度，评审中有人坚持完整遍历后再比较所有叶子深度，你会接受吗？</strong></p>\\n<p>不需要完整遍历，按层执行 <code>BFS</code> 时首次遇到左右孩子都为空的叶子即可返回当前深度。队列中的节点按深度递增，因此更深层不可能给出更小答案；但不能把只有一个空孩子的节点误判为叶子。</p>\\n</li>\\n<li>\\n<p><strong>监控页计算每层节点均值时，某一层有大量高数值计数，结果出现精度异常，你会先检查哪两处？</strong></p>\\n<p>先检查累加变量的数值范围与精度，再确认除数使用进入该层时缓存的 <code>levelSize</code>，而不是入队后的动态队列长度。若业务要求精确整数统计，普通浮点数可能不够；改用更宽或精确类型时，还要处理最终均值的表示方式。</p>\\n</li>\\n<li>\\n<p><strong>百万级宽树要边遍历边写入流，架构师要求把峰值内存降到与树高相关，你会如何回应？</strong></p>\\n<p>标准层序遍历的空间下界受最大层宽影响，不能仅靠改写循环就稳定降到与树高相关。可以按层或按节点流式输出、及时释放已消费引用，并用头指针代替 <code>shift()</code>；若必须保持严格层序，当前前沿仍可能占用与最大宽度同阶的内存。</p>\\n</li>\\n</ul>\\n</section>\\n<blockquote>\\n<p>在 DFS 和 BFS 这一节，我们已经讲过了二叉树层序遍历的实现方法。层序遍历本本身难度不大，但一想到这是一个关键考点，出题这帮人就每天绞尽脑汁地想要把简单的问题复杂化。于是，就有了我们眼下这个命题方向——层序遍历的衍生问题。</p>\\n</blockquote>\\n<p>对于这类问题，我们接下来会讲最有代表性的一道作为例题。各位只要能吃透这一道的基本思路，就能够轻松地在类似的变体中举一反三（例题请大家好好把握，在大厂真题训练环节，我会给出一道变体来检验各位的学习效果）。</p>\\n<blockquote>\\n<p>题目描述：给你一个二叉树，请你返回其按 层序遍历 得到的节点值。 （即逐层地，从左到右访问所有节点）。</p>\\n</blockquote>\\n<p>示例：</p>\\n<blockquote>\\n<p>二叉树：[3,9,20,null,null,15,7],</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>  3\\n / \\\\\\n9  20\\n  /  \\\\\\n 15   7\\n</code></pre>\\n<p>返回其层次遍历结果：</p>\\n<pre class=\\"language-text\\"><code>[\\n[3],\\n[9,20],\\n[15,7]\\n]\\n</code></pre>\\n<p><strong>思路分析</strong></p>\\n<blockquote>\\n<p>层序遍历没有那么多幺蛾子，大家看到层序遍历就应该条件反射出 BFS+队列  这对好基友。所谓变体，不过是在 BFS 的过程中围绕结果数组的内容做文章。<br>\\n拿这道题来说，相对于我们 14 节中讲过的层序遍历基本思路，它变出的花样仅仅在于要求我们对层序遍历结果进行分层。也就是说只要我们能在 BFS 的过程中感知到当前层级、同时用不同的数组把不同的层级区分开，这道题就得解了。</p>\\n</blockquote>\\n<p>如何做到这一点？大家需要知道一个非常重要的信息：我们在对二叉树进行层序遍历时，每一次 while 循环其实都对应着二叉树的某一层。只要我们在进入while循环之初，记录下这一层结点个数，然后将这个数量范围内的元素 push 进同一个数组，就能够实现二叉树的分层。</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {TreeNode} root\\n * @return {number[][]}\\n */\\nconst levelOrder = function(root) {\\n    // 初始化结果数组\\n    const res = []  \\n    // 处理边界条件\\n    if(!root) {\\n        return res\\n    }  \\n    // 初始化队列\\n    const queue = []   \\n    // 队列第一个元素是根结点\\n    queue.push(root)  \\n    // 当队列不为空时，反复执行以下逻辑\\n    while(queue.length) {\\n        // level 用来存储当前层的结点\\n        const level = []  \\n        // 缓存刚进入循环时的队列长度，这一步很关键，因为队列长度后面会发生改变\\n        const len = queue.length  \\n        // 循环遍历当前层级的结点\\n        for(let i=0;i&lt;len;i++) {\\n            // 取出队列的头部元素\\n            const top = queue.shift()  \\n            // 将头部元素的值推入 level 数组\\n            level.push(top.val)\\n            // 如果当前结点有左孩子，则推入下一层级\\n            if(top.left) {\\n                queue.push(top.left)\\n            }\\n            // 如果当前结点有右孩子，则推入下一层级\\n            if(top.right) {\\n                queue.push(top.right)\\n            }\\n        }\\n        // 将 level 推入结果数组\\n        res.push(level)\\n    }\\n    // 返回结果数组\\n    return res\\n};\\n</code></pre>\\n<h2 id=\\"翻转二叉树\\"><a class=\\"header-anchor\\" href=\\"#翻转二叉树\\">#</a> 翻转二叉树</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>每个节点交换 <code>left</code> 与 <code>right</code>，然后继续处理两个孩子。</li>\\n<li>递归与 BFS/DFS 迭代都可行；时间 <code>O(n)</code>，额外空间取决于树高或最大宽度。</li>\\n<li>原地翻转会修改输入；若树被共享，应创建新节点做持久化转换。</li>\\n<li>空树直接返回 <code>null</code>，单节点交换后保持不变。</li>\\n</ul>\\n<p><strong>翻转二叉树本质上是遍历每个结点，并交换它的 <code>left</code> 和 <code>right</code>。</strong> 递归写法很自然：先翻转左右子树，再把返回的两棵子树交叉挂回当前结点，因为每棵子树都要执行同样的操作。也可以用队列逐个处理结点，交换后继续加入左右孩子。空树直接返回 <code>null</code>；如果不允许修改原树，就不能沿用这种原地交换写法。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“翻转是一个局部操作加完整遍历：对每个节点交换左右引用。我要先确认是否允许原地修改，否则返回一棵新树。”</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function invertTree(root) {\\n  if (!root) return null\\n  const queue = [root]\\n  for (let head = 0; head &lt; queue.length; head += 1) {\\n    const node = queue[head]\\n    ;[node.left, node.right] = [node.right, node.left]\\n    if (node.left) queue.push(node.left)\\n    if (node.right) queue.push(node.right)\\n  }\\n  return root\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>权限树翻转后，测试发现某些节点只交换了一侧就结束；代码只处理“左右孩子都存在”的节点，反例在哪里？</strong></p>\\n<p>只有一个孩子的节点同样必须交换引用，否则原来的左孩子不会移动到右侧，或反之。局部规则应对每个非空节点无条件执行 <code>[node.left, node.right] = [node.right, node.left]</code>；遍历终止只取决于节点是否为空。</p>\\n</li>\\n<li>\\n<p><strong>前端树形编辑器点击“镜像”后仍要支持撤销和差异对比，产品不允许修改原对象，你会怎样落地？</strong></p>\\n<p>应创建一棵新树，并在复制当前节点时递归或迭代地把原右子树放到新左侧、原左子树放到新右侧。这样原树引用保持不变，便于撤销和比较；代价是新增与节点数同阶的对象和内存。</p>\\n</li>\\n<li>\\n<p><strong>服务端给出的结构可能被多个父节点共享，开发仍直接套用普通二叉树的原地翻转代码，会出现什么语义风险？</strong></p>\\n<p>共享节点意味着输入更接近 <code>DAG</code>，同一对象可能被不同路径重复翻转，最终结果取决于访问次数而非单纯镜像。需要先明确是按对象翻转还是按路径复制，并用访问状态防止重复处理；若还可能含环，普通树遍历甚至无法自然终止。</p>\\n</li>\\n<li>\\n<p><strong>线上出现翻转后部分子树恢复原状，代码是“先交换，再分别递归 <code>root.left</code> 和原先保存的 <code>left</code>”，你会如何定位？</strong></p>\\n<p>重点检查递归目标是否同时混用了交换后的引用和交换前的临时变量，这可能让同一子树被处理两次、另一棵被遗漏。统一约定交换后递归新的左右孩子，或先递归原孩子再交换；同时用节点标识记录访问顺序验证覆盖是否恰好一次。</p>\\n</li>\\n<li>\\n<p><strong>一棵极深的配置树需要镜像，评审在递归与队列版之间争论，你会按什么约束选择？</strong></p>\\n<p>递归写法贴近“处理节点并翻转两棵子树”的结构，但深度过大时受调用栈限制。队列版按节点交换并继续入队，不依赖递归栈，却需要容纳遍历前沿；树很深且较窄时通常更稳，树很宽时则要评估队列峰值。</p>\\n</li>\\n</ul>\\n</section>\\n<blockquote>\\n<p>翻转二叉树是一个非常经典的问题。之前有一个关于这道题的笑话，说是Homebrew的作者去面 Google，结果因为不会翻转二叉树被挂掉了。Google 在给这位大佬的拒信中写道：</p>\\n</blockquote>\\n<p>我们90％的工程师使用您编写的软件(Homebrew)，但是您却无法在面试时在白板上写出翻转二叉树这道题，这太糟糕了。</p>\\n<p>这个故事之所以是个笑话，是因为翻转二叉树在算法面试中实在太常见了——只要你准备算法面试，你就不得不做这个题。在面试中做不出这道题的同学，会给面试官留下基础不牢的糟糕印象。</p>\\n<p>接下来我们就一起来搞定这道翻转二叉树，成为比homebrew作者更懂算法面试的人（逃</p>\\n<blockquote>\\n<p>题目描述：翻转一棵二叉树。</p>\\n</blockquote>\\n<p>示例：</p>\\n<p>输入：</p>\\n<pre class=\\"language-text\\"><code>     4\\n   /   \\\\\\n  2     7\\n / \\\\   / \\\\\\n1   3 6   9\\n</code></pre>\\n<p>输出：</p>\\n<pre class=\\"language-text\\"><code>     4\\n   /   \\\\\\n  7     2\\n / \\\\   / \\\\\\n9   6 3   1\\n</code></pre>\\n<p><strong>思路分析</strong></p>\\n<p>这道题是一道非常经典的递归应用题。</p>\\n<p>一棵二叉树，经过翻转后会有什么特点？答案是每一棵子树的左孩子和右孩子都发生了交换。既然是“每一棵子树”，那么就意味着重复，既然涉及了重复，就没有理由不用递归。</p>\\n<p>于是这道题解题思路就非常明确了：以递归的方式，遍历树中的每一个结点，并将每一个结点的左右孩子进行交换。</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {TreeNode} root\\n * @return {TreeNode}\\n */\\nconst invertTree = function(root) {\\n    // 定义递归边界\\n    if(!root) {\\n        return root;\\n    }\\n    // 递归交换右孩子的子结点\\n    let right = invertTree(root.right);\\n    // 递归交换左孩子的子结点\\n    let left = invertTree(root.left);\\n    // 交换当前遍历到的两个左右孩子结点\\n    root.left = right;\\n    root.right = left;\\n    return root;\\n};\\n</code></pre>\\n"}')}}]);