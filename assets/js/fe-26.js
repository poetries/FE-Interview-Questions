(window.webpackJsonp=window.webpackJsonp||[]).push([[26],{1133:function(n){n.exports=JSON.parse('{"html":"<blockquote>\\n<p>二叉搜索树（Binary Search Tree）简称 BST，是二叉树的一种特殊形式。它有很多别名，比如排序二叉树、二叉查找树等等。</p>\\n</blockquote>\\n<p>虽然二叉搜索树多年来一直作为算法面试的“必要考点”存在，但在实际面试中，它的考察频率并不能和常规二叉树相提并论，算不上“大热”的考点，同时考察内容也是相对比较稳定的。对于二叉搜索树，我们只要能够把握好它的限制条件和特性，就足以应对大部分的考题。</p>\\n<h2 id=\\"什么是二叉搜索树\\"><a class=\\"header-anchor\\" href=\\"#什么是二叉搜索树\\">#</a> 什么是二叉搜索树</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>对每个结点，左子树所有值小于它，右子树所有值大于它；该约束作用于整棵子树而非只看直接孩子</li>\\n<li>中序遍历严格递增是无重复 BST 的等价特征，搜索可根据大小每次排除一侧</li>\\n<li>操作复杂度是 <code>O(h)</code>：平衡时约 <code>O(log n)</code>，退化成单链时为 <code>O(n)</code></li>\\n</ul>\\n<p><strong>二叉搜索树是一种有序二叉树：任意结点的左子树值都不大于它，右子树值都不小于它，并且两棵子树也满足同样规则。</strong> 这个定义是递归的，所以空树本身也是二叉搜索树。判断时不能只比较结点和直接孩子，还要保证整棵左、右子树都符合大小关系。边界上该定义允许出现相等值，但相等结点具体放在哪一侧仍要遵守题目的约定。</p>\\n</section>\\n<p>树的定义总是以递归的形式出现，二叉搜索树也不例外，它的递归定义如下：</p>\\n<ul>\\n<li>是一棵空树</li>\\n<li>是一棵由根结点、左子树、右子树组成的树，同时左子树和右子树都是二叉搜索树，且左子树上所有结点的数据域都小于等于根结点的数据域，右子树上所有结点的数据域都大于等于根结点的数据域</li>\\n</ul>\\n<p>满足以上两个条件之一的二叉树，就是二叉搜索树。</p>\\n<blockquote>\\n<p>从这个定义我们可以看出，二叉搜索树强调的是数据域的有序性。也就是说，二叉搜索树上的每一棵子树，都应该满足 <code>左孩子 &lt;= 根结点 &lt;= 右孩子</code> 这样的大小关系。下图我给出了几个二叉搜索树的示例</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123444.png\\" alt=\\"\\"></p>\\n<p>以第三棵树为例，根结点的数据域为6，它的左子树的所有结点都小于等于6、右子树的所有结点都大于等于6。同时在任意子树的内部，也满足这个条件——比如左子树中，根结点值为3，根结点对应左子树的所有结点都小于等于3、右子树的所有结点都大于等于3。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审里验证函数只检查每个结点的左孩子不大于它、右孩子不小于它；页面上的树为根 <code>10</code>、左子树中出现结点 <code>12</code>，为什么局部检查会误判？</strong></p>\\n<p>结点 <code>12</code> 虽可能满足其直接父结点的局部关系，却违反了整个左子树都不大于根 <code>10</code> 的约束。二叉搜索树的条件作用于每棵子树的全部结点，验证时必须携带祖先限定，不能只比较父子三元组。</p>\\n</li>\\n<li>\\n<p><strong>你要给后台树形配置页实现 <code>isBST</code>，数据由接口反序列化而来且可能为空；递归代码应怎样传递约束，才能覆盖深层非法结点？</strong></p>\\n<p>空树应直接判定为合法，非空结点则同时接受祖先传下来的下界与上界。检查当前值落在允许区间后，左子树收紧上界、右子树收紧下界并继续递归；边界是否包含等号必须与重复值规则一致。</p>\\n</li>\\n<li>\\n<p><strong>订单索引允许多个结点拥有相同键值，开发者一处把相等值插到左侧，另一处搜索时只向右找相等值，这种约束变化该怎么处理？</strong></p>\\n<p>定义允许左子树值小于等于根、右子树值大于等于根，因此相等值可能出现在任一侧，但实现仍需统一契约。插入、搜索、验证和删除必须采用兼容策略，或把重复次数集中记录；否则树形式合法，业务查找仍可能漏项。</p>\\n</li>\\n<li>\\n<p><strong>线上搜索偶发找不到已写入的配置，日志显示从根结点按大小选分支后提前走到空结点；你会怎样判断是搜索代码还是树结构损坏？</strong></p>\\n<p>先沿失败搜索路径记录当前值与选择方向，再校验每个祖先对应子树的整体取值范围。若某个后代越过祖先边界，按大小剪枝必然跳过它，故障在构建或修改阶段；若结构满足约束，再检查相等值的分支规则是否一致。</p>\\n</li>\\n<li>\\n<p><strong>团队为只读查询页争论使用二叉搜索树还是有序数组，数据加载后不再修改且页面频繁按位置读取，你会依据什么做取舍？</strong></p>\\n<p>二叉搜索树强调按数据域维持递归有序，但它并不天然提供数组式的直接下标访问。数据固定且按位置读取占主导时，有序数组通常更贴合访问路径；若需要频繁插入，数组移动元素的代价才会让树形结构更有讨论价值。</p>\\n</li>\\n<li>\\n<p><strong>调试工具把一棵树中序输出为非递减序列，评审者据此认定它满足二叉搜索树定义；在允许重复值的前提下，这个判断为什么成立或可能失效？</strong></p>\\n<p>若中序遍历覆盖全部结点且输出非递减，它与左子树值不大于根、右子树值不小于根的递归约束相符，可作为验证依据。若遍历漏结点、比较器与建树规则不一致，或业务要求重复值只能放固定一侧，仅看数值序列就不足以验证额外契约。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"二叉搜索树-编码基本功\\"><a class=\\"header-anchor\\" href=\\"#二叉搜索树-编码基本功\\">#</a> 二叉搜索树：编码基本功</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>查找、插入、删除都从根开始比较目标值，每次只进入可能包含答案的一侧</li>\\n<li>插入必须把递归返回的新子树根重新赋给 <code>node.left/right</code>，才能接回原树</li>\\n<li>删除分叶子、单孩子、双孩子三类；双孩子可用右子树最小值或左子树最大值替换</li>\\n</ul>\\n<p><strong>二叉搜索树的查找、插入和删除，本质上都是从根节点开始比较，并且只处理可能包含目标的一侧。</strong> 查找沿着值的大小关系继续向下，插入则把新节点放到查找路径遇到的空位。递归插入或删除后，要把返回的新子树根重新赋给 <code>node.left</code> 或 <code>node.right</code>，否则修改后的结构接不回原树。删除时需要区分叶子、单孩子和双孩子，双孩子可用右子树最小值或左子树最大值替换。</p>\\n</section>\\n<blockquote>\\n<p>关于二叉搜索树，大家需要掌握以下高频操作：</p>\\n</blockquote>\\n<ul>\\n<li>查找数据域为某一特定值的结点</li>\\n<li>插入新结点</li>\\n<li>删除指定结点</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品管理页把价格依次按升序插入普通二叉搜索树，后来查最大价格时一路走右孩子；有人仍宣称查找固定是 <code>O(log n)</code>，你怎么反驳？</strong></p>\\n<p>升序插入可能让普通二叉搜索树退化成只有右孩子的链，查找最大值需要沿整条链访问。复杂度应按树高写成 <code>O(h)</code>，最坏情况下 <code>h</code> 可随结点数线性增长；只有额外保证树高受控时，才能使用对数级结论。</p>\\n</li>\\n<li>\\n<p><strong>你在订单索引模块实现递归插入，插入叶子成功后页面查询却始终找不到新订单；代码调用了 <code>insert(node.left, value)</code>，但没有写回也没有 <code>return node</code>，问题出在哪里？</strong></p>\\n<p>新结点只成为递归调用中的局部结果，没有重新连接到父结点的 <code>left</code> 或 <code>right</code>。递归函数应返回更新后的子树根，父层再执行类似 <code>node.left = insert(node.left, value)</code>，最后返回当前 <code>node</code>；遗漏任一层写回都会让修改丢失。</p>\\n</li>\\n<li>\\n<p><strong>用户编号索引原本保证值唯一，现在产品要求允许重复编号；查找、插入和删除仍沿用原代码，会出现什么契约冲突？</strong></p>\\n<p>原实现默认一个值对应一个结点，允许重复后必须先规定重复值放左侧、放右侧，还是在结点中维护计数。查找和删除必须遵循同一约定，否则可能漏查或只删除任意一个副本；若业务仍要求集合语义，更稳妥的做法是在插入入口拒绝重复值。</p>\\n</li>\\n<li>\\n<p><strong>线上删除一个同时有左右孩子的分类结点后，中序结果出现重复值，随后部分查找落到了意外结点；你会优先检查哪段代码？</strong></p>\\n<p>优先检查双孩子删除分支是否只复制了前驱或后继的值，却没有继续从来源子树删除原结点。复制后两个位置暂时持有相同值，必须递归删除来源结点并把更新后的子树根写回；若结点还承载其他业务字段，仅复制 <code>val</code> 也可能造成数据错配。</p>\\n</li>\\n<li>\\n<p><strong>团队争论删除双孩子结点时该用左子树最大值还是右子树最小值，评审会上你如何定方案？</strong></p>\\n<p>两种替代值都能维持二叉搜索树的有序关系，关键是选定一种并完整实现来源子树的后续删除。应结合现有重复值策略、辅助函数和测试覆盖保持一致，而不是混用两套规则；普通二叉搜索树还可能因长期操作失衡，替代方向本身不提供平衡保证。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"查找数据域为某一特定值的结点\\"><a class=\\"header-anchor\\" href=\\"#查找数据域为某一特定值的结点\\">#</a> 查找数据域为某一特定值的结点</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>当前值等于目标就返回；目标更小只走左子树，更大只走右子树</li>\\n<li>复杂度统一写 <code>O(h)</code>：平衡树为 <code>O(log n)</code>，退化链为 <code>O(n)</code></li>\\n<li>比较的是键序而不是遍历全部节点；空树或查找失败返回 <code>null</code></li>\\n<li>重复键策略必须与插入、验证保持一致，不能在不同函数里临时决定</li>\\n</ul>\\n<p><strong>查找时比较当前节点和目标值：相等就返回，更小走左子树，更大走右子树。</strong> 这样做利用了二叉搜索树的有序性，不需要遍历无关分支；走到空节点时，就说明目标不存在，可以返回 <code>null</code>。时间复杂度取决于树高 <code>h</code>，统一写作 <code>O(h)</code>，平衡时是 <code>O(log n)</code>，退化成链时是 <code>O(n)</code>。如果允许重复键，查找规则还要和插入、校验时的约定保持一致。</p>\\n</section>\\n<p>假设这个目标结点的数据域值为 n，我们借助二叉搜索树数据域的有序性，可以有以下查找思路：</p>\\n<ul>\\n<li>递归遍历二叉树，若当前遍历到的结点为空，就意味着没找到目标结点，直接返回。</li>\\n<li>若当前遍历到的结点对应的数据域值刚好等于n，则查找成功，返回。</li>\\n<li>若当前遍历到的结点对应的数据域值大于目标值n，则应该在左子树里进一步查找，设置下一步的遍历范围为 root.left 后，继续递归。</li>\\n<li>若当前遍历到的结点对应的数据域值小于目标值n，则应该在右子树里进一步查找，设置下一步的遍历范围为 root.right 后，继续递归。</li>\\n</ul>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>function search(root, n) {\\n    // 若 root 为空，查找失败，直接返回\\n    if(!root) {\\n        return \\n    }\\n    // 找到目标结点，输出结点对象\\n    if(root.val === n) {\\n        console.log(&#39;目标结点是：&#39;, root)\\n    } else if(root.val &gt; n) {\\n        // 当前结点数据域大于n，向左查找\\n        search(root.left, n)\\n    } else {\\n        // 当前结点数据域小于n，向右查找\\n        search(root.right, n)\\n    }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>搜索页用普通二叉搜索树保存按时间递增的任务编号，数据达到数万条后查找最新编号明显变慢；同事说二叉搜索树查找天然是 <code>O(log n)</code>，你如何判断？</strong></p>\\n<p>按递增顺序插入可能使树退化为持续向右延伸的链，查找最新编号就要逐层访问大量结点。普通二叉搜索树的查找复杂度应记为 <code>O(h)</code>，其中 <code>h</code> 是实际树高；没有平衡约束时，不能把它固定描述为 <code>O(log n)</code>。</p>\\n</li>\\n<li>\\n<p><strong>详情页调用 <code>search(root, n)</code> 后想展示目标结点，但现有实现只在命中时执行 <code>console.log</code>，调用方拿到的却是 <code>undefined</code>；你会怎样改接口？</strong></p>\\n<p>命中分支应直接 <code>return root</code>，向左或向右递归时也必须 <code>return search(...)</code>，空结点则明确返回 <code>null</code> 或 <code>undefined</code>。这样结果才能沿调用栈传回页面逻辑，而不是只产生日志副作用；返回空值的约定还需与调用方判空方式统一。</p>\\n</li>\\n<li>\\n<p><strong>账户索引允许相同手机号对应多条待合并记录，但插入规则没有写清重复值放哪边；现在 <code>search</code> 在首次相等时就返回，能否满足查询需求？</strong></p>\\n<p>首次相等只能找到某一个匹配结点，无法保证收集全部重复记录。必须先明确重复值的存储契约，例如固定放在一侧、在结点中维护集合，或改用其他索引结构，再让查找逻辑与插入规则一致；契约缺失时继续扩展递归无法保证完整性。</p>\\n</li>\\n<li>\\n<p><strong>线上搜索偶发漏数据，你打印路径后发现目标值比当前结点小，却进入了 <code>right</code>；除了修正分支，还应验证哪些树结构问题？</strong></p>\\n<p>应先核对比较条件是否遵循“大于目标走左、低于目标走右”，再检查插入或删除后是否仍保持同一有序约束。可用中序遍历检查值序列是否有序，并构造根结点、叶子、空树和不存在目标值等用例；若树本身已损坏，只修查找分支仍会继续漏查。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中一方坚持保留递归查找，另一方担心退化树导致调用栈过深，页面线程不稳定；在不改变二叉搜索树结构的前提下你怎么选？</strong></p>\\n<p>两种写法都利用相同的有序比较规则，但迭代版本用循环推进 <code>root.left</code> 或 <code>root.right</code>，不会为每层额外占用递归调用栈。若树高缺乏约束或输入可能形成长链，迭代实现更稳妥；它仍无法改善退化后的线性查找成本，只降低栈溢出的风险。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"插入新结点\\"><a class=\\"header-anchor\\" href=\\"#插入新结点\\">#</a> 插入新结点</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>沿查找路径走到第一个空孩子，再把新节点挂到父节点对应一侧</li>\\n<li>必须先规定重复值策略：拒绝、节点计数或固定放一侧</li>\\n<li>时间 <code>O(h)</code>、新节点额外空间 <code>O(1)</code>；有序连续插入可能退化成链</li>\\n<li>插入后仍要满足所有左子树键更小、右子树键更大的全局不变量</li>\\n</ul>\\n<p><strong>插入节点就是沿着查找路径向下比较，直到遇到第一个空位置，再把新节点放进去。</strong> 目标值小于当前值就进入左子树，否则进入右子树；递归写法必须把返回值重新赋给 <code>root.left</code> 或 <code>root.right</code>，才能把新节点接回整棵树。时间复杂度是 <code>O(h)</code>，新节点本身只需要 <code>O(1)</code> 额外空间，但连续有序插入可能让树退化成链。遇到重复值时，还要提前约定拒绝、计数或固定放到一侧。</p>\\n</section>\\n<blockquote>\\n<p>插入结点的思路其实和寻找结点非常相似。大家反思一下，在上面寻找结点的时候，为什么我们会在判定当前结点为空时，就认为查找失败了呢？\\n这是因为，二叉搜索树的查找路线是一个非常明确的路径：我们会根据当前结点值的大小，决定路线应该是向左走还是向右走。如果最后走到了一个空结点处，这就意味着我们没有办法再往深处去搜索了，也就没有了找到目标结点的可能性。</p>\\n</blockquote>\\n<p>换一个角度想想，如果这个空结点所在的位置恰好有一个值为 n 的结点，是不是就可以查找成功了？那么如果我把 n 值塞到这个空结点所在的位置，是不是刚好符合二叉搜索树的排序规则？</p>\\n<blockquote>\\n<p>实不相瞒，二叉搜索树插入结点的过程，和搜索某个结点的过程几乎是一样的：从根结点开始，把我们希望插入的数据值和每一个结点作比较。若大于当前结点，则向右子树探索；若小于当前结点，则向左子树探索。最后找到的那个空位，就是它合理的栖身之所。</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>function insertIntoBST(root, n) {\\n    // 若 root 为空，说明当前是一个可以插入的空位\\n    if(!root) { \\n        // 用一个值为n的结点占据这个空位\\n        root = new TreeNode(n)\\n        return root\\n    }\\n    \\n    if(root.val &gt; n) {\\n        // 当前结点数据域大于n，向左查找\\n        root.left = insertIntoBST(root.left, n)\\n    } else {\\n        // 当前结点数据域小于n，向右查找\\n        root.right = insertIntoBST(root.right, n)\\n    }\\n\\n    // 返回插入后二叉搜索树的根结点\\n    return root\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品页依次插入价格 <code>10</code>、<code>20</code>、<code>30</code> 后，新同事说每次插入都只要走 <code>O(log n)</code>，你会怎样纠正？</strong></p>\\n<p>普通二叉搜索树不保证每次插入都是 <code>O(log n)</code>，这组升序数据会让结点连续落到右侧，最终退化成链。插入耗时取决于树高，平衡时接近 <code>O(log n)</code>，最坏会变成 <code>O(n)</code>，递归实现还可能因树过深触发栈溢出。</p>\\n</li>\\n<li>\\n<p><strong>后台批量导入用户编号时，<code>insertIntoBST(root, n)</code> 执行成功却没有改变调用方的空树，最可能漏了什么？</strong></p>\\n<p>调用方很可能没有接收函数返回的新根，应写成 <code>root = insertIntoBST(root, n)</code>。递归中同样必须把结果赋回 <code>root.left</code> 或 <code>root.right</code>，因为插入位置原本是空引用；若只创建 <code>new TreeNode(n)</code> 而不回接，结点会丢失。</p>\\n</li>\\n<li>\\n<p><strong>订单索引允许重复订单金额，而代码把所有不小于当前值的数据放到右侧，校验逻辑仍要求右子树严格大于根，会出现什么冲突？</strong></p>\\n<p>插入策略与校验不变量发生了冲突，重复值落到右侧后会被严格区间校验判为非法。要么在结点中维护 <code>count</code>，要么明确采用“左侧严格小于、右侧大于等于”的规则，并同步修改查找、删除和校验；混用规则会造成结果不一致。</p>\\n</li>\\n<li>\\n<p><strong>线上搜索页偶发缺少刚插入的关键词，日志显示递归已经走到空位并创建结点，你会沿哪条引用链排查？</strong></p>\\n<p>先检查每一层是否执行了 <code>root.left = ...</code> 或 <code>root.right = ...</code>，再确认最外层是否保存返回的根结点。随后核对比较分支与业务约定，尤其是相等值究竟应计数、覆盖还是固定放一侧；创建成功不等于已经挂入可达的树结构。</p>\\n</li>\\n<li>\\n<p><strong>百万级编号以接近有序的顺序导入，团队在“保留普通 <code>BST</code>”与“改用平衡树”之间争论，你如何定方案？</strong></p>\\n<p>若只做一次构建且数据已排序，可直接按中点递归构造，避免逐个插入形成长链；若持续动态插入并要求稳定的查找延迟，应选择能维持高度的平衡树。普通 <code>BST</code> 实现简单，但性能受输入顺序支配，不能把平均情况当作线上保证。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"删除指定结点\\"><a class=\\"header-anchor\\" href=\\"#删除指定结点\\">#</a> 删除指定结点</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>叶子节点直接删除；只有一个孩子时让父节点改接该孩子</li>\\n<li>两个孩子时用右子树最小值（中序后继）替换，再删除原后继</li>\\n<li>后继没有左孩子，因此第二次删除自动降级为至多一个孩子的情况</li>\\n<li>查找与结构修复都沿树高进行，时间 <code>O(h)</code>；根节点删除要单独更新返回值</li>\\n</ul>\\n<p><strong>删除节点时，叶子直接移除，单孩子节点由父节点改接它的孩子，双孩子节点则用有序位置上的相邻节点替换。</strong> 我一般选择右子树最小值，也就是中序后继，覆盖当前值后，再到右子树中删除原后继。因为这个后继没有左孩子，第二次删除会自然降级为叶子或单孩子的情况；也可以对称地使用左子树最大值。整个过程沿树高进行，时间为 <code>O(h)</code>，删除根节点时要用函数返回值更新根引用。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“删除的难点只有两个孩子：我用中序后继替换当前值，随后问题降级成删除至多一个孩子的节点。”</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>管理后台删除值为 <code>8</code> 的结点时，它同时有左右孩子；候选人直接把整棵右子树挂到父结点上，哪里会破坏排序关系？</strong></p>\\n<p>直接提升整棵右子树会丢失或错误安置原左子树，无法保证所有较小值仍位于正确位置。应以中序后继覆盖当前值，再删除右子树中的该后继，使任务降级为删除至多一个孩子的结点；引用更新遗漏仍会造成结点丢失。</p>\\n</li>\\n<li>\\n<p><strong>实现 <code>deleteNode</code> 时选了右子树最小结点作为后继，代码复制后继值后却没有继续删除原后继，页面会出现什么现象？</strong></p>\\n<p>中序结果会保留两个相同值，相当于只复制而没有完成结构删除。覆盖目标值后，必须在右子树中递归删除该后继，并把返回的新子树根重新赋给右孩子；若业务本就允许重复值，还需按统一的重复值规则处理。</p>\\n</li>\\n<li>\\n<p><strong>库存树允许结点维护 <code>count</code> 表示重复 <code>SKU</code>，删除一个 <code>SKU</code> 时还能无条件执行后继替换吗？</strong></p>\\n<p>当目标结点的 <code>count</code> 大于 <code>1</code> 时，应先递减计数，不必改变树结构。只有计数降到需要移除整个结点时，才按零个、一个或两个孩子的分支删除；若复制后继数据，还要连同计数语义一起设计，避免凭空复制重复量。</p>\\n</li>\\n<li>\\n<p><strong>线上删除根结点后接口仍返回旧数据，但删除普通叶子正常，你会优先检查哪处？</strong></p>\\n<p>优先检查调用方是否接收删除函数返回的新根，例如 <code>root = deleteNode(root, key)</code>。根结点没有父引用可供回接，删除后可能变成某个孩子或后继结点；若仍沿用旧 <code>root</code>，内部递归即使正确，外部入口也不会更新。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中一方坚持用中序后继，另一方要求改用中序前驱，哪一方在正确性上更占优？</strong></p>\\n<p>两者在正确性上是对称的：后继取右子树最小值，前驱取左子树最大值，都能保持中序有序。选择应与现有实现、重复值规则及子树回接逻辑保持一致；单纯替换方向不会自动改善树高，也不能省略第二次删除。</p>\\n</li>\\n</ul>\\n</section>\\n<blockquote>\\n<p>想要删除某个结点，首先要找到这个结点。在定位结点后，我们需要考虑以下情况：</p>\\n</blockquote>\\n<ul>\\n<li>结点不存在，定位到了空结点。直接返回即可。</li>\\n<li>需要删除的目标结点没有左孩子也没有右孩子——它是一个叶子结点，删掉它不会对其它结点造成任何影响，直接删除即可。</li>\\n<li>需要删除的目标结点存在左子树，那么就去左子树里寻找小于目标结点值的最大结点，用这个结点覆盖掉目标结点</li>\\n<li>需要删除的目标结点存在右子树，那么就去右子树里寻找大于目标结点值的最小结点，用这个结点覆盖掉目标结点</li>\\n<li>需要删除的目标结点既有左子树、又有右子树，这时就有两种做法了：要么取左子树中值最大的结点，要么取右子树中取值最小的结点。两个结点中任取一个覆盖掉目标结点，都可以维持二叉搜索树的数据有序性</li>\\n</ul>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>function deleteNode(root, n) {\\n    // 如果没找到目标结点，则直接返回\\n    if(!root) {\\n        return root\\n    }\\n    // 定位到目标结点，开始分情况处理删除动作\\n    if(root.val === n) {\\n        // 若是叶子结点，则不需要想太多，直接删除\\n        if(!root.left &amp;&amp; !root.right) {\\n            root = null\\n        } else if(root.left) {\\n            // 寻找左子树里值最大的结点\\n            const maxLeft = findMax(root.left)\\n            // 用这个 maxLeft 覆盖掉需要删除的当前结点  \\n            root.val = maxLeft.val\\n            // 覆盖动作会消耗掉原有的 maxLeft 结点\\n            root.left = deleteNode(root.left, maxLeft.val)\\n        } else {\\n            // 寻找右子树里值最小的结点\\n            const minRight = findMin(root.right)\\n            // 用这个 minRight 覆盖掉需要删除的当前结点  \\n            root.val = minRight.val\\n            // 覆盖动作会消耗掉原有的 minRight 结点\\n            root.right = deleteNode(root.right, minRight.val)\\n        }\\n    } else if(root.val &gt; n) {\\n        // 若当前结点的值比 n 大，则在左子树中继续寻找目标结点\\n        root.left = deleteNode(root.left, n)\\n    } else  {\\n        // 若当前结点的值比 n 小，则在右子树中继续寻找目标结点\\n        root.right = deleteNode(root.right, n)\\n    }\\n    return root\\n}\\n\\n// 寻找左子树最大值\\nfunction findMax(root) {\\n    while(root.right) {\\n        root = root.right\\n    }\\n    return root \\n}\\n\\n// 寻找右子树的最小值\\nfunction findMin(root) {\\n    while(root.left) {\\n        root = root.left\\n    }\\n    return root\\n}\\n</code></pre>\\n<h3 id=\\"编码复盘\\"><a class=\\"header-anchor\\" href=\\"#编码复盘\\">#</a> 编码复盘</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>每个 BST 函数都要明确输入/返回的是结点还是整棵子树根，修改型递归通常返回新根</li>\\n<li>查找可迭代避免深树调用栈，插入删除要覆盖空树、根结点和不存在目标</li>\\n<li>测试不能只看目标值，还要在每次操作后做中序检查，验证全树仍有序</li>\\n</ul>\\n<p><strong>删除同时拥有左右子树的结点时，既可以用左子树最大值替换，也可以选择右子树最小值。</strong> 固定从一侧取值虽然正确，但反复操作可能让左右子树的高度越来越不均衡。如果题目还要求兼顾平衡，我会记录子树高度，优先从较高的一侧选择替换结点。</p>\\n</section>\\n<p>上面这段代码展示了二叉搜索树删除的基本思路：在这个思路的基础上，大家可以做很多个性化的修改。</p>\\n<p>举个例子，细心的同学会发现，如果目标结点既有左子树又有右子树，那么在上面这段逻辑里，会优先去找它左子树里的最大值，而不会去 care 右子树的最小值这个选项。</p>\\n<p>这样做，得到的结果从正确性上来说是没问题的，但是却不太美观：每次都删除一侧子树的结点，会导致二叉树的左右子树高度不平衡。</p>\\n<p>如果题目中要求我们顾及二叉树的平衡度，那么我们就可以在删除的过程中记录子树的高度，每次选择高度较高的子树作为查找目标，用这个子树里的结点去覆盖需要删除的目标结点。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>删除接口每次都拿左子树最大值覆盖双子结点，测试说中序序列始终有序，因此不会产生结构问题，你同意吗？</strong></p>\\n<p>中序有序只能证明排序不变量仍成立，不能证明树形足够均衡。长期固定从左侧取前驱，会反复削减同一侧，可能使左右子树高度差扩大；功能测试应同时观察结构高度，但普通 <code>BST</code> 本身仍不承诺平衡。</p>\\n</li>\\n<li>\\n<p><strong>内容平台准备改删除逻辑：双子结点删除时先记录左右子树高度，再从较高一侧选前驱或后继，代码要怎样落地？</strong></p>\\n<p>左侧较高时取左子树最大值，右侧较高时取右子树最小值，覆盖目标后再到对应子树删除替代结点。若每次临时遍历计算高度，删除成本会增加；若缓存高度，则每次插入和删除后都必须沿路径更新。</p>\\n</li>\\n<li>\\n<p><strong>产品要求在频繁插入、删除后始终保持严格的高度约束，只采用“从较高子树取替代结点”够不够？</strong></p>\\n<p>不够，这只是删除替代方向的启发式选择，不能保证每次操作后满足严格平衡条件。若高度上界是硬约束，应采用带旋转或其他再平衡机制的树结构；仅记录高度而不做结构调整，仍可能在特定输入下退化。</p>\\n</li>\\n<li>\\n<p><strong>上线后监控发现树高不断增长，但中序输出与排序数组完全一致，你会怎样定位是策略偏斜还是高度字段失真？</strong></p>\\n<p>先用递归重新计算真实高度，并与结点缓存值逐一比对，区分结构确实偏斜还是维护字段错误。再统计双子结点删除时前驱、后继的选择及删除后的高度变化；若缓存更新漏过祖先，基于“较高侧”的后续选择也会持续失准。</p>\\n</li>\\n<li>\\n<p><strong>评审会上有人主张每次删除都算高度，另一人要求结点永久保存 <code>height</code>，两种方案怎样取舍？</strong></p>\\n<p>临时计算无需维护额外状态，实现更稳妥，但频繁删除时会重复遍历子树。缓存 <code>height</code> 能直接支持选择，却增加插入、删除和结构调整的维护负担，任何漏更新都会污染决策；应根据操作频率与正确性复杂度选择，不能把启发式当作平衡树替代品。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"二叉搜索树的特性\\"><a class=\\"header-anchor\\" href=\\"#二叉搜索树的特性\\">#</a> 二叉搜索树的特性</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>中序有序支持第 k 小、范围查询和有序输出；搜索路径上的比较支持最近公共祖先等剪枝</li>\\n<li>性能取决于高度而不是结点总数的对数承诺，普通 BST 不自动平衡</li>\\n<li>范围查询只需进入可能与区间相交的子树，时间与树高及输出量相关</li>\\n</ul>\\n<p><strong>二叉搜索树最需要记住的特性，就是它的中序遍历结果天然有序。</strong> 这个性质来自左子树、根结点和右子树之间稳定的大小关系，也是很多题目能够简化处理的关键。遇到相关问题时，我一般会先判断能否直接利用这份有序结果。</p>\\n</section>\\n<p>关于二叉搜索树的特性，有且仅有一条是需要大家背诵默写的：</p>\\n<p>二叉搜索树的中序遍历序列是有序的！</p>\\n<p>OK，基本功就修炼到这里，下面大家一起来开开心心地碾碎真题吧！</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>搜索结果页把 <code>BST</code> 的先序遍历输出当成升序列表，样例只有三个结点时碰巧通过，你会给出什么反例？</strong></p>\\n<p>根为 <code>2</code>、左孩子为 <code>1</code>、右孩子为 <code>3</code> 时，先序结果是 <code>2,1,3</code>，并非升序。<code>BST</code> 能直接得到有序序列的是中序遍历，即按“左子树、当前结点、右子树”访问；重复值是否严格递增取决于既定存储规则。</p>\\n</li>\\n<li>\\n<p><strong>报表页需要把树中所有价格按升序流式输出，不允许先收集后排序，遍历器应怎样写？</strong></p>\\n<p>使用中序遍历，在访问左子树后输出当前值，再访问右子树，即可利用 <code>BST</code> 的局部大小关系生成有序结果。若树可能很深，迭代实现应使用显式栈规避递归栈风险；额外空间仍与树高相关。</p>\\n</li>\\n<li>\\n<p><strong>业务把规则改成“左侧可等于当前值、右侧必须更大”，中序序列还能作为合法性判断的唯一证据吗？</strong></p>\\n<p>中序序列仍应非递减，但仅检查非递减无法确认重复值被放在规定的左侧。验证结构时还要沿递归路径传递带开闭边界的取值区间，确保相等值只出现在允许方向；否则一棵违反放置规则的树也可能产出相同序列。</p>\\n</li>\\n<li>\\n<p><strong>线上校验器报告中序结果局部逆序 <code>12,9</code>，但单点搜索大多正常，你会从哪些结构错误开始查？</strong></p>\\n<p>先定位产生逆序的相邻访问位置，再检查对应祖先的左右引用是否接反，或插入、删除后是否把返回的子树根漏接。还要排查值覆盖后是否删除了错误的前驱或后继；搜索偶尔命中不能证明全局排序不变量成立。</p>\\n</li>\\n<li>\\n<p><strong>接口要返回区间 <code>[low, high]</code> 内的值，团队一方主张完整中序后过滤，另一方要求利用 <code>BST</code> 剪枝，你支持哪种？</strong></p>\\n<p>在树仍满足 <code>BST</code> 不变量时应利用边界剪枝：当前值小于 <code>low</code> 就无需访问左侧，大于 <code>high</code> 就无需访问右侧，区间内按中序收集。完整遍历更简单且适合先验证数据，但会访问无关结点；若结构可能损坏，剪枝反而可能漏掉异常位置的数据。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"真题实战环节\\"><a class=\\"header-anchor\\" href=\\"#真题实战环节\\">#</a> 真题实战环节</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>做 BST 真题先判断能否利用有序性剪掉一半分支，再决定遍历还是按路径搜索</li>\\n<li>验证题传上下界；构造平衡 BST 取中点；第 k 小使用中序，方法与性质一一对应</li>\\n<li>题目若允许重复值、修改原树或要求返回新树，必须先澄清契约</li>\\n</ul>\\n<p><strong>做二叉搜索树题时，核心就是把定义和中序有序这个特性用到具体条件里。</strong> 像验证二叉搜索树，本质上考的是每棵子树是否持续满足左侧更小、右侧更大的限制。题目一旦涉及重复值，还要先确认等于根结点的值是否被允许，避免套错判断条件。</p>\\n</section>\\n<p>开篇我们说过，我们只要能够把握好二叉搜索树的限制条件（即定义）和特性，就足以应对大部分的考题。下面我们就来看看定义和特性的考察是如何在真题中体现的：</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一道题只写“二叉树中查找值 <code>7</code>”，候选人看到树形像 <code>BST</code> 就立即按大小剪枝；作为面试官你会指出什么前提缺失？</strong></p>\\n<p>只有题目明确满足 <code>BST</code> 定义时，才能根据当前值决定只走左侧或右侧。若它只是普通二叉树，按大小剪枝可能直接跳过目标；解题前应先确认结构约束，不能从示意图外观推断隐藏条件。</p>\\n</li>\\n<li>\\n<p><strong>在线判题要求验证一棵输入树是否为 <code>BST</code>，候选人只比较每个结点与两个直接孩子，为什么会漏错？</strong></p>\\n<p>局部父子比较无法约束更深后代，例如某个结点位于根的左子树却大于根，仍可能通过直接孩子检查。应为递归路径维护全局上下界，或检查中序序列是否满足约定的有序性；存在重复值时还必须明确边界是否包含等号。</p>\\n</li>\\n<li>\\n<p><strong>题目把键从唯一整数改成允许重复值，但没有说明重复值放哪边，你会要求候选人先补充什么约定？</strong></p>\\n<p>必须先约定重复值是合并为 <code>count</code>，还是固定放在左侧或右侧，因为这会改变插入、搜索、删除与合法性验证。若题目不给规则，只能明确陈述自己的假设并保持全程一致；直接套用严格大小关系会让边界样例含糊。</p>\\n</li>\\n<li>\\n<p><strong>判题只在大规模近似有序输入上超时，小样例全部正确，代码又反复使用 <code>BST</code> 单路径搜索，你会怎样排查？</strong></p>\\n<p>先测量实际树高并检查输入是否按近似升序逐个插入，普通 <code>BST</code> 此时可能退化成长链，单次操作不再接近 <code>O(log n)</code>。还应确认递归深度是否逼近运行环境上限；若题目要求稳定复杂度，需要平衡结构或改变构建方式。</p>\\n</li>\\n<li>\\n<p><strong>一道真题既能用中序遍历，也能利用大小关系沿单一路径求解，面试现场怎样判断选哪种？</strong></p>\\n<p>若目标依赖完整有序序列或需要访问全部结点，中序遍历更直接；若目标值能与当前结点比较并排除一整侧，应优先沿单一路径搜索。最终复杂度仍受树高控制，普通 <code>BST</code> 退化后单路径也可能达到 <code>O(n)</code>，因此要同时说明结构前提。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"对定义的考察-二叉搜索树的验证\\"><a class=\\"header-anchor\\" href=\\"#对定义的考察-二叉搜索树的验证\\">#</a> 对定义的考察：二叉搜索树的验证</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>不能只检查 <code>left &lt; root &lt; right</code>，深层节点还受全部祖先约束。</li>\\n<li>递归传递开区间 <code>(lower, upper)</code>：左子树收紧上界，右子树抬高下界。</li>\\n<li>时间 <code>O(n)</code>，递归空间 <code>O(h)</code>；默认严格 BST 不接受重复值。</li>\\n</ul>\\n<p><strong>验证二叉搜索树不能只比较父子结点，还要保证每个结点满足所有祖先共同限定的取值范围。</strong> 我会在递归时传入开区间 <code>(minValue, maxValue)</code>，进入左子树就收紧上界，进入右子树就抬高下界。空树直接合法；题目不考虑相等时，碰到边界值也要返回 <code>false</code>。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“每个节点不仅和父节点比较，还必须落在祖先共同限定的区间里。我用上下界随递归向下传递。”</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品分类页传入一棵树，某节点小于父节点却大于根节点，代码只比较 <code>node.val &lt; parent.val</code>，为什么仍会把非法树判成合法？</strong></p>\\n<p>只比较父子关系会遗漏祖先施加的约束，该节点虽然满足局部大小关系，却可能越过根节点限定的范围。递归时应携带当前节点允许的上下界，进入左、右子树时分别收紧上界或下界；若边界没有沿路径传递，深层违规仍会漏检。</p>\\n</li>\\n<li>\\n<p><strong>后台一次校验几十万节点的导入数据，递归版在浏览器中出现调用栈溢出，你会怎样保留上下界判定逻辑？</strong></p>\\n<p>应改用显式栈，每个待处理项同时保存 <code>node</code>、<code>lower</code> 和 <code>upper</code>，弹出后先检查区间，再把子节点与收紧后的边界压栈。这样不依赖 <code>JavaScript</code> 调用栈，但辅助空间仍与待处理节点数量相关，极宽或极深的树都要关注内存峰值。</p>\\n</li>\\n<li>\\n<p><strong>订单索引树允许重复编号，产品要求重复值只能进入右子树，原来的严格上下界比较要怎样调整？</strong></p>\\n<p>应先把规则明确为左侧严格小于、右侧大于等于，再让边界同时携带数值与开闭属性。进入右子树时，下界取当前值且设为闭区间；若只把两侧比较都改成非严格，重复值也会被错误地接受在左子树中。</p>\\n</li>\\n<li>\\n<p><strong>线上校验偶发误判，节点值可能是 <code>BigInt</code>，实现却用 <code>-Infinity</code> 和 <code>Infinity</code> 初始化边界，你会先查哪里？</strong></p>\\n<p>先检查比较过程中是否混用了 <code>BigInt</code> 与 <code>Number</code>，以及哨兵是否迫使值进入不兼容或有精度风险的比较路径。更稳妥的实现是用可空边界表示“尚无限制”，仅在边界存在时比较；同时核对反序列化是否已把大整数改成普通浮点数。</p>\\n</li>\\n<li>\\n<p><strong>评审会上有人主张中序遍历只保存前一个值，另一人坚持递归传上下界；验证严格二叉搜索树时你如何选？</strong></p>\\n<p>两者都能正确验证严格二叉搜索树：中序序列必须严格递增，而上下界方案直接表达祖先共同约束。只做布尔校验时中序写法状态更少；需要报告节点违反了哪一侧祖先边界时，上下界更容易给出诊断，但两种递归实现都可能受树高限制。</p>\\n</li>\\n</ul>\\n</section>\\n<blockquote>\\n<p>题目描述：给定一个二叉树，判断其是否是一个有效的二叉搜索树。</p>\\n</blockquote>\\n<ul>\\n<li>假设一个二叉搜索树具有如下特征：</li>\\n<li>节点的左子树只包含小于当前节点的数。</li>\\n<li>节点的右子树只包含大于当前节点的数。</li>\\n<li>所有左子树和右子树自身必须也是二叉搜索树。</li>\\n</ul>\\n<p>示例 1:</p>\\n<pre class=\\"language-text\\"><code>输入:\\n\\n    2\\n   / \\\\\\n  1   3\\n输出: true\\n</code></pre>\\n<p>示例 2:</p>\\n<pre class=\\"language-text\\"><code>输入:\\n\\n    5\\n   / \\\\\\n  1   4\\n     / \\\\\\n    3   6\\n输出: false\\n</code></pre>\\n<p>解释: 输入为: <code>[5,1,4,null,null,3,6]</code>。\\n根节点的值为 5 ，但是其右子节点值为 4</p>\\n<p><strong>思路分析</strong></p>\\n<p>对于这道题，我们需要好好咀嚼一下二叉搜索树的定义：</p>\\n<ul>\\n<li>它可以是一棵空树</li>\\n<li>它可以是一棵由根结点、左子树、右子树组成的树，同时左子树和右子树都是二叉搜索树，且左子树上所有结点的数据域都小于等于根结点的数据域，右子树上所有结点的数据域都大于等于根结点的数据域</li>\\n</ul>\\n<p>只有符合以上两种情况之一的二叉树，可以称之为二叉搜索树。</p>\\n<p>空树的判定比较简单，关键在于非空树的判定：需要递归地对非空树中的左右子树进行遍历，检验每棵子树中是否都满足 左 &lt; 根 &lt; 右 这样的关系（注意题中声明了不需要考虑相等情况）。</p>\\n<p>基于这样的思路，我们可以编码如下：</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {TreeNode} root\\n * @return {boolean}\\n */\\nconst isValidBST = function(root) {\\n  // 定义递归函数\\n  function dfs(root, minValue, maxValue) {\\n      // 若是空树，则合法\\n      if(!root) {\\n          return true\\n      }\\n      // 若右孩子不大于根结点值，或者左孩子不小于根结点值，则不合法\\n      if(root.val &lt;= minValue || root.val &gt;= maxValue) return false\\n      // 左右子树必须都符合二叉搜索树的数据域大小关系\\n      return dfs(root.left, minValue,root.val) &amp;&amp; dfs(root.right, root.val, maxValue)\\n  }\\n  // 初始化最小值和最大值为极小或极大\\n  return dfs(root, -Infinity, Infinity)\\n};\\n</code></pre>\\n<p><strong>编码复盘</strong></p>\\n<p>这个题的编码实现比较有意思，对 minValue 和 maxValue 的处理值得我们反刍一下：</p>\\n<p>递归过程中，起到决定性作用的是这两个判定条件：</p>\\n<ul>\\n<li>左孩子的值是否小于根结点值</li>\\n<li>右孩子的值是否大于根结点值</li>\\n</ul>\\n<blockquote>\\n<p>在递归式中，如果单独维护一段逻辑，用于判定当前是左孩子还是右孩子，进而决定是进行大于判定还是小于判定，也是没问题的。但是在上面的编码中我们采取了一种更简洁的手法，通过设置 minValue 和 maxValue 为极小和极大值，来确保 root.val &lt;= minValue || root.val &gt;= maxValue 这两个条件中有一个是一定为 false 的。</p>\\n</blockquote>\\n<blockquote>\\n<p>比如当前我需要检查的是root 的左孩子，那么就会进入 dfs(root.left, minValue,root.val) 这段逻辑。这个dfs调用将最大值更新为了root根结点的值，将当前root结点更新为了左孩子结点，同时保持最小值为 -Infinity 不变。进入 dfs逻辑后，root.val &lt;= minValue || root.val &gt;= maxValue 中的 root.val &lt;= minValue 一定为 false ，起决定性作用的条件实际是 root.val &gt;= maxValue（这里这个 maxValue 正是根结点的数据域值)。若root.val &gt;= maxValue返回 true，就意味着左孩子的值大于等于（也就是不小于）根结点的数据域值，这显然是不合法的。此时整个或语句都会返回true，递归式返回false，二叉搜索树进而会被判定为不合法。</p>\\n</blockquote>\\n<h3 id=\\"对特性的考察-将排序数组转化为二叉搜索树\\"><a class=\\"header-anchor\\" href=\\"#对特性的考察-将排序数组转化为二叉搜索树\\">#</a> 对特性的考察：将排序数组转化为二叉搜索树</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>每次选择有序区间中点为根，左半区构建左子树、右半区构建右子树</li>\\n<li>中点让左右节点数最多相差一，生成高度为 <code>O(log n)</code> 的平衡 BST</li>\\n<li>每个数组元素只创建一次节点，时间 <code>O(n)</code>、递归栈 <code>O(log n)</code></li>\\n<li>偶数区间选左中点或右中点都可，但子区间必须严格排除当前中点</li>\\n</ul>\\n<p><strong>每次取有序区间的中点作为根，再用左右半区递归构建左右子树。</strong> 因为数组有序，这样天然满足二叉搜索树的顺序；中点又能让两侧节点数最多相差一，所以树高保持在 <code>O(log n)</code>。每个元素只会创建一次节点，时间复杂度是 <code>O(n)</code>，递归栈为 <code>O(log n)</code>。区间长度为偶数时选左中点或右中点都可以，但递归子区间必须排除已经使用的中点。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“有序数组的中点能把剩余元素均分到左右子树，因此递归选择中点同时满足 BST 有序性与高度平衡。”</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>搜索页把升序数组从左到右逐个插入二叉搜索树，数据一多查询就接近链表，为什么“输入有序”反而造成了退化？</strong></p>\\n<p>顺序插入会让后续元素持续落到同一侧，最终形成高度接近元素数量的单链结构。应直接选择区间中点作为根，再递归构造左右子树，使两侧元素数量尽量接近；若仍按插入流程建树，无法利用数组已排序这一条件保证平衡。</p>\\n</li>\\n<li>\\n<p><strong>前端需要把十万条有序索引转成平衡树，你会怎样避免每层都用 <code>slice()</code> 复制左右数组？</strong></p>\\n<p>应让递归函数接收原数组及左右下标，通过中点划分区间，不实际创建子数组。每个元素只被构造成一个节点，额外开销主要来自树节点和递归深度；若频繁 <code>slice()</code>，会产生不必要的数组分配与复制，增加内存和回收压力。</p>\\n</li>\\n<li>\\n<p><strong>数据列表长度为偶数，设计评审争论必须选左中点还是右中点，否则无法保证平衡，你会如何裁决？</strong></p>\\n<p>选择左中点或右中点都能让左右区间规模至多相差一个，因此都满足高度平衡和二叉搜索树的有序关系。两种选择只会形成不同但同样有效的树形；若快照测试或序列化结果要求稳定，则必须统一中点计算规则，而不是宣称其中一种才正确。</p>\\n</li>\\n<li>\\n<p><strong>接口从“有序数组”改成“有序单链表”，代码仍用下标反复找中点，构建过程明显变慢，你会怎样改？</strong></p>\\n<p>链表不支持常数时间随机访问，递归区间里反复寻找中点会重复扫描节点。可先取得长度，再按中序构造顺序递归生成左子树、消费当前链表游标作为根、最后生成右子树；代价是实现更依赖共享游标及长度边界，游标推进错误会破坏整棵树。</p>\\n</li>\\n<li>\\n<p><strong>测试发现构建后的树中序遍历有序，但部分节点高度差异常，排查时你会分别验证哪些不变量？</strong></p>\\n<p>先验证每次递归是否从当前闭区间计算中点，并确认左右调用排除了该中点，避免区间未缩小或元素重复。再分别检查中序结果与各节点左右子树高度差：前者证明有序性，后者证明平衡性；仅看到中序有序，不能说明中点划分正确。</p>\\n</li>\\n</ul>\\n</section>\\n<blockquote>\\n<p>题目描述：将一个按照升序排列的有序数组，转换为一棵高度平衡二叉搜索树。</p>\\n</blockquote>\\n<p>本题中，一个高度平衡二叉树是指一个二叉树每个节点 的左右两个子树的高度差的绝对值不超过 1。</p>\\n<p>示例:</p>\\n<ul>\\n<li>给定有序数组: [-10,-3,0,5,9],</li>\\n<li>一个可能的答案是：[0,-3,9,-10,null,5]，它可以表示下面这个高度平衡二叉搜索树：</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>      0\\n     / \\\\\\n   -3   9\\n   /   /\\n -10  5\\n</code></pre>\\n<p><strong>思路分析</strong></p>\\n<blockquote>\\n<p>这道题出现在这个位置真是太棒了（陶醉脸），它不仅是一道典型的二叉搜索树应用题，还涉及到了平衡二叉树的基本知识，对下一个专题的学习起到了很好的铺垫作用。</p>\\n</blockquote>\\n<p>做这个题，大家可以先观察一下它的输入和输出，你会发现它们之间有着很微妙的关系。先看输入：</p>\\n<pre class=\\"language-text\\"><code>[-10,-3,0,5,9]\\n</code></pre>\\n<p>再看输出：</p>\\n<pre class=\\"language-text\\"><code>      0\\n     / \\\\\\n   -3   9\\n   /   /\\n -10  5\\n</code></pre>\\n<ul>\\n<li>这个二叉树从形状上来看，像不像是把数组从 0 这个中间位置给“提起来”了？</li>\\n<li>别笑，我们接下来要做的事情，还真就是要想办法把这个数组给“提”成二叉树。</li>\\n</ul>\\n<p>在想办法之前，我们先来反思一下为什么可以通过“提起来”来实现数组到目标二叉树的转换，这里面蕴含了两个依据：</p>\\n<ul>\\n<li>二叉搜索树的特性：题目中指明了目标树是一棵二叉搜索树，二叉搜索树的中序遍历序列是有序的，题中所给的数组也是有序的，因此我们可以认为题目中给出的数组就是目标二叉树的中序遍历序列。中序遍历序列的顺序规则是 左 -&gt; 根 -&gt; 右，因此数组中间位置的元素一定对应着目标二叉树的根结点。以根结点为抓手，把这个数组“拎”起来，得到的二叉树一定是符合二叉搜索树的排序规则的。</li>\\n<li>平衡二叉树的特性：虽然咱们还没有讲啥是平衡二叉树，但是题目中已经妥妥地给出了一个平衡二叉树的定义：</li>\\n</ul>\\n<blockquote>\\n<p>一个高度平衡二叉树是指一个二叉树每个节点 的左右两个子树的高度差的绝对值不超过 1</p>\\n</blockquote>\\n<p>要做到这一点，只需要把“提起来”这个动作贯彻到底就行了：当我们以有序数组的中间元素为根结点，“提”出一个二叉树时，有两种可能的情况：</p>\\n<p>数组中元素为奇数个，此时以数组的中间元素为界，两侧元素个数相同：</p>\\n<pre class=\\"language-text\\"><code>[-10,-3,0,5,9]\\n</code></pre>\\n<blockquote>\\n<p>如果我们以中间元素为根结点，把数组“提”成二叉树，那么根结点左右两侧的元素个数是一样的，所以站在根结点来看，左右子树的高度差为0：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>      0\\n     / \\\\\\n   -3   9\\n   /   /\\n -10  5\\n</code></pre>\\n<blockquote>\\n<p>数组中元素为偶数个，此时无论是选择中间靠左的元素为界、还是选择中间靠右的元素为界，两侧元素个数差值的绝对值都是1：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[-10,-3,0,5]\\n</code></pre>\\n<p>在这个例子里，若以 -3 为根结点，那么左右子树的高度差的绝对值就是1：</p>\\n<pre class=\\"language-text\\"><code>      -3\\n     / \\\\\\n   -10   0\\n          \\\\\\n           5\\n</code></pre>\\n<p>以 <code>0</code> 为根结点亦然。</p>\\n<blockquote>\\n<p>通过对以上情况进行探讨，我们发现“以中间元素为根结点，将数组提成树”这种操作，可以保证根结点左右两侧的子树高度绝对值不大于1。要想保证每一棵子树都满足这个条件，我们只需要对有序数组的每一个对半分出来的子序列都递归地执行这个操作即可</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {number[]} nums\\n * @return {TreeNode}\\n */\\nconst sortedArrayToBST = function(nums) {\\n    // 处理边界条件\\n    if(!nums.length) {\\n        return null\\n    }\\n    \\n    // root 结点是递归“提”起数组的结果\\n    const root = buildBST(0, nums.length-1)\\n\\n    // 定义二叉树构建函数，入参是子序列的索引范围\\n    function buildBST(low, high) {\\n        // 当 low &gt; high 时，意味着当前范围的数字已经被递归处理完全了\\n        if(low &gt; high) {\\n            return null\\n        }\\n        // 二分一下，取出当前子序列的中间元素\\n        const mid = Math.floor(low + (high - low)/2)  \\n        // 将中间元素的值作为当前子树的根结点值\\n        const cur = new TreeNode(nums[mid]) \\n        // 递归构建左子树，范围二分为[low,mid)\\n        cur.left = buildBST(low,mid-1)\\n        // 递归构建左子树，范围二分为为(mid,high]\\n        cur.right = buildBST(mid+1, high)\\n        // 返回当前结点\\n        return cur\\n    }\\n    // 返回根结点\\n    return root\\n};\\n</code></pre>\\n"}')}}]);