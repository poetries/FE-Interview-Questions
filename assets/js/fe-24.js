(window.webpackJsonp=window.webpackJsonp||[]).push([[24],{1130:function(n){n.exports=JSON.parse('{"html":"<p>本节我们学习两种关键的基本算法思想：DFS（深度优先搜索）和BFS（广度优先搜索）。这两种算法和栈、队列有着千丝万缕的关系，如果前两节你认真学习掌握了，那么这一节对你来说相信不是问题。</p>\\n<h2 id=\\"深度优先搜索思想-不撞南墙不回头的-迷宫游戏\\"><a class=\\"header-anchor\\" href=\\"#深度优先搜索思想-不撞南墙不回头的-迷宫游戏\\">#</a> 深度优先搜索思想：不撞南墙不回头的“迷宫游戏”</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>DFS 沿一条分支走到底，走不通才回退；递归调用栈或显式栈都能实现。</li>\\n<li>图搜索必须在“入栈/进入递归”时标记 <code>visited</code>，否则有环图会重复访问甚至死循环。</li>\\n<li>时间复杂度是 <code>O(V + E)</code>；空间最坏 <code>O(V)</code>，树高为 <code>h</code> 时递归栈通常是 <code>O(h)</code>。</li>\\n<li>DFS 擅长枚举路径、连通性、拓扑与回溯，不保证无权图最短路。</li>\\n</ul>\\n<p><strong><code>DFS</code> 会沿当前分支一直向深处搜索，走不通时再回退到最近的岔路口。</strong> 它可以用递归调用栈实现，也可以自己维护显式栈，本质都是后进先出。遍历有环图时，我一般会在节点入栈或进入递归时标记 <code>visited</code>，避免重复访问甚至死循环。它适合路径枚举、连通性和回溯，时间为 <code>O(V + E)</code>，但不保证找到无权图的最短路径。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“DFS 的核心不是递归语法，而是后进先出的待办集合。我会先定义访问标记时机，再说明当前路径与已完成节点各代表什么。”</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function dfs(graph, start) {\\n  if (start == null) return []\\n  const stack = [start]\\n  const visited = new Set([start])\\n  const order = []\\n\\n  while (stack.length) {\\n    const node = stack.pop()\\n    order.push(node)\\n    const neighbors = graph.get(node) ?? []\\n    for (let i = neighbors.length - 1; i &gt;= 0; i -= 1) {\\n      const next = neighbors[i]\\n      if (!visited.has(next)) {\\n        visited.add(next)\\n        stack.push(next)\\n      }\\n    }\\n  }\\n  return order\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>迷宫页面把 <code>DFS</code> 理解成“必须写递归”，代码改为数组 <code>stack</code> 后评审认为已经不是 <code>DFS</code>，你会怎样用执行现象反驳？</strong></p>\\n<p>只要待办节点按后进先出处理，搜索就会沿一条分支持续深入，遇到无路可走再回到最近分叉点，这仍是 <code>DFS</code>。递归只是借用函数调用栈实现同一调度方式；显式栈更便于控制深度，但邻居压栈顺序会直接影响访问顺序。</p>\\n</li>\\n<li>\\n<p><strong>关系图中一个节点被多个前驱指向，页面内存突然上涨；代码直到 <code>pop()</code> 后才写入 <code>visited</code>，为什么会出现大量重复项？</strong></p>\\n<p>节点在真正弹出前仍被视为未访问，因此多个前驱都可能把它压入栈中，造成重复状态和更高的空间峰值。通常应在首次入栈时立即加入 <code>visited</code>，从源头阻止再次入栈；若业务需要枚举不同路径，节点级去重又可能过早剪枝，需改为路径状态。</p>\\n</li>\\n<li>\\n<p><strong>路由依赖图可能出现环，开发只把当前节点放进 <code>path</code>，回退时删除，却没有全局 <code>visited</code>，查询为何可能反复绕圈？</strong></p>\\n<p>当前路径集合只能阻止本轮路径内的直接成环，节点退出路径后仍可能从其他分支再次进入。若目标是普通可达性遍历，应使用全局 <code>visited</code> 并在入栈时标记；若目标是枚举所有简单路径，则保留路径级标记，但必须接受重复探索和更高计算成本。</p>\\n</li>\\n<li>\\n<p><strong>同一份邻接表，递归版访问顺序是 <code>A、B、C</code>，改成显式栈后却变成 <code>A、C、B</code>，你会检查哪段代码？</strong></p>\\n<p>应检查邻居压栈顺序，因为栈是后进先出，按邻接表正序压入会让最后一个邻居先被访问。若要复现递归中从左到右的顺序，应从邻接表末尾向前压栈；这只影响确定性的访问次序，不改变 <code>DFS</code> 的可达性结果。</p>\\n</li>\\n<li>\\n<p><strong>导航页面既要找到出口，也要展示一条可回放路线；团队在“栈内容就是路径”和 <code>parent</code> 映射之间争论，你会选哪种？</strong></p>\\n<p>若显式栈中的元素始终代表当前分支，命中出口时栈内容可以直接形成路线，但常见的待办栈会同时保存尚未探索的分支，并不天然等于路径。通用实现更适合在首次发现节点时记录 <code>parent</code>，命中后反向恢复；代价是为已发现节点额外保存映射。</p>\\n</li>\\n</ul>\\n</section>\\n<p>20世纪90年代，小霸王学习机风靡全国。没有哪个小学生，不想拥有一台自己的小霸王学习机——我，也不例外；没有哪个小学生在拥有了小霸王学习机之后，会真的用它来搞学习——我，也不例外。在那个没有王者也没有吃鸡的年代里，小霸王里让我欲罢不能的除了魂斗罗、超级玛丽，还有它——迷宫游戏：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123013.png\\" alt=\\"\\"></p>\\n<p>很多同学跟我说他入门算法的时候就挂在 DFS 这里，觉得太高深了，学不动。傻孩子，今天你就会知道，深度优先搜索也不过是在用编码的方式玩一场迷宫游戏。</p>\\n<p>现在把图上的小黄点想象成你自己，由于你手里没有地图、没有无人机，因此你对迷宫整体的地形一无所知。放眼望去，你眼前只有冰冷的墙壁和并不知道能不能走通的道路。如何走出一条通路？你只能尝试把每一条能走的路都走一遍——也就是所谓的“穷举法”：</p>\\n<ul>\\n<li>以当前位置为起点，闷头往前走</li>\\n<li>在前进的过程中，难免会遇到岔路口。这个路口可能分叉出去两条、三条、四条甚至更多的道路，你只能选择其中的一条路、然后继续前进（注意，你可能会不止一次遇到岔路口；每遇到一个新的岔路口，你都需要做一次选择）。</li>\\n<li>你选择的这条路未必是一条通路。如果你走到最后发现此路不通，那么你就要退回到离你最近的那个分叉路口，然后尝试看其它的岔路能不能走通。如果当前的岔路口分叉出去的所有道路都走不通，那么就需要退回到当前岔路口的上一个岔路口，进一步去寻找新的路径。</li>\\n</ul>\\n<blockquote>\\n<p>按照这个思路走下去，只要迷宫是有出口的，你就一定能找到这个出口。在这个过程里，我们贯彻了“不撞南墙不回头”的原则：只要没有碰壁，就决不选择其它的道路，而是坚持向当前道路的深处挖掘——像这样将“深度”作为前进的第一要素的搜索方法，就是所谓的“深度优先搜索”。</p>\\n</blockquote>\\n<blockquote>\\n<p>深度优先搜索的核心思想，是试图穷举所有的完整路径。</p>\\n</blockquote>\\n<h2 id=\\"深度优先搜索的本质-栈结构\\"><a class=\\"header-anchor\\" href=\\"#深度优先搜索的本质-栈结构\\">#</a> 深度优先搜索的本质——栈结构</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>DFS 把尚未探索的分支保存在调用栈或显式栈中，总是继续处理最近发现的结点</li>\\n<li>图遍历必须在入栈或进入递归时标记 <code>visited</code>，否则环会导致重复访问甚至无限循环</li>\\n<li>时间为 <code>O(V + E)</code>，额外空间最坏 <code>O(V)</code>；树没有共享回边时可省 visited，但要明确前提</li>\\n</ul>\\n<p><strong><code>DFS</code> 的本质是用栈保存尚未探索完的分支，并优先处理最近发现的节点。</strong> 递归写法依赖函数调用栈，迭代写法则显式维护栈，两者的前进和回退过程是一致的。图中可能存在环，所以节点应在入栈或进入递归时加入 <code>visited</code>，否则可能重复访问甚至无法结束。完整遍历的时间是 <code>O(V + E)</code>，额外空间最坏为 <code>O(V)</code>；只有确认是无共享回边的树时，才可以省略访问标记。</p>\\n</section>\\n<blockquote>\\n<p>那么如何使用编码来实现深度优先搜索呢？我们继续讨论迷宫问题，这里我给大家一个抽象过后的简单迷宫结构：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123023.png\\" alt=\\"\\"></p>\\n<p>图中蓝色的是入口，灰色的是岔路口，黑色的是死胡同，绿色的是出口。</p>\\n<blockquote>\\n<p>基于眼前的这个迷宫结构，我们来一步一步模拟一下深度优先搜索的具体过程：</p>\\n</blockquote>\\n<ul>\\n<li>从 A 出发，沿着唯一的一条道路往下走，遇到了第1个岔路口B。眼前有三个选择：C、D、E。这里我按照从上到下的顺序来走（你也可以按照其它顺序），先走C。</li>\\n<li>发现 C是死胡同，后退到最近的岔路口 B，尝试往D方向走。</li>\\n<li>发现D 是死胡同，，后退到最近的岔路口 B，尝试往E方向走。</li>\\n<li>E 是一个岔路口，眼前有两个选择：F 和 G。按照从上到下的顺序来走，先走F。</li>\\n<li>发现F 是死胡同，后退到最近的岔路口 E，尝试往G方向走。</li>\\n<li>G 是一个岔路口，眼前有两个选择：H 和 I。按照从上到下的顺序来走，先走H。</li>\\n<li>发现 H 是死胡同，后退到最近的岔路口 G，尝试往I方向走。</li>\\n<li>I 就是出口，成功走出迷宫。</li>\\n</ul>\\n<p>大家观察一下这个过程，会不会觉得这些前进、后退的操作，其实和栈结构的入栈、出栈过程非常相似呢？</p>\\n<blockquote>\\n<p>现在我们把迷宫中的每一个坐标看做是栈里的一个元素，用栈来模拟这个过程：</p>\\n</blockquote>\\n<ul>\\n<li>从 A 出发（A入栈），经过了B（B入栈），接下来面临 C、D、E三条路。这里按照从上到下的顺序来走（你也可以选择其它顺序），先走C（C入栈）。</li>\\n<li>发现 C是死胡同，后退到最近的岔路口 B（C出栈），尝试往D方向走（D入栈）。</li>\\n<li>E 是一个岔路口，眼前有两个选择：F 和 G。按照从上到下的顺序来走，先走F（F入栈）。</li>\\n<li>发现F 是死胡同，后退到最近的岔路口 E（F出栈），尝试往G方向走（G入栈）。</li>\\n<li>G 是一个岔路口，眼前有两个选择：H 和 I。按照从上到下的顺序来走，先走H（H入栈）。</li>\\n<li>发现 H 是死胡同，后退到最近的岔路口 G（H出栈），尝试往I方向走（I入栈）。</li>\\n<li>I 就是出口，成功走出迷宫。</li>\\n</ul>\\n<blockquote>\\n<p>此时栈里面的内容就是A、B、E、G、I，因此 A-&gt;B-&gt;E-&gt;G-&gt;I 就是走出迷宫的路径。通过深度优先搜索，我们不仅可以定位到迷宫的出口，还可以记录下相关的路径信息。</p>\\n</blockquote>\\n<p>现在大家知道了深度优先搜索的过程可以转化为一系列的入栈、出栈操作。那么深度优先搜索在编码上一般会如何实现呢？这里，就需要大家回忆一下第 5 节的内容了——DFS 中，我们往往使用递归来模拟入栈、出栈的逻辑。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>迷宫演示页把所有相邻岔路一次性压入待办栈，然后直接把整个栈渲染成当前路线，为什么画面会出现互不相连的节点？</strong></p>\\n<p>待办栈保存的是未来要探索的候选状态，其中可能同时存在来自不同分支的节点，因此不一定构成一条连续路线。要展示当前路径，应让栈帧保存节点及下一邻居位置，或另外维护可回退的路径；把普通遍历待办栈直接当路径只在特定推进方式下成立。</p>\\n</li>\\n<li>\\n<p><strong>协作图有大量汇聚边，代码在节点出栈时才去重，压栈数量远超节点数；你会把标记动作移到哪里？</strong></p>\\n<p>应在节点首次入栈时就写入 <code>visited</code>，这样后续邻居发现同一节点时会立即跳过，避免重复进入待办集合。出栈标记虽然最终也能避免重复处理，却可能积累许多重复栈项；若状态是否重复还取决于路径，则不能只按节点标记。</p>\\n</li>\\n<li>\\n<p><strong>产品要求迷宫分支严格按页面从上到下探索，邻接表也是这个顺序，显式栈实现为什么必须反向压入邻居？</strong></p>\\n<p>因为栈后进先出，最后压入的邻居会最先弹出；若按从上到下正序压栈，实际探索次序会完全反过来。应从邻接表末尾向前压栈，使最上方分支最后入栈、最先处理；如果产品不关心路线稳定性，则无需为顺序增加约束。</p>\\n</li>\\n<li>\\n<p><strong>递归迷宫在一条极深走廊上触发 <code>Maximum call stack size exceeded</code>，改成显式栈后应保留哪些递归帧信息？</strong></p>\\n<p>至少要保存当前节点；若需要精确模拟“尝试一个分支、失败后继续下一个分支”，栈帧还应保存邻居列表中的下一处理位置。仅保存节点适合普通遍历，但不能自然还原完整回溯过程；显式栈绕开调用栈上限，却把状态管理责任交给实现者。</p>\\n</li>\\n<li>\\n<p><strong>面试中候选人说 <code>BFS</code> 也能找到出口，为什么迷宫题仍可能选择 <code>DFS</code> 的栈方案？</strong></p>\\n<p>若只要求找到任意出口或展示沿分支深入再回退的过程，<code>DFS</code> 与迷宫叙事和路径栈更直接，也不必承诺最短路线。若需求变为无权图中的最少步数，<code>BFS</code> 的分层探索更匹配目标；继续使用 <code>DFS</code> 即使找到出口，也不能据此证明路径最短。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"dfs-与二叉树的遍历\\"><a class=\\"header-anchor\\" href=\\"#dfs-与二叉树的遍历\\">#</a> DFS 与二叉树的遍历</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>二叉树先序、中序、后序本质都是 DFS，只是访问根结点的时机不同</li>\\n<li>树高决定递归深度；退化树可能让 JavaScript 调用栈溢出，应准备显式栈版本</li>\\n<li>路径题要区分共享状态与分支状态，回溯时撤销当前选择，或把路径复制给子分支</li>\\n</ul>\\n<p><strong>二叉树的先序、中序和后序遍历本质上都是 <code>DFS</code>，区别只是处理根节点的时机。</strong> 递归不断进入子树，相当于沿分支向深处走；遇到空节点返回，就是从死胡同回退。递归深度由树高决定，退化成链表时，<code>JavaScript</code> 可能出现调用栈溢出，这时可以改用显式栈。处理路径状态时还要分清是否共享数据：共享路径需要在回退时撤销选择，也可以为每个子分支复制一份路径。</p>\\n</section>\\n<p>现在我们站在深度优先搜索的角度，重新理解一下二叉树的先序遍历过程：</p>\\n<p><img src=\\"https://p1-jj.byteimg.com/tos-cn-i-t2oaga2asx/gold-user-assets/2020/4/6/1714ec42acc57e04~tplv-t2oaga2asx-watermark.awebp\\" alt=\\"\\"></p>\\n<p>从 A 结点出发，访问左侧的子结点；如果左子树同样存在左侧子结点，就头也不回地继续访问下去。一直到左侧子结点为空时，才退回到距离最近的父结点、再尝试\\n去访问父结点的右侧子结点——这个过程，和走迷宫是何其相似！事实上，在二叉树中，结点就好比是迷宫里的坐标，图中的每个结点在作为父结点时无疑是岔路口，而空结点就是死胡同。我们回顾一下二叉树先序遍历的编码实现：</p>\\n<pre class=\\"language-js\\"><code>// 所有遍历函数的入参都是树的根结点对象\\nfunction preorder(root) {\\n    // 递归边界，root 为空\\n    if(!root) {\\n        return \\n    }\\n     \\n    // 输出当前遍历的结点值\\n    console.log(&#39;当前遍历的结点值是：&#39;, root.val)  \\n    // 递归遍历左子树 \\n    preorder(root.left)  \\n    // 递归遍历右子树  \\n    preorder(root.right)\\n}\\n</code></pre>\\n<blockquote>\\n<p>在这个递归函数中，递归式用来先后遍历左子树、右子树（分别探索不同的道路），递归边界在识别到结点为空时会直接返回（撞到了南墙）。因此，我们可以认为，递归式就是我们选择道路的过程，而递归边界就是死胡同。二叉树的先序遍历正是深度优先搜索思想的递归实现。可以说深度优先搜索过程就类似于树的先序遍历、是树的先序遍历的推广。</p>\\n</blockquote>\\n<p>这时候，可能有同学会有疑问：在二叉树遍历的递归实现里，完全没有栈的影子——这东西似乎和栈没有什么直接联系啊，为啥咱们还说深度优先搜索的本质是栈呢？</p>\\n<blockquote>\\n<p>我们从两个角度来理解这个事情：</p>\\n</blockquote>\\n<ul>\\n<li>首先，函数调用的底层，仍然是由栈来实现的。JS 会维护一个叫“函数调用栈”的东西，preorder每调用一次自己，相关调用的上下文就会被push进函数调用栈中；待函数执行完毕后，对应的上下文又会从调用栈中被pop出来。因此，即便二叉树的递归调用过程中，并没有出现栈这种数据结构，也依然改变不了递归的本质是栈的事实。</li>\\n<li>其次，DFS 作为一种思想，它和树的递归遍历一脉相承、却并不能完全地画上等号——DFS 的解题场景其实有很多，其中有一类会要求我们记录每一层递归式里路径的状态，此时就会强依赖栈结构（这一点会在下一节的真题实战中体现得淋漓尽致）。</li>\\n</ul>\\n<p>基于上述的两个例子，相信大家已经对深度优先搜索的思想和实现思路形成了自己的理解。本节我们着重理解概念，不急着做题——深度优先搜索的应用是非常广泛的，在后续的小节中，大家自然会见到许许多多使用递归来实现深度优先搜索的实战案例。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>组件树调试页把先序遍历写成“访问根、递归右子树、递归左子树”，结果节点都能访问却与设计稿顺序相反，这还是 <code>DFS</code> 吗？</strong></p>\\n<p>它仍是 <code>DFS</code>，因为搜索依然沿子树深入到空节点后回退，只是分支选择次序从左优先变成了右优先。若页面要求先序的固定顺序，应执行“根、左、右”；<code>DFS</code> 描述搜索策略，而具体遍历序列还由访问时机和子树顺序决定。</p>\\n</li>\\n<li>\\n<p><strong>评论树深度不可控，递归先序遍历在线上偶发调用栈溢出，你会如何改写并保持“根、左、右”的输出？</strong></p>\\n<p>应使用显式栈，先压入根节点，每次弹出后访问，再依次压入右子节点和左子节点，使左侧因后进先出而先处理。这样把深度状态移到堆上的数据结构中，但待办栈仍会占用空间，且压栈顺序写反会改变页面输出。</p>\\n</li>\\n<li>\\n<p><strong>权限树可能复用同一个节点对象形成图，原来的二叉树递归没有 <code>visited</code>，为什么迁移后可能无限递归？</strong></p>\\n<p>普通二叉树默认每个节点只有唯一父路径且不存在环，所以遍历到空节点即可终止；复用节点或出现回边后，这个前提不再成立。迁移到一般图结构时应增加身份级 <code>visited</code> 或当前路径标记，具体选择取决于共享节点是否允许被重复处理。</p>\\n</li>\\n<li>\\n<p><strong>根到叶路径页面显示的多条结果最后都变成同一个数组，代码命中叶子时执行 <code>result.push(path)</code>，故障原因是什么？</strong></p>\\n<p><code>result</code> 保存的是同一个 <code>path</code> 数组引用，后续递归的追加与回退会继续修改它，因此已记录结果也随之变化。命中叶子时应保存 <code>path.slice()</code> 或展开后的副本，并在返回父层前撤销当前节点；复制会产生额外分配，但能隔离各条路径。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中有人把“树的先序遍历”和“<code>DFS</code>”视为完全同义，遇到需要在回退时计算子树高度的页面，你会怎样区分？</strong></p>\\n<p>先序遍历是在进入节点时处理，属于 <code>DFS</code> 的一种访问时机；子树高度需要先得到左右子树结果，更接近后序阶段的处理。<code>DFS</code> 还可以在进入、分支之间或退出节点时维护状态，因此不能与单一先序序列画等号；混淆两者容易把计算放到依赖尚未就绪的位置。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"广度优先搜索思想-找到迷宫出口的另一种思路\\"><a class=\\"header-anchor\\" href=\\"#广度优先搜索思想-找到迷宫出口的另一种思路\\">#</a> 广度优先搜索思想——找到迷宫出口的另一种思路</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>BFS 按距离一层层扩展，依赖 FIFO 队列；无权图中首次到达就是最短边数。</li>\\n<li>节点入队时立即标记，避免同层多个父节点把它重复入队。</li>\\n<li>时间 <code>O(V + E)</code>、空间最坏 <code>O(V)</code>；层序遍历可按 <code>queue.length</code> 固定本层大小。</li>\\n<li>JavaScript 不要高频 <code>shift()</code>；用数组加 <code>head</code> 指针保持均摊 <code>O(1)</code> 出队。</li>\\n</ul>\\n<p><strong>广度优先搜索会从入口开始逐层扫描迷宫，首次到达出口时，得到的就是无权图中的最短步数。</strong> 它用先进先出的队列保存待访问坐标，因此会先处理距离入口更近的位置，再处理下一层。坐标入队时就应加入 <code>visited</code>，避免同一层的多个位置把它重复加入队列。<code>JavaScript</code> 中我一般用数组配合 <code>head</code> 指针出队，避免频繁调用 <code>shift()</code> 搬移元素。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“BFS 的不变量是队列按离起点的距离非递减排列，所以第一次访问某节点时得到的就是无权最短距离。”</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function shortestDistance(graph, start, target) {\\n  const queue = [[start, 0]]\\n  const visited = new Set([start])\\n  for (let head = 0; head &lt; queue.length; head += 1) {\\n    const [node, distance] = queue[head]\\n    if (node === target) return distance\\n    for (const next of graph.get(node) ?? []) {\\n      if (visited.has(next)) continue\\n      visited.add(next)\\n      queue.push([next, distance + 1])\\n    }\\n  }\\n  return -1\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>迷宫页面里有一条绕远但先写进邻接表的路线，代码第一次发现出口就返回；评审同事认为邻接顺序会让结果不是最短，你怎么判断？</strong></p>\\n<p>在每一步代价相同的迷宫中，邻接顺序只影响同一距离内的访问先后，不影响最短距离。队列始终按距起点的距离非递减处理，因此出口第一次被访问时，不可能还有更短路径尚未展开；若边的代价不同，该结论就不成立。</p>\\n</li>\\n<li>\\n<p><strong>前端寻路演示要同时展示最短步数和完整路线，现有队列只保存 <code>[node, distance]</code>，你会怎样改而不在每个队列元素里复制整条路径？</strong></p>\\n<p>保留距离推进逻辑，并在节点首次入队时记录 <code>parent.set(next, node)</code>。找到目标后从 <code>target</code> 沿父节点回溯到 <code>start</code>，再反转得到路线；这样避免反复复制路径，但需要为已访问节点额外保存前驱关系。</p>\\n</li>\\n<li>\\n<p><strong>产品把迷宫改成沼泽、平地两种格子，进入格子的代价分别不同，还要求返回总代价最小的路线，原来的普通 <code>BFS</code> 能继续用吗？</strong></p>\\n<p>普通 <code>BFS</code> 不能保证总代价最小，因为它只按经过的边数分层，不会按累计代价排序。边权非负且不相等时应改用 <code>Dijkstra</code>；只有所有移动代价相等时，最少步数才等价于最小总代价。</p>\\n</li>\\n<li>\\n<p><strong>线上迷宫偶发内存持续增长，同一个坐标在队列中出现很多次；代码是在节点出队后才加入 <code>visited</code>，你会先查哪里？</strong></p>\\n<p>先检查是否把 <code>visited</code> 标记推迟到了出队阶段，这会让同一节点被多个前驱重复入队。应在确认未访问并执行入队时立即标记，保证每个节点至多进入队列一次；若状态还包含钥匙或方向，访问键也必须覆盖完整状态。</p>\\n</li>\\n<li>\\n<p><strong>地图有明确入口和出口、分支很多，单向 <code>BFS</code> 的队列峰值过大；有人建议直接改成双向 <code>BFS</code>，你会要求确认哪些前提？</strong></p>\\n<p>先确认能从出口反向生成合法邻居，并且两端使用一致的状态与访问判定，再从较小的前沿扩展。双向搜索可能显著减少中间状态，但相遇检测和路径拼接更复杂；若移动规则不可逆，就不能直接套用。</p>\\n</li>\\n</ul>\\n</section>\\n<p>我们回头再来看看这个迷宫结构：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123045.png\\" alt=\\"\\"></p>\\n<p>当我们使用深度优先搜索来寻找迷宫出口时，会走出图示这样一条一条的完整路径</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123052.png\\" alt=\\"\\"></p>\\n<p>其中红色的圆点意味着路径的起点，红色箭头意味着路径的终点。我们看到从起点开始，一共探索出了 5 条完整的路径。</p>\\n<p>与深度优先搜索不同的是，广度优先搜索（BFS）并不执着于“一往无前”这件事情。它关心的是眼下自己能够直接到达的所有坐标，其动作有点类似于“扫描”——比如说站在 B 这个岔路口，它会只关注 C、D、E 三个坐标，至于 F、G、H、I这些遥远的坐标，现在不在它的关心范围内：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123100.png\\" alt=\\"\\"></p>\\n<p>只有在走到了 E处时，它发现此时可以触达的坐标变成了 F、G，此时才会去扫描F、G：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123108.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>按照这个思路，广度优先搜索每次以“广度”为第一要务、雨露均沾，一层一层地扫描，最后也能够将所有的坐标扫描完全：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123116.png\\" alt=\\"\\"></p>\\n<p>当扫描到 I 的时候，发现 I 是出口，照样能够找到答案。</p>\\n<blockquote>\\n<p>按照 BFS 的遍历规则，具体的访问步骤会变成下面这样:</p>\\n</blockquote>\\n<ul>\\n<li>站在入口A处（第一层），发现直接能抵达的坐标只有B，于是接下来需要访问的就是 B。</li>\\n<li>入口A访问完毕，走到 B 处（第二层），发现直接能抵达的坐标变成了C、D和E，于是把这三个坐标记为下一层的访问对象。\\nB访问完毕，访问第三层。这里我按照从上到下的顺序（你也可以按照其它顺序），先访问 C和D，然后访问E。站在C处和D处都没有见到新的可以直接抵达的坐标，- 所以不做额外的动作。但是在E处见到了可以直接抵达的F和G，因此把F和G记为下一层（第四层）需要访问的对象。</li>\\n<li>第三层访问完毕，访问第四层。第四层按照从上到下的顺序，先访问的是 F。从F出发没有可以直接触达的坐标，因此不做额外的操作。接着访问G，发现从G出发可以直接抵达H和I，因此把H和I记为下一层（第五层）需要访问的对象。</li>\\n<li>第四层访问完毕，访问第五层。第五层按照从上到下的顺序，先访问的是H，发现从H出发没有可以直接抵达的坐标，因此不作额外的操作。接着访问I，发现I就是出口，问题得解。</li>\\n</ul>\\n<p>当然啦，这个问题若采用 BFS 的思路来解，那么它其实已经不能说是一个严格的迷宫游戏了——在一个真正的迷宫游戏里，大概率并不会允许我们如此顺利地逐个访问身在同一层次的所有坐标（比如C和D之间可能就会隔了厚厚的一堵墙，导致你无法在访问C后直接去访问D）。这里我们基于迷宫游戏，抽象出来的其实是一个更为简单的模型。大家不必拘泥于游戏本身，而应该着重理解这个分层遍历的过程。</p>\\n<blockquote>\\n<p>在分层遍历的过程中，大家会发现两个规律：</p>\\n</blockquote>\\n<ul>\\n<li>每访问完毕一个坐标，这个坐标在后续的遍历中都不会再被用到了，也就是说它可以被丢弃掉。</li>\\n<li>站在某个确定坐标的位置上，我们所观察到的可直接抵达的坐标，是需要被记录下来的，因为后续的遍历还要用到它们。</li>\\n</ul>\\n<p>丢弃已访问的坐标、记录新观察到的坐标，这个顺序毫无疑问符合了“先进先出”的原则，因此整个 BFS 算法的实现过程，和队列有着密不可分的关系。</p>\\n<blockquote>\\n<p>下面我用一个队列 queue 来模拟一下上面的过程</p>\\n</blockquote>\\n<ul>\\n<li>初始化，先将入口A入队（queue里现在只有A）。</li>\\n<li>访问入口A（第一层），访问完毕后将A出队。发现直接能抵达的坐标只有B，于是将B入队（queue里现在只有B）。</li>\\n<li>访问B（第二层），访问完毕后将B出队。发现直接能抵达的坐标变成了C、D和E，于是把这三个坐标记为下一层的访问对象，也就是把它们全部入队（queue里现在是C、D、E）</li>\\n<li>访问第三层。这里我按照从上到下的顺序（你也可以按照其它顺序），先访问 C（访问完毕后C出队）和D（访问完毕后D出队），然后访问E（访问完毕后E出队）。访问C处和D处都没有见到新的可以直接抵达的坐标，所以不做额外的动作。但是在E处我们见到了可以直接抵达的F和G，因此把F和G记为下一层（第四层）需要访问的对象，F、G依次入队（queue里现在是 F、G）。</li>\\n<li>访问第五层。第五层按照从上到下的顺序，先访问的是H（访问完毕后H出队），发现从H出发没有可以直接抵达的坐标，因此不作额外的操作。接着访问I（访问完毕后I出队），发现I就是出口，问题得解（此时 queue 队列已经被清空）</li>\\n</ul>\\n<blockquote>\\n<p>在这个过程里，我们其实循环往复地做了以下事情：依次访问队列里已经有的坐标，将其出队；记录从当前坐标出发可直接抵达的所有坐标，将其入队。</p>\\n</blockquote>\\n<p>以上逻辑用伪代码表述如下：</p>\\n<pre class=\\"language-js\\"><code>function BFS(入口坐标) {\\n    const queue = [] // 初始化队列queue\\n    // 入口坐标首先入队\\n    queue.push(入口坐标)\\n    // 队列不为空，说明没有遍历完全\\n    while(queue.length) {\\n        const top = queue[0] // 取出队头元素  \\n        \\n        访问 top // 此处是一些和 top 相关的逻辑，比如记录它对应的信息、检查它的属性等等\\n        \\n        // 注意这里也可以不用 for 循环，视题意而定\\n        for(检查 top 元素出发能够遍历到的所有元素)  {\\n            queue.push(top能够直接抵达的元素)\\n        }\\n        \\n        queue.shift() // 访问完毕。将队头元素出队\\n    }\\n}\\n</code></pre>\\n<blockquote>\\n<p>注意，理论上来说只要我们拿到了 top，那么就不再关心队头元素了。因此这个 shift 出队的过程，其实是比较灵活的。一般只要我们拿到了 top，就可以执行 shift了。一些同学习惯于把top元素的访问和出队放在一起来做：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>const top = queue.shift()\\n</code></pre>\\n<p>这样做也是没问题的（除非题目中对出队的时机有强约束，但这种情况非常少见）。</p>\\n<h2 id=\\"bfs实战-二叉树的层序遍历\\"><a class=\\"header-anchor\\" href=\\"#bfs实战-二叉树的层序遍历\\">#</a> BFS实战：二叉树的层序遍历</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>BFS 使用队列保证同一层按发现顺序处理；每轮先记录当前队列长度，恰好消费这一层</li>\\n<li>子结点在父结点出队时入队，空结点不入队；每个结点一次入队出队，时间 <code>O(n)</code></li>\\n<li>JavaScript 不应连续 <code>shift()</code> 大数组，使用 <code>head</code> 指针实现队列可避免索引搬移</li>\\n</ul>\\n<p><strong>二叉树的层序遍历本质上就是用 <code>BFS</code> 按从上到下、从左到右的顺序访问每个结点。</strong> 先把根结点放入队列，每次取出队头并访问，再依次把存在的左、右子结点入队，最终得到 <code>A、B、C、D、E、F</code>。如果需要按层收集结果，可以在每轮开始时固定当前队列长度，只消费这一层。每个结点只会入队和出队一次，时间复杂度是 <code>O(n)</code>；实现时可用 <code>head</code> 指针代替频繁的 <code>shift()</code>。</p>\\n</section>\\n<p>大家现在回顾一下我们在第 5 节展示过的这个二叉树实例：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123129.png\\" alt=\\"\\"></p>\\n<p>这棵二叉树的编码实现如下：</p>\\n<pre class=\\"language-js\\"><code>const root = {\\n  val: &quot;A&quot;,\\n  left: {\\n    val: &quot;B&quot;,\\n    left: {\\n      val: &quot;D&quot;\\n    },\\n    right: {\\n      val: &quot;E&quot;\\n    }\\n  },\\n  right: {\\n    val: &quot;C&quot;,\\n    right: {\\n      val: &quot;F&quot;\\n    }\\n  }\\n};\\n</code></pre>\\n<blockquote>\\n<p>现在我们要做的是对这个二叉树进行层序遍历。层序遍历的概念很好理解：按照层次的顺序，从上到下，从左到右地遍历一个二叉树，如图所示（红色数字即为遍历的序号）：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123138.png\\" alt=\\"\\"></p>\\n<p>正确的遍历序列为：</p>\\n<pre class=\\"language-text\\"><code>A\\nB\\nC\\nD\\nE\\nF\\n</code></pre>\\n<p>看到“层次”关键字，大家应该立刻想到“扫描”；想到“扫描”，就应该立刻想到 BFS。因此层序遍历，我们就用 BFS 的思路来实现。这里咱们可以直接套用上面的伪代码：</p>\\n<pre class=\\"language-js\\"><code>function BFS(root) {\\n    const queue = [] // 初始化队列queue\\n    // 根结点首先入队\\n    queue.push(root)\\n    // 队列不为空，说明没有遍历完全\\n    while(queue.length) {\\n        const top = queue[0] // 取出队头元素  \\n        // 访问 top\\n        console.log(top.val)\\n        // 如果左子树存在，左子树入队\\n        if(top.left) {\\n            queue.push(top.left)\\n        }\\n        // 如果右子树存在，右子树入队\\n        if(top.right) {\\n            queue.push(top.right)\\n        }\\n        queue.shift() // 访问完毕，队头元素出队\\n    }\\n}\\n</code></pre>\\n<p>执行 BFS：</p>\\n<pre class=\\"language-text\\"><code>BFS(root)\\n</code></pre>\\n<p>输出结果符合预期：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123151.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>树形组件要求输出 <code>A、B、C、D、E、F</code>，候选人用递归先完整访问左子树再访问右子树；页面却显示成 <code>A、B、D、E、C、F</code>，根因是什么？</strong></p>\\n<p>这段递归执行的是深度优先遍历，会先深入左子树，因此不能维持从上到下、同层从左到右的顺序。层序遍历应使用队列：根节点先入队，每次取出队头，再依次把左、右孩子加入队尾。</p>\\n</li>\\n<li>\\n<p><strong>一个评论树页面要把每层节点渲染成独立数组，处理本层时子节点还会持续入队；本层循环的边界应怎样固定？</strong></p>\\n<p>进入每一轮前先快照 <code>levelSize = queue.length</code>，本轮只消费这 <code>levelSize</code> 个节点，新入队的孩子自然留到下一轮。若直接把不断变化的 <code>queue.length</code> 当作循环上限，下一层节点会被提前混入当前层。</p>\\n</li>\\n<li>\\n<p><strong>层序遍历代码在十万级节点的宽树上明显卡顿，核心循环反复调用 <code>queue.shift()</code>；不改变遍历顺序时你会怎样调整？</strong></p>\\n<p>改用数组配合递增的 <code>head</code> 下标读取队头，例如在 <code>head &lt; queue.length</code> 时访问 <code>queue[head++]</code>。这能避免每次删除首元素可能引发的数组搬移；代价是已消费元素仍暂留数组，遍历结束后再整体释放。</p>\\n</li>\\n<li>\\n<p><strong>线上接口偶尔返回 <code>root = null</code>，当前实现仍执行 <code>queue.push(root)</code>，随后读取 <code>top.val</code> 报错；修复应该放在哪里？</strong></p>\\n<p>应在入队前处理空根节点，例如直接返回空结果，而不是让 <code>null</code> 进入队列。随后只有真实节点才能访问 <code>val</code> 并检查 <code>left</code>、<code>right</code>；如果输入并非严格的树，还需另设访问集合防止引用环。</p>\\n</li>\\n<li>\\n<p><strong>数据看板不仅要层序输出，还要计算每层平均值；有人主张先生成完整遍历序列再按深度分组，你会选哪种实现？</strong></p>\\n<p>直接在每轮固定的 <code>levelSize</code> 内累计节点值，消费完本层后计算平均值，同时照常把孩子入队。这样不必再保存每个节点的深度并二次分组；不过节点值若不是可计算的数值，必须先明确转换或异常处理规则。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"结语\\"><a class=\\"header-anchor\\" href=\\"#结语\\">#</a> 结语</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>DFS 优先走深，适合回溯、连通性和树形递归；BFS 按层扩展，适合无权最短步数和层级统计</li>\\n<li>两者都必须定义状态、邻居生成、访问标记与终止条件，差别主要是待处理集合用栈还是队列</li>\\n<li>复杂度通常同为 <code>O(V + E)</code>，但内存峰值分别受搜索深度与最大层宽影响</li>\\n</ul>\\n<p><strong>简单来说，<code>DFS</code> 适合沿一条路径深入探索，<code>BFS</code> 更适合按层扫描和寻找无权图的最短步数。</strong> 两者都要明确当前状态、相邻状态、访问标记和终止条件，核心区别只是待处理节点分别由栈和队列管理。它们的时间复杂度通常都是 <code>O(V + E)</code>，但空间压力不同：<code>DFS</code> 更受搜索深度影响，<code>BFS</code> 更受最大层宽影响。实际选择时，我会先看问题强调路径深入还是距离层级。</p>\\n</section>\\n<blockquote>\\n<p>经过本节的学习，相信大家对 DFS、BFS 的核心思想及实现方法都有了比较扎实的掌握。这两种算法在我们今后的做题过程中会反复出现，彼时大家会对它们的应用场景有更加深刻的认知。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审中有人说 <code>DFS</code> 和 <code>BFS</code> 只是写法不同，反正最终都会访问全部节点；导航系统却要求无权图的最少跳数，你怎么反驳？</strong></p>\\n<p>两者在完整遍历时可能覆盖相同节点，但访问顺序决定能否直接得到最少跳数。<code>BFS</code> 按距离分层展开，目标首次到达时就是无权最短距离；普通 <code>DFS</code> 先得到的路径可能只是某条可行路径。</p>\\n</li>\\n<li>\\n<p><strong>面试白板题要求遍历一个可能存在环的页面依赖图，候选人把二叉树的 <code>DFS</code> 或 <code>BFS</code> 模板原样搬过来，工程上缺了什么？</strong></p>\\n<p>依赖图需要维护 <code>visited</code>，并在节点进入待处理结构时建立稳定的去重语义，否则环会造成重复访问甚至无法结束。访问键必须能唯一表示业务节点；若同一节点携带不同状态，不能只用页面名称粗略去重。</p>\\n</li>\\n<li>\\n<p><strong>文件目录扫描从树结构变成共享子目录构成的有向图，产品仍要求遍历全部可达节点；你会怎样调整原来的树遍历假设？</strong></p>\\n<p>不能再假设每个节点只有一个父节点，也不能仅靠叶子终止来避免重复。无论采用 <code>DFS</code> 还是 <code>BFS</code>，都应加入访问集合并遍历邻接节点；若业务要求保留所有到达路径，则去重策略必须与路径输出需求分开设计。</p>\\n</li>\\n<li>\\n<p><strong>线上搜索接口偶发超时，日志显示待处理节点数量快速膨胀，但没有死循环；你会如何区分是分支过宽还是重复入队？</strong></p>\\n<p>先记录每层前沿大小、累计访问数和重复命中数，并核对节点是在入队时还是出队时标记。重复入队应通过更早的 <code>visited</code> 标记修正；若确属分支因子过大，则需设置搜索边界或改用双向搜索等策略。</p>\\n</li>\\n<li>\\n<p><strong>起点和终点都明确的关系图搜索中，团队在单向 <code>BFS</code> 与双向 <code>BFS</code> 之间争论，你会依据什么做选型？</strong></p>\\n<p>若能从两端生成邻居、状态判等可靠且分支较大，可优先扩展较小前沿并在访问集合相交时停止。双向 <code>BFS</code> 会增加相遇判断与路径拼接复杂度；终点不明确或反向邻居不可获得时，单向搜索更稳妥。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);