(window.webpackJsonp=window.webpackJsonp||[]).push([[42],{1148:function(o){o.exports=JSON.parse('{"html":"<p><img src=\\"https://s.poetries.top/uploads/2022/07/47b89e6c7852b4c4.png\\" alt=\\"CSS CHEAT SHEET\\"></p>\\n<h2 id=\\"_1-盒模型\\"><a class=\\"header-anchor\\" href=\\"#_1-盒模型\\">#</a> 1 盒模型</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>一个盒子由内到外是 <code>content</code> → <code>padding</code> → <code>border</code> → <code>margin</code> 四层</li>\\n<li><code>content-box</code>（默认）的 <code>width</code> 只算内容区，<code>padding</code> 和 <code>border</code> 往外加</li>\\n<li><code>border-box</code> 的 <code>width</code> 含 <code>padding</code> 和 <code>border</code>，量出来就是设计稿标注的值</li>\\n<li>重置样式里几乎都会全局设 <code>border-box</code>，写法用 <code>inherit</code> 更好留例外口子</li>\\n<li><code>margin</code> 永远在盒子外面，两种模式都不含它 —— 这是最常被答错的一点</li>\\n</ul>\\n<p><strong>盒模型由内到外分为 <code>content</code>、<code>padding</code>、<code>border</code> 和 <code>margin</code> 四层。</strong> 默认的 <code>content-box</code> 中，<code>width</code> 和 <code>height</code> 只表示内容区，内边距和边框会继续把盒子撑大。设置 <code>box-sizing: border-box</code> 后，声明的宽高会包含 <code>padding</code> 与 <code>border</code>，因此更方便按设计稿控制尺寸。两种模型都不包含 <code>margin</code>，它始终属于盒子外部；需要统一项目行为时，可以全局设置并通过 <code>inherit</code> 保留调整空间。</p>\\n</section>\\n<blockquote>\\n<ul>\\n<li>有两种， <code>IE</code>盒子模型、<code>W3C</code>盒子模型；</li>\\n<li>盒模型： 内容(<code>content</code>)、填充(<code>padding</code>)、边界(<code>margin</code>)、 边框(<code>border</code>)；</li>\\n<li>区  别： <code>IE</code>的<code>content</code>部分把 <code>border</code> 和 <code>padding</code>计算了进去;</li>\\n</ul>\\n</blockquote>\\n<p><strong>标准盒子模型的模型图</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/5c302a15e52c1b08.png\\" alt=\\"\\"></p>\\n<p>从上图可以看到：</p>\\n<ul>\\n<li>盒子总宽度 = <code>width</code> + <code>padding</code> + <code>border</code> + <code>margin</code>;</li>\\n<li>盒子总高度 = <code>height</code> + <code>padding</code> + <code>border</code> + <code>margin</code></li>\\n</ul>\\n<p>也就是，<code>width/height</code> 只是内容高度，不包含 <code>padding</code> 和 <code>border</code> 值</p>\\n<p><strong>IE 怪异盒子模型</strong></p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/55340636fa7cdcee.png\\" alt=\\"\\"></p>\\n<p>从上图可以看到：</p>\\n<ul>\\n<li>盒子总宽度 = <code>width</code> + <code>margin</code>;</li>\\n<li>盒子总高度 = <code>height</code> + <code>margin</code>;</li>\\n</ul>\\n<p>也就是，<code>width/height</code> 包含了 <code>padding</code> 和 <code>border</code>值</p>\\n<blockquote>\\n<p>页面渲染时，<code>dom</code> 元素所采用的 布局模型。可通过<code>box-sizing</code>进行设置</p>\\n</blockquote>\\n<p><strong>通过 box-sizing 来改变元素的盒模型</strong></p>\\n<p>CSS 中的 <code>box-sizing</code> 属性定义了引擎应该如何计算一个元素的总宽度和总高度</p>\\n<ul>\\n<li><code>box-sizing: content-box;</code>  默认的标准(W3C)盒模型元素效果，元素的 <code>width/height</code> 不包含<code>padding</code>，<code>border</code>，与标准盒子模型表现一致</li>\\n<li><code>box-sizing: border-box;</code>   触发怪异(IE)盒模型元素的效果，元素的 <code>width/height</code> 包含 <code>padding</code>，<code>border</code>，与怪异盒子模型表现一致</li>\\n<li><code>box-sizing: inherit;</code>      继承父元素 <code>box-sizing</code> 属性的值</li>\\n</ul>\\n<p><strong>小结</strong></p>\\n<ul>\\n<li>盒子模型构成：内容(<code>content</code>)、内填充(<code>padding</code>)、 边框(<code>border</code>)、外边距(<code>margin</code>)</li>\\n<li><code>IE8</code>及其以下版本浏览器，未声明 <code>DOCTYPE</code>，内容宽高会包含内填充和边框，称为怪异盒模型(<code>IE</code>盒模型)</li>\\n<li>标准(<code>W3C</code>)盒模型：元素宽度 = <code>width + padding + border + margin</code></li>\\n<li>怪异(<code>IE</code>)盒模型：元素宽度 = <code>width + margin</code></li>\\n<li>标准浏览器通过设置 css3 的 <code>box-sizing: border-box</code> 属性，触发“怪异模式”解析计算宽高</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品卡片写了 <code>width: 240px; padding: 16px; border: 1px solid</code>，父容器正好 <code>240px</code>，上线后却横向溢出 <code>34px</code>；同事说把 <code>margin</code> 也算进 <code>width</code> 就行，你怎么纠正？</strong></p>\\n<p>默认 <code>content-box</code> 下，<code>width</code> 只约束内容区，实际边框盒宽度是 <code>240 + 16×2 + 1×2 = 274px</code>，因此溢出 <code>34px</code>。<code>margin</code> 位于边框盒之外，不会被 <code>width</code> 吸收；若设计稿的 <code>240px</code> 指外框，应改用 <code>box-sizing: border-box</code>。</p>\\n</li>\\n<li>\\n<p><strong>设计系统要求输入框、按钮和卡片的标注宽度都表示外框尺寸，你会怎样设置 <code>box-sizing</code>，同时给老旧第三方编辑器保留回退空间？</strong></p>\\n<p>可在根元素设置 <code>html { box-sizing: border-box; }</code>，再让 <code>*</code>、<code>*::before</code>、<code>*::after</code> 使用 <code>box-sizing: inherit</code>，统一按边框盒计算。第三方子树若依赖 <code>content-box</code>，只需在其根节点切回 <code>content-box</code>，后代便可继续继承，代价是接入时必须确认组件自身的尺寸假设。</p>\\n</li>\\n<li>\\n<p><strong>同一个 <code>width: 200px</code> 的输入框把水平 <code>padding</code> 从 <code>12px</code> 增加到 <code>20px</code>，产品要求外框不能变；在 <code>content-box</code> 和 <code>border-box</code> 下分别会发生什么？</strong></p>\\n<p>在 <code>content-box</code> 下，内容区仍为 <code>200px</code>，外框会随两侧 <code>padding</code> 增加而继续变宽，无法满足固定外框要求。在 <code>border-box</code> 下，外框仍是 <code>200px</code>，增加的 <code>padding</code> 和 <code>border</code> 会挤占内容区；空间过小时，内容可用宽度会成为新的风险。</p>\\n</li>\\n<li>\\n<p><strong>线上只有一组表单比设计稿宽，规则里已经有 <code>box-sizing: border-box</code>；你打开 <code>DevTools</code> 后会按什么顺序确认是盒模型、继承还是外边距造成的？</strong></p>\\n<p>先在盒模型面板核对 <code>content</code>、<code>padding</code>、<code>border</code> 和 <code>margin</code> 的实际值，再看 <code>Computed</code> 中最终生效的 <code>box-sizing</code> 及规则来源。若元素通过 <code>inherit</code> 取值，就继续检查父级是否在第三方子树中被切回 <code>content-box</code>；即使边框盒宽度正确，额外 <code>margin</code> 仍会扩大布局占用。</p>\\n</li>\\n<li>\\n<p><strong>一个定宽侧栏既能删掉左右 <code>padding</code>，也能保留视觉留白并改成 <code>border-box</code>，评审时你会怎样取舍？</strong></p>\\n<p>若设计要求侧栏外框尺寸固定且内部仍需留白，应保留 <code>padding</code> 并使用 <code>border-box</code>，让尺寸语义与标注一致。删除 <code>padding</code> 只是绕开宽度计算，会改变内容与边框的距离；但当内容区被挤得过窄时，仍需调整总宽或留白，而不是把 <code>border-box</code> 当成无条件修复。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_2-bfc\\"><a class=\\"header-anchor\\" href=\\"#_2-bfc\\">#</a> 2 BFC</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>BFC</code> 是一块独立的渲染区域，内部布局不影响外部，外部也不影响它</li>\\n<li>触发方式：根元素、<code>float</code> 不为 <code>none</code>、<code>position</code> 为 <code>absolute</code>/<code>fixed</code>、<code>overflow</code> 不为 <code>visible</code>、<code>display: flow-root</code>/<code>inline-block</code>/<code>table-cell</code>/<code>flex</code> 项等</li>\\n<li>三个经典用途：清除浮动导致的高度塌陷、阻断相邻元素的外边距重叠、让元素不被浮动元素覆盖</li>\\n<li>首选 <code>display: flow-root</code> —— 它是专为&quot;只要一个干净 BFC&quot;设计的，没有 <code>overflow: hidden</code> 的裁剪副作用</li>\\n</ul>\\n<p><strong><code>BFC</code> 是独立的块级格式化区域，内部元素的布局不会直接干扰外部。</strong> 常见触发方式可以归为定位、浮动和布局属性，例如绝对定位、<code>float</code> 不为 <code>none</code>，或 <code>overflow</code> 不为 <code>visible</code>。它计算高度时会包含浮动子元素，区域也不会与外部浮动盒重叠，所以常用于解决高度塌陷和自适应两栏布局。相邻盒子的外边距会在同一 <code>BFC</code> 中重叠，要阻止重叠，就需要让它们分属不同的 <code>BFC</code>。</p>\\n</section>\\n<blockquote>\\n<p>块级格式化上下文，是一个独立的渲染区域，让处于 <code>BFC</code> 内部的元素与外部的元素相互隔离，使内外元素的定位不会相互影响。</p>\\n</blockquote>\\n<blockquote>\\n<p><code>IE</code>下为 <code>Layout</code>，可通过 <code>zoom:1</code> 触发</p>\\n</blockquote>\\n<p><strong>触发条件:</strong></p>\\n<ul>\\n<li>根元素，即HTML元素</li>\\n<li>绝对定位元素 <code>position: absolute/fixed</code></li>\\n<li>行内块元素 <code>display</code>的值为<code>inline-block</code>、<code>table</code>、<code>flex</code>、<code>inline-flex</code>、<code>grid</code>、<code>inline-grid</code></li>\\n<li>浮动元素：<code>float</code>值为<code>left</code>、<code>right</code></li>\\n<li><code>overflow值</code>不为 <code>visible</code>，为 <code>auto</code>、<code>scroll</code>、<code>hidden</code></li>\\n</ul>\\n<p><strong>规则:</strong></p>\\n<ol>\\n<li>属于同一个 <code>BFC</code> 的两个相邻 <code>Box</code> 垂直排列</li>\\n<li>属于同一个 <code>BFC</code> 的两个相邻 <code>Box</code> 的 <code>margin</code> 会发生重叠</li>\\n<li><code>BFC</code> 中子元素的 <code>margin box</code> 的左边， 与包含块 (BFC) <code>border box</code>的左边相接触 (子元素 <code>absolute</code> 除外)</li>\\n<li><code>BFC</code> 的区域不会与 <code>float</code> 的元素区域重叠</li>\\n<li>计算 <code>BFC</code> 的高度时，浮动子元素也参与计算</li>\\n<li>文字层不会被浮动层覆盖，环绕于周围</li>\\n</ol>\\n<p><strong>应用:</strong></p>\\n<ul>\\n<li>利用<code>2</code>：阻止<code>margin</code>重叠</li>\\n<li>利用<code>4</code>：自适应两栏布局</li>\\n<li>利用 <code>5</code> ，可以避免高度塌陷</li>\\n<li>可以包含浮动元素 —— 清除内部浮动(清除浮动的原理是两个<code>div</code>都位于同一个 <code>BFC</code> 区域之中)</li>\\n</ul>\\n<p><strong>示例</strong></p>\\n<p><strong>1. 防止margin重叠（塌陷）</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n    p {\\n      color: #f55;\\n      background: #fcc;\\n      width: 200px;\\n      line-height: 100px;\\n      text-align:center;\\n      margin: 100px;\\n    }\\n&lt;/style&gt;\\n&lt;body&gt;\\n  &lt;p&gt;Haha&lt;/p &gt;\\n  &lt;p&gt;Hehe&lt;/p &gt;\\n&lt;/body&gt;\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/d8f4f47649805c27.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>两个<code>p</code>元素之间的距离为<code>100px</code>，发生了<code>margin</code>重叠（塌陷），以最大的为准，如果第一个<code>P</code>的<code>margin</code>为<code>80</code>的话，两个<code>P</code>之间的距离还是<code>100</code>，以最大的为准。</li>\\n<li>同一个<code>BFC</code>的俩个相邻的盒子的<code>margin</code>会发生重叠</li>\\n<li>可以在<code>p</code>外面包裹一层容器，并触发这个容器生成一个<code>BFC</code>，那么两个<code>p</code>就不属于同一个<code>BFC</code>，则不会出现<code>margin</code>重叠</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n    .wrap {\\n        overflow: hidden;// 新的BFC\\n    }\\n    p {\\n        color: #f55;\\n        background: #fcc;\\n        width: 200px;\\n        line-height: 100px;\\n        text-align:center;\\n        margin: 100px;\\n    }\\n&lt;/style&gt;\\n&lt;body&gt;\\n    &lt;p&gt;Haha&lt;/p &gt;\\n    &lt;div class=&quot;wrap&quot;&gt;\\n        &lt;p&gt;Hehe&lt;/p &gt;\\n    &lt;/div&gt;\\n&lt;/body&gt;\\n</code></pre>\\n<p>这时候，边距则不会重叠：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/5d6156d0fd00e6da.png\\" alt=\\"\\"></p>\\n<p><strong>2. 清除内部浮动</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n    .par {\\n        border: 5px solid #fcc;\\n        width: 300px;\\n    }\\n \\n    .child {\\n        border: 5px solid #f66;\\n        width:100px;\\n        height: 100px;\\n        float: left;\\n    }\\n&lt;/style&gt;\\n&lt;body&gt;\\n    &lt;div class=&quot;par&quot;&gt;\\n      &lt;div class=&quot;child&quot;&gt;&lt;/div&gt;\\n      &lt;div class=&quot;child&quot;&gt;&lt;/div&gt;\\n    &lt;/div&gt;\\n&lt;/body&gt;\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/f8eb06e196dd211b.png\\" alt=\\"\\"></p>\\n<p>而<code>BFC</code>在计算高度时，浮动元素也会参与，所以我们可以触发<code>.par</code>元素生成<code>BFC</code>，则内部浮动元素计算高度时候也会计算</p>\\n<pre class=\\"language-css\\"><code>.par {\\n    overflow: hidden;\\n}\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/32984028a445dbc7.png\\" alt=\\"\\"></p>\\n<p><strong>3. 自适应多栏布局</strong></p>\\n<p>这里举个两栏的布局</p>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n    body {\\n        width: 300px;\\n        position: relative;\\n    }\\n \\n    .aside {\\n        width: 100px;\\n        height: 150px;\\n        float: left;\\n        background: #f66;\\n    }\\n \\n    .main {\\n        height: 200px;\\n        background: #fcc;\\n    }\\n&lt;/style&gt;\\n&lt;body&gt;\\n    &lt;div class=&quot;aside&quot;&gt;&lt;/div&gt;\\n    &lt;div class=&quot;main&quot;&gt;&lt;/div&gt;\\n&lt;/body&gt;\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/07acd0b2a466762f.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>每个元素的左外边距与包含块的左边界相接触</li>\\n<li>因此，虽然<code>.aslide</code>为浮动元素，但是main的左边依然会与包含块的左边相接触，而<code>BFC</code>的区域不会与浮动盒子重叠</li>\\n<li>所以我们可以通过触发<code>main</code>生成<code>BFC</code>，以此适应两栏布局</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>.main {\\n  overflow: hidden;\\n}\\n</code></pre>\\n<p>这时候，新的<code>BFC</code>不会与浮动的<code>.aside</code>元素重叠。因此会根据包含块的宽度，和<code>.aside</code>的宽度，自动变窄</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/8749636e9068ae4a.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>文章列表里两个相邻段落分别写了 <code>margin-bottom: 24px</code> 和 <code>margin-top: 16px</code>，设计师却量到间距只有 <code>24px</code>；有人判断是浏览器漏算了 <code>16px</code>，你怎么解释并处理？</strong></p>\\n<p>两者处于同一个 <code>BFC</code> 且垂直相邻时，上下外边距会重叠，间距取较大的 <code>24px</code>，并非浏览器漏算。若目标是稳定的 <code>40px</code>，可统一由一侧提供间距；只有确实需要隔离两组外边距时，才为其中一组增加新的 <code>BFC</code> 容器。</p>\\n</li>\\n<li>\\n<p><strong>旧版图文卡片的两张缩略图都设了 <code>float: left</code>，父卡片只剩边框高度，后续内容顶了上来；不改浮动结构时你会怎样修？</strong></p>\\n<p>让父卡片形成 <code>BFC</code>，例如设置 <code>overflow: hidden</code>，计算父级高度时浮动子元素便会参与，边框可重新包住内容。选择 <code>overflow</code> 前要检查卡片是否存在阴影、下拉层或越界装饰，因为 <code>hidden</code> 同时具有裁剪效果，可能引入新的显示故障。</p>\\n</li>\\n<li>\\n<p><strong>新闻详情页是左侧 <code>120px</code> 浮动头像、右侧正文自适应；正文背景钻到了头像下面，且产品要求正文区域始终不能与头像重叠，你会改哪一侧？</strong></p>\\n<p>应让右侧正文形成新的 <code>BFC</code>，利用其区域不会与浮动元素区域重叠的规则，正文会在剩余宽度内自动变窄。只给正文增加左内边距依赖头像宽度，头像尺寸变化时还要同步维护；使用 <code>BFC</code> 的边界是窄屏剩余空间不足时仍需另做布局降级。</p>\\n</li>\\n<li>\\n<p><strong>线上卡片加了 <code>overflow: hidden</code> 清浮动后，悬浮菜单和 <code>box-shadow</code> 被截断；你如何确认根因并选择替代方式？</strong></p>\\n<p>先在 <code>DevTools</code> 临时关闭 <code>overflow: hidden</code>，若菜单和阴影恢复且父级高度再次塌陷，就能确认清浮动与裁剪来自同一条规则。可改用专门建立独立格式化上下文的方案，或用清除浮动伪元素保留溢出展示；若必须兼容既有环境，则需根据支持范围选择，而不能仅为清浮动接受裁剪。</p>\\n</li>\\n<li>\\n<p><strong>新页面能用 <code>flex</code> 完成两栏布局，旧模块则依赖 <code>float + BFC</code>；架构评审中有人要求全部保留 <code>BFC</code> 写法以减少改动，你会怎么判断？</strong></p>\\n<p>旧模块若结构稳定且改造风险高，可以保留 <code>float + BFC</code>，它能实现避开浮动和包含浮动元素。新页面更适合直接用 <code>flex</code> 表达两栏关系，避免把 <code>overflow</code> 的裁剪副作用带进布局；迁移旧模块时还要核对文本环绕行为，因为它可能正是原实现需要的效果。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_3-层叠上下文\\"><a class=\\"header-anchor\\" href=\\"#_3-层叠上下文\\">#</a> 3 层叠上下文</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>层叠上下文是三维方向的隔离层，<code>z-index</code> 只在<strong>同一个</strong>层叠上下文内比大小</li>\\n<li>触发它的不只是 <code>position</code> + <code>z-index</code>：<code>transform</code>、<code>opacity &lt; 1</code>、<code>filter</code>、<code>will-change</code>、<code>fixed</code>/<code>sticky</code>、<code>contain</code> 都会</li>\\n<li>典型症状是&quot;弹窗写了 <code>z-index: 9999</code> 还被盖住&quot; —— 因为它被关在某个祖先创建的层里</li>\\n<li>排查靠沿父级看 <code>Computed</code>，根治靠 <code>Portal</code> 把弹层挂到 <code>body</code></li>\\n<li><code>isolation: isolate</code> 可以主动开一层，把组件内部的 <code>z-index</code> 关起来不外溢</li>\\n</ul>\\n<p><strong>层叠上下文本质上是三维空间里的隔离层，<code>z-index</code> 只能在所属层叠上下文内比较。</strong> 除了定位元素配合 <code>z-index</code>，<code>transform</code>、<code>opacity &lt; 1</code>、<code>filter</code> 和 <code>isolation: isolate</code> 等属性也可能创建新层。弹窗即使设置 <code>z-index: 9999</code> 仍被遮住，通常是因为祖先形成了层叠上下文，整层在外部的层级更低。排查时可沿父级检查计算样式，弹层则可通过 <code>Portal</code> 挂到 <code>body</code>；组件内部也能用 <code>isolation: isolate</code> 限制层级影响范围。</p>\\n</section>\\n<p>层叠上下文（stacking context）是<strong>三维空间上的一层隔离</strong>：一旦某个元素创建了层叠上下文，它的所有子元素就被&quot;关&quot;在这一层里，无论 <code>z-index</code> 写多大，都只能在这层内部比高低。</p>\\n<p><strong>常见的触发条件：</strong></p>\\n<ul>\\n<li>根元素 <code>html</code></li>\\n<li><code>position</code> 为 <code>relative</code>/<code>absolute</code> 且 <code>z-index</code> 不为 <code>auto</code></li>\\n<li><code>position: fixed</code> / <code>sticky</code>（无条件创建）</li>\\n<li><code>opacity</code> 小于 1</li>\\n<li><code>transform</code>、<code>filter</code>、<code>perspective</code>、<code>clip-path</code>、<code>mask</code> 不为 <code>none</code></li>\\n<li><code>will-change</code> 指定了上述任一属性</li>\\n<li><code>isolation: isolate</code></li>\\n<li><code>flex</code>/<code>grid</code> 容器的子项且 <code>z-index</code> 不为 <code>auto</code></li>\\n<li><code>contain: layout / paint / strict / content</code></li>\\n</ul>\\n<p><strong>同一层叠上下文内的绘制顺序</strong>（由低到高）：</p>\\n<pre class=\\"language-text\\"><code>背景和边框 → 负 z-index 的子层叠上下文 → 块级盒 → 浮动盒\\n→ 行内内容 → z-index: 0 / auto → 正 z-index 的子层叠上下文\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/09/111.png\\" alt=\\"\\"></p>\\n<p><strong>最典型的坑：<code>z-index: 9999</code> 的弹窗还是被盖住。</strong></p>\\n<pre class=\\"language-css\\"><code>.card { transform: translateY(0); }   /* 无意中创建了层叠上下文 */\\n.card .modal { position: fixed; z-index: 9999; }  /* 只在 .card 这层里最高 */\\n.header { position: sticky; z-index: 10; }        /* 和 .card 同级，10 &gt; 0 */\\n</code></pre>\\n<p>弹窗虽然是 <code>9999</code>，但它被关在 <code>.card</code> 创建的层里；<code>.card</code> 自己在父层的层级是 <code>auto</code>（相当于 0），所以整块都被 <code>z-index: 10</code> 的 header 压住。</p>\\n<p><strong>排查方法</strong>：选中弹窗，沿父级逐层看 <code>Computed</code> 里有没有 <code>transform</code>、<code>opacity &lt; 1</code>、<code>filter</code>、<code>will-change</code>、<code>contain</code>；<code>DevTools</code> 的 <code>Layers</code> 面板也能直观看到层的归属关系。</p>\\n<p><strong>根治办法</strong>：把弹窗用 <code>Portal</code> 渲染到 <code>body</code> 下，彻底脱离那些祖先。这也是所有 UI 库的弹层默认都挂在 <code>body</code> 上的原因。</p>\\n<p><strong>反过来用</strong>：<code>isolation: isolate</code> 可以<strong>主动</strong>创建一个层叠上下文，把一个组件内部的 <code>z-index</code> 关起来，避免它影响到外部 —— 写组件库时很有用。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>后台页面的弹窗写了 <code>position: fixed; z-index: 9999</code>，仍被 <code>z-index: 10</code> 的吸顶导航盖住；弹窗祖先 <code>.card</code> 有 <code>transform: translateY(0)</code>，为什么继续加大数字没有用？</strong></p>\\n<p><code>.card</code> 的 <code>transform</code> 创建了层叠上下文，弹窗的 <code>9999</code> 只能在该上下文内部比较，无法越过祖先参与外层竞争。导航与 <code>.card</code> 在外层比较时，导航的 <code>z-index: 10</code> 仍可能压住整棵卡片；修复应调整层级结构或把弹窗渲染到 <code>body</code> 下。</p>\\n</li>\\n<li>\\n<p><strong>组件库要同时提供提示气泡、抽屉和模态框，业务方主张统一写 <code>z-index: 99999</code>；你会怎样设计层级，避免组件之间互相污染？</strong></p>\\n<p>全局弹层应通过 <code>Portal</code> 挂到 <code>body</code> 附近，再用少量语义化层级管理气泡、抽屉和模态框之间的顺序。普通业务组件可用 <code>isolation: isolate</code> 主动封闭内部层级，避免局部数字泄漏到页面；但隔离后子元素也无法越过该组件，跨区域浮层仍应走全局容器。</p>\\n</li>\\n<li>\\n<p><strong>动画团队给列表容器加了 <code>opacity: 0.99</code> 和 <code>will-change: transform</code>，动画结束后其中的下拉菜单被相邻列表项遮住；代码里没有显式 <code>z-index</code> 冲突，你怎么判断？</strong></p>\\n<p><code>opacity &lt; 1</code> 会创建层叠上下文，<code>will-change: transform</code> 也可能提前建立相同隔离，因此下拉菜单被限制在列表项所属层内。应在动画结束后移除不再需要的属性，或把菜单通过 <code>Portal</code> 脱离祖先；保留这些属性时，再大的菜单 <code>z-index</code> 也只能解决层内排序。</p>\\n</li>\\n<li>\\n<p><strong>线上只有某个营销页的日期选择器被遮挡，你在 <code>Styles</code> 面板没看到异常的 <code>z-index</code>；接下来会沿祖先检查哪些属性，并怎样验证层的归属？</strong></p>\\n<p>沿日期选择器的父级逐层检查 <code>position</code> 与非 <code>auto</code> 的 <code>z-index</code>，以及 <code>transform</code>、<code>filter</code>、<code>opacity</code>、<code>will-change</code>、<code>contain</code> 和 <code>isolation</code>。再结合 <code>DevTools</code> 的 <code>Layers</code> 面板观察它属于哪个层叠上下文，并临时关闭可疑属性验证；不要只检查选择器自身，因为真正的边界通常由祖先创建。</p>\\n</li>\\n<li>\\n<p><strong>卡片里的角标只需压住图片，但有人建议把它直接 <code>Portal</code> 到 <code>body</code>，理由是以后不会遇到层级问题；你会接受吗？</strong></p>\\n<p>角标只在卡片内部与图片竞争时，保留在组件中并建立局部层叠规则更合适，必要时可用 <code>isolation: isolate</code> 限定影响范围。<code>Portal</code> 适合必须越过祖先的模态框、菜单等全局浮层；把局部装饰移到 <code>body</code> 会增加定位、滚动同步和生命周期管理成本。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_4-居中布局\\"><a class=\\"header-anchor\\" href=\\"#_4-居中布局\\">#</a> 4 居中布局</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>水平居中：行内内容用 <code>text-align: center</code>，块级定宽用 <code>margin: 0 auto</code>，宽度不定用 <code>flex</code> + <code>justify-content: center</code></li>\\n<li>垂直居中：单行文本可用 <code>line-height</code> 等于高度，其余一律 <code>flex</code> 的 <code>align-items: center</code></li>\\n<li>双向居中最省事的是 <code>display: grid; place-items: center</code>，一行搞定且与内容尺寸无关</li>\\n<li>绝对定位方案用 <code>left: 50%</code> + <code>translateX(-50%)</code>，百分比 <code>translate</code> 按自身宽度算所以不需要知道宽度</li>\\n<li>移动端整屏居中别用 <code>100vh</code>，地址栏会让它溢出，改用 <code>100dvh</code></li>\\n</ul>\\n<p><strong>居中布局要根据元素宽高是否确定，以及是否允许脱离文档流来选择方案。</strong> 普通页面布局我一般用 <code>flex</code> 或 <code>grid</code>，通过主轴和交叉轴对齐即可，子元素不定宽高也能同时水平、垂直居中。需要脱离文档流时，可以用绝对定位配合 <code>left: 50%</code>、<code>top: 50%</code> 和 <code>transform: translate(-50%, -50%)</code>。如果尺寸固定，也能用负外边距或四边设为 <code>0</code> 再配合 <code>margin: auto</code>；行内内容则优先考虑 <code>text-align</code>、<code>line-height</code> 或 <code>table-cell</code>。</p>\\n</section>\\n<h3 id=\\"元素水平垂直居中的方法有哪些-如果元素不定宽高呢\\"><a class=\\"header-anchor\\" href=\\"#元素水平垂直居中的方法有哪些-如果元素不定宽高呢\\">#</a> 元素水平垂直居中的方法有哪些？如果元素不定宽高呢？</h3>\\n<ol>\\n<li><strong>利用绝对定位+transform</strong>，设置 <code>left: 50%</code>  和 <code>top: 50%</code>  现将子元素左上角移到父元素中心位置，然后再通过 <code>translate</code>  来调整子元素的中心点到父元素的中心。该方法可以<strong>不定宽高</strong></li>\\n</ol>\\n<pre class=\\"language-css\\"><code>.father {\\n  position: relative;\\n}\\n.son {\\n  position: absolute;\\n  left: 50%;\\n  top: 50%;\\n  transform: translate(-50%, -50%);\\n}\\n</code></pre>\\n<ol start=\\"2\\">\\n<li><strong>利用绝对定位+margin:auto</strong>，子元素所有方向都为 <code>0</code> ，将 <code>margin</code>  设置为 <code>auto</code> ，由于宽高固定，对应方向实现平分，该方法必须<strong>盒子有宽高</strong></li>\\n</ol>\\n<pre class=\\"language-css\\"><code>.father {\\n  position: relative;\\n}\\n.son {\\n  position: absolute;\\n  top: 0;\\n  left: 0;\\n  right: 0;\\n  bottom: 0px;\\n  margin: auto;\\n  height: 100px;\\n  width: 100px;\\n}\\n</code></pre>\\n<ol start=\\"3\\">\\n<li><strong>利用绝对定位+margin:负值</strong>，设置 <code>left: 50%</code> 和 <code>top: 50%</code> 现将子元素左上角移到父元素中心位置，然后再通过 <code>margin-left</code>  和 <code>margin-top</code>  以子元素自己的一半宽高进行负值赋值。该方法必须定宽高</li>\\n</ol>\\n<pre class=\\"language-css\\"><code>.father {\\n  position: relative;\\n}\\n.son {\\n  position: absolute;\\n  left: 50%;\\n  top: 50%;\\n  width: 200px;\\n  height: 200px;\\n  margin-left: -100px;\\n  margin-top: -100px;\\n}\\n</code></pre>\\n<ol start=\\"4\\">\\n<li><strong>利用 flex</strong> ，最经典最方便的一种了，不用解释，<strong>定不定宽高无所谓</strong></li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n    .father {\\n        display: flex;\\n        justify-content: center;\\n        align-items: center;\\n        width: 200px;\\n        height: 200px;\\n        background: skyblue;\\n    }\\n    .son {\\n        width: 100px;\\n        height: 100px;\\n        background: red;\\n    }\\n&lt;/style&gt;\\n&lt;div class=&quot;father&quot;&gt;\\n    &lt;div class=&quot;son&quot;&gt;&lt;/div&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<ol start=\\"5\\">\\n<li><strong>grid网格布局</strong></li>\\n</ol>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n.father {\\n  display: grid;\\n  align-items:center;\\n  justify-content: center;\\n  width: 200px;\\n  height: 200px;\\n  background: skyblue;\\n\\n}\\n.son {\\n  width: 10px;\\n  height: 10px;\\n  border: 1px solid red\\n}\\n&lt;/style&gt;\\n&lt;div class=&quot;father&quot;&gt;\\n  &lt;div class=&quot;son&quot;&gt;&lt;/div&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<ol start=\\"6\\">\\n<li><strong>table布局</strong></li>\\n</ol>\\n<p>设置父元素为<code>display:table-cell</code>，子元素设置 <code>display: inline-block</code>。利用<code>vertical</code>和<code>text-align</code>可以让所有的行内块级元素水平垂直居中</p>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n    .father {\\n        display: table-cell;\\n        width: 200px;\\n        height: 200px;\\n        background: skyblue;\\n        vertical-align: middle;\\n        text-align: center;\\n    }\\n    .son {\\n        display: inline-block;\\n        width: 100px;\\n        height: 100px;\\n        background: red;\\n    }\\n&lt;/style&gt;\\n&lt;div class=&quot;father&quot;&gt;\\n    &lt;div class=&quot;son&quot;&gt;&lt;/div&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p><strong>小结</strong></p>\\n<p>不知道元素宽高大小仍能实现水平垂直居中的方法有：</p>\\n<ul>\\n<li><code>利用定位+transform</code></li>\\n<li><code>flex</code>布局</li>\\n<li><code>grid</code>布局</li>\\n</ul>\\n<p><strong>根据元素标签的性质，可以分为：</strong></p>\\n<ul>\\n<li>内联元素居中布局</li>\\n<li>块级元素居中布局</li>\\n</ul>\\n<p><strong>内联元素居中布局</strong></p>\\n<ul>\\n<li>水平居中\\n<ul>\\n<li>行内元素可设置：<code>text-align: center</code></li>\\n<li><code>flex</code>布局设置父元素：<code>display: flex; justify-content: center</code></li>\\n</ul>\\n</li>\\n<li>垂直居中\\n<ul>\\n<li>单行文本父元素确认高度：<code>height === line-height</code></li>\\n<li>多行文本父元素确认高度：<code>display: table-cell; vertical-align: middle</code></li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>块级元素居中布局</strong></p>\\n<ul>\\n<li>水平居中\\n<ul>\\n<li>定宽: <code>margin: 0 auto</code></li>\\n<li><code>绝对定位+left:50%+margin:负自身一半</code></li>\\n</ul>\\n</li>\\n<li>垂直居中\\n<ul>\\n<li><code>position: absolute</code>设置<code>left</code>、<code>top</code>、<code>margin-left</code>、<code>margin-top</code>(定高)</li>\\n<li><code>display: table-cell</code></li>\\n<li><code>transform: translate(x, y)</code></li>\\n<li><code>flex</code>(不定高，不定宽)</li>\\n<li><code>grid</code>(不定高，不定宽)，兼容性相对比较差</li>\\n</ul>\\n</li>\\n</ul>\\n<h3 id=\\"左右居中\\"><a class=\\"header-anchor\\" href=\\"#左右居中\\">#</a> 左右居中</h3>\\n<ul>\\n<li>行内元素: <code>text-align: center</code></li>\\n<li>定宽块状元素: 左右 <code>margin</code> 值为 <code>auto</code></li>\\n<li>不定宽块状元素: <code>table</code>布局，<code>position + transform</code></li>\\n</ul>\\n<pre class=\\"language-css\\"><code>/* 方案1 */\\n.wrap {\\n  text-align: center\\n}\\n.center {\\n  display: inline;\\n  /* or */\\n  /* display: inline-block; */\\n}\\n/* 方案2 */\\n.center {\\n  width: 100px;\\n  margin: 0 auto;\\n}\\n/* 方案2 */\\n.wrap {\\n  position: relative;\\n}\\n.center {\\n  position: absulote;\\n  left: 50%;\\n  transform: translateX(-50%);\\n}\\n</code></pre>\\n<h3 id=\\"上下居中\\"><a class=\\"header-anchor\\" href=\\"#上下居中\\">#</a> 上下居中</h3>\\n<ul>\\n<li>定高：<code>margin</code>，<code>position + margin</code>(负值)</li>\\n<li>不定高：<code>position</code> + <code>transform</code>，<code>flex</code>，<code>IFC + vertical-align:middle</code></li>\\n</ul>\\n<pre class=\\"language-css\\"><code>/* 定高方案1 */\\n.center {\\n  height: 100px;\\n  margin: 50px 0;   \\n}\\n/* 定高方案2 */\\n.center {\\n  height: 100px;\\n  position: absolute;\\n  top: 50%;\\n  margin-top: -25px;\\n}\\n/* 不定高方案1 */\\n.center {\\n  position: absolute;\\n  top: 50%;\\n  transform: translateY(-50%);\\n}\\n/* 不定高方案2 */\\n.wrap {\\n  display: flex;\\n  align-items: center;\\n}\\n.center {\\n  width: 100%;\\n}\\n/* 不定高方案3 */\\n/* 设置 inline-block 则会在外层产生 IFC，高度设为 100% 撑开 wrap 的高度 */\\n.wrap::before {\\n  content: &#39;&#39;;\\n  height: 100%;\\n  display: inline-block;\\n  vertical-align: middle;\\n}\\n.wrap {\\n  text-align: center;\\n}\\n.center {\\n  display: inline-block;  \\n  vertical-align: middle;\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>登录页容器高 <code>600px</code>，表单高 <code>100px</code>，同事写了 <code>top: 50%; margin-top: -25px</code>，结果表单中心偏下；你会怎样指出计算错误？</strong></p>\\n<p>负外边距应是元素自身高度的一半，这里应为 <code>margin-top: -50px</code>，<code>-25px</code> 只抵消了四分之一高度。该方案依赖已知高度，表单因校验文案增高后还会再次偏移；高度可能变化时应改用 <code>top: 50%</code> 配合 <code>transform: translateY(-50%)</code>。</p>\\n</li>\\n<li>\\n<p><strong>弹窗正文会因接口错误信息从两行增长到十行，设计要求无论内容多高都尽量垂直居中；你会选负 <code>margin</code>、<code>transform</code> 还是 <code>flex</code>？</strong></p>\\n<p>正文高度不确定时不应使用固定负 <code>margin</code>，可让弹窗容器使用 <code>flex</code> 和 <code>align-items: center</code>，或对内容使用 <code>top: 50%</code> 与 <code>translateY(-50%)</code>。若内容最终高于容器，强行居中可能让顶部越界，因此还要配合可滚动区域或改为顶部对齐。</p>\\n</li>\\n<li>\\n<p><strong>一个老式邮件模板不能依赖现代弹性布局，容器高度固定，里面的图标和两行文字要一起居中；基于现有约束你会怎样处理？</strong></p>\\n<p>可利用 <code>inline-block</code> 与 <code>vertical-align: middle</code>，通过容器的伪元素设置 <code>height: 100%</code> 撑出行内格式化上下文，再让内容块同样按中线对齐。该写法对行内空白和内容结构较敏感，容器高度也必须明确；若运行环境允许，<code>flex</code> 会更直接且更易维护。</p>\\n</li>\\n<li>\\n<p><strong>线上加载态原本垂直居中，加入一段异步错误提示后整体突然偏移；样式同时存在固定高度、负 <code>margin</code> 和 <code>transform</code>，你会怎样排查？</strong></p>\\n<p>先确认实际参与定位的容器高度和定位包含块，再检查负 <code>margin</code> 是否仍按旧内容高度计算，以及 <code>transform</code> 是否与旧补偿重复生效。临时逐条关闭定位规则可定位偏移来源；若内容高度会变化，应收敛为一种不依赖固定高度的方案，避免多个补偿值叠加。</p>\\n</li>\\n<li>\\n<p><strong>产品要求内容区只做垂直居中并保持横向撑满，开发把容器写成 <code>display: flex; align-items: center; justify-content: center</code>；为什么可能破坏原布局，应该怎样改？</strong></p>\\n<p><code>align-items: center</code> 在默认横向主轴下负责垂直方向，而 <code>justify-content: center</code> 会额外把内容在水平方向居中，可能改变原有横向位置。只需保留 <code>align-items: center</code>，并让子项按需要设置 <code>width: 100%</code>；轴方向若改成 <code>column</code>，两者职责会交换，评审时必须结合 <code>flex-direction</code> 判断。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_5-选择器权重计算方式\\"><a class=\\"header-anchor\\" href=\\"#_5-选择器权重计算方式\\">#</a> 5 选择器权重计算方式</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>权重是三元组 <code>(ID, 类/属性/伪类, 元素/伪元素)</code>，<strong>逐位比较</strong>，不进位</li>\\n<li>完整顺序：<code>!important</code> &gt; 内联 <code>style</code> &gt; ID &gt; 类/属性/伪类 &gt; 元素/伪元素 &gt; 通配符 &gt; 继承</li>\\n<li>权重相同时后写的赢 —— 这正是 <code>LVHA</code> 顺序必须遵守的原因</li>\\n<li>三个易错点：<code>:not()</code> 自身不计但参数计、<code>:where()</code> 全按 0 算、<code>:is()</code> 取参数里最高的</li>\\n<li>盖不住时先看 <code>DevTools</code> 里哪条规则被划掉，别一路加 <code>!important</code></li>\\n</ul>\\n<p><strong>选择器权重按 <code>(ID, 类/属性/伪类, 元素/伪元素)</code> 三元组逐位比较，前一位更大就胜出，不存在进位。</strong> 整体优先级是 <code>!important</code>、内联 <code>style</code>、<code>ID</code>、类、元素、通配符、继承；权重相同则后写的规则生效。要注意 <code>:not()</code> 计算参数、<code>:where()</code> 权重为零、<code>:is()</code> 取参数中的最高权重。样式覆盖失败时，我一般先看 <code>DevTools</code> 中被划掉的规则，不会直接堆 <code>!important</code>。</p>\\n</section>\\n<p>权重不是一个数字，而是一个<strong>三元组</strong> <code>(A, B, C)</code>，逐位比较，前一位大就直接赢，不存在&quot;十个 class 顶一个 id&quot;这种进位：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>位</th>\\n<th>计入什么</th>\\n<th>例子</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><strong>A</strong></td>\\n<td>ID 选择器</td>\\n<td><code>#app</code></td>\\n</tr>\\n<tr>\\n<td><strong>B</strong></td>\\n<td>类、属性、伪类</td>\\n<td><code>.btn</code>、<code>[type=text]</code>、<code>:hover</code>、<code>:not()</code> 的参数</td>\\n</tr>\\n<tr>\\n<td><strong>C</strong></td>\\n<td>元素、伪元素</td>\\n<td><code>div</code>、<code>::before</code></td>\\n</tr>\\n</tbody>\\n</table>\\n<pre class=\\"language-text\\"><code>#nav .item a:hover   →  (1, 2, 1)\\n.item .active a      →  (0, 2, 1)      ← 输，因为 A 位 0 &lt; 1\\n*, 后代/兄弟组合符   →  不计权重\\n</code></pre>\\n<p><strong>优先级的完整顺序</strong>（从高到低）：</p>\\n<pre class=\\"language-text\\"><code>!important &gt; 内联 style 属性 &gt; ID &gt; 类/属性/伪类 &gt; 元素/伪元素 &gt; 通配符 &gt; 继承\\n</code></pre>\\n<p><strong>权重相同时，后写的赢</strong> —— 这就是 <code>LVHA</code> 顺序必须遵守的原因（四个链接伪类权重完全一样）。</p>\\n<p><strong>三个容易答错的细节：</strong></p>\\n<ol>\\n<li><strong><code>:not()</code> 本身不计权重，但它的参数计。</strong> <code>div:not(.foo)</code> 是 <code>(0,1,1)</code> 而不是 <code>(0,0,1)</code> —— 这条经常导致&quot;明明只加了个 <code>:not</code> 怎么就盖不住了&quot;。</li>\\n<li><strong><code>:where()</code> 里的一切都按 0 算。</strong> <code>:where(.a, .b) p</code> 只有 <code>(0,0,1)</code>。写组件库默认样式时用它，使用方一个类名就能覆盖，不用打权重战。</li>\\n<li><strong><code>:is()</code> 取参数里权重最高的那个</strong>，和 <code>:where()</code> 正好相反。</li>\\n</ol>\\n<pre class=\\"language-css\\"><code>/* 组件库默认样式：权重压到最低，方便使用方覆盖 */\\n:where(.card) h3 { margin: 0; }\\n/* 使用方一个类就能赢 */\\n.my-title { margin: 12px 0; }\\n</code></pre>\\n<p><strong>关于 <code>!important</code></strong>：它是&quot;逃生口&quot;不是&quot;工具&quot;。一旦用了，下一个人只能用更多的 <code>!important</code> 或更长的选择器，最后没人敢动样式。盖不住时先看 <code>DevTools</code> 的 <code>Styles</code> 面板 —— 被划掉的规则旁边就写着赢的是谁，往往加一层组件根类名就解决了。</p>\\n<p><strong>顺带一提解析方向</strong>：浏览器匹配选择器是<strong>从右往左</strong>的。因为从左往右要一路走到最后才知道不匹配，而从右往左发现最右边的&quot;关键选择器&quot;不匹配就能立刻放弃，效率高得多。这也是不建议写 <code>div &gt; ul &gt; li &gt; a &gt; span</code> 这种超长选择器的原因。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>主题样式 <code>.page .toolbar .btn</code> 想覆盖 <code>#app .btn</code>，同事准备再加十个类名；你会怎样现场判断谁赢？</strong></p>\\n<p><code>#app .btn</code> 的权重是 <code>(1,1,0)</code>，<code>.page .toolbar .btn</code> 是 <code>(0,3,0)</code>，比较到 <code>ID</code> 位时前者已经胜出，继续堆类名也不能通过进位反超。应减少上游 <code>ID</code> 选择器，或在明确作用域内调整规则来源；盲目拉长选择器只会增加后续覆盖成本。</p>\\n</li>\\n<li>\\n<p><strong>组件库默认标题规则写成 <code>.card h3</code>，业务方希望用一个 <code>.report-title</code> 就能改间距；作为库维护者你会怎样调整默认规则？</strong></p>\\n<p>可将默认规则写成 <code>:where(.card) h3</code>，其权重只有元素选择器贡献的 <code>(0,0,1)</code>，业务方的 <code>.report-title</code> 为 <code>(0,1,0)</code>，能够直接覆盖。这样能降低默认样式侵入性；若库内存在 <code>!important</code> 或内联样式，单纯降低选择器权重仍无法解决。</p>\\n</li>\\n<li>\\n<p><strong>筛选器把规则从 <code>div.foo</code> 改成 <code>div:not(.disabled)</code> 后，另一条元素样式突然盖不住了；评审人说 <code>:not()</code> 不计权重，所以不可能有关，你怎么回应？</strong></p>\\n<p><code>:not()</code> 这个伪类本身不额外计权重，但参数 <code>.disabled</code> 会计入，因此 <code>div:not(.disabled)</code> 的权重是 <code>(0,1,1)</code>。它会压过只有元素或更低权重的规则；修复时应核对参数带来的权重，而不是用更多 <code>!important</code> 掩盖来源。</p>\\n</li>\\n<li>\\n<p><strong>线上按钮颜色只在悬停后错误，<code>DevTools</code> 里 <code>.btn:hover</code> 和 <code>.toolbar .btn</code> 都没有 <code>!important</code>；你会怎样从权重和源码顺序定位？</strong></p>\\n<p><code>.btn:hover</code> 与 <code>.toolbar .btn</code> 都是 <code>(0,2,0)</code>，权重相同时由源码中后出现的规则获胜，因此要先看 <code>Styles</code> 面板里哪条声明被划掉及其加载顺序。若构建产物改变了样式注入次序，开发环境正常、线上异常也可能由此产生；修复应稳定顺序或明确作用域。</p>\\n</li>\\n<li>\\n<p><strong>团队在覆盖第三方组件时争论用 <code>!important</code> 还是写 <code>.page .dialog .header .title</code>；你会怎样选，什么情况下两者都不理想？</strong></p>\\n<p>先在 <code>DevTools</code> 找到真正获胜的规则，通常用一个稳定的组件根类名限定作用域即可，不必堆出超长选择器。<code>!important</code> 会迫使后续覆盖继续升级，长选择器则提高耦合且浏览器从右向左匹配；若第三方本身依赖内联样式或 <code>!important</code>，更稳妥的方向是使用其主题接口或调整样式入口。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_6-清除浮动\\"><a class=\\"header-anchor\\" href=\\"#_6-清除浮动\\">#</a> 6 清除浮动</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>浮动脱离常规流，父容器算高度时不计入，于是高度塌陷、后面内容重叠</li>\\n<li>首选 <code>display: flow-root</code>：一行、语义明确、<strong>零副作用</strong></li>\\n<li>兼容老浏览器用 <code>::after { display: block; clear: both; content: \'\' }</code></li>\\n<li><code>overflow: hidden</code> 能清但会裁阴影和弹层，还会让容器变成滚动容器导致内部 <code>sticky</code> 失效</li>\\n<li>新布局别再用浮动 —— 它现在的本职工作只剩「文字环绕图片」，多列排版交给 <code>flex</code>/<code>grid</code></li>\\n</ul>\\n<p><strong>清除浮动是为了解决浮动元素不参与父容器常规流高度计算，导致父容器塌陷和后续内容重叠的问题。</strong> 新代码首选 <code>display: flow-root</code>，写法简单、语义明确，也没有裁剪内容等副作用。需要兼容老浏览器时，可以用 <code>::after</code> 配合 <code>clear: both</code>；<code>overflow: hidden</code> 虽然也有效，却可能裁掉阴影、菜单，并影响内部 <code>sticky</code>。现在多列布局应交给 <code>flex</code> 或 <code>grid</code>，浮动更适合文字环绕图片。</p>\\n</section>\\n<p>浮动元素会脱离常规流，父容器算高度时不把它算进去，于是父容器高度塌成 0，后面的内容跑上来重叠 —— 这就是要&quot;清除浮动&quot;的原因。</p>\\n<p><strong>三种做法，按推荐程度排：</strong></p>\\n<p><strong>① <code>display: flow-root</code>（首选）</strong></p>\\n<pre class=\\"language-css\\"><code>.container { display: flow-root; }\\n</code></pre>\\n<p>一行、语义明确（&quot;给我一个独立的格式化上下文&quot;）、<strong>没有任何副作用</strong>。现代浏览器全支持，新代码直接用这个。</p>\\n<p><strong>② 伪元素 clearfix（兼容老浏览器）</strong></p>\\n<pre class=\\"language-css\\"><code>.clearfix::after {\\n  display: block;\\n  clear: both;\\n  content: &#39;&#39;;\\n}\\n</code></pre>\\n<p>不用往 DOM 里塞空标签，结构干净。老资料里那套带 <code>content: &quot;.&quot;</code>、<code>height: 0</code>、<code>visibility: hidden</code> 的写法是为了兼容更古老的浏览器，现在可以精简成上面三行。</p>\\n<p><strong>③ <code>overflow: hidden</code> / <code>auto</code>（触发 BFC）</strong></p>\\n<p>能清浮动，但<strong>副作用明显</strong>：会裁掉超出容器的 <code>box-shadow</code>、下拉菜单、tooltip。而且 <code>overflow: hidden</code> 会让 <code>overflow-y</code> 隐式变成 <code>auto</code>，容器变成滚动容器，内部所有 <code>position: sticky</code> 都会失效 —— 这个坑非常隐蔽。只想裁剪横向溢出的话，用 <code>overflow-x: clip</code> 才不会建立滚动容器。</p>\\n<p><strong>④ 加空 <code>&lt;div style=&quot;clear:both&quot;&gt;</code>（不要用）</strong></p>\\n<p>为了样式往 DOM 里塞无语义的节点，结构和表现混在一起，现在没有任何理由这么写。</p>\\n<p><strong>更重要的一句话</strong>：新写的布局根本不该用浮动。<code>flex</code> 和 <code>grid</code> 出现之前，浮动被拿来做多列布局是不得已；现在浮动的本职工作只剩一个 —— <strong>文字环绕图片</strong>，这个 <code>flex</code> 确实做不到，图文混排的富文本里还在用。老项目里的浮动布局能重构就重构，看得懂是为了改得动。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品详情页的图片区和参数区都用了 <code>float</code>，父容器背景只剩一条线，后面的推荐模块还顶了上来；有人说浮动元素既然“脱离文档流”，给父元素写固定高度就行，你接受吗？</strong></p>\\n<p>不接受，固定高度只是掩盖父容器未计入浮动子元素的问题，内容增减或换行后仍会溢出。应让父容器建立独立格式化上下文，现代浏览器优先使用 <code>display: flow-root</code>；固定高度只适合尺寸确实不变的组件。</p>\\n</li>\\n<li>\\n<p><strong>你接手的活动页有几十个浮动卡片容器，需要在不增加 <code>DOM</code> 节点的前提下修复高度塌陷，同时还要兼容较老的浏览器，你会怎样统一改造？</strong></p>\\n<p>给这些父容器统一添加 <code>clearfix</code> 类，并通过 <code>::after</code> 设置 <code>display: block</code>、<code>clear: both</code> 和空 <code>content</code>。它不需要插入清浮动的空标签，也比逐个写固定高度稳定；代价是项目里要长期保留一个兼容性工具类。</p>\\n</li>\\n<li>\\n<p><strong>一个弹层列表原来用 <code>overflow: hidden</code> 清浮动，改版后卡片阴影、下拉菜单和 <code>tooltip</code> 都被截断，内部吸顶标题也失效了；你会如何修复？</strong></p>\\n<p>把清浮动职责改交给父容器的 <code>display: flow-root</code>，并移除仅为触发 <code>BFC</code> 而设置的 <code>overflow: hidden</code>。后者会裁剪溢出内容，还可能让容器成为滚动容器并影响内部 <code>position: sticky</code>；若只想限制横向溢出，可评估 <code>overflow-x: clip</code>。</p>\\n</li>\\n<li>\\n<p><strong>线上新闻正文只有“图片靠左、文字环绕”这一处仍在用 <code>float</code>，同事要求为了技术统一全部改成 <code>flex</code>，你会支持吗？</strong></p>\\n<p>不应为统一而改掉这里的 <code>float</code>，文字环绕图片正是浮动仍然合适的用途，<code>flex</code> 不能提供相同的逐行环绕效果。多列页面布局应迁移到 <code>flex</code> 或 <code>grid</code>，但富文本图文混排可以保留浮动，并在外层正确处理高度。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_7-link-与-import-的区别\\"><a class=\\"header-anchor\\" href=\\"#_7-link-与-import-的区别\\">#</a> 7 link 与 @import 的区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>link</code> 是 <code>HTML</code> 标签，能做 <code>preload</code>/<code>RSS</code> 等多种事；<code>@import</code> 是 <code>CSS</code> 规则，只能引样式</li>\\n<li>关键差别在加载时机：<code>link</code> 在 <code>HTML</code> 解析阶段被预加载扫描器发现并<strong>并行</strong>下载</li>\\n<li><code>@import</code> 要等外层 <code>CSS</code> 下载并解析到那一行才发请求，等于<strong>串行</strong>多一次往返</li>\\n<li>因为 <code>CSS</code> 是渲染阻塞资源，这次往返会直接推迟首屏绘制</li>\\n<li>⚠️ 项目里常见的 <code>@import</code> 是 <code>Sass</code>/<code>PostCSS</code> 的编译期合并，不产生运行时请求，两回事</li>\\n</ul>\\n<p><strong><code>&lt;link&gt;</code> 是 <code>HTML</code> 标签，能声明样式、<code>preload</code> 等资源关系；<code>@import</code> 是 <code>CSS</code> 规则，只负责引入样式。</strong> 性能差异来自发现时机：<code>&lt;link&gt;</code> 在解析 <code>HTML</code> 时就能被预加载扫描器发现并行下载，<code>@import</code> 则要等外层样式下载、解析后才发起请求。由于 <code>CSS</code> 会阻塞渲染，这次串行请求可能推迟首屏绘制。项目中的 <code>Sass</code> 或 <code>PostCSS</code> <code>@import</code> 通常会在编译期合并，不等同于浏览器原生请求。</p>\\n</section>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th><code>&lt;link&gt;</code></th>\\n<th><code>@import</code></th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>归属</td>\\n<td><code>HTML</code> 标签</td>\\n<td><code>CSS</code> 规则</td>\\n</tr>\\n<tr>\\n<td>功能</td>\\n<td>除样式外还能定义 <code>rel</code>、<code>RSS</code>、<code>preload</code> 等</td>\\n<td>只能引入样式</td>\\n</tr>\\n<tr>\\n<td>加载时机</td>\\n<td><code>HTML</code> 解析阶段就被<strong>预加载扫描器</strong>发现并并行下载</td>\\n<td>要等 <code>CSS</code> 文件下载并解析到那一行才发起请求</td>\\n</tr>\\n<tr>\\n<td>位置要求</td>\\n<td>任意</td>\\n<td>必须在样式表最前面（除 <code>@charset</code>、<code>@layer</code> 外）</td>\\n</tr>\\n<tr>\\n<td><code>JS</code> 动态插入</td>\\n<td>可以</td>\\n<td>不方便</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>核心差别是加载时机，这决定了性能。</strong></p>\\n<p><code>@import</code> 是<strong>串行</strong>的：浏览器要先下载 <code>a.css</code>，解析到 <code>@import \'b.css\'</code> 才知道还要下 <code>b.css</code>，等于两次往返相加。而 <code>&lt;link&gt;</code> 在 <code>HTML</code> 解析阶段就被发现，所有样式表并行下载。</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- ✅ 并行下载 --&gt;\\n&lt;link rel=&quot;stylesheet&quot; href=&quot;a.css&quot;&gt;\\n&lt;link rel=&quot;stylesheet&quot; href=&quot;b.css&quot;&gt;\\n</code></pre>\\n<pre class=\\"language-css\\"><code>/* ❌ 串行：必须等 a.css 到了才知道要 b.css */\\n@import &#39;b.css&#39;;\\n</code></pre>\\n<p>因为 <code>CSS</code> 是<strong>渲染阻塞</strong>资源，这多出来的一次往返会直接推迟首屏绘制，在弱网下尤其明显。</p>\\n<p><strong>一个必须澄清的误解</strong>：现在项目里到处都是 <code>@import</code>，那是<strong>构建工具的 <code>@import</code></strong> —— <code>Sass</code>/<code>Less</code>/<code>PostCSS</code> 在编译期就把文件合并进产物了，运行时根本不产生额外请求，和这题说的浏览器原生 <code>@import</code> 完全是两回事。所以&quot;<code>@import</code> 不能用&quot;这句话要限定在&quot;浏览器直接解析的 <code>CSS</code>&quot;这个前提下。</p>\\n<p><strong>顺带说清 <code>CSS</code> 的阻塞行为</strong>（面试常连着问）：</p>\\n<ul>\\n<li><code>CSS</code> <strong>不阻塞</strong> <code>HTML</code> 解析 —— DOM 树照常构建</li>\\n<li><code>CSS</code> <strong>阻塞渲染</strong> —— 必须等 <code>CSSOM</code> 建好才敢绘制第一帧，否则会闪现无样式内容</li>\\n<li><code>CSS</code> <strong>阻塞后面的 <code>JS</code> 执行</strong> —— 因为脚本可能读取样式（<code>getComputedStyle</code>），必须等样式就绪</li>\\n</ul>\\n<p>所以优化方向是：首屏关键样式内联进 <code>HTML</code>，非关键样式用 <code>media</code> 属性或 <code>preload</code> + <code>onload</code> 异步加载，而不是纠结用哪种引入方式。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>性能评审时有人看到源码里的 <code>@import</code> 就判定样式一定串行加载，但这些语句都写在 <code>Sass</code> 文件中并经过构建；你会怎样纠正这个结论？</strong></p>\\n<p>不能只凭源码中的 <code>@import</code> 判断运行时性能，<code>Sass</code>、<code>Less</code> 或 <code>PostCSS</code> 可能在编译期把依赖合并进最终产物。真正产生额外串行请求的是浏览器直接解析的原生 <code>CSS</code> <code>@import</code>；应先检查构建产物和网络请求瀑布，再决定是否改造。</p>\\n</li>\\n<li>\\n<p><strong>一个无构建工具的专题页在 <code>a.css</code> 顶部写了 <code>@import \'b.css\'</code>，弱网下首屏长期空白；如果两份样式互不依赖，你会怎样调整加载结构？</strong></p>\\n<p>把两份样式都改成 <code>HTML</code> <code>&lt;head&gt;</code> 中的 <code>&lt;link rel=&quot;stylesheet&quot;&gt;</code>，让预加载扫描器在解析 <code>HTML</code> 时尽早发现并并行下载。原生 <code>@import</code> 必须等 <code>a.css</code> 下载并解析到规则后才请求 <code>b.css</code>，额外往返会继续推迟首次绘制。</p>\\n</li>\\n<li>\\n<p><strong>设计团队要求按运行时配置动态启停一套主题样式，同时还想复用同一入口里的 <code>@import</code>；在“可由 <code>JavaScript</code> 控制”和“下载时机”之间你怎么选？</strong></p>\\n<p>需要运行时启停时更适合动态插入或切换 <code>&lt;link&gt;</code>，因为它是 <code>HTML</code> 元素，能直接由 <code>JavaScript</code> 管理。原生 <code>@import</code> 只能引入样式且不便动态操作，并会延后依赖样式的发现；代价是动态加载仍需处理加载完成前的短暂样式状态。</p>\\n</li>\\n<li>\\n<p><strong>线上页面 <code>DOM</code> 很快就生成了，但白屏要等大体积 <code>CSS</code> 返回后才结束，后面的同步脚本也迟迟不执行；有人据此说“<code>CSS</code> 阻塞 <code>HTML</code> 解析”，你如何定位并解释？</strong></p>\\n<p><code>DOM</code> 能持续构建说明 <code>CSS</code> 没有阻塞 <code>HTML</code> 解析，白屏来自样式表阻塞渲染，浏览器要等 <code>CSSOM</code> 就绪后再绘制。后续脚本可能读取计算样式，因此执行也会等待相关 <code>CSS</code>；可结合网络瀑布确认慢样式，并内联关键样式、异步加载非关键部分。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_8-css3的新特性\\"><a class=\\"header-anchor\\" href=\\"#_8-css3的新特性\\">#</a> 8 CSS3的新特性</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>布局：<code>flex</code>、<code>grid</code>、多列，彻底取代浮动做多列排版</li>\\n<li>视觉：圆角、阴影、渐变、<code>transform</code>、<code>transition</code>、<code>animation</code>、<code>filter</code></li>\\n<li>选择器：属性选择器、结构性伪类（<code>:nth-child</code>）、<code>::before</code>/<code>::after</code></li>\\n<li>响应式：媒体查询、<code>vw</code>/<code>vh</code>/<code>rem</code>、<code>calc()</code></li>\\n<li>真正改变工作方式的是 <code>CSS</code> 自定义属性（运行时可改，主题切换靠它）和后来的 <code>@container</code>、<code>:has()</code></li>\\n</ul>\\n<p><strong><code>CSS3</code> 更适合理解为持续演进的一组模块，常用能力可以归到样式表现、布局、交互动画和响应式设计。</strong> 样式表现包括圆角、阴影、渐变和新的颜色写法；布局通常看 <code>Flex</code>、<code>Grid</code>，响应式则依靠媒体查询。交互方面，<code>transition</code> 适合状态触发后的两端过渡，<code>animation</code> 能结合关键帧自行播放更完整的动画。实际开发中应按问题选能力，例如页面排版优先考虑 <code>Flex</code> 或 <code>Grid</code>，不必为了展示新特性而堆效果。</p>\\n</section>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/29fc55febf95ee90.png\\" alt=\\"\\"></p>\\n<p><strong>1. 是什么</strong></p>\\n<p>css，即层叠样式表（Cascading Style Sheets）的简称，是一种标记语言，由浏览器解释执行用来使页面变得更美观</p>\\n<p><code>css3</code>是css的最新标准，是向后兼容的，<code>CSS1/2</code>的特性在 <code>CSS3</code> 里都是可以使用的</p>\\n<p>而 <code>CSS3</code> 也增加了很多新特性，为开发带来了更佳的开发体验</p>\\n<p><strong>2. 选择器</strong></p>\\n<p><code>css3</code>中新增了一些选择器，主要为如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/bfd8984363dc561d.png\\" alt=\\"\\"></p>\\n<p><strong>3. 新样式</strong></p>\\n<ul>\\n<li><strong>边框</strong> <code>css3</code>新增了三个边框属性，分别是：\\n<ul>\\n<li><code>border-radius</code>：创建圆角边框</li>\\n<li><code>box-shadow</code>：为元素添加阴影</li>\\n<li><code>border-image</code>：使用图片来绘制边框</li>\\n</ul>\\n</li>\\n<li><strong>box-shadow</strong> 设置元素阴影，设置属性如下（其中水平阴影和垂直阴影是必须设置的）\\n<ul>\\n<li>水平阴影</li>\\n<li>垂直阴影</li>\\n<li>模糊距离(虚实)</li>\\n<li>阴影尺寸(影子大小)</li>\\n<li>阴影颜色</li>\\n<li>内/外阴影</li>\\n</ul>\\n</li>\\n<li><strong>背景</strong> 新增了几个关于背景的属性，分别是<code>background-clip</code>、<code>background-origin</code>、<code>background-size</code>和<code>background-break</code>\\n<ul>\\n<li><strong><code>background-clip</code></strong> 用于确定背景画区，有以下几种可能的属性：通常情况，背景都是覆盖整个元素的，利用这个属性可以设定背景颜色或图片的覆盖范围\\n<ul>\\n<li><code>background-clip: border-box</code>; 背景从<code>border</code>开始显示</li>\\n<li><code>background-clip: padding-box</code>; 背景从<code>padding</code>开始显示</li>\\n<li><code>background-clip: content-box</code>; 背景显<code>content</code>区域开始显示</li>\\n<li><code>background-clip: no-clip</code>; 默认属性，等同于b<code>order-box</code></li>\\n</ul>\\n</li>\\n<li><strong><code>background-origin</code></strong> 当我们设置背景图片时，图片是会以左上角对齐，但是是以<code>border</code>的左上角对齐还是以<code>padding</code>的左上角或者<code>content</code>的左上角对齐? <code>border-origin</code>正是用来设置这个的\\n<ul>\\n<li><code>background-origin: border-box</code>; 从<code>border</code>开始计算<code>background-position</code></li>\\n<li><code>background-origin: padding-box</code>; 从<code>padding</code>开始计算<code>background-position</code></li>\\n<li><code>background-origin: content-box</code>; 从<code>content</code>开始计算<code>background-position</code></li>\\n<li>默认情况是<code>padding-box</code>，即以<code>padding</code>的左上角为原点</li>\\n</ul>\\n</li>\\n<li><strong><code>background-size</code></strong> 常用来调整背景图片的大小，主要用于设定图片本身。有以下可能的属性：\\n<ul>\\n<li><code>background-size: contain</code>; 缩小图片以适合元素（维持像素长宽比）</li>\\n<li><code>background-size: cover</code>; 扩展元素以填补元素（维持像素长宽比）</li>\\n<li><code>background-size: 100px 100px</code>; 缩小图片至指定的大小</li>\\n<li><code>background-size: 50% 100%</code>; 缩小图片至指定的大小，百分比是相对包 含元素的尺寸</li>\\n</ul>\\n</li>\\n<li><strong><code>background-break</code></strong> 元素可以被分成几个独立的盒子（如使内联元素<code>span</code>跨越多行），<code>background-break</code> 属性用来控制背景怎样在这些不同的盒子中显示\\n<ul>\\n<li><code>background-break: continuous</code>; 默认值。忽略盒之间的距离（也就是像元素没有分成多个盒子，依然是一个整体一样）</li>\\n<li><code>background-break: bounding-box</code>; 把盒之间的距离计算在内；</li>\\n<li><code>background-break: each-box</code>; 为每个盒子单独重绘背景</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li><strong>文字</strong>\\n<ul>\\n<li><strong><code>word-wrap: normal|break-word</code></strong>\\n<ul>\\n<li><code>normal</code>：使用浏览器默认的换行</li>\\n<li><code>break-all</code>：允许在单词内换行</li>\\n</ul>\\n</li>\\n<li><strong><code>text-overflow</code></strong> 设置或检索当当前行超过指定容器的边界时如何显示，属性有两个值选择\\n<ul>\\n<li><code>clip</code>：修剪文本</li>\\n<li><code>ellipsis</code>：显示省略符号来代表被修剪的文本</li>\\n</ul>\\n</li>\\n<li><strong><code>text-shadow</code></strong> 可向文本应用阴影。能够规定水平阴影、垂直阴影、模糊距离，以及阴影的颜色</li>\\n<li><strong><code>text-decoration</code></strong> CSS3里面开始支持对文字的更深层次的渲染，具体有三个属性可供设置：\\n<ul>\\n<li><code>text-fill-color</code>: 设置文字内部填充颜色</li>\\n<li><code>text-stroke-color</code>: 设置文字边界填充颜色</li>\\n<li><code>text-stroke-width</code>: 设置文字边界宽度</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li><strong>颜色</strong>\\n<ul>\\n<li><code>css3</code>新增了新的颜色表示方式<code>rgba</code>与<code>hsla</code></li>\\n<li><code>rgba</code>分为两部分，<code>rgb</code>为颜色值，<code>a</code>为透明度</li>\\n<li><code>hala</code>分为四部分，<code>h</code>为色相，<code>s</code>为饱和度，<code>l</code>为亮度，<code>a</code>为透明度</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>4. transition 过渡</strong></p>\\n<p><code>transition</code>属性可以被指定为一个或多个CSS属性的过渡效果，多个属性之间用逗号进行分隔，必须规定两项内容：</p>\\n<ul>\\n<li>过度效果</li>\\n<li>持续时间</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>transition： CSS属性，花费时间，效果曲线(默认ease)，延迟时间(默认0)\\n</code></pre>\\n<p>上面为简写模式，也可以分开写各个属性</p>\\n<pre class=\\"language-css\\"><code>transition-property: width; \\ntransition-duration: 1s;\\ntransition-timing-function: linear;\\ntransition-delay: 2s;\\n</code></pre>\\n<p><strong>5. transform 转换</strong></p>\\n<ul>\\n<li><code>transform</code>属性允许你旋转，缩放，倾斜或平移给定元素</li>\\n<li><code>transform-origin</code>：转换元素的位置（围绕那个点进行转换），默认值为<code>(x,y,z):(50%,50%,0)</code></li>\\n</ul>\\n<p>使用方式：</p>\\n<ul>\\n<li><code>transform: translate(120px, 50%)</code>：位移</li>\\n<li><code>transform: scale(2, 0.5)</code>：缩放</li>\\n<li><code>transform: rotate(0.5turn)</code>：旋转</li>\\n<li><code>transform: skew(30deg, 20deg)</code>：倾斜</li>\\n</ul>\\n<p><strong>6. animation 动画</strong></p>\\n<p>动画这个平常用的也很多，主要是做一个预设的动画。和一些页面交互的动画效果，结果和过渡应该一样，让页面不会那么生硬</p>\\n<p><code>animation</code>也有很多的属性</p>\\n<ul>\\n<li><code>animation-name</code>：动画名称</li>\\n<li><code>animation-duration</code>：动画持续时间</li>\\n<li><code>animation-timing-function</code>：动画时间函数</li>\\n<li><code>animation-delay</code>：动画延迟时间</li>\\n<li><code>animation-iteration-count</code>：动画执行次数，可以设置为一个整数，也可以设置为infinite，意思是无限循环</li>\\n<li><code>animation-direction</code>：动画执行方向</li>\\n<li><code>animation-paly-state</code>：动画播放状态</li>\\n<li><code>animation-fill-mode</code>：动画填充模式</li>\\n</ul>\\n<p><strong>7. 渐变</strong></p>\\n<p>颜色渐变是指在两个颜色之间平稳的过渡，<code>css3</code>渐变包括</p>\\n<ul>\\n<li><code>linear-gradient</code>：线性渐变 <code>background-image: linear-gradient(direction, color-stop1, color-stop2, ...)</code>;</li>\\n<li><code>radial-gradient</code>：径向渐变 <code>linear-gradient(0deg, red, green)</code></li>\\n</ul>\\n<p><strong>8. 其他</strong></p>\\n<ul>\\n<li><code>Flex</code>弹性布局</li>\\n<li><code>Grid</code>栅格布局</li>\\n<li>媒体查询 <code>@media screen and (max-width: 960px) {}</code>还有打印<code>print</code></li>\\n</ul>\\n<p><strong>transition和animation的区别</strong></p>\\n<blockquote>\\n<p><code>Animation</code>和<code>transition</code>大部分属性是相同的，他们都是随时间改变元素的属性值，他们的主要区别是<code>transition</code>需要触发一个事件才能改变属性，而<code>animation</code>不需要触发任何事件的情况下才会随时间改变属性值，并且<code>transition</code>为2帧，从<code>from .... to</code>，而<code>animation</code>可以一帧一帧的</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个旧后台把所有 <code>CSS3</code> 能力概括成“圆角和阴影”，但页面同时用了 <code>flex</code>、媒体查询和关键帧动画；作为面试官，你会要求候选人怎样纠正这种归类？</strong></p>\\n<p>应把 <code>CSS3</code> 能力按用途拆开，而不是只列视觉装饰：选择器与颜色表示、边框和背景、<code>transition</code>、<code>transform</code>、<code>animation</code>、渐变，以及 <code>flex</code>、<code>grid</code> 和媒体查询都属于重要扩展。只背 <code>border-radius</code> 和 <code>box-shadow</code> 无法说明对布局与交互能力的理解。</p>\\n</li>\\n<li>\\n<p><strong>品牌页的横幅背景图必须铺满容器，允许裁掉边缘但不能拉伸变形；移动端与桌面端尺寸差异很大，你会给 <code>background-size</code> 选什么值？</strong></p>\\n<p>应使用 <code>background-size: cover</code>，它会保持图片宽高比并扩展到覆盖整个元素，因此在比例不一致时可能裁掉部分边缘。若产品要求完整展示图片，应改用 <code>contain</code>，但容器中可能出现未被图片填满的区域，两者不能同时保证。</p>\\n</li>\\n<li>\\n<p><strong>按钮悬停只需从当前颜色平滑变化到目标颜色，而加载图标要在没有用户操作时持续旋转；同事想全部用 <code>animation</code>，你如何取舍？</strong></p>\\n<p>按钮的两态变化用 <code>transition</code> 更贴合，因为它依赖悬停等状态触发，并在起止属性值之间过渡。加载图标应使用 <code>animation</code> 配合 <code>@keyframes</code> 和 <code>infinite</code> 自主循环；统一使用动画虽能实现，却会增加关键帧和状态维护成本。</p>\\n</li>\\n<li>\\n<p><strong>线上卡片旋转后明显绕着左上附近运动，而设计稿要求围绕中心旋转；代码只有 <code>transform: rotate(0.5turn)</code>，你会先检查什么？</strong></p>\\n<p>先检查元素或公共样式是否改写了 <code>transform-origin</code>，旋转、缩放等转换都会围绕该原点进行。默认原点是元素中心附近的 <code>50% 50% 0</code>，若被设到边角就会产生偏心轨迹；修正原点时还要确认没有被更高优先级规则覆盖。</p>\\n</li>\\n<li>\\n<p><strong>数据看板要在宽度不超过 <code>960px</code> 时切换布局，打印时又必须去掉交互区域；有人提出用 <code>JavaScript</code> 监听窗口宽度并逐个改样式，你会怎么评估？</strong></p>\\n<p>这类展示条件优先交给媒体查询，用屏幕宽度条件处理窄屏布局，并用 <code>print</code> 媒体规则维护打印样式。<code>JavaScript</code> 适合参与业务状态，而纯视口和媒介差异由 <code>CSS</code> 表达更直接；若布局变化还依赖数据状态，再考虑脚本协同。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_9-css动画和过渡\\"><a class=\\"header-anchor\\" href=\\"#_9-css动画和过渡\\">#</a> 9 CSS动画和过渡</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>transition</code> 是&quot;状态 A 到状态 B&quot;的补间，需要有触发（<code>hover</code>、加类名）；<code>animation</code> 靠 <code>@keyframes</code> 定义多帧，可自动播放、循环、暂停</li>\\n<li>只有 <code>transform</code> 和 <code>opacity</code> 能在合成线程上跑，不占主线程；动 <code>left</code>/<code>top</code>/<code>width</code> 会触发重排</li>\\n<li><code>will-change</code> 可以提前提层，但用完要撤 —— 长期挂着会一直占显存</li>\\n<li><code>transition</code> 对 <code>display: none</code> 无效，做淡入淡出要配合 <code>visibility</code> 或新的 <code>@starting-style</code>/<code>transition-behavior: allow-discrete</code></li>\\n<li>必须响应 <code>@media (prefers-reduced-motion: reduce)</code>，这是无障碍硬要求</li>\\n</ul>\\n<p><strong><code>transition</code> 适合处理两个状态之间的渐变，<code>animation</code> 则通过 <code>@keyframes</code> 描述多帧动画。</strong> 前者需要悬停或类名变化等触发条件，后者可以自动播放、循环，并控制方向和暂停状态。<code>transform</code> 负责平移、缩放、旋转和倾斜，本身不是动画，通常配合前两者使用。需要注意，传统方式下 <code>display: none</code> 和 <code>display: block</code> 不能直接过渡。</p>\\n</section>\\n<p>常见的动画效果有很多，如<code>平移</code>、<code>旋转</code>、<code>缩放</code>等等，复杂动画则是多个简单动画的组合</p>\\n<p><strong>css实现动画的方式，有如下几种：</strong></p>\\n<ul>\\n<li><code>transition</code> 实现渐变动画</li>\\n<li><code>transform</code> 转变动画</li>\\n<li><code>animation</code> 实现自定义动画</li>\\n</ul>\\n<h3 id=\\"transition-实现渐变动画\\"><a class=\\"header-anchor\\" href=\\"#transition-实现渐变动画\\">#</a> transition 实现渐变动画</h3>\\n<p><strong>transition的属性如下：</strong></p>\\n<ul>\\n<li><code>transition-property:填写需要变化的css属性</code></li>\\n<li><code>transition-duration:完成过渡效果需要的时间单位(s或者ms)默认是 0</code></li>\\n<li><code>transition-timing-function:完成效果的速度曲线</code></li>\\n<li><code>transition-delay: （规定过渡效果何时开始。默认是</code>0<code>）</code></li>\\n</ul>\\n<blockquote>\\n<p>一般情况下，我们都是写一起的，比如：<code>transition： width 2s ease 1s</code></p>\\n</blockquote>\\n<p>其中<code>timing-function</code>的值有如下：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>值</th>\\n<th>描述</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>linear</code></td>\\n<td>匀速（等于 <code>cubic-bezier(0,0,1,1)</code>）</td>\\n</tr>\\n<tr>\\n<td><code>ease</code></td>\\n<td>从慢到快再到慢（<code>cubic-bezier(0.25,0.1,0.25,1)</code>）</td>\\n</tr>\\n<tr>\\n<td><code>ease-in</code></td>\\n<td>慢慢变快（等于 <code>cubic-bezier(0.42,0,1,1)</code>）</td>\\n</tr>\\n<tr>\\n<td><code>ease-out</code></td>\\n<td>慢慢变慢（等于 <code>cubic-bezier(0,0,0.58,1)</code>）</td>\\n</tr>\\n<tr>\\n<td><code>ease-in-out</code></td>\\n<td>先变快再到慢（等于 cubic-bezier(0.42,0,0.58,1)`），渐显渐隐效果</td>\\n</tr>\\n<tr>\\n<td><code>cubic-bezier(*n*,*n*,*n*,*n*)</code></td>\\n<td>在 <code>cubic-bezier</code> 函数中定义自己的值。可能的值是 <code>0</code> 至 <code>1</code> 之间的数值</td>\\n</tr>\\n</tbody>\\n</table>\\n<p>注意：并不是所有的属性都能使用过渡的，如<code>display:none&lt;-&gt;display:block</code></p>\\n<p>举个例子，实现鼠标移动上去发生变化动画效果</p>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n  .base {\\n    width: 100px;\\n    height: 100px;\\n    display: inline-block;\\n    background-color: #0EA9FF;\\n    border-width: 5px;\\n    border-style: solid;\\n    border-color: #5daf34;\\n    transition-property: width, height, background-color, border-width;\\n    transition-duration: 2s;\\n    transition-timing-function: ease-in;\\n    transition-delay: 500ms;\\n  }\\n\\n  /*简写*/\\n  /*transition: all 2s ease-in 500ms;*/\\n  .base:hover {\\n    width: 200px;\\n    height: 200px;\\n    background-color: #5daf34;\\n    border-width: 10px;\\n    border-color: #3a8ee6;\\n  }\\n&lt;/style&gt;\\n&lt;div class=&quot;base&quot;&gt;&lt;/div&gt;\\n</code></pre>\\n<h3 id=\\"transform-转变动画\\"><a class=\\"header-anchor\\" href=\\"#transform-转变动画\\">#</a> transform 转变动画</h3>\\n<p>包含四个常用的功能：</p>\\n<ul>\\n<li><code>translate(x,y)</code>：位移</li>\\n<li><code>scale</code>：缩放</li>\\n<li><code>rotate</code>：旋转</li>\\n<li><code>skew</code>：倾斜</li>\\n</ul>\\n<p>一般配合<code>transition</code>过度使用</p>\\n<blockquote>\\n<p>注意的是，<code>transform</code>不支持<code>inline元</code>素，使用前把它变成<code>block</code></p>\\n</blockquote>\\n<p>举个例子</p>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n.base {\\n  width: 100px;\\n  height: 100px;\\n  display: inline-block;\\n  background-color: #0EA9FF;\\n  border-width: 5px;\\n  border-style: solid;\\n  border-color: #5daf34;\\n  transition-property: width, height, background-color, border-width;\\n  transition-duration: 2s;\\n  transition-timing-function: ease-in;\\n  transition-delay: 500ms;\\n}\\n.base2 {\\n  transform: none;\\n  transition-property: transform;\\n  transition-delay: 5ms;\\n}\\n.base2:hover {\\n  transform: scale(0.8, 1.5) rotate(35deg) skew(5deg) translate(15px, 25px);\\n}\\n&lt;/style&gt;\\n&lt;div class=&quot;base base2&quot;&gt;&lt;/div&gt;\\n</code></pre>\\n<p>可以看到盒子发生了旋转，倾斜，平移，放大</p>\\n<h3 id=\\"animation-实现自定义动画\\"><a class=\\"header-anchor\\" href=\\"#animation-实现自定义动画\\">#</a> animation 实现自定义动画</h3>\\n<blockquote>\\n<p>一个关键帧动画，最少包含两部分，<code>animation</code> 属性及属性值（动画的名称和运行方式运行时间等）<code>@keyframes</code>（规定动画的具体实现过程）</p>\\n</blockquote>\\n<p><code>animation</code>是由 8 个属性的简写，分别如下：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>属性</th>\\n<th>描述</th>\\n<th>属性值</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>animation-duration</code></td>\\n<td>指定动画完成一个周期所需要时间，单位秒（<code>s</code>）或毫秒（<code>ms</code>），默认是 <code>0</code></td>\\n<td></td>\\n</tr>\\n<tr>\\n<td><code>animation-timing-function</code></td>\\n<td>指定动画计时函数，即动画的速度曲线，默认是 &quot;<code>ease</code>&quot;</td>\\n<td><code>linear</code>、<code>ease</code>、<code>ease-in</code>、<code>ease-out</code>、<code>ease-in-out</code></td>\\n</tr>\\n<tr>\\n<td><code>animation-delay</code></td>\\n<td>指定动画延迟时间，即动画何时开始，默认是 <code>0</code></td>\\n<td></td>\\n</tr>\\n<tr>\\n<td><code>animation-iteration-count</code></td>\\n<td>指定动画播放的次数，默认是 <code>1</code>。但我们一般用<code>infinite</code>，一直播放</td>\\n<td></td>\\n</tr>\\n<tr>\\n<td><code>animation-direction</code> 指定动画播放的方向</td>\\n<td>默认是 <code>normal</code></td>\\n<td><code>normal</code>、<code>reverse</code>、<code>alternate</code>、<code>alternate-reverse</code></td>\\n</tr>\\n<tr>\\n<td><code>animation-fill-mode</code></td>\\n<td>指定动画填充模式。默认是 <code>none</code></td>\\n<td><code>forwards</code>、<code>backwards</code>、<code>both</code></td>\\n</tr>\\n<tr>\\n<td><code>animation-play-state</code></td>\\n<td>指定动画播放状态，正在运行或暂停。默认是 <code>running</code></td>\\n<td><code>running</code>、<code>pauser</code></td>\\n</tr>\\n<tr>\\n<td><code>animation-name</code></td>\\n<td>指定 <code>@keyframes</code> 动画的名称</td>\\n<td></td>\\n</tr>\\n</tbody>\\n</table>\\n<p><code>CSS</code> 动画只需要定义一些关键的帧，而其余的帧，浏览器会根据计时函数插值计算出来，</p>\\n<blockquote>\\n<p><code>@keyframes</code>定义关键帧，可以是<code>from-&gt;to</code>（等同于<code>0%</code>和<code>100%</code>），也可以是从<code>0%-&gt;100%</code>之间任意个的分层设置</p>\\n</blockquote>\\n<p>因此，如果我们想要让元素旋转一圈，只需要定义开始和结束两帧即可：</p>\\n<pre class=\\"language-css\\"><code>@keyframes rotate{\\n  from {\\n    transform: rotate(0deg);\\n  }\\n  to {\\n    transform: rotate(360deg);\\n  }\\n}\\n</code></pre>\\n<blockquote>\\n<p><code>from</code> 表示最开始的那一帧，<code>to</code> 表示结束时的那一帧</p>\\n</blockquote>\\n<p><strong>也可以使用百分比刻画生命周期</strong></p>\\n<pre class=\\"language-css\\"><code>@keyframes rotate{\\n  0%{\\n    transform: rotate(0deg);\\n  }\\n  50%{\\n    transform: rotate(180deg);\\n  }\\n  100%{\\n    transform: rotate(360deg);\\n  }\\n}\\n</code></pre>\\n<p>定义好了关键帧后，下来就可以直接用它了：</p>\\n<pre class=\\"language-css\\"><code>animation: rotate 2s;\\n</code></pre>\\n<p><strong>总结</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>属性</th>\\n<th>含义</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>transition（过度）</code></td>\\n<td>用于设置元素的样式过度，和<code>animation</code>有着类似的效果，但细节上有很大的不同</td>\\n</tr>\\n<tr>\\n<td><code>transform（变形）</code></td>\\n<td>用于元素进行旋转、缩放、移动或倾斜，和设置样式的动画并没有什么关系，就相当于color一样用来设置元素的“外表”</td>\\n</tr>\\n<tr>\\n<td><code>translate（移动）</code></td>\\n<td>只是<code>transform</code>的一个属性值，即移动</td>\\n</tr>\\n<tr>\\n<td><code>animation（动画）</code></td>\\n<td>用于设置动画属性，他是一个简写的属性，包含<code>6</code>个属性</td>\\n</tr>\\n</tbody>\\n</table>\\n<h3 id=\\"用css3动画使一个图片旋转\\"><a class=\\"header-anchor\\" href=\\"#用css3动画使一个图片旋转\\">#</a> 用css3动画使一个图片旋转</h3>\\n<pre class=\\"language-css\\"><code>#loader {\\n  display: block;\\n  position: relative;\\n  -webkit-animation: spin 2s linear infinite;\\n  animation: spin 2s linear infinite;\\n}\\n\\n@-webkit-keyframes spin {\\n  0%   {\\n    -webkit-transform: rotate(0deg);\\n    -ms-transform: rotate(0deg);\\n    transform: rotate(0deg);\\n  }\\n  100% {\\n    -webkit-transform: rotate(360deg);\\n    -ms-transform: rotate(360deg);\\n    transform: rotate(360deg);\\n  }\\n}\\n\\n@keyframes spin {\\n  0%   {\\n    -webkit-transform: rotate(0deg);\\n    -ms-transform: rotate(0deg);\\n    transform: rotate(0deg);\\n  }\\n  100% {\\n    -webkit-transform: rotate(360deg);\\n    -ms-transform: rotate(360deg);\\n    transform: rotate(360deg);\\n  }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>加载页的图片只写了 <code>transform: rotate(360deg)</code>，刷新后始终停在原位，同事说浏览器不支持旋转；你会指出代码缺了什么？</strong></p>\\n<p>单独设置 <code>transform</code> 只会得到一个静态的最终状态，不会自动产生随时间变化的旋转。应定义从 <code>rotate(0deg)</code> 到 <code>rotate(360deg)</code> 的 <code>@keyframes</code>，再通过 <code>animation</code> 指定名称和持续时间；是否循环还要单独配置。</p>\\n</li>\\n<li>\\n<p><strong>一个上传进度图标只转一圈就停了，代码是 <code>animation: spin 2s linear</code>，产品要求在上传结束前持续旋转；你会怎么改？</strong></p>\\n<p>在动画简写中加入 <code>infinite</code>，写成类似 <code>animation: spin 2s linear infinite</code>，让 <code>spin</code> 关键帧持续重复。上传结束时仍需由页面状态移除或替换该动画，否则图标会无限运行，<code>CSS</code> 本身不知道业务何时完成。</p>\\n</li>\\n<li>\\n<p><strong>同一张加载图片每两秒旋转一圈，但每轮开始时速度快、中间慢，视觉上像在顿挫；代码没有显式设置时间函数，你会调整哪一项？</strong></p>\\n<p>把时间函数明确设为 <code>linear</code>，使从 <code>0deg</code> 到 <code>360deg</code> 的角度按恒定速率变化。默认缓动会在周期内改变速度，循环边界处更容易显得不连续；若设计本来需要加减速，则不应机械改成匀速。</p>\\n</li>\\n<li>\\n<p><strong>线上样式里已经定义了 <code>@keyframes spin</code>，图片却完全不动；元素规则是 <code>animation: rotate 2s linear infinite</code>，你会如何排查？</strong></p>\\n<p>先核对动画名称，元素声明使用的是 <code>rotate</code>，而已定义的关键帧叫 <code>spin</code>，两者不一致就无法关联。将 <code>animation-name</code> 改为 <code>spin</code>，或统一重命名关键帧；随后再检查持续时间是否有效以及规则是否被覆盖。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_10-有哪些方式-css-可以隐藏页面元素\\"><a class=\\"header-anchor\\" href=\\"#_10-有哪些方式-css-可以隐藏页面元素\\">#</a> 10 有哪些方式（CSS）可以隐藏页面元素</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>选型看三件事：<strong>占不占空间、能不能交互、读屏器读不读得到</strong></li>\\n<li><code>display: none</code> 三样全无，开销最小；<code>visibility: hidden</code> 占位不可交互、读屏器也读不到</li>\\n<li>⚠️ <code>opacity: 0</code> 的元素<strong>仍可点击、仍可 <code>Tab</code> 聚焦</strong>，要配 <code>pointer-events: none</code></li>\\n<li>想&quot;视觉隐藏但读屏器能读&quot;必须用 <code>.sr-only</code> 那套 <code>clip-path</code> 写法，不能用前两种</li>\\n<li><code>display</code> 不能直接过渡，做展开动画传统靠 <code>max-height</code>，现在可用 <code>transition-behavior: allow-discrete</code></li>\\n</ul>\\n<p><strong>隐藏元素常用 <code>display: none</code>、<code>visibility: hidden</code> 和 <code>opacity: 0</code>，区别主要在是否占位、能否交互以及读屏器是否可见。</strong> <code>display: none</code> 不占空间也不能交互，<code>visibility: hidden</code> 保留布局位置但不可操作。<code>opacity: 0</code> 只是视觉透明，元素仍可能被点击、被 <code>Tab</code> 聚焦，也仍能被读屏器读取。需要保留无障碍文本时，我会使用 <code>.sr-only</code> 这类视觉隐藏方案。</p>\\n</section>\\n<p>隐藏元素的方式很多，<strong>关键差异在三件事：占不占空间、能不能交互、读屏器读不读得到</strong>。选错会直接导致无障碍缺陷。</p>\\n<table>\\n<thead>\\n<tr>\\n<th>方式</th>\\n<th style=\\"text-align:center\\">占空间</th>\\n<th style=\\"text-align:center\\">可交互</th>\\n<th style=\\"text-align:center\\">读屏器可见</th>\\n<th style=\\"text-align:center\\">可过渡动画</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>display: none</code></td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">❌</td>\\n</tr>\\n<tr>\\n<td><code>visibility: hidden</code></td>\\n<td style=\\"text-align:center\\">✅</td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">✅（离散）</td>\\n</tr>\\n<tr>\\n<td><code>opacity: 0</code></td>\\n<td style=\\"text-align:center\\">✅</td>\\n<td style=\\"text-align:center\\">✅ ⚠️</td>\\n<td style=\\"text-align:center\\">✅ ⚠️</td>\\n<td style=\\"text-align:center\\">✅</td>\\n</tr>\\n<tr>\\n<td><code>transform: scale(0)</code></td>\\n<td style=\\"text-align:center\\">✅</td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">✅ ⚠️</td>\\n<td style=\\"text-align:center\\">✅</td>\\n</tr>\\n<tr>\\n<td><code>z-index: -9999</code></td>\\n<td style=\\"text-align:center\\">✅</td>\\n<td style=\\"text-align:center\\">取决于遮挡</td>\\n<td style=\\"text-align:center\\">✅</td>\\n<td style=\\"text-align:center\\">—</td>\\n</tr>\\n<tr>\\n<td><code>clip-path: inset(100%)</code></td>\\n<td style=\\"text-align:center\\">✅</td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">✅ ⚠️</td>\\n<td style=\\"text-align:center\\">✅</td>\\n</tr>\\n<tr>\\n<td><code>hidden</code> 属性</td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">❌</td>\\n</tr>\\n<tr>\\n<td><code>.sr-only</code> 视觉隐藏</td>\\n<td style=\\"text-align:center\\">几乎不占</td>\\n<td style=\\"text-align:center\\">✅</td>\\n<td style=\\"text-align:center\\">✅</td>\\n<td style=\\"text-align:center\\">—</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>两个最容易出事的点：</strong></p>\\n<p><strong>① <code>opacity: 0</code> 的元素还能被点到、还能被 <code>Tab</code> 到。</strong> 一个&quot;隐藏&quot;的按钮盖在正常内容上，用户点不到真正的按钮却触发了隐形按钮 —— 这是线上很常见的诡异 bug。要么加 <code>pointer-events: none</code>，要么改用 <code>visibility: hidden</code>。</p>\\n<p><strong>② <code>display: none</code> 和 <code>visibility: hidden</code> 对读屏器都是不可见的。</strong> 想做&quot;视觉上隐藏但读屏器能读&quot;（比如图标按钮的文字说明），要用专门的视觉隐藏写法：</p>\\n<pre class=\\"language-css\\"><code>.sr-only {\\n  position: absolute;\\n  width: 1px; height: 1px;\\n  padding: 0; margin: -1px;\\n  overflow: hidden;\\n  clip-path: inset(50%);\\n  white-space: nowrap;\\n  border: 0;\\n}\\n</code></pre>\\n<p><strong>性能上的差异也值得说</strong>：<code>display: none</code> 的元素完全不参与布局和绘制，隐藏大块内容时开销最小；<code>visibility: hidden</code> 仍然参与布局（占位置）只是不画；<code>opacity: 0</code> 不但参与布局还会参与合成，最贵。</p>\\n<p><strong>做展开/收起动画时的经典难题</strong>：<code>display</code> 不能过渡，所以传统做法是&quot;用 <code>max-height</code> 骗一个动画&quot;或者用 <code>JS</code> 延迟切换 <code>display</code>。现在有了 <code>transition-behavior: allow-discrete</code> 和 <code>@starting-style</code>，可以直接对 <code>display</code> 做过渡了，不过还要考虑兼容性。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>结算页有个透明优惠券按钮覆盖在“提交订单”上方，肉眼看不到，却能被鼠标点击和键盘 <code>Tab</code> 聚焦；代码用了 <code>opacity: 0</code>，你会怎样修？</strong></p>\\n<p>不能把 <code>opacity: 0</code> 当成完整的隐藏方案，它仍占据布局、可接收点击，也可能继续进入键盘焦点序列。临时过渡可配合 <code>pointer-events: none</code> 并管理焦点；彻底隐藏则改用 <code>visibility: hidden</code> 或 <code>display: none</code>。</p>\\n</li>\\n<li>\\n<p><strong>导航栏的图标按钮需要给读屏器提供“搜索”文本，但视觉稿不允许显示文字；同事把文字写进 <code>span</code> 后加了 <code>display: none</code>，你接受吗？</strong></p>\\n<p>不接受，<code>display: none</code> 会同时把文字从布局、交互和无障碍树中隐藏，读屏器通常无法获得这个名称。应使用专门的 <code>.sr-only</code> 视觉隐藏样式，将文字压缩并裁剪到视觉不可见；还要避免它意外覆盖可点击区域。</p>\\n</li>\\n<li>\\n<p><strong>一个有上千行内容的后台面板关闭后无需保留占位，也不需要退出动画；候选人在 <code>visibility: hidden</code>、<code>opacity: 0</code> 和 <code>display: none</code> 之间选了前者，你会追问什么取舍？</strong></p>\\n<p>这里更适合 <code>display: none</code>，因为面板关闭后不占空间，也不参与布局和绘制，符合大块内容彻底移除的目标。<code>visibility: hidden</code> 仍保留布局位置，<code>opacity: 0</code> 还会参与布局和合成；若后来要求过渡动画，方案需要重新设计。</p>\\n</li>\\n<li>\\n<p><strong>线上弹窗淡出后虽然看不见，底下的输入框仍点不到；样式从 <code>opacity: 1</code> 过渡到 <code>opacity: 0</code>，你会在开发者工具里重点检查什么？</strong></p>\\n<p>先用元素选取工具确认点击位置实际命中的节点，并检查淡出层的尺寸、层叠顺序和 <code>pointer-events</code>。<code>opacity: 0</code> 不会移除命中区域，动画结束后应禁用指针事件或切换为真正隐藏状态；仅降低 <code>z-index</code> 仍可能留下焦点问题。</p>\\n</li>\\n<li>\\n<p><strong>折叠面板要求保留平滑收起效果，但现有实现直接从 <code>display: block</code> 切到 <code>display: none</code>，动画完全没有执行；你会怎样解释并选方案？</strong></p>\\n<p>传统情况下 <code>display</code> 不能像连续数值那样直接过渡，因此状态切换会立即完成。可用 <code>max-height</code> 等属性模拟，或由 <code>JavaScript</code> 在动画结束后再切换 <code>display</code>；较新的离散过渡能力也可评估，但必须把目标浏览器兼容性作为采用边界。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_11-说说em-px-rem-vh-vw区别\\"><a class=\\"header-anchor\\" href=\\"#_11-说说em-px-rem-vh-vw区别\\">#</a> 11 说说em/px/rem/vh/vw区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>px</code> 绝对单位；<code>em</code> 相对<strong>父元素</strong>字号（用于 <code>font-size</code> 时）会层层累乘，容易失控</li>\\n<li><code>rem</code> 相对<strong>根元素</strong>字号，只有一个参照点，是移动端适配和整体缩放的主力</li>\\n<li><code>vw</code>/<code>vh</code> 相对视口，<code>1vw</code> = 视口宽的 1%；移动端慎用 <code>vh</code>，地址栏会让它跳变，改用 <code>dvh</code></li>\\n<li>用户在浏览器里调大默认字号时，<code>px</code> 不跟随而 <code>rem</code> 会 —— 所以字号该用 <code>rem</code>，边框该用 <code>px</code></li>\\n<li>现在做流体排版更推荐 <code>clamp(最小值, 首选值, 最大值)</code>，一行替代一堆媒体查询</li>\\n</ul>\\n<p><strong><code>px</code> 是绝对长度单位，<code>em</code> 和 <code>rem</code> 跟随字号，<code>vw</code>、<code>vh</code> 则按视口尺寸计算。</strong> <code>em</code> 的基准会受当前元素或父级字号影响，因此嵌套后同一个数值未必一样；<code>rem</code> 只参考根元素 <code>html</code> 的字号，更方便统一缩放。<code>1vw</code> 等于视口宽度的 <code>1%</code>，<code>1vh</code> 等于视口高度的 <code>1%</code>，适合响应式布局。实际使用时，精确尺寸可用 <code>px</code>，整体适配更常用 <code>rem</code> 或视口单位。</p>\\n</section>\\n<p>传统的项目开发中，我们只会用到<code>px</code>、<code>%</code>、<code>em</code>这几个单位，它可以适用于大部分的项目开发，且拥有比较良好的兼容性</p>\\n<p>从CSS3开始，浏览器对计量单位的支持又提升到了另外一个境界，新增了<code>rem</code>、<code>vh</code>、<code>vw</code>、<code>vm</code>等一些新的计量单位</p>\\n<p>利用这些新的单位开发出比较良好的响应式页面，适应多种不同分辨率的终端，包括移动设备等</p>\\n<p>在<code>css</code>单位中，可以分为长度单位、绝对单位，如下表所指示</p>\\n<table>\\n<thead>\\n<tr>\\n<th>CSS单位</th>\\n<th></th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>相对长度单位</td>\\n<td>em、ex、ch、rem、vw、vh、vmin、vmax、%</td>\\n</tr>\\n<tr>\\n<td>绝对长度单位</td>\\n<td>cm、mm、in、px、pt、pc</td>\\n</tr>\\n</tbody>\\n</table>\\n<p>这里我们主要讲述<code>px</code>、<code>em</code>、<code>rem</code>、<code>vh</code>、<code>vw</code></p>\\n<p><strong>px</strong></p>\\n<p><code>px</code>，表示像素，所谓像素就是呈现在我们显示器上的一个个小点，每个像素点都是大小等同的，所以像素为计量单位被分在了绝对长度单位中</p>\\n<p>有些人会把<code>px</code>认为是相对长度，原因在于在移动端中存在设备像素比，<code>px</code>实际显示的大小是不确定的</p>\\n<p>这里之所以认为<code>px</code>为绝对单位，在于<code>px</code>的大小和元素的其他属性无关</p>\\n<p><strong>em</strong></p>\\n<p>em是相对长度单位。相对于当前对象内文本的字体尺寸。如当前对行内文本的字体尺寸未被人为设置，则相对于浏览器的默认字体尺寸（<code>1em = 16px</code>）</p>\\n<p>为了简化 <code>font-size</code> 的换算，我们需要在<code>css</code>中的 <code>body</code> 选择器中声明<code>font-size</code>= <code>62.5%</code>，这就使 em 值变为 <code>16px*62.5% = 10px</code></p>\\n<p>这样 <code>12px = 1.2em</code>, <code>10px = 1em</code>, 也就是说只需要将你的原来的<code>px</code> 数值除以 10，然后换上 <code>em</code>作为单位就行了</p>\\n<p>特点：</p>\\n<ul>\\n<li><code>em</code> 的值并不是固定的</li>\\n<li><code>em</code> 会继承父级元素的字体大小</li>\\n<li><code>em</code> 是相对长度单位。相对于当前对象内文本的字体尺寸。如当前对行内文本的字体尺寸未被人为设置，则相对于浏览器的默认字体尺寸</li>\\n<li>任意浏览器的默认字体高都是 <code>16px</code></li>\\n</ul>\\n<p>举个例子</p>\\n<pre class=\\"language-html\\"><code>&lt;div class=&quot;big&quot;&gt;\\n    我是14px=1.4rem&lt;div class=&quot;small&quot;&gt;我是12px=1.2rem&lt;/div&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p>样式为</p>\\n<pre class=\\"language-css\\"><code>&lt;style&gt;\\n    html {font-size: 10px;  } /*  公式16px*62.5%=10px  */  \\n    .big{font-size: 1.4rem}\\n    .small{font-size: 1.2rem}\\n&lt;/style&gt;\\n</code></pre>\\n<p>这时候<code>.big</code>元素的<code>font-size</code>为14px，而<code>.small</code>元素的<code>font-size</code>为12px</p>\\n<p><strong>rem(常用)</strong></p>\\n<ul>\\n<li>根据屏幕的分辨率动态设置<code>html</code>的文字大小，达到等比缩放的功能</li>\\n<li>保证<code>html</code>最终算出来的字体大小，不能小于<code>12px</code></li>\\n<li>在不同的移动端显示不同的元素比例效果</li>\\n<li>如果<code>html</code>的<code>font-size:20px</code>的时候，那么此时的<code>1rem = 20px</code></li>\\n<li>把设计图的宽度分成多少分之一，根据实际情况</li>\\n<li><code>rem</code>做盒子的宽度，<code>viewport</code>缩放</li>\\n</ul>\\n<p><code>head</code>加入常见的<code>meta</code>属性</p>\\n<pre class=\\"language-html\\"><code>&lt;meta name=&quot;format-detection&quot; content=&quot;telephone=no&quot;&gt;\\n&lt;meta name=&quot;apple-mobile-web-app-capable&quot; content=&quot;yes&quot;&gt;\\n&lt;meta name=&quot;apple-mobile-web-app-status-bar-style&quot; content=&quot;black&quot;&gt;\\n&lt;!--这个是关键--&gt;\\n&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0，minimum-scale=1.0&quot;&gt;\\n</code></pre>\\n<p>把这段代码加入<code>head</code>中的<code>script</code>预先加载</p>\\n<pre class=\\"language-js\\"><code>// rem适配用这段代码动态计算html的font-size大小\\n(function(win) {\\n    var docEl = win.document.documentElement;\\n    var timer = &#39;&#39;;\\n\\n    function changeRem() {\\n        var width = docEl.getBoundingClientRect().width;\\n        if (width &gt; 750) { // 750是设计稿大小\\n            width = 750;\\n        }\\n        var fontS = width / 10; // 把设备宽度十等分 1rem&lt;=75px\\n        docEl.style.fontSize = fontS + &quot;px&quot;;\\n    }\\n    win.addEventListener(&quot;resize&quot;, function() {\\n        clearTimeout(timer);\\n        timer = setTimeout(changeRem, 30);\\n    }, false);\\n    win.addEventListener(&quot;pageshow&quot;, function(e) {\\n        if (e.persisted) { //清除缓存\\n            clearTimeout(timer);\\n            timer = setTimeout(changeRem, 30);\\n        }\\n    }, false);\\n    changeRem();\\n})(window)\\n</code></pre>\\n<ul>\\n<li>或者使用淘宝提供的库 <a href=\\"https://github.com/amfe/lib-flexible\\">https://github.com/amfe/lib-flexible</a></li>\\n</ul>\\n<p><strong>vh、vw</strong></p>\\n<p><code>vw</code> ，就是根据窗口的宽度，分成<code>100</code>等份，<code>100vw</code>就表示满宽，<code>50vw</code>就表示一半宽。（<code>vw</code> 始终是针对窗口的宽），同理，<code>vh</code>则为窗口的高度</p>\\n<p>这里的窗口分成几种情况：</p>\\n<ul>\\n<li>在桌面端，指的是浏览器的可视区域</li>\\n<li>移动端指的就是布局视口</li>\\n</ul>\\n<p>像<code>vw</code>、<code>vh</code>，比较容易混淆的一个单位是<code>%</code>，不过百分比宽泛的讲是相对于父元素：</p>\\n<ul>\\n<li>对于普通定位元素就是我们理解的父元素</li>\\n<li>对于<code>position: absolute;</code>的元素是相对于已定位的父元素</li>\\n<li>对于<code>position: fixed;</code>的元素是相对于 <code>ViewPort</code>（可视窗口）</li>\\n</ul>\\n<p><strong>总结</strong></p>\\n<ul>\\n<li><strong>px</strong>：绝对单位，页面按精确像素展示</li>\\n<li><strong>%</strong>：相对于父元素的宽度比例</li>\\n<li><strong>em</strong>：相对单位，基准点为父节点字体的大小，如果自身定义了<code>font-size</code>按自身来计算（浏览器默认字体是<code>16px</code>），整个页面内<code>1em</code>不是一个固定的值</li>\\n<li><strong>rem</strong>：相对单位，可理解为<code>root em</code>, 相对根节点<code>html</code>的字体大小来计算</li>\\n<li><strong>vh、vw</strong>：主要用于页面视口大小布局，在页面布局上更加方便简单\\n<ul>\\n<li><code>vw</code>：屏幕宽度的<code>1%</code></li>\\n<li><code>vh</code>：屏幕高度的<code>1%</code></li>\\n<li><code>vmin</code>：取<code>vw</code>和<code>vh</code>中较小的那个（如：<code>10vh=100px 10vw=200px</code> 则<code>vmin=10vh=100px</code>）</li>\\n<li><code>vmax</code>：取<code>vw</code>和<code>vh</code>中较大的那个（如：<code>10vh=100px 10vw=200px</code> 则<code>vmax=10vw=200px</code>）</li>\\n</ul>\\n</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个评论组件嵌套三层，每层都设置 <code>font-size: 0.9em</code>，线上最内层文字明显过小；为什么只改最外层字号不能根治？</strong></p>\\n<p><code>em</code> 用在 <code>font-size</code> 上时会以上一级元素的计算字号为基准，三层嵌套会形成连续缩放，而不是始终取同一个基准。组件正文可改用相对根元素的 <code>rem</code>，需要随当前文字同步变化的内边距再保留 <code>em</code>；否则层级变化仍会让字号漂移。</p>\\n</li>\\n<li>\\n<p><strong>设计系统要求按钮提供小、中、大三档，同一档按钮的文字变大时，图标间距和内边距也要同步变化；你会全部写成 <code>rem</code> 吗？</strong></p>\\n<p>不会全部使用 <code>rem</code>，按钮的字号可用 <code>rem</code> 保持跨层级一致，内边距和图标间距则适合用 <code>em</code> 跟随当前字号。这样调整一个按钮的 <code>font-size</code> 就能带动局部比例，但若间距必须严格对齐全局栅格，仍应使用统一的 <code>rem</code> 或固定令牌。</p>\\n</li>\\n<li>\\n<p><strong>移动端活动页原来用脚本按 <code>750px</code> 设计稿动态修改根字号，现在改成桌面端也要复用；<code>rem</code> 方案需要增加什么约束？</strong></p>\\n<p>应给用于计算根字号的视口宽度设置上限，避免桌面宽屏把整个页面无限等比放大，同时确认页面已有正确的 <code>viewport</code> 配置。根字号变化会影响所有 <code>rem</code> 尺寸，因此复用前要区分需要整体缩放的页面和只需自适应排版的业务区域。</p>\\n</li>\\n<li>\\n<p><strong>一个弹窗写了 <code>height: 100vh</code>，在桌面浏览器正常，在部分移动端却出现底部内容难以触达；你会先排查什么？</strong></p>\\n<p>先确认这里的 <code>vh</code> 取自移动端布局视口，并检查浏览器界面变化时可用高度与布局高度是否一致。若业务只要求内容区占满可视范围，应避免把关键操作建立在固定 <code>100vh</code> 假设上，并配合滚动或安全区处理；单纯换成 <code>rem</code> 不能解决视口高度问题。</p>\\n</li>\\n<li>\\n<p><strong>数据看板要同时实现“边框始终清晰、字号尊重全局比例、主视觉占屏幕一半宽”，<code>px</code>、<code>rem</code>、<code>vw</code> 怎么分工？</strong></p>\\n<p>边框等不依赖其他属性的精确尺寸可用 <code>px</code>，字号和通用间距用相对根字号的 <code>rem</code>，主视觉宽度可用 <code>50vw</code> 表达视口一半。单位应按尺寸依赖关系选择，不能为了统一而全部换算；否则视口缩放、根字号调整和精确呈现会彼此牵连。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_12-flex布局\\"><a class=\\"header-anchor\\" href=\\"#_12-flex布局\\">#</a> 12 flex布局</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>flex</code> 是一维布局：容器管主轴排列，交叉轴管对齐</li>\\n<li><code>flex: 1</code> = <code>flex-grow: 1</code> + <code>flex-shrink: 1</code> + <code>flex-basis: 0%</code></li>\\n<li><code>flex-basis: 0%</code> 才是真等宽；<code>auto</code> 会先按内容撑开，所以内容多的那栏更宽</li>\\n<li>⚠️ 子项 <code>min-width</code> 默认是 <code>auto</code>，长内容会把容器撑破，自适应栏要写 <code>min-width: 0</code></li>\\n<li>间距优先用 <code>gap</code>；<code>order</code> 只改视觉顺序，键盘 <code>Tab</code> 仍按 DOM 顺序走</li>\\n</ul>\\n<p><strong><code>flex</code> 是一维布局模型，主要解决主轴上的排列和交叉轴上的对齐问题。</strong> 容器通过 <code>flex-direction</code>、<code>justify-content</code>、<code>align-items</code> 和 <code>gap</code> 控制方向、对齐与间距，子项再用 <code>flex</code> 分配空间。<code>flex: 1</code> 会把初始主轴尺寸按 <code>0%</code> 参与计算，因此多个子项更容易真正等宽。遇到长文本或宽图片把容器撑破时，要给自适应项补上 <code>min-width: 0</code>。</p>\\n</section>\\n<p><code>flex</code> 是<strong>一维</strong>布局模型：容器管一条主轴上的排列，交叉轴上做对齐。</p>\\n<p><strong>容器上的属性：</strong></p>\\n<pre class=\\"language-css\\"><code>.container {\\n  display: flex;\\n  flex-direction: row;        /* 主轴方向 row | row-reverse | column | column-reverse */\\n  flex-wrap: nowrap;          /* 是否换行 */\\n  justify-content: flex-start;/* 主轴对齐 */\\n  align-items: stretch;       /* 交叉轴对齐（单行） */\\n  align-content: stretch;     /* 交叉轴对齐（多行时） */\\n  gap: 12px;                  /* 间距，比 margin 干净得多 */\\n}\\n</code></pre>\\n<p><strong>项目上的属性：</strong></p>\\n<pre class=\\"language-css\\"><code>.item {\\n  flex: 1;                    /* = flex-grow:1 flex-shrink:1 flex-basis:0% */\\n  align-self: center;         /* 单独覆盖交叉轴对齐 */\\n  order: 0;                   /* 视觉顺序，不改 DOM 顺序 */\\n}\\n</code></pre>\\n<p><strong><code>flex: 1</code> 到底是什么，这是高频考点：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>属性</th>\\n<th>默认值</th>\\n<th>含义</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>flex-grow</code></td>\\n<td><code>0</code></td>\\n<td>有剩余空间时的放大比例，<code>0</code> 表示不放大</td>\\n</tr>\\n<tr>\\n<td><code>flex-shrink</code></td>\\n<td><code>1</code></td>\\n<td>空间不足时的缩小比例</td>\\n</tr>\\n<tr>\\n<td><code>flex-basis</code></td>\\n<td><code>auto</code></td>\\n<td>分配空间<strong>之前</strong>元素占据的主轴尺寸</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><code>flex: 1</code> 展开是 <code>flex-grow: 1; flex-shrink: 1; flex-basis: 0%</code>。</p>\\n<p><strong><code>flex-basis: 0%</code> 和 <code>auto</code> 的差别决定了布局结果</strong>：<code>auto</code> 会先按内容撑开再分配剩余空间，所以内容多的那一栏最终更宽；<code>0%</code> 表示&quot;假装我初始宽度是 0&quot;，剩余空间完全按 <code>grow</code> 比例分，几个 <code>flex: 1</code> 的项目才会<strong>真正等宽</strong>。做两栏自适应时如果右栏写 <code>flex: auto</code>（<code>basis: auto</code>），宽度会被内容影响，这就是&quot;设了 flex 还是不等宽&quot;的原因。</p>\\n<p><strong>最容易踩的坑：<code>min-width: auto</code></strong></p>\\n<p><code>flex</code> 子项的 <code>min-width</code> 默认是 <code>auto</code>，意思是&quot;不能小于内容的最小尺寸&quot;。所以只要子项里有长表格、不换行的长字符串或者一个宽图片，它就会<strong>把容器撑破</strong>：</p>\\n<pre class=\\"language-css\\"><code>.main {\\n  flex: 1;\\n  min-width: 0;      /* 👈 允许收缩，否则被内容撑破 */\\n  overflow: hidden;  /* 配合省略号或滚动 */\\n}\\n</code></pre>\\n<p>纵向布局同理，要写 <code>min-height: 0</code>。这条几乎是每个人都会踩一次的坑。</p>\\n<p><strong>另外两个实践点</strong>：优先用 <code>gap</code> 而不是给子项加 <code>margin</code>（不用处理首尾项）；<code>order</code> 只改视觉顺序不改 DOM 顺序，所以键盘 <code>Tab</code> 顺序仍按 DOM 走，滥用会让键盘导航变得混乱。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>三张商品卡都写了 <code>flex-grow: 1</code>，中间卡片文案更长，结果仍比另外两张宽；为什么“增长比例相同”不等于最终等宽？</strong></p>\\n<p><code>flex-grow</code> 只规定剩余空间的分配比例，而项目默认的 <code>flex-basis: auto</code> 会先把内容尺寸作为分配起点。要让三张卡真正等宽，应使用 <code>flex: 1</code>，使基准尺寸变为 <code>0%</code>；若卡片还有最小宽度约束，仍可能无法等分。</p>\\n</li>\\n<li>\\n<p><strong>后台页面左侧导航固定 <code>240px</code>，右侧表格列很多，一加载数据导航就被挤压且页面横向溢出；右栏应该怎么改？</strong></p>\\n<p>右栏应设置 <code>flex: 1</code> 和 <code>min-width: 0</code>，允许它缩到内容的最小尺寸以下，再由内部表格区域使用 <code>overflow-x: auto</code> 承担滚动。只加 <code>overflow</code> 未必能突破项目默认的 <code>min-width: auto</code>，而把导航设为不可收缩也不能消除右栏溢出。</p>\\n</li>\\n<li>\\n<p><strong>聊天窗口采用纵向 <code>flex</code>，头部和输入框固定，中间消息列表写了 <code>flex: 1</code>，消息达到上千条后整个页面被撑高而不是列表内滚动；缺了什么？</strong></p>\\n<p>中间列表需要 <code>min-height: 0</code>，并设置合适的纵向 <code>overflow</code>，这样它才能在主轴空间不足时继续收缩并在内部滚动。纵向布局对应的是默认最小高度约束，只套用横向场景的 <code>min-width: 0</code> 不会生效。</p>\\n</li>\\n<li>\\n<p><strong>设计师要求标签超过一行就自动换行且行间距统一，开发用每个标签的右下 <code>margin</code> 实现，首尾总有多余空白；你会改哪些容器属性？</strong></p>\\n<p>容器应使用 <code>display: flex</code>、<code>flex-wrap: wrap</code> 和 <code>gap</code>，由容器统一管理行列间距，避免对子项首尾做特殊抵消。多行整体在交叉轴上的分布由 <code>align-content</code> 控制，<code>align-items</code> 只处理每一行内项目的交叉轴对齐。</p>\\n</li>\\n<li>\\n<p><strong>移动端导航为了匹配视觉稿，用 <code>order</code> 把“退出登录”移到最前面，但键盘测试时焦点仍最后才到；该保留这种实现吗？</strong></p>\\n<p>不应依赖 <code>order</code> 修正具有交互意义的阅读顺序，因为它只改变视觉排列，不改变 <code>DOM</code> 和键盘 <code>Tab</code> 顺序。应优先调整 <code>DOM</code> 结构，使视觉、阅读和焦点顺序一致；若多端顺序确实不同，需要评估是否拆分结构及其维护成本。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_13-关于伪类-lvha-的解释\\"><a class=\\"header-anchor\\" href=\\"#_13-关于伪类-lvha-的解释\\">#</a> 13 关于伪类 LVHA 的解释</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>四个状态伪类顺序：<code>:link → :visited → :hover → :active</code>，记作 <code>LoVe HAte</code></li>\\n<li>原因是四者<strong>权重相同</strong>（都是 <code>(0,1,1)</code>），同权重时后写的赢，而这些状态可以同时成立</li>\\n<li>只有 <code>:link</code> 和 <code>:visited</code> 能互换 —— 它们互斥，不存在覆盖问题</li>\\n<li><code>:visited</code> 出于隐私限制<strong>只能改颜色类属性</strong>，改字号、<code>content</code> 一律无效</li>\\n<li>触屏没有稳定 <code>hover</code>，要用 <code>@media (hover: hover)</code> 圈定，并补 <code>:focus-visible</code> 照顾键盘用户</li>\\n</ul>\\n<p><strong>链接状态伪类通常按 <code>:link → :visited → :hover → :active</code> 排列，也就是常说的 <code>LoVe HAte</code>。</strong> 它们的权重相同，状态又可能同时成立，所以同权重下后写的规则会覆盖前面的规则。<code>:link</code> 和 <code>:visited</code> 彼此互斥，可以交换位置，但 <code>:hover</code>、<code>:active</code> 的先后关系不能随便改。实际开发还要注意，<code>:visited</code> 只能修改颜色类属性，触屏设备则应通过 <code>@media (hover: hover)</code> 限定悬停效果，并补上 <code>:focus-visible</code>。</p>\\n</section>\\n<p><code>a</code> 标签有四种状态伪类：<code>:link</code>（未访问）、<code>:visited</code>（已访问）、<code>:hover</code>（悬停）、<code>:active</code>（按下）。</p>\\n<p><strong>顺序必须是 <code>:link → :visited → :hover → :active</code></strong>，记作 <strong><code>LoVe HAte</code></strong>。</p>\\n<p><strong>为什么是这个顺序？<strong>因为这四个选择器</strong>权重完全相同</strong>（都是 <code>(0,1,1)</code>），权重相同时 <code>CSS</code> 取后写的那条。而这些状态是<strong>可以同时成立</strong>的：</p>\\n<ul>\\n<li>鼠标悬停在一个未访问的链接上 → 同时满足 <code>:link</code> 和 <code>:hover</code>，要让 <code>:hover</code> 生效就必须写在 <code>:link</code> 后面</li>\\n<li>按下鼠标时 → 同时满足 <code>:link</code>、<code>:hover</code>、<code>:active</code>，所以 <code>:active</code> 要排最后</li>\\n<li>访问过的链接同理，把 <code>:link</code> 换成 <code>:visited</code></li>\\n</ul>\\n<pre class=\\"language-css\\"><code>/* ❌ 访问过之后，hover 永远不生效 */\\na:hover   { color: green; }\\na:visited { color: red; }\\n\\n/* ✅ 越&quot;临时&quot;的状态排越后 */\\na:link    { color: #1677ff; }\\na:visited { color: #722ed1; }\\na:hover   { color: #40a9ff; }\\na:active  { color: #096dd9; }\\n</code></pre>\\n<p><strong>这个顺序能不能变？</strong> 只有 <code>:link</code> 和 <code>:visited</code> 可以互换 —— 一个链接要么访问过要么没访问过，两者互斥，不存在覆盖问题。<code>:hover</code> 和 <code>:active</code> 的相对位置不能动。</p>\\n<p><strong>现代代码里更该关心的三件事：</strong></p>\\n<p><strong>① 别死背顺序，理解权重。</strong> 一旦你写的是 <code>.nav a:hover</code> 和 <code>a:visited</code>（权重不同了），顺序就不再是决定因素。真正的排查方法是打开 <code>DevTools</code> 的 <code>Styles</code> 面板看哪条规则被划掉了。</p>\\n<p><strong>② <code>:visited</code> 能改的属性极少。</strong> 出于隐私考虑（防止用它探测用户浏览历史），浏览器只允许它修改颜色类属性（<code>color</code>、<code>background-color</code>、<code>border-color</code>），改字号、改 <code>content</code> 都无效，<code>getComputedStyle</code> 读到的也是未访问时的值。所以&quot;已读文章加个角标&quot;这种需求只能靠业务数据实现。</p>\\n<p><strong>③ 触屏没有稳定的 <code>hover</code>。</strong> 手机上点击后 <code>hover</code> 样式可能一直粘着不消失。关键信息不能只放在 <code>hover</code> 里，并且应该把悬停效果限定在真正支持悬停的设备上：</p>\\n<pre class=\\"language-css\\"><code>@media (hover: hover) {\\n  a:hover { text-decoration: underline; }\\n}\\n</code></pre>\\n<p><strong>④ 别忘了 <code>:focus-visible</code>。</strong> 只写 <code>hover</code> 等于把键盘用户排除在外。<code>:focus-visible</code> 的好处是鼠标点击时不显示焦点环、键盘 <code>Tab</code> 时才显示，两边都照顾到。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>文章链接先写了 <code>a:hover</code>，后面又写 <code>a:visited</code>；用户访问后再悬停，标题始终是已访问颜色，为什么？</strong></p>\\n<p>两个选择器的权重相同，而悬停与已访问状态能够同时成立，后写的 <code>:visited</code> 因源码顺序覆盖了 <code>:hover</code>。应把临时交互状态放在后面，通常按 <code>:link</code>、<code>:visited</code>、<code>:hover</code>、<code>:active</code> 排列；仅背顺序无法处理权重不同的规则。</p>\\n</li>\\n<li>\\n<p><strong>导航组件里 <code>.nav a:hover</code> 写在前面，通用的 <code>a:visited</code> 写在后面，悬停样式却仍然生效；这是否推翻了 <code>LVHA</code>？</strong></p>\\n<p>没有推翻，因为 <code>.nav a:hover</code> 的选择器权重高于 <code>a:visited</code>，此时决定结果的不再只是声明顺序。应在 <code>DevTools</code> 的样式面板检查匹配规则和被覆盖项，而不是机械移动代码；继续增加更高权重会让后续覆盖更难维护。</p>\\n</li>\\n<li>\\n<p><strong>资讯站想给已读文章增加“已读”角标，团队坚持只用 <code>a:visited::after { content: \'已读\' }</code>；为什么上线后不能依赖它？</strong></p>\\n<p>浏览器出于历史记录隐私保护，只允许 <code>:visited</code> 改有限的颜色类属性，<code>content</code>、字号和布局类变化不会按预期生效。角标应由业务阅读记录驱动并添加真实类名或节点；依赖浏览器历史既无法稳定展示，也不能由脚本可靠读取。</p>\\n</li>\\n<li>\\n<p><strong>触屏设备上菜单项点过一次后，<code>:hover</code> 背景可能持续保留，用户以为它仍被选中；样式层怎么约束？</strong></p>\\n<p>应把纯悬停效果放进 <code>@media (hover: hover)</code>，只让真正支持稳定悬停的设备应用，同时用独立状态表达当前选中项。关键信息不能只藏在 <code>:hover</code> 中，因为触屏设备没有可靠的悬停交互，残留样式也不等于业务状态。</p>\\n</li>\\n<li>\\n<p><strong>代码评审发现全局写了 <code>a:focus { outline: none }</code>，理由是鼠标点击时焦点框破坏视觉稿；你会要求怎样修改？</strong></p>\\n<p>应移除无替代方案的焦点隐藏，并用 <code>:focus-visible</code> 为键盘 <code>Tab</code> 操作提供清晰焦点样式，鼠标点击时通常不会显示同样的焦点环。只设计 <code>:hover</code> 会排除键盘用户，而直接清除 <code>outline</code> 会让他们无法判断当前位置。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_14-calc函数\\"><a class=\\"header-anchor\\" href=\\"#_14-calc函数\\">#</a> 14 calc函数</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>calc()</code> 的核心价值是<strong>混合单位运算</strong>（<code>%</code> 和 <code>px</code>/<code>rem</code>/<code>vw</code> 同式），这是预处理器做不到的</li>\\n<li>⚠️ <code>+</code> 和 <code>-</code> 两侧<strong>必须有空格</strong>，否则整条声明被丢弃；<code>/</code> 的除数必须是纯数字</li>\\n<li>表达式无效时整条声明失效，需要兜底就在前面写一条固定值</li>\\n<li>最常用的三个场景：减去固定头部高度、配合 <code>var()</code> 做设计令牌、<code>env()</code> 安全区适配</li>\\n<li><code>-webkit-calc</code> 前缀今天已经完全不需要；等分布局用 <code>grid</code> 的 <code>fr</code> + <code>gap</code> 比 <code>calc</code> 更清楚</li>\\n</ul>\\n<p><strong><code>calc()</code> 主要用来在运行时完成混合单位运算，比如把 <code>%</code>、<code>px</code>、<code>rem</code> 放进同一个表达式。</strong> 这类计算依赖浏览器实际布局结果，预处理器在编译阶段无法替代。使用时要注意 <code>+</code>、<code>-</code> 两侧必须留空格，<code>/</code> 的除数只能是纯数字，表达式无效会让整条声明失效，因此可以在前面写固定值兜底。我一般把它用于扣除固定头部、结合 <code>var()</code> 计算间距或适配安全区；等分布局则优先考虑 <code>grid</code> 的 <code>fr</code> 和 <code>gap</code>。</p>\\n</section>\\n<p><code>calc()</code> 让 <code>CSS</code> 能做<strong>混合单位的运算</strong>，这是它最大的价值 —— 百分比、<code>px</code>、<code>rem</code>、<code>vw</code> 可以放在同一个表达式里，而这是预处理器做不到的（<code>Sass</code> 在编译期算，那时根本不知道百分比对应多少像素）。</p>\\n<pre class=\\"language-css\\"><code>.main {\\n  /* 总宽减去固定的侧边栏 */\\n  width: calc(100% - 240px);\\n  /* 视口高度减去头尾 */\\n  height: calc(100vh - 64px - 48px);\\n  /* 配合自定义属性，运行时可调 */\\n  padding: calc(var(--gap) * 2);\\n}\\n</code></pre>\\n<p><strong>四条必须记住的规则：</strong></p>\\n<ol>\\n<li><strong><code>+</code> 和 <code>-</code> 两侧必须有空格</strong>。<code>calc(100%-10px)</code> 是无效的，会被整条忽略。因为 <code>-10px</code> 可以被解析成一个负数值，不加空格就有歧义。<code>*</code> 和 <code>/</code> 不强制，但建议统一加。</li>\\n<li><strong><code>/</code> 的除数必须是纯数字</strong>，不能带单位。<code>calc(100% / 3)</code> ✅，<code>calc(100% / 3px)</code> ❌。</li>\\n<li><strong>可以嵌套</strong>，也可以和 <code>var()</code> 混用 —— 后者是它在现代 <code>CSS</code> 里最主要的用法。</li>\\n<li><strong>整个表达式无效时，整条声明被丢弃</strong>，不会退回上一条。需要兜底就在前面写一条固定值：</li>\\n</ol>\\n<pre class=\\"language-css\\"><code>.box {\\n  width: 90%;                  /* 不支持 calc 的浏览器用这条 */\\n  width: calc(100% - 2rem);    /* 支持的覆盖上面 */\\n}\\n</code></pre>\\n<p><strong>兼容性提醒</strong>：<code>-webkit-calc</code>、<code>-moz-calc</code> 这些前缀在今天已经完全不需要了，老资料里的写法可以删掉。</p>\\n<p><strong>实际用得最多的三个场景：</strong></p>\\n<ul>\\n<li><strong>减去固定元素</strong>：<code>height: calc(100dvh - var(--header-h))</code> 做整屏内容区</li>\\n<li><strong>配合自定义属性做设计系统</strong>：<code>--space: 8px</code>，然后 <code>margin: calc(var(--space) * 3)</code></li>\\n<li><strong>安全区适配</strong>：<code>padding-bottom: calc(12px + env(safe-area-inset-bottom, 0px))</code></li>\\n</ul>\\n<p><strong>现在的替代方案</strong>：很多以前需要 <code>calc</code> 的场景，用 <code>flex</code>/<code>grid</code> 的 <code>fr</code> 单位和 <code>gap</code> 会更自然 —— 比如&quot;三栏等分减去间距&quot;，写 <code>grid-template-columns: repeat(3, 1fr); gap: 16px</code> 比 <code>calc(33.33% - 11px)</code> 清楚得多，也不会有小数误差。另外 <code>min()</code>、<code>max()</code>、<code>clamp()</code> 是 <code>calc</code> 的同族函数，做流体尺寸比手写媒体查询省事。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>侧栏页面写了 <code>width: calc(100%-240px)</code>，浏览器控制台没有报错，主区域却回到了默认宽度；这条声明为什么失效？</strong></p>\\n<p><code>+</code> 和 <code>-</code> 两侧必须保留空格，否则 <code>-240px</code> 会带来解析歧义，使整个 <code>calc()</code> 表达式无效并被浏览器静默丢弃。应改成 <code>calc(100% - 240px)</code>，并在 <code>DevTools</code> 中查看被划掉的声明，而不是等待脚本错误。</p>\\n</li>\\n<li>\\n<p><strong>主题系统把间距定义为 <code>--space: 8px</code>，卡片要使用三倍间距；用预处理器先算还是写 <code>calc(var(--space) * 3)</code>？</strong></p>\\n<p>应写 <code>calc(var(--space) * 3)</code>，因为自定义属性可在运行时按主题或组件作用域变化，编译期预处理器无法提前知道最终值。若变量可能缺失，需要给 <code>var()</code> 设计合理兜底；变量替换后表达式非法时，整条声明仍会被丢弃。</p>\\n</li>\\n<li>\\n<p><strong>团队把三栏布局写成 <code>width: calc(33.33% - 11px)</code>，列数改成四栏后每处都要重算；你会保留 <code>calc()</code> 吗？</strong></p>\\n<p>不会，等分列与固定间距更适合 <code>grid-template-columns: repeat(3, 1fr)</code> 配合 <code>gap</code>，列数变化时只改轨道定义。<code>calc()</code> 更适合混合单位或减去固定区域，这里手工百分比既难维护，也可能带来小数舍入后的缝隙。</p>\\n</li>\\n<li>\\n<p><strong>整屏内容区写了 <code>height: calc(100dvh - var(--header-h))</code>，某个租户没注入 <code>--header-h</code> 后整条高度消失；怎样设置可靠兜底？</strong></p>\\n<p>应为变量提供默认值，例如 <code>calc(100dvh - var(--header-h, 64px))</code>，并可在前面保留一条业务可接受的固定高度声明。后写的表达式一旦在计算后无效会被整条丢弃，不能假设浏览器会自动修补缺失变量。</p>\\n</li>\\n<li>\\n<p><strong>有人写 <code>width: calc(100% / 3px)</code>，想表达容器三等分；为什么不能工作，怎样选更合适的实现？</strong></p>\\n<p><code>calc()</code> 的除数必须是无单位数字，因此应写成 <code>calc(100% / 3)</code>，<code>3px</code> 不能作为除数。若目标只是稳定三等分并包含间距，使用 <code>grid</code> 的 <code>repeat(3, 1fr)</code> 与 <code>gap</code> 更直接；只有确实需要混合单位计算时才值得保留 <code>calc()</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_15-伪类和伪元素\\"><a class=\\"header-anchor\\" href=\\"#_15-伪类和伪元素\\">#</a> 15 伪类和伪元素</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>伪类（单冒号 <code>:</code>）描述元素的<strong>状态</strong>：<code>:hover</code>、<code>:focus-visible</code>、<code>:nth-child()</code>、<code>:checked</code>、<code>:has()</code></li>\\n<li>伪元素（双冒号 <code>::</code>）创建<strong>文档树里不存在的虚拟元素</strong>：<code>::before</code>、<code>::after</code>、<code>::placeholder</code>、<code>::selection</code></li>\\n<li><code>::before</code>/<code>::after</code> 必须写 <code>content</code> 才会生成，且对<strong>替换元素</strong>（<code>img</code>、<code>input</code>、<code>br</code>）无效</li>\\n<li>伪元素内容不在 DOM 里 —— 读屏器支持不稳定、无法选中复制、<code>JS</code> 取不到，别放关键信息</li>\\n<li><code>:has()</code> 是&quot;父选择器&quot;，让&quot;父元素里有 X 就怎样&quot;这类需求不再需要 <code>JS</code></li>\\n</ul>\\n<p><strong>伪类用来描述元素的状态或结构关系，伪元素则用于表示文档树中不存在的虚拟部分。</strong> 前者通常写单冒号，例如 <code>:hover</code>、<code>:checked</code>、<code>:nth-child()</code>；后者写双冒号，例如 <code>::before</code>、<code>::after</code>、<code>::selection</code>。<code>::before</code> 和 <code>::after</code> 需要设置 <code>content</code> 才会生成，而且不适用于 <code>img</code>、<code>input</code> 这类替换元素。伪元素内容不在 <code>DOM</code> 中，读屏、复制和 <code>JavaScript</code> 获取都有限制，所以关键信息仍应放进真实节点；需要根据子元素改变父元素样式时，可以考虑 <code>:has()</code>。</p>\\n</section>\\n<blockquote>\\n<p><code>css</code>引入伪类和伪元素概念是为了格式化文档树以外的信息。也就是说，伪类和伪元素都是用来修饰不在文档树中的部分</p>\\n</blockquote>\\n<p><strong>伪类</strong></p>\\n<blockquote>\\n<p>伪类存在的意义是为了通过选择器找到那些不存在DOM树中的信息以及不能被常规CSS选择器获取到的信息</p>\\n</blockquote>\\n<ol>\\n<li>获取不存在与DOM树中的信息。比如a标签的<code>:link</code>、<code>visited</code>等，这些信息不存在与DOM树结构中，只能通过CSS选择器来获取；</li>\\n<li>获取不能被常规CSS选择器获取的信息。比如：要获取第一个子元素，我们无法用常规的CSS选择器获取，但可以通过 <code>:first-child</code> 来获取到。</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/01/24.png\\" alt=\\"\\"></p>\\n<p><strong>伪元素</strong></p>\\n<blockquote>\\n<p>伪元素用于创建一些不在文档树中的元素，并为其添加样式。比如说，我们可以通过<code>:before</code>来在一个元素前增加一些文本，并为这些文本添加样式。虽然用户可以看到这些文本，但是这些文本实际上不在文档树中。常见的伪元素有：<code>::before</code>，<code>::after</code>，<code>::first-line</code>，<code>::first-letter</code>，<code>::selection</code>、<code>::placeholder</code>等</p>\\n</blockquote>\\n<blockquote>\\n<p>因此，伪类与伪元素的区别在于：有没有创建一个文档树之外的元素</p>\\n</blockquote>\\n<p><strong>::after和:after的区别</strong></p>\\n<ul>\\n<li>在实际的开发工作中，我们会看到有人把伪元素写成<code>:after</code>，这实际是 <code>CSS2</code> 与<code>CSS3</code>新旧标准的规定不同而导致的。</li>\\n<li><code>CSS2</code> 中的伪元素使用1个冒号，在 <code>CSS3</code> 中，为了区分伪类和伪元素，规定伪元素使用2个冒号。所以，对于 CSS2 标准的老伪元素，比如<code>:first-line</code>，<code>:first-letter</code>，<code>:before</code>，<code>:after</code>，写一个冒号浏览器也能识别，但对于 CSS3 标准的新伪元素，比如<code>::selection</code>，就必须写2个冒号了</li>\\n</ul>\\n<p><strong>CSS3新增伪类有那些？</strong></p>\\n<ul>\\n<li><code>p:first-of-type</code> 选择属于其父元素的首个<code>&lt;p&gt;</code>元素的每个<code>&lt;p&gt;</code> 元素。</li>\\n<li><code>p:last-of-type</code> 选择属于其父元素的最后 <code>&lt;p&gt;</code> 元素的每个<code>&lt;p&gt;</code> 元素。</li>\\n<li><code>p:only-of-type</code> 选择属于其父元素唯一的 <code>&lt;p&gt;</code>元素的每个 <code>&lt;p&gt;</code> 元素。</li>\\n<li><code>p:only-child</code> 选择属于其父元素的唯一子元素的每个 <code>&lt;p&gt;</code> 元素。</li>\\n<li><code>p:nth-child(2)</code> 选择属于其父元素的第二个子元素的每个 <code>&lt;p&gt;</code> 元素。</li>\\n<li><code>:after</code> 在元素之前添加内容,也可以用来做清除浮动。</li>\\n<li><code>:before</code> 在元素之后添加内容</li>\\n<li><code>:enabled</code></li>\\n<li><code>:disabled</code> 控制表单控件的禁用状态。</li>\\n<li><code>:checked</code> 单选框或复选框被选中</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>表单里需要选中第二个复选框后改变它的背景，同事准备插入一个额外 <code>span</code> 再监听点击；伪类和伪元素分别能否替代这段逻辑？</strong></p>\\n<p>已有复选框的选中状态可直接由 <code>:checked</code> 匹配，这是伪类对元素状态的筛选，不需要新增节点或点击脚本。若只需附加一个装饰标记，可再用 <code>::after</code> 创建文档树之外的视觉盒子；它不应承担真实表单状态。</p>\\n</li>\\n<li>\\n<p><strong>设计稿要求必填项标签前显示红色星号，团队准备用 <code>label::before { content: \'*\' }</code>；工程上还必须补什么？</strong></p>\\n<p>红色星号可作为视觉装饰由 <code>::before</code> 生成，但必填约束仍应落在真实表单控件及可访问语义上，不能只依赖伪元素文本。伪元素内容不在文档树中，若用户必须理解或操作该信息，就应有真实的 <code>DOM</code> 或属性作为依据。</p>\\n</li>\\n<li>\\n<p><strong>旧项目同时存在 <code>a:before</code> 和 <code>a::before</code>，重构时有人要全量改成单冒号以保持兼容；这个选择合理吗？</strong></p>\\n<p>不合理，现代写法应使用双冒号区分伪元素和伪类，<code>::before</code>、<code>::after</code> 更能表达其创建文档树外盒子的语义。早期伪元素的单冒号主要是历史兼容形式，而较新的伪元素如 <code>::selection</code> 要使用双冒号，继续扩散旧写法只会增加认知成本。</p>\\n</li>\\n<li>\\n<p><strong>列表混有 <code>h3</code>、<code>p</code> 和图片，需求是选中父容器里的第一个 <code>p</code>，开发写 <code>p:first-child</code> 却匹配不到；应该换成什么？</strong></p>\\n<p>应使用 <code>p:first-of-type</code>，它选择同一父元素下首个 <code>p</code>，不要求该元素是父容器的第一个子节点。<code>p:first-child</code> 只有在这个 <code>p</code> 恰好位于全部子节点首位时才匹配，混合标签结构变化后很容易失效。</p>\\n</li>\\n<li>\\n<p><strong>一个纯 <code>CSS</code> 清除浮动方案使用 <code>::after</code>，另一个通知角标也使用 <code>::after</code>；组件同时启用时后者消失，怎么判断和处理？</strong></p>\\n<p>同一元素的同一个 <code>::after</code> 只有一个伪元素盒子，两套规则会通过层叠共同作用，冲突时不能把它当成两个独立节点。应检查选择器权重及 <code>content</code> 等被覆盖的属性，并把其中一项迁到真实子节点或另一个可用伪元素；伪元素数量有限，不适合承载多份结构。</p>\\n</li>\\n<li>\\n<p><strong>卡片选中内部单选框后要高亮边框，团队争论是监听 <code>change</code> 给父级加类，还是只用 <code>CSS</code>；怎样划分边界？</strong></p>\\n<p>若高亮仅由当前可被选择器表达的表单状态决定，可用 <code>.card:has(input:checked)</code> 让父卡片响应子元素状态，避免只为样式同步维护类名。若状态还依赖接口结果、跨组件流程或需要持久化，仍应由业务数据驱动；<code>CSS</code> 只能表达匹配关系，不能替代业务状态管理。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_16-浏览器是怎样解析-css-选择器的\\"><a class=\\"header-anchor\\" href=\\"#_16-浏览器是怎样解析-css-选择器的\\">#</a> 16 浏览器是怎样解析 CSS 选择器的</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>浏览器匹配选择器是<strong>从右往左</strong>的，最右边那部分叫「关键选择器」</li>\\n<li>原因：祖先链是单线、子孙集合是发散的，从右往左发现不匹配可以一次淘汰，避免大量无效遍历</li>\\n<li>实践指导：关键选择器要具体，别写 <code>.a *</code>；别写五六层的后代选择器（<code>Sass</code> 嵌套最容易踩）</li>\\n<li>但别过度优化 —— 现代浏览器样式计算很快，性能大头在重排重绘和 <code>JS</code></li>\\n<li>完整流程：<code>CSSOM</code> → 渲染树 → 布局 → 绘制 → 合成</li>\\n</ul>\\n<p><strong>浏览器匹配 <code>CSS</code> 选择器时通常从右往左进行，最右侧部分就是关键选择器。</strong> 例如处理 <code>#nav ul li a</code> 时，会先找到 <code>a</code>，再沿祖先链验证 <code>li</code>、<code>ul</code> 和 <code>#nav</code>；因为祖先链是单线的，一旦不匹配就能立即淘汰。工程上应避免 <code>.a *</code> 和五六层后代选择器，尤其要留意 <code>Sass</code> 嵌套编译出的长选择器。不过不必过度优化，现代页面更常见的性能大头仍是重排、重绘和 <code>JavaScript</code>；整体流程是解析为 <code>CSSOM</code>，再生成渲染树并完成布局、绘制和合成。</p>\\n</section>\\n<p><strong>结论先行：从右往左匹配。</strong></p>\\n<p>对 <code>#nav ul li a</code> 这条规则，浏览器不是先找 <code>#nav</code> 再往下走，而是先找到页面上所有 <code>a</code>（这个最右边的部分叫<strong>关键选择器</strong>），再逐个向上验证祖先里有没有 <code>li</code>、<code>ul</code>、<code>#nav</code>。</p>\\n<p><strong>为什么反着来？</strong></p>\\n<p>假设从左往右：找到 <code>#nav</code>（1 个）→ 遍历它下面所有 <code>ul</code>（可能几十个）→ 每个 <code>ul</code> 下所有 <code>li</code> → 每个 <code>li</code> 下所有 <code>a</code>。这是一次<strong>树的深度遍历</strong>，走到最后才知道匹不匹配，而且绝大多数分支最终都是无用功。</p>\\n<p>从右往左：拿到一个 <code>a</code>，往上看父级 —— 如果它父级不是 <code>li</code>，<strong>立刻放弃</strong>，一次比较就淘汰。DOM 树上&quot;祖先链&quot;是一条线（每个节点只有一个父节点），而&quot;子孙集合&quot;是发散的，所以沿着单线往上走的代价远小于往下发散。</p>\\n<p><strong>这对我们写 <code>CSS</code> 有什么指导意义？</strong></p>\\n<ol>\\n<li><strong>关键选择器要尽量具体。</strong> <code>.sidebar *</code> 这种写法的关键选择器是 <code>*</code>，等于对页面上每个元素都要向上验证一遍，是最贵的写法。</li>\\n<li><strong>别写超长的后代选择器。</strong> <code>div &gt; ul &gt; li &gt; a &gt; span</code> 每匹配一次要向上验证四层。改成一个语义化的类名（<code>.nav-label</code>）既快又好维护。</li>\\n<li><strong>但别过度优化。</strong> 现代浏览器的样式计算已经非常快，规则数量在几千条以内时，选择器的开销通常不是性能瓶颈 —— 真正的大头是重排、重绘和 <code>JS</code>。这条知识的价值在于<strong>理解 <code>CSS</code> 的心智模型</strong>，以及在写出 <code>* { }</code> 或深层嵌套时知道代价在哪。</li>\\n</ol>\\n<p><strong>顺带说清 <code>CSS</code> 的整体解析流程</strong>（面试常连着问）：</p>\\n<pre class=\\"language-text\\"><code>下载 CSS → 解析成 CSSOM → 与 DOM 合并成渲染树 → 布局(Layout) → 绘制(Paint) → 合成(Composite)\\n</code></pre>\\n<p>样式计算这一步要为每个元素算出<strong>最终的计算样式</strong>，涉及层叠、继承、默认值的解析。用 <code>Sass</code> 时要特别注意嵌套层级 —— 写起来只是缩进，编译出来就是一条长后代选择器，很容易无意中写出五六层的规则。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表里有一条 <code>.page .list .item .meta a</code>，同事说浏览器会先找到唯一的 <code>.page</code>，所以层级越完整越快；你会怎么反驳？</strong></p>\\n<p>这个判断把匹配方向说反了，浏览器会先找关键选择器 <code>a</code>，再沿祖先链验证 <code>.meta</code>、<code>.item</code>、<code>.list</code> 和 <code>.page</code>。左侧限定再唯一，也不会改变从右往左的起点；层级过长只会增加逐级验证与维护成本。</p>\\n</li>\\n<li>\\n<p><strong>一个后台系统由 <code>Sass</code> 编译出大量五六层后代选择器，评审时你会要求团队怎样收敛？</strong></p>\\n<p>应优先给目标元素增加语义化类名，把长链改成类似 <code>.nav-label</code> 的单类选择器，并限制 <code>Sass</code> 嵌套深度。嵌套写法看似只是缩进，产物却会累积匹配层级和权重；状态修饰可保留 <code>&amp;:hover</code>、<code>&amp;.is-active</code>。</p>\\n</li>\\n<li>\\n<p><strong>全站只有几千条规则，但性能专项要求把所有三层选择器改成单类；你会接受这项改造吗？</strong></p>\\n<p>不应仅凭选择器层级启动全量改造，现代浏览器下几千条规则的匹配开销通常不是主要瓶颈。先用性能数据确认样式计算是否显著，再优先处理重排、重绘、脚本和无用样式；否则改造收益有限，还可能引入回归。</p>\\n</li>\\n<li>\\n<p><strong>线上页面滚动变慢，样式表里同时出现 <code>.sidebar *</code> 和多条深层规则，你怎么判断哪类规则更值得先查？</strong></p>\\n<p>先查 <code>.sidebar *</code>，因为它的关键选择器是 <code>*</code>，页面上的每个元素都可能成为候选，再向上验证是否位于 <code>.sidebar</code>。深层规则也会增加祖先验证次数，但候选集合取决于最右侧选择器；排查仍需结合样式计算耗时，不能只凭源码定责。</p>\\n</li>\\n<li>\\n<p><strong>设计系统想用 <code>div &gt; ul &gt; li &gt; a &gt; span</code> 保证结构严格，业务团队想改成 <code>.nav-label</code>；你支持哪一边？</strong></p>\\n<p>更倾向 <code>.nav-label</code>，它减少祖先层级验证，也不会把样式强绑定到当前标签结构，组件重构时更稳。结构选择器只有在样式确实必须依赖该层级关系时才合理；单纯借层级表达语义，会同时增加匹配和维护成本。</p>\\n</li>\\n<li>\\n<p><strong>候选人只说“选择器从右往左”，你把代码换成 <code>#nav ul li a</code>，会继续追问浏览器为什么不从唯一的 <code>#nav</code> 往下找吗？</strong></p>\\n<p>应说明祖先链只有一条，而子孙集合会向下发散；从 <code>a</code> 向上时，父级不是 <code>li</code> 就能立刻淘汰。若从 <code>#nav</code> 向下遍历，需要探索多个 <code>ul</code>、<code>li</code> 和 <code>a</code> 分支，往往走到末端才知道是否匹配。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_17-浏览器如何判断是否支持-webp-格式图片\\"><a class=\\"header-anchor\\" href=\\"#_17-浏览器如何判断是否支持-webp-格式图片\\">#</a> 17 浏览器如何判断是否支持 webp 格式图片</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><strong>最优解是不判断</strong>：用 <code>&lt;picture&gt;</code> + <code>&lt;source type=&quot;image/webp&quot;&gt;</code> 让浏览器自己挑，零 <code>JS</code>、零延迟</li>\\n<li>服务端方案同样不需要前端参与：按请求头里的 <code>Accept: image/webp</code> 返回不同格式，配 <code>Vary: Accept</code> 让缓存分流</li>\\n<li><code>JS</code> 检测两种：<code>canvas.toDataURL(\'image/webp\')</code> 看前缀（同步但测的是编码能力）、加载 1px <code>base64</code> 图（异步但最准）</li>\\n<li><code>canvas</code> 方案不支持时会静默回退成 <code>png</code>，所以必须检查返回值前缀而不是捕获异常</li>\\n<li><code>CSS</code> 背景图没有 <code>picture</code> 协商机制，那种场景可以用 <code>image-set()</code></li>\\n</ul>\\n<p><strong>通常不需要手动判断，优先用 <code>&lt;picture&gt;</code> 的 <code>type</code> 让浏览器自行选择 <code>WebP</code>，并用普通图片兜底。</strong> 服务端也可以根据请求的 <code>Accept</code> 头返回对应格式，同时设置 <code>Vary: Accept</code>，避免缓存串用。如果必须用 <code>JS</code>，加载一张极小的 <code>WebP</code> 并监听成功或失败，比 <code>canvas.toDataURL()</code> 更准确，因为后者检测的是编码能力。异步检测可能造成首屏等待或图片闪换，所以一般只在背景图等不好使用 <code>&lt;picture&gt;</code> 的场景考虑。</p>\\n</section>\\n<p><strong>最推荐的做法：不判断。</strong> 用 <code>&lt;picture&gt;</code> 让浏览器自己挑：</p>\\n<pre class=\\"language-html\\"><code>&lt;picture&gt;\\n  &lt;source srcset=&quot;hero.avif&quot; type=&quot;image/avif&quot;&gt;\\n  &lt;source srcset=&quot;hero.webp&quot; type=&quot;image/webp&quot;&gt;\\n  &lt;img src=&quot;hero.jpg&quot; alt=&quot;主图&quot; width=&quot;1200&quot; height=&quot;600&quot;&gt;\\n&lt;/picture&gt;\\n</code></pre>\\n<p>浏览器取<strong>第一个它认识的</strong> <code>type</code>，都不认就落到 <code>&lt;img&gt;</code>。零 <code>JS</code>、零延迟、不会闪、也不需要维护任何检测逻辑。这是今天的标准答案。</p>\\n<p><strong>服务端方案（同样不需要前端判断）：</strong></p>\\n<p>浏览器发图片请求时会带 <code>Accept</code> 头，支持 <code>WebP</code> 的会包含 <code>image/webp</code>。服务端或 <code>CDN</code> 按这个头返回不同格式，配合 <code>Vary: Accept</code> 让缓存正确分流。这样前端写一个 <code>URL</code> 就行，格式协商完全在后端完成 —— 主流图片 <code>CDN</code> 都是这么做的。</p>\\n<p><strong>如果面试官一定要问 <code>JS</code> 检测，有两种经典写法：</strong></p>\\n<pre class=\\"language-js\\"><code>// 方案一：canvas.toDataURL —— 同步，但结果不一定可靠\\nfunction supportWebpSync() {\\n  const canvas = document.createElement(&#39;canvas&#39;)\\n  canvas.width = canvas.height = 1\\n  return canvas.toDataURL(&#39;image/webp&#39;).startsWith(&#39;data:image/webp&#39;)\\n}\\n\\n// 方案二：真正加载一张 1px 的 webp —— 异步，但最准\\nfunction supportWebp() {\\n  return new Promise((resolve) =&gt; {\\n    const img = new Image()\\n    img.onload = () =&gt; resolve(img.width === 1)\\n    img.onerror = () =&gt; resolve(false)\\n    img.src = &#39;data:image/webp;base64,UklGRhoAAABXRUJQVlA4TA0AAAAvAAAAEAcQERGIiP4HAA==&#39;\\n  })\\n}\\n</code></pre>\\n<p><strong><code>canvas</code> 方案的坑</strong>：它测的是&quot;能不能<strong>编码</strong>成 <code>WebP</code>&quot;，而我们关心的是&quot;能不能<strong>解码</strong>显示&quot;。历史上有浏览器两者不一致，所以它只能算快速判断，不能作为唯一依据。而且 <code>toDataURL</code> 在不支持时会静默回退成 <code>image/png</code>，判断必须检查返回的前缀而不是有没有报错。</p>\\n<p><strong><code>Image</code> 方案的坑</strong>：它是异步的，所以要么阻塞首屏等结果（不可接受），要么先渲染再替换（会闪）。而且检测结果要缓存到 <code>localStorage</code>，否则每次进页面都测一遍。</p>\\n<p><strong>结论</strong>：这题考的是&quot;你知不知道有更好的办法&quot;。能说出 <code>&lt;picture&gt;</code> 和 <code>Accept</code> 头两种零成本方案，比背两段检测代码更有价值 —— <code>JS</code> 检测是 2015 年前后的历史方案，今天只在必须用 <code>background-image</code> 的场景才偶尔用得上（因为 <code>CSS</code> 背景图没有 <code>picture</code> 这种协商机制，那时可以用 <code>image-set()</code>）。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>有人用 <code>canvas.toDataURL(\'image/webp\')</code> 不抛异常就判定支持，结果少数浏览器仍显示失败；这段判断错在哪里？</strong></p>\\n<p>不抛异常不能证明支持，因为不支持时 <code>toDataURL</code> 可能静默回退为 <code>image/png</code>，至少要检查结果是否以 <code>data:image/webp</code> 开头。更关键的是它测试编码能力，而页面需要解码显示能力，两者历史上并不始终一致。</p>\\n</li>\\n<li>\\n<p><strong>首屏主图要同时兼容 <code>AVIF</code>、<code>WebP</code> 和 <code>JPEG</code>，前端不允许增加异步检测，你会怎样写资源声明？</strong></p>\\n<p>使用 <code>&lt;picture&gt;</code>，按 <code>AVIF</code>、<code>WebP</code> 的顺序声明带 <code>type</code> 的 <code>&lt;source&gt;</code>，最后用 <code>&lt;img src=&quot;hero.jpg&quot;&gt;</code> 兜底。浏览器会选择第一个认识的格式，整个过程零 <code>JS</code>、无检测等待；仍要为图片保留准确的尺寸和替代文本。</p>\\n</li>\\n<li>\\n<p><strong>同一图片地址由 <code>CDN</code> 自动返回不同格式，但缓存命中后老浏览器拿到了 <code>WebP</code>；服务端协商缺了什么？</strong></p>\\n<p>服务端不仅要读取请求的 <code>Accept</code>，还要返回 <code>Vary: Accept</code>，让缓存按格式能力正确分流。否则中间缓存可能把先生成的 <code>WebP</code> 响应复用给不支持它的客户端；采用该方案还需确认 <code>CDN</code> 的缓存键确实遵循该响应头。</p>\\n</li>\\n<li>\\n<p><strong>旧项目用 <code>Image</code> 加载一张 <code>1px</code> 的 <code>WebP</code>，上线后首屏图片先是 <code>JPEG</code> 再闪成 <code>WebP</code>；你怎么处理？</strong></p>\\n<p>闪动来自异步检测完成后的地址替换，继续优化检测代码无法消除这个时序。首屏内容应改用 <code>&lt;picture&gt;</code>，或交给服务端按 <code>Accept</code> 协商；若遗留场景必须检测，应缓存结果，但首次访问仍存在等待或替换的代价。</p>\\n</li>\\n<li>\\n<p><strong>营销页主图和卡片背景都要格式降级，团队要求统一用一套 <code>JS</code> 检测；你会接受吗？</strong></p>\\n<p>不建议为统一而强行使用 <code>JS</code>：主图适合 <code>&lt;picture&gt;</code>，背景图可用 <code>image-set()</code>，两者都让浏览器参与选择。只有现有背景实现无法改造时才保留检测逻辑；统一代码表面简单，却会引入异步时序、缓存维护和首屏闪动风险。</p>\\n</li>\\n<li>\\n<p><strong>前端已经用 <code>&lt;picture&gt;</code>，平台组又提出由 <code>CDN</code> 按 <code>Accept</code> 转码；两套方案该怎样取舍？</strong></p>\\n<p>资源格式和地址由页面明确管理时，<code>&lt;picture&gt;</code> 的降级关系更直观；希望业务只维护一个地址时，可由 <code>CDN</code> 根据 <code>Accept</code> 协商。后者必须正确配置 <code>Vary: Accept</code> 和缓存分流，前者则需要维护多份资源及 <code>srcset</code> 声明。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_18-css加载问题\\"><a class=\\"header-anchor\\" href=\\"#_18-css加载问题\\">#</a> 18 CSS加载问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>CSS</code> <strong>不阻塞</strong> <code>DOM</code> 解析 —— 两棵树并行构建</li>\\n<li><code>CSS</code> <strong>阻塞渲染</strong> —— 渲染树 = DOM + CSSOM，样式没就绪就不画，避免闪现无样式内容</li>\\n<li><code>CSS</code> <strong>阻塞后面 <code>JS</code> 的执行</strong> —— 脚本可能读 <code>getComputedStyle</code>，必须等样式就绪</li>\\n<li>所以 <code>CSS</code> 体积直接决定 <code>FCP</code>：关键样式内联，其余用 <code>media=&quot;print&quot; onload</code> 异步化</li>\\n<li><code>Coverage</code> 面板能看首屏实际用了多少样式，很多项目不到 20%</li>\\n</ul>\\n<p><strong><code>CSS</code> 不会阻塞 <code>DOM</code> 解析，但会阻塞页面渲染，也可能阻塞它后面的 <code>JavaScript</code> 执行。</strong> 浏览器可以并行构建 <code>DOM</code> 和 <code>CSSOM</code>，但只有两者就绪后才能生成渲染树，否则提前绘制会出现无样式内容闪烁。后续脚本可能调用 <code>getComputedStyle()</code>，因此也要等待前面的样式完成解析。工程上我一般内联首屏关键样式、按媒体拆分或异步加载非关键样式，并用 <code>Coverage</code> 清理没有用到的规则。</p>\\n</section>\\n<p>三条结论，背后是同一套渲染流水线：</p>\\n<p><strong>① <code>CSS</code> 不阻塞 <code>DOM</code> 解析。</strong>\\n<code>HTML</code> 解析器遇到 <code>&lt;link rel=&quot;stylesheet&quot;&gt;</code> 会继续往下构建 DOM 树，<code>CSS</code> 在另一条线上并行下载解析成 <code>CSSOM</code>。两棵树互不干扰。</p>\\n<p><strong>② <code>CSS</code> 阻塞渲染。</strong>\\n渲染树 = DOM + CSSOM。<code>CSSOM</code> 没就绪，浏览器就不知道每个节点该长什么样，<strong>只能不画</strong>。如果先画出无样式的内容再套样式，用户会看到一次刺眼的闪烁（FOUC），所以浏览器宁可白屏等着。</p>\\n<p>这意味着 <strong><code>CSS</code> 的体积和加载速度直接决定 <code>FCP</code></strong>。一个 500KB 的样式表在弱网下能把首屏拖后好几秒，而这段时间页面是纯白的。</p>\\n<p><strong>③ <code>CSS</code> 阻塞后面 <code>JS</code> 的执行。</strong>\\n因为脚本可能调用 <code>getComputedStyle</code> 读取样式，为了保证读到的值正确，浏览器必须等前面的 <code>CSS</code> 解析完才执行脚本。所以下面这段里，<code>app.js</code> 要等 <code>big.css</code> 下完才跑：</p>\\n<pre class=\\"language-html\\"><code>&lt;link rel=&quot;stylesheet&quot; href=&quot;big.css&quot;&gt;\\n&lt;script src=&quot;app.js&quot;&gt;&lt;/script&gt;   &lt;!-- 被 big.css 拖住 --&gt;\\n</code></pre>\\n<p><strong>由此得出的优化策略：</strong></p>\\n<ul>\\n<li>\\n<p><strong>关键 <code>CSS</code> 内联</strong>：把首屏用到的那部分样式直接写进 <code>&lt;style&gt;</code>，首屏不再等网络往返。</p>\\n</li>\\n<li>\\n<p><strong>非关键 <code>CSS</code> 异步化</strong>：</p>\\n<pre class=\\"language-html\\"><code>&lt;link rel=&quot;stylesheet&quot; href=&quot;rest.css&quot; media=&quot;print&quot; onload=&quot;this.media=&#39;all&#39;&quot;&gt;\\n</code></pre>\\n<p><code>media=&quot;print&quot;</code> 让它不参与渲染阻塞，加载完再切回 <code>all</code>。</p>\\n</li>\\n<li>\\n<p><strong>按媒体拆分</strong>：<code>&lt;link media=&quot;(min-width: 1024px)&quot;&gt;</code> 这样的样式表在小屏上不阻塞渲染（仍会下载，但优先级低）。</p>\\n</li>\\n<li>\\n<p><strong>删无用样式</strong>：<code>DevTools</code> 的 <code>Coverage</code> 面板能看到首屏实际用了百分之多少，很多项目这个数字不到 20%。</p>\\n</li>\\n</ul>\\n<p><strong>顺带说清 <code>JS</code> 那边</strong>：<code>&lt;script&gt;</code> 会阻塞 <code>HTML</code> 解析（因为可能有 <code>document.write</code>），加 <code>defer</code> 或 <code>async</code> 就不会。所以标准的头部写法是：关键 <code>CSS</code> 内联 + 其余 <code>CSS</code> 正常 <code>link</code> + 所有脚本加 <code>defer</code>。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>同事说 <code>&lt;link rel=&quot;stylesheet&quot;&gt;</code> 会让后面的 <code>HTML</code> 停止解析，所以想把所有样式移到 <code>body</code> 底部；你怎么纠正？</strong></p>\\n<p>样式表不会阻塞 <code>DOM</code> 解析，浏览器会继续构建 <code>DOM</code>，同时下载并解析 <code>CSSOM</code>；真正被阻塞的是首次渲染。移到底部可能让页面先以无样式状态出现再重排，造成 <code>FOUC</code>，并不是合理的首屏优化。</p>\\n</li>\\n<li>\\n<p><strong>资讯站弱网下首屏长期白屏，主样式文件很大且首屏只用到少量规则，你会怎样拆加载路径？</strong></p>\\n<p>把首屏必需的关键 <code>CSS</code> 内联，消除这部分网络往返，再把非关键样式异步加载或按媒体条件拆分。可用 <code>Coverage</code> 核对首屏实际使用范围并删除无用规则；关键样式过度内联会增大 <code>HTML</code>，需要控制边界。</p>\\n</li>\\n<li>\\n<p><strong>桌面端专用样式通过 <code>&lt;link media=&quot;(min-width: 1024px)&quot;&gt;</code> 引入，移动端是否就完全不会请求它？</strong></p>\\n<p>不能把媒体条件理解成不请求：在不匹配的小屏上，它通常仍会下载，只是不阻塞当前渲染且优先级较低。若目标是彻底减少传输，还需要构建或资源层面的拆分；仅加 <code>media</code> 主要改变渲染阻塞关系。</p>\\n</li>\\n<li>\\n<p><strong>页面里 <code>big.css</code> 后紧跟同步的 <code>app.js</code>，接口已经返回但组件迟迟没初始化；你如何定位这条依赖链？</strong></p>\\n<p>先在网络和性能时间线确认 <code>app.js</code> 是否等待前置 <code>big.css</code> 完成，因为脚本可能读取 <code>getComputedStyle</code>，浏览器必须保证样式可用。再给脚本采用合适的 <code>defer</code>，并压缩或拆分样式；只优化接口不会解除这条阻塞链。</p>\\n</li>\\n<li>\\n<p><strong>团队在“所有样式正常 <code>&lt;link&gt;</code>”和“全部内联”之间争论，首屏页面应该选哪种方案？</strong></p>\\n<p>应只内联首屏关键 <code>CSS</code>，其余样式保留外链并按关键程度加载，而不是选择任一极端。全部外链会增加首屏等待，全部内联则放大 <code>HTML</code>、削弱独立缓存价值；拆分质量取决于首屏边界是否稳定。</p>\\n</li>\\n<li>\\n<p><strong><code>CSS</code>、同步 <code>&lt;script&gt;</code> 和渲染树同时出现在一道排障题里，你会让候选人说清哪三种阻塞关系？</strong></p>\\n<p><code>CSS</code> 不阻塞 <code>DOM</code> 解析，但会等待形成 <code>CSSOM</code>，从而阻塞渲染；前置样式还会阻塞后续脚本执行。普通同步 <code>&lt;script&gt;</code> 会阻塞 <code>HTML</code> 解析，使用 <code>defer</code> 或 <code>async</code> 可避免解析阶段阻塞，但两者执行时序并不等价。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_19-文字单超出显示省略号\\"><a class=\\"header-anchor\\" href=\\"#_19-文字单超出显示省略号\\">#</a> 19 文字单超出显示省略号</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>单行三件套：<code>overflow: hidden</code> + <code>white-space: nowrap</code> + <code>text-overflow: ellipsis</code></li>\\n<li>⚠️ 必须有宽度约束才生效；在 <code>flex</code> 子项里要加 <code>min-width: 0</code>，否则长文本撑破容器而不是省略</li>\\n<li>多行用 <code>display: -webkit-box</code> + <code>-webkit-box-orient: vertical</code> + <code>-webkit-line-clamp: n</code>，现在各家浏览器都支持</li>\\n<li>多行方案不要和 <code>text-overflow</code> 混用，两套机制会互相干扰</li>\\n<li>&quot;超出才显示 tooltip&quot; 要用 <code>JS</code> 比 <code>scrollWidth &gt; clientWidth</code>，且必须等字体加载完再判断</li>\\n</ul>\\n<p><strong>单行省略要同时设置 <code>overflow: hidden</code>、<code>white-space: nowrap</code> 和 <code>text-overflow: ellipsis</code>，并保证元素存在宽度约束。</strong> 如果文本位于 <code>flex</code> 子项中，通常还要加 <code>min-width: 0</code>，否则子项会被长文本撑开。多行场景使用 <code>display: -webkit-box</code>、<code>-webkit-box-orient: vertical</code> 和 <code>-webkit-line-clamp</code>，不要再混用 <code>text-overflow</code>。需要仅在溢出时显示提示，可用 <code>scrollWidth</code> 与 <code>clientWidth</code> 比较，但应等字体加载完成后再判断。</p>\\n</section>\\n<p><strong>单行省略 —— 三条属性缺一不可：</strong></p>\\n<pre class=\\"language-css\\"><code>.ellipsis {\\n  overflow: hidden;        /* 裁掉溢出 */\\n  white-space: nowrap;     /* 不换行，才会溢出 */\\n  text-overflow: ellipsis; /* 溢出处显示省略号 */\\n}\\n</code></pre>\\n<p>注意它必须有一个<strong>确定的宽度约束</strong> —— 元素本身定宽，或者作为 <code>flex</code> 子项时要加 <code>min-width: 0</code>（因为 <code>flex</code> 子项默认 <code>min-width: auto</code> 不允许收缩，长文本会把容器撑破而不是省略）。这是&quot;省略号不生效&quot;最常见的原因。</p>\\n<p><strong>多行省略 —— <code>-webkit-line-clamp</code>：</strong></p>\\n<pre class=\\"language-css\\"><code>.clamp-3 {\\n  display: -webkit-box;\\n  -webkit-box-orient: vertical;\\n  -webkit-line-clamp: 3;\\n  overflow: hidden;\\n}\\n</code></pre>\\n<p>虽然带 <code>-webkit-</code> 前缀，但它今天在所有主流浏览器（含 Firefox）都支持，可以放心用。新标准 <code>line-clamp</code> 也已经开始落地，将来可以去掉前缀。</p>\\n<p><strong>三个坑：</strong></p>\\n<ol>\\n<li><code>display: -webkit-box</code> 会改变元素的布局模式，里面的子元素排列可能和预期不一样，通常要在外面再包一层。</li>\\n<li>它对 <code>padding-bottom</code> 的处理有历史问题，有些浏览器上第四行会从下边距里露出一角，稳妥做法是把纵向内边距挪到外层容器。</li>\\n<li>和 <code>text-overflow: ellipsis</code> <strong>不要混用</strong>，两套机制会互相干扰。</li>\\n</ol>\\n<p><strong>纯 <code>CSS</code> 的兼容兜底方案</strong>（用伪元素模拟省略号）：</p>\\n<pre class=\\"language-css\\"><code>.fallback {\\n  position: relative;\\n  overflow: hidden;\\n  line-height: 1.4em;\\n  height: 4.2em;          /* 3 行 */\\n}\\n.fallback::after {\\n  position: absolute;\\n  right: 0;\\n  bottom: 0;\\n  padding-left: 2em;\\n  background: linear-gradient(90deg, transparent, #fff 45%);\\n  content: &#39;...&#39;;\\n}\\n</code></pre>\\n<p>缺点很明显：<strong>文字没满三行时省略号也会显示</strong>。所以现在基本不用了，了解它是为了看懂老代码。</p>\\n<p><strong>什么时候该用 <code>JS</code></strong>：需要&quot;超出才显示 tooltip&quot;时，得先判断有没有真的溢出 —— 比较 <code>el.scrollWidth &gt; el.clientWidth</code>（单行）或 <code>scrollHeight &gt; clientHeight</code>（多行）。注意这个判断要在字体加载完成后做，否则会误判；而且它是布局属性读取，别放在高频回调里。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>表格标题只写了 <code>text-overflow: ellipsis</code>，长文本仍完整溢出单元格；缺失的约束是什么？</strong></p>\\n<p>单行省略还需要 <code>overflow: hidden</code> 和 <code>white-space: nowrap</code>，并且元素必须受到确定的宽度约束。<code>text-overflow</code> 只决定溢出位置如何呈现，不负责制造溢出或裁剪内容；容器可无限扩张时自然不会出现省略号。</p>\\n</li>\\n<li>\\n<p><strong>“头像 + 自适应标题 + 按钮”的 <code>flex</code> 行里，标题三件套齐全却把按钮挤出屏幕，你会改哪一条？</strong></p>\\n<p>给承载标题的 <code>flex</code> 子项加 <code>min-width: 0</code>，允许它缩小到内容最小宽度以下，省略机制才有机会生效。默认 <code>min-width: auto</code> 会让长文本撑开容器；同时仍需确认祖先链上存在实际宽度约束。</p>\\n</li>\\n<li>\\n<p><strong>卡片摘要要固定三行，组件内部还有图标和标签子元素；直接给根节点加 <code>-webkit-line-clamp</code> 有什么风险？</strong></p>\\n<p>三行截断需要 <code>display: -webkit-box</code>、<code>-webkit-box-orient: vertical</code>、<code>-webkit-line-clamp: 3</code> 和 <code>overflow: hidden</code>。它会改变根节点布局模式，可能影响内部子元素排列；更稳妥的是增加专门的文本包裹层承担截断。</p>\\n</li>\\n<li>\\n<p><strong>多行摘要偶尔从底部露出第四行的一角，样式同时设置了三行截断和 <code>padding-bottom</code>；你怎样修？</strong></p>\\n<p>先把纵向内边距移到外层容器，避免 <code>-webkit-line-clamp</code> 对 <code>padding-bottom</code> 的历史处理问题暴露下一行。再确认没有混用 <code>text-overflow: ellipsis</code>，因为单行和多行截断机制不应叠加；修复后要覆盖不同字体与字号验证。</p>\\n</li>\\n<li>\\n<p><strong>产品要求只有真实截断时才展示 <code>tooltip</code>，列表有很多标题且窗口频繁变化；你会怎样检测？</strong></p>\\n<p>单行比较 <code>scrollWidth &gt; clientWidth</code>，多行比较 <code>scrollHeight &gt; clientHeight</code>，并在 <code>document.fonts.ready</code> 后测量。尺寸变化时可借助 <code>ResizeObserver</code> 重新判断，避免在 <code>scroll</code> 或 <code>resize</code> 高频回调中反复读取布局属性而触发同步布局。</p>\\n</li>\\n<li>\\n<p><strong>老项目用绝对定位的 <code>::after</code> 永远画 <code>...</code>，设计投诉短文案也有省略号；是继续修补还是换方案？</strong></p>\\n<p>应优先换成原生单行三件套或 <code>-webkit-line-clamp</code>，因为伪元素方案无法天然知道文字是否真的超过限定行数。渐变遮罩只能改善视觉，不能修正短文本误显示；若必须兼容遗留环境，就需要接受误差或引入测量逻辑。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_20-页面变灰\\"><a class=\\"header-anchor\\" href=\\"#_20-页面变灰\\">#</a> 20 页面变灰</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>一行 <code>html { filter: grayscale(100%) }</code> 即可，参数支持百分比或 <code>0~1</code></li>\\n<li>加在 <code>html</code> 比 <code>body</code> 更彻底 —— 否则 <code>body</code> 之外的背景区域不会变灰</li>\\n<li>⚠️ <strong>最大的坑</strong>：<code>filter</code> 会创建包含块，导致页面里所有 <code>position: fixed</code> 改为相对该元素定位而不是视口，固定头部/悬浮按钮会错位</li>\\n<li>整页滤镜会强制提层，低端机上滚动和动画明显变卡</li>\\n<li>工程上做成服务端配置开关，别硬编码发版 —— 这类需求生效和撤销都要求分钟级</li>\\n</ul>\\n<p><strong>整页置灰可以给 <code>html</code> 设置 <code>filter: grayscale(100%)</code>，其中参数也可以写成 <code>0</code> 到 <code>1</code> 的小数。</strong> 放在 <code>html</code> 上比放在 <code>body</code> 上覆盖得更完整，页面背景、视频、<code>iframe</code> 和 <code>canvas</code> 等内容都会一起处理。需要注意，<code>filter</code> 会创建层叠上下文和包含块，可能让 <code>position: fixed</code> 元素不再相对视口定位，同时整页合成也有性能代价。工程上更适合通过 <code>html.is-mourning</code> 这类状态类和服务端配置控制，方便快速启用、调节和撤销。</p>\\n</section>\\n<p>国家公祭日、重大纪念日时网站整体置灰，是一个很常见的需求。</p>\\n<p><strong>基础写法：</strong></p>\\n<pre class=\\"language-css\\"><code>html {\\n  filter: grayscale(100%);\\n}\\n</code></pre>\\n<p>参数可以是百分比或 <code>0~1</code> 的小数，<code>grayscale(1)</code> 等价于 <code>100%</code>。</p>\\n<p><strong>为什么建议加在 <code>html</code> 而不是 <code>body</code>？</strong>\\n<code>body</code> 之外的区域（比如 <code>html</code> 的背景色、某些浏览器的滚动条区域）不会被覆盖，加在 <code>html</code> 上更彻底。</p>\\n<p><strong>四个必须知道的坑：</strong></p>\\n<p><strong>① <code>filter</code> 会创建层叠上下文，还会创建包含块。</strong>\\n这意味着页面里所有 <code>position: fixed</code> 的元素会<strong>改为相对 <code>html</code> 定位</strong>而不是视口 —— 固定头部、悬浮按钮、全屏弹窗可能全部错位。这是置灰上线后最常见的事故。</p>\\n<p><strong>② 会强制提层，内存和性能有代价。</strong>\\n整页做滤镜相当于给整个页面加了一层合成处理，低端机上滚动和动画会明显变卡。</p>\\n<p><strong>③ 视频、<code>iframe</code>、<code>canvas</code> 也会被置灰</strong>，通常这正是想要的效果；但如果有不该置灰的元素（比如某些必须保持原色的标识），可以单独用 <code>filter: grayscale(0)</code> 反转 —— 不过这只在它不在滤镜元素内部时才有效，实际上很难局部豁免。</p>\\n<p><strong>④ 打印时不生效</strong>，需要在 <code>@media print</code> 里单独处理。</p>\\n<p><strong>工程上的做法：</strong></p>\\n<pre class=\\"language-css\\"><code>/* 通过一个类名控制，方便开关和灰度调节 */\\nhtml.is-mourning {\\n  filter: grayscale(var(--gray, 100%));\\n}\\n</code></pre>\\n<p>上线方式建议做成<strong>服务端下发的配置开关</strong>而不是硬编码发版 —— 这类需求通常时间紧、生效和撤销都要快，走配置能在几分钟内切换，不用等一次完整的构建部署。</p>\\n<p><strong>兼容性补充</strong>：老 <code>IE</code> 用的是 <code>filter: progid:DXImageTransform.Microsoft.BasicImage(grayscale=1)</code>，现在完全不需要了。另外还有一个 <code>-webkit-filter</code> 前缀，现代浏览器也不再需要。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>运营把 <code>filter: grayscale(100%)</code> 加在 <code>body</code>，结果页面边缘和部分浏览器区域仍有颜色；你会把规则放在哪里？</strong></p>\\n<p>应把滤镜放到 <code>html</code>，它比 <code>body</code> 覆盖得更完整，包括 <code>html</code> 自身背景等区域。<code>grayscale(1)</code> 与 <code>grayscale(100%)</code> 等价；但覆盖范围扩大也意味着整页都进入滤镜处理，必须同步验证性能和定位副作用。</p>\\n</li>\\n<li>\\n<p><strong>公祭日置灰上线后，固定头部、悬浮按钮和全屏弹窗开始错位或随页面滚动；根因是什么？</strong></p>\\n<p>非 <code>none</code> 的 <code>filter</code> 会创建层叠上下文和包含块，使后代 <code>position: fixed</code> 元素改为相对滤镜元素定位，而不再相对视口。应先盘点所有固定元素并调整置灰边界或挂载位置；仅提高 `z-index`` 通常解决不了包含块变化。</p>\\n</li>\\n<li>\\n<p><strong>品牌标识必须保留彩色，但它位于已经置灰的 <code>html</code> 内，同事给它加 <code>filter: grayscale(0)</code>；为什么可能无效？</strong></p>\\n<p>祖先滤镜作用于合成后的后代整体，内部元素再设 <code>grayscale(0)</code> 不能恢复已经丢失的颜色信息。要保留原色，通常需要让该标识位于滤镜元素之外或重新设计置灰范围；这会增加布局、层级和挂载管理成本。</p>\\n</li>\\n<li>\\n<p><strong>低端设备开启整站置灰后滚动和动画明显变卡，你会先检查什么，而不是继续加兼容前缀？</strong></p>\\n<p>先确认整页滤镜带来的合成层和内存压力，因为整页需要额外的滤镜合成处理，低端设备更容易受影响。再减少置灰范围、动画或同时存在的重型内容；现代浏览器通常不需要 <code>-webkit-filter</code>，加前缀不会解决性能问题。</p>\\n</li>\\n<li>\\n<p><strong>需求要求当天零点生效、次日快速撤销，团队准备各发一次版本；你会怎样设计开关？</strong></p>\\n<p>使用类似 <code>html.is-mourning</code> 的类名控制滤镜，并由服务端配置决定是否下发，便于快速生效和撤销。还可通过 <code>--gray</code> 调节灰度；配置化降低发版依赖，但上线前仍要验证缓存刷新、固定定位和低端机性能。</p>\\n</li>\\n<li>\\n<p><strong>验收只检查了普通网页，打印稿仍保持彩色；这属于浏览器故障还是方案边界？</strong></p>\\n<p>这是方案边界，屏幕上的整页 <code>filter</code> 不应被假定会自动作用于打印输出。需要在 <code>@media print</code> 中单独定义打印样式并实际预览；同时确认业务究竟要求打印置灰，避免把纪念日展示规则误扩展到正式票据或材料。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_21-css中可继承的属性\\"><a class=\\"header-anchor\\" href=\\"#_21-css中可继承的属性\\">#</a> 21 CSS中可继承的属性</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>可继承的都是「文字相关且不影响布局」的：<code>font</code> 全家、<code>color</code>、<code>line-height</code>、<code>text-align</code>、<code>letter-spacing</code>、<code>list-style</code>、<code>visibility</code>、<code>cursor</code></li>\\n<li>不可继承的是盒子和布局相关的：<code>width</code>/<code>height</code>、<code>margin</code>/<code>padding</code>/<code>border</code>、<code>background</code>、<code>display</code>、<code>position</code></li>\\n<li><strong>自定义属性 <code>--x</code> 是可继承的</strong>，这正是 <code>CSS</code> 变量做主题系统的基础</li>\\n<li>表单控件不继承字体，重置样式里要写 <code>input, button, select, textarea { font: inherit }</code></li>\\n<li>四个全局关键字：<code>inherit</code> / <code>initial</code> / <code>unset</code>（继承的取 inherit、不继承的取 initial）/ <code>revert</code></li>\\n</ul>\\n<p><strong><code>CSS</code> 中通常可继承的是字体、文本、列表等不直接影响盒子布局的属性，例如 <code>font</code>、<code>color</code>、<code>line-height</code> 和 <code>text-align</code>。</strong> 像 <code>width</code>、<code>margin</code>、<code>background</code>、<code>position</code> 这类盒子或布局属性默认不继承，否则父元素的样式会层层影响子元素。自定义属性也会继承，所以很适合用 <code>--brand</code> 之类的变量做主题传递。表单控件常被浏览器样式指定字体，我一般会显式写 <code>font: inherit</code>；另外 <code>line-height</code> 建议用无单位数字，让子元素按自己的字号计算。</p>\\n</section>\\n<p><strong>可继承的属性有一个共同规律：都是&quot;文字相关&quot;和&quot;不影响布局&quot;的。</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>分类</th>\\n<th>属性</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>字体</td>\\n<td><code>font</code> 全家（<code>font-family</code>、<code>font-size</code>、<code>font-weight</code>、<code>font-style</code>…）</td>\\n</tr>\\n<tr>\\n<td>文本</td>\\n<td><code>color</code>、<code>line-height</code>、<code>letter-spacing</code>、<code>word-spacing</code>、<code>text-align</code>、<code>text-indent</code>、<code>text-transform</code>、<code>white-space</code>、<code>direction</code></td>\\n</tr>\\n<tr>\\n<td>列表</td>\\n<td><code>list-style</code> 全家</td>\\n</tr>\\n<tr>\\n<td>表格</td>\\n<td><code>border-collapse</code>、<code>border-spacing</code>、<code>caption-side</code></td>\\n</tr>\\n<tr>\\n<td>其他</td>\\n<td><code>visibility</code>、<code>cursor</code>、<code>quotes</code>、以及所有<strong>自定义属性</strong>（<code>--x</code>）</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>不可继承的是布局和视觉盒子相关的</strong>：<code>width</code>/<code>height</code>、<code>margin</code>/<code>padding</code>/<code>border</code>、<code>background</code>、<code>display</code>、<code>position</code>、<code>float</code>、<code>overflow</code>、<code>z-index</code> 等。</p>\\n<p><strong>为什么这么分？</strong> 想象一下如果 <code>border</code> 可继承 —— 给容器加个边框，里面每个元素都套一层框，页面立刻没法看。而字体和颜色天然是&quot;整段文字统一&quot;的，继承正好符合直觉。</p>\\n<p><strong>几个实际会用到的点：</strong></p>\\n<p><strong>① 表单控件不继承字体，必须显式要回来。</strong></p>\\n<pre class=\\"language-css\\"><code>input, button, select, textarea { font: inherit; }\\n</code></pre>\\n<p>它们的字体是浏览器 UA 样式表直接指定的，不走继承，不写这一行表单和正文永远对不齐。这是每套重置样式里都有的一条。</p>\\n<p><strong>② 自定义属性是可继承的，这正是主题系统的基础。</strong></p>\\n<pre class=\\"language-css\\"><code>:root { --brand: #1677ff; }\\n.btn { color: var(--brand); }   /* 任意深度都能读到 */\\n</code></pre>\\n<p><strong>③ 四个全局关键字，可以手动控制继承行为：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>关键字</th>\\n<th>含义</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>inherit</code></td>\\n<td>强制继承父元素的计算值（可用于本不继承的属性）</td>\\n</tr>\\n<tr>\\n<td><code>initial</code></td>\\n<td>回到 <code>CSS</code> 规范定义的初始值</td>\\n</tr>\\n<tr>\\n<td><code>unset</code></td>\\n<td>可继承的属性取 <code>inherit</code>，不可继承的取 <code>initial</code></td>\\n</tr>\\n<tr>\\n<td><code>revert</code></td>\\n<td>回到浏览器 UA 样式表的值</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><code>all: unset</code> 可以一次性把元素的所有样式清干净，写&quot;完全自定义的按钮&quot;时比逐条覆盖省事得多（但要记得补回 <code>cursor: pointer</code> 和焦点样式）。</p>\\n<p><strong>④ <code>line-height</code> 的继承有个特殊之处</strong>，值得单独记：写数字（<code>1.5</code>）继承的是<strong>倍数</strong>，子元素按自己的字号算；写百分比或具体值（<code>150%</code>、<code>24px</code>）继承的是<strong>计算后的结果</strong>，子元素字号变大时会被挤在一起。所以 <code>line-height</code> 一律建议写无单位的数字。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>设计稿要求文章容器统一加浅灰背景和蓝色文字，开发只在父级写了 <code>background</code> 与 <code>color</code>，结果子元素文字变蓝、背景却没有逐层铺开，这是浏览器漏掉继承了吗？</strong></p>\\n<p>不是漏掉继承，而是 <code>color</code> 默认可继承，<code>background</code> 默认不可继承。文字类属性适合沿后代统一传播，盒子视觉属性若也自动传播，会让每层元素重复绘制背景；确需一致时应显式设置或使用 <code>inherit</code>，但要评估嵌套组件是否也会被覆盖。</p>\\n</li>\\n<li>\\n<p><strong>后台系统在 <code>body</code> 上统一设置了 <code>font-family</code>，正文已经生效，但几十个 <code>input</code>、<code>select</code> 和 <code>button</code> 仍显示系统字体，你会怎样一次修正？</strong></p>\\n<p>应在基础样式中为表单控件补上 <code>input, button, select, textarea { font: inherit; }</code>。这些控件通常被浏览器 <code>UA</code> 样式直接指定字体，因而不会表现为从 <code>body</code> 继承；全局修正会影响所有控件，第三方组件若依赖自己的字体需要提高选择器针对性。</p>\\n</li>\\n<li>\\n<p><strong>主题组件把 <code>--brand</code> 定义在页面容器上，弹窗按钮在多层普通 <code>div</code> 中能取到颜色，但通过 <code>Portal</code> 挂到 <code>body</code> 后变量失效，你怎么解释并处理？</strong></p>\\n<p>自定义属性会沿真实的 <code>DOM</code> 祖先链继承，<code>Portal</code> 改变挂载位置后，弹窗不再是主题容器的后代。可把变量提升到 <code>:root</code>、同步到弹窗挂载节点，或让组件库使用主题专属容器；提升到全局最省事，但会削弱多主题区域之间的隔离。</p>\\n</li>\\n<li>\\n<p><strong>富文本区域父级写了 <code>font-size: 16px; line-height: 150%</code>，其中标题改成 <code>32px</code> 后上下文字挤在一起；检查时为什么要先改 <code>line-height</code>，而不是继续加标题外边距？</strong></p>\\n<p><code>150%</code> 会先按父级字号计算为具体行高，子元素继承后即使字号变大，仍可能沿用该计算结果。将父级改为 <code>line-height: 1.5</code>，子元素便按自己的字号计算行高；外边距只能遮掩段落间距，无法修复标题内部行盒过窄的问题。</p>\\n</li>\\n<li>\\n<p><strong>自定义按钮使用 <code>all: unset</code> 清掉浏览器样式后，尺寸正常却没有手型光标和键盘焦点提示；你会保留这套方案还是改成逐项覆盖？</strong></p>\\n<p><code>all: unset</code> 会让可继承属性按继承处理、不可继承属性回到初始值，因此交互提示也可能被一并清除。若继续使用，必须显式补回 <code>cursor</code>、焦点样式及所需布局属性；组件状态多或可访问性要求高时，逐项重置更啰嗦，但更容易审计遗漏。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_22-常规流-文档流-是个怎样的排列关系\\"><a class=\\"header-anchor\\" href=\\"#_22-常规流-文档流-是个怎样的排列关系\\">#</a> 22 常规流(文档流)是个怎样的排列关系</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>常规流是默认排版规则：块级元素<strong>从上到下</strong>各占一行，行内内容在一行内<strong>从左到右</strong>排、排满换行</li>\\n<li>块级排版有外边距重叠（相邻上下 <code>margin</code> 取较大值），行内排版有空白间隙和基线对齐两个坑</li>\\n<li>行内元素的 <code>width</code>/<code>height</code> 无效，上下 <code>margin</code>/<code>padding</code> 不撑开行高</li>\\n<li>脱离常规流：<code>float</code>（部分脱离，文字仍环绕）、<code>absolute</code>/<code>fixed</code>（完全脱离不占位）；<code>relative</code> <strong>不脱流</strong>，原位仍保留</li>\\n<li><code>flex</code>/<code>grid</code> 子项进入各自的格式化上下文，所以 <code>float</code> 失效、<code>margin</code> 不重叠</li>\\n</ul>\\n<p><strong>常规流就是浏览器默认的排版方式：块级盒子从上到下排列，行内内容在一行中依次排布，空间不足时换行。</strong> 块级元素通常独占一行，并可能发生上下外边距重叠；行内元素则不能靠 <code>width</code>、<code>height</code> 控制尺寸，还要留意空白字符和基线对齐。<code>absolute</code>、<code>fixed</code> 会完全脱流，<code>float</code> 只是部分脱流，而 <code>relative</code> 仍保留原来的占位。进入 <code>flex</code> 或 <code>grid</code> 后，子项遵循新的格式化规则，因此浮动会失效，外边距也不会重叠。</p>\\n</section>\\n<p>常规流（normal flow）是浏览器<strong>默认的排版规则</strong> —— 不设 <code>float</code>、不设 <code>position: absolute/fixed</code> 时，元素就按这套规则排。</p>\\n<p>它内部又分两种格式化上下文：</p>\\n<p><strong>① 块级格式化上下文（BFC）—— 管块级盒子</strong></p>\\n<p>块级元素（<code>div</code>、<code>p</code>、<code>h1</code>）<strong>从上到下逐行排列</strong>，每个独占一行，宽度默认撑满父容器（<code>width: auto</code> 时），高度由内容决定。相邻的上下 <code>margin</code> 会<strong>重叠</strong>（取较大值，而不是相加），这是块级排版的一个专属特性。</p>\\n<p><strong>② 行内格式化上下文（IFC）—— 管行内内容</strong></p>\\n<p>行内元素（<code>span</code>、<code>a</code>、文本）在<strong>一行内从左到右排</strong>（<code>direction: rtl</code> 时从右到左），排满换行。行内盒子的 <code>width</code>/<code>height</code> 无效，上下 <code>margin</code>/<code>padding</code> 不影响行高（会溢出但不撑开行），左右的则有效。</p>\\n<p><strong>行内排版有两个经典坑：</strong></p>\\n<ul>\\n<li><strong>空白间隙</strong>：<code>&lt;span&gt;A&lt;/span&gt; &lt;span&gt;B&lt;/span&gt;</code> 之间的换行和空格会被当成一个空格渲染，导致元素之间莫名多出几像素。这是 <code>inline-block</code> 布局的老问题，解决办法是父元素 <code>font-size: 0</code>、去掉标签间空白，或者干脆改用 <code>flex</code>。</li>\\n<li><strong>基线对齐</strong>：行内元素默认按 <code>baseline</code> 对齐，图片下面会莫名多出几像素空隙 —— 那是给字母 <code>g</code>、<code>p</code> 的下伸部分预留的位置。修法是 <code>img { vertical-align: middle }</code> 或 <code>display: block</code>。</li>\\n</ul>\\n<p><strong>脱离常规流的三种方式：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>方式</th>\\n<th style=\\"text-align:center\\">是否脱流</th>\\n<th style=\\"text-align:center\\">是否占位</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>float: left/right</code></td>\\n<td style=\\"text-align:center\\">部分脱离</td>\\n<td style=\\"text-align:center\\">行内内容仍会环绕它</td>\\n</tr>\\n<tr>\\n<td><code>position: absolute/fixed</code></td>\\n<td style=\\"text-align:center\\">完全脱离</td>\\n<td style=\\"text-align:center\\">完全不占位</td>\\n</tr>\\n<tr>\\n<td><code>position: relative</code></td>\\n<td style=\\"text-align:center\\"><strong>不脱离</strong></td>\\n<td style=\\"text-align:center\\">仍占原位，只是视觉偏移</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><code>relative</code> 经常被误以为脱流 —— 它只是把自己&quot;画&quot;到别处，原来的位置仍然保留，后面的元素不会补上来。</p>\\n<p><strong>现代补充</strong>：<code>flex</code> 和 <code>grid</code> 容器的子项既不属于常规流的块级排版，也不是脱流，而是进入了各自的<strong>弹性/网格格式化上下文</strong>。这也是为什么 <code>flex</code> 子项的 <code>float</code> 会失效、<code>margin</code> 不会重叠 —— 它们已经不按常规流的规则玩了。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>资讯页里两个普通 <code>div</code> 都没有设置宽度，却从上到下各占一行；把它们改成 <code>span</code> 后又挤到同一行，这能简单归因于标签不同吗？</strong></p>\\n<p>现象来自它们在常规流中参与的格式化方式不同，而不只是标签名称。块级盒子通常纵向排列，<code>width: auto</code> 时撑满可用宽度；行内内容则在行内格式化上下文中按书写方向排列并在空间不足时换行，修改 <code>display</code> 也能改变这种行为。</p>\\n</li>\\n<li>\\n<p><strong>文章列表给相邻两个区块分别设置 <code>margin-bottom: 24px</code> 和 <code>margin-top: 16px</code>，产品却只看到约 <code>24px</code> 间距；继续把两者相加补偿合理吗？</strong></p>\\n<p>不合理，这符合常规流中相邻块级盒子垂直外边距重叠的表现，结果通常取较大值而非简单相加。应先确认两者确实处于会发生重叠的块级排版关系，再统一由单侧外边距控制节奏；若改成 <code>flex</code> 或 <code>grid</code>，原有重叠假设将不再成立。</p>\\n</li>\\n<li>\\n<p><strong>相册用多个 <code>inline-block</code> 缩略图排列，模板换行后每张图之间多出几像素，最后一列因此掉到下一行；在不能改 <code>DOM</code> 输出的情况下你怎么落地修复？</strong></p>\\n<p>标签间的换行和缩进会在行内格式化上下文中折叠成空格，因此会占据实际宽度。不能改 <code>DOM</code> 时可在父级设 <code>font-size: 0</code> 并为子项恢复字号，但更稳妥的是改用 <code>flex</code> 或 <code>grid</code> 并用 <code>gap</code> 管理间距；前一种方案容易遗漏字号恢复。</p>\\n</li>\\n<li>\\n<p><strong>商品详情页的图片外层没有 <code>padding</code> 和 <code>margin</code>，图片底部仍留出一条空隙，换成 <code>display: block</code> 后消失；你会从哪条排版规则解释？</strong></p>\\n<p>图片作为行内级内容时默认参与基线对齐，行盒会为字母下伸部分保留空间，所以底部看起来有空隙。可用 <code>vertical-align: middle</code> 等方式调整对齐，也可在无需文字混排时设为 <code>display: block</code>；两者语义不同，前者仍保留行内排版。</p>\\n</li>\\n<li>\\n<p><strong>侧边浮层设置 <code>position: relative; left: 40px</code> 后覆盖了正文，但正文没有补到它原来的位置；同事因此认定它已经脱离文档流，你怎么判断？</strong></p>\\n<p><code>position: relative</code> 仍占据常规流中的原始位置，只是视觉绘制位置发生偏移，所以后续内容不会补位。真正完全不占位的是 <code>position: absolute</code> 和 <code>fixed</code>，<code>float</code> 则是部分脱离且允许行内内容环绕；覆盖现象本身不能作为脱流依据。</p>\\n</li>\\n<li>\\n<p><strong>导航从块级元素改成 <code>display: flex</code> 后，原先的垂直 <code>margin</code> 重叠消失，子项上的 <code>float</code> 也不再起作用，这是浏览器兼容故障吗？</strong></p>\\n<p>不是兼容故障，子项已经进入弹性格式化上下文，不再按常规流的块级规则排版。<code>flex</code> 子项的外边距不会按相邻块级盒子的方式重叠，<code>float</code> 也不再承担原来的布局作用；迁移时应重新用 <code>gap</code>、对齐属性和弹性尺寸表达布局。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_23-inline-block的使用场景\\"><a class=\\"header-anchor\\" href=\\"#_23-inline-block的使用场景\\">#</a> 23 inline-block的使用场景</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>inline-block</code> 是混合体：外部像行内（可与文字同排、受 <code>text-align</code> 影响），内部像块级（可设宽高和上下内外边距）</li>\\n<li>相比 <code>float</code> 的好处是不脱流，父容器不会高度塌陷，不用清浮动</li>\\n<li>两个历史坑：<strong>标签间空白被渲染成空格</strong>（约 4px 缝隙）、默认 <code>baseline</code> 对齐导致高度不同的元素参差</li>\\n<li>横向导航、整体居中、等分排列这些老场景，现在都该用 <code>flex</code>/<code>grid</code> —— 没有空白间隙、对齐明确、<code>gap</code> 统一管间距</li>\\n<li>它今天唯一不可替代的场景：<strong>需要跟着文字流一起换行</strong>的小块（正文里的标签、徽章）</li>\\n</ul>\\n<p><strong><code>inline-block</code> 适合需要跟文字同排、同时又要设置宽高和上下内外边距的小块元素。</strong> 它外部按行内元素参与排版，内部具有块级盒子的尺寸能力，而且不会像 <code>float</code> 那样脱流并造成父容器高度塌陷。它的问题是标签间空白会形成额外间隙，默认的 <code>baseline</code> 对齐也可能让不同高度的元素参差不齐。导航、居中工具栏和等分布局现在更适合用 <code>flex</code> 或 <code>grid</code>，正文中的标签、徽章这类需要随文字换行的内容才是它更合适的场景。</p>\\n</section>\\n<p><code>inline-block</code> 是一个混合体：<strong>外部像行内元素</strong>（能和文字排在一行、受 <code>text-align</code> 影响），<strong>内部像块级元素</strong>（能设宽高、上下 <code>margin</code>/<code>padding</code> 生效）。</p>\\n<p><strong>三个经典用途：</strong></p>\\n<ol>\\n<li><strong>横向导航栏</strong> —— <code>a { display: inline-block }</code> 让链接能设内边距和宽高，同时保持横向排列</li>\\n<li><strong>一排子元素在父容器里居中</strong> —— 父级 <code>text-align: center</code> 就能让它们整体居中，宽度不定也行</li>\\n<li><strong>需要和文字混排的小块</strong> —— 比如正文里的标签、徽章，既要能设内边距又要跟着文字流动</li>\\n</ol>\\n<p><strong>相比 <code>float</code> 的优势</strong>：不脱离文档流，所以父容器高度不会塌陷，不需要清浮动。</p>\\n<p><strong>但它有两个绕不开的历史问题：</strong></p>\\n<p><strong>① 空白间隙。</strong> 标签之间的换行会被渲染成一个空格，元素之间凭空多出几像素：</p>\\n<pre class=\\"language-html\\"><code>&lt;span class=&quot;tag&quot;&gt;A&lt;/span&gt;\\n&lt;span class=&quot;tag&quot;&gt;B&lt;/span&gt;   &lt;!-- A 和 B 之间有约 4px 缝隙 --&gt;\\n</code></pre>\\n<p>历史解法五花八门：父元素 <code>font-size: 0</code> 再在子元素恢复、标签写成一行、用注释填满空白、给子元素负 <code>margin</code> —— 全都很别扭。</p>\\n<p><strong>② 基线对齐。</strong> 默认按 <code>baseline</code> 对齐，高度不同的元素底部会参差不齐，通常要加 <code>vertical-align: top</code> 或 <code>middle</code> 修正。</p>\\n<p><strong>结论：新代码里基本不该再用它做布局。</strong></p>\\n<p>上面三个场景现在都有更好的方案：</p>\\n<pre class=\\"language-css\\"><code>/* 横向导航 */\\n.nav { display: flex; gap: 24px; }\\n\\n/* 整体居中 */\\n.toolbar { display: flex; justify-content: center; gap: 12px; }\\n\\n/* 等分网格 */\\n.cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 16px; }\\n</code></pre>\\n<p><code>flex</code>/<code>grid</code> 没有空白间隙问题、对齐方式明确、间距用 <code>gap</code> 统一管理，各方面都更好。</p>\\n<p><strong><code>inline-block</code> 今天还有价值的地方只剩一个</strong>：需要<strong>和文本真正混排</strong>的元素 —— 比如一段话中间嵌一个带背景色的标签，它要跟着文字一起换行、一起被 <code>text-align</code> 影响。这种&quot;文字流内的小块&quot;是 <code>flex</code> 做不到的，因为 <code>flex</code> 子项会脱离文字流。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>老导航把十个链接设成 <code>inline-block</code>，既能横排又能设置 <code>padding</code>，同事说它与普通 <code>inline</code> 没区别；你会拿哪个现象当场反驳？</strong></p>\\n<p><code>inline-block</code> 的外部参与行内排版，但内部盒子可设置宽高，上下 <code>margin</code> 和 <code>padding</code> 也能生效；普通行内盒子的宽高无效，上下间距不会按块盒方式撑开行高。可直接给链接设置固定高度验证，但这不代表它仍是现代导航布局的优选。</p>\\n</li>\\n<li>\\n<p><strong>营销页的一排按钮要整体水平居中，数量和文案长度都不固定，现有方案是子项 <code>inline-block</code>、父级 <code>text-align: center</code>；是否值得立即改成 <code>flex</code>？</strong></p>\\n<p>现有方案能利用行内级盒子受 <code>text-align</code> 影响的特性完成不定宽居中，本身并非错误。新代码通常更适合用 <code>display: flex; justify-content: center; gap: ...</code>，对齐和间距更明确；若页面稳定且无空白、基线问题，重构收益要与回归成本权衡。</p>\\n</li>\\n<li>\\n<p><strong>服务端模板输出六个 <code>inline-block</code> 卡片，标签之间保留缩进，某些字体环境下最后一张换行；产品要求间距始终为 <code>12px</code>，你会选负 <code>margin</code> 还是重做布局？</strong></p>\\n<p>应优先改为 <code>flex</code> 或 <code>grid</code>，并由 <code>gap: 12px</code> 明确控制间距。负 <code>margin</code> 只是抵消由空白字符产生的字体相关宽度，字号或字体变化后仍可能失效；只有受限于旧结构时才考虑 <code>font-size: 0</code> 等兼容手段，并补回子项字号。</p>\\n</li>\\n<li>\\n<p><strong>消息列表里的徽章需要嵌在一句话中，跟随文本换行并受段落 <code>text-align</code> 影响；评审建议把整句改成 <code>flex</code>，你会接受吗？</strong></p>\\n<p>不应仅为徽章改成 <code>flex</code>，因为它需要作为文字流中的小块参与换行和基线排版。将徽章设为 <code>inline-block</code>，即可保留行内流动能力并获得背景、内边距等块盒能力；若需求变成固定的一行左右布局，<code>flex</code> 才更合适。</p>\\n</li>\\n<li>\\n<p><strong>价格行里三个不同高度的 <code>inline-block</code> 标签底部参差不齐，给它们统一高度会截断国际化文案；你会怎么排查和修正？</strong></p>\\n<p>先检查默认的 <code>baseline</code> 对齐，它会让不同内容的行内块按基线排列，而不是按盒子顶部或中部对齐。可按设计改为 <code>vertical-align: top</code> 或 <code>middle</code>，避免用固定高度掩盖问题；若标签还需要稳定分布和拉伸，对齐明确的 <code>flex</code> 更易维护。</p>\\n</li>\\n<li>\\n<p><strong>团队准备把老页面所有 <code>inline-block</code> 一次替换为 <code>flex</code>，其中包含正文内标签、导航和卡片网格；这项重构的边界应该怎么定？</strong></p>\\n<p>导航和卡片网格属于布局结构，适合分别迁移到 <code>flex</code> 或 <code>grid</code>，可消除空白间隙并统一使用 <code>gap</code>。正文内标签仍应保留 <code>inline-block</code>，因为它必须与文本真正混排；机械替换会改变换行、基线及 <code>text-align</code> 行为。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_24-position-fixed什么时候会失效\\"><a class=\\"header-anchor\\" href=\\"#_24-position-fixed什么时候会失效\\">#</a> 24 position: fixed什么时候会失效？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>fixed</code> 正常相对<strong>视口</strong>定位，但祖先一旦创建「包含块」就改为相对该祖先定位，表现为固定失效</li>\\n<li>会触发的祖先属性：<code>transform</code>、<code>filter</code>、<code>backdrop-filter</code>、<code>perspective</code>、含这些值的 <code>will-change</code>、<code>contain: layout/paint/strict/content</code></li>\\n<li>三个常见场景：入场动画的 <code>transform</code> 没清除、页面置灰的 <code>filter</code>、为&quot;开启硬件加速&quot;加的 <code>translateZ(0)</code></li>\\n<li>排查靠沿父级看 <code>Computed</code>；根治靠 <code>Portal</code> 把弹层挂到 <code>body</code> 直下</li>\\n<li>⚠️ <code>sticky</code> 失效是另一套原因：祖先有滚动容器（<code>overflow: hidden</code> 会隐式建立）或没写偏移值</li>\\n</ul>\\n<p><strong><code>position: fixed</code> 在祖先元素创建了新的包含块时，会从相对视口定位变成相对该祖先定位，看起来就像失效了。</strong> 常见触发源可以归为变换与隔离两类，例如非 <code>none</code> 的 <code>transform</code>、<code>filter</code>，以及相关的 <code>will-change</code> 或 <code>contain</code>。入场动画保留 <code>transform: translateY(0)</code>、页面置灰使用 <code>filter</code>，都可能让内部固定元素跟着容器滚动。排查时应沿父级检查计算样式，弹窗一类组件通常用 <code>Portal</code> 挂到 <code>body</code> 直下；<code>sticky</code> 则主要检查滚动容器和偏移值，不要混在一起判断。</p>\\n</section>\\n<p><code>position: fixed</code> 正常情况下相对<strong>视口</strong>定位，滚动时纹丝不动。但如果它的<strong>任意祖先</strong>创建了&quot;包含块&quot;，它就会改为相对那个祖先定位 —— 表现就是&quot;固定失效&quot;，跟着页面一起滚。</p>\\n<p><strong>会导致 <code>fixed</code> 失效的祖先属性：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>属性</th>\\n<th>条件</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>transform</code></td>\\n<td>不为 <code>none</code>（哪怕是 <code>translateZ(0)</code>）</td>\\n</tr>\\n<tr>\\n<td><code>filter</code></td>\\n<td>不为 <code>none</code>（页面置灰最常踩）</td>\\n</tr>\\n<tr>\\n<td><code>backdrop-filter</code></td>\\n<td>不为 <code>none</code></td>\\n</tr>\\n<tr>\\n<td><code>perspective</code></td>\\n<td>不为 <code>none</code></td>\\n</tr>\\n<tr>\\n<td><code>will-change</code></td>\\n<td>值里含 <code>transform</code>/<code>filter</code>/<code>perspective</code></td>\\n</tr>\\n<tr>\\n<td><code>contain</code></td>\\n<td><code>layout</code> / <code>paint</code> / <code>strict</code> / <code>content</code></td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>为什么？</strong> 规范规定：这些属性会让元素成为其后代中 <code>fixed</code> 和 <code>absolute</code> 元素的<strong>包含块</strong>。因为 <code>transform</code> 建立了新的坐标系，如果 <code>fixed</code> 还相对视口定位，元素就会跑到变换后的坐标系之外，视觉上完全对不上。</p>\\n<p><strong>实际会遇到的三个场景：</strong></p>\\n<ol>\\n<li><strong>入场动画</strong>：给卡片加 <code>transform: translateY(20px)</code> 做动画，卡片里的固定按钮立刻错位。动画结束后如果 <code>transform</code> 保留（哪怕是 <code>translateY(0)</code>）问题依然存在 —— 必须彻底移除这个属性。</li>\\n<li><strong>页面置灰</strong>：<code>html { filter: grayscale(1) }</code> 会让全站的 <code>fixed</code> 元素全部失效，是纪念日置灰上线后最常见的事故。</li>\\n<li><strong>性能优化好心办坏事</strong>：有人给容器加 <code>will-change: transform</code> 或 <code>translateZ(0)</code> 想&quot;开启硬件加速&quot;，结果把里面的固定定位搞挂了。</li>\\n</ol>\\n<p><strong>排查方法</strong>：选中失效的 <code>fixed</code> 元素，沿父级逐层看 <code>Computed</code> 里有没有上面那几个属性。<code>DevTools</code> 里对 <code>fixed</code> 元素通常会有提示。</p>\\n<p><strong>根治办法</strong>：把 <code>fixed</code> 元素用 <code>Portal</code> 渲染到 <code>body</code> 直下，彻底脱离那些祖先。这也是所有 UI 库的 <code>Modal</code>、<code>Drawer</code>、<code>Message</code> 默认都挂在 <code>body</code> 上的原因 —— 不只是为了 <code>z-index</code>，也是为了躲开这个包含块问题。</p>\\n<p><strong>顺带一提</strong>：<code>position: sticky</code> 有另一套失效原因 —— 它要求祖先里不能有 <code>overflow</code> 不为 <code>visible</code> 的滚动容器（<code>overflow: hidden</code> 会让 <code>overflow-y</code> 隐式变成 <code>auto</code>，从而建立滚动容器），而且必须指定 <code>top</code>/<code>bottom</code> 等偏移值。两者别混为一谈。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>移动端底部购买栏写了 <code>position: fixed; bottom: 0</code>，放在普通页面时固定正常，移进带 <code>transform: translateY(0)</code> 的内容容器后却随容器滚动；<code>0</code> 位移为什么仍会出问题？</strong></p>\\n<p>关键不在位移量，而在祖先的 <code>transform</code> 是否为 <code>none</code>；<code>translateY(0)</code> 仍会让祖先建立新的包含块。购买栏因此改为相对该祖先定位，而不是视口；应在动画结束后彻底移除 <code>transform</code>，或把购买栏挂到不受该祖先影响的位置。</p>\\n</li>\\n<li>\\n<p><strong>纪念日上线 <code>html { filter: grayscale(1) }</code> 后，全站几十个弹窗和吸底按钮开始错位；既要保留置灰效果，又要快速止损，你会先改哪里？</strong></p>\\n<p>应先确认根元素上的非 <code>none</code> <code>filter</code> 已成为后代 <code>fixed</code> 元素的包含块，这与层级高低无关。快速止损可调整置灰施加范围，避免包住浮层挂载点；长期应让弹窗通过 <code>Portal</code> 挂到合适的顶层节点，但全局滤镜与顶层浮层的视觉一致性仍需单独处理。</p>\\n</li>\\n<li>\\n<p><strong>性能评审要求给应用根容器加 <code>will-change: transform</code>，理由是所有动画都能提前优化；页面内又有多个 <code>fixed</code> 浮层，你会批准吗？</strong></p>\\n<p>不应把 <code>will-change: transform</code> 无差别加到根容器，它会改变后代 <code>fixed</code> 元素的包含块，可能让浮层跟随容器滚动。应只对确实需要变换的局部元素按需使用，并在不需要时移除；即使视觉上尚未出错，这种全局坐标系变化也会增加隐蔽维护风险。</p>\\n</li>\\n<li>\\n<p><strong>线上客服反馈抽屉只在某个带毛玻璃头图的页面无法固定，代码里没找到 <code>transform</code>；你沿祖先链还会重点检查哪些计算值？</strong></p>\\n<p>应继续检查 <code>filter</code>、<code>backdrop-filter</code>、<code>perspective</code>，以及包含 <code>transform</code>、<code>filter</code> 或 <code>perspective</code> 的 <code>will-change</code>。还要查看 <code>contain</code> 是否为 <code>layout</code>、<code>paint</code>、<code>strict</code> 或 <code>content</code>；这些属性都可能建立包含块，排查时必须看实际计算值而不是只搜组件源码。</p>\\n</li>\\n<li>\\n<p><strong><code>UI</code> 库坚持把 <code>Modal</code> 渲染在业务卡片内部以方便继承主题，业务方则要求它始终相对视口固定；你会怎样取舍挂载位置？</strong></p>\\n<p>若视口固定是硬约束，应优先通过 <code>Portal</code> 把 <code>Modal</code> 挂到 <code>body</code> 直下或独立顶层容器，避开业务祖先创建的包含块。主题可通过全局变量或向挂载节点同步变量解决；留在卡片内虽便于继承局部样式，却无法保证祖先未来不引入 <code>transform</code> 或 <code>contain</code>。</p>\\n</li>\\n<li>\\n<p><strong>同事把一个 <code>sticky</code> 标题失效也归因于祖先 <code>transform</code>，但页面祖先实际设置了 <code>overflow: hidden</code>，标题也没写 <code>top</code>；你会如何区分两类故障？</strong></p>\\n<p><code>fixed</code> 失效主要排查祖先是否建立了新的包含块，而 <code>sticky</code> 还依赖滚动容器和偏移条件。祖先的非 <code>visible</code> <code>overflow</code> 可能改变其参照的滚动容器，未设置 <code>top</code> 或 <code>bottom</code> 也不会产生预期吸附；不能套用同一份排查清单。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_25-回流-reflow-和重绘-repaint-的理解\\"><a class=\\"header-anchor\\" href=\\"#_25-回流-reflow-和重绘-repaint-的理解\\">#</a> 25 回流（reflow）和重绘（repaint）的理解</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>回流（重排）= 重新计算元素的几何位置和尺寸，代价最大，会连带触发重绘</li>\\n<li>重绘 = 几何没变只是外观变（颜色、阴影、<code>visibility</code>），跳过布局阶段</li>\\n<li>合成 = 只动 <code>transform</code>/<code>opacity</code>，跳过布局和绘制，能在合成线程上跑，最便宜</li>\\n<li>读 <code>offsetTop</code>/<code>scrollTop</code>/<code>getBoundingClientRect</code>/<code>getComputedStyle</code> 会<strong>强制同步布局</strong>，和写操作交替就是布局抖动</li>\\n<li>优化：读写分离、批量改用 <code>class</code> 或 <code>cssText</code>、动画只动 <code>transform</code>/<code>opacity</code>、频繁变动的元素脱离文档流</li>\\n</ul>\\n<p><strong>回流会重新计算元素的位置和尺寸，并连带触发重绘；重绘只更新外观，不需要重新布局。</strong> 页面初次渲染、增删可见节点或修改宽高都会触发回流，而颜色、背景等变化通常只触发重绘。读 <code>offsetTop</code>、<code>scrollTop</code>、<code>getBoundingClientRect()</code> 或 <code>getComputedStyle()</code> 时，浏览器可能为返回最新结果而强制同步布局。工程上我会分离读写、缓存布局数据、批量修改 <code>class</code>，动画优先使用 <code>transform</code> 和 <code>opacity</code>。</p>\\n</section>\\n<h3 id=\\"分析\\"><a class=\\"header-anchor\\" href=\\"#分析\\">#</a> 分析</h3>\\n<p>在<code>HTML</code>中，每个元素都可以理解成一个盒子，在浏览器解析过程中，会涉及到回流与重绘：</p>\\n<ul>\\n<li><strong>回流</strong>：布局引擎会根据各种样式计算每个盒子在页面上的大小与位置</li>\\n<li><strong>重绘</strong>：当计算好盒模型的位置、大小及其他属性后，浏览器根据每个盒子特性进行绘制</li>\\n</ul>\\n<p>具体的浏览器解析渲染机制如下所示：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/b3fc0062bac6f0ff.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>解析HTML，生成<code>DOM</code>树，解析<code>CSS</code>，生成<code>CSSOM</code>树</li>\\n<li>将<code>DOM</code>树和<code>CSSOM</code>树结合，生成渲染树(<code>Render Tree</code>)</li>\\n<li><code>Layout</code>(回流):根据生成的渲染树，进行回流(<code>Layout</code>)，得到节点的几何信息（位置，大小）</li>\\n<li><code>Painting</code>(重绘):根据渲染树以及回流得到的几何信息，得到节点的绝对像素</li>\\n<li><code>Display</code>:将像素发送给<code>GPU</code>，展示在页面上</li>\\n</ul>\\n<blockquote>\\n<ul>\\n<li>在页面初始渲染阶段，回流不可避免的触发，可以理解成页面一开始是空白的元素，后面添加了新的元素使页面布局发生改变</li>\\n<li>当我们对 <code>DOM</code> 的修改引发了 <code>DOM</code>几何尺寸的变化（比如修改元素的宽、高或隐藏元素等）时，浏览器需要重新计算元素的几何属性，然后再将计算的结果绘制出来</li>\\n<li>当我们对 <code>DOM</code>的修改导致了样式的变化（<code>color</code>或<code>background-color</code>），却并未影响其几何属性时，浏览器不需重新计算元素的几何属性、直接为该元素绘制新的样式，这里就仅仅触发了重绘</li>\\n</ul>\\n</blockquote>\\n<h3 id=\\"如何触发\\"><a class=\\"header-anchor\\" href=\\"#如何触发\\">#</a> 如何触发</h3>\\n<p>要想减少回流和重绘的次数，首先要了解回流和重绘是如何触发的</p>\\n<p><strong>回流触发时机</strong></p>\\n<blockquote>\\n<p>回流这一阶段主要是计算节点的位置和几何信息，那么<strong>当页面布局和几何信息发生变化的时候，就需要回流</strong>，如下面情况：</p>\\n</blockquote>\\n<ul>\\n<li>添加或删除可见的DOM元素</li>\\n<li>元素的位置发生变化</li>\\n<li>元素的尺寸发生变化（包括外边距、内边框、边框大小、高度和宽度等）</li>\\n<li>内容发生变化，比如文本变化或图片被另一个不同尺寸的图片所替代</li>\\n<li>页面一开始渲染的时候（这避免不了）</li>\\n<li>浏览器的窗口尺寸变化（因为回流是根据视口的大小来计算元素的位置和大小的）</li>\\n<li>还有一些容易被忽略的操作：获取一些特定属性的值。浏览器为了获取这些值，需要进行回流操作\\n<ul>\\n<li><code>offsetTop</code></li>\\n<li><code>offsetLeft</code></li>\\n<li><code>offsetWidth</code></li>\\n<li><code>offsetHeight</code></li>\\n<li><code>scrollTop</code></li>\\n<li><code>scrollLeft</code></li>\\n<li><code>scrollWidth</code></li>\\n<li><code>scrollHeight</code></li>\\n<li><code>clientTop</code></li>\\n<li><code>clientLeft</code></li>\\n<li><code>clientWidth</code></li>\\n<li><code>clientHeight</code></li>\\n<li>这些属性有一个共性，就是需要通过即时计算得到。因此浏览器为了获取这些值，也会进行回流</li>\\n</ul>\\n</li>\\n<li>除此还包括<code>getComputedStyle</code>方法，原理是一样的</li>\\n</ul>\\n<blockquote>\\n<p><strong>回流过程</strong>：由于DOM的结构发生了改变，所以需要从生成DOM这一步开始，重新经过<code>样式计算</code>、<code>生成布局树</code>、<code>建立图层树</code>、再到<code>生成绘制列表</code>以及之后的显示器显示这整一个渲染过程走一遍，开销是非常大的</p>\\n</blockquote>\\n<p><strong>重绘触发时机</strong></p>\\n<blockquote>\\n<p>通过构造渲染树和重排（回流）阶段，我们知道了哪些节点是可见的，以及可见节点的样式和具体的几何信息(元素在视口内的位置和尺寸大小)，接下来就可以将渲染树的每个节点都转换为屏幕上的实际像素，这个阶段就叫做重绘</p>\\n</blockquote>\\n<p>触发回流一定会触发重绘</p>\\n<ul>\\n<li>当 <strong><code>DOM</code> 的修改导致了样式的变化</strong>，并且没有影响几何属性的时候，会导致<code>重绘</code>(<code>repaint</code>)。</li>\\n<li>重绘过程：由于没有导致 <code>DOM</code> 几何属性的变化，因此元素的位置信息不需要更新，所以当发生重绘的时候，会跳过<code>生存布局树</code>和<code>建立图层树</code>的阶段，直接到<code>生成绘制列表</code>，然后继续进行分块、生成位图等后面一系列操作。</li>\\n</ul>\\n<p><strong>浏览器优化机制</strong></p>\\n<ul>\\n<li>由于每次重排都会造成额外的计算消耗，因此大多数浏览器都会通过队列化修改并批量执行来优化重排过程。浏览器会将修改操作放入到队列里，直到过了一段时间或者操作达到了一个阈值，才清空队列</li>\\n<li>当你获取布局信息的操作的时候，会强制队列刷新，包括前面讲到的<code>offsetTop</code>等方法都会返回最新的数据</li>\\n<li>因此浏览器不得不清空队列，触发回流重绘来返回正确的值</li>\\n</ul>\\n<h3 id=\\"如何避免\\"><a class=\\"header-anchor\\" href=\\"#如何避免\\">#</a> 如何避免</h3>\\n<ol>\\n<li>避免频繁使用 <code>style</code>，而是采用修改<code>class</code>的方式。</li>\\n<li>将动画效果应用到<code>position</code>属性为<code>absolute</code>或<code>fixed</code>的元素上。</li>\\n<li>批量操作 <code>DOM</code>，比如读取某元素 <code>offsetWidth</code> 属性存到一个临时变量，再去使用，而不是频繁使用这个计算属性；又比如利用 <code>document.createDocumentFragment()</code> 来添加要被添加的节点，处理完之后再插入到实际 DOM 中</li>\\n<li>也可以先为元素设置<code>display: none</code>，操作结束后再把它显示出来。因为在<code>display</code>属性为<code>none</code>的元素上进行的DOM操作不会引发回流和重绘</li>\\n<li>对于 <code>resize</code>、<code>scroll</code> 等进行防抖/节流处理。</li>\\n<li>避免频繁读取会引发回流/重绘的属性，如果确实需要多次使用，就用一个变量缓存起来。</li>\\n<li>利用 CSS3 的<code>transform</code>、<code>opacity</code>、<code>filter</code>这些属性可以实现合成的效果，也就是<code>CPU</code>加速。</li>\\n</ol>\\n<p><strong>以下举例分析</strong></p>\\n<p>例如，多次修改一个把元素布局的时候，我们很可能会如下操作</p>\\n<pre class=\\"language-js\\"><code>const el = document.getElementById(&#39;el&#39;)\\nfor(let i=0;i&lt;10;i++) {\\n    el.style.top  = el.offsetTop  + 10 + &quot;px&quot;;\\n    el.style.left = el.offsetLeft + 10 + &quot;px&quot;;\\n}\\n</code></pre>\\n<p>每次循环都需要获取多次<code>offset</code>属性，比较糟糕，可以使用变量的形式缓存起来，待计算完毕再提交给浏览器发出重计算请求</p>\\n<pre class=\\"language-js\\"><code>// 缓存offsetLeft与offsetTop的值\\nconst el = document.getElementById(&#39;el&#39;)\\nlet offLeft = el.offsetLeft, offTop = el.offsetTop\\n\\n// 在JS层面进行计算\\nfor(let i=0;i&lt;10;i++) {\\n  offLeft += 10\\n  offTop  += 10\\n}\\n\\n// 一次性将计算结果应用到DOM上\\nel.style.left = offLeft + &quot;px&quot;\\nel.style.top = offTop  + &quot;px&quot;\\n</code></pre>\\n<p>我们还可避免改变样式，使用类名去合并样式</p>\\n<pre class=\\"language-js\\"><code>const container = document.getElementById(&#39;container&#39;)\\ncontainer.style.width = &#39;100px&#39;\\ncontainer.style.height = &#39;200px&#39;\\ncontainer.style.border = &#39;10px solid red&#39;\\ncontainer.style.color = &#39;red&#39;\\n</code></pre>\\n<p>使用类名去合并样式</p>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n  .basic_style {\\n    width: 100px;\\n    height: 200px;\\n    border: 10px solid red;\\n    color: red;\\n  }\\n&lt;/style&gt;\\n&lt;script&gt;\\n  const container = document.getElementById(&#39;container&#39;)\\n  container.classList.add(&#39;basic_style&#39;)\\n&lt;/script&gt;\\n</code></pre>\\n<ul>\\n<li>前者每次单独操作，都去触发一次渲染树更改（新浏览器不会），</li>\\n<li>都去触发一次渲染树更改，从而导致相应的回流与重绘过程</li>\\n<li>合并之后，等于我们将所有的更改一次性发出</li>\\n<li>我们还可以通过通过设置元素属性<code>display: none</code>，将其从页面上去掉，然后再进行后续操作，这些后续操作也不会触发回流与重绘，这个过程称为<strong>离线操作</strong></li>\\n</ul>\\n<pre class=\\"language-js\\"><code>const container = document.getElementById(&#39;container&#39;)\\ncontainer.style.width = &#39;100px&#39;\\ncontainer.style.height = &#39;200px&#39;\\ncontainer.style.border = &#39;10px solid red&#39;\\ncontainer.style.color = &#39;red&#39;\\n</code></pre>\\n<p>离线操作后</p>\\n<pre class=\\"language-js\\"><code>let container = document.getElementById(&#39;container&#39;)\\ncontainer.style.display = &#39;none&#39;\\ncontainer.style.width = &#39;100px&#39;\\ncontainer.style.height = &#39;200px&#39;\\ncontainer.style.border = &#39;10px solid red&#39;\\ncontainer.style.color = &#39;red&#39;\\n...（省略了许多类似的后续操作）\\ncontainer.style.display = &#39;block&#39;\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>列表动画循环十次执行 <code>el.style.top = el.offsetTop + 10 + \'px\'</code>，同时又修改 <code>left</code>，页面出现明显卡顿；代码量很小，为什么这种写法仍然危险？</strong></p>\\n<p>每轮都在写入样式后读取 <code>offsetTop</code> 或 <code>offsetLeft</code>，浏览器为返回最新几何值，可能被迫提前完成待处理的布局计算。应先缓存初始偏移，在 <code>JavaScript</code> 中完成累计计算，再一次性写回；若其他代码会同时改变位置，缓存值也可能过期。</p>\\n</li>\\n<li>\\n<p><strong>管理后台要给一个节点连续设置宽、高、边框和颜色，开发争论逐条写 <code>style</code> 还是切换一个 <code>class</code>；你会基于什么选择？</strong></p>\\n<p>样式组合固定时应优先切换 <code>class</code>，让多项变更以一次意图提交，也便于复用和维护。现代浏览器可能合并连续写入，因此不能断言每条 <code>style</code> 都立即触发一次渲染；真正需要避免的是写入之间夹杂几何读取，形成反复同步布局。</p>\\n</li>\\n<li>\\n<p><strong>虚拟列表准备一次插入数百个新节点，同事建议每创建一个就 <code>appendChild</code> 到页面，另一位建议使用 <code>DocumentFragment</code>；你支持哪种方案？</strong></p>\\n<p>应先在 <code>DocumentFragment</code> 中完成节点组装，再整体插入实际 <code>DOM</code>，以减少对在线文档的反复结构修改。若后续逻辑仍在每次写入后读取尺寸，批量插入也无法消除同步布局风险；数据规模很大时还需控制单次工作量，但来源没有给出固定阈值。</p>\\n</li>\\n<li>\\n<p><strong>复杂表格要批量改几十项样式，有人提议先设 <code>display: none</code>，完成后恢复为 <code>block</code>；原元素本来可能是 <code>table</code> 或 <code>flex</code>，你会直接采用吗？</strong></p>\\n<p>隐藏后进行离线操作可避免中间过程参与页面回流和重绘，但恢复时不能武断写成 <code>block</code>，否则会破坏原有显示类型。应保存并恢复原状态，或通过专用隐藏类切换；隐藏与重新显示本身仍会引发布局变化，也可能造成可见闪动。</p>\\n</li>\\n<li>\\n<p><strong>窗口 <code>resize</code> 和页面 <code>scroll</code> 事件中同时读取尺寸并改动多个元素，低端设备滚动不稳；只加防抖或节流就算修复了吗？</strong></p>\\n<p>防抖或节流能降低高频事件处理次数，但不能修复处理函数内部读写交错造成的同步布局。还应先集中读取并缓存所需几何信息，再批量写入样式；节流间隔过大可能让界面响应滞后，防抖则不适合必须连续反馈的交互。</p>\\n</li>\\n<li>\\n<p><strong>抽屉动画在 <code>left</code>、<code>transform</code> 和 <code>opacity</code> 之间选型，设计还要求关闭后不占据普通布局空间；你会如何组合，而不是只说用 <code>CSS3</code> 加速？</strong></p>\\n<p>抽屉可先用 <code>absolute</code> 或 <code>fixed</code> 避免动画持续影响普通流，再优先用 <code>transform</code> 完成位移、用 <code>opacity</code> 完成淡入淡出，减少布局层面的改动。<code>filter</code> 也可参与合成效果，但不应因此假定完全没有其他成本；定位方式还会改变包含块和占位语义，必须结合页面结构验证。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_26-gpu加速的原因\\"><a class=\\"header-anchor\\" href=\\"#_26-gpu加速的原因\\">#</a> 26 GPU加速的原因</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>普通属性动画每帧都要走 <code>布局 → 绘制 → 合成</code>，前两步在<strong>主线程</strong>，主线程一忙就掉帧</li>\\n<li><code>transform</code>/<code>opacity</code> 的元素被提成独立合成层后，改变只是矩阵运算，直接交给<strong>合成线程 + GPU</strong></li>\\n<li>两大好处：<code>GPU</code> 擅长位图和矩阵变换；不占主线程，主线程卡住动画照样流畅</li>\\n<li>⚠️ 提层不免费：每层占 <code>宽 × 高 × 4</code> 字节显存，还可能引发层爆炸和文字发虚</li>\\n<li>规矩：只给真在动的元素加 <code>will-change</code>，<strong>动画结束后移除</strong>；用 <code>Layers</code> 面板核对层数</li>\\n</ul>\\n<p><strong><code>GPU</code> 加速的核心，是让动画尽量绕过主线程上的布局和绘制，直接在合成阶段处理已有位图。</strong> 像 <code>left</code> 这类属性变化通常每帧都要重新布局和绘制，而独立合成层上的 <code>transform</code>、<code>opacity</code> 主要是位图与矩阵运算，更适合交给合成线程和 <code>GPU</code>。这样主线程被长任务占用时，合成动画仍可能保持流畅。提层会增加显存、层数量和耗电成本，因此只给确实在动画的元素设置 <code>will-change</code>，结束后及时移除。</p>\\n</section>\\n<p>要理解 <code>GPU</code> 加速，先看浏览器渲染的完整流水线：</p>\\n<pre class=\\"language-text\\"><code>样式计算 → 布局(Layout) → 分层(Layer) → 绘制(Paint) → 光栅化(Raster) → 合成(Composite)\\n</code></pre>\\n<p><strong>普通属性的动画</strong>（比如动 <code>left</code>）每一帧都要走完 <code>布局 → 绘制 → 合成</code>，前两步都在<strong>主线程</strong>上做。主线程一旦被 <code>JS</code> 占住，动画就掉帧。</p>\\n<p><strong><code>transform</code> 和 <code>opacity</code> 的动画可以直接跳到最后一步。</strong> 因为元素被提升成了独立的<strong>合成层</strong>，它的位图已经生成好了，改变位置只是告诉合成器&quot;把这张位图挪到新坐标&quot;或&quot;用新的透明度混合&quot; —— 这两件事都是矩阵运算，正是 <code>GPU</code> 最擅长的。</p>\\n<p><strong>两大好处：</strong></p>\\n<ol>\\n<li><strong>发挥 <code>GPU</code> 优势</strong>：合成线程生成位图时会调用线程池并用 <code>GPU</code> 加速，而 <code>GPU</code> 天生就是处理位图和矩阵变换的。</li>\\n<li><strong>不占主线程</strong>：合成在独立线程上跑，即使主线程被一段长任务卡住，动画依然流畅。这是为什么用 <code>transform</code> 做的加载动画在页面卡顿时还能转，而用 <code>left</code> 做的会跟着一起卡。</li>\\n</ol>\\n<p><strong>怎么触发提层</strong>（按推荐程度）：</p>\\n<pre class=\\"language-css\\"><code>/* ✅ 语义明确，浏览器知道你要动什么 */\\n.card { will-change: transform; }\\n\\n/* ⚠️ 老写法，靠&quot;欺骗&quot;浏览器进入 3D 渲染路径 */\\n.card { transform: translateZ(0); }\\n</code></pre>\\n<p><strong>但提层不是免费的，这才是这题的加分点：</strong></p>\\n<ul>\\n<li><strong>显存开销</strong>：每层要占 <code>宽 × 高 × 4</code> 字节。一张全屏的层在 <code>1080p</code> 下就是约 8MB，几十层下来低端机直接吃不消。</li>\\n<li><strong>层爆炸</strong>：一个元素提层后，覆盖在它上面的元素也可能被迫提层（否则遮挡关系画不对），层数会连锁膨胀。</li>\\n<li><strong>文字渲染变化</strong>：某些情况下提层会导致文字从子像素抗锯齿退化成灰度抗锯齿，看起来发虚。</li>\\n</ul>\\n<p><strong>所以规矩是：</strong></p>\\n<ol>\\n<li>只给<strong>真正在动</strong>的元素提层，别无差别加。</li>\\n<li>用 <code>will-change</code> 而不是 <code>translateZ(0)</code>，语义清楚。</li>\\n<li><strong>动画结束后移除 <code>will-change</code></strong> —— 它是&quot;提前准备&quot;的提示，长期挂着等于一直占着显存。</li>\\n<li>用 <code>DevTools</code> 的 <code>Layers</code> 面板确认层数和各自的内存占用，看到几十上百层就该收敛了。</li>\\n</ol>\\n<p><strong>顺带一提</strong>：<code>transform</code> 会创建包含块，导致后代的 <code>position: fixed</code> 相对它定位而不是视口 —— 给根容器无脑加 <code>translateZ(0)</code> 常常会引发固定定位错乱的事故。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表用 <code>left</code> 做横向滑入，主线程同时执行一段耗时 <code>JS</code> 后动画明显掉帧；改成 <code>transform</code> 为什么通常会更稳？</strong></p>\\n<p><code>left</code> 的变化会让每一帧重新经过布局、绘制和合成，而这些关键工作依赖主线程，容易被耗时 <code>JS</code> 阻塞。<code>transform</code> 可复用已经光栅化的位图，只让合成器执行矩阵变换；前提是元素进入独立合成层，否则仍要结合实际渲染轨迹判断。</p>\\n</li>\\n<li>\\n<p><strong>同事给两百个列表项统一加 <code>transform: translateZ(0)</code>，认为合成层越多越流畅，你会批准这次改动吗？</strong></p>\\n<p>不会批准，无差别提层会让每个图层持有自己的位图，显存成本大致按 <code>宽 × 高 × 4</code> 字节增长。图层过多还会增加合成负担，并可能因遮挡关系触发连锁提层；应只处理真正参与动画的元素，并在结束后撤销提示。</p>\\n</li>\\n<li>\\n<p><strong>首页只有一个全屏轮播图需要长期做位移动画，团队在 <code>will-change: transform</code> 和 <code>translateZ(0)</code> 之间争论，你怎么选？</strong></p>\\n<p>优先选 <code>will-change: transform</code>，它明确告诉浏览器即将变化的属性，比借助 <code>translateZ(0)</code> 进入 3<code>D</code> 渲染路径更容易维护。提示应在动画前设置并在结束后移除；如果该轮播确实持续运动，可保留更久，但仍需核对显存和图层数量。</p>\\n</li>\\n<li>\\n<p><strong>上线后低端机滚动卡顿，<code>Layers</code> 面板里出现几十上百个合成层，而且列表上方的静态元素也被提层，你怎么定位？</strong></p>\\n<p>先在 <code>Layers</code> 面板检查各层的内存占用和提层原因，再沿覆盖关系寻找最早被主动提层的列表项。某个图层可能迫使上方元素为保持遮挡顺序而连锁提层；应收窄 <code>will-change</code> 的作用范围，并用修改前后的图层数量验证。</p>\\n</li>\\n<li>\\n<p><strong>根容器加了 <code>translateZ(0)</code> 后，原本贴住视口的 <code>position: fixed</code> 客服按钮开始跟着容器移动，为什么优化动画会改坏定位？</strong></p>\\n<p><code>transform</code> 会创建新的包含块，使后代的 <code>position: fixed</code> 改为相对该元素定位，而不再相对视口。应移除根容器上的强制提层，把合成提示下沉到实际动画节点；若必须保留变换，则把固定按钮移出该包含块。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_27-说说will-change\\"><a class=\\"header-anchor\\" href=\\"#_27-说说will-change\\">#</a> 27 说说will-change</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>will-change</code> 是给浏览器的<strong>预告</strong>：&quot;这个属性马上要变，请提前做好优化准备&quot;</li>\\n<li>典型值：<code>transform</code>、<code>opacity</code>、<code>scroll-position</code>、<code>contents</code></li>\\n<li>它会让元素提前提升为合成层，省掉动画开始那一刻的准备开销（避免第一帧卡顿）</li>\\n<li>⚠️ 三条纪律：<strong>别写在常驻样式里</strong>、别无差别加给大量元素、<strong>动画结束要移除</strong></li>\\n<li>副作用和 <code>transform</code> 一样：创建层叠上下文和包含块，会让后代的 <code>fixed</code> 定位失效</li>\\n</ul>\\n<p><strong><code>will-change</code> 是提前告知浏览器某个属性即将变化，让它预先准备分层或渲染优化，减少动画开始时的卡顿。</strong> 常见值有 <code>transform</code>、<code>opacity</code>、<code>scroll-position</code> 和 <code>contents</code>，但它只是优化提示，并不意味着越多越好。大量元素长期保留该属性会占用更多显存、增加耗电，针对 <code>transform</code> 的优化还可能带来层叠上下文和包含块方面的影响。我一般在交互前添加，动画结束后设回 <code>auto</code> 或直接移除。</p>\\n</section>\\n<blockquote>\\n<p><code>will-change</code>是<code>CSS3</code>新增的标准属性，它的作用很单纯，就是<code>&quot;增强页面渲染性能&quot;</code>，当我们在通过某些行为触发页面进行大面积绘制的时候，浏览器往往是没有准备，只能被动的使用CUP去计算和重绘，由于事先没有准备，对于一些复杂的渲染可能会出现掉帧、卡顿等情况。而<code>will-change</code>则是在真正的行为触发之前告诉浏览器可能要进行重绘了，相当于浏览器把CUP拉上了，能从容的面对接下来的变形。</p>\\n</blockquote>\\n<p><strong>常用的语法主要有：</strong></p>\\n<ul>\\n<li><code>whil-change: scroll-position;</code> 即将开始滚动</li>\\n<li><code>will-change: contents;</code> 内容要动画或者变化了</li>\\n<li><code>will-transform;</code> transform相关的属性要变化了(常用)</li>\\n</ul>\\n<p><strong>注意：</strong></p>\\n<ul>\\n<li><code>will-change</code>虽然可以开启加速，但是一定要适度使用</li>\\n<li>开启加速的代价为手机的耗电量会增加</li>\\n<li>使用时遵循最小化影响原则，可以对伪元素开启加速，独立渲染</li>\\n<li>可以写在伪类中，例如<code>hover</code>中，这样移出元素的时候就会自动<code>remove</code>掉<code>will-change</code>了</li>\\n<li>如果使用<code>JS</code>添加了<code>will-change</code>，注意要及时<code>remove</code>掉，方式就是<code>style.willChange = \'auto\'</code></li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>两百张卡片常驻 <code>will-change: transform</code>，但用户只有悬停时才会看到单张卡片放大，这种写法为什么可能比不加更差？</strong></p>\\n<p>常驻提示会让浏览器长期为大量卡片准备后续变化，可能产生额外图层、显存占用和耗电，而绝大多数卡片并未运动。应把提示限制到即将交互的卡片，并在动画结束后恢复为 <code>auto</code>；浏览器是否实际提层仍由其实现决定。</p>\\n</li>\\n<li>\\n<p><strong>一个展开面板点击后首帧偶尔卡顿，产品要求保留现有效果，你会在什么时机设置 <code>will-change</code>？</strong></p>\\n<p>应在可预见的交互信号出现时提前设置，例如指针进入触发区域后写入 <code>will-change: transform</code>，真正点击时浏览器已有准备时间。动画完成后要移除提示；若交互无法提前预测或元素很少，长期保留的资源成本可能高于首帧收益。</p>\\n</li>\\n<li>\\n<p><strong>团队想把 <code>will-change: contents</code> 当成所有复杂组件的通用性能开关，并写进组件库基类，你会怎么处理？</strong></p>\\n<p>不应把 <code>will-change: contents</code> 作为通用开关，它只是提示内容可能变化，不能保证消除重绘，也不等于必然启用 <code>GPU</code>。只有确认组件即将频繁改变内容时才局部使用，并通过渲染表现验证；常驻基类会扩大影响范围和资源消耗。</p>\\n</li>\\n<li>\\n<p><strong>移动端页面耗电上升，排查发现脚本在 <code>mouseenter</code> 时写入 <code>style.willChange</code>，动画结束后没有清理，你会怎么修？</strong></p>\\n<p>在动画完成、取消或组件卸载路径中把 <code>style.willChange</code> 恢复为 <code>auto</code>，避免浏览器持续保留优化准备。还要检查异常中断是否绕过清理逻辑；只监听正常的 <code>animationend</code> 可能遗漏取消、节点移除等边界。</p>\\n</li>\\n<li>\\n<p><strong>纯 <code>CSS</code> 的按钮悬停动画和脚本驱动的拖拽动画都想使用 <code>will-change</code>，两者的清理策略应当一样吗？</strong></p>\\n<p>不必一样，悬停动画可把 <code>will-change</code> 写在 <code>:hover</code> 中，指针移出后规则自然失效。脚本驱动动画则需显式设置并在结束时恢复 <code>auto</code>；触屏没有稳定的悬停阶段，相关预热时机还要根据实际输入事件调整。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_28-z-index和background的覆盖关系\\"><a class=\\"header-anchor\\" href=\\"#_28-z-index和background的覆盖关系\\">#</a> 28 z-index和background的覆盖关系</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>同一层叠上下文内的绘制顺序（由下到上）：<code>background/border</code> → 负 <code>z-index</code> → 块级盒 → 浮动 → 行内内容 → <code>z-index: 0/auto</code> → 正 <code>z-index</code></li>\\n<li>⚠️ <strong><code>background</code> 在最底层</strong>，所以子元素设 <code>z-index: -1</code> 会沉到父元素背景之下，直接看不见</li>\\n<li>定位元素（哪怕 <code>z-index: auto</code>）会盖住浮动和普通块级元素，与 DOM 顺序无关</li>\\n<li>行内内容高于浮动元素 —— 这正是「文字环绕图片」能成立的基础</li>\\n<li>同层级同 <code>z-index</code> 时按 DOM 顺序，后面的盖前面的；很多覆盖问题调 DOM 顺序就解决了</li>\\n</ul>\\n<p><strong>同一层叠上下文中，背景和边框位于最底层，其上依次是负 <code>z-index</code>、普通块、浮动、行内内容、<code>z-index: auto/0</code> 的定位元素和正 <code>z-index</code>。</strong> 因此子元素使用负 <code>z-index</code> 时，可能沉到父元素背景下面而不可见。定位元素即使没有显式层级，也可能盖住普通块和浮动元素；相同层级下通常由 <code>DOM</code> 顺序决定，后出现的元素在上面。排查覆盖问题时要先确认元素是否属于同一个层叠上下文，否则直接比较 <code>z-index</code> 没有意义。</p>\\n</section>\\n<p>同一个层叠上下文内，元素的绘制顺序是固定的 —— 这就是<strong>层叠顺序（stacking order）</strong>，从下往上一共七层：</p>\\n<pre class=\\"language-text\\"><code>7. z-index 为正的子层叠上下文\\n6. inline / inline-block 内容\\n5. float 浮动元素\\n4. block 块级盒子\\n3. z-index: 0 / auto 的定位元素\\n2. z-index 为负的子层叠上下文\\n1. 当前层叠上下文元素的 background 和 border   ← 最底层\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/gitee/2020/09/4.png\\" alt=\\"\\"></p>\\n<p><strong>几个由此推出的、反直觉的结论：</strong></p>\\n<p><strong>① <code>background</code> 永远在最底层。</strong> 所以给父元素设 <code>z-index: -1</code> 的子元素，会沉到父元素背景<strong>之下</strong>，直接看不见了：</p>\\n<pre class=\\"language-css\\"><code>.parent { position: relative; background: #fff; }\\n.child  { position: absolute; z-index: -1; }  /* 被父元素的白色背景盖住 */\\n</code></pre>\\n<p>这是&quot;我明明设了负 z-index 想让它在文字下面，结果整个消失了&quot;的原因。解法是别让父元素有背景色，或者把背景改用父元素的 <code>::before</code> 伪元素画（伪元素是子元素，参与层叠排序）。</p>\\n<p><strong>② 定位元素（<code>z-index: auto</code>）会盖住浮动元素和普通块级元素</strong>，哪怕它在 DOM 里写在前面。因为第 3 层高于第 4、5 层。</p>\\n<p><strong>③ 行内内容比浮动元素高。</strong> 所以文字会显示在浮动的图片之上 —— 这正是&quot;文字环绕&quot;效果能成立的基础。</p>\\n<p><strong>④ 不设 <code>z-index</code> 时，同层级的定位元素按 DOM 顺序排</strong>，后面的盖前面的。所以很多覆盖问题不用加 <code>z-index</code>，调一下 DOM 顺序就解决了，还避免了 <code>z-index</code> 军备竞赛。</p>\\n<p><strong>排查覆盖问题的正确顺序：</strong></p>\\n<ol>\\n<li>先确认两个元素是不是在<strong>同一个层叠上下文</strong>里 —— 不在的话 <code>z-index</code> 比较毫无意义（见&quot;层叠上下文&quot;那题）。</li>\\n<li>在同一层叠上下文内，按上面七层判断谁在上面。</li>\\n<li>都是定位元素且 <code>z-index</code> 相同时，看 DOM 顺序。</li>\\n</ol>\\n<p><strong>实践建议</strong>：项目里维护少数几个语义化的层级变量（<code>--z-dropdown: 1000</code>、<code>--z-modal: 2000</code>、<code>--z-toast: 3000</code>），而不是随手写 <code>9999</code>。数字段位一旦失控，后面的人只能不断加零。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>活动页把绝对定位的光晕设成 <code>z-index: -1</code>，本想垫在标题后面，却被父容器的白色 <code>background</code> 完全盖住，你怎么解释？</strong></p>\\n<p>负 <code>z-index</code> 子层叠上下文处在普通内容之下，而当前层叠上下文自身的 <code>background</code> 和 <code>border</code> 位于绘制底层，按该场景的层叠关系会使装饰内容不可见。可移除父背景，或改用父元素的 <code>::before</code> 绘制背景并重新组织子元素排序。</p>\\n</li>\\n<li>\\n<p><strong>浮动缩略图旁的文字发生覆盖，开发认为图片写在 <code>DOM</code> 前面就一定压住文字，你会如何判断？</strong></p>\\n<p>不能只看 <code>DOM</code> 先后，同一层叠上下文中行内内容的绘制层级高于浮动元素，这也是文字环绕浮动内容能够成立的基础。应先确认图片是否只是 <code>float</code>，再检查文字或图片有没有定位、<code>z-index</code> 等因素创建不同的排序条件。</p>\\n</li>\\n<li>\\n<p><strong>两个未设置 <code>z-index</code> 的绝对定位徽标发生重叠，设计要求后渲染的徽标在下面，必须增加一个更大的数字吗？</strong></p>\\n<p>不必先加数字，同一层叠上下文内、同为 <code>z-index: auto</code> 的定位元素通常按 <code>DOM</code> 顺序绘制，后面的覆盖前面的。可调整节点顺序满足需求；若组件语义不允许换序，再引入受控层级，避免开启新的数字竞赛。</p>\\n</li>\\n<li>\\n<p><strong>弹窗写了 <code>z-index: 999999</code> 仍被页面头部盖住，值再加一个零有没有意义，你会先查什么？</strong></p>\\n<p>继续加零通常没有意义，应先确认弹窗和头部是否属于同一个层叠上下文，因为不同上下文中的子元素不能直接比较数值。用开发者工具沿祖先查找创建上下文的节点，再决定调整祖先层级或把弹层挂到统一容器。</p>\\n</li>\\n<li>\\n<p><strong>一个大型站点同时存在下拉菜单、固定导航、弹窗和提示条，业务不断提交更大的 <code>z-index</code>，你会怎样收敛？</strong></p>\\n<p>建立少量语义化层级变量，例如 <code>--z-dropdown</code>、<code>--z-modal</code> 和 <code>--z-toast</code>，让业务表达角色而不是猜数字。弹层还应尽量进入统一挂载点，减少嵌套层叠上下文；变量只能管理同一排序体系，无法修复祖先上下文被压住的问题。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_29-移动端中css你是使用什么单位\\"><a class=\\"header-anchor\\" href=\\"#_29-移动端中css你是使用什么单位\\">#</a> 29 移动端中css你是使用什么单位</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>主流两套方案：<code>rem</code> + 动态根字号（配 <code>postcss-pxtorem</code> 自动转换），或直接用 <code>vw</code></li>\\n<li><code>vw</code> 更简单（纯 <code>CSS</code>、无需 <code>JS</code> 计算），<code>rem</code> 更可控（能配合媒体查询限制最大最小值）</li>\\n<li>字号建议用 <code>rem</code> 而不是 <code>vw</code> —— 用户调大系统字号时 <code>rem</code> 会跟随，<code>vw</code> 不会，这是无障碍问题</li>\\n<li>边框和细节尺寸用 <code>px</code>，别跟着缩放；<code>1px</code> 边框问题用 <code>transform: scaleY(0.5)</code> 或 <code>0.5px</code> 处理</li>\\n<li>现在更推荐 <code>clamp(最小, 首选, 最大)</code> 做流体尺寸，一行替代一堆媒体查询</li>\\n</ul>\\n<p><strong>移动端布局我一般以 <code>rem</code> 配合 <code>flex</code> 为主，需要直接跟随视口时也会用 <code>vw</code>、<code>vh</code>。</strong> <code>rem</code> 以根元素字号为基准，方便结合媒体查询统一控制字体和尺寸；<code>vw</code>、<code>vh</code> 则分别按视口宽高计算。局部尺寸也可以用 <code>%</code> 或 <code>em</code>，前者通常相对父级宽度，后者和字体大小相关。至于单独做移动端页面还是与 <code>PC</code> 端共用响应式页面，要根据项目场景取舍。</p>\\n</section>\\n<p><strong>比较常用的</strong>：</p>\\n<ul>\\n<li><code>em</code>：定义字体大小时以父级的字体大小为基准；定义长度单位时以当前字体大小为基准。例父级<code>font-size: 14px</code>，则子级<code>font-size: 1em;</code>为<code>font-size: 14px;</code>；若定义长度时，子级的字体大小如果为<code>14px</code>，则子级<code>width: 2em;</code>为<code>width: 24px</code>。</li>\\n<li><code>rem</code>：以根元素的字体大小为基准。例如<code>html</code>的<code>font-size: 14px</code>，则子级<code>1rem = 14px</code>。</li>\\n<li><code>%</code>：以父级的宽度为基准。例父级<code>width: 200px</code>，则子级<code>width: 50%;height:50%;</code>为<code>width: 100px;height: 100px;</code></li>\\n<li><code>vw和vh</code>：基于视口的宽度和高度(视口不包括浏览器的地址栏工具栏和状态栏)。例如视口宽度为<code>1000px</code>，则<code>60vw = 600px;</code></li>\\n<li><code>vmin和vmax</code>：<code>vmin</code>为当前<code>vw</code> 和<code>vh</code>中较小的一个值；<code>vmax</code>为较大的一个值。例如视口宽度<code>375px</code>，视口高度<code>812px</code>，则<code>100vmin = 375px;</code>，<code>100vmax = 812px;</code></li>\\n</ul>\\n<p><strong>不常用的：</strong></p>\\n<ul>\\n<li><code>ex和ch</code>：<code>ex</code>以字符<code>&quot;x&quot;</code>的高度为基准；例如<code>1ex</code>表示和字符<code>&quot;x&quot;</code>一样长。<code>ch</code>以数字<code>&quot;0&quot;</code>的宽度为基准；例如<code>2ch</code>表示和2个数字<code>&quot;0&quot;</code>一样长。</li>\\n</ul>\\n<p><strong>移动端布局总结</strong>：</p>\\n<ol>\\n<li>移动端布局的方式主要使用rem和flex，可以结合各自的优点，比如flex布局很灵活，但是字体的大小不好控制，我们可以使用rem和媒体查询控制字体的大小，媒体查询视口的大小，然后不同的上视口大小下设置设置<code>html</code>的<code>font-size</code>。</li>\\n<li>可单独制作移动端页面也可响应式pc端移动端共用一个页面。没有好坏，视情况而定，因势利导</li>\\n</ol>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>设计稿里把按钮宽度写成 <code>20em</code>，按钮自身字体从 <code>14px</code> 改到 <code>18px</code> 后布局也变宽了，同事认为 <code>em</code> 永远只看父级字体，你怎么纠正？</strong></p>\\n<p><code>em</code> 用于字体大小时通常以父级字体为基准，用于宽度等长度时则以当前元素的字体大小为基准，因此按钮改字号会同步改变宽度。若尺寸不应跟随组件字号，可改用以根字号为基准的 <code>rem</code>，但根字号变化仍会整体影响它。</p>\\n</li>\\n<li>\\n<p><strong>移动端文章区宽度设为 <code>50%</code>，父容器是 <code>200px</code> 时宽度符合预期，但高度设为 <code>50%</code> 却没有得到想要的比例，你会怎么解释选型问题？</strong></p>\\n<p>百分比尺寸依赖对应包含块条件，宽度按父级宽度计算较直观，而高度需要父级具有可用于计算的明确高度。若目标是保持视觉比例，应先明确容器高度或采用专门的比例约束；不能把宽度百分比的经验直接套到高度。</p>\\n</li>\\n<li>\\n<p><strong>竖屏海报用 <code>100vmin</code> 做正方形画布，在 <code>375 × 812</code> 的视口中会得到什么效果，横屏后风险是什么？</strong></p>\\n<p>竖屏时 <code>vmin</code> 取 <code>vw</code> 与 <code>vh</code> 中较小者，因此 <code>100vmin</code> 对应约 <code>375px</code>，正方形能贴合较短边。横屏后较短边变成视口高度，画布会随之缩放；若页面还受工具栏或安全区域影响，仍需结合容器约束检查。</p>\\n</li>\\n<li>\\n<p><strong>团队要同时维护独立移动页和 <code>PC</code>、移动响应式共用页，负责人要求统一采用一套单位，你会接受吗？</strong></p>\\n<p>不应仅靠统一单位决定架构，独立移动页和响应式共用页都可能合理，选择取决于内容结构与维护边界。移动布局可用 <code>flex</code> 负责弹性排列，再用 <code>rem</code> 和媒体查询控制字号；单位组合解决尺寸适配，不会自动消除两套页面的业务差异。</p>\\n</li>\\n<li>\\n<p><strong>移动端导航要求每项宽度随父容器分配、字号随整体设计基准变化、验证码输入框恰好容纳六个数字，你会分别选什么单位？</strong></p>\\n<p>导航项宽度可用 <code>%</code> 或 <code>flex</code> 分配，整体字号可用由根字号控制的 <code>rem</code>，固定字符容量则适合用以数字 <code>0</code> 宽度为基准的 <code>ch</code>。这些单位各自依赖不同参照物，混用时要明确父宽、根字号和字体变化带来的边界。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_30-说说设备像素、css像素、设备独立像素、dpr、ppi-之间的区别\\"><a class=\\"header-anchor\\" href=\\"#_30-说说设备像素、css像素、设备独立像素、dpr、ppi-之间的区别\\">#</a> 30 说说设备像素、css像素、设备独立像素、dpr、ppi 之间的区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><strong>设备像素（物理像素）</strong>：屏幕上真实的发光点，出厂就固定</li>\\n<li><strong>CSS 像素</strong>：我们写样式用的逻辑单位，是一个抽象概念</li>\\n<li><strong>设备独立像素（DIP）</strong>：设备厂商定义的逻辑分辨率，理想视口宽度用的就是它</li>\\n<li><strong>DPR</strong> = 设备像素 / 设备独立像素。<code>iPhone</code> 上常见 2 或 3，意味着 1 个 CSS 像素由 4 个或 9 个物理像素显示</li>\\n<li><strong>PPI</strong> 是每英寸物理像素数，衡量屏幕精细度，和前面几个不是一个维度</li>\\n<li>两个实际后果：<code>1px</code> 边框在 <code>DPR=2</code> 上会被画成 2 个物理像素显得粗；位图要按 <code>DPR</code> 出 <code>@2x</code>/<code>@3x</code> 才不糊</li>\\n</ul>\\n<p><strong>设备像素是真实显示点，设备独立像素和 <code>CSS</code> 像素是布局使用的逻辑单位，<code>DPR</code> 与 <code>PPI</code> 则描述不同的换算和密度关系。</strong> 无缩放时，<code>1px</code> 通常等于一个设备独立像素，但最终可能由多个设备像素显示。<code>DPR = 设备像素 / 设备独立像素</code>，例如 <code>DPR=2</code> 时，一个逻辑像素区域对应 <code>2×2</code> 个物理像素。<code>PPI</code> 表示每英寸包含的物理像素数，数值越高，屏幕通常越细腻。</p>\\n</section>\\n<p><strong>1. 背景</strong></p>\\n<p>在<code>css</code>中我们通常使用<code>px</code>作为单位，在<code>PC</code>浏览器中<code>css</code>的<code>1</code>个像素都是对应着电脑屏幕的<code>1</code>个物理像素</p>\\n<p>这会造成一种错觉，我们会认为<code>css</code>中的像素就是设备的物理像素</p>\\n<p>但实际情况却并非如此，<code>css</code>中的像素只是一个抽象的单位，在不同的设备或不同的环境中，<code>css</code>中的<code>1px</code>所代表的设备物理像素是不同的</p>\\n<p>当我们做移动端开发时，同为<code>1px</code>的设置，在不同分辨率的移动设备上显示效果却有很大差异</p>\\n<p>这背后就涉及了<code>css</code>像素、设备像素、设备独立像素、<code>dpr</code>、<code>ppi</code>的概念</p>\\n<p><strong>2. 介绍</strong></p>\\n<p><strong>CSS像素</strong></p>\\n<p>CSS像素（<code>css pixel</code>,<code>px</code>）: 适用于<code>web</code>编程，在 CSS 中以 <code>px</code> 为后缀，是一个长度单位</p>\\n<p>在 CSS 规范中，长度单位可以分为两类，绝对单位以及相对单位</p>\\n<p><code>px</code>是一个相对单位，相对的是设备像素（<code>device pixel</code>）</p>\\n<p>一般情况，页面缩放比为<code>1</code>，<code>1</code>个CSS像素等于<code>1</code>个设备独立像素</p>\\n<p><code>CSS</code>像素又具有两个方面的相对性：</p>\\n<ul>\\n<li>在同一个设备上，每<code>1</code>个 <code>CSS</code> 像素所代表的设备像素是可以变化的（比如调整屏幕的分辨率）</li>\\n<li>在不同的设备之间，每<code>1</code>个 <code>CSS</code> 像素所代表的设备像素是可以变化的（比如两个不同型号的手机）</li>\\n</ul>\\n<p>在页面进行缩放操作也会引起<code>css</code>中<code>px</code>的变化，假设页面放大一倍，原来的 <code>1px</code> 的东西变成 <code>2px</code>，在实际宽度不变的情况下<code>1px</code> 变得跟原来的 <code>2px</code> 的长度（长宽）一样了（元素会占据更多的设备像素）</p>\\n<p>假设原来需要 <code>320px</code> 才能填满的宽度现在只需要 <code>160px</code></p>\\n<p><code>px</code>会受到下面的因素的影响而变化：</p>\\n<ul>\\n<li>每英寸像素（<code>PPI</code>）</li>\\n<li>设备像素比（<code>DPR</code>）</li>\\n</ul>\\n<p><strong>设备像素</strong></p>\\n<p>设备像素（<code>device pixels</code>），又称为物理像素</p>\\n<p>指设备能控制显示的最小物理单位，不一定是一个小正方形区块，也没有标准的宽高，只是用于显示丰富色彩的一个“点”而已</p>\\n<p>可以参考公园里的景观变色彩灯，一个彩灯(物理像素)由红、蓝、绿小灯组成，三盏小灯不同的亮度混合出各种色彩</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/c4d2f6aaf3b13a69.png\\" alt=\\"\\"></p>\\n<p>从屏幕在工厂生产出的那天起，它上面设备像素点就固定不变了，单位为<code>pt</code></p>\\n<p><strong>设备独立像素</strong></p>\\n<p>设备独立像素（Device Independent Pixel）：与设备无关的逻辑像素，代表可以通过程序控制使用的虚拟像素，是一个总体概念，包括了CSS像素</p>\\n<p>在<code>javaScript</code>中可以通过<code>window.screen.width/ window.screen.height</code> 查看</p>\\n<p>比如我们会说“电脑屏幕在 <code>2560x1600</code> 分辨率下不适合玩游戏，我们把它调为 <code>1440x900</code>”，这里的“分辨率”（非严谨说法）指的就是设备独立像素</p>\\n<p>一个设备独立像素里可能包含<code>1</code>个或者多个物理像素点，包含的越多则屏幕看起来越清晰</p>\\n<p>至于为什么出现设备独立像素这种虚拟像素单位概念，下面举个例子：</p>\\n<p>iPhone 3GS 和 iPhone 4/4s 的尺寸都是 3.5 寸，但 iPhone 3GS 的分辨率是 320x480，iPhone 4/4s 的分辨率是 640x960</p>\\n<p>这意味着，iPhone 3GS 有 320 个物理像素，iPhone 4/4s 有 640 个物理像素</p>\\n<p>如果我们按照真实的物理像素进行布局，比如说我们按照 320 物理像素进行布局，到了 640 物理像素的手机上就会有一半的空白，为了避免这种问题，就产生了虚拟像素单位</p>\\n<p>我们统一 iPhone 3GS 和 iPhone 4/4s 都是 320 个虚拟像素，只是在 iPhone 3GS 上，最终 1 个虚拟像素换算成 1 个物理像素，在 iphone 4s 中，1 个虚拟像素最终换算成 2 个物理像素</p>\\n<p>至于 1 个虚拟像素被换算成几个物理像素，这个数值我们称之为设备像素比，也就是下面介绍的<code>dpr</code></p>\\n<p><strong>dpr</strong></p>\\n<p><code>dpr</code>（device pixel ratio），设备像素比，代表设备独立像素到设备像素的转换关系，在<code>JavaScript</code>中可以通过 <code>window.devicePixelRatio</code> 获取</p>\\n<p>计算公式如下：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/c9feff5d47e9fba6.png\\" alt=\\"\\"></p>\\n<p>当设备像素比为<code>1:1</code>时，使用1（<code>1×1</code>）个设备像素显示1个CSS像素</p>\\n<p>当设备像素比为<code>2:1</code>时，使用4（<code>2×2</code>）个设备像素显示1个CSS像素</p>\\n<p>当设备像素比为<code>3:1</code>时，使用9（<code>3×3</code>）个设备像素显示1个CSS像素</p>\\n<p>如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/38d1db89f0b327e0.png\\" alt=\\"\\"></p>\\n<p>当<code>dpr</code>为3，那么<code>1px</code>的<code>CSS</code>像素宽度对应<code>3px</code>的物理像素的宽度，1px的<code>CSS</code>像素高度对应<code>3px</code>的物理像素高度</p>\\n<p><strong>ppi</strong></p>\\n<p><code>ppi</code> （pixel per inch），每英寸像素，表示每英寸所包含的像素点数目，更确切的说法应该是像素密度。数值越高，说明屏幕能以更高密度显示图像</p>\\n<p>计算公式如下：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/7fbfde9fbf465a8f.png\\" alt=\\"\\"></p>\\n<p><strong>3. 总结</strong></p>\\n<ul>\\n<li>无缩放情况下，1个CSS像素等于1个设备独立像素</li>\\n<li>设备像素由屏幕生产之后就不发生改变，而设备独立像素是一个虚拟单位会发生改变</li>\\n<li>PC端中，1个设备独立像素 = 1个设备像素 （在<code>100%</code>，未缩放的情况下）</li>\\n<li>在移动端中，标准屏幕（<code>160ppi</code>）下 1个设备独立像素 = 1个设备像素</li>\\n<li>设备像素比（<code>dpr</code>） = <code>设备像素 / 设备独立像素</code></li>\\n<li>每英寸像素（<code>ppi</code>），值越大，图像越清晰</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>同一个移动页面写了 <code>width: 100px</code>，在两台物理分辨率不同但逻辑视口相同的手机上占据比例接近，为什么不能说它固定占一百个物理像素？</strong></p>\\n<p><code>CSS</code> 的 <code>px</code> 是抽象长度单位，无缩放时通常对应一个设备独立像素，而不是固定对应一个物理像素。不同设备会按 <code>DPR</code> 把逻辑像素映射到不同数量的物理像素，因此布局比例可接近，但实际参与显示的像素点数量不同。</p>\\n</li>\\n<li>\\n<p><strong>一台设备的 <code>DPR=2</code>，设计师问一个 <code>1px × 1px</code> 的 <code>CSS</code> 方块会占多少物理像素，你怎么现场算？</strong></p>\\n<p><code>DPR=2</code> 表示一个设备独立像素在宽、高方向各对应两个设备像素，因此该方块通常覆盖 <code>2 × 2</code>，即四个物理像素。这个换算以未额外缩放的常规页面为前提；页面缩放会改变 <code>CSS</code> 像素实际占用的设备像素范围。</p>\\n</li>\\n<li>\\n<p><strong>前端读取 <code>window.screen.width</code> 得到的是逻辑尺寸，测试却拿它和屏幕厂商标注的物理分辨率直接比较，为什么数据对不上？</strong></p>\\n<p><code>window.screen.width</code>、<code>window.screen.height</code> 反映的是设备独立像素维度，不等同于面板固定的物理像素数量。应结合 <code>window.devicePixelRatio</code> 理解两者的映射；系统分辨率设置或页面环境变化也可能影响逻辑尺寸，不能只凭一个值反推面板规格。</p>\\n</li>\\n<li>\\n<p><strong>用户把页面放大一倍后，原来需要约 <code>320px</code> 填满的区域现在只需约 <code>160px</code>，这是物理像素变少了吗？</strong></p>\\n<p>不是，屏幕出厂后物理像素数量不会因页面缩放而改变，变化的是 <code>CSS</code> 像素与设备显示区域之间的映射。放大后一个 <code>CSS</code> 像素占据更多设备像素，所以填满同一实际宽度所需的 <code>CSS</code> 像素数减少；排查时应同时确认缩放比例和 <code>DPR</code>。</p>\\n</li>\\n<li>\\n<p><strong>采购拿两块同为高 <code>DPR</code> 的屏幕比较清晰度，开发直接断言 <code>DPR</code> 更高就一定拥有更高 <code>PPI</code>，这个结论可靠吗？</strong></p>\\n<p>不可靠，<code>DPR</code> 描述设备独立像素到物理像素的映射比例，<code>PPI</code> 描述每英寸包含的物理像素数量，两者关注的维度不同。判断像素密度还需要物理尺寸和物理分辨率；高 <code>DPR</code> 常服务于高密度显示，但不能单独推出具体 <code>PPI</code>。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_31-在移动端中怎样初始化根元素的字体大小\\"><a class=\\"header-anchor\\" href=\\"#_31-在移动端中怎样初始化根元素的字体大小\\">#</a> 31 在移动端中怎样初始化根元素的字体大小</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>rem</code> 适配的原理：根字号跟着屏幕宽度走，所有 <code>rem</code> 尺寸自动等比缩放</li>\\n<li>经典 <code>JS</code> 写法是 <code>html.style.fontSize = 屏宽 / 10 + \'px\'</code>，必须<strong>内联在 <code>head</code></strong> 里最先执行，否则首屏会跳变</li>\\n<li>三个必补细节：<strong>限制最大值</strong>（平板上会撑爆）、监听 <code>resize</code>、监听 <code>pageshow</code> 处理 <code>bfcache</code> 恢复</li>\\n<li>配套用 <code>postcss-pxtorem</code> 自动转换，但要把边框和第三方组件库排除在外</li>\\n<li>现代替代方案是纯 <code>CSS</code>：<code>html { font-size: clamp(50px, 10vw, 75px) }</code>，无脚本、无跳变、自带上下限</li>\\n</ul>\\n<p><strong>做 <code>rem</code> 适配时，我会让根元素字号随屏幕宽度变化，例如按视口宽度除以 <code>10</code> 计算，并设置最大值。</strong> 经典方案是在 <code>head</code> 中内联脚本，通过 <code>getBoundingClientRect().width</code> 取宽度，同时监听 <code>resize</code> 和 <code>pageshow</code>，避免旋转屏幕或从缓存恢复后尺寸不准。工程中可配合 <code>postcss-pxtorem</code> 转换设计稿尺寸，并排除边框和第三方组件。新项目也可以直接写 <code>font-size: clamp(50px, 10vw, 75px)</code>，省掉脚本和首屏跳变。</p>\\n</section>\\n<p><code>rem</code> 适配的核心是：<strong>让根元素字号跟着屏幕宽度走</strong>，这样所有用 <code>rem</code> 写的尺寸都会等比缩放。</p>\\n<p><strong>经典的 <code>JS</code> 方案</strong>（假设设计稿 <code>750px</code>，希望 <code>1rem = 75px</code>，即把屏幕十等分）：</p>\\n<pre class=\\"language-javascript\\"><code>;(function () {\\n  const html = document.documentElement\\n  function setRootFontSize() {\\n    // 屏幕宽度 / 10 作为根字号；限制上限，避免平板上元素被撑得过大\\n    const width = Math.min(html.getBoundingClientRect().width, 750)\\n    html.style.fontSize = width / 10 + &#39;px&#39;\\n  }\\n  setRootFontSize()\\n  window.addEventListener(&#39;resize&#39;, setRootFontSize)\\n  window.addEventListener(&#39;pageshow&#39;, (e) =&gt; e.persisted &amp;&amp; setRootFontSize())\\n})()\\n</code></pre>\\n<p>几个细节：</p>\\n<ul>\\n<li>用 <code>getBoundingClientRect().width</code> 而不是 <code>clientWidth</code>，前者是小数，精度更高</li>\\n<li><strong>必须限制最大值</strong>，否则在 iPad 或折叠屏展开时元素会被撑到荒唐的尺寸</li>\\n<li>监听 <code>pageshow</code> 是为了处理 <code>bfcache</code> 恢复 —— 从其他页面后退回来时不会重新执行脚本，横竖屏变了尺寸就错了</li>\\n<li>这段脚本必须<strong>内联在 <code>head</code> 里、在样式生效之前执行</strong>，否则会出现一瞬间的尺寸跳变</li>\\n</ul>\\n<p><strong>配套的两件事：</strong></p>\\n<ol>\\n<li>\\n<p><code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;</code> —— 注意<strong>不要</strong>加 <code>user-scalable=no</code>，<code>iOS 10</code> 之后已忽略，而且它伤害低视力用户。</p>\\n</li>\\n<li>\\n<p>用 <code>postcss-pxtorem</code> 自动把设计稿的 <code>px</code> 转成 <code>rem</code>，开发时按标注写 <code>px</code> 就行：</p>\\n<pre class=\\"language-js\\"><code>// postcss.config.js\\n&#39;postcss-pxtorem&#39;: {\\n  rootValue: 75,          // 设计稿宽 750 时，1rem = 75px\\n  propList: [&#39;*&#39;, &#39;!border*&#39;],  // 边框保留 px，避免被算成 0.5px 消失\\n  selectorBlackList: [&#39;.van-&#39;], // 排除按 px 设计的第三方组件库\\n}\\n</code></pre>\\n</li>\\n</ol>\\n<p><strong>更现代的替代方案 —— 纯 <code>CSS</code>，不用 <code>JS</code>：</strong></p>\\n<pre class=\\"language-css\\"><code>html {\\n  /* 1vw = 屏宽的 1%，750 设计稿下 75px ≈ 10vw */\\n  font-size: clamp(50px, 10vw, 75px);\\n}\\n</code></pre>\\n<p><code>clamp()</code> 一行同时解决了&quot;跟随缩放&quot;和&quot;上下限&quot;两件事，不需要脚本、不会有首屏跳变、也不用处理 <code>resize</code> 和 <code>bfcache</code>。新项目直接用这个。</p>\\n<p><strong>一个无障碍提醒</strong>：不要把根字号写成固定 <code>px</code>（比如 <code>html { font-size: 16px }</code>），那会覆盖用户在浏览器里设置的默认字号偏好，视力不好的用户没法整体放大。用 <code>vw</code>、<code>%</code> 或 <code>clamp</code> 都不影响这个能力。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个 <code>750px</code> 设计稿的活动页直接写了 <code>html { font-size: 75px }</code>，在 <code>375px</code> 手机和折叠屏展开态都按同一尺寸显示，为什么这不算 <code>rem</code> 适配？</strong></p>\\n<p>固定 <code>75px</code> 只建立了设计稿换算关系，没有让根字号随视口宽度变化，因此不同屏幕上的元素不会等比缩放。应按容器宽度计算，例如取 <code>getBoundingClientRect().width / 10</code>，并设置宽度上限；否则平板和折叠屏展开后，组件可能被放大到不合理尺寸。</p>\\n</li>\\n<li>\\n<p><strong>电商首屏要求样式加载时就完成尺寸换算，团队仍想用 <code>JS</code> 设置根字号，脚本应该放在哪里，为什么不能等应用挂载后再执行？</strong></p>\\n<p>计算脚本应内联在 <code>head</code> 中，并在依赖 <code>rem</code> 的样式生效前执行，初始化时立即写入根字号。若等框架挂载或异步脚本执行，浏览器会先按默认根字号绘制，再按新值重排，用户会看到明显的首屏尺寸跳变；脚本前置则会增加模板中的同步逻辑。</p>\\n</li>\\n<li>\\n<p><strong>同一套 <code>rem</code> 页面既要覆盖 <code>320px</code> 小屏，也要避免在 <code>900px</code> 平板上无限放大，纯 <code>CSS</code> 方案怎么写？</strong></p>\\n<p>可用 <code>html { font-size: clamp(50px, 10vw, 75px); }</code>，让中间区间跟随视口缩放，同时用上下限约束极端设备。它不依赖 <code>resize</code> 脚本，也没有脚本初始化造成的跳变；上下限仍需结合设计稿和实际断点确定，不能把示例数值机械套到所有项目。</p>\\n</li>\\n<li>\\n<p><strong>多页应用从商品详情后退到列表后，横屏状态下卡片仍保持进入详情前的竖屏尺寸，但刷新后恢复正常，你会先查哪条生命周期？</strong></p>\\n<p>应先检查页面是否由 <code>bfcache</code> 恢复，因为这种恢复不会重新执行初始化脚本，根字号可能停留在旧视口宽度。监听 <code>pageshow</code>，在 <code>event.persisted</code> 为 <code>true</code> 时重新计算，同时保留 <code>resize</code> 监听；仅修复应用挂载流程无法覆盖这条返回路径。</p>\\n</li>\\n<li>\\n<p><strong>构建组希望用 <code>postcss-pxtorem</code> 全量转换设计稿标注，但测试发现细边框和第三方组件出现偏差，配置上怎么划边界？</strong></p>\\n<p>业务尺寸可按 <code>rootValue: 75</code> 转换，而 <code>propList</code> 应排除 <code>border</code> 相关属性，避免细边框转成小数 <code>rem</code> 后显示异常。对原本按 <code>px</code> 设计的第三方组件，可用 <code>selectorBlackList</code> 排除其选择器；代价是项目会同时存在 <code>rem</code> 与 <code>px</code>，需要把转换边界写进规范。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_32-移动端中不同手机html默认的字体大小都是一样的吗\\"><a class=\\"header-anchor\\" href=\\"#_32-移动端中不同手机html默认的字体大小都是一样的吗\\">#</a> 32 移动端中不同手机html默认的字体大小都是一样的吗</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>默认是一样的：主流浏览器根元素默认字号统一为 <code>16px</code>，所以 <code>1rem = 16px</code> 是可靠起点</li>\\n<li>但三种情况会变：<strong>用户改了浏览器字体设置</strong>、系统级字体缩放（安卓/iOS 都有）、<code>iOS</code> 的自动文本缩放</li>\\n<li>用户能调大根字号正是无障碍的基础机制，所以<strong>绝不能写 <code>html { font-size: 16px }</code></strong> 把它覆盖掉</li>\\n<li>要设根字号就用相对值：<code>100%</code>、<code>clamp(50px, 10vw, 75px)</code></li>\\n<li>⚠️ <code>rem</code> 适配方案用 <code>JS</code> 强设根字号，实际上就牺牲了这个能力 —— 正文字号最好单独处理</li>\\n</ul>\\n<p><strong>默认情况下基本一样，主流浏览器的 <code>html</code> 根字号通常是 <code>16px</code>，因此默认可按 <code>1rem = 16px</code> 理解。</strong> 但这不是固定不变的，用户修改浏览器字号、系统字体缩放，以及 <code>iOS</code> 的自动文本调整都可能影响实际结果。为了保留无障碍能力，不建议用 <code>html { font-size: 16px }</code> 强行覆盖用户设置，可优先使用 <code>100%</code> 等相对值。若用脚本按屏宽设置根字号，布局能统一缩放，但正文最好单独处理，避免牺牲用户放大文字的能力。</p>\\n</section>\\n<p><strong>默认情况下是一样的：所有主流浏览器的根元素默认字号都是 <code>16px</code>。</strong></p>\\n<p>这是 <code>CSS</code> 规范建议、各家 UA 样式表统一实现的结果，所以 <code>1rem = 16px</code> 可以作为一个可靠的起点。</p>\\n<p><strong>但&quot;默认&quot;这两个字很关键，实际上有三种情况会让它变：</strong></p>\\n<p><strong>① 用户自己改了浏览器设置。</strong>\\n浏览器的&quot;字体大小&quot;设置项直接改的就是根字号 —— 这正是我们<strong>应该保护</strong>的能力。视力不好的用户把默认字号调到 <code>20px</code>，页面上所有用 <code>rem</code> 写的尺寸都会跟着放大，这是无障碍设计的基础机制。</p>\\n<p>所以千万别写 <code>html { font-size: 16px }</code> 这种固定值 —— 它会<strong>彻底覆盖</strong>用户的偏好设置。要设根字号就用相对值：</p>\\n<pre class=\\"language-css\\"><code>/* ❌ 覆盖用户偏好，视力不好的用户没法放大 */\\nhtml { font-size: 16px; }\\n\\n/* ✅ 相对用户设置的百分比，仍然可缩放 */\\nhtml { font-size: 100%; }\\nhtml { font-size: clamp(50px, 10vw, 75px); }  /* 移动端适配，也不写死 px */\\n</code></pre>\\n<p><strong>② 系统级的字体缩放。</strong>\\n安卓和 iOS 系统设置里都有&quot;字体大小&quot;选项。iOS 上 <code>Safari</code> 的&quot;文本大小&quot;会影响页面，安卓的系统字号在部分浏览器（尤其是国产 WebView）也会生效。所以<strong>同一台设备在不同用户手里，根字号可能不同</strong>。</p>\\n<p><strong>③ <code>iOS</code> 的自动文本缩放。</strong>\\n横竖屏切换或某些布局下，<code>Safari</code> 可能自动调整文本大小。用 <code>-webkit-text-size-adjust: 100%</code> 可以禁掉这个行为（注意别写 <code>none</code>，那会连用户主动缩放也一起禁掉）。</p>\\n<p><strong>这对 <code>rem</code> 适配方案意味着什么？</strong></p>\\n<p>如果你用 <code>JS</code> 强行把根字号设成 <code>屏宽 / 10</code>，实际上就把用户的字号偏好覆盖掉了 —— 这是 <code>rem</code> 适配方案一个被普遍忽视的无障碍代价。折中做法是：<strong>布局尺寸用 <code>rem</code> 跟随屏宽，但正文字号单独用 <code>em</code> 或固定的可缩放单位</strong>，让文字仍然能被用户放大。</p>\\n<p>面试时能把&quot;默认 16px&quot;和&quot;但用户和系统都可能改，而且我们应该允许他们改&quot;一起说出来，比只答一个数字有价值得多。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>验收人员说主流手机的 <code>1rem</code> 都是 <code>16px</code>，所以代码里写死 <code>html { font-size: 16px }</code> 最稳定；面对低视力用户把默认字号调到 <code>20px</code> 的场景，你同意吗？</strong></p>\\n<p>不同手机在默认设置下通常都以 <code>16px</code> 为根字号，但不能据此写死 <code>16px</code>。固定值会覆盖用户的浏览器字号偏好，使原本应随 <code>rem</code> 放大的内容失去无障碍能力；若无需改造比例，使用 <code>100%</code> 更合适，但仍要检查固定高度容器是否会截断文字。</p>\\n</li>\\n<li>\\n<p><strong>资讯页要求布局按屏宽缩放，同时正文要响应用户放大字体；现有脚本却把根字号强设为 <code>屏宽 / 10</code>，你会怎样拆分职责？</strong></p>\\n<p>脚本写入像素根字号会覆盖用户偏好，因此不应让正文排版完全依赖这一个根值。可让布局尺寸继续按屏宽体系计算，而正文字号使用能够响应用户设置的相对单位，并让文本容器高度自适应；这种拆分会增加字号与布局两套尺度的维护成本。</p>\\n</li>\\n<li>\\n<p><strong>同一页面在桌面模拟器正常，到了部分安卓 <code>WebView</code> 或开启系统大字体的设备上文字明显变大，能否直接断定浏览器默认根字号不是 <code>16px</code>？</strong></p>\\n<p>不能直接断定默认值发生了变化，还要区分用户浏览器设置、系统级字体缩放和宿主 <code>WebView</code> 的文本调整行为。应在目标环境读取计算样式，并对照系统字号与浏览器设置逐项复现；若只在特定宿主出现，修复范围应限制在该环境，避免全局抵消用户主动放大。</p>\\n</li>\\n<li>\\n<p><strong>横竖屏切换后，<code>Safari</code> 自动把新闻正文放大，产品要求保持排版稳定但不能禁用用户缩放，你会怎样处理？</strong></p>\\n<p>可设置 <code>-webkit-text-size-adjust: 100%</code>，抑制浏览器在特定布局或方向变化下的自动文本调整，同时保留用户主动缩放能力。不要写成 <code>none</code>，因为它可能一并限制用户需要的文字放大；修改后仍需在横竖屏和系统大字体条件下复测。</p>\\n</li>\\n<li>\\n<p><strong>无障碍走查要求字号放大后正文不能被按钮和卡片裁掉，但视觉团队坚持所有卡片高度与设计稿一致，你会如何处理这组选型冲突？</strong></p>\\n<p>应优先取消承载文字区域的固定 <code>height</code>，改用内容自适应或 <code>min-height</code>，否则字号变化后截断几乎不可避免。布局尺度可以继续跟随屏宽，但文本必须保留可缩放路径；代价是卡片高度可能不再整齐，需要通过间距、网格对齐或内容策略重新协调视觉。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_33-line-height-如何继承\\"><a class=\\"header-anchor\\" href=\\"#_33-line-height-如何继承\\">#</a> 33 line-height 如何继承</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>继承规则和写法有关：写具体值（<code>30px</code>）或百分比（<code>200%</code>）传的是<strong>计算后的结果</strong>，写无单位数字（<code>1.5</code>）传的是<strong>倍数</strong></li>\\n<li>只有无单位数字能让子元素按<strong>自己的字号</strong>算行高 —— 这是唯一&quot;跟着走&quot;的写法</li>\\n<li>经典事故：父级 <code>line-height: 200%</code> 算出 <code>32px</code> 固定传下去，子元素字号调到 <code>32px</code> 时文字挤成一团</li>\\n<li><strong>规范做法：<code>line-height</code> 一律写无单位数字</strong>，这是所有主流设计系统的统一建议</li>\\n<li>行盒高度取一行内所有内容行高的最大值；<code>normal</code> 由字体决定（约 1.1~1.3），精确排版不要依赖它</li>\\n</ul>\\n<p><strong><code>line-height</code> 写具体值或百分比时，子元素继承计算后的行高；写无单位数字时，继承的是倍数。</strong> 比如父元素设为 <code>200%</code>，按 <code>16px</code> 字号算出的 <code>32px</code> 会直接传下去，子元素字号变大后行高仍不变。改成 <code>1.5</code>，子元素就会用自己的 <code>font-size</code> 重新计算，所以我一般优先使用无单位数字。只有明确需要固定单行高度时才考虑具体值，而 <code>normal</code> 受字体影响，不适合精确排版。</p>\\n</section>\\n<p><code>line-height</code> 的继承规则和写法直接相关，这是它区别于其他继承属性的地方：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>父元素写法</th>\\n<th>子元素继承到什么</th>\\n<th>结果</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>line-height: 30px</code></td>\\n<td>继承<strong>计算后的具体值</strong> <code>30px</code></td>\\n<td>子元素字号再大，行高还是 30px</td>\\n</tr>\\n<tr>\\n<td><code>line-height: 200%</code></td>\\n<td>继承<strong>父元素算出来的结果</strong></td>\\n<td>父级 <code>16px</code> × 200% = <code>32px</code>，固定传下去</td>\\n</tr>\\n<tr>\\n<td><code>line-height: 1.5</code></td>\\n<td>继承<strong>倍数本身</strong></td>\\n<td>子元素按<strong>自己的</strong> <code>font-size</code> × 1.5</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>关键差别就在最后一行：只有无单位数字是&quot;传比例&quot;，其余都是&quot;传结果&quot;。</strong></p>\\n<p><strong>这会出什么问题？</strong></p>\\n<pre class=\\"language-css\\"><code>.parent { font-size: 16px; line-height: 200%; }  /* 算出 32px 传给子元素 */\\n.child  { font-size: 32px; }                      /* 行高还是 32px，文字紧贴在一起 */\\n</code></pre>\\n<p>标题、大字号的价格、加粗的强调文本都会撞上这个问题 —— 字号变大了但行高没跟上，文字挤成一团甚至上下重叠。</p>\\n<p>改成无单位数字就正常：</p>\\n<pre class=\\"language-css\\"><code>.parent { font-size: 16px; line-height: 1.5; }   /* 传的是 1.5 这个倍数 */\\n.child  { font-size: 32px; }                      /* 自己算：32 × 1.5 = 48px ✅ */\\n</code></pre>\\n<p><strong>所以规范是：<code>line-height</code> 一律写无单位数字。</strong> 这几乎是所有 <code>CSS</code> 规范文档和主流设计系统的统一建议，也是最容易被忽略的一条排版规则。</p>\\n<p><strong>什么时候用具体值？</strong> 只有一种情况 —— 你<strong>故意</strong>要固定行高，比如单行文本垂直居中（<code>height: 40px; line-height: 40px</code>）。但这种写法本身就该被 <code>flex</code> 取代了，因为它一换行就崩。</p>\\n<p><strong>顺带说清 <code>line-height</code> 的几个相关概念：</strong></p>\\n<ul>\\n<li><strong>行盒高度</strong>由行内所有内容的行高共同决定，取最大的那个 —— 所以一行里混了一个大字号的 <code>span</code>，整行都会被撑高。</li>\\n<li><strong>半леading</strong>：<code>line-height</code> 减去字体本身高度的差值会被<strong>平均分配到文字上下</strong>，这就是为什么文字看起来不是紧贴行盒顶部的。</li>\\n<li><code>line-height: normal</code> 由字体自己决定（通常 1.1~1.3 之间），不同字体不一样，所以做精确排版时不该依赖它。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>设计系统在 <code>body</code> 上写了 <code>font-size: 16px; line-height: 200%</code>，文章页的 <code>32px</code> 两行标题却显得很挤；子元素不是会继承 <code>200%</code> 吗？</strong></p>\\n<p>子元素继承的不是 <code>200%</code> 这个比例，而是父元素按 <code>16px × 200%</code> 算出的 <code>32px</code> 行高。标题字号变成 <code>32px</code> 后，行高仍为 <code>32px</code>，自然失去上下留白；改用无单位的 <code>line-height: 1.5</code>，子元素才会按自己的字号重新计算。</p>\\n</li>\\n<li>\\n<p><strong>组件库里标题、正文和价格字号跨度很大，团队准备统一写一条全局行高规则，应该选择 <code>24px</code>、<code>150%</code> 还是 <code>1.5</code>？</strong></p>\\n<p>全局继承规则应优先使用无单位的 <code>1.5</code>，因为传给后代的是倍数，各组件会结合自身 <code>font-size</code> 计算行高。<code>24px</code> 和 <code>150%</code> 都会把父级算出的具体结果传下去，容易压缩大字号内容；局部确需精确视觉节奏时，再由组件覆盖。</p>\\n</li>\\n<li>\\n<p><strong>商品价格一行里混入一个更大字号的折扣 <code>span</code> 后，整行高度突然增加，但父元素的 <code>line-height</code> 没改，你会怎样解释并定位？</strong></p>\\n<p>行盒高度会受该行所有行内内容影响，并由其中较大的行高撑开，因此大字号 <code>span</code> 足以改变整行高度。应检查它的计算后 <code>font-size</code> 与 <code>line-height</code>，而不只看父元素；若强行压低子元素行高，可能造成字形拥挤或上下重叠。</p>\\n</li>\\n<li>\\n<p><strong>一个 <code>40px</code> 高的旧导航用 <code>height: 40px; line-height: 40px</code> 做垂直居中，国际化后文案换成两行并被裁切，你会怎么修？</strong></p>\\n<p>固定高度等于固定行高只适用于确定的单行文本，文案换行后第二行没有可用空间。应改用 <code>flex</code> 的 <code>align-items: center</code> 或 <code>grid</code> 的对齐能力，并允许容器高度随内容增长；如果产品坚持固定栏高，就必须限制文案行数并接受信息截断。</p>\\n</li>\\n<li>\\n<p><strong>品牌字体切换后，使用 <code>line-height: normal</code> 的正文密度与原字体不一致，排版负责人却认为浏览器会自动给出统一结果，你会如何判断？</strong></p>\\n<p><code>normal</code> 由具体字体决定，常见结果大约落在字号的 <code>1.1</code> 到 <code>1.3</code> 倍之间，并不保证跨字体一致。需要在目标字体下检查计算结果和多行文本，再显式设置无单位行高；显式值提高一致性，但仍应为不同字形和语言保留测试空间。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_34-css-怎么开启硬件加速-gpu-加速\\"><a class=\\"header-anchor\\" href=\\"#_34-css-怎么开启硬件加速-gpu-加速\\">#</a> 34 css 怎么开启硬件加速(GPU 加速)</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>触发方式：<code>transform: translateZ(0)</code> / <code>translate3d(0,0,0)</code>（老写法）、<code>will-change: transform</code>（推荐）、<code>backface-visibility: hidden</code></li>\\n<li>原理是把元素提升为独立<strong>合成层</strong>，位移和透明度变化只需矩阵运算，交给合成线程 + <code>GPU</code>，不占主线程</li>\\n<li>首选 <code>will-change</code> 而不是 <code>translateZ(0)</code> —— 语义明确，而且不会顺带引入 3D 变换的副作用</li>\\n<li>⚠️ 代价：每层占 <code>宽 × 高 × 4</code> 字节显存、可能引发层爆炸、部分场景文字抗锯齿退化发虚</li>\\n<li>三条纪律：只给真在动的元素加、<strong>动画结束要移除</strong>、用 <code>Layers</code> 面板核对层数</li>\\n</ul>\\n<p><strong>常见做法是使用 <code>transform: translateZ(0)</code>、<code>translate3d(0, 0, 0)</code> 或 <code>will-change: transform</code>，让元素有机会提升为合成层并使用 <code>GPU</code>。</strong> <code>transform</code>、<code>opacity</code>、<code>filter</code> 等变化进入合成阶段后，可以减少部分重复渲染工作。实际开发中我会优先用语义更明确的 <code>will-change</code>，并且只加在确实需要动画的元素上。合成层过多会造成层爆炸和额外资源开销，还要留意元素重叠带来的非预期层提升。</p>\\n</section>\\n<p>浏览器在处理下面的 <code>css</code> 的时候，会使用 <code>GPU</code> 渲染</p>\\n<ul>\\n<li><code>transform</code>（当 <code>3D</code> 变换的样式出现时会使用 <code>GPU</code> 加速）</li>\\n<li><code>opacity</code></li>\\n<li><code>filter</code></li>\\n<li><code>will-change</code></li>\\n</ul>\\n<blockquote>\\n<ul>\\n<li>采用 <code>transform: translateZ(0)</code></li>\\n<li>采用 <code>transform: translate3d(0, 0, 0)</code></li>\\n<li>使用 <code>CSS</code> 的 <code>will-change</code>属性。 <code>will-change</code> 可以设置为<code>opacity</code>、<code>transform</code>、<code>top、</code>left<code>、</code>bottom<code>、</code>right`</li>\\n</ul>\\n</blockquote>\\n<blockquote>\\n<p>注意！层爆炸，由于某些原因可能导致产生大量不在预期内的合成层，虽然有浏览器的层压缩机制，但是也有很多无法进行压缩的情况，这就可能出现层爆炸的现象（简单理解就是，很多不需要提升为合成层的元素因为某些不当操作成为了合成层）。解决层爆炸的问题，最佳方案是打破 <code>overlap</code> 的条件，也就是说让其他元素不要和合成层元素重叠。简单直接的方式：使用 <code>3D</code> 硬件加速提升动画性能时，最好给元素增加一个 <code>z-index</code> 属性，人为干扰合成的排序，可以有效减少创建不必要的合成层，提升渲染性能，移动端优化效果尤为明显。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>动画评审时有人要求给所有静态卡片加 <code>translateZ(0)</code>，理由是“只要上 <code>GPU</code> 就一定更快”；一个长列表里这样做的反例是什么？</strong></p>\\n<p>把大量静态卡片强制提升为合成层可能造成层爆炸，额外的层管理与合成成本会抵消收益，因此不能把提层当成通用加速开关。应只处理确有动画或绘制瓶颈的元素，并通过浏览器工具观察分层；具体是否提层仍由浏览器实现和页面重叠关系决定。</p>\\n</li>\\n<li>\\n<p><strong>轮播组件只在切换时执行位移动画，工程上你会选 <code>translate3d(0, 0, 0)</code> 还是 <code>will-change: transform</code> 来表达优化意图？</strong></p>\\n<p>更适合用 <code>will-change: transform</code> 声明即将变化的属性，或直接让实际 <code>transform</code> 动画交给浏览器处理。<code>translate3d(0, 0, 0)</code> 和 <code>translateZ(0)</code> 能触发常见的提层路径，但属于人为技巧，容易长期残留；<code>will-change</code> 也不应无差别常驻。</p>\\n</li>\\n<li>\\n<p><strong>一个弹层同时动画 <code>opacity</code>、<code>transform</code> 和 <code>top</code>，视觉希望全部交给 <code>GPU</code>，你会怎样调整属性选择？</strong></p>\\n<p>应优先用 <code>transform</code> 表达位移，并配合 <code>opacity</code> 做透明度变化，这些属性更适合合成阶段处理。虽然 <code>will-change</code> 可以声明 <code>top</code> 等即将变化的属性，但声明本身不等于保证完整的 <code>GPU</code> 加速；直接动画定位属性还可能带来额外布局成本。</p>\\n</li>\\n<li>\\n<p><strong>移动端页面加了 <code>3D transform</code> 后，开发者工具里出现大量意外合成层，而且它们与浮层区域互相覆盖，你会按什么线索排查？</strong></p>\\n<p>应先定位被提升的目标层及与其重叠的兄弟元素，重叠关系可能迫使浏览器创建更多合成层。优先减少不必要的 <code>3D transform</code> 或打破覆盖关系；若排序允许，也可用明确的 <code>z-index</code> 干预合成顺序，但错误层叠上下文可能改变遮挡效果。</p>\\n</li>\\n<li>\\n<p><strong>低端移动设备上的滚动页已经出现分层过多，交互负责人仍想给每个按钮预设 <code>will-change</code> 保证点击动画流畅，你会如何取舍？</strong></p>\\n<p>不应为所有按钮长期预设 <code>will-change</code>，因为潜在合成层数量会继续增加，反而可能恶化滚动和合成表现。只对即将发生动画的少量元素声明，并在不再需要时撤销，同时核对层叠与重叠情况；若浏览器已能合理处理动画，额外声明可能没有收益。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_35-flex-1-是哪些属性组成的\\"><a class=\\"header-anchor\\" href=\\"#_35-flex-1-是哪些属性组成的\\">#</a> 35 flex:1 是哪些属性组成的</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>flex</code> 是 <code>flex-grow</code>、<code>flex-shrink</code>、<code>flex-basis</code> 的缩写，<code>flex: 1</code> 展开为 <code>1 1 0%</code></li>\\n<li><code>flex-grow</code>（默认 <code>0</code>）：有剩余空间时的<strong>放大</strong>比例；都设 1 就等分剩余空间</li>\\n<li><code>flex-shrink</code>（默认 <code>1</code>）：空间不足时的<strong>缩小</strong>比例；设 <code>0</code> 表示&quot;我绝不缩小&quot;</li>\\n<li><code>flex-basis</code>（默认 <code>auto</code>）：分配空间<strong>之前</strong>的主轴尺寸 —— <code>0%</code> 才是真等宽，<code>auto</code> 会先按内容打底</li>\\n<li>常用简写：<code>flex: 1</code>（等分）、<code>flex: auto</code>（<code>1 1 auto</code>，内容优先）、<code>flex: none</code>（<code>0 0 auto</code>，固定不伸缩）</li>\\n</ul>\\n<p><strong><code>flex: 1</code> 是 <code>flex-grow</code>、<code>flex-shrink</code> 和 <code>flex-basis</code> 的简写，通常展开为 <code>1 1 0%</code>。</strong> <code>flex-grow: 1</code> 表示有剩余空间时参与放大，<code>flex-shrink: 1</code> 表示空间不足时允许缩小，而 <code>0%</code> 表示分配前不按内容尺寸打底。简单来说，多个项目都写 <code>flex: 1</code> 时更容易等分容器空间。若希望先参考项目原本大小，可以用 <code>flex: auto</code>；完全不伸缩则用 <code>flex: none</code>。</p>\\n</section>\\n<ul>\\n<li><code>flex</code> 实际上是 <code>flex-grow</code>、<code>flex-shrink</code> 和 <code>flex-basis</code> 三个属性的缩写。</li>\\n<li><code>flex-grow</code>：定义项目的的放大比例；\\n<ul>\\n<li>默认为<code>0</code>，即 即使存在剩余空间，也不会放大；</li>\\n<li>所有项目的<code>flex-grow</code>为<code>1</code>：等分剩余空间（自动放大占位）；</li>\\n<li><code>flex-grow</code>为<code>n</code>的项目，占据的空间（放大的比例）是<code>flex-grow</code>为<code>1</code>的<code>n</code>倍</li>\\n</ul>\\n</li>\\n<li><code>flex-shrink</code>：定义项目的缩小比例；\\n<ul>\\n<li>默认为<code>1</code>，即 如果空间不足，该项目将缩小；</li>\\n<li>所有项目的<code>flex-shrink</code>为<code>1</code>：当空间不足时，缩小的比例相同；</li>\\n<li><code>flex-shrink</code>为<code>0</code>：空间不足时，该项目不会缩小；</li>\\n<li><code>flex-shrink</code>为<code>n</code>的项目，空间不足时缩小的比例是<code>flex-shrink</code>为<code>1</code>的<code>n</code>倍。</li>\\n</ul>\\n</li>\\n<li><code>flex-basis</code>： 定义在分配多余空间之前，项目占据的主轴空间（<code>main size</code>），浏览器根据此属性计算主轴是否有多余空间\\n<ul>\\n<li>默认值为<code>auto</code>，即 项目原本大小；</li>\\n<li>设置后项目将占据固定空间。</li>\\n</ul>\\n</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>三列看板都写了 <code>flex-grow: 1</code>，但中间列初始内容更长，最终宽度并不相等；为什么“增长比例相同”不等于“三列等宽”？</strong></p>\\n<p><code>flex-grow: 1</code> 只规定剩余空间按相同比例分配，并没有清空各项目原本的主轴基准尺寸。内容或既有宽度不同，分配前的起点就不同；若目标是真正等分，应同时把 <code>flex-basis</code> 设为 <code>0</code>，并检查最小内容尺寸是否仍在限制收缩。</p>\\n</li>\\n<li>\\n<p><strong>后台页面需要左侧导航固定 <code>240px</code>、右侧内容占满剩余宽度，且窗口缩小时导航不能被压缩，你会怎样写两个项目的 <code>flex</code>？</strong></p>\\n<p>导航可写 <code>flex: 0 0 240px</code>，表示不放大、不缩小且基准尺寸为 <code>240px</code>；内容区写 <code>flex: 1</code> 接收剩余空间。若内容区包含长文本，还应允许它实际收缩，否则可能由内容的最小尺寸撑破容器；固定侧栏会把窄屏压力全部转移给主区。</p>\\n</li>\\n<li>\\n<p><strong>一个横向工具栏原本用 <code>flex: 1</code> 等分按钮，现在产品要求主按钮宽度是其他按钮增长份额的两倍，但每个按钮文案长度不同，单改 <code>flex-grow</code> 能满足吗？</strong></p>\\n<p>单改 <code>flex-grow</code> 只能控制剩余空间的增长比例，文案形成的基础尺寸仍会影响最终宽度。若要按份额稳定分配，应把各项 <code>flex-basis</code> 统一为 <code>0</code>，再分别设置增长系数；文案过长时仍要处理换行、溢出或最小宽度。</p>\\n</li>\\n<li>\\n<p><strong>订单卡片在窄屏上溢出，所有子项的 <code>flex-shrink</code> 都是 <code>1</code>，开发者因此认定它们一定会等比例缩小；你会先检查什么？</strong></p>\\n<p>应先检查每项的 <code>flex-basis</code>、原始主轴尺寸以及内容形成的最小尺寸，因为收缩并不只由 <code>flex-shrink</code> 数字决定。长单词、固定宽度或最小尺寸都可能阻止项目缩小；需要明确哪个区域允许收缩，必要时处理溢出，而不是继续提高收缩系数。</p>\\n</li>\\n<li>\\n<p><strong>组件库评审中，一方主张自适应区域统一用 <code>flex: auto</code>，另一方要求统一用 <code>flex: 1</code>；在内容长度差异很大的卡片组里怎么选？</strong></p>\\n<p>希望各项先保留自身内容或原始尺寸，再分配剩余空间时，应选以 <code>auto</code> 为基准的方案；希望忽略初始内容宽度、主要按增长比例分配时，应把基准设为 <code>0</code>，常见写法是 <code>flex: 1</code>。两者不能仅按代码长短统一，长内容和空间不足场景必须纳入验收。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_36-css选择器有哪些-优先级-哪些属性可以继承\\"><a class=\\"header-anchor\\" href=\\"#_36-css选择器有哪些-优先级-哪些属性可以继承\\">#</a> 36 css选择器有哪些？优先级？哪些属性可以继承？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>选择器分五类：基础（元素/类/ID/通配）、组合（后代 / <code>&gt;</code> / <code>+</code> / <code>~</code>）、属性（<code>[type=text]</code>）、伪类（<code>:hover</code>/<code>:nth-child</code>/<code>:has</code>）、伪元素（<code>::before</code>）</li>\\n<li>优先级是三元组 <code>(ID, 类/属性/伪类, 元素/伪元素)</code> 逐位比较，不进位；同权重时后写的赢</li>\\n<li>完整顺序：<code>!important</code> &gt; 内联 <code>style</code> &gt; ID &gt; 类 &gt; 元素 &gt; 通配符 &gt; 继承</li>\\n<li>可继承的是文字相关属性（<code>font</code>/<code>color</code>/<code>line-height</code>/<code>text-align</code>/<code>list-style</code>）和自定义属性 <code>--x</code></li>\\n<li>不可继承的是盒子相关属性（<code>width</code>/<code>margin</code>/<code>border</code>/<code>background</code>/<code>display</code>/<code>position</code>）</li>\\n</ul>\\n<p><strong><code>CSS</code> 选择器可归为基础、组合、属性和伪类／伪元素几类，优先级按 <code>ID</code>、类级、元素级的权重逐位比较。</strong> <code>ID</code> 高于类、属性和伪类，后者又高于元素和伪元素；权重相同时，后写的规则生效，<code>!important</code> 和内联样式还要单独考虑。可继承的通常是文字与列表相关属性，如 <code>color</code>、<code>font</code>、<code>line-height</code>、<code>text-align</code> 和 <code>list-style</code>。盒模型、背景、布局及定位属性通常不继承，例如 <code>width</code>、<code>margin</code>、<code>display</code> 和 <code>position</code>。</p>\\n</section>\\n<p>关于<code>css</code>属性选择器常用的有：</p>\\n<ul>\\n<li><code>id</code>选择器（<code>#box</code>），选择id为box的元素</li>\\n<li>类选择器（<code>.one</code>），选择类名为one的所有元素</li>\\n<li>标签选择器（<code>div</code>），选择标签为div的所有元素</li>\\n<li>后代选择器（<code>#box div</code>），选择id为box元素内部所有的div元素</li>\\n<li>子选择器（<code>.one&gt;one_1</code>），选择父元素为.one的所有.one_1的元素</li>\\n<li>相邻同胞选择器（<code>.one+.two</code>），选择紧接在.one之后的所有.two元素</li>\\n<li>群组选择器（<code>div,p</code>），选择div、p的所有元素</li>\\n</ul>\\n<p>还有一些使用频率相对没那么多的选择器：</p>\\n<ul>\\n<li>伪类选择器</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>:link ：选择未被访问的链接\\n:visited：选取已被访问的链接\\n:active：选择活动链接\\n:hover ：鼠标指针浮动在上面的元素\\n:focus ：选择具有焦点的\\n:first-child：父元素的首个子元素\\n</code></pre>\\n<ul>\\n<li>伪元素选择器</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>:first-letter ：用于选取指定选择器的首字母\\n:first-line ：选取指定选择器的首行\\n:before : 选择器在被选元素的内容前面插入内容\\n:after : 选择器在被选元素的内容后面插入内容\\n</code></pre>\\n<ul>\\n<li>属性选择器</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>[attribute] 选择带有attribute属性的元素\\n[attribute=value] 选择所有使用attribute=value的元素\\n[attribute~=value] 选择attribute属性包含value的元素\\n[attribute|=value]：选择attribute属性以value开头的元素·\\n</code></pre>\\n<p>在<code>CSS3</code>中新增的选择器有如下：</p>\\n<ul>\\n<li>层次选择器（<code>p~ul</code>），选择前面有<code>p</code>元素的每个<code>ul</code>元素</li>\\n<li>伪类选择器</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>:first-of-type 表示一组同级元素中其类型的第一个元素\\n:last-of-type 表示一组同级元素中其类型的最后一个元素\\n:only-of-type 表示没有同类型兄弟元素的元素\\n:only-child 表示没有任何兄弟的元素\\n:nth-child(n) 根据元素在一组同级中的位置匹配元素\\n:nth-last-of-type(n) 匹配给定类型的元素，基于它们在一组兄弟元素中的位置，从末尾开始计数\\n:last-child 表示一组兄弟元素中的最后一个元素\\n:root 设置HTML文档\\n:empty 指定空的元素\\n:enabled 选择可用元素\\n:disabled 选择被禁用元素\\n:checked 选择选中的元素\\n:not(selector) 选择与 &lt;selector&gt; 不匹配的所有元素\\n</code></pre>\\n<ul>\\n<li>属性选择器</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>[attribute*=value]：选择attribute属性值包含value的所有元素\\n[attribute^=value]：选择attribute属性开头为value的所有元素\\n[attribute$=value]：选择attribute属性结尾为value的所有元素\\n</code></pre>\\n<p><strong>继承属性</strong></p>\\n<p>在<code>css</code>中，继承是指的是给父元素设置一些属性，后代元素会自动拥有这些属性</p>\\n<p>关于继承属性，可以分成：</p>\\n<ul>\\n<li>字体系列属性</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>font:组合字体\\nfont-family:规定元素的字体系列\\nfont-weight:设置字体的粗细\\nfont-size:设置字体的尺寸\\nfont-style:定义字体的风格\\nfont-variant:偏大或偏小的字体\\n</code></pre>\\n<ul>\\n<li>文本系列属性</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>text-indent：文本缩进\\ntext-align：文本水平对刘\\nline-height：行高\\nword-spacing：增加或减少单词间的空白\\nletter-spacing：增加或减少字符间的空白\\ntext-transform：控制文本大小写\\ndirection：规定文本的书写方向\\ncolor：文本颜色\\n</code></pre>\\n<ul>\\n<li>元素可见性</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>visibility\\n</code></pre>\\n<ul>\\n<li>表格布局属性</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>caption-side：定位表格标题位置\\nborder-collapse：合并表格边框\\nborder-spacing：设置相邻单元格的边框间的距离\\nempty-cells：单元格的边框的出现与消失\\ntable-layout：表格的宽度由什么决定\\n</code></pre>\\n<ul>\\n<li>列表属性</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>list-style-type：文字前面的小点点样式\\nlist-style-position：小点点位置\\nlist-style：以上的属性可通过这属性集合\\n</code></pre>\\n<ul>\\n<li>引用</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>quotes：设置嵌套引用的引号类型\\n</code></pre>\\n<ul>\\n<li>光标属性</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>cursor：箭头可以变成需要的形状\\n</code></pre>\\n<p>继承中比较特殊的几点：</p>\\n<ul>\\n<li><code>a</code> 标签的字体颜色不能被继承</li>\\n<li><code>h1-h6</code>标签字体的大下也是不能被继承的</li>\\n</ul>\\n<p><strong>无继承的属性</strong></p>\\n<ul>\\n<li><code>display</code></li>\\n<li>文本属性：<code>vertical-align</code>、<code>text-decoration</code></li>\\n<li>盒子模型的属性：宽度、高度、内外边距、边框等</li>\\n<li>背景属性：背景图片、颜色、位置等</li>\\n<li>定位属性：浮动、清除浮动、定位<code>position</code>等</li>\\n<li>生成内容属性：<code>content</code>、<code>counter-reset</code>、<code>counter-increment</code></li>\\n<li>轮廓样式属性：<code>outline-style</code>、<code>outline-width</code>、<code>outline-color</code>、<code>outline</code></li>\\n<li>页面样式属性：<code>size</code>、<code>page-break-before</code>、<code>page-break-after</code></li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品卡片里同时命中 <code>#app .card p</code> 和 <code>.page .card__desc</code>，后者写在文件末尾却仍被划掉；有人坚持“越靠后优先级越高”，你怎么判断？</strong></p>\\n<p>文件顺序只在优先级相同且来源等条件一致时决定胜负，这里应先比较选择器权重，含 <code>id</code> 的规则通常更强。用 <code>DevTools</code> 查看最终命中的声明与来源，再减少库样式权重或增加稳定的作用域；不要靠无限嵌套或滥用 <code>!important</code>。</p>\\n</li>\\n<li>\\n<p><strong>组件库的按钮需要业务侧稳定换色，你会把默认规则写成 <code>.dialog .footer button.primary</code>，还是给调用方留一个简单类名？为什么？</strong></p>\\n<p>默认规则应保持较低且可预测的权重，让业务通过组件根类名或公开类名完成覆盖。复杂后代链既增加耦合，也会迫使调用方继续堆权重；若能维护组件库，可用扁平类名组织样式，避免把 <code>DOM</code> 层级变成隐含接口。</p>\\n</li>\\n<li>\\n<p><strong>评论列表混有 <code>h3</code> 分组标题和 <code>p</code> 评论，产品要求每隔一条评论换底色；你写的 <code>p:nth-child(2n)</code> 出现断档，应该怎么改？</strong></p>\\n<p><code>nth-child</code> 按全部兄弟元素的位置计数，因此插入 <code>h3</code> 后，偶偶数位置不再等同于评论序号。若评论均为 <code>p</code>，可改用 <code>p:nth-of-type(2n)</code> 按同类型元素计数；若节点类型也不稳定，应增加明确的结构或业务类名。</p>\\n</li>\\n<li>\\n<p><strong>主题色设在页面根节点后，普通正文继承成功，但链接和标题仍显示浏览器或组件样式；排查时你会先看哪些地方？</strong></p>\\n<p>先在 <code>Computed</code> 面板确认 <code>color</code>、<code>font-size</code> 的最终值及声明来源，链接和标题可能已有直接命中的用户代理或组件规则，从而覆盖继承值。继承只在元素没有更直接的指定值时生效；需要统一外观时，应显式为 <code>a</code>、<code>h1</code> 至 <code>h6</code> 设置规则。</p>\\n</li>\\n<li>\\n<p><strong>表格组件把 <code>border-collapse</code>、<code>color</code> 放在外层容器，希望内部表格和文字都自动继承；代码评审时你会接受吗？</strong></p>\\n<p>不能把“常见可继承属性”当成所有后代都必然得到目标效果，属性只会沿元素继承链传播，且直接声明仍会覆盖继承值。<code>color</code> 适合在容器统一下发，而表格布局属性应优先写到实际的 <code>table</code> 上；盒模型、背景、定位等属性则不能依赖继承。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_37-flex弹性盒布局模型及适用场景\\"><a class=\\"header-anchor\\" href=\\"#_37-flex弹性盒布局模型及适用场景\\">#</a> 37 flex弹性盒布局模型及适用场景？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>flex</code> 是<strong>一维</strong>布局：容器管主轴排列（<code>justify-content</code>），交叉轴管对齐（<code>align-items</code>）</li>\\n<li>适用场景：导航栏、工具栏、卡片内部的图文排列、&quot;一侧固定一侧自适应&quot;、垂直居中 —— 凡是&quot;一条线上排东西&quot;</li>\\n<li>不适用：需要行列同时对齐的二维布局（那是 <code>grid</code> 的活），比如表单的标签列对齐、复杂仪表盘</li>\\n<li>三个高频坑：子项 <code>min-width</code> 默认 <code>auto</code> 会撑破容器、<code>flex: 1</code> 与 <code>flex: auto</code> 的 <code>basis</code> 差异、<code>order</code> 只改视觉不改 <code>Tab</code> 顺序</li>\\n<li>间距一律用 <code>gap</code>，比给子项加 <code>margin</code> 干净（不用处理首尾项）</li>\\n</ul>\\n<p><strong><code>Flex</code> 是一种沿主轴排列、在交叉轴对齐的一维弹性布局模型。</strong> 容器通过 <code>flex-direction</code>、<code>justify-content</code> 和 <code>align-items</code> 控制整体排列，项目则用 <code>flex</code>、<code>order</code>、<code>align-self</code> 调整伸缩、顺序和单项对齐。它很适合水平垂直居中、两栏或三栏自适应，以及移动端常见的一行式布局。需要同时控制行列关系时，通常应考虑 <code>Grid</code>。</p>\\n</section>\\n<ul>\\n<li>采用<code>Flex</code>布局的元素，称为<code>flex</code>容器<code>container</code></li>\\n<li>它的所有子元素自动成为容器成员，称为<code>flex</code>项目<code>item</code></li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/ccecc1e86f1ea0e1.png\\" alt=\\"\\"></p>\\n<p>容器中默认存在两条轴，主轴和交叉轴，呈90度关系。项目默认沿主轴排列，通过<code>flex-direction</code>来决定主轴的方向</p>\\n<p>每根轴都有起点和终点，这对于元素的对齐非常重要</p>\\n<p>关于flex常用的属性，我们可以划分为容器属性和容器成员属性</p>\\n<p><strong>容器属性有：</strong></p>\\n<ul>\\n<li><code>flex-direction: row | row-reverse | column | column-reverse;</code> 决定主轴的方向(即项目的排列方向)\\n<ul>\\n<li><code>row</code>（默认值）：主轴为水平方向，起点在左端</li>\\n<li><code>row-reverse</code>：主轴为水平方向，起点在右端</li>\\n<li><code>column</code>：主轴为垂直方向，起点在上沿。</li>\\n<li><code>column-reverse</code>：主轴为垂直方向，起点在下沿\\n<img src=\\"https://s.poetries.top/uploads/2022/09/a6eee92bd809de3c.png\\" alt=\\"\\"></li>\\n</ul>\\n</li>\\n<li><code>flex-wrap: nowrap | wrap | wrap-reverse;</code> 弹性元素永远沿主轴排列，那么如果主轴排不下，通过<code>flex-wrap</code>决定容器内项目是否可换行\\n<ul>\\n<li><code>nowrap</code>（默认值）：默认情况是不换行，但这里也不会任由元素直接溢出容器，会涉及到元素的弹性伸缩</li>\\n<li><code>wrap</code>：换行，第一行在下方</li>\\n<li><code>wrap-reverse</code>：换行，第一行在上方</li>\\n</ul>\\n</li>\\n<li><code>flex-flow: &lt;flex-direction&gt; || &lt;flex-wrap&gt;;</code> 是<code>flex-direction</code>属性和<code>flex-wrap</code>属性的简写形式，默认值为<code>row nowrap</code></li>\\n<li><code>justify-content: flex-start | flex-end | center | space-between | space-around;</code> 定义了项目在主轴上的对齐方式\\n<ul>\\n<li><code>flex-start</code>（默认值）：左对齐</li>\\n<li><code>flex-end</code>：右对齐</li>\\n<li><code>center</code>：居中</li>\\n<li><code>space-between</code>：两端对齐，项目之间的间隔都相等</li>\\n<li><code>space-around</code>：两个项目两侧间隔相等\\n<img src=\\"https://s.poetries.top/uploads/2022/09/bce899ac16248797.png\\" alt=\\"\\"></li>\\n</ul>\\n</li>\\n<li><code>align-items: flex-start | flex-end | center | baseline | stretch;</code> 定义项目在交叉轴上如何对齐\\n<ul>\\n<li><code>flex-start</code>：交叉轴的起点对齐</li>\\n<li><code>flex-end</code>：交叉轴的终点对齐</li>\\n<li><code>center</code>：交叉轴的中点对齐</li>\\n<li><code>baseline</code>: 项目的第一行文字的基线对齐</li>\\n<li><code>stretch</code>（默认值）：如果项目未设置高度或设为auto，将占满整个容器的高度</li>\\n</ul>\\n</li>\\n<li><code>align-content: flex-start | flex-end | center | space-between | space-around | stretch;</code> 定义了多根轴线的对齐方式。如果项目只有一根轴线，该属性不起作用\\n<ul>\\n<li><code>flex-start</code>：与交叉轴的起点对齐</li>\\n<li><code>flex-end</code>：与交叉轴的终点对齐</li>\\n<li><code>center</code>：与交叉轴的中点对齐</li>\\n<li><code>space-between</code>：与交叉轴两端对齐，轴线之间的间隔平均分布</li>\\n<li><code>space-around</code>：每根轴线两侧的间隔都相等。所以，轴线之间的间隔比轴线与边框的间隔大一倍</li>\\n<li><code>stretch</code>（默认值）：轴线占满整个交叉轴\\n<img src=\\"https://s.poetries.top/uploads/2022/09/9611b37a46dab667.png\\" alt=\\"\\"></li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>容器成员属性如下：</strong></p>\\n<ul>\\n<li><code>order: &lt;integer&gt;;</code> 定义项目的排列顺序。数值越小，排列越靠前，默认为<code>0</code></li>\\n<li><code>flex-grow: &lt;number&gt;;</code> 上面讲到当容器设为<code>flex-wrap: nowrap;</code>不换行的时候，容器宽度有不够分的情况，弹性元素会根据<code>flex-grow</code>来决定\\n<ul>\\n<li>定义项目的放大比例（容器宽度&gt;元素总宽度时如何伸展）</li>\\n<li>默认为<code>0</code>，即如果存在剩余空间，也不放大</li>\\n<li>如果所有项目的<code>flex-grow</code>属性都为<code>1</code>，则它们将等分剩余空间（如果有的话）\\n<img src=\\"https://s.poetries.top/uploads/2022/09/e9d637aaeb865268.png\\" alt=\\"\\"></li>\\n<li>如果一个项目的<code>flex-grow</code>属性为<code>2</code>，其他项目都为<code>1</code>，则前者占据的剩余空间将比其他项多一倍\\n<img src=\\"https://s.poetries.top/uploads/2022/09/2cf5e657b6f968c9.png\\" alt=\\"\\"></li>\\n<li>弹性容器的宽度正好等于元素宽度总和，无多余宽度，此时无论<code>flex-grow</code>是什么值都不会生效</li>\\n</ul>\\n</li>\\n<li><code>flex-shrink: &lt;number&gt;; /* default 1 */</code> 定义了项目的缩小比例（容器宽度&lt;元素总宽度时如何收缩），默认为<code>1</code>，即如果空间不足，该项目将缩小\\n<ul>\\n<li>如果所有项目的<code>flex-shrink</code>属性都为<code>1</code>，当空间不足时，都将等比例缩小</li>\\n<li>如果一个项目的<code>flex-shrink</code>属性为<code>0</code>，其他项目都为<code>1</code>，则空间不足时，前者不缩小</li>\\n<li>在容器宽度有剩余时，<code>flex-shrink</code>也是不会生效的\\n<img src=\\"https://s.poetries.top/uploads/2022/09/e4846a5a063570ec.png\\" alt=\\"\\"></li>\\n</ul>\\n</li>\\n<li><code>flex-basis: &lt;length&gt; | auto; /* default auto */</code> 设置的是元素在主轴上的初始尺寸，所谓的初始尺寸就是元素在<code>flex-grow</code>和<code>flex-shrink</code>生效前的尺寸\\n<ul>\\n<li>浏览器根据这个属性，计算主轴是否有多余空间，默认值为<code>auto</code>，即项目的本来大小，如设置了<code>width</code>则元素尺寸由<code>width/height</code>决定（主轴方向），没有设置则由内容决定</li>\\n<li>当设置为<code>0</code>的是，会根据内容撑开</li>\\n<li>它可以设为跟<code>width</code>或<code>height</code>属性一样的值（比如<code>350px</code>），则项目将占据固定空间</li>\\n</ul>\\n</li>\\n<li><code>flex: none | [ &lt;\'flex-grow\'&gt; &lt;\'flex-shrink\'&gt;? || &lt;\'flex-basis\'&gt; ]</code> 属性是<code>flex-grow</code>, <code>flex-shrink</code> 和 <code>flex-basis</code>的简写，默认值为<code>0 1 auto</code>，也是比较难懂的一个复合属性\\n<ul>\\n<li><code>flex: 1 = flex: 1 1 0%</code></li>\\n<li><code>flex: 2 = flex: 2 1 0%</code></li>\\n<li><code>flex: auto = flex: 1 1 auto</code></li>\\n<li><code>flex: none = flex: 0 0 auto</code>，常用于固定尺寸不伸缩</li>\\n<li><code>flex:1</code> 和 <code>flex:auto</code> 的区别，可以归结于<code>flex-basis:0</code>和<code>flex-basis:auto</code>的区别</li>\\n<li>当设置为<code>0</code>时（绝对弹性元素），此时相当于告诉<code>flex-grow</code>和<code>flex-shrink</code>在伸缩的时候不需要考虑我的尺寸</li>\\n<li>当设置为<code>auto</code>时（相对弹性元素），此时则需要在伸缩时将元素尺寸纳入考虑</li>\\n<li>注意：建议优先使用这个属性，而不是单独写三个分离的属性，因为浏览器会推算相关值</li>\\n</ul>\\n</li>\\n<li><code>align-self: auto | flex-start | flex-end | center | baseline | stretch;</code> 允许单个项目有与其他项目不一样的对齐方式，可覆盖<code>align-items</code>属性\\n<ul>\\n<li>默认值为<code>auto</code>，表示继承父元素的<code>align-items</code>属性，如果没有父元素，则等同于<code>stretch</code>\\n<img src=\\"https://s.poetries.top/uploads/2022/09/0bbe195a6bf6a2a5.png\\" alt=\\"\\"></li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>应用场景</strong></p>\\n<ul>\\n<li><code>flex</code>简单粗暴的实现元素水平垂直方向的居中，以及在两栏三栏自适应布局中通过<code>flex</code>完成</li>\\n<li>在移动端、小程序这边的开发，都建议使用<code>flex</code>进行布局</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个两栏页面用 <code>display: flex</code>，左栏写死 <code>240px</code>，右栏设 <code>flex: 1</code>；右栏放入长代码后却把页面撑出横向滚动条，为什么 <code>flex: 1</code> 没兜住？</strong></p>\\n<p><code>flex: 1</code> 表示 <code>1 1 0%</code>，会参与剩余空间分配，但项目仍可能受内容最小尺寸约束而无法继续收缩。通常给右栏补 <code>min-width: 0</code>，并对长文本设置换行或滚动；若内容本身必须保持完整宽度，横向滚动反而是合理边界。</p>\\n</li>\\n<li>\\n<p><strong>聊天工具栏有四个按钮，要求两端贴边、中间间距相等；设计师又要求按钮不足一行时自动换行，你会怎样配置容器？</strong></p>\\n<p>容器可使用 <code>display: flex</code>、<code>justify-content: space-between</code> 与 <code>flex-wrap: wrap</code>，主轴负责一行内分布，换行由 <code>flex-wrap</code> 控制。换行后每一行会独立分配主轴剩余空间，若产品要求跨行严格对齐成网格，<code>Grid</code> 往往更合适。</p>\\n</li>\\n<li>\\n<p><strong>移动端商品栏原先横向排列，现在横屏不变、竖屏要改成纵向，但图标与文字仍需居中；需要重写对齐属性吗？</strong></p>\\n<p>切换 <code>flex-direction: row</code> 与 <code>column</code> 就会改变主轴方向，<code>justify-content</code> 始终控制主轴，<code>align-items</code> 始终控制交叉轴。若直接把“水平居中”等同于某个属性，改轴后就会出错；应按当前主轴重新核对两个方向的对齐语义。</p>\\n</li>\\n<li>\\n<p><strong>卡片列表设置了 <code>align-content: center</code>，单行卡片却完全没移动，团队怀疑浏览器兼容问题；你会怎么定位？</strong></p>\\n<p><code>align-content</code> 调整的是多根交叉轴线，只有一行时本来就不会产生效果，并非优先怀疑兼容性。单行项目应看 <code>align-items</code>，单个例外则用 <code>align-self</code>；若期望出现多行，还要确认容器允许 <code>flex-wrap: wrap</code> 且交叉轴存在可分配空间。</p>\\n</li>\\n<li>\\n<p><strong>同一行三块内容要按原始内容宽度再分剩余空间，开发分别提出 <code>flex: 1</code> 和 <code>flex: auto</code>；你会选哪个？</strong></p>\\n<p>需要把原始尺寸纳入伸缩计算时应选 <code>flex: auto</code>，它相当于 <code>1 1 auto</code>；<code>flex: 1</code> 的基础尺寸为 <code>0%</code>，更倾向于等分可用空间。两者都可能收缩，固定按钮若不能变窄可用 <code>flex: none</code>，代价是空间不足时其他项压力更大。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_38-介绍一下grid网格布局\\"><a class=\\"header-anchor\\" href=\\"#_38-介绍一下grid网格布局\\">#</a> 38 介绍一下grid网格布局</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>grid</code> 是<strong>二维</strong>布局：同时管行和列，能让不同行的内容在列方向上天然对齐</li>\\n<li>核心属性：<code>grid-template-columns/rows</code>、<code>gap</code>、<code>grid-template-areas</code>（用字符画布局，可读性极好）</li>\\n<li><code>fr</code> 是&quot;剩余空间的份数&quot;，<code>repeat(auto-fill, minmax(200px, 1fr))</code> 一行实现响应式卡片墙，连媒体查询都省了</li>\\n<li>相比 <code>flex</code> 的最大优势：布局定义在<strong>容器</strong>上，子元素不需要知道自己该多宽，换位置只改容器的 <code>areas</code></li>\\n<li>常见搭配：整页骨架用 <code>grid</code>，局部一行内的排列用 <code>flex</code> —— 两者不是替代关系</li>\\n</ul>\\n<p><strong><code>Grid</code> 是能同时处理行和列的二维网格布局，适合划分页面区域并控制它们的尺寸和位置。</strong> 给容器设置 <code>display: grid</code> 后，可以用 <code>grid-template-columns</code>、<code>grid-template-rows</code> 定义网格，用 <code>gap</code> 控制间距。<code>fr</code>、<code>repeat()</code>、<code>minmax()</code> 便于表达比例和自适应列数，项目还能通过网格线或 <code>grid-area</code> 精确定位。居中、两列和三列等布局都容易实现，但旧版浏览器需要考虑兼容性。</p>\\n</section>\\n<p><code>Grid</code> 布局即网格布局，是一个二维的布局方式，由纵横相交的两组网格线形成的框架性布局结构，能够同时处理行与列</p>\\n<p>擅长将一个页面划分为几个主要区域，以及定义这些区域的大小、位置、层次等关系</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/7a6bebde16cd0dc1.png\\" alt=\\"\\"></p>\\n<p>这与<code>flex</code>一维布局不相同</p>\\n<p>设置<code>display:grid/inline-grid</code>的元素就是网格布局容器，这样就能出发浏览器渲染引擎的网格布局算法</p>\\n<pre class=\\"language-html\\"><code>&lt;div class=&quot;container&quot;&gt;\\n    &lt;div class=&quot;item item-1&quot;&gt;\\n        &lt;p class=&quot;sub-item&quot;&gt;&lt;/p &gt;\\n &lt;/div&gt;\\n    &lt;div class=&quot;item item-2&quot;&gt;&lt;/div&gt;\\n    &lt;div class=&quot;item item-3&quot;&gt;&lt;/div&gt;\\n&lt;/div&gt; \\n</code></pre>\\n<p>上述代码实例中，<code>.container</code>元素就是网格布局容器，<code>.item</code>元素就是网格的项目，由于网格元素只能是容器的顶层子元素，所以<code>p</code>元素并不是网格元素</p>\\n<p>这里提一下，网格线概念，有助于下面对<code>grid-column</code>系列属性的理解</p>\\n<p>网格线，即划分网格的线，如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/f3265f7748c5cab6.png\\" alt=\\"\\"></p>\\n<p>上图是一个 <code>2 x 3</code> 的网格，共有<code>3</code>根水平网格线和<code>4</code>根垂直网格线</p>\\n<p><strong>Grid 布局属性可以分为两大类</strong></p>\\n<ul>\\n<li>容器属性</li>\\n<li>项目属性</li>\\n</ul>\\n<p><strong>关于容器属性有如下：</strong></p>\\n<ul>\\n<li><code>display</code> 在元素上设置<code>display：grid</code> 或 <code>display：inline-grid</code> 来创建一个网格容器\\n<ul>\\n<li><code>display：grid</code> 则该容器是一个块级元素</li>\\n<li><code>display: inline-grid</code> 则容器元素为行内元素</li>\\n</ul>\\n</li>\\n<li><code>grid-template-columns</code> 属性，<code>grid-template-rows</code> 属性\\n<ul>\\n<li><code>grid-template-columns</code> 属性设置列宽，<code>grid-template-rows</code> 属性设置行高</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>.wrapper {\\n  display: grid;\\n  /*  声明了三列，宽度分别为 200px 200px 200px */\\n  grid-template-columns: 200px 200px 200px;\\n  grid-gap: 5px;\\n  /*  声明了两行，行高分别为 50px 50px  */\\n  grid-template-rows: 50px 50px;\\n}\\n</code></pre>\\n<ul>\\n<li>以上表示固定列宽为 <code>200px 200px 200px</code>，行高为 <code>50px 50px</code></li>\\n<li>上述代码可以看到重复写单元格宽高，通过使用<code>repeat()</code>函数，可以简写重复的值\\n<ul>\\n<li>第一个参数是重复的次数</li>\\n<li>第二个参数是重复的值</li>\\n</ul>\\n</li>\\n<li>所以上述代码可以简写成</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>.wrapper {\\n    display: grid;\\n    grid-template-columns: repeat(3,200px);\\n    grid-gap: 5px;\\n    grid-template-rows:repeat(2,50px);\\n  }\\n</code></pre>\\n<ul>\\n<li>除了上述的<code>repeat</code>关键字，还有：\\n<ul>\\n<li><code>auto-fill</code>：示自动填充，让一行（或者一列）中尽可能的容纳更多的单元格\\n<ul>\\n<li><code>grid-template-columns: repeat(auto-fill, 200px)</code> 表示列宽是 <code>200 px</code>，但列的数量是不固定的，只要浏览器能够容纳得下，就可以放置元素</li>\\n</ul>\\n</li>\\n<li><code>fr</code>：片段，为了方便表示比例关系\\n<ul>\\n<li><code>grid-template-columns: 200px 1fr 2fr</code> 表示第一个列宽设置为 <code>200px</code>，后面剩余的宽度分为两部分，宽度分别为剩余宽度的 <code>1/3</code> 和 <code>2/3</code></li>\\n</ul>\\n</li>\\n<li><code>minmax</code>：产生一个长度范围，表示长度就在这个范围之中都可以应用到网格项目中。第一个参数就是最小值，第二个参数就是最大值\\n<ul>\\n<li><code>minmax(100px, 1fr)</code>表示列宽不小于<code>100px</code>，不大于<code>1fr</code></li>\\n</ul>\\n</li>\\n<li><code>auto</code>：由浏览器自己决定长度\\n<ul>\\n<li><code>grid-template-columns: 100px auto 100px</code> 表示第一第三列为 <code>100px</code>，中间由浏览器决定长度</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li><code>grid-row-gap</code> 属性， <code>grid-column-gap</code> 属性， <code>grid-gap</code> 属性\\n<ul>\\n<li><code>grid-row-gap</code> 属性、<code>grid-column-gap</code> 属性分别设置行间距和列间距。<code>grid-gap</code> 属性是两者的简写形式</li>\\n<li><code>grid-row-gap: 10px</code> 表示行间距是 <code>10px</code></li>\\n<li><code>grid-column-gap: 20px</code> 表示列间距是 <code>20px</code></li>\\n<li><code>grid-gap: 10px 20px</code> 等同上述两个属性</li>\\n</ul>\\n</li>\\n<li><code>grid-template-areas</code> 属性：用于定义区域，一个区域由一个或者多个单元格组成<pre class=\\"language-css\\"><code>.container {\\n    display: grid;\\n    grid-template-columns: 100px 100px 100px;\\n    grid-template-rows: 100px 100px 100px;\\n    grid-template-areas: &#39;a b c&#39;\\n                        &#39;d e f&#39;\\n                        &#39;g h i&#39;;\\n  }\\n</code></pre>\\n<ul>\\n<li>上面代码先划分出<code>9</code>个单元格，然后将其定名为<code>a</code>到<code>i</code>的九个区域，分别对应这九个单元格。</li>\\n<li>多个单元格合并成一个区域的写法如下</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>grid-template-areas: &#39;a a a&#39;\\n                   &#39;b b b&#39;\\n                   &#39;c c c&#39;;\\n</code></pre>\\n<ul>\\n<li>上面代码将<code>9</code>个单元格分成<code>a、b、c</code>三个区域</li>\\n<li>如果某些区域不需要利用，则使用&quot;点&quot;（<code>.</code>）表示</li>\\n</ul>\\n</li>\\n<li><code>grid-auto-flow</code> 属性\\n<ul>\\n<li>划分网格以后，容器的子元素会按照顺序，自动放置在每一个网格。</li>\\n<li>顺序就是由<code>grid-auto-flow</code>决定，默认为行，代表&quot;先行后列&quot;，即先填满第一行，再开始放入第二行\\n<img src=\\"https://s.poetries.top/uploads/2022/09/d973d2d25b7a9641.png\\" alt=\\"\\"></li>\\n<li>当修改成<code>column</code>后，放置变为如下\\n<img src=\\"https://s.poetries.top/uploads/2022/09/59cb05f9412d447a.png\\" alt=\\"\\"></li>\\n</ul>\\n</li>\\n<li><code>justify-items</code> 属性， <code>align-items</code> 属性， <code>place-items</code> 属性\\n<ul>\\n<li><code>justify-items</code> 属性设置单元格内容的水平位置（左中右），<code>align-items</code> 属性设置单元格的垂直位置（上中下）</li>\\n<li>两者属性的值完成相同</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>.container {\\n  justify-items: start | end | center | stretch;\\n  align-items: start | end | center | stretch;\\n}\\n</code></pre>\\n<ul>\\n<li>属性对应如下：\\n<ul>\\n<li><code>start</code>：对齐单元格的起始边缘</li>\\n<li><code>end</code>：对齐单元格的结束边缘</li>\\n<li><code>center</code>：单元格内部居中</li>\\n<li><code>stretch</code>：拉伸，占满单元格的整个宽度（默认值）</li>\\n<li><code>place-items</code>属性是<code>align-items</code>属性和<code>justify-items</code>属性的合并简写形式</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li><code>justify-content</code> 属性， <code>align-content</code> 属性， <code>place-content</code> 属性\\n<ul>\\n<li><code>justify-content</code>属性是整个内容区域在容器里面的水平位置（左中右），<code>align-content</code>属性是整个内容区域的垂直位置（上中下）</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>.container {\\n  justify-content: start | end | center | stretch | space-around | space-between | space-evenly;\\n  align-content: start | end | center | stretch | space-around | space-between | space-evenly;  \\n}\\n</code></pre>\\n<ul>\\n<li>两个属性的写法完全相同，都可以取下面这些值：</li>\\n<li><code>start</code> - 对齐容器的起始边框</li>\\n<li><code>end</code> - 对齐容器的结束边框</li>\\n<li><code>center</code> - 容器内部居中\\n<img src=\\"https://s.poetries.top/uploads/2022/09/aa3e504f73f0a7dc.png\\" alt=\\"\\"></li>\\n<li><code>space-around</code> - 每个项目两侧的间隔相等。所以，项目之间的间隔比项目与容器边框的间隔大一倍</li>\\n<li><code>space-between</code> - 项目与项目的间隔相等，项目与容器边框之间没有间隔</li>\\n<li><code>space-evenly</code> - 项目与项目的间隔相等，项目与容器边框之间也是同样长度的间隔</li>\\n<li><code>stretch</code> - 项目大小没有指定时，拉伸占据整个网格容器\\n<img src=\\"https://s.poetries.top/uploads/2022/09/36511ef68d7b786b.png\\" alt=\\"\\"></li>\\n</ul>\\n</li>\\n<li><code>grid-auto-columns</code> 属性和 <code>grid-auto-rows</code> 属性\\n<ul>\\n<li>有时候，一些项目的指定位置，在现有网格的外部，就会产生显示网格和隐式网格</li>\\n<li>比如网格只有<code>3</code>列，但是某一个项目指定在第<code>5</code>行。这时，浏览器会自动生成多余的网格，以便放置项目。超出的部分就是隐式网格</li>\\n<li>而<code>grid-auto-rows</code>与<code>grid-auto-columns</code>就是专门用于指定隐式网格的宽高</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>关于项目属性，有如下：</strong></p>\\n<ul>\\n<li><code>grid-column-start</code> 属性、<code>grid-column-end</code> 属性、<code>grid-row-start</code> 属性以及<code>grid-row-end</code> 属性\\n<ul>\\n<li>指定网格项目所在的四个边框，分别定位在哪根网格线，从而指定项目的位置</li>\\n<li><code>grid-column-start</code> 属性：左边框所在的垂直网格线</li>\\n<li><code>grid-column-end</code> 属性：右边框所在的垂直网格线</li>\\n<li><code>grid-row-start</code> 属性：上边框所在的水平网格线</li>\\n<li><code>grid-row-end</code> 属性：下边框所在的水平网格线</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>  &lt;style&gt;\\n    #container{\\n        display: grid;\\n        grid-template-columns: 100px 100px 100px;\\n        grid-template-rows: 100px 100px 100px;\\n    }\\n    .item-1 {\\n        grid-column-start: 2;\\n        grid-column-end: 4;\\n    }\\n  &lt;/style&gt;\\n\\n  &lt;div id=&quot;container&quot;&gt;\\n      &lt;div class=&quot;item item-1&quot;&gt;1&lt;/div&gt;\\n      &lt;div class=&quot;item item-2&quot;&gt;2&lt;/div&gt;\\n      &lt;div class=&quot;item item-3&quot;&gt;3&lt;/div&gt;\\n  &lt;/div&gt;\\n</code></pre>\\n<ul>\\n<li>通过设置<code>grid-column</code>属性，指定<code>1</code>号项目的左边框是第二根垂直网格线，右边框是第四根垂直网格线\\n<img src=\\"https://s.poetries.top/uploads/2022/09/8fdf4b8f5e6babf4.png\\" alt=\\"\\"></li>\\n</ul>\\n</li>\\n<li><code>grid-area</code> 属性\\n<ul>\\n<li><code>grid-area</code> 属性指定项目放在哪一个区域</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>item-1 {\\n  grid-area: e;\\n}\\n</code></pre>\\n<ul>\\n<li>意思为将<code>1</code>号项目位于<code>e</code>区域</li>\\n</ul>\\n</li>\\n<li><code>justify-self</code> 属性、<code>align-self</code> 属性以及 <code>place-self</code> 属性\\n<ul>\\n<li><code>justify-self</code>属性设置单元格内容的水平位置（左中右），跟`justify-items属性的用法完全一致，但只作用于单个项目。</li>\\n<li><code>align-self</code>属性设置单元格内容的垂直位置（上中下），跟<code>align-items</code>属性的用法完全一致，也是只作用于单个项目</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>.item {\\n  justify-self: start | end | center | stretch;\\n  align-self: start | end | center | stretch;\\n}\\n</code></pre>\\n<ul>\\n<li>这两个属性都可以取下面四个值。\\n<ul>\\n<li><code>start</code>：对齐单元格的起始边缘。</li>\\n<li><code>end</code>：对齐单元格的结束边缘。</li>\\n<li><code>center</code>：单元格内部居中。</li>\\n<li><code>stretch</code>：拉伸，占满单元格的整个宽度（默认值）</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>应用场景</strong></p>\\n<p>Grid是一个强大的布局，如一些常见的 <code>CSS 布局</code>，如<code>居中</code>，<code>两列布局</code>，<code>三列布局</code>等等是很容易实现的</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/6e019fdb0f08a21f.png\\" alt=\\"\\"></p>\\n<p>总体兼容性还不错，但在 <code>IE 10</code> 以下不支持</p>\\n<p>目前，<code>Grid</code>布局在手机端支持还不算太友好</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>仪表盘要同时控制三列宽度和多行卡片位置，开发仍坚持每一行套一层 <code>flex</code>；你会用什么现象说明该换成 <code>Grid</code>？</strong></p>\\n<p>当行与列需要共同对齐、卡片还要跨越多个单元格时，二维 <code>Grid</code> 比逐行嵌套 <code>flex</code> 更直接。容器用 <code>grid-template-columns</code>、<code>grid-template-rows</code> 定义轨道，项目按网格线或区域放置；纯单轴分布仍由 <code>flex</code> 更简洁。</p>\\n</li>\\n<li>\\n<p><strong>后台首页固定左栏 <code>200px</code>，主内容吃掉其余空间，右侧信息栏占主内容两倍；不写宽度计算时，列模板怎么定？</strong></p>\\n<p>可写成 <code>grid-template-columns: 200px 2fr 1fr</code>，固定列先占 <code>200px</code>，剩余宽度再按 <code>2:1</code> 分给后两列。<code>fr</code> 分配的是可用剩余空间，不等于整个容器的固定比例；内容过长时仍要处理项目的最小尺寸与溢出。</p>\\n</li>\\n<li>\\n<p><strong>卡片墙用 <code>repeat(auto-fill, 200px)</code> 后，大屏右侧留下明显空白；产品希望列宽至少 <code>200px</code> 且自动铺满，你会怎样调整？</strong></p>\\n<p>可改为 <code>repeat(auto-fill, minmax(200px, 1fr))</code>，浏览器会尽量容纳更多列，并让每列在不小于 <code>200px</code> 的前提下分配剩余空间。容器窄于最小列宽时仍可能溢出，因此最小值应结合真实内容和可用宽度确定。</p>\\n</li>\\n<li>\\n<p><strong>一个项目写了 <code>grid-column-start: 2; grid-column-end: 4</code>，结果横跨两列而不是三列；评审现场你怎么解释？</strong></p>\\n<p>这两个值指向项目左右边界所在的网格线，第二根线到第四根线之间只有两个轨道，所以会跨两列。定位时应数网格线而不是数单元格；复杂页面可改用命名区域和 <code>grid-area</code>，可读性更好，但区域模板也要同步维护。</p>\\n</li>\\n<li>\\n<p><strong>设计稿只声明三行，某个通知组件却被定位到第五行，线上高度突然异常；你会检查哪个隐式网格配置？</strong></p>\\n<p>项目超出显式网格后，浏览器会自动创建隐式行，第五行的尺寸应检查 <code>grid-auto-rows</code>。给隐式行设置明确或合适的自动尺寸可稳定布局；若越界源于错误的网格线编号，单纯限制行高只会掩盖定位问题。</p>\\n</li>\\n<li>\\n<p><strong>九宫格中图标设置 <code>justify-self: center</code> 后只水平居中，开发又把容器的 <code>justify-content</code> 改为 <code>center</code>，结果整块网格一起移动；两者应怎么选？</strong></p>\\n<p><code>justify-self</code> 控制单个项目在自身单元格内的位置，而 <code>justify-content</code> 控制整个网格内容区域在容器中的位置。统一调整全部项目可用 <code>justify-items</code>，单项例外用 <code>justify-self</code>；只有网格整体存在剩余空间时，<code>justify-content</code> 的变化才明显。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_39-什么是响应式设计-响应式设计的基本原理是什么\\"><a class=\\"header-anchor\\" href=\\"#_39-什么是响应式设计-响应式设计的基本原理是什么\\">#</a> 39 什么是响应式设计？响应式设计的基本原理是什么</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>响应式 = 一套代码适配多种屏幕，核心手段是<strong>弹性布局 + 弹性图片 + 媒体查询</strong>三件套</li>\\n<li>断点应该由<strong>内容</strong>决定（布局在哪个宽度开始难看就在哪断），不是照抄设备尺寸列表</li>\\n<li>移动优先（<code>min-width</code> 媒体查询）比桌面优先更好：默认样式给最简单的场景，逐步增强</li>\\n<li>现代补充：<code>clamp()</code> 做流体尺寸、<code>@container</code> 按父容器宽度而非视口判断、<code>grid</code> 的 <code>auto-fill</code> 自动换列</li>\\n<li>别忘了 <code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;</code>，没有它一切都不成立</li>\\n</ul>\\n<p><strong>响应式设计就是让同一页面根据设备环境和视口尺寸，自动调整布局、模块大小与位置。</strong> 基本原理是先声明 <code>viewport</code>，再用媒体查询针对不同宽度应用样式，并配合 <code>%</code>、<code>vw</code>、<code>vh</code> 或 <code>rem</code> 等相对单位实现弹性尺寸。比如页面在手机端可把导航切换成抽屉形式，在平板和电脑端恢复展开布局。它能减少多端适配成本，但复杂页面可能出现代码冗余、隐藏内容增加加载时间等取舍。</p>\\n</section>\\n<blockquote>\\n<p>响应式网站设计（<code>Responsive Web design</code>）是一种网络页面设计布局，页面的设计与开发应当根据用户行为以及设备环境(系统平台、屏幕尺寸、屏幕定向等)进行相应的响应和调整</p>\\n</blockquote>\\n<p><strong>响应式网站常见特点：</strong></p>\\n<ul>\\n<li>同时适配PC + 平板 + 手机等</li>\\n<li>标签导航在接近手持终端设备时改变为经典的抽屉式导航</li>\\n<li>网站的布局会根据视口来调整模块的大小和位置</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/uploads/2022/09/7595e50c5fcf7382.png\\" alt=\\"\\"></p>\\n<p><strong>实现方式</strong></p>\\n<p>响应式设计的基本原理是通过媒体查询检测不同的设备屏幕尺寸做处理，为了处理移动端，页面头部必须有<code>meta</code>声明<code>viewport</code></p>\\n<pre class=\\"language-html\\"><code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no”&gt;\\n</code></pre>\\n<p>属性对应如下：</p>\\n<ul>\\n<li><code>width=device-width</code>: 是自适应手机屏幕的尺寸宽度</li>\\n<li><code>maximum-scale</code>:是缩放比例的最大值</li>\\n<li><code>inital-scale</code>:是缩放的初始化</li>\\n<li><code>user-scalable</code>:是用户的可以缩放的操作</li>\\n</ul>\\n<p>实现响应式布局的方式有如下：</p>\\n<ul>\\n<li><strong>媒体查询</strong>\\n<ul>\\n<li>CSS3中的增加了更多的媒体查询，就像if条件表达式一样，我们可以设置不同类型的媒体条件，并根据对应的条件，给相应符合条件的媒体调用相对应的样式表</li>\\n<li>使用<code>@Media</code>查询，可以针对不同的媒体类型定义不同的样式，如： <code>@media screen and (max-width: 1920px) { ... }</code></li>\\n<li>当视口在<code>375px - 600px</code>之间，设置特定字体大小<code>18px</code> <code>@media screen (min-width: 375px) and (max-width: 600px) {body: {font-size: 18px}}</code></li>\\n<li>通过媒体查询，可以通过给不同分辨率的设备编写不同的样式来实现响应式的布局，比如我们为不同分辨率的屏幕，设置不同的背景图片</li>\\n<li>比如给小屏幕手机设置<code>@2x</code>图，为大屏幕手机设置<code>@3x</code>图，通过媒体查询就能很方便的实现</li>\\n</ul>\\n</li>\\n<li><strong>百分比</strong>\\n<ul>\\n<li>通过百分比单位 &quot; <code>%</code> &quot; 来实现响应式的效果</li>\\n<li>比如当浏览器的宽度或者高度发生变化时，通过百分比单位，可以使得浏览器中的组件的宽和高随着浏览器的变化而变化，从而实现响应式的效果</li>\\n<li><code>height</code>、<code>width</code>属性的百分比依托于父标签的宽高，但是其他盒子属性则不完全依赖父元素\\n<ul>\\n<li>子元素的<code>top/left</code>和<code>bottom/right</code>如果设置百分比，则相对于直接非<code>static</code>定位(默认定位)的父元素的高度/宽度</li>\\n<li>子元素的<code>padding</code>如果设置百分比，不论是垂直方向或者是水平方向，都相对于直接父亲元素的<code>width</code>，而与父元素的<code>height</code>无关</li>\\n<li>子元素的<code>margin</code>如果设置成百分比，不论是垂直方向还是水平方向，都相对于直接父元素的<code>width</code></li>\\n<li><code>border-radius</code>不一样，如果设置<code>border-radius</code>为百分比，则是相对于自身的宽度</li>\\n</ul>\\n</li>\\n<li>可以看到每个属性都使用百分比，会照成布局的复杂度，所以<code>不建议使用百分比来实现响应式</code></li>\\n</ul>\\n</li>\\n<li><strong>vw/vh</strong>\\n<ul>\\n<li><code>vw</code>表示相对于视图窗口的宽度，<code>vh</code>表示相对于视图窗口高度。 任意层级元素，在使用<code>vw</code>单位的情况下，<code>1vw</code>都等于视图宽度的百分之一</li>\\n</ul>\\n</li>\\n<li><strong>rem</strong>\\n<ul>\\n<li><code>rem</code>是相对于根元素<code>html</code>的<code>font-size</code>属性，默认情况下浏览器字体大小为<code>16px</code>，此时<code>1rem = 16px</code></li>\\n<li>可以利用前面提到的媒体查询，针对不同设备分辨率改变<code>font-size</code>的值，如下：</li>\\n</ul>\\n<pre class=\\"language-css\\"><code> @media screen and (max-width: 414px) {\\n   html {\\n     font-size: 18px\\n   }\\n }\\n\\n @media screen and (max-width: 375px) {\\n   html {\\n     font-size: 16px\\n   }\\n }\\n\\n @media screen and (max-width: 320px) {\\n   html {\\n     font-size: 12px\\n   }\\n }\\n</code></pre>\\n<ul>\\n<li>为了更准确监听设备可视窗口变化，我们可以在<code>css</code>之前插入<code>script</code>标签，内容如下：</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>//动态为根元素设置字体大小\\nfunction init () {\\n    // 获取屏幕宽度\\n    var width = document.documentElement.clientWidth\\n    // 设置根元素字体大小。此时为宽的10等分\\n    document.documentElement.style.fontSize = width / 10 + &#39;px&#39;\\n}\\n\\n//首次加载应用，设置一次\\ninit()\\n// 监听手机旋转的事件的时机，重新设置\\nwindow.addEventListener(&#39;orientationchange&#39;, init)\\n// 监听手机窗口变化，重新设置\\nwindow.addEventListener(&#39;resize&#39;, init)\\n</code></pre>\\n<ul>\\n<li>无论设备可视窗口如何变化，始终设置<code>rem</code>为<code>width</code>的<code>1/10</code>，实现了百分比布局</li>\\n<li>除此之外，我们还可以利用主流UI框架，如：<code>element ui</code>、<code>antd</code>提供的栅格布局实现响应式</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>总结</strong></p>\\n<ul>\\n<li>响应式布局优点可以看到：</li>\\n<li>面对不同分辨率设备灵活性强</li>\\n<li>能够快捷解决多设备显示适应问题</li>\\n<li>缺点：</li>\\n<li>仅适用布局、信息、框架并不复杂的部门类型网站</li>\\n<li>兼容各种设备工作量大，效率低下</li>\\n<li>代码累赘，会出现隐藏无用的元素，加载时间加长</li>\\n<li>其实这是一种折中性质的设计解决方案，多方面因素影响而达不到最佳效果</li>\\n<li>一定程度上改变了网站原有的布局结构，会出现用户混淆的情况</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>桌面浏览器缩到 <code>390px</code> 时布局已切成手机样式，但真机打开仍按宽桌面缩小显示；<code>CSS</code> 断点明明命中了，页面头部还缺什么？</strong></p>\\n<p>应先检查是否声明了 <code>viewport</code>，例如 <code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;</code>，让布局视口与设备宽度对应。没有它时，移动浏览器可能按较宽视口排版后整体缩放；通常也不应禁止用户缩放，以免影响可访问性。</p>\\n</li>\\n<li>\\n<p><strong>营销页在 <code>375px</code> 到 <code>600px</code> 需要把正文设为 <code>18px</code>，有人写了 <code>@media screen (min-width: 375px)</code>；为什么规则没有按预期生效？</strong></p>\\n<p>媒体类型与条件之间缺少 <code>and</code>，可写为 <code>@media screen and (min-width: 375px) and (max-width: 600px)</code>。还要在 <code>DevTools</code> 核对规则是否被后续声明覆盖；边界重叠时，源码顺序可能使相邻断点的结果难以判断。</p>\\n</li>\\n<li>\\n<p><strong>设计师要求卡片宽度、高度、上下边距都按父容器百分比缩放，开发认为所有 <code>%</code> 都相对父元素对应方向；你会指出什么风险？</strong></p>\\n<p>不同属性的百分比参照并不统一：宽高依赖包含块相应尺寸，而垂直与水平 <code>padding</code>、<code>margin</code> 通常都以包含块宽度为参照。全量使用 <code>%</code> 会让尺寸关系难以推断，且百分比高度还需要明确的参照高度；更适合结合弹性布局、媒体查询和有限的相对单位。</p>\\n</li>\\n<li>\\n<p><strong>移动页面把标题设为 <code>8vw</code>，在小屏表现正常，到了桌面模拟器却大得离谱；怎样保留随视口变化的能力又控制边界？</strong></p>\\n<p><code>1vw</code> 始终是视口宽度的百分之一，因此视口越宽，纯 <code>vw</code> 字号就会持续放大。可通过媒体查询在宽屏切换字号，或把 <code>vw</code> 只用于受控范围；无论选哪种方式，都要为过窄和过宽视口验证可读性。</p>\\n</li>\\n<li>\\n<p><strong>团队计划监听每次 <code>resize</code>，把根字号设为 <code>document.documentElement.clientWidth / 10</code>，另一方主张只用媒体查询；你会如何取舍？</strong></p>\\n<p>动态根字号能让 <code>rem</code> 随可视宽度连续变化，但会引入脚本执行、事件监听和全局尺寸联动，排查成本更高。页面只有少量布局断点时优先使用媒体查询；确有整套按宽度缩放的设计体系时再采用该方案，并验证旋转和窗口变化。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_40-如果要做优化-css提高性能的方法有哪些\\"><a class=\\"header-anchor\\" href=\\"#_40-如果要做优化-css提高性能的方法有哪些\\">#</a> 40 如果要做优化，CSS提高性能的方法有哪些？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>加载层：关键 <code>CSS</code> 内联、非关键的用 <code>media=&quot;print&quot; onload</code> 异步化、删无用样式（<code>Coverage</code> 面板能看）</li>\\n<li>渲染层：动画只动 <code>transform</code>/<code>opacity</code>，避开会触发重排的属性；频繁变动的元素脱离文档流</li>\\n<li>合成层：按需 <code>will-change</code> 并在动画结束移除，用 <code>Layers</code> 面板核对层数别爆炸</li>\\n<li>选择器层：关键选择器要具体，别写 <code>.a *</code>，控制 <code>Sass</code> 嵌套深度 —— 但这块收益最小，别过度优化</li>\\n<li>资源层：字体配 <code>font-display: swap</code> + <code>preload</code>，图片给 <code>width</code>/<code>height</code> 防 <code>CLS</code></li>\\n</ul>\\n<p><strong><code>CSS</code> 性能优化的核心是缩短样式加载时间，并减少浏览器不必要的重排和重绘。</strong> 我一般会内联少量首屏关键 <code>CSS</code>，其余样式外链、压缩或按需异步加载，同时避免 <code>@import</code> 带来的额外下载延迟。渲染阶段应控制选择器嵌套层级，谨慎使用 <code>box-shadow</code>、<code>filter</code> 等开销较高的属性。动画优先使用 <code>transform</code> 和 <code>opacity</code>，小图标也可通过雪碧图或 <code>base64</code> 减少请求，但不宜把大量 <code>CSS</code> 全部内联。</p>\\n</section>\\n<p>实现方式有很多种，主要有如下：</p>\\n<ul>\\n<li><strong>内联首屏关键CSS</strong>\\n<ul>\\n<li>在打开一个页面，页面首要内容出现在屏幕的时间影响着用户的体验，而通过内联<code>css</code>关键代码能够使浏览器在下载完html后就能立刻渲染</li>\\n<li>而如果外部引用<code>css</code>代码，在解析<code>html</code>结构过程中遇到外部<code>css</code>文件，才会开始下载<code>css</code>代码，再渲染</li>\\n<li>所以，CSS内联使用使渲染时间提前</li>\\n<li>注意：但是较大的<code>css</code>代码并不合适内联（初始拥塞窗口、没有缓存），而其余代码则采取外部引用方式</li>\\n</ul>\\n</li>\\n<li><strong>异步加载CSS</strong>\\n<ul>\\n<li>在CSS文件请求、下载、解析完成之前，CSS会阻塞渲染，浏览器将不会渲染任何已处理的内容</li>\\n<li>前面加载内联代码后，后面的外部引用css则没必要阻塞浏览器渲染。这时候就可以采取异步加载的方案，主要有如下：\\n<ul>\\n<li>使用javascript将<code>link</code>标签插到<code>head</code>标签最后</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>// 创建link标签\\nconst myCSS = document.createElement( &quot;link&quot; );\\nmyCSS.rel = &quot;stylesheet&quot;;\\nmyCSS.href = &quot;mystyles.css&quot;;\\n// 插入到header的最后位置\\ndocument.head.insertBefore( myCSS, document.head.childNodes[ document.head.childNodes.length - 1 ].nextSibling )\\n</code></pre>\\n<ul>\\n<li>设置<code>link</code>标签<code>media</code>属性为<code>noexis</code>，浏览器会认为当前样式表不适用当前类型，会在不阻塞页面渲染的情况下再进行下载。加载完成后，将media的值设为<code>screen</code>或<code>all</code>，从而让浏览器开始解析CSS</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;link rel=&quot;stylesheet&quot; href=&quot;mystyles.css&quot; media=&quot;noexist&quot; onload=&quot;this.media=&#39;all&#39;&quot;&gt;\\n</code></pre>\\n<ul>\\n<li>通过<code>rel</code>属性将<code>link</code>元素标记为<code>alternate</code>可选样式表，也能实现浏览器异步加载。同样别忘了加载完成之后，将<code>rel</code>设回<code>stylesheet</code></li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;link rel=&quot;alternate stylesheet&quot; href=&quot;mystyles.css&quot; onload=&quot;this.rel=&#39;stylesheet&#39;&quot;&gt;\\n</code></pre>\\n</li>\\n</ul>\\n</li>\\n<li><strong>资源压缩</strong>\\n<ul>\\n<li>利用<code>webpack</code>、<code>gulp/grunt</code>、<code>rollup</code>等模块化工具，将<code>css</code>代码进行压缩，使文件变小，大大降低了浏览器的加载时间</li>\\n</ul>\\n</li>\\n<li><strong>合理使用选择器</strong>\\n<ul>\\n<li>css匹配的规则是从右往左开始匹配，例如<code>#markdown .content h3</code>匹配规则如下：\\n<ul>\\n<li>先找到<code>h3</code>标签元素</li>\\n<li>然后去除祖先不是<code>.content</code>的元素</li>\\n<li>最后去除祖先不是<code>#markdown</code>的元素</li>\\n</ul>\\n</li>\\n<li>如果嵌套的层级更多，页面中的元素更多，那么匹配所要花费的时间代价自然更高</li>\\n<li>所以我们在编写选择器的时候，可以遵循以下规则：\\n<ul>\\n<li>不要嵌套使用过多复杂选择器，最好不要三层以上</li>\\n<li>使用id选择器就没必要再进行嵌套</li>\\n<li>通配符和属性选择器效率最低，避免使用</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li><strong>减少使用昂贵的属性</strong>\\n<ul>\\n<li>在页面发生重绘的时候，昂贵属性如<code>box-shadow/border-radius/filter/透明度/:nth-child</code>等，会降低浏览器的渲染性能</li>\\n</ul>\\n</li>\\n<li><strong>不要使用@import</strong>\\n<ul>\\n<li>css样式文件有两种引入方式，一种是<code>link</code>元素，另一种是<code>@import</code></li>\\n<li><code>@import</code>会影响浏览器的并行下载，使得页面在加载时增加额外的延迟，增添了额外的往返耗时</li>\\n<li>而且多个<code>@import</code>可能会导致下载顺序紊乱</li>\\n<li>比如一个css文件<code>index.css</code>包含了以下内容：<code>@import url(&quot;reset.css&quot;)</code></li>\\n<li>那么浏览器就必须先把<code>index.css</code>下载、解析和执行后，才下载、解析和执行第二个文件<code>reset.css</code></li>\\n</ul>\\n</li>\\n<li><strong>其他</strong>\\n<ul>\\n<li>减少重排操作，以及减少不必要的重绘</li>\\n<li>了解哪些属性可以继承而来，避免对这些属性重复编写</li>\\n<li><code>css Sprite</code>，合成所有<code>icon</code>图片，用宽高加上b<code>ackgroud-position</code>的背景图方式显现出我们要的<code>icon</code>图，减少了<code>http</code>请求</li>\\n<li>把小的<code>icon</code>图片转成<code>base64</code>编码</li>\\n<li>CSS3动画或者过渡尽量使用<code>transform</code>和o<code>pacity</code>来实现动画，不要使用<code>left</code>和<code>top</code>属性</li>\\n</ul>\\n</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>首屏样式文件有多个模块，产品要求尽快显示顶部导航；开发提议把整份 <code>CSS</code> 全塞进 <code>HTML</code>，你会接受吗？</strong></p>\\n<p>只应考虑内联首屏真正需要的关键 <code>CSS</code>，让 <code>HTML</code> 下载后能尽早开始渲染，其余样式继续外部加载。整份内联会增大 <code>HTML</code>、占用初始传输空间且失去独立缓存优势；关键样式范围划错还可能造成后续样式切换或闪动。</p>\\n</li>\\n<li>\\n<p><strong>首页已经内联关键样式，剩余主题 <code>CSS</code> 仍阻塞渲染；有人把 <code>link</code> 的 <code>media</code> 随便改成不存在的值，你会怎样让方案可控？</strong></p>\\n<p>可让非首屏样式先以当前不匹配的 <code>media</code> 加载，并在 <code>onload</code> 后恢复为 <code>all</code> 或 <code>screen</code>，也可由脚本插入 <code>link</code>。必须保留无脚本或加载失败时的降级路径；若异步文件包含首屏必需规则，页面会先以错误样式出现。</p>\\n</li>\\n<li>\\n<p><strong>主样式 <code>index.css</code> 顶部连续 <code>@import</code> 三个文件，弱网下依赖样式明显晚到；为什么换成多个构建期合并或直接 <code>link</code> 更合理？</strong></p>\\n<p>浏览器要先下载并解析 <code>index.css</code>，发现 <code>@import</code> 后才继续请求依赖文件，额外依赖链会延后样式就绪。应由构建工具合并压缩，或让 <code>HTML</code> 直接声明可并行发现的资源；拆分过细同样会增加管理和请求成本。</p>\\n</li>\\n<li>\\n<p><strong>列表滚动时出现卡顿，样式里有多层后代选择器、大量 <code>box-shadow</code>、<code>filter</code>，同事只想把类名改成 <code>id</code>；你会先查哪一类成本？</strong></p>\\n<p>先区分选择器匹配、布局与绘制成本，不能假定提高选择器权重就能解决滚动卡顿。可先简化超过三层的选择器，再针对频繁重绘区域检查 <code>box-shadow</code>、<code>filter</code>、透明度等效果；视觉效果必须保留时，优化收益可能受设计约束。</p>\\n</li>\\n<li>\\n<p><strong>侧滑面板动画用每帧修改 <code>left</code>，低端设备上明显不顺；改成 <code>transform</code> 后还要警惕什么？</strong></p>\\n<p>位移动画优先使用 <code>transform</code>，透明变化使用 <code>opacity</code>，通常能减少由 <code>left</code>、<code>top</code> 引发的重排。仍需控制同时运动的元素数量和昂贵绘制效果，不能把属性替换当成性能保证；动画前后若读取并写入布局信息，也可能继续制造布局压力。</p>\\n</li>\\n<li>\\n<p><strong>图标库准备把几十个小图全部转成 <code>base64</code> 内嵌，另一组坚持做 <code>CSS</code> <code>Sprite</code>；你会依据什么取舍？</strong></p>\\n<p>两者都能减少独立图片请求，但 <code>base64</code> 会进入 <code>CSS</code> 并随样式一起下载，<code>Sprite</code> 则需要维护尺寸和 <code>background-position</code>。少量极小且稳定的图标可考虑内嵌，大量图标更要评估样式体积与缓存复用；不能只按请求数量决定。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_41-如何实现单行-多行文本溢出的省略样式\\"><a class=\\"header-anchor\\" href=\\"#_41-如何实现单行-多行文本溢出的省略样式\\">#</a> 41 如何实现单行／多行文本溢出的省略样式？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>单行三件套：<code>overflow: hidden</code> + <code>white-space: nowrap</code> + <code>text-overflow: ellipsis</code>，前提是元素有宽度约束</li>\\n<li>多行用 <code>display: -webkit-box</code> + <code>-webkit-box-orient: vertical</code> + <code>-webkit-line-clamp: n</code>，现在各浏览器都支持</li>\\n<li>⚠️ 在 <code>flex</code> 子项里必须加 <code>min-width: 0</code>，否则长文本撑破容器而不是省略</li>\\n<li>多行方案不要和 <code>text-overflow</code> 混用；纵向 <code>padding</code> 最好挪到外层，避免第四行从下边距露出</li>\\n<li>&quot;超出才显示 tooltip&quot; 要用 <code>JS</code> 比 <code>scrollWidth &gt; clientWidth</code>，且必须等字体加载完再判断</li>\\n</ul>\\n<p><strong>单行省略需要同时设置 <code>overflow: hidden</code>、<code>white-space: nowrap</code> 和 <code>text-overflow: ellipsis</code>，并给元素限定宽度。</strong> 多行通常用 <code>display: -webkit-box</code>，配合 <code>-webkit-box-orient: vertical</code>、<code>-webkit-line-clamp</code> 和 <code>overflow: hidden</code> 按行数截断。若更看重兼容性，也可以固定行高与高度，再用绝对定位的伪元素补省略号。遇到连续英文或数字时，还要用 <code>word-break</code> 或 <code>word-wrap</code> 避免长单词撑开布局。</p>\\n</section>\\n<p>对于文本的溢出，我们可以分成两种形式：</p>\\n<ul>\\n<li>单行文本溢出\\n<ul>\\n<li>涉及的css属性有：\\n<ul>\\n<li><code>text-overflow</code>：规定当文本溢出时，显示省略符号来代表被修剪的文本</li>\\n<li><code>white-space</code>：设置文字在一行显示，不能换行</li>\\n<li><code>overflow</code>：文字长度超出限定宽度，则隐藏超出的内容</li>\\n</ul>\\n</li>\\n<li><code>overflow</code>设为<code>hidden</code>，普通情况用在块级元素的外层隐藏内部溢出元素，或者配合下面两个属性实现文本溢出省略</li>\\n<li><code>white-space:nowrap</code>，作用是设置文本不换行，是<code>overflow:hidden</code>和<code>text-overflow：ellipsis</code>生效的基础</li>\\n<li><code>text-overflow</code> 属性值有如下\\n<ul>\\n<li><code>clip</code>：当对象内文本溢出部分裁切掉</li>\\n<li><code>ellipsis</code>：当对象内文本溢出时显示省略标记（...）</li>\\n</ul>\\n</li>\\n<li><code>text-overflow</code>只有在设置了<code>overflow:hidden</code>和<code>white-space:nowrap</code>才能够生效的</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n    p{\\n        overflow: hidden;\\n        line-height: 40px;\\n        width:400px;\\n        height:40px;\\n        border:1px solid red;\\n        text-overflow: ellipsis;\\n        white-space: nowrap;\\n    }\\n&lt;/style&gt;\\n&lt;p 这是一些文本这是一些文本这是一些文本这是一些文本这是一些文本这是一些文本这是一些文本这是一些文本这是一些文本这是一些文本&lt;/p &gt;\\n</code></pre>\\n<img src=\\"https://s.poetries.top/uploads/2022/09/25ff36e3de8eaa54.png\\" alt=\\"\\"></li>\\n<li>多行文本溢出：多行文本溢出的时候，我们可以分为两种情况：\\n<ul>\\n<li>基于高度截断\\n<ul>\\n<li>伪元素 + 定位：核心的css代码结构如下：\\n<ul>\\n<li><code>position: relative</code>：为伪元素绝对定位</li>\\n<li><code>overflow: hidden</code>：文本溢出限定的宽度就隐藏内容）</li>\\n<li><code>position: absolute</code>：给省略号绝对定位</li>\\n<li><code>line-height: 20px</code>：结合元素高度,高度固定的情况下,设定行高, 控制显示行数</li>\\n<li><code>height: 40px</code>：设定当前元素高度</li>\\n<li><code>::after {}</code> ：设置省略号样式</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>  &lt;style&gt;\\n      .demo {\\n          position: relative;\\n          line-height: 20px;\\n          height: 40px;\\n          overflow: hidden;\\n      }\\n      .demo::after {\\n          content: &quot;...&quot;;\\n          position: absolute;\\n          bottom: 0;\\n          right: 0;\\n          padding: 0 20px 0 10px;\\n      }\\n  &lt;/style&gt;\\n\\n  &lt;body&gt;\\n      &lt;div class=&#39;demo&#39;&gt;这是一段很长的文本&lt;/div&gt;\\n  &lt;/body&gt;\\n</code></pre>\\n<ul>\\n<li>实现原理很好理解，就是通过伪元素绝对定位到行尾并遮住文字，再通过 <code>overflow: hidden</code> 隐藏多余文字</li>\\n<li>这种实现具有以下优点：\\n<ul>\\n<li>兼容性好，对各大主流浏览器有好的支持</li>\\n<li>响应式截断，根据不同宽度做出调整</li>\\n</ul>\\n</li>\\n<li>一般文本存在英文的时候，可以设置<code>word-break: break-all</code>使一个单词能够在换行时进行拆分</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li>基于行数截断：纯css实现也非常简单，核心的css代码如下：\\n<ul>\\n<li><code>-webkit-line-clamp: 2</code>：用来限制在一个块元素显示的文本的行数，为了实现该效果，它需要组合其他的<code>WebKit</code>属性）</li>\\n<li><code>display: -webkit-box</code>：和1结合使用，将对象作为弹性伸缩盒子模型显示</li>\\n<li><code>-webkit-box-orient: vertical</code>：和1结合使用 ，设置或检索伸缩盒对象的子元素的排列方式</li>\\n<li><code>overflow: hidden</code>：文本溢出限定的宽度就隐藏内容</li>\\n<li><code>text-overflow: ellipsis</code>：多行文本的情况下，用省略号“…”隐藏溢出范围的文本</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\n    p {\\n        width: 400px;\\n        border-radius: 1px solid red;\\n        -webkit-line-clamp: 2;\\n        display: -webkit-box;\\n        -webkit-box-orient: vertical;\\n        overflow: hidden;\\n        text-overflow: ellipsis;\\n    }\\n&lt;/style&gt;\\n&lt;p&gt;\\n    这是一些文本这是一些文本这是一些文本这是一些文本这是一些文本\\n    这是一些文本这是一些文本这是一些文本这是一些文本这是一些文本\\n&lt;/p &gt;\\n</code></pre>\\n<ul>\\n<li>可以看到，上述使用了<code>webkit</code>的CSS属性扩展，所以兼容浏览器范围是PC端的<code>webkit</code>内核的浏览器，由于移动端大多数是使用<code>webkit</code>，所以移动端常用该形式</li>\\n<li>需要注意的是，如果文本为一段很长的英文或者数字，则需要添加<code>word-wrap: break-word</code>属性</li>\\n<li>还能通过使用javascript实现配合css，实现代码如下所示：</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>  p {\\n      position: relative;\\n      width: 400px;\\n      line-height: 20px;\\n      overflow: hidden;\\n\\n  }\\n  .p-after:after{\\n      content: &quot;...&quot;; \\n      position: absolute; \\n      bottom: 0; \\n      right: 0; \\n      padding-left: 40px;\\n      background: -webkit-linear-gradient(left, transparent, #fff 55%);\\n      background: -moz-linear-gradient(left, transparent, #fff 55%);\\n      background: -o-linear-gradient(left, transparent, #fff 55%);\\n      background: linear-gradient(to right, transparent, #fff 55%);\\n  } \\n</code></pre>\\n<pre class=\\"language-js\\"><code>$(function(){\\n  //获取文本的行高，并获取文本的高度，假设我们规定的行数是五行，那么对超过行数的部分进行限制高度，并加上省略号\\n    $(&#39;p&#39;).each(function(i, obj){\\n          var lineHeight = parseInt($(this).css(&quot;line-height&quot;));\\n          var height = parseInt($(this).height());\\n          if((height / lineHeight) &gt;3 ){\\n              $(this).addClass(&quot;p-after&quot;)\\n              $(this).css(&quot;height&quot;,&quot;60px&quot;);\\n          }else{\\n              $(this).removeClass(&quot;p-after&quot;);\\n          }\\n      });\\n  })\\n</code></pre>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表里标题设置了 <code>text-overflow: ellipsis</code>，宽度也固定为 <code>240px</code>，但长标题仍完整撑出卡片，你会先检查哪几项？</strong></p>\\n<p>先检查同一元素是否同时具备 <code>overflow: hidden</code> 和 <code>white-space: nowrap</code>，单独设置 <code>text-overflow: ellipsis</code> 不会完成裁剪。还要确认元素确实受到宽度约束；若位于弹性布局中，父子项的收缩规则也可能让预期宽度没有真正生效。</p>\\n</li>\\n<li>\\n<p><strong>资讯卡片要求标题最多显示两行，超过后省略，你会怎样写核心样式，并处理连续英文或数字？</strong></p>\\n<p>标题元素可设置 <code>display: -webkit-box</code>、<code>-webkit-box-orient: vertical</code>、<code>-webkit-line-clamp: 2</code> 与 <code>overflow: hidden</code>。连续英文或数字可能无法在预期位置换行，可补充 <code>word-wrap: break-word</code>；这套写法带有 <code>WebKit</code> 扩展属性，使用前需确认目标浏览器范围。</p>\\n</li>\\n<li>\\n<p><strong>旧版企业终端不能依赖 <code>-webkit-line-clamp</code>，但公告摘要仍须固定显示三行，你会怎样降级？</strong></p>\\n<p>可按固定 <code>line-height</code> 计算三行高度，容器设置对应 <code>height</code>、<code>position: relative</code> 和 <code>overflow: hidden</code>，再用绝对定位的 <code>::after</code> 在右下角绘制省略号并遮住尾部文字。它兼容思路更保守，但省略号可能在文本未溢出时仍出现，且依赖固定行高。</p>\\n</li>\\n<li>\\n<p><strong>两行摘要在桌面端正常，换成更窄的卡片后，右下角的伪元素省略号压住了半个汉字，你会怎样排查？</strong></p>\\n<p>先核对容器的 <code>line-height</code>、固定高度与实际行数是否匹配，再检查 <code>::after</code> 的 <code>right</code>、<code>bottom</code>、内边距和背景遮罩范围。伪元素方案本质是覆盖文字而非识别真实截断位置，字号、宽度或中英文换行变化后都可能错位，需要同步调整或改用行数裁剪。</p>\\n</li>\\n<li>\\n<p><strong>评论列表既要响应式宽度，又要兼顾较广浏览器范围，你会在基于高度和基于行数的两种截断方案间怎么选？</strong></p>\\n<p>目标环境支持 <code>-webkit-line-clamp</code> 时优先按行数截断，代码短且能随容器宽度重新排版。兼容范围更保守时可采用固定行高加伪元素，但必须接受高度耦合和遮挡误差；若还要精确判断是否溢出，可用脚本比较内容高度后再添加省略样式。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_42-让chrome支持小于12px-的文字方式有哪些\\"><a class=\\"header-anchor\\" href=\\"#_42-让chrome支持小于12px-的文字方式有哪些\\">#</a> 42 让Chrome支持小于12px 的文字方式有哪些</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>起因：<code>Chrome</code> 早期在中文环境下有 <code>12px</code> 最小字号限制，写 <code>font-size: 10px</code> 会被强制渲染成 <code>12px</code></li>\\n<li>三种绕法：<code>transform: scale()</code> 缩放（最常用，但要注意它不改变占位空间，且会创建包含块）、<code>-webkit-text-size-adjust: none</code>（新版已失效）、用图片或 <code>SVG</code> 代替文字</li>\\n<li><code>zoom</code> 也能缩小，但它会真正改变布局尺寸，和 <code>scale</code> 的表现不同</li>\\n<li><strong>重要</strong>：这个限制在现代 <code>Chrome</code> 上已经不存在了（用户主动改设置的除外），这题现在更多是考察历史知识</li>\\n<li>更该说的是：小于 <code>12px</code> 的文字本身就是可读性问题，无障碍规范建议正文不小于 <code>14px</code></li>\\n</ul>\\n<p><strong>可以先把字号设为 <code>12px</code>，再通过 <code>zoom</code> 或 <code>transform: scale()</code> 缩小视觉尺寸。</strong> <code>zoom</code> 会改变元素实际占用空间并触发重排，而且属于非标准属性；<code>transform</code> 不改变原布局空间，通常要配合 <code>display: inline-block</code> 和 <code>transform-origin: left top</code>。<code>-webkit-text-size-adjust: none</code> 受版本和文本类型限制，且会影响页面缩放时的字体调整，因此不适合作为通用方案。</p>\\n</section>\\n<ul>\\n<li><strong>背景</strong>\\n<ul>\\n<li>Chrome 中文版浏览器会默认设定页面的最小字号是<code>12px</code>，英文版没有限制</li>\\n<li>原由 Chrome 团队认为汉字小于<code>12px</code>就会增加识别难度</li>\\n<li>中文版浏览器\\n<ul>\\n<li>与网页语言无关，取决于用户在Chrome的设置里（<code>chrome://settings/languages</code>）把哪种语言设置为默认显示语言</li>\\n</ul>\\n</li>\\n<li>系统级最小字号\\n<ul>\\n<li>浏览器默认设定页面的最小字号，用户可以前往 <code>chrome://settings/fonts</code> 根据需求更改</li>\\n<li>而我们在实际项目中，不能奢求用户更改浏览器设置</li>\\n<li>对于文本需要以更小的字号来显示，就需要用到一些小技巧</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li><strong>常见的解决方案</strong>\\n<ul>\\n<li><strong>zoom</strong> 非标属性，有兼容问题，缩放会改变了元素占据的空间大小，触发重排\\n<ul>\\n<li><code>zoom</code> 的字面意思是“变焦”，可以改变页面上元素的尺寸，属于真实尺寸</li>\\n<li>其支持的值类型有：\\n<ul>\\n<li><code>zoom:50%</code>，表示缩小到原来的一半</li>\\n<li><code>zoom:0.5</code>，表示缩小到原来的一半</li>\\n</ul>\\n</li>\\n<li>使用 <code>zoom</code> 来”支持“ <code>12px</code> 以下的字体</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;style type=&quot;text/css&quot;&gt;\\n.span1{\\n    font-size: 12px;\\n    display: inline-block;\\n    zoom: 0.8;\\n}\\n.span2{\\n    display: inline-block;\\n    font-size: 12px;\\n}\\n&lt;/style&gt;\\n&lt;body&gt;\\n    &lt;span class=&quot;span1&quot;&gt;测试10px&lt;/span&gt;\\n    &lt;span class=&quot;span2&quot;&gt;测试12px&lt;/span&gt;\\n&lt;/body&gt;\\n</code></pre>\\n<ul>\\n<li>需要注意的是，<code>Zoom</code> 并不是标准属性，需要考虑其兼容性\\n<img src=\\"https://s.poetries.top/uploads/2022/09/09eaff33b52d18d5.png\\" alt=\\"\\"></li>\\n</ul>\\n</li>\\n<li><strong>-webkit-transform:scale()</strong> 大部分现代浏览器支持，并且对英文、数字、中文也能够生效，缩放不会改变了元素占据的空间大小，页面布局不会发生变化\\n<ul>\\n<li>针对<code>chrome</code>浏览器,加<code>webkit</code>前缀，用<code>transform:scale()</code>这个属性进行放缩</li>\\n<li>当我们在使用<code>transform: scale</code>缩小元素的时候，顺便把<code>transform-origin</code>改成<code>left top</code>，才能保证左上角不动，因为左上角一动就导致定位就不准了（<code>top</code>、<code>left</code>、<code>margin</code>等都不准了）</li>\\n<li>注意的是，使用<code>scale</code>属性<strong>只对可以定义宽高的元素生效</strong>，所以，下面代码中将<code>span</code>元素转为行内块元素</li>\\n</ul>\\n<pre class=\\"language-html\\"><code>&lt;style type=&quot;text/css&quot;&gt;\\n  .span1{\\n    font-size: 12px;\\n    display: inline-block;\\n    -webkit-transform:scale(0.8);\\n  }\\n  .span2{\\n    display: inline-block;\\n    font-size: 12px;\\n  }\\n&lt;/style&gt;\\n&lt;body&gt;\\n  &lt;span class=&quot;span1&quot;&gt;测试10px&lt;/span&gt;\\n  &lt;span class=&quot;span2&quot;&gt;测试12px&lt;/span&gt;\\n&lt;/body&gt;\\n</code></pre>\\n</li>\\n<li><strong>-webkit-text-size-adjust:none</strong> 对谷歌浏览器有版本要求，在<code>27</code>之后，就取消了该属性的支持，并且只对英文、数字生效\\n<ul>\\n<li>该属性用来设定文字大小是否根据设备(浏览器)来自动调整显示大小</li>\\n<li>属性值：\\n<ul>\\n<li><code>percentage</code>：字体显示的大小；</li>\\n<li><code>auto</code>：默认，字体大小会根据设备/浏览器来自动调整；</li>\\n<li><code>none</code>:字体大小不会自动调整</li>\\n</ul>\\n</li>\\n</ul>\\n<pre class=\\"language-css\\"><code>html { -webkit-text-size-adjust: none; }\\n</code></pre>\\n<ul>\\n<li>这样设置之后会有一个问题，就是当你放大网页时，一般情况下字体也会随着变大，而设置了以上代码后，字体只会显示你当前设置的字体大小，不会随着网页放大而变大了</li>\\n<li>所以，我们不建议全局应用该属性，而是单独对某一属性使用</li>\\n<li>需要注意的是，自从<code>chrome 27</code>之后，就取消了对这个属性的支持。同时，该属性只对英文、数字生效，对中文不生效</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>设计稿把中文角标写成 <code>10px</code>，你直接设置 <code>font-size: 10px</code>，但某些中文界面的 <code>Chrome</code> 仍显示得接近 <code>12px</code>，这是样式优先级问题吗？</strong></p>\\n<p>不一定是优先级问题，可能是浏览器设置了页面最小字号，中文界面的默认配置尤其容易暴露这一现象，而且限制取决于浏览器显示语言与字体设置。网页无法要求用户修改 <code>chrome://settings/fonts</code>，因此需要用元素缩放实现视觉尺寸，或与设计重新确认最小字号。</p>\\n</li>\\n<li>\\n<p><strong>价格标签必须呈现约 <code>10px</code> 的视觉字号，你准备基于 <code>12px</code> 使用 <code>transform: scale()</code>，样式要怎样写才不明显错位？</strong></p>\\n<p>将文字设为可定义尺寸的 <code>inline-block</code>，以 <code>12px</code> 为基础字号，再使用 <code>transform: scale(0.8)</code>，并把 <code>transform-origin</code> 设为 <code>left top</code>。缩放只改变视觉结果，不改变原始占位尺寸，后续元素可能留下空隙或对不齐，布局需要单独补偿。</p>\\n</li>\\n<li>\\n<p><strong>同一个小字号徽标既要缩小文字，也要求后面的图标自动贴紧，<code>zoom: 0.8</code> 和 <code>transform: scale(0.8)</code> 你会选哪一个？</strong></p>\\n<p>若必须让后续布局跟随缩小后的实际占位变化，<code>zoom</code> 更符合该现象，因为它会改变元素占据的空间并触发重排。若更看重现代浏览器支持和布局稳定，则选 <code>transform</code> 并自行修正留白；<code>zoom</code> 属于非标准方案，必须先核对目标环境兼容性。</p>\\n</li>\\n<li>\\n<p><strong>一个内联 <code>span</code> 加了 <code>transform: scale(0.8)</code>，开发者工具里能看到规则，页面却没有预期缩小，你会检查什么？</strong></p>\\n<p>先检查该 <code>span</code> 是否仍是普通行内元素，<code>scale</code> 应用于能够定义宽高的元素更可靠，可改成 <code>display: inline-block</code>。随后确认是否设置了合适的 <code>transform-origin</code>，否则元素虽已缩放，却会围绕默认原点偏移，让定位和间距看起来像样式失效。</p>\\n</li>\\n<li>\\n<p><strong>有人建议在站点根节点加 <code>-webkit-text-size-adjust: none</code>，一次解决全部小字号需求，你会接受吗？</strong></p>\\n<p>不接受把它作为全局方案；该属性控制浏览器的文字自动调整，还可能使页面缩放时字体不随之变化。资料所述的 <code>Chrome 27</code> 之后不再支持该方式，而且它只对英文和数字有效、不能解决中文最小字号，因此应优先采用局部缩放并评估布局代价。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_43-说说对css预编语言的理解-有哪些区别\\"><a class=\\"header-anchor\\" href=\\"#_43-说说对css预编语言的理解-有哪些区别\\">#</a> 43 说说对CSS预编语言的理解？有哪些区别?</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>预处理器（<code>Sass</code>/<code>Less</code>/<code>Stylus</code>）解决的是原生 <code>CSS</code> 早年缺的能力：变量、嵌套、混入、函数、模块化</li>\\n<li>三者差别主要在语法和生态：<code>Sass</code>（<code>SCSS</code> 语法）生态最大、功能最全；<code>Less</code> 语法接近 <code>CSS</code>、上手快；<code>Stylus</code> 语法最自由但用的人少</li>\\n<li>⚠️ 预处理器变量是<strong>编译期</strong>的，运行时改不了；主题切换、暗色模式必须用 <code>CSS</code> 自定义属性</li>\\n<li>现在原生 <code>CSS</code> 已经补上了变量、嵌套、<code>@layer</code>、<code>color-mix()</code>，预处理器的不可替代性在下降</li>\\n<li>另一条路线是 <code>PostCSS</code>：不是语言而是插件平台，<code>Autoprefixer</code>、<code>postcss-preset-env</code> 都在这上面</li>\\n</ul>\\n<p><strong><code>CSS</code> 预处理语言是在原生 <code>CSS</code> 上增加变量、嵌套、混入和模块化能力，再由解析器编译成浏览器可识别的 <code>CSS</code>。</strong> <code>Sass</code> 较成熟，既有缩进语法，也有兼容 <code>CSS</code> 的 <code>SCSS</code>；<code>Less</code> 更接近原生语法，因此上手更直接。<code>Stylus</code> 来自 <code>Node.js</code> 社区，写法更自由，例如变量可以用 <code>=</code> 声明。三者解决的问题相近，实际取舍通常看现有工程生态、团队语法习惯和维护成本。</p>\\n</section>\\n<ul>\\n<li><strong>前言</strong>\\n<ul>\\n<li>Css 作为一门标记性语言，语法相对简单，对使用者的要求较低，但同时也带来一些问题</li>\\n<li>需要书写大量看似没有逻辑的代码，不方便维护及扩展，不利于复用，尤其对于非前端开发工程师来讲，往往会因为缺少 Css 编写经验而很难写出组织良好且易于维护的 Css 代码</li>\\n<li>Css预处理器便是针对上述问题的解决方案</li>\\n<li>预处理语言\\n<ul>\\n<li>扩充了 Css 语言，增加了诸如变量、混合（mixin）、函数等功能，让 Css 更易维护、方便</li>\\n<li>本质上，预处理是Css的超集</li>\\n<li>包含一套自定义的语法及一个解析器，根据这些语法定义自己的样式规则，这些规则最终会通过解析器，编译生成对应的 Css 文件</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li><strong>Css预编译语言在前端里面有三大优秀的预编处理器，分别是</strong>\\n<ul>\\n<li><strong>sass</strong>\\n<ul>\\n<li>2007 年诞生，最早也是最成熟的 Css预处理器，拥有 <code>Ruby</code> 社区的支持和 <code>Compass</code> 这一最强大的 <code>Css</code>框架，目前受 <code>LESS</code> 影响，已经进化到了全面兼容 <code>Css</code> 的 <code>Scss</code></li>\\n<li>文件后缀名为<code>.sas</code>s与<code>.scss</code>，可以严格按照 <code>sass</code> 的缩进方式省去大括号和分号</li>\\n</ul>\\n</li>\\n<li><strong>less</strong>\\n<ul>\\n<li>2009年出现，受<code>SASS</code>的影响较大，但又使用 <code>Css</code> 的语法，让大部分开发者和设计师更容易上手，在 <code>Ruby</code>社区之外支持者远超过 <code>SASS</code></li>\\n<li>其缺点是比起 <code>SASS</code>来，可编程功能不够，不过优点是简单和兼容 <code>Css</code>，反过来也影响了 <code>SASS</code>演变到了<code>Scss</code> 的时代</li>\\n</ul>\\n</li>\\n<li><strong>stylus</strong>\\n<ul>\\n<li><code>Stylus</code>是一个<code>Css</code>的预处理框架，<code>2010</code> 年产生，来自 <code>Node.js</code>社区，主要用来给 <code>Node</code> 项目进行 <code>Css</code> 预处理支持</li>\\n<li>所以<code>Stylus</code> 是一种新型语言，可以创建健壮的、动态的、富有表现力的<code>Css</code>。比较年轻，其本质上做的事情与<code>SASS/LESS</code>等类似</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n<li><strong>区别</strong>\\n<ul>\\n<li>虽然各种预处理器功能强大，但使用最多的，还是以下特性：\\n<ul>\\n<li>变量（<code>variables</code>）</li>\\n<li>作用域（<code>scope</code>）</li>\\n<li>代码混合（<code>mixins</code>）</li>\\n<li>嵌套（<code>nested rules</code>）</li>\\n<li>代码模块化（<code>Modules</code>）</li>\\n</ul>\\n</li>\\n<li><strong>基本使用</strong>\\n<ul>\\n<li>less和scss<pre class=\\"language-css\\"><code>.box {\\n  display: block;\\n}\\n</code></pre>\\n</li>\\n<li>sass<pre class=\\"language-css\\"><code>.box\\n  display: block\\n</code></pre>\\n</li>\\n<li>stylus<pre class=\\"language-css\\"><code>.box\\n  display: block\\n</code></pre>\\n</li>\\n</ul>\\n</li>\\n<li><strong>嵌套</strong>\\n<ul>\\n<li>三者的嵌套语法都是一致的，甚至连引用父级选择器的标记 <code>&amp;</code> 也相同</li>\\n<li>区别只是 <code>Sass</code> 和 <code>Stylus</code> 可以用没有大括号的方式书写</li>\\n<li>less<pre class=\\"language-css\\"><code>.a {\\n  &amp;.b {\\n    color: red;\\n  }\\n}\\n</code></pre>\\n</li>\\n</ul>\\n</li>\\n<li><strong>变量</strong>\\n<ul>\\n<li>变量无疑为 <code>Css</code> 增加了一种有效的复用方式，减少了原来在 <code>Css</code> 中无法避免的重复「硬编码」</li>\\n<li><code>less</code>声明的变量必须以<code>@</code>开头，后面紧跟变量名和变量值，而且变量名和变量值需要<code>使用冒号:分隔开</code><pre class=\\"language-css\\"><code>@red: #c00;\\n\\nstrong {\\n  color: @red;\\n}\\n</code></pre>\\n</li>\\n<li><code>sass</code>声明的变量跟<code>less</code>十分的相似，只是变量名前面使用<code>$</code>开头<pre class=\\"language-css\\"><code>$red: #c00;\\n\\nstrong {\\n  color: $red;\\n}\\n</code></pre>\\n</li>\\n<li><code>stylus</code>声明的变量没有任何的限定，可以使用<code>$</code>开头，结尾的分号;可有可无，但变量与变量值之间需要使用<code>=</code>。在<code>stylus</code>中我们不建议使用<code>@</code>符号开头声明变量<pre class=\\"language-css\\"><code>red = #c00\\n\\nstrong\\n  color: red\\n</code></pre>\\n</li>\\n</ul>\\n</li>\\n<li><strong>作用域</strong>\\n<ul>\\n<li>Css 预编译器把变量赋予作用域，也就是存在生命周期。就像 js一样，它会先从局部作用域查找变量，依次向上级作用域查找</li>\\n<li><code>sass</code>中不存在全局变量<pre class=\\"language-css\\"><code>$color: black;\\n.scoped {\\n  $bg: blue;\\n  $color: white;\\n  color: $color;\\n  background-color:$bg;\\n}\\n.unscoped {\\n  color:$color;\\n} \\n</code></pre>\\n<pre class=\\"language-css\\"><code>/**编译后 */\\n.scoped {\\n  color:white;/*是白色*/\\n  background-color:blue;\\n}\\n.unscoped {\\n  color:white;/*白色（无全局变量概念）*/\\n} \\n</code></pre>\\n<ul>\\n<li>所以，在<code>sass</code>中最好不要定义相同的变量名</li>\\n</ul>\\n</li>\\n<li><code>less</code>与<code>stylus</code>的作用域跟javascript十分的相似，首先会查找局部定义的变量，如果没有找到，会像冒泡一样，一级一级往下查找，直到根为止<pre class=\\"language-css\\"><code>@color: black;\\n.scoped {\\n  @bg: blue;\\n  @color: white;\\n  color: @color;\\n  background-color:@bg;\\n}\\n.unscoped {\\n  color:@color;\\n}\\n</code></pre>\\n<pre class=\\"language-css\\"><code>/**编译后 */\\n.scoped {\\n  color:white;/*白色（调用了局部变量）*/\\n  background-color:blue;\\n}\\n.unscoped {\\n  color:black;/*黑色（调用了全局变量）*/\\n} \\n</code></pre>\\n</li>\\n</ul>\\n</li>\\n<li><strong>混入</strong>\\n<ul>\\n<li>混入（<code>mixin</code>）应该说是预处理器最精髓的功</li>\\n<li>可以在<code>Mixins</code>中定义变量或者默认参数</li>\\n<li>在<code>less</code>中，混合的用法是指将定义好的<code>ClassA</code>中引入另一个已经定义的<code>Class</code>，也能使用够传递参数，参数变量为<code>@</code>声明<pre class=\\"language-css\\"><code>.alert {\\n  font-weight: 700;\\n}\\n\\n.highlight(@color: red) {\\n  font-size: 1.2em;\\n  color: @color;\\n}\\n\\n.heads-up {\\n  .alert;\\n  .highlight(red);\\n}\\n</code></pre>\\n<pre class=\\"language-css\\"><code>/**编译后 */\\n.alert {\\n  font-weight: 700;\\n}\\n.heads-up {\\n  font-weight: 700;\\n  font-size: 1.2em;\\n  color: red;\\n}\\n</code></pre>\\n</li>\\n<li><code>Sass</code>声明<code>mixins</code>时需要使用<code>@mixin</code>，后面紧跟<code>mixin</code>的名，也可以设置参数，参数名为变量<code>$</code>声明的形式<pre class=\\"language-css\\"><code>  @mixin large-text {\\n    font: {\\n      family: Arial;\\n      size: 20px;\\n      weight: bold;\\n    }\\n    color: #ff0000;\\n  }\\n\\n  .page-title {\\n    @include large-text;\\n    padding: 4px;\\n    margin-top: 10px;\\n  }\\n</code></pre>\\n</li>\\n<li><code>stylus</code>中的混合和前两款Css预处理器语言的混合略有不同，他可以不使用任何符号，就是直接声明<code>Mixins</code>名，然后在定义参数和默认值之间用等号（<code>=</code>）来连接<pre class=\\"language-css\\"><code>error(borderWidth= 2px) {\\n  border: borderWidth solid #F00;\\n  color: #F00;\\n}\\n.generic-error {\\n  padding: 20px;\\n  margin: 4px;\\n  error(); /* 调用error mixins */\\n}\\n.login-error {\\n  left: 12px;\\n  position: absolute;\\n  top: 20px;\\n  error(5px); /* 调用error mixins，并将参数$borderWidth的值指定为5px */\\n} \\n</code></pre>\\n</li>\\n</ul>\\n</li>\\n<li><strong>代码模块化</strong>\\n<ul>\\n<li>模块化就是将Css代码分成一个个模块</li>\\n<li><code>scss、less、stylus</code>三者的使用方法都如下所示<pre class=\\"language-css\\"><code>@import &#39;./common&#39;;\\n@import &#39;./github-markdown&#39;;\\n@import &#39;./mixin&#39;;\\n@import &#39;./variables&#39;;\\n</code></pre>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>团队把 <code>Sass</code>、<code>Less</code> 当成浏览器能够直接识别的新 <code>CSS</code> 语法，准备把源文件原样部署到静态服务器，你会指出什么问题？</strong></p>\\n<p>预处理语言包含自定义语法和解析器，本质流程是先把变量、混入、函数与嵌套规则编译成浏览器可识别的 <code>CSS</code>。若直接部署 <code>.scss</code>、<code>.less</code> 或 <code>.styl</code> 源文件，浏览器不会替构建系统完成解析；工程中必须把编译产物纳入构建和发布链路。</p>\\n</li>\\n<li>\\n<p><strong>一个多人维护的组件库存在大量颜色硬编码和重复前缀规则，你会优先用预处理器的哪些能力治理？</strong></p>\\n<p>可先用变量集中管理颜色等重复值，用 <code>mixin</code> 封装带参数的成组声明，再按职责拆分模块并通过导入组合。嵌套只能适度减少选择器重复，层级过深仍会生成复杂 <code>CSS</code>；预处理器改善的是源码组织，不能替代命名规范和产物检查。</p>\\n</li>\\n<li>\\n<p><strong>旧项目允许 <code>Sass</code>、<code>Less</code>、<code>Stylus</code> 三选一，但团队成员只熟悉标准 <code>CSS</code>，且不需要复杂编程能力，你会如何权衡？</strong></p>\\n<p><code>Less</code> 使用接近 <code>CSS</code> 的大括号语法，变量以 <code>@</code> 开头，通常更容易让现有成员理解；<code>SCSS</code> 同样兼容常见 <code>CSS</code> 写法，变量使用 <code>$</code>，混入通过 <code>@mixin</code> 和 <code>@include</code>。<code>Stylus</code> 表达更自由，也可省略括号和分号，但自由度会提高团队约定成本。</p>\\n</li>\\n<li>\\n<p><strong>迁移样式时，<code>.unscoped</code> 的颜色在 <code>Sass</code> 与 <code>Less</code> 编译结果不同，你会从哪里定位变量污染？</strong></p>\\n<p>先检查同名变量是否在嵌套作用域内被重新赋值，并分别核对编译器的作用域规则，不能假设三种预处理器完全一致。资料示例中 <code>Less</code> 会优先取局部变量、外部仍取根变量，而 <code>Sass</code> 示例的内部赋值影响了后续结果；迁移时应避免复用同名变量并直接比较编译产物。</p>\\n</li>\\n<li>\\n<p><strong>按钮、提示框和登录错误样式都要复用一组边框规则，团队在复制 <code>class</code> 与参数化 <code>mixin</code> 之间争论，你会怎么定？</strong></p>\\n<p>边框宽度或颜色需要按调用点变化时，参数化 <code>mixin</code> 更合适：<code>Less</code> 可调用带 <code>@</code> 参数的混合，<code>Sass</code> 用 <code>@mixin</code> 定义并通过 <code>@include</code> 引入，<code>Stylus</code> 可用带默认值的函数式写法。若规则完全相同且应共享语义，普通 <code>class</code> 更直接，也能避免编译后重复展开声明。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_44-编程题\\"><a class=\\"header-anchor\\" href=\\"#_44-编程题\\">#</a> 44 编程题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>三角形原理：把宽高设为 <code>0</code>，相邻边框在交界处<strong>斜切</strong>，只给一条边颜色就得到三角形</li>\\n<li>梯形是三角形的变体 —— 给元素留一点宽度，两条斜切边就成了梯形的腰</li>\\n<li>圆/半圆/扇形都靠 <code>border-radius</code>，它支持 <code>x轴/y轴</code> 两个半径和四角分别设置</li>\\n<li><code>0.5px</code> 细线用 <code>transform: scaleY(0.5)</code> 实现，别直接写 <code>0.5px</code>（部分安卓渲染成 0）</li>\\n<li>两栏/三栏布局现在一行 <code>grid-template-columns</code> 就够；圣杯和双飞翼是浮动时代的历史绕法，要能说清它当年解决什么问题</li>\\n</ul>\\n<p><strong>这类 <code>CSS</code> 编程题本质上是在考盒模型、边框绘制和布局方式，代码之外还要讲清楚为什么能实现。</strong> <code>0.5px</code> 细线可以画 <code>1px</code> 后用 <code>transform: scaleY(0.5)</code> 缩小；三角形和梯形则利用宽高为零或保留少量宽度时，边框交界处的斜切效果。圆、半圆和扇形主要靠 <code>border-radius</code> 控制各角半径。两栏或三栏布局现在优先考虑 <code>flex</code>、<code>grid</code>，而 <code>float</code>、负 <code>margin</code>、圣杯和双飞翼更适合用来说明旧布局方案的原理。</p>\\n</section>\\n<p>这一节是 <code>CSS</code> 面试里最常见的几道手写题。<strong>它们考的不是记忆，而是你对盒模型、边框绘制和布局模型的理解</strong>，所以下面每道题除了代码，重点看&quot;为什么能这么画&quot;。</p>\\n<p><strong>一句话原理速览：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>题目</th>\\n<th>原理</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>0.5px</code> 细线</td>\\n<td>画 <code>1px</code> 再 <code>transform: scaleY(0.5)</code> 缩小 —— 直接写 <code>0.5px</code> 在部分安卓会被渲染成 0</td>\\n</tr>\\n<tr>\\n<td>三角形</td>\\n<td>相邻边框在交界处是<strong>斜切</strong>的，把宽高设为 0、只留一条边有颜色，剩下的就是三角</td>\\n</tr>\\n<tr>\\n<td>圆 / 半圆 / 扇形</td>\\n<td><code>border-radius</code> 按 <code>x轴/y轴</code> 两个半径控制，四个角可以分别设</td>\\n</tr>\\n<tr>\\n<td>梯形</td>\\n<td>三角形的变体 —— 给元素留一点宽度，斜切边就变成了梯形的两腰</td>\\n</tr>\\n<tr>\\n<td>两栏 / 三栏</td>\\n<td>老方案靠 <code>float</code> + 负 <code>margin</code> 绕开&quot;中间列先渲染&quot;的限制；现在一行 <code>grid</code> 就够了</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>答这类题的加分点</strong>：先说清原理，再给现代写法，最后说明老方案存在的历史原因。比如三栏布局，圣杯和双飞翼是浮动时代为了让中间列在 DOM 里排最前（优先渲染）又不被两侧遮住而发明的绕法；今天写 <code>grid-template-columns: 200px 1fr 200px</code> 一行搞定，还能用 <code>grid-template-areas</code> 让 DOM 顺序和视觉顺序解耦。能说出这层演进，比默写负 <code>margin</code> 的值有价值得多。</p>\\n<h3 id=\\"画一条-0-5px-的线\\"><a class=\\"header-anchor\\" href=\\"#画一条-0-5px-的线\\">#</a> 画一条 0.5px 的线</h3>\\n<ul>\\n<li>采用 <code>meta viewport</code> 的方式 <code>&lt;meta name=&quot;viewport&quot; content=&quot;initial-scale=1.0, maximum-scale=1.0, user-scalable=no&quot; /&gt;</code></li>\\n<li>采用 <code>border-image</code> 的方式</li>\\n<li>采用 <code>transform: scale()</code> 的方式</li>\\n</ul>\\n<h3 id=\\"如何画一个三角形\\"><a class=\\"header-anchor\\" href=\\"#如何画一个三角形\\">#</a> 如何画一个三角形</h3>\\n<p>三角形原理:边框的均分原理</p>\\n<pre class=\\"language-css\\"><code>div {\\n  width:0px;\\n  height:0px;\\n  border-top:10px solid red; \\n  border-right:10px solid transparent; \\n  border-bottom:10px solid transparent; \\n  border-left:10px solid transparent;\\n}\\n</code></pre>\\n<h3 id=\\"圆-半圆-椭圆\\"><a class=\\"header-anchor\\" href=\\"#圆-半圆-椭圆\\">#</a> 圆？半圆？椭圆？</h3>\\n<pre class=\\"language-css\\"><code>div {\\n  width: 100px;\\n  height: 100px;\\n  background-color: red;\\n  margin-top: 20px;\\n}\\n.box1 { /* 圆 */\\n  /* border-radius: 50%; */\\n  border-radius: 50px;\\n}\\n.box2 { /* 半圆 */\\n  height: 50px;\\n  border-radius: 50px 50px 0 0;\\n}\\n.box3 { /* 椭圆 */\\n  height: 50px;\\n  border-radius: 50px/25px; /* x轴/y轴 */\\n}\\n</code></pre>\\n<h3 id=\\"css画圆半圆扇形三角梯形\\"><a class=\\"header-anchor\\" href=\\"#css画圆半圆扇形三角梯形\\">#</a> CSS画圆半圆扇形三角梯形</h3>\\n<pre class=\\"language-css\\"><code>div{\\n    margin: 50px;\\n    width: 100px;\\n    height: 100px;\\n    background: red;\\n}\\n/* 半圆 */\\n.half-circle{\\n    height: 50px;\\n    border-radius: 50px 50px 0 0;\\n}\\n/* 扇形 */\\n.sector{\\n    border-radius: 100px 0 0;\\n}\\n/* 三角 */\\n.triangle{\\n    width: 0px;\\n    height: 0px;\\n    background: none;\\n    border: 50px solid red;\\n    border-color: red transparent transparent transparent;\\n}\\n/* 梯形 */\\n.ladder{\\n    width: 50px;\\n    height: 0px;\\n    background: none;\\n    border: 50px solid red;\\n    border-color: red transparent transparent transparent;\\n}\\n</code></pre>\\n<h3 id=\\"两栏布局-左边定宽-右边自适应方案\\"><a class=\\"header-anchor\\" href=\\"#两栏布局-左边定宽-右边自适应方案\\">#</a> 两栏布局：左边定宽，右边自适应方案</h3>\\n<pre class=\\"language-html\\"><code>&lt;div class=&quot;box&quot;&gt;\\n  &lt;div class=&quot;box-left&quot;&gt;&lt;/div&gt;\\n  &lt;div class=&quot;box-right&quot;&gt;&lt;/div&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p><strong>利用float + margin实现</strong></p>\\n<pre class=\\"language-css\\"><code>.box {\\n height: 200px;\\n}\\n\\n.box &gt; div {\\n  height: 100%;\\n}\\n\\n.box-left {\\n  width: 200px;\\n  float: left;\\n  background-color: blue;\\n}\\n\\n.box-right {\\n  margin-left: 200px;\\n  background-color: red;\\n}\\n</code></pre>\\n<p><strong>利用calc计算宽度</strong></p>\\n<pre class=\\"language-css\\"><code>.box {\\n height: 200px;\\n}\\n\\n.box &gt; div {\\n  height: 100%;\\n}\\n\\n.box-left {\\n  width: 200px;\\n  float: left;\\n  background-color: blue;\\n}\\n\\n.box-right {\\n  width: calc(100% - 200px);\\n  float: right;\\n  background-color: red;\\n}\\n</code></pre>\\n<p><strong>利用float + overflow实现</strong></p>\\n<pre class=\\"language-css\\"><code>.box {\\n height: 200px;\\n}\\n\\n.box &gt; div {\\n  height: 100%;\\n}\\n\\n.box-left {\\n  width: 200px;\\n  float: left;\\n  background-color: blue;\\n}\\n\\n.box-right {\\n  overflow: hidden;\\n  background-color: red;\\n}\\n</code></pre>\\n<p><strong>利用flex实现</strong></p>\\n<pre class=\\"language-css\\"><code>.box {\\n  height: 200px;\\n  display: flex;\\n}\\n\\n.box &gt; div {\\n  height: 100%;\\n}\\n\\n.box-left {\\n  width: 200px;\\n  background-color: blue;\\n}\\n\\n.box-right {\\n  flex: 1; // 设置flex-grow属性为1，默认为0\\n  background-color: red;\\n}\\n</code></pre>\\n<h3 id=\\"三栏布局-左右两边宽度固定中间自适应\\"><a class=\\"header-anchor\\" href=\\"#三栏布局-左右两边宽度固定中间自适应\\">#</a> 三栏布局：左右两边宽度固定中间自适应</h3>\\n<blockquote>\\n<p>float，float + calc, 圣杯布局（设置BFC，margin负值法），flex</p>\\n</blockquote>\\n<pre class=\\"language-css\\"><code>.wrap {\\n  width: 100%;\\n  height: 200px;\\n}\\n.wrap &gt; div {\\n  height: 100%;\\n}\\n/* 方案1：两边使用 float，中间使用 margin */\\n.left {\\n  width: 120px;\\n  float: left;\\n}\\n.right {\\n  float: right;\\n  width: 120px;\\n}\\n.center {\\n  margin: 0 120px; \\n}\\n/* 方案2：两边使用 float，中间使用 calc */\\n.left {\\n  width: 120px;\\n  float: left;\\n}\\n.right {\\n  float: right;\\n  width: 120px;\\n}\\n.center {\\n  width: calc(100% - 240px);\\n  margin-left: 120px;\\n}\\n/* 方案3：flex实现 */\\n.wrap {\\n  display: flex;\\n}\\n.left {\\n  width: 120px;\\n}\\n.right {\\n  width: 120px;\\n}\\n.center {\\n  flex: 1;\\n}\\n/* 方案4：grid网格布局 */\\n.wrap {\\n  display: grid;\\n  width: 100%;\\n  grid-template-columns: 300px auto 300px;\\n}\\n.left {\\n  background: coral;\\n}\\n.right {\\n  background: lightblue;\\n}\\n.center {\\n  background: #555;\\n}\\n.left,.right,.center {\\n  height: 100px;\\n}\\n</code></pre>\\n<blockquote>\\n<p>圣杯布局和双飞翼布局是前端工程师需要日常掌握的重要布局方式。两者的功能相同，都是为了实现一个两侧宽度固定，中间宽度自适应的三栏布局。</p>\\n</blockquote>\\n<p><strong>圣杯布局</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\nbody{\\n    min-width: 550px;\\n}\\n#container{\\n    padding-left: 200px;\\n    padding-right: 150px;\\n}\\n#container .column{\\n    float: left;\\n}\\n#center{\\n    width: 100%;\\n}\\n#left{\\n    width: 200px;\\n    margin-left: -100%;\\n    position: relative;\\n    right: 200px;\\n}\\n#right{\\n    width: 150px;\\n    margin-right: -150px;\\n}\\n&lt;/style&gt;\\n&lt;div id=&quot;container&quot;&gt;\\n    &lt;div id=&quot;center&quot; class=&quot;column&quot;&gt;center&lt;/div&gt;\\n    &lt;div id=&quot;left&quot; class=&quot;column&quot;&gt;left&lt;/div&gt;\\n    &lt;div id=&quot;right&quot; class=&quot;column&quot;&gt;right&lt;/div&gt;\\n&lt;/div&gt;\\n</code></pre>\\n<p><strong>双飞翼布局</strong></p>\\n<pre class=\\"language-html\\"><code>&lt;style&gt;\\nbody {\\n    min-width: 500px;\\n}\\n#container {\\n    width: 100%;\\n}\\n.column {\\n    float: left;\\n}\\n#center {\\n    margin-left: 200px;\\n    margin-right: 150px;\\n}\\n#left {\\n    width: 200px;\\n    margin-left: -100%;\\n}\\n#right {\\n    width: 150px;\\n    margin-left: -150px;\\n}\\n&lt;/style&gt;\\n&lt;div id=&quot;container&quot; class=&quot;column&quot;&gt;\\n    &lt;div id=&quot;center&quot;&gt;center&lt;/div&gt;\\n&lt;/div&gt;\\n&lt;div id=&quot;left&quot; class=&quot;column&quot;&gt;left&lt;/div&gt;\\n&lt;div id=&quot;right&quot; class=&quot;column&quot;&gt;right&lt;/div&gt;\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>后台页面要求左右栏各 <code>120px</code>、中间自适应，有人只给中间栏写 <code>width: 100%</code>，结果三栏换行或溢出，你会怎样解释？</strong></p>\\n<p><code>width: 100%</code> 会让中间栏先占满容器，若两侧仍参与正常布局，总宽度自然超过容器。可让两侧分别左右浮动并给中间设置 <code>margin: 0 120px</code>，或直接使用 <code>flex</code>、<code>grid</code> 分配剩余空间；选择取决于目标浏览器和现有布局约束。</p>\\n</li>\\n<li>\\n<p><strong>新建管理台没有旧浏览器包袱，左右导航固定宽度、中间表格随窗口伸缩，你会写哪种布局？</strong></p>\\n<p>可使用 <code>display: flex</code>，左右栏设固定 <code>width</code>，中间栏设置 <code>flex: 1</code>；也可用 <code>grid-template-columns: 120px auto 120px</code> 明确三列轨道。<code>flex</code> 更适合一维伸缩，<code>grid</code> 对列结构表达更直接；还要为过窄窗口规定最小宽度或溢出策略。</p>\\n</li>\\n<li>\\n<p><strong>产品要求中间正文在 <code>HTML</code> 中排最前，但视觉上仍位于两侧栏之间，且项目只能沿用浮动方案，你会怎么实现？</strong></p>\\n<p>可采用圣杯或双飞翼布局：三列浮动，中间列先写并占满宽度，两侧再用负 <code>margin</code> 拉回同一行。圣杯通过外层左右 <code>padding</code> 留位并相对定位左栏，双飞翼则在中间列内部用左右 <code>margin</code> 留位；两者都依赖精确宽度和最小宽度约束。</p>\\n</li>\\n<li>\\n<p><strong>双飞翼布局在窗口缩窄后右栏掉到下一行，中间内容也被挤压，你会优先核对哪些数值？</strong></p>\\n<p>先核对页面 <code>min-width</code> 是否至少容纳左右固定宽度及可用的中间区域，再检查中间内部的左右 <code>margin</code> 是否分别等于侧栏宽度。还要确认左栏的 <code>margin-left: -100%</code>、右栏的负边距和三列 <code>float</code> 未被其他规则覆盖；这些值不匹配就会换行。</p>\\n</li>\\n<li>\\n<p><strong>维护十年前的三栏页面时，重构方想把圣杯布局一次性换成 <code>grid</code>，兼容方坚持不动，你会怎样做选型？</strong></p>\\n<p>若目标浏览器已支持网格布局，<code>grid-template-columns</code> 能直接表达固定侧栏与自适应中栏，代码和维护成本更低；否则保留浮动方案更稳妥。迁移不能只看视觉一致，还要验证原有 <code>DOM</code> 顺序、最小宽度、溢出行为以及旧样式对 <code>float</code> 清除的依赖。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);