(window.webpackJsonp=window.webpackJsonp||[]).push([[122],{1230:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：理解原理要建立输入、状态变化、输出和失败边界，而不是只记 API 与结论</li>\\n<li>原理主线：围绕 「高阶组件（HOC）：最经典的组件逻辑复用」、「Render Props：逻辑复用的另一」、「有状态组件与无状态组件：“单一职责”原则」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：讲解了 React 中的高阶组件（HOC）等设计模式，介绍了其原理、实现方式及在实际开发中如何复用组件逻辑，帮助开发者更好地理解和应用 React 设计思想。</li>\\n<li>边界与代价：文章中的实现通常有版本和场景前提，迁移到当前项目时必须先核对运行环境与依赖</li>\\n<li>工程落地：面试回答按核心机制、执行链路、边界反例和工程验证展开，最后说明取舍</li>\\n</ul>\\n<p><strong><code>React</code> 中的设计模式，本质上是在控制变化范围的同时复用组件逻辑。</strong> <code>HOC</code> 通过函数包裹组件统一注入能力，<code>render props</code> 则把数据适配权交给调用方，因此面对不同属性名时更灵活。数据处理和界面渲染也应尽量拆到有状态组件与无状态组件中，让两类变化互不牵连。实际开发中我一般优先考虑自定义 <code>Hook</code>，因为它能减少嵌套和属性冲突；但维护旧类组件时，<code>HOC</code> 与 <code>render props</code> 仍有价值。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文若分析 <code>ReactDOM.render</code>、旧生命周期或栈调和，应把它视为理解架构演进的历史路径。<code>React 19</code> 已移除 <code>ReactDOM.render</code>，当前客户端入口使用 <code>createRoot</code>；并发渲染也必须区分可中断的渲染阶段与同步提交阶段。迁移前对照 <a href=\\"https://react.dev/blog/2024/04/25/react-19-upgrade-guide\\">React 19 官方升级指南</a>。</p>\\n<h2 id=\\"高阶组件-hoc-最经典的组件逻辑复用方式\\"><a class=\\"header-anchor\\" href=\\"#高阶组件-hoc-最经典的组件逻辑复用方式\\">#</a> 高阶组件（HOC）：最经典的组件逻辑复用方式</h2>\\n<h3 id=\\"什么是高阶组件\\"><a class=\\"header-anchor\\" href=\\"#什么是高阶组件\\">#</a> 什么是高阶组件</h3>\\n<blockquote>\\n<p>高阶组件（HOC）是 React 中用于复用组件逻辑的一种高级技巧。HOC 自身不是 React API 的一部分，它是一种基于 React 的组合特性而形成的设计模式。——React 官方</p>\\n</blockquote>\\n<p>高阶函数的概念：<code>接收函数作为输入，或者输出另一个函数的一类函数</code>，就是高阶函数。</p>\\n<p>相应的，<code>高阶组件指的就是参数为组件，返回值为新组件的函数</code>。没错，高阶组件本质上是一个函数。下面是一个简单的高阶组件示例：</p>\\n<pre class=\\"language-js\\"><code>const withProps = (WrappedComponent) =&gt; {\\n    const targetComponent = (props) =&gt; (\\n        &lt;div className=&quot;wrapper-container&quot;&gt;\\n            &lt;WrappedComponent {...props} /&gt;\\n        &lt;/div&gt;\\n    );\\n    return targetComponent;\\n};\\n</code></pre>\\n<p>在这段代码中，<code>withProps</code> 就是一个高阶组件。</p>\\n<h3 id=\\"高阶组件是如何实现逻辑复用的\\"><a class=\\"header-anchor\\" href=\\"#高阶组件是如何实现逻辑复用的\\">#</a> 高阶组件是如何实现逻辑复用的？</h3>\\n<p>现在我们考虑这样一种情况：我有一个名为 checkUserAccess 的方法，这个方法专门用来校验用户的身份是否合法，若不合法，那么一部分组件就要根据这个不合法的身份调整自身的展示逻辑（比如查看个人信息界面需要提示“请校验身份”等）。</p>\\n<p>假如说页面中的 A、B、C、D、E 五个组件都需要甄别用户身份是否合法，那么这五个组件在理论上都需要先请求一遍 checkUserAccess 这个接口。但一个一个对组件进行修改未免太麻烦了，我们期望对“获取 checkUserAccess 接口信息，并通知到对应组件”这层逻辑进行复用，这时候就可以请出高阶组件来帮忙了。</p>\\n<p>我们可以像下面代码这样在高阶组件中定义这层通用的逻辑：</p>\\n<pre class=\\"language-js\\"><code>// 假设 checkUserAccess 已经在 utils 文件中被封装为了一段独立的逻辑\\nimport checkUserAccess from &#39;./utils\\n// 用高阶组件包裹目标组件\\nconst withCheckAccess = (WrappedComponent) =&gt; {\\n    // 这部分是通用的逻辑：判断用户身份是否合法\\n    const isAccessible = checkUserAccess()  \\n    // 将 isAccessible（是否合法） 这个信息传递给目标组件\\n    const targetComponent = (props) =&gt; (\\n        &lt;div className=&quot;wrapper-container&quot;&gt;\\n            &lt;WrappedComponent {...props} isAccessible={isAccessible} /&gt;\\n        &lt;/div&gt;\\n    );\\n    return targetComponent;\\n};\\n</code></pre>\\n<p>这样当我们需要为某个组件复用这层请求逻辑的时候，只需要直接用 withCheckAccess 包裹这个组件就可以了。以 A 组件为例，假设 A 组件的原始版本为 AComponent，那么包裹它的形式就是下面代码这样：</p>\\n<pre class=\\"language-js\\"><code>const EnhancedAComponent = withCheckAccess(Acomponent);\\n</code></pre>\\n<p>通过简单地对高阶组件 withCheckAccess 进行引入，EnhancedAComponent 轻松具备了校验用户合法性的能力。这样一来，即便再多出 5 个组件想要引入 checkUserAccess，我们也不会怂——毕竟包裹五个组件和重写五段逻辑的工作量是没法相提并论的</p>\\n<p>高阶组件不仅能够帮助我们简化逻辑的引入过程，还可以帮助我们规避掉逻辑变更带来的烦琐的修改步骤：假如这段 checkUserAccess 的逻辑是散落在 A、B、C、D、E 这五个组件之中的，那么一旦 checkUserAccess 的判定规则需要修改，我们就得需要去修改五段代码；但现在，checkUserAccess 被抽离进了一个独立的高阶组件里，我们在高阶组件中的一次修改，将在所有被它处理过的组件中生效。</p>\\n<p>由此可以看出，高阶组件可以帮助我们<code>从根本上减少重复的编写和修改工作</code>，这不仅是高阶组件这一种模式的利好，更是“逻辑复用”这件事情的意义所在。</p>\\n<h2 id=\\"render-props-逻辑复用的另一种思路\\"><a class=\\"header-anchor\\" href=\\"#render-props-逻辑复用的另一种思路\\">#</a> Render Props：逻辑复用的另一种思路</h2>\\n<blockquote>\\n<p>术语“render prop”是指一种在 React 组件之间使用一个值为函数的 prop 共享代码的简单技术。——React 官方</p>\\n</blockquote>\\n<h3 id=\\"什么是-render-props\\"><a class=\\"header-anchor\\" href=\\"#什么是-render-props\\">#</a> 什么是 render props</h3>\\n<p>render props 是 React 中复用组件逻辑的另一种思路，它在实现上和高阶组件有异曲同工之妙——两者都是把通用的逻辑提取到某一处。区别主要在于使用层面，<code>高阶组件的使用姿势是用“函数”包裹“组件”</code>，而 <code>render props</code> 恰恰相反，<code>它强调的是用“组件”包裹“函数”</code></p>\\n<p>一个简单的 <code>render props</code> 可以是这样的，见下面代码：</p>\\n<pre class=\\"language-js\\"><code>import React from &#39;react&#39;  \\nconst RenderChildren = (props) =&gt; {\\n  return(\\n     &lt;React.Fragment&gt;\\n        {props.children(props)}\\n     &lt;/React.Fragment&gt;\\n  );\\n};\\n</code></pre>\\n<blockquote>\\n<p><code>RenderChildren</code> 将渲染它所有的子组件。从这段代码里，你需要把握住两个要点：</p>\\n</blockquote>\\n<ul>\\n<li><code>render props</code> 的载体应该是一个<code>React 组件</code>，这一点是与高阶组件不同的（高阶组件本质是函数）；</li>\\n<li><code>render props</code> 组件正常工作的前提是它的<code>子组件需要以函数形式存在</code>。</li>\\n</ul>\\n<p>第 1 点相对明显一点，你可能会对第 2 点感到迷惑。没关系，我们直接来看 <code>RenderChildren</code>的使用方式，请看下面代码：</p>\\n<pre class=\\"language-js\\"><code>&lt;RenderChildren&gt;         \\n  {() =&gt; &lt;p&gt;我是 RenderChildren 的子组件&lt;/p&gt;}       \\n&lt;/RenderChildren&gt;\\n</code></pre>\\n<blockquote>\\n<p><code>RenderChildren</code> 本身是一个 React 组件，它可以包裹其他的 React 组件。一般来说，我们习惯于看到的包裹形式是“标签包裹着标签”，也就是下面代码演示的这种效果：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>&lt;RenderChildren&gt;         \\n  &lt;p&gt;我是 RenderChildren 的子组件&lt;/p&gt;     \\n&lt;/RenderChildren&gt;\\n</code></pre>\\n<p>但在 <code>render props</code> 这种模式下，它要求被 <code>render props</code> 组件标签包裹的一定是个函数，也就是所谓的“函数作为子组件传入”。这样一来，<code>render props</code> 组件就可以通过调用这个函数，传递 <code>props</code>，从而实现和目标组件的通信了。</p>\\n<h3 id=\\"render-props-是如何实现逻辑复用的\\"><a class=\\"header-anchor\\" href=\\"#render-props-是如何实现逻辑复用的\\">#</a> render props 是如何实现逻辑复用的</h3>\\n<p>这里我仍然以 checkUserAccess 这个场景举例。使用 <code>render props 复用 checkUserAccess 这段逻辑</code>，我们可以这样做，请看下面代码：</p>\\n<pre class=\\"language-js\\"><code>// 假设 checkUserAccess 已经在 utils 文件中被封装为了一段独立的逻辑\\nimport checkUserAccess from &#39;./utils\\n// 定义 render props 组件\\nconst CheckAccess = (props) =&gt; {\\n    // 这部分是通用的逻辑：判断用户身份是否合法\\n    const isAccessible = checkUserAccess()  \\n    // 将 isAccessible（是否合法） 这个信息传递给目标组件\\n    return &lt;React.Fragment&gt;\\n        {props.children({ ...props, isAccessible })}\\n      &lt;/React.Fragment&gt;\\n};\\n</code></pre>\\n<p>接下来 CheckAccess 子组件就可以这样获取 isAccessible 的值，见下面代码：</p>\\n<pre class=\\"language-js\\"><code>&lt;CheckAccess&gt;\\n  {\\n    (props) =&gt; {\\n      const { isAccessible } = props;\\n      return &lt;ChildComponent {...props} isAccessible={isAccessible} /&gt;\\n    }\\n  }\\n&lt;/CheckAccess&gt;\\n</code></pre>\\n<blockquote>\\n<p>到这里，“函数作为子组件传入”这种情况，我们已经了解了它的来龙去脉。但其实，<code>对于  render props 这种模式来说，函数并不一定要作为子组件传入</code>，它也可以以任意属性名传入，只要 render props 组件可以感知到它就行`。</p>\\n</blockquote>\\n<p>举个例子，我可以允许函数通过一个名为 checkTaget 的属性传入 render props 组件，那么 CheckAccess 组件只需要改写一下它接收函数的形式即可，见下面代码：</p>\\n<pre class=\\"language-js\\"><code>// 假设 checkUserAccess 已经在 utils 文件中被封装为了一段独立的逻辑\\nimport checkUserAccess from &#39;./utils\\n// 定义 render props 组件\\nconst CheckAccess = (props) =&gt; {\\n    // 这部分是通用的逻辑：判断用户身份是否合法\\n    const isAccessible = checkUserAccess()  \\n    // 将 isAccessible（是否合法） 这个信息传递给目标组件\\n    return &lt;React.Fragment&gt;\\n        {props.checkTaget({ ...props, isAccessible })}\\n      &lt;/React.Fragment&gt;\\n};\\n</code></pre>\\n<blockquote>\\n<p>在使用 <code>CheckAccess</code> 组件的时候，我们将函数放在 <code>checkTaget</code> 中传入组件即可，见下面代码：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>&lt;CheckAccess\\n    checkTaget={(props) =&gt; {\\n      const { isAccessible } = props;\\n      return &lt;ChildComponent {...props} isAccessible={isAccessible} /&gt;\\n    }}\\n  /&gt;\\n</code></pre>\\n<blockquote>\\n<p>像这样使用 <code>render props</code>，也是完全可以的。</p>\\n</blockquote>\\n<h3 id=\\"理解-render-props-的灵活之处\\"><a class=\\"header-anchor\\" href=\\"#理解-render-props-的灵活之处\\">#</a> 理解 render props 的灵活之处</h3>\\n<p>读到这里，你不免会产生这样的困惑：高阶组件和 render props 都能复用逻辑，那我到底用哪个好呢？</p>\\n<blockquote>\\n<p>这里我先给出结论：<code>render props 将是你更好的选择，因为它更灵活</code>。这“更灵活”从何说起呢？</p>\\n</blockquote>\\n<p><code>render props 和高阶组件一个非常重要的区别</code>，在于对数据的处理上：<code>在高阶组件中，目标组件对于数据的获取没有主动权，数据的分发逻辑全部收敛在高阶组件的内部</code>；而在 <code>render props</code> 中，<code>除了父组件可以对数据进行分发处理之外，子组件也可以选择性地对数据进行接收</code></p>\\n<p>这样说你可能会觉得有点抽象，我举个例子：假如说我们现在多出一个 F 组件，它同样需要 checkUserAccess 这段逻辑。但是这个 F 组件是一个老组件，它识别不了 props.isAccessible，只认识 props.isValidated。带着这个需求，我们先来看看高阶组件怎么解决问题。原有的高阶组件逻辑是下面这样的：</p>\\n<pre class=\\"language-js\\"><code>// 假设 checkUserAccess 已经在 utils 文件中被封装为了一段独立的逻辑\\nimport checkUserAccess from &#39;./utils\\n// 用高阶组件包裹目标组件\\nconst withCheckAccess = (WrappedComponent) =&gt; {\\n    // 这部分是通用的逻辑：判断用户身份是否合法\\n    const isAccessible = checkUserAccess()  \\n    // 将 isAccessible（是否合法） 这个信息传递给目标组件\\n    const targetComponent = (props) =&gt; (\\n        &lt;div className=&quot;wrapper-container&quot;&gt;\\n            &lt;WrappedComponent {...props} isAccessible={isAccessible} /&gt;\\n        &lt;/div&gt;\\n    );\\n    return targetComponent;\\n};\\n</code></pre>\\n<p>它会不由分说地给所有组件安装上 isAccessible 这个变量。要想让它适配 F 组件的逻辑，最直接的一个思路就是在 withCheckAccess 中增加一个组件类型的判断，一旦判断出当前入参是 F 组件，就专门将 isAccessible 改名为 isValidated。</p>\\n<p>这样做虽然能够暂时解决问题，但这并不是一个灵活的解法：假如需要改属性名的组件越来越多，那么withCheckAccess 内部将不可避免变得越来越臃肿，长此以往将难以维护。</p>\\n<p>事实上，在软件设计模式中，有一个非常重要的原则，叫“开放封闭原则”。一个好的模式，应该尽可能做到对拓展开放，对修改封闭。</p>\\n<p>当我们发现 withCheckAccess 的内部逻辑需要频繁地跟随需求的变化而变化时，此时就应该提高警惕了，因为这已经违反了“对修改封闭”这一原则。</p>\\n<blockquote>\\n<p>处理同样的需求，render props 就能够在保全“开放封闭”原则的基础上，帮我们达到目的。</p>\\n</blockquote>\\n<p>前面说过，在 render props 中，除了父组件可以对数据进行分发处理之外，子组件也可以选择性地对数据进行接收。这就意味着我们可以在新增的 F 组件相关的逻辑中把数据适配这件事情给做掉（如下面代码所示），而不会影响老的 CheckAccess 组件中的逻辑。</p>\\n<pre class=\\"language-js\\"><code>&lt;CheckAccess&gt;\\n  {\\n    (props) =&gt; {\\n      const { isAccessible } = props;\\n      return &lt;ChildComponent {...props} isValidated={isAccessible} /&gt;\\n    }\\n  }\\n&lt;/CheckAccess&gt;\\n</code></pre>\\n<blockquote>\\n<p>这样一来，不管你新来的组件有多少个，需要变更的属性名有多少个，影响面都会被牢牢地控制在“新增逻辑”这个范畴里。契合了“开放封闭”原则的 render props 模式显然比高阶组件灵活多了。</p>\\n</blockquote>\\n<h2 id=\\"有状态组件与无状态组件-单一职责-原则在组件设计模式中的实践\\"><a class=\\"header-anchor\\" href=\\"#有状态组件与无状态组件-单一职责-原则在组件设计模式中的实践\\">#</a> 有状态组件与无状态组件：“单一职责”原则在组件设计模式中的实践</h2>\\n<p><strong>什么是“单一职责”原则？</strong></p>\\n<p>单一职责原则又叫“单一功能原则”，它指的是一个类或者模块应该有且只有一个改变的原因。通俗来讲，就是说咱们的组件功能要尽可能地聚合，不要试图让一个组件做太多的事情。</p>\\n<p><strong>什么是有状态组件？什么是无状态组件？</strong></p>\\n<p>无状态组件这个概念我们在第 06 讲中已经介绍过了，这里简单复习一下：</p>\\n<p>函数组件顾名思义，就是以函数的形态存在的 React 组件。早期并没有 React-Hooks 的加持，函数组件内部无法定义和维护 state，因此它还有一个别名叫“无状态组件”。</p>\\n<p>如下面代码所示，就是一个典型的无状态组件：</p>\\n<pre class=\\"language-js\\"><code>function DemoFunction(props) {\\n  const { text } = props\\n  return (\\n    &lt;div className=&quot;demoFunction&quot;&gt;\\n      &lt;p&gt;{`function 组件所接收到的来自外界的文本内容是：[${text}]`}&lt;/p&gt;\\n    &lt;/div&gt;\\n  );\\n}\\n</code></pre>\\n<p>无状态组件不一定是函数组件，不维护内部状态的类组件也可以被认为是无状态组件。\\n相比之下，能够在组件内部维护状态、管理数据的组件，就是“有状态组件”。</p>\\n<p><strong>为何需要剥离有状态组件和无状态组件？</strong></p>\\n<p>有状态组件和无状态组件有很多别名，有的书籍里也会管它们叫“容器组件”和“展示组件”，甚至“聪明组件”和“傻瓜组件”。不管叫啥，核心目的就一个——把数据处理和界面渲染这两个工作剥离开来。</p>\\n<p>为什么要这样做？别忘了，React 的核心特征是“数据驱动视图”，我们经常用下图的公式来表达它的工作模式：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210428220645.png\\" alt=\\"\\"></p>\\n<p>因此对一个 React 组件来说，它做的事情说到底无外乎是这两件：</p>\\n<ul>\\n<li>处理数据（包括数据的获取、格式化、分发等）</li>\\n<li>渲染界面</li>\\n</ul>\\n<p>我们当然也可以在一个组件里面做完这两件事情，但这样不够优雅。</p>\\n<p>按照“单一职责”的原则，我们应该将数据处理的逻辑和界面渲染的逻辑剥离到不同的组件中去，这样功能模块的组合将会更加灵活，也会更加有利于逻辑的复用。此外，单一职责还能够帮助我们尽可能地控制变更范围，降低代码的维护成本：当数据相关的逻辑发生变化时，我们只需要去修改有状态组件就可以了，无状态组件将完全不受影响。</p>\\n<h2 id=\\"why-hooks-设计模式解决不了所有的问题\\"><a class=\\"header-anchor\\" href=\\"#why-hooks-设计模式解决不了所有的问题\\">#</a> Why Hooks：设计模式解决不了所有的问题</h2>\\n<p>设计模式虽好，但它并非万能。</p>\\n<p>就 React 来说，无论是高阶组件，还是 render props，两者的出现都是为了弥补类组件在“逻辑复用”这个层面的不灵活性。它们各自都有着自己的不足，这些不足包括但不限于以下几点：</p>\\n<ul>\\n<li>嵌套地狱问题，当嵌套层级过多后，数据源的追溯会变得十分困难</li>\\n<li>较高的学习成本</li>\\n<li>props 属性命名冲突问题</li>\\n</ul>\\n<blockquote>\\n<p>总体来看，“HOC/render props+类组件”这种研发模式，还是不够到位。当设计模式解决不了问题时，我们本能地需要从编程模式上寻找答案。于是便有了如今大家在 React 中所看到的 “函数式编程”对“面向对象”的补充（并且大有替代之势），有了今天我们所看到的“一切皆可 Hooks”的大趋势。</p>\\n</blockquote>\\n<p>现在，当我们想要去复用一段逻辑时，第一反应肯定不是“高阶函数”或者“render props”，而应该是“自定义 Hook”。Hooks 能够很好地规避掉旧时类组件中各种设计模式带来的弊端，比如说它不存在嵌套地狱，允许属性重命名、允许我们在任何需要它的地方引入并访问目标状态等。由此可以看出，一个好的编程模式可以帮我们节约掉大量“打补丁”式地学习各种组件设计模式的时间。框架设计越合理，开发者的工作就越轻松。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>权限页连续套了六层 <code>HOC</code>，线上报错时只看到最外层组件拿到了错误的 <code>props</code>，为什么再加一个调试型 <code>HOC</code> 不是根治方案？</strong></p>\\n<p>再包一层只会继续加深嵌套，使状态来源和传递路径更难追溯。应先识别各层复用的状态逻辑，把适合独立复用的部分收敛为自定义 <code>Hook</code>；迁移仍要保留清晰边界，避免把互不相关的职责塞进一个大 <code>Hook</code>。</p>\\n</li>\\n<li>\\n<p><strong>表单团队的两个 <code>HOC</code> 都注入名为 <code>value</code> 的属性，接入页面后其中一个值被覆盖，你会怎么处理这场接口归属冲突？</strong></p>\\n<p>这属于 <code>props</code> 命名空间共享带来的碰撞，继续约定更多前缀只是在维护补丁。可将两段逻辑改为自定义 <code>Hook</code>，由消费组件在解构时自行重命名；代价是调用方要显式组织返回值，也必须遵守 <code>Hook</code> 的调用约束。</p>\\n</li>\\n<li>\\n<p><strong>组件库负责人认为 <code>render props</code> 已能复用全部逻辑，没必要引入 <code>Hooks</code>；当业务页出现多层函数嵌套时，你如何判断是否值得迁移？</strong></p>\\n<p>若嵌套已经让数据源追踪、属性命名和阅读成本持续上升，自定义 <code>Hook</code> 更适合作为逻辑复用边界。它允许组件在需要的位置取得目标状态并自行命名，但不会自动改善职责混乱；迁移收益应由结构清晰度决定，而非为了统一语法。</p>\\n</li>\\n<li>\\n<p><strong>搜索页从类组件迁到自定义 <code>Hook</code> 后，筛选状态来自哪里反而更难看懂，你会先查实现的哪一层？</strong></p>\\n<p>先检查 <code>Hook</code> 是否把请求、筛选、展示状态和外部参数全部隐藏在同一返回对象中，而不是先否定 <code>Hooks</code>。<code>Hooks</code> 能消除包装层级，却不能替代合理的职责拆分；应按稳定业务能力拆分并清楚命名输入输出，过度碎片化同样会增加追踪成本。</p>\\n</li>\\n<li>\\n<p><strong>一个基础组件只需给子节点暴露一次主题值，评审却要求把所有 <code>render props</code> 全面改成 <code>Hooks</code>，你赞成吗？</strong></p>\\n<p>不应仅因 <code>Hooks</code> 更常用就机械替换所有现有模式，应看当前实现是否真的存在嵌套、命名冲突或学习维护成本。稳定且边界清楚的 <code>render props</code> 可以保留；强制迁移会制造改动风险，而新增复杂逻辑复用可优先采用自定义 <code>Hook</code>。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);