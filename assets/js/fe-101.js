(window.webpackJsonp=window.webpackJsonp||[]).push([[101],{1208:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：状态管理的核心是把状态变化约束成可追踪的数据流，中间件在派发边界扩展异步、日志和副作用</li>\\n<li>原理主线：围绕 「一、redux-thunk」、「二、redux-saga 简介」、「三、redux-saga使用案例」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：总结了redux-saga中间件的原理、用法及其在处理副作用、异步操作中的优势，帮助前端开发者深入理解redux-saga的工作机制与实际应用场景。</li>\\n<li>边界与代价：集中式状态不是越多越好；可变性、订阅粒度和异步取消决定性能与可维护性</li>\\n<li>工程落地：选型要从状态归属、更新频率、调试需求和团队约束出发，而不是只比较 API 数量</li>\\n</ul>\\n<p><strong><code>redux-saga</code> 通过 <code>Generator</code> 和声明式 <code>Effect</code> 集中编排 <code>Redux</code> 的异步操作与副作用，同时让业务 <code>action</code> 保持为普通对象。</strong> <code>Saga</code> 通常由 <code>watcher</code> 监听 <code>action</code>，再由 <code>worker</code> 使用 <code>call</code> 执行请求、用 <code>put</code> 派发新 <code>action</code>，最终仍由 <code>reducer</code> 同步更新状态。相比 <code>redux-thunk</code> 直接执行函数型 <code>action</code>，<code>Saga</code> 先产出描述操作的对象，再由中间件解释执行，因此流程更集中，也更方便测试和取消。并发场景要注意选择：<code>takeEvery</code> 会处理每次触发，<code>takeLatest</code> 只保留最近一次任务。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文若分析 <code>ReactDOM.render</code>、旧生命周期或栈调和，应把它视为理解架构演进的历史路径。<code>React 19</code> 已移除 <code>ReactDOM.render</code>，当前客户端入口使用 <code>createRoot</code>；并发渲染也必须区分可中断的渲染阶段与同步提交阶段。迁移前对照 <a href=\\"https://react.dev/blog/2024/04/25/react-19-upgrade-guide\\">React 19 官方升级指南</a>。</p>\\n<h2 id=\\"一、redux-thunk\\"><a class=\\"header-anchor\\" href=\\"#一、redux-thunk\\">#</a> 一、redux-thunk</h2>\\n<h3 id=\\"_1-1-redux的副作用处理\\"><a class=\\"header-anchor\\" href=\\"#_1-1-redux的副作用处理\\">#</a> 1.1 redux的副作用处理</h3>\\n<blockquote>\\n<p>redux中的数据流大致是</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>UI—————&gt;action（plain）—————&gt;reducer——————&gt;state——————&gt;UI\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/gitee/2019/10/484.png\\" alt=\\"img\\"></p>\\n<ul>\\n<li><code>redux</code>是遵循函数式编程的规则，上述的数据流中，<code>action</code>是一个原始js对象（<code>plain object</code>）且<code>reducer</code>是一个纯函数，对于同步且没有副作用的操作，上述的数据流起到可以管理数据，从而控制视图层更新的目的</li>\\n<li>如果存在副作用函数，那么我们需要首先处理副作用函数，然后生成原始的js对象。如何处理副作用操作，在<code>redux</code>中选择在发出<code>action</code>，到<code>reducer</code>处理函数之间使用中间件处理副作用</li>\\n</ul>\\n<blockquote>\\n<p>redux增加中间件处理副作用后的数据流大致如下：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>UI——&gt;action(side function)—&gt;middleware—&gt;action(plain)—&gt;reducer—&gt;state—&gt;UI\\n</code></pre>\\n<p><img src=\\"https://s.poetries.top/gitee/2019/10/485.png\\" alt=\\"img\\"></p>\\n<blockquote>\\n<p>在有副作用的<code>action</code>和原始的<code>action</code>之间增加中间件处理，从图中我们也可以看出，中间件的作用就是：</p>\\n</blockquote>\\n<ul>\\n<li>转换异步操作，<strong>生成原始的action</strong>，这样，<code>reducer</code>函数就能处理相应的<code>action</code>，从而改变<code>state</code>，更新<code>UI</code></li>\\n</ul>\\n<h3 id=\\"_1-2-redux-thunk源码\\"><a class=\\"header-anchor\\" href=\\"#_1-2-redux-thunk源码\\">#</a> 1.2 redux-thunk源码</h3>\\n<blockquote>\\n<p>在redux中，thunk是redux作者给出的中间件，实现极为简单，10多行代码</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>function createThunkMiddleware(extraArgument) {\\n  return ({ dispatch, getState }) =&gt; next =&gt; action =&gt; {\\n    if (typeof action === &#39;function&#39;) {\\n      return action(dispatch, getState, extraArgument);\\n    }\\n\\n    return next(action);\\n  };\\n}\\n\\nconst thunk = createThunkMiddleware();\\nthunk.withExtraArgument = createThunkMiddleware;\\n\\nexport default thunk;\\n</code></pre>\\n<blockquote>\\n<p>这几行代码做的事情也很简单，判别action的类型，如果action是函数，就调用这个函数，调用的步骤为</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>action(dispatch, getState, extraArgument);\\n</code></pre>\\n<blockquote>\\n<p>发现实参为<code>dispatch</code>和<code>getState</code>，因此我们在定义<code>action</code>为<code>thunk</code>函数是，一般形参为<code>dispatch</code>和<code>getState</code></p>\\n</blockquote>\\n<h3 id=\\"_1-3-redux-thunk的缺点\\"><a class=\\"header-anchor\\" href=\\"#_1-3-redux-thunk的缺点\\">#</a> 1.3 redux-thunk的缺点</h3>\\n<blockquote>\\n<pre class=\\"language-text\\"><code>thunk`的缺点也是很明显的，`thunk`仅仅做了执行这个函数，并不在乎函数主体内是什么，也就是说`thunk`使得`redux`可以接受函数作为`action`，但是函数的内部可以多种多样。比如下面是一个获取商品列表的异步操作所对应的`action\\n</code></pre>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>export default ()=&gt;(dispatch)=&gt;{\\n    fetch(&#39;/api/goodList&#39;,{ //fecth返回的是一个promise\\n      method: &#39;get&#39;,\\n      dataType: &#39;json&#39;,\\n    }).then(function(json){\\n      var json=JSON.parse(json);\\n      if(json.msg==200){\\n        dispatch({type:&#39;init&#39;,data:json.data});\\n      }\\n    },function(error){\\n      console.log(error);\\n    });\\n};\\n</code></pre>\\n<blockquote>\\n<p>从这个具有副作用的<code>action</code>中，我们可以看出，函数内部极为复杂。如果需要为每一个异步操作都如此定义一个<code>action</code>，显然<code>action</code>不易维护</p>\\n</blockquote>\\n<p><strong>action不易维护的原因</strong></p>\\n<ul>\\n<li><code>action</code>的形式不统一</li>\\n<li>就是异步操作太为分散，分散在了各个<code>action</code>中</li>\\n</ul>\\n<h2 id=\\"二、redux-saga-简介\\"><a class=\\"header-anchor\\" href=\\"#二、redux-saga-简介\\">#</a> 二、redux-saga 简介</h2>\\n<blockquote>\\n<p><code>redux-saga</code>是一个 <code>redux</code>中间件，它具有如下特性</p>\\n</blockquote>\\n<ul>\\n<li>集中处理 <code>redux</code> 副作用问题。</li>\\n<li>被实现为 <code>generator</code> 。</li>\\n<li>类 <code>redux-thunk</code> 中间件。</li>\\n<li><code>watch</code>/<code>worker</code>（监听-&gt;执行） 的工作形式</li>\\n</ul>\\n<p><strong>redux-saga的优点</strong></p>\\n<ul>\\n<li>集中处理了所有的异步操作，异步接口部分一目了然</li>\\n<li><code>action</code>是普通对象，这跟<code>redux</code>同步的<code>action</code>一模一样</li>\\n<li>通过<code>Effect</code>，方便异步接口的测试</li>\\n<li>通过<code>worker</code> 和<code>watcher</code>可以实现非阻塞异步调用，并且同时可以实现非阻塞调用下的事件监听</li>\\n<li>异步操作的流程是可以控制的，可以随时取消相应的异步操作</li>\\n</ul>\\n<blockquote>\\n<p>基本用法</p>\\n</blockquote>\\n<ul>\\n<li>使用<code>createSagaMiddleware</code>方法创建<code>saga</code> 的<code>Middleware</code>，然后在创建的<code>redux</code>的<code>store</code>时，使用<code>applyMiddleware</code>函数将创建的<code>saga Middleware</code>实例绑定到<code>store</code>上，最后可以调用<code>saga Middleware</code>的<code>run</code>函数来执行某个或者某些<code>Middleware</code>。</li>\\n<li>在<code>saga</code>的<code>Middleware</code>中，可以使用<code>takeEvery</code>或者<code>takeLatest</code>等<code>API</code>来监听某个<code>action</code>，当某个<code>action</code>触发后，<code>saga</code>可以使用<code>call</code>发起异步操作，操作完成后使用<code>put</code>函数触发<code>action</code>，同步更新<code>state</code>，从而完成整个<code>State</code>的更新。</li>\\n</ul>\\n<h2 id=\\"三、redux-saga使用案例\\"><a class=\\"header-anchor\\" href=\\"#三、redux-saga使用案例\\">#</a> 三、redux-saga使用案例</h2>\\n<ul>\\n<li><code>redux-saga</code>是控制执行的<code>generator</code>，在<code>redux-saga</code>中<code>action</code>是原始的<code>js</code>对象，把所有的异步副作用操作放在了<code>saga</code>函数里面。这样既统一了<code>action</code>的形式，又使得异步操作集中可以被集中处理</li>\\n<li><code>redux-saga</code>是通过<code>genetator</code>实现的，如果不支持<code>generator</code>需要通过插件<code>babel-polyfill</code>转义。我们接着来实现一个输出<code>hellosaga</code>的例子</li>\\n</ul>\\n<p><strong>创建一个helloSaga.js文件</strong></p>\\n<pre class=\\"language-text\\"><code>export function * helloSaga() {\\n  console.log(&#39;Hello Sagas!&#39;);\\n}\\n</code></pre>\\n<p><strong>在redux中使用redux-saga中间件</strong></p>\\n<blockquote>\\n<p>在<code>main.js</code>中</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>import { createStore, applyMiddleware } from &#39;redux&#39;\\nimport createSagaMiddleware from &#39;redux-saga&#39;\\nimport { helloSaga } from &#39;./sagas&#39;\\nconst sagaMiddleware=createSagaMiddleware();\\nconst store = createStore(\\n reducer,\\n applyMiddleware(sagaMiddleware)\\n);\\nsagaMiddleware.run(helloSaga);\\n//会输出Hello, Sagas!\\n</code></pre>\\n<blockquote>\\n<p>和调用<code>redux</code>的其他中间件一样，如果想使用<code>redux-saga</code>中间件，那么只要在<code>applyMiddleware</code>中调用一个<code>createSagaMiddleware</code>的实例。唯一不同的是需要调用<code>run</code>方法使得<code>generator</code>可以开始执行</p>\\n</blockquote>\\n<h2 id=\\"四、redux-saga使用细节\\"><a class=\\"header-anchor\\" href=\\"#四、redux-saga使用细节\\">#</a> 四、redux-saga使用细节</h2>\\n<h3 id=\\"_4-1-声明式的effect\\"><a class=\\"header-anchor\\" href=\\"#_4-1-声明式的effect\\">#</a> 4.1 声明式的Effect</h3>\\n<blockquote>\\n<p>在<code>redux-saga</code>中提供了一系列的<code>api</code>，比如<code>take</code>、<code>put</code>、<code>all</code>、<code>select</code>等<code>API</code>，在<code>redux-saga</code>中将这一系列的<code>api</code>都定义为Effect。这些<code>Effect</code>执行后，当函数<code>resolve</code>时返回一个描述对象，然后<code>redux-saga</code>中间件根据这个描述对象恢复执行<code>generator</code>中的函数</p>\\n</blockquote>\\n<p><strong>redux-thunk的大体过程</strong></p>\\n<blockquote>\\n<pre class=\\"language-text\\"><code>action1(side function)`—&gt;`redux-thunk`监听—&gt;执行相应的有副作用的方法—&gt;`action2(plain object)\\n</code></pre>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2019/10/486.png\\" alt=\\"img\\"></p>\\n<blockquote>\\n<p>转化到<code>action2</code>是一个原始<code>js</code>对象形式的<code>action</code>，然后执行<code>reducer</code>函数就会更新<code>store</code>中的<code>state</code></p>\\n</blockquote>\\n<p><strong>redux-saga的大体过程</strong></p>\\n<blockquote>\\n<p>action1(plain object)——&gt;redux-saga监听—&gt;执行相应的Effect方法——&gt;返回描述对象—&gt;恢复执行异步和副作用函数—&gt;action2(plain object)</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2019/10/487.png\\" alt=\\"img\\"></p>\\n<blockquote>\\n<p>对比<code>redux-thunk</code>我们发现，<code>redux-saga</code>中监听到了原始<code>js</code>对象<code>action</code>，并不会马上执行副作用操作，会先通过<code>Effect</code>方法将其转化成一个描述对象，然后再将描述对象，作为标识，再恢复执行副作用函数</p>\\n</blockquote>\\n<h3 id=\\"_4-2-effect提供的具体方法\\"><a class=\\"header-anchor\\" href=\\"#_4-2-effect提供的具体方法\\">#</a> 4.2 Effect提供的具体方法</h3>\\n<blockquote>\\n<p>下面来介绍几个<code>Effect</code>中常用的几个方法，从低阶的API，比如<code>take</code>，<code>call(apply)</code>，<code>fork</code>，<code>put</code>，<code>select</code>等，以及高阶<code>API</code>，比如<code>takeEvery</code>和<code>takeLatest</code>等</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>import {take,call,put,select,fork,takeEvery,takeLatest} from &#39;redux-saga/effects&#39;\\n</code></pre>\\n<h4 id=\\"_4-2-1-take\\"><a class=\\"header-anchor\\" href=\\"#_4-2-1-take\\">#</a> 4.2.1 take</h4>\\n<blockquote>\\n<p><code>take</code>这个方法，是用来监听<code>action</code>，返回的是监听到的<code>action</code>对象。比如</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>const loginAction = {\\n   type:&#39;login&#39;\\n}\\n</code></pre>\\n<blockquote>\\n<p>在<code>UI Component</code>中<code>dispatch</code>一个<code>action</code></p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>dispatch(loginAction)\\n</code></pre>\\n<p>在saga中使用：</p>\\n<pre class=\\"language-text\\"><code>const action = yield take(&#39;login&#39;);\\n</code></pre>\\n<blockquote>\\n<p>可以监听到UI传递到中间件的<code>Action</code>,上述<code>take</code>方法的返回，就是<code>dipath</code>的原始对象。一旦监听到<code>login</code>动作，返回的<code>action</code>为：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>{\\n  type:&#39;login&#39;\\n}\\n</code></pre>\\n<h4 id=\\"_4-2-2-call-apply\\"><a class=\\"header-anchor\\" href=\\"#_4-2-2-call-apply\\">#</a> 4.2.2 call(apply)</h4>\\n<blockquote>\\n<p><code>call</code>和<code>apply</code>方法与<code>js</code>中的<code>call</code>和<code>apply</code>相似，我们以<code>call</code>方法为例</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>call(fn, ...args)\\n</code></pre>\\n<blockquote>\\n<p><code>call</code>方法调用<code>fn</code>，参数为<code>args</code>，返回一个描述对象。不过这里<code>call</code>方法传入的函数<code>fn</code>可以是普通函数，也可以是<code>generator</code>。<code>call</code>方法应用很广泛，在<code>redux-saga</code>中使用异步请求等常用<code>call</code>方法来实现</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>yield call(fetch,&#39;/userInfo&#39;,username)\\n</code></pre>\\n<h4 id=\\"_4-2-3-put\\"><a class=\\"header-anchor\\" href=\\"#_4-2-3-put\\">#</a> 4.2.3 put</h4>\\n<blockquote>\\n<p>redux-saga做为中间件，工作流是这样的</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>UI——&gt;action1————&gt;redux-saga中间件————&gt;action2————&gt;reducer..\\n</code></pre>\\n<blockquote>\\n<p>从工作流中，我们发现<code>redux-saga</code>执行完副作用函数后，必须发出<code>action</code>，然后这个<code>action</code>被<code>reducer</code>监听，从而达到更新<code>state</code>的目的。相应的这里的<code>put</code>对应与<code>redux</code>中的<code>dispatch</code>，工作流程图如下</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2019/10/488.png\\" alt=\\"img\\"></p>\\n<blockquote>\\n<p>可以看出<code>redux-saga</code>执行副作用方法转化<code>action</code>时，<code>put</code>这个<code>Effect</code>方法跟<code>redux</code>原始的<code>dispatch</code>相似，都是可以发出<code>action</code>，且发出的<code>action</code>都会被<code>reducer</code>监听到。<code>put</code>的使用方法</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>yield put({type:&#39;login&#39;})\\n</code></pre>\\n<h4 id=\\"_4-2-4-select\\"><a class=\\"header-anchor\\" href=\\"#_4-2-4-select\\">#</a> 4.2.4 select</h4>\\n<blockquote>\\n<p><code>put</code>方法与<code>redux</code>中的<code>dispatch</code>相对应，同样的如果我们想在中间件中获取<code>state</code>，那么需要使用<code>select</code>。<code>select</code>方法对应的是<code>redux</code>中的<code>getState</code>，用户获取<code>store</code>中的<code>state</code>，使用方法：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>const id = yield select(state =&gt; state.id);\\n</code></pre>\\n<h4 id=\\"_4-2-5-fork\\"><a class=\\"header-anchor\\" href=\\"#_4-2-5-fork\\">#</a> 4.2.5 fork</h4>\\n<blockquote>\\n<p><code>fork</code>方法相当于<code>web work</code>，<code>fork</code>方法不会阻塞主线程，在非阻塞调用中十分有用</p>\\n</blockquote>\\n<h4 id=\\"_4-2-6-takeevery和takelatest\\"><a class=\\"header-anchor\\" href=\\"#_4-2-6-takeevery和takelatest\\">#</a> 4.2.6 takeEvery和takeLatest</h4>\\n<blockquote>\\n<p><code>takeEvery</code>和<code>takeLatest</code>用于监听相应的动作并执行相应的方法，是构建在<code>take</code>和<code>fork</code>上面的高阶<code>api</code>，比如要监听<code>login</code>动作，好用<code>takeEvery</code>方法可以</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>takeEvery(&#39;login&#39;,loginFunc)\\n</code></pre>\\n<ul>\\n<li><code>takeEvery</code>监听到<code>login</code>的动作，就会执行<code>loginFunc</code>方法，除此之外，<code>takeEvery</code>可以同时监听到多个相同的<code>action</code>。</li>\\n<li><code>takeLatest</code>方法跟<code>takeEvery</code>是相同方式调用</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>takeLatest(&#39;login&#39;,loginFunc)\\n</code></pre>\\n<blockquote>\\n<p>与<code>takeLatest</code>不同的是，<code>takeLatest</code>是会监听执行最近的那个被触发的<code>action</code></p>\\n</blockquote>\\n<h2 id=\\"五、案例分析一\\"><a class=\\"header-anchor\\" href=\\"#五、案例分析一\\">#</a> 五、案例分析一</h2>\\n<blockquote>\\n<p>接着我们来实现一个<code>redux-saga</code>样例，存在一个登陆页，登陆成功后，显示列表页，并且，在列表页，可以点击登出，返回到登陆页。例子的最终展示效果如下</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2019/10/489.png\\" alt=\\"img\\"></p>\\n<blockquote>\\n<p>样例的功能流程图为</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/gitee/2019/10/490.png\\" alt=\\"img\\"></p>\\n<h3 id=\\"_5-1-loginpanel-登陆页\\"><a class=\\"header-anchor\\" href=\\"#_5-1-loginpanel-登陆页\\">#</a> 5.1 LoginPanel(登陆页)</h3>\\n<p><strong>输入时时保存用户名和密码</strong></p>\\n<ul>\\n<li>用户名输入框和密码框onchange时触发的函数为</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>changeUsername:(e)=&gt;{\\n    dispatch({type:&#39;CHANGE_USERNAME&#39;,value:e.target.value});\\n },\\nchangePassword:(e)=&gt;{\\n  dispatch({type:&#39;CHANGE_PASSWORD&#39;,value:e.target.value});\\n}\\n</code></pre>\\n<blockquote>\\n<p>在函数中最后会<code>dispatch</code>两个<code>action：CHANGE_USERNAME和CHANGE_PASSWORD</code></p>\\n</blockquote>\\n<ul>\\n<li>在<code>saga.js</code>文件中监听这两个方法并执行副作用函数，最后<code>put</code>发出转化后的<code>action</code>，给<code>reducer</code>函数调用</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>function * watchUsername(){\\n  while(true){\\n    const action= yield take(&#39;CHANGE_USERNAME&#39;);\\n    yield put({type:&#39;change_username&#39;,\\n    value:action.value});\\n  }\\n}\\nfunction * watchPassword(){\\n  while(true){\\n    const action=yield take(&#39;CHANGE_PASSWORD&#39;);\\n    yield put({type:&#39;change_password&#39;,\\n    value:action.value});\\n  }\\n}\\n</code></pre>\\n<blockquote>\\n<p>最后在<code>reducer</code>中接收到<code>redux-saga</code>的<code>put</code>方法传递过来的<code>action：change_username</code>和<code>change_password</code>，然后更新<code>state</code></p>\\n</blockquote>\\n<p><strong>监听登陆事件判断登陆是否成功</strong></p>\\n<blockquote>\\n<p>在UI中发出的登陆事件为</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>toLoginIn:(username,password)=&gt;{\\n  dispatch({type:&#39;TO_LOGIN_IN&#39;,username,password});\\n}\\n</code></pre>\\n<blockquote>\\n<p>登陆事件的<code>action</code>为：<code>TO_LOGIN_IN</code>.对于登入事件的处理函数为：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>while(true){\\n    //监听登入事件\\n    const action1=yield take(&#39;TO_LOGIN_IN&#39;);\\n    const res=yield call(fetchSmart,&#39;/login&#39;,{\\n      method:&#39;POST&#39;,\\n      body:JSON.stringify({\\n        username:action1.username,\\n        password:action1.password\\n    })\\n    if(res){\\n      put({type:&#39;to_login_in&#39;});\\n    }\\n});\\n</code></pre>\\n<blockquote>\\n<p>在上述的处理函数中，首先监听原始动作提取出传递来的用户名和密码，然后请求是否登陆成功，如果登陆成功有返回值，则执行<code>put</code>的<code>action:to_login_in</code></p>\\n</blockquote>\\n<h3 id=\\"_5-2-loginsuccess\\"><a class=\\"header-anchor\\" href=\\"#_5-2-loginsuccess\\">#</a> 5.2 LoginSuccess</h3>\\n<blockquote>\\n<p>(登陆成功列表展示页)</p>\\n</blockquote>\\n<ul>\\n<li>登陆成功后的页面功能包括：\\n<ul>\\n<li>获取列表信息，展示列表信息</li>\\n<li>登出功能，点击可以返回登陆页面</li>\\n</ul>\\n</li>\\n</ul>\\n<p><strong>获取列表信息</strong></p>\\n<pre class=\\"language-text\\"><code>import {delay} from &#39;redux-saga&#39;;\\n\\nfunction * getList(){\\n  try {\\n   yield delay(3000);\\n   const res = yield call(fetchSmart,&#39;/list&#39;,{\\n     method:&#39;POST&#39;,\\n     body:JSON.stringify({})\\n   });\\n   yield put({type:&#39;update_list&#39;,list:res.data.activityList});\\n } catch(error) {\\n   yield put({type:&#39;update_list_error&#39;, error});\\n }\\n}\\n</code></pre>\\n<blockquote>\\n<p>为了演示请求过程，我们在本地<code>mock</code>，通过<code>redux-saga</code>的工具函数<code>delay</code>，<code>delay</code>的功能相当于延迟xx秒，因为真实的请求存在延迟，因此可以用delay在本地模拟真实场景下的请求延迟</p>\\n</blockquote>\\n<p><strong>登出功能</strong></p>\\n<pre class=\\"language-text\\"><code>const action2=yield take(&#39;TO_LOGIN_OUT&#39;);\\nyield put({type:&#39;to_login_out&#39;});\\n</code></pre>\\n<blockquote>\\n<p>与登入相似，登出的功能从UI处接受<code>action:TO_LOGIN_OUT</code>,然后转发<code>action:to_login_out</code></p>\\n</blockquote>\\n<p><strong>完整的实现登入登出和列表展示的代码</strong></p>\\n<pre class=\\"language-text\\"><code>function * getList(){\\n  try {\\n   yield delay(3000);\\n   const res = yield call(fetchSmart,&#39;/list&#39;,{\\n     method:&#39;POST&#39;,\\n     body:JSON.stringify({})\\n   });\\n   yield put({type:&#39;update_list&#39;,list:res.data.activityList});\\n } catch(error) {\\n   yield put({type:&#39;update_list_error&#39;, error});\\n }\\n}\\n\\nfunction * watchIsLogin(){\\n  while(true){\\n    //监听登入事件\\n    const action1=yield take(&#39;TO_LOGIN_IN&#39;);\\n    \\n    const res=yield call(fetchSmart,&#39;/login&#39;,{\\n      method:&#39;POST&#39;,\\n      body:JSON.stringify({\\n        username:action1.username,\\n        password:action1.password\\n      })\\n    });\\n    \\n    //根据返回的状态码判断登陆是否成功\\n    if(res.status===10000){\\n      yield put({type:&#39;to_login_in&#39;});\\n      //登陆成功后获取首页的活动列表\\n      yield call(getList);\\n    }\\n    \\n    //监听登出事件\\n    const action2=yield take(&#39;TO_LOGIN_OUT&#39;);\\n    yield put({type:&#39;to_login_out&#39;});\\n  }\\n}\\n</code></pre>\\n<blockquote>\\n<p>通过请求状态码判断登入是否成功，在登陆成功后，可以通过</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>yield call(getList)\\n</code></pre>\\n<blockquote>\\n<p>注意call方法调用是会阻塞主线程的，具体来说</p>\\n</blockquote>\\n<ul>\\n<li>在call方法调用结束之前，call方法之后的语句是无法执行的</li>\\n<li>如果<code>call(getList)</code>存在延迟，<code>call(getList)</code>之后的语句 <code>const action2=yieldtake(\'TO_LOGIN_OUT\')</code>在<code>call</code>方法返回结果之前无法执行</li>\\n<li>在延迟期间的登出操作会被忽略</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/gitee/2019/10/491.png\\" alt=\\"img\\"></p>\\n<p><strong>无阻塞调用</strong></p>\\n<pre class=\\"language-text\\"><code>yield call(getList)\\n</code></pre>\\n<p>修改为</p>\\n<pre class=\\"language-text\\"><code>yield fork(getList)\\n</code></pre>\\n<blockquote>\\n<p>通过fork方法不会阻塞主线程，在白屏时点击登出，可以立刻响应登出功能，从而返回登陆页面</p>\\n</blockquote>\\n<h2 id=\\"六、案例分析二\\"><a class=\\"header-anchor\\" href=\\"#六、案例分析二\\">#</a> 六、案例分析二</h2>\\n<h3 id=\\"_6-1-配置saga信息\\"><a class=\\"header-anchor\\" href=\\"#_6-1-配置saga信息\\">#</a> 6.1 配置saga信息</h3>\\n<blockquote>\\n<pre class=\\"language-text\\"><code>src/store/configureStore.js\\n</code></pre>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>import { createStore, applyMiddleware, compose } from &#39;redux&#39;\\n// import {createLogger } from &#39;redux-logger&#39;\\nimport createHistory from &#39;history/createBrowserHistory&#39;\\nimport createSagaMiddleware from &#39;redux-saga&#39;;\\nimport { routerMiddleware } from &#39;react-router-redux&#39;\\nimport rootSaga from &#39;../sagas&#39;\\nimport rootReducer from &#39;../reducers/&#39;\\n\\nexport const history = createHistory()\\n\\nconst middleware = routerMiddleware(history)\\n\\n//创建saga middleware\\nconst sagaMiddleware = createSagaMiddleware();\\n\\n\\nconst configureStore = preloadedState =&gt; {\\n\\t// 安装 Redux-DevTools Chrome 插件后可用 composeEnhancers()\\n\\tconst composeEnhancers = window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ || compose\\n\\n\\tconst store = createStore(\\n\\t\\trootReducer,\\n\\t\\tpreloadedState,\\n\\t\\tcomposeEnhancers(\\n\\t\\t\\tapplyMiddleware(sagaMiddleware,middleware)\\n\\t\\t)\\n\\t)\\n\\tsagaMiddleware.run(rootSaga);\\n\\tif (module.hot) {\\n\\t\\t// Enable Webpack hot module replacement for reducers\\n\\t\\tmodule.hot.accept(&#39;../reducers&#39;, () =&gt; {\\n\\t\\t\\tconst nextRootReducer = require(&#39;../reducers&#39;).default\\n\\t\\t\\tstore.replaceReducer(nextRootReducer)\\n\\t\\t})\\n\\t}\\n\\n\\treturn store\\n}\\n\\n\\nexport default configureStore\\n</code></pre>\\n<h3 id=\\"_6-2-配置reduce\\"><a class=\\"header-anchor\\" href=\\"#_6-2-配置reduce\\">#</a> 6.2 配置reduce</h3>\\n<pre class=\\"language-text\\"><code>// src/reducers/index.js\\nimport {combineReducers} from &#39;redux&#39;\\nimport {routerReducer as routing} from &#39;react-router-redux&#39;\\n\\nconst rootReducer = combineReducers({\\n      routing,\\n      poetry \\t\\t\\t\\t: require(&#39;./poetry&#39;).default\\n})\\n\\nexport default rootReducer\\n// src/reducers/poetry.js\\n\\nimport * as ActionTypes from &#39;../actions&#39;\\n\\nexport default (state = {\\n\\tfetching:false,\\n\\terror:false,\\n\\terrMsg:&#39;&#39;,\\n\\tdata:[]\\n},action) =&gt; {\\n\\tif(action.type === ActionTypes.FETCH_POETRY_REQUEST){\\n\\t\\treturn Object.assign({...state,fetching:true,errMsg:&#39;&#39;})\\n\\t}else if(action.type === ActionTypes.FETCH_POETRY_SUCCESS){\\n\\t\\tconst data = action.payload.data\\n\\t\\treturn Object.assign({...state,fetching:false,data,errMsg:&#39;&#39;})\\n\\t}else if(action.type === ActionTypes.FETCH_POETRY_FAILURE){\\n\\t\\treturn Object.assign({...state,fetching:false,error:true,errMsg:action.payload.errMsg})\\n\\t}\\n\\treturn state\\n}\\n</code></pre>\\n<h3 id=\\"_6-3-处理action\\"><a class=\\"header-anchor\\" href=\\"#_6-3-处理action\\">#</a> 6.3 处理action</h3>\\n<pre class=\\"language-text\\"><code>// src/action/index.js\\n\\nimport { createAction } from &#39;redux-actions&#39;;\\n\\nexport const COMMON_FETCHING = &#39;COMMON_FETCHING&#39;\\nexport const COMMON_OVER = &#39;COMMON_OVER&#39;\\nexport const MSG_SHOW = &#39;MSG_SHOW&#39;\\nexport const MSG_INIT = &#39;MSG_INIT&#39;\\nexport const POP_LOGIN = &#39;POP_LOGIN&#39;\\nexport const initMsg = () =&gt; ({type : MSG_INIT})\\n\\n\\n// 相比用thunk多了一步 多了个action 来触发saga woker\\nexport const FETCH_POETRY_REQUEST = &#39;FETCH_POETRY_REQUEST&#39;\\nexport const FETCH_POETRY_SUCCESS = &#39;FETCH_POETRY_SUCCESS&#39;\\nexport const FETCH_POETRY_FAILURE = &#39;FETCH_POETRY_FAILURE&#39;\\nexport const fetchPoetryRequest = createAction(FETCH_POETRY_REQUEST)\\nexport const fetchPoetrySuccess = createAction(FETCH_POETRY_SUCCESS)\\nexport const fetchPoetryFauilure= createAction(FETCH_POETRY_FAILURE)\\n</code></pre>\\n<h3 id=\\"_6-4-处理sagas\\"><a class=\\"header-anchor\\" href=\\"#_6-4-处理sagas\\">#</a> 6.4 处理sagas</h3>\\n<pre class=\\"language-text\\"><code>// src/sagas/index.js\\n\\nimport { all } from &#39;redux-saga/effects&#39;\\n\\nexport default function* rootSaga() {\\n    yield all([\\n        ...require(&#39;./fetchPoetry&#39;).default\\n    ])\\n  }\\n// src/fetchPoetry.js\\n\\nimport {put,take,call,fork,takeEvery,select} from &#39;redux-saga/effects&#39;\\nimport {delay} from &#39;redux-saga&#39;\\nimport  * as api  from &#39;../api&#39;\\nimport * as actionTypes from &#39;../actions/&#39;\\n\\n// saga worker 监听FETCH_POETRY_REQUEST动作触发执行相应操作\\nfunction* fetchPoetrySaga(){\\n    // yield delay(100)\\n    // ======== 写法一 ========= \\n    // yield takeEvery(actionTypes.FETCH_POETRY_REQUEST,function*(action){\\n    //     // 调用this.props.fetchPoetryRequest({user:&#39;poetries&#39;,age:23}) 传参进来这里\\n    //     // 也可以通过这样获取state中的参数 const state = yield select()\\n    //     const {user,age} = action\\n    //     try{\\n    //         const data =  yield call(api.get({\\n    //             url:&#39;/mock/5b7fd63f719c7b7241f4e2fa/tangshi/tang-shi&#39;\\n    //         }))\\n    //         yield put(actionTypes.fetchPoetrySuccess({data:data.data.data}))\\n    //     }catch(error){\\n    //         yield put(actionTypes.fetchPoetryFauilure({errMsg:error.message}))\\n    //     }\\n     \\n    // })\\n    // === 写法二====\\n  while(true){\\n      // 当dispatch({type:FETCH_POETRY_REQUEST})的时候被这里监听 执行对应的请求\\n    const {user,age} =  yield take(actionTypes.FETCH_POETRY_REQUEST)\\n    try{\\n         const data =  yield call(api.get({\\n             url:&#39;/mock/5b7fd63f719c7b7241f4e2fa/tangshi/tang-shi&#39;\\n         }))\\n          yield put(actionTypes.fetchPoetrySuccess({data:data.data.data}))\\n     }catch(error){\\n         yield put(actionTypes.fetchPoetryFauilure({errMsg:error.message}))\\n     }\\n  }\\n\\n} \\n\\n\\n// 导出所有的saga\\nexport default  [\\n    fork(fetchPoetrySaga)\\n]\\n</code></pre>\\n<blockquote>\\n<p>完整代码例子 <a href=\\"https://github.com/poetries/redux-saga-template\\">https://github.com/poetries/redux-saga-template</a></p>\\n</blockquote>\\n<h2 id=\\"七、总结\\"><a class=\\"header-anchor\\" href=\\"#七、总结\\">#</a> 七、总结</h2>\\n<blockquote>\\n<p><code>redux-saga</code>做为<code>redux</code>中间件的全部优点</p>\\n</blockquote>\\n<ul>\\n<li>统一<code>action</code>的形式，在<code>redux-saga</code>中，从<code>UI</code>中<code>dispatch</code>的<code>action</code>为原始对象</li>\\n<li>集中处理异步等存在副作用的逻辑</li>\\n<li>通过转化<code>effects</code>函数，可以方便进行单元测试</li>\\n<li>完善和严谨的流程控制，可以较为清晰的控制复杂的逻辑</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>订单页把请求逻辑从组件迁到 <code>redux-saga</code> 后，评审同事说它只是把 <code>Promise</code> 换成 <code>Generator</code>，没有工程收益，你怎么反驳？</strong></p>\\n<p>收益不在语法替换，而在于把异步和其他副作用从 <code>UI</code> 集中到中间件流程中，组件只需 <code>dispatch</code> 原始对象形式的 <code>action</code>。如果流程很简单，引入额外抽象未必划算，不能仅凭使用了 <code>Generator</code> 就判断设计更好。</p>\\n</li>\\n<li>\\n<p><strong>支付页要串联下单、查询结果和刷新订单三个步骤，你会怎样划分组件、<code>action</code> 与 <code>saga</code> 的职责？</strong></p>\\n<p>组件负责表达用户意图并派发原始对象 <code>action</code>，<code>saga</code> 集中编排异步步骤和流程分支，再通过后续 <code>action</code> 更新状态。这样复杂控制逻辑不散落在 <code>UI</code> 中，但仍需明确每个步骤的输入、输出及失败路径，避免形成难以维护的巨型流程。</p>\\n</li>\\n<li>\\n<p><strong>搜索页原本每次输入只发一个请求，现在产品要求处理连续输入、并发结果和流程终止，你为什么更倾向把控制逻辑留在 <code>redux-saga</code>？</strong></p>\\n<p>需求变化后，重点已从单次异步调用变成多步骤流程控制，<code>redux-saga</code> 更适合集中表达这些副作用及其先后关系。具体并发与终止策略仍要显式设计；中间件只提供编排位置，不会自动保证旧结果一定不覆盖新结果。</p>\\n</li>\\n<li>\\n<p><strong>线上用户点击提交后页面无更新，组件确认已经 <code>dispatch</code> 了原始对象 <code>action</code>，你会怎样判断故障在 <code>saga</code> 流程还是状态更新链路？</strong></p>\\n<p>先记录派发的 <code>action</code>，确认对应 <code>saga</code> 是否接收并执行预期 <code>effect</code>，再检查后续成功或失败 <code>action</code> 是否真正进入状态更新链路。若流程停在某个分支，应缩小到该 <code>effect</code> 的输入和返回值；只看到首次派发不能证明整个副作用流程完成。</p>\\n</li>\\n<li>\\n<p><strong>团队在 <code>redux-thunk</code> 与 <code>redux-saga</code> 之间争论：一个资料编辑页只有两次独立请求，但结算页有多步依赖流程，你会统一选型吗？</strong></p>\\n<p>不会仅为技术栈统一而强制使用同一种复杂度。独立且简单的异步逻辑未必需要完整流程编排，而多步依赖、分支清晰度和可测试性要求较高时，<code>redux-saga</code> 的集中处理与 <code>effect</code> 转换更有价值；代价是团队要维护额外抽象。</p>\\n</li>\\n<li>\\n<p><strong>测试同学要求结算流程不发真实请求也能验证每一步，你会利用 <code>redux-saga</code> 的哪一层设计测试？</strong></p>\\n<p>应围绕流程产出的 <code>effect</code> 描述验证分支和执行顺序，而不是让单元测试直接依赖真实副作用。<code>effect</code> 可被转换和检查，使复杂流程更容易隔离测试；但这主要证明编排逻辑，真实接口、状态链路仍需其他层级的测试覆盖。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);