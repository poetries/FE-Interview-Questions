(window.webpackJsonp=window.webpackJsonp||[]).push([[29],{1135:function(n){n.exports=JSON.parse('{"html":"<h2 id=\\"前置知识-完全二叉树\\"><a class=\\"header-anchor\\" href=\\"#前置知识-完全二叉树\\">#</a> 前置知识：完全二叉树</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>完全二叉树除最后一层外都填满，最后一层从左到右连续填充，因此适合用数组紧凑存储</li>\\n<li>零基数组中父结点是 <code>Math.floor((i - 1) / 2)</code>，左右孩子是 <code>2i + 1</code>、<code>2i + 2</code></li>\\n<li>完全性是形状约束，不规定父子值大小；堆还要额外满足堆序性质</li>\\n</ul>\\n<p><strong>完全二叉树除最后一层外都必须填满，最后一层的结点还要从左到右连续排列。</strong> 正因为形状紧凑，它可以按从上到下、从左到右的顺序存进数组，不需要额外保存连接关系。零基数组中，索引 <code>i</code> 的父结点是 <code>Math.floor((i - 1) / 2)</code>，左右孩子分别是 <code>2 * i + 1</code> 和 <code>2 * i + 2</code>。这里约束的是树的形状，并没有规定父子结点值的大小。</p>\\n</section>\\n<blockquote>\\n<p>完全二叉树是指同时满足下面两个条件的二叉树：</p>\\n</blockquote>\\n<ul>\\n<li>从第一层到倒数第二层，每一层都是满的，也就是说每一层的结点数都达到了当前层所能达到的最大值</li>\\n<li>最后一层的结点是从左到右连续排列的，不存在跳跃排列的情况（也就是说这一层的所有结点都集中排列在最左边）。</li>\\n</ul>\\n<p>完全二叉树可以是这样的：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123548.png\\" alt=\\"\\"></p>\\n<p>也可以是这样的：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123557.png\\" alt=\\"\\"></p>\\n<p>但不能是这样的：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123603.png\\" alt=\\"\\"></p>\\n<p>更不能是这样的：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123609.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>注意，完全二叉树中有着这样的索引规律：假如我们从左到右、从上到下依次对完全二叉树中的结点从0开始进行编码：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123616.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>那么对于索引为 n 的结点来说：</p>\\n</blockquote>\\n<ul>\\n<li>索引为 <code>(n-1)/2</code> 的结点是它的父结点</li>\\n<li>索引 <code>2*n+1</code> 的结点是它的左孩子结点</li>\\n<li>索为引 <code>2*n+2</code> 的结点是它的右孩子结点</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>可视化页面中一棵树除最后一层外都满，但最后一层节点出现在最右侧、左侧留空，设计师称它仍是完全二叉树，你怎么反驳？</strong></p>\\n<p>它不是完全二叉树，因为最后一层必须从左到右连续占位，不能在左侧留下空洞后又出现节点。仅满足前面各层为满层还不够；这个反例会破坏按层连续存入数组时的紧凑索引关系。</p>\\n</li>\\n<li>\\n<p><strong>堆组件用零基数组保存节点，代码评审要求你写出索引 <code>i</code> 的父节点和两个孩子位置，并说明边界怎么处理？</strong></p>\\n<p>左孩子是 <code>2*i+1</code>，右孩子是 <code>2*i+2</code>，非根节点的父节点是 <code>Math.floor((i-1)/2)</code>。计算出的孩子下标必须小于数组长度才真实存在，根节点没有父节点；公式成立依赖节点按层且从左到右连续存放。</p>\\n</li>\\n<li>\\n<p><strong>批量建堆时同事从数组末尾每个节点都执行下沉，你会把起点改到哪里，依据是什么？</strong></p>\\n<p>应从最后一个非叶节点 <code>Math.floor(n/2)-1</code> 开始向前下沉，因为零基数组中其后的节点都没有孩子。叶节点天然满足局部堆约束，无需处理；空数组或单元素数组得到负起点时应直接结束。</p>\\n</li>\\n<li>\\n<p><strong>序列化模块收到普通二叉树，需要判断它能否紧凑编码为完全二叉树；日志显示某层出现空位后，后续又读到非空节点，你如何判定？</strong></p>\\n<p>应立即判为非完全二叉树，因为层序扫描一旦遇到首个空孩子，后续位置只能继续为空。实现时可设置“已进入空缺区”标记，之后发现非空节点即失败；若随意跳过空位，就会掩盖中间空洞。</p>\\n</li>\\n<li>\\n<p><strong>存储组选型时，一方要用数组保存任意稀疏二叉树，另一方只接受完全二叉树，你会怎样解释空间取舍？</strong></p>\\n<p>完全二叉树按层连续排列，数组下标即可定位父子，不需要额外指针，也不会因结构产生大量空槽。任意稀疏树若强行沿用同样的位置编码，深层少量节点可能对应很大的下标；此时显式节点引用通常更合适。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"什么是堆\\"><a class=\\"header-anchor\\" href=\\"#什么是堆\\">#</a> 什么是堆</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>堆是完全二叉树加父子偏序：最大堆只保证父节点不小于孩子，不保证整棵树有序。</li>\\n<li>数组下标关系：父 <code>(i-1)&gt;&gt;1</code>，左子 <code>2i+1</code>，右子 <code>2i+2</code>。</li>\\n<li>读堆顶 <code>O(1)</code>，插入与删除堆顶 <code>O(log n)</code>，查任意值仍是 <code>O(n)</code>。</li>\\n<li>连续数组存储不需要节点指针，缓存局部性好。</li>\\n</ul>\\n<p><strong>堆是一棵满足父子大小约束的完全二叉树，分为大顶堆和小顶堆。</strong> 大顶堆要求父节点不小于孩子，小顶堆正好相反，但这不代表同层节点或整棵树全局有序。用数组存储时，父节点下标是 <code>(i-1)&gt;&gt;1</code>，左右孩子分别是 <code>2i+1</code> 和 <code>2i+2</code>。因此读取堆顶是 <code>O(1)</code>，插入和删除堆顶是 <code>O(log n)</code>，查找任意值仍可能需要 <code>O(n)</code>。</p>\\n</section>\\n<p>堆是完全二叉树的一种特例。根据约束规则的不同，堆又分为两种：</p>\\n<ul>\\n<li>大顶堆</li>\\n<li>小顶堆</li>\\n</ul>\\n<blockquote>\\n<p>如果对一棵完全二叉树来说，它每个结点的结点值都不小于其左右孩子的结点值，这样的完全二叉树就叫做“大顶堆”：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123623.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>若树中每个结点值都不大于其左右孩子的结点值，这样的完全二叉树就叫做“小顶堆”</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123629.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>排行榜页面拿到数组 <code>[9, 8, 6, 3, 1]</code>，同事看到父节点都更大，就断言任意中序遍历也会降序，你会给出什么判断？</strong></p>\\n<p>这个断言不成立，大顶堆只保证每个父节点不小于直接孩子，不规定左子树与右子树之间的全序关系。中序遍历次序由树形位置决定，因此不能当作排序结果；要获得全序仍需持续取堆顶并恢复堆。</p>\\n</li>\\n<li>\\n<p><strong>任务调度器只关心下一项最高优先级，为什么选择大顶堆而不是维护一个普通无序数组？</strong></p>\\n<p>大顶堆把当前最大值稳定放在根部，读取下一项时可直接访问堆顶，新增或删除后再沿父子路径恢复约束。无序数组插入简单，但选最大项需要扫描；堆的代价是不能保持全部元素的有序展示。</p>\\n</li>\\n<li>\\n<p><strong>产品把“每个父节点都不小于孩子”的规则应用到一棵缺口很多的普通树，并称其为大顶堆，你是否认可？</strong></p>\\n<p>不认可，堆除了父子大小约束，还要求底层结构是完全二叉树。普通树即使局部大小关系全部成立，也不具备堆的紧凑层序布局；缺口会使基于数组下标计算父子的实现失去前提。</p>\\n</li>\\n<li>\\n<p><strong>搜索接口要在百万级大顶堆中按 <code>id</code> 定位任意任务，开发声称能像二叉搜索树一样每次排除一半分支，你会如何纠正？</strong></p>\\n<p>堆序只能说明父节点与孩子的相对大小，左右分支之间没有可用于二分搜索的全序关系。按任意 <code>id</code> 或普通目标值查找时，最坏仍可能检查大量节点；若定位是核心需求，应增加映射索引或改用更匹配的结构。</p>\\n</li>\\n<li>\\n<p><strong>告警显示大顶堆根节点不是全局最大值，但抽查部分父子关系正常，你会优先验证哪些不变量？</strong></p>\\n<p>应遍历所有有效父节点，检查其值是否都不小于存在的左右孩子，同时确认数组对应的是无空洞的完全二叉树。只抽查一条路径不足以证明堆成立；还要追踪最近的插入或删除是否漏做了上浮、下沉或边界更新。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"堆的基本操作-以大顶堆为例\\"><a class=\\"header-anchor\\" href=\\"#堆的基本操作-以大顶堆为例\\">#</a> 堆的基本操作：以大顶堆为例</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>大顶堆只保证每个父结点不小于孩子，不保证同层或整个数组全局有序</li>\\n<li>插入先放数组末尾再上浮；删除堆顶用末尾元素补根后下沉，两者时间都是 <code>O(log n)</code></li>\\n<li>下沉必须在左右孩子中选择更大的一个交换，否则可能修好一侧却仍违反另一侧堆序</li>\\n</ul>\\n<p><strong>大顶堆的核心操作是删除堆顶和追加元素，两者本质上都是在恢复父节点不小于孩子的约束。</strong> 插入时把新值放到数组末尾，再沿祖先链向上比较和交换；删除时用末尾元素补到根节点，再向下调整。下沉时要从左右孩子中选择更大的那个，否则交换后另一侧仍可能不满足堆序。两种调整最多走一层树高，所以时间复杂度都是 <code>O(log n)</code>。</p>\\n</section>\\n<blockquote>\\n<p>大顶堆和小顶堆除了约束条件中的大小关系规则完全相反以外，其它方面都保持高度一致。现在我们以大顶堆为例，一起来看看堆结构有哪些玩法。</p>\\n</blockquote>\\n<p>这里我给出一个现成的大顶堆：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123635.png\\" alt=\\"\\"></p>\\n<p>很多时候，为了考察你对完全二叉树索引规律的掌握情况，题目中与堆结构同时出现的，还有它的层序遍历序列：</p>\\n<pre class=\\"language-text\\"><code>[9, 8, 6, 3, 1]\\n</code></pre>\\n<p><strong>我们需要关注的动作有两个：</strong></p>\\n<ul>\\n<li>如何取出堆顶元素（删除操作）</li>\\n<li>往堆里追加一个元素（插入操作）</li>\\n</ul>\\n<blockquote>\\n<p>至于堆的初始化，也只不过是从空堆开始，重复执行动作2而已。因此，上面这两个动作就是堆操作的核心。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>优先队列删除大顶堆的根后，代码直接把数组首项移除并让其余元素左移，页面随即读到错误堆顶，问题出在哪？</strong></p>\\n<p>直接左移会改变几乎所有节点的父子位置，并且不会自动恢复大顶堆约束。通常应把末尾元素移到堆顶、缩短数组，再从根向下与较大的孩子交换；下沉必须持续到父子关系重新满足或到达叶子。</p>\\n</li>\\n<li>\\n<p><strong>实时任务进入大顶堆时，新元素被追加到数组末尾，但高优先级任务一直没有升到堆顶，你会怎样补全插入流程？</strong></p>\\n<p>追加到末尾只维持了完全二叉树形状，还需要将新节点与父节点比较并持续上浮。只要它大于父节点就交换，直到不再违反大顶堆约束或到达根；比较方向写反会构造出小顶堆或局部失序。</p>\\n</li>\\n<li>\\n<p><strong>初始化十万级任务堆时，工程师从空数组逐个插入，另一位建议直接对完整数组自底向下下沉，你怎么取舍？</strong></p>\\n<p>逐个插入语义直观，但每个新节点都可能沿高度上浮，整体上界会达到 <code>O(n log n)</code>。已有完整数组时，从最后一个非叶节点开始依次下沉可做到 <code>O(n)</code> 建堆；若数据本来就是流式到达，则逐项插入仍是自然方案。</p>\\n</li>\\n<li>\\n<p><strong>线上删除堆顶后偶发父节点小于右孩子，排查发现下沉时总是优先和左孩子交换，你会如何修复？</strong></p>\\n<p>大顶堆下沉时应在存在的孩子中选择值更大的一个，再判断是否需要与父节点交换。固定选左孩子可能让父节点交换后仍小于右孩子；还要单独处理只有左孩子的尾部节点，避免越界读取。</p>\\n</li>\\n<li>\\n<p><strong>搜索团队要求大顶堆同时支持任意任务按值 <code>O(log n)</code> 删除，但不愿维护额外索引，你会接受这个指标吗？</strong></p>\\n<p>不能仅凭堆结构保证该指标，因为找到任意目标最坏需要 <code>O(n)</code>，只有定位后的上浮或下沉通常受树高限制。若任务具有唯一 <code>id</code>，可维护 <code>id</code> 到数组下标的映射，并在交换时同步更新；代价是额外空间和一致性维护。</p>\\n</li>\\n<li>\\n<p><strong>排序模块想复用大顶堆，每次取出堆顶后把最大值放到数组末尾，这和普通优先队列删除有什么衔接？</strong></p>\\n<p>两者核心动作相同：交换堆顶与当前有效区末尾，缩小堆的有效范围，再对新根执行下沉。堆排序把移出的最大值保留在数组尾部形成有序区，而优先队列通常直接删除它；实现时必须区分有效堆长度与数组总长度。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"取出堆顶元素\\"><a class=\\"header-anchor\\" href=\\"#取出堆顶元素\\">#</a> 取出堆顶元素</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先保存堆顶，再把数组末元素移到根并缩短堆大小</li>\\n<li>下沉时先比较两个孩子，最大堆必须选择更大的孩子作为交换候选</li>\\n<li>当前值已不小于候选孩子时立即停止，父子偏序已经恢复</li>\\n<li>单元素堆移除后直接为空；一般情况时间 <code>O(log n)</code>、额外空间 <code>O(1)</code></li>\\n</ul>\\n<p><strong>取出大顶堆的堆顶时，要先保存根节点，再用数组末尾元素补到根部并向下调整。</strong> 下沉过程中先比较左右孩子，选择值更大的孩子与当前节点比较，因为交换后必须同时满足两侧的父子约束。当前节点已经不小于这个孩子时就可以停止，说明堆序已经恢复。单元素堆删除后直接变为空堆；一般情况下耗时 <code>O(log n)</code>，额外空间为 <code>O(1)</code>。</p>\\n</section>\\n<blockquote>\\n<p>取出元素本身并不难，难的是如何在删除元素的同时，保持住队的“大顶”结构特性。为了做到这点，我们需要执行以下操作：</p>\\n</blockquote>\\n<ul>\\n<li>用堆里的最后一个元素（对应图中的数字1）替换掉堆顶元素。</li>\\n<li>对比新的堆顶元素（1）与其左右孩子的值，如果其中一个孩子大于堆顶元素，则交换两者的位置：</li>\\n</ul>\\n<p><img src=\\"https://s.poetries.top/images/20210905123643.png\\" alt=\\"\\"></p>\\n<p>交换后，继续向下对比1与当前左右孩子的值，如果其中一个大于1，则交换两者的位置：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123648.png\\" alt=\\"\\"></p>\\n<p>重复这个向下对比+交换的过程，直到无法继续交换为止，我们就得到了一个符合“大顶”原则的新的堆结构：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123648.png\\" alt=\\"\\"></p>\\n<p>上述这个反复向下对比+交换的过程，用编码实现如下（仔细看注释）：</p>\\n<pre class=\\"language-js\\"><code>// 入参是堆元素在数组里的索引范围，low表示下界，high表示上界\\nfunction downHeap(low, high) {\\n    // 初始化 i 为当前结点，j 为当前结点的左孩子\\n    let i=low,j=i*2+1 \\n    // 当 j 不超过上界时，重复向下对比+交换的操作\\n    while(j &lt;= high) {\\n        // 如果右孩子比左孩子更大，则用右孩子和根结点比较\\n        if(j+1 &lt;= high &amp;&amp; heap[j+1] &gt; heap[j]) {\\n            j = j+1\\n        }\\n        \\n        // 若当前结点比孩子结点小，则交换两者的位置，把较大的结点“拱上去”\\n        if(heap[i] &lt; heap[j]) {\\n            // 交换位置\\n            const temp = heap[j]  \\n            heap[j] = heap[i]  \\n            heap[i] = temp\\n            \\n            // i 更新为被交换的孩子结点的索引\\n            i=j  \\n            // j 更新为孩子结点的左孩子的索引\\n            j=j*2+1\\n        } else {\\n            break\\n        }\\n    }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>页面上的大顶堆弹出后，新堆顶 <code>1</code> 的左孩子是 <code>8</code>、右孩子是 <code>9</code>；同事建议见到左孩子更大就立刻交换，你会指出什么反例？</strong></p>\\n<p>不能先遇到左孩子更大就交换，必须先从现存孩子中选出较大的一个，再与当前结点比较。若先把 <code>1</code> 与 <code>8</code> 交换，右侧的 <code>9</code> 仍大于新堆顶，会立即破坏大顶堆性质；代码因此先比较 <code>heap[j+1]</code> 与 <code>heap[j]</code>。</p>\\n</li>\\n<li>\\n<p><strong>你在优先队列组件里实现 <code>popMax</code>，数组首项要返回给调用方；替换堆顶、收缩有效区间和调用 <code>downHeap</code> 的顺序应怎样安排？</strong></p>\\n<p>先保存 <code>heap[0]</code>，再用最后一个有效元素覆盖堆顶并收缩数组或有效上界，随后仅在堆仍非空时调用 <code>downHeap(0, high)</code>。这样下沉不会访问已移除元素；若先下沉再收缩，尾部旧值可能被重新参与比较，造成结果错误。</p>\\n</li>\\n<li>\\n<p><strong>任务调度页允许堆中只剩一个元素，或下沉节点只有左孩子；现有 <code>downHeap(low, high)</code> 需要满足哪些边界约束？</strong></p>\\n<p>单元素弹出后应直接返回原堆顶，不再调用下沉；只有左孩子时，<code>j &lt;= high</code> 仍允许进入循环。右孩子必须通过 <code>j+1 &lt;= high</code> 检查后才能参与比较，否则会读取越界值；这里的 <code>high</code> 是包含端点的有效上界，调用方必须保持一致。</p>\\n</li>\\n<li>\\n<p><strong>线上告警显示弹出最大值后，数组根节点偶尔小于右孩子，但左子树仍正常；你会怎样定位 <code>downHeap</code> 的故障？</strong></p>\\n<p>优先检查选择孩子时是否遗漏 <code>j+1 &lt;= high</code>，以及右孩子更大时是否执行了 <code>j = j+1</code>。再确认交换后把 <code>i</code> 更新到被换下去的位置，并重新计算 <code>j = j*2+1</code>；若仍异常，应验证传入的 <code>high</code> 是否已排除刚弹出的尾元素。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中有人想把每次弹出最大值改为删除数组第 <code>0</code> 项后整体重建堆，另一方坚持“尾元素补顶再下沉”；你会支持哪一种？</strong></p>\\n<p>应支持用最后一个有效元素补到堆顶，再沿较大孩子方向执行 <code>downHeap</code>，因为删除只破坏根到某个叶子的局部路径。直接删除数组第 <code>0</code> 项会搬移后续元素，并使原有父子索引关系整体变化，随后还要重建堆；前者更贴合数组堆结构，但边界处理更容易出错。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"往堆里追加一个元素\\"><a class=\\"header-anchor\\" href=\\"#往堆里追加一个元素\\">#</a> 往堆里追加一个元素</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>为保持完全二叉树形状，新值只能追加到数组末尾</li>\\n<li>父下标为 <code>(i - 1) &gt;&gt; 1</code>，违反最大堆偏序时交换并继续上浮</li>\\n<li>遇到不小于当前值的父节点立即停止，不需要检查其他分支</li>\\n<li>最坏上浮到根为 <code>O(log n)</code>，无需交换时最好 <code>O(1)</code></li>\\n</ul>\\n<p><strong>往大顶堆追加元素时，新值先放到数组末尾，再沿父节点方向不断上浮。</strong> 完全二叉树决定了新节点只能追加在末尾，而新增操作只可能破坏它到根节点这一条祖先链。当前下标为 <code>i</code> 时，父下标是 <code>(i-1)&gt;&gt;1</code>；只要新值大于父节点就交换，否则立即停止。最坏情况下会上浮到根节点，耗时 <code>O(log n)</code>；如果一开始就满足堆序，则是 <code>O(1)</code>。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“完全二叉树决定新节点只能放末尾；新增只可能破坏它到根这一条祖先链，所以沿父下标上浮即可。”</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品榜单页的最大堆已有多个相同热度值，新商品追加后与父节点相等，代码仍继续交换到根部，这会带来什么问题？</strong></p>\\n<p>相等时通常应停止上浮，因为父子关系已经满足最大堆约束，继续交换不会改善堆序。这样能减少无意义的写入和索引更新；但堆不保证稳定性，不能借此承诺相同热度商品保持原有顺序。</p>\\n</li>\\n<li>\\n<p><strong>实时调度页面每秒追加任务，数组实现的最大堆应怎样写插入逻辑，才能避免扫描整棵树？</strong></p>\\n<p>先把任务追加到数组末尾，再从该下标沿父节点方向逐级比较并交换，直到父节点优先级不低于它。完全二叉树决定新增节点只能位于末尾，新增操作也只可能破坏这条祖先链；若堆高增长，单次上浮成本随高度增加。</p>\\n</li>\\n<li>\\n<p><strong>前端内存受限，只保留最新 <code>k</code> 条告警的最小堆；当堆已满又收到一条新告警时，还能直接执行普通追加吗？</strong></p>\\n<p>不能无条件追加，否则堆容量会突破 <code>k</code>。新告警不大于堆顶时可直接丢弃，大于堆顶时用它替换堆顶并向下调整；这种处理适合筛选较大的 <code>k</code> 项，但会主动舍弃未入选数据。</p>\\n</li>\\n<li>\\n<p><strong>线上告警队列偶发出现子节点优先级高于父节点，日志显示异常总发生在追加后，你会先检查哪些下标计算和终止条件？</strong></p>\\n<p>先核对新元素是否确实写入数组末尾，再检查父下标公式是否与数组从 <code>0</code> 还是从 <code>1</code> 开始一致。随后验证每次交换后当前下标是否更新，以及到根节点或满足堆序时是否停止；任一处混用都可能让祖先链残留逆序。</p>\\n</li>\\n<li>\\n<p><strong>任务中心既要追加新任务，又要修改任意旧任务的优先级，团队在“线性查找后调整”和“额外维护下标映射”之间如何取舍？</strong></p>\\n<p>更新频繁且任务量较大时，应维护任务标识到堆下标的映射，修改后根据与父节点的关系选择上浮，否则尝试下沉。映射必须在每次交换时同步更新；若修改极少，线性查找更简单，但定位成本会随堆规模增长。</p>\\n</li>\\n</ul>\\n</section>\\n<p>当添加一个新元素进堆的时候，我们同样需要考虑堆结构的排序原则：</p>\\n<ol>\\n<li>新来的数据首先要追加到当前堆里最后一个元素的后面。比如我现在要新增一个10，它就应该排在最后一层的最后一个位置：</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/images/20210905123704.png\\" alt=\\"\\"></p>\\n<ol start=\\"2\\">\\n<li>不断进行向上对比+交换的操作：如果发现10比父结点的结点值要大，那么就和父结点的元素相互交换，再接着往上进行比较，直到无法再继续交换为止。首先被比下去的是值为6的直接父结点：</li>\\n</ol>\\n<p><img src=\\"https://s.poetries.top/images/20210905123711.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>接着继续往上找，发现10比根结点9还要大，于是继续进行交换：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905123716.png\\" alt=\\"\\"></p>\\n<p>根结点被换掉后，再也无法向上比较了。此时，我们已经得到了一个追加过数字10的新的堆结构：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905123724.png\\" alt=\\"\\"></p>\\n<p>上述这个反复向上对比+交换的过程，用编码实现如下（仔细看注释）：</p>\\n<pre class=\\"language-js\\"><code>// 入参是堆元素在数组里的索引范围，low表示下界，high表示上界\\nfunction upHeap(low, high) {\\n    // 初始化 i（当前结点索引）为上界\\n    let i = high  \\n    // 初始化 j 为 i 的父结点\\n    let j = Math.floor((i-1)/2)  \\n    // 当 j 不逾越下界时，重复向上对比+交换的过程\\n    while(j&gt;=low)  {\\n        // 若当前结点比父结点大\\n        if(heap[j]&lt;heap[i]) {\\n            // 交换当前结点与父结点，保持父结点是较大的一个\\n            const temp = heap[j] \\n            heap[j] = heap[i]  \\n            heap[i] = temp\\n            \\n            // i更新为被交换父结点的位置\\n            i=j   \\n            // j更新为父结点的父结点\\n            j=Math.floor((i-1)/2)  \\n        } else {\\n            break\\n        }\\n    }\\n}\\n</code></pre>\\n<p>上面这两个过程，需要大家反复理解、深刻记忆。尤其是要记住这几个关键字：“删除”就是“向下比较+交换”，而“添加”则是“向上比较+交换”。</p>\\n<p>这里写给大家的两段代码，在实战中具备一定的通用性。希望大家能够充分熟悉，在理解的基础上记忆。下次如果真的用到，争取能够默写。</p>\\n<h2 id=\\"堆结构在排序中的应用-优先队列\\"><a class=\\"header-anchor\\" href=\\"#堆结构在排序中的应用-优先队列\\">#</a> 堆结构在排序中的应用——优先队列</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>优先队列按优先级而非到达顺序出队，二叉堆是常见实现。</li>\\n<li>建堆可从最后一个非叶节点开始下沉，整体 <code>O(n)</code>，不是逐个插入的 <code>O(n log n)</code>。</li>\\n<li>Top K 常用大小为 <code>k</code> 的反向堆，将空间降到 <code>O(k)</code>。</li>\\n<li>比较器必须保持传递性；优先级相同若需稳定，要附加递增序号。</li>\\n</ul>\\n<p><strong>求数组第 <code>k</code> 大元素时，可以维护一个容量为 <code>k</code> 的小顶堆，最终堆顶就是答案。</strong> 先把前 <code>k</code> 个数放入堆，此时堆顶是这些数里的最小值。继续遍历剩余元素，只有比堆顶更大的数才替换堆顶，并向下调整恢复堆结构。这样不用把整个数组完全排好序；题目已保证 <code>k</code> 在有效范围内。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“堆适合持续获取极值。若只要最大的 k 个，我维护容量 k 的最小堆，堆顶是当前答案里最容易被淘汰的元素。”</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>搜索结果页只展示分数最高的 <code>100</code> 条，服务端却把百万条结果全部排序后再截取，为什么容量为 <code>100</code> 的最小堆更贴合需求？</strong></p>\\n<p>只需要最大的 <code>100</code> 条时，维护容量为 <code>100</code> 的最小堆即可，堆顶始终是当前候选中最容易被淘汰的元素。新结果超过堆顶才替换并调整，无须让其余数据形成完整顺序；若最终页面要求有序，仍需对保留下来的候选再排序。</p>\\n</li>\\n<li>\\n<p><strong>消息调度器持续接收和取出最高优先级任务，评审时有人主张始终维护排序数组，你会如何判断两种实现？</strong></p>\\n<p>持续插入和弹出极值时，堆通常更均衡，插入与弹出都沿树高调整；排序数组虽然读取极值直接，但插入往往需要查找位置并移动元素。若系统主要查询、极少插入，排序数组可能更简单；选择应由读写比例决定。</p>\\n</li>\\n<li>\\n<p><strong>榜单需求从“取最高的 <code>k</code> 个”改成“取最低的 <code>k</code> 个”，现有代码使用容量为 <code>k</code> 的最小堆，应该怎样调整？</strong></p>\\n<p>应改用容量为 <code>k</code> 的最大堆，让堆顶代表当前答案中最大的、也就是最容易被更小元素淘汰的一项。新值小于堆顶时才执行替换并向下调整；若仍沿用最小堆，淘汰方向会反转，最终集合不再正确。</p>\\n</li>\\n<li>\\n<p><strong>线上优先队列偶发弹出旧优先级任务，系统采用“优先级变化时插入新版本”的惰性方案，你会怎样定位？</strong></p>\\n<p>先检查每条堆记录是否携带任务标识和版本，并确认弹出时会与当前版本表核对，旧版本必须被丢弃后继续取堆顶。还要核查任务取消路径是否同步更新有效状态；惰性方案实现简单，但旧记录会暂时占用内存。</p>\\n</li>\\n<li>\\n<p><strong>离线报表一次性输出全部记录的完整顺序，团队仍想复用线上优先队列逐个弹出；这种选型与直接排序相比有什么代价？</strong></p>\\n<p>逐个入堆再弹出能够得到有序结果，但它更适合持续到达、持续获取极值的场景。一次性数据且需要完整顺序时，直接排序通常更直观，也避免维护额外的队列状态；若还要流式接收或随时取极值，堆的增量能力才更有价值。</p>\\n</li>\\n</ul>\\n</section>\\n<p>在认识优先队列之前，我们先来看一道题：</p>\\n<blockquote>\\n<p>题目描述：在未排序的数组中找到第 k 个最大的元素。请注意，你需要找的是数组排序后的第 k 个最大的元素，而不是第 k 个不同的元素。</p>\\n</blockquote>\\n<p>示例 1:</p>\\n<ul>\\n<li>输入: [3,2,1,5,6,4] 和 k = 2</li>\\n<li>输出: 5</li>\\n</ul>\\n<p>示例 2:</p>\\n<ul>\\n<li>输入: [3,2,3,1,2,4,5,5,6] 和 k = 4</li>\\n<li>输出: 4</li>\\n</ul>\\n<blockquote>\\n<p>说明:你可以假设 k 总是有效的，且 1 ≤ k ≤ 数组的长度。</p>\\n</blockquote>\\n<p><strong>思路分析</strong></p>\\n<p>这道题的诉求非常直接——要求你对给定数组进行排序。关于排序，我们在下一节会展开讲解N种排序算法的实现方式，包括快速排序、归并排序、选择排序等等。这些排序有一个共同的特点——在排序的过程中，你很难去明确元素之间的大小关系，只有在排序彻底完成后，你才能找出第 k 大的元素是哪个。</p>\\n<p>对整个数组进行排序、然后按顺序返回索引为k-1的元素，这正是笔者在面试场上写出的第一个解法：</p>\\n<pre class=\\"language-js\\"><code>/**\\n* @param {number[]} nums\\n* @param {number} k\\n* @return {number}\\n*/\\nconst findKthLargest = function(nums, k) {\\n // 将数组逆序\\n const sorted = nums.sort((a,b)=&gt; {\\n     return b-a\\n })\\n // 取第k大的元素\\n return sorted[k-1]\\n};;\\n</code></pre>\\n<blockquote>\\n<p>是的，你没有看错，我甚至没有手动实现任何一个排序算法，而是直接调了 JS 的sort 方法。</p>\\n</blockquote>\\n<blockquote>\\n<p>大家不要笑，这个sort方法真的可以救命。如果你理解不了本节接下来要讲的基于堆结构的解法，又没信心记住后面两节涉及的各种各样的花式排序算法。那么你一定要紧紧抓住这个sort API。面试的时候，万一被问到“你为什么不会写xx排序算法”，这时候用一句“我用sort方法比较多，不喜欢自己造轮子”糊弄过去，还是有一定成功率的。</p>\\n</blockquote>\\n<p>好了，学渣小剧场结束。我们继续来看这个题：有没有一种排序方法能够在不对所有元素进行排序的情况下，帮我们提前定位到第 k 大的元素是哪个呢？当然有——构建一个堆结构就能解决问题！</p>\\n<p>对于这道题来说，要想求出第 k 大的元素，我们可以维护一个大小为 k 的小顶堆。这个堆的初始化过程可以通过遍历并插入数组的前 k 个元素来实现。当堆被填满后，再尝试用数组的第 k+1 到末尾的这部分元素来更新这个小顶堆，更新过程中遵循以下原则：</p>\\n<ul>\\n<li>若遍历到的数字比小顶堆的堆顶元素值大，则用该数字替换掉小顶堆的堆顶元素值</li>\\n<li>若遍历到的数字比小顶堆的堆顶元素值小，则忽略这个数字</li>\\n</ul>\\n<p>仔细想想，为什么要这样做？假设数组中元素的总个数是 n，那么：</p>\\n<blockquote>\\n<p>维护大小为 k 的小顶堆的目的，是为了确保堆中除了堆顶元素之外的 k-1 个元素值都大于堆顶元素。</p>\\n</blockquote>\\n<p>当我们用数组的[0, k-1]区间里的 数字初始化完成这个堆时，堆顶元素值就对应着前 k 个数字里的最小值。</p>\\n<p>紧接着我们尝试用索引区间为 [k, n-1]的数字来更新堆，在这个过程中，只允许比堆顶元素大的值进入堆。这一波操作过后，堆里的 k 个数字就是整个数组中最\\n大的 k 个数字，而堆顶的数字正是这 k 个数中最小的那个。于是本题得解。</p>\\n<blockquote>\\n<p>我们用示例中的[3,2,1,5,6,4]这个序列来模拟一下上面的过程。初始化一个规模为 k=2 的小顶堆，它长这样：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>    2\\n   /\\n  3\\n</code></pre>\\n<blockquote>\\n<p>用[k, n-1]索引范围内的元素来更新这个小顶堆：首先是用索引为2的数字1来试，发现1比堆顶的2还要小，忽略它。接着用索引为3的5来试，5是比2大的，用它把2换掉：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>    5\\n   /\\n  3\\n</code></pre>\\n<p>经过向下对比+调整，新的堆长这样：</p>\\n<pre class=\\"language-text\\"><code>    3\\n   /\\n  5\\n</code></pre>\\n<blockquote>\\n<p>我们发现，现在这个堆里面保存的正是索引范围[0, 3]内的前 k 个最大的数。以此类推，当对数组中最后一个元素执行过尝试入堆的逻辑后，堆里面保存的就是整个数组范围内的前 k 个最大的数。</p>\\n</blockquote>\\n<p>在解题的过程中，不出所料地用到了上文中提及的downHeap方法和upHeap 方法。不过大家千万不要直接复制粘贴，别忘了，前面我们是用大顶堆举例，这道题需要构造的是小顶堆——记得调整大小关系规则。</p>\\n<pre class=\\"language-js\\"><code>/**\\n* @param {number[]} nums\\n* @param {number} k\\n* @return {number}\\n*/\\nconst findKthLargest = function(nums, k) {\\n   // 初始化一个堆数组\\n   const heap = []  \\n   // n表示堆数组里当前最后一个元素的索引\\n   let n = 0\\n   // 缓存 nums 的长度\\n   const len = nums.length  \\n   // 初始化大小为 k 的堆\\n   function createHeap() {\\n       for(let i=0;i&lt;k;i++) {\\n           // 逐个往堆里插入数组中的数字\\n           insert(nums[i])\\n       }\\n   }\\n   \\n   // 尝试用 [k, n-1] 区间的元素更新堆\\n   function updateHeap() {\\n       for(let i=k;i&lt;len;i++) {\\n           // 只有比堆顶元素大的才有资格进堆\\n           if(nums[i]&gt;heap[0]) {\\n               // 用较大数字替换堆顶数字\\n               heap[0] = nums[i]  \\n               // 重复向下对比+交换的逻辑\\n               downHeap(0, k)\\n           }\\n       }\\n   }\\n   \\n   // 向下对比函数\\n   function downHeap(low, high) {\\n       // 入参是堆元素在数组里的索引范围，low表示下界，high表示上界\\n       let i=low,j=i*2+1 \\n       // 当 j 不超过上界时，重复向下对比+交换的操作\\n       while(j&lt;=high) {\\n           // // 如果右孩子比左孩子更小，则用右孩子和根结点比较\\n           if(j+1&lt;=high &amp;&amp; heap[j+1]&lt;heap[j]) {\\n               j = j+1\\n           }\\n           \\n           // 若当前结点比孩子结点大，则交换两者的位置，把较小的结点“拱上去”\\n           if(heap[i] &gt; heap[j]) {\\n               // 交换位置\\n               const temp = heap[j]  \\n               heap[j] = heap[i]  \\n               heap[i] = temp\\n               \\n               // i 更新为被交换的孩子结点的索引\\n               i=j  \\n               // j 更新为孩子结点的左孩子的索引\\n               j=j*2+1\\n           } else {\\n               break\\n           }\\n       }\\n   }\\n   \\n   // 入参是堆元素在数组里的索引范围，low表示下界，high表示上界\\n   function upHeap(low, high) {\\n       // 初始化 i（当前结点索引）为上界\\n       let i = high  \\n       // 初始化 j 为 i 的父结点\\n       let j = Math.floor((i-1)/2)  \\n       // 当 j 不逾越下界时，重复向上对比+交换的过程\\n       while(j&gt;=low)  {\\n           // 若当前结点比父结点小\\n           if(heap[j]&gt;heap[i]) {\\n               // 交换当前结点与父结点，保持父结点是较小的一个\\n               const temp = heap[j] \\n               heap[j] = heap[i]  \\n               heap[i] = temp\\n               \\n               // i更新为被交换父结点的位置\\n               i=j   \\n               // j更新为父结点的父结点\\n               j=Math.floor((i-1)/2)  \\n           } else {\\n               break\\n           }\\n       }\\n   }\\n\\n   // 插入操作=将元素添加到堆尾部+向上调整元素的位置\\n   function insert(x) {\\n       heap[n] = x  \\n       upHeap(0, n)\\n       n++\\n   }\\n   \\n   // 调用createHeap初始化元素个数为k的队\\n   createHeap()\\n   // 调用updateHeap更新堆的内容，确保最后堆里保留的是最大的k个元素\\n   updateHeap()\\n   // 最后堆顶留下的就是最大的k个元素中最小的那个，也就是第k大的元素\\n   return heap[0]\\n};\\n</code></pre>\\n<h2 id=\\"编码复盘\\"><a class=\\"header-anchor\\" href=\\"#编码复盘\\">#</a> 编码复盘</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>堆实现要把比较器集中封装，才能在最大堆、最小堆及对象优先级之间复用同一套上浮下沉</li>\\n<li><code>pop</code> 要分别处理空堆、单元素和一般情况，并在返回前保持数组长度与堆序一致</li>\\n<li>每次修改后可检查所有父子关系作为测试不变量，随机序列应与排序后的参考结果一致</li>\\n</ul>\\n<p><strong>编码时可以把优先队列理解为用数组表示的二叉堆，索引 <code>0</code> 始终保存当前最大值或最小值。</strong> 对索引 <code>i</code>，父结点位置是 <code>(i-1)/2</code>，左右孩子分别位于 <code>2*i+1</code> 和 <code>2*i+2</code>。这些下标关系来自完全二叉树的结构，也是上浮、下沉调整的基础。遇到“第 <code>k</code> 大”或“第 <code>k</code> 高”一类问题时，可以用它避免完整排序。</p>\\n</section>\\n<p>上面这个题解中出现的 heap 数组，就是一个优先队列。</p>\\n<blockquote>\\n<p>优先队列的本质是二叉堆结构，它具有以下特性：</p>\\n</blockquote>\\n<ul>\\n<li>队列的头部元素，也即索引为0的元素，就是整个数组里的最值——最大值或者最小值</li>\\n<li>对于索引为 i 的元素来说，它的父结点下标是 (i-1)/2（上面咱们讲过了，这与完全二叉树的结构特性有关）</li>\\n<li>对于索引为 i 的元素来说，它的左孩子下标应为2<em>i+1，右孩子下标应为2</em>i+2。</li>\\n</ul>\\n<blockquote>\\n<p>当题目中出现类似于“第k大”或者“第k高“这样的关键字时，就是在暗示你用优先队列/堆结构来做题——这样的手法可以允许我们在不对序列进行完全排序的情况下，找到第 k 个最值</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>排行榜页面要找第 <code>K</code> 大分数，同事却直接读取最大堆下标 <code>K-1</code>，测试数据只有十条时偶尔正确，你会怎样指出反例？</strong></p>\\n<p>不能把堆数组的下标顺序当成完整排序结果，堆只保证下标 <code>0</code> 是全局最值，并保证父子结点之间满足堆序。除堆顶外，其余元素没有全局次序，读取 <code>K-1</code> 不能可靠得到第 <code>K</code> 大值，必须配合出堆或维护大小为 <code>K</code> 的堆。</p>\\n</li>\\n<li>\\n<p><strong>任务调度页需要手写数组最小堆，评审发现删除堆顶后只交换末尾元素却没有继续调整，你会要求怎样实现并验证？</strong></p>\\n<p>删除堆顶后应把末尾元素移到下标 <code>0</code>，再与下标 <code>2*i+1</code>、<code>2*i+2</code> 的孩子比较并持续下沉，直到恢复堆序。插入则从末尾按父结点下标 <code>Math.floor((i-1)/2)</code> 上浮；测试需覆盖单元素、重复值和只有左孩子的边界。</p>\\n</li>\\n<li>\\n<p><strong>日志分析服务原来取第 <code>K</code> 大值，现在数据流无法一次装入内存且 <code>K</code> 远小于总量，负责人仍主张收齐后完整排序，你会如何调整？</strong></p>\\n<p>应顺序读取数据并维护一个容量为 <code>K</code> 的最小堆，堆顶始终作为当前前 <code>K</code> 大元素中的最低门槛。新值大于堆顶时才替换并下沉，处理成本约为 <code>O(n log k)</code>、额外空间为 <code>O(k)</code>；若还要输出完整有序序列，则仍需额外排序。</p>\\n</li>\\n<li>\\n<p><strong>线上优先队列偶发返回非最小任务，监控显示问题多发生在删除堆顶之后，值本身没有重复，你会从哪些代码现象开始定位？</strong></p>\\n<p>先检查下沉时是否同时比较左右孩子，并选择值更小的孩子交换；只和左孩子比较会在右孩子更小时破坏最小堆。再核对循环边界、末尾元素移除顺序以及孩子下标 <code>2*i+1</code>、<code>2*i+2</code>，任何越界或提前停止都会使堆顶不再是全局最小值。</p>\\n</li>\\n<li>\\n<p><strong>订单系统既要随时查看最高优先级，又要求相同优先级严格按进入顺序处理，架构师认为普通最大堆已经足够，你如何做选型取舍？</strong></p>\\n<p>普通最大堆只能保证最高优先级位于堆顶，不能保证相同优先级元素的先后顺序，因为上浮和下沉可能改变它们的位置。可为元素附加递增序号，让比较器先比较优先级、再比较序号；代价是保存额外字段，并需防范序号溢出或重启后重复。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);