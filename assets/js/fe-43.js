(window.webpackJsonp=window.webpackJsonp||[]).push([[43],{1151:function(e){e.exports=JSON.parse('{"html":"<h2 id=\\"_1-es5、es6和es2015有什么区别\\"><a class=\\"header-anchor\\" href=\\"#_1-es5、es6和es2015有什么区别\\">#</a> 1 ES5、ES6和ES2015有什么区别?</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>ES6</code> 就是 <code>ES2015</code>，同一版标准的两个叫法 —— <code>ES6</code> 是制定期间的代号，发布后改用年份命名</li>\\n<li>日常语境里 <code>ES6</code> 常被<strong>泛指</strong>&quot;2015 年之后的现代 <code>JS</code>&quot;，包含 <code>async/await</code>（ES2017）、可选链（ES2020）等</li>\\n<li><code>ES5</code> 是 2009 年的上一代标准，两者相隔六年</li>\\n<li>改成年度发布制是因为 <code>ES6</code> 憋了六年、一次性发布导致规模过大、浏览器实现缓慢</li>\\n<li>工程上真正决定&quot;能用什么&quot;的不是标准版本，而是 <code>browserslist</code> 基线 + <code>Babel</code> 转译 + <code>core-js</code> polyfill</li>\\n</ul>\\n<p><strong>严格来说，<code>ES6</code> 就是 <code>ES2015</code>，而 <code>ES5</code> 是 2009 年发布的上一代标准。</strong> <code>ES6</code> 是第六版标准在制定期间的叫法，2015 年正式发布后改用年份命名；因为这一版积累了六年、规模较大，之后转成年度发布。日常说“<code>ES6</code> 代码”时，常常泛指 <code>ES2015</code> 之后的现代 <code>JavaScript</code>，甚至包含 <code>async/await</code> 和可选链。工程里真正决定能否使用某项能力的，是 <code>browserslist</code>、<code>Babel</code> 与 <code>core-js</code> 配置，而不是口头上的版本名。</p>\\n</section>\\n<p><strong>先分清这三个词指的是什么：</strong></p>\\n<ul>\\n<li><strong><code>ES5</code></strong> —— 2009 年发布的语言标准，是 <code>ES6</code> 之前用了六年的版本。</li>\\n<li><strong><code>ES2015</code></strong> —— 2015 年 6 月发布的那一版标准，正式名称就叫 <code>ECMAScript 2015</code>。</li>\\n<li><strong><code>ES6</code></strong> —— <code>ES2015</code> 在制定期间的代号。因为它是第 6 版，社区叫惯了 <code>ES6</code>，发布后名字改成按年份命名，但口语里 <code>ES6</code> 一直沿用下来。</li>\\n</ul>\\n<p>所以严格说：<strong><code>ES6</code> = <code>ES2015</code></strong>，是同一版标准的两个叫法。</p>\\n<p><strong>但日常语境里 <code>ES6</code> 经常被泛化使用</strong>，指&quot;<code>ES2015</code> 及之后的所有现代 <code>JS</code>&quot;—— 比如有人说&quot;这段代码用了 <code>ES6</code>&quot;，里面可能有 <code>ES2017</code> 的 <code>async/await</code> 或 <code>ES2020</code> 的可选链。这种用法不精确但很常见，面试时能点破这一点是加分项。</p>\\n<p><strong>为什么 2015 年之后改成按年份命名？</strong></p>\\n<p><code>ES6</code> 制定了整整六年（<code>ES5</code> 是 2009 年），中间积压了大量特性一次性发布，导致规模庞大、浏览器实现缓慢。TC39 因此改成了<strong>年度发布制</strong>：每年 6 月发布一版，成熟一个特性就纳入一个，避免再出现&quot;一版憋六年&quot;的情况。</p>\\n<p>后续版本的代表特性：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>版本</th>\\n<th>代表特性</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>ES2015</code></td>\\n<td><code>let</code>/<code>const</code>、箭头函数、<code>class</code>、模板字符串、解构、<code>Promise</code>、<code>Module</code>、<code>Map</code>/<code>Set</code>、<code>Symbol</code></td>\\n</tr>\\n<tr>\\n<td><code>ES2016</code></td>\\n<td><code>includes</code>、指数运算符 <code>**</code></td>\\n</tr>\\n<tr>\\n<td><code>ES2017</code></td>\\n<td><code>async/await</code>、<code>Object.entries</code>、<code>padStart</code></td>\\n</tr>\\n<tr>\\n<td><code>ES2018</code></td>\\n<td>对象展开、<code>Promise.finally</code>、异步迭代</td>\\n</tr>\\n<tr>\\n<td><code>ES2019</code></td>\\n<td><code>flat</code>、<code>Object.fromEntries</code>、可选 <code>catch</code> 绑定</td>\\n</tr>\\n<tr>\\n<td><code>ES2020</code></td>\\n<td>可选链 <code>?.</code>、空值合并 <code>??</code>、<code>BigInt</code>、动态 <code>import</code></td>\\n</tr>\\n<tr>\\n<td><code>ES2021</code></td>\\n<td><code>replaceAll</code>、逻辑赋值 <code>??=</code>、<code>WeakRef</code></td>\\n</tr>\\n<tr>\\n<td><code>ES2022</code></td>\\n<td>类私有字段 <code>#</code>、<code>at()</code>、顶层 <code>await</code></td>\\n</tr>\\n<tr>\\n<td><code>ES2023+</code></td>\\n<td><code>findLast</code>、数组的不可变方法（<code>toSorted</code>/<code>toReversed</code>）</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>工程上更值得关心的是&quot;我能用哪些&quot;</strong>：这不由标准版本决定，而由你的 <code>browserslist</code> 基线和构建配置决定。语法交给 <code>Babel</code> 转译，新 <code>API</code> 靠 <code>core-js</code> polyfill —— 所以真正要回答的是&quot;目标浏览器支持到哪&quot;，而不是&quot;这是 <code>ES</code> 几&quot;。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审里有人把含有 <code>async/await</code> 和可选链 <code>?.</code> 的文件统一标成“纯 <code>ES6</code>”，并据此判断旧浏览器可运行；你会怎样纠正？</strong></p>\\n<p>我会指出 <code>ES6</code> 严格等同于 <code>ES2015</code>，而 <code>async/await</code> 属于 <code>ES2017</code>、可选链属于 <code>ES2020</code>，口语中的“<code>ES6</code>”常被泛指现代 <code>JavaScript</code>。兼容性不能由这个标签推导，仍要回到目标浏览器和构建配置。</p>\\n</li>\\n<li>\\n<p><strong>营销页要覆盖项目既定的旧浏览器基线，开发者新增了箭头函数和 <code>Object.fromEntries</code>；构建评审时你分别检查什么？</strong></p>\\n<p>我会检查 <code>Babel</code> 是否按 <code>browserslist</code> 转译箭头函数等语法，并确认 <code>core-js</code> 是否为 <code>Object.fromEntries</code> 这类新 <code>API</code> 提供所需 <code>polyfill</code>。语法转译与运行时能力是两条链路，只配置其中之一仍可能在线上报错。</p>\\n</li>\\n<li>\\n<p><strong>团队把目标环境改成较新的内置 <code>WebView</code> 后，负责人想立刻删除全部转译和 <code>polyfill</code>；你会要求哪些证据？</strong></p>\\n<p>我会先根据新的 <code>browserslist</code> 基线核对实际引擎支持，并分别审查代码使用的语法和内建 <code>API</code>，不能用“较新”代替兼容性证据。确认产物和运行环境一致后才能逐项移除；过早清理会把构建收益换成运行时失败风险。</p>\\n</li>\\n<li>\\n<p><strong>线上只有一类浏览器报 <code>Object.fromEntries is not a function</code>，同一页面的箭头函数执行正常；你会从哪条构建链路排查？</strong></p>\\n<p>我会优先检查 <code>core-js</code> 的注入模式、目标基线和最终产物，因为箭头函数正常只能说明语法已被支持或转译，不能证明新 <code>API</code> 存在。再用受影响浏览器复现并查看 <code>polyfill</code> 是否加载；盲目调整 <code>Babel</code> 语法插件通常解决不了该错误。</p>\\n</li>\\n<li>\\n<p><strong>架构师主张所有新特性都手工查 <code>caniuse</code>，工具负责人则要求完全依赖 <code>browserslist</code>；你会怎样划分职责？</strong></p>\\n<p>我会让 <code>browserslist</code> 成为项目可执行的兼容基线，由 <code>Babel</code> 和 <code>core-js</code> 据此生成产物；对关键或边缘能力再查兼容资料并做目标环境验证。提案是否进入标准还应看 <code>TC39</code> 阶段，只有 <code>Stage 4</code> 才表示已纳入标准，不能与浏览器落地程度混为一谈。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_2-babel是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_2-babel是什么-有什么作用\\">#</a> 2 babel是什么，有什么作用?</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Babel</code> 是源码到源码的编译器，流程是 <strong>解析成 <code>AST</code> → 插件转换 → 生成代码</strong> 三段式</li>\\n<li>⚠️ <strong>默认只转语法不转 API</strong>：箭头函数会转，<code>Promise</code>、<code>Array.at</code> 不会 —— 后者要靠 <code>core-js</code> polyfill</li>\\n<li><code>useBuiltIns</code> 三种模式：<code>usage</code>（按实际用到注入，应用首选）、<code>entry</code>（入口全量，保险但大）、<code>false</code></li>\\n<li>⚠️ <code>usage</code> 默认不处理 <code>node_modules</code>，第三方库用了新 <code>API</code> 会漏，是&quot;线上老手机白屏&quot;的常见原因</li>\\n<li>写库要用 <code>transform-runtime</code> 避免污染使用方全局；现在 <code>SWC</code>/<code>esbuild</code> 在速度上已取代它</li>\\n</ul>\\n<p><strong><code>Babel</code> 是源码到源码的 <code>JavaScript</code> 编译器，作用是把新语法转换成目标环境能够运行的代码。</strong> 它会依次解析源码生成 <code>AST</code>、通过插件改写节点，再生成目标代码，<code>JSX</code> 和 <code>TypeScript</code> 类型擦除也依赖这套机制。要注意它默认只转换语法，<code>Promise</code>、<code>Array.prototype.at</code> 等新 <code>API</code> 仍需配合 <code>core-js</code>。应用通常用 <code>useBuiltIns</code> 注入补丁，库则更适合用 <code>transform-runtime</code>，避免污染使用方的全局环境。</p>\\n</section>\\n<p><code>Babel</code> 是一个 <code>JavaScript</code> <strong>编译器</strong>（更准确说是源码到源码的转换器），核心作用是让你能用最新的语法写代码，同时产出目标环境能跑的代码。</p>\\n<p><strong>它的工作流程是标准的三段式：</strong></p>\\n<pre class=\\"language-text\\"><code>源码 → [解析 Parse] → AST → [转换 Transform] → 新 AST → [生成 Generate] → 目标代码\\n</code></pre>\\n<ul>\\n<li><strong>解析</strong>：<code>@babel/parser</code> 把源码变成抽象语法树</li>\\n<li><strong>转换</strong>：各个插件遍历 <code>AST</code>，把新语法节点改写成旧语法节点 —— 这一步是所有插件工作的地方</li>\\n<li><strong>生成</strong>：<code>@babel/generator</code> 把新 <code>AST</code> 打印回代码字符串</li>\\n</ul>\\n<p><strong>最关键的一条：<code>Babel</code> 默认只转换语法，不转换 API。</strong></p>\\n<pre class=\\"language-js\\"><code>// ✅ 语法：箭头函数会被转成 function\\nconst fn = () =&gt; {}\\n\\n// ❌ API：Promise、Array.prototype.at、Object.fromEntries 不会被转\\n// 低版本环境依然报 undefined is not a function\\n[1, 2, 3].at(-1)\\n</code></pre>\\n<p>要补 <code>API</code> 得靠 <code>core-js</code> 提供的 polyfill，通过 <code>@babel/preset-env</code> 的 <code>useBuiltIns</code> 配置注入：</p>\\n<pre class=\\"language-js\\"><code>{\\n  presets: [[&#39;@babel/preset-env&#39;, {\\n    useBuiltIns: &#39;usage&#39;,   // 按代码里实际用到的 API 注入\\n    corejs: 3,\\n  }]]\\n}\\n</code></pre>\\n<table>\\n<thead>\\n<tr>\\n<th>配置</th>\\n<th>行为</th>\\n<th>适用</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>false</code>（默认）</td>\\n<td>完全不注入</td>\\n<td>自己控制 polyfill</td>\\n</tr>\\n<tr>\\n<td><code>\'entry\'</code></td>\\n<td>入口按目标浏览器一次性引入全部所需</td>\\n<td>保险但体积大</td>\\n</tr>\\n<tr>\\n<td><code>\'usage\'</code></td>\\n<td>按文件实际用到的注入</td>\\n<td>应用项目首选</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>注意 <code>usage</code> 默认不处理 <code>node_modules</code></strong>，第三方库里用了新 <code>API</code> 时会漏掉，这类&quot;本地好好的、线上老手机白屏&quot;的问题通常就出在这。</p>\\n<p><strong>写库和写应用的配置不一样</strong>：库项目应该用 <code>@babel/plugin-transform-runtime</code>，它把 helper 函数和 polyfill 从全局环境改为按模块引入，避免污染使用方的全局对象。应用项目才用 <code>useBuiltIns</code>。</p>\\n<p><strong><code>Babel</code> 的另一半价值是插件生态</strong>：<code>JSX</code> 转换、<code>TypeScript</code> 类型擦除、装饰器、按需引入组件库（<code>babel-plugin-import</code>）、<code>React Fast Refresh</code> 都是 <code>Babel</code> 插件。所以它早已不只是&quot;<code>ES6</code> 转 <code>ES5</code>&quot;的工具，而是整个 <code>JS</code> 工具链的 <code>AST</code> 处理层。</p>\\n<p><strong>现在的替代品</strong>：<code>esbuild</code>、<code>SWC</code> 用 <code>Go</code>/<code>Rust</code> 重写了同样的能力，速度快一个数量级，<code>Next.js</code>、<code>Vite</code> 都已经默认用它们。<code>Babel</code> 的优势剩下插件生态和对边缘语法的完整支持。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>营销页用了箭头函数和 <code>Array.prototype.at</code>，在旧安卓上前者正常、后者却报 <code>at is not a function</code>；既然都经过了 <code>Babel</code>，为什么结果不同？</strong></p>\\n<p>箭头函数属于语法，<code>Babel</code> 能通过插件把对应 <code>AST</code> 节点改写成旧语法；<code>at</code> 属于运行时 <code>API</code>，默认不会被转换。要兼容它需由 <code>core-js</code> 注入 <code>polyfill</code>，并结合目标浏览器配置验证；只检查编译产物里有没有箭头函数并不能证明运行时能力完整。</p>\\n</li>\\n<li>\\n<p><strong>一个面向旧浏览器的后台应用已经配置 <code>@babel/preset-env</code>，希望只补业务代码真正使用的 <code>API</code>，你会怎样设置并验证产物？</strong></p>\\n<p>应用项目可配置 <code>useBuiltIns: \'usage\'</code> 和 <code>corejs: 3</code>，让转换过程按文件中实际使用的能力注入 <code>core-js</code> 模块。还要核对目标环境配置，并在构建产物中确认相关模块确实存在；若 <code>useBuiltIns</code> 仍为默认的 <code>false</code>，仅启用 <code>preset-env</code> 不会自动补齐这些 <code>API</code>。</p>\\n</li>\\n<li>\\n<p><strong>同一套源码既要构建官网应用，又要发布给第三方使用的工具库；两边都直接使用 <code>useBuiltIns: \'usage\'</code> 合适吗？</strong></p>\\n<p>官网应用可用 <code>useBuiltIns</code> 补目标环境能力，但工具库更适合 <code>@babel/plugin-transform-runtime</code>，把 <code>helper</code> 和相关能力改为模块依赖。库若直接修改全局对象，可能污染使用方环境或与其 <code>polyfill</code> 策略冲突；代价是消费者仍需正确安装和打包对应的 <code>runtime</code> 依赖。</p>\\n</li>\\n<li>\\n<p><strong>线上只有旧手机白屏，堆栈指向某个依赖包里的 <code>Object.fromEntries</code>；业务源码已启用 <code>useBuiltIns: \'usage\'</code>，你会从哪里开始排查？</strong></p>\\n<p>先确认报错代码是否来自未经过 <code>babel-loader</code> 的 <code>node_modules</code>，因为 <code>usage</code> 只能分析实际进入转换流程的文件。可将明确需要兼容的依赖加入 <code>loader</code> 的 <code>include</code>，重新构建并检查 <code>polyfill</code> 是否注入；不要无差别转译全部依赖，否则会扩大构建成本和兼容性风险。</p>\\n</li>\\n<li>\\n<p><strong>团队为提升构建速度准备把大型 <code>React</code> 项目从 <code>Babel</code> 全量换成 <code>SWC</code>，但项目依赖装饰器、按需引入和 <code>Fast</code> <code>Refresh</code> 插件；你会怎样决定迁移边界？</strong></p>\\n<p>先盘点每个 <code>Babel</code> 插件是否有等价转换及一致的语义，再用代表性代码和开发链路验证，而不是只比较编译器速度。<code>SWC</code> 或 <code>esbuild</code> 适合承担已有等价能力的转换；若关键插件没有可靠替代，应保留局部 <code>Babel</code> 流程，否则可能以更快构建换来行为偏差。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_3-let有什么用-有了var为什么还要用let\\"><a class=\\"header-anchor\\" href=\\"#_3-let有什么用-有了var为什么还要用let\\">#</a> 3 let有什么用，有了var为什么还要用let？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>var</code> 的四个缺陷 <code>let</code> 逐个修：没有块级作用域、提升后拿 <code>undefined</code>、允许重复声明、顶层变量挂 <code>window</code></li>\\n<li><code>let</code> <strong>也会提升</strong>，只是在赋值前处于暂时性死区（<code>TDZ</code>），访问直接抛 <code>ReferenceError</code> —— &quot;不提升&quot;是简化说法</li>\\n<li>循环里 <code>let</code> <strong>每轮迭代新建一个绑定</strong>，所以异步回调能拿到各自那一轮的值，这是它专为循环设计的特性</li>\\n<li><code>const</code> 约束的是&quot;绑定不可重新赋值&quot;，不是值不可变 —— <code>const obj = {}</code> 后改属性完全合法</li>\\n<li>规范：默认 <code>const</code>，需要重新赋值才用 <code>let</code>，永远不用 <code>var</code></li>\\n</ul>\\n<p><strong><code>let</code> 用来声明可重新赋值的块级变量，解决了 <code>var</code> 容易泄漏作用域、静默覆盖和提前读到 <code>undefined</code> 的问题。</strong> <code>let</code> 其实也会提升，只是初始化前处于暂时性死区，访问会直接抛出 <code>ReferenceError</code>，能让错误更早暴露。它在循环的每轮迭代中还会创建新绑定，因此异步回调可以拿到各自那一轮的值。实践中我一般默认使用 <code>const</code>，确实需要重新赋值时才改用 <code>let</code>。</p>\\n</section>\\n<p><code>var</code> 的三个设计缺陷，<code>let</code> 逐个修掉：</p>\\n<p><strong>① 没有块级作用域 → 变量泄漏</strong></p>\\n<pre class=\\"language-js\\"><code>for (var i = 0; i &lt; 3; i++) {}\\nconsole.log(i)   // 3 —— 循环结束了 i 还在\\n\\nfor (let j = 0; j &lt; 3; j++) {}\\nconsole.log(j)   // ReferenceError ✅\\n</code></pre>\\n<p>这个缺陷最经典的表现是循环里的异步回调：</p>\\n<pre class=\\"language-js\\"><code>for (var i = 0; i &lt; 3; i++) setTimeout(() =&gt; console.log(i))  // 3 3 3\\nfor (let i = 0; i &lt; 3; i++) setTimeout(() =&gt; console.log(i))  // 0 1 2 ✅\\n</code></pre>\\n<p><code>var</code> 只有一个 <code>i</code>，三个回调共享它；<code>let</code> 在<strong>每轮迭代都创建一个新的块级绑定</strong>，所以每个回调捕获的是各自那一轮的值。这是 <code>let</code> 一个专门为循环设计的特性，不是&quot;块级作用域&quot;四个字能直接推出来的。</p>\\n<p><strong>② 变量提升拿到 <code>undefined</code> → 逻辑静默出错</strong></p>\\n<pre class=\\"language-js\\"><code>console.log(a)  // undefined —— 不报错，但值是错的\\nvar a = 1\\n\\nconsole.log(b)  // ReferenceError: Cannot access &#39;b&#39; before initialization ✅\\nlet b = 1\\n</code></pre>\\n<p><code>let</code> 也会提升（声明被登记到块顶部），但在赋值前处于<strong>暂时性死区（TDZ）</strong>，访问直接抛错。这比拿到 <code>undefined</code> 再一路错下去好得多 —— <strong>错误暴露得越早越便宜</strong>。</p>\\n<p>注意：&quot;<code>let</code> 不提升&quot;是简化说法，面试时最好把 <code>TDZ</code> 说清楚。</p>\\n<p><strong>③ 允许重复声明 → 变量覆盖</strong></p>\\n<pre class=\\"language-js\\"><code>var x = 1\\nvar x = 2   // 静默覆盖，没有任何提示\\nlet y = 1\\nlet y = 2   // SyntaxError ✅\\n</code></pre>\\n<p>大文件里两个人各自声明了同名变量，<code>var</code> 不会有任何警告。</p>\\n<p><strong>④ 顶层 <code>var</code> 会挂到 <code>window</code> 上</strong></p>\\n<pre class=\\"language-js\\"><code>var a = 1;  window.a   // 1  —— 污染全局对象\\nlet b = 1;  window.b   // undefined ✅\\n</code></pre>\\n<p><strong><code>const</code> 补充一点</strong>：它约束的是<strong>绑定不可重新赋值</strong>，不是值不可变。<code>const obj = {}</code> 之后 <code>obj.a = 1</code> 完全合法。</p>\\n<p><strong>实践规范</strong>：默认用 <code>const</code>，确实要重新赋值才改成 <code>let</code>，永远不用 <code>var</code>。这个顺序的好处是&quot;能不能被改&quot;在声明处就一目了然，读代码时少一层心智负担。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>列表页循环注册三个延迟回调，<code>for (var i = 0; i &lt; 3; i++)</code> 最后连续打印 <code>3</code>，换成 <code>let</code> 就打印 <code>0、1、2</code>；这只是块级作用域带来的结果吗？</strong></p>\\n<p>不只是一般的块级作用域，<code>for</code> 循环会为 <code>let</code> 在每轮迭代创建新的绑定，因此三个闭包捕获不同的值。<code>var</code> 在函数作用域内只有一个共享绑定，回调执行时循环已经结束；若不能改声明，可用 <code>IIFE</code> 参数显式保存每轮值，但代码更繁琐。</p>\\n</li>\\n<li>\\n<p><strong>结算页有一段代码先读取 <code>price</code> 再声明变量，使用 <code>var</code> 时得到 <code>undefined</code> 并继续计算，改成 <code>let</code> 后立即抛出 <code>ReferenceError</code>；你如何解释这种差异？</strong></p>\\n<p><code>let</code> 的声明同样会被登记到当前块，但初始化前位于暂时性死区，任何访问都会报错。<code>var</code> 则在作用域建立时初始化为 <code>undefined</code>，容易让错误数据继续传播；把声明移到首次使用之前才是修复，不能用可选链或默认值掩盖错误顺序。</p>\\n</li>\\n<li>\\n<p><strong>两个团队合并一个旧脚本后都声明了 <code>var status</code>，页面没有语法错误却出现状态覆盖；改成 <code>let</code> 能提供什么保护，又不能解决什么？</strong></p>\\n<p>同一作用域内重复声明 <code>let status</code> 会直接产生 <code>SyntaxError</code>，能在加载阶段暴露命名冲突，而 <code>var</code> 会静默接受重复声明。它不能解决不同模块之间的业务命名混乱，也不能阻止后续重新赋值；仍应通过模块作用域、清晰命名和必要的 <code>const</code> 约束降低冲突。</p>\\n</li>\\n<li>\\n<p><strong>一个非模块化的老页面通过全局变量与第三方脚本通信，把顶层 <code>var token</code> 改成 <code>let token</code> 后对方读取 <code>window.token</code> 得到 <code>undefined</code>；这是兼容性故障还是预期行为？</strong></p>\\n<p>这是预期的绑定差异：传统脚本中的顶层 <code>var</code> 会成为全局对象属性，而顶层 <code>let</code> 不会挂到 <code>window</code>。迁移前应找出依赖 <code>window.token</code> 的调用方，若确需公开接口就显式赋给受控命名空间；直接保留隐式全局会继续带来覆盖和污染风险。</p>\\n</li>\\n<li>\\n<p><strong>代码评审规定所有变量都改成 <code>const</code>，但分页游标需要在循环中更新；坚持统一风格还是允许 <code>let</code>，判断标准是什么？</strong></p>\\n<p>分页游标确实需要重新赋值时应使用 <code>let</code>，其余绑定默认使用 <code>const</code>，让可变性在声明处可见。<code>const</code> 只禁止变量重新指向别的值，并不会冻结对象属性；若业务要求对象内容不可变，还需 <code>Object.freeze</code> 或类型约束，而且浅冻结不能保护嵌套对象。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_4-举一些es6对string字符串类型做的常用升级优化\\"><a class=\\"header-anchor\\" href=\\"#_4-举一些es6对string字符串类型做的常用升级优化\\">#</a> 4 举一些ES6对String字符串类型做的常用升级优化?</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>模板字符串：变量内嵌 <code>\\\\${}</code>、原样保留换行缩进；<strong>标签模板</strong>能拿到静态片段和变量，是自动转义和 <code>CSS-in-JS</code> 的基础</li>\\n<li>查找类：<code>includes</code> / <code>startsWith</code> / <code>endsWith</code> 取代 <code>indexOf !== -1</code>，语义清楚且不会写错比较符</li>\\n<li>补全类：<code>padStart</code> / <code>padEnd</code> / <code>repeat</code>，时间和金额格式化很常用</li>\\n<li>后续版本补充：<code>trimStart</code>（ES2019）、<code>replaceAll</code>（ES2021）、<code>at(-1)</code>（ES2022）</li>\\n<li>⚠️ <code>\'👍\'.length === 2</code> —— 字符串是 <code>UTF-16</code> 码元序列，截断要用 <code>[...str]</code> 或 <code>Intl.Segmenter</code>，否则会切出乱码</li>\\n</ul>\\n<p><strong><code>ES6</code> 对字符串最实用的升级是模板字符串，以及 <code>includes</code>、<code>startsWith</code>、<code>endsWith</code> 这类语义更清楚的查找方法。</strong> 模板字符串支持变量插值、多行内容和任意表达式，比手动用 <code>+</code> 拼接更直观，标签模板还可用于转义或国际化。查找方法则避免了 <code>indexOf</code> 与 <code>-1</code> 比较时容易漏掉下标为 <code>0</code> 的情况，<code>repeat</code>、<code>padStart</code> 和 <code>padEnd</code> 也方便处理重复与补齐。需要留意 <code>length</code> 统计的是 <code>UTF-16</code> 码元，处理 <code>emoji</code> 字数或截断时应使用扩展运算符或 <code>Intl.Segmenter</code>。</p>\\n</section>\\n<p><strong>① 模板字符串（最常用的一个）</strong></p>\\n<pre class=\\"language-js\\"><code>// 旧写法：拼接容易漏空格、多行要靠 \\\\n\\nconst s1 = &#39;Hello, &#39; + name + &#39;! You have &#39; + count + &#39; messages.&#39;\\n\\n// 新写法：变量内嵌、原样保留换行和缩进\\nconst s2 = \\\\`Hello, \\\\${name}! You have \\\\${count} messages.\\\\`\\nconst html = \\\\`\\n  &lt;ul&gt;\\n    \\\\${list.map((x) =&gt; \\\\`&lt;li&gt;\\\\${x}&lt;/li&gt;\\\\`).join(&#39;&#39;)}\\n  &lt;/ul&gt;\\n\\\\`\\n</code></pre>\\n<p><code>${}</code> 里可以放任意表达式，包括三元、函数调用、嵌套模板。</p>\\n<p><strong>标签模板</strong>是它一个被低估的能力 —— 模板字符串前面加一个函数名，就能拿到拆分后的静态片段和变量，用于自动转义、国际化、<code>styled-components</code> 的 <code>CSS-in-JS</code>：</p>\\n<pre class=\\"language-js\\"><code>const safe = (strings, ...values) =&gt;\\n  strings.reduce((acc, s, i) =&gt; acc + s + escapeHtml(values[i] ?? &#39;&#39;), &#39;&#39;)\\nconst el = safe\\\\`&lt;div&gt;\\\\${userInput}&lt;/div&gt;\\\\`   // userInput 被自动转义\\n</code></pre>\\n<p><strong>② 查找类方法：语义比 <code>indexOf</code> 清楚</strong></p>\\n<pre class=\\"language-js\\"><code>str.includes(&#39;abc&#39;)      // 旧：str.indexOf(&#39;abc&#39;) !== -1\\nstr.startsWith(&#39;http&#39;)   // 旧：str.slice(0, 4) === &#39;http&#39;\\nstr.endsWith(&#39;.png&#39;)\\n</code></pre>\\n<p><code>indexOf</code> 返回下标，判断&quot;有没有&quot;时要和 <code>-1</code> 比较 —— 既啰嗦又容易写成 <code>&gt; 0</code>（漏掉开头就匹配的情况）。</p>\\n<p><strong>③ 补全类方法：对齐输出的利器</strong></p>\\n<pre class=\\"language-js\\"><code>&#39;5&#39;.padStart(2, &#39;0&#39;)       // &#39;05&#39;  —— 时间格式化\\n&#39;9&#39;.padEnd(4, &#39;.&#39;)         // &#39;9...&#39;\\n&#39;ab&#39;.repeat(3)             // &#39;ababab&#39;\\n</code></pre>\\n<p><strong>④ 后续版本的补充</strong>（面试时能带一句是加分）</p>\\n<pre class=\\"language-js\\"><code>&#39;  x  &#39;.trimStart()        // ES2019\\n&#39;a-b-c&#39;.replaceAll(&#39;-&#39;, &#39;&#39;) // ES2021，旧写法要用正则加 g\\n&#39;abc&#39;.at(-1)               // ES2022，取最后一个字符\\n</code></pre>\\n<p><strong>⑤ 一个容易被忽略的坑：<code>length</code> 和 <code>Unicode</code></strong></p>\\n<pre class=\\"language-js\\"><code>&#39;👍&#39;.length        // 2 —— 不是 1\\n[...&#39;👍&#39;].length   // 1 ✅ 扩展运算符按码点拆\\n</code></pre>\\n<p>因为 <code>JS</code> 字符串是 <code>UTF-16</code> 码元序列，超出基本平面的字符（emoji、部分生僻字）要用两个码元表示。所以做字数统计、截断时不能直接用 <code>length</code> 和 <code>slice</code>，否则会把一个 emoji 切成两个乱码。正确做法是用 <code>[...str]</code> 或 <code>Intl.Segmenter</code> 按字素簇处理。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>搜索页用 <code>if (text.indexOf(keyword) &gt; 0)</code> 判断命中，关键词位于标题开头时却被漏掉；你会怎样改，并说明原判断错在哪里？</strong></p>\\n<p>这里应直接使用 <code>text.includes(keyword)</code>，因为业务只关心是否包含，而不是匹配位置。<code>indexOf</code> 在开头命中会返回 <code>0</code>，条件 <code>&gt; 0</code> 因而误判；若后续还需要高亮起点才保留下标，同时必须用 <code>!== -1</code> 判断是否命中。</p>\\n</li>\\n<li>\\n<p><strong>评论页按 <code>str.slice(0, 20)</code> 截断内容，用户输入 <code>emoji</code> 后末尾出现乱码；在不改变“限制可见字符”需求的前提下怎么处理？</strong></p>\\n<p><code>slice</code> 按 <code>UTF-16</code> 码元截取，可能切断由两个码元组成的字符，最低限度可用 <code>[...str].slice(0, 20).join(\'\')</code> 按码点处理。若限制的是用户看到的字符，应使用 <code>Intl.Segmenter</code> 按字素簇分段；组合 <code>emoji</code> 可能包含多个码点，扩展运算符仍会拆散它。</p>\\n</li>\\n<li>\\n<p><strong>运营后台用模板字符串把昵称插进 <code>&lt;li&gt;${name}&lt;/li&gt;</code>，随后赋给 <code>innerHTML</code>，昵称来自用户资料；怎样保留模板写法又避免形成直接的 <code>XSS</code> 入口？</strong></p>\\n<p>可用标签模板统一对所有插值执行 <code>escapeHtml</code>，静态片段保持不变，再把结果交给渲染层。更稳妥的是用 <code>textContent</code> 和 <code>DOM</code> <code>API</code> 创建节点，让用户内容不进入 <code>HTML</code> 解析；若允许部分富文本，则需要经过明确的白名单净化，简单替换尖括号并不可靠。</p>\\n</li>\\n<li>\\n<p><strong>日志导出要求序号固定为四位、状态字段右侧补点，团队有人主张继续手写循环补齐；你会选哪些字符串能力，边界是什么？</strong></p>\\n<p>可分别使用 <code>String(id).padStart(4, \'0\')</code> 和 <code>status.padEnd(width, \'.\')</code>，意图比手写循环清楚，重复片段则可用 <code>repeat</code>。这些方法依据字符串长度补齐，不理解终端显示宽度；若内容包含全角字符或 <code>emoji</code>，列对齐仍需按实际显示宽度处理。</p>\\n</li>\\n<li>\\n<p><strong>消息模板既要支持变量插值，又要由国际化团队调整语序，开发者准备在代码里堆叠嵌套模板字符串；标签模板在这里能解决什么，不能解决什么？</strong></p>\\n<p>标签模板能取得静态片段与插值值，适合集中做转义、格式化或把内容交给国际化函数处理。它本身不是完整的翻译资源管理方案，也不会自动处理复数和语言语序；文案规模扩大后仍应使用结构化词条，并避免把用户输入当作模板源码执行。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_5-举一些es6对array数组类型做的常用升级优化\\"><a class=\\"header-anchor\\" href=\\"#_5-举一些es6对array数组类型做的常用升级优化\\">#</a> 5 举一些ES6对Array数组类型做的常用升级优化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>创建：<code>Array.from()</code>（类数组/可迭代对象转真数组，还能传 map 函数）、<code>Array.of()</code>（避开 <code>new Array(3)</code> 产生空位的坑）</li>\\n<li>查找：<code>find</code> / <code>findIndex</code> 返回元素或下标、<code>includes</code> 能正确判断 <code>NaN</code>（<code>indexOf</code> 不能）</li>\\n<li>遍历：<code>entries</code> / <code>keys</code> / <code>values</code> 返回迭代器，配合 <code>for...of</code> 能同时拿下标和值</li>\\n<li>填充与展平：<code>fill</code>、<code>flat</code>、<code>flatMap</code>（ES2019）</li>\\n<li>ES2023 起有了不可变版本：<code>toSorted</code> / <code>toReversed</code> / <code>toSpliced</code> / <code>with</code>，不再原地修改，正好解决 <code>sort</code> 污染上游数组的老问题</li>\\n</ul>\\n<p><strong><code>ES6</code> 对数组的常用优化主要体现在解构赋值、扩展运算符和更直接的查找方法上。</strong> 解构可以清楚地把数组项映射到变量并设置默认值，扩展运算符则能在数组与松散序列之间转换，也可替代部分 <code>arguments</code> 和 <code>apply</code> 用法。查找时可以用 <code>find</code> 获取符合条件的元素，<code>includes</code> 还能正确判断数组中是否存在 <code>NaN</code>，避免 <code>indexOf</code> 的限制。复制数组时常写成 <code>[...source]</code>，但它表达的是展开复制，不应和业务上的深层复制混为一谈。</p>\\n</section>\\n<p><strong>优化部分</strong></p>\\n<ul>\\n<li>数组解构赋值。<code>ES6</code>可以直接以<code>let [a,b,c] = [1,2,3]</code>形式进行变量赋值，在声明较多变量时，不用再写很多<code>let(var),</code>且映射关系清晰，且支持赋默认值</li>\\n<li>扩展运算符。<code>ES6</code>新增的扩展运算符(<code>...</code>)(重要),可以轻松的实现数组和松散序列的相互转化，可以取代<code>arguments</code>对象和<code>apply</code>方法，轻松获取未知参数个数情况下的参数集合。（尤其是在<code>ES5</code>中，<code>arguments</code>并不是一个真正的数组，而是一个类数组的对象，但是扩展运算符的逆运算却可以返回一个真正的数组）。扩展运算符还可以轻松方便的实现数组的复制和解构赋值（<code>let a = [2,3,4]</code>; <code>let b = [...a]</code>）</li>\\n</ul>\\n<p><strong>升级部分</strong></p>\\n<blockquote>\\n<p><code>ES6</code>在<code>Array</code>原型上新增了<code>find()</code>方法，用于取代传统的只能用<code>indexOf</code>查找包含数组项目的方法,且修复了<code>indexOf</code>查找不到<code>NaN的bug([NaN].indexOf(NaN) === -1)</code>.此外还新增了<code>copyWithin()</code>,<code>includes()</code>, <code>fill()</code>,<code>flat()</code>等方法，可方便的用于字符串的查找，补全,转换等</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>接口返回 <code>{0:\'a\',1:\'b\',length:2}</code>，开发者用 <code>[...result]</code> 转数组时抛出“不可迭代”，但 <code>Array.from(result)</code> 能工作；两者差异在哪里？</strong></p>\\n<p>扩展运算符要求对象实现 <code>Symbol.iterator</code>，只有数字下标和 <code>length</code> 的纯类数组对象并不满足条件。<code>Array.from</code> 既能消费可迭代对象，也能按类数组的长度读取元素；若数据连合法的 <code>length</code> 都没有，则应先校验接口结构，而不是依赖转换结果。</p>\\n</li>\\n<li>\\n<p><strong>报表页要生成 <code>0</code> 到 <code>999</code> 的序号，代码写成 <code>Array(1000).map((_, i) =&gt; i)</code>，结果得到的仍是空槽数组；你会如何改？</strong></p>\\n<p>可写成 <code>Array.from({length: 1000}, (_, i) =&gt; i)</code>，它在创建数组时就执行映射函数并填入每个位置。<code>Array(1000)</code> 产生的是空槽，普通 <code>map</code> 会跳过这些位置；用 <code>fill</code> 后再映射也能实现，但会多一道操作且更容易误用共享对象。</p>\\n</li>\\n<li>\\n<p><strong>筛选页要判断标签数组是否包含 <code>NaN</code>，旧代码用 <code>indexOf(NaN)</code> 始终返回 <code>-1</code>；应该选 <code>find</code> 还是 <code>includes</code>，为什么？</strong></p>\\n<p>只判断是否存在应使用 <code>tags.includes(NaN)</code>，它能识别数组中的 <code>NaN</code>，返回值也直接表达布尔语义。<code>find</code> 更适合按谓词返回首个元素，例如寻找满足字段条件的对象；若需要元素下标，则应选择相应的索引查找方式，不能把返回元素与位置混用。</p>\\n</li>\\n<li>\\n<p><strong>组件把 <code>const backup = [...rows]</code> 当作深拷贝，随后修改 <code>backup[0].name</code> 却让原表格同步变化；扩展运算符为什么没有隔离数据？</strong></p>\\n<p><code>[...rows]</code> 只复制数组这一层，元素若是对象，原数组和副本仍持有同一引用，所以修改属性会同时可见。可按数据结构复制需要变更的对象，例如用 <code>map</code> 对目标项展开；嵌套层级更深时要明确采用深拷贝或不可变更新策略，避免误以为语法展开等于深拷贝。</p>\\n</li>\\n<li>\\n<p><strong>一个公共函数原来通过 <code>arguments</code> 接收不定数量参数，团队准备改成剩余参数；调用方还大量使用 <code>apply</code>，怎样迁移更清晰？</strong></p>\\n<p>函数定义可改为 <code>function fn(...args)</code>，此时 <code>args</code> 是真正的数组，可直接使用 <code>map</code>、解构等数组能力。调用处可由 <code>fn.apply(ctx, values)</code> 改为 <code>fn.call(ctx, ...values)</code>，若不依赖 <code>this</code> 则直接 <code>fn(...values)</code>；迁移前需确认原函数是否读取 <code>arguments.callee</code> 等旧行为。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_6-举一些es6对number数字类型做的常用升级优化\\"><a class=\\"header-anchor\\" href=\\"#_6-举一些es6对number数字类型做的常用升级优化\\">#</a> 6 举一些ES6对Number数字类型做的常用升级优化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Number.isNaN</code> / <code>Number.isFinite</code>：不做隐式转换，比全局的 <code>isNaN(\'abc\') === true</code> 靠谱得多</li>\\n<li><code>Number.isInteger</code>、<code>Number.EPSILON</code>（判断浮点相等的容差）、<code>Number.MAX_SAFE_INTEGER</code></li>\\n<li><code>Math</code> 新增 <code>trunc</code>（直接截断小数）、<code>sign</code>、<code>cbrt</code>、<code>hypot</code> 等</li>\\n<li>二进制 <code>0b</code>、八进制 <code>0o</code> 字面量；数字分隔符 <code>1_000_000</code>（ES2021）提升可读性</li>\\n<li><code>BigInt</code>（ES2020）解决超过 <code>2^53-1</code> 的整数精度问题 —— 后端返回的雪花 <code>ID</code> 必须用字符串或 <code>BigInt</code> 接</li>\\n</ul>\\n<p><strong>数字类型的常用升级主要是更严格的数值判断，以及补充常见的数学运算能力。</strong> <code>Number.isNaN()</code> 和 <code>Number.isFinite()</code> 不会先做隐式类型转换，因此比全局同名方法更符合直觉，例如 <code>Number.isNaN(\'NaN\')</code> 返回 <code>false</code>。<code>Math</code> 也增加了 <code>cbrt()</code>、<code>trunc()</code>、<code>hypot()</code> 等方法，可以直接完成立方根、小数截断等计算。工程里我会优先使用这些明确的 <code>API</code>，避免类型转换让判断结果变得难以预料。</p>\\n</section>\\n<p><strong>优化部分</strong></p>\\n<blockquote>\\n<p>ES6在<code>Number</code>原型上新增了<code>isFinite()</code>, <code>isNaN()</code>方法，用来取代传统的全局<code>isFinite(),</code> <code>isNaN()</code>方法检测数值是否有限、是否是<code>NaN</code>。<code>ES5</code>的<code>isFinite()</code>, <code>isNaN()</code>方法都会先将非数值类型的参数转化为<code>Number</code>类型再做判断，这其实是不合理的，最造成i<code>sNaN(\'NaN\') === true</code>的奇怪行为<code>--\'NaN\'</code>是一个字符串，但是<code>isNaN</code>却说这就是<code>NaN</code>。而<code>Number.isFinite()</code>和<code>Number.isNaN()</code>则不会有此类问题(<code>Number.isNaN(\'NaN\') === false</code>)。（<code>isFinite()</code>同上）</p>\\n</blockquote>\\n<p><strong>升级部分</strong></p>\\n<blockquote>\\n<p><code>ES6</code>在<code>Math</code>对象上新增了<code>Math.cbrt()</code>，<code>trunc()</code>，<code>hypot()</code>等等较多的科学计数法运算方法，可以更加全面的进行立方根、求和立方根等等科学计算</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>表单校验用全局 <code>isNaN(value)</code>，用户输入字符串 <code>\'NaN\'</code> 时返回 <code>true</code>，同事因此断言该变量已经是数值型 <code>NaN</code>；这个判断哪里不可靠？</strong></p>\\n<p>全局 <code>isNaN</code> 会先把参数转换为 <code>Number</code>，所以字符串也可能被判定为转换后的 <code>NaN</code>，不能证明原值就是数值型 <code>NaN</code>。需要严格判断时应使用 <code>Number.isNaN(value)</code>；若业务允许数字字符串，应先按明确规则解析，再分别校验解析是否成功和数值范围。</p>\\n</li>\\n<li>\\n<p><strong>监控面板收到字符串 <code>\'100\'</code> 和数值 <code>Infinity</code>，旧代码用全局 <code>isFinite</code> 判断后把前者当成有效数字；若接口要求字段必须原本就是有限数值，怎么改？</strong></p>\\n<p>应使用 <code>Number.isFinite(value)</code>，它不会先做类型转换，因此 <code>\'100\'</code> 会返回 <code>false</code>，<code>Infinity</code> 也会被拒绝。若接口确实允许数字字符串，应在边界层显式转换并处理空串等异常输入；把隐式转换藏在校验函数里会让数据契约变得含糊。</p>\\n</li>\\n<li>\\n<p><strong>后端把订单 <code>ID</code> 以数字 <code>1234567890123456789</code> 放进 <code>JSON</code>，详情页展示时末尾已经变化；前端改用 <code>BigInt(id)</code> 能挽回原值吗？</strong></p>\\n<p>不能，超过 <code>Number</code> 安全整数范围后的精度可能在 <code>JSON.parse</code> 时已经丢失，把错误的 <code>Number</code> 再转成 <code>BigInt</code> 只会固化错误值。可靠做法是让后端把标识符序列化为字符串；若接口暂时无法调整，只能在数值被解析前使用专门方案保留原始文本，维护成本更高。</p>\\n</li>\\n<li>\\n<p><strong>支付页直接计算 <code>0.1 + 0.2</code> 并与 <code>0.3</code> 严格比较，偶发进入金额异常分支；展示前调用 <code>toFixed(2)</code> 能彻底解决吗？</strong></p>\\n<p><code>toFixed(2)</code> 只负责格式化输出，不能消除前面计算过程的二进制浮点误差。金额应优先用最小货币单位的整数计算，或采用精确十进制库；仅做近似结果比较时可设置与业务量级相符的容差，机械使用 <code>Number.EPSILON</code> 未必覆盖累计误差。</p>\\n</li>\\n<li>\\n<p><strong>数据团队要在浏览器里计算一批数值的立方根并截去小数，开发者准备手写幂运算和字符串截断；<code>ES6</code> 的 <code>Math</code> 能力怎样选，负数和取整语义要注意什么？</strong></p>\\n<p>立方根可使用 <code>Math.cbrt(value)</code>，截去小数部分可使用 <code>Math.trunc(value)</code>，比把数字转成字符串再切割更直接。<code>Math.trunc</code> 是向零截断，因此负数结果与 <code>Math.floor</code> 不同；输入仍应先通过严格数值和有限性校验，否则 <code>NaN</code> 或无限值会继续传播。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_7-举一些es6对object类型做的常用升级优化\\"><a class=\\"header-anchor\\" href=\\"#_7-举一些es6对object类型做的常用升级优化\\">#</a> 7 举一些ES6对Object类型做的常用升级优化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>简写：属性简写 <code>{ name }</code>、方法简写 <code>{ fn() {} }</code>、计算属性名 <code>{ [key]: v }</code></li>\\n<li>静态方法：<code>Object.assign</code>（浅合并）、<code>keys/values/entries</code>、<code>fromEntries</code>（ES2019，和 <code>entries</code> 互逆）</li>\\n<li>解构 + 默认值 + 重命名 + 剩余参数，是&quot;取几个字段、其余打包&quot;的标准写法</li>\\n<li>对象展开 <code>{...obj}</code>（ES2018）是<strong>浅拷贝</strong>，嵌套对象仍指向同一引用</li>\\n<li>⚠️ 合并外部数据要防原型链污染：过滤 <code>__proto__</code>/<code>constructor</code>，或用 <code>Object.create(null)</code> 接</li>\\n</ul>\\n<p><strong>对象相关的升级，本质上分为更简洁的写法和更完整的对象操作能力。</strong> 属性简写、方法简写、解构和扩展运算符，让对象的拆分与合并更直观；<code>Object.is()</code> 则补足了 <code>===</code> 在 <code>NaN</code> 比较上的特殊情况。<code>Object.assign()</code> 适合合并对象，但它只处理自身可枚举属性，而且会读取访问器的返回值，不能完整复制属性描述符。需要保留 <code>get</code>、<code>set</code> 等描述信息时，应结合 <code>Object.getOwnPropertyDescriptors()</code> 和 <code>Object.defineProperties()</code>。</p>\\n</section>\\n<p><strong>优化部分</strong></p>\\n<blockquote>\\n<p>对象属性变量式声明。<code>ES6</code>可以直接以变量形式声明对象属性或者方法，。比传统的键值对形式声明更加简洁，更加方便，语义更加清晰</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>let [apple, orange] = [&#39;red appe&#39;, &#39;yellow orange&#39;];\\nlet myFruits = {apple, orange};    // let myFruits = {apple: &#39;red appe&#39;, orange: &#39;yellow orange&#39;};\\n</code></pre>\\n<blockquote>\\n<p>尤其在对象解构赋值(见优化部分b.)或者模块输出变量时，这种写法的好处体现的最为明显</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>let {keys, values, entries} = Object;\\nlet MyOwnMethods = {keys, values, entries}; // let MyOwnMethods = {keys: keys, values: values, entries: entries}\\n</code></pre>\\n<p>可以看到属性变量式声明属性看起来更加简洁明了。方法也可以采用简洁写法</p>\\n<pre class=\\"language-js\\"><code>let es5Fun = {\\n    method: function(){}\\n}; \\nlet es6Fun = {\\n    method(){}\\n}\\n</code></pre>\\n<blockquote>\\n<p>对象的解构赋值。 <code>ES6</code>对象也可以像数组解构赋值那样，进行变量的解构赋值</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>let {apple, orange} = {apple: &#39;red appe&#39;, orange: &#39;yellow orange&#39;};\\n</code></pre>\\n<blockquote>\\n<p>对象的扩展运算符(<code>...</code>)。 ES6对象的扩展运算符和数组扩展运算符用法本质上差别不大，毕竟数组也就是特殊的对象。对象的扩展运算符一个最常用也最好用的用处就在于可以轻松的取出一个目标对象内部全部或者部分的可遍历属性，从而进行对象的合并和分解</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>let {apple, orange, ...otherFruits} = {apple: &#39;red apple&#39;, orange: &#39;yellow orange&#39;, grape: &#39;purple grape&#39;, peach: &#39;sweet peach&#39;}; \\n// otherFruits  {grape: &#39;purple grape&#39;, peach: &#39;sweet peach&#39;}\\n// 注意: 对象的扩展运算符用在解构赋值时，扩展运算符只能用在最有一个参数(otherFruits后面不能再跟其他参数)\\nlet moreFruits = {watermelon: &#39;nice watermelon&#39;};\\nlet allFruits = {apple, orange, ...otherFruits, ...moreFruits};\\n</code></pre>\\n<blockquote>\\n<p><code>super</code> 关键字。<code>ES6</code>在<code>Class</code>类里新增了类似<code>this</code>的关键字<code>super</code>。同<code>this</code>总是指向当前函数所在的对象不同，<code>super</code>关键字总是指向当前函数所在对象的原型对象</p>\\n</blockquote>\\n<p><strong>升级部分</strong></p>\\n<blockquote>\\n<p><code>ES6</code>在<code>Object</code>原型上新增了<code>is()</code>方法，做两个目标对象的相等比较，用来完善<code>\'===\'</code>方法。<code>\'===\'</code>方法中<code>NaN === NaN //false</code>其实是不合理的，<code>Object.is</code>修复了这个小<code>bug</code>。<code>(Object.is(NaN, NaN) // true)</code></p>\\n</blockquote>\\n<blockquote>\\n<p><code>ES6</code>在<code>Object</code>原型上新增了<code>assign()</code>方法，用于对象新增属性或者多个对象合并</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const target = { a: 1 };\\nconst source1 = { b: 2 };\\nconst source2 = { c: 3 };\\nObject.assign(target, source1, source2);\\ntarget // {a:1, b:2, c:3}\\n</code></pre>\\n<blockquote>\\n<p><strong>注意</strong>: <code>assign</code>合并的对象<code>target</code>只能合并<code>source1</code>、s<code>ource2</code>中的自身属性，并不会合并<code>source1</code>、<code>source2</code>中的继承属性，也不会合并不可枚举的属性，且无法正确复制get和set属性（会直接执行<code>get/set</code>函数，取<code>return</code>的值）</p>\\n</blockquote>\\n<ul>\\n<li><code>ES6</code>在<code>Object</code>原型上新增了<code>getOwnPropertyDescriptors()</code>方法，此方法增强了<code>ES5</code>中<code>getOwnPropertyDescriptor()</code>方法，可以获取指定对象所有自身属性的描述对象。结合<code>defineProperties()</code>方法，可以完美复制对象，包括复制<code>get</code>和<code>set</code>属性</li>\\n<li><code>ES6</code>在<code>Object</code>原型上新增了<code>getPrototypeOf()</code>和<code>setPrototypeOf()</code>方法，用来获取或设置当前对象的<code>prototype</code>对象。这个方法存在的意义在于，<code>ES5</code>中获取设置<code>prototype</code>对像是通过<code>__proto__</code>属性来实现的，然而<code>__proto__</code>属性并不是ES规范中的明文规定的属性，只是浏览器各大产商“私自”加上去的属性，只不过因为适用范围广而被默认使用了，再非浏览器环境中并不一定就可以使用，所以为了稳妥起见，获取或设置当前对象的<code>prototype</code>对象时，都应该采用ES6新增的标准用法</li>\\n<li><code>ES6</code>在<code>Object</code>原型上还新增了<code>Object.keys()</code>，<code>Object.values()</code>，<code>Object.entries()</code>方法，用来获取对象的所有键、所有值和所有键值对数组</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>价格校验代码用 <code>price === NaN</code> 判断接口脏数据，线上却从未进入异常分支；换成 <code>Object.is</code> 后为什么能命中，<code>0</code> 和 <code>-0</code> 又会怎样？</strong></p>\\n<p><code>NaN</code> 与自身做 <code>===</code> 仍是 <code>false</code>，因此原判断永远无法命中，应该使用 <code>Object.is(price, NaN)</code> 或 <code>Number.isNaN(price)</code>。<code>Object.is(0, -0)</code> 为 <code>false</code>，而 <code>0 === -0</code> 为 <code>true</code>，所以不能把两种相等规则不加区分地全局替换。</p>\\n</li>\\n<li>\\n<p><strong>状态仓库用 <code>Object.assign(state, patch)</code> 合并接口结果，订阅方依赖引用变化却没有更新；你会怎样改这段代码？</strong></p>\\n<p><code>Object.assign</code> 会修改第一个参数，因此这里仍返回原来的 <code>state</code> 引用，依赖浅比较的订阅方可能认为状态未变。可改为 <code>Object.assign({}, state, patch)</code> 或 <code>{...state, ...patch}</code> 创建新对象，但两者都只是浅合并，嵌套字段仍需逐层复制。</p>\\n</li>\\n<li>\\n<p><strong>组件配置对象包含继承字段、不可枚举字段和一个 <code>get token()</code>，同事用 <code>{...config}</code> 做完整克隆；哪些内容会丢失或发生变化？</strong></p>\\n<p>对象展开只处理自身可枚举属性，不会复制继承属性和不可枚举属性；访问器还会在读取时执行，结果对象得到的是当时的值，而非原来的描述符。若确实要保留访问器及属性特征，应结合 <code>Object.getOwnPropertyDescriptors()</code> 与 <code>Object.defineProperties()</code>，但原型仍要单独处理。</p>\\n</li>\\n<li>\\n<p><strong>接口返回 <code>{user:{profile:{name:\'A\'}}}</code>，页面用 <code>{...data}</code> 备份后修改 <code>backup.user.profile.name</code>，原数据也被改了；你怎么定位并修复？</strong></p>\\n<p>对象展开只复制顶层，自身可枚举的 <code>user</code> 仍与原对象指向同一引用，所以修改第三层会同时污染原数据。应按变更路径逐层展开，或采用能生成新引用的不可变更新工具；数据结构很深时，扁平化状态能降低漏复制风险。</p>\\n</li>\\n<li>\\n<p><strong>埋点模块要从事件对象中排除 <code>userId</code>、<code>token</code>，并把其余字段与公共字段合并；你会如何写，字段冲突时谁覆盖谁？</strong></p>\\n<p>可用 <code>const {userId, token, ...rest} = event</code> 提取剩余自身可枚举属性，再写 <code>{...common, ...rest}</code> 完成合并。后出现的同名属性覆盖前面的值，且对象解构中的剩余元素必须放在最后；若字段含访问器或不可枚举属性，这种写法不适合做无损复制。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_8-举一些es6对function函数类型做的常用升级优化\\"><a class=\\"header-anchor\\" href=\\"#_8-举一些es6对function函数类型做的常用升级优化\\">#</a> 8 举一些ES6对Function函数类型做的常用升级优化?</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>默认参数 <code>function f(a = 1)</code>：取代 <code>a = a || 1</code>（后者会把 <code>0</code> 和 <code>\'\'</code> 也当成缺省）</li>\\n<li>剩余参数 <code>...args</code> 是<strong>真数组</strong>，取代类数组的 <code>arguments</code>，可直接用 <code>map</code>/<code>filter</code></li>\\n<li>箭头函数：没有自己的 <code>this</code>/<code>arguments</code>/<code>prototype</code>，不能 <code>new</code>，<code>this</code> 由定义位置决定且 <code>call</code> 改不了</li>\\n<li>函数参数支持解构 + 默认值组合：<code>function f({ page = 1, size = 10 } = {})</code>，配置对象的标准写法</li>\\n<li><code>length</code> 属性只统计<strong>第一个默认参数之前</strong>的参数个数，写通用中间件时要注意</li>\\n</ul>\\n<p><strong>函数方面最实用的升级是箭头函数、默认参数和剩余参数，它们分别简化了 <code>this</code>、缺省值与参数收集。</strong> 箭头函数没有自己的 <code>this</code> 和 <code>arguments</code>，其 <code>this</code> 取决于定义位置，所以不能作为构造函数使用，也无法通过 <code>call()</code> 改变指向。默认参数比 <code>a = a || 1</code> 更准确，因为后者会把 <code>0</code>、空字符串等有效值也当成缺省值。需要收集不定数量参数时，我一般用 <code>...args</code>，拿到的是真数组，可以直接调用数组方法。</p>\\n</section>\\n<p><strong>优化部分</strong></p>\\n<blockquote>\\n<p>箭头函数(核心)。箭头函数是ES6核心的升级项之一，箭头函数里没有自己的this,这改变了以往JS函数中最让人难以理解的this运行机制。主要优化点</p>\\n</blockquote>\\n<ul>\\n<li>箭头函数内的this指向的是函数定义时所在的对象，而不是函数执行时所在的对象。ES5函数里的this总是指向函数执行时所在的对象，这使得在很多情况下<code>this</code>的指向变得很难理解，尤其是非严格模式情况下，<code>this</code>有时候会指向全局对象，这甚至也可以归结为语言层面的bug之一。ES6的箭头函数优化了这一点，它的内部没有自己的<code>this</code>,这也就导致了<code>this</code>总是指向上一层的<code>this</code>，如果上一层还是箭头函数，则继续向上指，直到指向到有自己<code>this</code>的函数为止，并作为自己的<code>this</code></li>\\n<li>箭头函数不能用作构造函数，因为它没有自己的<code>this</code>，无法实例化</li>\\n<li>也是因为箭头函数没有自己的this,所以箭头函数 内也不存在<code>arguments</code>对象。（可以用扩展运算符代替）</li>\\n<li>函数默认赋值。<code>ES6</code>之前，函数的形参是无法给默认值得，只能在函数内部通过变通方法实现。<code>ES6</code>以更简洁更明确的方式进行函数默认赋值</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>function es6Fuc (x, y = &#39;default&#39;) {\\n    console.log(x, y);\\n}\\nes6Fuc(4) // 4, default\\n</code></pre>\\n<p><strong>升级部分</strong></p>\\n<blockquote>\\n<p>ES6新增了双冒号运算符，用来取代以往的<code>bind</code>，<code>call</code>,和<code>apply</code>。(浏览器暂不支持，<code>Babel</code>已经支持转码)</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>foo::bar;\\n// 等同于\\nbar.bind(foo);\\n\\nfoo::bar(...arguments);\\n// 等同于\\nbar.apply(foo, arguments);\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>订单页写了 <code>function load(pageSize = 20) {}</code>，调用 <code>load(null)</code> 后请求仍携带 <code>null</code>，而 <code>load(undefined)</code> 才得到 <code>20</code>；这是默认参数失效了吗？</strong></p>\\n<p>默认参数只在对应实参为 <code>undefined</code> 或没有传入时生效，<code>null</code> 是明确传入的值，因此会被保留。若业务上也把 <code>null</code> 视为缺省值，需要在函数内显式归一化，但要确认不会误伤合法的空值语义。</p>\\n</li>\\n<li>\\n<p><strong>搜索组件在实例方法里用 <code>setTimeout(() =&gt; this.fetch())</code> 能正常工作，改成 <code>setTimeout(function(){ this.fetch() })</code> 就报错；两种写法的 <code>this</code> 为什么不同？</strong></p>\\n<p>箭头函数没有自己的 <code>this</code>，会沿词法作用域捕获外层实例方法中的 <code>this</code>，因此仍能调用组件实例。普通函数的 <code>this</code> 取决于调用方式，作为定时器回调执行时不会自动绑定该实例；可保留箭头函数或显式 <code>bind(this)</code>，但需警惕捕获了错误的外层上下文。</p>\\n</li>\\n<li>\\n<p><strong>团队把对象方法统一改成 <code>getName: () =&gt; this.name</code>，用户页开始返回 <code>undefined</code>；为什么简写成 <code>getName() { return this.name }</code> 才符合预期？</strong></p>\\n<p>箭头函数中的 <code>this</code> 来自定义位置的外层作用域，不会因为 <code>obj.getName()</code> 的调用方式而指向 <code>obj</code>。方法简写创建的是有自身动态 <code>this</code> 的普通方法，调用时才能取得对象上的 <code>name</code>；若方法会被单独传递，仍可能需要绑定实例。</p>\\n</li>\\n<li>\\n<p><strong>工具函数从 <code>function collect(){ return [...arguments] }</code> 改成箭头函数后，测试发现读取到了外层函数的参数；你会怎么修？</strong></p>\\n<p>箭头函数没有自己的 <code>arguments</code>，其中的 <code>arguments</code> 会沿作用域向外查找，所以可能拿到完全无关的参数集合。应改写为 <code>const collect = (...args) =&gt; args</code>；剩余参数是真正的数组，但它只收集声明位置之后的参数。</p>\\n</li>\\n<li>\\n<p><strong>依赖注入容器用 <code>new Factory()</code> 创建实例，重构时把 <code>Factory</code> 改成箭头函数后直接抛错；这类工厂应选哪种函数形式？</strong></p>\\n<p>箭头函数没有自己的 <code>this</code>，也不能作为构造函数被 <code>new</code> 调用，因此这里必须保留普通函数或改用 <code>class</code>。如果它本质只是返回对象的工厂，可去掉 <code>new</code> 后使用箭头函数，但这会改变调用契约，并可能影响现有的实例判断。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_9-symbol是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_9-symbol是什么-有什么作用\\">#</a> 9 Symbol是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Symbol</code> 是第七种原始类型，核心特性是<strong>每次调用都返回独一无二的值</strong>，<code>Symbol(\'a\') !== Symbol(\'a\')</code></li>\\n<li>用途一：给对象加属性不怕撞名（给第三方对象挂元数据的标准做法）</li>\\n<li>用途二：模拟半私有属性 —— 不被 <code>for...in</code>/<code>Object.keys</code>/<code>JSON.stringify</code> 遍历，但 <code>getOwnPropertySymbols</code> 仍能拿到</li>\\n<li>用途三（最重要）：<strong>知名 Symbol</strong> 定制语言行为 —— <code>Symbol.iterator</code> 让对象支持 <code>for...of</code>，还有 <code>asyncIterator</code>、<code>toStringTag</code>、<code>hasInstance</code>、<code>toPrimitive</code></li>\\n<li><code>Symbol.for()</code> 走全局注册表，跨模块可复用 —— <code>React</code> 的 <code>$$typeof</code> 就是靠它 + &quot;<code>JSON</code> 表达不出 <code>Symbol</code>&quot; 来防伪造元素</li>\\n</ul>\\n<p><strong><code>Symbol</code> 是一种原始类型，每次调用 <code>Symbol()</code> 都会得到独一无二的值，最直接的作用是避免对象属性重名。</strong> 它适合给第三方对象挂元数据，因为即使描述相同，两个 <code>Symbol</code> 也不会互相覆盖。<code>Symbol</code> 属性不会出现在 <code>for...in</code>、<code>Object.keys()</code> 和 <code>JSON.stringify()</code> 中，但仍可通过 <code>Object.getOwnPropertySymbols()</code> 获取，所以只能算半私有。它还可借助 <code>Symbol.iterator</code> 等知名符号定制语言行为；跨模块需要共享同一个值时，则使用 <code>Symbol.for()</code>。</p>\\n</section>\\n<p><code>Symbol</code> 是 <code>ES6</code> 新增的<strong>第七种原始类型</strong>（另外六种：<code>undefined</code>、<code>null</code>、<code>boolean</code>、<code>number</code>、<code>string</code>、<code>bigint</code>）。</p>\\n<p>它的核心特性只有一条：<strong>每次调用 <code>Symbol()</code> 都返回一个全新的、独一无二的值</strong>，即使描述文字相同：</p>\\n<pre class=\\"language-js\\"><code>Symbol(&#39;id&#39;) === Symbol(&#39;id&#39;)   // false —— 永远不相等\\n</code></pre>\\n<p><strong>它解决什么实际问题？</strong></p>\\n<p><strong>① 给对象加属性而不怕撞名。</strong> 典型场景是给第三方对象或框架实例挂自己的元数据：</p>\\n<pre class=\\"language-js\\"><code>const CACHE = Symbol(&#39;cache&#39;)\\nelement[CACHE] = data        // 绝不会和别人的属性冲突，也不会覆盖已有的\\n</code></pre>\\n<p>用字符串 <code>element.cache = data</code> 就有风险 —— 别的库可能也在用这个名字。</p>\\n<p><strong>② 模拟私有属性（半私有）。</strong></p>\\n<p><code>Symbol</code> 属性不会被 <code>for...in</code>、<code>Object.keys()</code>、<code>JSON.stringify()</code> 遍历到：</p>\\n<pre class=\\"language-js\\"><code>const secret = Symbol(&#39;secret&#39;)\\nconst obj = { name: &#39;a&#39;, [secret]: 123 }\\n\\nObject.keys(obj)          // [&#39;name&#39;] —— 看不到 secret\\nJSON.stringify(obj)       // {&quot;name&quot;:&quot;a&quot;}\\nObject.getOwnPropertySymbols(obj)  // [Symbol(secret)] —— 但仍能拿到\\n</code></pre>\\n<p>所以它是&quot;不容易被误碰&quot;而不是&quot;真私有&quot;。真私有现在有 <code>class</code> 的 <code>#</code> 字段。</p>\\n<p><strong>③ 定制语言内置行为 —— 这才是 <code>Symbol</code> 最重要的用途。</strong></p>\\n<p>规范预定义了一批<strong>知名 Symbol</strong>，实现它们就能改变对象在语言层面的表现：</p>\\n<pre class=\\"language-js\\"><code>class Range {\\n  constructor(a, b) { this.a = a; this.b = b }\\n  // 实现迭代协议，这个对象就能被 for...of 和 [...] 使用\\n  *[Symbol.iterator]() { for (let i = this.a; i &lt;= this.b; i++) yield i }\\n  // 定制 Object.prototype.toString.call() 的结果\\n  get [Symbol.toStringTag]() { return &#39;Range&#39; }\\n}\\n;[...new Range(1, 4)]   // [1, 2, 3, 4]\\n</code></pre>\\n<p>常用的还有 <code>Symbol.asyncIterator</code>（<code>for await...of</code>）、<code>Symbol.hasInstance</code>（定制 <code>instanceof</code>）、<code>Symbol.toPrimitive</code>（定制类型转换）。</p>\\n<p><strong>④ 全局注册表 <code>Symbol.for()</code></strong></p>\\n<p><code>Symbol()</code> 每次都新建，跨模块共享同一个 <code>Symbol</code> 就要用 <code>Symbol.for()</code> —— 它在全局注册表里按 key 查找，没有才新建：</p>\\n<pre class=\\"language-js\\"><code>Symbol.for(&#39;app.id&#39;) === Symbol.for(&#39;app.id&#39;)   // true ✅\\n</code></pre>\\n<p><code>React</code> 元素上的 <code>$$typeof</code> 用的就是 <code>Symbol.for(\'react.element\')</code> —— 用全局注册表是为了让不同副本的 <code>React</code> 也能互认，同时因为 <code>Symbol</code> 无法被 <code>JSON</code> 表达，攻击者伪造不出来。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>两个插件都执行 <code>Symbol(\'cache\')</code>，宿主却想用其中一个插件的键读取另一个插件写入的数据，结果是 <code>undefined</code>；描述文字相同为什么仍不能互认？</strong></p>\\n<p>每次调用 <code>Symbol(\'cache\')</code> 都会创建全新的唯一值，描述文字只用于识别和调试，不参与相等判断。若插件确实约定共享同一键，应导出同一个 <code>Symbol</code> 实例，或使用约定良好的 <code>Symbol.for()</code>；后者会进入全局注册表，命名冲突面也随之扩大。</p>\\n</li>\\n<li>\\n<p><strong>埋点 <code>SDK</code> 要在第三方 <code>DOM</code> 节点上保存内部状态，又不能覆盖页面已有的 <code>cache</code> 字段；你会选字符串键、<code>Symbol</code> 还是私有字段？</strong></p>\\n<p>对无法修改类定义的第三方对象，可用模块内持有的 <code>Symbol</code> 作为属性键，避免与普通字符串属性或其他唯一 <code>Symbol</code> 撞名。它适合降低误碰概率，但并非真正私有，<code>Object.getOwnPropertySymbols()</code> 仍能发现；能控制类定义时，严格封装更适合使用 <code>#</code> 私有字段。</p>\\n</li>\\n<li>\\n<p><strong>审计页面用 <code>Object.keys()</code> 和 <code>JSON.stringify()</code> 导出对象，却漏掉了风控模块写入的 <code>Symbol</code> 字段；这是序列化故障还是预期行为，怎样核对？</strong></p>\\n<p>这是预期行为：<code>Symbol</code> 键不会出现在 <code>Object.keys()</code>、<code>for...in</code> 或 <code>JSON.stringify()</code> 的结果中。排查时可用 <code>Object.getOwnPropertySymbols(obj)</code> 获取这些自身符号键；若该字段必须跨网络或落库，就应显式转换为可序列化的字符串字段。</p>\\n</li>\\n<li>\\n<p><strong>自定义分页结果希望同时支持 <code>for...of</code> 和 <code>[...result]</code>，但当前只有 <code>nextPage()</code> 方法；你会接入哪个协议，失败边界是什么？</strong></p>\\n<p>应在对象上实现 <code>[Symbol.iterator]()</code>，返回符合迭代协议的迭代器，或使用生成器方法逐项产出数据，这样 <code>for...of</code> 与展开语法都能消费它。若数据获取本身是异步的，则同步迭代器不合适，应考虑 <code>Symbol.asyncIterator</code> 与 <code>for await...of</code>。</p>\\n</li>\\n<li>\\n<p><strong>微前端中两份库都要识别 <code>Symbol.for(\'app.widget\')</code> 标记的组件，升级后却有人改成 <code>Symbol(\'app.widget\')</code> 导致跨应用识别失败；你如何解释选型差异？</strong></p>\\n<p><code>Symbol.for(\'app.widget\')</code> 会按 <code>key</code> 从全局注册表复用同一个值，适合不同模块或库副本依据共同协议互认。<code>Symbol(\'app.widget\')</code> 每次都不同，只适合局部防撞名；全局注册表方案需要使用足够明确的 <code>key</code>，否则不相关代码可能意外共享标记。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_10-set是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_10-set是什么-有什么作用\\">#</a> 10 Set是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Set</code> 是成员唯一的集合，保持插入顺序，方法有 <code>add</code>/<code>has</code>/<code>delete</code>/<code>size</code>/<code>clear</code></li>\\n<li>用途一：数组去重 <code>[...new Set(arr)]</code> —— 比 <code>filter + indexOf</code> 的 <code>O(n²)</code> 快到 <code>O(n)</code></li>\\n<li>用途二：高频存在性判断，<code>set.has(id)</code> 是 <code>O(1)</code>，比 <code>arr.includes(id)</code> 的 <code>O(n)</code> 快一个数量级</li>\\n<li>判断规则是 <code>SameValueZero</code>：<code>NaN</code> 被视为相同（<code>===</code> 认为不同），但<strong>对象按引用比较，内容一样不去重</strong></li>\\n<li><code>WeakSet</code> 成员只能是对象且是弱引用，不可遍历、无 <code>size</code>，适合&quot;标记处理过&quot;而不阻止回收</li>\\n</ul>\\n<p><strong><code>Set</code> 是 <code>ES6</code> 提供的成员唯一集合，保持插入顺序，常用于去重和存在性判断。</strong> 数组可以用 <code>[...new Set(arr)]</code> 去重，高频查询则用 <code>has</code>，通常比反复调用数组的 <code>includes</code> 更合适。它按 <code>SameValueZero</code> 判断重复，因此 <code>NaN</code> 会合并，但内容相同的两个对象仍因引用不同而保留。<code>WeakSet</code> 只接收对象且不会阻止回收，适合记录某个对象是否处理过，不过不能遍历，也没有 <code>size</code>。</p>\\n</section>\\n<p><code>Set</code> 是 <code>ES6</code> 新增的集合类型，特点是<strong>成员唯一、不重复</strong>，并且<strong>保持插入顺序</strong>。</p>\\n<pre class=\\"language-js\\"><code>const s = new Set([1, 2, 2, 3])\\ns.size            // 3\\ns.add(4).add(4)   // 链式调用，重复的不生效\\ns.has(2)          // true\\ns.delete(2)\\ns.clear()\\n</code></pre>\\n<p><strong>最常见的用途 —— 数组去重：</strong></p>\\n<pre class=\\"language-js\\"><code>const unique = [...new Set(arr)]\\n</code></pre>\\n<p>比 <code>filter + indexOf</code> 快得多：后者是 <code>O(n²)</code>（每个元素都要遍历一遍数组），<code>Set</code> 基于哈希是 <code>O(n)</code>。数组一大差距非常明显。</p>\\n<p><strong>第二个高频用途 —— 高频存在性判断：</strong></p>\\n<pre class=\\"language-js\\"><code>// ❌ O(n)：每次判断都要遍历数组\\nif (allowList.includes(id)) {}\\n\\n// ✅ O(1)：哈希查找\\nconst allow = new Set(allowList)\\nif (allow.has(id)) {}\\n</code></pre>\\n<p>权限判断、已选中项、已加载 ID 这类场景，用 <code>Set</code> 比数组快一个数量级，代码也更清楚（<code>has</code> 的语义就是&quot;在不在集合里&quot;）。</p>\\n<p><strong>去重的判断规则是 <code>SameValueZero</code></strong>，和 <code>===</code> 有一处不同：</p>\\n<pre class=\\"language-js\\"><code>new Set([NaN, NaN]).size   // 1 ✅ —— NaN 被认为相同\\nnew Set([0, -0]).size      // 1\\nnew Set([{}, {}]).size     // 2 —— 对象按引用比较，内容一样也不去重\\n</code></pre>\\n<p>最后一条是最容易踩的：<strong><code>Set</code> 去不掉&quot;内容相同的对象&quot;</strong>。要按业务字段去重得自己处理：</p>\\n<pre class=\\"language-js\\"><code>const uniqueBy = (arr, key) =&gt; [...new Map(arr.map((x) =&gt; [x[key], x])).values()]\\n</code></pre>\\n<p><strong><code>WeakSet</code> 的区别</strong>：成员只能是对象，且是<strong>弱引用</strong> —— 对象没有其他引用时会被垃圾回收，<code>WeakSet</code> 里的记录自动消失。因为成员随时可能消失，所以它不可遍历、没有 <code>size</code>。典型用途是&quot;标记某个对象是否被处理过&quot;而不阻止它被回收：</p>\\n<pre class=\\"language-js\\"><code>const visited = new WeakSet()\\nfunction walk(node) {\\n  if (visited.has(node)) return   // 防重复处理\\n  visited.add(node)\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品页把两条内容相同但分别反序列化的商品对象放进 <code>new Set(list)</code>，数量仍是 <code>2</code>；为什么基础类型能去重，对象却不行？</strong></p>\\n<p><code>Set</code> 对对象按引用判断，两次反序列化会得到两个不同引用，即使字段完全相同也会保留两项。若业务唯一性由 <code>id</code> 决定，可用 <code>Map</code> 以 <code>id</code> 为键再取 <code>values()</code>；必须同时明确重复项是保留前者还是后者。</p>\\n</li>\\n<li>\\n<p><strong>权限页每次渲染都要对一批菜单项执行 <code>allowList.includes(id)</code>，团队考虑换成 <code>Set</code>；转换应放在哪里，什么情况下反而不划算？</strong></p>\\n<p>允许列表稳定且存在性判断频繁时，应先构建一次 <code>Set</code>，渲染期间使用 <code>allow.has(id)</code>，语义也更贴合集合判断。若每次只查询一两次却反复重建 <code>Set</code>，构建成本和额外存储可能抵消收益，因此应跟随源列表变化进行缓存。</p>\\n</li>\\n<li>\\n<p><strong>数据清洗要求把 <code>[NaN, NaN, 0, -0]</code> 去重，同事断言结果应有三项；按 <code>Set</code> 的比较规则实际会怎样？</strong></p>\\n<p>实际只会留下两类成员：一个 <code>NaN</code> 和一个零值，因为 <code>Set</code> 使用 <code>SameValueZero</code>，会把两个 <code>NaN</code> 视为相同，也不区分 <code>0</code> 与 <code>-0</code>。若业务必须区分正负零，就不能直接依赖 <code>Set</code>，需要自行设计键或比较规则。</p>\\n</li>\\n<li>\\n<p><strong>长时间运行的可视化页面用 <code>Set</code> 记录“已初始化”的 <code>DOM</code> 节点，节点移除后内存仍持续占用；你会优先检查什么并怎样替换？</strong></p>\\n<p>应先检查这个 <code>Set</code> 是否仍强引用已从文档移除的节点，因为只要集合持有引用，节点及其关联数据就可能无法回收。若需求只是判断某对象是否处理过，可换成 <code>WeakSet</code>；代价是它不可遍历、没有 <code>size</code>，不能承担统计或批量清理职责。</p>\\n</li>\\n<li>\\n<p><strong>图遍历代码因环引用发生无限递归，节点都是对象且遍历结束后无需保留记录；用 <code>Set</code> 还是 <code>WeakSet</code> 更贴合约束？</strong></p>\\n<p>两者都能按对象引用标记已访问节点并阻止重复递归，但遍历记录无需枚举且不应延长节点生命周期时，<code>WeakSet</code> 更贴合约束。若还要在结束后输出访问数量或访问顺序，则应使用可遍历且有 <code>size</code> 的 <code>Set</code>，并在任务结束后主动释放它。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_11-map是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_11-map是什么-有什么作用\\">#</a> 11 Map是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Map</code> 与 <code>Object</code> 的四个实质区别：<strong>键可以是任意类型</strong>（含对象/函数/NaN）、严格按插入顺序、有 <code>size</code>、没有原型键干扰</li>\\n<li>对象的键会被强制转字符串，两个不同对象当键会撞成同一个 <code>\'[object Object]\'</code></li>\\n<li>对象的整数键会被自动提前并升序排列，做有序结构（<code>LRU</code>、最近访问）必须用 <code>Map</code></li>\\n<li>用对象当字典时 <code>o[\'toString\']</code> 会拿到继承的函数 —— 要用对象当纯字典得 <code>Object.create(null)</code></li>\\n<li><code>WeakMap</code> 键为对象弱引用，键被回收时条目自动消失，适合给外部对象挂数据（<code>Vue 3</code> 的依赖收集就用它）</li>\\n</ul>\\n<p><strong><code>Map</code> 是专门存放键值对的集合，键可以是对象、函数等任意值，并严格保持插入顺序。</strong> 相比普通对象，它没有原型键干扰，可直接通过 <code>size</code> 获取数量，也更适合频繁增删或依赖顺序的结构。结构固定、需要解构或 <code>JSON</code> 序列化时，我一般仍会选普通对象；键类型复杂、要做缓存或关联外部对象时则选 <code>Map</code>。<code>WeakMap</code> 的键只能是对象且为弱引用，适合保存私有数据或依赖关系，避免阻止键对象被回收。</p>\\n</section>\\n<p><code>Map</code> 是键值对集合，相比普通对象有四个实质区别：</p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th><code>Object</code></th>\\n<th><code>Map</code></th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>键的类型</td>\\n<td>只能是 <code>string</code> 或 <code>Symbol</code></td>\\n<td><strong>任意值</strong>，包括对象、函数、<code>NaN</code></td>\\n</tr>\\n<tr>\\n<td>顺序</td>\\n<td>整数键会被排到前面并升序</td>\\n<td>严格按插入顺序</td>\\n</tr>\\n<tr>\\n<td>取大小</td>\\n<td><code>Object.keys(o).length</code></td>\\n<td><code>map.size</code></td>\\n</tr>\\n<tr>\\n<td>原型污染</td>\\n<td>会继承 <code>Object.prototype</code> 上的键</td>\\n<td>干净，没有默认键</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>① 键可以是任意类型 —— 这是最本质的差别</strong></p>\\n<pre class=\\"language-js\\"><code>const meta = new Map()\\nmeta.set(domNode, { clicks: 0 })    // 用 DOM 节点当键\\nmeta.set(fn, &#39;handler&#39;)             // 用函数当键\\n</code></pre>\\n<p>普通对象会把键强制转成字符串，两个不同的对象当键会变成同一个 <code>\'[object Object]\'</code>，直接冲突。</p>\\n<p><strong>② 顺序可预测</strong></p>\\n<pre class=\\"language-js\\"><code>const o = {}; o.b = 1; o.a = 2; o[2] = 3; o[1] = 4\\nObject.keys(o)   // [&#39;1&#39;, &#39;2&#39;, &#39;b&#39;, &#39;a&#39;] —— 整数键被提前并排序了！\\n\\nconst m = new Map([[&#39;b&#39;,1], [&#39;a&#39;,2], [2,3], [1,4]])\\n[...m.keys()]    // [&#39;b&#39;, &#39;a&#39;, 2, 1] ✅ 严格按插入顺序\\n</code></pre>\\n<p>做&quot;最近访问列表&quot;、<code>LRU</code> 缓存这类依赖顺序的结构时，这一点很关键。</p>\\n<p><strong>③ 没有原型键的干扰</strong></p>\\n<pre class=\\"language-js\\"><code>const o = {}\\no[&#39;toString&#39;]       // 拿到继承来的函数，不是 undefined！\\n&#39;constructor&#39; in o  // true —— 用对象当字典时的经典陷阱\\n\\nconst m = new Map()\\nm.get(&#39;toString&#39;)   // undefined ✅\\n</code></pre>\\n<p>用对象当字典时，如果 key 来自用户输入，<code>__proto__</code>、<code>constructor</code> 这类键会引发意外甚至安全问题。要用对象当纯字典应该 <code>Object.create(null)</code>。</p>\\n<p><strong>④ 频繁增删性能更好</strong>，因为 <code>Map</code> 就是为此设计的哈希结构，而对象在频繁 <code>delete</code> 时会退化。</p>\\n<p><strong>怎么选：</strong></p>\\n<ul>\\n<li>结构固定、要 <code>JSON</code> 序列化、要用解构 → 普通对象</li>\\n<li>键不是字符串、需要顺序、频繁增删、需要 <code>size</code> → <code>Map</code></li>\\n</ul>\\n<p><strong><code>WeakMap</code></strong>：键只能是对象且为弱引用，键对象被回收时条目自动消失。典型用途是给外部对象挂私有数据而不造成泄漏 —— 比如给 DOM 节点存关联数据、给类实例存私有字段。这也是 <code>Vue 3</code> 响应式系统用 <code>WeakMap</code> 存依赖关系的原因：组件销毁后对应的依赖表自动被回收。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>缓存模块把两个不同 <code>DOM</code> 节点当普通对象的键，最终都落在 <code>\'[object Object]\'</code> 下并互相覆盖；改成 <code>Map</code> 为什么能解决？</strong></p>\\n<p>普通对象只接受字符串或 <code>Symbol</code> 键，对象键会被强制转换成字符串，因此不同节点可能产生同一个属性名。<code>Map</code> 可以直接以节点引用为键，使用 <code>set(node, data)</code> 与 <code>get(node)</code> 不会发生这类转换；不同包装对象仍会被视为不同键。</p>\\n</li>\\n<li>\\n<p><strong>最近访问页用对象保存数字商品 <code>ID</code>，插入顺序是 <code>20、3、11</code>，渲染时却变成升序；如果还要支持把访问项移到末尾，你会怎样设计？</strong></p>\\n<p>对象的整数型属性键会优先按数值顺序排列，不能可靠表达最近访问顺序，应改用严格保持插入顺序的 <code>Map</code>。访问已有项时先 <code>delete(id)</code> 再 <code>set(id, value)</code> 可把它移到末尾；仍需配合容量上限删除最早条目，避免缓存无限增长。</p>\\n</li>\\n<li>\\n<p><strong>网关把用户输入直接当对象字典的键，遇到 <code>constructor</code> 和 <code>__proto__</code> 后行为异常；团队在 <code>Object.create(null)</code> 与 <code>Map</code> 之间该怎么选？</strong></p>\\n<p>仅需字符串键且最终要直接按对象形态序列化时，可使用 <code>Object.create(null)</code> 消除继承键干扰，并继续校验危险输入。若需要任意类型键、稳定插入顺序、频繁增删或直接读取 <code>size</code>，<code>Map</code> 更合适；但它不能被 <code>JSON.stringify()</code> 直接表达为普通键值对象。</p>\\n</li>\\n<li>\\n<p><strong>单页应用用 <code>Map&lt;HTMLElement, Meta&gt;</code> 保存节点元数据，路由切换后出现大量脱离文档的节点；你会怎样确认根因并调整结构？</strong></p>\\n<p>应检查全局 <code>Map</code> 是否仍以已移除节点为键，因为 <code>Map</code> 对键是强引用，会阻止节点及元数据被回收。若只需随节点生命周期保存附加数据，可改用 <code>WeakMap</code>；它不可遍历且没有 <code>size</code>，因此监控统计应另建不持有节点引用的结构。</p>\\n</li>\\n<li>\\n<p><strong>接口层准备把一份键值集合写入 <code>localStorage</code>，有人坚持全程使用 <code>Map</code>，另一方要求普通对象；你会怎样划分边界？</strong></p>\\n<p>运行时若存在非字符串键、依赖顺序或频繁增删，可保留 <code>Map</code>，在持久化边界显式转换为条目数组或普通对象。结构固定、以字段表达业务实体且需要直接 <code>JSON</code> 序列化时，普通对象更自然；转换时还要确认对象键是否能无损表示，否则反序列化后引用型键无法恢复。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_12-proxy是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_12-proxy是什么-有什么作用\\">#</a> 12 Proxy是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Proxy</code> 在目标对象前架一层代理，用 <strong>13 种陷阱</strong>拦截 <code>get</code>/<code>set</code>/<code>has</code>/<code>deleteProperty</code>/<code>ownKeys</code> 等底层操作</li>\\n<li>与 <code>defineProperty</code> 的本质区别：拦的是<strong>整个对象</strong>而非单个属性，所以新增属性、<code>delete</code>、数组下标赋值都能拦到</li>\\n<li>而且是<strong>惰性</strong>的 —— 访问到哪层才代理哪层，不像 <code>defineProperty</code> 要递归遍历整个对象</li>\\n<li>这正是 <code>Vue 3</code> 换掉它的原因：<code>Vue 2</code> 才需要 <code>Vue.set</code> 和重写七个数组方法；代价是 <code>Proxy</code> <strong>无法 polyfill</strong>，直接放弃 <code>IE</code></li>\\n<li>⚠️ 两个坑：代理 <code>Map</code>/<code>Set</code> 要在陷阱里 <code>bind(target)</code>；转发默认行为要用 <code>Reflect.get(t, k, receiver)</code> 保留 <code>receiver</code></li>\\n</ul>\\n<p><strong><code>Proxy</code> 是包在目标对象外的一层代理，可以拦截读取、赋值、删除和枚举等底层操作并改写行为。</strong> 它拦截的是整个对象，所以新增属性、<code>delete</code> 和数组下标变化也能处理；相比 <code>Object.defineProperty</code> 逐个劫持属性，还可以按访问路径惰性代理。这也是 <code>Vue 3</code> 用它实现响应式的原因，不过 <code>Proxy</code> 无法通过垫片兼容 <code>IE</code>。实际转发时应使用带 <code>receiver</code> 的 <code>Reflect.get</code>；代理 <code>Map</code> 或 <code>Set</code> 时，还要处理方法的 <code>this</code> 绑定。</p>\\n</section>\\n<p><code>Proxy</code> 在目标对象前面架了一层&quot;代理&quot;，你可以拦截对这个对象的<strong>底层操作</strong>并改写行为。</p>\\n<pre class=\\"language-js\\"><code>const p = new Proxy(target, {\\n  get(target, key, receiver) { /* 拦截读取 */ },\\n  set(target, key, value, receiver) { /* 拦截赋值 */ },\\n  has(target, key) { /* 拦截 in 操作符 */ },\\n  deleteProperty(target, key) { /* 拦截 delete */ },\\n  ownKeys(target) { /* 拦截 Object.keys / for...in */ },\\n})\\n</code></pre>\\n<p>一共 <strong>13 种陷阱</strong>，覆盖了对象几乎所有的底层操作，且和 <code>Reflect</code> 的 13 个方法一一对应。</p>\\n<p><strong>和 <code>Object.defineProperty</code> 的本质区别 —— 这是面试重点：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th><code>defineProperty</code></th>\\n<th><code>Proxy</code></th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>拦截粒度</td>\\n<td><strong>单个属性</strong></td>\\n<td><strong>整个对象</strong></td>\\n</tr>\\n<tr>\\n<td>新增属性</td>\\n<td>❌ 拦不到</td>\\n<td>✅</td>\\n</tr>\\n<tr>\\n<td><code>delete</code></td>\\n<td>❌</td>\\n<td>✅</td>\\n</tr>\\n<tr>\\n<td>数组下标赋值 / <code>length</code></td>\\n<td>❌</td>\\n<td>✅</td>\\n</tr>\\n<tr>\\n<td>初始化成本</td>\\n<td>递归遍历所有属性</td>\\n<td>惰性，访问到哪层才代理哪层</td>\\n</tr>\\n<tr>\\n<td>兼容性</td>\\n<td><code>IE9+</code></td>\\n<td><strong>无法 polyfill</strong></td>\\n</tr>\\n</tbody>\\n</table>\\n<p>这张表直接解释了 <code>Vue 2</code> → <code>Vue 3</code> 的取舍：<code>Vue 2</code> 用 <code>defineProperty</code> 逐个属性劫持，所以新增属性要靠 <code>Vue.set</code>、数组要重写七个方法；<code>Vue 3</code> 换 <code>Proxy</code> 之后这些补丁全部去掉了，代价是彻底放弃 <code>IE</code>。</p>\\n<p><strong>实际能做什么：</strong></p>\\n<pre class=\\"language-js\\"><code>// ① 属性不存在时抛错，而不是静默返回 undefined\\nconst strict = new Proxy(config, {\\n  get(t, k) {\\n    if (!(k in t)) throw new Error(`配置项 ${String(k)} 不存在`)\\n    return Reflect.get(t, k)\\n  },\\n})\\n\\n// ② 负数索引（像 Python 那样 arr[-1]）\\nconst arr = new Proxy([1, 2, 3], {\\n  get: (t, k) =&gt; (typeof k === &#39;string&#39; &amp;&amp; +k &lt; 0 ? t[t.length + +k] : Reflect.get(t, k)),\\n})\\n\\n// ③ 响应式：读时收集依赖、写时通知更新 —— Vue 3 的核心\\n</code></pre>\\n<p><strong>两个必须知道的坑：</strong></p>\\n<p><strong>① 代理 <code>Map</code>/<code>Set</code> 会报错。</strong> 它们的方法要访问对象的内部插槽，而 <code>get</code> 陷阱返回方法时 <code>this</code> 是代理对象，找不到插槽就抛 <code>Incompatible receiver</code>。要在陷阱里 <code>bind(target)</code>。<code>Vue 3</code> 对集合类型单独写了一套 handler 就是为此。</p>\\n<p><strong>② 转发默认行为要用 <code>Reflect</code> 并带上 <code>receiver</code>。</strong> 直接 <code>return target[key]</code> 会丢掉 <code>receiver</code>，导致 <code>getter</code> 里的 <code>this</code> 指向原对象而非代理，嵌套属性的依赖就收集不到。</p>\\n<p>下面是一个&quot;把所有属性访问都变成 Promise&quot;的例子，能直观看到 <code>Proxy</code> 的自由度：</p>\\n<blockquote>\\n<p><code>Proxy</code>是<code>ES6</code>新增的一个构造函数，可以理解为JS语言的一个代理，用来改变JS默认的一些语言行为，包括拦截默认的<code>get/set</code>等底层方法，使得JS的使用自由度更高，可以最大限度的满足开发者的需求。比如通过拦截对象的<code>get/set</code>方法，可以轻松地定制自己想要的<code>key</code>或者<code>value</code>。下面的例子可以看到，随便定义一个<code>myOwnObj</code>的<code>key</code>,都可以变成自己想要的函数`</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function createMyOwnObj() {\\n\\t//想把所有的key都变成函数，或者Promise,或者anything\\n\\treturn new Proxy({}, {\\n\\t\\tget(target, propKey, receiver) {\\n\\t\\t\\treturn new Promise((resolve, reject) =&gt; {\\n\\t\\t\\t\\tsetTimeout(() =&gt; {\\n\\t\\t\\t\\t\\tlet randomBoolean = Math.random() &gt; 0.5;\\n\\t\\t\\t\\t\\tlet Message;\\n\\t\\t\\t\\t\\tif (randomBoolean) {\\n\\t\\t\\t\\t\\t\\tMessage = `你的${propKey}运气不错，成功了`;\\n\\t\\t\\t\\t\\t\\tresolve(Message);\\n\\t\\t\\t\\t\\t} else {\\n\\t\\t\\t\\t\\t\\tMessage = `你的${propKey}运气不行，失败了`;\\n\\t\\t\\t\\t\\t\\treject(Message);\\n\\t\\t\\t\\t\\t}\\n\\t\\t\\t\\t}, 1000);\\n\\t\\t\\t});\\n\\t\\t}\\n\\t});\\n}\\n\\nlet myOwnObj = createMyOwnObj();\\n\\nmyOwnObj.hahaha.then(result =&gt; {\\n\\tconsole.log(result) //你的hahaha运气不错，成功了\\n}).catch(error =&gt; {\\n\\tconsole.log(error) //你的hahaha运气不行，失败了\\n})\\n\\nmyOwnObj.wuwuwu.then(result =&gt; {\\n\\tconsole.log(result) //你的wuwuwu运气不错，成功了\\n}).catch(error =&gt; {\\n\\tconsole.log(error) //你的wuwuwu运气不行，失败了\\n})\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>配置页用 <code>Proxy</code> 做严格校验，读取不存在的字段就抛错；同事却说用 <code>Object.defineProperty</code> 也能实现，两者在动态新增配置项时真的等价吗？</strong></p>\\n<p>两者在已知属性的读取校验上都能工作，但动态结构下并不等价。<code>Object.defineProperty</code> 只能逐个劫持已有属性，新增字段必须再次定义；<code>Proxy</code> 拦截整个对象，可统一处理 <code>get</code>、<code>set</code>、<code>deleteProperty</code> 等操作，代价是无法为旧环境完整 <code>polyfill</code>。</p>\\n</li>\\n<li>\\n<p><strong>表单设计器允许用户动态添加字段、删除字段和调整数组项，响应式层应该怎样利用 <code>Proxy</code>，而不是为每种操作单独打补丁？</strong></p>\\n<p>应在代理的 <code>set</code>、<code>deleteProperty</code> 等陷阱中统一记录变更，并用 <code>Reflect</code> 转发默认操作。这样新增字段、删除字段、数组下标赋值和 <code>length</code> 变化都能进入同一套通知流程；嵌套对象仍需在访问时继续包装，单次 <code>new Proxy</code> 不会自动深度代理。</p>\\n</li>\\n<li>\\n<p><strong>公司仍要支持 <code>IE</code>，但新后台又依赖动态属性响应式；技术负责人坚持全量迁移到 <code>Proxy</code>，你会怎样判断这个选型？</strong></p>\\n<p>若必须在 <code>IE</code> 中运行，就不能把完整功能只建立在 <code>Proxy</code> 上，因为它无法通过普通补丁实现等价兼容。可以保留基于 <code>Object.defineProperty</code> 的降级路径，并显式处理新增属性和数组变更；否则只能调整浏览器支持范围，不能承诺无差异兼容。</p>\\n</li>\\n<li>\\n<p><strong>线上监控显示代理对象的 <code>getter</code> 会读取另一个字段，但修改那个字段后页面不更新；<code>get</code> 陷阱里写的是 <code>return target[key]</code>，你先查哪里？</strong></p>\\n<p>先检查默认读取是否改为 <code>Reflect.get(target, key, receiver)</code>。直接访问 <code>target[key]</code> 会让访问器中的 <code>this</code> 落到原对象，后续字段读取可能绕过代理，导致依赖没有被收集；修正转发后还要确认嵌套值是否也进入了代理层。</p>\\n</li>\\n<li>\\n<p><strong>数据看板要代理一个包含大量嵌套节点的状态树，团队在“初始化时递归劫持全部属性”和“访问到哪层再代理哪层”之间争论，你倾向哪种方案？</strong></p>\\n<p>动态且层级较深的状态树更适合按访问路径惰性创建 <code>Proxy</code>，避免初始化时逐项改写全部属性。实现时通常还要缓存原对象与代理的对应关系，防止重复包装和身份混乱；收益取决于实际访问范围，不能仅凭使用 <code>Proxy</code> 就断言整体更快。</p>\\n</li>\\n<li>\\n<p><strong>订单页把 <code>Map</code> 直接包进 <code>Proxy</code> 后，读取 <code>size</code> 或调用 <code>get</code> 报 <code>Incompatible receiver</code>，为什么普通对象的处理器不能直接复用？</strong></p>\\n<p><code>Map</code> 的访问器和方法依赖实例内部插槽，而代理对象本身没有这些插槽，所以以代理作为 <code>this</code> 调用会失败。简单场景可把函数绑定到原始 <code>target</code>，完整响应式实现则要为 <code>Map</code>、<code>Set</code> 设计专用处理器；盲目绑定还可能绕开依赖追踪。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_13-reflect是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_13-reflect是什么-有什么作用\\">#</a> 13 Reflect是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Reflect</code> 是一组静态方法的容器（不能 <code>new</code>），把散落在 <code>Object</code>、<code>Function.prototype</code> 和操作符里的对象操作统一收拢</li>\\n<li>它的 13 个方法和 <code>Proxy</code> 的 13 个陷阱<strong>一一对应</strong> —— 就是为 <code>Proxy</code> 配套设计的</li>\\n<li>失败时返回 <code>false</code> 而不是抛异常，更适合写条件判断</li>\\n<li><strong>最重要的用途</strong>：在 <code>Proxy</code> 陷阱里转发默认行为，<code>Reflect.get(target, key, receiver)</code> 能保留 <code>receiver</code></li>\\n<li>丢掉 <code>receiver</code> 会让 <code>getter</code> 里的 <code>this</code> 指向原对象而非代理，嵌套对象的依赖就收集不到（<code>Vue 3</code> 靠这个细节）</li>\\n</ul>\\n<p><strong><code>Reflect</code> 是 <code>ES6</code> 提供的静态方法集合，用来统一执行对象操作，并为 <code>Proxy</code> 转发默认行为。</strong> 它不能被 <code>new</code>，其 <code>13</code> 个方法与 <code>Proxy</code> 的陷阱一一对应，把原本分散在 <code>Object</code>、函数原型和操作符中的能力收拢起来。部分操作失败时会返回 <code>false</code>，比抛异常更方便做条件判断。工程里最常见的是在代理中调用 <code>Reflect.get(target, key, receiver)</code>，这样能保留正确的 <code>receiver</code>，避免 <code>getter</code> 内部访问绕过代理。</p>\\n</section>\\n<p><code>Reflect</code> 是 <code>ES6</code> 新增的内置对象，它不是构造函数（不能 <code>new</code>），只是一组静态方法的容器。它有两个明确的设计目标：</p>\\n<p><strong>① 把散落各处的对象操作 API 收拢到一个命名空间</strong></p>\\n<pre class=\\"language-js\\"><code>// 旧：分散在 Object、Function.prototype、操作符里\\nObject.defineProperty(obj, k, desc)\\ndelete obj[k]\\n&#39;x&#39; in obj\\nfn.apply(ctx, args)\\n\\n// 新：统一到 Reflect\\nReflect.defineProperty(obj, k, desc)\\nReflect.deleteProperty(obj, k)\\nReflect.has(obj, &#39;x&#39;)\\nReflect.apply(fn, ctx, args)\\n</code></pre>\\n<p><code>Reflect</code> 的 13 个方法和 <code>Proxy</code> 的 13 个陷阱<strong>一一对应</strong>，这不是巧合 —— 它就是为 <code>Proxy</code> 配套设计的。</p>\\n<p><strong>② 让失败有更合理的返回值</strong></p>\\n<p><code>Object</code> 上的一些方法在失败时抛异常，<code>Reflect</code> 改成返回布尔值，更适合写条件判断：</p>\\n<pre class=\\"language-js\\"><code>// 抛异常，必须 try/catch\\ntry { Object.defineProperty(obj, &#39;x&#39;, desc) } catch (e) {}\\n\\n// 返回 false，直接判断\\nif (!Reflect.defineProperty(obj, &#39;x&#39;, desc)) { /* 处理失败 */ }\\n</code></pre>\\n<p><strong>③ 在 <code>Proxy</code> 里做&quot;默认行为&quot;的转发 —— 这是它最重要的实际用途</strong></p>\\n<p>写 <code>Proxy</code> 陷阱时，拦截之后通常还要执行原本的行为。直接操作 <code>target</code> 看起来可以，但会丢掉 <code>receiver</code>：</p>\\n<pre class=\\"language-js\\"><code>const p = new Proxy(obj, {\\n  get(target, key, receiver) {\\n    track(key)                                  // 自己的逻辑\\n    return Reflect.get(target, key, receiver)   // ✅ 正确转发，保留 receiver\\n    // return target[key]                       // ❌ 丢了 receiver\\n  },\\n})\\n</code></pre>\\n<p><strong><code>receiver</code> 为什么重要？</strong> 当对象上有 <code>getter</code> 且存在原型链继承时，<code>getter</code> 内部的 <code>this</code> 应该指向<strong>最初被访问的那个对象</strong>（即代理），而不是原型。用 <code>target[key]</code> 会让 <code>this</code> 指向 <code>target</code>，导致 <code>getter</code> 里访问的其他属性绕过了代理、依赖收集不到。<code>Vue 3</code> 的响应式系统里这个细节直接影响嵌套对象的响应性是否正确。</p>\\n<p><strong>④ 保留被改写前的原生行为</strong></p>\\n<p>如果有人改写了 <code>Object.defineProperty</code> 之类的方法，<code>Reflect</code> 上的版本仍是原生实现，可以作为可靠的备份。</p>\\n<p><strong>总结成一句</strong>：<code>Reflect</code> 单独用的场景不多，它的价值几乎全部体现在<strong>和 <code>Proxy</code> 配对使用</strong>时 —— 一个负责拦截，一个负责把默认行为原样转发下去。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>权限页的代理只想记录读取日志，同事在 <code>get</code> 里直接返回 <code>target[key]</code>，并认为这与 <code>Reflect.get(target, key, receiver)</code> 完全相同；遇到原型上的 <code>getter</code> 时哪里会出错？</strong></p>\\n<p>存在访问器或原型链时，两种写法并不等价。<code>Reflect.get</code> 可以保留最初接收访问的 <code>receiver</code>，让 <code>getter</code> 内的 <code>this</code> 指向代理；直接读取 <code>target[key]</code> 可能绕过后续陷阱，使日志、校验或响应式依赖缺失。</p>\\n</li>\\n<li>\\n<p><strong>组件库需要统一封装属性读取、删除、函数调用和属性定义，代码评审中有人主张继续混用操作符与 <code>Object</code> <code>API</code>，使用 <code>Reflect</code> 有什么工程价值？</strong></p>\\n<p><code>Reflect</code> 把这些底层操作收拢为一致的函数式接口，例如 <code>Reflect.has</code>、<code>Reflect.deleteProperty</code>、<code>Reflect.apply</code> 和 <code>Reflect.defineProperty</code>。它们又与 <code>Proxy</code> 陷阱对应，便于拦截后转发原行为；若项目没有元编程需求，单纯替换语法带来的收益会比较有限。</p>\\n</li>\\n<li>\\n<p><strong>低代码平台要给不可扩展对象动态定义字段，调用方不希望每次都写 <code>try/catch</code>；选 <code>Object.defineProperty</code> 还是 <code>Reflect.defineProperty</code>？</strong></p>\\n<p>更适合使用 <code>Reflect.defineProperty</code>，因为定义失败时返回布尔值，调用方可以直接进入失败分支。<code>Object.defineProperty</code> 的失败通常通过异常表达，更适合把失败视为程序错误的场景；无论选哪种，都不能绕过对象描述符和可扩展性约束。</p>\\n</li>\\n<li>\\n<p><strong>线上响应式页面中，读取计算属性后没有订阅到它内部访问的基础字段，代理陷阱已执行但更新仍丢失；你会怎样沿着 <code>Reflect</code> 的参数排查？</strong></p>\\n<p>先确认陷阱调用的是 <code>Reflect.get(target, key, receiver)</code>，而不是漏传第三个参数或直接读取目标对象。再检查计算属性内部的 <code>this</code> 是否确实为代理，以及返回的嵌套对象是否继续代理；<code>Reflect</code> 只负责正确转发，不会自动完成依赖收集。</p>\\n</li>\\n<li>\\n<p><strong>安全审计工具需要列出对象的不可枚举字段和 <code>Symbol</code> 键，同事准备用 <code>Object.keys</code>；为什么这里更适合 <code>Reflect.ownKeys</code>？</strong></p>\\n<p><code>Reflect.ownKeys</code> 会返回对象全部自有键，包括不可枚举字符串键和 <code>Symbol</code> 键，而 <code>Object.keys</code> 只返回自有、可枚举的字符串键。审计或元数据工具因此更不容易漏项；它仍不会沿原型链查找，且读取这些键对应的值可能触发访问器。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_14-promise是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_14-promise是什么-有什么作用\\">#</a> 14 Promise是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Promise</code> 表示异步操作的最终结果，三种状态 <code>pending</code>/<code>fulfilled</code>/<code>rejected</code>，<strong>一旦改变不可逆</strong></li>\\n<li>解决三件事：回调地狱变链式、错误统一由 <code>catch</code> 接、把&quot;控制权&quot;从第三方回调手里拿回来</li>\\n<li>⚠️ <code>executor</code> 是<strong>同步执行</strong>的，只有 <code>then</code> 的回调进微任务队列</li>\\n<li><code>then</code> 返回新 <code>Promise</code> 所以能链式；<code>catch</code> 之后链条会恢复成 fulfilled，常用来做降级</li>\\n<li>四个组合方法：<code>all</code>（一失败就短路）、<code>allSettled</code>（不短路）、<code>race</code>（先落定者胜）、<code>any</code>（先成功者胜）</li>\\n<li>它<strong>不能取消</strong> —— <code>race</code> 做超时只是不再关心结果，真要中断请求得配 <code>AbortController</code></li>\\n</ul>\\n<p><strong><code>Promise</code> 是表示异步操作最终结果的对象，作用是把嵌套回调改成可组合的链式流程，并统一处理错误。</strong> 它只有 <code>pending</code>、<code>fulfilled</code>、<code>rejected</code> 三种状态，状态一旦落定就不可逆；<code>then</code> 会返回新的 <code>Promise</code>，所以可以继续串联异步步骤。需要注意，<code>executor</code> 会同步执行，只有 <code>then</code> 的回调进入微任务队列，而 <code>catch</code> 返回后链条通常会恢复为成功状态，可用于降级。它本身不能取消，<code>race</code> 实现超时也不会中断底层请求，真正终止请求要配合 <code>AbortController</code>，同时还应兜底处理未捕获的拒绝。</p>\\n</section>\\n<p><code>Promise</code> 是一个<strong>表示异步操作最终结果</strong>的对象。它有三种状态，且<strong>状态一旦改变就不可逆</strong>：</p>\\n<pre class=\\"language-text\\"><code>pending（进行中） ──→ fulfilled（已成功）\\n                 └──→ rejected（已失败）\\n</code></pre>\\n<p><strong>它解决了回调函数的三个问题：</strong></p>\\n<p><strong>① 回调地狱 —— 嵌套变成链式</strong></p>\\n<pre class=\\"language-js\\"><code>// 旧：层层嵌套，错误处理散落各处\\ngetUser(id, (u) =&gt; getOrders(u, (o) =&gt; getDetail(o[0], (d) =&gt; render(d))))\\n\\n// 新：扁平的链式调用\\ngetUser(id).then(getOrders).then((o) =&gt; getDetail(o[0])).then(render).catch(onError)\\n</code></pre>\\n<p><strong>② 错误处理统一</strong> —— 链上任何一环出错都会跳到最近的 <code>catch</code>，不用每层都判断 <code>if (err)</code>。</p>\\n<p><strong>③ 控制反转问题</strong> —— 传统回调把函数交给第三方库，你无法保证它只调一次、不吞掉异常。<code>Promise</code> 的状态由创建方掌握，消费方只是订阅结果，语义更可靠。</p>\\n<p><strong>基本用法：</strong></p>\\n<pre class=\\"language-js\\"><code>const p = new Promise((resolve, reject) =&gt; {\\n  setTimeout(() =&gt; resolve(&#39;done&#39;), 1000)     // 或 reject(new Error(&#39;failed&#39;))\\n})\\np.then(onFulfilled, onRejected)\\n .catch(onError)      // 等价于 .then(null, onError)\\n .finally(cleanup)    // 无论成败都执行，不接收参数\\n</code></pre>\\n<p><strong>几个必须知道的行为：</strong></p>\\n<ul>\\n<li><strong><code>executor</code> 是同步执行的</strong>，<code>new Promise(fn)</code> 时 <code>fn</code> 立即跑，只有 <code>then</code> 的回调才是异步的（微任务）。</li>\\n<li><strong><code>then</code> 返回新的 <code>Promise</code></strong>，所以能链式调用；回调里 <code>return</code> 一个值会被包成 resolved 状态传给下一环，<code>return</code> 一个 <code>Promise</code> 则会等它落定。</li>\\n<li><strong><code>catch</code> 之后链条会&quot;恢复&quot;</strong>：<code>catch</code> 返回的仍是 fulfilled 的 <code>Promise</code>，后面的 <code>then</code> 照样执行 —— 这常被用来做降级。</li>\\n</ul>\\n<p><strong>四个静态组合方法（高频考点）：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>方法</th>\\n<th>行为</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>all</code></td>\\n<td>全部成功才成功；<strong>任一失败立即失败</strong>（短路）</td>\\n</tr>\\n<tr>\\n<td><code>allSettled</code></td>\\n<td>等全部落定，返回每一项的状态，不短路</td>\\n</tr>\\n<tr>\\n<td><code>race</code></td>\\n<td>第一个落定的决定结果（无论成败）</td>\\n</tr>\\n<tr>\\n<td><code>any</code></td>\\n<td>第一个<strong>成功</strong>的决定结果；全失败才失败</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>它没有解决的问题：</strong></p>\\n<ul>\\n<li><strong>不能取消</strong>。<code>Promise</code> 一旦创建就会跑到底，<code>race</code> 出来的&quot;超时&quot;只是不再关心结果，底层请求还在。要真正中断得配 <code>AbortController</code>。</li>\\n<li><strong>不处理未捕获的拒绝</strong>。没有 <code>catch</code> 的 rejected <code>Promise</code> 会触发 <code>unhandledrejection</code>，在业务里表现为&quot;loading 一直转但没报错&quot;，所以要有全局监听兜底。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>支付页里 <code>new Promise</code> 的执行器先打印日志，随后注册的 <code>.then</code> 再打印；新人说执行器本身也会排进微任务，控制台顺序应该怎样判断？</strong></p>\\n<p>执行器会在创建 <code>Promise</code> 时同步运行，只有已经注册并等待状态结果的 <code>.then</code> 回调进入微任务队列。因此执行器中的日志先出现，当前同步代码结束后才运行 <code>.then</code>；把重计算放进执行器也不会自动让它脱离主线程。</p>\\n</li>\\n<li>\\n<p><strong>附件页要上传 50 个文件，产品要求成功项立即保留、失败项可单独重试，直接使用 <code>Promise.all</code> 为什么不合适？</strong></p>\\n<p><code>Promise.all</code> 会在任一任务拒绝时立即返回拒绝结果，不适合汇总每个文件的最终状态。应使用 <code>Promise.allSettled</code> 收集各项的 <code>status</code> 与结果，再筛选失败项重试；它只改变结果聚合方式，并不会自动限制并发或取消仍在执行的上传。</p>\\n</li>\\n<li>\\n<p><strong>搜索页用 <code>Promise.race([request, timeout])</code> 做超时，界面已经提示失败，抓包却显示请求仍在传输；需求改成超时后必须停止流量，怎么处理？</strong></p>\\n<p><code>Promise.race</code> 只让最先落定的结果决定外层状态，不会终止落后的异步任务。请求必须接入 <code>AbortController</code>，超时时调用 <code>abort()</code>，并把中止异常纳入清理逻辑；如果底层 <code>API</code> 不支持取消，就只能忽略结果，无法保证停止实际工作。</p>\\n</li>\\n<li>\\n<p><strong>旧版地图 <code>SDK</code> 用 <code>setTimeout</code> 回调结果，你把它包进 <code>Promise</code> 后，回调里的 <code>throw new Error()</code> 没有进入 <code>.catch</code>，线上只出现全局异常；原因是什么？</strong></p>\\n<p>异步回调执行时已经离开 <code>Promise</code> 执行器的同步调用栈，所以其中直接抛出的错误不会自动转成该 <code>Promise</code> 的拒绝。应在回调内用 <code>try/catch</code> 捕获并显式调用 <code>reject(error)</code>；执行器同步抛错和 <code>.then</code> 回调抛错才会沿链传播。</p>\\n</li>\\n<li>\\n<p><strong>商品页在 <code>.catch</code> 中展示兜底数据，后面的 <code>.then</code> 却继续提交埋点，同事认为链条失败后不该再执行；这是故障还是正常语义？</strong></p>\\n<p>这是正常语义，因为 <code>.catch</code> 若正常返回值，会生成一个已兑现的新 <code>Promise</code>，后续 <code>.then</code> 会继续执行。若希望失败保持传播，应在记录或清理后重新 <code>throw</code> 原错误；使用兜底值则意味着链条已经恢复，后续逻辑必须按降级成功处理。</p>\\n</li>\\n<li>\\n<p><strong>三个镜像接口只要任意一个成功就展示数据，但失败最快的接口不该让页面失败；你会选 <code>race</code> 还是 <code>any</code>？</strong></p>\\n<p>应选 <code>Promise.any</code>，它忽略单个拒绝，由第一个成功结果决定外层状态，只有全部失败才拒绝。<code>Promise.race</code> 由第一个落定的任务决定结果，最快的失败也会直接结束竞争；两者都不会取消其余请求，仍需额外处理中止与资源清理。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_15-iterator是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_15-iterator是什么-有什么作用\\">#</a> 15 Iterator是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>迭代器是一个<strong>协议</strong>：对象有 <code>[Symbol.iterator]()</code> 方法、返回带 <code>next()</code> 的对象、<code>next()</code> 返回 <code>{ value, done }</code></li>\\n<li>实现了它，对象就能用 <code>for...of</code>、扩展运算符 <code>[...x]</code>、解构赋值、<code>Array.from</code>、<code>yield*</code></li>\\n<li>原生可迭代的有：<code>Array</code>、<code>String</code>、<code>Map</code>、<code>Set</code>、<code>arguments</code>、<code>NodeList</code> —— <strong>普通对象不可迭代</strong></li>\\n<li>用 <code>generator</code> 实现最省事：<code>*[Symbol.iterator]() { yield* this.items }</code></li>\\n<li>它的工程价值是<strong>统一了&quot;怎么遍历&quot;这件事</strong>，使用方不需要知道内部是数组、树还是分页数据</li>\\n</ul>\\n<p><strong><code>Iterator</code> 是一套统一遍历数据的协议，用来让不同类型按相同方式依次提供值。</strong> 对象实现 <code>[Symbol.iterator]()</code>，返回带 <code>next()</code> 的遍历器，而每次调用 <code>next()</code> 都会得到包含 <code>value</code> 和 <code>done</code> 的结果。这样 <code>Array</code>、<code>String</code>、<code>Set</code>、<code>Map</code> 等结构都能被 <code>for...of</code> 消费，调用方不必关心内部数据如何存储。普通对象默认不可迭代；自定义结构需要实现这套接口，也可以借助 <code>Generator</code> 简化实现。</p>\\n</section>\\n<ul>\\n<li><code>Iterator</code>是<code>ES6</code>中一个很重要概念，它并不是对象，也不是任何一种数据类型。因为<code>ES6</code>新增了<code>Set</code>、<code>Map</code>类型，他们和<code>Array</code>、<code>Object</code>类型很像，<code>Array</code>、<code>Object</code>都是可以遍历的，但是<code>Set</code>、<code>Map</code>都不能用<code>for</code>循环遍历，解决这个问题有两种方案，一种是为<code>Set</code>、<code>Map</code>单独新增一个用来遍历的<code>API</code>，另一种是为<code>Set</code>、<code>Map</code>、<code>Array</code>、<code>Object</code>新增一个统一的遍历<code>API</code>，显然，第二种更好，<code>ES6</code>也就顺其自然的需要一种设计标准，来统一所有可遍历类型的遍历方式。<code>Iterator</code>正是这样一种标准。或者说是一种规范理念</li>\\n<li>就好像<code>JavaScript</code>是<code>ECMAScript</code>标准的一种具体实现一样，<code>Iterator</code>标准的具体实现是<code>Iterator</code>遍历器。<code>Iterator</code>标准规定，所有部署了<code>key</code>值为<code>[Symbol.iterator]</code>，且<code>[Symbol.iterator]</code>的<code>value</code>是标准的<code>Iterator</code>接口函数(标准的<code>Iterator</code>接口函数: 该函数必须返回一个对象，且对象中包含<code>next</code>方法，且执行<code>next()</code>能返回包含<code>value/done</code>属性的<code>Iterator</code>对象)的对象，都称之为可遍历对象，<code>next()</code>后返回的<code>Iterator</code>对象也就是<code>Iterator</code>遍历器</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>//obj就是可遍历的，因为它遵循了Iterator标准，且包含[Symbol.iterator]方法，方法函数也符合标准的Iterator接口规范。\\n//obj.[Symbol.iterator]() 就是Iterator遍历器\\nlet obj = {\\n  data: [ &#39;hello&#39;, &#39;world&#39; ],\\n  [Symbol.iterator]() {\\n    const self = this;\\n    let index = 0;\\n    return {\\n      next() {\\n        if (index &lt; self.data.length) {\\n          return {\\n            value: self.data[index++],\\n            done: false\\n          };\\n        } else {\\n          return { value: undefined, done: true };\\n        }\\n      }\\n    };\\n  }\\n};\\n</code></pre>\\n<blockquote>\\n<p><code>ES6</code>给<code>Set</code>、<code>Map</code>、<code>Array</code>、<code>String</code>都加上了<code>[Symbol.iterator]</code>方法，且<code>[Symbol.iterator]</code>方法函数也符合标准的<code>Iterator</code>接口规范，所以<code>Set</code>、<code>Map</code>、<code>Array</code>、<code>String</code>默认都是可以遍历的</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>//Array\\nlet array = [&#39;red&#39;, &#39;green&#39;, &#39;blue&#39;];\\narray[Symbol.iterator]() //Iterator遍历器\\narray[Symbol.iterator]().next() //{value: &quot;red&quot;, done: false}\\n\\n//String\\nlet string = &#39;1122334455&#39;;\\nstring[Symbol.iterator]() //Iterator遍历器\\nstring[Symbol.iterator]().next() //{value: &quot;1&quot;, done: false}\\n\\n//set\\nlet set = new Set([&#39;red&#39;, &#39;green&#39;, &#39;blue&#39;]);\\nset[Symbol.iterator]() //Iterator遍历器\\nset[Symbol.iterator]().next() //{value: &quot;red&quot;, done: false}\\n\\n//Map\\nlet map = new Map();\\nlet obj= {map: &#39;map&#39;};\\nmap.set(obj, &#39;mapValue&#39;);\\nmap[Symbol.iterator]().next()  {value: Array(2), done: false}\\n\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>报表接口返回 <code>{0:\'A\',1:\'B\',length:2}</code>，页面执行 <code>[...result]</code> 报 <code>is not iterable</code>，明明它能按下标读取，为什么仍不能展开？</strong></p>\\n<p>扩展运算符消费的是迭代器协议，而这个类数组对象只有索引和 <code>length</code>，没有 <code>[Symbol.iterator]</code>。可以用 <code>Array.from(result)</code> 按类数组规则转换，或显式实现迭代器；伪造 <code>length</code> 并不等于可迭代，普通对象也不会因此支持 <code>for...of</code>。</p>\\n</li>\\n<li>\\n<p><strong>分页组件内部保存 20 页数据，业务方希望直接 <code>for...of</code> 获得所有记录，不感知页结构；这个数据结构需要暴露什么接口？</strong></p>\\n<p>应实现 <code>[Symbol.iterator]()</code>，返回带有 <code>next()</code> 的迭代器，每次产出一条记录并返回 <code>{value, done}</code>。也可用生成器和 <code>yield*</code> 简化扁平遍历；同步迭代器只适合数据已在内存中的情况，不能透明等待远程下一页。</p>\\n</li>\\n<li>\\n<p><strong>日志查看器改为边拉取边渲染，每一页都要等待网络响应，原来的同步 <code>[Symbol.iterator]</code> 设计还够用吗？</strong></p>\\n<p>同步迭代器的 <code>next()</code> 不能表达等待远程结果的消费节奏，应改用 <code>Symbol.asyncIterator</code>，由 <code>for await...of</code> 逐步取得数据。实现仍需定义结束和异常传播方式；异步迭代解决的是遍历协议，不会自动提供重试、取消或并发控制。</p>\\n</li>\\n<li>\\n<p><strong>自定义集合执行两次 <code>iterator.next()</code> 后一直返回同一个值，页面 <code>for...of</code> 卡死；你会重点检查迭代器返回对象的哪些状态？</strong></p>\\n<p>先检查 <code>next()</code> 是否推进内部游标，并在数据耗尽时稳定返回 <code>done: true</code>。若始终返回 <code>done: false</code> 或没有更新索引，<code>for...of</code> 就无法结束；还要确认每次调用 <code>[Symbol.iterator]()</code> 是否按预期创建独立状态，避免多个遍历互相覆盖。</p>\\n</li>\\n<li>\\n<p><strong>设计评审中，一方要暴露 <code>getPage()</code>、<code>getItem()</code> 等多套遍历 <code>API</code>，另一方主张实现标准迭代协议；作为公共组件你怎么选？</strong></p>\\n<p>若调用方主要关心按顺序消费元素，标准迭代协议更易与 <code>for...of</code>、扩展运算符等语言能力协作，也能隐藏内部存储结构。分页跳转、随机访问等领域能力仍可保留专用 <code>API</code>；迭代器只统一顺序遍历，不能替代所有查询语义。</p>\\n</li>\\n<li>\\n<p><strong>同一个 <code>Map</code> 用 <code>for...of</code> 得到 <code>[key,value]</code>，而 <code>Set</code> 只得到单个值；这与它们的 <code>[Symbol.iterator]</code> 有什么关系？</strong></p>\\n<p><code>for...of</code> 不决定产出形状，而是按对象默认 <code>[Symbol.iterator]</code> 的结果消费。<code>Map</code> 的默认迭代器产出键值对，<code>Set</code> 的默认迭代器产出元素值；若业务需要不同视图，应显式使用 <code>keys()</code>、<code>values()</code> 或 <code>entries()</code>，不要在循环体里猜测类型。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_16-for-in-和for-of有什么区别\\"><a class=\\"header-anchor\\" href=\\"#_16-for-in-和for-of有什么区别\\">#</a> 16 for...in 和for...of有什么区别？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>for...in</code> 遍历<strong>键名</strong>、依据是可枚举属性、<strong>会爬原型链</strong>、顺序不保证</li>\\n<li><code>for...of</code> 遍历<strong>值</strong>、依据是迭代器协议、不爬原型链、顺序严格</li>\\n<li>遍历数组用 <code>for...in</code> 有三个坑：拿到字符串下标、会遍历到自定义属性和原型属性、顺序不保证</li>\\n<li>规矩：数组用 <code>for...of</code>，对象用 <code>Object.keys/entries</code>；非要用 <code>for...in</code> 就配 <code>Object.hasOwn</code> 过滤</li>\\n<li><code>for...of</code> 要下标用 <code>arr.entries()</code>；<code>for await...of</code> 用于流式/异步数据</li>\\n</ul>\\n<p><strong><code>for...in</code> 遍历的是可枚举属性的键，<code>for...of</code> 遍历的是可迭代对象按迭代器产出的值。</strong> <code>for...in</code> 还可能拿到自定义属性和原型链上的可枚举属性，而且数组下标是字符串，所以不适合直接遍历数组。数组、字符串、<code>Map</code>、<code>Set</code> 等通常用 <code>for...of</code>；普通对象可以用 <code>for...in</code>，但要配合 <code>Object.hasOwn</code> 过滤原型属性。实际开发中，我更常用 <code>Object.keys</code> 或 <code>Object.entries</code> 遍历对象，需要数组下标时则使用 <code>arr.entries()</code>。</p>\\n</section>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th><code>for...in</code></th>\\n<th><code>for...of</code></th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>遍历什么</td>\\n<td><strong>键名</strong>（key）</td>\\n<td><strong>值</strong>（value）</td>\\n</tr>\\n<tr>\\n<td>依据</td>\\n<td>对象的可枚举属性</td>\\n<td>迭代器协议</td>\\n</tr>\\n<tr>\\n<td>原型链</td>\\n<td><strong>会遍历到原型上的可枚举属性</strong></td>\\n<td>不会</td>\\n</tr>\\n<tr>\\n<td>顺序</td>\\n<td>不保证（整数键会被提前排序）</td>\\n<td>严格按迭代器定义的顺序</td>\\n</tr>\\n<tr>\\n<td>适用</td>\\n<td>普通对象</td>\\n<td>数组、字符串、<code>Map</code>、<code>Set</code>、<code>NodeList</code> 等可迭代对象</td>\\n</tr>\\n<tr>\\n<td>中断</td>\\n<td>可以 <code>break</code></td>\\n<td>可以 <code>break</code></td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong><code>for...in</code> 遍历数组的三个坑：</strong></p>\\n<pre class=\\"language-js\\"><code>const arr = [&#39;a&#39;, &#39;b&#39;]\\narr.extra = &#39;x&#39;\\nArray.prototype.custom = &#39;y&#39;      // 某个库污染了原型\\n\\nfor (const k in arr) console.log(k)   // &#39;0&#39;, &#39;1&#39;, &#39;extra&#39;, &#39;custom&#39; ❌\\nfor (const v of arr) console.log(v)   // &#39;a&#39;, &#39;b&#39; ✅\\n</code></pre>\\n<ol>\\n<li>拿到的是<strong>字符串下标</strong>（<code>\'0\'</code> 而不是 <code>0</code>），做数学运算会出错</li>\\n<li>会遍历到自定义属性和<strong>原型链上</strong>的可枚举属性</li>\\n<li>顺序不保证</li>\\n</ol>\\n<p>所以规矩很简单：<strong>遍历数组永远用 <code>for...of</code>，遍历对象用 <code>for...in</code> 或 <code>Object.keys</code></strong>。</p>\\n<p><strong><code>for...in</code> 一定要用时，配 <code>hasOwnProperty</code> 过滤原型：</strong></p>\\n<pre class=\\"language-js\\"><code>for (const k in obj) {\\n  if (!Object.hasOwn(obj, k)) continue   // ES2022，比 obj.hasOwnProperty(k) 安全\\n  // ...\\n}\\n</code></pre>\\n<p>用 <code>Object.hasOwn</code> 而不是 <code>obj.hasOwnProperty(k)</code>，因为后者在 <code>Object.create(null)</code> 创建的对象上不存在，也可能被同名属性覆盖。</p>\\n<p><strong>更推荐的写法是干脆不用 <code>for...in</code>：</strong></p>\\n<pre class=\\"language-js\\"><code>Object.keys(obj).forEach((k) =&gt; {})            // 只有自有可枚举字符串键\\nfor (const [k, v] of Object.entries(obj)) {}   // 同时拿键和值，还能 break\\n</code></pre>\\n<p><strong><code>for...of</code> 遍历数组要下标怎么办？</strong></p>\\n<pre class=\\"language-js\\"><code>for (const [i, v] of arr.entries()) console.log(i, v)\\n</code></pre>\\n<p><strong>一个补充</strong>：<code>for await...of</code> 用于异步迭代器，遍历流式数据（<code>SSE</code>、分页拉取、<code>Node</code> 的可读流）时非常顺手：</p>\\n<pre class=\\"language-js\\"><code>for await (const chunk of response.body) { render(chunk) }\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表给数组加了 <code>extra</code> 字段，某个库又给 <code>Array.prototype</code> 加了可枚举方法；为什么 <code>for...in</code> 会多遍历两项，而 <code>for...of</code> 不会？</strong></p>\\n<p><code>for...in</code> 枚举对象及其原型链上的可枚举键，所以数组的自定义属性和原型方法都可能出现，得到的下标还是字符串。<code>for...of</code> 按数组迭代器产出元素值，不枚举这些属性；因此数组数据遍历应优先使用 <code>for...of</code>。</p>\\n</li>\\n<li>\\n<p><strong>权限配置页要遍历普通对象的自有键值，并且循环中发现非法项就立刻 <code>break</code>；你会怎样写，避免原型属性混入？</strong></p>\\n<p>可以使用 <code>for (const [key, value] of Object.entries(config))</code>，它只生成自有、可枚举的字符串键值对，并允许直接 <code>break</code>。若必须用 <code>for...in</code>，应通过 <code>Object.hasOwn(config, key)</code> 过滤；两种方式都不会包含不可枚举属性和 <code>Symbol</code> 键。</p>\\n</li>\\n<li>\\n<p><strong>批处理页面要求 30 个任务严格串行，同事在 <code>forEach(async item =&gt; await run(item))</code> 外面等待完成，为什么提交函数会提前返回？</strong></p>\\n<p><code>forEach</code> 会同步调用所有回调，并忽略回调返回的 <code>Promise</code>，所以外层不会等待这些异步任务。严格串行应在 <code>async</code> 函数中使用 <code>for...of</code> 并逐次 <code>await run(item)</code>；若需求改为并发，应显式聚合 <code>map</code> 产生的承诺并考虑失败策略。</p>\\n</li>\\n<li>\\n<p><strong>线上对象字典由 <code>Object.create(null)</code> 创建，<code>for...in</code> 中调用 <code>obj.hasOwnProperty(key)</code> 直接报错；应该怎样修复？</strong></p>\\n<p>这类对象没有继承 <code>Object.prototype</code>，因此不存在实例方法 <code>hasOwnProperty</code>。应改用 <code>Object.hasOwn(obj, key)</code> 过滤自有属性，它也能避免同名字段覆盖方法；若运行环境不提供该 <code>API</code>，需要采用等价且安全的显式调用方式。</p>\\n</li>\\n<li>\\n<p><strong>数据导出要求保持服务端定义的记录顺序，评审中有人坚持用 <code>for...in</code> 遍历数组下标；你会接受吗？</strong></p>\\n<p>不应把 <code>for...in</code> 当作数组顺序遍历工具，因为它枚举的是属性键，还可能混入额外属性与原型属性，整数键也有自身的枚举次序规则。应使用 <code>for...of</code> 按数组迭代器顺序取值；需要下标时可遍历 <code>arr.entries()</code> 获得 <code>[index, value]</code>。</p>\\n</li>\\n<li>\\n<p><strong>聊天页通过异步流持续接收消息块，普通 <code>for...of</code> 无法等待下一块数据；它与 <code>for await...of</code> 的协议边界是什么？</strong></p>\\n<p>普通 <code>for...of</code> 面向同步可迭代对象，循环按同步 <code>next()</code> 结果推进；异步流应暴露异步迭代器，并由 <code>for await...of</code> 等待每次产出。它适合顺序消费流式数据，但错误、取消和资源关闭仍要由流实现与调用方共同处理。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_17-generator函数是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_17-generator函数是什么-有什么作用\\">#</a> 17 Generator函数是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>function*</code> 声明，调用后<strong>不执行函数体</strong>，返回一个迭代器；每次 <code>next()</code> 执行到下一个 <code>yield</code> 暂停</li>\\n<li>核心能力是<strong>双向通信</strong>：<code>yield</code> 把值交出去，<code>next(v)</code> 又能把值送回来作为 <code>yield</code> 表达式的结果</li>\\n<li>它的本质价值是&quot;把执行控制权交给调用方&quot;—— 任务能被暂停、取消、重放</li>\\n<li><code>redux-saga</code> 用它做副作用管理，正因为 <code>yield</code> 出去的只是&quot;要做什么&quot;的描述，执行由中间件负责，所以可取消、可测试</li>\\n<li><code>async/await</code> 是 <code>generator + 自动执行器</code> 的语法糖；<code>generator</code> 更通用但更啰嗦</li>\\n</ul>\\n<p><strong><code>Generator</code> 函数是 <code>Iterator</code> 接口的一种具体实现，用来暂停和恢复函数执行，也可用于异步编程与控制流管理。</strong> 调用它不会一次执行完函数体，而是返回遍历器对象；每次调用 <code>next()</code>，代码会继续运行到下一个 <code>yield</code>，并返回 <code>{ value, done }</code>。同时，<code>next(value)</code> 可以把值传回函数内部，从而影响后续执行。它配合 <code>Thunk</code> 函数能更灵活地组织异步流程，但需要调用方持续调用 <code>next()</code> 驱动执行。</p>\\n</section>\\n<ul>\\n<li>如果说<code>JavaScript</code>是<code>ECMAScript</code>标准的一种具体实现、<code>Iterator</code>遍历器是<code>Iterator</code>的具体实现，那么<code>Generator</code>函数可以说是<code>Iterator</code>接口的具体实现方式。</li>\\n<li>执行<code>Generator</code>函数会返回一个遍历器对象，每一次<code>Generator</code>函数里面的<code>yield</code>都相当一次遍历器对象的<code>next()</code>方法，并且可以通过<code>next(value)</code>方法传入自定义的value,来改变<code>Generator</code>函数的行为。</li>\\n<li><code>Generator</code>函数可以通过配合<code>Thunk</code> 函数更轻松更优雅的实现异步编程和控制流管理。</li>\\n</ul>\\n<h3 id=\\"generator-原理\\"><a class=\\"header-anchor\\" href=\\"#generator-原理\\">#</a> generator 原理</h3>\\n<blockquote>\\n<p><code>Generator</code> 是 <code>ES6</code>中新增的语法，和 <code>Promise</code> 一样，都可以用来异步编程</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// 使用 * 表示这是一个 Generator 函数\\n// 内部可以通过 yield 暂停代码\\n// 通过调用 next 恢复执行\\nfunction* test() {\\n  let a = 1 + 2;\\n  yield 2;\\n  yield 3;\\n}\\nlet b = test();\\nconsole.log(b.next()); // &gt;  { value: 2, done: false }\\nconsole.log(b.next()); // &gt;  { value: 3, done: false }\\nconsole.log(b.next()); // &gt;  { value: undefined, done: true }\\n</code></pre>\\n<blockquote>\\n<p>从以上代码可以发现，加上 <code>*</code>的函数执行后拥有了 <code>next</code> 函数，也就是说函数执行后返回了一个对象。每次调用 <code>next</code> 函数可以继续执行被暂停的代码。以下是 <code>Generator</code> 函数的简单实现</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// cb 也就是编译过的 test 函数\\nfunction generator(cb) {\\n  return (function() {\\n    var object = {\\n      next: 0,\\n      stop: function() {}\\n    };\\n\\n    return {\\n      next: function() {\\n        var ret = cb(object);\\n        if (ret === undefined) return { value: undefined, done: true };\\n        return {\\n          value: ret,\\n          done: false\\n        };\\n      }\\n    };\\n  })();\\n}\\n// 如果你使用 babel 编译后可以发现 test 函数变成了这样\\nfunction test() {\\n  var a;\\n  return generator(function(_context) {\\n    while (1) {\\n      switch ((_context.prev = _context.next)) {\\n        // 可以发现通过 yield 将代码分割成几块\\n        // 每次执行 next 函数就执行一块代码\\n        // 并且表明下次需要执行哪块代码\\n        case 0:\\n          a = 1 + 2;\\n          _context.next = 4;\\n          return 2;\\n        case 4:\\n          _context.next = 6;\\n          return 3;\\n\\t\\t// 执行完毕\\n        case 6:\\n        case &quot;end&quot;:\\n          return _context.stop();\\n      }\\n    }\\n  });\\n}\\n</code></pre>\\n<h3 id=\\"generator-实现\\"><a class=\\"header-anchor\\" href=\\"#generator-实现\\">#</a> Generator 实现</h3>\\n<blockquote>\\n<p><code>Generator</code> 是 <code>ES6</code>中新增的语法，和 <code>Promise</code> 一样，都可以用来异步编程</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// 使用 * 表示这是一个 Generator 函数\\n// 内部可以通过 yield 暂停代码\\n// 通过调用 next 恢复执行\\nfunction* test() {\\n  let a = 1 + 2;\\n  yield 2;\\n  yield 3;\\n}\\nlet b = test();\\nconsole.log(b.next()); // &gt;  { value: 2, done: false }\\nconsole.log(b.next()); // &gt;  { value: 3, done: false }\\nconsole.log(b.next()); // &gt;  { value: undefined, done: true }\\n</code></pre>\\n<blockquote>\\n<p>从以上代码可以发现，加上 <code>*</code> 的函数执行后拥有了 <code>next</code>函数，也就是说函数执行后返回了一个对象。每次调用 <code>next</code>函数可以继续执行被暂停的代码。以下是 <code>Generator</code> 函数的简单实现</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// cb 也就是编译过的 test 函数\\nfunction generator(cb) {\\n  return (function() {\\n    var object = {\\n      next: 0,\\n      stop: function() {}\\n    };\\n\\n    return {\\n      next: function() {\\n        var ret = cb(object);\\n        if (ret === undefined) return { value: undefined, done: true };\\n        return {\\n          value: ret,\\n          done: false\\n        };\\n      }\\n    };\\n  })();\\n}\\n// 如果你使用 babel 编译后可以发现 test 函数变成了这样\\nfunction test() {\\n  var a;\\n  return generator(function(_context) {\\n    while (1) {\\n      switch ((_context.prev = _context.next)) {\\n        // 可以发现通过 yield 将代码分割成几块\\n        // 每次执行 next 函数就执行一块代码\\n        // 并且表明下次需要执行哪块代码\\n        case 0:\\n          a = 1 + 2;\\n          _context.next = 4;\\n          return 2;\\n        case 4:\\n          _context.next = 6;\\n          return 3;\\n\\t\\t// 执行完毕\\n        case 6:\\n        case &quot;end&quot;:\\n          return _context.stop();\\n      }\\n    }\\n  });\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>页面里调用 <code>test()</code> 后没有打印任何 <code>yield</code> 前的日志，只有执行 <code>iterator.next()</code> 才出现；同事却说 <code>Generator</code> 和普通函数一样会立即执行函数体，你怎么用返回值解释这个现象？</strong></p>\\n<p>调用 <code>test()</code> 只会创建并返回迭代器对象，不会立刻跑完整个函数体；首次调用 <code>next()</code> 才执行到第一个 <code>yield</code> 并暂停。返回的 <code>{ value, done }</code> 同时描述本次产出和是否结束，最后一次通常是 <code>{ value: undefined, done: true }</code>。</p>\\n</li>\\n<li>\\n<p><strong>你要给一个只含两个 <code>yield</code> 的流程写简化版执行器，评审要求不能依赖原生 <code>Generator</code>；状态对象和 <code>next()</code> 至少要承担哪些职责？</strong></p>\\n<p>执行器需要保存下一段代码的位置，并让每次 <code>next()</code> 调用状态机继续运行到下一个暂停点。编译后的函数可通过 <code>switch</code> 分段，在返回 <code>yield</code> 值前更新状态；回调返回 <code>undefined</code> 时再把结果标记为 <code>done: true</code>，但这只是简化实现。</p>\\n</li>\\n<li>\\n<p><strong>原流程只有顺序执行的两个 <code>yield</code>，现在要求支持循环、分支以及多次暂停；把每段逻辑手工塞进递增的 <code>case</code> 是否仍然可靠？</strong></p>\\n<p>仍可用状态机表达，但状态不能只机械递增，还要准确记录分支跳转、循环回边和结束位置。真实编译器还需处理传值、异常与控制流恢复，示例里的 <code>next</code> 和空 <code>stop()</code> 并不足够；手写版本越复杂，越容易遗漏语义边界。</p>\\n</li>\\n<li>\\n<p><strong>线上迭代器连续三次 <code>next()</code> 都返回 <code>{ done: false }</code>，原函数明明只有两个 <code>yield</code>；你会先检查编译产物里的哪些状态变化？</strong></p>\\n<p>先检查每个 <code>case</code> 返回前是否更新了 <code>_context.next</code>，以及结束分支是否确实调用了 <code>_context.stop()</code>。若状态仍停在原来的 <code>case</code>，下一次 <code>next()</code> 就会重复产出同一个值；还要确认执行器把结束信号误当成普通返回值的可能。</p>\\n</li>\\n<li>\\n<p><strong>十万条列表格式化会卡住页面，团队在“用 <code>Generator</code> 分片”和“直接放进 <code>Web Worker</code>”之间争论，你会怎么选？</strong></p>\\n<p>若任务适合在主线程按小批次推进，可让循环定期 <code>yield</code>，由外部调度器分帧调用 <code>next()</code>，从而给渲染和输入留出机会。纯计算且数据传输成本可接受时，<code>Web Worker</code> 隔离主线程更彻底；<code>Generator</code> 只暂停执行，并不会自动提供并行能力。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_18-async函数是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_18-async函数是什么-有什么作用\\">#</a> 18 async函数是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>async</code> 函数<strong>总是返回 <code>Promise</code></strong>，<code>return</code> 的值会被自动包装，抛错等价于 <code>reject</code></li>\\n<li><code>await</code> 会暂停函数执行、把后面的代码放进微任务，等 <code>Promise</code> 落定后再继续</li>\\n<li>它是 <code>generator + 自动执行器</code> 的语法糖，本质是让异步代码写得像同步</li>\\n<li>错误处理回归 <code>try/catch</code>，比 <code>.catch()</code> 更贴近同步代码的直觉</li>\\n<li>⚠️ 循环里 <code>await</code> 是<strong>串行</strong>，无依赖的请求要用 <code>Promise.all</code> 并发；未捕获的 reject 会触发 <code>unhandledrejection</code>，表现为&quot;loading 一直转但没报错&quot;</li>\\n</ul>\\n<p><strong><code>async</code> 函数是内置自动执行器的 <code>Generator</code> 语法糖，配合 <code>await</code> 能把异步流程写得更像同步代码。</strong> 它调用后会返回 <code>Promise</code>，执行到 <code>await</code> 时暂时交还控制权，等当前同步代码执行完再继续。相比连续的 <code>.then()</code>，这种写法更清晰，也方便用 <code>try/catch</code> 处理错误。要注意，没有依赖关系的异步任务如果逐个 <code>await</code>，会失去原本可以利用的并发性。</p>\\n</section>\\n<blockquote>\\n<p><code>async</code>函数可以理解为内置自动执行器的<code>Generator</code>函数语法糖，它配合<code>ES6</code>的<code>Promise</code>近乎完美的实现了异步编程解决方案</p>\\n</blockquote>\\n<p><strong>async、await 优缺点</strong></p>\\n<blockquote>\\n<p><code>async</code> 和 <code>await</code> 相比直接使用 <code>Promise</code> 来说，优势在于处理 then 的调用链，能够更清晰准确的写出代码。缺点在于滥用 <code>await</code> 可能会导致性能问题，因为 <code>await</code> 会阻塞代码，也许之后的异步代码并不依赖于前者，但仍然需要等待前者完成，导致代码失去了并发性</p>\\n</blockquote>\\n<p>下面来看一个使用 <code>await</code> 的代码。</p>\\n<pre class=\\"language-js\\"><code>var a = 0\\nvar b = async () =&gt; {\\n  a = a + await 10\\n  console.log(&#39;2&#39;, a) // -&gt; &#39;2&#39; 10\\n  a = (await 10) + a\\n  console.log(&#39;3&#39;, a) // -&gt; &#39;3&#39; 20\\n}\\nb()\\na++\\nconsole.log(&#39;1&#39;, a) // -&gt; &#39;1&#39; 1\\n</code></pre>\\n<ul>\\n<li>首先函数<code>b</code> 先执行，在执行到 <code>await 10</code> 之前变量 <code>a</code> 还是 <code>0</code>，因为在 <code>await</code> 内部实现了 <code>generators</code> ，<code>generators</code> 会保留堆栈中东西，所以这时候 <code>a = 0</code> 被保存了下来</li>\\n<li>因为 <code>await</code> 是异步操作，遇到<code>await</code>就会立即返回一个<code>pending</code>状态的<code>Promise</code>对象，暂时返回执行代码的控制权，使得函数外的代码得以继续执行，所以会先执行 <code>console.log(\'1\', a)</code></li>\\n<li>这时候同步代码执行完毕，开始执行异步代码，将保存下来的值拿出来使用，这时候 <code>a = 10</code></li>\\n<li>然后后面就是常规执行代码了</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>结算页依次写了 <code>await loadCoupon()</code> 和 <code>await loadAddress()</code>，两项数据互不依赖，同事却认为用了 <code>async</code> 就天然并发；页面为什么仍可能多等一段时间？</strong></p>\\n<p>两个连续 <code>await</code> 会让后一个操作等前一个完成，即使业务上不存在依赖，也会被写成串行流程。应先同时创建两个 <code>Promise</code>，再统一等待结果；代价是错误处理要明确整体失败还是允许部分成功，不能只为缩短等待而忽略语义。</p>\\n</li>\\n<li>\\n<p><strong>控制台依次打印 <code>\'1\'</code>、<code>\'2\'</code>、<code>\'3\'</code>，代码却先调用了含两个 <code>await 10</code> 的 <code>b()</code>，评审质疑执行顺序；你怎样解释函数内外的控制权切换？</strong></p>\\n<p><code>b()</code> 运行到首个 <code>await</code> 时会暂停自身并交还控制权，因此外层的 <code>a++</code> 和 <code>\'1\'</code> 先执行。待当前同步代码结束后，函数从保存的位置恢复并继续打印 <code>\'2\'</code>、<code>\'3\'</code>；<code>await</code> 暂停的是该异步函数后续，不是阻塞整个线程。</p>\\n</li>\\n<li>\\n<p><strong>搜索页原来用一长串 <code>.then()</code>，改成 <code>async/await</code> 后代码更直，但产品又要求三个接口并发且结果统一组装；你会保留哪些改动、撤回哪些写法？</strong></p>\\n<p>保留 <code>async/await</code> 对控制流和异常路径的清晰表达，但不要给三个无依赖请求逐个加 <code>await</code>。应先发起请求，再在汇合点等待并组装结果；若其中存在先后依赖，则只串行必要部分，否则可读性提升会以丢失并发性为代价。</p>\\n</li>\\n<li>\\n<p><strong>订单按钮点击后一直显示加载中，日志只到 <code>await submitOrder()</code> 前，接口已经返回失败；你会怎样定位是流程暂停还是异常中断？</strong></p>\\n<p>先确认 <code>submitOrder()</code> 返回的 <code>Promise</code> 是否被拒绝，并在调用链附近补上 <code>try/catch</code> 或显式拒绝处理。<code>await</code> 遇到拒绝会使后续语句不再执行，因此关闭加载状态应放在可靠的清理路径中；全局未处理拒绝监听只能用于兜底观测，不能替代业务恢复。</p>\\n</li>\\n<li>\\n<p><strong>团队有人说 <code>async</code> 只是让 <code>Promise</code> 写法更像同步代码，另一人说它本质上接近带自动执行器的 <code>Generator</code>；这两种说法冲突吗？</strong></p>\\n<p>两种说法描述的是不同层面，并不冲突：使用层面由 <code>Promise</code> 承载异步结果，语法层面则可理解为自动推进的 <code>Generator</code> 式流程。开发者无需手动调用 <code>next()</code>，但每个 <code>await</code> 仍形成暂停点；这种抽象不会消除错误处理和并发设计责任。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_19-class、extends是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_19-class、extends是什么-有什么作用\\">#</a> 19 Class、extends是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>class</code> 是原型继承的<strong>语法糖</strong>，本质仍是函数和原型链，但语义更清晰</li>\\n<li>三个实质差别：必须 <code>new</code> 调用（漏写直接报错）、方法<strong>不可枚举</strong>、类声明<strong>不提升</strong>（有 TDZ）</li>\\n<li><code>class</code> 内部默认严格模式，<code>this</code> 在未绑定时是 <code>undefined</code> 而不是 <code>window</code></li>\\n<li><code>extends</code> + <code>super()</code> 能正确继承内置类型（<code>Array</code>、<code>Error</code>），这是 ES5 寄生组合继承做不到的</li>\\n<li><code>#</code> 私有字段（ES2022）是语法层面强制的真私有，比 <code>_</code> 前缀约定和 <code>Symbol</code> 都可靠</li>\\n</ul>\\n<p><strong><code>class</code> 是构造函数和原型写法的语法糖，<code>extends</code> 用来以更清晰的方式表达类之间的继承关系。</strong> 它本质上没有脱离原型机制，但类中的方法默认不可枚举，而且类必须先声明、再通过 <code>new</code> 实例化。子类构造函数需要先调用 <code>super()</code>，在父类初始化的 <code>this</code> 基础上再添加或修改属性。相比 <code>ES5</code> 手动操作原型，这套语法更接近常见的面向对象写法，也更容易读懂。</p>\\n</section>\\n<blockquote>\\n<p><code>ES6</code> 的<code>class</code>可以看作只是一个<code>ES5</code>生成实例对象的构造函数的语法糖。它参考了<code>java</code>语言，定义了一个类的概念，让对象原型写法更加清晰，对象实例化更像是一种面向对象编程。<code>Class</code>类可以通过<code>extends</code>实现继承。它和ES5构造函数的不同点</p>\\n</blockquote>\\n<p>类的内部定义的所有方法，都是不可枚举的</p>\\n<pre class=\\"language-js\\"><code>///ES5\\nfunction ES5Fun (x, y) {\\n\\tthis.x = x;\\n\\tthis.y = y;\\n}\\nES5Fun.prototype.toString = function () {\\n\\t return &#39;(&#39; + this.x + &#39;, &#39; + this.y + &#39;)&#39;;\\n}\\nvar p = new ES5Fun(1, 3);\\np.toString();\\nObject.keys(ES5Fun.prototype); //[&#39;toString&#39;]\\n\\n//ES6\\nclass ES6Fun {\\n\\tconstructor (x, y) {\\n\\t\\tthis.x = x;\\n\\t\\tthis.y = y;\\n\\t}\\n\\ttoString () {\\n\\t\\treturn &#39;(&#39; + this.x + &#39;, &#39; + this.y + &#39;)&#39;;\\n\\t}\\n}\\n\\nObject.keys(ES6Fun.prototype); //[]\\n</code></pre>\\n<ul>\\n<li><code>ES6</code>的<code>class</code>类必须用<code>new</code>命令操作，而<code>ES5</code>的构造函数不用<code>new</code>也可以执行。</li>\\n<li><code>ES6</code>的<code>class</code>类不存在变量提升，必须先定义<code>class</code>之后才能实例化，不像<code>ES5</code>中可以将构造函数写在实例化之后。</li>\\n<li><code>ES5</code> 的继承，实质是先创造子类的实例对象<code>this</code>，然后再将父类的方法添加到<code>this</code>上面。<code>ES6</code> 的继承机制完全不同，实质是先将父类实例对象的属性和方法，加到<code>this</code>上面（所以必须先调用<code>super</code>方法），然后再用子类的构造函数修改<code>this</code>。</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>埋点脚本用 <code>Object.keys(Service.prototype)</code> 收集方法，迁移成 <code>class Service</code> 后结果从多个方法变成空数组；方法明明还能调用，为什么扫描不到？</strong></p>\\n<p><code>class</code> 内声明的原型方法默认不可枚举，所以实例仍能沿原型链调用，但 <code>Object.keys(Service.prototype)</code> 不会列出它们。若工具确实要发现这些方法，应改用能读取非枚举自有属性的反射方式并排除 <code>constructor</code>；依赖可枚举性的旧逻辑必须迁移。</p>\\n</li>\\n<li>\\n<p><strong>后台页面把 <code>User</code> 从构造函数改成 <code>class</code> 后，旧代码仍写 <code>User(data)</code>，线上直接抛错；你会修调用方还是给类再包一层兼容函数？</strong></p>\\n<p><code>class</code> 必须通过 <code>new User(data)</code> 构造，不能像普通函数那样直接调用，因此直接修正所有明确的实例化入口最符合新语义。若外部调用面暂时无法一次迁完，可保留兼容工厂，但它会形成两套入口并掩盖错误用法，应限定过渡范围。</p>\\n</li>\\n<li>\\n<p><strong>子类 <code>Admin</code> 的构造器想先写 <code>this.role = \'admin\'</code> 再调用 <code>super(name)</code>，评审认为顺序更直观；这段代码为什么不能按他的方案执行？</strong></p>\\n<p>派生类必须先调用 <code>super()</code>，父类构造过程完成后才会得到可用的 <code>this</code>，因此提前访问 <code>this</code> 会失败。应先让父类初始化公共状态，再由子类修改或补充属性；若初始化顺序存在冲突，应调整父类参数或拆分职责，而不是绕过 <code>super()</code>。</p>\\n</li>\\n<li>\\n<p><strong>一个模块在文件顶部执行 <code>new Report()</code>，类声明却放在文件末尾；改造前的函数构造器正常，换成 <code>class</code> 后初始化失败，你先查什么？</strong></p>\\n<p>先检查实例化是否发生在类声明之前，因为 <code>class</code> 不存在可供提前使用的声明提升效果。把声明移到首次使用之前，或调整模块初始化顺序即可；不要把故障误判为导入问题，循环依赖仍需另外核查实际执行时序。</p>\\n</li>\\n<li>\\n<p><strong>团队争论继承 <code>BaseController</code> 还是复制几个方法到新控制器，需求只有复用行为且父类状态不断增加；你会依据什么限制 <code>extends</code>？</strong></p>\\n<p><code>extends</code> 适合确有“子类属于父类”关系，并且需要沿用父类初始化与原型方法的场景。若只是复用少量行为，组合独立函数或对象通常能减少对子类构造顺序和父类内部状态的耦合；语法更清晰不代表层级越深越合理。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_20-module、export、import是什么-有什么作用\\"><a class=\\"header-anchor\\" href=\\"#_20-module、export、import是什么-有什么作用\\">#</a> 20 module、export、import是什么，有什么作用？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>ESM</code> 是语言级模块方案，统一了 <code>AMD</code>/<code>CMD</code>/<code>CommonJS</code> 各行其是的局面</li>\\n<li>与 <code>CJS</code> 的三个本质区别：<strong>静态分析</strong>（编译期确定依赖）、<strong>值的活绑定</strong>（不是拷贝）、<strong>异步加载</strong></li>\\n<li>静态结构是 <code>Tree Shaking</code> 的前提 —— 这就是 <code>lodash</code> 主包摇不掉、要用 <code>lodash-es</code> 的原因</li>\\n<li>活绑定意味着导出模块内部改了值，引用方能立刻看到；<code>CJS</code> 拿到的是导出那一刻的拷贝</li>\\n<li><code>import()</code>（ES2020）补上运行时按需加载，是路由懒加载和代码分割的基础</li>\\n</ul>\\n<p><strong><code>ESM</code> 是 <code>JavaScript</code> 的官方模块方案，<code>export</code> 负责暴露内容，<code>import</code> 负责声明和使用依赖。</strong> 它的静态结构能在执行前确定依赖关系，因此打包器可以做 <code>Tree Shaking</code>；导入值还是活绑定，源模块更新后引用方也能看到。浏览器中的模块会异步获取和解析，而 <code>CommonJS</code> 的 <code>require</code> 属于运行时同步加载。需要按需加载时可以使用返回 <code>Promise</code> 的 <code>import()</code>，常用于路由懒加载和代码分割。</p>\\n</section>\\n<p><code>ESM</code>（<code>ECMAScript Module</code>）是语言层面的官方模块方案，统一了此前 <code>AMD</code>、<code>CMD</code>、<code>CommonJS</code> 各行其是的局面。</p>\\n<p><strong>基本语法：</strong></p>\\n<pre class=\\"language-js\\"><code>// 具名导出（可多个）\\nexport const PI = 3.14\\nexport function area(r) {}\\n\\n// 默认导出（每个模块最多一个）\\nexport default class Circle {}\\n\\n// 导入\\nimport Circle, { PI, area as calcArea } from &#39;./circle.js&#39;\\nimport * as circle from &#39;./circle.js&#39;\\n\\n// 重导出（做统一出口文件）\\nexport { PI } from &#39;./circle.js&#39;\\n</code></pre>\\n<p><strong>它和 <code>CommonJS</code> 的三个本质区别，这才是面试重点：</strong></p>\\n<p><strong>① 静态 vs 动态</strong></p>\\n<p><code>ESM</code> 的 <code>import</code> 是<strong>编译期</strong>确定的：模块依赖关系在代码执行前就能分析出来。所以 <code>import</code> 必须写在顶层，不能放进 <code>if</code> 里。<code>CommonJS</code> 的 <code>require</code> 是运行时函数调用，可以动态拼路径。</p>\\n<p>这个差别直接决定了 <strong><code>Tree Shaking</code> 只对 <code>ESM</code> 有效</strong> —— 打包器要在编译期就知道&quot;哪些导出没被用到&quot;才能删掉。这也是引 <code>lodash</code> 主包（<code>CJS</code>）摇不掉、必须用 <code>lodash-es</code> 的原因。</p>\\n<p><strong>② 值的引用 vs 值的拷贝</strong></p>\\n<pre class=\\"language-js\\"><code>// counter.js\\nexport let count = 0\\nexport const inc = () =&gt; count++\\n\\n// main.js —— ESM：拿到的是&quot;活的绑定&quot;\\nimport { count, inc } from &#39;./counter.js&#39;\\nconsole.log(count)  // 0\\ninc()\\nconsole.log(count)  // 1 ✅ 跟着变了\\n</code></pre>\\n<p><code>CommonJS</code> 里 <code>require</code> 拿到的是导出那一刻的值拷贝，之后模块内部再改，引用方看不到。这个差别在循环依赖时表现得尤其明显。</p>\\n<p><strong>③ 异步加载 vs 同步加载</strong></p>\\n<p><code>ESM</code> 在浏览器里是异步获取和解析的（<code>&lt;script type=&quot;module&quot;&gt;</code> 天生带 <code>defer</code> 行为）；<code>CommonJS</code> 是同步读文件，只适合服务端。</p>\\n<p><strong>动态导入 <code>import()</code></strong>（ES2020）填补了&quot;运行时按需加载&quot;的空缺：</p>\\n<pre class=\\"language-js\\"><code>const { heavy } = await import(&#39;./heavy.js&#39;)   // 返回 Promise\\n</code></pre>\\n<p>它是路由懒加载和代码分割的基础 —— 打包器看到 <code>import()</code> 就会把那部分单独打成一个 <code>chunk</code>。</p>\\n<p><strong>工程上的常见问题</strong>：一个包只发了 <code>CJS</code> 产物时，<code>ESM</code> 项目里 <code>import</code> 它可能拿不到期望的 <code>default</code>（因为它用的是 <code>exports.foo = ...</code> 具名导出）。这时要么 <code>import * as pkg</code>，要么让打包器预构建成 <code>ESM</code>。发包时用 <code>package.json</code> 的 <code>exports</code> 字段精确声明各环境入口，是避免这类问题的正解。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>权限页面试图在 <code>if (isAdmin)</code> 中写静态 <code>import \'./admin.js\'</code>，构建阶段就报语法问题；产品又要求普通用户不要下载这段代码，你怎么改？</strong></p>\\n<p>静态 <code>import</code> 必须位于顶层，依赖关系要在执行前确定，不能直接放进条件分支。应在运行时使用返回 <code>Promise</code> 的 <code>import()</code>，让打包器有机会生成独立 <code>chunk</code>；是否真正减少下载还取决于构建配置和该模块是否被其他入口引用。</p>\\n</li>\\n<li>\\n<p><strong>计数器模块导出 <code>count</code> 和 <code>inc()</code>，详情页调用 <code>inc()</code> 后再次读取导入的 <code>count</code> 变成了 <code>1</code>；同事担心导入时不是已经复制了 <code>0</code> 吗？</strong></p>\\n<p><code>ESM</code> 的具名导入是活绑定，不是把导出值在导入时固定复制一份，所以模块内更新 <code>count</code> 后，引用方再次读取会看到新值。导入方不能把该绑定当成本地变量随意重赋；需要修改时应通过导出函数维护模块边界。</p>\\n</li>\\n<li>\\n<p><strong>组件库同时服务浏览器构建和服务端工具，维护者想继续只发布一份 <code>CommonJS</code>，但业务方要求可靠的 <code>Tree Shaking</code>；你如何裁决入口设计？</strong></p>\\n<p>需要静态裁剪时应提供可静态分析的 <code>ESM</code> 入口，因为运行时 <code>require()</code> 的动态性会限制打包器判断未使用导出。包可通过 <code>package.json</code> 的 <code>exports</code> 为不同环境声明入口，但双产物会增加发布和互操作测试成本，不能只改文件扩展名。</p>\\n</li>\\n<li>\\n<p><strong>同一个依赖在 <code>Node</code> 的 <code>require()</code> 中正常，迁到 <code>Vite</code> 后默认导入是 <code>undefined</code>，包源码使用 <code>exports.parse = ...</code>；你会按什么顺序排查？</strong></p>\\n<p>先确认包只提供了 <code>CommonJS</code> 产物，以及实际导出的是具名属性还是 <code>module.exports</code> 整体。可尝试 <code>import * as pkg</code> 读取命名空间，或让构建工具预构建为 <code>ESM</code>；长期应由包维护者正确声明 <code>exports</code>，调用方兼容写法可能受工具互操作规则影响。</p>\\n</li>\\n<li>\\n<p><strong>路由模块既能用顶层静态 <code>import</code>，也能在进入页面时 <code>import()</code>；首屏体积和页面切换稳定性发生冲突时，你怎么选？</strong></p>\\n<p>首屏必需且体积可接受的依赖适合静态导入，构建器能提前分析并稳定加载；低频或较重页面适合 <code>import()</code> 拆分。动态加载会增加切换时的网络与失败处理路径，因此要配合加载态、错误恢复和合理分包，不能把所有模块都盲目懒加载。</p>\\n</li>\\n<li>\\n<p><strong>浏览器入口改成 <code>&lt;script type=&quot;module&quot;&gt;</code> 后，团队仍按 <code>CommonJS</code> 的同步读文件模型推断执行时序；这会导致什么判断偏差？</strong></p>\\n<p>浏览器中的模块脚本会异步获取和解析，并具有类似 <code>defer</code> 的执行特征，不能按 <code>require()</code> 同步读取文件的顺序推断页面初始化。依赖关系仍由静态导入建立，但网络完成时机可能不同；若代码依赖传统脚本的即时副作用，需要显式调整启动边界。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_21-日常前端代码开发中-有哪些值得用es6去改进的编程优化或者规范\\"><a class=\\"header-anchor\\" href=\\"#_21-日常前端代码开发中-有哪些值得用es6去改进的编程优化或者规范\\">#</a> 21 日常前端代码开发中，有哪些值得用ES6去改进的编程优化或者规范？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>声明：默认 <code>const</code>、需要重新赋值才 <code>let</code>、永不用 <code>var</code> —— 可变性在声明处就一目了然</li>\\n<li><code>this</code>：箭头函数取代 <code>var self = this</code>，但<strong>对象方法和原型方法不能用箭头函数</strong></li>\\n<li>参数：解构 + 默认值让函数签名自解释，顺带避开 <code>a = a || 默认值</code> 把 <code>0</code> 误判为缺省的老 bug</li>\\n<li>取值：<code>?.</code> 和 <code>??</code> 替掉长串 <code>&amp;&amp;</code>；注意 <code>??</code> 只在 <code>null</code>/<code>undefined</code> 兜底，<code>||</code> 会把 <code>0</code>/<code>\'\'</code> 也当成没有</li>\\n<li>结构：<code>Map</code>/<code>Set</code> 取代对象和数组的误用；模块拆小才能真正 <code>Tree Shaking</code></li>\\n<li>这些一律用 <code>ESLint</code> 强制（<code>no-var</code>/<code>prefer-const</code>/<code>prefer-template</code>），别靠人记</li>\\n</ul>\\n<p><strong>日常开发中，我会优先用 <code>const</code> 和 <code>let</code>、箭头函数、解构默认值及模块化来改善作用域、参数表达和代码组织。</strong> 比如用箭头函数保留外层 <code>this</code>，可以替代 <code>var self = this</code>，但对象方法和原型方法不适合随意改成箭头函数。取深层数据时，<code>?.</code> 配合 <code>??</code> 比连续的 <code>&amp;&amp;</code> 更清楚，而且不会把 <code>0</code>、空字符串或 <code>false</code> 误判为缺省值。这些约定最好交给 <code>ESLint</code> 约束，同时避免为了链式或函数式写法牺牲可读性。</p>\\n</section>\\n<p>这题问的是<strong>工程习惯</strong>，答的时候要给出&quot;旧写法 → 新写法 → 收益&quot;三段，而不是罗列特性名。</p>\\n<p><strong>① 变量声明：<code>const</code> 优先，永不用 <code>var</code></strong></p>\\n<pre class=\\"language-js\\"><code>// 收益：能不能被改在声明处一目了然；块级作用域避免变量泄漏和循环闭包问题\\nconst list = []      // 默认\\nlet index = 0        // 确实要重新赋值才用 let\\n</code></pre>\\n<p><strong>② 用箭头函数取代 <code>var self = this</code></strong></p>\\n<pre class=\\"language-js\\"><code>// 旧\\nvar self = this\\nsetTimeout(function () { self.update() }, 100)\\n// 新：箭头函数没有自己的 this，直接用外层的\\nsetTimeout(() =&gt; this.update(), 100)\\n</code></pre>\\n<p>但注意<strong>对象方法和原型方法不要用箭头函数</strong> —— 它的 <code>this</code> 不指向对象本身。</p>\\n<p><strong>③ 解构 + 默认值：让函数签名自解释</strong></p>\\n<pre class=\\"language-js\\"><code>// 旧：调用处 fetchList(1, 10, true, false) 完全看不懂\\nfunction fetchList(page, size, withTotal, cache) {}\\n// 新：调用处 fetchList({ page: 2, cache: false }) 一目了然\\nfunction fetchList({ page = 1, size = 10, withTotal = true, cache = true } = {}) {}\\n</code></pre>\\n<p>顺带避免了 <code>a = a || 默认值</code> 会把 <code>0</code> 和 <code>\'\'</code> 误判成缺省的老 bug。</p>\\n<p><strong>④ 可选链和空值合并：替掉一长串 <code>&amp;&amp;</code></strong></p>\\n<pre class=\\"language-js\\"><code>// 旧\\nconst name = res &amp;&amp; res.data &amp;&amp; res.data.user &amp;&amp; res.data.user.name || &#39;匿名&#39;\\n// 新\\nconst name = res?.data?.user?.name ?? &#39;匿名&#39;\\n</code></pre>\\n<p><code>??</code> 和 <code>||</code> 的区别很关键：<code>||</code> 会把 <code>0</code>、<code>\'\'</code>、<code>false</code> 也当成&quot;没有&quot;，<code>??</code> 只在 <code>null</code>/<code>undefined</code> 时兜底。价格显示为 <code>0</code> 却被替换成默认值，就是 <code>||</code> 的锅。</p>\\n<p><strong>⑤ 模板字符串取代字符串拼接</strong>，尤其是多行和多变量的场景。</p>\\n<p><strong>⑥ <code>Map</code>/<code>Set</code> 取代对象和数组的误用</strong></p>\\n<pre class=\\"language-js\\"><code>if (allowSet.has(id)) {}          // O(1)，替代 arr.includes(id) 的 O(n)\\nconst cache = new Map()           // 键可以是对象，顺序可预测\\n</code></pre>\\n<p><strong>⑦ 数组方法链式表达取代命令式循环</strong></p>\\n<pre class=\\"language-js\\"><code>const total = orders.filter((o) =&gt; o.paid).reduce((s, o) =&gt; s + o.amount, 0)\\n</code></pre>\\n<p>但别为了函数式而函数式 —— 一条链超过三四环就该拆开或换回 <code>for...of</code>，可读性优先。</p>\\n<p><strong>⑧ 模块化：明确的 <code>import</code>/<code>export</code>，避免巨型工具文件</strong></p>\\n<p>小而明确的模块 + <code>Tree Shaking</code> 才能真正减小包体。一个导出了两百个函数的 <code>utils.js</code>，只要有一处副作用就可能整个摇不掉。</p>\\n<p><strong>最后一条工程建议</strong>：这些规范不要靠人记，用 <code>ESLint</code> 强制（<code>no-var</code>、<code>prefer-const</code>、<code>prefer-template</code>、<code>prefer-destructuring</code>），代码评审的精力应该留给业务逻辑而不是语法风格。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>库存页把 <code>res.count || 10</code> 统一替换成默认值写法后，库存为 <code>0</code> 的商品仍被显示成 <code>10</code>；代码规范应该要求改成什么，边界是什么？</strong></p>\\n<p>这里应使用 <code>res.count ?? 10</code>，它只在值为 <code>null</code> 或 <code>undefined</code> 时采用默认值，不会吞掉合法的 <code>0</code>。若业务确实把空字符串、<code>false</code> 等所有假值都视为无效，才适合 <code>||</code>；规范不能脱离字段语义机械替换。</p>\\n</li>\\n<li>\\n<p><strong>列表组件用 <code>setTimeout(function () { this.refresh() }, 100)</code> 后报 <code>this.refresh is not a function</code>，同事建议所有函数都换成箭头函数；你接受这个规范吗？</strong></p>\\n<p>回调可改成 <code>setTimeout(() =&gt; this.refresh(), 100)</code>，箭头函数会沿用外层 <code>this</code>，能替代旧式的 <code>var self = this</code>。但对象方法和原型方法不应一律改成箭头函数，因为它们没有自己的动态 <code>this</code>；规范应限定在确实需要词法绑定的回调。</p>\\n</li>\\n<li>\\n<p><strong>分页接口原来调用 <code>fetchList(1, 10, true, false)</code>，评审连续两次把布尔参数顺序传反；你会怎样重构函数签名并控制迁移风险？</strong></p>\\n<p>把位置参数改为对象解构，例如 <code>fetchList({ page: 1, size: 10, withTotal: true, cache: false })</code>，并在形参处设置默认值。调用含义会更直观，也便于以后新增选项；这是接口变更，旧调用方必须同步迁移或经过兼容层。</p>\\n</li>\\n<li>\\n<p><strong>十万条订单去重时，代码在循环里反复调用数组 <code>includes()</code>，同时还要用对象作为缓存键；评审在 <code>Array</code>、普通对象和 <code>Map</code>/<code>Set</code> 之间怎么选？</strong></p>\\n<p>成员判定适合用 <code>Set.has()</code>，缓存键可能是对象时适合用 <code>Map</code>，两者也能更清楚地表达集合与映射意图。普通对象会把键限制在属性键语义内，数组查找则随数据增长重复扫描；若数据很小且只执行一次，转换结构也有额外成本。</p>\\n</li>\\n<li>\\n<p><strong>报表代码把过滤、映射、分组和汇总串成七层数组方法链，团队以“函数式更现代”为由拒绝拆分；你会怎样处理可读性和性能争议？</strong></p>\\n<p>链式方法适合表达短而清晰的数据变换，但七层链应拆成有名称的中间步骤，或在需要单次遍历时改用 <code>for...of</code>。每个数组方法通常都会形成独立遍历或中间结果；不应凭风格断言性能，数据量敏感时再用实际场景验证。</p>\\n</li>\\n<li>\\n<p><strong>代码评审每周都在重复提醒 <code>var</code>、字符串拼接和可变声明，负责人想靠团队自觉，另一方要求引入 <code>ESLint</code>；你支持哪种落地方式？</strong></p>\\n<p>应把稳定且可机械判断的约束交给 <code>ESLint</code>，例如 <code>no-var</code>、<code>prefer-const</code> 和 <code>prefer-template</code>，让评审聚焦业务逻辑。规则需要结合现有代码分阶段启用并固定在持续集成中；过度自动化 <code>prefer-destructuring</code> 等风格项也可能降低局部可读性。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_22-es6的了解\\"><a class=\\"header-anchor\\" href=\\"#_22-es6的了解\\">#</a> 22 ES6的了解</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>有结构地答：按「作用域声明 / 语法糖 / 数据结构 / 异步方案 / 元编程模块化」五类组织，别罗列 API</li>\\n<li>作用域：<code>let</code>/<code>const</code> 修掉 <code>var</code> 的四个缺陷（泄漏、循环闭包、重复声明、污染 <code>window</code>）</li>\\n<li>语法糖：模板字符串、解构、默认参数、箭头函数 —— 箭头函数是唯一有语义变化的（没有自己的 <code>this</code>）</li>\\n<li>数据结构：<code>Map</code>/<code>Set</code>/<code>Symbol</code>/<code>WeakMap</code>，补齐语言的基础设施</li>\\n<li>异步与元编程：<code>Promise</code> → <code>Generator</code> → <code>async/await</code> 这条演进线；<code>Proxy</code>/<code>Reflect</code> 是 <code>Vue 3</code> 响应式的基础</li>\\n<li>加分句：口语里的 <code>ES6</code> 泛指现代 <code>JS</code>，实际能用什么取决于 <code>browserslist</code> + <code>Babel</code> 配置</li>\\n</ul>\\n<p><strong>我理解的 <code>ES6</code>，本质上是从作用域、语法、数据结构、异步和模块化几个方向补强了 <code>JavaScript</code>。</strong> <code>let</code>、<code>const</code> 解决了 <code>var</code> 的块级作用域缺陷，解构和模板字符串则减少了代码噪音。<code>Map</code>、<code>Set</code> 补齐了基础数据结构，<code>Promise</code> 改善了异步流程。大型应用里，<code>Module</code> 的静态结构还能支持 <code>Tree Shaking</code>，实际能否使用则要看 <code>browserslist</code>、<code>Babel</code> 和 <code>core-js</code> 配置。</p>\\n</section>\\n<p>这是一道开放题，考的是<strong>你能不能有结构地组织知识</strong>，而不是背得多。建议按&quot;解决了什么问题&quot;分五类讲，每类举一个例子和它的收益。</p>\\n<p><strong>① 作用域与声明：修掉 <code>var</code> 的历史缺陷</strong></p>\\n<p><code>let</code>/<code>const</code> 带来块级作用域，修掉了变量泄漏、循环闭包、重复声明、顶层变量污染 <code>window</code> 四个问题。<code>let</code> 也提升，只是在赋值前处于暂时性死区，访问直接报错 —— 错误暴露得更早。</p>\\n<p><strong>② 语法糖：让代码更短更清楚</strong></p>\\n<p>模板字符串、解构赋值、默认参数、剩余参数、对象简写、箭头函数。这些不增加新能力，但显著降低了噪音。箭头函数是唯一有语义变化的：它没有自己的 <code>this</code>/<code>arguments</code>/<code>prototype</code>，<code>this</code> 由定义位置决定且 <code>call</code> 改不了。</p>\\n<p><strong>③ 新的数据结构：补齐语言的基础设施</strong></p>\\n<p><code>Map</code>（键可以是任意类型、顺序可预测）、<code>Set</code>（去重和 <code>O(1)</code> 查找）、<code>Symbol</code>（独一无二的键 + 定制语言行为的钩子）、<code>WeakMap</code>/<code>WeakSet</code>（弱引用，不阻止垃圾回收）。</p>\\n<p><strong>④ 异步方案：从回调地狱到线性代码</strong></p>\\n<p><code>Promise</code> 把嵌套回调变成链式；<code>Generator</code> 提供可暂停的执行控制；<code>async/await</code>（ES2017）把异步写成同步的形状。这条线是 <code>JS</code> 十年来最大的改进。</p>\\n<p><strong>⑤ 元编程与模块化：让 <code>JS</code> 能撑起大型应用</strong></p>\\n<p><code>class</code>/<code>extends</code> 规范化了面向对象写法；<code>Proxy</code>/<code>Reflect</code> 提供了拦截和转发对象操作的能力（<code>Vue 3</code> 响应式的基础）；<code>Module</code> 统一了模块化方案，其静态结构是 <code>Tree Shaking</code> 的前提。</p>\\n<p><strong>一个能体现深度的补充</strong>：<code>ES6</code> 之后改成了年度发布，所以口语里的&quot;<code>ES6</code>&quot;经常泛指 <code>ES2015</code> 之后的所有现代 <code>JS</code>。真正决定项目里能用什么的不是标准版本，而是 <code>browserslist</code> 基线 + <code>Babel</code> + <code>core-js</code> 的配置 —— 能把这层说出来，比多背十个 API 更能体现工程视角。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表页的筛选按钮在 <code>for (var i = 0; i &lt; 5; i++)</code> 中绑定点击事件，点击任意按钮都打印 <code>5</code>；同事说把 <code>var</code> 换成 <code>let</code> 只是代码风格，你认同吗？</strong></p>\\n<p>不认同，<code>let</code> 的块级作用域会让每轮循环形成独立绑定，因此回调能拿到对应下标。<code>var</code> 只有函数作用域，五个回调共享同一个最终值；也可用闭包修复，但表达更绕，迁移时还要检查暂时性死区带来的提前访问错误。</p>\\n</li>\\n<li>\\n<p><strong>订单详情页需要先取用户信息，再并发请求地址、优惠券和库存；评审中有人把六层回调直接改成六个连续 <code>await</code>，你会通过吗？</strong></p>\\n<p>不会直接通过，<code>async/await</code> 只改善控制流形状，不会自动获得并发能力。存在依赖的步骤应顺序 <code>await</code>，互不依赖的请求应先创建任务再用 <code>Promise.all</code> 汇合；代价是任一请求失败会使整体拒绝，是否允许部分降级要另行设计。</p>\\n</li>\\n<li>\\n<p><strong>团队要让管理后台兼容一批老浏览器，产品却要求使用模块、<code>Promise</code> 和 <code>Proxy</code>；你会怎样判断这些 <code>ES6</code> 特性能否上线？</strong></p>\\n<p>先用 <code>browserslist</code> 明确真实兼容基线，再区分语法与运行时能力：模块语法可由构建工具处理，<code>Promise</code> 等 <code>API</code> 可按需补充 <code>core-js</code>。<code>Proxy</code> 无法被完整 <code>polyfill</code>，若目标环境不支持，只能改实现或调整兼容范围，不能仅靠 <code>Babel</code> 解决。</p>\\n</li>\\n<li>\\n<p><strong>线上报错显示某个对象方法里的 <code>this</code> 变成外层实例，排查后发现重构时把普通函数全部替换成箭头函数；你如何定位根因？</strong></p>\\n<p>根因是箭头函数没有自己的 <code>this</code>，它从定义位置捕获绑定，而且 <code>call</code>、<code>apply</code> 也无法改写。对象方法、原型方法或依赖动态接收者的回调应恢复普通函数；机械替换还要检查对 <code>arguments</code> 和构造调用的依赖，因为箭头函数同样不提供这些能力。</p>\\n</li>\\n<li>\\n<p><strong>大型前端项目选模块方案时，一方坚持运行时动态加载对象，另一方要求构建阶段做 <code>Tree Shaking</code>；你会偏向哪种结构？</strong></p>\\n<p>静态依赖部分应优先使用 <code>ES</code> <code>Module</code>，因为 <code>import</code>、<code>export</code> 的静态结构能让构建工具分析未使用导出，这是 <code>Tree Shaking</code> 的前提。确需按条件加载时再使用动态导入；若依赖关系全部藏在运行时对象操作中，构建器难以可靠裁剪。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_23-说说你对promise的理解\\"><a class=\\"header-anchor\\" href=\\"#_23-说说你对promise的理解\\">#</a> 23 说说你对Promise的理解</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Promise</code> 表示&quot;一个尚未结束的操作的最终结果&quot;，三状态 <code>pending</code>/<code>fulfilled</code>/<code>rejected</code>，<strong>一旦落定不可逆</strong></li>\\n<li><code>then</code> 返回新的 <code>Promise</code> 所以能链式；回调 <code>return</code> 值会被包成 fulfilled 传给下一环，<code>return</code> <code>Promise</code> 则等它落定</li>\\n<li><code>catch</code> 是 <code>then(null, fn)</code> 的语法糖；<strong><code>catch</code> 之后链条会恢复</strong>成 fulfilled，常用来做降级</li>\\n<li><code>executor</code> 同步执行，只有 <code>then</code> 的回调进<strong>微任务</strong>队列 —— 这是事件循环题的常考点</li>\\n<li>它不解决取消问题，也不自动处理未捕获的 reject（会触发 <code>unhandledrejection</code>）</li>\\n</ul>\\n<p><strong><code>Promise</code> 可以理解为一个承载延迟或异步计算结果的状态机。</strong> 它初始是 <code>pending</code>，之后只能变为 <code>fulfilled</code> 或 <code>rejected</code>，两者合称 <code>settled</code>，而且状态一旦改变就不能再次变化。<code>then</code> 每次都会返回一个新的 <code>Promise</code>，因此多个异步步骤才能继续链式衔接。如果复用原实例，已经落定的状态无法改变，后续多个 <code>then</code> 调用也就失去了意义。</p>\\n</section>\\n<ul>\\n<li>依照 <code>Promise/A+</code> 的定义，Promise 有四种状态：\\n<ul>\\n<li><code>pending</code>: 初始状态, 非 <code>fulfilled</code> 或 <code>rejected.</code></li>\\n<li><code>fulfilled</code>: 成功的操作.</li>\\n<li><code>rejected</code>: 失败的操作.</li>\\n<li><code>settled</code>: <code>Promise</code>已被<code>fulfilled</code>或<code>rejected</code>，且不是<code>pending</code></li>\\n</ul>\\n</li>\\n<li>另外， <code>fulfilled</code> 与 <code>rejected</code> 一起合称 settled</li>\\n<li><code>Promise</code> 对象用来进行延迟(<code>deferred</code>) 和异步(<code>asynchronous</code> ) 计算</li>\\n<li>可以把 <code>Promise</code>看成一个状态机。初始是 <code>pending</code> 状态，可以通过函数 <code>resolve</code> 和 <code>reject</code>，将状态转变为 <code>resolved</code> 或者 <code>rejected</code> 状态，状态一旦改变就不能再次变化。</li>\\n<li><code>then</code> 函数会返回一个 <code>Promise</code> 实例，并且该返回值是一个新的实例而不是之前的实例。因为 <code>Promise</code> 规范规定除了 <code>pending</code> 状态，其他状态是不可以改变的，如果返回的是一个相同实例的话，多个 <code>then</code>调用就失去意义了</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>支付页创建一个 <code>Promise</code> 后先执行 <code>resolve(\'paid\')</code>，异常分支又调用 <code>reject(new Error(\'timeout\'))</code>；监控只看到成功，开发认为两个结果都该通知订阅者，你怎么解释？</strong></p>\\n<p>订阅者只会收到第一次状态迁移，因为 <code>Promise</code> 从 <code>pending</code> 变为 <code>fulfilled</code> 或 <code>rejected</code> 后便不可逆，后续调用会被忽略。若业务确实需要连续通知多次状态，单个 <code>Promise</code> 并不合适，应改用事件或流式抽象，同时约束取消与清理逻辑。</p>\\n</li>\\n<li>\\n<p><strong>搜索页把同一个请求对象连续写成 <code>p.then(renderList); p.then(recordMetrics)</code>，评审者担心第二个 <code>then</code> 会消费掉第一个结果，你如何判断？</strong></p>\\n<p>两个回调都可订阅原始 <code>Promise</code> 的落定结果，调用 <code>then</code> 不会消耗或修改原实例。每次 <code>then</code> 都返回一个新的 <code>Promise</code>，各自后续链条互不相同；但某个分支抛错只会进入该分支的拒绝路径，不会自动通知另一个分支。</p>\\n</li>\\n<li>\\n<p><strong>导入页面需要依次完成解析、校验和保存，开发在每个 <code>then</code> 中返回普通对象，有一处改为返回异步保存的 <code>Promise</code>；后续步骤拿到的是对象还是嵌套 <code>Promise</code>？</strong></p>\\n<p>后续步骤会等待异步保存落定并取得其结果，不会拿到一层嵌套的 <code>Promise</code>。这是 <code>then</code> 返回新实例并吸收所返回 <code>Promise</code> 状态的效果；若回调抛错，新链会转为 <code>rejected</code>，因此错误是否被中途恢复必须看后续拒绝处理。</p>\\n</li>\\n<li>\\n<p><strong>线上下单链偶发停在错误页，代码是 <code>load().then(validate).then(save)</code>；日志显示 <code>validate</code> 抛错，但团队只检查了原始 <code>load()</code> 的状态，你会从哪里查？</strong></p>\\n<p>应沿每次 <code>then</code> 返回的新 <code>Promise</code> 继续检查，因为 <code>validate</code> 抛错会拒绝下一环，而不会反向改变已经成功的 <code>load()</code>。确认链尾是否注册拒绝处理，并检查中间是否丢失了返回值；若没有返回整条链，调用方也无法感知后续失败。</p>\\n</li>\\n<li>\\n<p><strong>批量上传的负责人想用一个 <code>Promise</code> 同时表示“进行中、成功、失败、已取消”四种可反复切换的状态，你会接受这个建模吗？</strong></p>\\n<p>不会，<code>Promise</code> 只能从 <code>pending</code> 一次性落定为成功或失败，<code>settled</code> 只是两种终态的合称，不是可再次迁移的独立状态。进度和取消应由额外信号或状态机表达，最终结果再交给 <code>Promise</code>；强行复用会丢失多次事件。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_24-promise-的构造函数\\"><a class=\\"header-anchor\\" href=\\"#_24-promise-的构造函数\\">#</a> 24 Promise 的构造函数</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>new Promise(executor)</code>，<code>executor</code> 接收 <code>resolve</code>/<code>reject</code> 两个参数，<strong>同步立即执行</strong></li>\\n<li>状态只能改变一次，之后的 <code>resolve</code>/<code>reject</code> 被静默忽略 —— 这个&quot;不可逆&quot;保证是它比回调可靠的根本</li>\\n<li><code>executor</code> 里抛的<strong>同步</strong>错误会自动变 rejected；<strong>异步回调里抛的错捕获不到</strong>，必须显式 <code>reject</code></li>\\n<li><code>resolve(另一个 Promise)</code> 会&quot;跟随&quot;它的状态，这是链式调用能扁平化的底层机制</li>\\n<li><code>then</code> 返回新 <code>Promise</code>；回调 <code>return</code> 值→下一环 fulfilled，<code>return</code> <code>Promise</code>→等它落定，抛错→下一环 rejected</li>\\n<li>反模式：别用 <code>new Promise</code> 去包装本来就返回 <code>Promise</code> 的东西</li>\\n</ul>\\n<p><strong><code>Promise</code> 构造函数写作 <code>new Promise(executor)</code>，其中 <code>executor</code> 会同步执行，并接收 <code>resolve</code> 和 <code>reject</code>。</strong> 状态只能改变一次，后续调用会被忽略，所以消费方不用担心结果被反复改写。同步抛错会自动转成 <code>rejected</code>，但异步回调里的异常必须显式 <code>reject</code>。<code>resolve</code> 传入另一个 <code>Promise</code> 时会跟随其状态；实践中也不该再包装本就返回 <code>Promise</code> 的接口。</p>\\n</section>\\n<p><code>new Promise(executor)</code> 接收一个函数，这个函数接收 <code>resolve</code> 和 <code>reject</code> 两个参数：</p>\\n<pre class=\\"language-js\\"><code>const p = new Promise((resolve, reject) =&gt; {\\n  // ⚠️ 这段是同步立即执行的\\n  fs.readFile(path, (err, data) =&gt; {\\n    if (err) reject(err)          // 失败：传 Error 对象，别传字符串\\n    else resolve(data)            // 成功\\n  })\\n})\\n</code></pre>\\n<p><strong>四个必须知道的行为：</strong></p>\\n<p><strong>① <code>executor</code> 同步执行</strong></p>\\n<pre class=\\"language-js\\"><code>console.log(1)\\nnew Promise((resolve) =&gt; { console.log(2); resolve() }).then(() =&gt; console.log(4))\\nconsole.log(3)\\n// 输出 1 2 3 4\\n</code></pre>\\n<p>所以在 <code>executor</code> 里写重计算照样阻塞主线程 —— <code>Promise</code> 不会帮你&quot;挪到后台&quot;。</p>\\n<p><strong>② 状态只能改变一次，之后的调用被忽略</strong></p>\\n<pre class=\\"language-js\\"><code>new Promise((resolve, reject) =&gt; {\\n  resolve(&#39;A&#39;)\\n  reject(new Error(&#39;B&#39;))   // 无效，被静默忽略\\n  resolve(&#39;C&#39;)             // 无效\\n})\\n</code></pre>\\n<p>这个&quot;不可逆&quot;的保证很重要：它意味着消费方不用担心回调被调用多次 —— 而传统回调没有这个保证。</p>\\n<p><strong>③ <code>executor</code> 里抛出的同步错误会自动变成 rejected</strong></p>\\n<pre class=\\"language-js\\"><code>new Promise(() =&gt; { throw new Error(&#39;x&#39;) }).catch(e =&gt; console.log(e.message))  // &#39;x&#39;\\n</code></pre>\\n<p>但<strong>异步回调里抛的错捕获不到</strong>，必须显式 <code>reject</code>：</p>\\n<pre class=\\"language-js\\"><code>new Promise((resolve, reject) =&gt; {\\n  setTimeout(() =&gt; { throw new Error(&#39;捕获不到！&#39;) })   // ❌ 变成未捕获异常\\n  setTimeout(() =&gt; reject(new Error(&#39;正确做法&#39;)))       // ✅\\n})\\n</code></pre>\\n<p><strong>④ <code>resolve</code> 一个 <code>thenable</code> 会&quot;跟随&quot;它</strong></p>\\n<p><code>resolve(anotherPromise)</code> 不会把 <code>Promise</code> 当普通值传下去，而是等它落定后采用它的状态。这是链式调用能扁平化的底层机制。</p>\\n<p><strong><code>then</code> 的规则：</strong></p>\\n<pre class=\\"language-js\\"><code>promise.then(onFulfilled, onRejected)\\n</code></pre>\\n<ul>\\n<li>返回<strong>新的</strong> <code>Promise</code>，所以能链</li>\\n<li>回调 <code>return</code> 普通值 → 下一环收到这个值（fulfilled）</li>\\n<li>回调 <code>return</code> <code>Promise</code> → 等它落定，取它的结果</li>\\n<li>回调抛错 → 下一环进 rejected</li>\\n</ul>\\n<p><strong>实践建议</strong>：不要习惯性地手写 <code>new Promise</code> 包装已经返回 <code>Promise</code> 的东西（这叫 Promise 反模式），也不要在 <code>executor</code> 里写业务逻辑 —— 它只该负责&quot;把回调式 API 转成 Promise&quot;这一件事。现在 <code>Node</code> 有 <code>util.promisify</code>，浏览器里大部分 API 也已经原生返回 <code>Promise</code>，真正需要手写构造函数的场景越来越少。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>首屏初始化代码打印顺序是 <code>1</code>、<code>2</code>、<code>3</code>、<code>4</code>，其中 <code>2</code> 位于 <code>new Promise</code> 的执行器内，<code>4</code> 位于 <code>then</code>；同事断言构造 <code>Promise</code> 会把整段逻辑异步化，你怎么纠正？</strong></p>\\n<p>执行器会在构造时同步立即运行，所以其中的 <code>2</code> 先于后续同步代码 <code>3</code>，只有 <code>then</code> 回调稍后执行。把大计算放进执行器仍会阻塞主线程；<code>Promise</code> 负责结果和链式传播，不提供后台线程或自动调度能力。</p>\\n</li>\\n<li>\\n<p><strong>文件预览页把 <code>fs.readFile</code> 包成 <code>Promise</code>，回调收到错误时执行 <code>throw err</code>，线上却没有进入链尾 <code>catch</code>；你会怎样改？</strong></p>\\n<p>异步回调执行时，构造函数执行器的同步调用栈已经结束，因此其中的 <code>throw</code> 不会自动转成该 <code>Promise</code> 的拒绝。应在错误分支显式调用 <code>reject(err)</code>，成功分支调用 <code>resolve(data)</code>；只有执行器同步阶段抛出的异常才会自动拒绝。</p>\\n</li>\\n<li>\\n<p><strong>数据层已有 <code>fetchUser()</code> 返回 <code>Promise</code>，新同事仍写 <code>new Promise(resolve =&gt; resolve(fetchUser()))</code>；你会要求删除这层包装吗？</strong></p>\\n<p>会，直接返回 <code>fetchUser()</code> 更清晰，额外构造没有改变最终状态，因为 <code>resolve</code> 遇到 <code>thenable</code> 本来就会跟随其落定结果。手写构造器更适合把回调式 <code>API</code> 转换为 <code>Promise</code>；包装不完整时还可能遗漏拒绝路径或增加调试噪音。</p>\\n</li>\\n<li>\\n<p><strong>消息发送适配器可能因底层库缺陷重复触发回调：先返回成功，随后又报告超时；消费方要求保证只处理一次，<code>Promise</code> 包装能否兜底？</strong></p>\\n<p>能兜住结果层面的重复落定：首次 <code>resolve</code> 或 <code>reject</code> 生效后，后续调用都会被忽略，消费方只看到一个终态。这不能阻止底层回调继续执行或产生副作用，因此仍应修复重复触发，并在需要时主动注销监听器。</p>\\n</li>\\n<li>\\n<p><strong>上传接口执行 <code>resolve(serverTask)</code>，其中 <code>serverTask</code> 是稍后才完成的 <code>Promise</code>；产品认为外层应立即成功并把任务对象交给页面，你如何判断？</strong></p>\\n<p>外层不会立即把它当普通值交付，而会跟随 <code>serverTask</code>，等其落定后采用相同结果或失败状态。这种吸收 <code>thenable</code> 的规则让链条保持扁平；若页面确实要立即取得任务句柄，应把它包装在普通对象中，避免直接作为 <code>resolve</code> 参数。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_25-谈一谈你了解ecmascript6的新特性\\"><a class=\\"header-anchor\\" href=\\"#_25-谈一谈你了解ecmascript6的新特性\\">#</a> 25 谈一谈你了解ECMAScript6的新特性？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>和「22 ES6 的了解」是同一类开放题，按五类组织：作用域声明 / 语法糖 / 数据结构 / 异步方案 / 元编程与模块化</li>\\n<li>每一类只举一个例子并说清<strong>它解决了什么老问题</strong>，比罗列二十个 API 更有说服力</li>\\n<li>准备一个&quot;在项目里真用上并有数字收益&quot;的例子，面试官几乎必追问</li>\\n<li>也要准备&quot;哪个特性不该滥用&quot;——箭头函数用在对象方法上、深层解构在中间层为 <code>undefined</code> 时抛错</li>\\n<li>加分句：<code>ES6</code> 之后改年度发布，实际能用什么由 <code>browserslist</code> + <code>Babel</code> + <code>core-js</code> 决定</li>\\n</ul>\\n<p><strong><code>ECMAScript 6</code> 的新特性可以概括为更安全的声明、更简洁的表达方式，以及更完整的异步和模块能力。</strong> <code>let</code>、<code>const</code> 提供块级作用域和常量，解构、模板字符串与扩展运算符让常见的数据处理更直接。<code>Symbol</code>、<code>Map</code>、<code>Set</code> 和 <code>for...of</code> 补充了类型、结构与遍历能力。工程中更关键的是 <code>Promise</code>、<code>Generator</code>、<code>Class</code> 和 <code>Module</code>；<code>async</code> 函数则属于后续版本。</p>\\n</section>\\n<ul>\\n<li>块级作用区域              <code>let a = 1;</code></li>\\n<li>可定义常量                <code>const PI = 3.141592654;</code></li>\\n<li>变量解构赋值              <code>var [a, b, c] = [1, 2, 3];</code></li>\\n<li>字符串的扩展(模板字符串)  <code>var sum =</code>${a + b}<code>;</code></li>\\n<li>数组的扩展(转换数组类型)   <code>Array.from($(\'li\'));</code></li>\\n<li>函数的扩展(扩展运算符)     <code>[1, 2].push(...[3, 4, 5]);</code></li>\\n<li>对象的扩展(同值相等算法)   <code>Object.is(NaN, NaN);</code></li>\\n<li>新增数据类型(Symbol)      <code>let uid = Symbol(\'uid\');</code></li>\\n<li>新增数据结构(Map)        <code>let set = new Set([1, 2, 2, 3]);</code></li>\\n<li>for...of循环            <code>for(let val of arr){};</code></li>\\n<li>Promise对象            <code>var promise = new Promise(func);</code></li>\\n<li>Generator函数          <code>function* foo(x){yield x; return x*x;}</code></li>\\n<li>引入Class(类)          <code>class Foo {}</code></li>\\n<li>引入模块体系            <code>export default func;</code></li>\\n<li>引入async函数[ES7]</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>async function asyncPrint(value, ms) {\\n      await timeout(ms);\\n      console.log(value)\\n     }\\n     \\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>报表页用 <code>const rows = []</code>，随后执行 <code>rows.push(item)</code> 没有报错；评审者据此认为 <code>const</code> 根本不是常量，你怎样回应？</strong></p>\\n<p><code>const</code> 约束的是变量绑定不能再次赋值，并不冻结数组或对象内部内容，因此 <code>push</code> 合法而 <code>rows = []</code> 会报错。若业务要求数据不可变，还需采用冻结或不可变更新策略；仅把 <code>let</code> 改成 <code>const</code> 不能阻止深层修改。</p>\\n</li>\\n<li>\\n<p><strong>活动页要把类数组的节点集合转为数组、给按钮列表去重并遍历值，团队准备分别手写循环；你会选哪些 <code>ES6</code> 能力？</strong></p>\\n<p>节点集合可用 <code>Array.from</code> 转成数组，去重可借助 <code>Set</code>，遍历可使用 <code>for...of</code>，这些能力分别解决类型转换、唯一值集合和可迭代对象遍历。仍要先确认输入是否符合对应协议，不能把 <code>Set</code> 当成带任意键值映射的 <code>Map</code>。</p>\\n</li>\\n<li>\\n<p><strong>公共组件库既要发布 <code>class</code>、模块和模板字符串写法，又要供旧环境使用；构建负责人说源码能解析就代表运行时一定兼容，你会接受吗？</strong></p>\\n<p>不会，语法能否被构建工具处理与运行时是否拥有 <code>Promise</code>、<code>Map</code>、<code>Symbol</code> 等能力是两回事。应按目标环境分别验证语法转换和 <code>API</code> 支持，并配置必要的 <code>polyfill</code>；无法可靠补齐的能力需要降级实现或提高环境基线。</p>\\n</li>\\n<li>\\n<p><strong>线上列表去重后仍出现两个内容相同的商品对象，代码使用 <code>new Set(items)</code>；值班同事怀疑 <code>Set</code> 失效，你如何排查？</strong></p>\\n<p>先检查重复项是否是两个不同对象引用；<code>Set</code> 能按值消除重复的原始值，但不会按对象字段做深比较。若业务唯一性由商品 <code>id</code> 决定，应基于该键构建索引或使用 <code>Map</code>，并明确冲突时保留首项还是末项。</p>\\n</li>\\n<li>\\n<p><strong>架构评审中，一方主张所有模块只用默认导出，另一方希望工具能明确分析多个能力并裁剪未使用代码；你会如何取舍？</strong></p>\\n<p>需要暴露多个独立能力时，具名 <code>export</code> 更能表达模块边界，也便于静态分析具体引用；只有模块天然代表单一主能力时，默认导出才更顺手。无论采用哪种形式，<code>ES</code> <code>Module</code> 的静态结构才是分析基础，实际裁剪仍受构建配置和副作用影响。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_26-object-is-与原来的比较操作符-、-的区别\\"><a class=\\"header-anchor\\" href=\\"#_26-object-is-与原来的比较操作符-、-的区别\\">#</a> 26 <a href=\\"http://Object.is\\">Object.is</a>() 与原来的比较操作符 ===、== 的区别？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>JS</code> 有<strong>四种</strong>相等判断：<code>==</code>（宽松，会隐式转换）、<code>===</code>（严格）、同值零（<code>includes</code>/<code>Set</code>）、同值（<code>Object.is</code>）</li>\\n<li><code>Object.is</code> 只在两处和 <code>===</code> 不同：认为 <code>NaN</code> 等于 <code>NaN</code>、认为 <code>+0</code> 不等于 <code>-0</code></li>\\n<li><code>indexOf</code> 用的是 <code>===</code> 所以找不到 <code>NaN</code>，<code>includes</code> 用同值零所以能找到</li>\\n<li><code>==</code> 的转换规则不满足传递性（<code>\'\' == 0</code> 且 <code>\'0\' == 0</code> 但 <code>\'\' != \'0\'</code>），唯一被接受的用法是 <code>x == null</code></li>\\n<li>实际用途：<code>React</code> 的 <code>useState</code> 用 <code>Object.is</code> 判断值有没有变，避免 <code>NaN</code> 导致的重复渲染</li>\\n</ul>\\n<p><strong><code>==</code> 会做隐式类型转换，<code>===</code> 不转换类型，而 <code>Object.is()</code> 与 <code>===</code> 基本一致，只对 <code>NaN</code> 和正负零采用不同判断。</strong> <code>Object.is(NaN, NaN)</code> 为 <code>true</code>，但它认为 <code>+0</code> 与 <code>-0</code> 不相等。相比之下，<code>==</code> 的转换规则复杂且可能不满足传递性，通常只保留 <code>x == null</code> 这种同时判断 <code>null</code> 和 <code>undefined</code> 的用法。实际开发一般使用 <code>===</code>，判断状态是否变化或处理 <code>NaN</code> 等边界时再考虑 <code>Object.is()</code>。</p>\\n</section>\\n<p><code>JS</code> 里一共有<strong>四种</strong>相等判断，从宽松到严格：</p>\\n<table>\\n<thead>\\n<tr>\\n<th>算法</th>\\n<th>对应</th>\\n<th>特点</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>宽松相等</td>\\n<td><code>==</code></td>\\n<td>会做隐式类型转换</td>\\n</tr>\\n<tr>\\n<td>严格相等</td>\\n<td><code>===</code></td>\\n<td>不转换类型，但 <code>NaN !== NaN</code>、<code>+0 === -0</code></td>\\n</tr>\\n<tr>\\n<td>同值零</td>\\n<td><code>includes</code>、<code>Set</code>、<code>Map</code> 的键</td>\\n<td>和 <code>===</code> 一样，但认为 <code>NaN</code> 等于 <code>NaN</code></td>\\n</tr>\\n<tr>\\n<td>同值</td>\\n<td><code>Object.is</code></td>\\n<td>在同值零基础上，还区分 <code>+0</code> 和 <code>-0</code></td>\\n</tr>\\n</tbody>\\n</table>\\n<pre class=\\"language-js\\"><code>NaN === NaN            // false\\nObject.is(NaN, NaN)    // true  ✅\\n[NaN].includes(NaN)    // true  ✅（同值零）\\n[NaN].indexOf(NaN)     // -1    ❌（indexOf 用的是 ===）\\n\\n+0 === -0              // true\\nObject.is(+0, -0)      // false ✅\\n</code></pre>\\n<p><strong><code>Object.is</code> 只在这两个边缘情况上和 <code>===</code> 不同</strong>，其余完全一致 —— 它不是&quot;更严格的 <code>===</code>&quot;，只是修了这两个历史遗留的怪异行为。</p>\\n<p><strong><code>==</code> 为什么要避免？</strong> 它的转换规则很反直觉：</p>\\n<pre class=\\"language-js\\"><code>&#39;&#39; == 0          // true\\n&#39;0&#39; == 0         // true\\n&#39;&#39; == &#39;0&#39;        // false   ← 不传递！\\nnull == undefined // true\\nnull == 0        // false   ← null 只和 undefined 相等\\n[] == false      // true\\n</code></pre>\\n<p>规则复杂且不满足传递性，所以规范和所有主流 lint 规则都要求用 <code>===</code>。唯一被广泛接受的例外是 <code>x == null</code> —— 它能一次判断 <code>null</code> 和 <code>undefined</code>，比 <code>x === null || x === undefined</code> 简洁，而且没有歧义。</p>\\n<p><strong>实际会用到 <code>Object.is</code> 的地方：</strong></p>\\n<ul>\\n<li><strong>判断 <code>NaN</code></strong>：不过更直接的是 <code>Number.isNaN(x)</code></li>\\n<li><strong>框架内部的浅比较</strong>：<code>React</code> 的 <code>Object.is</code> 就用在 <code>useState</code> 的&quot;值没变就不重渲染&quot;判断上 —— 这样 <code>setState(NaN)</code> 两次不会重复渲染</li>\\n<li><strong>区分 <code>+0</code>/<code>-0</code></strong>：极少见，通常出现在数学库里</li>\\n</ul>\\n<p><strong>日常业务代码里几乎用不到 <code>Object.is</code></strong>，这题考的是你知不知道 <code>JS</code> 有四种相等算法、以及 <code>NaN</code> 和 <code>±0</code> 这两个历史包袱。能顺带说出 <code>React</code> 用它做状态比较，是很好的加分点。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>数据清洗页执行 <code>NaN === NaN</code> 得到 <code>false</code>，但把 <code>NaN</code> 放进数组后 <code>includes(NaN)</code> 又得到 <code>true</code>；同事认为浏览器比较规则不一致，你怎么解释？</strong></p>\\n<p>两处使用的相等算法不同：<code>===</code> 认为 <code>NaN</code> 不等于自身，而 <code>includes</code> 使用同值零，认为两个 <code>NaN</code> 相等。若目标只是判断单值是否为 <code>NaN</code>，优先使用 <code>Number.isNaN</code>；改用 <code>indexOf</code> 又会回到 <code>===</code> 的行为。</p>\\n</li>\\n<li>\\n<p><strong>财务计算模块需要区分 <code>+0</code> 与 <code>-0</code>，普通业务代码则只想做严格比较；评审中有人提议全局把 <code>===</code> 替换成 <code>Object.is</code>，你会同意吗？</strong></p>\\n<p>不会，<code>Object.is</code> 只在 <code>NaN</code> 和正负零两个边缘行为上与 <code>===</code> 不同，并非普遍意义上更严格。数学模块可在确需保留零符号时局部使用，日常判断继续用 <code>===</code> 更符合预期；全局替换可能改变零值分支。</p>\\n</li>\\n<li>\\n<p><strong>表单校验中有人写 <code>value == null</code> 同时拦截未传值和空值，代码规范却禁止所有 <code>==</code>；你会保留还是改写？</strong></p>\\n<p>这是一种边界清楚的例外，因为 <code>x == null</code> 只会匹配 <code>null</code> 和 <code>undefined</code>，没有其他宽松转换歧义。若团队规范不允许例外，可显式写两个严格判断；在读取与默认值场景，也可视语义改用 <code>?.</code> 或 <code>??</code>。</p>\\n</li>\\n<li>\\n<p><strong>线上筛选条件出现 <code>[] == false</code> 为 <code>true</code>，但 <code>[] === false</code> 为 <code>false</code>；值班同事准备在相关分支继续补更多 <code>==</code> 判断，你会怎样止损？</strong></p>\\n<p>应先移除依赖宽松相等的分支，并在输入边界明确转换类型，因为 <code>==</code> 会执行复杂的隐式转换，容易产生反直觉结果。修复时记录原始值和类型，统一规范化后再用 <code>===</code>；盲目追加宽松判断只会扩大不可预测范围。</p>\\n</li>\\n<li>\\n<p><strong>状态管理组件连续两次写入 <code>NaN</code>，负责人希望第二次被视为“值没变”，但用 <code>===</code> 比较总判为变化；该选 <code>Object.is</code> 还是自定义判断？</strong></p>\\n<p>此处 <code>Object.is</code> 更贴合需求，因为它认为 <code>NaN</code> 与自身相同，框架浅比较也常利用这一语义避免重复更新。需要同时确认业务是否希望区分 <code>+0</code> 和 <code>-0</code>，因为 <code>Object.is</code> 会把它们视为不同；若不希望区分，应采用同值零语义。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_27-什么是-babel\\"><a class=\\"header-anchor\\" href=\\"#_27-什么是-babel\\">#</a> 27 什么是 Babel</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>Babel</code> 是 <code>JS</code> 编译器，三段式：<code>parser</code> 解析成 <code>AST</code> → 插件遍历转换 → <code>generator</code> 生成代码</li>\\n<li><code>preset</code> 只是插件的打包，<code>preset-env</code> 会按 <code>browserslist</code> 基线<strong>自动决定转哪些</strong>，能少转就少转</li>\\n<li>⚠️ <strong>只转语法不转 API</strong> —— <code>Array.at</code>、<code>Object.fromEntries</code> 要靠 <code>core-js</code> 补，这是线上白屏的常见原因</li>\\n<li>它早已不只是转 <code>ES6</code>：<code>JSX</code>、<code>TS</code> 类型擦除、按需引入、<code>Fast Refresh</code>、装饰器全是 <code>Babel</code> 插件</li>\\n<li>应用配 <code>useBuiltIns: \'usage\'</code>，<strong>库要配 <code>transform-runtime</code></strong> 避免污染使用方全局</li>\\n<li><code>SWC</code>/<code>esbuild</code> 已在速度上取代它，<code>Babel</code> 剩下的优势是插件生态和边缘语法支持</li>\\n</ul>\\n<p><strong><code>Babel</code> 是一个 <code>JavaScript</code> 编译器，负责把源码解析成 <code>AST</code>，通过插件转换后再生成目标代码。</strong> <code>preset</code> 本质上是一组插件，<code>@babel/preset-env</code> 会依据 <code>browserslist</code> 决定需要转换哪些语法。它只转换语法，不会自动补齐 <code>Array.at()</code>、<code>Object.fromEntries()</code> 这类新 <code>API</code>，兼容旧环境还要配合 <code>core-js</code>。应用通常按使用情况注入 <code>polyfill</code>，开发库则更适合用 <code>transform-runtime</code>，避免污染使用方的全局环境。</p>\\n</section>\\n<p><code>Babel</code> 是一个 <code>JavaScript</code> 编译器。和「2 babel 是什么」那题从流程角度讲不同，这里换个角度看它在<strong>工具链里的位置</strong>。</p>\\n<p><strong>它的三段式流程：</strong></p>\\n<pre class=\\"language-text\\"><code>源码 → @babel/parser（解析）→ AST → 插件遍历转换 → @babel/generator（生成）→ 目标代码\\n</code></pre>\\n<p>真正干活的是中间那层插件。<code>preset</code> 只是一组插件的打包，<code>@babel/preset-env</code> 会根据你的 <code>browserslist</code> 基线<strong>自动决定启用哪些插件</strong>——目标浏览器都支持箭头函数就不转它，能少转一点是一点。</p>\\n<p><strong>一条必须记住的边界：只转语法，不转 API。</strong></p>\\n<pre class=\\"language-js\\"><code>const f = () =&gt; {}          // ✅ 语法，会被转成 function\\n[1, 2].at(-1)               // ❌ API，不转，低版本环境直接报错\\nObject.fromEntries(pairs)   // ❌ 同上\\n</code></pre>\\n<p>补 <code>API</code> 要靠 <code>core-js</code>，通过 <code>useBuiltIns</code> 注入。这条边界是&quot;本地跑得好好的，线上老手机白屏&quot;最常见的原因。</p>\\n<p><strong><code>Babel</code> 早已不只是&quot;<code>ES6</code> 转 <code>ES5</code>&quot;：</strong></p>\\n<table>\\n<thead>\\n<tr>\\n<th>插件</th>\\n<th>作用</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>@babel/preset-react</code></td>\\n<td><code>JSX</code> → <code>jsx()</code> 调用</td>\\n</tr>\\n<tr>\\n<td><code>@babel/preset-typescript</code></td>\\n<td>擦除类型（只删不校验，类型检查还得靠 <code>tsc</code>）</td>\\n</tr>\\n<tr>\\n<td><code>babel-plugin-import</code></td>\\n<td>组件库按需引入，减小包体</td>\\n</tr>\\n<tr>\\n<td><code>react-refresh/babel</code></td>\\n<td>热更新时保留组件状态</td>\\n</tr>\\n<tr>\\n<td><code>@babel/plugin-proposal-decorators</code></td>\\n<td>装饰器等提案阶段语法</td>\\n</tr>\\n</tbody>\\n</table>\\n<p>所以它的定位是<strong>整个 <code>JS</code> 工具链的 <code>AST</code> 处理层</strong> —— 任何&quot;在编译期改写代码&quot;的需求都可以写成 <code>Babel</code> 插件。理解这一点，看 <code>Vue</code>/<code>React</code> 的编译期优化（静态提升、自动 memo）时就不会觉得神秘。</p>\\n<p><strong>写库和写应用的配置不一样：</strong></p>\\n<ul>\\n<li><strong>应用</strong>：用 <code>useBuiltIns: \'usage\'</code> + <code>corejs: 3</code>，按实际用到的 <code>API</code> 注入 polyfill</li>\\n<li><strong>库</strong>：用 <code>@babel/plugin-transform-runtime</code>，把 helper 和 polyfill 改成从模块引入，避免污染使用方的全局环境</li>\\n</ul>\\n<p><strong>现在的替代品</strong>：<code>SWC</code>（Rust）和 <code>esbuild</code>（Go）实现了同样的能力，速度快一个数量级，<code>Next.js</code> 和 <code>Vite</code> 都已经默认改用它们。<code>Babel</code> 剩下的优势是插件生态最全、对提案阶段的边缘语法支持最完整。新项目可以直接用 <code>SWC</code>，除非依赖了某个只有 <code>Babel</code> 版本的插件。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个兼容旧手机的活动页经过 <code>@babel/preset-env</code> 构建后，箭头函数已经消失，但执行 <code>[1, 2].at(-1)</code> 仍然白屏，为什么？</strong></p>\\n<p><code>Babel</code> 转换的是语法，默认不会补齐运行环境缺失的 <code>API</code>，所以旧浏览器仍找不到 <code>Array.prototype.at</code>。应根据 <code>browserslist</code> 配置 <code>core-js</code> 与 <code>useBuiltIns</code>，同时检查最终产物是否实际注入对应 <code>polyfill</code>；目标环境不明确时容易出现漏补或包体膨胀。</p>\\n</li>\\n<li>\\n<p><strong>业务应用配置了 <code>useBuiltIns: &quot;usage&quot;</code> 和 <code>corejs: 3</code>，团队却准备把同一套配置直接用于发布组件库，这会给使用方带来什么风险？</strong></p>\\n<p>应用可以按使用情况注入 <code>polyfill</code>，但组件库更适合用 <code>@babel/plugin-transform-runtime</code> 将 <code>helper</code> 和相关能力改成模块引用。直接沿用应用配置可能修改使用方的全局环境，并让依赖关系难以控制；代价是库需要正确声明运行时依赖，且仍要核对目标环境。</p>\\n</li>\\n<li>\\n<p><strong>一个 <code>TypeScript</code> 项目删掉 <code>tsc --noEmit</code>，只保留 <code>@babel/preset-typescript</code> 后仍能正常产出 <code>JavaScript</code>，能否据此认为类型安全没有变化？</strong></p>\\n<p>不能，<code>@babel/preset-typescript</code> 主要负责擦除类型标注，并不执行类型检查，构建成功不能证明类型正确。应在本地检查或 <code>CI</code> 中单独运行 <code>tsc --noEmit</code>；若使用依赖跨文件类型信息的语法，还要验证 <code>Babel</code> 单文件转换的支持边界。</p>\\n</li>\\n<li>\\n<p><strong>线上包里出现了本应被目标浏览器原生支持的语法转换代码和大量 <code>helper</code>，你会从 <code>Babel</code> 配置的哪几层开始排查？</strong></p>\\n<p>先核对 <code>browserslist</code> 是否被正确读取，再检查 <code>@babel/preset-env</code> 的目标配置、插件重复加载以及生成产物。<code>preset</code> 只是插件集合，真正的改写由插件完成；目标基线过旧或多套配置叠加都会造成过度转换，但移除插件前必须确认全部运行环境。</p>\\n</li>\\n<li>\\n<p><strong>新项目在 <code>SWC</code>、<code>esbuild</code> 和 <code>Babel</code> 之间选型，构建负责人强调速度，框架团队却依赖自定义编译插件，你会怎么定？</strong></p>\\n<p>若需求主要是常规语法转换，可优先评估 <code>SWC</code> 或 <code>esbuild</code>；若依赖现成的 <code>Babel</code> 插件、提案语法或复杂 <code>AST</code> 改写，保留 <code>Babel</code> 更稳妥。迁移前应列出插件兼容矩阵并验证产物，速度收益不能抵消重写插件及语义偏差的成本。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_28-symbol-有什么用处\\"><a class=\\"header-anchor\\" href=\\"#_28-symbol-有什么用处\\">#</a> 28 symbol 有什么用处</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>第一层用途：独一无二的属性键，给第三方对象挂元数据不怕撞名</li>\\n<li>第二层：模拟半私有 —— 不被 <code>for...in</code>/<code>Object.keys</code>/<code>JSON.stringify</code> 遍历，但 <code>getOwnPropertySymbols</code> 仍能拿到</li>\\n<li><strong>第三层（最重要）</strong>：知名 Symbol 定制语言行为 —— <code>iterator</code>、<code>asyncIterator</code>、<code>hasInstance</code>、<code>toPrimitive</code>、<code>toStringTag</code></li>\\n<li><code>Symbol.for()</code> 走全局注册表，跨模块可复用；<code>React</code> 的 <code>$$typeof</code> 靠它 + &quot;JSON 表达不出 Symbol&quot; 防伪造元素</li>\\n<li>被追问&quot;还有吗&quot;时，答&quot;它给了语言一套可扩展的协议接口&quot;比继续列场景更有分量</li>\\n</ul>\\n<p><strong><code>Symbol</code> 既能创建不会撞名的属性键，也提供了一套定制语言内置行为的协议接口。</strong> 它适合给第三方对象挂元数据，而且不会被 <code>Object.keys()</code>、<code>for...in</code> 或 <code>JSON.stringify()</code> 遍历到，但仍可通过 <code>Object.getOwnPropertySymbols()</code> 获取，所以不是真正的私有属性。实现 <code>Symbol.iterator</code>、<code>Symbol.toPrimitive</code> 等知名 <code>Symbol</code>，可以改变对象参与遍历或类型转换时的表现。跨模块需要共享同一个标识时，应使用 <code>Symbol.for()</code>，而不是每次调用都会创建新值的 <code>Symbol()</code>。</p>\\n</section>\\n<p><code>Symbol</code> 的用处可以分三层，面试时按这个顺序说会显得有深度：</p>\\n<p><strong>第一层：独一无二的属性键（大多数人只答到这里）</strong></p>\\n<pre class=\\"language-js\\"><code>const CACHE = Symbol(&#39;cache&#39;)\\nthirdPartyObj[CACHE] = myData   // 绝不会和别人的属性冲突\\n</code></pre>\\n<p>给第三方对象或框架实例挂自己的元数据时，用字符串键有撞名风险，<code>Symbol</code> 没有。</p>\\n<p><strong>第二层：模拟私有属性（半私有）</strong></p>\\n<p><code>Symbol</code> 属性不会被 <code>for...in</code>、<code>Object.keys()</code>、<code>JSON.stringify()</code> 遍历到：</p>\\n<pre class=\\"language-js\\"><code>const _state = Symbol(&#39;state&#39;)\\nclass Store {\\n  constructor() { this[_state] = {} }\\n}\\nObject.keys(new Store())   // [] —— 看不到\\n</code></pre>\\n<p>但 <code>Object.getOwnPropertySymbols()</code> 仍能拿到，所以是&quot;不容易被误碰&quot;而非真私有。真私有现在用 <code>class</code> 的 <code>#</code> 字段。</p>\\n<p><strong>第三层：定制语言内置行为 —— 这才是 <code>Symbol</code> 存在的主要理由</strong></p>\\n<p>规范预定义了一批<strong>知名 Symbol</strong>，实现它们就能改变对象在语言层面的表现：</p>\\n<pre class=\\"language-js\\"><code>class Range {\\n  constructor(a, b) { this.a = a; this.b = b }\\n  *[Symbol.iterator]() { for (let i = this.a; i &lt;= this.b; i++) yield i }\\n  get [Symbol.toStringTag]() { return &#39;Range&#39; }\\n  [Symbol.toPrimitive](hint) { return hint === &#39;number&#39; ? this.b - this.a : `${this.a}~${this.b}` }\\n}\\nconst r = new Range(1, 4)\\n;[...r]                              // [1,2,3,4]      ← Symbol.iterator\\nObject.prototype.toString.call(r)    // &#39;[object Range]&#39; ← toStringTag\\n</code></pre>\\n<table>\\n<thead>\\n<tr>\\n<th>知名 Symbol</th>\\n<th>定制什么</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>Symbol.iterator</code></td>\\n<td><code>for...of</code>、扩展运算符、解构</td>\\n</tr>\\n<tr>\\n<td><code>Symbol.asyncIterator</code></td>\\n<td><code>for await...of</code></td>\\n</tr>\\n<tr>\\n<td><code>Symbol.hasInstance</code></td>\\n<td><code>instanceof</code> 的判断逻辑</td>\\n</tr>\\n<tr>\\n<td><code>Symbol.toPrimitive</code></td>\\n<td>对象参与 <code>+</code>、模板字符串时的转换</td>\\n</tr>\\n<tr>\\n<td><code>Symbol.toStringTag</code></td>\\n<td><code>Object.prototype.toString.call()</code> 的结果</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong>第四层：全局注册表 <code>Symbol.for()</code></strong></p>\\n<p><code>Symbol()</code> 每次新建，跨模块共享同一个 <code>Symbol</code> 要用 <code>Symbol.for()</code>：</p>\\n<pre class=\\"language-js\\"><code>Symbol.for(&#39;app.id&#39;) === Symbol.for(&#39;app.id&#39;)   // true\\n</code></pre>\\n<p><code>React</code> 元素上的 <code>$$typeof</code> 就是 <code>Symbol.for(\'react.element\')</code> —— 用全局注册表让不同副本的 <code>React</code> 也能互认，同时因为 <code>JSON</code> 表达不出 <code>Symbol</code>，攻击者无法伪造合法的 <code>React</code> 元素来注入。</p>\\n<p><strong>面试的关键</strong>：被追问&quot;还有吗&quot;时，答&quot;知名 Symbol 让 <code>JS</code> 有了一套可扩展的协议接口&quot;，比继续列举场景更有分量。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>监控 <code>SDK</code> 要给宿主页面的 <code>window</code> 和第三方实例挂内部状态，字符串键已经带了公司前缀，为什么仍可能改用 <code>Symbol</code>？</strong></p>\\n<p>公司前缀只能降低撞名概率，<code>Symbol()</code> 生成的键才具有身份唯一性，更适合在不受控对象上附加元数据。它还能避开 <code>Object.keys()</code>、<code>for...in</code> 和 <code>JSON.stringify()</code> 的常规遍历；但 <code>Object.getOwnPropertySymbols()</code> 仍可发现它，不能当作安全隔离。</p>\\n</li>\\n<li>\\n<p><strong>一个状态对象把敏感字段存到 <code>Symbol(\'token\')</code> 下，安全评审认为这样已经实现私有属性，你会接受吗？</strong></p>\\n<p>不会，<code>Symbol</code> 属性只是较难被常规遍历误碰，调用方仍能通过 <code>Object.getOwnPropertySymbols()</code> 找到并读取。自有类需要真正私有状态时应使用 <code>#</code> 私有字段；若必须给既有对象附加数据，<code>Symbol</code> 仍有价值，但不能承载保密承诺。</p>\\n</li>\\n<li>\\n<p><strong>两个独立加载的插件都执行 <code>Symbol(\'app.cache\')</code>，却无法读取对方写入宿主对象的缓存，问题出在哪里？</strong></p>\\n<p>描述文本相同不代表身份相同，<code>Symbol(\'app.cache\')</code> 每次调用都会产生新的键。确实需要跨模块或跨副本共享时，应约定并使用 <code>Symbol.for(\'app.cache\')</code>；全局注册表也会扩大键的共享范围，因此命名和访问边界需要统一治理。</p>\\n</li>\\n<li>\\n<p><strong>自定义 <code>Range</code> 在页面上能手动调用方法取值，但 <code>[...range]</code> 和 <code>for...of</code> 都报不可迭代，你会检查哪个协议入口？</strong></p>\\n<p>应检查对象是否实现了 <code>[Symbol.iterator]()</code>，以及该方法是否返回符合迭代器协议的对象或生成器。扩展运算符、解构和 <code>for...of</code> 都通过这个知名 <code>Symbol</code> 接入；只提供普通的 <code>next</code> 方法或拼错键名，不会自动成为可迭代对象。</p>\\n</li>\\n<li>\\n<p><strong>基础库团队想让一个权限对象参与 <code>instanceof</code> 判断，同时还能控制模板字符串里的展示结果，需要分别实现什么？</strong></p>\\n<p>可通过 <code>Symbol.hasInstance</code> 定制 <code>instanceof</code> 的判断逻辑，通过 <code>Symbol.toPrimitive</code> 控制对象转为字符串或数字时的结果。它们体现了知名 <code>Symbol</code> 作为语言协议接口的价值；过度改写内置直觉会降低可读性，规则必须稳定且有测试覆盖。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_29-模块化\\"><a class=\\"header-anchor\\" href=\\"#_29-模块化\\">#</a> 29 模块化</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>演进线：全局变量 → 命名空间 → <code>IIFE</code> → <code>AMD</code>/<code>CMD</code>（浏览器异步）→ <code>CommonJS</code>（Node 同步）→ <code>ESM</code>（语言级统一）</li>\\n<li><code>ESM</code> 与 <code>CJS</code> 的三个本质区别：<strong>静态分析</strong>（编译期确定依赖）、<strong>值的活绑定</strong>（不是拷贝）、<strong>异步加载</strong></li>\\n<li>静态结构是 <code>Tree Shaking</code> 的前提 —— <code>lodash</code> 主包是 <code>CJS</code> 所以摇不掉，要用 <code>lodash-es</code></li>\\n<li><code>import()</code> 动态导入是路由懒加载和代码分割的基础</li>\\n<li>发包要配好 <code>package.json</code> 的 <code>exports</code> 字段，精确声明各环境入口，否则使用方会引到错误格式的产物</li>\\n</ul>\\n<p><strong>模块化就是把代码按职责拆分并明确导入导出，现代前端通常以语言级的 <code>ESM</code> 为主。</strong> <code>CommonJS</code> 使用 <code>require()</code> 和 <code>module.exports</code>，依赖可动态确定且按值导出；<code>ESM</code> 的依赖结构可静态分析，并通过活绑定反映导出值的变化。静态结构让构建工具能够做 <code>Tree Shaking</code>，因此使用 <code>CommonJS</code> 产物时往往不如 <code>ESM</code> 容易移除无用代码。需要按路由或功能拆包时可以使用 <code>import()</code>，发布库则要正确配置 <code>package.json</code> 的 <code>exports</code>，避免使用方加载到错误格式的入口。</p>\\n</section>\\n<blockquote>\\n<p>在有 <code>Babel</code> 的情况下，我们可以直接使用 <code>ES6</code>的模块化</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// file a.js\\nexport function a() {}\\nexport function b() {}\\n// file b.js\\nexport default function() {}\\n\\nimport {a, b} from &#39;./a.js&#39;\\nimport XXX from &#39;./b.js&#39;\\n</code></pre>\\n<p><strong>CommonJS</strong></p>\\n<blockquote>\\n<p><code>CommonJs</code> 是 <code>Node</code> 独有的规范，浏览器中使用就需要用到 <code>Browserify</code>解析了。</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// a.js\\nmodule.exports = {\\n    a: 1\\n}\\n// or\\nexports.a = 1\\n\\n// b.js\\nvar module = require(&#39;./a.js&#39;)\\nmodule.a // -&gt; log 1\\n</code></pre>\\n<blockquote>\\n<p>在上述代码中，<code>module.exports</code> 和 <code>exports</code> 很容易混淆，让我们来看看大致内部实现</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>var module = require(&#39;./a.js&#39;)\\nmodule.a\\n// 这里其实就是包装了一层立即执行函数，这样就不会污染全局变量了，\\n// 重要的是 module 这里，module 是 Node 独有的一个变量\\nmodule.exports = {\\n    a: 1\\n}\\n// 基本实现\\nvar module = {\\n  exports: {} // exports 就是个空对象\\n}\\n// 这个是为什么 exports 和 module.exports 用法相似的原因\\nvar exports = module.exports\\nvar load = function (module) {\\n    // 导出的东西\\n    var a = 1\\n    module.exports = a\\n    return module.exports\\n};\\n</code></pre>\\n<blockquote>\\n<p>再来说说 <code>module.exports</code> 和<code>exports</code>，用法其实是相似的，但是不能对 <code>exports</code> 直接赋值，不会有任何效果。</p>\\n</blockquote>\\n<blockquote>\\n<p>对于 <code>CommonJS</code> 和 <code>ES6</code> 中的模块化的两者区别是：</p>\\n</blockquote>\\n<ul>\\n<li>前者支持动态导入，也就是 <code>require(${path}/xx.js)</code>，后者目前不支持，但是已有提案,前者是同步导入，因为用于服务端，文件都在本地，同步导入即使卡住主线程影响也不大。</li>\\n<li>而后者是异步导入，因为用于浏览器，需要下载文件，如果也采用同步导入会对渲染有很大影响</li>\\n<li>前者在导出时都是值拷贝，就算导出的值变了，导入的值也不会改变，所以如果想更新值，必须重新导入一次。</li>\\n<li>但是后者采用实时绑定的方式，导入导出的值都指向同一个内存地址，所以导入值会跟随导出值变化</li>\\n<li>后者会编译成 <code>require/exports</code> 来执行的</li>\\n</ul>\\n<p><strong>AMD</strong></p>\\n<blockquote>\\n<p><code>AMD</code> 是由 <code>RequireJS</code> 提出的</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>// AMD\\ndefine([&#39;./a&#39;, &#39;./b&#39;], function(a, b) {\\n    a.do()\\n    b.do()\\n})\\ndefine(function(require, exports, module) {   \\n    var a = require(&#39;./a&#39;)  \\n    a.doSomething()   \\n    var b = require(&#39;./b&#39;)\\n    b.doSomething()\\n})\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong><code>Node</code> 服务里有人写了 <code>exports = { start }</code>，调用方 <code>require()</code> 后却得到空对象，而 <code>exports.start = start</code> 正常，为什么？</strong></p>\\n<p>初始化时 <code>exports</code> 只是指向 <code>module.exports</code> 的局部引用，给它重新赋值只会断开这层引用，不会修改真正的导出对象。整体替换应写 <code>module.exports = { start }</code>；混用两种写法时还要留意后续属性赋值究竟落在哪个对象上。</p>\\n</li>\\n<li>\\n<p><strong>一个计数模块导出 <code>count</code> 并持续更新，页面迁到 <code>ES</code> 模块后导入方能看到变化，而旧的 <code>CommonJS</code> 解构写法仍停留在初始值，差异来自哪里？</strong></p>\\n<p><code>ES</code> 模块的具名导入是对导出绑定的实时引用，导出方更新后，导入方读取时能观察到新值。<code>CommonJS</code> 获取的是 <code>module.exports</code> 对象，而解构会把当时的属性值取到局部变量；若要观察后续变化，应保留模块对象引用并读取其属性。</p>\\n</li>\\n<li>\\n<p><strong>浏览器首屏代码根据用户权限才加载管理后台模块，评审却说 <code>ES</code> 模块不支持动态路径，所以只能改回 <code>require()</code>，你会怎么纠正？</strong></p>\\n<p>静态 <code>import</code> 的依赖在解析阶段确定，但运行时按需加载可使用 <code>import()</code>，它返回 <code>Promise</code>，适合代码分割和异步下载。动态表达式能否被打包器枚举仍受工具约束，路径完全不可分析时可能打入过多模块或在运行时失败。</p>\\n</li>\\n<li>\\n<p><strong>一个 <code>npm</code> 包同时输出 <code>CommonJS</code> 和 <code>ES</code> 模块，消费者报出 <code>{ default: fn }</code>，而另一套构建能直接调用 <code>fn</code>，你会先查什么？</strong></p>\\n<p>先核对包入口指向的产物格式，以及默认导出在 <code>CommonJS</code> 与 <code>ES</code> 模块之间的互操作规则。再查看转译器或打包器是否额外包装了 <code>default</code>；修复时应统一入口契约并做 <code>import</code>、<code>require</code> 两种消费测试，否则局部兼容代码会继续扩散。</p>\\n</li>\\n<li>\\n<p><strong>页面构建团队主张所有模块统一编译成 <code>require/exports</code>，性能团队却希望保留 <code>ES</code> 模块给打包器分析，应该如何取舍？</strong></p>\\n<p>面向旧运行环境时可以转换模块格式，但现代构建链通常需要静态的 <code>import</code>、<code>export</code> 关系进行依赖分析和摇树优化。应让源码保持 <code>ES</code> 模块，并只在确有兼容需求的产物中转换；过早降级为 <code>CommonJS</code> 会削弱静态分析能力，也会增加互操作成本。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_30-箭头函数的特点\\"><a class=\\"header-anchor\\" href=\\"#_30-箭头函数的特点\\">#</a> 30 箭头函数的特点</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>四个&quot;没有&quot;：没有自己的 <code>this</code>、没有 <code>arguments</code>、没有 <code>prototype</code>（不能 <code>new</code>）、没有自己的 <code>super</code>/<code>new.target</code></li>\\n<li><code>this</code> 在<strong>定义时</strong>从最近的非箭头函数捞，之后 <code>call</code>/<code>apply</code>/<code>bind</code> 都改不了 —— 是&quot;锁死&quot;不是&quot;绑定&quot;</li>\\n<li>不能用的四种场景：对象方法、原型方法、需要 <code>this</code> 指向元素的原生事件回调、<code>Vue</code> 的 <code>methods</code>/<code>data</code></li>\\n<li>正好该用的：回调里要用外层 <code>this</code>、数组方法的简短回调、<code>React</code> 类组件的 class 字段</li>\\n<li>它是匿名的，直接作参数传时错误栈里没名字，热点路径上具名函数更好排查</li>\\n</ul>\\n<p><strong>箭头函数没有自己的 <code>this</code>、<code>arguments</code>、<code>prototype</code>、<code>super</code> 和 <code>new.target</code>，也不能通过 <code>new</code> 调用。</strong> 它的 <code>this</code> 取自最近的非箭头函数，之后用 <code>call</code>、<code>apply</code> 或 <code>bind</code> 也改不了，参数则通常用剩余参数接收。它适合数组回调或需要沿用外层 <code>this</code> 的场景，但不适合对象方法、原型方法、依赖元素 <code>this</code> 的事件回调以及 <code>Vue</code> 的 <code>methods</code>。直接把匿名箭头函数作为参数传递时，错误栈可读性也会稍差。</p>\\n</section>\\n<p>箭头函数不只是&quot;更短的函数写法&quot;，它有<strong>四个&quot;没有&quot;</strong>，每一个都会改变行为：</p>\\n<p><strong>① 没有自己的 <code>this</code></strong></p>\\n<p>它的 <code>this</code> 在<strong>定义时</strong>从外层作用域捞，之后永远不变，<code>call</code>/<code>apply</code>/<code>bind</code> 都改不了：</p>\\n<pre class=\\"language-js\\"><code>function a() {\\n  return () =&gt; () =&gt; console.log(this)\\n}\\na()()()   // this 取决于 a 是怎么调用的；直接调用 a() 时是 window（非严格模式）\\n</code></pre>\\n<p>嵌套多少层箭头函数都一样 —— <code>this</code> 由<strong>最近的那个非箭头函数</strong>决定。这正是它取代 <code>var self = this</code> 的原因。</p>\\n<p><strong>② 没有 <code>arguments</code></strong></p>\\n<p>用剩余参数代替：<code>const f = (...args) =&gt; args</code>。箭头函数里访问 <code>arguments</code> 拿到的是外层函数的。</p>\\n<p><strong>③ 没有 <code>prototype</code>，不能 <code>new</code></strong></p>\\n<pre class=\\"language-js\\"><code>const F = () =&gt; {}\\nnew F()   // TypeError: F is not a constructor\\n</code></pre>\\n<p><strong>④ 没有自己的 <code>super</code> 和 <code>new.target</code></strong>，同样取外层的。</p>\\n<p><strong>什么时候不能用箭头函数 —— 这是这题的实战考点：</strong></p>\\n<pre class=\\"language-js\\"><code>// ❌ 对象方法：this 指向外层作用域，不是 obj\\nconst obj = { name: &#39;x&#39;, getName: () =&gt; this.name }\\n\\n// ❌ 原型方法：箭头函数没有自己的 this，且挂在原型上没意义\\nFoo.prototype.render = () =&gt; { this.update() }\\n\\n// ❌ 需要 this 指向触发元素的原生事件回调\\nbtn.addEventListener(&#39;click&#39;, () =&gt; console.log(this))   // 不是 btn\\n\\n// ❌ Vue 的 methods / data：Vue 靠 bind 注入实例，箭头函数会让绑定失效\\n</code></pre>\\n<p><strong>什么时候正好该用：</strong></p>\\n<pre class=\\"language-js\\"><code>// ✅ 回调里要用外层 this\\nclass Timer {\\n  start() { setInterval(() =&gt; this.tick(), 1000) }\\n}\\n// ✅ 数组方法的简短回调\\nconst doubled = list.map((x) =&gt; x * 2)\\n// ✅ React 类组件的 class 字段（自动绑定实例）\\nhandleClick = () =&gt; { this.setState({}) }\\n</code></pre>\\n<p><strong>一个容易忽略的点</strong>：箭头函数是<strong>匿名</strong>的，赋值给变量后引擎会推断出名字，但直接作为参数传时栈里没有名字，调试和错误栈的可读性会差一些。热点路径上的具名函数更好排查。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>一个对象写成 <code>const user = { name: \'A\', getName: () =&gt; this.name }</code>，页面调用 <code>user.getName()</code> 却拿不到 <code>A</code>，调用位置看起来明明正确，原因是什么？</strong></p>\\n<p>箭头函数没有自己的 <code>this</code>，它从定义位置的外层词法作用域取值，因此不会因为作为 <code>user</code> 的方法调用就指向 <code>user</code>。这里应改用普通方法语法；若函数本来就不依赖接收者，才适合继续使用箭头函数。</p>\\n</li>\\n<li>\\n<p><strong><code>Vue</code> 组件的 <code>methods</code> 中使用箭头函数后，框架即使执行绑定也读不到实例字段，为什么 <code>bind</code> 没有效果？</strong></p>\\n<p>箭头函数的 <code>this</code> 在定义时由外层作用域决定，<code>call</code>、<code>apply</code> 和 <code>bind</code> 都不能重新指定它。<code>Vue</code> 需要通过绑定让方法访问组件实例，因此这里应使用普通函数；相反，需要固定外层实例的回调才适合箭头函数。</p>\\n</li>\\n<li>\\n<p><strong>一个通用事件工具原本用普通函数让调用方通过 <code>call(element)</code> 指定上下文，重构成箭头函数后测试全部失效，你会如何处理？</strong></p>\\n<p>这类函数的 <code>this</code> 应由调用方决定，改成箭头函数会直接破坏契约，因为传给 <code>call</code> 的首个参数不会成为它的上下文。应恢复普通函数，或把上下文改成显式参数；后者更清晰，但属于接口变更，需要同步所有调用方。</p>\\n</li>\\n<li>\\n<p><strong>日志显示某个箭头回调读取到外层函数的 <code>arguments</code>，而不是回调收到的参数，排查时应怎样改写？</strong></p>\\n<p>箭头函数没有自己的 <code>arguments</code>，其中的标识符会继续向外层函数查找，所以日志内容可能与回调实参无关。应在形参中明确接收需要的值，参数数量不定时使用 <code>(...args)</code>；依赖外层 <code>arguments</code> 虽然可行，但会让作用域关系难以辨认。</p>\\n</li>\\n<li>\\n<p><strong>组件团队想把所有短函数都改成箭头函数，其中包括构造器、原型方法和数组回调，这项规范为什么不可行？</strong></p>\\n<p>箭头函数没有 <code>prototype</code>，也不能通过 <code>new</code> 调用，同时原型方法通常需要动态接收实例 <code>this</code>，因此前两类不能机械替换。数组回调若只使用形参或外层上下文则很合适；选型标准应是调用语义，而不是函数长度。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_31-es5-es6-的继承除了写法以外还有什么区别\\"><a class=\\"header-anchor\\" href=\\"#_31-es5-es6-的继承除了写法以外还有什么区别\\">#</a> 31 ES5 / ES6 的继承除了写法以外还有什么区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>class</code> 必须用 <code>new</code> 调用，ES5 构造函数漏写 <code>new</code> 会污染全局（<code>this</code> 指向 <code>window</code>）</li>\\n<li><code>class</code> 的方法<strong>不可枚举</strong>，ES5 挂在 <code>prototype</code> 上的方法会被 <code>for...in</code> 遍历到</li>\\n<li><code>class</code> 声明<strong>不提升</strong>（有 TDZ），函数声明会提升 —— 所以 <code>class</code> 必须先定义后使用</li>\\n<li><strong>继承内置类型</strong>：<code>class X extends Array/Error</code> 能正确工作，ES5 寄生组合继承做不到（拿不到 <code>length</code> 联动和正确的 <code>stack</code>）</li>\\n<li>原因是 <code>super()</code> 先创建父类实例再把 <code>this</code> 交给子类，而 ES5 是先创建子类 <code>this</code> 再调父构造函数 —— 顺序相反</li>\\n</ul>\\n<p><strong>核心区别是创建 <code>this</code> 的顺序：<code>ES5</code> 先创建子类实例再调用父构造函数，<code>ES6</code> 则先通过 <code>super()</code> 创建父类实例，再由子类加工。</strong> 因此派生类必须先调用 <code>super()</code> 才能使用 <code>this</code>，否则实例化时会报错。除此之外，<code>class</code> 必须用 <code>new</code> 调用，类方法默认不可枚举，而且类声明存在暂时性死区，不能像函数声明那样提前使用。这个机制也让 <code>class X extends Array</code> 这类内置对象继承可以正确工作。</p>\\n</section>\\n<ul>\\n<li>ES5 的继承实质上是先创建子类的实例对象，然后再将父类的方法添加到 this 上（Parent.apply(this)）</li>\\n<li>ES6 的继承机制完全不同，实质上是先创建父类的实例对象 this（所以必须先调用父类的 super()方法），然后再用子类的构造函数修改 this。</li>\\n<li>ES5 的继承时通过原型或构造函数机制来实现。</li>\\n<li>ES6 通过 class 关键字定义类，里面有构造方法，类之间通过 extends 关键字实现继承。</li>\\n<li>子类必须在 constructor 方法中调用 super 方法，否则新建实例报错。因为子类没有自己的 this 对象，而是继承了父类的 this 对象，然后对其进行加工。如果不调用 super 方法，子类得不到 this 对象。</li>\\n<li>注意 super 关键字指代父类的实例，即父类的 this 对象。</li>\\n<li>注意：在子类构造函数中，调用 super 后，才可使用 this 关键字，否则报错。function 声明会提升，但不会初始化赋值。Foo 进入暂时性死区，类似于 let、const 声明变量</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>子类构造器里先执行 <code>this.ready = true</code>，下一行才调用 <code>super()</code>，页面实例化时立即报错，为什么不能先改自己的字段？</strong></p>\\n<p>派生类在 <code>super()</code> 返回前还没有可用的 <code>this</code>，因为实例要先由父类构造过程建立，再交给子类加工。应把所有 <code>this</code> 访问放到 <code>super()</code> 之后；若构造器存在条件分支，每条最终创建实例的路径都必须正确完成父类初始化。</p>\\n</li>\\n<li>\\n<p><strong>旧代码用 <code>Parent.call(this)</code> 做组合继承，父类迁成 <code>class Parent</code> 后子类开始报不能直接调用类构造器，应该怎么改？</strong></p>\\n<p><code>class</code> 构造器只能通过构造调用机制执行，不能再用 <code>call</code> 或 <code>apply</code> 借用。子类应迁为 <code>class Child extends Parent</code> 并在构造器中调用 <code>super()</code>；若暂时无法整体迁移，只能重新设计初始化接口，不能假装它仍是普通函数。</p>\\n</li>\\n<li>\\n<p><strong>一个 <code>ES5</code> 自定义错误通过 <code>Error.call(this, message)</code> 初始化后，线上日志缺少预期的 <code>stack</code>，并且 <code>instanceof</code> 表现异常，为什么内置类尤其容易暴露差异？</strong></p>\\n<p><code>ES5</code> 模式通常先创建子对象再调用父构造函数，但 <code>Error</code> 等内置构造过程可能自行创建并返回特殊实例，传入的 <code>this</code> 不一定得到完整内部状态。使用 <code>class CustomError extends Error</code> 和 <code>super(message)</code> 更符合父类先创建实例的机制；仍需验证目标运行环境的继承支持。</p>\\n</li>\\n<li>\\n<p><strong>代码审查中有人说 <code>super</code> 就是父类实例，所以准备把它存进变量后到处调用，这个理解哪里不准确？</strong></p>\\n<p>在派生构造器中，<code>super()</code> 表示调用父类构造过程并初始化当前实例，并不是一个可任意保存的普通父类对象。实例方法中的 <code>super.method()</code> 还涉及父类原型上的方法查找；把两种语境混为一谈，会误判 <code>this</code> 和方法分派行为。</p>\\n</li>\\n<li>\\n<p><strong>团队只为了语法统一，准备把稳定的 <code>ES5</code> 原型继承全部机械替换成 <code>class</code>，评审时你会要求验证哪些行为边界？</strong></p>\\n<p>需要验证构造器是否曾被不带 <code>new</code> 调用、是否依赖 <code>Parent.call(this)</code>、以及子类在父类初始化前是否访问 <code>this</code>。<code>class</code> 与 <code>extends</code> 不只是语法糖式替换，它们对构造调用和实例创建顺序有明确约束；迁移能提升一致性，但可能破坏旧代码依赖的宽松行为。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_32-全局作用域中-用-const-和-let-声明的变量不在-window-上-那到底在哪里-如何去获取\\"><a class=\\"header-anchor\\" href=\\"#_32-全局作用域中-用-const-和-let-声明的变量不在-window-上-那到底在哪里-如何去获取\\">#</a> 32 全局作用域中，用 const 和 let 声明的变量不在 window 上，那到底在哪里？如何去获取？</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>var</code> 和 <code>function</code> 声明的全局变量挂在 <code>window</code> 上；<code>let</code>/<code>const</code>/<code>class</code> <strong>不挂</strong></li>\\n<li>它们住在**「脚本作用域」(Script Scope)** 里 —— <code>DevTools</code> 断点的 <code>Scope</code> 面板能直接看到这一层</li>\\n<li>规范里全局环境由两部分组成：对象式环境记录（对应 <code>window</code>）+ 声明式环境记录（<code>let</code> 系列）</li>\\n<li>怎么取？<strong>直接用变量名</strong> —— 它就在全局作用域里，只是不挂在 <code>window</code> 上</li>\\n<li>这么设计是为了修掉&quot;全局 <code>var</code> 污染 <code>window</code>&quot;的历史缺陷；检测全局变量要用 <code>typeof x !== \'undefined\'</code></li>\\n<li><code>globalThis</code>（ES2020）统一了顶层对象访问，但它指向的仍是 <code>window</code> 那一半，找不到 <code>let</code> 变量</li>\\n</ul>\\n<p><strong>全局 <code>let</code>、<code>const</code> 和 <code>class</code> 位于脚本作用域的声明式环境记录中，直接使用变量名就能获取。</strong> 全局环境还包含对应 <code>window</code> 的对象式环境记录，传统的 <code>var</code> 和 <code>function</code> 声明才会进入这一部分，所以能通过 <code>window.xxx</code> 访问。这样设计避免了新式声明继续污染全局对象；判断变量是否存在时，可以用 <code>typeof x !== \'undefined\'</code>。<code>globalThis</code> 只是统一了顶层对象入口，同样访问不到这部分变量，而 <code>ESM</code> 顶层声明则属于模块作用域。</p>\\n</section>\\n<p><strong>先看现象：</strong></p>\\n<pre class=\\"language-js\\"><code>var a = 1\\nfunction f() {}\\nwindow.a   // 1    ✅ var 和 function 声明的全局变量挂在顶层对象上\\nwindow.f   // f(){}\\n\\nlet b = 2\\nconst c = 3\\nclass D {}\\nwindow.b   // undefined ❌\\nwindow.c   // undefined\\nwindow.D   // undefined\\n</code></pre>\\n<p><strong>它们在哪里？</strong></p>\\n<p>在一个叫**「脚本作用域」（Script Scope）** 的地方 —— 打开 <code>DevTools</code> 打个断点，在 <code>Scope</code> 面板里能清楚看到除了 <code>Global</code> 之外还有一个 <code>Script</code> 层级，<code>let</code>/<code>const</code>/<code>class</code> 声明的全局变量就住在那里。</p>\\n<p>规范里的说法是：全局环境记录（Global Environment Record）由两部分组成 ——</p>\\n<ul>\\n<li><strong>对象式环境记录</strong>：对应 <code>window</code>，<code>var</code> 和 <code>function</code> 声明放这里</li>\\n<li><strong>声明式环境记录</strong>：不对应任何对象，<code>let</code>/<code>const</code>/<code>class</code> 放这里</li>\\n</ul>\\n<p>两者共同构成全局作用域，所以查找变量时都能找到，但只有前者能通过 <code>window.xxx</code> 访问。</p>\\n<p><strong>怎么获取？直接用变量名就行：</strong></p>\\n<pre class=\\"language-js\\"><code>let b = 2\\nconsole.log(b)          // 2 ✅ 它就在全局作用域里，直接访问即可\\nconsole.log(window.b)   // undefined —— 只是不挂在 window 上而已\\n</code></pre>\\n<p><strong>为什么要这么设计？</strong></p>\\n<p>因为 <code>var</code> 挂 <code>window</code> 是一个历史缺陷：任何一个全局 <code>var</code> 都会污染全局对象，和浏览器内置属性撞名时后果不可预测（比如 <code>var name = \'x\'</code> 会覆盖 <code>window.name</code>）。<code>ES6</code> 引入新的声明方式时顺手修掉了这个问题 —— <strong>新的声明不再污染全局对象</strong>。</p>\\n<p><strong>实践中的两个影响：</strong></p>\\n<p><strong>① 判断&quot;某个全局变量存不存在&quot;的写法要改</strong></p>\\n<pre class=\\"language-js\\"><code>if (window.myLib) {}        // let 声明的检测不到\\nif (typeof myLib !== &#39;undefined&#39;) {}   // ✅ 通用写法\\n</code></pre>\\n<p><strong>② <code>globalThis</code>（ES2020）统一了顶层对象的访问方式</strong></p>\\n<p>以前浏览器是 <code>window</code>、<code>Node</code> 是 <code>global</code>、<code>Worker</code> 是 <code>self</code>，写跨环境代码要判断一圈；现在一律用 <code>globalThis</code>。但注意它指向的仍是<strong>对象式环境记录</strong>，<code>let</code> 声明的变量在它上面同样找不到。</p>\\n<p><strong>现代工程里这题的实际意义在下降</strong> —— 因为模块（<code>ESM</code>）里的顶层声明本来就是模块作用域的，连 <code>var</code> 都不会挂到 <code>window</code> 上。只有直接写在 <code>&lt;script&gt;</code>（非 module）里的代码才有这个区别。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>普通 <code>&lt;script&gt;</code> 里执行 <code>let token = \'x\'</code>，控制台输入 <code>token</code> 能拿到值，输入 <code>window.token</code> 却是 <code>undefined</code>；它究竟是不是全局变量？</strong></p>\\n<p>它仍是全局变量，只是存放在全局环境记录的声明式部分，而不是与 <code>window</code> 对应的对象式部分。标识符查找会覆盖这两部分，因此可直接访问 <code>token</code>；但不存在某个对象属性可供 <code>window.token</code> 读取。</p>\\n</li>\\n<li>\\n<p><strong>旧页面一直用 <code>if (window.SDK)</code> 检查第三方库，供应方把顶层声明从 <code>var SDK</code> 改成 <code>let SDK</code> 后检测失效，接入方该怎么处理？</strong></p>\\n<p>若双方代码处于可直接访问同一脚本全局的环境，可改用 <code>typeof SDK !== \'undefined\'</code> 检测。更稳妥的库接口是由供应方显式执行 <code>window.SDK = SDK</code>，把对外暴露变成契约；否则声明方式变化仍会破坏依赖。</p>\\n</li>\\n<li>\\n<p><strong>团队把浏览器、<code>Worker</code> 和 <code>Node</code> 的顶层访问统一成 <code>globalThis</code>，有人认为这样也能读到普通脚本里的 <code>const config</code>，这个判断对吗？</strong></p>\\n<p>不对，<code>globalThis</code> 统一的是不同运行环境中的顶层对象入口，不会改变词法声明的存储位置。<code>const config</code> 位于声明式环境记录，<code>globalThis.config</code> 仍是 <code>undefined</code>；只有显式设置对象属性后才能这样读取。</p>\\n</li>\\n<li>\\n<p><strong>线上控制台能直接打印 <code>featureFlag</code>，监控脚本上报的 <code>window.featureFlag</code> 却为空，如何确认是作用域差异而不是赋值时序问题？</strong></p>\\n<p>先在同一执行时点分别检查 <code>typeof featureFlag</code> 与 <code>window.featureFlag</code>，再通过断点查看 <code>Scope</code> 面板中的 <code>Script</code> 和 <code>Global</code> 层级。若变量出现在 <code>Script</code> 中，就是 <code>let</code>、<code>const</code> 或 <code>class</code> 的词法声明；若直接访问也不存在，才继续排查加载顺序。</p>\\n</li>\\n<li>\\n<p><strong>页面准备迁移到 <code>&lt;script type=&quot;module&quot;&gt;</code>，负责人仍要求靠 <code>window.xxx</code> 区分 <code>var</code> 与 <code>let</code>，这套排查思路还成立吗？</strong></p>\\n<p>不成立，<code>ESM</code> 顶层声明属于模块作用域，连顶层 <code>var</code> 也不会自动成为 <code>window</code> 属性。模块间应使用 <code>export</code> 和 <code>import</code> 传递能力；确需兼容旧脚本时再显式挂到 <code>globalThis</code>，代价是重新引入全局命名冲突。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_33-介绍下-set、map、weakset-和-weakmap-的区别\\"><a class=\\"header-anchor\\" href=\\"#_33-介绍下-set、map、weakset-和-weakmap-的区别\\">#</a> 33 介绍下 Set、Map、WeakSet 和 WeakMap 的区别</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>两个维度划分：<strong>存什么</strong>（值 / 键值对）× <strong>引用强弱</strong></li>\\n<li><code>Set</code>（值唯一，去重和 <code>O(1)</code> 查找）、<code>Map</code>（键可任意类型、保序、无原型干扰）</li>\\n<li><code>WeakSet</code>/<code>WeakMap</code>：成员/键<strong>只能是对象</strong>且是<strong>弱引用</strong>，不阻止垃圾回收</li>\\n<li>正因为条目会随 GC 消失，所以它们<strong>不可遍历、没有 <code>size</code>、没有 <code>clear</code></strong></li>\\n<li><code>WeakMap</code> 两个经典用途：给 DOM 节点挂数据（用 <code>Map</code> 就是 <code>Detached DOM</code> 泄漏）、<code>#</code> 出现前的类私有字段</li>\\n<li><code>Vue 3</code> 的依赖收集就用 <code>WeakMap</code> 存&quot;目标对象 → 依赖表&quot;，组件销毁后自动回收</li>\\n</ul>\\n<p><strong>四者可以看成“存值还是键值对”和“强引用还是弱引用”两组：<code>Set</code>、<code>WeakSet</code> 存值，<code>Map</code>、<code>WeakMap</code> 存键值对。</strong> <code>Set</code> 和 <code>Map</code> 支持任意类型、遍历和 <code>size</code>；弱集合的成员或键只能是对象，并且不会阻止对象被垃圾回收。因为其中的条目可能随时随对象消失，<code>WeakSet</code> 和 <code>WeakMap</code> 不可遍历，也没有 <code>size</code> 和 <code>clear</code>。需要去重或遍历时选前两者，给 <code>DOM</code> 节点等外部对象附加数据且不想影响其生命周期时，我一般会选 <code>WeakMap</code>。</p>\\n</section>\\n<p>四者可以按两个维度划分：<strong>存什么</strong>（值 / 键值对）和<strong>引用是强还是弱</strong>。</p>\\n<table>\\n<thead>\\n<tr>\\n<th></th>\\n<th>存什么</th>\\n<th>成员限制</th>\\n<th>引用</th>\\n<th style=\\"text-align:center\\">可遍历</th>\\n<th style=\\"text-align:center\\">有 <code>size</code></th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td><code>Set</code></td>\\n<td>值的集合</td>\\n<td>任意类型</td>\\n<td>强</td>\\n<td style=\\"text-align:center\\">✅</td>\\n<td style=\\"text-align:center\\">✅</td>\\n</tr>\\n<tr>\\n<td><code>Map</code></td>\\n<td>键值对</td>\\n<td>键可任意类型</td>\\n<td>强</td>\\n<td style=\\"text-align:center\\">✅</td>\\n<td style=\\"text-align:center\\">✅</td>\\n</tr>\\n<tr>\\n<td><code>WeakSet</code></td>\\n<td>值的集合</td>\\n<td><strong>只能是对象</strong></td>\\n<td><strong>弱</strong></td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">❌</td>\\n</tr>\\n<tr>\\n<td><code>WeakMap</code></td>\\n<td>键值对</td>\\n<td><strong>键只能是对象</strong></td>\\n<td><strong>键弱</strong></td>\\n<td style=\\"text-align:center\\">❌</td>\\n<td style=\\"text-align:center\\">❌</td>\\n</tr>\\n</tbody>\\n</table>\\n<p><strong><code>Set</code> / <code>Map</code>：</strong></p>\\n<ul>\\n<li><code>Set</code> 成员唯一，方法 <code>add</code>/<code>has</code>/<code>delete</code>/<code>clear</code>。主要用途是数组去重（<code>[...new Set(arr)]</code>）和 <code>O(1)</code> 的存在性判断。</li>\\n<li><code>Map</code> 的键可以是对象、函数、<code>NaN</code>；严格按插入顺序；不会被原型上的键干扰。方法 <code>set</code>/<code>get</code>/<code>has</code>/<code>delete</code>。</li>\\n</ul>\\n<p><strong><code>WeakSet</code> / <code>WeakMap</code> —— &quot;弱&quot;字是关键：</strong></p>\\n<p>弱引用意味着<strong>它不阻止垃圾回收</strong>。当一个对象除了 <code>WeakMap</code> 的键之外没有其他引用时，它会被回收，对应的条目自动消失。</p>\\n<p>正因为条目可能随时消失，它们<strong>不可遍历、没有 <code>size</code>、没有 <code>clear</code></strong> —— 否则结果不确定，也会给攻击者提供观测 GC 时机的侧信道。</p>\\n<p><strong><code>WeakMap</code> 的两个经典用途：</strong></p>\\n<pre class=\\"language-js\\"><code>// ① 给 DOM 节点挂关联数据，节点移除后数据自动回收\\nconst nodeData = new WeakMap()\\nnodeData.set(el, { clicks: 0 })\\n\\n// ② 类的私有数据（# 私有字段出现之前的标准做法）\\nconst privates = new WeakMap()\\nclass Store {\\n  constructor() { privates.set(this, { state: {} }) }\\n  get state() { return privates.get(this).state }\\n}\\n</code></pre>\\n<p><strong>用 <code>Map</code> 做第一种事就是内存泄漏</strong>：节点从文档移除后，<code>Map</code> 还强引用着它，节点和它的整棵子树都回收不掉 —— 这就是 <code>DevTools</code> 里能看到的 <code>Detached HTMLElement</code>。</p>\\n<p><strong>一个真实的应用</strong>：<code>Vue 3</code> 的响应式系统用 <code>WeakMap</code> 存&quot;目标对象 → 依赖表&quot;的映射。组件销毁、对象没人引用时，依赖表跟着自动回收，不需要任何手动清理逻辑。</p>\\n<p><strong>选型速查</strong>：需要遍历、需要 <code>size</code> → <code>Set</code>/<code>Map</code>；给外部对象附加数据且不想影响它的生命周期 → <code>WeakSet</code>/<code>WeakMap</code>。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>去重接口结果时，数组里既有两个 <code>NaN</code>，又有两个内容相同但引用不同的 <code>{id: 1}</code>，直接交给 <code>Set</code> 后能得到几个业务实体？</strong></p>\\n<p><code>Set</code> 会把重复的 <code>NaN</code> 合并，但两个独立对象仍会保留，因为对象按引用身份判断，而不是比较字段内容。若业务唯一性由 <code>id</code> 决定，应先按 <code>id</code> 建立 <code>Map</code> 或生成业务键；序列化对象可能受字段顺序和数据形态影响。</p>\\n</li>\\n<li>\\n<p><strong>编辑器要给上万个 <code>DOM</code> 节点保存交互状态，节点移除后不希望维护额外清理名单，应该选 <code>Map</code> 还是 <code>WeakMap</code>？</strong></p>\\n<p>应选以 <code>DOM</code> 节点为键的 <code>WeakMap</code>，这样关联表不会阻止失去其他引用的节点被垃圾回收。若改用 <code>Map</code>，被移除的节点仍会被键强引用，可能形成 <code>Detached HTMLElement</code>；代价是无法遍历全部状态或读取 <code>size</code>。</p>\\n</li>\\n<li>\\n<p><strong>权限模块既要用对象作为键缓存结果，又要在管理页展示全部缓存项和数量，架构师坚持使用 <code>WeakMap</code> 防泄漏，能同时满足吗？</strong></p>\\n<p>不能，<code>WeakMap</code> 没有遍历能力和 <code>size</code>，条目还可能随对象回收而消失，不适合作为管理页的数据源。需要完整枚举时应使用 <code>Map</code> 并设计显式淘汰或清理；是否泄漏取决于生命周期管理，不能只靠替换容器解决。</p>\\n</li>\\n<li>\\n<p><strong>线上堆快照出现大量已从文档移除的节点，代码用 <code>Map&lt;Element, Meta&gt;</code> 保存元数据，排查时怎样验证容器确实延长了节点生命周期？</strong></p>\\n<p>检查这些节点的引用链，若保留路径指向该 <code>Map</code> 的键，就说明强引用阻止了回收。可把仅用于附加数据的映射改成 <code>WeakMap</code>，同时确认业务代码没有其他引用；仅替换容器不能消除事件处理器或数组中的残留引用。</p>\\n</li>\\n<li>\\n<p><strong><code>Vue</code> 风格的响应式依赖表要保存“目标对象 → 依赖表”，同时要求目标无人使用后自动释放，为什么 <code>WeakMap</code> 比给对象直接加属性更合适？</strong></p>\\n<p><code>WeakMap</code> 能把元数据与目标对象关联，又不会因键的存在延长目标生命周期，也避免修改外部对象本身。直接加属性会污染对象并可能遇到不可扩展对象；不过依赖表若需要全量巡检，仍要另建可枚举的数据结构并承担清理成本。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"_34-promise-all-和-promise-allsettled-的比较\\"><a class=\\"header-anchor\\" href=\\"#_34-promise-all-和-promise-allsettled-的比较\\">#</a> 34 Promise.all() 和 Promise.allSettled()的比较</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>all</code>：全部成功才成功，<strong>任一失败立即短路</strong>，返回第一个错误 —— 适合&quot;缺一不可&quot;的场景</li>\\n<li><code>allSettled</code>：等全部落定，返回 <code>[{status, value|reason}]</code> 数组，<strong>不短路</strong> —— 适合&quot;部分失败可接受&quot;的场景</li>\\n<li>两者都<strong>不会取消</strong>已发出的请求：<code>all</code> 短路后其余请求照样跑完，只是结果没人要</li>\\n<li>另外两个：<code>race</code>（第一个落定的胜出，无论成败，常用于超时）、<code>any</code>（第一个<strong>成功</strong>的胜出，全失败才失败）</li>\\n<li>大批量并发要限流 —— 浏览器同域连接只有 6 个，一次甩 50 个请求既不会更快也不好做失败重试</li>\\n</ul>\\n<p><strong><code>Promise.all()</code> 要求所有任务都成功，而 <code>Promise.allSettled()</code> 会等待所有任务结束并保留每一项的结果。</strong> 前者只要有一个 <code>Promise</code> 变为 <code>rejected</code>，整体就会失败；全部成功时，结果顺序与入参一一对应。后者不会因为单项失败而提前结束，返回项通过 <code>status</code> 区分 <code>fulfilled</code> 和 <code>rejected</code>，并分别携带 <code>value</code> 或 <code>reason</code>。依赖缺一不可时用 <code>Promise.all()</code>，需要收集全部成功与失败结果时用 <code>Promise.allSettled()</code>。</p>\\n</section>\\n<h3 id=\\"promise-allsettled\\"><a class=\\"header-anchor\\" href=\\"#promise-allsettled\\">#</a> Promise.allSettled()</h3>\\n<blockquote>\\n<p>接受的结果与入参时的promise实例一一对应，且结果的每一项都是一个对象，告诉你结果和值，对象内都有一个属性叫“status”，用来明确知道对应的这个promise实例的状态（fulfilled或rejected），fulfilled时，对象有value属性，rejected时有reason属性，对应两种状态的返回值。</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>const resolved = Promise.resolve(42);\\nconst rejected = Promise.reject(-1);\\n\\nconst allSettledPromise = Promise.allSettled([resolved, rejected]);\\n\\nallSettledPromise.then(function (results) {\\n  console.log(results);\\n});\\n// [\\n//    { status: &#39;fulfilled&#39;, value: 42 },\\n//    { status: &#39;rejected&#39;, reason: -1 }\\n// ]\\n</code></pre>\\n<blockquote>\\n<p>重要的一点是，他不论接受入参的promise本身的状态，会返回所有promise的结果，但这一点<code>Promise.all</code>做不到，如果你需要知道所有入参的异步操作的所有结果，或者需要知道这些异步操作是否全部结束，应该使用<code>promise.allSettled()</code></p>\\n</blockquote>\\n<h3 id=\\"promise-all\\"><a class=\\"header-anchor\\" href=\\"#promise-all\\">#</a> Promise.all()</h3>\\n<blockquote>\\n<p>只有当所有入参的promise实例都是<code>fulfilled</code>状态，才会在<code>Promise.all().then()</code>方法中结果，返回结果也是与入参一一对应，结果中只包含实际的<code>resolve</code>的结果，不包含类似<code>allSettled</code>的<code>status</code>和<code>value</code>属性。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>订单页并行查询库存、优惠和运费，三个入参都已经 <code>resolve</code>，但同事希望 <code>then</code> 拿到 <code>{status, value}</code> 结构，<code>Promise.all</code> 会这样返回吗？</strong></p>\\n<p>不会，三个入参都进入 <code>fulfilled</code> 后，<code>Promise.all</code> 返回的是各项实际的 <code>resolve</code> 结果数组，不会附加 <code>status</code> 或 <code>value</code> 字段。消费端应按原始结果结构读取；若代码依赖状态包装，就不能把它当作该 <code>API</code> 的默认输出。</p>\\n</li>\\n<li>\\n<p><strong>批量读取 20 个分片时，各分片完成时间不同，业务却要求聚合结果仍与请求参数逐项对应，能直接依赖 <code>Promise.all</code> 的返回顺序吗？</strong></p>\\n<p>可以依赖入参与结果一一对应，而不应按各任务实际完成的先后解释结果。调用方应保留稳定的入参数组，并用相同索引关联分片；若中途重排或过滤入参，索引关系也会随之改变，需要额外保存标识。</p>\\n</li>\\n<li>\\n<p><strong>报表页把 8 个异步结果交给 <code>Promise.all</code>，其中 7 个已完成、最后一个长期未进入 <code>fulfilled</code>，聚合后的 <code>then</code> 会先返回已有数据吗？</strong></p>\\n<p>不会，只有全部入参对应的 <code>Promise</code> 都进入 <code>fulfilled</code>，聚合结果才会进入后续 <code>then</code>。因此它适合“结果必须齐备后再渲染”的场景；若页面允许先展示部分数据，就需要拆分等待关系，而不能指望它增量产出。</p>\\n</li>\\n<li>\\n<p><strong>接口联调时单个请求日志都显示成功，聚合层却读到 <code>result[0].value</code> 为 <code>undefined</code>，该从哪里排查？</strong></p>\\n<p>先打印每个 <code>resolve</code> 的实际值以及 <code>Promise.all</code> 返回数组，确认聚合层是否误按 <code>{status, value}</code> 结构解包。<code>Promise.all</code> 只保留实际完成值并按入参对应排列；若某项主动 <code>resolve(undefined)</code>，聚合层同样只能得到 <code>undefined</code>。</p>\\n</li>\\n<li>\\n<p><strong>结算页要求库存、价格和资格校验全部完成后才能提交，而推荐位允许各自完成后独立展示，两处都统一套一层 <code>Promise.all</code> 合适吗？</strong></p>\\n<p>结算校验适合使用 <code>Promise.all</code>，因为业务门槛就是所有结果均已 <code>fulfilled</code> 后统一继续。推荐位若需要逐项展示，统一等待会让已完成内容也被最后一项拖住；应拆开消费各自结果，代价是状态管理和渲染分支更多。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);