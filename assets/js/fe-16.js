(window.webpackJsonp=window.webpackJsonp||[]).push([[16],{1124:function(n){n.exports=JSON.parse('{"html":"<p>我们现在要开始做题啦！</p>\\n<p>万里长征第一步，仍然是数组。\\n单纯针对数组来考察的题目，总体来说，都不算太难——数组题目要想往难了出，基本都要结合排序、二分和动态规划这些相对复杂的算法思想才行。</p>\\n<p>咱们本节要解决的正是这一类“不算太难”的数组题目——并不是只有难题才拥有成为真题的入场券，一道好题不一定会难，它只要能够反映问题就可以了。</p>\\n<p>本节所涉及的题目在面试中普遍具有较高的出镜率、同时兼具一定的综合性，对培养大家的通用解题能力大有裨益 。</p>\\n<p>相信这节你会学得很开心，在轻松中收获自己的第一份算法解题锦囊。</p>\\n<h2 id=\\"map-的妙用-两数求和问题\\"><a class=\\"header-anchor\\" href=\\"#map-的妙用-两数求和问题\\">#</a> Map 的妙用——两数求和问题</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>暴力枚举两下标是 <code>O(n²)</code>；哈希表把“找另一个数”降为期望 <code>O(1)</code>，总时间 <code>O(n)</code>、空间 <code>O(n)</code></li>\\n<li>每轮先算 <code>need = target - nums[i]</code> 并查询已扫描前缀，再写当前值，才能保证不重复使用同一元素</li>\\n<li><code>Map</code> 的键可安全表示数字；普通对象会字符串化键，还要处理原型属性，不是更稳的默认选择</li>\\n<li>重复数字是否合法取决于下标：<code>[3, 3]</code> 可组成 <code>6</code>，只要它们来自两个位置</li>\\n<li>返回一组、全部组合或不存在时的结果必须先约定，不能让实现细节替题目决定契约</li>\\n</ul>\\n<p><strong>两数之和可以用 <code>Map</code> 记录“已遍历的值到下标”，把暴力枚举的 <code>O(n²)</code> 优化为期望 <code>O(n)</code>。</strong> 遍历到 <code>nums[i]</code> 时，先查询补数 <code>target - nums[i]</code>，未命中再写入当前值，这样不会重复使用同一个元素。重复数字本身没问题，例如两个不同位置的 <code>3</code> 可以组成 <code>6</code>。额外空间是 <code>O(n)</code>；若题目要求返回全部组合，就需要保存每个值的下标列表并另外约定去重规则。</p>\\n</section>\\n<p>哈希解法的不变量是：进入第 <code>i</code> 轮时，<code>seen</code> 只保存区间 <code>[0, i)</code> 的值及下标。因此命中补数时，它一定来自不同元素；未命中才写入当前值。原文使用对象并用 <code>!== undefined</code> 判断，会把“索引值是否存在”和“属性值是否为 undefined”混在一起，使用 <code>Map.has()</code> 更直接。</p>\\n<pre class=\\"language-js\\"><code>function twoSum(nums, target) {\\n  const seen = new Map()\\n  for (let i = 0; i &lt; nums.length; i += 1) {\\n    const need = target - nums[i]\\n    if (seen.has(need)) return [seen.get(need), i]\\n    seen.set(nums[i], i)\\n  }\\n  return []\\n}\\n</code></pre>\\n<p>每个元素最多查询和写入一次，期望时间 <code>O(n)</code>、额外空间 <code>O(n)</code>。若要求全部不重复下标组合，<code>Map&lt;number, number&gt;</code> 不够，需要保存每个值的下标列表或采用另一套去重契约；若输入含浮点数，还要先确认能否直接用精确相等比较。</p>\\n<blockquote>\\n<p>真题描述： 给定一个整数数组 <code>nums</code> 和一个目标值 <code>target</code>，请你在该数组中找出和为目标值的那 两个 整数，并返回他们的数组下标。</p>\\n</blockquote>\\n<p>你可以假设每种输入只会对应一个答案。但是，你不能重复利用这个数组中同样的元素。</p>\\n<p>示例:</p>\\n<pre class=\\"language-text\\"><code>给定 nums = [2, 7, 11, 15], target = 9\\n因为 nums[0] + nums[1] = 2 + 7 = 9 所以返回 [0, 1]\\n</code></pre>\\n<h3 id=\\"思路分析\\"><a class=\\"header-anchor\\" href=\\"#思路分析\\">#</a> 思路分析：</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>两数之和用 <code>Map</code> 保存“已经见过的值 → 下标”，遍历当前值时查找补数 <code>target - value</code></li>\\n<li>必须先查补数再写当前值，避免同一个元素被使用两次；命中后返回两个下标而不是两个数值</li>\\n<li>单次哈希查找期望 <code>O(1)</code>，整体时间 <code>O(n)</code>、额外空间 <code>O(n)</code>，以空间换掉双重循环</li>\\n</ul>\\n<p><strong>我会把求和转成求差：遍历数组时，用 <code>Map</code> 保存已经出现的值及其下标，并查找 <code>target - value</code>。</strong> 补数命中后，直接返回它的历史下标和当前下标；必须先查询再写入，才能保证同一个位置不会被使用两次。相比双重循环，这种做法把期望时间从 <code>O(n²)</code> 降到 <code>O(n)</code>，代价是增加 <code>O(n)</code> 空间。实现时应使用 <code>Map.has()</code> 判断键是否存在，避免把索引值和不存在混为一谈。</p>\\n</section>\\n<p><strong>一个“淳朴”的解法</strong></p>\\n<blockquote>\\n<p>这道题相信很多同学看一眼就很快能得出一个最基本的思路：两层循环来遍历同一个数组；第一层循环遍历的值记为 a，第二层循环时遍历的值记为 b；若 a+b = 目标值，那么 a 和 b 对应的数组下标就是我们想要的答案。</p>\\n</blockquote>\\n<p><strong>对“淳朴”解法的反思</strong></p>\\n<p>大家以后做算法题的时候，要有这样的一种本能：当发现自己的代码里有两层循环时，先反思一下，能不能用空间换时间，把它优化成一层循环。</p>\\n<blockquote>\\n<p>因为两层循环很多情况下都意味着 <code>O(n^2)</code> 的复杂度，这个复杂度非常容易导致你的算法超时。即便没有超时，在明明有一层遍历解法的情况下，你写了两层遍历，面试官对你的印象分会大打折扣。</p>\\n</blockquote>\\n<p><strong>空间换时间，Map 来帮忙</strong></p>\\n<p>拿我们这道题来说，其实二层遍历是完全不必要的。</p>\\n<blockquote>\\n<p>大家记住一个结论：几乎所有的求和问题，都可以转化为求差问题。 这道题就是一个典型的例子，通过把求和问题转化为求差问题，事情会变得更加简单。</p>\\n</blockquote>\\n<p>我们可以在遍历数组的过程中，增加一个 Map 来记录已经遍历过的数字及其对应的索引值。然后每遍历到一个新数字的时候，都回到 <code>Map</code> 里去查询 <code>targetNum</code> 与该数的差值是否已经在前面的数字中出现过了。若出现过，那么答案已然显现，我们就不必再往下走了。</p>\\n<p>我们以 <code>nums = [2, 7, 11, 15]</code> 这个数组为例，来模拟一下这个思路：</p>\\n<p>第一次遍历到 <code>2</code>，此时 <code>Map</code> 为空：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111542.png\\" alt=\\"\\"></p>\\n<p>以 <code>2</code> 为 <code>key</code>，索引 0 为 value 作存储，继续往下走；遇到了 7：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111602.png\\" alt=\\"\\"></p>\\n<p>计算 targetNum 和 7 的差值为2，去 Map 中检索 2 这个 key，发现是之前出现过的值：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111610.png\\" alt=\\"\\"></p>\\n<p>那么 2 和 7 的索引组合就是这道题的答案啦。</p>\\n<p>键值对存储我们可以用 ES6 里的 Map 来做，如果图省事，直接用对象字面量来定义也没什么问题</p>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {number[]} nums\\n * @param {number} target\\n * @return {number[]}\\n */\\nconst twoSum = function(nums, target) {\\n    // 这里我用对象来模拟 map 的能力\\n    const diffs = {}\\n    // 缓存数组长度\\n    const len = nums.length\\n    // 遍历数组\\n    for(let i=0;i&lt;len;i++) {\\n        // 判断当前值对应的 target 差值是否存在（是否已遍历过）\\n        if(diffs[target-nums[i]]!==undefined) {\\n            // 若有对应差值，那么答案get！\\n            return [diffs[target - nums[i]], i]\\n        }\\n        // 若没有对应差值，则记录当前值\\n        diffs[nums[i]]=i\\n    }\\n};\\n</code></pre>\\n<blockquote>\\n<p>tips：这道题也可以用 ES6 中的 Map 来做，你试试呢？</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在白板上写了双层循环处理 <code>nums = [2,7,11,15]</code>、<code>target = 9</code>，并说数据量不大就不用优化，你会怎样追问它的复杂度风险？</strong></p>\\n<p>双层循环的时间复杂度是 <code>O(n²)</code>，数组增长后比较次数会快速增加，即使样例能通过也不是理想方案。应把求和转成查找补数，用 <code>Map</code> 保存已遍历值及下标，将平均查找和整体遍历收敛到线性级别，代价是 <code>O(n)</code> 额外空间。</p>\\n</li>\\n<li>\\n<p><strong>结算页面传入 <code>nums = [3,3]</code>、<code>target = 6</code>，代码把当前值写进 <code>Map</code> 后再查补数，为什么可能返回同一个下标两次？</strong></p>\\n<p>先写后查会让当前的 <code>3</code> 立即成为自己的候选补数，首轮就可能拼出 <code>[0,0]</code>，违反必须使用两个元素的约束。实现应先查询 <code>target - nums[i]</code>，未命中再写入当前值，使容器始终只保存当前下标之前的数据。</p>\\n</li>\\n<li>\\n<p><strong>搜索服务要求返回所有满足目标值的下标组合，但现有实现用 <code>Map&lt;number, number&gt;</code> 且首次命中就 <code>return</code>，你会改哪两处？</strong></p>\\n<p>单个下标会被后续同值覆盖，首次返回也会截断剩余候选，因此两处都不符合全部组合的契约。应让每个值关联历史下标列表，当前补数命中后逐个生成组合，再追加当前下标；结果规模可能达到 <code>O(n²)</code>，无法仍承诺线性输出成本。</p>\\n</li>\\n<li>\\n<p><strong>线上日志显示输入里明明有补数，使用普通对象模拟 <code>Map</code> 却没有命中，排查时你先看哪段判断和键值存储？</strong></p>\\n<p>先检查命中条件是否依赖 <code>!== undefined</code>，以及对象键是否被字符串化或碰到继承属性，再用失败输入逐轮记录补数与已存键。数值题更稳妥的实现是使用 <code>ES6</code> <code>Map</code> 的 <code>has</code>、<code>get</code> 和 <code>set</code>；若输入含非数值或浮点误差，仍需先明确相等语义。</p>\\n</li>\\n<li>\\n<p><strong>商品列表已经按价格升序排列，只需判断是否存在一对价格等于预算，你会选 <code>Map</code> 还是左右指针？</strong></p>\\n<p>在有序且不要求原下标的契约下，左右指针能以 <code>O(n)</code> 时间和 <code>O(1)</code> 额外空间完成，比 <code>Map</code> 更节省内存。若接口必须返回排序前下标，就要保留值与原索引的绑定，或直接使用哈希方案；不能把排序后位置冒充原位置。</p>\\n</li>\\n</ul>\\n</section>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审中有人把流程改成“先执行 <code>diffs[nums[i]] = i</code>，再判断补数”，输入 <code>[3,3]</code>、目标 <code>6</code> 时具体会错在哪里？</strong></p>\\n<p>首轮写入 <code>3 → 0</code> 后再查询补数 <code>3</code>，会直接命中当前元素并返回重复下标，而不是等待第二个 <code>3</code>。查询必须发生在写入之前，使 <code>diffs</code> 只代表已遍历的前缀；第二轮才能合法返回 <code>[0,1]</code>。</p>\\n</li>\\n<li>\\n<p><strong>支付页面把 <code>0.1</code>、<code>0.2</code> 和目标 <code>0.3</code> 直接交给两数之和，监控显示始终查不到组合，你会如何修复而不是随手加 <code>epsilon</code>？</strong></p>\\n<p>故障来自二进制浮点表示导致补数与已存键不能精确相等，哈希查询不会自动进行近似匹配。金额应按明确币种精度转换为最小货币单位整数，或采用十进制定点方案后再建表；任意 <code>epsilon</code> 会让等价关系不稳定，也难以作为可靠的 <code>Map</code> 键。</p>\\n</li>\\n<li>\\n<p><strong>风控接口从“返回任意一组”改为“返回全部下标组合”，输入含大量重复值时，原来的单值映射为什么会漏结果？</strong></p>\\n<p><code>Map&lt;number, number&gt;</code> 对同值只保留一个下标，且首次命中立即结束，因此既丢失历史重复项，也不会继续扫描后续元素。应保存 <code>Map&lt;number, number[]&gt;</code>，对每个补数的历史下标逐一产出组合；当结果本身很多时，时间和空间会随输出规模上升。</p>\\n</li>\\n<li>\\n<p><strong>线上返回了 <code>[undefined, 4]</code>，实现仍用对象字面量保存下标并以真假值判断命中，你会怎样定位下标 <code>0</code> 被漏掉的问题？</strong></p>\\n<p>若条件写成 <code>if (diffs[complement])</code>，合法下标 <code>0</code> 会被当作假值，后续取值还可能形成异常结果。应改用 <code>diffs[complement] !== undefined</code>，更推荐使用 <code>Map.has(complement)</code> 区分不存在与合法值；同时回放输入确认键和值没有被非数值数据污染。</p>\\n</li>\\n<li>\\n<p><strong>移动端数组规模较大但内存受限，产品又只要任意一组结果；无序输入下你会保留哈希解法还是先排序再双指针？</strong></p>\\n<p>哈希解法保持平均 <code>O(n)</code> 时间，但需要 <code>O(n)</code> 额外空间，并能自然保留扫描时的原下标。排序加双指针减少额外查找结构，却引入 <code>O(n log n)</code> 排序成本；若还要原下标，必须携带索引副本，空间优势可能随之减弱。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"强大的双指针法\\"><a class=\\"header-anchor\\" href=\\"#强大的双指针法\\">#</a> 强大的双指针法</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>双指针的价值是利用单调性排除一段候选，而不是“代码里声明两个变量”</li>\\n<li>对撞指针通常要求有序：和偏小移动左端、和偏大移动右端，每步都能证明被排除区域不可能有答案</li>\\n<li>同向快慢指针常处理去重、滑窗、读写位置和链表距离；选择形态要看指针各自代表什么</li>\\n<li>若先排序，整体复杂度至少包含 <code>O(n log n)</code>，并且可能改变原数组和原下标</li>\\n<li>循环前写清区间不变量和终止条件，避免漏解、死循环及重复答案</li>\\n</ul>\\n<p><strong>双指针真正强在利用单调关系，一次移动就能排除一段不可能的候选。</strong> 对撞指针常用于有序区间，同向指针更适合去重、滑窗或读写位置，关键是明确每个指针代表什么。比如三数之和会固定一个数，再让左右边界按当前和收缩。若需要先排序，要把 <code>O(n log n)</code>、原数组被修改和原下标丢失这些取舍算进去。</p>\\n</section>\\n<p>双指针不是固定模板。合并有序数组时，两个读指针分别指向尚未合并部分的最大值，写指针指向最终数组的空位；三数之和则固定一个值，让左右边界根据当前和单向收缩。两者共享的核心是：移动后，被越过的元素已有数学依据不再成为候选。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人说“双指针就是一左一右向中间走”，但合并有序数组用了两个尾部读指针和一个写指针，你会让他解释哪里不成立？</strong></p>\\n<p>双指针不是固定的一左一右模板，指针含义取决于候选区间和写入约束。合并时两个读指针分别指向两个有效区间的最大值，写指针定位最终空位；三数之和才是左右边界相向收缩，二者共同点是越过元素后无需回退。</p>\\n</li>\\n<li>\\n<p><strong>报表服务对无序数组直接使用左右指针找目标和，测试偶尔成功、换一组数据就漏解，你会怎样指出根因？</strong></p>\\n<p>无序数据中，和偏大时左移右指针并不能保证排除的元素不再可能成解，因此移动缺少单调性依据。应先排序再使用对撞指针，或改用哈希查补数；若接口要求原下标，排序时还必须绑定并保留索引。</p>\\n</li>\\n<li>\\n<p><strong>三数之和页面出现重复卡片，代码命中后只执行一次 <code>left++</code> 和 <code>right--</code>，为什么还需要 <code>while</code> 跳重？</strong></p>\\n<p>相邻重复值可能连续出现多个，只移动一次仍会让下一轮生成相同的数值三元组。命中后应同时移动两端，再在 <code>left &lt; right</code> 条件下分别越过连续重复值；外层固定值也要跳重，否则内层处理仍挡不住跨轮重复。</p>\\n</li>\\n<li>\\n<p><strong>合并模块上线后偶发覆盖尚未读取的 <code>nums1</code> 元素，而三数之和模块没有覆盖问题，你会比较两种指针的不变量吗？</strong></p>\\n<p>合并模块必须保证写位置位于预留区或已经消费的区域，从尾部写才能维持未读前缀完整。三数之和只读取排序副本并收缩候选边界，不承担原地写入；排查合并故障应重点核对 <code>i</code>、<code>j</code>、<code>k</code> 的初值和每次递减是否同步。</p>\\n</li>\\n<li>\\n<p><strong>架构评审要求你总结合并有序数组与三数之和为何都能单向移动指针，但又不能共享一段通用模板，你会怎么解释？</strong></p>\\n<p>两者都依赖有序性，使每次比较都能证明被越过的元素不再属于候选集合，所以指针无需回退。合并关注未消费最大值和安全写位，三数之和关注当前和与零的大小关系；状态、不变量和终止条件不同，强行抽象会掩盖边界错误。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"合并两个有序数组\\"><a class=\\"header-anchor\\" href=\\"#合并两个有序数组\\">#</a> 合并两个有序数组</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li><code>nums1</code> 前 <code>m</code> 项与 <code>nums2</code> 前 <code>n</code> 项有效，尾部预留区不是参与比较的真实数据</li>\\n<li>从末尾比较较大值并回填，保证尚未读取的 <code>nums1</code> 前缀不会被覆盖</li>\\n<li>不变量：<code>[k+1, m+n)</code> 始终是已确定的最终后缀，<code>i</code>、<code>j</code> 指向两边尚未消费的最大值</li>\\n<li><code>nums2</code> 剩余时必须复制；<code>nums1</code> 剩余时已在正确位置，无需动作</li>\\n<li>时间 <code>O(m+n)</code>、额外空间 <code>O(1)</code>，实现会原地修改 <code>nums1</code>，调用方必须接受所有权契约</li>\\n</ul>\\n<p><strong>合并时应从两个有效区间的末尾比较，把较大值倒序写入 <code>nums1</code> 的预留位置。</strong> 这样写入的是空位，不会覆盖 <code>nums1</code> 中尚未读取的数据；其中 <code>i</code>、<code>j</code> 指向待处理的最大值，<code>k</code> 指向当前写入位置。<code>nums2</code> 有剩余时必须继续复制，<code>nums1</code> 有剩余则不用动。该方案时间为 <code>O(m+n)</code>、额外空间为 <code>O(1)</code>，但会原地修改 <code>nums1</code>。</p>\\n</section>\\n<p>原文实现可以把主循环收敛为“只要 <code>nums2</code> 未耗尽就继续”，因为 <code>nums1</code> 的剩余项无需复制：</p>\\n<pre class=\\"language-js\\"><code>function mergeSorted(nums1, m, nums2, n) {\\n  let i = m - 1\\n  let j = n - 1\\n  let k = m + n - 1\\n  while (j &gt;= 0) {\\n    if (i &gt;= 0 &amp;&amp; nums1[i] &gt; nums2[j]) nums1[k--] = nums1[i--]\\n    else nums1[k--] = nums2[j--]\\n  }\\n  return nums1\\n}\\n</code></pre>\\n<p>若业务不允许修改输入，应新建长度 <code>m+n</code> 的结果数组从前向后归并，额外空间变为 <code>O(m+n)</code>。还要校验 <code>m</code>、<code>n</code> 与实际有效区间一致，否则预留的零可能被误当成数据。</p>\\n<blockquote>\\n<p>真题描述：给你两个有序整数数组 nums1 和 nums2，请你将 nums2 合并到 nums1 中，使 nums1 成为一个有序数组。</p>\\n</blockquote>\\n<p>说明: 初始化 nums1 和 nums2 的元素数量分别为 m 和 n 。 你可以假设 nums1 有足够的空间（空间大小大于或等于 m + n）来保存 nums2 中的元素。</p>\\n<p>示例:</p>\\n<pre class=\\"language-text\\"><code>输入:\\nnums1 = [1,2,3,0,0,0], m = 3\\nnums2 = [2,5,6], n = 3\\n输出: [1,2,2,3,5,6]\\n</code></pre>\\n<p><strong>思路分析</strong></p>\\n<ol>\\n<li>标准解法</li>\\n</ol>\\n<p>这道题没有太多的弯弯绕绕，标准解法就是双指针法。首先我们定义两个指针，各指向两个数组生效部分的尾部：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111643.png\\" alt=\\"\\"></p>\\n<p>每次只对指针所指的元素进行比较。取其中较大的元素，把它从 nums1 的末尾往前面填补：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111650.png\\" alt=\\"\\"></p>\\n<p><strong>这里有一点需要解释一下：</strong></p>\\n<p>为什么是从后往前填补？因为是要把所有的值合并到 nums1 里，所以说我们这里可以把 nums1 看做是一个“容器”。但是这个容器，它不是空的，而是前面几个坑有内容的。如果我们从前往后填补，就没法直接往对应的坑位赋值了（会产生值覆盖）。</p>\\n<p>从后往前填补，我们填的都是没有内容的坑，这样会省掉很多麻烦。</p>\\n<p>由于 nums1 的有效部分和 nums2 并不一定是一样长的。我们还需要考虑其中一个提前到头的这种情况：</p>\\n<p>如果提前遍历完的是 nums1 的有效部分，剩下的是 nums2。那么这时意味着 nums1 的头部空出来了，直接把 nums2 整个补到 nums1 前面去即可。</p>\\n<p>如果提前遍历完的是 nums2，剩下的是 nums1。由于容器本身就是 nums1，所以此时不必做任何额外的操作。</p>\\n<p>编码实现：</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {number[]} nums1\\n * @param {number} m\\n * @param {number[]} nums2\\n * @param {number} n\\n * @return {void} Do not return anything, modify nums1 in-place instead.\\n */\\nconst merge = function(nums1, m, nums2, n) {\\n    // 初始化两个指针的指向，初始化 nums1 尾部索引k\\n    let i = m - 1, j = n - 1, k = m + n - 1\\n    // 当两个数组都没遍历完时，指针同步移动\\n    while(i &gt;= 0 &amp;&amp; j &gt;= 0) {\\n        // 取较大的值，从末尾往前填补\\n        if(nums1[i] &gt;= nums2[j]) {\\n            nums1[k] = nums1[i] \\n            i-- \\n            k--\\n        } else {\\n            nums1[k] = nums2[j] \\n            j-- \\n            k--\\n        }\\n    }\\n    \\n    // nums2 留下的情况，特殊处理一下 \\n    while(j&gt;=0) {\\n        nums1[k] = nums2[j]  \\n        k-- \\n        j--\\n    }\\n};\\n</code></pre>\\n<p>找点乐子：</p>\\n<p>上面我们给出的，是面试官最喜欢看到的一种解法，这种解法适用于各种语言。</p>\\n<p>但是就 JS 而言，我们还可以“另辟蹊径”，仔细想想，你有什么妙招？</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在编辑器里从索引 <code>0</code> 开始把较小值写回 <code>nums1 = [1,2,3,0,0,0]</code>，你会用哪个覆盖现象说明方案不可行？</strong></p>\\n<p>写入头部会覆盖 <code>nums1</code> 尚未比较的有效元素，例如写入另一个数组的 <code>2</code> 后，原有值可能失去唯一副本。应从两个有效区间尾部比较较大值，并从 <code>k = m + n - 1</code> 向前写；这样写位只落在预留区或已消费区。</p>\\n</li>\\n<li>\\n<p><strong>代码主循环只写成 <code>while (j &gt;= 0)</code>，同事质疑没有单独处理 <code>nums1</code> 剩余元素，你如何证明它是完整的？</strong></p>\\n<p>当 <code>nums2</code> 先耗尽时，<code>nums1[0..i]</code> 本就有序，并且不大于已经放好的后缀，因此仍处在最终位置，无需自我复制。若 <code>nums1</code> 先耗尽，条件仍成立，后续会持续把 <code>nums2</code> 的剩余前缀写到头部，直到 <code>j &lt; 0</code>。</p>\\n</li>\\n<li>\\n<p><strong>数据层禁止修改缓存中的原数组，但接口仍要合并两个有序列表，你会如何调整实现和复杂度说明？</strong></p>\\n<p>不能再复用 <code>nums1</code> 的尾部预留区，应新建长度为 <code>m+n</code> 的结果数组，从前向后选择较小值写入。时间复杂度仍为 <code>O(m+n)</code>，额外空间变为 <code>O(m+n)</code>；若调用方依赖对象引用不变，还需在契约层明确返回新数组。</p>\\n</li>\\n<li>\\n<p><strong>线上合并结果里凭空多出几个 <code>0</code>，请求参数中的 <code>m</code> 偶尔大于 <code>nums1</code> 的真实有效长度，你先检查什么？</strong></p>\\n<p>先核对 <code>m</code>、<code>n</code> 是否为非负整数，<code>nums1</code> 容量是否至少为 <code>m+n</code>，以及声明的有效区间是否真有对应数据。预留的 <code>0</code> 一旦落入 <code>0..m-1</code> 就会被算法当作合法元素；题库可依赖输入保证，生产接口则应拒绝或结构化报告非法参数。</p>\\n</li>\\n<li>\\n<p><strong>前端同事建议直接用 <code>nums1.splice(m, n, ...nums2)</code> 后整体 <code>sort()</code>，而你主张尾部归并，选型依据是什么？</strong></p>\\n<p>整体排序没有利用两个输入区间已经有序的条件，复杂度会高于线性归并，而且 <code>sort()</code> 仍会修改数组。尾部归并在允许原地修改且容量足够时只需 <code>O(1)</code> 额外空间；若代码可读性优先且数据很小，简写可讨论，但不能误报复杂度。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"三数求和问题\\"><a class=\\"header-anchor\\" href=\\"#三数求和问题\\">#</a> 三数求和问题</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>先排序，再固定 <code>nums[i]</code>，在右侧区间用对撞指针寻找和为 <code>-nums[i]</code> 的两数</li>\\n<li>和小于 <code>0</code> 时左指针右移，和大于 <code>0</code> 时右指针左移；有序性保证排除安全</li>\\n<li>固定值、命中后的左值和右值都要跳过连续重复，三处缺一都会产生重复答案</li>\\n<li>排序 <code>O(n log n)</code> 加外层与双指针 <code>O(n²)</code>，总时间 <code>O(n²)</code>；结果空间不计时辅助空间取决于排序实现</li>\\n<li>原地排序会修改输入；需要保留原数组时先复制，若返回下标则要携带原索引</li>\\n</ul>\\n<p><strong>三数之和可以先排序，再固定一个数，用左右指针在其右侧寻找另外两个数。</strong> 当前和小于 <code>0</code> 就右移左指针，大于 <code>0</code> 就左移右指针，因为有序性保证这种排除不会漏解。固定值以及命中后的左右值都要跳过重复项，否则会得到重复三元组。整体时间是 <code>O(n²)</code>；若不希望原地排序改变输入，可以复制数组后再处理。</p>\\n</section>\\n<p>可在原文实现上增加两个安全剪枝：排序后若 <code>nums[i] &gt; 0</code>，后续三数之和只会更大，可直接结束；若当前固定值与前一项相同则跳过。为避免修改调用方，先复制再排序：</p>\\n<pre class=\\"language-js\\"><code>function threeSum(input) {\\n  const nums = [...input].sort((a, b) =&gt; a - b)\\n  const result = []\\n  for (let i = 0; i &lt; nums.length - 2; i += 1) {\\n    if (nums[i] &gt; 0) break\\n    if (i &gt; 0 &amp;&amp; nums[i] === nums[i - 1]) continue\\n    let left = i + 1\\n    let right = nums.length - 1\\n    while (left &lt; right) {\\n      const sum = nums[i] + nums[left] + nums[right]\\n      if (sum &lt; 0) left += 1\\n      else if (sum &gt; 0) right -= 1\\n      else {\\n        result.push([nums[i], nums[left++], nums[right--]])\\n        while (left &lt; right &amp;&amp; nums[left] === nums[left - 1]) left += 1\\n        while (left &lt; right &amp;&amp; nums[right] === nums[right + 1]) right -= 1\\n      }\\n    }\\n  }\\n  return result\\n}\\n</code></pre>\\n<p>双指针法能处理的问题多到你想不到。不信来瞅瞅两数求和它儿子——三数求和问题！</p>\\n<p>俗话说，青出于蓝而胜于蓝，三数求和虽然和两数求和只差了一个字，但是思路却完全不同。</p>\\n<blockquote>\\n<p>真题描述：给你一个包含 n 个整数的数组 nums，判断 nums 中是否存在三个元素 a，b，c ，使得 a + b + c = 0 ？请你找出所有满足条件且不重复的三元组。</p>\\n</blockquote>\\n<p>注意：答案中不可以包含重复的三元组。</p>\\n<p>示例：</p>\\n<pre class=\\"language-text\\"><code>给定数组 nums = [-1, 0, 1, 2, -1, -4]， 满足要求的三元组集合为： [ [-1, 0, 1], [-1, -1, 2] ]\\n</code></pre>\\n<p><strong>思路分析</strong></p>\\n<p>三数之和延续两数之和的思路，我们可以把求和问题变成求差问题——固定其中一个数，在剩下的数中寻找是否有两个数和这个固定数相加是等于0的。</p>\\n<p>虽然乍一看似乎还是需要三层循环才能解决的样子，不过现在我们有了双指针法，定位效率将会被大大提升，从此告别过度循环~</p>\\n<p>（这里大家相信已经能察觉出来双指针法的使用场景了，一方面，它可以做到空间换时间；另一方面，它也可以帮我们降低问题的复杂度。）</p>\\n<blockquote>\\n<p>双指针法用在涉及求和、比大小类的数组题目里时，大前提往往是：该数组必须有序。否则双指针根本无法帮助我们缩小定位的范围，压根没有意义。因此这道题的第一步是将数组排序：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>nums = nums.sort((a,b)=&gt;{\\n  return a-b\\n})\\n</code></pre>\\n<p>然后，对数组进行遍历，每次遍历到哪个数字，就固定哪个数字。然后把左指针指向该数字后面一个坑里的数字，把右指针指向数组末尾，让左右指针从起点开始，向中间前进：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111714.png\\" alt=\\"\\"></p>\\n<p>每次指针移动一次位置，就计算一下两个指针指向数字之和加上固定的那个数之后，是否等于0。如果是，那么我们就得到了一个目标组合；否则，分两种情况来看：</p>\\n<p>相加之和大于0，说明右侧的数偏大了，右指针左移</p>\\n<p>相加之和小于0，说明左侧的数偏小了，左指针右移</p>\\n<blockquote>\\n<p>tips：这个数组在题目中要求了“不重复的三元组”，因此我们还需要做一个重复元素的跳过处理。这一点在编码实现环节大家会注意到。</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>/**\\n * @param {number[]} nums\\n * @return {number[][]}\\n */\\nconst threeSum = function(nums) {\\n    // 用于存放结果数组\\n    let res = [] \\n    // 给 nums 排序\\n    nums = nums.sort((a,b)=&gt;{\\n        return a-b\\n    })\\n    // 缓存数组长度\\n    const len = nums.length\\n    // 注意我们遍历到倒数第三个数就足够了，因为左右指针会遍历后面两个数\\n    for(let i=0;i&lt;len-2;i++) {\\n        // 左指针 j\\n        let j=i+1 \\n        // 右指针k\\n        let k=len-1   \\n        // 如果遇到重复的数字，则跳过\\n        if(i&gt;0&amp;&amp;nums[i]===nums[i-1]) {\\n            continue\\n        }\\n        while(j&lt;k) {\\n            // 三数之和小于0，左指针前进\\n            if(nums[i]+nums[j]+nums[k]&lt;0){\\n                j++\\n               // 处理左指针元素重复的情况\\n               while(j&lt;k&amp;&amp;nums[j]===nums[j-1]) {\\n                    j++\\n                }\\n            } else if(nums[i]+nums[j]+nums[k]&gt;0){\\n                // 三数之和大于0，右指针后退\\n                k--\\n               \\n               // 处理右指针元素重复的情况\\n               while(j&lt;k&amp;&amp;nums[k]===nums[k+1]) {\\n                    k--\\n                }\\n            } else {\\n                // 得到目标数字组合，推入结果数组\\n                res.push([nums[i],nums[j],nums[k]])\\n                \\n                // 左右指针一起前进\\n                j++  \\n                k--\\n               \\n                // 若左指针元素重复，跳过\\n                while(j&lt;k&amp;&amp;nums[j]===nums[j-1]) {\\n                    j++\\n                }  \\n               \\n               // 若右指针元素重复，跳过\\n               while(j&lt;k&amp;&amp;nums[k]===nums[k+1]) {\\n                    k--\\n                }\\n            }\\n        }\\n    }\\n    \\n    // 返回结果数组\\n    return res\\n};\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>结果页输入 <code>[0,0,0,0]</code> 却渲染出多个 <code>[0,0,0]</code>，你会要求候选人指出哪三处去重缺失？</strong></p>\\n<p>外层固定值需要跳过与前一项相同的元素，命中后左右指针都要移动，并分别越过连续重复值。只处理其中一处仍可能从同一轮或不同固定值轮次生成重复三元组；所有跳重都必须守住 <code>left &lt; right</code> 边界。</p>\\n</li>\\n<li>\\n<p><strong>共享状态中的数组顺序被三数之和函数悄悄改变，导致后续列表索引错位，你会如何修改排序代码？</strong></p>\\n<p><code>JavaScript</code> 的 <code>sort()</code> 会原地修改输入，直接排序共享数组会破坏调用方依赖的顺序和索引语义。应使用 <code>[...input].sort((a, b) =&gt; a - b)</code> 创建排序副本；代价是 <code>O(n)</code> 额外空间，若契约明确允许修改才可省略复制。</p>\\n</li>\\n<li>\\n<p><strong>需求从“和为 <code>0</code>”改成“和为任意 <code>target</code>”，现有双指针移动规则和剪枝 <code>nums[i] &gt; 0</code> 哪部分需要变化？</strong></p>\\n<p>内层应比较三数和与 <code>target</code>，小于目标时右移 <code>left</code>，大于目标时左移 <code>right</code>，单调收缩逻辑仍成立。<code>nums[i] &gt; 0</code> 的提前终止只适用于目标为零且数组升序的场景，不能原样保留；新剪枝必须结合目标和剩余边界重新证明。</p>\\n</li>\\n<li>\\n<p><strong>线上大数组执行时间异常，日志显示固定值已经为正数后仍扫描到末尾，你会补哪两个安全剪枝并怎样验证？</strong></p>\\n<p>排序后若目标仍是零且 <code>nums[i] &gt; 0</code>，后续元素只会更大，可以直接 <code>break</code>；若 <code>nums[i] === nums[i-1]</code>，应 <code>continue</code> 跳过重复固定值。排查时还要确认排序使用数值比较器，否则默认字典序会破坏这些单调性判断。</p>\\n</li>\\n<li>\\n<p><strong>候选人主张给每个固定值套一遍哈希两数之和，另一位主张排序加双指针；在必须返回不重复数值三元组时你选谁？</strong></p>\\n<p>两种方案通常都要枚举固定元素，整体仍是 <code>O(n²)</code> 量级，哈希并不会把三数问题降成线性。排序双指针能利用相邻重复值直接去重，状态更清晰；代价是排序成本和顺序变化，因此业务函数宜先复制输入。</p>\\n</li>\\n<li>\\n<p><strong>三数之和与两数之和都说“把求和转成求差”，为什么前者仍需要有序数组和双指针而不是一张全局 <code>Map</code>？</strong></p>\\n<p>三数之和必须先固定一个元素，再在其后的候选区间寻找另外两个数，同时还要保证数值三元组不重复。全局 <code>Map</code> 难以自然表达固定轮次、索引互异和统一去重；排序后的双指针用单调移动缩小范围，并能通过相邻元素完成稳定跳重。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"双指针法中的-对撞指针-法\\"><a class=\\"header-anchor\\" href=\\"#双指针法中的-对撞指针-法\\">#</a> 双指针法中的“对撞指针”法</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>对撞指针从有序区间两端向内移动，用比较结果决定唯一方向</li>\\n<li>循环不变量是答案若存在一定仍在 <code>[left, right]</code> 内；每次移动必须证明被排除值不可能参与答案</li>\\n<li>典型题包括两数和、三数和内层、回文、盛水容器与有序数组去重，但移动规则各不相同</li>\\n<li>排序创造单调性会增加 <code>O(n log n)</code> 并可能丢失原索引，不能见数组就无脑排序</li>\\n<li>指针必须严格推进并守住 <code>left &lt; right</code> 或 <code>left &lt;= right</code> 的契约，否则会死循环或漏掉中点</li>\\n</ul>\\n<p><strong>对撞指针就是从有序区间两端向中间收缩，并根据比较结果确定唯一的移动方向。</strong> 本质上，每次移动都要能证明被越过的值不可能参与答案，而不是凭经验猜一个方向。它适合有序两数和、三数和内层这类存在单调关系的场景。若题目需要先排序，还要考虑 <code>O(n log n)</code> 成本、输入被修改以及原下标可能丢失的问题。</p>\\n</section>\\n<p>对撞指针是否正确，关键在“排除证明”。以有序两数和为例，当前和太小，则固定右值时，左指针当前值已经是区间最小值，它不可能和任何更小值组成目标，因此可以安全右移；和太大时对右端同理。若没有这种单调关系，移动就只是猜测。</p>\\n<p>在上面这道题中，左右指针一起从两边往中间位置相互迫近，这样的特殊双指针形态，被称为“对撞指针”。</p>\\n<p><strong>什么时候你需要联想到对撞指针？</strong></p>\\n<p>这里我给大家两个关键字——“有序”和“数组”。</p>\\n<p>没错，见到这两个关键字，立刻把双指针法调度进你的大脑内存。普通双指针走不通，立刻想对撞指针！</p>\\n<p>即便数组题目中并没有直接给出“有序”这个关键条件，我们在发觉普通思路走不下去的时候，也应该及时地尝试手动对其进行排序试试看有没有新的切入点——没有条件，创造条件也要上。</p>\\n<p>对撞指针可以帮助我们缩小问题的范围，这一点在“三数求和”问题中体现得淋漓尽致：因为数组有序，所以我们可以用两个指针“画地为牢”圈出一个范围，这个范围以外的值不是太大就是太小、直接被排除在我们的判断逻辑之外，这样我们就可以把时间花在真正有意义的计算和对比上。如此一来，不仅节省了计算的时间，更降低了问题本身的复杂度，我们做题的速度也会大大加快。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表已经按价格升序排列，现在要找两件总价最接近预算、且品牌不能相同的商品，为什么不能看到“有序数组”就直接套对撞指针？</strong></p>\\n<p>价格有序只能让总价比较具备单调性，品牌约束却可能使当前组合失效，却不能据此安全淘汰任意一端。移动前必须证明被跳过的商品不可能参与更优解；若证明不了，就要增加状态、分组处理或改用其他搜索方案，不能凭经验移动。</p>\\n</li>\\n<li>\\n<p><strong>结算页需要从已排序价格数组中找两件商品恰好凑满 <code>target</code>，你会怎样向评审证明 <code>sum &lt; target</code> 时执行 <code>left += 1</code> 不会漏解？</strong></p>\\n<p>固定当前 <code>right</code> 后，<code>nums[left]</code> 是候选区间的最小值，当前和仍小于 <code>target</code>，它与任何更靠左或不大于右端的可选值都无法达到目标。因此当前左值可以整体淘汰；证明依赖升序与两数和的单调关系，乱序输入或非单调条件下不成立。</p>\\n</li>\\n<li>\\n<p><strong>三数求和页面有 <code>10^5</code> 个整数，产品又要求返回原始下标且禁止修改输入；你还会先排序再使用对撞指针吗？</strong></p>\\n<p>仍可考虑排序，但要复制为“值与原下标”的记录，再固定一个元素并在剩余区间内对撞，避免破坏原数组和丢失索引。排序带来额外空间与 <code>O(n log n)</code> 预处理，而三数枚举通常仍为 <code>O(n²)</code>；数据规模是否可接受必须结合输出数量上限判断。</p>\\n</li>\\n<li>\\n<p><strong>线上两数和接口在重复值数据 <code>[3,3]</code>、目标值 <code>6</code> 时返回空数组，代码循环条件写成了 <code>left &lt; right - 1</code>，你先查什么？</strong></p>\\n<p>先检查候选区间定义和终止条件，因为该条件会在两个指针相邻时提前退出，唯一合法组合根本没有被检查。若两个元素不能复用同一位置，循环应覆盖所有 <code>left &lt; right</code> 的状态；改完还要补相邻指针、双元素数组和全重复值用例。</p>\\n</li>\\n<li>\\n<p><strong>同一个已排序两数和需求，前端负责人主张对撞指针，服务端负责人主张哈希表；在需要常量额外空间并返回任意一组值时你怎么选？</strong></p>\\n<p>已有升序数组且只需返回一组值时，对撞指针更贴合约束，扫描为 <code>O(n)</code>，额外状态只需两个下标。哈希表也能线性查找，但需要 <code>O(n)</code> 额外空间；若输入无序且禁止排序，或必须保留扫描过程中的原始位置，取舍才可能转向哈希。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);