(window.webpackJsonp=window.webpackJsonp||[]).push([[16],{1122:function(n){n.exports=JSON.parse('{"html":"<p>结束了数据结构基本功的学习，接下来在真正开始撸真题之前，大家还需要具备评价算法的能力。</p>\\n<p>平时我们定义一个人是否“懂行”，一个重要的依据就是看这个人对某一个事物是否具备正确的评价能力。\\n举个例子，同样是买手机，外行进到手机店，他关注的可能是手机有没有跑马灯、有没有皮套护体、有没有“八心八箭”——这些东西，任何一部手机随便包装一下就都有了，根本没法反映出这台手机的本质问题。但如果是一个相对懂手机的人，他可能就会去关注这台手机的芯片、内存、屏幕材质及分辨率等等，从而对手机的整体性能和质量作出一个合理的判断，这样他买到好手机的概率就更大。</p>\\n<p>回到做算法题上，也是一样的道理。在面试时，自己给出的算法到底过不过得去，这一点在面试官给出评语之前，自己就应该有所感知。做到这一点，你才会掌握改进算法的主动权。</p>\\n<p>本节我们要学习的就是评价算法的两个重要依据——时间复杂度和空间复杂度。</p>\\n<p>很多同学算法入门直接就跪在复杂度理解这一环。时间复杂度、空间复杂度，直接读概念确实太无聊，我们本节从代码入手，大家的理解会更直观一点。</p>\\n<h2 id=\\"时间复杂度\\"><a class=\\"header-anchor\\" href=\\"#时间复杂度\\">#</a> 时间复杂度</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>大 O 描述输入规模增大时的渐进上界，不是精确耗时；分析前必须定义 <code>n</code> 代表什么</li>\\n<li>顺序代码复杂度相加取主导项，独立嵌套循环通常相乘，相关循环要按实际总迭代次数求和</li>\\n<li>每轮把问题缩小固定比例通常是 <code>O(log n)</code>，递归还要结合子问题数量和每层工作量</li>\\n<li>哈希 <code>O(1)</code> 常是期望/均摊结论，快排 <code>O(n log n)</code> 常是平均结论，必须带条件</li>\\n<li>数据规模、常数、缓存局部性、I/O 和最坏延迟都会影响工程选型，量级分析后仍需基准测试</li>\\n</ul>\\n<p><strong>时间复杂度描述输入规模增大时，算法执行次数的增长趋势，而不是精确运行时间。</strong> 分析前要明确 <code>n</code> 的含义：顺序代码相加后取主导项，独立嵌套循环通常相乘，但相关循环要计算实际总次数。比如内层规模依次减半时，总工作量小于 <code>2n</code>，即使有两层循环仍是 <code>O(n)</code>。递归要同时看分支、规模缩减和每层工作量，工程选型还应结合常数、缓存、<code>I/O</code> 与基准测试。</p>\\n</section>\\n<p>看到两层循环不能机械报 <code>O(n²)</code>。下面内层总次数是 <code>n + n/2 + n/4 + ... &lt; 2n</code>，因此整体仍为 <code>O(n)</code>：</p>\\n<pre class=\\"language-js\\"><code>function halvingWork(items) {\\n  let operations = 0\\n  for (let size = items.length; size &gt; 0; size = Math.floor(size / 2)) {\\n    for (let i = 0; i &lt; size; i += 1) operations += 1\\n  }\\n  return operations\\n}\\n\\nconsole.log(halvingWork(Array(8))) // 15\\n</code></pre>\\n<p>两个前后执行的线性循环是 <code>O(n) + O(n) = O(n)</code>，而不是 <code>O(n²)</code>。矩阵应使用行数 <code>r</code> 与列数 <code>c</code> 表达 <code>O(r × c)</code>，只有二者都等于 <code>n</code> 才写 <code>O(n²)</code>。递归则必须同时分析分支数、规模缩减和每层额外工作。</p>\\n<p>大家先来看这样一个问题：下面这段代码，一共会执行多少次？</p>\\n<pre class=\\"language-js\\"><code>function traverse(arr) {\\n    var len = arr.length\\n    for(var i=0;i&lt;len;i++) {\\n        console.log(arr[i])\\n    }\\n}\\n</code></pre>\\n<p>首先，最没有悬念的是函数里的第一行代码，它只会被执行1次：</p>\\n<pre class=\\"language-text\\"><code>var len = arr.length\\n</code></pre>\\n<p>其次没有悬念的是循环体：</p>\\n<pre class=\\"language-text\\"><code>console.log(arr[i])\\n</code></pre>\\n<p>for循环跑了 n 次，因此这条语句就会被执行 n 次。</p>\\n<p>循环体上面的几个部分我们拆开来看，首先是 i 的初始化语句：</p>\\n<pre class=\\"language-text\\"><code>var i = 0\\n</code></pre>\\n<p>初始化只有1次，因此它也只会被执行1次。</p>\\n<blockquote>\\n<p>接着是 i &lt; len 这个判断。这里有个规律大家可以记下：在所有的 for 循环里，判断语句都会比递增语句多执行一次。在这里，判断语句执行的次数就是 n+1。</p>\\n</blockquote>\\n<p>再往下就是递增语句 i++ 了，它跟随整个循环体，毫无疑问会被执行 n 次。</p>\\n<p>假如把总的执行次数记为 T(n)，下面咱们就可以来做个简单的加法：</p>\\n<pre class=\\"language-text\\"><code>T(n) = 1 + n + 1 + (n+1) + n = 3n + 3\\n</code></pre>\\n<p>接下来我们看看规模为 n*n 的二维数组的遍历，一共需要执行多少次代码：</p>\\n<pre class=\\"language-js\\"><code>function traverse(arr) {\\n    var outLen = arr.length\\n\\n    for(var i=0;i&lt;outLen;i++) {\\n        var inLen = arr[i].length\\n\\n        for(var j=0;j&lt;inLen;j++) { \\n            console.log(arr[i][j])\\n        }\\n    }\\n}\\n</code></pre>\\n<p>首先仍然是没有悬念的第一行代码，它只会被执行一次：</p>\\n<pre class=\\"language-text\\"><code>var outLen = arr.length\\n</code></pre>\\n<p>接下来我们来看最内层的循环体：</p>\\n<pre class=\\"language-text\\"><code>console.log(arr[i][j])\\n</code></pre>\\n<blockquote>\\n<p>因为咱们是两层循环，所以这货会被执行 <code>n*n = n^2</code> 次。</p>\\n</blockquote>\\n<p>其它语句的计算思路和咱们第一个🌰区别不大，这里我就不重复讲了，直接给出大家答案：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111506.png\\" alt=\\"\\"></p>\\n<p>继续来做个求总执行次数 T(n) 的加法看看：</p>\\n<pre class=\\"language-text\\"><code>T(n) = 1 + 1 + (n+1) + n + n + n + n*(n+1) + n*n + n*n = 3n^2 + 5n + 3\\n</code></pre>\\n<blockquote>\\n<p>代码的执行次数，可以反映出代码的执行时间。但是如果每次我们都逐行去计算 <code>T(n)</code>，事情会变得非常麻烦。算法的时间复杂度，它反映的不是算法的逻辑代码到底被执行了多少次，而是随着输入规模的增大，算法对应的执行总次数的一个变化趋势。要想反映趋势，那就简单多了，直接抓主要矛盾就行。我们可以尝试对 T(n) 做如下处理：</p>\\n</blockquote>\\n<ul>\\n<li>若 <code>T(n)</code> 是常数，那么无脑简化为1</li>\\n<li>若 <code>T(n)</code> 是多项式，比如 <code>3n^2 + 5n + 3</code>，我们只保留次数最高那一项，并且将其常数系数无脑改为1。</li>\\n</ul>\\n<p>经过这么一波操作，T(n) 就被简化为了 O(n)：</p>\\n<pre class=\\"language-text\\"><code>T(n) = 10  \\nO(n) = 1\\nT(n) = 3n^2 + 5n + 3\\nO(n) = n^2\\n</code></pre>\\n<blockquote>\\n<p>到这里，我们思路仍然是 <code>计算T(n) -&gt; 推导O(n)</code>。这么讲是为了方便大家理解 O(n) 的简化过程，实际操作中，O(n) 基本可以目测，比如咱们上面的两个遍历函数：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>function traverse1(arr) {\\n    var len = arr.length\\n    for(var i=0;i&lt;len;i++) {\\n        console.log(arr[i])\\n    }\\n}\\n\\nfunction traverse2(arr) {\\n    var outLen = arr.length\\n\\n    for(var i=0;i&lt;outLen;i++) {\\n        var inLen = arr[i].length\\n\\n        for(var j=0;j&lt;inLen;j++) { \\n            console.log(arr[i][j])\\n        }\\n    }\\n}\\n</code></pre>\\n<p>遍历 N 维数组，需要 N 层循环，我们只需要关心其最内层那个循环体被执行多少次就行了。</p>\\n<blockquote>\\n<p>我们可以看出，规模为 n 的一维数组遍历时，最内层的循环会执行 n 次，其对应的时间复杂度是 O(n)；规模为 n<em>n 的二维数组遍历时，最内层的循环会执行 n</em>n 次，其对应的时间复杂度是 O(n^2)。</p>\\n</blockquote>\\n<blockquote>\\n<p>以此类推，规模为 n<em>m 的二维数组最内层循环会执行 n</em>m 次，其对应的时间复杂度就是 O(n<em>m)；规模为 n</em>n*n 的三维数组最内层循环会执行 n^3 次，因此其对应的时间复杂度就表示为 O(n^3)。</p>\\n</blockquote>\\n<p>常见的时间复杂度表达，除了多项式以外，还有<code>logn</code>。我们一起来看另一个算法：</p>\\n<pre class=\\"language-js\\"><code>function fn(arr) {\\n    var len = arr.length  \\n    \\n    for(var i=1;i&lt;len;i=i*2) {\\n        console.log(arr[i])\\n    }\\n}\\n</code></pre>\\n<p>这个算法读取一个一维数组作为入参，然后对其中的元素进行跳跃式的输出。这个跳跃的规则，就是数组下标从1开始，每次会乘以二。</p>\\n<blockquote>\\n<p>如何计算这个函数的时间复杂度呢？在有循环的地方，我们关心的永远是最内层的循环体。这个算法中，我们关心的就是 <code>console.log(arr[i])</code> 到底被执行了几次，换句话说，也就是要知道 <code>i&lt;n（ len === n）</code> 这个条件是在 i 递增多少次后才不成立的。</p>\\n</blockquote>\\n<p>假设 i 在以 <code>i=i*2</code>的规则递增了 <code>x</code> 次之后，<code>i&lt;n</code> 开始不成立（反过来说也就是 <code>i&gt;=n</code> 成立）。那么此时我们要计算的其实就是这样一个数学方程：</p>\\n<pre class=\\"language-text\\"><code>2^x &gt;= n\\n</code></pre>\\n<blockquote>\\n<p><code>x</code>解出来，就是要大于等于以 <code>2</code> 为底数的 <code>n</code> 的对数：</p>\\n</blockquote>\\n<p><img src=\\"https://s.poetries.top/images/20210905111520.png\\" alt=\\"\\"></p>\\n<blockquote>\\n<p>也就是说，只有当 x 小于 log2n 的时候，循环才是成立的、循环体才能执行。注意涉及到对数的时间复杂度，底数和系数都是要被简化掉的。那么这里的 O(n) 就可以表示为：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>O(n) = logn\\n</code></pre>\\n<p>没错，这时的主要矛盾，就变成了一个对数表达式。</p>\\n<p>关于常见的时间复杂度，我们会在后面讲到具体知识点（尤其是排序算法）时，结合实例来给大家做分析。这里大家首先要认识一下常见时间复杂度有哪些，并且对这些常见时间复杂度之间的大小关系做个把握。</p>\\n<p>常见的时间复杂度按照从小到大的顺序排列，有以下几种：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210905111527.png\\" alt=\\"\\"></p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审里有人看到 <code>halvingWork</code> 有两层循环，直接标成 <code>O(n²)</code> 并要求重写，你会怎样当场验证他的判断？</strong></p>\\n<p>不能按循环层数直接相乘，应先计算内层循环的总执行次数。各轮规模形成 <code>n + n/2 + n/4 + ...</code>，总和小于 <code>2n</code>，所以整体是 <code>O(n)</code>；若循环边界改为每轮都执行 <code>n</code> 次，结论才会变成 <code>O(n log n)</code>。</p>\\n</li>\\n<li>\\n<p><strong>数据看板先后用两个循环处理同一批 <code>n</code> 条记录，负责人担心复杂度从 <code>O(n)</code> 升到 <code>O(n²)</code>，你会如何解释并决定是否合并循环？</strong></p>\\n<p>两个前后执行的线性循环总工作量是 <code>n + n</code>，忽略常数因子后仍为 <code>O(n)</code>。是否合并应看中间结果、缓存局部性和可读性，而不是为了改变渐进复杂度；若第二个循环实际遍历另一批 <code>m</code> 条数据，则应保留为 <code>O(n + m)</code>。</p>\\n</li>\\n<li>\\n<p><strong>表格组件遍历一个 <code>r</code> 行、<code>c</code> 列的矩阵，产品经理坚持需求文档统一写成 <code>O(n²)</code>，你为什么会要求改成 <code>O(r × c)</code>？</strong></p>\\n<p>行数与列数是两个可独立变化的输入规模，遍历次数应表达为 <code>r × c</code>。只有明确约束 <code>r = c = n</code> 时才能简写成 <code>O(n²)</code>；若线上表格是少量行配大量列，错误记号会掩盖真正随哪一维增长的成本。</p>\\n</li>\\n<li>\\n<p><strong>分页接口把循环步长从 <code>i += 1</code> 改成 <code>i *= 2</code> 后，监控显示大数据下增长明显放缓，你会怎样推导它的时间复杂度？</strong></p>\\n<p>循环执行 <code>x</code> 次后索引约为 <code>2^x</code>，终止条件对应 <code>2^x &gt;= n</code>，因此迭代次数是 <code>O(log n)</code>。大 <code>O</code> 中对数底数和常数系数会被忽略；若循环体每轮还处理与当前索引等量的数据，就不能只依据步长判为对数复杂度。</p>\\n</li>\\n<li>\\n<p><strong>一个递归搜索每层分成两个调用、输入规模各自减半，同时每层还线性扫描当前数据，候选人只凭“规模减半”报 <code>O(log n)</code>，你会追问他补充什么分析？</strong></p>\\n<p>要同时核对递归分支数、子问题规模和每层额外工作，不能只看递归深度。两个规模减半的分支会形成多层调用树，而每层线性工作的总量还需按整层汇总；缺少重复子问题和终止条件信息时，不应武断给出单一复杂度。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"空间复杂度\\"><a class=\\"header-anchor\\" href=\\"#空间复杂度\\">#</a> 空间复杂度</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>空间复杂度通常分析除输入外的辅助空间；若题目计算总占用，要先声明口径</li>\\n<li>固定数量变量是 <code>O(1)</code>，但一个随 <code>n</code> 增长的数组仍是 <code>O(n)</code>，不能按变量名个数计算</li>\\n<li>递归调用栈必须计入：平衡树 <code>O(log n)</code>，退化树 <code>O(n)</code></li>\\n<li>原地算法也可能使用隐藏空间，如排序递归栈、字符串切片副本和运行库缓冲区</li>\\n<li>原地修改省空间却可能污染调用方数据，空间优化要同时说明所有权和失败中间态</li>\\n</ul>\\n<p><strong>空间复杂度描述算法运行时内存占用随输入规模增长的趋势，通常关注输入之外的辅助空间。</strong> 固定数量的变量是 <code>O(1)</code>，但长度随 <code>n</code> 增长的数组是 <code>O(n)</code>，不能只数变量名。递归调用栈也要计入，例如平衡树通常占 <code>O(log n)</code>，退化树可能达到 <code>O(n)</code>。空间看峰值而非累计分配，同时还要留意原地修改对调用方数据的影响。</p>\\n</section>\\n<p>下面两种求和时间都是 <code>O(n)</code>，辅助空间却不同：</p>\\n<pre class=\\"language-js\\"><code>function sumCopy(values) {\\n  const normalized = values.map(Number)\\n  return normalized.reduce((sum, value) =&gt; sum + value, 0)\\n}\\n\\nfunction sumInPlace(values) {\\n  let sum = 0\\n  for (const value of values) sum += Number(value)\\n  return sum\\n}\\n</code></pre>\\n<p><code>sumCopy</code> 同时保留输入与长度为 <code>n</code> 的新数组，辅助空间 <code>O(n)</code>；<code>sumInPlace</code> 只有固定变量，辅助空间 <code>O(1)</code>。空间看峰值而非累计分配：循环里反复创建短命对象，若前一轮对象已经不可达，理论峰值可能仍是 <code>O(1)</code>，但实际垃圾回收压力仍需测量。</p>\\n<p>空间复杂度是对一个算法在运行过程中临时占用存储空间大小的量度。和时间复杂度相似，它是内存增长的趋势。</p>\\n<p>常见的空间复杂度有 <code>O(1)</code>、<code>O(n)</code> 和 <code>O(n^2)</code>。</p>\\n<p>理解空间复杂度，我们照样来看一个🌰：</p>\\n<pre class=\\"language-js\\"><code>function traverse(arr) {\\n    var len = arr.length\\n    for(var i=0;i&lt;len;i++) {\\n        console.log(arr[i])\\n    }\\n}\\n</code></pre>\\n<p>在 traverse 中，占用空间的有以下变量：</p>\\n<pre class=\\"language-text\\"><code>arr    \\nlen   \\ni    \\n</code></pre>\\n<blockquote>\\n<p>后面尽管咱们做了很多次循环，但是这些都是时间上的开销。循环体在执行时，并没有开辟新的内存空间。因此，整个 traverse 函数对内存的占用量是恒定的，它对应的空间复杂度就是 O(1)。</p>\\n</blockquote>\\n<p>下面我们来看另一个🌰，此时我想要初始化一个规模为 n 的数组，并且要求这个数组的每个元素的值与其索引始终是相等关系，我可以这样写：</p>\\n<pre class=\\"language-js\\"><code>function init(n) {\\n    var arr = []\\n    for(var i=0;i&lt;n;i++) {\\n        arr[i] = i\\n    }\\n    return arr\\n}\\n</code></pre>\\n<p>在这个 init 中，涉及到的占用内存的变量有以下几个：</p>\\n<pre class=\\"language-text\\"><code>n \\narr\\ni\\n</code></pre>\\n<blockquote>\\n<p>注意这里这个 arr，它并不是一个一成不变的数组。arr最终的大小是由输入的 n 的大小决定的，它会随着 n 的增大而增大、呈一个线性关系。因此这个算法的空间复杂度就是 O(n)。</p>\\n</blockquote>\\n<p>由此我们不难想象，假如需要初始化的是一个规模为 <code>n*n</code> 的数组，那么它的空间复杂度就是 <code>O(n^2)</code> 啦。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>日志清洗函数循环处理一百万条记录，但始终只复用 <code>i</code>、<code>sum</code> 和当前值，评审为什么不能因为循环很多次就判空间为 <code>O(n)</code>？</strong></p>\\n<p>循环次数属于时间开销，空间应看运行期间的峰值占用。若函数只保留固定数量的变量，且没有把每轮结果存入集合，其辅助空间仍是 <code>O(1)</code>；输入数组本身是否计入，还要先明确讨论的是总空间还是辅助空间。</p>\\n</li>\\n<li>\\n<p><strong>结算页面用 <code>values.map(Number)</code> 后再 <code>reduce</code>，另一版在原数组上直接累加，两版时间都是 <code>O(n)</code>，你会怎样向负责人说明内存差异？</strong></p>\\n<p><code>map</code> 会创建并同时保留一个长度为 <code>n</code> 的新数组，因此辅助空间是 <code>O(n)</code>；直接遍历只维护固定数量的变量，辅助空间是 <code>O(1)</code>。后者减少峰值占用，但若后续仍需要规范化后的数组，就不能仅为省空间删除这份结果。</p>\\n</li>\\n<li>\\n<p><strong>埋点处理循环每轮创建一个临时对象，压测时垃圾回收频繁，候选人却说空间是 <code>O(1)</code> 所以无需处理，你会如何纠正？</strong></p>\\n<p>若前一轮对象不再被引用，理论上的峰值同时存活量确实可能保持 <code>O(1)</code>。但渐进空间复杂度不反映分配频率和垃圾回收压力，仍应检查对象是否被闭包、数组或异步任务保留，并用代表性负载测量实际内存与停顿。</p>\\n</li>\\n<li>\\n<p><strong>图片编辑器初始化一个随输入增长的 <code>n × n</code> 临时网格，开发者只统计了循环变量并声称空间为 <code>O(1)</code>，故障排查时你先看哪里？</strong></p>\\n<p>应先确认网格是否在处理期间完整存活，因为它包含随 <code>n²</code> 增长的元素，空间复杂度是 <code>O(n²)</code>。循环变量只有常量级，不能覆盖主要占用；若网格可按行流式处理，峰值可能下降，但会增加实现约束或重复计算。</p>\\n</li>\\n<li>\\n<p><strong>同事建议把所有中间数组都改成原地处理以降低空间，调用方又要求保留原始输入，你会如何做方案取舍？</strong></p>\\n<p>原地处理只有在允许修改输入且共享引用不会观察到中间状态时才合适，否则会破坏调用契约。可保留 <code>O(n)</code> 副本换取隔离性，或使用逐项转换保持 <code>O(1)</code> 辅助空间；最终选择应同时说明峰值内存、数据所有权和失败恢复代价。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"小结\\"><a class=\\"header-anchor\\" href=\\"#小结\\">#</a> 小结</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>复杂度描述输入规模增长时资源消耗的数量级，忽略常数和低阶项，但不能忽略不同输入维度</li>\\n<li>时间复杂度要找基本操作的执行次数；空间复杂度要区分输出空间、额外空间和递归调用栈</li>\\n<li>最坏、平均、最好情况必须明确口径；面试默认常讨论最坏情况，不能只报一个字母不解释前提</li>\\n</ul>\\n<p><strong>复杂度用于衡量输入规模增长时，算法时间和空间消耗的数量级。</strong> 分析时间时看基本操作执行次数，分析空间时要分清输出、辅助空间以及递归调用栈；常数和低阶项可以忽略，不同输入维度却不能混为一个 <code>n</code>。同一算法还可能有最好、平均和最坏情况，所以只报一个 <code>O(...)</code> 不够。面试中我会把采用的口径和成立条件一起说清楚。</p>\\n</section>\\n<p>结束了本节的学习，相信各位对时间复杂度和空间复杂度都有了一个感性的认知和初步的了解。在后续的学习中，我们会在必要的时候继续为大家提点真题中的时间复杂度和空间复杂度，带领大家在实战中强化对理论概念的认知</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审中两个连续的 <code>O(n)</code> 校验循环被标成 <code>O(n²)</code>，你会用什么执行次数模型纠正结论？</strong></p>\\n<p>两个循环前后执行时，总次数近似为 <code>n + n = 2n</code>，忽略常数因子后仍是 <code>O(n)</code>。只有一个循环嵌套在另一个循环内，且两者都完整遍历 <code>n</code> 个元素时，才形成 <code>O(n²)</code>；若规模分别为 <code>n</code> 和 <code>m</code>，应写成 <code>O(n + m)</code>。</p>\\n</li>\\n<li>\\n<p><strong>列表页的一段代码时间是 <code>O(n)</code>、辅助空间也是 <code>O(n)</code>，产品只说“复杂度太高”，你会要求团队怎样拆开评估？</strong></p>\\n<p>时间复杂度描述执行次数随输入规模的增长趋势，空间复杂度描述运行时额外存储的增长趋势，两者不能用一个“高”字替代。应分别找出主导循环和峰值存活数据，再结合页面的数据上限判断；降低一项有时会以增加另一项为代价。</p>\\n</li>\\n<li>\\n<p><strong>一个函数在小样本下耗时稳定，负责人据此认定它是 <code>O(1)</code>，当输入从百条扩大到百万条时你会要求补什么证据？</strong></p>\\n<p>固定规模下的耗时不能证明增长趋势，应检查执行次数是否随 <code>n</code>、<code>n²</code> 或 <code>log n</code> 变化，并使用多个数量级的输入观察走势。实测还会混入常数成本和运行时波动，所以应先做代码层面的复杂度推导，再用测量验证工程影响。</p>\\n</li>\\n<li>\\n<p><strong>线上任务没有创建数组，却在处理退化树时出现内存上涨和栈溢出，为什么“局部变量固定，所以空间 <code>O(1)</code>”的排查方向不成立？</strong></p>\\n<p>递归期间尚未返回的调用栈帧会同时存活，额外空间取决于递归深度 <code>h</code>，应记为 <code>O(h)</code>。退化树可能使 <code>h</code> 接近结点数 <code>n</code>，从而达到 <code>O(n)</code>；排查时要把隐式调用栈和显式容器一并纳入峰值空间。</p>\\n</li>\\n<li>\\n<p><strong>架构评审在复制数组的 <code>O(n)</code> 空间方案与原地扫描的 <code>O(1)</code> 方案之间争执，你会如何限定最终结论？</strong></p>\\n<p>若只需一次聚合且允许逐项转换，原地扫描能以固定辅助变量完成，更节省峰值空间。若调用方需要保留规范化后的完整结果，<code>O(n)</code> 数组就是需求成本；复杂度比较只能说明增长趋势，不能替代对数据所有权、可读性和错误恢复的判断。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);