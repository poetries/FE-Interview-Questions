(window.webpackJsonp=window.webpackJsonp||[]).push([[121],{1229:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：性能优化先建立用户指标与资源、主线程、渲染和网络证据，再定位最长关键路径</li>\\n<li>原理主线：围绕 「善用 shouldComponentUp」、「进阶玩法：PureComponent +」、「函数组件的性能优化：React.memo」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：系统讲解了 React 性能优化的核心原理与实践方法，包括 shouldComponentUpdate、PureComponent、React.memo、useMemo 等技术，帮助开发者有效减少不必要的渲染，提升大型 React 应用的性</li>\\n<li>边界与代价：实验室分数不等于真实用户体验，平均值也会掩盖长尾；优化一项指标可能转移成本</li>\\n<li>工程落地：用现场数据确定瓶颈、做最小改动、对照验证并持续监控回归</li>\\n</ul>\\n<p><strong><code>React</code> 性能优化的关键，是避免没有数据变化却仍然发生的重复渲染或重复计算。</strong> 类组件可用 <code>shouldComponentUpdate</code> 精确判断，也可用 <code>PureComponent</code> 对 <code>props</code> 和 <code>state</code> 做浅比较；函数组件则常用 <code>React.memo</code> 控制组件重渲染，用 <code>useMemo</code> 缓存局部计算。浅比较依赖引用稳定，内容未变但引用变了会多渲染，内容变了但引用没变又可能漏渲染。<code>Immutable.js</code> 能把内容变化和新引用关联起来，但存在学习成本，不一定适合所有团队。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文若分析 <code>ReactDOM.render</code>、旧生命周期或栈调和，应把它视为理解架构演进的历史路径。<code>React 19</code> 已移除 <code>ReactDOM.render</code>，当前客户端入口使用 <code>createRoot</code>；并发渲染也必须区分可中断的渲染阶段与同步提交阶段。迁移前对照 <a href=\\"https://react.dev/blog/2024/04/25/react-19-upgrade-guide\\">React 19 官方升级指南</a>。</p>\\n<ul>\\n<li>使用 <code>shouldComponentUpdate</code> 规避冗余的更新逻辑</li>\\n<li><code>PureComponent + Immutable.js</code></li>\\n<li><code>React.memo</code> 与 <code>useMemo</code></li>\\n</ul>\\n<blockquote>\\n<p>注：这 3 个思路同时也是 React 面试中“性能优化”这一环的核心所在</p>\\n</blockquote>\\n<h2 id=\\"善用-shouldcomponentupdate\\"><a class=\\"header-anchor\\" href=\\"#善用-shouldcomponentupdate\\">#</a> 善用 shouldComponentUpdate</h2>\\n<p><code>shouldComponentUpdate</code> 的调用形式如下：</p>\\n<pre class=\\"language-text\\"><code>shouldComponentUpdate(nextProps, nextState)\\n</code></pre>\\n<blockquote>\\n<ul>\\n<li><code>render</code> 方法由于伴随着对虚拟 <code>DOM</code> 的构建和对比，过程可以说相当耗时。而在 <code>React</code> 当中，很多时候我们会不经意间就频繁地调用了 <code>render</code>。为了避免不必要的 <code>render</code> 操作带来的性能开销，React 提供了 <code>shouldComponentUpdate</code> 这个口子。React 组件会根据 <code>shouldComponentUpdate</code> 的返回值，来决定是否执行该方法之后的生命周期，进而决定是否对组件进行 <code>re-render</code>（重渲染）。</li>\\n<li><code>shouldComponentUpdate</code> 的默认值为 <code>true</code>，也就是说 “无条件 re-render”。在实际的开发中，我们往往通过手动往 <code>shouldComponentUpdate</code> 中填充判定逻辑，来实现“有条件的 re-render”。</li>\\n</ul>\\n</blockquote>\\n<p>首先我们来看两个子组件的代码，这里为了尽量简化与数据变更无关的逻辑，ChildA 和 ChildB 都只负责从父组件处读取数据并渲染，它们的编码分别如下所示。</p>\\n<pre class=\\"language-js\\"><code>// ChildA.js：\\n\\nimport React from &quot;react&quot;;\\nexport default class ChildA extends React.Component {\\n  render() {\\n    console.log(&quot;ChildA 的render方法执行了&quot;);\\n    return (\\n      &lt;div className=&quot;childA&quot;&gt;\\n        子组件A的内容：\\n        {this.props.text}\\n      &lt;/div&gt;\\n    );\\n  }\\n}\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// ChildB.js：\\n\\nimport React from &quot;react&quot;;\\nexport default class ChildB extends React.Component {\\n  render() {\\n    console.log(&quot;ChildB 的render方法执行了&quot;);\\n    return (\\n      &lt;div className=&quot;childB&quot;&gt;\\n        子组件B的内容：\\n        {this.props.text}\\n      &lt;/div&gt;\\n    );\\n  }\\n}\\n</code></pre>\\n<pre class=\\"language-js\\"><code>// 在共同的父组件 App.js 中，会将 ChildA 和 ChildB 组合起来，并分别向其中注入数据：\\n\\nimport React from &quot;react&quot;;\\nimport ChildA from &#39;./ChildA&#39;\\nimport ChildB from &#39;./ChildB&#39;\\nclass App extends React.Component {\\n  state = {\\n    textA: &#39;我是A的文本&#39;,\\n    textB: &#39;我是B的文本&#39;\\n  }\\n  changeA = () =&gt; {\\n    this.setState({\\n      textA: &#39;A的文本被修改了&#39;\\n    })\\n  }\\n  changeB = () =&gt; {\\n    this.setState({\\n      textB: &#39;B的文本被修改了&#39;\\n    })\\n  }\\n  render() {\\n    return (\\n    &lt;div className=&quot;App&quot;&gt;\\n      &lt;div className=&quot;container&quot;&gt;\\n        &lt;button onClick={this.changeA}&gt;点击修改A处的文本&lt;/button&gt;\\n        &lt;button onClick={this.changeB}&gt;点击修改B处的文本&lt;/button&gt;\\n        &lt;ul&gt;\\n          &lt;li&gt;\\n            &lt;ChildA text={this.state.textA}/&gt;\\n          &lt;/li&gt;\\n        &lt;li&gt;\\n          &lt;ChildB text={this.state.textB}/&gt;\\n        &lt;/li&gt;\\n        &lt;/ul&gt;\\n      &lt;/div&gt;\\n    &lt;/div&gt;\\n  );\\n  }\\n}\\nexport default App;\\n</code></pre>\\n<p>App 组件最终渲染到界面上的效果如下图所示，两个子组件在图中分别被不同颜色的标注圈出：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210428213034.png\\" alt=\\"\\"></p>\\n<p>通过点击左右两个按钮，我们可以分别对 ChildA 和 ChildB 中的文案进行修改。</p>\\n<p>由于初次渲染时，两个组件的 render 函数都必然会被触发，因此控制台在挂载完成后的输出内容如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210428213053.png\\" alt=\\"\\"></p>\\n<p>接下来我点击左侧的按钮，尝试对 A 处的文本进行修改。我们可以看到界面上只有 A 处的渲染效果发生了改变，如下图箭头处所示：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210428213110.png\\" alt=\\"\\"></p>\\n<p>但是如果我们打开控制台，会发现输出的内容如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210428213135.png\\" alt=\\"\\"></p>\\n<p>这样的输出结果告诉我们，在刚刚的点击动作后，不仅 ChildA 的 re-render 被触发了，ChildB 的 re-render 也被触发了。</p>\\n<blockquote>\\n<p>在 React 中，<code>只要父组件发生了更新，那么所有的子组件都会被无条件更新</code>。这就导致了 ChildB 的 props 尽管没有发生任何变化，它本身也没有任何需要被更新的点，却还是会走一遍更新流程。</p>\\n</blockquote>\\n<p><strong>注：</strong> <code>同样的情况也适用于组件自身的更新：当组件自身调用了 setState 后，那么不管 setState 前后的状态内容是否真正发生了变化，它都会去走一遍更新流程</code>。</p>\\n<p>而在刚刚这个更新流程中，<code>shouldComponentUpdate</code> 函数没有被手动定义，因此它将返回“true”这个默认值。“true”则意味着对更新流程不作任何制止，也即所谓的“无条件 re-render”。在这种情况下，我们就可以考虑使用 <code>shouldComponentUpdate</code> 来对更新过程进行管控，避免没有意义的 re-render 发生。</p>\\n<p>现在我们就可以为 ChildB 加装这样一段 <code>shouldComponentUpdate</code> 逻辑：</p>\\n<pre class=\\"language-js\\"><code>shouldComponentUpdate(nextProps, nextState) {\\n  // 判断 text 属性在父组件更新前后有没有发生变化，若没有发生变化，则返回 false\\n  if(nextProps.text === this.props.text) {\\n    return false\\n  }\\n  // 只有在 text 属性值确实发生变化时，才允许更新进行下去\\n  return true\\n}\\n</code></pre>\\n<p>在这段逻辑中，我们对 ChildB 中的可变数据，也就是 this.props.text 这个属性进行了判断。</p>\\n<p>这样，当父组件 App 组件发生更新、进而试图触发 ChildB 的更新流程时，shouldComponentUpdate 就会充当一个“守门员”的角色：它会检查新下发的 <code>props.text</code> 是否和之前的值一致，如果一致，那么就没有更新的必要，直接返回“false”将整个 ChildB 的更新生命周期中断掉即可。只有当 props.text 确实发生变化时，它才会“准许” re-render 的发生。</p>\\n<p>在 <code>shouldComponentUpdate</code> 的加持下，当我们再次点击左侧按钮，试图修改 ChildA 的渲染内容时，控制台的输出就会变成下图这样：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210428213705.png\\" alt=\\"\\"></p>\\n<p>我们看到，控制台中现在只有 ChildA 的 re-render 提示。ChildB “稳如泰山”，成功躲开了一次多余的渲染。</p>\\n<p>使用 <code>shouldComponentUpdate</code> 来调停不必要的更新，避免无意义的 re-render 发生，这是 React 组件中最基本的性能优化手段，也是最重要的手段。许多看似高级的玩法，都是基于 <code>shouldComponentUpdate</code> 衍生出来的。我们接下来要讲的 <code>PureComponent</code>，就是这类玩法中的典型。</p>\\n<h2 id=\\"进阶玩法-purecomponent-immutable-js\\"><a class=\\"header-anchor\\" href=\\"#进阶玩法-purecomponent-immutable-js\\">#</a> 进阶玩法：PureComponent  + Immutable.js</h2>\\n<p><strong>PureComponent：提前帮你安排好更新判定逻辑</strong></p>\\n<blockquote>\\n<p><code>shouldComponentUpdate</code> 虽然一定程度上帮我们解决了性能方面的问题，但每次避免 re-render，都要手动实现一次 <code>shouldComponentUpdate</code>，未免太累了。作为一个不喜欢重复劳动的前端开发者来说，在写了不计其数个 <code>shouldComponentUpdate</code> 逻辑之后，难免会怀疑人生</p>\\n</blockquote>\\n<p><code>React 15.3</code> 很明显听到了开发者的声音，它新增了一个叫 <code>PureComponent</code> 的类，恰到好处地解决了“程序员写 <code>shouldComponentUpdate</code> 写出腱鞘炎”这个问题。</p>\\n<blockquote>\\n<p><code>PureComponent 与 Component 的区别点</code>，就在于它内置了对 <code>shouldComponentUpdate</code> 的实现：<code>PureComponent</code> 将会在 <code>shouldComponentUpdate</code> 中对组件更新前后的 <code>props 和 state 进行浅比较</code>，并根据浅比较的结果，决定是否需要继续更新流程。</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>import React from &quot;react&quot;;\\nexport default class ChildB extends React.PureComponent {\\n  render() {\\n    console.log(&quot;ChildB 的render方法执行了&quot;);\\n    return (\\n      &lt;div className=&quot;childB&quot;&gt;\\n        子组件B的内容：\\n        {this.props.text}\\n      &lt;/div&gt;\\n    );\\n  }\\n}\\n</code></pre>\\n<p>此时再去修改 ChildA 中的文本，我们会发现 ChildB 同样不受影响</p>\\n<blockquote>\\n<p>在值类型数据这种场景下，<code>PureComponent</code> 可以说是战无不胜。但是如果数据类型为引用类型，那么这种基于浅比较的判断逻辑就会带来这样两个风险：</p>\\n</blockquote>\\n<ul>\\n<li>若数据内容没变，但是引用变了，那么浅比较仍然会认为“数据发生了变化”，进而触发一次不必要的更新，导致过度渲染；</li>\\n<li>若数据内容变了，但是引用没变，那么浅比较则会认为“数据没有发生变化”，进而阻断一次更新，导致不渲染。</li>\\n</ul>\\n<p>怎么办呢？<code>Immutable.js</code> 来帮忙！</p>\\n<p><strong>Immutable：“不可变值”让“变化”无处遁形</strong></p>\\n<blockquote>\\n<p><code>PureComponent 浅比较带来的问题，本质上是对“变化”的判断不够精准导致的</code>。那有没有一种办法，能够让引用的变化和内容的变化之间，建立一种必然的联系呢？</p>\\n</blockquote>\\n<p>这就是 <code>Immutable.js</code> 所做的事情。</p>\\n<p>Immutable 直译过来是“不可变的”，顾名思义，Immutable.js 是对“不可变值”这一思想的贯彻实践。它在 2014 年被 Facebook 团队推出，Facebook 给它的定位是“实现持久性数据结构的库”。所谓“持久性数据”，指的是这个数据只要被创建出来了，就不能被更改。我们对当前数据的任何修改动作，都会导致一个新的对象的返回。这就将数据内容的变化和数据的引用严格地关联了起来，使得“变化”无处遁形。</p>\\n<p>这里我用一个简单的例子，来演示一下 Immutable.js 的效果。请看下面代码：</p>\\n<pre class=\\"language-js\\"><code>// 引入 immutable 库里的 Map 对象，它用于创建对象\\nimport { Map } from &#39;immutable&#39;\\n// 初始化一个对象 baseMap\\nconst baseMap = Map({\\n  name: &#39;po&#39;,\\n  career: &#39;fe&#39;,\\n  age: 99\\n})\\n// 使用 immutable 暴露的 Api 来修改 baseMap 的内容\\nconst changedMap = baseMap.set({\\n  age: 100\\n})\\n// 我们会发现修改 baseMap 后将会返回一个新的对象，这个对象的引用和 baseMap 是不同的\\nconsole.log(&#39;baseMap === changedMap&#39;, baseMap === changedMap)\\n</code></pre>\\n<p>由此可见，<code>PureComonent 和 Immutable.js</code> 真是一对好基友！在实际的开发中，我们也确实经常左手 PureComonent，右手 Immutable.js，研发质量大大地提升呀！</p>\\n<blockquote>\\n<p>值得注意的是，由于 <code>Immutable.js</code> 存在一定的学习成本，并不是所有场景下都可以作为最优解被团队采纳。因此，一些团队也会基于 <code>PureComonent</code> 和 <code>Immutable.js</code> 去打造将两者结合的公共类，通过改写 setState 来提升研发体验，这也是不错的思路。</p>\\n</blockquote>\\n<h2 id=\\"函数组件的性能优化-react-memo-和-usememo\\"><a class=\\"header-anchor\\" href=\\"#函数组件的性能优化-react-memo-和-usememo\\">#</a> 函数组件的性能优化：React.memo 和 useMemo</h2>\\n<blockquote>\\n<p>以上咱们讨论的都是类组件的优化思路。那么在函数组件中，有没有什么通用的手段可以阻止“过度 re-render”的发生呢？接下来我们就一起认识一下“函数版”的 <code>shouldComponentUpdate/Purecomponent</code> —— <code>React.memo</code>。</p>\\n</blockquote>\\n<p><strong>React.memo：“函数版”shouldComponentUpdate/PureComponent</strong></p>\\n<blockquote>\\n<p><code>React.memo</code> 是 React 导出的一个顶层函数，它本质上是一个高阶组件，负责对函数组件进行包装。基本的调用姿势如下面代码所示：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>import React from &quot;react&quot;;\\n// 定义一个函数组件\\nfunction FunctionDemo(props) {\\n  return xxx\\n}\\n// areEqual 函数是 memo 的第二个入参，我们之前放在 shouldComponentUpdate 里面的逻辑就可以转移至此处\\nfunction areEqual(prevProps, nextProps) {\\n  /*\\n  return true if passing nextProps to render would return\\n  the same result as passing prevProps to render,\\n  otherwise return false\\n  */\\n}\\n// 使用 React.memo 来包装函数组件\\nexport default React.memo(FunctionDemo, areEqual);\\n</code></pre>\\n<blockquote>\\n<p><code>React.memo</code>会帮我们“记住”函数组件的渲染结果，在组件前后两次 <code>props</code> 对比结果一致的情况下，它会直接复用最近一次渲染的结果。如果我们的组件在相同的 props 下会渲染相同的结果，那么使用 <code>React.memo</code> 来包装它将是个不错的选择。</p>\\n</blockquote>\\n<p>从示例中我们可以看出，<code>React.memo</code> 接收两个参数，<code>第一个参数是我们需要渲染的目标组件</code>，<code>第二个参数 areEqual 则用来承接 props 的对比逻辑</code>。之前我们在 <code>shouldComponentUpdate</code> 里面做的事情，现在就可以放在 areEqual 里来做。</p>\\n<p>比如开篇 Demo 中的 ChildB 组件，就完全可以用 <code>Function Component + React.memo</code> 来改造。改造后的 ChildB 代码如下：</p>\\n<pre class=\\"language-js\\"><code>import React from &quot;react&quot;;\\n\\n// 将 ChildB 改写为 function 组件\\nfunction ChildB(props) {\\n  console.log(&quot;ChildB 的render 逻辑执行了&quot;);\\n  return (\\n    &lt;div className=&quot;childB&quot;&gt;\\n      子组件B的内容：\\n      {props.text}\\n    &lt;/div&gt;\\n  );\\n}\\n\\n// areEqual 用于对比 props 的变化\\nfunction areEqual(prevProps, nextProps) {\\n  if(prevProps.text === nextProps.text) {\\n    return true\\n  }\\n  return false\\n}\\n\\n// 使用 React.memo 来包装 ChildB\\nexport default React.memo(ChildB, areEqual);\\n</code></pre>\\n<blockquote>\\n<p>改造后的组件在效果上就等价于 <code>shouldComponentUpdate</code> 加持后的类组件 <code>ChildB</code></p>\\n</blockquote>\\n<p>这里的 <code>areEqual 函数是一个可选参数</code>，当我们不传入 <code>areEqual</code> 时，<code>React.memo</code> 也可以工作，此时它的作用就类似于 <code>PureComponent——React.memo</code> 会自动为你的组件执行 <code>props 的浅比较逻辑</code>。</p>\\n<blockquote>\\n<p>和 <code>shouldComponentUpdate</code> 不同的是，<code>React.memo</code> 只负责对比 <code>props</code>，而不会去感知组件内部状态（<code>state</code>）的变化</p>\\n</blockquote>\\n<p><strong>useMemo：更加“精细”的 memo</strong></p>\\n<blockquote>\\n<p>通过上面的分析我们知道，<code>React.memo</code> 可以实现类似于 <code>shouldComponentUpdate</code> 或者 <code>PureComponent</code> 的效果，对组件级别的 re-render 进行管控。但是有时候，我们希望复用的并不是整个组件，而是组件中的某一个或几个部分。这种更加“精细化”的管控，就需要 <code>useMemo</code> 来帮忙了。</p>\\n</blockquote>\\n<p><strong>简而言之</strong>，<code>React.memo 控制是否需要重渲染一个组件</code>，而 <code>useMemo 控制的则是是否需要重复执行某一段逻辑</code></p>\\n<p><code>useMemo</code> 的使用方式如下面代码所示：</p>\\n<pre class=\\"language-js\\"><code>const memoizedValue = useMemo(() =&gt; computeExpensiveValue(a, b), [a, b]);\\n</code></pre>\\n<blockquote>\\n<p>我们可以把目标逻辑作为第一个参数传入，把逻辑的依赖项数组作为第二个参数传入。这样只有当依赖项数组中的某个依赖发生变化时，<code>useMemo</code> 才会重新执行第一个入参中的目标逻辑</p>\\n</blockquote>\\n<p>这里我仍然以开篇的示例为例，现在我尝试向 ChildB 中传入两个属性：text 和 count，它们分别是一段文本和一个数字。当我点击右边的按钮时，只有 count 数字会发生变化。改造后的 App 组件代码如下：</p>\\n<pre class=\\"language-js\\"><code>class App extends React.Component {\\n  state = {\\n    textA: &#39;我是A的文本&#39;,\\n    stateB: {\\n      text: &#39;我是B的文本&#39;,\\n      count: 10\\n    }\\n  }\\n  changeA = () =&gt; {\\n    this.setState({\\n      textA: &#39;A的文本被修改了&#39;\\n    })\\n  }\\n  changeB = () =&gt; {\\n    this.setState({\\n      stateB: {\\n        ...this.state.stateB,\\n        count: 100\\n      }\\n    })\\n  }\\n  render() {\\n    return (\\n    &lt;div className=&quot;App&quot;&gt;\\n      &lt;div className=&quot;container&quot;&gt;\\n        &lt;button onClick={this.changeA}&gt;点击修改A处的文本&lt;/button&gt;\\n        &lt;button onClick={this.changeB}&gt;点击修改B处的文本&lt;/button&gt;\\n        &lt;ul&gt;\\n          &lt;li&gt;\\n            &lt;ChildA text={this.state.textA}/&gt;\\n          &lt;/li&gt;\\n        &lt;li&gt;\\n          &lt;ChildB {...this.state.stateB}/&gt;\\n        &lt;/li&gt;\\n        &lt;/ul&gt;\\n      &lt;/div&gt;\\n    &lt;/div&gt;\\n  );\\n  }\\n}\\nexport default App;\\n</code></pre>\\n<p>在 ChildB 中，使用 <code>useMemo</code> 来加持 <code>text 和 count</code> 各自的渲染逻辑。改造后的 ChildB 代码如下所示：</p>\\n<pre class=\\"language-js\\"><code>import React,{ useMemo } from &quot;react&quot;;\\nexport default function ChildB({text, count}) {\\n  console.log(&quot;ChildB 的render 逻辑执行了&quot;);\\n  // text 文本的渲染逻辑\\n  const renderText = (text)=&gt; {\\n    console.log(&#39;renderText 执行了&#39;)\\n    return &lt;p&gt;\\n    子组件B的文本内容：\\n      {text}\\n  &lt;/p&gt;\\n  }\\n  // count 数字的渲染逻辑\\n  const renderCount = (count) =&gt; {\\n    console.log(&#39;renderCount 执行了&#39;)\\n    return &lt;p&gt;\\n      子组件B的数字内容：\\n        {count}\\n    &lt;/p&gt;\\n  }\\n  \\n  // 使用 useMemo 加持两段渲染逻辑\\n  const textContent = useMemo(()=&gt;renderText(text),[text])\\n  const countContent = useMemo(()=&gt;renderCount(count),[count])\\n  return (\\n    &lt;div className=&quot;childB&quot;&gt;\\n      {textContent}\\n      {countContent}\\n    &lt;/div&gt;\\n  );\\n}\\n</code></pre>\\n<p>渲染 App 组件，我们可以看到初次渲染时，renderText 和 renderCount 都执行了，控制台输出如下图所示：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210428214737.png\\" alt=\\"\\"></p>\\n<p>点击右边按钮，对 count 进行修改，修改后的界面会发生如下的变化：</p>\\n<p><img src=\\"https://s.poetries.top/images/20210428214749.png\\" alt=\\"\\"></p>\\n<p>可以看出，由于 <code>count</code> 发生了变化，因此 <code>useMemo</code> 针对 <code>renderCount</code> 的逻辑进行了重计算。而 <code>text</code> 没有发生变化，因此 <code>renderText</code> 的逻辑压根没有执行。</p>\\n<p>使用 <code>useMemo</code>，我们可以对函数组件的执行逻辑进行更加细粒度的管控（尤其是定向规避掉一些高开销的计算），同时也弥补了 <code>React.memo</code> 无法感知函数内部状态的遗憾，这对我们整体的性能提升是大有裨益的。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品卡片用了 <code>React.memo</code>，父组件每次都传入内容相同但新建的配置对象，开发者断言子组件绝不会重渲染，你怎么判断？</strong></p>\\n<p>不能这样断言；未提供 <code>areEqual</code> 时，<code>React.memo</code> 对 <code>props</code> 做浅比较，新建对象的引用变化可能让比较不一致。若组件确实在相同有效输入下输出相同结果，可稳定引用或谨慎定制比较，但比较成本和漏掉有效变化都要评估。</p>\\n</li>\\n<li>\\n<p><strong>报表页的函数组件包含一段高开销汇总计算，同时还有频繁变化的筛选提示，你会选 <code>React.memo</code> 还是 <code>useMemo</code>？</strong></p>\\n<p>若只需避免汇总逻辑随无关状态重复执行，应使用 <code>useMemo</code> 缓存该计算，并把真实输入放进依赖数组。<code>React.memo</code> 控制的是整个组件是否因 <code>props</code> 变化而重渲染，粒度不同；缓存本身也有成本，不应包裹廉价逻辑。</p>\\n</li>\\n<li>\\n<p><strong>列表从几十项扩大到大量重复卡片后出现卡顿，组员提议给每个组件都套 <code>React.memo</code>，你会要求先验证什么？</strong></p>\\n<p>先确认重复开销是否来自相同 <code>props</code> 下的子组件重渲染，以及传入对象或函数引用是否稳定。只有跳过渲染的收益大于比较和缓存成本时才值得包装；若多数卡片的 <code>props</code> 每次都变化，<code>React.memo</code> 很可能无法解决主要开销。</p>\\n</li>\\n<li>\\n<p><strong>线上详情页修改组件内部 <code>state</code> 后仍发生渲染，开发者认为 <code>React.memo</code> 失效了，你如何排查？</strong></p>\\n<p>这通常不是失效，因为 <code>React.memo</code> 只比较组件前后两次 <code>props</code>，不会屏蔽组件内部 <code>state</code> 变化带来的更新。应先区分更新来源，再检查 <code>props</code> 比较结果；若目标是避免某段昂贵逻辑重算，可评估用 <code>useMemo</code> 按依赖缓存。</p>\\n</li>\\n<li>\\n<p><strong>搜索结果页既可用自定义 <code>areEqual</code> 深度比较整个结果对象，也可让上游稳定必要字段引用，你倾向哪种方案？</strong></p>\\n<p>更倾向先缩小 <code>props</code> 并稳定必要引用，让默认浅比较有机会生效，判断规则也更直观。深度比较可能在每次父组件更新时产生额外开销，还可能因比较遗漏复用错误结果；只有数据结构明确且收益经过验证时才考虑定制。</p>\\n</li>\\n<li>\\n<p><strong>代码评审中有人把 <code>useMemo(() =&gt; &lt;ResultList data={data} /&gt;, [data])</code> 与 <code>React.memo(ResultList)</code> 说成完全等价，你怎么回应？</strong></p>\\n<p>两者都可能减少重复工作，但控制边界并不相同：前者按依赖复用一段逻辑或值，后者按 <code>props</code> 比较决定是否复用整个函数组件最近的渲染结果。依赖或 <code>props</code> 设计不准确时都会失效，不能脱离更新来源直接互换。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);