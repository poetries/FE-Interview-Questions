(window.webpackJsonp=window.webpackJsonp||[]).push([[31],{1137:function(n){n.exports=JSON.parse('{"html":"<h2 id=\\"认识-分治-思想\\"><a class=\\"header-anchor\\" href=\\"#认识-分治-思想\\">#</a> 认识“分治”思想</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>分治把原问题拆成规模更小、结构相同的子问题，分别求解后再合并结果</li>\\n<li>递归函数必须有可直接求解的基线，并保证每次子问题规模严格缩小</li>\\n<li>切分点和区间开闭要全程统一，合并步骤必须覆盖所有子结果</li>\\n</ul>\\n<p><strong>分治的本质是把一个大问题拆成若干个规模更小、结构相同的子问题，分别求解后再合并成原问题的答案。</strong> 使用递归实现时必须设置可直接求解的边界，并保证每次拆分后问题规模都会缩小，否则无法正常结束。落到排序问题里，我一般会重点检查切分区间是否统一，以及合并过程是否完整覆盖每个子问题的结果。</p>\\n</section>\\n<p>本节我们要学习的两个排序算法都是对“分治”思想的应用。\\n“分治”，分而治之。其思想就是将一个大问题分解为若干个子问题，针对子问题分别求解后，再将子问题的解整合为大问题的解。</p>\\n<blockquote>\\n<p>利用分治思想解决问题，我们一般分三步走：</p>\\n</blockquote>\\n<ul>\\n<li>分解子问题</li>\\n<li>求解每个子问题</li>\\n<li>合并子问题的解，得出大问题的解</li>\\n</ul>\\n<p>下面我们一起来看看分治思想是如何帮助我们提升排序算法效率的。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>评审一个排序页面时，有人把数组切成两半并分别排好序，却直接拼接左右结果；为什么这还不能算完整的分治？</strong></p>\\n<p>左右子数组各自有序并不能保证拼接后的整体有序，例如左侧最大值可能大于右侧最小值。分治必须包含分解、求解和整合三个环节，整合规则还要能把子问题的解可靠地转化为原问题的解；缺少合并就只完成了局部求解。</p>\\n</li>\\n<li>\\n<p><strong>你要在代码库里实现一种分治排序，递归函数的输入、返回值和终止条件会怎样设计，才能让团队容易验证？</strong></p>\\n<p>递归函数应接收边界明确的待排序区间或数组，并返回该范围的有序结果；规模缩小到单个元素时直接返回。调用方只依赖“返回结果有序”这一约定，合并逻辑集中处理子结果；若区间开闭混用，可能出现漏项或递归不收敛。</p>\\n</li>\\n<li>\\n<p><strong>如果数据规模增大后递归层数成为运行环境的约束，分治思想是否必须放弃？</strong></p>\\n<p>不必放弃分治，受限的是递归实现形式，而不是分解、求解、合并的结构。可以改成自底向上的迭代流程，从小规模子问题开始逐层合并；代价是控制逻辑更显式，也需要谨慎处理末尾不足一个完整分组的区间。</p>\\n</li>\\n<li>\\n<p><strong>线上排序接口偶发返回缺少元素的数组，日志显示每个叶子子问题都正常，你会优先检查分治流程的哪一段？</strong></p>\\n<p>应优先检查合并阶段，因为叶子结果正确只说明分解和最小子问题求解没有明显异常。核对每个子结果是否都被消费、某一侧耗尽后剩余部分是否保留，并对比分解前后的元素数量；若切分本身存在重叠或空洞，还需回查区间边界。</p>\\n</li>\\n<li>\\n<p><strong>同事主张把问题尽可能多切几份，另一位坚持每次只切两份；你会依据什么决定，而不是把“分得更多”当成更高效？</strong></p>\\n<p>切分数量应服从子问题是否易解以及结果是否易于合并，而不是越多越好。二分通常让边界和合并过程更直观，多分可能减少层数却增加一次整合的参与方；最终还要结合执行环境、数据访问方式和合并成本判断。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"归并排序\\"><a class=\\"header-anchor\\" href=\\"#归并排序\\">#</a> 归并排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>分到单元素，再用双指针合并两个有序段。</li>\\n<li>每层处理 <code>n</code> 个元素，共 <code>log n</code> 层：时间稳定为 <code>O(n log n)</code>。</li>\\n<li>数组版本通常需 <code>O(n)</code> 辅助空间；相等时先取左段可保持稳定。</li>\\n<li>适合链表、外部排序和需要稳定性的场景。</li>\\n</ul>\\n<p><strong>归并排序会先把数组不断拆到单个元素，再通过双指针把两个有序区间线性合并，真正的排序发生在合并阶段。</strong> 每一层都会处理全部 <code>n</code> 个元素，一共有 <code>log n</code> 层，所以时间复杂度稳定为 <code>O(n log n)</code>，数组实现通常还需要 <code>O(n)</code> 辅助空间。合并时若元素相等就先取左侧，可以保持稳定性，因此它适合链表、外部排序或明确要求稳定排序的场景。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“归并的核心是两个有序段线性合并。递归只负责划分，真正的排序发生在合并阶段。”</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审中有人说“递归调用完成时数组自然就有序了”，面对页面里的归并排序实现，你会怎样指出概念错误？</strong></p>\\n<p>递归调用只负责把数组不断划分并取得左右两个有序段，真正建立整体顺序的是合并阶段。若将两个递归结果直接连接，跨边界元素仍可能逆序；因此归并排序的核心约束是每次输入两个有序段，并用线性过程产出一个更大的有序段。</p>\\n</li>\\n<li>\\n<p><strong>前端页面需要按时间展示一批记录，你会怎样把“两个有序段线性合并”落实成可检查的代码结构？</strong></p>\\n<p>为左右有序段各维护一个指针，每轮比较当前元素并把较小者写入结果，随后移动对应指针。任一侧耗尽后追加另一侧剩余元素，代码评审重点检查指针推进和尾部处理；若比较规则不一致，最终顺序或稳定性会偏离预期。</p>\\n</li>\\n<li>\\n<p><strong>如果排序对象从普通数组换成通过 <code>next</code> 串联的链表，归并阶段的实现重点会发生什么变化？</strong></p>\\n<p>链表仍可沿用两个有序段逐项比较的合并逻辑，但结果可通过调整 <code>next</code> 指针连接，不必依赖数组随机访问。寻找切分点和维护链路完整性会成为重点；若断链位置或尾指针处理错误，可能丢失节点或形成环。</p>\\n</li>\\n<li>\\n<p><strong>一个超出内存容量的文件需要排序，团队仍想沿用归并思路，你会怎样拆分执行流程？</strong></p>\\n<p>可以把文件划分为能装入内存的数据块，分别排序并写回，再对这些有序块进行顺序归并。该方案利用归并只需读取各有序段当前元素的特点，但需要管理磁盘读写和多个输入流；块大小及归并路数应受实际资源约束。</p>\\n</li>\\n<li>\\n<p><strong>评审会上有人选择归并排序，另一人担心辅助存储；你如何说明这项选型冲突的核心？</strong></p>\\n<p>归并排序的优势来自稳定地划分，并在线性合并阶段完成排序，时间表现不依赖输入是否已经有序。数组实现通常需要承接合并结果的辅助空间，这是换取清晰线性合并的代价；若内存约束高，应评估数据结构或其他排序方案，而不能忽略空间成本。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"思路分析\\"><a class=\\"header-anchor\\" href=\\"#思路分析\\">#</a> 思路分析</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>把区间递归二分到单元素，再用双指针线性合并两个已有序子数组</li>\\n<li>合并过程中结果前缀始终有序，且包含两个输入中已经消费的全部最小元素</li>\\n<li>任一侧耗尽后必须追加另一侧剩余元素；切分区间要确保规模严格缩小</li>\\n</ul>\\n<p><strong>归并排序的核心是先把数组递归拆成单元素，再把相邻的有序子数组逐层合并。</strong> 单元素天然有序，所以问题会从排序整个数组，转化为反复合并两个有序数组。合并时用双指针取两侧较小的元素，保证结果中已经写入的部分始终有序。切分必须让区间持续缩小，某一侧耗尽后还要把另一侧的剩余元素全部追加进去。</p>\\n</section>\\n<blockquote>\\n<p>归并排序是对分治思想的典型应用，它按照如下的思路对分治思想“三步走”的框架进行了填充：</p>\\n</blockquote>\\n<ul>\\n<li>分解子问题：将需要被排序的数组从中间分割为两半，然后再将分割出来的每个子数组各分割为两半，重复以上操作，直到单个子数组只有一个元素为止。</li>\\n<li>求解每个子问题：从粒度最小的子数组开始，两两合并、确保每次合并出来的数组都是有序的。（这里的“子问题”指的就是对每个子数组进行排序）。\\n合并子问题的解，得出大问题的解：当数组被合并至原有的规模时，就得到了一个完全排序的数组</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>页面把数组递归切到单元素；如果有人改成切到空数组才停止，代码会出现什么现象，为什么不符合这里的分治框架？</strong></p>\\n<p>若非空数组的切分不能保证两侧都严格缩小，递归可能反复处理同一范围，最终导致调用栈耗尽。单元素本身已经有序，是可直接求解的子问题；终止条件还应覆盖空数组，避免边界输入进入没有意义的继续切分。</p>\\n</li>\\n<li>\\n<p><strong>实现归并排序时，左右递归结果已经各自有序，你会怎样写合并循环并证明它没有遗漏元素？</strong></p>\\n<p>合并时为左右数组分别维护指针，每轮取两个当前元素中较小者加入结果，并只推进被取值的一侧。循环结束说明至少一侧已耗尽，此时必须把另一侧尚未消费的后缀完整追加；同时核对输出长度等于两侧长度之和，才能及时发现漏项。</p>\\n</li>\\n<li>\\n<p><strong>如果需求要求排序结果保留相等记录的原始先后顺序，合并阶段的比较条件应该怎样处理？</strong></p>\\n<p>相等时应优先选择左侧有序段中的元素，因为左侧元素在原数组中整体更靠前，这样才能保持相对顺序。若代码使用严格小于并在相等时取右侧，排序值仍正确，但稳定性会被破坏；前提是此前各层合并也遵守同一规则。</p>\\n</li>\\n<li>\\n<p><strong>线上出现“数组长度正常但局部逆序”，单元素和双元素用例都通过，你会怎样定位是切分还是合并的问题？</strong></p>\\n<p>记录每层左右输入及合并输出，先确认两个输入段是否各自有序，再检查输出中跨段边界的元素顺序。若输入无序，故障来自更深层递归；若输入有序而输出逆序，则重点核对比较方向、指针推进以及剩余后缀的追加逻辑。</p>\\n</li>\\n<li>\\n<p><strong>同事提出省掉合并步骤，先递归排好左右两半，再交换两半的位置；在什么条件下这个方案才可能正确？</strong></p>\\n<p>只有能够证明一侧所有元素都不大于另一侧所有元素时，直接拼接或整体交换才可能得到有序结果。一般输入并不存在这样的跨段关系，因此仍需逐项合并；额外扫描边界只能识别少数可直接连接的情况，不能替代通用合并。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"真实排序过程演示\\"><a class=\\"header-anchor\\" href=\\"#真实排序过程演示\\">#</a> 真实排序过程演示</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>把区间递归二分到单元素，再用双指针线性合并两个已有序子数组</li>\\n<li>合并过程中结果前缀始终有序，且包含两个输入中已经消费的全部最小元素</li>\\n<li>任一侧耗尽后必须追加另一侧剩余元素；切分区间要确保规模严格缩小</li>\\n</ul>\\n<p><strong>这个数组会先从规模 <code>8</code> 逐层拆到规模 <code>1</code>，再按 <code>1→2→4→8</code> 的顺序合并成有序数组。</strong> 例如 <code>[8,7]</code> 合并为 <code>[7,8]</code>，相邻结果继续合并，最终得到 <code>[1,2,3,4,5,6,7,8]</code>。代码里的递归负责完成有去有回的分割过程，双指针负责合并两个已有序数组。合并时若一侧已经遍历完，另一侧剩余部分本身有序，可以直接拼到结果末尾。</p>\\n</section>\\n<p>下面我们基于归并排序的思路，尝试对以下数组进行排序：</p>\\n<pre class=\\"language-js\\"><code>[8, 7, 6, 5, 4, 3, 2, 1]\\n</code></pre>\\n<ul>\\n<li>首先重复地分割数组，整个分割过程如下：</li>\\n<li>首次分割，将数组整个对半分：</li>\\n</ul>\\n<pre class=\\"language-text\\"><code>[8, 7, 6, 5,| 4, 3, 2, 1]\\n</code></pre>\\n<blockquote>\\n<p>二次分割，将分割出的左右两个子数组各自对半分：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[8, 7,| 6, 5,| 4, 3,| 2, 1]\\n</code></pre>\\n<blockquote>\\n<p>三次分割，四个子数组各自对半分后，每个子数组内都只有一个元素了：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[8,| 7,| 6,| 5,| 4,| 3,| 2,| 1]\\n</code></pre>\\n<blockquote>\\n<p>接下来开始尝试解决每个子问题。将规模为1的子数组两两合并为规模为2的子数组，合并时确保有序，我们会得到这样的结果：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[7, 8,| 5, 6,| 3, 4,| 1, 2]\\n</code></pre>\\n<blockquote>\\n<p>继续将规模为2的按照有序原则合并为规模为4的子数组：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[5, 6, 7, 8,| 1, 2, 3, 4]  \\n</code></pre>\\n<blockquote>\\n<p>最后将规模为4的子数组合并为规模为8的数组：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[1, 2, 3, 4, 5, 6, 7, 8]  \\n</code></pre>\\n<p>整个数组就完全有序了。</p>\\n<p>编码实现</p>\\n<blockquote>\\n<p>通过上面的讲解，我们可以总结出归并排序中的两个主要动作：</p>\\n</blockquote>\\n<ul>\\n<li>分割</li>\\n<li>合并</li>\\n</ul>\\n<blockquote>\\n<p>这两个动作是紧密关联的，分割是将大数组反复分解为一个一个的原子项，合并是将原子项反复地组装回原有的大数组。整个过程符合两个特征：</p>\\n</blockquote>\\n<ul>\\n<li>重复（令人想到递归或迭代）</li>\\n<li>有去有回（令人想到回溯，进而明确递归这条路）</li>\\n</ul>\\n<p>因此，归并排序在实现上依托的就是递归思想。</p>\\n<blockquote>\\n<p>除此之外，这里还涉及到另一个小小的知识点——两个有序数组的合并。合并有序数组是咱们在第 7 节讲过的一道真题，涉及到双指针法。此处强烈建议印象模糊的同学回头复习一下完整的解题思路。</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>function mergeSort(arr) {\\n    const len = arr.length\\n    // 处理边界情况\\n    if(len &lt;= 1) {\\n        return arr\\n    }   \\n    // 计算分割点\\n    const mid = Math.floor(len / 2)    \\n    // 递归分割左子数组，然后合并为有序数组\\n    const leftArr = mergeSort(arr.slice(0, mid)) \\n    // 递归分割右子数组，然后合并为有序数组\\n    const rightArr = mergeSort(arr.slice(mid,len))  \\n    // 合并左右两个有序数组\\n    arr = mergeArr(leftArr, rightArr)  \\n    // 返回合并后的结果\\n    return arr\\n}\\n  \\nfunction mergeArr(arr1, arr2) {  \\n    // 初始化两个指针，分别指向 arr1 和 arr2\\n    let i = 0, j = 0   \\n    // 初始化结果数组\\n    const res = []    \\n    // 缓存arr1的长度\\n    const len1 = arr1.length  \\n    // 缓存arr2的长度\\n    const len2 = arr2.length  \\n    // 合并两个子数组\\n    while(i &lt; len1 &amp;&amp; j &lt; len2) {\\n        if(arr1[i] &lt; arr2[j]) {\\n            res.push(arr1[i])\\n            i++\\n        } else {\\n            res.push(arr2[j])\\n            j++\\n        }\\n    }\\n    // 若其中一个子数组首先被合并完全，则直接拼接另一个子数组的剩余部分\\n    if(i&lt;len1) {\\n        return res.concat(arr1.slice(i))\\n    } else {\\n        return res.concat(arr2.slice(j))\\n    }\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>页面演示把 <code>[8,7,6,5,4,3,2,1]</code> 切成单元素后再合并；如果直接把相邻单元素原样拼回去，最终会是什么现象？</strong></p>\\n<p>原样拼回只会恢复输入的逆序结构，不会产生页面展示的有序结果。每一级都必须把两个已经有序的子数组按大小重新合并，例如先得到 <code>[7,8]</code> 和 <code>[5,6]</code>；递归提供处理顺序，比较与合并才真正改变元素排列。</p>\\n</li>\\n<li>\\n<p><strong>把页面中的 <code>mergeSort</code> 放进业务代码时，为什么 <code>len &lt;= 1</code> 和 <code>Math.floor(len / 2)</code> 必须一起检查？</strong></p>\\n<p><code>len &lt;= 1</code> 保证最小规模直接返回，<code>Math.floor(len / 2)</code> 则让更大的数组被分成两个规模更小的部分。二者共同保证递归能够收敛，并覆盖奇数长度输入；若切分点或 <code>slice</code> 边界写错，可能出现遗漏、重复或无限递归。</p>\\n</li>\\n<li>\\n<p><strong>如果输入包含大量重复值，页面中 <code>arr1[i] &lt; arr2[j]</code> 的写法会带来什么额外行为？</strong></p>\\n<p>当两个值相等时，该条件会先取右侧元素，所以数值排序仍可能正确，但相等元素的原始相对次序可能改变。若业务依赖稳定排序，应在相等时优先取左侧元素，例如使用 <code>&lt;=</code>；同时要保证比较器在所有递归层保持一致。</p>\\n</li>\\n<li>\\n<p><strong>线上报错显示 <code>Maximum call stack size exceeded</code>，输入是一个很大的数组，而小数组结果正确，你会先排查哪些位置？</strong></p>\\n<p>先确认递归基线能够命中，以及左右 <code>slice</code> 的范围是否都比原数组小，排除同一数组被反复传入。若收敛逻辑正确，故障才可能来自运行环境对递归栈的限制；此时可改用自底向上的迭代归并，但需重写分组控制。</p>\\n</li>\\n<li>\\n<p><strong>评审者想用原地交换替代 <code>mergeArr</code> 的结果数组，以降低分配；你会怎样评估这个改动？</strong></p>\\n<p>当前实现依靠独立结果数组，让两个输入段在读取期间不被覆盖，逻辑清晰且便于验证。原地合并需要处理元素搬移和尚未读取数据被覆盖的问题，实现复杂度明显上升；除非空间约束确实关键，否则不应只凭“少建数组”判断更优。</p>\\n</li>\\n<li>\\n<p><strong>为什么这里既出现递归又出现双指针，它们分别解决排序流程中的哪一层职责？</strong></p>\\n<p>递归负责反复分割数组，并在子问题完成后按层返回，形成从单元素到原规模的处理顺序。双指针负责把每一对有序子数组线性合并；两者缺一不可，前者不负责元素比较，后者也不负责建立完整的分治层级。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"编码复盘-归并排序的时间复杂度分析\\"><a class=\\"header-anchor\\" href=\\"#编码复盘-归并排序的时间复杂度分析\\">#</a> 编码复盘——归并排序的时间复杂度分析</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>把区间递归二分到单元素，再用双指针线性合并两个已有序子数组</li>\\n<li>合并过程中结果前缀始终有序，且包含两个输入中已经消费的全部最小元素</li>\\n<li>递归层数 <code>log n</code>，每层合并总工作量 <code>n</code>，时间 <code>O(n log n)</code>、常见数组实现空间 <code>O(n)</code></li>\\n</ul>\\n<p><strong>归并排序的时间复杂度是 <code>O(n log n)</code>，常见数组实现的额外空间复杂度是 <code>O(n)</code>。</strong> 数组每次对半拆分，因此递归深度大约是 <code>log n</code>；同一层虽然包含多个子数组，但合并时总共仍会处理 <code>n</code> 个元素。把层数和每层工作量相乘，就得到 <code>O(n log n)</code>。工程上还要注意，合并结果需要额外数组，递归调用也会占用调用栈。</p>\\n</section>\\n<blockquote>\\n<p>归并排序的时间复杂度的分析，同样是基于分治法。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>性能评审中有人只看到递归调用，就断言归并排序的复杂度是 <code>O(log n)</code>；你会用代码执行过程怎样反驳？</strong></p>\\n<p><code>O(log n)</code> 只描述数组反复对半分割形成的层数，没有计入每层的合并工作。每一层合并最终都要处理全部 <code>n</code> 个元素，因此总时间由约 <code>log n</code> 层乘以每层 <code>O(n)</code> 得到；不能只按递归深度估算。</p>\\n</li>\\n<li>\\n<p><strong>分析页面实现时，怎样从 <code>mergeArr</code> 的两个指针判断单次合并的时间上界？</strong></p>\\n<p>两个指针都只向前移动，每个元素最多被选取并写入结果一次，所以合并两个总长度为 <code>m</code> 的有序数组需要 <code>O(m)</code> 时间。末尾的 <code>concat</code> 或 <code>slice</code> 仍会处理剩余元素，不会改变线性量级；具体常数则受实现影响。</p>\\n</li>\\n<li>\\n<p><strong>如果输入已经完全有序或完全逆序，归并排序的时间复杂度会不会像某些排序那样明显改变？</strong></p>\\n<p>按照这里固定对半分割并逐层合并的实现，两类输入都仍会经历相同数量的分解层级，并在各层处理全部元素。比较次数的细节可能不同，但总体仍是 <code>O(n log n)</code>；除非额外加入可跳过合并的检测逻辑。</p>\\n</li>\\n<li>\\n<p><strong>线上监控显示大数组排序耗时异常，但递归层数符合预期，你会怎样判断问题是否出在合并阶段？</strong></p>\\n<p>应按递归层级统计合并输入长度与调用次数，检查同一层处理的元素总量是否显著超过原数组规模。再观察是否存在重复复制、错误切片或结果反复拼接；复杂度结论假设每层合并是线性的，若实现引入额外遍历，实际耗时会偏离分析。</p>\\n</li>\\n<li>\\n<p><strong>有人因归并排序是 <code>O(n log n)</code> 就认定它必然是当前页面列表的最佳选型，你会补充哪些代价？</strong></p>\\n<p>时间复杂度只描述增长趋势，不能单独决定工程选型。页面中的数组实现通过 <code>slice</code> 和结果数组完成分割、合并，会产生额外存储与复制；还需结合数据规模、内存约束、稳定性要求和数据结构评估，不能仅凭渐进时间下结论。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"基于数学计算的分析\\"><a class=\\"header-anchor\\" href=\\"#基于数学计算的分析\\">#</a> 基于数学计算的分析</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>把区间递归二分到单元素，再用双指针线性合并两个已有序子数组</li>\\n<li>合并过程中结果前缀始终有序，且包含两个输入中已经消费的全部最小元素</li>\\n<li>递归层数 <code>log n</code>，每层合并总工作量 <code>n</code>，时间 <code>O(n log n)</code>、常见数组实现空间 <code>O(n)</code></li>\\n</ul>\\n<p><strong>设规模为 <code>n</code> 的排序耗时为 <code>F(n)</code>，递推关系可以写成 <code>F(n)=2F(n/2)+O(n)</code>。</strong> 前半部分来自两个规模为 <code>n/2</code> 的子问题，<code>O(n)</code> 则是合并两个有序子数组时遍历全部元素的成本。持续展开到子数组只剩一个元素，一共需要约 <code>log n</code> 层，而每层合并的总工作量都是 <code>O(n)</code>。因此 <code>F(n)=nF(1)+O(n log n)</code>，最终时间复杂度为 <code>O(n log n)</code>。</p>\\n</section>\\n<blockquote>\\n<p>我们假设规模为 n 的数组对应的排序的时间复杂度是一个关于 n 的函数 F(n)。那么它和自己的两个子数组之间就有如下关系：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>F(n) = F(n/2) + F(n/2) + 合并两个数组的时间\\n</code></pre>\\n<blockquote>\\n<p>合并两个数组的过程一共要对 n 个元素进行一轮循环，因此时间复杂度可以目测出来是 O(n)，代入上面公式：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>F(n) = F(n/2) + F(n/2) + O(n) = 2^1*T(n/2)+2^0*O(n)\\n</code></pre>\\n<blockquote>\\n<p>继续细分，两个子数组被划分为四个子数组，仍然遵循上面公式所描述的关系。代入 n/4 后可以得到四个子数组和大数组之间的关系：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>F(n/2) = 2*F(n/4)+O(n)   \\n\\nF(n) = 2*(2*F(n/4)+O(n))+O(n) = 2^2*F(n/4)+2^1*O(n)\\n</code></pre>\\n<blockquote>\\n<p>这样不断划分下去，直到每个序列里只有一个数位置。对于规模为 n 的数组来说，需要划分的次数为 log(n)，用 log(n) 替换掉上述公式中的2的次数，我们就可以得到归并排序的时间复杂度：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>F(n) = nF(1) + O(nlog(n)) = O(nlog(n))\\n</code></pre>\\n<blockquote>\\n<p>综上所述， 归并排序的时间复杂度是 O(nlog(n))。</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人说递归深度是 <code>log(n)</code>，所以归并排序自然就是 <code>O(nlog(n))</code>；如果合并阶段每层只处理一个元素，这个结论还成立吗？</strong></p>\\n<p>不成立，递归深度只能说明层数，不能单独决定总复杂度。归并排序之所以得到 <code>O(nlog(n))</code>，还依赖同一层所有合并任务合计处理 <code>n</code> 个元素；若每层总工作量改变，必须重新展开递推式。</p>\\n</li>\\n<li>\\n<p><strong>在一个处理百万级数组的页面中，你准备根据代码写出归并排序的递推式，怎样避免把一次局部合并的规模误写成整个数组的 <code>n</code>？</strong></p>\\n<p>应先写成 <code>F(n)=2F(n/2)+O(n)</code>，其中 <code>O(n)</code>表示当前递归节点合并两个总长度为 <code>n</code> 的子数组。再按递归树汇总：每层节点增多，但该层处理的元素总量仍为 <code>n</code>，共有约 <code>log(n)</code> 层。</p>\\n</li>\\n<li>\\n<p><strong>如果服务端拿到的数组长度不是 <code>2</code> 的幂，左右子数组分别是 <code>⌊n/2⌋</code> 和 <code>⌈n/2⌉</code>，原来的复杂度推导是否失效？</strong></p>\\n<p>不会失效，递推式可更准确地写成 <code>F(n)=F(⌊n/2⌋)+F(⌈n/2⌉)+O(n)</code>。两侧规模仍近似减半，递归高度保持在 <code>O(log n)</code>，每层总合并量仍为 <code>O(n)</code>；取整只影响常数和边界层。</p>\\n</li>\\n<li>\\n<p><strong>线上排序任务的耗时随数据量翻倍后明显恶化，代码仍声称满足 <code>F(n)=2F(n/2)+O(n)</code>，你会先核查递推式里的哪一项？</strong></p>\\n<p>先核查合并阶段是否真的只线性扫描一次，以及递归切分是否严格缩小输入。若合并中存在重复查找、头部删除或额外嵌套遍历，附加项就可能不再是 <code>O(n)</code>；若区间未缩小，还会出现无限递归或栈溢出。</p>\\n</li>\\n<li>\\n<p><strong>架构评审中有人用主定理，有人坚持逐层展开 <code>F(n)</code>，为了让团队验证归并排序复杂度，你会选择哪种表达？</strong></p>\\n<p>这里优先使用递推展开或递归树，因为它能直接展示每层总工作量为 <code>O(n)</code>、层数为 <code>O(log n)</code>。主定理能更快得到同一结论，但要求团队熟悉适用条件；若递归划分或合并成本发生变化，仍应回到递推式检查。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"基于逻辑的分析\\"><a class=\\"header-anchor\\" href=\\"#基于逻辑的分析\\">#</a> 基于逻辑的分析</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>把区间递归二分到单元素，再用双指针线性合并两个已有序子数组</li>\\n<li>合并过程中结果前缀始终有序，且包含两个输入中已经消费的全部最小元素</li>\\n<li>递归层数 <code>log n</code>，每层合并总工作量 <code>n</code>，时间 <code>O(n log n)</code>、常见数组实现空间 <code>O(n)</code></li>\\n</ul>\\n<p><strong>归并排序有 <code>log n</code> 层递归，每层合并的总工作量是 <code>n</code>，所以时间复杂度为 <code>O(n log n)</code>。</strong> 简单来说，数组每次减半，直到只剩一个元素，切分轮数就是 <code>log n</code>。同一层虽然有多次合并，但处理的元素总数仍是 <code>n</code>，合起来就是 <code>n × log n</code>。常见数组实现还要创建临时结果数组，因此空间复杂度通常是 <code>O(n)</code>。</p>\\n</section>\\n<p>如果上面的数学公式让你感到不友好，那么我们通过简单的逻辑估算，也可以得出归并排序的时间复杂度：</p>\\n<p>逻辑估算的核心思想是“抓主要矛盾”。我们可以回顾一下归并排序的代码：</p>\\n<pre class=\\"language-js\\"><code>function mergeSort(arr) {\\n    const len = arr.length\\n    // 处理边界情况\\n    if(len &lt;= 1) {\\n        return arr\\n    }   \\n    // 计算分割点\\n    const mid = Math.floor(len / 2)    \\n    // 递归分割左子数组，然后合并为有序数组\\n    const leftArr = mergeSort(arr.slice(0, mid)) \\n    // 递归分割右子数组，然后合并为有序数组\\n    const rightArr = mergeSort(arr.slice(mid,len))  \\n    // 合并左右两个有序数组\\n    arr = mergeArr(leftArr, rightArr)  \\n    // 返回合并后的结果\\n    return arr\\n}\\n  \\nfunction mergeArr(arr1, arr2) {  \\n    // 初始化两个指针，分别指向 arr1 和 arr2\\n    let i = 0, j = 0   \\n    // 初始化结果数组\\n    const res = []    \\n    // 缓存arr1的长度\\n    const len1 = arr1.length  \\n    // 缓存arr2的长度\\n    const len2 = arr2.length  \\n    // 合并两个子数组\\n    while(i &lt; len1 &amp;&amp; j &lt; len2) {\\n        if(arr1[i] &lt; arr2[j]) {\\n            res.push(arr1[i])\\n            i++\\n        } else {\\n            res.push(arr2[j])\\n            j++\\n        }\\n    }\\n    // 若其中一个子数组首先被合并完全，则直接拼接另一个子数组的剩余部分\\n    if(i&lt;len1) {\\n        return res.concat(arr1.slice(i))\\n    } else {\\n        return res.concat(arr2.slice(j))\\n    }\\n}\\n</code></pre>\\n<ul>\\n<li>我们把每一次切分+归并看做是一轮。对于规模为 n 的数组来说，需要切分 log(n) 次，因此就有 log(n) 轮。</li>\\n</ul>\\n<p>每一轮中，切分动作都是小事情，只需要固定的几步：</p>\\n<pre class=\\"language-js\\"><code> // 计算分割点\\nconst mid = Math.floor(len / 2)    \\n// 递归分割左子数组，然后合并为有序数组\\nconst leftArr = mergeSort(arr.slice(0, mid)) \\n// 递归分割右子数组，然后合并为有序数组\\nconst rightArr = mergeSort(arr.slice(mid,len))\\n</code></pre>\\n<p>因此单次切分对应的是常数级别的时间复杂度 O(1)。</p>\\n<blockquote>\\n<p>再看合并，单次合并的时间复杂度为 O(n)。O(n) 和 O(1) 完全不在一个复杂度量级上，因此本着“抓主要矛盾”的原则，我们可以认为：决定归并排序时间复杂度的操作就是合并操作。</p>\\n</blockquote>\\n<blockquote>\\n<p>log(n) 轮对应 log(n) 次合并操作，因此归并排序的时间复杂度就是 O(nlog(n))。</p>\\n</blockquote>\\n<p>以上两种时间复杂度的计算思路，大家理解其中一种即可，不必死磕。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审时同事说“有 <code>log(n)</code> 轮，每轮只有一次 <code>O(n)</code> 合并”，但页面上同一层其实有多个 <code>mergeArr</code> 调用，你如何纠正这句话？</strong></p>\\n<p>应改成每层有多次合并，但这些合并处理的元素总数为 <code>n</code>，所以整层成本是 <code>O(n)</code>。递归约有 <code>log(n)</code> 层，因此总成本为 <code>O(nlog(n))</code>；把一层描述成一次函数调用会掩盖真正的汇总依据。</p>\\n</li>\\n<li>\\n<p><strong>前端页面直接使用示例中的 <code>slice</code> 递归排序大数组，算法分析把切分视为 <code>O(1)</code>，工程评审时你会接受吗？</strong></p>\\n<p>只能把它当作简化的算法模型，不能直接当作 <code>JavaScript</code> 实现成本结论。<code>slice</code>通常会创建新数组并复制元素，还会带来额外内存与回收压力；评估页面卡顿时，应把切片成本和合并成本一起测量，或改用索引区间。</p>\\n</li>\\n<li>\\n<p><strong>如果业务要求排序过程不能频繁创建子数组，但仍希望保留归并排序的 <code>O(nlog(n))</code> 时间界，你会怎样调整这段代码？</strong></p>\\n<p>可以递归传递左右边界，避免每层通过 <code>slice</code> 构造子数组，并复用一个辅助数组完成合并。比较和搬移的总体量级仍可保持 <code>O(nlog(n))</code>，但实现会增加索引管理复杂度；边界写错可能造成漏元素或重复覆盖。</p>\\n</li>\\n<li>\\n<p><strong>线上出现部分结果缺项，日志显示问题只发生在某一侧子数组先耗尽时，你会沿着 <code>mergeArr</code> 的哪条路径排查？</strong></p>\\n<p>先检查 <code>while(i &lt; len1 &amp;&amp; j &lt; len2)</code> 退出后，是否完整拼接未耗尽一侧的剩余区间。还要核对 <code>i</code>、<code>j</code> 的递增位置和 <code>slice(i)</code>、<code>slice(j)</code> 的起点；遗漏尾部不会改变复杂度，却会直接破坏结果完整性。</p>\\n</li>\\n<li>\\n<p><strong>产品要求相同键值的记录保持原有顺序，而示例在相等时进入右侧分支，你会如何修改比较条件，并说明代价吗？</strong></p>\\n<p>应在相等时优先取左数组元素，把判断调整为类似 <code>arr1[i] &lt;= arr2[j]</code>，从而保持跨分区记录的原始先后关系。时间复杂度仍是 <code>O(nlog(n))</code>，但前提是左右子数组自身也按同一稳定规则合并；自定义比较器还需保持一致性。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"快速排序\\"><a class=\\"header-anchor\\" href=\\"#快速排序\\">#</a> 快速排序</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>partition 把区间分成小于基准与大于等于基准两侧，基准最终归位。</li>\\n<li>平均 <code>O(n log n)</code>，固定坏基准时最坏 <code>O(n²)</code>；递归栈平均 <code>O(log n)</code>。</li>\\n<li>通常不稳定；随机基准、三数取中、三路划分可降低退化风险。</li>\\n<li>递归较小一侧、循环处理较大一侧，可把最坏栈空间压到 <code>O(log n)</code>。</li>\\n</ul>\\n<p><strong>快速排序通过 <code>partition</code> 把区间分成两侧，再递归排序两个子区间，关键是始终维护分区不变量。</strong> 平均情况下每层处理 <code>n</code> 个元素、递归约 <code>log n</code> 层，因此时间复杂度是 <code>O(n log n)</code>；基准持续选得很差时会退化到 <code>O(n²)</code>。它通常不稳定，递归栈平均占用 <code>O(log n)</code>。工程上可用随机基准、三数取中或三路划分降低退化风险，并优先递归较小的一侧来控制栈深度。</p>\\n</section>\\n<blockquote>\\n<p><strong>回答参考：</strong>“快排的关键是分区不变量，而不是背模板。我先定义指针两侧分别存什么，再保证基准归位后两个子区间互不重叠。”</p>\\n</blockquote>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>排序演示页把快排解释成“找到比基准小和大的元素再递归”，但代码交换后仍可能留下未检查区域，你会用什么反例说明只背这句话不足以保证正确？</strong></p>\\n<p>用包含重复值、逆序元素和交错大小关系的短数组逐步执行，检查每次交换后各指针之间仍有哪些元素尚未归类。真正的依据是分区不变量，而不是某个中间画面看起来有序；若区域含义没有预先定义，指针相遇时就无法证明基准两侧满足条件。</p>\\n</li>\\n<li>\\n<p><strong>前端团队要在数据可视化页面实现快排动画，交换步骤必须逐帧展示，你会怎样组织分区代码，避免动画逻辑破坏排序边界？</strong></p>\\n<p>先明确每个指针两侧分别代表已确认区和待检查区，再让每次比较、交换只完成一次合法的区域扩张，同时记录对应动画事件。基准归位后，递归区间必须互不重叠且排除已归位位置；若渲染状态反过来驱动指针，暂停或异步更新容易破坏不变量。</p>\\n</li>\\n<li>\\n<p><strong>同一段快排代码从普通数组改为包含大量相等键值的列表，产品又要求保留当前分区结构，你会重点检查哪项约束是否仍成立？</strong></p>\\n<p>重点检查相等元素被划入哪一侧，以及扫描指针在遇到相等值时是否仍会推进，保证分区过程能够结束。即使最终结果可排序，若相等值持续进入高度不均衡的子区间，递归工作也可能明显增加；在来源有限时不能据此断言具体复杂度退化幅度。</p>\\n</li>\\n<li>\\n<p><strong>线上排序偶发漏掉一个元素，日志显示基准归位后仍递归了包含该位置的区间，你会怎样定位是分区错误还是递归边界错误？</strong></p>\\n<p>为每轮记录原区间、基准最终下标及两个递归区间，并断言子区间互不重叠且都不包含已归位基准。若归位后两侧元素关系成立而递归范围仍覆盖该下标，问题在边界；若关系本身不成立，则回查扫描和交换是否始终维护既定不变量。</p>\\n</li>\\n<li>\\n<p><strong>评审会上有人主张把快排模板逐行照搬，另一位要求先写循环不变量再编码，你会支持哪种方案，验收标准是什么？</strong></p>\\n<p>支持先写清指针两侧含义和待处理区间，再据此实现模板，因为不同分区写法的边界并不天然兼容。验收时用基准归位、子区间不重叠以及递归终止作为可检查条件；代价是前期说明更细，但能降低混用模板边界造成的隐蔽错误。</p>\\n</li>\\n</ul>\\n</section>\\n<blockquote>\\n<p>快速排序在基本思想上和归并排序是一致的，仍然坚持“分而治之”的原则不动摇。区别在于，快速排序并不会把真的数组分割开来再合并到一个新数组中去，而是直接在原有的数组内部进行排序。</p>\\n</blockquote>\\n<h3 id=\\"思路分析-2\\"><a class=\\"header-anchor\\" href=\\"#思路分析-2\\">#</a> 思路分析</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>选择基准并做 partition，使基准左侧不大于它、右侧不小于它，再递归处理两边</li>\\n<li>分区扫描过程中，已划定区域始终满足与基准的大小关系，结束后基准落在最终位置</li>\\n<li>递归区间必须排除已经归位的基准；重复值很多时可用三路划分避免严重失衡</li>\\n</ul>\\n<p><strong>快速排序的做法是选一个基准，通过 <code>partition</code> 把较小值和较大值划到两侧，再递归处理两个子区间。</strong> 分区扫描时，已经划定的区域要一直满足与基准的大小关系，这样扫描结束后才能得到可靠的分界。递归边界不能重复包含已经确定的位置，否则可能产生无效递归。重复值很多时，普通二路划分容易失衡，可以改用三路划分。</p>\\n</section>\\n<ul>\\n<li>快速排序会将原始的数组筛选成较小和较大的两个子数组，然后递归地排序两个子数组。</li>\\n<li>这个描述对初学者来说可能会比较抽象，我们直接通过真实排序的过程来理解它：</li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>教程页用 <code>[3,1,2]</code> 演示“筛成较小和较大的两个子数组”，学生却认为两个子数组生成后就已经整体有序，你会怎样用页面过程纠正这个判断？</strong></p>\\n<p>分区只保证元素按与基准的关系进入两侧，并不保证任一侧内部已经有序，<code>[3,1,2]</code> 的中间状态也不能替代递归。应继续对两个子数组重复同一过程，直到区间无需再分；若把分区结果直接当成答案，稍长的交错数组就会暴露局部乱序。</p>\\n</li>\\n<li>\\n<p><strong>交互课件要展示一个数组如何逐层变成两个子数组，设计师希望直接复制切片，开发者担心过程与原地实现不一致，你会怎样约定展示模型？</strong></p>\\n<p>把课件的核心语义约定为“按基准划出较小侧和较大侧，再分别递归”，复制切片只是可视化载体，不应伪装成唯一实现。页面还要标出当前处理区间和递归层级；若省略边界信息，观众容易把展示数组与原数组下标混为一谈。</p>\\n</li>\\n<li>\\n<p><strong>同一套排序讲解从十几个元素的课堂示例扩展到很长的页面列表，仍要求完整展开每次递归，你会先调整算法还是展示约束？</strong></p>\\n<p>先调整展示约束，只展开当前分区和必要的递归分支，因为完整过程的节点与动画事件会随输入增长。排序逻辑仍保持“分区后递归两个子数组”，展示层可折叠或抽样；若直接删减实际递归步骤，课件可能呈现无法推出最终有序结果的过程。</p>\\n</li>\\n<li>\\n<p><strong>排序动画出现“左侧已经比基准小，但最终数组仍乱序”的故障，测试只截图了第一次分区结果，你会要求补充哪些状态来排查？</strong></p>\\n<p>要求记录每次递归的输入区间、分区后的两侧范围、递归返回顺序和最终合并视图，先确认两个子数组是否都被继续排序。第一次分区正确只能排除部分筛选错误，不能证明递归完整；若某个空区间或单元素区间之外的分支被提前跳过，最终仍会乱序。</p>\\n</li>\\n<li>\\n<p><strong>面试现场候选人想用归并排序的“先排子数组再合并”解释快排，而题图展示的是先筛分再递归，你会追问两者的流程差异落在哪里？</strong></p>\\n<p>快排的叙述重点是先围绕基准完成分区，再递归处理两个子数组，最终顺序依赖分区关系逐层成立。若套用“递归后再执行核心合并”的流程，会把操作时机和正确性依据说反；两者都使用分治，但不能因此共用同一段过程解释。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"真实排序过程演示-2\\"><a class=\\"header-anchor\\" href=\\"#真实排序过程演示-2\\">#</a> 真实排序过程演示</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>选择基准并做 partition，使基准左侧不大于它、右侧不小于它，再递归处理两边</li>\\n<li>分区扫描过程中，已划定区域始终满足与基准的大小关系，结束后基准落在最终位置</li>\\n<li>递归区间必须排除已经归位的基准；重复值很多时可用三路划分避免严重失衡</li>\\n</ul>\\n<p><strong>这段排序过程的核心是左右指针向中间扫描，把不符合分区关系的元素交换到另一侧，最终得到两个可继续递归的区间。</strong> 左指针遇到不小于基准的值就停下，右指针遇到不大于基准的值也停下；只要二者尚未交错，就交换元素并继续移动。整个过程中，指针已经越过的区域始终满足与基准的大小关系。这里 <code>partition</code> 返回的是下一轮的分界索引，递归区间要写成 <code>left</code> 到 <code>lineIndex - 1</code>，以及 <code>lineIndex</code> 到 <code>right</code>。</p>\\n</section>\\n<p>首先要做的事情就选取一个基准值。基准值的选择有很多方式，这里我们选取数组中间的值：</p>\\n<pre class=\\"language-text\\"><code>[5, 1, 3, 6, 2, 0, 7]\\n ↑       基准      ↑\\n</code></pre>\\n<blockquote>\\n<p>左右指针分别指向数组的两端。接下来我们要做的，就是先移动左指针，直到找到一个不小于基准值的值为止；然后再移动右指针，直到找到一个不大于基准值的值为止。\\n首先我们来看左指针，5比6小，故左指针右移一位：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[5, 1, 3, 6, 2, 0, 7]\\n    ↑    基准      ↑\\n</code></pre>\\n<blockquote>\\n<p>继续对比，1比6小，继续右移左指针：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[5, 1, 3, 6, 2, 0, 7]\\n       ↑ 基准      ↑\\n</code></pre>\\n<blockquote>\\n<p>继续对比，3比6小，继续右移左指针，左指针最终指向了基准值：</p>\\n</blockquote>\\n<pre class=\\"language-js\\"><code>[5, 1, 3, 6, 2, 0, 7]\\n         基准      ↑\\n          ↑\\n</code></pre>\\n<blockquote>\\n<p>此时由于 6===6，左指针停止移动。开始看右指针：</p>\\n</blockquote>\\n<p>右指针指向7，7&gt;6，故左移右指针：</p>\\n<pre class=\\"language-js\\"><code>[5, 1, 3, 6, 2, 0, 7]\\n         基准   ↑\\n          ↑\\n</code></pre>\\n<blockquote>\\n<p>发现 0 比 6 小，停下来，交换 6 和 0，同时两个指针共同向中间走一步：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[5, 1, 3, 0, 2, 6, 7]\\n             ↑ 基准\\n             ↑   \\n</code></pre>\\n<blockquote>\\n<p>时 2 比 6 小，故右指针不动，左指针继续前进：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[5, 1, 3, 0, 2, 6, 7]\\n             ↑ 基准\\n           right↑\\n               left   \\n</code></pre>\\n<blockquote>\\n<p>此时右指针所指的值不大于 6，左指针所指的值不小于 6，故两个指针都不再移动。此时我们会发现，对于左指针所指的数字来说，它左边的所有数字都比它小，右边的所有数字都比它大（这里注意也可能存在相等的情况）。由此我们就能够以左指针为轴心，划分出一左一右、一小一大两个子数组：</p>\\n</blockquote>\\n<pre class=\\"language-text\\"><code>[5, 1, 3, 0, 2]   \\n[6, 7]\\n</code></pre>\\n<blockquote>\\n<p>针对两个子数组，重复执行以上操作，直到数组完全排序为止。这就是快速排序的整个过程。</p>\\n</blockquote>\\n<p>编码实现</p>\\n<pre class=\\"language-js\\"><code>// 快速排序入口\\nfunction quickSort(arr, left = 0, right = arr.length - 1) {\\n  // 定义递归边界，若数组只有一个元素，则没有排序必要\\n  if(arr.length &gt; 1) {\\n      // lineIndex表示下一次划分左右子数组的索引位\\n      const lineIndex = partition(arr, left, right)\\n      // 如果左边子数组的长度不小于1，则递归快排这个子数组\\n      if(left &lt; lineIndex-1) {\\n        // 左子数组以 lineIndex-1 为右边界\\n        quickSort(arr, left, lineIndex-1)\\n      }\\n      // 如果右边子数组的长度不小于1，则递归快排这个子数组\\n      if(lineIndex&lt;right) {\\n        // 右子数组以 lineIndex 为左边界\\n        quickSort(arr, lineIndex, right)\\n      }\\n  }\\n  return arr\\n}\\n// 以基准值为轴心，划分左右子数组的过程\\nfunction partition(arr, left, right) {\\n  // 基准值默认取中间位置的元素\\n  let pivotValue = arr[Math.floor(left + (right-left)/2)]\\n  // 初始化左右指针\\n  let i = left\\n  let j = right\\n  // 当左右指针不越界时，循环执行以下逻辑\\n  while(i&lt;=j) {\\n      // 左指针所指元素若小于基准值，则右移左指针\\n      while(arr[i] &lt; pivotValue) {\\n          i++\\n      }\\n      // 右指针所指元素大于基准值，则左移右指针\\n      while(arr[j] &gt; pivotValue) {\\n          j--\\n      }\\n\\n      // 若i&lt;=j，则意味着基准值左边存在较大元素或右边存在较小元素，交换两个元素确保左右两侧有序\\n      if(i&lt;=j) {\\n          swap(arr, i, j)\\n          i++\\n          j--\\n      }\\n\\n  }\\n  // 返回左指针索引作为下一次划分左右子数组的依据\\n  return i\\n}\\n\\n// 快速排序中使用 swap 的地方比较多，我们提取成一个独立的函数\\nfunction swap(arr, i, j) {\\n  [arr[i], arr[j]] = [arr[j], arr[i]]\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>代码评审时同事说 <code>partition</code> 返回的 <code>lineIndex</code> 就是基准值的最终下标；拿 <code>[3, 2, 1]</code> 在页面上单步执行，你会接受这个结论吗？</strong></p>\\n<p>不能把 <code>lineIndex</code> 解释为基准值的最终下标，它只是左右子区间的新分界。该实现保存的是 <code>pivotValue</code>，交换后基准元素可能离开原位置；退出循环时只能保证分界左侧不大于基准、右侧不小于基准，因此递归边界应使用 <code>[left, lineIndex-1]</code> 和 <code>[lineIndex, right]</code>。</p>\\n</li>\\n<li>\\n<p><strong>你接手一个表格排序组件，需要把页面中的 <code>quickSort</code> 改成按用户传入的 <code>compare(a, b)</code> 排序，同时保持原地排序；<code>partition</code> 里的三处比较应怎样落地？</strong></p>\\n<p>应把 <code>arr[i] &lt; pivotValue</code>、<code>arr[j] &gt; pivotValue</code> 和大小判断统一改为调用 <code>compare</code>，分别检查结果小于零或大于零，交换逻辑仍保留。基准值要在扫描前保存，避免其所在元素被交换后改变比较对象；比较器若不满足一致性，指针分区和最终顺序都无法保证。</p>\\n</li>\\n<li>\\n<p><strong>线上批量导入页一次要排序大量已排序、逆序和高重复数据，产品又要求不能因递归过深卡死；继续固定取中间元素时，你会怎样调整实现？</strong></p>\\n<p>中间取值能避开首元素作基准在已排序输入上的典型失衡，但不能保证每次分区均衡，高重复数据也会增加无效交换。可优先处理较小子区间、把较大区间改为循环，或对重复值采用三路划分；这些改动降低栈风险，却会增加实现和验证成本。</p>\\n</li>\\n<li>\\n<p><strong>监控显示排序接口偶发无限递归，日志里某轮 <code>partition</code> 返回 <code>lineIndex === left</code>；你会先检查哪几处指针和递归边界？</strong></p>\\n<p>先确认交换后是否同时执行了 <code>i++</code> 和 <code>j--</code>，否则遇到等于 <code>pivotValue</code> 的元素时指针可能停住。再核对递归区间是否严格缩小为 <code>[left, lineIndex-1]</code> 与 <code>[lineIndex, right]</code>，并用空数组、单元素和全重复数组复现；比较器异常也可能破坏推进条件。</p>\\n</li>\\n<li>\\n<p><strong>架构评审中一方坚持当前双指针分区，另一方要求为大量重复状态码改成三路划分；你会依据什么做选型？</strong></p>\\n<p>若数据分布普通，当前双指针实现短小，并且等值元素也会通过交换后推进指针，可以继续使用。若重复值长期占主导，三路划分能把小于、等于和大于基准的区域一次分开，减少对等值区间的递归；代价是边界状态更多，测试与维护复杂度更高。</p>\\n</li>\\n</ul>\\n</section>\\n<h3 id=\\"编码复盘-快速排序的时间复杂度分析\\"><a class=\\"header-anchor\\" href=\\"#编码复盘-快速排序的时间复杂度分析\\">#</a> 编码复盘——快速排序的时间复杂度分析</h3>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>选择基准并做 partition，使基准左侧不大于它、右侧不小于它，再递归处理两边</li>\\n<li>分区扫描过程中，已划定区域始终满足与基准的大小关系，结束后基准落在最终位置</li>\\n<li>平均 <code>O(n log n)</code>，持续选到极端基准时最坏 <code>O(n²)</code>；递归栈平均 <code>O(log n)</code></li>\\n</ul>\\n<p><strong>快速排序平均和最好时间复杂度都是 <code>O(n log n)</code>，最坏会退化到 <code>O(n²)</code>，关键取决于基准值能否把数组划分得足够均衡。</strong> 每次都选中当前子数组的中间数时，数组会被分成两半，递归深度约为 <code>log(n)</code>。如果持续选中最大值或最小值，划分会严重失衡，执行过程就会退化。因此分析快排时，不能只背平均复杂度，还要说明基准值选择带来的边界情况。</p>\\n</section>\\n<blockquote>\\n<p>快速排序的时间复杂度的好坏，是由基准值来决定的。</p>\\n</blockquote>\\n<ul>\\n<li>最好时间复杂度：它对应的是这种情况——我们每次选择基准值，都刚好是当前子数组的中间数。这时，可以确保每一次分割都能将数组分为两半，进而只需要递归 <code>log(n)</code> 次。这时，快速排序的时间复杂度分析思路和归并排序相似，最后结果也是 <code>O(nlog(n))</code></li>\\n<li>最坏时间复杂度：每次划分取到的都是当前数组中的最大值/最小值。大家可以尝试把这种情况代入快排的思路中，你会发现此时快排已经退化为了冒泡排序，对应的时间复杂度是 <code>O(n^2)</code>。</li>\\n<li>平均时间复杂度： <code>O(nlog(n))</code></li>\\n</ul>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>商品列表已经按价格升序排列，代码又固定拿区间末尾元素做基准；有人仍声称快速排序一定是 <code>O(nlog(n))</code>，你怎么反驳？</strong></p>\\n<p>这组输入会让基准反复成为当前区间的最大值，每次划分只能减少一个元素，递归层数接近 <code>n</code>。每层分区仍要扫描当前区间，总工作量形成递减求和，因此最坏时间复杂度是 <code>O(n^2)</code>，不能把平均复杂度当成保证。</p>\\n</li>\\n<li>\\n<p><strong>前端页面要对一批无明显顺序的数据执行快速排序，你会怎样在代码评审中解释一次分区成本与总复杂度的关系？</strong></p>\\n<p>一次分区通常需要线性扫描当前子数组，因此该层所有互不重叠子数组的工作量合计约为 <code>O(n)</code>。若基准持续把区间近似平分，递归深度为 <code>O(log n)</code>，总时间就是 <code>O(nlog n)</code>；关键风险是划分是否长期失衡。</p>\\n</li>\\n<li>\\n<p><strong>数据服务把输入改成大量有序或逆序数组，但团队不允许更换快速排序，你会优先调整哪个选型点？</strong></p>\\n<p>应优先避免固定选择容易成为最大值或最小值的元素作为基准，因为这正是退化到 <code>O(n^2)</code> 的触发条件。可采用随机化或更稳健的基准选择来降低持续失衡的概率，但它改善的是典型表现，不能把理论最坏界改成 <code>O(nlog n)</code>。</p>\\n</li>\\n<li>\\n<p><strong>线上排序任务偶发卡顿，日志显示正常请求很快，只有某些输入递归层数异常深；你会收集哪些证据定位？</strong></p>\\n<p>先记录每轮基准值、左右分区长度和递归深度，检查是否反复出现 <code>0</code> 与 <code>n-1</code> 这样的极端划分。再对照输入是否已排序、逆序或让固定基准持续取到极值；若证据吻合，卡顿来自 <code>O(n^2)</code> 路径，而不是平均复杂度估算错误。</p>\\n</li>\\n<li>\\n<p><strong>架构评审中，一方要求快速排序必须给出 <code>O(nlog n)</code> 的最坏保证，另一方只关注平均吞吐，你如何裁决？</strong></p>\\n<p>快速排序的平均时间复杂度是 <code>O(nlog n)</code>，但基准连续取到极值时最坏为 <code>O(n^2)</code>，所以它无法满足前一方的严格保证。若最坏界是硬约束，应改选具有相应保证的排序方案；若接受概率性风险，再通过基准策略改善常见输入表现。</p>\\n</li>\\n</ul>\\n</section>\\n<h2 id=\\"小结\\"><a class=\\"header-anchor\\" href=\\"#小结\\">#</a> 小结</h2>\\n<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>选择基准并做 partition，使基准左侧不大于它、右侧不小于它，再递归处理两边</li>\\n<li>分区扫描过程中，已划定区域始终满足与基准的大小关系，结束后基准落在最终位置</li>\\n<li>递归区间必须排除已经归位的基准；重复值很多时可用三路划分避免严重失衡</li>\\n</ul>\\n<p><strong>排序算法这部分需要真正掌握常考的五种实现，并能通过反复练习熟悉到独立写出的程度。</strong> 学习它们不只是为了记代码，更重要的是建立对时间效率的敏感度，以及根据执行成本优化算法的意识。常考内容掌握扎实后，如果时间充裕，还可以继续了解基数排序、桶排序和堆排序。面试准备阶段，我一般会先保证核心算法写得稳，再扩展这些相对不常考的内容。</p>\\n</section>\\n<blockquote>\\n<p>经过两节的学习，大家已经掌握了前端算法面试中最常考、最关键的5种排序算法。对于已经学过的这些知识，希望大家课下多消化多反思，以“默写”为目标去反复熟悉每一个算法。</p>\\n</blockquote>\\n<p>排序算法的学习，对于培养大家的时间效率敏感度、提升算法优化思维等方面是大有裨益的。在整个算法知识体系中，还有一些虽然不常考察，但同样有趣的排序算法，比如基数排序、桶排序、堆排序等等，在这里推荐学有余力、时间充裕的同学课下多读多看，在排序算法这个专题下更进一步。</p>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>候选人在排序专题结束后只会背五种算法的名称，却无法在白板上写出任何一种；作为面试官你会如何判断学习是否真正完成？</strong></p>\\n<p>仅记住名称不能证明已经掌握，至少应能脱离资料写出核心流程，并说明关键操作如何影响时间效率。小结明确把反复熟悉到能够“默写”作为目标；若代码无法落地，说明理解尚未沉淀为稳定的解题能力。</p>\\n</li>\\n<li>\\n<p><strong>团队安排一周复习排序算法，成员在“继续看讲解”和“反复手写已学算法”之间争执，你会怎样分配重点？</strong></p>\\n<p>应把主要时间放在手写、运行和复盘已经学过的常考算法上，目标是看到题型后能迅速还原实现。继续阅读可作为补充，但不能替代反复编码；若基本算法仍无法独立写出，扩展阅读的收益会被知识未固化所限制。</p>\\n</li>\\n<li>\\n<p><strong>候选人准备时间从两周压缩到两天，还计划同时学习基数排序、桶排序和堆排序，你会如何调整范围？</strong></p>\\n<p>应先确保前端面试中最常考、最关键的已学排序算法能够默写并解释效率，再决定是否扩展。基数排序、桶排序和堆排序属于学有余力时的延伸内容；时间紧张时全面铺开，代价往往是核心实现也不够熟练。</p>\\n</li>\\n<li>\\n<p><strong>模拟面试中，候选人能复述排序思路，但一写代码就频繁停顿并混淆步骤；你会如何设计下一轮排查训练？</strong></p>\\n<p>先要求他在无提示条件下完整写出单个算法，再用小规模输入逐步执行，定位是流程记忆断裂还是复杂度判断含糊。随后重复默写并对照修正，直到核心步骤稳定；只继续听课会掩盖编码映射不足，无法解决现场卡顿。</p>\\n</li>\\n<li>\\n<p><strong>学习负责人想用“刷更多排序变体”替代复杂度复盘，理由是题量更大更保险；你会接受这个方案吗？</strong></p>\\n<p>不应完全替代，因为排序专题的价值还包括培养时间效率敏感度和算法优化思维，而不只是积累题型。变体训练可在核心算法熟练后加入；若不能判断实现的效率变化，刷题数量增加也可能只是重复记忆代码外形。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);