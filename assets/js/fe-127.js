(window.webpackJsonp=window.webpackJsonp||[]).push([[127],{1234:function(e){e.exports=JSON.parse('{"html":"<section class=\\"custom-block recall\\"><p class=\\"custom-block-title recall-title\\">⚡ 30 秒速记</p>\\n<ul>\\n<li>核心判断：虚拟节点是对界面结构的描述，渲染器通过挂载与更新把声明式描述映射为宿主环境操作</li>\\n<li>原理主线：围绕 「key 和 ref」、「parentVNode 以及它的作用」、「contextVNode」 建立输入、状态变化与输出之间的因果关系</li>\\n<li>文章范围：解析了Vue有状态组件的设计原理，介绍了插槽（slots）在VNode结构中的实现方式，以及如何通过组件实例访问和管理插槽内容，帮助开发者深入理解Vue组件的底层设计与渲染机制。</li>\\n<li>边界与代价：<code>key</code>、节点类型和稳定序列决定复用边界；虚拟 DOM 解决可预测更新，不保证永远比手写 DOM 快</li>\\n<li>工程落地：性能分析要看实际提交次数、节点移动和组件边界，不能只用“减少 DOM 操作”解释</li>\\n</ul>\\n<p><strong>有状态组件的设计核心，是让组件实例保存自身状态和渲染结果，并通过关联的 <code>VNode</code> 接收外部信息。</strong> 挂载时先创建实例，再注入 <code>slots</code>，执行 <code>render</code> 得到组件内部的 <code>VNode</code>，最后把它挂载为真实 <code>DOM</code>。普通插槽和作用域插槽本质上都可以看成返回 <code>VNode</code> 的函数，区别只是后者接收子组件传入的数据。<code>ref</code>、事件和父子关系依赖不同的 <code>VNode</code> 引用，而函数式组件没有实例，不能照搬这套实例链。</p>\\n</section>\\n<p>这篇文章不要按 <code>API</code> 清单来背。先用上面的 <code>Mind Map</code> 建立全局结构，再通过交互 DEMO 观察正常路径和边界路径如何改变状态；阅读正文时重点核对每一步的输入、负责执行的参与者、产生的中间状态以及最终可观察结果。遇到版本敏感结论，要把“历史实现”“当前行为”和“工程兼容策略”分开说明；遇到性能或架构取舍，则用实际指标、失败现象和验证手段支撑判断。</p>\\n<p><strong>版本校准：</strong> 本文出现 <code>Object.defineProperty</code>、<code>Dep</code>、<code>Watcher</code> 和双端 <code>Diff</code> 时，主要描述 <code>Vue 2</code> 实现；<code>Vue 3</code> 使用 <code>Proxy</code>、<code>effect</code> 与新的渲染器路径。<code>Vue 2</code> 已于 <code>2023-12-31</code> 结束维护，新项目应以 <code>Vue 3</code> 为基线，旧项目参考 <a href=\\"https://v2.vuejs.org/eol/\\">Vue 2 EOL 官方说明</a> 制定迁移与安全策略。</p>\\n<p>假设我们有如下模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;MyComponent&gt;\\n  &lt;div&gt;&lt;/div&gt;\\n&lt;/MyComponent&gt;\\n</code></pre>\\n<p>由这段模板可知，我们为 <code>MyComponent</code> 组件提供了一个空的 <code>div</code> 标签作为默认插槽内容，从DOM结构上看 <code>&lt;MyComponent&gt;</code> 标签有一个 <code>div</code> 标签作为子节点，通常我们可以将其编译为如下 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>const compVNode = {\\n  flags: VNodeFlags.COMPONENT_STATEFUL_NORMAL,\\n  tag: MyComponent,\\n  children: {\\n    flags: VNodeFlags.ELEMENT,\\n    tag: &#39;div&#39;\\n  }\\n}\\n</code></pre>\\n<p>这其实没什么问题，但是我们更倾向于新建一个 <code>slots</code> 属性来存储这些子节点，这在语义上更加贴切，所以我们希望将模板编译为如下 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>const compVNode = {\\n  flags: VNodeFlags.COMPONENT,\\n  tag: MyComponent,\\n  children: null,\\n  slots: {\\n    // 默认插槽\\n    default: {\\n      flags: VNodeFlags.ELEMENT,\\n      tag: &#39;div&#39;\\n    }\\n  }\\n}\\n</code></pre>\\n<p>可以看到，如上 <code>VNode</code> 的 <code>children</code> 属性值为 <code>null</code>。当我们使用 <code>mountComponent</code> 函数挂载如上 <code>VNode</code> 时，我们可以在<strong>组件实例化之后</strong>并且在<strong>组件的渲染函数执行之前</strong>将 <code>compVNode.slots</code> 添加到组件实例对象上，这样当组件的渲染函数执行的时候，就可以访问插槽数据：</p>\\n<pre class=\\"language-js\\"><code>function mountComponent(vnode, container) {\\n  // 创建组件实例\\n  const instance = new vnode.tag()\\n\\n  // 设置 slots\\n  instance.$slots = vnode.slots\\n\\n  // 渲染\\n  instance.$vnode = instance.render()\\n  // 挂载\\n  mountElement(instance.$vnode, container)\\n\\n  vnode.ref &amp;&amp; vnode.ref(instance)\\n}\\n</code></pre>\\n<p>在 <code>MyComponent</code> 组件的 <code>render</code> 函数内，我们就可以通过组件实例访问 <code>slots</code> 数据：</p>\\n<pre class=\\"language-js\\"><code>class MyComponent {\\n  render() {\\n    return {\\n      flags: VNodeFlags.ELEMENT,\\n      tag: &#39;h1&#39;\\n      children: this.$slots.default\\n    }\\n  }\\n}\\n</code></pre>\\n<p>实际上，这就是普通插槽的实现原理，至于作用域插槽(<code>scopedSlots</code>)，与普通插槽并没有什么本质的区别，我们知道作用域插槽可以访问子组件的数据，在实现上来看其实就是函数传参：</p>\\n<pre class=\\"language-js\\"><code>class MyComponent {\\n  render() {\\n    return {\\n      flags: VNodeFlags.ELEMENT,\\n      tag: &#39;h1&#39;\\n      // 插槽变成了函数，可以传递参数\\n      children: this.$slots.default(1)\\n    }\\n  }\\n}\\n</code></pre>\\n<p>如上代码所示，只要 <code>this.$slots.default</code> 是函数即可实现，所以在模板编译时，我们最终需要得到如下 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>const compVNode = {\\n  flags: VNodeFlags.COMPONENT,\\n  tag: MyComponent,\\n  children: null,\\n  slots: {\\n    // 作用域插槽，可以接受组件传递过来的数据\\n    default: (arg) =&gt; {\\n      const tag = arg === 1 ? &#39;div&#39; : &#39;h1&#39;\\n      return {\\n        flags: VNodeFlags.ELEMENT,\\n        tag\\n      }\\n    }\\n  }\\n}\\n</code></pre>\\n<p>现在你应该明白为什么普通插槽和作用域插槽本质上并没有区别了，因为普通插槽也可以是函数，只是不接收参数罢了。这么看的话其实普通插槽是作用域插槽的子集，那为什么不将它们合并呢？没错从 <code>Vue2.6</code> 起已经将之合并，所有插槽在 <code>VNode</code> 中都是函数，一个返回 <code>VNode</code> 的函数。</p>\\n<div class=\\"tip\\">\\n<p>用过 <code>React</code> 的朋友，这让你想起 <code>Render Prop</code> 了吗！</p>\\n</div>\\n<h2 id=\\"key-和-ref\\"><a class=\\"header-anchor\\" href=\\"#key-和-ref\\">#</a> key 和 ref</h2>\\n<p><code>key</code> 就像 <code>VNode</code> 的唯一标识，用于 <code>diff</code> 算法的优化，它可以是数字也可以是字符串：</p>\\n<pre class=\\"language-js\\"><code>{\\n  flags: VNodeFlags.ELEMENT_HTML,\\n  tag: &#39;li&#39;,\\n  key: &#39;li_0&#39;\\n}\\n</code></pre>\\n<p><code>ref</code> 的设计是为了提供一种能够拿到真实DOM的方式，当然如果将 <code>ref</code> 应用到组件上，那么拿到的就是组件实例，我们通常会把 <code>ref</code> 设计成一个函数，假设我们有如下模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;div :ref=&quot;el =&gt; elRef = el&quot;&gt;&lt;/div&gt;\\n</code></pre>\\n<p>我们可以把这段模板编译为如下 <code>VNode</code>：</p>\\n<pre class=\\"language-js\\"><code>const elementVNode = {\\n  flags: VNodeFlags.ELEMENT_HTML,\\n  tag: &#39;div&#39;,\\n  ref: el =&gt; elRef = el\\n}\\n</code></pre>\\n<p>在使用 <code>mountElement</code> 函数挂载如上 <code>VNode</code> 时，可以轻松的实现 <code>ref</code> 功能：</p>\\n<pre class=\\"language-js\\"><code>function mountElement(vnode, container) {\\n  const el = document.createElement(vnode.tag)\\n  container.appendChild(el)\\n  vnode.ref &amp;&amp; vnode.ref(el)\\n}\\n</code></pre>\\n<p>如果挂载的是组件而非普通标签，那么只需要将组件实例传递给 <code>vnode.ref</code> 函数即可：</p>\\n<pre class=\\"language-js\\"><code>function mountComponent(vnode, container) {\\n  // 创建组件实例\\n  const instance = new vnode.tag()\\n  // 渲染\\n  instance.$vnode = instance.render()\\n  // 挂载\\n  mountElement(instance.$vnode, container)\\n\\n  vnode.ref &amp;&amp; vnode.ref(instance)\\n}\\n</code></pre>\\n<h2 id=\\"parentvnode-以及它的作用\\"><a class=\\"header-anchor\\" href=\\"#parentvnode-以及它的作用\\">#</a> parentVNode 以及它的作用</h2>\\n<p>与 <code>VNode</code> 的 <code>slots</code> 属性相同，<code>parentVNode</code> 属性也是给组件的 <code>VNode</code> 准备的，组件的 <code>VNode</code> 为什么需要这两个属性呢？它俩的作用又是什么呢？想弄清楚这些，我们至少要先弄明白：<strong>一个组件所涉及的 <code>VNode</code> 都有哪些</strong>。什么意思呢？看如下模板思考一个问题：</p>\\n<pre class=\\"language-html\\"><code>&lt;template&gt;\\n  &lt;div&gt;\\n    &lt;MyComponent /&gt;\\n  &lt;/div&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>从这个模板来看 <code>MyComponent</code> 组件至少涉及到两个 <code>VNode</code>，第一个 <code>VNode</code> 是标签 <code>&lt;MyComponent /&gt;</code> 的描述，其次 <code>MyComponent</code> 组件本身也有要渲染的内容，这就是第二个 <code>VNode</code>。</p>\\n<ul>\\n<li>第一个 <code>VNode</code> 用来描述 <code>&lt;MyComponent /&gt;</code> 标签：</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>{\\n  // 省略...\\n  tag: MyComponent\\n}\\n</code></pre>\\n<ul>\\n<li>第二个 <code>VNode</code> 是组件渲染内容的描述，即组件的 <code>render</code> 函数产出的 <code>VNode</code>：</li>\\n</ul>\\n<pre class=\\"language-js\\"><code>class MyComponent {\\n  render () {\\n    return {/* .. */} // 产出的 VNode\\n  }\\n}\\n</code></pre>\\n<p>组件实例的 <code>$vnode</code> 属性值就是组件 <code>render</code> 函数产出的 <code>VNode</code>，这通过如下代码可以一目了然：</p>\\n<pre class=\\"language-js\\"><code>function mountComponent(vnode, container) {\\n  // 创建组件实例\\n  const instance = new vnode.tag()\\n  // 渲染，$vnode 的值就是组件 render 函数产出的 VNode\\n  instance.$vnode = instance.render()\\n  // 挂载\\n  mountElement(instance.$vnode, container)\\n\\n  vnode.ref &amp;&amp; vnode.ref(instance)\\n}\\n</code></pre>\\n<p>而 <code>instance.$vnode.parentVNode</code> 的值就是用来描述组件(如：<code>&lt;MyComponent /&gt;</code>)标签的 <code>VNode</code>，我们只需在如上代码中添加一行代码即可实现：</p>\\n<pre class=\\"language-js\\"><code>function mountComponent(vnode, container) {\\n  // 创建组件实例\\n  const instance = new vnode.tag()\\n  // 渲染，$vnode 的值就是组件 render 函数产出的 VNode\\n  instance.$vnode = instance.render()\\n  // vnode 就是用来描述组件标签的 VNode\\n  instance.$vnode.parentVNode = vnode\\n  // 挂载\\n  mountElement(instance.$vnode, container)\\n\\n  vnode.ref &amp;&amp; vnode.ref(instance)\\n}\\n</code></pre>\\n<p>同时我们也可以在组件实例上添加 <code>$parentVNode</code> 属性，让其同样引用组件的标签描述：</p>\\n<pre class=\\"language-js\\"><code>function mountComponent(vnode, container) {\\n  // 创建组件实例\\n  const instance = new vnode.tag()\\n  // 渲染，$vnode 的值就是组件 render 函数产出的 VNode\\n  instance.$vnode = instance.render()\\n  // vnode 就是用来描述组件标签的 VNode\\n  instance.$parentVNode = instance.$vnode.parentVNode = vnode\\n  // 挂载\\n  mountElement(instance.$vnode, container)\\n\\n  vnode.ref &amp;&amp; vnode.ref(instance)\\n}\\n</code></pre>\\n<p>组件的实例为什么需要引用 <code>parentVNode</code> 呢？这是因为组件的事件监听器都在 <code>parentVNode</code> 上，如下模板所示：</p>\\n<pre class=\\"language-html\\"><code>&lt;MyComponent @click=&quot;handleClick&quot; /&gt;\\n</code></pre>\\n<p>这段模板可以用如下 <code>VNode</code> 描述：</p>\\n<pre class=\\"language-js\\"><code>const parentVNode = {\\n  // 省略...\\n  tag: MyComponent,\\n  data: {\\n    onclick: () =&gt; handleClick()\\n  }\\n}\\n</code></pre>\\n<p>当你在组件中发射(<code>emit</code>)事件时，就需要去 <code>parentVNode</code> 中找到对应的事件监听器并执行：</p>\\n<pre class=\\"language-js\\"><code>// 组件实例的 $emit 实现\\nclass MyComponent {\\n  $emit(eventName, ...payload) {\\n    // 通过 parentVNode 拿到其 VNodeData\\n    const parentData = this.$parentVNode.data\\n    // 执行 handler\\n    parentData[`on${eventName}`](payload)\\n  },\\n\\n  handleClick() {\\n    // 这里就可以通过 this.$emit 发射事件\\n    this.$emit(&#39;click&#39;, 1)\\n  }\\n}\\n</code></pre>\\n<p>实际上，这就是事件的实现思路。由于 <code>$emit</code> 是框架层面的设计，所以我们在设计框架时可以提供一个最基本的组件，将框架层面的设计都归纳到该组件中：</p>\\n<pre class=\\"language-js\\"><code>class Component {\\n  $emit(eventName, ...payload) {\\n    // 通过 parentVNode 拿到其 VNodeData\\n    const parentData = this.$parentVNode.data\\n    // 执行 handler\\n    parentData[`on${eventName}`](payload)\\n  }\\n  // 其他......\\n}\\n</code></pre>\\n<p>这样框架的使用者在开发组件时，只需要继承我们的 <code>Component</code> 即可：</p>\\n<pre class=\\"language-js\\"><code>// 用户的代码\\nimport { Component } from &#39;vue&#39;\\n\\nclass MyComponent extends Component {\\n  handleClick() {\\n    // 直接使用即可\\n    this.$emit(&#39;click&#39;, 1)\\n  }\\n}\\n</code></pre>\\n<h2 id=\\"contextvnode\\"><a class=\\"header-anchor\\" href=\\"#contextvnode\\">#</a> contextVNode</h2>\\n<p>我们已经知道了与一个组件相关的 <code>VNode</code> 有两个，一个是组件自身产出的 <code>VNode</code>，可以通过组件实例的 <code>instance.$vnode</code> 访问，另一个是当使用组件时用来描述组件标签的 <code>VNode</code>，我们可以通过组件实例的 <code>instance.$parentVNode</code> 访问，并且：</p>\\n<pre class=\\"language-js\\"><code>instance.$vnode.parentVNode === instance.$parentVNode\\n</code></pre>\\n<p>那么 <code>contextVNode</code> 是什么呢？实际上<strong>子组件标签描述的 <code>VNode.contextVNode</code> 是父组件的标签描述 <code>VNode</code></strong>，或者说<strong>子组件实例的 <code>$parentVNode.contextVNode</code> 是父组件实例的 <code>$parentVNode</code></strong>，假设根组件渲染了 <code>Foo</code> 组件，而 <code>Foo</code> 组件又渲染 <code>Bar</code> 组件，此时就形成了一条父子链：<strong><code>Bar</code> 组件的父组件是 <code>Foo</code></strong>。</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2024/02/f27d595b932c886a.png\\" alt=\\"\\"></p>\\n<p>为什么子组件的标签描述 <code>VNode</code> 需要引用父组件的标签描述 <code>VNode</code> 呢？这是因为一个组件的标签描述 <code>VNode</code> 中存储着该组件的实例对象，即 <code>VNode.children</code> 属性。还记得之前我们讲到过，对于组件来说，它的 <code>VNode.children</code> 属性会存储组件实例对象吗。这样通过这一层引用关系，子组件就知道它的父组件是谁，同时父组件也知道它有哪些子组件。</p>\\n<p>语言描述会有些抽象，我们拿具体案例演示一下，假设我们的根组件有如下模板：</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- 根组件模板 --&gt;\\n&lt;template&gt;\\n  &lt;Foo/&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>它对应的 <code>VNode</code> 如下：</p>\\n<pre class=\\"language-js\\"><code>const FooVNode = {\\n  flags: VNodeFlags.COMPONENT,\\n  tag: Foo, // Foo 指的是 class Foo {}\\n}\\n</code></pre>\\n<p>接着 <code>Foo</code> 组件的模板如下，它渲染了 <code>Bar</code> 组件：</p>\\n<pre class=\\"language-html\\"><code>&lt;!-- 组件 Foo 的模板 --&gt;\\n&lt;template&gt;\\n  &lt;Bar/&gt;\\n&lt;/template&gt;\\n</code></pre>\\n<p>它对应的 <code>VNode</code> 如下：</p>\\n<pre class=\\"language-js\\"><code>const BarVNode = {\\n  flags: VNodeFlags.COMPONENT,\\n  tag: Bar, // Foo 指的是 class Bar {}\\n}\\n</code></pre>\\n<p>我们使用 <code>mountComponent</code> 函数挂载 <code>FooVNode</code> 组件：</p>\\n<pre class=\\"language-js\\"><code>mountComponent(FooVNode, container)\\n\\nfunction mountComponent(vnode, container) {\\n  // 创建 Foo 组件实例\\n  const instance = new vnode.tag()\\n  // 渲染，instance.$vnode 的值就是 BarVNode\\n  instance.$vnode = instance.render()\\n  // 使用 mountComponent 函数递归挂载 BarVNode\\n  mountComponent(instance.$vnode, container)\\n\\n  vnode.ref &amp;&amp; vnode.ref(instance)\\n}\\n</code></pre>\\n<p>如上代码所示，首先我们调用 <code>mountComponent</code> 函数挂载 <code>FooVNode</code>，会创建 <code>Foo</code> 组件实例，接着调用 <code>Foo</code> 组件实例的 <code>render</code> 函数得到 <code>Foo</code> 组件产出的 <code>VNode</code>，这其实就是 <code>BarVNode</code>，由于 <code>BarVNode</code> 的类型也是组件，所以我们会递归调用 <code>mountComponent</code> 挂载 <code>BarVNode</code>，最终 <code>mountComponent</code> 函数会执行两次。接下来我们为使用 <code>contextVNode</code> 完善上面的代码，看看如何来建立起父子链：</p>\\n<pre class=\\"language-js\\"><code>mountComponent(FooVNode, container)\\n\\nfunction mountComponent(vnode, container, contextVNode = null) {\\n  // 创建组件实例\\n  const instance = new vnode.tag()\\n\\n  if (contextVNode) {\\n    const parentComponent = contextVNode.children\\n    instance.$parent = parentComponent\\n    parentComponent.$children.push(instance)\\n    instance.$root = parentComponent.$root\\n  } else {\\n    instance.$root = instance\\n  }\\n\\n  // 渲染\\n  instance.$vnode = instance.render()\\n  // 使用 mountComponent 函数递归挂载\\n  mountComponent(instance.$vnode, container, vnode)\\n\\n  vnode.ref &amp;&amp; vnode.ref(instance)\\n}\\n</code></pre>\\n<p>如上高亮代码所示，我们为 <code>mountComponent</code> 函数添加了第三个参数 <code>contextVNode</code>，我们可以一下这个过程发生了什么：</p>\\n<ul>\\n<li>1、初次调用 <code>mountComponent</code> 挂载 <code>FooVNode</code> 时，没有传递第三个参数，所以 <code>contextVNode = null</code>，这时说明当前挂载的组件就是根组件，所以我们让当前组件实例的 <code>$root</code> 属性值引用其自身。</li>\\n<li>2、当递归调用 <code>mountComponent</code> 挂载 <code>BarVNode</code> 时，我们传递了第三个参数，并且点三个参数是 <code>FooVNode</code>。此时 <code>contextVNode = FooVNode</code>，我们通过 <code>contextVNode.children</code> 即可拿到 <code>Foo</code> 组件的实例，并把它赋值给 <code>Bar</code> 组件实例的 <code>$parent</code> 属性，同时把 <code>Bar</code> 组件实例添加到 <code>Foo</code> 组件实例的 <code>$children</code> 数组中，这样这条父子链就成功建立了。</li>\\n</ul>\\n<p>实际上，除了组件实例间建立父子关系，组件的 <code>VNode</code> 间也可以建立父子关系，只需要增加一行代码即可：</p>\\n<pre class=\\"language-js\\"><code>mountComponent(FooVNode, container)\\n\\nfunction mountComponent(vnode, container, contextVNode = null) {\\n  vnode.contextVNode = contextVNode\\n  // 省略...\\n}\\n</code></pre>\\n<p>为什么要在组件的 <code>VNode</code> 上也建立这种父子联系呢？答案是在其他地方有用到，这么做就是为了在某些情况下少传递一些参数，直接通过 <code>VNode</code> 之间的联系找到我们想要的信息即可。另外在如上的演示中，我们省略了避开函数式组件的逻辑，因为函数式组件没有组件实例，所谓的父子关系只针对于有状态组件。实现逻辑很简单，就是通过一个 <code>while</code> 循环<strong>沿着父子链一直向上找到第一个非函数式组件，并把该组件的实例作为当前组件实例的 <code>$parent</code> 即可</strong>。</p>\\n<h2 id=\\"el\\"><a class=\\"header-anchor\\" href=\\"#el\\">#</a> el</h2>\\n<p><code>VNode</code> 既然是真实DOM的描述，那么理所应当的，当它被渲染完真实DOM之后，我们需要将真实DOM对象的引用添加到 <code>VNode</code> 的 <code>el</code> 属性上。由于 <code>VNode</code> 具有不同的类型，不同类型的 <code>VNode</code> 其 <code>el</code> 属性所引用的真实DOM对象也不同，下图展示了所有 <code>VNode</code> 类型：</p>\\n<p><img src=\\"https://s.poetries.top/uploads/2024/02/022e6a497c274c59.png\\" alt=\\"\\"></p>\\n<ul>\\n<li>1、<code>html/svg</code> 标签</li>\\n</ul>\\n<p>其 <code>el</code> 属性值为真实DOM元素的引用：</p>\\n<pre class=\\"language-js\\"><code>{\\n  tag: &#39;div&#39;,\\n  el: div 元素的引用 // 如 document.createElement(&#39;div&#39;)\\n}\\n</code></pre>\\n<ul>\\n<li>2、组件</li>\\n</ul>\\n<p>其 <code>el</code> 属性值为组件本身所渲染真实DOM的根元素：</p>\\n<pre class=\\"language-js\\"><code>{\\n  tag: MyComponent,\\n  el: instance.$vnode.el\\n}\\n</code></pre>\\n<ul>\\n<li>3、纯文本</li>\\n</ul>\\n<p>其 <code>el</code> 属性值为文本元素的引用：</p>\\n<pre class=\\"language-js\\"><code>{\\n  tag: null,\\n  children: &#39;txt&#39;,\\n  el: 文本元素的引用 // 如 document.createTextNode(&#39;txt&#39;)\\n}\\n</code></pre>\\n<ul>\\n<li>4、Fragment</li>\\n</ul>\\n<p>其 <code>el</code> 属性值为片段中第一个DOM元素的引用：</p>\\n<pre class=\\"language-js\\"><code>{\\n  tag: null,\\n  children: [\\n    {\\n      tag: &#39;h1&#39;\\n    },\\n    {\\n      tag: &#39;h2&#39;\\n    }\\n  ],\\n  el: h1 元素的引用而非 h2\\n}\\n</code></pre>\\n<p>当然片段本身可能是一个空数组，即 <code>children</code> 属性值为 <code>[]</code>，此时代表该片段不渲染任何东西，但在框架设计中，我们会渲染一个空的文本节点占位，所以此时 <code>el</code> 属性值为该占位的空文本元素的引用：</p>\\n<pre class=\\"language-js\\"><code>{\\n  tag: null,\\n  children: [],\\n  el: 占位的空文本元素 // document.createTextNode(&#39;&#39;)\\n}\\n</code></pre>\\n<ul>\\n<li>5、Portal</li>\\n</ul>\\n<p><code>Portal</code> 比较特殊，根据 <code>Portal</code> 寓意，其内容可以被渲染到任何地方，但其真正的挂载点会有一个空文本节点占位，所以 <code>Portal</code> 的 <code>VNode.el</code> 属性值引用的始终是这个空文本节点。当然这是 <code>vue3</code> 的设计，理论上将我们完全可以做到让 <code>el</code> 引用真正的挂载容器元素。</p>\\n<pre class=\\"language-js\\"><code>{\\n  tag: null,\\n  children: [...],\\n  el: 占位的空文本元素 // document.createTextNode(&#39;&#39;)\\n}\\n</code></pre>\\n<section class=\\"custom-block followup\\"><p class=\\"custom-block-title followup-title\\">💬 面试官追问</p>\\n<ul>\\n<li>\\n<p><strong>列表页把组件、文本和 <code>Fragment</code> 都当成普通元素处理，统一令 <code>vnode.el = container.lastChild</code>；首屏看似正常，筛选后节点定位错乱，你会指出哪个反例？</strong></p>\\n<p>统一取 <code>lastChild</code> 会破坏不同 <code>VNode</code> 类型的引用语义。元素和文本应引用自身真实节点，组件应引用其渲染结果的根节点，<code>Fragment</code> 则引用片段中的第一个节点；一旦片段含多个节点，末节点就不能代表其起始位置。</p>\\n</li>\\n<li>\\n<p><strong>你在实现渲染器的挂载流程，组件外层需要支持后续插入和移动；元素、组件、纯文本三类 <code>VNode</code> 的 <code>el</code> 应在什么时机、分别写入什么？</strong></p>\\n<p>真实节点创建并挂载后，应立即回写对应 <code>VNode.el</code>。元素保存 <code>createElement</code> 得到的元素，纯文本保存 <code>createTextNode</code> 得到的文本节点，组件保存其渲染结果的根节点引用；若组件尚未完成子树挂载，就不能提前得到可靠的 <code>el</code>。</p>\\n</li>\\n<li>\\n<p><strong>产品把原本单根组件改成返回两个并列节点，渲染器仍假设组件的 <code>el</code> 能代表完整 <code>DOM</code> 范围；你会怎样调整判断？</strong></p>\\n<p>组件的 <code>el</code> 只能作为其渲染结果的根或起始节点引用，不能天然表达多个并列节点的完整范围。若结果按 <code>Fragment</code> 处理，<code>el</code> 应指向第一个真实节点，涉及整段移动或删除时还需额外掌握边界；仅凭一个 <code>el</code> 无法安全覆盖整个片段。</p>\\n</li>\\n<li>\\n<p><strong>线上弹层使用类似 <code>Portal</code> 的跨容器挂载后，卸载逻辑误删了目标容器旁的节点；你会沿着哪些引用检查故障？</strong></p>\\n<p>先核对 <code>Portal</code> 的内容容器与原挂载点占位节点是否被混为一谈。按该设计，<code>Portal VNode.el</code> 始终引用原位置的空文本占位，而不是远端容器；卸载与定位必须分别处理占位和实际内容，否则用 <code>el.parentNode</code> 推导目标容器会得到错误结论。</p>\\n</li>\\n<li>\\n<p><strong>空 <code>Fragment</code> 不渲染业务内容，同事主张让 <code>vnode.el = null</code> 省掉一个文本节点；后续它可能被替换成非空片段，你支持吗？</strong></p>\\n<p>不支持直接设为 <code>null</code>，因为后续更新需要一个稳定的 <code>DOM</code> 定位点。渲染空 <code>Fragment</code> 时创建 <code>document.createTextNode(\'\')</code> 并让 <code>el</code> 引用它，可保留插入位置；代价是多一个占位节点，但能避免更新时重新猜测锚点。</p>\\n</li>\\n</ul>\\n</section>\\n"}')}}]);